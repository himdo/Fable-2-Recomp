# Generated by ReXGlue v0.10.0 on 2026-09-02 20:36:00 UTC.
#
# Migrated 2026-09-19 from XenonRecomp/fable2.toml (see that file for the
# original XenonRecomp/XenonAnalyse configuration).
#
# XenonRecomp fields with no ReXGlue equivalent (kept in XenonRecomp/fable2.toml):
#   - restgprlr_14 / savegprlr_14 / restfpr_14 / savefpr_14 / restvmx_14 /
#     savevmx_14 / restvmx_64 / savevmx_64 helper addresses
#     (register save/restore thunks found via byte-pattern scan):
#       restgprlr_14 = 0x82CA2C00   savegprlr_14 = 0x82CA2BB0
#       restfpr_14   = 0x82CA751C   savefpr_14   = 0x82CA74D0
#       restvmx_14   = 0x82FFFF68   savevmx_14   = 0x82FFFCD0
#       restvmx_64   = 0x82FFFFFC   savevmx_64   = 0x82FFFD64
#   - switch_table_file_path: ReXGlue has no separate switch-table file; the
#     XenonAnalyse tables are converted to [[switch_tables]] entries in
#     fable2_switch_tables.toml and pulled in via `includes` below.
#   - patch_file_path: none. The only available patch
#     ($SystemUpdate/su20076000_00000000) is a PIRS container, not a raw
#     XEX2 delta patch, so the recompiler runs against the unpatched
#     default.xex.

[project]
name = "fable_2"
sdk_version = "0.10.0"
game_root = "."

[entrypoint]
file_path = "default.xex"
out_directory_path = "generated/default"
includes = ["fable2_switch_tables.toml"]

# setjmp/longjmp: the recompiler redirects these to native implementations.
# longjmp (0x82CA9260) restores the full guest state from the jmp_buf and
# calls RtlUnwind (thunk 0x832B25BC) on the unwind path.
# setjmp (0x83000200) stores the full guest state (f14-f31, r13-r31, VMX,
# CR, LR) to the jmp_buf, using the same layout longjmp restores.
longjmp_address = 0x82CA9260
setjmp_address = 0x83000200

# --- Recomp-level patches via mid-asm hooks (see docs/patches.md) ---------
# Code-region Xenia patches are inert at runtime in this recomp (guest .text
# is never executed), so they are applied to the RECOMPILED code instead: a
# hook is injected at a specific guest instruction address and a C++ function
# (defined in src/core/fable2_hooks.cpp) rewrites a register, mirroring the patch's
# intent. Codegen emits each hook's extern prototype automatically. Hooks are
# keyed by address in codegen (a duplicate address overrides the earlier one),
# so each address appears exactly once below.
#
# Unlock Website Items (Xenia "Unlock Website Items" by Guy). sub_8256E368 is
# the "is a website/Guild-chest item available" check. It is gated by the
# player's website-registration flags on the chest object (r4): GATE 1 = bit6
# of *(r4+0x90), GATE 1b = bit0 of *(r4+0x40). Only when BOTH pass does the
# type-0xf0 item lookup run and the item get GRANTED via a vtable call
# (bctrl at 0x8256E4CC); every other path funnels to the return-0 tail at
# 0x8256E4E0. We force each gate's bit-extract result to 1 (r9 after the
# GATE-1 rlwinm, r8 after the GATE-1b clrlwi) so the function always treats the
# player as registered and performs the real grant. The getters run at save
# load, so this populates the chest inventory on load.
[[entrypoint.midasm_hook]]
address = 0x8256E384
name = "fable2_hook_website_g1"
registers = ["r9"]
after_instruction = true
[[entrypoint.midasm_hook]]
address = 0x8256E3AC
name = "fable2_hook_website_g1b"
registers = ["r8"]
after_instruction = true

# Unlock Collectors Edition Content (Xenia "Unlock Collectors Edition Content"
# by Guy). Same shape: sub_824B3528 is the "is CE-chest content available"
# check, gated by GATE 1 = bit6 of *(r4+0x90) and GATE 1b = a bit of
# *(r4+0x28); only when both pass does the type-0x39 item lookup run and the
# content get GRANTED via a vtable call (bctrl at 0x824B3678). We force each
# gate's bit-extract result to 1 (r10 after the GATE-1 rlwinm, r9 after the
# GATE-1b rlwinm) so the function always treats the player as registered.
[[entrypoint.midasm_hook]]
address = 0x824B3540
name = "fable2_hook_ce_g1"
registers = ["r10"]
after_instruction = true
[[entrypoint.midasm_hook]]
address = 0x824B3568
name = "fable2_hook_ce_g1b"
registers = ["r9"]
after_instruction = true

# --- Unlock grant methods (make the getter report each item as granted) -----
# Forcing the two registration gates alone is not enough: after the gates the
# getter finds the item and calls a per-object GRANT vtable method, and the
# getter returns THAT method's result. For an unregistered player the grant
# method returns 0 (item not granted), so the getter reports "not available"
# and the chest stays locked. We force each grant method's return to 1.
#
# Website: sub_8256D940 is the website-chest vtable[1] "is this item new?"
# check. Its final instruction is `xori r3, r9, 1` at 0x8256D9E4; hook right
# after it and set r3 = 1 so the getter reports the item as granted.
[[entrypoint.midasm_hook]]
address = 0x8256D9E4
name = "fable2_hook_website_grantnew"
registers = ["r3"]
after_instruction = true
#
# CE: sub_824ACAE0 is the CE-chest grant vtable slot called from
# GetCEContent. Its entire body is `lbz r3, 993(r3)` (returns the byte at
# item+0x3E1, the "CE content available" flag). Hook right after it and set
# r3 = 1 so the getter reports the CE content as available.
[[entrypoint.midasm_hook]]
address = 0x824ACAE0
name = "fable2_hook_ce_grantavail"
registers = ["r3"]
after_instruction = true

# Conservative settings: no register optimizations
skip_lr = false
skip_msr = false
ctr_as_local = false
xer_as_local = false
reserved_as_local = false
cr_as_local = false
non_argument_as_local = false
non_volatile_as_local = false

# Invalid instruction skips: 32-bit values (big-endian) the recompiler skips
# over during its linear .text walk instead of analyzing them as functions.
#   0x00000000      -- zero padding between functions (not a valid PPC insn)
#   0x00485645      -- end-of-.text data table at 0x832B31E0 (185 words of
#                      offset data, 0x832B31E0-0x832B34C4); skip all 740 bytes
[[entrypoint.invalid_instructions]]
data = 0x00000000
size = 4

[[entrypoint.invalid_instructions]]
data = 0x00485645
size = 740

# Manual function boundaries from the XenonRecomp config.
#
# ReXGlue rejects overlapping standalone function boundaries (XenonRecomp
# allowed them), so four entries that nested inside a larger entry were trimmed
# to end where the inner function begins. Original sizes are preserved in
# XenonRecomp/fable2.toml if these turns out to be wrong:
#   0x82BE9668: 0x08D4 -> 0x0018  (inner 0x82BE9680 starts at +0x18; both
#               originally ended at 0x82BE9F3C)
#   0x82C89840: 0x01D8 -> 0x0078  (inner 0x82C898B8 starts at +0x78)
#   0x82C898B8: 0x0154 -> 0x00A8  (inner 0x82C89960 starts at +0xA8)
#   0x82C8D3F0: 0x01E8, and the nested 0x82C8D4E8 entry was removed. With both
#               present the gap-fill extended the outer past its trimmed size and
#               codegen emitted a cross-function goto into 0x82C8D4E8 (compile
#               error). Keeping the outer at its original full size makes the
#               region one function; calls into it mid-body are handled as tail
#               calls to internal labels. Original inner entry: size 0x00F0.

[entrypoint.functions."0x8217F250"]
name = "SwitchDispatch_8217F250"
size = 0x00F0

# These are the loading screen functions when starting a new game
[entrypoint.functions."0x822142D0"]
name = "GetNewGameLoadingGlobal"
[entrypoint.functions."0x82988ED8"]
name = "LoadingScreen_Virtual43"
[entrypoint.functions."0x82988F28"]
name = "LoadingScreen_Virtual48"
[entrypoint.functions."0x8274B748"]
name = "LoadingScreen_Virtual42"
[entrypoint.functions."0x82EAA980"]
name = "LoadingScreen_Virtual36"
[entrypoint.functions."0x8274B758"]
name = "LoadingScreen_Virtual39"
[entrypoint.functions."0x8274B778"]
name = "LoadingScreen_Virtual28"
[entrypoint.functions."0x822D6678"]
name = "LoadingScreen_VectorOffset"

# After new game video plays
[entrypoint.functions."0x82988EC8"]
name = "Story_AfterNewGameVideo"

# After new game after "look little sparrow" LT button
[entrypoint.functions."0x82988F48"]
name = "Story_LookLittleSparrow"

#talking to camera man in child scene
[entrypoint.functions."0x82988F38"]
name = "Story_TalkToCameraMan"

# First Combat childhood fight
[entrypoint.functions."0x8297EB48"]
name = "Story_FirstChildCombat"

# Interacting with sister after first fight
[entrypoint.functions."0x8274B788"]
name = "Story_SisterAfterFirstFight"

# Entering the warehouse as child
[entrypoint.functions."0x8274B738"]
name = "Story_EnterWarehouseAsChild"

# Shooting Beetle in the warehouse as child
[entrypoint.functions."0x82DDC4D8"]
name = "Story_ShootBeetleInWarehouse"

# Swimming
[entrypoint.functions."0x8297EB28"]
name = "Swimming"

# Interacting with job sign (blacksmith)
[entrypoint.functions."0x823A1810"]
name = "Story_JobSignBlacksmith"

# Aiming range weapon (skill)
[entrypoint.functions."0x82988EE8"]
name = "Skill_AimRangeWeapon"

# Tatter spire right after you get there when the man says "Get Moving"
[entrypoint.functions."0x8274B768"]
name = "Story_TatterSpireGetMoving"

# === Guild chest (fable2.com website items / CE content unlock) ===
# The Guild chest holds items obtainable from the fable2.com website and the
# Collectors Edition content. An unregistered player who clicks it sees
# "Go to www.fable2.com for information on how to access the gold and items
# your heroic ancestors left behind." These are the functions on the
# chest-click path (see docs/patches.md). Names are best-effort from runtime
# tracing; refine them as behaviour is confirmed.

# Website-item getter. The Xenia "Unlock Website Items" patch targets its
# GATE2 branch (0x8256E4B8) and return-0 tail (0x8256E4E3) inside this fn.
[entrypoint.functions."0x8256E368"]
name = "GuildChest_GetWebsiteItem_8256E368"

# CE-content getter. The Xenia "Unlock CE Content" patch forces r9=1 at
# 0x824B366C (a load) inside this fn.
[entrypoint.functions."0x824B3528"]
name = "GuildChest_GetCEContent_824B3528"

# Per-item worker: called once per chest item on the click; calls the item op
# method (0x8256E4F8) through the website-object vtable.
[entrypoint.functions."0x82228830"]
name = "GuildChest_ProcessItem_82228830"

# Item-processing loop: the click handler that calls ProcessItem per item.
[entrypoint.functions."0x822DE008"]
name = "GuildChest_ProcessItems_822DE008"

# Kernel wait wrappers (KeWait* re-implemented over the Nt* syscalls)
#
# WARNING on the _Guest suffix: do NOT name these bare "KeWaitForSingleObject"
# / "KeWaitForMultipleObjects". Codegen emits the body as
# __imp__<name>, and rexruntimed.dll also exports __imp__KeWaitForSingleObject
# / __imp__KeWaitForMultipleObjects (the real xboxkrnl XAPI hooks). A local
# definition silently shadows the DLL import, so the XAPI import thunks
# (0x832B28AC / 0x832B2CDC, registered in fable_2_register.cpp) would resolve
# to the guest wrapper instead of the SDK kernel hook. The wrapper expects
# millisecond timeouts in the LpWakeTime field while XAPI callers pass kernel
# 100ns LARGE_INTEGERs (0 = infinite there!), which broke all audio/video
# worker-thread waits. Any future name must not collide with a rexruntimed.dll
# export (strip the __imp__ prefix and compare; see tools/check_manifest_collisions.py).
# Fills the NTAPI_WAIT_ENTRY LpWakeTime from a millisecond timeout:
# -1 (INFINITE) -> 0, otherwise timeoutMs * -10000 (relative, 100ns units).
# Called constantly (every alertable event/mutex wait funnels through it).
[entrypoint.functions."0x822D7508"]
name = "WaitEntrySetLpWakeTime"

# Alertable KeWaitForSingleObject (guest-image-internal): NtWaitForSingleObjectEx(Alertable=1),
# loops on STATUS_USER_APC (257) while a timeout was given.
[entrypoint.functions."0x822D74A0"]
name = "KeWaitForSingleObject_Guest"

# Alertable KeWaitForMultipleObjects (guest-image-internal): count <= 64 (KE_WAIT_VARIABLE limit),
# NtWaitForMultipleObjectsEx, loops on STATUS_USER_APC (257).
[entrypoint.functions."0x822D7528"]
name = "KeWaitForMultipleObjects_Guest"

# SDK mutex family (Xbox 360 mutexes are mutants).
#
# Lock side: 2-instruction thunk, r5 = 0 then tail-calls KeWaitForSingleObject_Guest.
# Timeout 0 -> WaitEntrySetLpWakeTime writes LpWakeTime = 0 (no timer) so the
# wait is INFINITE, not a zero-wait poll (and -1 also maps to 0, so 0 cannot
# mean "don't block" or INFINITE would be unexpressible). Blocks until the
# object is signaled; APCs are not retried (timeout==0), so STATUS_USER_APC
# (257) can be returned. r4 (wait reason) is ignored by the wrapper; callers
# still pass -1/0/flags in it. 42 call sites: audio/video/rendering worker
# threads blocking on per-object handles, SDK alert/exit loops. Paired with
# MutexUnlock_ReleaseMutant (0x83004F30) at sites such as sub_832B8DA8.
[entrypoint.functions."0x82196C58"]
name = "MutexLock_WaitInfinite"

# Unlock side: NtReleaseMutant(handle, NULL). Returns 1 on success; on
# failure logs the NTSTATUS through sub_82CC1C38 (same error logger as
# KeWaitForSingleObject_Guest) and returns 0. 22 call sites, the release half of
# the lock pairs that acquire via MutexLock_WaitInfinite.
[entrypoint.functions."0x83004F30"]
name = "MutexUnlock_ReleaseMutant"

# Create side: NtCreateMutant(outHandle, attrs, initialCount). r3 = out
# handle pointer, r4 = initial owned count, r5 = optional object-attributes
# pointer (built via sub_82CC1DF0 when non-null). Sanity-checks the returned
# handle (0x10000 check, error code 183 via sub_82CC0750) and returns it;
# logs and returns 0 on NtCreateMutant failure.
[entrypoint.functions."0x83004EA8"]
name = "MutexCreate_OutHandle"

# Time/clock family.
#
# The game's master millisecond clock, cached in a global singleton:
# returns *( *(0x82000908) + 16 ) — no arguments. 147 call sites in 89 files,
# almost all of the form "stw r3, N(obj)" storing it into per-object timestamp fields
# (lastUpdate/startTime/nextFire); a few compare against an adjacent
# previous-time field (32-bit wrap handling) or double-sample it (before/
# after bracketing, e.g. sub_8237E3E0 stores two reads 4 bytes apart).
# Millisecond scale proven by callers: rate limiters compute
# consumption = rate * (now - last) / 1000 (sub_82FFA508), poll loops use
# deadline = saved + 100 (sub_82388658), and the 2^30 jump guard in
# GetElapsedMsSinceStart below only makes sense in ms (~12.4 days).
[entrypoint.functions."0x82266070"]
name = "GetCurrentTimeMs"

# Elapsed milliseconds since first call, reset/wrap-guarded.
# X = GetCurrentTimeMs(); base A = global 0x81FFBDFC (captured from X on the
# first call, when A == 0); delta = X - A. Cached last delta B = global
# 0x81FFBE00: if (delta - B) > 0xC0000000 unsigned the update is skipped and
# B is returned instead (X wrapped or reset). 24 call sites, all in the
# 0x832B end-of-.text region, all passing the result into tick/update
# methods (e.g. sub_832B5530 -> sub_832B50C0(obj, now)).
[entrypoint.functions."0x832B9398"]
name = "GetElapsedMsSinceStart"

# Gap-region leaf with NO callers in the current code (registered by the
# gap walk over 0x832B3D6C-0x832B3DE0; the original branch source was
# re-segmented away, so it is unreachable/dead in this build). Mechanism:
# v = *(r3 - 4); if (v == r4) return 0; else return ~v
# (0xFFFFFFFF - v). Looks like a tag/checksum verifier over a 32-bit word
# stored immediately before a buffer, returning 0 on match and a
# non-zero token encoding the actual value on mismatch. Purpose not
# recoverable without the lost caller; name describes the mechanism only.
[entrypoint.functions."0x832B3D78"]
name = "CheckPrefixedWord_ReturnZeroOrComplement_832B3D78"

# SDK runtime memcpy: memcpy(dest=r3, src=r4, len=r5), returns dest
# (saved at -8(r1) on entry). ~290 insns: byte-wise dest alignment, 4/8-byte
# misaligned paths, 64-byte unrolled main loop, 128-byte dcbt prefetch for
# len >= 128, dcbtst writeback hints on the tail. 1310 call sites — the
# image's dominant copy primitive (game + SDK code). Named MemCpy_SdkRuntime
# (not bare "memcpy") to keep the guest symbol distinct from host libc in
# traces and to avoid any global-namespace ambiguity.
[entrypoint.functions."0x82CA2C60"]
name = "MemCpy_SdkRuntime_82CA2C60"

# KeWaitForMultipleObjects_Guest variant with the APC-retry flag (r7) forced to 0:
# 2-insn thunk "li r7,0; b KeWaitForMultipleObjects_Guest". With r7==0 the wrapper
# does NOT retry on STATUS_USER_APC (257 is returned to the caller) and
# passes Restart=0 to NtWaitForMultipleObjectsEx. 4 call sites: batch
# WaitAll locking of N mutex handles (sub_832B8C28 with a 1ms timeout,
# sub_832B8B80 with 2 handles + variable timeout) and single-object waits
# (sub_822BA4F0 infinite, sub_8262DDD0 variable). Note: the wrapper's
# timeout is r6 (fed to WaitEntrySetLpWakeTime), r5 is the mode
# (0=WaitAll, nonzero=WaitAny).
[entrypoint.functions."0x82179FB0"]
name = "KeWaitForMultipleObjects_NoApcRetry"

# Locked per-entry processing loop over a 388-byte-entry array. Args: obj
# (r3). Guards: tail word *(obj->280 + obj->276*388 - 40) != 0 and
# obj->692 != 0. Locks the mutex at (obj+852)->+4 via
# MutexLock_WaitInfinite (bails without processing if the lock cannot be
# taken). Loops i over obj->276 entries (array at obj->280, stride 388):
# vcall entry->348 (skip entry if it returns 0), range checks over entry
# fields 0/8/12/16/24/48/52/68, vcall entry->352 (fetch buffer info into
# stack slots r1+80/84), then a byte transform that toggles each byte's
# high bit (b+128 == b XOR 0x80, i.e. 8-bit signed<->unsigned conversion)
# between the entry buffer and the obj->12 buffer with MemCpy_SdkRuntime
# moves and entry->24 offset bookkeeping, then vcall entry->356 and
# entry->348 again. Unlocks via MutexUnlock_ReleaseMutant. 7 call sites,
# all sibling methods in the same 0x832B end-of-.text family, each gating
# the call on a different non-zero object field. The XOR-0x80 byte churn is
# the signature of 8-bit sample conversion — plausibly an audio voice/sound
# pipeline, but the vtable contents are unresolved so the name stays
# mechanism-based.
[entrypoint.functions."0x832B3700"]
name = "CriticalSection_ProcessEntryArray_832B3700"

# Vtable method of the 0x832B family (NO static call sites — reached only
# through virtual dispatch). Args: r3 = the sub-object at base+852, r4 = a
# 32-bit value. base = r3 - 852. If base->276 (the entry count) is non-zero
# it runs CriticalSection_ProcessEntryArray_832B3700(base), then unconditionally
# stores r4 into base->848 (a result/status word — no in-family reader found,
# so it is likely consumed by the same family's other vtable methods or
# exported state). ~15 insns.
[entrypoint.functions."0x832B3D90"]
name = "EntryArray_ProcessAndStoreResult_832B3D90"

# Time-gated tick of a ring-buffer object (vtable method, NO static call
# sites — invoked through virtual dispatch). Args: obj (r3). Guards:
# obj->212 == 0, obj->56 != 0, obj->208 != 0. Layout: obj->188 = capacity,
# obj->216 = write cursor, obj->228 = read cursor, obj->192 = watermark W,
# obj->184 = buffer pointer P, obj->244 = fill byte, obj->236 = last tick
# time, obj->240 = tick rate limit (ms), obj->92 = consumer sub-object S.
# Flow: now = GetElapsedMsSinceStart(); if (now - obj->236 > 12) skip the
# space check (starvation override). free = (write - read) mod capacity
# (standard formula, both branches verified). If free >= 1024 the candidate
# length is (free - 1024) & ~3 (keeps a 1024-byte reserve, 4-aligned), else
# 0. Watermark gates vs obj->192/obj->232 (flag obj->220 bypasses). When it
# passes: obj->84 = 1, then MemSet (sub_82CA3190) fills 2*W bytes at
# P + write with byte obj->244 (two chunks if it wraps capacity), and the
# write cursor advances 2*W mod capacity. Then a probe loop calls
# sub_82CD2DF8(S, &out) (three probes, looping while the out-difference is
# >= 1024) and stores the final probe result into the READ cursor (obj->228)
# plus now into obj->236; if (now - old236 >= obj->240) it re-enters the
# fill step, else re-checks free space. Returns 1 (acted) or 0 (skipped).
# Hypothesis: 8-bit audio silence padding (fill byte would be 0x80 = signed
# silence; the sibling family does XOR-0x80 sample conversion and the 12 ms
# / 1024-byte constants fit sample streaming) — but the fill byte and the
# consumer probe are runtime values, so the name stays mechanism-based.
[entrypoint.functions."0x832B68A0"]
name = "RingBuffer_TickPadWithFillByte_832B68A0"

# Priority-ordered multi-mutex arbitration loop. Args: manager M (r3).
# Layout: M->0 = state (must be 1 to accept requests), M->24 = pending-list
# head, M->28 = monotonically increasing generation counter (incremented
# once per entry). Node layout (16-byte stride vtable at node->0..20, next
# at node->8): node->4 = mutex handle, node->12 = priority, node->16 =
# "prepare" vtable slot, node->20 = "granted" vtable slot. Loop: while
# M->0 == 1 and the pending list is non-empty, drain every pending node
# (linked through node->0): priority = vcall node->16(node, M->28);
# node->12 = priority; priority 0 = skip; otherwise insert the node into an
# acquired list sorted by DESCENDING priority (higher priority toward the
# head, linked through node->8). After each drain pass, build stack arrays
# (cap 64): handles[i] = node->4, objs[i] = node, then
# ret = KeWaitForMultipleObjects_NoApcRetry(count, handles, mode=0
# (WaitAll), timeout=1ms). If ret >= count (timeout/APC) the loop drains
# again; on success (ret = 0) it runs the "granted" callback vcall
# objs[ret]->20(objs[ret]) and immediately MutexUnlock_ReleaseMutant
# (handles[ret]) — the highest-priority requester is notified and its gate
# released, while the remaining acquired mutexes stay held (released by
# sibling family methods). Sole caller: sub_832B8DA8, a sibling method that
# first locks M->8 and M->20 (MutexLock_WaitInfinite) and gates the call on
# M->24 != 0, i.e. the pending head — confirming the layout.
[entrypoint.functions."0x832B8C28"]
name = "PriorityArbitrate_PendingMutexRequests_832B8C28"

# CS-guarded virtual-call wrapper, one of ~11 uniform siblings in the
# 0x82CD2Cxx-0x82CD2Fxx block. Pattern (verified across the cluster):
# RtlEnterCriticalSection(global CS at 0x82CD0324); target = arg1->76
# (a C++ object, vptr at +0); call vtable[+60/4 = slot 15] with
# (this=target, out=arg2); RtlLeaveCriticalSection; return the result.
# The sibling slots are 28/56/60/64/72/80/92/96 (plus two that forward to
# helpers sub_82CDC180/sub_82CDCFE8) — i.e. a synchronized proxy class in
# the SDK runtime around one core object, all methods sharing the single
# global CS. 2 call sites: RingBuffer_TickPadWithFillByte_832B68A0 (polled
# 3x, looping while successive out-values differ by >= 1024 — a consumer
# progress query) and sub_832B6A98 (called after sibling slots 72/64 and a
# helper, in a fetch-and-advance sequence). The vtable contents are
# unresolved, so the name stays mechanism-based (slot offset in bytes).
[entrypoint.functions."0x82CD2DF8"]
name = "CsGuardedVcall_Slot60_82CD2DF8"

# 5-insn leaf: *r4 = (uint16_t)obj->108 * obj->120; returns 0.
# Field roles pinned down by the inverse operation sub_82CE37C0, which
# (spin-lock protected: KeRaiseIrqlToDpcLevel + KeAcquireSpinLockAtRaisedIrql
# on global 0x82CA7060, with a twllei div-by-zero trap guard) computes
# obj->120 = (*total) / obj->108 and sets obj->272 = 1 — so obj->108 is a
# unit/divisor and obj->120 the total/unit count; this function is the
# count -> total direction (unit * count, classic frameSize*frameCount /
# sampleSize*sampleCount byte-length math). Several siblings in the 0x82CE
# region write these fields (sub_82CE3BE0/sub_82CE3400/sub_82CE24B0/
# sub_82CE0C20 store the u16; sub_82CE3CF0/sub_82CE1588 update the u32).
# ZERO static call sites — vtable method of that object family.
[entrypoint.functions."0x82CE32B0"]
name = "StoreUnitTimesCount_82CE32B0"

# SDK C-runtime ctype predicate: returns bit 0 (the _UPPER flag) of the
# 256-entry u16 character-type table. Table base is runtime-loaded from
# global 0x82CCC8C4 (entry = *(base + c*2), zero-extended 16-bit). One of
# 9 uniform siblings in the 0x82CA6AB0-0x82CA6BB0 block, each extracting a
# different bit/mask of the same u16 entry: bit0 (this one, sub_82CA6AD0),
# bits 0|1|8 (sub_82CA6AB0: andi 259, sub_82CA6B90: andi 263), and single
# bits 24/26/27/28/29/30 (sub_82CA6B30/BB0/B70/B50/B10/AF0 — note: beyond
# the 16-bit load, so these read zero in the current table layout). The
# _UPPER meaning of bit 0 is pinned by its only non-cluster consumer,
# ToLower_SdkRuntime_82CA4288, which gates an A-Z -> a-z fold on it.
# 2 call sites: ToLower_SdkRuntime_82CA4288 and the dispatch table
# sub_82A25230 (which routes to the sibling predicates by index).
[entrypoint.functions."0x82CA6AD0"]
name = "Ctype_IsUpper_82CA6AD0"

# SDK C-runtime tolower, in classic CRT shape:
# if (0 <= c < 256 && Ctype_IsUpper(c)) return sub_821EE9E8(c); else return c
# where sub_821EE9E8 is plain ASCII fold (A-Z -> a-z, else c unchanged;
# 22 callers image-wide, so it is also the general tolower). The ctype
# guard is what makes this the locale-aware variant. 2 call sites, both
# string processing: sub_82448030 is a CASE-INSENSITIVE FNV-1 string hash
# (hash = tolower(byte) ^ (hash * 16777619), 16777619 = 0x01000193 = the
# 32-bit FNV prime) and sub_83002B80 converts a character to a 0-25 letter
# index (result - 96, 'a' = 97). Named ToLower_SdkRuntime (not bare
# "tolower") to keep the guest symbol distinct from host libc, matching the
# MemCpy_SdkRuntime convention.
[entrypoint.functions."0x82CA4288"]
name = "ToLower_SdkRuntime_82CA4288"

# Plain ASCII tolower, the raw fold without the ctype guard:
# if (c < 65) return c; if (c > 90) return c; return c + 32.
# i.e. A-Z -> a-z, everything else passes through unchanged. 22 call sites
# image-wide, so this is the general tolower used across the engine (the
# locale-aware ToLower_SdkRuntime_82CA4288 is a rarer variant that gates the
# same fold on Ctype_IsUpper_82CA6AD0). 6 insns, no prologue (leaf). Named
# ToLower_Ascii to distinguish it from ToLower_SdkRuntime and to keep the
# guest symbol distinct from host libc (MemCpy_SdkRuntime convention).
[entrypoint.functions."0x821EE9E8"]
name = "ToLower_Ascii_821EE9E8"

# Constant-true predicate / stub: "li r3,1; blr" — ignores every argument
# and always returns 1. 6 call sites, all treating the result as a boolean
# gate: sub_83205F58 sets up (r3=*(obj+16), r4, r6, r7) then "cmpwi r3,1;
# bne" to choose between (byte & 0x1F) and a fallback value (so it always
# takes the & 0x1F path); four 0x8304-region callers (sub_83040418,
# Func_83040FEC, Func_8304100C, Func_8304101C) share an identical
# "lwz r3,192(r31); li r5,0; li r6,0; bl" shape; sub_8301D688 gates on a
# byte == 118 ('v') before the call. The call shape (this + args, result
# used as yes/no) suggests a query method the compiler folded to a constant
# (a feature always on, an "accept all" policy, or a debug gate compiled
# out) — the original semantic is not recoverable without the lost
# vtable/branch source, so the name describes the mechanism only.
[entrypoint.functions."0x82C43198"]
name = "ConstTrue_Predicate_82C43198"

# The game's SIMD-optimized byte-copy kernel (a second memcpy, distinct from
# the SDK C-runtime MemCpy_SdkRuntime_82CA2C60). Convention: copy(dst=r3,
# src=r4, len=r5), returns dst (r26). Forward-only, NO overlap/direction
# handling (plain memcpy semantics). Two paths: len < 16 takes a small path
# (optional 8-byte head if src is 8-aligned, then an aligned 4-byte chunk via
# sub_82CA30E8, then a 4-byte remainder loop); len >= 16 takes a vectorized
# path using 128-bit SIMD (lvx128/vperm128/stvx128) for 16-byte main-loop
# moves with dcbt (source) and dcbzl (destination) 128-byte prefetch, chunked
# at 1024. 34 call sites across the engine. Named MemCpy_Vectorized to
# distinguish it from the SDK memcpy and to keep the guest symbol distinct
# from host libc (the MemCpy_SdkRuntime convention).
[entrypoint.functions."0x822085D0"]
name = "MemCpy_Vectorized_822085D0"

# 1-instruction tail-branch thunk: "b 0x822085d0" — a plain (unswapped)
# alias to the vectorized memcpy kernel MemCpy_Vectorized_822085D0, passing
# (dst=r3, src=r4, len=r5) through unchanged. Single caller: sub_830FB4E8,
# which sets up (r3=r23=dst, r4=r31=src, r5=*(r25)=len) — a straightforward
# forward copy. Its 8-byte neighbor sub_82B93040 is the SWEAP-direction
# sibling (swaps r3/r4 then tail-calls the same kernel, single caller
# sub_83045C00), so the pair offers both argument orders of the same SIMD
# copy kernel. Named as a thunk/trampoline to the vectorized memcpy.
[entrypoint.functions."0x82B93038"]
name = "MemCpy_Vectorized_Thunk_82B93038"

# Spin-lock acquire (test-and-set with spin). Args: addr (r3) -> a u32
# flag where 0 = free / 1 = held. Behavior: read *addr; if 0, atomically
# CAS it to 1 (claim) and return; if non-zero (held), spin re-reading
# *addr until it reads 0, then claim it. Uses an lwarx/stwcx. atomic pair
# inside the recompiler's global-lock region (mfmsr/mtmsrd MSR sync), with
# an lwsync after the claim, and a db16cyc delay hint once the spin counter
# passes 40 (bus-contention back-off). Returns void. Sits in the wait/
# sync infrastructure (immediately before KeWaitForSingleObject at
# 0x822D74A0). 15 call sites, all in a data-processing subsystem: each
# loads a global object, adds an offset (a per-object lock word at +12/+32/
# +36/+40/-7828 etc.), and calls this BEFORE reading that object's fields —
# i.e. a spin-lock guard. Named SpinLock_Acquire (the matching release that
# sets the flag back to 0 is a separate function).
[entrypoint.functions."0x822D7408"]
name = "SpinLock_Acquire_822D7408"

# Vtable method of the 0x82B49 object family (ZERO static call sites —
# reached only through virtual dispatch). One of the two largest methods in
# the region (291 insns; near-identical twin sub_82B49320 at 290). Args:
# obj (r3), src (r4, a pointer to a u64), len (r5). obj->16 is a 64-bit
# read position (it has a public getter, sub_82B495F8: "ld r3,16(r3)");
# obj->24 a 64-bit bound; obj->32 a size; obj->36 an index into a 32-byte-
# stride entry array. Flow: bounds-check (position + len <= bound, else
# return the 0x80004005 failure code), then a loop that (a) reads a value
# from src, (b) dispatches to a handler from a GLOBAL FUNCTION-POINTER
# TABLE (r24) indexed by the high 32 bits of *src, passing (bound,
# computed_ptr, chunk_len), (c) advances the position (obj->16) by the
# chunk, and (d) calls sibling sub_82B49730(obj) for a status (low byte
# checked) and sub_82B49678(obj, flag) on the overflow path. Returns 0 on
# success, a 0x8000xxxx status on failure. Mechanism is clear (bounds-
# checked, position-advancing, handler-table-dispatched chunk processing —
# a buffer/stream reader of some kind) but the object identity and handler
# table are unresolved, so the name describes the mechanism only and keeps
# the address suffix.
[entrypoint.functions."0x82B49110"]
name = "ProcessChunk_TableDispatch_82B49110"

# Pure no-op stub: "blr" — does nothing, returns whatever was in r3.
# 396 call sites (one of the hottest functions in the image). The dominant
# caller pattern is "call a helper (sub_82317E50 / sub_82317F98 / ...), then
# addi r3,r1,N; bl sub_829CE870" — i.e. it is invoked on a stack slot (often
# twice, at two different offsets) right after constructing/using an object,
# and a second common shape is "mr r3,obj; bl sub_829CE870" (called on an
# object, then another op follows). A no-op called this uniformly on
# objects/stack slots is the signature of an EMPTY virtual method — most
# likely a no-op destructor / release / delete for an object type whose
# teardown does nothing in this build (the call is still emitted for ABI/
# dispatch correctness). The exact interface slot is not resolvable without
# the vtables, so the name states the observable fact: it is a no-op.
[entrypoint.functions."0x829CE870"]
name = "NoOp_Stub_829CE870"

# Spin-locked pool / free-list node acquisition. Args: obj (r3) — selected
# by the 4 callers by indexing an array (base = caller's obj->4, 4-aligned
# index) to pick a per-slot object. Layout: obj->16 = head of a node list,
# obj->4 = step/size, obj->24 = lower window bound, obj->28 = upper window
# bound, obj+36 = the spin-lock word (guarded by SpinLock_Acquire_822D7408).
# Flow: acquire the spin-lock; if the list is empty call sub_83230568(obj)
# to produce a node (if that also yields nothing, release and return 0);
# otherwise take the head node, advance its internal chain pointer
# (node->0 = *node->0), increment a counter (node->4 += 1), and if the
# chain is exhausted (*node->0 == 0) unlink the node from the list (via the
# node->8 next pointers, fixing the head if needed); then advance the
# allocation window (obj->24 += obj->4, obj->28 -= obj->4); release the
# lock; return the new chain head (node->0) or 0. A synchronized block/node
# dispense from a pool. The object identity is unresolved (it is one of the
# per-slot objects in the data-processing subsystem), so the name describes
# the mechanism and keeps the address suffix.
[entrypoint.functions."0x83230688"]
name = "Pool_AcquireNode_83230688"

# ---------------------------------------------------------------------------
# MAIN SIZE-BUCKETED ALLOCATOR (the game's primary "malloc", 3725 call sites)
#
# This is a SEPARATE, higher-level allocator from the low-level custom heap
# (Heap_AllocateBlock_82238828 / Heap_FreeBlock_822394F0, which carve blocks
# out of NtAllocateVirtualMemory regions). Three functions form the entry
# chain:
#
#   sub_8221F388 (3725 callers) -- the "safe" wrapper everyone calls:
#       r31 = sub_8221F3F0(size);            // allocate
#       if (r31 == 0) {                       // OOM:
#           if (*(global_fnptr @ ~0x82000B0C)) { r4 = 0; call it }  // OOM error-report callback
#           sub_82CBBB58();                   // = __imp__DbgBreakPoint (1-insn thunk)
#       }
#       return r31;
#   sub_8221F3F0 (142 callers) -- 1-insn thunk: "b 0x83231d20" -> the core.
#   sub_83231D20 (2 callers)   -- the core size-bucketed allocator.
#
# So sub_8221F388 = "allocate(size); on OOM report + debug-break". OOM is
# treated as fatal (DbgBreakPoint), which is why this is the dominant
# allocation path: game code allocates through it and never checks for NULL
# (the wrapper guarantees non-NULL or a breakpoint).
# ---------------------------------------------------------------------------

# The core size-bucketed allocator. Arg: size (r3). Returns a pointer, or 0
# if the size can't be satisfied. Flow:
#   1. Lazy init: if an init-flag byte (game data) is 0, call sub_83231B60().
#   2. FAST PATH: linear-scan a 6-entry table of cached sizes; if `size` is
#      one of them, jump to a per-size fast path.
#   3. Otherwise dispatch by size to a pool:
#        size <= 64        -> index (size+3) into a pre-built node pool and
#                            Pool_AcquireNode_83230688 the selected node
#                            (returns 0 if size > that pool's max entry).
#        64  < size <= 512 -> spin-locked pool: SpinLock_Acquire(pool+40),
#                            sub_83231590(pool, size), release (->40 = 0).
#        512 < size <= 10240 -> a second spin-locked pool (same shape).
#        10240 < size <= 10485760 (10 MB) -> a third spin-locked pool.
#        size > 10 MB        -> sub_83230900(size) (large-allocation path).
#   Each pool has a max-size cap (checked via a global); exceeding it returns 0.
# A size-bucketed allocator: small sizes use a fixed-node pool, mid/large sizes
# use dedicated spin-locked pools, and huge sizes fall through to a large path.
[entrypoint.functions."0x83231D20"]
name = "Allocate_SizeBucketed_83231D20"

# 1-insn thunk: "b 0x83231d20" -- forwards (size) to the core allocator
# Allocate_SizeBucketed_83231D20 unchanged and returns its pointer. 142
# callers use this entry directly (they handle the 0/NULL result themselves
# rather than going through the OOM-crashing wrapper sub_8221F388).
[entrypoint.functions."0x8221F3F0"]
name = "Allocate_SizeBucketed_Thunk_8221F3F0"

# The game's PRIMARY allocation entry point (3725 call sites -- the dominant
# "malloc"). Arg: size (r3). Calls Allocate_SizeBucketed_Thunk_8221F3F0(size)
# (-> Allocate_SizeBucketed_83231D20) to get a pointer. If the result is 0
# (OOM): read a global function pointer (~0x82000B0C), and if non-zero call it
# with (r4 = 0) as an OOM error-report callback, then call sub_82CBBB58 which
# is a 1-insn thunk to __imp__DbgBreakPoint (debug breakpoint). Returns the
# pointer. Net effect: "allocate(size); on failure report + debug-break", i.e.
# OOM is fatal and callers never need to NULL-check. Sits one layer above the
# low-level custom heap (Heap_AllocateBlock_82238828).
[entrypoint.functions."0x8221F388"]
name = "Allocate_OrDbgBreak_8221F388"

# 2-insn pointer getter: "lwz r3, 32(r3); blr" -- returns the u32/pointer
# stored at offset +32 of the object in r3. 6 callers (all in the 0x830F-0x831B
# region); the result is passed as the first arg to various functions, and one
# caller null-checks it ("cmplwi r3,0"), so offset 32 holds a NULL-able pointer
# (a sub-object / back-reference). The owning class isn't resolved, so the name
# states the mechanism (get the pointer field at offset 32) + address suffix.
[entrypoint.functions."0x824C23D0"]
name = "GetPtr_Field32_824C23D0"

# ---------------------------------------------------------------------------
# FREE SIDE of the size-bucketed allocator (the game's primary "free",
# 4498 call sites -- even hotter than the allocate entry at 3725).
#
#   sub_8221BE68 (4498 callers) -- the main free() entry: 1-insn thunk
#       "b 0x83231be8" -> Free_SizeBucketed_83231BE8.
#   sub_83231BE8 (2 callers)    -- the core free(): given a pointer, try to
#       release it from each size pool in turn; return 1 if freed, 0 if not.
#       - not initialized (same init byte as the allocator) -> return 0
#       - ptr == NULL                                        -> return 1 (no-op ok)
#       - pool <=64   : Pool_FreeByAddress_832304E8(ptr, pool)  -> freed?
#       - pool 64-512 : Pool_FreeByAddress_832304E8(ptr, pool)  -> freed?
#       - pool 512-10240 / 10240-10MB / one more : SpinLock +
#         sub_83231518(pool, ptr) (spin-locked pool free)        -> freed?
#       - else (large): sub_83230848(ptr) (large-path free)     -> freed?
#   sub_832304E8 (4 callers)    -- find the block containing `ptr` in a pool
#       (walk the pool's block list r4->28; base r4->20) and free it; return
#       1 if the pointer was in this pool, 0 otherwise. Used by the two small
#       pools of Free_SizeBucketed.
#   sub_83230320 (1 caller)     -- the spin-locked single-block free: push the
#       address back onto its block's free list, decrement the block's in-use
#       count, advance the pool window; if the block empties, unlink the whole
#       block from the pool's free-block list. Called only by sub_832304E8.
#
# This is the exact counterpart of the allocate side (Allocate_OrDbgBreak / 
# Allocate_SizeBucketed): free walks the same size buckets the allocator used.
# ---------------------------------------------------------------------------

# 1-insn thunk: "b 0x83231be8" -- forwards the pointer to Free_SizeBucketed_
# 83231BE8. The game's PRIMARY free() entry point (4498 call sites -- the
# dominant free, called more often than the allocate entry). Plain free(ptr):
# returns 1 if the pointer was released (or was NULL), 0 if it was not owned
# by this allocator. Sits one layer above the low-level custom heap
# (Heap_FreeBlock_822394F0).
[entrypoint.functions."0x8221BE68"]
name = "Free_SizeBucketed_Thunk_8221BE68"

# The core size-bucketed free(). Arg: pointer (r3). Returns 1 if freed, 0 if
# not owned. Flow: (1) if the allocator's init byte is 0, return 0. (2) NULL
# -> return 1 (freeing NULL is a valid no-op). (3) Try the small pools first
# via Pool_FreeByAddress_832304E8(ptr, pool) for the <=64 and 64-512 pools;
# if either reports the pointer was freed, return 1. (4) Then the spin-locked
# pools (512-10240, 10240-10MB, and one more) via SpinLock_Acquire(pool+40) +
# sub_83231518(pool, ptr) + release; return 1 if one frees it. (5) Otherwise
# the large path: sub_83230848(ptr); return the freed bit. The exact mirror of
# Allocate_SizeBucketed_83231D20's size buckets. 2 callers (the thunk and
# sub_82BEAB88).
[entrypoint.functions."0x83231BE8"]
name = "Free_SizeBucketed_83231BE8"

# Find-and-free by address for a pool's small blocks. Args: ptr (r3), pool
# (r4). Walks the pool's block list (head at r4->28; each node ->0 = start
# offset, ->12 = next; base at r4->20) looking for the block whose range
# [node_start, base + node_start) contains ptr. If found: align ptr down to
# the pool's granularity (r4->0), load the block descriptor at the aligned
# address + 12, call Pool_FreeNode_83230320(desc, ptr) to release it, and
# return 1. If no block contains ptr, return 0. 4 callers: the two small pools
# of Free_SizeBucketed_83231BE8, plus sub_828456C8 and sub_8240DAA8.
[entrypoint.functions."0x832304E8"]
name = "Pool_FreeByAddress_832304E8"

# The spin-locked single-block free for a pool. Args: pool (r31), ptr (r4).
# SpinLock_Acquire(pool+36). Aligns ptr to the block granularity, pushes the
# address onto its block's free list (head at block->0, in-use count at
# block->4, decrementing it), and advances the pool's window (pool->24 /
# pool->28, step pool->4). If the block's in-use count reaches 0, unlinks the
# whole block from the pool's free-block list (head pool->16, via block->8
# next). Calls sub_83235FC0(pool->20) as a helper, recomputes the window, then
# releases the lock (pool+36 = 0). The release counterpart of
# Pool_AcquireNode_83230688. Single caller: Pool_FreeByAddress_832304E8.
[entrypoint.functions."0x83230320"]
name = "Pool_FreeNode_83230320"

# ---------------------------------------------------------------------------
# REFERENCE-COUNTED RESOURCE RELEASE (the game's main "Release()", 2794
# call sites) plus two sub-object vtable-slot-3 delegates.
# ---------------------------------------------------------------------------

# The game's primary reference-counted Release(). Arg: wrapper object (r3) that
# holds a reference-counted resource at wrapper->4. Flow: if wrapper->4 is NULL,
# just clear the wrapper and return. Otherwise ATOMICALLY decrement the
# resource's reference count (*(wrapper->4)) using an lwarx/stwcx. loop under the
# global lock (mfmsr/mtmsrd). If the count is still > 0, another holder remains
# -- just clear the wrapper's fields and return. If it reaches 0 (last reference):
# call the resource's destructor callback (resource->4, invoked with resource->8
# as the object) and then free the resource via Free_SizeBucketed_Thunk_8221BE68
# (the main free). Finally clear wrapper->0 and wrapper->4. A classic COM-style
# Release(): drop one reference, and if this was the last, destroy + free.
# 2794 call sites -- the dominant reference-release path.
[entrypoint.functions."0x829FF648"]
name = "Release_RefCounted_829FF648"

# A vtable method (0 DIRECT callers -- reached only via virtual dispatch) that
# delegates to a sub-object's vtable slot 3 and returns a boolean. Args: obj
# (r3), arg1 (r4). Loads the sub-object at obj->16, builds a 2-word arg block
# on the stack {arg1, 0}, and calls sub_obj->vtable[3](sub_obj, &argblock, 4)
# (vtable = *(sub_obj->0); slot at vtable offset 12 / index 3). Reduces the
# call's result to a 0/1 boolean via a clz-based bit test and returns it. The
# interface method (slot 3) is not resolvable without the vtable, so the name
# states the mechanism: a delegating vtable method over the +16 sub-object that
# returns a bool.
[entrypoint.functions."0x82C01E78"]
name = "SubObject_VcallSlot3_Bool_82C01E78"

# Conditionally delegates to a sub-object's vtable slot 3 and stores a 32-bit
# result. Args: obj (r3), dest (r4). Reads a flag byte at obj->5 and, when a
# clz-based bit test on it is set, loads the sub-object at obj->0, builds a
# 2-word arg block {dest, 0} on the stack, and calls
# sub_obj->vtable[3](sub_obj, &argblock, 4) (same vtable slot 3 / offset 12 as
# SubObject_VcallSlot3_Bool_82C01E78). Stores the call's 32-bit result into
# *dest. 17 callers (sub_82C680F8, sub_82372108, sub_82A4AF78, ...). The
# interface method (slot 3) isn't resolvable without the vtable, so the name
# states the mechanism: a flag-gated vtable-slot-3 delegate over the +0
# sub-object that writes a u32 result.
[entrypoint.functions."0x82C842C8"]
name = "SubObject_VcallSlot3_GatedStore32_82C842C8"

# ---------------------------------------------------------------------------
# A word-aligned memcpy (SDK region, sibling of memset sub_82CA3190), a
# triple-vcall gate, and a table-dispatched chunk writer.
# ---------------------------------------------------------------------------

# A word-aligned memcpy: memcpy(dst=r3, src=r4, len=r5), returns nothing
# (void). Classic PPC byte-align -> word-copy -> tail-bytes structure: a byte
# loop aligns dst to a 4-byte boundary, then a 4-byte word loop (with a separate
# unaligned-source branch) copies the bulk, then a final byte loop copies the
# <4 remainder. Forward-only, no overlap handling (plain memcpy semantics).
# Sits in the SDK runtime region immediately before memset (sub_82CA3190).
# 34 DIRECT call sites (e.g. `r4=src; r5=496; bl`), AND it is the aligned-chunk
# helper that MemCpy_Vectorized_822085D0 calls for its small-path (len<16) word
# copy. Named MemCpy_Aligned to distinguish it from MemCpy_SdkRuntime_82CA2C60
# (the ~1.2 KB unrolled CRT memcpy) and MemCpy_Vectorized_822085D0 (SIMD), and
# to keep the guest symbol distinct from host libc.
[entrypoint.functions."0x82CA30E8"]
name = "MemCpy_Aligned_82CA30E8"

# A triple-vcall gate: makes THREE virtual calls in sequence and returns 1 only
# if all three succeed (nonzero), else 0. Args: obj (r3), arg (r5). Reads the
# vtable at *(obj->0) and, in order: (1) calls vtable slot 6 (offset 24);
# if it returns 0, bail with 0. (2) calls vtable slot 8 (offset 32) passing the
# arg; if it returns 0, bail with 0. (3) calls vtable slot 7 (offset 28); the
# low byte of its result is the return value (1 = all three succeeded, 0 = some
# step failed). 40 callers, all in the 0x82C0-0x82C4 region (sub_82C033D8,
# sub_82C49680, sub_82C2D040, ...). The three interface methods (slots 6/8/7)
# aren't resolvable without the vtable, so the name states the mechanism: a
# three-step all-or-nothing virtual sequence.
[entrypoint.functions."0x82C011E8"]
name = "VcallTriple_AllSuccess_82C011E8"

# A table-dispatched chunked write/emit (the write-side sibling of
# ProcessChunk_TableDispatch_82B49110, same 0x82B48/0x82B49 data-processing
# family and the same 0x80004005 failure code). Args: obj (r3), a pointer to a
# 64-bit descriptor (r4), and a total length (r5). The descriptor's high 32
# bits select a handler from a global function-pointer table (index = (hi<<2)
# & ~3) and its low 32 bits is the data pointer being emitted. Guards: if the
# descriptor is 0, return 0x80004005. Then loops: computes the chunk length as
# min(obj->24 - obj->20, remaining) (calling sub_82B48A40(obj) first when
# obj->24 is 0, returning 0x80004005 if that fails), looks up the handler,
# calls it with (data, obj->16 + position, chunk_len, ...), and advances the
# position (obj->24) and a 64-bit byte offset (obj->40); repeats until the
# length is exhausted. Returns 0 on success, 0x80004005 on failure. Single
# caller: sub_82B48898.
[entrypoint.functions."0x82B486D0"]
name = "WriteChunk_TableDispatch_82B486D0"

# ---------------------------------------------------------------------------
# A general CS enter/try-enter (270 callers), a CS-guarded handler-dispatch
# vtable method, and a position-advancing vtable-slot-3 write.
# ---------------------------------------------------------------------------

# A general critical-section acquire: enters (blocking) or try-enters
# (non-blocking) a CS and records whether it succeeded. Args: lock-state obj
# (r3), CS pointer (r4), mode flag (r5). Stores the CS pointer at obj->0. If
# the flag (low byte of r5) is 0: call __imp__RtlEnterCriticalSection(CS)
# (blocking). If nonzero: call __imp__RtlTryEnterCriticalSection(CS) (non-
# blocking); if it returns != 1 (failed to acquire), set the acquired flag to 0.
# On success set obj->4 = 1 (acquired), on try-enter failure obj->4 = 0.
# 270 call sites -- a widely-used lock-acquire helper (the matching release is
# a direct __imp__RtlLeaveCriticalSection call at the callers). The obj is a
# small lock-state wrapper: obj->0 = CS pointer, obj->4 = acquired flag.
[entrypoint.functions."0x82200688"]
name = "CriticalSection_EnterOrTry_82200688"

# A CS-guarded handler-table dispatch -- a VTABLE METHOD (0 direct callers,
# reached only via virtual dispatch) in the 0x82B43 data-processing family
# (same 0x80004005 failure code as ProcessChunk_TableDispatch / 
# WriteChunk_TableDispatch). Args: obj (r3), a pointer (r4), an offset (r5),
# and a value (r6). Guards (else return 0x80004005): obj->12 != 0, *r4 != 0,
# and (r6_low32 + r5) <= obj->32 (a 64-bit bounds check). On success: acquires
# a CS via CriticalSection_EnterOrTry_82200688 on the wrapper at obj+40, makes
# a virtual call on obj->36 (vtable slot 4) plus sub_82C6F8E8, and if that
# returns 1, computes a handler-table index and dispatches to a global
# function-pointer table with (*r4, r5 + obj->12, r6, ...); releases the CS
# (__imp__RtlLeaveCriticalSection) and returns 0. Any failed guard/step returns
# 0x80004005. The interface method and the handler-table entries aren't
# resolvable without the vtables, so the name states the mechanism: a
# bounds-checked, CS-protected dispatch.
[entrypoint.functions."0x82B43CF0"]
name = "CsGuarded_HandlerDispatch_82B43CF0"

# A position-advancing write via vtable slot 3 (a member of the same slot-3
# interface as SubObject_VcallSlot3_Bool_82C01E78 and 
# SubObject_VcallSlot3_GatedStore32_82C842C8, but over a +8 sub-object). Args:
# obj (r3), arg (r4), count (r5). Guards (else return): count > 0 and obj->8 !=
# 0. Loads the sub-object at obj->8 and calls sub_obj->vtable[3](sub_obj, 
# &{arg, 0}, sign_extend(obj->12), count) (vtable slot at offset 12 / index 3),
# then advances the position counter obj->12 += count. Returns void. Single
# caller: sub_829FCBE0. The slot-3 interface method isn't resolvable without
# the vtable, so the name states the mechanism: a slot-3 write that advances a
# position by the count.
[entrypoint.functions."0x82C62EB0"]
name = "VcallSlot3_AdvancePosition_82C62EB0"

# ---------------------------------------------------------------------------
# A signed-byte strcmp (169 callers), two vtable methods (a slot-3 product
# write and a global-callback accumulator), and a value/state processor.
# ---------------------------------------------------------------------------

# A signed-byte string compare (strcmp with sign-extended bytes). Args: str1
# (r3), str2 (r4). Loop: load *str1 as a SIGNED byte (lbz then extsb) into r10;
# if *str1 != 0 go to compare; else (str1 hit its 0 terminator) load *str2 and
# if *str2 == 0 return 0 (both empty / equal), else fall through to compare
# (where 0 < *str2 so str1 is shorter). Compare: load *str2 as a signed byte
# into r11; if r10 < r11 return -1, if r10 > r11 return 1, else advance both
# and loop. Returns -1 (str1 < str2), 0 (equal), 1 (str1 > str2). The sign-
# extension (extsb) makes this a SIGNED byte compare (bytes >= 0x80 sort as
# negative), distinct from an unsigned strcmp. 169 call sites. Named
# StrCmp_Signed to convey the signed-byte ordering and to keep the guest symbol
# distinct from host libc.
[entrypoint.functions."0x8226D7A8"]
name = "StrCmp_Signed_8226D7A8"

# A vtable method (0 direct callers, reached only via virtual dispatch) that
# computes a 32-bit product and writes it via vtable slot 3. Args: arg (r3),
# factor a (r4), factor b (r5), and a pointer-to-object (r6). Computes
# product = (int32)a * (int32)b, dereferences r6 to get the object, and calls
# object->vtable[3](r6, arg, product, 0) (vtable slot at offset 12 / index 3 --
# the same slot-3 interface as the other SubObject_VcallSlot3_* wrappers, but
# here the object is a dereferenced pointer and the product is passed in). 
# Returns the product. A "unit x count, write it" shape (cf.
# StoreUnitTimesCount_82CE32B0). The slot-3 method isn't resolvable without the
# vtable, so the name states the mechanism: a slot-3 write carrying a product.
[entrypoint.functions."0x82305270"]
name = "VcallSlot3_WithProduct_82305270"

# A vtable method (0 direct callers, reached only via virtual dispatch) that
# accumulates the results of calling a global function-pointer callback in a
# loop. Args: obj (r3), base (r4), count (r5). Guards: if the two byte fields
# obj->8 == obj->9, or count == 1, take a single-iteration/special path. 
# Otherwise loop (count-1) times: each iteration calls the global callback (a
# fixed function pointer in game data) with (base + descending_counter, 1,
# obj->4, 1) and adds the return to an accumulator. Returns the accumulated
# sum. The callback's identity and the object class aren't resolvable without
# more context, so the name states the mechanism: a looped global-callback
# accumulator.
[entrypoint.functions."0x82BC30D0"]
name = "AccumulateGlobalCallback_82BC30D0"

# A value/state processor that returns the non-zero-ness of its value argument.
# Args: obj (r3), value (r4), flag (r5). Every exit path evaluates the idiom
# (r11 = 0 - value; r10 = -1 + carry; return r10 & 1), which is 1 iff value !=
# 0 and 0 iff value == 0 -- so the RETURN is simply (value != 0). The real work
# is value-dependent SIDE EFFECTS along the way: if value > -2 call
# sub_82CD11D0(); if obj->24 < value call sub_821A9170(obj, value, obj->20);
# else (obj->24 >= value), if flag != 0 and value < 16 and value < obj->20 call
# sub_822F1DB0(obj, 1, value), and if value == 0 and obj->20 < 16 store 0 to
# obj->20 and a byte to obj->4. 8 callers, all consuming the low byte of the
# result as a boolean gate. The object class and helper identities aren't
# resolvable, so the name states the observable contract: process a value
# (with side effects) and return whether it was non-zero.
[entrypoint.functions."0x821A9090"]
name = "ProcessValue_FlagNonZero_821A9090"

# ---------------------------------------------------------------------------
# Nine functions: two hot 0x822F value/buffer operators, a hot bounded buffer
# copy with error codes, a heap carve, a lazy getter, two vtable-slot-3
# methods, an all-success vcall chain, a vtable thunk, and a return-0 stub.
# ---------------------------------------------------------------------------

# A lazy-initialized (memoized) getter for a 64-bit field -- a VTABLE METHOD
# (0 direct callers, reached only via virtual dispatch). Args: obj (r3). Loads
# obj->40; if it is already nonzero, return it immediately. If it is 0: call
# sub_82CC1460(obj->8, &out) to compute the value, cache the result at obj->40,
# then return it. A classic "compute once, cache, return" memoized getter for
# the obj->40 resource. The cached value is a 64-bit pointer/resource handle
# (ld/stw of the full 64 bits). Named for the mechanism: a lazy getter of the
# +40 field.
[entrypoint.functions."0x82B4F958"]
name = "LazyGet_Field40_82B4F958"

# A bounded buffer operation that copies or zero-fills and raises an error
# code on the failure paths. 61 call sites (a hot SDK-region routine at 0x82CA).
# Args (r3, r4, r5, r6): guards on r6 != 0 (else return 0) and r3 != 0 (else
# raise error 22). Compares two length fields (r4 vs r6): when the first is >=
# the second it copies via MemCpy_SdkRuntime_82CA2C60; when r5 == 0 it
# zero-fills via the SDK memset (sub_82CA3190) and then raises error 22 (or 34
# on one length-branch). The error is raised via a sub_82CAB770() call, a
# store of the code (22 or 34) into an out-slot, then sub_82CAB630() -- the
# SDK exception/bug path. Returns the error code (22 or 34) on failure paths
# and 0 otherwise. The exact class/args aren't resolvable (the recompiler reuses
# r3), so the name states the observable behavior: a length-checked buffer copy
# (or zero) that raises an error code on the failure paths.
[entrypoint.functions."0x82CA3730"]
name = "Buffer_CopyOrZero_Error_82CA3730"

# A vtable method (0 direct callers) -- the fifth known member of the recurring
# vtable-slot-3 interface, this one over the +16 sub-object (same sub-object
# offset and 0x82C01E region as SubObject_VcallSlot3_Bool_82C01E78). Args:
# wrapper (r3), arg (r4). Loads the sub-object at wrapper->16 and calls
# sub_obj->vtable[3](sub_obj, &{arg, 0}, 1) (vtable slot at offset 12 / index
# 3). From the returned value it derives a boolean through a
# clz/clz/rotate/xor-1 bit idiom and returns 0 or 1 (a negated/derived bit of
# the slot-3 result). The slot-3 method isn't resolvable without the vtable, so
# the name states the mechanism: a slot-3 vcall that returns a derived bool.
[entrypoint.functions."0x82C01E20"]
name = "VcallSlot3_Bool_82C01E20"

# A low-level custom-heap block carve/split in the 0x8323 allocator (sibling of
# the named Pool_*/Allocate_SizeBucketed family). Args: obj (r3), a key/value
# (r4), a pointer to the sorted-list head (r5), and a pointer to an offset (r6).
# Walks a SORTED linked list (node->0 = key, node->12 = next) to find the first
# node whose key is >= the value (else return 0). On a hit it runs alignment/
# mask arithmetic (obj->0 is an alignment mask, 16-byte block overhead), calls
# sub_83230D00 to carve/split the block, then UNLINKS the node from the list
# (fixing the predecessor's ->12 and the head) and rewrites the block's
# next/size fields. Single caller: sub_83231590 (the spin-locked pool carve). 
# Named for the mechanism: carving an aligned block out of a sorted free-list
# node.
[entrypoint.functions."0x83231058"]
name = "Heap_CarveBlock_83231058"

# A trivial 2-instruction stub: `li r3,0; blr` (returns 0). 20 call sites, all
# in the SDK region (sub_82CAB7E0, sub_82C00C90, sub_82CADF00, ...). This is a
# no-op default method / interface fallback (the same shape as NoOp_Stub_829CE870
# but returning 0 instead of void). Named for the mechanism: a return-zero stub.
[entrypoint.functions."0x831FD318"]
name = "Stub_ReturnZero_831FD318"

# A pure vtable thunk (a vtable_x forwarder, Section G). Args: wrapper (r3).
# Dereferences r3 to get the object, loads vtable slot 14 (offset 56 / 4), and
# `bctrl`s straight into it, passing the args through unmodified. Single caller:
# sub_82C14F38 (as the middle step of that all-success chain). Named per the
# Section G convention: VtableSlotX<index>.
[entrypoint.functions."0x82C01090"]
name = "VtableSlotX14_82C01090"

# A chained all-success vcall gate -- a sibling of VcallTriple_AllSuccess_82C011E8
# (same "run a sequence of virtual calls, short-circuit on the first failure, 
# return 1 only if every step succeeds" pattern). Args: wrapper (r3), arg (r5).
# (1) vcall (*wrapper)->vtable[6] (offset 24); if it returns 0, return 0. (2)
# Otherwise call VtableSlotX14_82C01090(wrapper, arg); if it returns 0, return
# 0. (3) Otherwise vcall (*wrapper)->vtable[7] (offset 28); return 1 if nonzero,
# else 0. So it returns 1 iff all three virtual steps succeed. 5 callers, all in
# the 0x82C1-0x82C3 region. Named for the mechanism: an all-success vcall chain.
[entrypoint.functions."0x82C14F38"]
name = "VcallChain_AllSuccess_82C14F38"

# A value/state processor -- a SIBLING of ProcessValue_FlagNonZero_821A9090
# (same structure, different thresholds and helpers). Args: obj (r3), value
# (r4), flag (r5). Every exit path evaluates the (r11 = 0 - value; r10 = -1 +
# carry; return r10 & 1) idiom, so the RETURN is simply (value != 0). The real
# work is value-dependent side effects: if value > 32767 call sub_82CD11D0();
# if obj->24 < value call sub_8217A4F8(obj, value, obj->20); else (obj->24 >=
# value), if flag != 0 and value < 8 and value < obj->20 call a helper and do
# bookkeeping. 7 callers (sub_826C3AA8, sub_822F6CB0, sub_822F52A8, ...), all
# consuming the low byte as a boolean gate. Named parallel to its 0x821A sibling:
# process a value (with side effects) and return whether it was non-zero.
[entrypoint.functions."0x8217A5E0"]
name = "ProcessValue_FlagNonZero_8217A5E0"

# A hot (51 callers) range/clamp + object-update operator in the 0x822F
# value/buffer subsystem (the same region as the ProcessValue_FlagNonZero_*
# pair, which it calls). Args: obj a (r3), obj b (r4), offset (r5), value (r6).
# If (b->20 < offset) call sub_82CD12C8() (a grow/extend); then compute a
# CLAMPED amount = min(value, b->20 - offset). If obj a == obj b: call
# sub_822F6E18 twice to update in place. Otherwise (obj a != obj b): call
# ProcessValue_FlagNonZero_8217A5E0(obj a, clamped, 0) and update both objects'
# fields (->4 / ->24), using a small-buffer inline-vs-pointer indirection (when
# the size field is < 8, treat the field as an inline 4-byte slot; otherwise
# dereference it). The object class isn't resolvable, so the name states the
# observable behavior: clamp a value to a range (growing if needed) and update
# the objects.
[entrypoint.functions."0x822F6380"]
name = "ClampAndUpdate_822F6380"

# A load-from-source (copy-assign) for the 0x822F object class.

# Initializes an object from a source: sets obj->20 = 0, obj->24 = 7 (a fixed
# type/size constant), and the 16-bit obj->4 = 0; then calls
# ClampAndUpdate_822F6380(obj, src, 0, -1) to load the variable-length buffer
# part from src; then copies the tail fields src->28 -> obj->28 and
# src->32 -> obj->32. Returns void. 9 callers (sub_823725E8, sub_82376A90,
# sub_825F2E10, ...). The "init to fixed defaults + clamp/copy the buffer + copy
# the tail" shape is a load-from / copy-assign for this class. The class itself
# isn't resolvable, so the name states the observable behavior: load this object
# from a source (initializing the fixed fields first).
[entrypoint.functions."0x822F2398"]
name = "LoadFrom_Source_822F2398"

# A recursive tree/list destructor that frees its nodes.

# Args: obj (r3, the owning object, passed down unchanged), node (r4). Reads
# node->17 (a byte flag): if it is nonzero, STOP (this node is a leaf/sentinel).
# Otherwise loop: (1) recurse on the child node->8 -- sub_82859340(obj, node->8);
# (2) FREE the current node via Free_SizeBucketed_Thunk_8221BE68(node); (3)
# advance node = node->0 (next) and continue while the new node->17 is 0. So it
# recursively destroys the child subtree at +8, frees each node on the way, and
# walks the +0 chain, stopping at a node whose +17 flag is set. 19 callers
# (sub_829FDAE8, sub_825F0248, sub_824FF8B8, ...). The node class isn't
# resolvable, so the name states the mechanism: a recursive destroy that frees
# the nodes of this tree/list.
[entrypoint.functions."0x82859340"]
name = "DestroyTreeNodes_82859340"

# A write-a-range-at-position operation on the 0x821A/0x822F small-buffer
# object (the same class as LoadFrom_Source_822F2398 / ClampAndUpdate_822F6380 /
# ProcessValue_FlagNonZero_*).

# Args: obj (r3), position (r4), length (r5). Uses the small-buffer
# inline-vs-pointer indirection: the buffer base is *obj->4 (external) when
# obj->24 >= 16, else &obj->4 (inline). If the position lies within
# [base, base + obj->20) it updates IN PLACE via sub_8218EA38(obj, position -
# base, length). Otherwise (position out of the current range) it routes through
# ProcessValue_FlagNonZero_821A9090(obj, length, 0) and does follow-up bookkeeping
# (a grow/realloc-style path). Returns void. 32 callers (sub_82CD1A98,
# sub_82C694C0, sub_82C83900, ...). Named for the mechanism: write a range at a
# position, in place when it fits, else via the grow path.
[entrypoint.functions."0x821A8F68"]
name = "WriteRange_AtPos_821A8F68"

# The game's PRIMARY 32-bit string hash -- FNV-1 (case-SENSITIVE). 2167 call
# sites, making it one of the hottest functions in the binary.

# Args: str (r3, a null-terminated byte string), seed (r4, the initial hash
# value). First scans str to find its length (stop at the 0 byte); if the string
# is empty, return immediately (returning the seed). Otherwise run the FNV-1
# loop over each byte: hash = (hash * 0x01000193) ^ byte, where 0x01000193
# (16777619) is the FNV-1 32-bit prime. Returns the 32-bit FNV-1 hash. The
# callers pass seed = 0x811C9DC5 (the standard FNV-1 32-bit offset basis), so
# in practice this is the canonical FNV-1 32 hash of a string. This is the
# case-SENSITIVE variant; the case-insensitive FNV-1 is sub_82448030. Named
# FNV1Hash to convey the exact algorithm.
[entrypoint.functions."0x821F3C28"]
name = "FNV1Hash_821F3C28"

# A lazy/memoized getter for the data pointer (obj->28) of the 0x822F/0x821A
# small-buffer object (same class as LoadFrom_Source_822F2398 / 
# WriteRange_AtPos_821A8F68).

# Arg: obj (r3). If obj->28 (the cached data pointer) is already set, return it.
# Otherwise, if obj->20 (length) is 0, return obj->28 (which is 0). Else resolve
# the buffer base using the small-buffer indirection -- *obj->4 (external) when
# obj->24 >= 8, else &obj->4 (inline) -- call sub_822CA580(base), cache the
# result in obj->28, and return it. A "compute once, cache, return" getter for
# the object's data pointer. 19 callers (sub_825ADB40, sub_825B27D0, ...). Named
# for the mechanism: a lazy getter of the data pointer.
[entrypoint.functions."0x822F2088"]
name = "LazyGetData_822F2088"

# An insert-at-position (with possible growth) on a 12-byte-stride dynamic
# array in the 0x8237 region.

# Args: obj (r3), a value (r4), a count/offset (r5), and a data triple (r6, a
# 3-word struct). The obj holds a 12-byte-stride array: start = obj->4, current
# position = obj->8, end/capacity = obj->12. It computes element indices as
# (obj->12 - obj->4)/12 (total) and (obj->8 - obj->4)/12 (current), clamps the
# requested index/insertion point against these, and -- when the array must
# grow -- reallocates (frees via Free_SizeBucketed_Thunk_8221BE68 and calls
# sub_82684B38 for the new block, with sub_8264EFE0 as a helper). Returns void.
# 2 callers (sub_8236FDD8, sub_82B423F0). The exact element semantics aren't
# resolvable, so the name states the mechanism: insert at a position in a
# 12-byte-stride dynamic array, growing it as needed.
[entrypoint.functions."0x82370A98"]
name = "Array_InsertAt_82370A98"

# A validated single-element insert into the 12-byte-stride dynamic array --
# the count==1 wrapper around Array_InsertAt_82370A98 (same 0x8236/0x8237 array
# family).

# Args: obj (r3), array (r4), value (r5), data triple (r6). Validates the array
# preconditions with debug assertions (ppc_trap 22 on violation): array->4 (start)
# ordering vs array->8 (current), and the data pointer. Computes the current
# element index (array->8 - array->4)/12, then calls Array_InsertAt_82370A98
# (array, value, count=1, data) to insert ONE element. Returns void. 3 callers
# (sub_8236F0F0, sub_8236E1B0, sub_82B41738). Named for the mechanism: a
# single-element (count==1) insert with precondition checks.
[entrypoint.functions."0x8236FDD8"]
name = "Array_InsertOne_8236FDD8"

# A major refcount Release (690 callers) -- a sibling of 
# Release_RefCounted_829FF648, but over the obj->0 resource.

# Arg: obj (r3). Loads obj->0 (a reference-counted resource pointer); if it is
# 0, return. Otherwise atomically decrement the refcount at (obj->0 + 12) using
# an lwarx/stwcx. retry loop under the global lock (mfmsr/mtmsrd + 
# REX_ENTER/LEAVE_GLOBAL_LOCK). If the new count is still > 0, return (not the
# last reference). If it reaches <= 0 (last reference), call the destructor
# sub_82B39A30(obj->0) and then Free_SizeBucketed_Thunk_8221BE68(obj->0). Returns
# void. 690 callers (sub_824E7BA0, sub_8328C2E0, sub_828592B0, ...) -- one of
# the hottest release paths in the binary. The COM-style "decrement, destroy +
# free on zero" contract is the same as Release_RefCounted_829FF648, except the
# resource lives at obj->0 (not obj->4) and its refcount at resource+12.
[entrypoint.functions."0x821C67D8"]
name = "Release_RefCounted_821C67D8"

# A free-forwarding thunk (743 callers) -- a single `b` to the primary free.

# 1-instruction body: `b Free_SizeBucketed_Thunk_8221BE68`. It forwards its
# argument straight to the game's primary free(). 743 callers (sub_82C0D9D0,
# sub_82C2F000, sub_82C748D0, ...) -- a distinct free entry point (most likely a
# vtable operator-delete / destructor-tail for a specific class) that simply
# delegates to the size-bucketed free. Named for the mechanism: a free thunk.
[entrypoint.functions."0x824FE010"]
name = "Free_Thunk_824FE010"

# A pure vtable thunk (a vtable_x forwarder, Section G) -- the offset-52 sibling
# of VtableSlotX14_82C01090.

# Arg: wrapper (r3). Dereferences r3 to get the object, loads vtable slot 13
# (offset 52 / 4), and `bctr`s straight into it, passing the args through
# unmodified. 2 callers (sub_82C15068, sub_82C4A410). Named per the Section G
# convention: VtableSlotX<index>.
[entrypoint.functions."0x82C010B0"]
name = "VtableSlotX13_82C010B0"

# A chained all-success vcall gate -- a sibling of VcallChain_AllSuccess_82C14F38
# (same "run a sequence of virtual calls, short-circuit on the first failure, 
# return 1 only if every step succeeds" pattern; only the middle thunk differs).

# Args: wrapper (r3), arg (r5). (1) vcall `(*wrapper)->vtable[6]` (offset 24);
# if it returns 0, return 0. (2) Otherwise call VtableSlotX13_82C010B0(wrapper,
# arg) (vtable slot 13 / offset 52); if it returns 0, return 0. (3) Otherwise
# vcall `(*wrapper)->vtable[7]` (offset 28); return 1 if nonzero, else 0. So it
# returns 1 iff all three virtual steps succeed. 16 callers, all in the
# 0x82C1-0x82C4 region. Named parallel to its 0x82C14F38 sibling (which uses
# VtableSlotX14 in the middle instead of VtableSlotX13).
[entrypoint.functions."0x82C15068"]
name = "VcallChain_AllSuccess_82C15068"

# A string setter / copy-constructor for the 0x822F/0x821A small-buffer object
# (same class as LoadFrom_Source_822F2398 / WriteRange_AtPos_821A8F68).

# 330 callers (sub_82AB77A8, sub_82A665B8, sub_82370060, ...) -- a major string-
# setting operation. Args: obj (r3), src (r4, a null-terminated byte string).
# Initializes the object: obj->20 = 0 (length), obj->24 = 15 (the max inline
# size, matching the < 16 inline threshold of the small-buffer indirection), and
# clears the inline byte at obj->4. Computes strlen(src) by scanning to the 0
# byte, then copies the string in via WriteRange_AtPos_821A8F68(obj, src,
# strlen). Returns void. So it resets this object and makes it hold a copy of
# the C string. Named for the mechanism: set this (small-buffer) object to a
# string.
[entrypoint.functions."0x822F2020"]
name = "SetString_822F2020"

# A name-hash initializer: computes the FNV-1 hash of a fixed global string and
# stores it in obj->0.

# Arg: obj (r3). Loads a fixed global string (via a pointer at ~0x81FF7B98),
# copies it into a temporary small-buffer on the stack via SetString_822F2020,
# resolves the buffer (inline vs external per the small-buffer indirection), and
# computes its FNV-1 32 hash via FNV1Hash_821F3C28 (seed = 0x811C9DC5, the offset
# basis). Stores the hash in obj->0. If the temp buffer was external it frees it
# (Free_SizeBucketed_Thunk_8221BE68). Finally clears bit 0 of obj->4 (obj->4 &=
# 0x1FFFFFFF). Returns void. 3 callers (sub_8236E1B0, sub_8236D6A8, 
# sub_8236E348). So it initializes obj->0 to the FNV-1 hash of a known global
# name and clears a flag bit in obj->4. Named for the mechanism: initialize the
# name hash from a global string.
[entrypoint.functions."0x8236D358"]
name = "InitNameHash_8236D358"

# A case-INSENSITIVE string compare (stricmp). 211 call sites.

# Args: str1 (r3), str2 (r4). Walks both strings byte-by-byte (lbzu
# pre-increment). At each step compute diff = str1byte - str2byte; if str2byte is
# the 0 terminator, return (0 if the strings are equal, else the running diff);
# if diff == 0 continue. On the FIRST difference, fold both bytes to lowercase
# (if a byte is in 'A'..'Z', OR in bit 5 / 32) and recompute diff; if the
# folded diff is 0 (they differ only by case) keep scanning, else return the
# signed folded difference. So it returns 0 when the strings are equal ignoring
# case, and a nonzero signed value otherwise. Named StrCmpIgnoreCase to convey
# the case-insensitive ordering (and keep the guest symbol distinct from host
# libc); the signed-byte variant is StrCmp_Signed_8226D7A8.
[entrypoint.functions."0x82CA6320"]
name = "StrCmpIgnoreCase_82CA6320"

# A push_back / append to a u32 dynamic array (4-byte stride).

# Args: obj (r3, the array), value (r4). The array layout: obj->0 = base
# pointer, obj->8 = current index/count, obj->4 = capacity. If (obj->4 - obj->8)
# < 1 (no room), it calls sub_82C21C90 (an ensure-capacity/grow) with a new
# size. Then stores value at base[obj->8] (a 4-byte-stride slot: *(obj->0 +
# obj->8 * 4) = value) and increments obj->8. Returns void. 38 callers, all in
# the 0x82C2-0x82C3 region. Named for the mechanism: append a u32 to a dynamic
# array (grow as needed, then store + bump the index).
[entrypoint.functions."0x82C21E08"]
name = "Array_PushBack_82C21E08"

# A binary search (lower_bound-style) over a sorted array of keyed objects,
# returning the index or insertion point plus a found flag.

# Args: obj (r3, the array: obj->0 = base pointer, obj->8 = count), a key
# pointer (r4, the target key is at r4->0), and an out flag (r5). Sets *out = 0
# (not found). If count == 0, return 0. Otherwise binary-search: low = 0, high =
# count-1; while low <= high: mid = (low+high)/2, elem = base[mid] (4-byte
# stride), key = elem->0; if key > target, high = mid-1; if key == target, set
# *out = 1 and RETURN mid (found); if key < target, low = mid+1. If the loop
# exits without a match, RETURN the insertion point (mid or mid+1) with *out
# still 0. So it's a lower_bound: exact index when found (flag set), else the
# insertion position. 11 callers (sub_82C0DEA8, sub_82C1ACD8, sub_82224FE0,
# ...). Named for the mechanism: a key-based binary search with a found flag.
[entrypoint.functions."0x82C0BC08"]
name = "BinarySearch_82C0BC08"

# The primary refcounted copy-assignment (operator=) -- 2524 callers, one of
# the hottest functions in the binary. The AddRef counterpart to
# Release_RefCounted_821C67D8.

# Args: obj (r3, this), src (r4, other). Sets obj->0 = 0. Atomically increments
# a global counter (lwarx/stwcx. retry under the global lock) -- a side-effecting
# generation/sequence counter. If src == obj, return (self-assign guard). Then:
# (1) RELEASE the old -- if obj->0 != 0, call Release_RefCounted_821C67D8(obj);
# (2) ACQUIRE the new -- if src->0 != 0, copy it into obj->0 (obj->0 = src->0)
# and atomically increment the new resource's refcount at (src->0 + 12) via the
# same lwarx/stwcx. retry loop. Returns obj (r3). So this is the classic
# refcounted copy-assign: Release(old), copy the pointer, AddRef(new). 2524
# callers (sub_82690588, sub_82594EA0, sub_824B2540, ...).
[entrypoint.functions."0x821F0108"]
name = "CopyAssign_RefCounted_821F0108"

# A push of a 36-byte element (a small-buffer object) into a 36-byte-stride
# array (the 0x8237 array family; the elements are the small-buffer class that
# LoadFrom_Source_822F2398 operates on).

# Args: obj (r3, the array: obj->4 = start, obj->8 = current, obj->12 = end,
# 36-byte stride), source element (r4). Computes the element counts (obj->8 -
# obj->4)/36 and (obj->12 - obj->4)/36. If there is room (current element index
# < end element index): copy the source element into the slot at obj->8 via
# LoadFrom_Source_822F2398, then advance obj->8 by 36 (one element). Otherwise
# (array full) it routes to sub_82376118 (a grow path). Returns void. 7 callers
# (sub_82B3EDD0, sub_82B4B0E0, sub_82375270, ...). Named for the mechanism: push
# a 36-byte element into the array (copy in, bump the position, grow if full).
[entrypoint.functions."0x823758C0"]
name = "Array_PushElement_823758C0"

# A bounds-checked copy that validates its parameters and delegates to 
# sub_82CAA2E0 (a later TODO function) -- a sibling of 
# Buffer_CopyOrZero_Error_82CA3730 (same 0x82CA region, same error codes).

# Args: dst (r3), capacity (r4), src (r5), len (r6). Guards (else raise an
# error): len != 0, dst != 0, and src != 0 -- any null raises error 22 (via the
# sub_82CAB770() + store-code + sub_82CAB630() SDK exception path). Then if
# capacity < len, raise error 34 (buffer too small). Otherwise call
# sub_82CAA2E0(dst, src, len) to perform the copy and return 0. So it's a
# validated copy: check dst/src non-null and capacity >= len, then copy. 
# Returns 0 on success, 22 (null pointer) or 34 (insufficient capacity) on
# failure. 137 callers (sub_82B39B48, sub_82A4D7F0, sub_829BBE00, ...). Named
# for the mechanism: a bounds-checked copy.
[entrypoint.functions."0x82CA3808"]
name = "CheckedCopy_82CA3808"

# A SIMD (vector) memcpy KERNEL -- the 128-bytes-per-iteration vectorized loop
# that a vectorized memcpy dispatches to for large blocks.

# Args: dst (r3), src (r4), length (r5). For length >= 128 the main loop loads
# 8 x 128-bit vectors from src (lvx at offsets 0/16/32/48/63/64/80/96 with vperm
# alignment), stores them to dst (stvx at the same offsets), then advances dst
# and src by 128 and length -= 128, looping while length >= 128 (with dcbt/dcbzl
# cache-block hints for the large case). A scalar path (lvsl shift + tail store)
# handles length < 128. Single caller: MemCpy_Vectorized_822085D0 (it calls this
# as its large-block kernel). So it is the raw SIMD copy kernel. Named for the
# mechanism: a SIMD (128-byte-unrolled) memcpy kernel.
[entrypoint.functions."0x82CC9DA0"]
name = "MemCpy_SIMDKernel_82CC9DA0"

# A validated tree-node removal: destroys the node's subtree and rewires the
# links to take the node out of the structure.

# Args: obj (r3), node (r4), and two pointers (r5, r6). Validates preconditions
# with `trap 22` assertions (r5 and r6 must be nonzero, not equal to node, and
# must match node's fields). On success it destroys the node's subtree via
# DestroyTreeNodes_82859340(node, node->4), then rewires: obj->0 = node, clears
# node->8, sets the node's self-reference fields, and copies node->4->0 into
# obj->4 (splicing the node out). Returns void. 11 callers (sub_82A6D9F0,
# sub_82A56CA0, sub_82B150D8, ...). Named for the mechanism: remove a node from
# a tree (destroy its subtree, rewire the links).
[entrypoint.functions."0x826A4A20"]
name = "Tree_RemoveNode_826A4A20"

# A specialized allocation entry point: allocates `size` bytes via the primary
# malloc, with conditional setup for size >= 2.

# Args: size (r3), flag (r4, always 0 in the callers). If size == 0 or size == 1
# (detected via (-1 / size) >= 1), skip the setup. Otherwise (size >= 2) it runs
# a setup that references two fixed global strings (~0x81FF563C / 0x81FF5630) and
# calls sub_822F1F00 (a local-stack preparation). Then it always calls
# Allocate_OrDbgBreak_8221F388(size) (the game's primary malloc) and returns the
# allocated pointer. So it's a wrapper around the primary allocator that does
# some conditional initialization first. 4 callers (sub_821A9170, sub_82A1DA08,
# sub_82A1DC28, sub_826C0D38). Named for the mechanism: allocate (with
# conditional init) via the primary malloc.
[entrypoint.functions."0x821A9258"]
name = "Allocate_Init_821A9258"

# A buffer grow/realloc for the 0x821A/0x822F small-buffer object -- the grow
# path called by ProcessValue_FlagNonZero_821A9090.

# Args: obj (r3), new size request (r4), new length (r5). Computes the new size
# (r4 | 15, i.e. rounded up toward a 16-multiple, or a combined size derived from
# obj->24 for small values), allocates a new buffer of (new_size + 1) bytes via
# Allocate_Init_821A9258, and if the length (r5) is nonzero copies the existing
# data into the new buffer via Buffer_CopyOrZero_Error_82CA3730 (resolving the
# old base via the small-buffer inline-vs-external indirection). Frees the old
# external buffer if obj->24 >= 16 (Free_SizeBucketed_Thunk_8221BE68). Then
# updates obj->4 (new buffer pointer), obj->24 (new size), obj->20 (new length),
# and null-terminates the external buffer. Returns void. Single caller:
# ProcessValue_FlagNonZero_821A9090 (its grow path). So it's a data-preserving
# realloc of the small-buffer object's storage. Named for the mechanism: grow the
# buffer (allocate larger, copy, free old, update fields).
[entrypoint.functions."0x821A9170"]
name = "GrowBuffer_821A9170"

# A clamped range update -- a SIBLING of ClampAndUpdate_822F6380 (same
# structure, different update helper), and the in-place core that
# WriteRange_AtPos_821A8F68 calls.

# Args: obj a (r3), pos/obj b (r4), length (r5), extra (r6). If (b->20 < length)
# call sub_82CD12C8 (a grow/extend); then compute a CLAMPED amount = min(extra,
# b->20 - length). If obj a == obj b: update IN PLACE via sub_822F1F60 twice
# (with (clamped + length, -1) then (0, length)). Otherwise (obj a != obj b):
# a cross-object update path (loc_8218EAAC). Returns void. 41 callers (sub_82C6A248,
# sub_82C84920, sub_82C83900, ...). Parallel to ClampAndUpdate_822F6380 (which
# uses sub_822F6E18 for the in-place update); here the in-place helper is
# sub_822F1F60. Named for the mechanism: a clamped range update (grow if
# needed), in place or across objects.
[entrypoint.functions."0x8218EA38"]
name = "RangeUpdate_8218EA38"

# A case-INSENSITIVE FNV-1 32 string hash -- the ToLower-per-byte sibling of
# FNV1Hash_821F3C28.

# Args: str (r3, null-terminated), seed (r4, initial hash). Scans str for its
# length (stop at 0); if empty, return the seed. Otherwise run the FNV-1 loop
# over each byte, but FIRST lowercase it via ToLower_SdkRuntime_82CA4288, then
# hash = lowercased_byte ^ (hash * 0x01000193), where 0x01000193 (16777619) is
# the FNV-1 32-bit prime. Returns the 32-bit case-insensitive FNV-1 hash. The
# case-SENSITIVE variant is FNV1Hash_821F3C28. 2 callers (sub_821BC7E8,
# sub_82C67878). Named to convey the exact algorithm + case-insensitivity.
[entrypoint.functions."0x82448030"]
name = "FNV1Hash_CaseInsensitive_82448030"

# A vtable-slot-3 caller with a loop/single mode, selected by a flag bit in
# obj->5 -- a member of the recurring slot-3 interface (over the *obj->0
# sub-object).

# Args: obj (r3), value (r4), count (r5). A bit test on obj->5 (via
# clz/rotate/xor) selects the mode. MODE A (loop): if count != 0, loop `count`
# times -- each iteration vcall `(*obj->0)->vtable[3](obj->0, &{value++, 0}, 1)`
# (vtable slot at offset 12 / index 3), incrementing value each pass -- then
# return 1. MODE B (single): one vcall `(*obj->0)->vtable[3](obj->0, &{value, 0},
# count)`, returning a bit derived from the result (clz/rotate/xor). So it calls
# the slot-3 method either `count` times or once, depending on the obj->5 flag.
# 1 caller: sub_82C669C0. The slot-3 method isn't resolvable without the vtable,
# so the name states the mechanism: a slot-3 vcall in loop or single mode.
[entrypoint.functions."0x82C66330"]
name = "VcallSlot3_LoopOrSingle_82C66330"

# A stateful object processor in the 0x82C6 region that orchestrates a gated
# slot-3 store, a buffer update, and a slot-3 loop/single call, returning a byte
# flag.

# Args: (r3), obj a (r4), obj b (r5). If obj a->4 (a byte) != 0: call
# SubObject_VcallSlot3_GatedStore32_82C842C8(obj a, &out) and store the result
# back into obj a->4. Reads the out value, and updates a buffer via sub_822F1F60
# (if out-1 > obj b->20) or sub_826C2FF8 (else, with out-1-obj b->20). Resolves
# obj b's buffer base via the small-buffer indirection (obj b->24 >= 16 ? *obj
# b->4 : &obj b->4), then calls VcallSlot3_LoopOrSingle_82C66330(obj a, base,
# out). Finally returns the byte obj a->4. 2 callers (sub_82C85250, 
# sub_82C67878). The object class isn't resolvable, so the name states the
# observable behavior: process the object (gated slot-3 store + buffer update +
# slot-3 call), returning a byte flag.
[entrypoint.functions."0x82C669C0"]
name = "ProcessObject_82C669C0"

# A push of a 12-byte element into a 12-byte-stride array (with growth) -- a
# sibling of Array_PushElement_823758C0 (36-byte) and Array_PushBack_82C21E08
# (u32), but here the element is a raw 12-byte struct copied verbatim.

# Args: obj (r3, the array: obj->4 = start, obj->8 = current, obj->12 = end,
# 12-byte stride), src (r4, a 12-byte struct). Computes current element count
# (obj->8 - obj->4)/12 and total (obj->12 - obj->4)/12. If there is room (current
# < total): copy the 12 bytes from src into the slot at obj->8 (obj->8+0/+4/+8 =
# src->0/+4/+8), then advance obj->8 by 12. Otherwise (full): validate (obj->4 <=
# obj->8, else `trap 22`) and grow via sub_82C66BE0, then push. Returns void.
# Single caller: sub_82C67878. Named for the mechanism: push a 12-byte element
# into the array (copy in, bump the position, grow if full).
[entrypoint.functions."0x82C66E00"]
name = "Array_Push12_82C66E00"

# A 5-way state dispatch (state-machine switch) on the byte obj->16->21
# (values 0-4) -- a switch/jump-table dispatcher.

# Arg: obj (r3). Reads the state byte at obj->16->21. If it is > 4, jump to a
# default path. Otherwise switch on it via a function-pointer table:
#   case 0: sub_82BCB7E8(obj), return 0
#   case 1: (obj->16->36 related path)
#   case 2/3/4: other 0x82BC handler paths
# (each case calls a distinct handler -- sub_82BC9860, sub_82BCB0A8, 
# sub_82BCB610, sub_82BCB6E0, sub_82BCB8D0, sub_82BCD698, sub_8227BB58, ...).
# 2 callers (sub_82BC62B8, sub_8227B8B8). The individual states/handlers aren't
# resolvable, so the name states the mechanism: a 5-way dispatch on a state byte.
[entrypoint.functions."0x82BCBB40"]
name = "StateDispatch_82BCBB40"

# A binary search with a VIRTUAL (polymorphic) comparison -- a sibling of
# BinarySearch_82C0BC08, but the element-vs-target comparison is done by a
# vtable method instead of a direct field compare.

# Args: obj (r3, the array: obj->0 = base, obj->8 = count), a target (r4), and an
# out flag (r5). Sets *out = 0. If count == 0, return 0. Otherwise binary-search:
# low = 0, high = count-1; while low <= high: mid = (low+high)/2, element =
# base[mid] (4-byte stride), and call (element->4)->vtable[1] (a virtual compare
# method) to compare the element to the target; narrow low/high on the result. 
# Returns the index/insertion point and sets *out on a match (like a lower_bound).
# Single caller: sub_82C17350. The element class (and thus the compare method)
# isn't resolvable, so the name states the mechanism: a binary search whose
# comparison is a virtual method.
[entrypoint.functions."0x82C15850"]
name = "BinarySearch_VirtualCmp_82C15850"

# A lookup-by-key that binary-searches an array (wrapper->4) with a virtual
# comparison and returns the found element's field 4 -- the caller of
# BinarySearch_VirtualCmp_82C15850.

# Args: wrapper (r3, whose member at +4 is the array), key (r4). Calls
# BinarySearch_VirtualCmp_82C15850(wrapper->4, key, &found) to binary-search the
# array. If found (found != 0) and the element at the returned index is non-null:
# return element->4 (a sub-object). Otherwise return 0. So it's a find-by-key that
# returns the matched element's ->4 member (or 0 if not found). 0 direct callers
# (a virtual/dead method). Named for the mechanism: look up a key via the
# virtual-compare binary search and return the found element's field 4.
[entrypoint.functions."0x82C17350"]
name = "Lookup_ByVirtualCmp_82C17350"

# A tree insertion: walk a binary tree to find the insertion point for a key, then
# insert a node (with a fixup call).

# Args: root (r3, whose ->4 is the first node), key wrapper (r4, *r4 = the key).
# The nodes carry: ->8 = right child, ->0 = parent, ->12 = node key, ->21 = a
# state byte (0 / nonzero). The search loop walks the tree -- if node->12 >= key,
# step up to the parent (*node); else step right (node->8) -- until it reaches a
# node with a nonzero state byte (the insertion point). It then performs the
# insertion (splicing a new node in) and calls sub_82419280 (a tree fixup/rebalance
# step). 7 callers (sub_829FF798, sub_823715E8, sub_8236DFC0, sub_8226A650,
# sub_829FFC48, sub_82A000F8, sub_82509E40). The exact tree type (e.g. red-black)
# isn't confirmed from the fields, so the name states the observable behavior: a
# tree insert (find the point, insert, fix up).
[entrypoint.functions."0x82A00620"]
name = "Tree_Insert_82A00620"

# A constant-return stub: returns the integer 4 (a 2-insn function: li r3,4; blr).
# 0 direct callers, so it is almost certainly a VIRTUAL method (invoked through a
# vtable) -- likely a property/type-code/size accessor whose value is the constant
# 4. The exact meaning of 4 needs the vtable + class. Named for what it does: a
# stub returning the constant 4.
[entrypoint.functions."0x826A0520"]
name = "Stub_Return4_826A0520"

# A binary search (lower_bound) over a 12-byte-stride array, comparing each
# element's field 0 to a key -- a sibling of BinarySearch_82C0BC08 (4-byte) and
# BinarySearch_VirtualCmp_82C15850 (virtual cmp).

# Args: obj (r3, the array: obj->4 = start, obj->8 = current, obj->12 = end, 12-byte
# stride), key wrapper (r4, *r4 = the key). Validates the ranges (obj->8 <= obj->12,
# obj->4 <= obj->8, else `trap 22`). Binary-searches: mid, element = base + mid*12;
# compare element->0 (the element's key field) to the key -- if element->0 < key, move
# right (base += 12, shrink the range); else move left. On completion, validate the
# found position (several `trap 22` range checks) and return the insertion point / match.
# 2 callers (sub_8236DFC0, sub_82372108). The element class (a 12-byte struct keyed on
# its first word) isn't resolvable, so the name states the mechanism: a 12-byte-stride
# binary search by element->0.
[entrypoint.functions."0x8236F0F0"]
name = "BinarySearch_12Stride_8236F0F0"

# A RECURSIVE linked-list destroy: walk the list, releasing each node's ->0
# sub-object and freeing the node.

# Args: obj (r3, an owner/context), node (r4, the current list node; node->8 = next
# link, node->0 = a refcounted payload, node+16 = a refcount slot, node->25 = a flag
# byte). Recursively calls itself on node->8 (the next link); for the current node it
# releases node->0 via Release_RefCounted_829FF648 (at node+16) and frees the node via
# Free_SizeBucketed_Thunk_8221BE68, then advances to the next node (node->0) and loops
# while the node->25 flag is 0. Returns void. So it recursively tears down a linked
# list of refcounted nodes. 6 callers (sub_82443110, sub_823D46F8, sub_83218A40,
# sub_8247E448, sub_82A121D8, sub_83218350). The exact list type isn't resolvable, so
# the name states the mechanism: a recursive destroy of a linked list (release + free
# each node).
[entrypoint.functions."0x82443440"]
name = "DestroyList_Recursive_82443440"

# A string/buffer load into obj with a conditional FAST and SLOW path, selected by
# a field on a control object (r4->20).

# Args: obj (r3, the destination small-buffer object), control (r4, ->20 selects the
# path), source (r5, the source small-buffer object). If control->20 == 0 (fast path):
# just LoadFrom_Source_822F2398(obj, source). Otherwise (slow path): resolve the
# source buffer via the small-buffer indirection (source->24 >= 8 ? *source->4 : 
# &source->4), build a local buffer, and copy through ClampAndUpdate_822F6380 plus
# sub_8217AA20 / sub_8217AB30 / sub_822F5208 / sub_822F6CB0, freeing temporaries via
# Free_SizeBucketed_Thunk_8221BE68. 6 callers (sub_823735B0, sub_82B4FBD0, 
# sub_823728E0, sub_82B41738, sub_825F2E10, sub_8260B9E0). The exact semantics of the
# control object and the slow-path transform aren't fully resolvable, so the name
# states the observable structure: load a string/buffer, fast path when control->20
# is 0, otherwise the slower multi-step path.
[entrypoint.functions."0x823725E8"]
name = "LoadString_FastOrSlow_823725E8"

# A SIMPLE 12-byte-stride binary search (comparing element->0 to a key over [start,
# end)) -- a simpler sibling of BinarySearch_12Stride_8236F0F0 (no trap-22
# validation), storing the result into *out.

# Args: out (r3, receives the found position), start (r4), end (r5), key wrapper
# (r6, *r6 = the key). count = (end - start)/12 (12-byte element stride). Binary-
# searches: element = start + mid*12; if element->0 >= key move left, else move right
# (start += 12). On completion, stores the result into *out. Returns void. 2 callers
# (sub_82B40BD0, sub_82B41738). The element class (a 12-byte struct keyed on its first
# word) isn't resolvable, so the name states the mechanism: a simple 12-byte-stride
# binary search by element->0.
[entrypoint.functions."0x82B429A0"]
name = "BinarySearch_12Stride_Simple_82B429A0"

# An identity/passthrough: returns its 2nd argument (r4) unchanged (a 2-insn
# function: mr r3,r4; blr).

# The 3 callers (sub_82EE53D8, sub_82FFE460, sub_82EE95D8) each pass a constant in
# r3 (4, 752, or 312 -- an opcode/type-ish value) and a value in r4, and take back
# r4 (the value, unchanged). So the observable behavior is an identity on the second
# argument. This shape (pass a code + a value, get the value back) is the classic
# default/identity implementation of a VIRTUAL method (a base handler that returns
# its input unchanged), or an inlining artifact. The exact class/vtable isn't
# resolvable, so the name states what it does: return arg2 unchanged.
[entrypoint.functions."0x82C2CE58"]
name = "Identity_ReturnArg2_82C2CE58"

# A process-elements loop: walk a linked structure and, per element, dispatch 4
# vmethods (slots 0-3) on a target object.

# Args: obj (r3, ->0 = a vtable, ->4 = the end marker), target (r4, an object whose
# ->0 is a vtable), plus two values (r5, r6). Loop while the current element (r29,
# starting at *obj->0) != obj->4: each iteration calls four methods on target via its
# vtable -- target->vtable[0](target, elem->16), target->vtable[1](target, r5),
# target->vtable[2](target, prevResult), target->vtable[3](target, r6) -- then
# advances the element to elem->0 or elem->4 (chosen by the call results). When the
# element reaches obj->4, stop and return 0. 24 callers (sub_82C53B88, sub_82C14688,
# sub_82C02730, sub_82C17460, ...). The object class and the four methods aren't
# resolvable, so the name states the mechanism: process the elements, each with a
# 4-vcall chain on the target.
[entrypoint.functions."0x82C46C10"]
name = "ProcessElements_Vcall4_82C46C10"

# A constructor/initializer for a 12-byte (three u32 field) object from a value,
# with a small/large distinction (a tagged-value init).

# Args: obj (r3, zero-initializes obj->4, obj->8, obj->12), value (r4). If value ==
# 0, return 0 (an empty object). Otherwise, if value <= 0x3FFFFFFF (fits in 30 bits, a
# small/inline value): the small-value init path. Else (value > 0x3FFFFFFF, a pointer/
# large value): build a local string via SetString_822F2020 (from a global string) and
# convert it via sub_826C3EF0 into the object's fields. Returns a value the callers
# mask to a byte (clrlwi r3, 24 -- a status/size). 3 callers (sub_82B4BFD8, 
# sub_82B4B2B8, sub_82B4D300), all in the 0x82B4 data-processing region. The object
# class and the exact meaning of the three fields aren't resolvable, so the name
# states the mechanism: construct/init the object from a value (inline if small,
# converted if large).
[entrypoint.functions."0x82B4CB58"]
name = "Construct_FromValue_82B4CB58"

# A 7-way state dispatch (state-machine switch) on the byte (obj->4 - 4) (values
# 0-6) -- a switch/jump-table dispatcher, a sibling of StateDispatch_82BCBB40.

# Args: obj (r3), state obj (r4, whose ->4 is the switch key and ->5 a status byte).
# Clears the low 2 bits of obj->5 (stb obj->5 = obj->5 & 0xFFFFFFFC), computes the
# key = obj->4 - 4, and (if key is in 0..6) switches on it via a function-pointer
# table; each case (0-6) performs inline register operations (no external sub-calls --
# the case bodies manipulate obj's fields directly). 17 callers (sub_822DF280, 
# sub_82BCC6F0, sub_82BD3A90, sub_82BC56F8, sub_82BCB7E8, ...). The individual states
# and their effects aren't resolvable, so the name states the mechanism: a 7-way
# dispatch on a state byte.
[entrypoint.functions."0x82BCA7D8"]
name = "StateDispatch_82BCA7D8"

# A memmove (overlap-safe copy): copy `size` bytes from src to dst, handling the
# case where the regions overlap.

# Args: dst (r3), src (r4), size (r5). If dst == src, return (nothing to do). If dst
# < src (no dangerous overlap), delegate to the forward SDK copy -- 
# MemCpy_SdkRuntime_82CA2C60(dst, src, size). Otherwise (dst >= src, a backward
# overlap): copy BACKWARDS so the source isn't clobbered -- first byte-by-byte until
# dst is 4-byte aligned (from the end: *--dst = *--src), then 4-byte word-by-word
# backwards. Returns void. 80 callers (sub_82171010, sub_8319BA60, Func_82CB4CB8,
# sub_82BAF318, Exception_82CC031C, ...). This is the classic memmove: a copy that is
# safe when src and dst overlap. Sits next to MemCpy_SdkRuntime in the memory family.
[entrypoint.functions."0x82CAA2E0"]
name = "MemMove_SdkRuntime_82CAA2E0"

# A SIGNED 3-way comparator of two 32-bit values: returns -1 if a < b, 0 if a == b,
# 1 if a > b (where a = *r4 and b = *r5).

# Args: (r3), a wrapper (r4, a = *r4), b wrapper (r5, b = *r5). Compares a and b
# (signed, cmplw/bge): if a < b, return -1. Otherwise (a >= b): compute the borrow of
# (b - a) via subfc/subfe and return its LSB -- which is 0 when a == b (no borrow) and
# 1 when a > b (b - a underflows, borrow set). So the return is -1 / 0 / 1 for
# less-than / equal / greater-than -- a standard 3-way signed compare (like std::cmp / 
# the sign of a - b). 0 direct callers, so it is most likely a VIRTUAL comparison
# method (a comparator used by a sort/search). The operand class (a 4-byte value
# deref'd from r4/r5) isn't resolvable, so the name states the mechanism: a signed
# 3-way compare of two 32-bit values.
[entrypoint.functions."0x82C13458"]
name = "Compare3Way_Signed32_82C13458"

# A vtable-slot-12 call wrapper: deref obj, load vtable offset 48 (slot 12), and call
# it with obj as the sole argument, returning the result.

# Arg: obj (r3, whose ->0 is a vtable). Loads *(obj->0) = the vtable, then vtable[48]
# (slot 12, 48/4), and calls it with obj (bctr). Returns the callee's result. 1 caller:
# sub_82C14E08. The method (slot 12) isn't resolvable without the vtable, so the name
# states the mechanism: call the object's vtable slot 12.
[entrypoint.functions."0x82C010D0"]
name = "VcallSlot12_82C010D0"

# A 3-step vcall chain (slots 6 -> 12 -> 7), each step gated on the previous result's
# low byte being nonzero -- a sibling of VcallTriple_AllSuccess (6,8,7) and
# VcallChain_AllSuccess (6,14,7).

# Args: obj (r3, whose ->0 is a vtable), (r4), value (r5). Step 1: vcall
# obj->vtable[6] (offset 24); if the result's low byte == 0, return 0. Step 2: vcall
# slot 12 via VcallSlot12_82C010D0(obj, value); if the result's low byte == 0, return
# 0. Step 3: vcall obj->vtable[7] (offset 28); return (result's low byte != 0). So it's
# an all-or-nothing chain of three virtual calls (6, 12, 7), returning 1 only if all
# three report success (nonzero byte). 2 callers (sub_82C1E360, sub_82C15100). The
# methods (slots 6/12/7) aren't resolvable without the vtable, so the name states the
# mechanism: a 3-step vcall chain, slots 6 -> 12 -> 7, all-or-nothing.
[entrypoint.functions."0x82C14E08"]
name = "VcallChain_6_12_7_82C14E08"

# A vtable-slot-0 call with a null check and a flag of 1 -- the classic virtual
# destructor-call shape (obj->vtable[0](obj, 1)).

# Arg: obj (r3). If obj == null, return (no-op). Otherwise load *(obj->0) = the vtable
# and call vtable[0] (offset 0 / slot 0) with obj and r4 = 1 (bctr). Returns the
# callee's result. 0 direct callers, so it is most likely invoked VIRTUALLY (a
# destructor/dispose entry) -- vtable slot 0 is typically the destructor in the Itanium
# C++ ABI, and the r4 = 1 is plausibly a "deleting"/"full" flag. The exact class and
# whether slot 0 is the destructor aren't resolvable here, so the name states the
# mechanism: null-check, then call the object's vtable slot 0 with a flag of 1.
[entrypoint.functions."0x829681C0"]
name = "VcallSlot0_Flag1_829681C0"

# A refcounted value setter that also builds an owning 12-byte wrapper: release the
# current obj->0, store the new value, and (if non-null) allocate a refcounted wrapper.

# Args: obj (r3, an 8-byte object: obj->0 = the value, obj->4 = a wrapper slot), value
# (r4). First releases the current obj->0 via Release_RefCounted_829FF648, then sets
# obj->0 = value. If value == null, set obj->4 = 0 and stop. Otherwise allocate a 12-byte
# block via Allocate_OrDbgBreak_8221F388(12) and initialize it as a wrapper -- wrapper->0
# = 1 (a refcount), wrapper->4 = a fixed constant (a type/offset), wrapper->8 = value --
# then set obj->4 = that wrapper (or 0 if the allocation failed). Returns void. 171
# callers (sub_8290DA78, sub_8246A048, sub_82807978, ...). The wrapper's exact role (the
# constant and what obj->4 is used for) isn't resolvable, so the name states the
# mechanism: set a refcounted value and, for a non-null value, allocate + populate a
# 12-byte owning wrapper.
[entrypoint.functions."0x8238A848"]
name = "SetRefcountedWrapper_8238A848"

# A vtable-slot-3 call on the sub-object obj->0 (offset 12 / slot 3) that passes a
# value pointer and returns a BIT derived from the result -- a member of the recurring
# slot-3 interface.

# Args: obj (r3, whose ->0 is the sub-object and ->5 a flag byte), value (r4). A bit
# test on obj->5 (via clz/rotate/xor) selects which stack slot holds the value pointer
# (a 2-element struct {value, 0}). Then vcall `(*obj->0)->vtable[3](obj->0, &{value, 0},
# 1)` (vtable slot at offset 12). Finally it returns a single bit derived from the
# result (clz of the result, then rotate-right-27 and mask 1). 8 callers (sub_8236D3C8,
# sub_82C85538, sub_82C84838, sub_82A4AF78, sub_82C680F8, sub_82B483D0, sub_82C85758,
# sub_82365458). The slot-3 method isn't resolvable without the vtable, so the name
# states the mechanism: a slot-3 vcall that returns a bit.
[entrypoint.functions."0x82372268"]
name = "VcallSlot3_ReturnBit_82372268"

# A get-or-create-by-name: hash a name, binary-search a 12-byte-stride array for it,
# and (if not found) insert a new element.

# Args: obj (r3, whose member at +4 is the array: start/current/end, 12-byte stride),
# name (r4, a string). First hashes the name via InitNameHash_8236D358 into a local. 
# Validates the array ranges (start <= current, current <= end, else `trap 22`). Then
# binary-searches the array (12-byte stride) comparing each element's key (element->0,
# the stored name hash) to the computed hash. On a miss it inserts a new element via
# Array_InsertOne_8236FDD8. So it's a find-or-create keyed on the name's hash. 2
# callers (sub_82371820, sub_8236DD48). The element class (a 12-byte struct holding a
# name hash + the name) isn't fully resolvable, so the name states the mechanism:
# get-or-create an entry by name hash.
[entrypoint.functions."0x8236E1B0"]
name = "GetOrCreateByNameHash_8236E1B0"

# A constant-return global getter: loads and returns a fixed global address (a
# 2-insn function: lis r11, <hi>; addi r3, r11, <lo>; blr -- ~0x83654CE0).

# Takes no meaningful arguments and always returns the same global pointer. 0 direct
# callers, so it is most likely a VIRTUAL method (a getter for a global/singleton, or
# an accessor whose value is that fixed address) -- the exact global's meaning needs
# the vtable + class. The name states what it does: return a fixed global pointer.
[entrypoint.functions."0x82C1E350"]
name = "GetGlobal_82C1E350"

# The SDK memset: fill `size` bytes at dst with the byte value (r3 = dst, r4 = value,
# r5 = size).

# Implementation: fill byte-by-byte (*r6++ = r4) until dst is 4-byte aligned; then build
# a 32-bit word from the byte (rlwimi r4 = 0xNNNNNNNN, the byte replicated 4 times) and
# fill word-by-word in 16-byte chunks (four stw per iteration); finally handle the
# remaining tail bytes. Returns void. 479 callers (MemFree_82CBFD74, sub_831D9228,
# sub_831CE8D8, ...). This is the SDK's memset -- the memory-family sibling of
# MemCpy_SdkRuntime / MemMove_SdkRuntime. Named MemSet_SdkRuntime to keep the guest
# symbol distinct from the host libc memset.
[entrypoint.functions."0x82CA3190"]
name = "MemSet_SdkRuntime_82CA3190"

# A remove-and-free tree-node wrapper: remove obj->4 from the tree, then free it and
# clear the slots.

# Arg: obj (r3, an 8-byte object: obj->4 = the tree node, obj->8 = a related field). 
# Calls Tree_RemoveNode_826A4A20(obj, obj->4, &out1, &out2) to detach the node from the
# tree (the two out pointers are stack slots receiving the removal result), then frees
# the node via Free_SizeBucketed_Thunk_8221BE68(obj->4), then sets obj->4 = 0 and obj->8
# = 0. Returns void. So it's a detach-and-destroy of the node stored in obj->4. 20
# callers (sub_832AB150, sub_8281FB68, sub_82441E90, sub_832AF290, sub_82724AE8, ...). 
# The node/tree class isn't resolvable, so the name states the mechanism: remove the
# node from the tree and free it.
[entrypoint.functions."0x826A4978"]
name = "RemoveAndFreeNode_826A4978"

# A tree find-and-process: walk the tree to locate the node / insertion point for a
# key, then process it -- a sibling of Tree_Insert_82A00620 (same node layout: ->8 right,
# ->0 parent, ->12 key, ->25 state byte).

# Args: obj (r3, ->4 = first node), key wrapper (r4, *r4 = the key). Walks the tree --
# if node->12 < key step right (node->8), else step up to the parent (*node) -- until a
# node with a nonzero ->25 state byte (the target). Then it processes the located node:
# compares it to the key, and (depending on the result) calls sub_82A122F8 and
# Release_RefCounted_829FF648 (releasing a node's payload). 166 lines, 1 caller:
# sub_83218598. The exact find-vs-remove semantics and the node class aren't fully
# resolvable, so the name states the observable mechanism: walk the tree to find the
# node for a key, then process it (release + sub_82A122F8).
[entrypoint.functions."0x82A120B0"]
name = "Tree_FindNode_82A120B0"

# A large (364-line) tree-node processor: the slow-path handler called by
# Tree_FindNode_82A120B0, with a fast path (sub_82A128B0) and a validated slow path.

# Args: (r3), obj (r4, an object with ->4 and ->8 fields), plus r5 and r6. If obj->8 ==
# 0 (fast path): call sub_82A128B0(obj->4, r6, 1) and return. Otherwise (slow path):
# validate (obj->4 != 0 and a pointer-equality check on r5, else `trap 22`) and perform
# the heavier node processing (sub_823D4F20, sub_82498700, sub_82A126D0, sub_82A128B0).
# 1 caller: Tree_FindNode_82A120B0. The node/tree class and the exact processing aren't
# resolvable, so the name states the observable role: process a tree node (fast or slow
# path). It is the deep handler behind the Tree_FindNode walk.
[entrypoint.functions."0x82A122F8"]
name = "ProcessTreeNode_82A122F8"

# A large (329-line) node string-processing handler in the 0x82A1 tree cluster -- the
# fast-path / deep handler behind ProcessTreeNode_82A122F8.

# Args: (r3), obj (r4, ->8 = a size/length field), plus r5, r6, r7. Compares obj->8 to
# a constant (0x01555554): if below it, one path (loc_82A12928). Otherwise: build a local
# string via SetString_822F2020 (from a global string), convert it via sub_826C3EF0, then
# further process it via sub_822F1F00 and sub_826C3FA8, and (in the longer path) call
# sub_82171810, sub_827C6448, sub_827C64C0, and sub_82A12B90. 2 callers (ProcessTreeNode_
# 82A122F8, sub_82A126D0). The node class and the exact string transform aren't fully
# resolvable, so the name states the observable role: process a node's string (a
# build/convert/format path selected by obj->8).
[entrypoint.functions."0x82A128B0"]
name = "ProcessNodeString_82A128B0"

# A 28-byte node allocation + initialization (a "make a node" operation) -- the
# allocation step called by ProcessNodeString_82A128B0.

# Args: (r3), a (r4), b (r5), c (r6), src (r7). Allocates 28 bytes via
# Allocate_OrDbgBreak_8221F388(28). On success, initializes the block: block->0 = a,
# block->4 = b, block->8 = c, block->12 = src->0, block->16 = src->4, block->20 = src->8.
# If src->8 (a linked field) is nonzero, it acquires the global lock (REX_ENTER_GLOBAL_
# LOCK) and does an atomic load (lwarx) on src->8 (a lock-free linked-list append/splice
# under the global lock). Returns the allocated block (or null on allocation failure).
# 1 caller: ProcessNodeString_82A128B0. The node class isn't resolvable, so the name
# states the mechanism: allocate a 28-byte node and initialize it from the args + a
# source object.
[entrypoint.functions."0x82A12B90"]
name = "AllocateNode28_82A12B90"

# A 20-byte object constructor: set the vtable (obj->0) and allocate + zero a 20-byte
# payload.

# Arg: obj (r3). Sets obj->0 to a fixed global (~0x82C3BBC0, plausibly a vtable/
# type-object pointer). Then allocates 20 bytes via Allocate_OrDbgBreak_8221F388(20) and
# zeroes the block (writes 0 to each 4-byte slot at alloc+0/+4/+8/... -- with null and
# wraparound guards on the pointer arithmetic). Returns void. 21 callers (sub_83215B98,
# sub_832159E0, sub_83215D98, sub_83215768, ...), all in the 0x83215 constructor cluster. 
# The object class and the vtable's meaning aren't resolvable, so the name states the
# mechanism: construct a 20-byte object (install the vtable, allocate + zero the 
# payload).
[entrypoint.functions."0x83215640"]
name = "Construct20Byte_83215640"

# The DESTRUCTOR counterpart of Construct20Byte_83215640 (same vtable ~0x82C3BBC0):
# destroy the tree and free the node.

# Arg: obj (r3). Sets obj->0 to the fixed global (~0x82C3BBC0, the same vtable the
# constructor installs). Then destroys the object's tree: DestroyTreeNodes_82859340(
# obj->8, obj->8->4) (recursively destroy the tree rooted at obj->8), performs cleanup
# (obj->12 = 0 and some self-reference rewiring of the obj->8 node), then calls
# RemoveAndFreeNode_826A4978(obj+4) to detach and free the node. Returns void. 31
# callers (sub_83215A70, sub_82A146C0, sub_82A14720, sub_83215D08, sub_82A14600, ...). 
# The object class isn't resolvable, so the name states the role: the destructor for
# the 20-byte tree-owning object -- install the vtable, destroy the tree, remove +
# free the node.
[entrypoint.functions."0x832156E0"]
name = "Destruct20Byte_832156E0"

# A refcounted value setter that updates a tree node's value: release the old value,
# store the new one, and increment the new value's refcount.

# Args: obj (r3, an object; the tree is at obj+12), value wrapper (r4, ->0 = a source,
# ->4 = the new value). First makes a vcall on *r4->0 (vtable slot 1 / offset 4). Then
# finds the target node in the tree via Tree_FindNode_82A120B0(obj+12, ...). If the node's
# ->4 field differs from the new value (r4->4): release the old value via
# Release_RefCounted_829FF648, set node->0 = r4->0 and node->4 = r4->4, and (if the new
# value is non-null) increment its refcount (value->0 += 1). So it's a COM-style
# refcounted set on a tree node: drop the old reference, take a new one. 3 callers
# (sub_83218630, sub_83219588, sub_83218C10), all in the 0x83218 cluster. The node/tree
# class isn't resolvable, so the name states the mechanism: update a tree node's
# refcounted value (release old, store new, inc refcount).
[entrypoint.functions."0x83218598"]
name = "UpdateTreeNodeValue_83218598"

# The in-place buffer-range update core for the small-buffer object (0x822F) -- grow if
# needed, clamp the length, shift the buffer, resize, and null-terminate.

# Args: obj (r3, a small-buffer object: obj->4 = inline buffer / external pointer,
# obj->20 = current length, obj->24 = size field used for the indirection), pos (r4), len
# (r5). If obj->20 < pos, grow via sub_82CD12C8 (the grow helper). Clamp len to
# (obj->20 - pos). If len == 0, do nothing. Otherwise resolve the buffer base via the
# small-buffer indirection (obj->24 >= 16 ? *obj->4 : &obj->4), then shift the buffer via
# CheckedCopy_82CA3808, decrease obj->20 by len (obj->20 -= len), and null-terminate at
# the new end. The observable effect (shift + shrink + null-terminate) is consistent with
# an in-place range erase / truncate. 3 callers (sub_82BED0C8, ProcessObject_82C669C0,
# RangeUpdate_8218EA38) -- it is the in-place core behind WriteRange_AtPos / RangeUpdate.
# The exact semantics (erase vs truncate vs replace) aren't fully resolvable, so the name
# states the mechanism: update the buffer in place over a range.
[entrypoint.functions."0x822F1F60"]
name = "UpdateBufferRange_822F1F60"

# A byte-append to a growable buffer (SDK region): decrement the remaining capacity,
# write the byte at the current position, and advance.

# Args: byte (r3), buffer obj (r4, ->0 = current write pointer, ->4 = remaining capacity,
# ->8 = a secondary pointer, ->12 = a flags word with a bit-5 flag), plus r5. Checks a
# flag bit (bit 5 of obj->12) that selects an alternate path (obj->8). In the main path it
# decrements obj->4 (remaining capacity); if capacity remains (obj->4 >= 0), writes the
# byte to *obj->0 and advances the write pointer (obj->0 += 1). Returns void. So it's a
# push/append of a single byte into the buffer with a capacity guard. 3 callers (sub_
# 8223F990, sub_82CAE4A8, sub_82CAE410 -- the last two are sibling SDK buffer functions). 
# The exact buffer class isn't resolvable, so the name states the mechanism: append a
# byte to the buffer (decrement capacity, store, advance).
[entrypoint.functions."0x82CAE378"]
name = "Buffer_PushByte_82CAE378"

# A build-object-from-array: derive a size from an array range, construct the object,
# and (if the range is non-empty) copy the array data in.

# Args: obj (r3, the object to build), arr (r4, a range: arr->4 = start, arr->8 = end). 
# Computes the element count (arr->8 - arr->4) >> 2 (i.e. the range's byte count / 4 --
# a count of 4-byte elements; 0 if arr->4 == 0). Calls Construct_FromValue_82B4CB58(obj,
# count) to construct obj with that size. If the construction reports a non-zero low byte
# (obj was built), validates the range (arr->4 <= arr->8, else `trap 22`) and copies the
# array data into obj via CheckedCopy_82CA3808. So it's a "populate obj from the arr
# range": derive the count, construct, then copy the bytes in. 5 callers (sub_82B4B9A8,
# sub_82B4C1C0, sub_82B4C7A0, sub_82B4B2B8, sub_82B4BF70), all in the 0x82B4B data-
# processing cluster. The object/array class isn't resolvable, so the name states the
# mechanism: construct the object from an array range (derive count, construct, copy).
[entrypoint.functions."0x82B4BFD8"]
name = "ConstructFromArray_82B4BFD8"

# A 28-byte-stride array push (sibling of Array_Push12_82C66E00): if there is room,
# initialize a new 28-byte element at the array's current position and advance.

# Args: obj (r3, the array: obj->4 = start, obj->8 = current, obj->12 = end, 28-byte
# stride). Computes the current element count (obj->8 - obj->4)/28 and the capacity
# (obj->12 - obj->4)/28. If there is no room (count >= capacity), return. Otherwise, at
# obj->8, initialize a new 28-byte element (zeroes some fields, sets a few constants --
# e.g. element->20 = 0) and sets up the element's data via RangeUpdate_8218EA38 and
# sub_82C643F0, then advances obj->8 by 28. Returns void. 2 callers (sub_82C83378,
# sub_82C67878). The element class (a 28-byte struct) isn't resolvable, so the name
# states the mechanism: push a 28-byte element into the array (room check, init, advance).
[entrypoint.functions."0x82C644D0"]
name = "Array_Push28_82C644D0"

# A ctype predicate: look up a character in the u16 ctype table and return a specific
# bit (bit 2 / 0x4) -- a member of the 0x82CA6AB0-0x82CA6BB0 ctype cluster.

# Arg: code (r3, a character/code). Loads the table base from a global (~0x82CA70C8),
# then loads the u16 entry table[code] (lhzx at base + code*2), and returns bit 2 of it
# (r3 = entry & 0x4). So it's a "is this char [property]" test where the property is
# bit 2 of the ctype table. Sibling sub_82CA6AF0 returns a different bit (the cluster
# has one function per ctype bit, e.g. bit 0 = _UPPER). 48 callers (sub_82A256D0,
# sub_82A17518, sub_82F6DF40, ...), each loading a byte from a string and testing it. The
# exact property bit 2 denotes isn't resolvable without the table definition, so the name
# states the mechanism: a ctype predicate returning bit 2 of the table entry.
[entrypoint.functions."0x82CA6B10"]
name = "Ctype_Pred_Bit2_82CA6B10"

# A self-contained tree find (12 callers, no external callees -- all processing inline)
# with the same node layout and walk as Tree_FindNode_82A120B0 and Tree_Insert_82A00620
# (->8 right, ->0 parent, ->12 key, ->25 state byte).

# Args: (r3), obj (r4, ->4 = first node), key wrapper (r5, *r5 = the key). Walks the tree
# -- if node->12 < key step right (node->8), else step up to the parent (*node) -- until a
# node with a nonzero ->25 state byte (the target). Then it processes the located node
# entirely inline (no external sub-calls): compares it to the key and rewrites the
# surrounding node pointers. Returns the located/adjusted node. 12 callers (sub_828D9110,
# sub_828D9FE8, sub_828DA098, sub_823385D0, sub_83234418, ...). The node class and the
# exact find-vs-rebalance semantics aren't resolvable, so the name states the mechanism:
# a self-contained tree find (walk to the insertion point, process inline) -- a sibling
# of Tree_FindNode_82A120B0.
[entrypoint.functions."0x82A12230"]
name = "Tree_FindNode2_82A12230"

# A chain-root finder: validate, then walk up a chain (via node->0) to the topmost node
# (the one whose byte-17 flag is set) and store it in obj->4.

# Arg: obj (r3, obj->0 = a base, obj->4 = a starting node). Validates (obj->0 != 0, else
# `trap 22`; the head node's byte-17 == 0, else `trap 22`). Then walks up the chain via
# node->0 (the parent link) until it reaches a node whose byte-17 flag is nonzero (the
# root/top of the chain), and stores that node in obj->4. There are two entry paths
# (depending on an intermediate node->17 check), but both converge on the same walk-up. 
# Returns void. 62 callers (sub_82B6A088, sub_83233B40, sub_825D3100, sub_82C7DC58, ...). 
# The node/chain class and the exact meaning of the byte-17 flag aren't resolvable, so
# the name states the mechanism: walk up the chain to find the root node and store it.
[entrypoint.functions."0x82A596F0"]
name = "FindChainRoot_82A596F0"

# A refcounted copy-assignment (operator=) -- a second variant, a sibling of
# CopyAssign_RefCounted_821F0108 (the primary, 2524 callers). This one's refcount lives
# at value->12 and is incremented atomically under the global lock.

# Args: dst (r3, dst->0 = the stored value), src (r4, src->0 = the stored value). If
# dst == src (self-assign), do nothing. Otherwise: if dst->0 != 0, release it via
# Release_RefCounted_821C67D8. Then if src->0 != 0: set dst->0 = src->0 and atomically
# increment src->0's refcount at (src->0 + 12) (lwarx/stwcx under REX_ENTER_GLOBAL_LOCK). 
# So it's a COM-style refcounted operator=: drop the old reference, take a new one. 593
# callers (sub_82B46E78, sub_8230DFF0, sub_825A20A8, ...). The wrapper class isn't
# resolvable, so the name states the mechanism: a refcounted copy-assign (release old,
# copy, inc refcount) -- the "2" distinguishes it from CopyAssign_RefCounted_821F0108.
[entrypoint.functions."0x82265160"]
name = "CopyAssign_RefCounted2_82265160"

# A refcounted object DESTRUCTOR (one of the hottest functions in the game, 5636
# callers): release the object's value and atomically decrement a global live-object
# counter.

# Arg: obj (r3). Releases obj->0 via Release_RefCounted_821C67D8 (drops the reference to
# the stored value). Then atomically decrements a global counter at a fixed address
# (~0x82C489E0) via lwarx/stwcx under REX_ENTER_GLOBAL_LOCK -- a global live-object /
# allocation count. Returns void. 5636 callers (sub_832A62A0, sub_82492428, 
# sub_82663CA0, ...). The object class and the exact meaning of the global counter aren't
# resolvable, so the name states the mechanism: destruct a refcounted object (release its
# value, decrement the global count).
[entrypoint.functions."0x82214F08"]
name = "DestructRefCounted_82214F08"

# A single-character tolower (ASCII): lowercase a u16 char if it is A-Z.

# Arg: char (r3, a u16 code). If the char == 0xFFFF, return it unchanged. If it is in the
# range A-Z (65-90), add 32 (A-Z -> a-z) and return. Otherwise return the char unchanged.
# So it's a basic ASCII tolower for one character (no table lookup). A simpler sibling of
# ToLower_SdkRuntime_82CA4288 (which uses the ctype table). 10 callers (sub_82F70138,
# sub_82F701F8, sub_8238E1E8, sub_82F705E8, ...), mostly in the 0x82F7 region. Named
# ToLower_Ascii to distinguish the table-less single-char version from the SDK table-
# based one.
[entrypoint.functions."0x8217A980"]
name = "ToLower_Ascii_8217A980"

# A hash-table lookup (linear probing / open addressing): find a key in a flat hash
# table.

# Args: table (r3, ->0 = the bucket array base, ->4 = bucket count), key (r4). Computes
# the starting bucket as (key & (count - 1)) * 8 (8-byte slots), then linearly probes
# (wrapping around the table) comparing each stored entry to the key; stops on a match
# (entry == key) or an empty slot (entry == 0). Returns the found slot/index (or a
# not-found marker). So it's a find-in-hash-table with open addressing. 1 caller:
# sub_829FAC30 (a sibling, plausibly the insert / get-or-create counterpart). The table
# key type isn't resolvable, so the name states the mechanism: a linear-probing hash
# table lookup.
[entrypoint.functions."0x829FAD30"]
name = "HashTable_Find_829FAD30"

# A RECURSIVE structure processor: walk a linked structure of nodes (node->4 = a type
# byte, node+84 = a sub-node), looping over a count and recursing on sub-nodes.

# Args: obj (r3, ->16 = a context with a ->20 flag byte), node (r4, the current node),
# count (r5). Reads the context flag (obj->16->20, xored with 3). Loops over `count`
# elements: for each, if the node's type byte (node->4) == 8, recurse on the sub-node
# (sub_82BCB610(obj, node+84, -3)) -- processing nested structure; otherwise handle it
# inline (sub_82BCB480). Returns void. 119 lines. 2 external callers (sub_82BC7D80,
# StateDispatch_82BCBB40 -- a state dispatch) plus its own recursion. The node/structure
# class and the meaning of the type byte (8 = has-sub-node) aren't resolvable, so the
# name states the mechanism: a recursive processor over a linked structure of nodes.
[entrypoint.functions."0x82BCB610"]
name = "ProcessStructure_Recursive_82BCB610"

# A trivial field getter: return obj->4 (a 2-insn function: lwz r3, 4(r3); blr).

# Arg: obj (r3). Returns the u32 at obj->4. 23 callers (sub_82C80EB8, sub_824D4190,
# sub_82397348, sub_82C81578, ...). It's a plain accessor for the object's field at
# offset 4; the field's meaning needs the object class. Named for what it does: get the
# field at offset 4.
[entrypoint.functions."0x82A46DE8"]
name = "GetField4_82A46DE8"

# A vtable-slot-3 call on the sub-object obj->0 (offset 12 / slot 3) with a flag of 4
# (r5 = 4) -- a member of the recurring slot-3 interface, a sibling of
# VcallSlot3_ReturnBit_82372268 (which uses r5 = 1).

# Args: obj (r3, ->0 = the sub-object, ->4 = a gate byte, ->5 = a flag byte), value
# (r4). If obj->4 == 0, take an alternate path. Otherwise a bit test on obj->5 (via
# clz/rotate/xor) selects which stack slot holds the value pointer (a 2-element struct
# {value, 0}), then vcall `(*obj->0)->vtable[3](obj->0, &{value, 0}, 4)` (vtable slot at
# offset 12, with the 3rd arg = 4 instead of the sibling's 1). 2 callers (sub_82372108,
# sub_823715E8). The slot-3 method isn't resolvable without the vtable, so the name
# states the mechanism: a slot-3 vcall on the sub-object with a flag of 4.
[entrypoint.functions."0x82372070"]
name = "VcallSlot3_Flag4_82372070"

# A constructor/copy for a struct holding three floats plus an optional 40-byte
# payload: copy the three floats and (if the source has a payload) allocate + copy a new
# one.

# Args: dst (r3, ->0 = a payload pointer, ->4/->8/->12 = three floats), src (r4, same
# layout). Sets dst->0 = 0. Copies the three floats (src->4/8/12 -> dst->4/8/12). If
# src->0 (the payload pointer) is non-null: allocate 40 bytes via
# Allocate_OrDbgBreak_8221F388(40) and copy the source payload into it (dst->0 = the new
# payload). Returns void. 179 lines. 4 callers (sub_82A0BAA0, sub_82A0B5C8, sub_82A0B300
# -- all in the 0x82A0B cluster -- plus sub_8321F968). The struct's purpose (the three
# floats -- plausibly a vector/position -- and the 40-byte payload) aren't resolvable, so
# the name states the mechanism: construct the struct from a source (copy 3 floats, plus
# an optional 40-byte payload).
[entrypoint.functions."0x82A0C0A0"]
name = "Construct3FloatPayload_82A0C0A0"

# A vtable-slot-5 call wrapper: deref obj, load vtable offset 20 (slot 5), and call it
# with obj as the sole argument, returning the result.

# Arg: obj (r3, whose ->0 is a vtable). Loads *(obj->0) = the vtable, then vtable[20]
# (slot 5, 20/4), and calls it with obj (bctr). Returns the callee's result. 0 direct
# callers, so it is most likely a VIRTUAL method (a wrapper that forwards to the
# object's own vtable slot 5 -- a delegating thunk). The method (slot 5) isn't
# resolvable without the vtable, so the name states the mechanism: call the object's
# vtable slot 5.
[entrypoint.functions."0x82C1DC40"]
name = "VcallSlot5_82C1DC40"

# A string comparator (3-way): compare two string-wrapper objects (their ->0 holds the
# string pointer) using the signed string compare.

# Args: (r3), a (r4, a->0 = a pointer to a's string), b (r5, b->0 = a pointer to b's
# string). If a->0 == b->0, return 0 (equal). If a->0 == 0, return 1 (a is null). If
# b->0 == 0, return 0 (b is null). Otherwise both are non-null: StrCmp_Signed_8226D7A8
# (*a->0, *b->0) and return the sign bit of the result (negative/zero/positive -- a
# 3-way ordering). 42 callers (sub_825D8308, sub_822B84F0, sub_825D7718, ...). A
# string-ordering predicate for the string-wrapper class. Named CompareStrings to state
# the mechanism: compare two (wrapper) strings and return the ordering.
[entrypoint.functions."0x8228B5A0"]
name = "CompareStrings_8228B5A0"

# A doubly-linked-list splice: reorder / remove a node in a list by rewiring the
# ->0 / ->4 / ->8 links.

# Args: obj (r3, ->4 = a list context), node (r4, the node to splice; ->0 prev, ->4
# next, ->8 a link). Reads node->8's target, checks a ->25 flag on it, and rewires the
# node's prev/next pointers (and the neighboring nodes' links) to splice the node into
# or out of the list -- a classic doubly-linked-list manipulation. Returns void. 67
# lines, 0 external callees (all inline). 30 callers (sub_8234DB40, sub_82366668,
# sub_8278C580, ..., ProcessNodeString_82A128B0, ...). The list element class isn't
# resolvable, so the name states the mechanism: splice a node in a doubly-linked list.
[entrypoint.functions."0x827C6448"]
name = "List_SpliceNode_827C6448"

# A vtable-slot-8 call wrapper: deref obj, load vtable offset 32 (slot 8), and call it
# with obj as the sole argument, returning the result.

# Arg: obj (r3, whose ->0 is a vtable). Loads *(obj->0) = the vtable, then vtable[32]
# (slot 8, 32/4), and calls it with obj (bctr). Returns the callee's result. A sibling
# of VcallSlot5_82C1DC40 (same wrapper shape, different slot). 1 caller:
# sub_82C4A410. The method (slot 8) isn't resolvable without the vtable, so the name
# states the mechanism: call the object's vtable slot 8.
[entrypoint.functions."0x82C1DC70"]
name = "VcallSlot8_82C1DC70"

# A vtable-slot-1 call on an embedded sub-object: deref obj->4, treat (obj->4 + 12) as
# `this`, and call its vtable slot 1.

# Arg: obj (r3, ->4 = a container). Sets this = obj->4 + 12 (an embedded object at
# offset 12 within the container). Loads the vtable at (obj->4 + 12)->0, then vtable[4]
# (slot 1), and calls it with this (bctr). Returns the callee's result. 0 direct
# callers, so it is most likely a VIRTUAL method (a delegating thunk on the embedded
# sub-object). The method (slot 1) and the container class aren't resolvable, so the
# name states the mechanism: call slot 1 on the sub-object embedded at obj->4 + 12.
[entrypoint.functions."0x82C14D40"]
name = "VcallSlot1_SubObj12_82C14D40"

# A binary-search-tree walk (search/insert point): walk the tree from the root, comparing
# the key at each node, following the child pointer indicated by the comparison.

# Args: obj (r3, ->0 = the root node, ->4 = the key), key (r4), dst (r5, a 16-byte
# scratch/result). Sets dst->0 = dst->4 = obj->4 (the key). Starting from the root
# (obj->0), loads each node and compares the key to the node's value via sub_82C46A30
# (the comparator); follows the appropriate child pointer (node->0 or node->8) based on
# the comparison result, until it reaches the target / null. Returns the found node
# (or the insertion point). 104 lines. 1 caller: sub_82C46CF8 (a sibling, also in the
# TODO -- plausibly the get-or-create wrapper). The tree key type and the comparator
# (sub_82C46A30) aren't resolvable, so the name states the mechanism: a BST search walk.
[entrypoint.functions."0x82C46AF0"]
name = "Tree_Search_82C46AF0"

# A tree get-or-create (insert): search for an entry in the tree and, if absent, create
# and insert a new node at a computed offset.

# Args: obj (r3, the tree container), src (r4, a source / key wrapper, ->0 = a vtable
# object), offset (r5). Makes a vcall on src (vtable slot 4, offset 16), computes the
# destination as (obj + offset), then calls Tree_Search_82C46AF0 (sub_82C46AF0) to locate
# the entry; afterwards it initializes the resulting node (sets flags at ->12, ->16, and
# links it) -- so it's a get-or-create: find the key in the tree, creating a node if not
# present. Returns the node. 355 lines. 18 callers (sub_82C13F50, sub_82C50270,
# sub_82C1E2E0, ...). The tree key type and node layout aren't resolvable, so the name
# states the mechanism: search the tree and create a node if the entry is missing.
[entrypoint.functions."0x82C46CF8"]
name = "Tree_GetOrCreate_82C46CF8"

# A large (449-line) self-contained tree walk: traverse a BST, rewiring node links and
# setting a boolean flag (node->12) -- part of the 0x82C46 tree subsystem.

# Args: obj (r3, ->4 = a value), node (r4, a tree node with ->0 / ->4 / ->8 child links
# and a ->12 flag). Two entry paths (depending on whether node->12 is set). Walks the
# tree (a loop at loc_82C46FF4) comparing the running pointer to the target, following
# the child links (node->8 / ->4 / ->0), and rewiring the links as it goes. At the end it
# sets node->12 to 0 or 1 (a boolean flag). Returns void. 449 lines, 0 external callees
# (the whole tree manipulation is inline). 1 caller: sub_82C47318 (a sibling, also in the
# TODO). A core operation of the 0x82C46 BST subsystem (siblings: Tree_Search_82C46AF0,
# Tree_GetOrCreate_82C46CF8). The exact operation (walk + rewire + set a flag) is the
# resolvable mechanism; the node/key class isn't.
[entrypoint.functions."0x82C46FD8"]
name = "Tree_Walk_82C46FD8"

# A tree node-locate-and-process operation: find the target node in the tree (fast path
# or a search) and then process/rewire it -- part of the 0x82C46 tree subsystem.

# Args: obj (r3, ->4 = a value/key), node (r4, a starting node). If node->0 or node->4
# matches obj->4, use that node directly; otherwise call sub_82C46BA0 (a tree search) to
# locate the node. Then process the located node: compare it to the value, set up the
# child pointer (node->4), and call Tree_Walk_82C46FD8 (sub_82C46FD8) to rewire the
# surrounding tree. Returns void. 195 lines. 1 caller: sub_82C47470 (a sibling, also in
# the TODO). Siblings in the 0x82C46 BST subsystem: Tree_Search_82C46AF0,
# Tree_GetOrCreate_82C46CF8, Tree_Walk_82C46FD8. The node/key class and whether the
# rewire is specifically a removal aren't fully resolvable, so the name states the
# mechanism: locate a tree node and process (rewire) it.
[entrypoint.functions."0x82C47318"]
name = "Tree_ProcessNode_82C47318"

# A thin tree-process wrapper: make a vcall on the source, then dispatch to
# Tree_ProcessNode_82C47318 -- the 17-caller entry point of the 0x82C46 tree-process path.

# Args: obj (r3, the tree container), src (r4, a source wrapper, ->0 = a vtable object),
# offset (r5). Makes a vcall on src (vtable slot 4, offset 16), computes (src + offset),
# then calls Tree_ProcessNode_82C47318 (sub_82C47318) with obj and (src + offset). Returns
# void. 17 callers (sub_82C55F10, sub_82C103E8, sub_82C0CE28, ...). A wrapper in the
# 0x82C46 BST subsystem that prepares the call (vcall) and then processes the tree node.
# Named for what it does: a wrapper that dispatches to the tree-process path.
[entrypoint.functions."0x82C47470"]
name = "Tree_Process_Wrapper_82C47470"

# A reset/clear: zero four fields of an object (a 5-insn function).

# Arg: obj (r3). Sets obj->0, obj->4, obj->8, and obj->16 all to 0 (note it leaves
# offset 12 alone). Returns void. 18 callers, all in the 0x82C0-0x82C5 tree region
# (sub_82C0AF00, sub_82C0CC98, sub_82C0E5D8, sub_82C50270, sub_82C13F50, ...), so this
# clears a tree-node / tree-object back to its empty state. The object class isn't
# resolvable, so the name states the mechanism: reset the (tree) object's fields to zero.
[entrypoint.functions."0x82C474C8"]
name = "ResetTreeObj_82C474C8"

# A vtable-slot-10 call wrapper: deref obj, load vtable offset 40 (slot 10), and call it
# with obj as the sole argument, returning the result.

# Arg: obj (r3, whose ->0 is a vtable). Loads *(obj->0) = the vtable, then vtable[40]
# (slot 10, 40/4), and calls it with obj (bctr). Returns the callee's result. A sibling
# of VcallSlot12_82C010D0 (0x30 bytes away, slot 12) in the 0x82C010 vcall-wrapper family.
# 3 callers (sub_82C14EA0 -- also in the TODO -- plus sub_82EE42B8, sub_82EE3F70). The
# method (slot 10) isn't resolvable without the vtable, so the name states the mechanism:
# call the object's vtable slot 10.
[entrypoint.functions."0x82C010A0"]
name = "VcallSlot10_82C010A0"

# A vtable-slot-3 call on the sub-object at obj->16 (offset 12 / slot 3) with a flag of
# 2 (r5 = 2), returning a boolean derived from the result's sign -- a member of the
# recurring slot-3 interface.

# Args: obj (r3, ->16 = the sub-object), value (r4). Builds a 2-element struct {value, 0}
# on the stack, then vcall `(*obj->16)->vtable[3](obj->16, &{value, 0}, 2)` (vtable slot
# at offset 12, with the 3rd arg = 2). Takes the result, does a clz-based sign test, and
# returns (sign ^ 1) -- a 0/1 boolean. 0 direct callers, so it is most likely a VIRTUAL
# method. A slot-3 sibling (compare VcallSlot3_Bool_82C01E78 / _Bool_82C01E20,
# VcallSlot3_Flag4_82372070). The slot-3 method isn't resolvable without the vtable, so
# the name states the mechanism: a slot-3 vcall on obj->16 that returns a boolean.
[entrypoint.functions."0x82C01DC8"]
name = "VcallSlot3_Bool_82C01DC8"

# A multi-step vcall chain on obj: slot 6, then (if the result is nonzero) slot 10 via
# VcallSlot10_82C010A0, then (if that result is nonzero) slot 7 -- each step gated on the
# previous result's byte 3.

# Args: obj (r3, ->0 = a vtable), (r4), value (r5). Makes a vcall on obj (vtable slot 6,
# offset 24); if the result's top byte is nonzero, calls VcallSlot10_82C010A0 (sub_82C010A0,
# slot 10) with obj and value; if that result's top byte is nonzero, makes another vcall
# on obj (vtable slot 7, offset 28). Returns void. 86 lines. 1 caller: sub_82C15100 (a
# sibling, also in the TODO). A gated vcall chain (slots 6, 10, 7); a sibling of
# VcallChain_6_12_7_82C14E08 and VcallChain_AllSuccess. The slots aren't resolvable
# without the vtable, so the name states the mechanism: a gated slot-6 -> 10 -> 7 vcall
# chain.
[entrypoint.functions."0x82C14EA0"]
name = "VcallChain_6_10_7_82C14EA0"

# A complex vcall dispatcher (271 lines): make an initial vcall on the source, then
# orchestrate a sequence of vcall chains (VcallTriple_AllSuccess, VcallChain_AllSuccess,
# VcallChain_6_12_7, VcallChain_6_10_7, ...) based on each result.

# Args: obj (r3, a tree/object container), src (r4, a wrapper, ->0 = a vtable object),
# value (r5). Makes a vcall on src (vtable slot 5, offset 20); depending on the result it
# branches into a sequence of higher-level operations: VcallTriple_AllSuccess_82C011E8,
# VcallChain_AllSuccess_82C14F38, VcallChain_6_12_7_82C14E08, VcallChain_6_10_7_82C14EA0,
# plus DestructRefCounted_82214F08, sub_821F7798, sub_8228A4C0, and sub_82C14D70. So it's
# the top-level dispatcher that drives the object's virtual operation sequence. 271 lines.
# 1 caller: sub_82C168E0 (a sibling, also in the TODO). The object class and the exact
# semantics of each vcall chain aren't resolvable, so the name states the mechanism: a
# dispatcher that runs a gated sequence of vcall chains.
[entrypoint.functions."0x82C15100"]
name = "Dispatch_VcallSequence_82C15100"

# A multi-step vcall chain on obj: slot 6, then (if the result is nonzero)
# Dispatch_VcallSequence_82C15100, then (if that result is nonzero) slot 7 -- each step
# gated on the previous result's byte 3. Structurally like VcallChain_6_10_7_82C14EA0, but
# the middle step is the full dispatcher.

# Args: obj (r3, ->0 = a vtable), (r4), value (r5). Makes a vcall on obj (vtable slot 6,
# offset 24); if the result's top byte is nonzero, calls Dispatch_VcallSequence_82C15100
# (sub_82C15100) with value and obj; if that result's top byte is nonzero, makes another
# vcall on obj (vtable slot 7, offset 28). Returns void. 86 lines. 1 caller:
# sub_82C17E48 (a higher-level function). A gated vcall chain (slot 6 -> dispatch -> slot
# 7); a sibling of VcallChain_6_10_7_82C14EA0. The slots aren't resolvable without the
# vtable, so the name states the mechanism: a gated slot-6 -> dispatcher -> slot-7 vcall
# chain.
[entrypoint.functions."0x82C168E0"]
name = "VcallChain_6_Dispatch_7_82C168E0"

# A get-or-create for a 24-byte element: search for the element (via
# ProcessElements_Vcall4_82C46C10) and, if absent, allocate + initialize a new 24-byte
# node.

# Args: obj (r3, ->8 = a container), value (r4). Calls ProcessElements_Vcall4_82C46C10
# (sub_82C46C10) to search the container for the element; if it is found (result nonzero),
# returns 0 (the existing element). Otherwise it allocates 24 bytes via
# Allocate_OrDbgBreak_8221F388(24), resets it (ResetTreeObj_82C474C8), and inserts it
# (Tree_GetOrCreate_82C46CF8). So it's a get-or-create: find the 24-byte element, creating
# one if missing. 95 lines. 1 caller: sub_82C174D8 (a sibling, also in the TODO). Part of
# the 0x82C17/0x82C46 tree+element subsystem. The element class isn't resolvable, so the
# name states the mechanism: get-or-create a 24-byte element.
[entrypoint.functions."0x82C173C0"]
name = "GetOrCreate24Element_82C173C0"

# A process-element operation: search for the element (via ProcessElements_Vcall4_82C46C10)
# and then process it (via Tree_Process_Wrapper_82C47470) -- the "process" sibling of
# GetOrCreate24Element_82C173C0 (same caller, sub_82C174D8).

# Args: obj (r3, ->8 = a container), value (r4). Calls ProcessElements_Vcall4_82C46C10
# (sub_82C46C10) to search the container for the element; then calls
# Tree_Process_Wrapper_82C47470 (sub_82C47470) to process the located element. On some
# paths it frees (Free_Thunk_824FE010) or no-ops (NoOp_Stub_829CE870). 70 lines. 1 caller:
# sub_82C174D8 (a sibling, also in the TODO). Part of the 0x82C17/0x82C46 tree+element
# subsystem. The element class isn't resolvable, so the name states the mechanism: search
# for and process a 24-byte element.
[entrypoint.functions."0x82C17460"]
name = "Process24Element_82C17460"

# A binary-search lookup-and-handle for a 24-byte element: find the index (BinarySearch),
# then either create the element (GetOrCreate24Element) or process it (Process24Element).

# Args: obj (r3, ->4 = a container), key (r4). Calls BinarySearch_82C0BC08 (sub_82C0BC08)
# to find the index of the key in the sorted container (obj->4 + 52); then indexes a
# parallel array to fetch the associated element pointer, and dispatches to
# GetOrCreate24Element_82C173C0 (sub_82C173C0) or Process24Element_82C17460 (sub_82C17460)
# depending on the element's state. Returns the element (or null). 102 lines. 1 caller:
# sub_82C1ACD8. Part of the 0x82C17 tree+element subsystem (siblings: GetOrCreate24Element,
# Process24Element, BinarySearch_82C0BC08). The element class isn't resolvable, so the name
# states the mechanism: look up a 24-byte element by binary search and handle it.
[entrypoint.functions."0x82C174D8"]
name = "Lookup24Element_82C174D8"

# A binary search (4-byte stride over an array of pointers, comparing the dereferenced
# values): find the target and write a result byte.

# Args: obj (r3, ->0 = the array base, ->8 = the count), target (r4, ->0 = the target
# value), result (r5, an output byte). Sets *result = 0. Binary-searches the array
# (4-byte slots; each slot holds a pointer, and the comparison is on *slot): mid =
# (lo + hi) / 2, val = *array[mid]; if val > target, hi = mid - 1; if val == target, the
# result byte is set; else lo = mid + 1. Returns the index (or 0 if empty). 102 lines, 0
# external callees (all inline). 22 callers (sub_82C402B8, sub_82C26518, sub_82C29328,
# ...). A member of the binary-search family (sibling of BinarySearch_12Stride,
# BinarySearch_82C0BC08) -- the 4-byte pointer-array variant.
[entrypoint.functions."0x82C40EC0"]
name = "BinarySearch_4Stride_82C40EC0"

# A 5-way switch dispatch (a state/command dispatcher in the 0x82BCB region): set a flag
# bit, index a function-pointer table by (obj->36->4 - 5), and dispatch to one of five
# handlers.

# Arg: obj (r3, ->36 = a sub-object). Sets bit 2 of (obj->36)->5 (a flag). Computes an
# index = (obj->36)->4 - 5; if the index > 4, take the default path. Otherwise it loads a
# function pointer from a global table (indexed by the index) and calls it (bctr), then
# switches on the index (cases 0-4, each calling one of sub_82BCAA58 / sub_82BCAC98 /
# sub_82BCAE28 / sub_82BCAF40). So it's a state/command dispatcher keyed on the
# sub-object's byte-4 state. 271 lines. 2 callers (StateDispatch_82BCBB40 -- a state
# dispatcher in the same region -- plus sub_82BCB2A0). The states/commands aren't
# resolvable, so the name states the mechanism: a 5-way state dispatch (sibling of
# StateDispatch_82BCBB40 / StateDispatch_82BCA7D8).
[entrypoint.functions."0x82BCB0A8"]
name = "StateDispatch_82BCB0A8"

# A polynomial rolling hash of a C-string: a simple string hash/checksum.

# Arg: buffer (r3, a byte string). If buffer[0] == 0 (empty), return 0. Otherwise compute
# a rolling hash: crc = 0; for each byte (up to 100, or until a null byte): crc = crc * 
# 0x0001003F (65635) + signed_byte. Returns the hash. So it's a polynomial (base-65635)
# rolling hash of a null-terminated string, capped at 100 chars. 282 callers (sub_8328CC90,
# sub_8328E5F0, sub_8328D780, sub_823F75A0, ...), many in the 0x8328 region -- a high-value
# string-hash primitive. Named HashCstring to state the mechanism: hash a C-string with a
# polynomial roll.
[entrypoint.functions."0x82188CF0"]
name = "HashCstring_82188CF0"

# A trivial two-level getter: return (obj->0)->4, or 0 if obj->0 is null (a 5-insn
# function).

# Arg: obj (r3, ->0 = a pointer). If obj->0 == 0, return 0. Otherwise return *(obj->0 + 4)
# (the u32 at field 4 of the pointed-to object). 20 callers (sub_821EE858, sub_82A1A360,
# sub_82B3A9A8, ...). A plain accessor for a nested object's field at offset 4; the field's
# meaning needs the object class. Named for what it does: get field 4 of the object pointed
# to by obj->0.
[entrypoint.functions."0x821EEA78"]
name = "GetSubObjField4_821EEA78"

# A source-evaluation operation: conditionally run two slot-3 vcalls on the source object
# (updating its byte-4 flag) and copy a resulting value into the destination.

# Args: dst (r3), src (r4, ->4 = a byte flag). If src->4 != 0, calls
# SubObject_VcallSlot3_GatedStore32_82C842C8 (sub_82C842C8) with src and a stack slot, and
# stores the result back to src->4. Copies a stack value to dst->0. If src->4 is still
# nonzero, calls VcallSlot3_ReturnBit_82372268 (sub_82372268) and stores the result to
# src->4. So it evaluates the source (two slot-3 vcalls, gated on the flag) and writes a
# value to the destination. Returns void. 103 lines. 1 caller: sub_82371820. The object
# class and the slot-3 method semantics aren't resolvable, so the name states the
# mechanism: evaluate a source object (via two slot-3 vcalls) and copy out a value.
[entrypoint.functions."0x8236D3C8"]
name = "EvaluateSource_8236D3C8"

# A gated vcall sequence: call VcallTriple_AllSuccess_82C011E8 on the source, and (if the
# result's top byte is nonzero) call it again with obj+4 as the 3rd argument.

# Args: obj (r3), src (r4). Calls VcallTriple_AllSuccess_82C011E8 (sub_82C011E8) with src,
# a global (a 3rd-arg constant ~0x82C0F0C0), and obj; if the result's top byte is nonzero,
# calls VcallTriple_AllSuccess_82C011E8 again with src, a second global, and (obj + 4). So
# it's a gated two-step vcall sequence driving the source object. Returns the final result
# byte. 75 lines. 1 caller: sub_82C16AA8 (a sibling, also in the TODO). The vcall-triple
# slots aren't resolvable, so the name states the mechanism: a gated sequence of two
# VcallTriple_AllSuccess calls.
[entrypoint.functions."0x82C15358"]
name = "VcallTriple_Sequence_82C15358"

# A multi-step vcall chain on obj: slot 6, then (if the result is nonzero)
# VcallTriple_Sequence_82C15358, then (if that result is nonzero) slot 7 -- each step
# gated on the previous result's byte 3. Same shape as VcallChain_6_10_7_82C14EA0 and
# VcallChain_6_Dispatch_7_82C168E0, but the middle step is the vcall-triple sequence.

# Args: obj (r3, ->0 = a vtable), (r4), value (r5). Makes a vcall on obj (vtable slot 6,
# offset 24); if the result's top byte is nonzero, calls VcallTriple_Sequence_82C15358
# (sub_82C15358) with value and obj; if that result's top byte is nonzero, makes another
# vcall on obj (vtable slot 7, offset 28). Returns void. 86 lines. 1 caller:
# sub_82C188A8. A gated vcall chain (slot 6 -> vcall-triple sequence -> slot 7); a sibling
# of the other 6-?-7 chains in the 0x82C15/0x82C16 dispatcher cluster. The slots aren't
# resolvable without the vtable, so the name states the mechanism.
[entrypoint.functions."0x82C16AA8"]
name = "VcallChain_6_Triple_7_82C16AA8"

# A large (408-line) complex vcall dispatcher: make an initial pair of vcalls on the
# source (slots 5 and 0), then orchestrate a long gated sequence of vcall chains
# (VcallTriple_AllSuccess, VcallChain_AllSuccess x2, VcallChain_6_12_7, ...) -- the bigger
# sibling of Dispatch_VcallSequence_82C15100.

# Args: obj (r3, a tree/object container), src (r4, a wrapper, ->0 = a vtable object),
# value (r5). Makes a vcall on src (vtable slot 5, offset 20), then another vcall on src
# (vtable slot 0, offset 0), then VcallTriple_AllSuccess_82C011E8; depending on the results
# it branches through VcallChain_AllSuccess_82C14F38, VcallChain_AllSuccess_82C15068,
# VcallChain_6_12_7_82C14E08, plus DestructRefCounted_82214F08, sub_821F7798, sub_82BFD780,
# and sub_82C14D70. So it's the top-level dispatcher that drives a long virtual-operation
# sequence on the source object. 408 lines. 1 caller: sub_82C1EAF8 (a sibling, also in the
# TODO). The object class and the exact semantics of each vcall chain aren't resolvable, so
# the name states the mechanism: a dispatcher that runs a long gated sequence of vcall
# chains (the "2" distinguishes it from Dispatch_VcallSequence_82C15100).
[entrypoint.functions."0x82C1E360"]
name = "Dispatch_VcallSequence2_82C1E360"

# A lookup via vcall + binary search: obtain a value from a vtable slot-1 call on the
# source, then binary-search for it (BinarySearch_4Stride_82C40EC0).

# Args: (r3), src (r4, ->0 = a vtable object), value (r5). Makes a vcall on src (vtable
# slot 1, offset 4) passing src, a global (~0x82C24C60), and value; if the result is 0,
# returns 0. Otherwise it processes the result (result + 4) and calls
# BinarySearch_4Stride_82C40EC0 (sub_82C40EC0) to look the value up in a sorted array.
# Returns the lookup result. 83 lines. 2 callers (sub_82C1E7F0 -- also in the TODO -- plus
# sub_82C1E878). Part of the 0x82C1E lookup cluster. The object class and the slot-1 method
# aren't resolvable, so the name states the mechanism: obtain a value via a vcall and
# binary-search for it.
[entrypoint.functions."0x82C1E760"]
name = "LookupByVcall_82C1E760"

# A boolean predicate: call LookupByVcall_82C1E760 and return 0/1 based on the result's
# sign (a "does it exist / is it valid" test).

# Args: (r3), src (r4), value (r5). Calls LookupByVcall_82C1E760 (sub_82C1E760) with the
# same arguments, then does a clz-based sign test on the result and returns (sign ^ 1) --
# a 0/1 boolean. So it's a predicate built on the lookup: true (1) when the lookup returns
# a "nonzero-sign" result. 0 direct callers, so it is most likely a VIRTUAL method (or
# currently dead). Part of the 0x82C1E lookup cluster. The lookup semantics aren't fully
# resolvable, so the name states the mechanism: a boolean predicate over the vcall+binary-
# search lookup.
[entrypoint.functions."0x82C1E7F0"]
name = "LookupPredicate_82C1E7F0"

# A multi-step vcall chain on obj: slot 6, then (if the result is nonzero)
# Dispatch_VcallSequence2_82C1E360, then (if that result is nonzero) slot 7 -- each step
# gated on the previous result's byte 3. Same shape as VcallChain_6_Dispatch_7_82C168E0,
# but the middle step is the second (larger) dispatcher.

# Args: obj (r3, ->0 = a vtable), (r4), value (r5). Makes a vcall on obj (vtable slot 6,
# offset 24); if the result's top byte is nonzero, calls Dispatch_VcallSequence2_82C1E360
# (sub_82C1E360) with value and obj; if that result's top byte is nonzero, makes another
# vcall on obj (vtable slot 7, offset 28). Returns void. 86 lines. 1 caller: sub_82C1EBE8.
# A gated vcall chain (slot 6 -> dispatcher2 -> slot 7); a sibling of
# VcallChain_6_Dispatch_7_82C168E0 / VcallChain_6_Triple_7_82C16AA8. The slots aren't
# resolvable without the vtable, so the name states the mechanism.
[entrypoint.functions."0x82C1EAF8"]
name = "VcallChain_6_Dispatch2_7_82C1EAF8"

# A vcall + direct array index: make a vtable-slot-1 call on the source, then index a
# 4-byte-stride array (src->4) at the given index and return the pointed-to element.

# Args: (r3), src (r4, ->0 = a vtable object, ->4 = an array base), index (r5). Makes a
# vcall on src (vtable slot 1, offset 4) passing src and a global (~0x82C24C60) as the 2nd
# arg. Then computes slot = index * 4, loads the element pointer from src->4[slot], and
# returns *element. So it's a "call slot 1, then fetch array[index]" accessor. 50 lines,
# 0 external callees (the vcall + array index are inline). 0 direct callers, so it is most
# likely a VIRTUAL method (or currently dead). Part of the 0x82C1E lookup cluster (a
# sibling of LookupByVcall_82C1E760, but with a direct index instead of a binary search).
# The slot-1 method and array element class aren't resolvable, so the name states the
# mechanism.
[entrypoint.functions."0x82C1EB90"]
name = "VcallAndIndexArray_82C1EB90"

# A circular linked-list destroy: detach the list rooted at obj->4, walk it freeing each
# node (Free_SizeBucketed_Thunk), then clear the container's flag.

# Arg: obj (r3, ->4 = the head of a circular doubly-linked list, ->8 = a count/flag). Detaches
# the list (head->0 = head, head->4 = head, obj->8 = 0). Then walks the circular list (each
# node's ->0 is the next node) freeing every node via Free_SizeBucketed_Thunk_8221BE68 until
# it returns to the head. Returns void. 66 lines. 135 callers (sub_8290F760, sub_82882DB8,
# sub_8266CFD8, ...) -- a high-value list-destructor. The list element class isn't
# resolvable, so the name states the mechanism: destroy (free all nodes of) a circular
# linked list.
[entrypoint.functions."0x82A0F5C0"]
name = "DestroyCircularList_82A0F5C0"

# A refcounted object CONSTRUCTOR (the construct counterpart of DestructRefCounted_82214F08,
# one of the hottest functions in the game, 5200 callers): initialize the object and
# atomically increment the global live-object counter.

# Args: obj (r3, ->0 = the stored value), value (r4). Sets obj->0 = 0. Atomically
# increments the global counter at ~0x82C489E0 (lwarx/stwcx under REX_ENTER_GLOBAL_LOCK) --
# the SAME global live-object count that DestructRefCounted_82214F08 decrements. If value
# (r4) is non-null it does additional setup (Release_RefCounted_821C67D8 on the old value,
# sub_822079D8). So it's the COM-style refcounted operator=/constructor: take a reference
# and bump the global live count. 104 lines. 5200 callers (sub_827CD5F0, sub_8325D140,
# sub_83260CC0, ...). The object class isn't resolvable, so the name states the mechanism:
# construct a refcounted object (init + increment the global count).
[entrypoint.functions."0x8222CF18"]
name = "ConstructRefCounted_8222CF18"

# A chain-root finder (the byte-25-flag variant): validate, then walk up a chain (via
# node->0) to the topmost node (the one whose byte-25 flag is set) and store it in obj->4.
# Structurally identical to FindChainRoot_82A596F0 (which uses byte 17), but uses byte 25
# and validates more aggressively.

# Arg: obj (r3, ->0 = a base, ->4 = a starting node). Validates (obj->0 != 0, else `trap 22`;
# the head node's byte-25 == 0, else `trap 22`). Then walks up the chain via node->0 (the
# parent link) until it reaches a node whose byte-25 flag is nonzero (the root/top), and
# stores that node in obj->4. Returns void. 93 lines, 0 external callees (all inline). 108
# callers (sub_8267D818, sub_825D6FE8, sub_82367898, ...) -- a high-value chain-root lookup.
# A sibling of FindChainRoot_82A596F0 (the byte-17 variant); the "2" distinguishes the two.
# The node/chain class and the byte-25 flag's meaning aren't resolvable.
[entrypoint.functions."0x823D4F20"]
name = "FindChainRoot2_823D4F20"

# A tree find-and-remove: search a BST for the key (walk node->12 vs key, left = node->0,
# right = node->8, leaf flag = node->17), then remove the located node (Tree_RemoveNode).

# Args: obj (r3, ->4 = a container, ->4->4 = the head node), key (r4, *r4 = the key). Walks
# the tree from the head: at each node, if key >= node->12 go left (node->0), else go right
# (node->8), stopping at a leaf (node->17 != 0). Then it calls FindChainRoot_82A596F0 and
# Tree_RemoveNode_826A4A20 (sub_826A4A20) to detach/remove the located node. Returns the
# removed node (or a marker). 153 lines. 3 callers (sub_83218420, sub_83215610, sub_83218350
# -- the 0x83218/0x83215 constructor cluster). Part of the 0x82A14 tree cluster. The key
# type and node class aren't resolvable, so the name states the mechanism: find a node by
# key in the tree and remove it.
[entrypoint.functions."0x82A14AE0"]
name = "Tree_FindAndRemove_82A14AE0"

# A lazy-init singleton getter: check a global flag and, on first call only, initialize a
# global object, then return it.

# No real args (operates on globals). Checks bit 0 of a global flag (~0x82C73F98); if it is
# already set, skip to the return. Otherwise set the flag, initialize the global object at
# ~0x82C78640 (sub_82BA8BB0), and call sub_82CA3700 (a string/format routine) with another
# global (~0x82C38A80). Returns the global object. So it's a "get the singleton, initialize
# on first use" accessor. 72 lines. 94 callers, mostly in the 0x82BA/0x82BB region
# (sub_82BA8800, sub_82BA9938, sub_82BB1398, ...) -- a high-value subsystem getter. The
# object class isn't resolvable, so the name states the mechanism: get the (lazy-init)
# singleton.
[entrypoint.functions."0x82BA8718"]
name = "GetSingleton_82BA8718"

# A range/counter advance: consume a delta by incrementing counters and advancing several
# 12-byte range pointers (offsets 32/44 and 40/44) by the delta, with clamping.

# Args: obj (r3, a multi-range object: ->0/->12 = counters, ->8 = a clamped counter,
# ->32/->40/->44 = a range triple, and (obj + 32) = another range triple), delta (r4). If
# delta <= 0, return (no-op). Otherwise: obj->0 += 1, obj->12 += 1; clamp obj->8 to
# min(obj->8, obj->12); advance the ranges (obj->32 += delta, obj->44 += delta, and the
# (obj + 32) range) with similar clamping. So it's a "consume/advance N" operation over a
# multi-buffer / multi-range object. Returns void. 108 lines, 0 external callees (all
# inline). 2 callers (sub_82BAA4A8, sub_82BA8F18 -- both in the TODO). The object class and
# the exact range semantics aren't resolvable, so the name states the mechanism: advance the
# range pointers by a delta.
[entrypoint.functions."0x82BAA6F0"]
name = "AdvanceRanges_82BAA6F0"

# A value-at-position processor (in the 0x826C value-processing cluster): guard the
# position, process the value via ProcessValue_FlagNonZero, and memset as needed.

# Args: obj (r3, ->20 = a length), pos (r4), (r5). Guards the position (calls
# sub_82CD11D0 -- the high-value guard helper -- if pos is out of range). If pos == 0,
# returns. Otherwise it calls ProcessValue_FlagNonZero_821A9090 (sub_821A9090) with obj and
# (obj->20 + pos); if the result's top byte is nonzero it does further processing (memset via
# MemSet_SdkRuntime_82CA3190, and more). So it's a "process the value at a given position in
# the buffer" operation. 130 lines. 4 callers (ProcessObject_82C669C0 -- a processor in the
# 0x82C66 cluster -- plus sub_82C76100, sub_82C7AE40, sub_826C2250). The buffer class and
# exact value semantics aren't resolvable, so the name states the mechanism.
[entrypoint.functions."0x826C2FF8"]
name = "ProcessValueAtPos_826C2FF8"

# A hash-table lookup (linear probing, 4-byte stride): find a key in a flat open-addressing
# hash table and return the located slot -- a sibling of HashTable_Find_829FAD30 (which
# uses 8-byte stride).

# Args: table (r3, ->0 = the bucket count, ->4 = the array base), key (r4). Computes the
# starting bucket as (key & (count - 1)), then linearly probes (4-byte slots, wrapping
# around) comparing each entry's ->0 (the stored key) to the key; stops on a match or an
# empty slot (entry == 0). Returns the located slot (slot + 4) if found, else 0. 60 lines,
# 0 external callees (all inline). 35 callers (sub_828907F8, sub_823AAC18, sub_829FC6A0 --
# also in the TODO -- ...). The table key type isn't resolvable, so the name states the
# mechanism: a linear-probing hash table lookup (the "2" distinguishes it from
# HashTable_Find_829FAD30).
[entrypoint.functions."0x829FC630"]
name = "HashTable_Find2_829FC630"

# A processing loop: iterate `count` times over a sub-object, calling sub_82BCD8A8 each
# time (updating the counters) and copying data via MemCpy_SdkRuntime.

# Args: obj (r3, ->4 = a sub-object with ->0 = a counter, ->4 = a remaining count), (r4),
# count (r5). Loops `count` times: if sub->0 == 0, calls sub_82BCD8A8 (returns -1 or a
# value) and updates sub->0 += 1, sub->4 -= 1; then processes/copies data (MemCpy_SdkRuntime,
# sub_82BC8870, sub_82BCCB88). So it's a "process `count` elements" loop. 131 lines. 4
# callers, all in the 0x82BDD cluster (sub_82BDD1E0, sub_82BDD518, sub_82BDDA98, sub_82BDD000
# -- some also in the TODO). The element class and the per-element operation aren't
# resolvable, so the name states the mechanism: a loop that processes a count of elements.
[entrypoint.functions."0x82BDCF20"]
name = "ProcessElements_82BDCF20"

# A buffer reassignment: free the old buffer, allocate a new one of the requested size, set
# the length/size fields, and null-terminate -- a "reset the buffer to a new size" operation.

# Args: obj (r3, ->0 = the buffer, ->4 = the length, ->8 = a size field with a flag bit),
# (r4), pos (r5). If obj->0 != 0, free the old buffer (Free_SizeBucketed_Thunk_8221BE68) and
# clear obj->0. Then computes size = (pos + 4) aligned to 4, allocates a new buffer
# (Allocate_OrDbgBreak_8221F388), and sets obj->0 = buffer, obj->4 = pos, obj->8 = (size << 1)
# | (flag bit). Null-terminates at pos (stb). May also copy (MemCpy_Vectorized) or zero
# (MemSet_SdkRuntime). So it's a buffer reassign/resize. 88 lines. 7 callers (sub_822079D8 --
# a callee of ConstructRefCounted_8222CF18 and in the TODO -- plus sub_8222E840, sub_82275368,
# ...). The buffer class isn't resolvable, so the name states the mechanism: reassign the
# buffer to a new size.
[entrypoint.functions."0x822088B0"]
name = "ReassignBuffer_822088B0"

# A state handler (one of the five cases dispatched by StateDispatch_82BCB0A8): conditionally
# dispatch via StateDispatch_82BCA7D8, then loop over a sub-array checking elements.

# Args: obj (r3), state (r4, ->12 = a sub-object with a ->5 flag byte, ->6 and ->7 = gate
# bytes, ->20 = a sub-array). If (state->12->5) & 0x3 != 0, calls StateDispatch_82BCA7D8
# (sub_82BCA7D8). If state->6 != 0 and state->7 != 0, it loops over the sub-array at
# (state + 20) checking each element (element->4 == 4) and processing it. So it's a specific
# state/command handler in the 0x82BCA state cluster. 157 lines. 1 caller:
# StateDispatch_82BCB0A8 (a state dispatcher, also in this batch). The state semantics aren't
# resolvable, so the name states the mechanism: a state handler (one of the dispatcher's
# cases).
[entrypoint.functions."0x82BCAE28"]
name = "StateHandler_82BCAE28"

# A trivial pointer adjustment: return obj + 492 (a 2-insn function: addi r3, r3, 492; blr).

# Arg: obj (r3). Returns (obj + 492) -- a pointer to a sub-field / embedded object at offset
# 492 within the object. 0 direct callers, so it is most likely a VIRTUAL method (a vtable
# accessor) or currently dead. It's a plain "get the embedded object at offset 492" accessor;
# the field's meaning needs the object class. Named for what it does: return obj + 492.
[entrypoint.functions."0x82BAA138"]
name = "GetSubObj_492_82BAA138"

# A 2D region operation: compute a rectangle's width/height (in 4-byte units), derive a step
# via the Euclidean (gcd) algorithm, then traverse the 2D region.

# Args: base (r3), p1 (r4), p2 (r5) -- plausibly a base point and two offsets/points. Computes
# dx = p1 - base, dy = p2 - base; width = dx / 4, height = dy / 4. Runs the Euclidean algorithm
# (gcd of width and height via divw/mod in a loop). If the gcd is nonzero and < height, it
# traverses the 2D region (4-byte stride, a nested loop over the rectangle). Returns void. 122
# lines, 0 external callees (all inline). 10 callers (sub_82C2BB20, sub_82C289E8 -- in the TODO
# -- sub_82C2E000, sub_82C196E8, sub_82C204F0 -- in the TODO -- ...). The exact operation (the
# gcd-derived step suggests a strided/diagonal 2D traversal) isn't fully resolvable, so the name
# states the mechanism: process a 2D region (with a gcd-derived step).
[entrypoint.functions."0x82C3C010"]
name = "Process2DRegion_82C3C010"

# A 16-byte allocation + self-referential init: allocate a 16-byte block, make it point at
# itself (sentinel self-references), and store it in the container.

# Arg: obj (r3, ->4 = the block pointer, ->8 = a flag). Allocates a 16-byte block via
# Allocate_OrDbgBreak_8221F388(16). If the block is non-null, sets block->0 = block (a
# self-reference) and block->4 = block (another self-reference). Stores obj->4 = block and
# obj->8 = 0. So it's a constructor for a small (16-byte) self-referential object. Returns
# void. 56 lines. 48 callers (sub_828514D8, sub_82755FD0, sub_82646D18, ...) -- a high-value
# small-object constructor. The object class (the self-references are likely sentinels for a
# small buffer / string / node) isn't resolvable, so the name states the mechanism: allocate a
# 16-byte self-referential block.
[entrypoint.functions."0x821EE1B8"]
name = "Construct16SelfRef_821EE1B8"

# A very large (533-line) structure-processing operation: make a vcall on the source, copy
# variable-length byte data, and orchestrate a long sequence of build/teardown operations
# (DestroyCircularList, Construct16SelfRef, Free, plus the 0x82A0F/0x82A11/0x82A1B node ops).

# Args: obj (r3), src (r4, ->0 = a vtable object, ->4/->12 = data, ->20 = a size), (r5).
# Makes a vcall on src (vtable slot 2, offset 8). If src->20 >= 4, copies a variable-length
# byte array from src->12 (byte-by-byte to the stack). Then it branches into a long sequence
# of sub-operations: DestroyCircularList_82A0F5C0, Free_SizeBucketed_Thunk_8221BE68,
# Construct16SelfRef_821EE1B8, sub_82A0F0A0, sub_82A0F6B8, sub_82A118B0, sub_82A1B480, and
# sub_8322EFB0. So it's a top-level "process the data structure" routine (parse + build /
# teardown). 533 lines. 1 caller: sub_832206E8 (a sibling, also in the TODO). The data-structure
# class and the exact operation sequence aren't resolvable, so the name states the mechanism:
# a large structure-processing routine.
[entrypoint.functions."0x82A0DD10"]
name = "ProcessDataStructure_82A0DD10"

# A block reassign / cleanup (198 lines): free the old block and, on the larger path, allocate
# a new one and reinitialize it -- a "reassign the object's data block" operation.

# Args: obj (r3, ->0 = a data block pointer), gate (r4, ->8 = a count/flag), (r5). If gate->8
# <= 1, it frees obj->0 (Free_SizeBucketed_Thunk_8221BE68, if non-null) and clears obj->0
# (a teardown of a small object). Otherwise (gate->8 > 1) it allocates a new block
# (Allocate_OrDbgBreak_8221F388), reinitializes it, and calls sub_82A0FB80 (a further setup
# routine). So it's a reassign/realloc of the object's data block (free old, allocate new).
# Returns void. 198 lines. 2 callers (sub_82A0DD10 -- ProcessDataStructure_82A0DD10 -- plus
# sub_82A0E0C0). Part of the 0x82A0F structure cluster. The block class isn't resolvable, so
# the name states the mechanism: reassign the object's data block.
[entrypoint.functions."0x82A0F6B8"]
name = "ReassignBlock_82A0F6B8"

# A large (393-line) structure constructor / build: allocate self-referential blocks, build a
# multi-element structure, and orchestrate teardown (destroy/free) as needed.

# Args: obj (r3, ->8 = a count/flag), (r4). If obj->8 < 2, takes a short path. Otherwise it
# allocates 16-byte self-referential blocks (Allocate_OrDbgBreak_8221F388, each block set to
# point at itself -- the same pattern as Construct16SelfRef_821EE1B8) and builds a multi-element
# structure in a loop (r29 = 25 elements, plus further Allocate_SizeBucketed_Thunk_8221F3F0
# calls). It also orchestrates DestroyCircularList_82A0F5C0, Free_SizeBucketed_Thunk_8221BE68,
# sub_82A0F120, sub_82A0F290, and sub_82CBBB58. So it's a top-level "build the structure"
# routine. 393 lines. 3 callers (sub_82A0E0C0, sub_82A11500, sub_82A0DD10 --
# ProcessDataStructure_82A0DD10). Part of the 0x82A11 structure cluster. The structure class
# isn't resolvable, so the name states the mechanism: a large structure-construction routine.
[entrypoint.functions."0x82A118B0"]
name = "BuildStructure_82A118B0"

# A float-data processing routine (in the 0x8322 cluster): load global floats and a float
# array (indexed), build a stack struct, then process it via ProcessDataStructure.

# Args: obj (r3), (r4), index (r5), (r6), (r7). Loads several global floats (and a float from
# a global float array at index * 4), building a small stack struct (stack[80..92]). Then it
# calls ProcessDataStructure_82A0DD10 (sub_82A0DD10) and sub_82A0B5C8 (a constructor in the
# 0x82A0B cluster), and frees (Free_SizeBucketed_Thunk_8221BE68). So it's a "process float
# data" routine that feeds the data-structure processor. 127 lines. 8 callers, in the
# 0x8323/0x8321 region (sub_8323AE00, sub_8323F5B0, sub_83217688, ...). The float data's
# meaning (plausibly geometry / transform / animation values) and the object class aren't
# resolvable, so the name states the mechanism: process float data.
[entrypoint.functions."0x832206E8"]
name = "ProcessFloatData_832206E8"

# A 2D strided-processing routine (in the 0x82BDD cluster): call ProcessElements with
# count * stride, then dispatch on the stride (an 8-way switch) for stride-specific handling.

# Args: obj (r3, ->16 = a flag), (r4), count (r5), stride (r6), (r7). Computes (count * stride)
# and calls ProcessElements_82BDCF20 (sub_82BDCF20) to process the elements. If obj->16 != 0,
# it does an 8-way switch on (stride - 1) (cases 0-7) for stride-specific processing (via
# sub_82BC8870 / sub_82BCCB88). So it's a "process 2D data, with per-stride handling" routine.
# 213 lines. 5 callers, all in the 0x82BDD cluster (sub_82BDD518, sub_82BDD170, sub_82BDD1E0,
# sub_82BDD8A8, sub_82BDD258). The data class and the per-stride operations aren't resolvable,
# so the name states the mechanism: process 2D strided data.
[entrypoint.functions."0x82BDD000"]
name = "Process2DStrided_82BDD000"

# A range/counter advance (the sibling of AdvanceRanges_82BAA6F0): if delta > 0, update the
# counters and advance the range pointers -- but in the opposite (shrink/consume) direction
# (obj->36 += delta while obj->44 -= delta, etc.).

# Args: obj (r3, a multi-range object: ->4/->12 = counters, ->20/->28, ->36/->44, ->52/->60 =
# range triples), delta (r4). If delta <= 0, return (no-op). Otherwise: obj->12 -= 1, obj->4 +=
# 1, obj->20 += 1, obj->28 -= 1; advance the ranges (obj->36 += delta while obj->44 -= delta,
# obj->52 += delta while obj->60 -= delta) -- so it shrinks the ranges from both ends. Returns
# void. 64 lines, 0 external callees (all inline). 3 callers (sub_82BA9068, sub_82BAA570 -- in
# the TODO -- sub_82BA8D90). A sibling of AdvanceRanges_82BAA6F0 (same multi-range object,
# opposite direction); the "2" distinguishes the two. The object class and exact range
# semantics aren't resolvable.
[entrypoint.functions."0x82BAA7B8"]
name = "AdvanceRanges2_82BAA7B8"

# A string allocation: compute the string length (if pos == -1) or use the given length, then
# allocate the string's buffer (ReassignBuffer).

# Args: (r3), src (r4, a C-string), pos (r5, a length, or -1 to auto-compute). If pos == -1,
# it allocates a 16-byte block (zeroed: block->0/->4/->8 = 0) and computes the string length
# (a strlen loop over src until a null byte). Otherwise it uses the given length. Then it
# calls ReassignBuffer_822088B0 (sub_822088B0) to (re)allocate the string's data buffer at the
# computed length. So it's a "create/allocate a string object from a C-string" routine. 107
# lines. 3 callers (sub_821E3950, ConstructRefCounted_8222CF18 -- which calls it to set up a
# string value -- plus sub_82B3A0E8). The string class isn't resolvable, so the name states the
# mechanism: allocate a string (compute length, allocate buffer).
[entrypoint.functions."0x822079D8"]
name = "AllocateString_822079D8"

# A dispose / destructor for a specific object type: free the object's block, clear its
# value, and preserve a flag bit -- called by Release_RefCounted_821C67D8 when the refcount
# hits zero.

# Arg: obj (r3, ->0 = a data block, ->4 = a value, ->8 = a field with a flag bit). If obj->0
# != 0, frees it (Free_SizeBucketed_Thunk_8221BE68) and sets obj->0 = 0. Sets obj->4 = 0.
# Preserves bit 0 of obj->8 (obj->8 &= 1), clearing the rest (the size/length bits). So it's a
# dispose: release the block, reset the value, keep the flag. Returns void. 53 lines. 1 caller:
# Release_RefCounted_821C67D8 (the refcounted release -- this is the object-type-specific
# dispose it dispatches to when the reference count reaches zero). The object class isn't
# resolvable, so the name states the mechanism: dispose (free + reset, keep a flag).
[entrypoint.functions."0x82B39A30"]
name = "Dispose_82B39A30"

# A chain-root finder (the byte-33-flag variant): validate, then walk up a chain (via
# node->0) to the topmost node (the one whose byte-33 flag is set) and store it in obj->4.
# A third variant, structurally identical to FindChainRoot_82A596F0 (byte 17) and
# FindChainRoot2_823D4F20 (byte 25), but uses byte 33.

# Arg: obj (r3, ->0 = a base, ->4 = a starting node). Validates (obj->0 != 0, else `trap 22`;
# the head node's byte-33 == 0, else `trap 22`). Then walks up the chain via node->0 (the
# parent link) until it reaches a node whose byte-33 flag is nonzero (the root/top), and
# stores that node in obj->4. Returns void. 93 lines, 0 external callees (all inline). 64
# callers (sub_8278C8E0, sub_8292C840, sub_823B0DB8, ...) -- a high-value chain-root lookup.
# Siblings: FindChainRoot_82A596F0 (byte 17), FindChainRoot2_823D4F20 (byte 25); the "3"
# distinguishes this byte-33 variant. The node/chain class and the byte-33 flag's meaning
# aren't resolvable.
[entrypoint.functions."0x82A962B0"]
name = "FindChainRoot3_82A962B0"

# A linear search (8-byte stride): scan an array of 8-byte elements, comparing a masked 27-bit
# field of each to the target, and return the located slot offset.

# Args: obj (r3, ->28 = a u16 with a flag bit, ->31 = the count, ->32/->68 = the array base),
# target (r4). Selects the array base as (obj + 68) if bit 0 of (obj->28) is set, else (obj +
# 32). Reads count = obj->31; if 0, returns 0. Otherwise scans the array (8-byte slots): for
# each element, masks it to 27 bits (element & 0x7FFFFFF) and compares to target; on a match,
# returns the slot offset. Returns 0 if not found. 60 lines, 0 external callees (all inline).
# 13 callers (sub_821BD468, sub_823F75A0, sub_82283ED0, ...). The element class and the target
# type aren't resolvable, so the name states the mechanism: a linear search over an 8-byte
# array (masked-field comparison).
[entrypoint.functions."0x82284448"]
name = "LinearSearch_82284448"

# A string-key lookup: walk a node structure, comparing node keys via StrCmp_Signed_8226D7A8,
# and set a 0/1 result.

# Args: obj (r3, ->4 ->4 = a starting node, node->17 = a flag, node->12 = a key), src (r4,
# ->0 = the target). Reads the starting node (obj->4->4); if the node's byte-17 flag is 0, it
# walks the node structure comparing each node's key (node->12) to the target via
# StrCmp_Signed_8226D7A8, setting a 0/1 result (found/not) in r11. Returns that result. 100
# lines. 1 caller: sub_825D7688 (a sibling, also in the TODO). The node/key class (a string-key
# lookup over a tree/list of nodes) isn't resolvable, so the name states the mechanism: a
# string-key lookup (walk + StrCmp compare, return a 0/1 result).
[entrypoint.functions."0x822ADCB8"]
name = "LookupString_822ADCB8"

# A string-key lookup (the larger sibling of LookupString_822ADCB8): walk a node structure,
# comparing node keys via StrCmp_Signed_8226D7A8 / CompareStrings_8228B5A0, and set a 0/1
# result.

# Args: obj (r3, ->4 ->4 = a starting node, node->17 = a flag, node->12 = a key), (r4), src
# (r5, ->0 = the target). Reads the starting node (obj->4->4); if the node's byte-17 flag is 0,
# it walks the node structure comparing each node's key (node->12) to the target via
# StrCmp_Signed_8226D7A8 and CompareStrings_8228B5A0, setting a 0/1 result (found/not). Also
# calls sub_8246CF80 and sub_824708F8 (further lookup helpers). Returns the 0/1 result. 220
# lines. Many callers (sub_82514818, sub_825C7278, sub_8291FC20, ...). A larger sibling of
# LookupString_822ADCB8 (same walk + string-key compare + 0/1 result shape, but with extra
# helpers and a longer body); the "2" distinguishes the two. The node/key class isn't
# resolvable, so the name states the mechanism: a string-key lookup.
[entrypoint.functions."0x8246C010"]
name = "LookupString2_8246C010"

# A lookup-and-compare: call LookupString_822ADCB8 to find the node, validate it, then confirm
# the match with CompareStrings_8228B5A0.

# Args: obj (r3), target-obj (r4), src (r5). Calls LookupString_822ADCB8 (sub_822ADCB8) to
# find the matching node. If the result is 0, raises `trap 22` (an invalid/missing-node
# assertion). Otherwise it reads the node's key (result->4) and, if it doesn't already equal the
# lookup result, confirms the match with CompareStrings_8228B5A0 (sub_8228B5A0, comparing the
# node's +12 string to src). Returns the 0/1 match result. 86 lines. Many callers (sub_82B71020,
# sub_825C6368, sub_825C79D8, ...). It's a thin "find the node, then verify the string matches"
# wrapper around LookupString + CompareStrings. The node class and exact comparison semantics
# aren't resolvable, so the name states the mechanism: lookup and compare.
[entrypoint.functions."0x825D7688"]
name = "LookupAndCompare_825D7688"

# A register/init with a global counter: initialize the object, atomically increment a global
# counter (under the global lock), and run a couple of string lookups.

# Args: obj (r3, ->0 = a value, ->4 = a field), (r4), (r5). Sets obj->0 = r5 and obj->4 = 0
# (initializing the object). Then it atomically increments a global counter (lwarx/stwcx CAS
# loop under the global lock, REACQUIRE until success) -- the classic "register this object / assign
# it an ID" pattern. It then calls LookupString2_8246C010 (sub_8246C010), LookupAndCompare_825D7688
# (sub_825D7688), and CopyAssign_RefCounted2_82265160. Returns. 112 lines. 4 callers, in the
# 0x82B7 cluster (sub_82B5C8B0, sub_82B745A0, sub_82B714A0, sub_82B80D70). The object class and
# the exact registry semantics aren't resolvable, so the name states the mechanism: register/init
# the object and bump a global counter.
[entrypoint.functions."0x82B71020"]
name = "RegisterWithCounter_82B71020"

# A sorting network / bubble-sort over a small set of wrapper pointers: compare pairs
# (a,b), (b,c), ... and swap the pointers so they come out in order.

# Args: a (r3), b (r4), c (r5), (r6) -- three (or four) pointers to wrapper objects, where each
# wrapper's ->0 is a value object and value->0 / value->4 are the two sort keys. For each pair,
# it compares (value->0 of A vs value->0 of B); if A > B it swaps the two pointers; if equal it
# tiebreaks on (value->4). It walks the pairs (a,b), (b,c), ... performing swaps, producing an
# ordered sequence. Returns void. 89 lines, 0 external callees (all inline compares/swaps). 1
# caller: sub_82C170D0. The element class and the exact number of inputs aren't resolvable, so
# the name states the mechanism: a pointer-sorting (comparison/swap) network.
[entrypoint.functions."0x82C280C0"]
name = "SortPointers_82C280C0"

# A bit-flag processing routine: inspect bit 13 of the incoming flags and of obj->28, compute a
# parity bit (via a leading-zero count), and dispatch further flag handling.

# Args: (r3), obj (r4, ->28 = a u16 flag field), flags (r5), (r6). Tests bit 13 (0x1000) of the
# incoming flags; if it's clear, jumps to a fast path. Otherwise it inspects obj->28 (its upper
# bits and its bit 13) and, in one branch, computes a parity bit as (32 - clz(obj->28 & 0xFF)) &
# 1 (an odd/even bit-count test). It then calls sub_82284810 (a further flag handler). Returns.
# 110 lines. 0 direct callers (it's reached via a dispatch/vcall, not a direct call). The flag
# semantics (what bit 13 and the parity select) and the object class aren't resolvable, so the
# name states the mechanism: process bit flags.
[entrypoint.functions."0x82284750"]
name = "ProcessFlags_82284750"

# A comparator-based binary search over a 4-byte-stride array: linear scan for small ranges
# (<= 32 elements), otherwise binary search using sub_82C42828 as the comparator.

# Args: low (r3, array start), high (r4, array end), count (r5), (r6). Computes the element
# span as ((high - low) >> 2) (4-byte stride). If the span is <= 32, it takes a linear-scan path.
# Otherwise it binary-searches: computes a mid (rounding up), calls sub_82C42828 (the comparator,
# with (low, high, ...) args) to decide which half to recurse into, and narrows the range. It
# also calls sub_82C16D20, sub_82C273A8, sub_82C289E8 (further search/insert helpers) and recurses
# (self-call). Returns the located position. 176 lines. 8 callers, in the 0x82C1-0x82C2 cluster
# (sub_82C2E968, sub_82C3ADF0, sub_82C1B998, ...). A sibling of BinarySearch_4Stride_82C40EC0
# (same 4-byte-stride array search, but with an explicit comparator + linear fallback); the
# "Cmp" distinguishes it. The array element class and the comparator's ordering aren't
# resolvable.
[entrypoint.functions."0x82C39988"]
name = "BinarySearch_Cmp_82C39988"

# A bubble-sort over a range of 4-byte-stride wrapper pointers: walk the (start, end) range,
# compare adjacent wrappers (via ->0->0), and swap them into order.

# Args: start (r3, array start), end (r4, array end) -- a range of wrapper pointers, each
# wrapper's ->0 a value object (value->0 the sort key). If start == end (or start + 4 == end,
# i.e. fewer than 2 elements), returns immediately (nothing to sort). Otherwise it walks the
# range, comparing adjacent elements ((start)->0->0 vs (start+4)->0->0) and swapping them when
# out of order (a bubble sort). It also calls Process2DRegion_82C3C010 on one path. Returns
# void. 113 lines. 1 caller: BinarySearch_Cmp_82C39988 (sub_82C39988). A range-based sibling of
# SortPointers_82C280C0 (same compare/swap sorting of wrapper pointers, but over a start/end
# range); the name states the mechanism: bubble-sort the pointer range. The element class
# isn't resolvable.
[entrypoint.functions."0x82C289E8"]
name = "BubbleSortPointers_82C289E8"

# A heapify / priority-queue sift over a 4-byte-stride array: compare the root against the
# elements at the mid/child indices and swap them to maintain heap order.

# Args: heap (r3, ->0 = the root element, then a 4-byte-stride array), (r4), end (r5), (r6). Computes
# the element count as ((end - heap) >> 2). If the count is <= 40, it takes a linear path
# (loc_82C3C454). Otherwise it does a heap sift: computes a mid index (roughly (count + 1) / 8,
# rounded up) and compares the root (heap->0->0) against the elements at the mid and 2*mid
# positions (binary-heap parent/child indices); when out of order it swaps the elements
# (rewiring the pointer slots). 298 lines, 0 external callees (all inline compares/swaps). 1
# caller: sub_82C42828 (the same routine BinarySearch_Cmp_82C39988 uses as its comparator --
# so this is the sift it invokes). The element class and the heap's ordering (min/max) aren't
# resolvable, so the name states the mechanism: a heap sift.
[entrypoint.functions."0x82C3C298"]
name = "HeapSift_82C3C298"

# A heap-range processor: compute the mid index of a 4-byte-stride range, sift the left half
# (HeapSift_82C3C298), then continue processing / recursing over the range.

# Args: (r3), start (r4, array start), end (r5, array end), (r6) -- a range of 4-byte-stride
# elements (a heap). Computes the element count as ((end - start) >> 2) and the mid index
# (roughly count / 2, rounded up). It calls HeapSift_82C3C298 (sub_82C3C298) to sift the left
# half (start .. start + mid), then continues walking/processing the range (comparing and
# reordering elements). Returns. 310 lines, 1 callee (HeapSift_82C3C298). 1 caller:
# BinarySearch_Cmp_82C39988 (sub_82C39988) -- so it's the heap routine the comparator invokes
# during its search. The element class and the exact heap invariant (min/max) aren't
# resolvable, so the name states the mechanism: process the heap range (sift + recurse).
[entrypoint.functions."0x82C42828"]
name = "HeapProcess_82C42828"

# A recursive tree / structure destructor: recurse into the child subtree (child->8), free each
# node's data block (child->20), and clear the node's fields.

# Args: node (r3), child (r4), (r5). If the child's byte-33 flag is 0, it recurses into the
# child subtree: calls itself with (node, child->8) (the next child), frees child->20
# (Free_SizeBucketed_Thunk_8221BE68, a size-bucketed block, if non-null), advances to the next
# node (child->0), and zeroes the node's fields ((child + 16)->4/->8/->12 = 0). So it's a
# recursive teardown of a tree (free each node's block, recurse on the children). Returns void.
# 75 lines. 2 callers (sub_82B4CC70, sub_82371958). A sibling of DestroyList_Recursive (same
# recursive-free pattern, but for a tree with data blocks). The node class and the child link
# semantics aren't resolvable, so the name states the mechanism: a recursive tree destructor.
[entrypoint.functions."0x82B4D218"]
name = "DestroyTree_Recursive_82B4D218"

# An indexed-region processor: index a 16-byte-stride array in a large object (index clamped to
# 0-511), update the region's counters, and advance the ranges (AdvanceRanges2_82BAA7B8).

# Args: obj (r3, a large object with a 16-byte-stride array starting at offset 44), gate (r4,
# a non-zero check), index (r5, clamped to 511), (r6). If gate == 0, returns. Clamps index to
# 511. Accesses the region at (obj + 44 + (index + 196) * 16) and updates its counters (region->4
# += 1, region->12 -= 1), then walks the region (a loop over obj + 44) and calls
# AdvanceRanges2_82BAA7B8 (sub_82BAA7B8) and sub_82BAA570. So it's a "process the region at the
# clamped index" routine. 196 lines. Many callers, in the 0x82BB/0x82BA cluster (sub_82BB7700,
# sub_82BAE500, sub_823F8F40, ...). The region object and the exact counter semantics aren't
# resolvable, so the name states the mechanism: process an indexed region.
[entrypoint.functions."0x82BA9068"]
name = "ProcessIndexedRegion_82BA9068"

# A trivial constant-return stub: always returns 2.

# Args: (none used). Sets r3 = 2 and returns. 2 lines. 0 direct callers -- so it's most likely a
# virtual method reached via a vtable (a type-ID / small-value getter that always yields 2), or
# otherwise-dead code. The constant 2's meaning (a type tag, an element count, a mode, ...) can't
# be determined from the body alone. The name states the mechanism: a stub that returns the
# constant 2.
[entrypoint.functions."0x823314B8"]
name = "ReturnConstant2_823314B8"

# A trivial global getter: returns a fixed global address (0x83334D40).

# Args: (none used). Loads a fixed 32-bit address via lis/addi (0x83334D40, in the tail of the
# .text/.data region) into r3 and returns it. 2 lines. 0 direct callers -- so it's most likely a
# virtual method reached via a vtable (a "get the global object/pointer" accessor) or otherwise-
# dead code. The global it points at (0x83334D40) and its meaning can't be determined from the
# body alone. The name states the mechanism: a stub returning a fixed global pointer.
[entrypoint.functions."0x82C27FA0"]
name = "GetGlobalPtr_82C27FA0"

# A trivial global getter (the sibling of GetGlobalPtr_82C27FA0): returns a fixed global
# address (0x83334D28, 24 bytes before GetGlobalPtr_82C27FA0's 0x83334D40).

# Args: (none used). Loads a fixed 32-bit address via lis/addi (0x83334D28) into r3 and returns
# it. 2 lines. 0 direct callers -- most likely a virtual method reached via a vtable (a "get the
# global object/pointer" accessor) or otherwise-dead code. Sibling of GetGlobalPtr_82C27FA0
# (same shape, a nearby global in the same region); the "2" distinguishes the two. The global's
# meaning can't be determined from the body alone. The name states the mechanism: a stub
# returning a fixed global pointer.
[entrypoint.functions."0x82C23D00"]
name = "GetGlobalPtr2_82C23D00"

# An indexed-region processor (the sibling of ProcessIndexedRegion_82BA9068): index a
# 16-byte-stride array in a large object (index clamped to 0-511), update the region's counters,
# and advance the ranges (AdvanceRanges_82BAA6F0).

# Args: obj (r3, a large object with a 16-byte-stride array), index (r4, clamped to 511), (r5),
# (r6), (r7). Clamps index to 511. Accesses the region at (obj + (index + 196) * 16) and updates
# its counters (region->0 += 1, region->12 += 1), clamps region->8 to the new region->12, then
# walks the region and calls AdvanceRanges_82BAA6F0 (sub_82BAA6F0) and sub_82BAA4A8. So it's a
# "process the region at the clamped index" routine. 194 lines. Many callers, in the 0x82BB/
# 0x82BC/0x82BA cluster (sub_82BBDC90, sub_82BAFBE8, sub_82BC4040, ...). A sibling of
# ProcessIndexedRegion_82BA9068 (same 16-byte-stride array + clamped-index shape, but increments
# ->0/->12 and calls AdvanceRanges rather than AdvanceRanges2); the "2" distinguishes the two.
# The region object and the exact counter semantics aren't resolvable.
[entrypoint.functions."0x82BA8F18"]
name = "ProcessIndexedRegion2_82BA8F18"

# A range-advance + dispatch: call AdvanceRanges_82BAA6F0 to advance the object's ranges, then
# dispatch (a vcall via a global function pointer / sub_82BAA688).

# Args: obj (r3), (r4), (r5), (r6). Calls AdvanceRanges_82BAA6F0 (sub_82BAA6F0) with a global
# base pointer + obj + the range args to advance the ranges. Then it loads a global function
# pointer (from a global table) and dispatches a call through it (bctrl) with obj, and also calls
# sub_82BAA688 (a further advance/range helper). It also loads/compares a 2-field global
# (a low/high pair). So it's an "advance the ranges, then dispatch" routine. 116 lines. 1 caller:
# ProcessIndexedRegion2_82BA8F18 (sub_82BA8F18). Part of the 0x82BAA range/advance cluster. The
# object class and the dispatched call's meaning aren't resolvable, so the name states the
# mechanism: advance the ranges and dispatch.
[entrypoint.functions."0x82BAA4A8"]
name = "AdvanceAndDispatch_82BAA4A8"

# A large structure-node processor: branch on a child's flag, and on the null-child path call a
# helper (sub_82B4C1C0); otherwise process the node (FindChainRoot3_82A962B0 + helpers).

# Args: obj (r3), child (r4, ->4 = a value, ->8 = a flag), (r5), (r6). If child->8 == 0, it calls
# sub_82B4C1C0 (a leaf helper, with child->4 / 1 / r6) and returns. Otherwise it processes the
# node: calls FindChainRoot3_82A962B0 (sub_82A962B0) to find the chain root, plus sub_828836E8 and
# sub_82B4C080 (further structure helpers). So it's a "process the structure node" routine. 306
# lines. 1 caller: sub_82B4B2B8. Part of the 0x82B4B structure cluster (siblings of
# DestroyTree_Recursive_82B4D218, sub_82B4C080, sub_82B4C1C0). The node class and the exact
# processing aren't resolvable, so the name states the mechanism: process a structure node.
[entrypoint.functions."0x82B4BB10"]
name = "ProcessStructureNode_82B4BB10"

# A large node-value constructor (in the 0x82B4C cluster): check a type/code field, build a
# string, allocate, and construct a value (ConstructFromArray + the 0x826C3 helpers).

# Args: obj (r3), child (r4, ->8 = a type/code), (r5), (r6), (r7). Tests child->8 against a code
# (0x0CCE03); on the matching path it builds a string (SetString_822F2020 with a global string,
# then sub_826C3EF0 -- a format op), allocates (Allocate_OrDbgBreak_8221F388), and constructs a
# value (ConstructFromArray_82B4BFD8, a sibling in the 0x82B4B cluster, plus sub_822F1F00,
# sub_8234D600, sub_826C3FA8, sub_8292CC88, sub_82171810). So it's a "construct the node's value"
# routine (branch on the type, build the string, allocate, construct). 349 lines. 2 callers
# (sub_82B4BB10 -- ProcessStructureNode_82B4BB10 -- and sub_82B4C080). The node/value class and
# the 0x0CCE03 code's meaning aren't resolvable, so the name states the mechanism: construct a
# node value.
[entrypoint.functions."0x82B4C1C0"]
name = "ConstructNodeValue_82B4C1C0"

# A chain-root finder (a byte-33-flag variant): validate, then walk a chain (via node->8 / node->0)
# to the topmost node (byte-33 flag set) and store it in obj->4.

# Arg: obj (r3, ->0 = a base, ->4 = a starting node). Validates (obj->0 != 0, else `trap 22`).
# Reads the head node (obj->4): if the head's byte-33 flag is set, it advances to the next node
# (obj->4 = head->8) and returns if that node's byte-33 flag is 0 (else `trap 22`). Otherwise it
# walks up the chain via node->0 / node->8 until it reaches a node whose byte-33 flag is set (the
# root/top), and stores that node in obj->4. Returns void. 112 lines, 0 external callees (all
# inline). Many callers (sub_82883068, sub_822FF270, sub_82616CA0, ...). Siblings:
# FindChainRoot_82A596F0 (byte 17), FindChainRoot2_823D4F20 (byte 25), FindChainRoot3_82A962B0
# (byte 33, via node->0); this one also uses byte 33 but walks via node->8/node->0, hence the
# "4". The node/chain class and the byte-33 flag's meaning aren't resolvable.
[entrypoint.functions."0x828836E8"]
name = "FindChainRoot4_828836E8"

# A hash table insert / get-or-insert (the sibling of HashTable_Find2_829FC630): look up the
# key; if not present, increment the count, compute the value's length, allocate, and insert.

# Args: table (r3, ->24 = a count/size), key (r4), value (r5, a C-string). Calls
# HashTable_Find2_829FC630 (sub_829FC630) to look up the key. If it's already present, returns it.
# Otherwise it increments the count (table->24 += 1), computes the value's length (a strlen loop),
# allocates (Allocate_OrDbgBreak_8221F388) and copies the value (MemCpy_SdkRuntime_82CA2C60), then
# inserts the entry into the open-addressing table. So it's a "get or insert into the hash table"
# routine. 148 lines. 1 caller: sub_829FC7A8. Sibling of HashTable_Find2_829FC630 (the lookup
# this insert uses); the table's key/value class (a string-key -> string-value map, plausibly) isn't
# fully resolvable, so the name states the mechanism: insert into the hash table.
[entrypoint.functions."0x829FC6A0"]
name = "HashTable_Insert_829FC6A0"

# A hash table insert with auto-grow: call HashTable_Insert_829FC6A0, then check whether the
# table is at its load threshold and grow/rehash (sub_829FAB50) if needed.

# Args: table (r3, ->0 = a size/mask, ->24 = a count), (r4, a key/index), (r5). Calls
# HashTable_Insert_829FC6A0 (sub_829FC6A0) to insert the entry. Then it checks the table's load
# (count = table->24 against a threshold derived from the size table->0, a (3/4)-fill-style
# threshold); if it's at the threshold it grows the table by calling sub_829FAB50 (a rehash,
# with the new size table->0 << 1). It then recomputes the probe index ((size - 1) & key). So it's
# an "insert, and grow the table if it's getting full" routine. 101 lines. 1 caller: sub_829FC858.
# Part of the 0x829FC hash-table cluster (HashTable_Find2_829FC630, HashTable_Insert_829FC6A0,
# sub_829FAB50). The table's key/value class isn't resolvable, so the name states the mechanism:
# insert with auto-grow.
[entrypoint.functions."0x829FC7A8"]
name = "HashTable_InsertGrow_829FC7A8"

# A ctype predicate (a sibling of the 0x82CA6AB0-0x82CA6BB0 char-classification cluster): look up
# the u16 char table and return bit 3 (0x8) of the entry.

# Arg: c (r3, a char). Computes the table index as (c * 2) (a u16 per character), loads the u16
# entry from the global char-classification table (a fixed base near the .data region, the same
# table the 0x82CA6AB0-BB0 ctype siblings use), masks it to bit 3 (0x8) (rlwinm ... 28,28), and
# returns that single bit. So it's a character-classification predicate: "does char c have the
# bit-3 class set?". 5 lines, 0 external callees (all inline table lookup). 13 callers
# (sub_82C47A50, sub_83045EC0, sub_82B47610, ...). Siblings in the same cluster:
# Ctype_Pred_Bit2_82CA6B10 (bit 2), and the bit-0 (_UPPER) predicates; the "Bit3" identifies which
# class bit this one returns. The exact class (bit 3's meaning in the table) isn't resolvable from
# the body alone.
[entrypoint.functions."0x82CA6B50"]
name = "Ctype_Pred_Bit3_82CA6B50"

# A trivial constant-return stub: always returns 8.

# Args: (none used). Sets r3 = 8 and returns. 2 lines. 0 direct callers -- so it's most likely a
# virtual method reached via a vtable (a "get the size / count / type" getter that always yields
# 8), or otherwise-dead code. The constant 8's meaning (an element size, a count, a type tag, ...)
# can't be determined from the body alone. The name states the mechanism: a stub that returns the
# constant 8.
[entrypoint.functions."0x826EB210"]
name = "ReturnConstant8_826EB210"

# A float-to-int conversion + process: if a pair field matches, scale a float by an integer,
# convert the result to an int (fctiwz), and process it (sub_823F9AE0).

# Args: obj (r3, ->36/->40 = a pair, ->44 = a float), (r4). If obj->40 == obj->36, it does a
# float-to-int conversion: loads obj->44 (a float) and obj->36 (extended to a double), multiplies
# them, converts the product to an int (fctiwz, with the standard NaN/overflow saturation), and
# calls sub_823F9AE0 with that int (+ 1). So it's a "scale a float to an int and process" routine
# (a normalized-value -> index/count conversion). 94 lines. Many callers, in the 0x82BB/0x82BA/
# 0x82BC cluster plus ProcessFlags_82284750 (sub_82284750). The float's meaning (a scale factor,
# a probability, a normalized coordinate) and sub_823F9AE0's role aren't resolvable, so the name
# states the mechanism: float-to-int conversion and process.
[entrypoint.functions."0x82284810"]
name = "FloatToIntProcess_82284810"

# A trivial constant-return stub: always returns 16.

# Args: (none used). Sets r3 = 16 and returns. 2 lines. 0 direct callers -- so it's most likely a
# virtual method reached via a vtable (a "get the size / count / type" getter that always yields
# 16, e.g. a 16-byte element size), or otherwise-dead code. The constant 16's meaning (an element
# size, a count, a type tag, ...) can't be determined from the body alone. The name states the
# mechanism: a stub that returns the constant 16.
[entrypoint.functions."0x8269B5F8"]
name = "ReturnConstant16_8269B5F8"

# A structure validator + chain-root finder: validate obj->32 (non-null, not self-referential,
# else trap 22), then find the chain root (FindChainRoot_82A596F0).

# Args: obj (r3, ->28 = a field, ->32 = a pointer), (r4). Reads obj->32 and validates it (it must
# be non-null and must not equal obj + 28 -- a self-reference check -- else it raises `trap 22`).
# Then it walks/validates the structure (a loop checking obj->32 against obj + 28 and a stored
# value) and calls FindChainRoot_82A596F0 (sub_82A596F0) to find the chain root. So it's a
# "validate the structure, then find its chain root" routine. 90 lines. 0 direct callers (it's
# reached via a dispatch/vcall, not a direct call). Part of the 0x83217 constructor cluster. The
# structure class and the exact validation invariants aren't resolvable, so the name states the
# mechanism: validate and find the root.
[entrypoint.functions."0x83217AF8"]
name = "ValidateAndFindRoot_83217AF8"

# A trivial global getter (the third in the GetGlobalPtr series): returns a fixed global address
# (0x8332ECF0).

# Args: (none used). Loads a fixed 32-bit address via lis/addi (0x8332ECF0, in the .data region)
# into r3 and returns it. 2 lines. Most likely a virtual method reached via a vtable (a "get the
# global object/pointer" accessor) or otherwise-dead code. Siblings: GetGlobalPtr_82C27FA0 (returns
# 0x83334D40) and GetGlobalPtr2_82C23D00 (returns 0x83334D28); the "3" distinguishes this one. The
# global's meaning can't be determined from the body alone. The name states the mechanism: a stub
# returning a fixed global pointer.
[entrypoint.functions."0x82240578"]
name = "GetGlobalPtr3_82240578"

# A line-length scan (strcspn-like): scan a string for a line ending ('\n' = 10, '\r' = 13, or
# null) and return the length of the line.

# Arg: obj (r3, ->0 = a string base, ->32 = a byte, ->36 = an offset into the string). Computes
# the start as (obj->0 + obj->36) and scans forward byte-by-byte (sign-extended) until it hits a
# null, a newline (10), or a carriage return (13). It then returns the length of the line (the
# offset from the start to the line ending, i.e. r10 - start). If the line is empty, it returns 0
# (subf. ... beqlr). 117 lines, 0 external callees (all inline byte scanning). Many callers
# (sub_825F45A8, sub_826939A0, sub_82466098, ...). A string-line scanner ("how long is this line
# before the next newline?"), likely part of a text/log/line-based reader. The string's context
# isn't resolvable, so the name states the mechanism: scan a line and return its length.
[entrypoint.functions."0x82B477F0"]
name = "ScanLineLength_82B477F0"

# A chain lookup: walk a node chain (via node->8 / node->0, byte-25 flag) comparing each node's
# key (node->12) to the target, and locate the matching node.

# Args: obj (r3, ->4 ->4 = a starting node, node->25 = a flag, node->12 = a key), src (r4, ->0 =
# the target). Reads the starting node (obj->4->4); if the node's byte-25 flag is 0, it walks the
# chain: compares node->12 to the target, and depending on the comparison follows node->8 (next)
# or node->0 (parent), continuing while the byte-25 flag is 0. It then locates the matching node
# and calls Release_RefCounted_829FF648 (a refcounted release) and sub_82550F90 (a further
# lookup/processing helper). Returns the located node. 144 lines. 7 callers (sub_8323A1E8,
# sub_824426F0, sub_826898E8, ...). The byte-25 flag matches FindChainRoot2_823D4F20's, but this
# one walks + compares (a lookup) rather than just finding the root. The node/key class isn't
# resolvable, so the name states the mechanism: a chain lookup (walk + compare).
[entrypoint.functions."0x82550638"]
name = "ChainLookup_82550638"

# A struct copy: if src->20 >= 4, copy a variable-length byte array from src->12 (byte-by-byte to
# the stack) and advance src->12, then process further.

# Args: obj (r3), src (r4, ->4 = data, ->12 = a byte-array pointer, ->20 = a size). If src->20 <
# 4, takes a short path. Otherwise it copies (src->20 - 4) bytes from src->12 (a byte-by-byte loop
# to the stack), advances src->12 by 4, and continues processing (calling sub_82A1B480 and
# sub_8323A110). So it's a "copy the struct's data" routine (a variable-length byte-array copy,
# the same shape as Construct3FloatPayload_82A0C0A0's payload handling). 126 lines. 4 callers, in
# the 0x8321-0x8323 cluster (sub_83221548, sub_83238090, sub_8321FC38, sub_83218C10). The struct
# class and the exact field semantics aren't resolvable, so the name states the mechanism: copy
# the struct's data.
[entrypoint.functions."0x83235608"]
name = "CopyStructData_83235608"

# A node lookup / insert: call sub_82C343A0 to look up the node, validate the result (trap 22 if
# invalid), and run a chain lookup (ChainLookup_82550638).

# Args: obj (r3, ->4 = a field, ->8 = a value), (r4), gate (r5, ->4 = a value), (r6). If r4 ==
# gate->4, returns (already present). Otherwise it calls sub_82C343A0 (a lookup, with (obj + 4)
# args), validates the result (it must be non-null and must not equal obj + 4 -- a self-reference
# check -- else it raises `trap 22`), then reads the result's fields and calls
# ChainLookup_82550638 (sub_82550638), Release_RefCounted_829FF648, and sub_82A10A70 (further
# node helpers). So it's a "look up / insert the node" routine. 124 lines. 1 caller:
# CopyStructData_83235608 (sub_83235608). Part of the 0x8323 constructor cluster. The node class
# and the exact lookup/insert semantics aren't resolvable, so the name states the mechanism: look
# up / insert a node.
[entrypoint.functions."0x8323A110"]
name = "LookupAndInsertNode_8323A110"

# A buffer write / append: advance the offset and append bytes to a buffer via
# Buffer_PushByte_82CAE378 (in a loop), gated on the buffer's "done"/"in-use" flags.

# Args: src (r3, ->0 = a byte to append), count (r4), buffer (r5, ->8 = an in-use flag, ->12 = a
# flag with a "done" bit), pos (r6, ->0 = an offset), (r7). If bit 6 of buffer->12 is set (a
# "done"/"sealed" flag) or buffer->8 != 0 (in use), it returns without appending. Otherwise it
# advances the offset (pos->0 += count) and appends the byte(s) in a loop, calling
# Buffer_PushByte_82CAE378 (sub_82CAE378) and sub_82CAB770 (a further buffer helper). So it's an
# "append to the buffer (if it's still open)" routine. 92 lines. 2 callers (sub_82CAE4A8, a sibling
# in the same 0x82CAE buffer cluster, plus sub_8223F990). The buffer class and the exact flag
# semantics aren't resolvable, so the name states the mechanism: append bytes to a buffer.
[entrypoint.functions."0x82CAE410"]
name = "Buffer_Append_82CAE410"

# A trivial constant-return stub: always returns 0x8000 (32768).

# Args: (none used). Loads 0 into r3, ORs in 0x8000 (32768), and returns. 3 lines. 0 direct
# callers -- so it's most likely a virtual method reached via a vtable (a type-ID / flag getter
# that always yields 0x8000, the high bit of a 16-bit type/flag word), or otherwise-dead code. The
# constant 0x8000's meaning (a type tag, a capability bit, a default value, ...) can't be
# determined from the body alone. The name states the mechanism: a stub that returns the constant
# 0x8000.
[entrypoint.functions."0x82A11FF0"]
name = "ReturnConstant0x8000_82A11FF0"

# A sorting network over 12-byte records: compare pairs (a,b), (b,c), ... by record->0 and swap
# the full 12-byte records to order them.

# Args: a (r3), b (r4), c (r5), (r6) -- three 12-byte records (->0, ->4, ->8). For each pair, it
# compares the records' ->0 field; if the second is smaller, it swaps the two full 12-byte records
# (rewriting ->0/->4/->8 for both). It walks the pairs (a,b), (b,c), ... performing swaps,
# producing an ordered sequence. Returns void. 104 lines, 0 external callees (all inline
# compares/swaps). 1 caller: sub_82B42E28. A sibling of SortPointers_82C280C0 (same
# compare/swap sorting shape, but this one sorts 12-byte records directly by ->0 rather than
# wrapper pointers); the name states the mechanism: sort the records. The record class isn't
# resolvable.
[entrypoint.functions."0x82B43280"]
name = "SortRecords_82B43280"

# A hash lookup / get-or-create: hash the string (HashCstring_82188CF0), combine the hash with a
# stored value, then do a linear search (LinearSearch_82284448) to find or create the entry.

# Args: obj (r3, ->80 = a stored hash), (r4), (r5), src (r6, ->0 = a string, or 0 to use a global
# string). If r4 or r5 is 0, returns (no-op). Otherwise it hashes the string (HashCstring_82188CF0,
# sub_82188CF0), merges the hash bits into obj->80 (rlwimi/rlwinm bit manipulation), and does a
# linear search (LinearSearch_82284448, sub_82284448) to find the matching entry; if not found it
# creates one (ConstructRefCounted_8222CF18, a refcounted object). So it's a "hash the key, look up
# (or create) the entry" routine. 122 lines. 6 callers (sub_828152D0, sub_828C9360, sub_823F9620,
# ...). The entry class and the exact hash combination aren't resolvable, so the name states the
# mechanism: a hash lookup / get-or-create.
[entrypoint.functions."0x823F74D0"]
name = "HashLookup_823F74D0"

# A value setter / constructor: zero the object's 12-byte field, and if the incoming value is
# non-null, validate it (error if out of range) and construct it (sub_823093F8).

# Args: obj (r3, ->4/->8/->12 = a 12-byte field), value (r4). Zeros the field (obj->4 = obj->8 =
# obj->12 = 0). If value is 0, returns 0 (a null/empty result). Otherwise it validates the value
# (if value > a limit -- a 0x3FFF-style bound -- it calls sub_82684B38, an error/assert; else it
# calls sub_823093F8 to construct/store the value). Returns the result. 85 lines. 3 callers
# (sub_8236ED00, sub_8236EC48, sub_82B1BF08). A "set the value (validating it)" routine for a
# small value object. The value class and the exact bound aren't resolvable, so the name states
# the mechanism: set the value (zero, validate, construct).
[entrypoint.functions."0x8236FCE8"]
name = "SetValue_8236FCE8"

# A character-skip scan (the sibling of ScanLineLength_82B477F0): walk a string (from obj->0 +
# obj->36), advancing past characters that have the bit-3 ctype class (Ctype_Pred_Bit3_82CA6B50) or
# a newline, stopping at null.

# Arg: obj (r3, ->0 = a string base, ->36 = an offset). Computes the start as (obj->0 + obj->36)
# and scans forward: for each byte (sign-extended), if it has the bit-3 character class
# (Ctype_Pred_Bit3_82CA6B50, sub_82CA6B50) or is a newline (10), it advances; it stops at a null
# byte. So it's a "skip the leading characters of the bit-3 class" routine (e.g. skip leading
# whitespace / separators). 142 lines, 1 callee (Ctype_Pred_Bit3_82CA6B50). 8 callers (sub_82359BE8,
# sub_82B478C0, sub_826939A0, ...). The bit-3 ctype class's exact meaning (whitespace, separators,
# ...) and the string's context aren't resolvable from the body alone, so the name states the
# mechanism: skip characters matching the bit-3 ctype class.
[entrypoint.functions."0x82B47610"]
name = "SkipCtypeChars_82B47610"

# A quote-parity scan: walk a string (from obj->0 + obj->36, for a given length), count the
# quotes (double-quote " = 34, apostrophe ' = 39), and store the parity (odd/even) in obj->32.

# Args: obj (r3, ->0 = a string base, ->32 = a byte flag, ->36 = an offset), length (r4). Scans the
# (length) bytes starting at (obj->0 + obj->36); for each byte, if it's a double-quote (34) or an
# apostrophe (39), it toggles the parity (recomputes (32 - clz(obj->32 & 0xFF)) & 1 -- an odd/even
# quote-count). It stores the resulting parity in obj->32. So it's a "count the quotes and store
# the parity" routine (a quote-balance / unterminated-string detector for a text/log/line reader).
# 62 lines, 0 external callees (all inline byte scanning). Many callers (sub_82B3BAC0, sub_82607260,
# sub_82B475A0, ...). The string's context (a log line, a quoted string, ...) isn't resolvable,
# so the name states the mechanism: scan for quotes and store the parity.
[entrypoint.functions."0x82B47A40"]
name = "ScanQuoteParity_82B47A40"

# A region grow: if the incoming value exceeds the current size (obj->36), grow the region
# (ProcessIndexedRegion_82BA9068 / ProcessIndexedRegion2_82BA8F18) and zero/copy the new space
# (MemSet / MemCpy).

# Args: obj (r3, ->36 = a current size/limit, ->40 = a field), value (r4, the requested size),
# (r5). If value <= obj->36, the region is already large enough (a fast path). Otherwise it gets
# the global singleton (GetSingleton_82BA8718, sub_82BA8718), grows the region by calling
# ProcessIndexedRegion2_82BA8F18 (sub_82BA8F18, with (value * 4) and a constant 141) or
# ProcessIndexedRegion_82BA9068 (sub_82BA9068), and zeros/copies the newly added space
# (MemSet_SdkRuntime_82CA3190 / MemCpy_SdkRuntime_82CA2C60). So it's a "grow the region to fit the
# value" routine (a dynamic-array / region grow). 88 lines. Many callers (sub_82BB7700,
# sub_823F9398, ... plus FloatToIntProcess_82284810 -- sub_82284810). Part of the 0x82BA8 region/
# grow cluster. The region class and the 141 constant's meaning aren't resolvable, so the name
# states the mechanism: grow the region.
[entrypoint.functions."0x823F9AE0"]
name = "GrowRegion_823F9AE0"

# A u16 / UTF-16 string compare (a 16-bit memcmp): compare count 16-bit elements, return 0 if
# equal, else (a - b) as a 3-way result.

# Args: a (r3, a u16 array), b (r4, a u16 array), count (r5, the number of u16 elements). If count
# is 0, returns 0 (equal). Otherwise it walks the arrays (2 bytes at a time), comparing each u16;
# if all are equal it returns 0. On the first difference it computes (a_u16 - b_u16) (a
# sign-extended 3-way comparison result: negative if a < b, positive if a > b, 0 if equal) and
# returns that. So it's a "memcmp for 16-bit (UTF-16) strings" routine. 55 lines, 0 external
# callees (all inline u16 compares). 3 callers (sub_822F6480, sub_8217A858, sub_82377FD8 -- in the
# TODO). Part of the string-compare family (StrCmp_Signed_8226D7A8, CompareStrings_8228B5A0); this
# one operates on 16-bit elements. The string's context (UTF-16 text, a locale string, ...) isn't
# resolvable, so the name states the mechanism: compare u16 elements.
[entrypoint.functions."0x822F85C8"]
name = "CompareU16_822F85C8"

# A clamped range write / update: grow the object if needed (sub_82CD12C8), clamp the write
# length (to the available space and a max), then write the range.

# Args: obj (r3, ->20 = a size, ->24 = a field), pos (r4, the write position), len (r5, the
# requested length), max (r6, a max length), (r7). If obj->20 < pos, it grows the object by
# calling sub_82CD12C8 (a grow/extend helper). It then clamps the write length: to (obj->20 -
# pos) (the available space) and to max (whichever is smaller). It then writes the range (calling
# sub_822F85C8 -- CompareU16_822F85C8 -- to compare/write the 16-bit data). So it's a "write a
# range, growing and clamping as needed" routine. 107 lines. Many callers, in the 0x82C7 cluster
# (sub_82C71F00, sub_82C7EF10, sub_82C70750, ...). The object class (a string/buffer with a size
# field) and the exact write semantics aren't resolvable, so the name states the mechanism: a
# clamped range write.
[entrypoint.functions."0x82377FD8"]
name = "WriteRangeClamped_82377FD8"

# A 4-byte-stride array constructor: given a size, do an overflow check, run a setup step
# (sub_822F1F00), and allocate size * 4 bytes.

# Args: size (r3), (r4). If size is 0, skips the setup. Otherwise it does a size-overflow check
# ((0xFFFFFFFF / size) >= 4 -- guarding against size * 4 overflowing 32 bits); if it passes, it
# calls sub_822F1F00 (a setup, with a global base). Then it allocates (size * 4) bytes via
# Allocate_OrDbgBreak_8221F388 (a 4-byte-stride array) and returns the allocation. So it's a
# "construct a 4-byte-stride array of the given size" routine. 66 lines. Many callers (sub_82308690,
# sub_82370688, ... plus SetValue_8236FCE8 -- sub_8236FCE8 -- which uses it to build the value).
# The array's element class (32-bit values / pointers) isn't resolvable, so the name states the
# mechanism: construct a 4-byte-stride array.
[entrypoint.functions."0x823093F8"]
name = "ConstructArray4_823093F8"

# An error / exception handler: check a bound, and if the value is out of range, build an error
# message (SetString + a format) and raise it (sub_822F1F00).

# Args: obj (r3, ->8 = a value/bound), value (r4). Computes a bound (0x1FFF3FFF, a 0x1FFF-style
# limit) and checks (obj->8 - bound) >= value; if it passes, it returns normally (in range). If it
# fails (out of range), it builds an error message: SetString_822F2020 (a global error string),
# sub_826C3EF0 (a format op), and sub_826C3FA8 (a further format op), then stores a global error
# code and calls sub_822F1F00 (the exception raiser). So it's an "assert / raise on
# out-of-range" routine. 81 lines. 4 callers (sub_82A0F0A0, sub_82A0F290, sub_825180F0,
# sub_82461990). The exact error message and the bound's meaning aren't resolvable from the body
# alone, so the name states the mechanism: raise if out of range.
[entrypoint.functions."0x82461DA0"]
name = "RaiseIfOutOfRange_82461DA0"

# A trivial constant-return stub: always returns 512.

# Args: (none used). Sets r3 = 512 and returns. 2 lines. 0 direct callers -- so it's most likely a
# virtual method reached via a vtable (a "get the size / capacity / default" getter that always
# yields 512, e.g. a default buffer/capacity size), or otherwise-dead code. The constant 512's
# meaning (a default capacity, an element count, a type tag, ...) can't be determined from the body
# alone. The name states the mechanism: a stub that returns the constant 512.
[entrypoint.functions."0x82A145E8"]
name = "ReturnConstant512_82A145E8"

# A struct copy (the sibling of CopyStructData_83235608): if src->20 >= 4, copy a
# variable-length byte array from src->12 (byte-by-byte to the stack) and advance src->12, then
# process further.

# Args: obj (r3), src (r4, ->4 = data, ->12 = a byte-array pointer, ->20 = a size). If src->20 <
# 4, takes a short path. Otherwise it copies (src->20 - 4) bytes from src->12 (a byte-by-byte loop
# to the stack), advances src->12 by 4, and continues processing (calling sub_82A1B480, a sibling
# in the 0x82A1B cluster). So it's a "copy the struct's data" routine. 216 lines. 8 callers (sub
# _82A93850, sub_82B25930, sub_83212DC0, ...). A sibling of CopyStructData_83235608 (same
# variable-length byte-array copy shape); the "2" distinguishes the two. The struct class and the
# exact field semantics aren't resolvable, so the name states the mechanism: copy the struct's
# data.
[entrypoint.functions."0x82A1BD30"]
name = "CopyStructData2_82A1BD30"

# A thin guard + dispatch: if both arguments are null, return 0; otherwise load a global and
# tail-call sub_8240DAA8.

# Args: (r3), a (r4), (r5), b (r6). If both a and b are 0, it returns 0 (nothing to do). Otherwise
# it loads a global base (from a fixed .data address) and tail-calls sub_8240DAA8 (the actual
# work). So it's a "guard (both-null fast path) then dispatch to sub_8240DAA8" thunk. 24 lines.
# 0 direct callers (it's reached via a dispatch/vcall, not a direct call). The global's meaning and
# sub_8240DAA8's role (the real operation) aren't resolvable from this thunk alone, so the name
# states the mechanism: a guard that dispatches.
[entrypoint.functions."0x8227CB70"]
name = "GuardDispatch_8227CB70"

# A string compare (with two cases): if a->0 is null, compare b byte-by-byte against a global
# charset string; otherwise compare a->0 to b via StrCmp_Signed_8226D7A8.

# Args: a (r3, ->0 = a string or a null flag), b (r4, a string). If a->0 is 0 (null), it compares
# b against a global charset string byte-by-byte (a per-byte difference test). Otherwise it
# compares a->0 (the string) to b via StrCmp_Signed_8226D7A8 (sub_8226D7A8). It then derives a
# small result (via a leading-zero count of the comparison result) and returns it. So it's a
# "compare the string (charset match, or a signed compare)" routine. 74 lines, 1 callee
# (StrCmp_Signed_8226D7A8). Many callers (sub_824389E0, sub_82357550, sub_828214A8, ...). A
# sibling of CompareStrings_8228B5A0 and StrCmp_Signed_8226D7A8 (the string-compare family); this
# one adds a charset branch. The string/charset context isn't resolvable, so the name states the
# mechanism: compare the string.
[entrypoint.functions."0x8229AD78"]
name = "CompareString_8229AD78"

# A line / value parser: skip the leading characters (SkipCtypeChars_82B47610), then inspect the
# current character (a ctype-bit-2 class or a minus sign '-' = 45) and parse a value.

# Args: obj (r3, ->0 = a string base, ->36 = an offset), (r4). Calls SkipCtypeChars_82B47610 (sub
# _82B47610) to skip the leading characters of the bit-3 ctype class. It then reads the current
# character (obj->0 + obj->36), and if it has the bit-2 ctype class (Ctype_Pred_Bit2_82CA6B10,
# sub_82CA6B10) or is a minus sign (45), it parses the (possibly signed) value (checking the
# following character too, and calling sub_82B47AB0, sub_82B47C70, sub_82B47D68, sub_82CA6AB0 --
# the 0x82B47 line/number-parse helpers). So it's a "parse a value from the line" routine (likely
# a signed integer/float from a log/property line). 111 lines. 9 callers (sub_825F5238, sub_82B47148,
# sub_82B473F0, ...). The value type and the line's context (a log, a config, a save) aren't
# resolvable, so the name states the mechanism: parse a value from the line.
[entrypoint.functions."0x82B46D40"]
name = "ParseLineValue_82B46D40"

# A number / decimal value parser: scan the numeric part of the line (the bit-2 ctype class --
# digits -- plus a decimal point '.' = 46), and construct a refcounted value object.

# Args: obj (r3, ->0 = a string base, ->36 = an offset), (r4). Reads the current character (obj->0
# + obj->36) and scans the numeric part: it walks while the characters have the bit-2 ctype class
# (Ctype_Pred_Bit2_82CA6B10, sub_82CA6B10 -- plausibly a digit class) and handles a decimal point
# ('.' = 46). It then constructs a refcounted value object (ConstructRefCounted_8222CF18,
# sub_8222CF18), checks the quote parity (ScanQuoteParity_82B47A40, sub_82B47A40), and calls
# sub_82B47F98 (a further parse helper) and DestructRefCounted_82214F08 (sub_82214F08). So it's a
# "parse a numeric value from the line and build the value object" routine (a float/decimal
# parser for a log/property/line reader). 256 lines. 1 caller: ParseLineValue_82B46D40 (sub_82B46D40).
# The value type (float/decimal) and the line's context aren't resolvable, so the name states the
# mechanism: parse a numeric value.
[entrypoint.functions."0x82B47AB0"]
name = "ParseNumberValue_82B47AB0"

# A trivial constant-return stub: always returns 64.

# Args: (none used). Sets r3 = 64 and returns. 2 lines. 0 direct callers -- so it's most likely a
# virtual method reached via a vtable (a "get the size / capacity / default" getter that always
# yields 64, e.g. a 64-byte element/capacity size), or otherwise-dead code. Part of a small cluster
# of adjacent constant-return stubs (sub_82A145F0 returns 16384; siblings ReturnConstant512_82A145E8,
# ReturnConstant16_8269B5F8, ReturnConstant8_826EB210). The constant 64's meaning (an element size,
# a count, a type tag, ...) can't be determined from the body alone. The name states the mechanism:
# a stub that returns the constant 64.
[entrypoint.functions."0x82A145D0"]
name = "ReturnConstant64_82A145D0"

# A trivial constant-return stub: always returns 128.

# Args: (none used). Sets r3 = 128 and returns. 2 lines. 0 direct callers -- most likely a virtual
# method reached via a vtable (a "get the size / capacity / default" getter that always yields 128,
# e.g. a 128-byte element/capacity size), or otherwise-dead code. Part of the small cluster of
# adjacent constant-return stubs (sub_82A145D0 = 64, this one = 128, sub_82A145E8 = 512,
# sub_82A145F0 = 16384; siblings ReturnConstant512_82A145E8, etc.). The constant 128's meaning (an
# element size, a count, a type tag, ...) can't be determined from the body alone. The name states
# the mechanism: a stub that returns the constant 128.
[entrypoint.functions."0x82A145D8"]
name = "ReturnConstant128_82A145D8"

# A range-advance + dispatch (the sibling of AdvanceAndDispatch_82BAA4A8): run a setup (sub_82BAA5D8),
# advance the ranges (AdvanceRanges2_82BAA7B8), then dispatch a vcall.

# Args: obj (r3), (r4), (r5). If obj is null, it returns. Otherwise it calls sub_82BAA5D8 (a setup,
# passing obj), then AdvanceRanges2_82BAA7B8 (sub_82BAA7B8, with a global base and obj -- the second
# range-advance variant), and then dispatches a vcall (loading a global function pointer and
# bctrl'ing on it, passing obj). So it's a "set up, advance the ranges, and dispatch a vcall" routine
# (the 0x82BAA indexed-region / range subsystem). 61 lines. 1 caller: ProcessIndexedRegion_82BA9068
# (sub_82BA9068). The vcall's target and the range object's meaning aren't resolvable, so the name
# states the mechanism: advance the ranges and dispatch.
[entrypoint.functions."0x82BAA570"]
name = "AdvanceRangesDispatch_82BAA570"

# A global-array search: linearly search a global 20-byte-stride array for a value; on a match,
# compact the array (MemMove_SdkRuntime_82CAA2E0) -- i.e. find-and-remove.

# Args: value (r3). Loads a global base and its count (from base + 10244). If the count is > 0, it
# linearly scans the array (20-byte stride), comparing each element's ->0 field to value. On a match
# it computes the position (count - index) and compacts the array by calling MemMove_SdkRuntime_82CAA2E0
# (sub_82CAA2E0) to shift the trailing elements down (removing the matched one). So it's a "find the
# value in the global array and remove it" routine (part of the 0x82BAA indexed-region / range
# subsystem). 97 lines. 2 callers: AdvanceRangesDispatch_82BAA570 (sub_82BAA570) and sub_82BA8D90.
# The array's element class (20-byte records) and the value's meaning aren't resolvable, so the name
# states the mechanism: find the value in the global array.
[entrypoint.functions."0x82BAA5D8"]
name = "FindInGlobalArray_82BAA5D8"

# A trivial constant-return stub: always returns 256.

# Args: (none used). Sets r3 = 256 and returns. 2 lines. 0 direct callers -- most likely a virtual
# method reached via a vtable (a "get the size / capacity / default" getter that always yields 256,
# e.g. a 256-byte element/capacity size), or otherwise-dead code. Part of the small cluster of
# adjacent constant-return stubs (sub_82A145D0 = 64, sub_82A145D8 = 128, this one = 256,
# sub_82A145E8 = 512, sub_82A145F0 = 16384; siblings ReturnConstant512_82A145E8, etc.). The constant
# 256's meaning (an element size, a count, a type tag, ...) can't be determined from the body alone.
# The name states the mechanism: a stub that returns the constant 256.
[entrypoint.functions."0x82A145E0"]
name = "ReturnConstant256_82A145E0"

# A 16-byte value constructor: allocate 16 bytes and set up a 16-byte struct (from a global base and
# a source), with an out-of-range check.

# Args: obj (r3), value (r4), src (r5). Allocates 16 bytes (Allocate_OrDbgBreak_8221F388, sub_8221F388),
# then sets up the 16-byte struct: ->0 = a global base, ->4 = the global's ->4, ->8 = src->0 (a copied
# field), and further fields (the body continues past line 80). It also calls RaiseIfOutOfRange_82461DA0
# (sub_82461DA0, an error handler) to validate the value. So it's a "construct a 16-byte value object"
# routine. 80 lines. 4 callers: ProcessDataStructure_82A0DD10 (sub_82A0DD10), sub_82A0E0C0, sub_82A11500,
# sub_82870460. The struct's class (a 16-byte value/pair) and the field semantics aren't resolvable,
# so the name states the mechanism: construct a 16-byte value.
[entrypoint.functions."0x82A0F0A0"]
name = "Construct16ByteValue_82A0F0A0"

# A struct copy (a third sibling of CopyStructData_83235608 / CopyStructData2_82A1BD30): if obj->20 >=
# 4, copy a variable-length byte array from obj->12 (byte-by-byte to the stack) and advance obj->12,
# then process further.

# Args: obj (r3, ->4 = data, ->12 = a byte-array pointer, ->20 = a size), (r4). If obj->20 < 4, takes a
# short path. Otherwise it copies (obj->20 - 4) bytes from obj->12 (a byte-by-byte loop to the stack),
# advances obj->12 by 4, and continues processing (calling sub_82A1B480, the sibling in the 0x82A1B
# cluster that CopyStructData2_82A1BD30 also calls). So it's a "copy the struct's data" routine. 124
# lines. 1 caller: ProcessDataStructure_82A0DD10 (sub_82A0DD10). The struct class and the exact field
# semantics aren't resolvable, so the name states the mechanism: copy the struct's data. The "3"
# distinguishes this from CopyStructData_83235608 and CopyStructData2_82A1BD30 (the same shape).
[entrypoint.functions."0x8322EFB0"]
name = "CopyStructData3_8322EFB0"

# A chain-root finder (the sibling of the FindChainRoot family): validate the object, then walk the
# chain (via node->8, checking the flag at node->17) to find the root node.

# Args: obj (r3, ->0 = a value, ->4 = the current node). Validates obj->0 (a trap-22 on a null value,
# the same invariant the FindChainRoot family checks). It then walks the chain: it reads the current
# node (obj->4), checks a flag byte (node->17); while the flag is set, it advances to the next node
# (node->8) and updates obj->4, until the flag is clear (the root). So it's a "walk the chain to the
# root" routine (the 0x8247 / node-chain traversal, a sibling of FindChainRoot_82B1D0F0 and its
# variants). 112 lines, no callees (pure data manipulation). 8 callers: LookupString2_8246C010
# (sub_8246C010), sub_825BF630, sub_82BFC138, sub_822FF960, ... The node class (->8 = next, ->17 = a
# flag) and the object's meaning aren't resolvable, so the name states the mechanism: find the chain
# root. The "5" distinguishes it from FindChainRoot, FindChainRoot2, FindChainRoot3, and
# FindChainRoot4.
[entrypoint.functions."0x824708F8"]
name = "FindChainRoot5_824708F8"

# A count-compare + dispatch (the 0x82A0B / float-data cluster): compute two 16-byte-stride counts
# from the object, compare them, and dispatch (to Construct3FloatPayload_82A0C0A0).

# Args: obj (r3, ->4 = a base, ->8 = an end, ->12 = another end). Computes two 16-byte-stride counts:
# ((obj->12 - obj->4) >> 4) and ((obj->8 - obj->4) >> 4) (each >> 4 divides a byte span into 16-byte
# elements). It compares the two counts; when one is larger, it dispatches (calling
# Construct3FloatPayload_82A0C0A0, sub_82A0C0A0 -- the 3-float payload builder -- and sub_82A0BAA0, a
# sibling in the 0x82A0B cluster). So it's a "compare the float-data counts and dispatch" routine
# (part of the float-data processing, the 0x82A0B/0x82A0C cluster). 96 lines. 2 callers:
# ProcessFloatData_832206E8 (sub_832206E8) and sub_832207B8. The object's class (a float-data buffer)
# and the field semantics aren't resolvable, so the name states the mechanism: compare the float-data
# counts.
[entrypoint.functions."0x82A0B5C8"]
name = "CompareFloatCounts_82A0B5C8"

# A trivial global-getter stub: return a pointer to a fixed global (0x82CCC8C0 + 19788 = 0x82D0388C).

# Args: (none used). Loads the address 0x82D0388C (a fixed .data global) into r3 and returns. 2 lines.
# 0 direct callers (most likely a virtual method / a "get the global singleton" accessor). A sibling
# of GetGlobalPtr_82C27FA0, GetGlobalPtr2_82C23D00, and GetGlobalPtr3_82240578 (all trivial global-
# pointer getters). The global's meaning (a singleton, a type table, a global object) can't be
# determined from the body alone. The name states the mechanism: get a global pointer (the "4" is the
# series index).
[entrypoint.functions."0x82C21AD0"]
name = "GetGlobalPtr4_82C21AD0"

# A trivial field getter: return *(r3 + 48) -- the field at offset 48.

# Args: obj (r3). Loads the 32-bit field at offset 48 of obj and returns it. 8 lines (no callees).
# 0 direct callers (most likely a virtual method / accessor reached via a vtable, e.g. a "get field 48"
# getter). The field's meaning (a size, a count, a pointer, a type) can't be determined from the body
# alone -- only that it's a 32-bit field at offset 48 of the object. A sibling of other trivial field
# getters (GetField4_8320F978). The name states the mechanism: get the field at offset 48.
[entrypoint.functions."0x82C6F840"]
name = "GetField48_82C6F840"

# A byte-value check: mask a value to a byte, look it up (sub_82CC2028), and return 192 (0xC0) if the
# lookup yields 192, else 0.

# Args: (r3), value (r4). Masks value to its low byte (r4 & 0xFF), looks it up via sub_82CC2028 (a
# byte-value lookup / table probe), and checks the result: if it is 192 (0xC0), it returns 192; else
# it returns 0. So it's a "check whether this byte value maps to 0xC0 (return 0xC0 or 0)" routine. 35
# lines, 1 callee (sub_82CC2028). Many callers (sub_8236C520, sub_82C7E7B8, sub_82C10B90, ...). The
# byte's meaning and the 0xC0 sentinel (a category, a flag, a special code) aren't resolvable from the
# body alone, so the name states the mechanism: check a byte value (and return 0xC0 on a match).
[entrypoint.functions."0x82CBC678"]
name = "ByteValueCheck_82CBC678"

# A delay / sleep: compute a millisecond delay as a 100ns-tick relative time and call
# __imp__KeDelayExecutionThread (a wait) in a loop.

# Args: value (r3, a duration in milliseconds), flag (r4, ->0 = a flag byte). If value is -1, it uses a
# fixed 64-bit timeout (0xFFFFFFFF_00000000). Otherwise it computes value * -10000 (a negative 100ns-
# tick relative time: 10000 ticks = 1 ms, so value ms). It then loops, calling
# __imp__KeDelayExecutionThread (a kernel delay, passing the 100ns-tick time and flag), until the flag
# byte (value & 0xFF of the flag arg) is 0. So it's a "delay for value milliseconds (polling a flag)"
# routine. 83 lines. 2 callers: ByteValueCheck_82CBC678 (sub_82CBC678) and sub_82CBC6B0. The flag's
# meaning (an "interrupted" / "abort" flag) isn't resolvable, so the name states the mechanism: delay
# for a duration in milliseconds.
[entrypoint.functions."0x82CC2028"]
name = "DelayMilliseconds_82CC2028"

# A 16-byte-stride array constructor (the sibling of ConstructArray4_823093F8): given a size, do an
# overflow check, run a setup step (sub_822F1F00), and allocate size * 16 bytes.

# Args: (r3), size (r4), (more). If size is 0, skips the setup. Otherwise it does a size-overflow check
# ((0xFFFFFFFF / size) >= 16 -- guarding against size * 16 overflowing 32 bits); if it passes, it calls
# sub_822F1F00 (a setup, with a global base). Then it allocates (size * 16) bytes via
# Allocate_OrDbgBreak_8221F388 (a 16-byte-stride array) and returns the allocation. So it's a "construct
# a 16-byte-stride array of the given size" routine. 66 lines. Many callers (sub_82358250, sub_82442A78,
# sub_82A97298, ...). A sibling of ConstructArray4_823093F8 (sub_823093F8, the 4-byte-stride version);
# the "16" marks this 16-byte variant. The array's element class (16-byte records) isn't resolvable,
# so the name states the mechanism: construct a 16-byte-stride array.
[entrypoint.functions."0x82512640"]
name = "ConstructArray16_82512640"

# A string insert / append: if len is -1, compute strlen(src); check the object's capacity (obj->6);
# then copy the string in (MemCpy) and update the region (ProcessIndexedRegion / ProcessIndexedRegion2).

# Args: obj (r3, ->0 = a data pointer, ->6 = a u16 capacity), src (r4), len (r5). If len is -1, it
# computes the length of src (a strlen: walk to the null byte). Otherwise it uses len as the length.
# It then compares the object's capacity (obj->6, a u16) to the length; if there's room, it copies the
# string (MemCpy_SdkRuntime_82CA2C60, sub_82CA2C60) and updates the indexed region (ProcessIndexedRegion
# _82BA9068, ProcessIndexedRegion2_82BA8F18, GetSingleton_82BA8718 -- the 0x82BA8 indexed-region
# subsystem). So it's a "insert / append a string into the region" routine. 135 lines. Many callers (all
# in the 0x82BA/0x82BB region: sub_82BBE220, sub_82BB0D78, sub_82BAA310, ...). The region object's
# meaning (a string buffer / log) isn't resolvable, so the name states the mechanism: insert a string.
[entrypoint.functions."0x82BA8850"]
name = "StringInsert_82BA8850"

# A value-set wrapper (around SetValue_8236FCE8): compute a 4-byte element count from the source, call
# SetValue, and check the result.

# Args: obj (r3), src (r4, ->4 = a start, ->8 = an end). Computes a 4-byte element count: ((src->8 -
# src->4) >> 2) (a byte span divided into 4-byte elements). It then calls SetValue_8236FCE8 (sub_8236FCE8,
# the value setter) with the count, and checks the result (a byte: if non-zero, it compares src->4 and
# src->8 and continues processing -- calling sub_82371378, a sibling in the 0x8236/0x8237 cluster). So it's
# a "set the value from the source array" routine (a thin wrapper around SetValue_8236FCE8). 105 lines.
# 7 callers (sub_82C67088, sub_82C67220, sub_82C673D8, sub_82370EF8, ...). The value's type and the src
# array's meaning aren't resolvable, so the name states the mechanism: set the value (the "2" is the
# series index, distinct from SetValue_8236FCE8).
[entrypoint.functions."0x8236EC48"]
name = "SetValue2_8236EC48"

# A list node splice (the sibling of List_SpliceNode_8292CC00): manipulate a doubly-linked list (via
# ->0 = prev, ->4 = next, ->8) to insert/unlink a node.

# Args: list (r3, ->4 = the head), node (r4, ->0 = prev, ->4 = next). Reads node->8 (the next node),
# then manipulates the linked list: it unlinks/links the node (updating the prev/next pointers of the
# neighbors, and checking a flag byte at offset 33 of a neighbor, ->33). It walks the list (comparing
# node->4 and neighbor->4 to node->0) and re-splices the nodes (updating ->0 / ->4). So it's a "splice a
# node into / out of the doubly-linked list" routine. 67 lines, no callees (pure data manipulation).
# Many callers: ConstructNodeValue_82B4C1C0 (sub_82B4C1C0), sub_82308D70, sub_82323550, sub_823B0DB8, ... A
# sibling of List_SpliceNode_8292CC00 (sub_8292CC00, the same doubly-linked-list splice shape); the "2"
# distinguishes the two. The node's class (->33 = a flag) and the list's meaning aren't resolvable, so
# the name states the mechanism: splice a list node.
[entrypoint.functions."0x8292CC88"]
name = "List_SpliceNode2_8292CC88"

# An array copy with refcounts: copy 4-byte-stride elements from src to dst, incrementing each
# element's refcount (atomic lwarx/stwcx under the global lock).

# Args: dst (r3, the destination), pos (r4), end (r5), src (r6, the source). If pos == end, returns
# (nothing to copy). Otherwise it walks the 4-byte-stride elements (dst->0 = the element, dst->4 = the
# element's refcount) and copies each from src to dst. For each non-null element (dst->4 != 0), it
# atomically increments the element's refcount (an lwarx/stwcx compare-and-swap under the global lock,
# retrying on contention). So it's a "copy an array of refcounted elements (taking a reference on each)"
# routine (a reference-counted array insert/copy). 69 lines, no callees (pure data manipulation with
# atomic refcount ops). Many callers (sub_8260B008, sub_827F1AB0, sub_82AB4058, ...). The element's class
# (a refcounted value, ->4 = the refcount) isn't resolvable, so the name states the mechanism: copy a
# refcounted array.
[entrypoint.functions."0x827F2010"]
name = "CopyRefcountedArray_827F2010"

# A hash-lookup + compare: look up a value (HashLookup_823F74D0), compare the string
# (CompareString_8229AD78), and handle the refcounted result (CopyAssign / Destruct), recursing as needed.

# Args: obj (r3, the hash table / container), value (r4). If obj is null, returns. Otherwise it looks up
# the value (HashLookup_823F74D0, sub_823F74D0 -- the hash-table get-or-create), then compares the
# string (CompareString_8229AD78, sub_8229AD78). Depending on the comparison result, it handles the
# refcounted object: it may copy-assign (CopyAssign_RefCounted_821F0108, sub_821F0108) or destructure
# (DestructRefCounted_82214F08, sub_82214F08) the value, and recurses (sub_823F9620, plus sub_823F8C30 and
# sub_823F8C90, siblings in the 0x823F9 cluster). So it's a "look up a value in the hash table and
# compare it" routine. 116 lines. 2 callers: sub_82817D28 and sub_828152D0. The table's key/value class
# (strings) and the exact match semantics aren't resolvable, so the name states the mechanism: hash-
# lookup and compare.
[entrypoint.functions."0x823F9620"]
name = "HashLookupCompare_823F9620"

# A float-data processor (the sibling of CompareFloatCounts_82A0B5C8): compute 16-byte-stride counts
# from the object, build a 3-float payload (Construct3FloatPayload_82A0C0A0) and 16-byte arrays
# (ConstructArray16_82512640), and free intermediates.

# Args: obj (r3, ->4 = a base, ->8 = an end, ->12 = another end), (r4), (r5), (r6). Calls
# Construct3FloatPayload_82A0C0A0 (sub_82A0C0A0, the 3-float payload builder). It then computes two
# 16-byte-stride counts: ((obj->12 - obj->4) >> 4) and ((obj->8 - obj->4) >> 4) (each >> 4 divides a byte
# span into 16-byte elements). It processes the float data, allocating 16-byte arrays (ConstructArray16
# _82512640, sub_82512640) and freeing intermediates (Free_SizeBucketed_Thunk_8221BE68, sub_8221BE68), and
# calling sub_82A0F950 and sub_82A97648 (siblings in the 0x82A0B/0x82A97 clusters). So it's a "process the
# float data (build the payload and arrays)" routine (part of the float-data / animation-data cluster).
# 447 lines. 1 caller: CompareFloatCounts_82A0B5C8 (sub_82A0B5C8). The data's class (animation/float
# samples) and the field semantics aren't resolvable, so the name states the mechanism: process the float
# data. The "2" distinguishes it from ProcessFloatData_832206E8 (sub_832206E8).
[entrypoint.functions."0x82A0BAA0"]
name = "ProcessFloatData2_82A0BAA0"

# A struct copy (a fourth sibling of CopyStructData_83235608 / CopyStructData2_82A1BD30 /
# CopyStructData3_8322EFB0): if src->20 >= 4, copy a variable-length byte array from src->12 (byte-by-
# byte to the stack) and advance src->12, then process further.

# Args: obj (r3), src (r4, ->4 = data, ->12 = a byte-array pointer, ->20 = a size). If src->20 < 4, takes
# a short path. Otherwise it copies (src->20 - 4) bytes from src->12 (a byte-by-byte loop to the stack),
# advances src->12 by 4, and continues processing (calling sub_82A1B480, the sibling in the 0x82A1B cluster
# that the other CopyStructData* variants also call). So it's a "copy the struct's data" routine. 345 lines.
# Many callers (sub_82B250E8, sub_82B20180, sub_82B31CA0, ...). The struct class and the exact field
# semantics aren't resolvable, so the name states the mechanism: copy the struct's data. The "4"
# distinguishes this from CopyStructData, CopyStructData2, and CopyStructData3 (the same shape).
[entrypoint.functions."0x82A1BEA8"]
name = "CopyStructData4_82A1BEA8"

# A virtual-method dispatch + position adjust: call a vtable method on a sub-object, conditionally
# insert (sub_82BCCB88), then adjust a position field (obj->16->68 += delta - pos).

# Args: obj (r3, ->16 = a sub-object), (r4), pos (r5), delta (r6). Reads the sub-object (obj->16) and
# calls one of its virtual methods (loading obj->16->12 as the method address and bctrl'ing, with
# obj->16->16 as a first arg). If the method returns non-zero and delta is non-zero, it inserts (sub
# _82BCCB88, an insert, passing obj and the constant 4). Then it adjusts a position field:
# obj->16->68 = obj->16->68 - pos + delta (a position shift). So it's a "dispatch a virtual method and
# adjust the position" routine (part of the 0x82BC state-dispatch / position subsystem). 60 lines, 1
# callee (sub_82BCCB88). Many callers, all in the state-dispatch cluster: StateDispatch_82BCBB40 (sub
# _82BCBB40), sub_82BDE878, sub_82BCD7B0, sub_82BC9FD0, ... The sub-object's class (->12 = a vtable
# method, ->16 = an arg, ->68 = a position) and the dispatch's meaning aren't resolvable, so the name
# states the mechanism: dispatch and adjust the position.
[entrypoint.functions."0x8227BB58"]
name = "DispatchAndAdjustPos_8227BB58"

# A trivial global-getter stub: return a pointer to a fixed global (0x82CCC8C0 + 19776 = 0x82D03880).

# Args: (none used). Loads the address 0x82D03880 (a fixed .data global) into r3 and returns. 2 lines.
# 0 direct callers (most likely a virtual method / a "get the global singleton" accessor). A sibling of
# GetGlobalPtr_82C27FA0, GetGlobalPtr2_82C23D00, GetGlobalPtr3_82240578, and GetGlobalPtr4_82C21AD0 (all
# trivial global-pointer getters). Notably, GetGlobalPtr4_82C21AD0 returns 0x82D0388C, just 12 bytes above
# this one -- so this is a getter for a neighboring field of the same global object. The global's meaning
# (a singleton, a type table, a global object) can't be determined from the body alone. The name states
# the mechanism: get a global pointer (the "5" is the series index).
[entrypoint.functions."0x82C1F258"]
name = "GetGlobalPtr5_82C1F258"

# A line parser (the 0x82B47 line-reader cluster): atomically increment a global line counter, parse the
# line (ParseLineValue_82B46D40), and handle the refcounted result.

# Args: obj (r3, the line reader), (r4), (more). Atomically increments a global counter (from a fixed
# .data address, 0x82CCC8C0 + 28344 -- a line/record counter, incremented via an lwarx/stwcx compare-and-
# swap under the global lock, retrying on contention). It then parses the line (ParseLineValue_82B46D40,
# sub_82B46D40) and handles the result: it constructs a refcounted object (ConstructRefCounted_8222CF18,
# sub_8222CF18) and destructures it on a path (DestructRefCounted_82214F08, sub_82214F08), and calls
# sub_82B47E40 (a further line helper). So it's a "parse a line (incrementing the line counter and
# building the value object)" routine (a log/property line reader). 162 lines. 5 callers (sub_828152D0,
# sub_829DD350, sub_825F5238, sub_82817D28, sub_82466098). The line's content (a log, a config, a save)
# isn't resolvable, so the name states the mechanism: parse a line.
[entrypoint.functions."0x82B47218"]
name = "ParseLine_82B47218"

# A virtual-method dispatch thunk: call the method at offset 36 of the sub-object (obj->0->36), passing
# (obj, arg).

# Args: obj (r3, ->0 = a sub-object), arg (r4). Reads the sub-object (*obj), loads its method pointer
# (sub-object->36, a vtable slot at offset 36), and indirect-calls it (bctrl) with (obj, arg), returning
# the result. So it's a "dispatch to the sub-object's slot-36 virtual method" thunk. 13 lines, no direct
# callees (the target is an indirect vcall). 1 caller: sub_82C14FD0. The sub-object's class (->36 = the
# vtable slot) and the method's behavior (it depends on the concrete object's vtable) aren't resolvable
# from this thunk alone, so the name states the mechanism: dispatch to slot 36 of the sub-object.
[entrypoint.functions."0x82C010C0"]
name = "DispatchSlot36_82C010C0"

# A virtual-method dispatch triple: call the sub-object's slot-24 method; if non-zero, call slot 36
# (DispatchSlot36_82C010C0); if still non-zero, call slot 28.

# Args: obj (r3, ->0 = a sub-object), (r4), arg (r5). Reads the sub-object (*obj) and calls its slot-24
# virtual method (obj->0->24, bctrl) with (obj, arg), checking the result (a byte). If non-zero, it calls
# DispatchSlot36_82C010C0 (sub_82C010C0, the slot-36 dispatch) with (obj, arg), checking that result too.
# If still non-zero, it calls another sub-object virtual method (obj->0->28, bctrl). So it's a "dispatch a
# triple of virtual methods (slots 24, 36, 28), each gated on the previous non-zero result" routine (a
# virtual callback chain on the sub-object). 86 lines, 1 callee (DispatchSlot36_82C010C0). 3 callers
# (sub_82C29550, sub_82C161D8, sub_82C1F380). The sub-object's class (->24, ->28, ->36 = vtable slots) and
# the methods' behavior (concrete-vtable-dependent) aren't resolvable, so the name states the mechanism:
# dispatch a triple of vcalls. Sibling of Dispatch_VcallSequence_82BCB6E0 / Dispatch_VcallSequence2_82BCCF50
# (vcall-sequence dispatchers); the "Triple" marks this 3-slot variant.
[entrypoint.functions."0x82C14FD0"]
name = "DispatchVcallTriple_82C14FD0"

# A virtual-method dispatch sequence: call the source sub-object's slot-20 and slot-0 methods, then run
# a sequence of vcall-chain / vcall-triple dispatches.

# Args: obj (r3, ->4), src (r4, ->0 = a sub-object), (more). Reads the source sub-object (*src) and calls
# its slot-20 virtual method (src->0->20, bctrl, with src), then its slot-0 method (src->0->0, bctrl, with
# src). It then runs a sequence of vcall dispatches, calling VcallTriple_AllSuccess_82C011E8 (sub_82C011E8)
# with (src, a global, obj->4, ...), and (later in the body) VcallChain_AllSuccess_82C14F38, VcallChain
# _AllSuccess_82C15068, DispatchVcallTriple_82C14FD0 (sub_82C14FD0), DestructRefCounted_82214F08 (sub
# _82214F08), plus sub_821F7798, sub_82BFD780, sub_82C14D70. So it's a "dispatch a long sequence of virtual
# methods (a vcall chain) on the source sub-object" routine (a virtual callback / visitor chain). 467 lines.
# 1 caller: sub_82C1FBD0. The sub-object's class (->0, ->20 = vtable slots) and the methods' behavior
# (concrete-vtable-dependent) aren't resolvable, so the name states the mechanism: dispatch a sequence of
# vcalls. Sibling of Dispatch_VcallSequence_82BCB6E0 / Dispatch_VcallSequence2_82BCCF50 (vcall-sequence
# dispatchers); the "3" marks this variant.
[entrypoint.functions."0x82C1F380"]
name = "DispatchVcallSequence3_82C1F380"

# A virtual-method dispatch sequence: call the sub-object's slot-24 method; if non-zero, dispatch the
# vcall sequence (DispatchVcallSequence3_82C1F380); if still non-zero, call slot 28.

# Args: obj (r3, ->0 = a sub-object), (r4), arg (r5). Reads the sub-object (*obj) and calls its slot-24
# virtual method (obj->0->24, bctrl) with (obj, arg), checking the result (a byte). If non-zero, it calls
# DispatchVcallSequence3_82C1F380 (sub_82C1F380, the long vcall-sequence dispatcher) with (obj, arg),
# checking that result too. If still non-zero, it calls another sub-object virtual method (obj->0->28,
# bctrl). So it's a "dispatch slot 24, then the vcall sequence, then slot 28 (each gated on the previous
# non-zero result)" routine (a virtual callback chain on the sub-object). 86 lines, 1 callee
# (DispatchVcallSequence3_82C1F380). 1 caller: sub_82C1FF48. A structural sibling of
# DispatchVcallTriple_82C14FD0 (sub_82C14FD0, same shape but calling slot 36 in the middle instead of the
# vcall sequence). The sub-object's class (->24, ->28 = vtable slots) and the methods' behavior (concrete-
# vtable-dependent) aren't resolvable, so the name states the mechanism: dispatch a sequence of vcalls.
[entrypoint.functions."0x82C1FBD0"]
name = "DispatchVcallSequence4_82C1FBD0"

# A virtual-method dispatch + array lookup: call the sub-object's slot-4 method, then read an element
# from an indexed array (obj->4[index]->4).

# Args: (r3), obj (r4, ->0 = a sub-object, ->4 = an indexed array), index (r5). Reads the sub-object
# (*obj) and calls its slot-4 virtual method (obj->0->4, bctrl, with obj and a global). It then does an
# indexed array lookup: r8 = obj->4 (the array base), r7 = index * 4, r6 = *(r8 + r7) (the array
# element), and returns *(r6 + 4) (the element's ->4 field). So it's a "dispatch a vcall, then read the
# ->4 of the array element at the given index" routine. 50 lines, no direct callees (the vcall target is
# indirect). 0 direct callers (reached via a dispatch). The sub-object's class (->4 = a vtable slot) and
# the array's element class (->4 = a field) aren't resolvable, so the name states the mechanism: dispatch
# and index the array.
[entrypoint.functions."0x82C1FEF0"]
name = "DispatchAndIndex_82C1FEF0"

# A struct initializer / constructor: zero the pointer fields, load float fields from globals, and set a
# global pointer.

# Args: obj (r3), (r4 = 2), (r5 = -1), (r6 = a global), (more). Initializes the struct: it sets the
# pointer fields to 0 (obj->4, ->8, ->12, ->16, ->20, ->24 = 0), loads float fields from fixed global
# .data addresses (obj->28, ->32, ->36, ->44, ->48 = global floats), and sets obj->0 to a global pointer.
# So it's an "initialize a struct (zero the pointers, fill the floats from globals, set the base pointer)"
# constructor. 83 lines, no callees (pure data setup). 2 callers: sub_82C1FF48 (the caller of
# DispatchVcallSequence4_82C1FBD0) and sub_82C1E878. The struct's class (a 52+ byte value with pointer and
# float fields) and the float fields' meaning aren't resolvable, so the name states the mechanism: init
# the struct.
[entrypoint.functions."0x82C4DAC8"]
name = "InitStruct_82C4DAC8"

# A dispose / destroy: call the sub-object dispose (sub_8233E380), free the sub-object (obj->4), and zero
# the fields.

# Args: obj (r3, ->4 = a sub-object, ->8 = a field). Calls sub_8233E380 (a dispose, passing obj, obj->4,
# and the sub-object's ->0 element) to run the sub-object's teardown. It then frees the sub-object (obj->4)
# via Free_SizeBucketed_Thunk_8221BE68 (sub_8221BE68). Finally it zeros the fields (obj->4 = 0, obj->8 = 0)
# to mark the object empty. So it's a "dispose / destroy the object (tear down the sub-object, free it,
# zero the pointers)" routine. 61 lines, 2 callees (sub_8233E380, Free_SizeBucketed_Thunk_8221BE68). Many
# callers (sub_82878DA8, sub_82851DF8, sub_8256EAF8, ...). The object's class (->4 = a sub-object, ->8 = a
# field) and the sub-object's teardown aren't resolvable, so the name states the mechanism: dispose the
# object. The "2" distinguishes it from Dispose_82B39A30 (sub_82B39A30).
[entrypoint.functions."0x8233DD70"]
name = "Dispose2_8233DD70"

# A dispose (with validation + chain-root lookup): validate the value (trap-22), compare it to the
# element, look up the chain root (FindChainRoot_82A596F0), and tear down.

# Args: obj (r3), value (r4), (r5), (r6), (r7), element (r8). Validates the value: if value is 0 or
# value != element, it traps (trap-22, the same invariant the FindChainRoot family checks). It then
# compares the element to value (bailing on a mismatch) and tears down: it looks up the chain root
# (FindChainRoot_82A596F0, sub_82A596F0), and calls sub_8234C988 and sub_826A4C60 (siblings in the
# 0x8233/0x8234/0x826A clusters) to run the teardown. So it's a "dispose the sub-object (validate, find
# the chain root, tear down)" routine. 137 lines, 3 callees (FindChainRoot_82A596F0, sub_8234C988,
# sub_826A4C60). Many callers: Dispose2_8233DD70 (sub_8233DD70), sub_8322BED0, sub_8268BBA0, ... The
# object's class and the teardown's steps aren't fully resolvable, so the name states the mechanism:
# dispose the sub-object. The "3" distinguishes it from Dispose_82B39A30 and Dispose2_8233DD70.
[entrypoint.functions."0x8233E380"]
name = "Dispose3_8233E380"

# A tree-node destroy wrapper: call DestroyTreeNodes_82859340 on the sub-object's nodes, then clean up
# the object's fields.

# Args: obj (r3, ->4 = a sub-object, ->8 = a field). Calls DestroyTreeNodes_82859340 (sub_82859340, the
# tree-node destroy, passing obj->4->4) to tear down the tree nodes. It then cleans up the object's
# fields: obj->4->4 = obj->4, obj->8 = 0, obj->4->0 = obj->4, obj->4->8 = obj->4 (setting the sub-object's
# pointers to itself / zeroing, a teardown/cleanup pattern). So it's a "destroy the tree nodes and clean
# up the object" routine. 50 lines, 1 callee (DestroyTreeNodes_82859340). 3 callers: Dispose3_8233E380
# (sub_8233E380), sub_827CCDD0, sub_82878F08. The tree's node class (->4 = the nodes) and the cleanup's
# semantics aren't resolvable, so the name states the mechanism: destroy the tree nodes. The "2"
# distinguishes it from DestroyTreeNodes_82859340 (the actual node-destroy) and DestroyTree_Recursive_82B4D218.
[entrypoint.functions."0x8234C988"]
name = "DestroyTreeNodes2_8234C988"

# A 20-byte node constructor: allocate 20 bytes, set up the node (zero the pointer fields, set the flag
# bytes ->16 = 1 and ->17 = 0), and store it in obj->4.

# Args: obj (r3, ->4 = a node slot). Allocates 20 bytes (Allocate_OrDbgBreak_8221F388, sub_8221F388), then
# sets up the node: zeroes the pointer fields (->0, ->4, ->8 = 0), sets a flag byte to 1 (->16) and another
# to 0 (->17), and stores the node pointer in obj->4. So it's a "construct a 20-byte node and store it in
# the object" routine. 83 lines, 1 callee (Allocate_OrDbgBreak_8221F388). Many callers (sub_82474760,
# sub_8249C690, sub_825C0658, ...). The node's class (a 20-byte record, ->16 = a flag, ->17 = a flag) and
# its role (a list/tree node) aren't resolvable, so the name states the mechanism: construct a 20-byte
# node. The "20" marks the node size (20 bytes), distinguishing it from other node constructors.
[entrypoint.functions."0x82A12B08"]
name = "ConstructNode20_82A12B08"

# A struct copy (a fifth sibling of CopyStructData_83235608 / CopyStructData2_82A1BD30 / CopyStructData3
# _8322EFB0 / CopyStructData4_82A1BEA8): if obj->20 >= 4, copy a variable-length byte array from obj->12
# (byte-by-byte to the stack) and advance obj->12, then process further.

# Args: obj (r3, ->4 = data, ->12 = a byte-array pointer, ->20 = a size), (r4). If obj->20 < 4, takes a
# short path. Otherwise it copies (obj->20 - 4) bytes from obj->12 (a byte-by-byte loop to the stack),
# advances obj->12 by 4, and continues processing (calling sub_82A1B480, the sibling in the 0x82A1B cluster
# that the other CopyStructData* variants also call). So it's a "copy the struct's data" routine. 122
# lines. 3 callers (sub_83225E80, sub_83218300, sub_83225FD0). The struct class and the exact field
# semantics aren't resolvable, so the name states the mechanism: copy the struct's data. The "5"
# distinguishes this from CopyStructData, CopyStructData2, CopyStructData3, and CopyStructData4 (the same
# shape).
[entrypoint.functions."0x83217EC8"]
name = "CopyStructData5_83217EC8"

# A chain validation + find/remove: validate the pointers (trap-22), compare the node to the value,
# then look up the chain root (FindChainRoot2_823D4F20) and find/remove the node (Tree_FindAndRemove_82A14AE0).

# Args: obj (r3, ->12 = a sub-pointer, ->16 = a node). Validates the pointers (a trap-22 if a pointer is
# null or mismatched -- the same invariant the FindChainRoot family checks). It compares the node (obj->16)
# to a value, and on a match tears down / finds / removes: it looks up the chain root (FindChainRoot2_823D4F20,
# sub_823D4F20), finds and removes the node (Tree_FindAndRemove_82A14AE0, sub_82A14AE0), and destroys the
# list (DestroyList_Recursive_82443440, sub_82443440). So it's a "validate the node, then find/remove it from
# the tree" routine. 120 lines, 3 callees (FindChainRoot2_823D4F20, Tree_FindAndRemove_82A14AE0,
# DestroyList_Recursive_82443440). 2 callers: sub_83218C10 and sub_83219DF8. The node's class (->16 = a node,
# ->12 = a sub-pointer) and the tree's meaning aren't resolvable, so the name states the mechanism: validate
# and find/remove the node.
[entrypoint.functions."0x83218350"]
name = "ValidateAndFindRemove_83218350"

# A tree-node processor (a large routine, 1310 lines): validate the node (ValidateAndFindRemove_83218350),
# copy its struct data, construct 20-byte nodes, handle the refcounted value, and update the tree-node value.

# Args: obj (r3), value (r4, ->4 = data, ->12 = a byte-array pointer, ->20 = a size), (r5, r6, ... flags). It
# calls ValidateAndFindRemove_83218350 (sub_83218350, the validate + find/remove) first. It then does a struct
# copy (from value->12, the same shape as the CopyStructData* family -- calling sub_82A1B480), constructs
# 20-byte nodes (Construct20Byte_83215640, sub_83215640), handles the refcounted value (Release_RefCounted
# _829FF648, SetRefcountedWrapper_8238A848), and updates the tree-node value (UpdateTreeNodeValue_83218598,
# sub_83218598). It also calls a long sequence of sibling functions (sub_83215768 through sub_83215DF0, the
# 0x83215 cluster) and Allocate_OrDbgBreak_8221F388. So it's a "process / update a tree node (validate, copy,
# construct, and update)" routine (the big tree-node processor of the 0x83215/0x83218 cluster). 1310 lines --
# one of the largest functions in the recompile. 0 direct callers (reached via a dispatch / vcall). The node's
# class (->4, ->12, ->20 = data/size) and the tree's meaning (likely a game data structure: an animation/prop
# tree) aren't resolvable, so the name states the mechanism: process the tree node.
[entrypoint.functions."0x83218C10"]
name = "ProcessTreeNode_83218C10"

# A trivial field getter: return *(r4 + 12) -- the field at offset 12.

# Args: (r3), obj (r4). Loads the 32-bit field at offset 12 of obj (r4) and returns it. 8 lines (no
# callees). 0 direct callers (most likely a virtual method / accessor reached via a vtable, e.g. a "get
# field 12" getter). The field's meaning (a size, a count, a pointer, a type) can't be determined from the
# body alone -- only that it's a 32-bit field at offset 12. A sibling of other trivial field getters
# (GetField4_8320F978, GetField48_82C6F840; the adjacent sub_82284200 returns field 24). The name states
# the mechanism: get the field at offset 12.
[entrypoint.functions."0x82283EC8"]
name = "GetField12_82283EC8"

# A lookup + dispatch: look up a value (sub_823FE8F8), set a flag (0/1), and dispatch a virtual method
# (value->0->32).

# Args: value (r3), flag (r4), (more). Loads a global (from a fixed .data address) and, if flag is 0, looks
# up the value (sub_823FE8F8, a lookup). It sets a result flag (0 or 1, based on whether the lookup found
# something). If the flag is set (non-zero), it dispatches a virtual method on the value (loading value->0->32
# as the method address and bctrl'ing). So it's a "look up the value, and if found, dispatch its virtual
# method" routine (part of the 0x823F8/0x823FE hash/lookup cluster). 93 lines, 1 callee (sub_823FE8F8). 1
# caller: HashLookupCompare_823F9620 (sub_823F9620). The value's class (->0->32 = a vtable slot) and the
# lookup's meaning aren't resolvable, so the name states the mechanism: look up and dispatch.
[entrypoint.functions."0x823F8C90"]
name = "LookupAndDispatch_823F8C90"

# A virtual-method dispatch sequence: call VcallTriple_AllSuccess_82C011E8 repeatedly (with a different
# global base each time), gated on a non-zero result.

# Args: obj (r3, ->4), arg (r4). Calls VcallTriple_AllSuccess_82C011E8 (sub_82C011E8, the 3-slot vcall
# dispatcher) with (arg, a global base, obj), checking the result (a byte). If non-zero, it calls it again
# (with a different global base and obj->4), checking the result too, and continues the sequence (the body
# repeats this pattern with several different global bases). So it's a "dispatch a sequence of vcall
# triples (each with a different global base), gated on the previous non-zero result" routine (a virtual
# callback / visitor chain on the argument). 92 lines, 1 callee (VcallTriple_AllSuccess_82C011E8). 1 caller:
# sub_82C26AF0. The argument's class and the vcall targets' behavior (concrete-vtable-dependent) aren't
# resolvable, so the name states the mechanism: dispatch a sequence of vcalls. Sibling of
# Dispatch_VcallSequence_82BCB6E0, Dispatch_VcallSequence2_82BCCF50, DispatchVcallSequence3_82C1F380, and
# DispatchVcallSequence4_82C1FBD0; the "5" marks this variant.
[entrypoint.functions."0x82C269E8"]
name = "DispatchVcallSequence5_82C269E8"

# A virtual-method dispatch sequence: call the sub-object's slot-24 method; if non-zero, dispatch the vcall
# sequence (DispatchVcallSequence5_82C269E8); if still non-zero, call slot 28.

# Args: obj (r3, ->0 = a sub-object), (r4), arg (r5). Reads the sub-object (*obj) and calls its slot-24
# virtual method (obj->0->24, bctrl) with (obj, arg), checking the result (a byte). If non-zero, it calls
# DispatchVcallSequence5_82C269E8 (sub_82C269E8, the repeated VcallTriple_AllSuccess dispatcher) with (obj,
# arg), checking that result too. If still non-zero, it calls another sub-object virtual method (obj->0->28,
# bctrl). So it's a "dispatch slot 24, then the vcall sequence, then slot 28 (each gated on the previous
# non-zero result)" routine (a virtual callback chain on the sub-object). 86 lines, 1 callee
# (DispatchVcallSequence5_82C269E8). 1 caller: sub_82C26CA0. A structural sibling of DispatchVcallTriple
# _82C14FD0 and DispatchVcallSequence4_82C1FBD0 (same shape: slot 24, a middle dispatcher, slot 28). The
# sub-object's class (->24, ->28 = vtable slots) and the methods' behavior (concrete-vtable-dependent)
# aren't resolvable, so the name states the mechanism: dispatch a sequence of vcalls. The "6" marks this
# variant in the DispatchVcallSequence family.
[entrypoint.functions."0x82C26AF0"]
name = "DispatchVcallSequence6_82C26AF0"

# A 24-byte array copy (the plain, non-refcounting sibling of CopyRefcountedArray_827F2010): copy
# 24-byte-stride elements from src to dst (a straight memcpy-style element copy, no refcount increments).

# Args: dst (r3, the destination), pos (r4), end (r5), src (r6, the source). If pos == end, returns
# (nothing to copy). Otherwise it walks the 24-byte-stride elements, copying each one (a 24-byte element:
# six 32-bit words plus some float fields, copied word-by-word / float-by-float) from src to dst. Unlike
# CopyRefcountedArray_827F2010 (sub_827F2010, the 4-byte element copy that takes a reference on each
# element), this one does a plain copy (no refcount increments). So it's a "copy a 24-byte array of
# elements (plain, no refcounting)" routine. 82 lines, no callees (pure data copy). 1 caller: sub_82A13D30.
# The element's class (a 24-byte record: pointers + floats) isn't resolvable, so the name states the
# mechanism: copy a 24-byte array. The "24" marks the element size (24 bytes).
[entrypoint.functions."0x82A13FE0"]
name = "CopyArray24_82A13FE0"

# A dispose / destroy (with an optional free): set a vtable / type pointer, run the 20-byte destructor
# (Destruct20Byte_832156E0), and free the object (Free_SizeBucketed_Thunk_8221BE68) if a flag is set.

# Args: obj (r3, ->0 = a vtable / type pointer), flag (r4, bit 0 = an "owned / free" flag). Sets obj->0 to a
# fixed global (a vtable / type pointer, 0x82CCC8C0 + 7072). It then runs the 20-byte destructor
# (Destruct20Byte_832156E0, sub_832156E0). If the flag's bit 0 is set (the object is owned), it frees the
# object (Free_SizeBucketed_Thunk_8221BE68, sub_8221BE68); otherwise it leaves the memory (a
# destruct-but-don't-free path). So it's a "dispose the object (set the type, run the destructor, free if
# owned)" routine. 56 lines, 2 callees (Destruct20Byte_832156E0, Free_SizeBucketed_Thunk_8221BE68). 0 direct
# callers (reached via a dispatch / vcall, likely a type's destructor or dispose method). The object's class
# (a 20-byte object, ->0 = a vtable) and the free flag's meaning aren't resolvable, so the name states the
# mechanism: dispose the object (with an optional free). The "4" distinguishes it from Dispose_82B39A30,
# Dispose2_8233DD70, and Dispose3_8233E380.
[entrypoint.functions."0x82A146C0"]
name = "Dispose4_82A146C0"

# A struct copy (a sixth sibling of the CopyStructData family): if src->20 >= 4, copy a variable-length
# byte array from src->12 (byte-by-byte to the stack) and advance src->12, then process further.

# Args: obj (r3), src (r4, ->4 = data, ->12 = a byte-array pointer, ->20 = a size), (r5). If src->20 < 4,
# takes a short path. Otherwise it copies (src->20 - 4) bytes from src->12 (a byte-by-byte loop to the
# stack), advances src->12 by 4, and continues processing (calling sub_82A1B480, the sibling in the 0x82A1B
# cluster that the other CopyStructData* variants also call, plus sub_83211D48, a sibling in the 0x83211
# cluster). So it's a "copy the struct's data" routine. 128 lines. 0 direct callers (reached via a dispatch
# / vcall). The struct class and the exact field semantics aren't resolvable, so the name states the
# mechanism: copy the struct's data. The "6" distinguishes this from CopyStructData, CopyStructData2, 3, 4,
# and 5 (all the same shape).
[entrypoint.functions."0x83215018"]
name = "CopyStructData6_83215018"

# A 20-byte node constructor: call Construct20Byte_83215640 (the base 20-byte constructor), then set a
# vtable / type pointer and a float field.

# Args: obj (r3, ->0 = a vtable / type pointer, ->16 = a float). Calls Construct20Byte_83215640 (sub_83215640,
# the base 20-byte constructor) to allocate and zero the 20-byte node. It then sets the node's vtable / type
# pointer (obj->0 = a fixed global, 0x82CCC8C0 + 7072 -- the same type pointer Dispose4_82A146C0 uses) and a
# float field (obj->16 = a fixed global float). So it's a "construct a 20-byte node and set its type and a
# default float" routine (a typed 20-byte node constructor, part of the 0x83215 cluster). 48 lines, 1 callee
# (Construct20Byte_83215640). 3 callers: ProcessTreeNode_83218C10 (sub_83218C10), sub_83219588, sub_83218630.
# The node's class (a 20-byte typed record, ->0 = a type, ->16 = a float) isn't resolvable, so the name
# states the mechanism: construct a 20-byte node. Sibling of ConstructNode20_82A12B08 (sub_82A12B08, the
# plain 20-byte node); this one adds a type and a float.
[entrypoint.functions."0x83215D98"]
name = "Construct20ByteNode_83215D98"

# A node insert (with a chain lookup): if the node is empty, insert it (sub_82551330); otherwise look up
# the chain root (FindChainRoot2_823D4F20) and process the node.

# Args: obj (r3, the container), node (r4, ->4 = data, ->8 = a "filled" field), (r5, r6 = args). If node->8
# is 0 (the node is empty / not yet filled), it inserts the node (sub_82551330, an insert, passing obj,
# node->4, 1, and the args) and returns obj. Otherwise it processes the node: it looks up the chain root
# (FindChainRoot2_823D4F20, sub_823D4F20) and calls sub_82498700 and sub_825511F0 (siblings in the 0x8249/0x8255
# cluster). So it's a "insert the node if empty, otherwise find the chain root and process" routine (a
# node insertion with a chain lookup, part of the 0x8255 chain/lookup cluster). 306 lines, 4 callees (sub
# _82551330, FindChainRoot2_823D4F20, sub_82498700, sub_825511F0). 1 caller: ChainLookup_82550638 (sub_82550638).
# The node's class (->4 = data, ->8 = a filled flag) and the container's meaning aren't resolvable, so the
# name states the mechanism: insert the node (with a chain lookup).
[entrypoint.functions."0x82550F90"]
name = "InsertNodeChain_82550F90"

# A node insert with an overflow check: check a bound (node->8), raise an error if it's below the bound
# (an overflow), otherwise allocate and splice the node in (List_SpliceNode_827C6448).

# Args: obj (r3, the container), node (r4, ->8 = a size/counter), (r5 = a flag), (r6, r7 = args). Checks a
# bound: if node->8 is below a fixed limit (0x55B4555C, a high water mark), it builds an error message
# (SetString_822F2020, a global error string, plus sub_826C3EF0 and sub_826C3FA8, format ops) and raises it
# (sub_822F1F00, the exception raiser -- the same pattern as RaiseIfOutOfRange_82461DA0). Otherwise it
# inserts: it allocates (Allocate_OrDbgBreak_8221F388, sub_8221F388) and splices the node in
# (List_SpliceNode_827C6448, sub_827C6448, plus sub_827C64C0, a sibling). So it's a "check the overflow
# bound, raise on overflow, and insert the node" routine (a guarded node insert, part of the 0x8255
# chain/insert cluster). 381 lines, 7 callees (List_SpliceNode_827C6448, Allocate_OrDbgBreak_8221F388,
# SetString_822F2020, sub_822F1F00, ...). 4 callers: InsertNodeChain_82550F90 (sub_82550F90), sub_825511F0,
# sub_8267F338, sub_8247D6C8. The node's class (->8 = a size) and the container's meaning aren't resolvable,
# so the name states the mechanism: insert with an overflow check.
[entrypoint.functions."0x82551330"]
name = "InsertWithOverflowCheck_82551330"

# A chain traversal / find: walk a chain (via node->4, node->8, node->12, node->25) and find the node
# whose ->12 field matches / is at the given value.

# Args: obj (r3, ->4 = a start, ->8 = a node), value (r4). Reads the start node (obj->8) and walks the chain:
# for each node, it checks a flag (node->25); while the flag is clear, it compares the node's ->12 field to
# value (bailing when node->12 >= value), and advances to the next node (node->8). On a match / at the
# end, it returns the found node (and compares it to the object's ->4). So it's a "walk the chain to find
# the node at / for the given value" routine (a linear chain search keyed on node->12). 100 lines, no callees
# (pure data manipulation). Many callers (sub_83221A60, sub_82A0D630, sub_82A0C960, ...). The node's class
# (->12 = a key, ->25 = a flag, ->8 = next, ->4 = start) and the chain's meaning (a sorted/ordered list) aren't
# resolvable, so the name states the mechanism: walk the chain to find the node.
[entrypoint.functions."0x83239FA8"]
name = "WalkChainFind_83239FA8"

# A chain-lookup + assign: look up the value (ChainLookup_82550638), release the old value if it changed
# (Release_RefCounted_829FF648), assign the new value (src->0, src->4), and take a reference on it.

# Args: obj (r3, ->4 = a value slot), src (r4, ->0 = a new value, ->4 = a new value's refcount). Looks up
# the value (ChainLookup_82550638, sub_82550638, passing obj+4 and src->0->4). If the looked-up value's ->4
# differs from src->4, it releases the old value (Release_RefCounted_829FF648, sub_829FF648), assigns the new
# value (result->0 = src->0, result->4 = src->4), and takes a reference on the new value (if src->4 is non-
# null, it increments its refcount). So it's a "look up the value, and assign the new one (releasing the old
# and refcounting the new)" routine (a reference-counted value assignment via a chain lookup, part of the
# 0x8323A cluster). 79 lines, 2 callees (ChainLookup_82550638, Release_RefCounted_829FF648). 4 callers
# (sub_83225E00, sub_83227478, sub_83225538, sub_832259A0). The value's class (->0 = the value, ->4 = the
# refcount) isn't resolvable, so the name states the mechanism: look up and assign.
[entrypoint.functions."0x8323A1E8"]
name = "LookupAndAssign_8323A1E8"

# A flag-bit test: AND a flag word (obj->28) with a mask, count leading zeros, and return a (inverted) bit
# derived from the count.

# Args: (r3), obj (r4, ->28 = a u16 flag word), mask (r5). Loads the flag word (obj->28) and ANDs it with the
# mask. It then counts the leading zeros of the result (cntlzw) and extracts bit 5 of that count (the
# clz result shifted right by 5, masked to a single bit), and inverts it (xori with 1) to return the result
# (0 or 1). So it's a "test a flag bit (derive a boolean from the masked flag word's bit count)" routine. 16
# lines, no callees (pure bit manipulation). 0 direct callers (reached via a dispatch / vcall). The flag
# word's meaning (->28 = a u16 of flags) and the mask's meaning (a category / type) aren't resolvable, so
# the name states the mechanism: test a flag bit.
[entrypoint.functions."0x821D2C58"]
name = "TestFlagBit_821D2C58"

# A size-bucketed pool allocator: check the size against thresholds (64, 256, ...), and acquire a node
# from a pool (Pool_AcquireNode_83230688); free via Pool_FreeByAddress_832304E8 on a path.

# Args: (r3), (r4), (r5), size (r6), (more). Loads a global pool base (from a fixed .data address). If size
# is 0, returns. It then checks the size against thresholds: if size <= 64, it acquires a node from the pool
# (loading the pool's freelist and calling Pool_AcquireNode_83230688, sub_83230688); if size <= 256, it takes
# another path (a different pool / bucket); otherwise it takes the large-allocation path (calling sub_83231518
# and sub_83231590). It also frees a node (Pool_FreeByAddress_832304E8, sub_832304E8) and copies data
# (MemCpy_SdkRuntime_82CA2C60, sub_82CA2C60) in some paths. So it's a "size-bucketed pool allocation" routine
# (allocate a block of the given size from a pool, choosing the bucket by size). 171 lines, 5 callees (Pool
# _AcquireNode_83230688, Pool_FreeByAddress_832304E8, MemCpy_SdkRuntime_82CA2C60, sub_83231518, sub_83231590).
# 1 caller: GuardDispatch_8227CB70 (sub_8227CB70). The pool's class (a size-bucketed freelist) and the size
# thresholds' meaning aren't resolvable, so the name states the mechanism: allocate from the pool (size-
# bucketed).
[entrypoint.functions."0x8240DAA8"]
name = "PoolAllocate_8240DAA8"

# A range / region update: validate a value against the region's bounds (obj->16 to obj->4 + obj->20), and
# update the region (if in range) or reset it (if out of range / empty).

# Args: obj (r3, ->4 = a base, ->12 = a count, ->16 = a min, ->20 = a size), value (r4). If obj->12 is 0
# (the region is empty), or value < obj->16, or value > obj->4 + obj->20 (out of bounds), it resets the
# region (obj->4 = value, obj->12 = 0, obj->16 = 0, obj->20 = 0). Otherwise it updates the region:
# obj->4 = value (the new base), obj->20 = (obj->4 + obj->20) - value (the adjusted size), and
# obj->12 = (obj->12 - obj->4) + value (the adjusted count). So it's a "update the region to the new base
# (if the value is in range), else reset it" routine (a region/range update with bounds). 53 lines, no callees
# (pure data manipulation). 4 callers (sub_82B28398, sub_822C6DE8, sub_82A1CDD8, sub_82B7E050). The region's
# meaning (a buffer/region with a base, size, min, and count) isn't resolvable, so the name states the
# mechanism: update the region.
[entrypoint.functions."0x82175DE0"]
name = "UpdateRegion_82175DE0"

# A dispose / destroy (with an optional free) -- the sibling of Dispose4_82A146C0 (a different type
# pointer): set a vtable / type pointer, run the 20-byte destructor (Destruct20Byte_832156E0), and free the
# object (Free_SizeBucketed_Thunk_8221BE68) if a flag is set.

# Args: obj (r3, ->0 = a vtable / type pointer), flag (r4, bit 0 = an "owned / free" flag). Sets obj->0 to a
# fixed global (a vtable / type pointer, 0x82CCC8C0 + 7096 -- a different type than Dispose4_82A146C0's 7072).
# It then runs the 20-byte destructor (Destruct20Byte_832156E0, sub_832156E0). If the flag's bit 0 is set
# (the object is owned), it frees the object (Free_SizeBucketed_Thunk_8221BE68, sub_8221BE68); otherwise it
# leaves the memory (a destruct-but-don't-free path). So it's a "dispose the object (set the type, run the
# destructor, free if owned)" routine. 56 lines, 2 callees (Destruct20Byte_832156E0, Free_SizeBucketed_Thunk
# _8221BE68). 0 direct callers (reached via a dispatch / vcall, likely a type's destructor / dispose method).
# A structural sibling of Dispose4_82A146C0 (sub_82A146C0) -- same shape, different type pointer (7096 vs
# 7072). The object's class (a 20-byte object, ->0 = a vtable) and the free flag's meaning aren't resolvable,
# so the name states the mechanism: dispose the object (with an optional free). The "5" distinguishes it from
# Dispose_82B39A30, Dispose2_8233DD70, Dispose3_8233E380, and Dispose4_82A146C0.
[entrypoint.functions."0x82A14720"]
name = "Dispose5_82A14720"

# A struct copy (a seventh sibling of the CopyStructData family, and one of the largest -- 804 lines): if
# src->20 >= 4, copy a variable-length byte array from src->12 (byte-by-byte to the stack) and advance
# src->12, then process further.

# Args: obj (r3), src (r4, ->4 = data, ->12 = a byte-array pointer, ->20 = a size), (r5). If src->20 < 4,
# takes a short path. Otherwise it copies (src->20 - 4) bytes from src->12 (a byte-by-byte loop to the stack),
# advances src->12 by 4, and continues processing (calling sub_82A1B480, the sibling in the 0x82A1B cluster
# that the other CopyStructData* variants also call, plus sub_83211D48, a sibling in the 0x83211 cluster). So
# it's a "copy the struct's data" routine. 804 lines -- one of the largest functions in the recompile (the
# body has a long tail of additional processing beyond the initial copy). 0 direct callers (reached via a
# dispatch / vcall). The struct class and the exact field semantics aren't resolvable, so the name states the
# mechanism: copy the struct's data. The "7" distinguishes this from CopyStructData, CopyStructData2, 3, 4,
# 5, and 6 (all the same shape).
[entrypoint.functions."0x83214990"]
name = "CopyStructData7_83214990"

# A node constructor (the sibling of Construct20ByteNode_83215D98): call Construct20Byte_83215640 (the base
# 20-byte constructor), then set a vtable / type pointer, some counter fields (to 1), and float fields from
# globals.

# Args: obj (r3, ->0 = a vtable / type pointer, ->16, ->20, ->24, ->28, ->32, ->36, ->37, ->40). Calls
# Construct20Byte_83215640 (sub_83215640, the base 20-byte constructor) to allocate and zero the node. It then
# sets the node's vtable / type pointer (obj->0 = a fixed global, 0x82CCC8C0 + 7096 -- the same type pointer
# Dispose5_82A14720 uses), counter fields (obj->20 = 1, obj->24 = 1, obj->16 = 0, obj->28 = 0, obj->36 = 0,
# obj->37 = 0), and float fields (obj->32 and obj->40 = a fixed global float). So it's a "construct a 20-byte
# node and set its type, counters, and default floats" routine (a typed node constructor, part of the 0x83215
# cluster). 67 lines, 1 callee (Construct20Byte_83215640). 3 callers: ProcessTreeNode_83218C10 (sub_83218C10),
# sub_83218630, sub_83219588. A sibling of Construct20ByteNode_83215D98 (sub_83215D98) -- same base
# (Construct20Byte), different type / field layout. The node's class (a 20-byte typed record) isn't
# resolvable, so the name states the mechanism: construct a 20-byte node. The "2" distinguishes it from
# Construct20ByteNode_83215D98.
[entrypoint.functions."0x83215D18"]
name = "Construct20ByteNode2_83215D18"

# A hash computation: compute a hash of the data (a loop mixing the data's bytes with a seed), then use the
# hash (via obj->16).

# Args: obj (r3, ->16 = a hash-table / bucket), len (r4), seed (r5), data (r6), (more). Computes a hash of the
# data: it walks the data bytes (a loop, mixing each byte with the seed via a sequence of shifts, adds, and
# XORs -- r6 = r6 ^ (byte + (seed >> 5) + (seed >> 30))), producing a 32-bit hash. It then uses the hash: it
# loads obj->16 (a hash-table / bucket base) and its ->0 and ->8 fields, and (later in the body) calls sub
# _82BCD7B0 (a sibling in the 0x82BC cluster) to look up / insert the hashed entry. So it's a "compute a hash
# of the data and use it (a hash-table lookup/insert)" routine. 164 lines, 1 callee (sub_82BCD7B0). Many
# callers (sub_82BDDB88, sub_8219CDA8, sub_82BC7328, ...). The data's type (a string / buffer) and the hash
# table's meaning aren't resolvable, so the name states the mechanism: compute a hash.
[entrypoint.functions."0x8227BA30"]
name = "ComputeHash_8227BA30"

# A hash-table lookup: compute a bucket (a bit index + a mask), walk the bucket's chain, and return the
# entry whose key matches (or a default).

# Args: table (r3, ->7 = a bit index, ->16 = the bucket base), key (r4, ->8 = a bitmask / key), (more). Computes
# a bucket index: it uses the table's bit index (table->7) to form a mask (1 << index), ANDs it with the key's
# bitmask (key->8), and indexes into the table (bucket = table->16 + (mask + mask * 4)). It then walks the
# bucket's chain (a linked list via ->16): for each entry, if entry->12 == 4 (a type / size marker) and entry->8
# == key (a key match), it returns the entry. Otherwise it advances to the next entry (entry->16). If it
# reaches the end (null), it returns a default (a fixed global). So it's a "look up a key in the hash table
# (a bucketed chain search)" routine. 52 lines, no callees (pure data manipulation). Many callers (sub_82BCC038,
# sub_82BCCFF8, sub_82BCA948, ...). The table's key/value class (->8 = a key, ->16 = next, ->12 = a marker) isn't
# resolvable, so the name states the mechanism: hash-table find. The "3" distinguishes it from HashTable_Find
# _829FC630 and HashTable_Find2_829FC630.
[entrypoint.functions."0x821CF1D0"]
name = "HashTable_Find3_821CF1D0"

# A value-type dispatch: dispatch on the value's type field (obj->4) and handle the different value types
# (integers, floats, hash-table lookups, switch dispatches).

# Args: (r3), obj (r4, ->0 = a value, ->4 = a type), (more). Dispatches on the value's type (obj->4):
# - 0: a default / null path
# - 3: a float -- it converts obj->0 (a float) to an integer (fctiwz, the float-to-int conversion) and uses
#   it
# - 4: a keyed lookup -- it calls HashTable_Find3_821CF1D0 (sub_821CF1D0) with the value
# - otherwise: another path (calling SwitchDispatch_82BC8260, sub_82BC8260, SwitchDispatch_82BC9B28, sub
#   _82BC9B28, and sub_821E1498 in the various branches)

# So it's a "dispatch on a value's type and process it" routine (a value handler for a heterogeneous value
# type -- integers, floats, and keyed lookups). 139 lines, 4 callees (HashTable_Find3_821CF1D0, SwitchDispatch
# _82BC8260, SwitchDispatch_82BC9B28, sub_821E1498). Many callers (sub_82BD0A78, sub_82BC6F70, sub_82BB9C50,
# ...). The value's class (->0 = the value, ->4 = the type, a tagged union) isn't resolvable, so the name
# states the mechanism: dispatch on the value's type.
[entrypoint.functions."0x821CF230"]
name = "DispatchValue_821CF230"

# A dispose / destroy (with an optional free) -- the sibling of Dispose4_82A146C0 and Dispose5_82A14720 (a
# different type pointer, 7120): set a vtable / type pointer, run the 20-byte destructor (Destruct20Byte
# _832156E0), and free the object (Free_SizeBucketed_Thunk_8221BE68) if a flag is set.

# Args: obj (r3, ->0 = a vtable / type pointer), flag (r4, bit 0 = an "owned / free" flag). Sets obj->0 to a
# fixed global (a vtable / type pointer, 0x82CCC8C0 + 7120 -- a different type than Dispose4_82A146C0's 7072
# and Dispose5_82A14720's 7096). It then runs the 20-byte destructor (Destruct20Byte_832156E0, sub_832156E0).
# If the flag's bit 0 is set (the object is owned), it frees the object (Free_SizeBucketed_Thunk_8221BE68, sub
# _8221BE68); otherwise it leaves the memory (a destruct-but-don't-free path). So it's a "dispose the object
# (set the type, run the destructor, free if owned)" routine. 56 lines, 2 callees (Destruct20Byte_832156E0, Free
# _SizeBucketed_Thunk_8221BE68). 0 direct callers (reached via a dispatch / vcall, likely a type's destructor /
# dispose method). A structural sibling of Dispose4_82A146C0 (sub_82A146C0) and Dispose5_82A14720 (sub_82A14720)
# -- same shape, different type pointer (7120 vs 7072 / 7096). The object's class (a 20-byte object, ->0 = a
# vtable) and the free flag's meaning aren't resolvable, so the name states the mechanism: dispose the object
# (with an optional free). The "6" distinguishes it from Dispose_82B39A30, Dispose2_8233DD70, Dispose3_8233E380,
# Dispose4_82A146C0, and Dispose5_82A14720.
[entrypoint.functions."0x82A14780"]
name = "Dispose6_82A14780"

# A copy-and-process: copy the struct data (CopyStructData2_82A1BD30) to the stack, read the copied values
# into the object (obj->16, obj->20), and process them (sub_83211D48) if a flag is set.

# Args: obj (r3, ->16, ->20), (r4), flag (r5, low byte = a "process" flag). Calls CopyStructData2_82A1BD30
# (sub_82A1BD30) to copy the struct data to the stack. It then reads the copied values (stack[80]->0 -> obj
# ->16, stack[80]->4 -> obj->20). If the flag's low byte is non-zero (a process flag), it processes the object
# (calling sub_83211D48, a sibling in the 0x83211 cluster). So it's a "copy the struct data, store the key
# values, and process the object (if flagged)" routine (a copy-and-process for the 0x83214 cluster). 58 lines,
# 2 callees (CopyStructData2_82A1BD30, sub_83211D48). 0 direct callers (reached via a dispatch / vcall). The
# object's class (->16, ->20 = copied values) and the flag's meaning (a process flag) aren't resolvable, so
# the name states the mechanism: copy and process the struct data.
[entrypoint.functions."0x83214828"]
name = "CopyAndProcess_83214828"

# A node constructor (the sibling of Construct20ByteNode_83215D98 / Construct20ByteNode2_83215D18): call
# Construct20Byte_83215640 (the base 20-byte constructor), then set a vtable / type pointer (7120 -- the
# Dispose6_82A14780 type) and two float fields from globals.

# Args: obj (r3, ->0 = a vtable / type pointer, ->16 = a float, ->20 = a float). Calls Construct20Byte_83215640
# (sub_83215640, the base 20-byte constructor) to allocate and zero the node. It then sets the node's vtable /
# type pointer (obj->0 = a fixed global, 0x82CCC8C0 + 7120 -- the same type pointer Dispose6_82A14780 uses)
# and two float fields (obj->16 and obj->20 = a fixed global float). So it's a "construct a 20-byte node and
# set its type and default floats" routine (a typed node constructor, part of the 0x83215 cluster). 51 lines,
# 1 callee (Construct20Byte_83215640). 3 callers: sub_83219588, ProcessTreeNode_83218C10 (sub_83218C10), sub
# _83218630. A sibling of Construct20ByteNode_83215D98 (sub_83215D98) and Construct20ByteNode2_83215D18 (sub
# _83215D18) -- same base (Construct20Byte), different type / field layout (type 7120, floats at ->16 and ->20).
# The node's class (a 20-byte typed record) isn't resolvable, so the name states the mechanism: construct a 20-
# byte node. The "3" distinguishes it from Construct20ByteNode_83215D98 and Construct20ByteNode2_83215D18.
[entrypoint.functions."0x83215CB8"]
name = "Construct20ByteNode3_83215CB8"

# An index validation: set a result field (obj->0) to 0, then validate an index against a container (a bounds
# check), raising an assertion (trap 22) if the index is out of range.

# Args: obj (r3, ->0 = a result), index (r4), container (r5, ->4 = a base, ->20 = a count, ->24 = a capacity). Sets
# obj->0 to 0 (the result). If the container is null or the index is 0, it skips the validation (an early return).
# Otherwise it validates the index: it reads the container's base (container->4, or container->4->0 if the
# container is large), its count (container->20), and capacity (container->24), computes a bound, and checks the
# index against it. If the index is out of range (index > bound), it raises an assertion (twi 31, r0, 22 -- a
# trap 22, the game's debug-assert instruction). So it's a "validate an index (assert it is in range)" routine.
# 63 lines, no callees (pure data manipulation). 3 callers (sub_822F2238, sub_8217AA20, sub_8217A7F8). The
# container's class (a container with a base, count, and capacity) and the index's meaning aren't resolvable,
# so the name states the mechanism: validate an index (assert bounds).
[entrypoint.functions."0x822F6EE8"]
name = "ValidateIndex_822F6EE8"

# A trivial offset getter: return (r3 + 732), i.e. a sub-object / field embedded at offset 732 of the
# containing object.

# Args: obj (r3, a containing object). Returns obj + 732 (0x2DC) -- the address of a sub-object / field
# embedded at offset 732 of the containing object. 2 lines, no callees. 0 direct callers (a leaf thunk / vcall
# target). The containing object's class (a large object with an embedded sub-object at offset 732) and the
# sub-object's meaning aren't resolvable, so the name states the mechanism: get the offset-732 field.
[entrypoint.functions."0x82BAA148"]
name = "GetOffset732_82BAA148"

# A flag-bit test: return bit 12 of a flag word (obj->28) -- a simple one-bit test.

# Args: (r3), obj (r4, ->28 = a u16 flag word). Loads the flag word (obj->28) and returns its bit 12 (the flag
# word rotated/shifted so bit 12 becomes the low bit, then masked to a single bit, 0 or 1). So it's a "test a
# single flag bit" routine (a predicate: is bit 12 of the object's flag word set?). 10 lines, no callees (pure
# bit manipulation). 0 direct callers (reached via a dispatch / vcall, likely a type's flag predicate method).
# The flag word's meaning (->28 = a u16 of flags) and bit 12's specific meaning (a particular flag) aren't
# resolvable, so the name states the mechanism: test a flag bit. The "2" distinguishes it from TestFlagBit_821D
# 2C58 (a more involved bit test); this one is a plain single-bit extraction.
[entrypoint.functions."0x82284208"]
name = "TestFlagBit2_82284208"

# A 2D-strided process (the sibling of Process2DStrided_82BDD000): call Process2DStrided_82BDD000 to compute
# a 2D-strided value, then process it (a hash + element processing), or return 0 if empty.

# Args: obj (r3, ->0, ->8), (more). Calls Process2DStrided_82BDD000 (sub_82BDD000) to compute a 2D-strided value
# (into the stack). If the result is 0 (empty), it returns 0. Otherwise it processes the result: it calls sub
# _82BCD930 (a sibling in the 0x82BCD cluster, with obj->0, obj->8, and the result), then ProcessElements_82BDCF
# 20 (sub_82BDCF20, with obj and the result), and also ComputeHash_8227BA30 (sub_8227BA30) in a path. So it's a
# "process a 2D-strided region (compute the value, then process it)" routine (a higher-level 2D-strided process
# that builds on Process2DStrided, part of the 0x82BDD cluster). 73 lines, 4 callees (Process2DStrided_82BDD000,
# ProcessElements_82BDCF20, ComputeHash_8227BA30, sub_82BCD930). 2 callers (sub_82BDD258, sub_82BDD8A8). The
# region's class (a 2D-strided data region) and the processed result's meaning aren't resolvable, so the name
# states the mechanism: process a 2D-strided region. The "2" distinguishes it from Process2DStrided_82BDD000.
[entrypoint.functions."0x82BDD1E0"]
name = "Process2DStrided2_82BDD1E0"

# A state dispatch: check a state object's flag bytes (ctx->8) and dispatch on its state (StateDispatch_82BCA7D8
# and other handlers).

# Args: obj (r3, ->172 = a field), ctx (r4, ->8 = a state object). Checks the state object (ctx->8): if it is
# non-null and its byte at offset 5 has the low 2 bits set (a state / type flag), it calls StateDispatch_82BCA7
# D8 (sub_82BCA7D8) to dispatch on that state. It then checks the state object's byte at offset 6 (a flag, bit
# 3) and, if that flag is clear, continues processing (loading obj->172 and a constant, and calling sub_82BCD5E0
# and sub_82CA3980 in the various branches). So it's a "check a state object's flags and dispatch on its
# state" routine (a state-machine handler, part of the 0x82BCA cluster). 319 lines, 3 callees (StateDispatch
# _82BCA7D8, sub_82BCD5E0, sub_82CA3980). 1 caller: StateDispatch_82BCB0A8 (sub_82BCB0A8). The state object's
# class (->5, ->6 = flag bytes) and the state's meaning (a game / animation / physics state) aren't resolvable,
# so the name states the mechanism: dispatch on the state. The "2" distinguishes it from StateDispatch_82BCB0A8
# and StateDispatch_82BCA7D8.
[entrypoint.functions."0x82BCAA58"]
name = "StateDispatch2_82BCAA58"

# A trivial offset getter: return (r3 + 684), i.e. a sub-object / field embedded at offset 684 of the
# containing object -- the sibling of GetOffset732_82BAA148.

# Args: obj (r3, a containing object). Returns obj + 684 (0x2AC) -- the address of a sub-object / field
# embedded at offset 684 of the containing object. 2 lines, no callees. 0 direct callers (a leaf thunk / vcall
# target). The containing object's class (a large object with embedded sub-objects at offsets 684 and 732) and
# the sub-object's meaning aren't resolvable, so the name states the mechanism: get the offset-684 field. A
# sibling of GetOffset732_82BAA148 (sub_82BAA148) -- same shape (addi + blr), different offset (684 vs 732), and
# the two sit adjacent in the binary (0x82BAA148 and 0x82BAA150), so they likely return two different sub-
# objects of the same large containing object.
[entrypoint.functions."0x82BAA150"]
name = "GetOffset684_82BAA150"

# A very large (1793-line) buffer-building / serialization routine: build a buffer (Buffer_Append, Buffer
# _PushByte) and call many SDK / runtime functions to process the data.

# Args: obj (r3, ->12 = a flag), (r4), (r6), (more). This is one of the largest routines in the recompile
# (1793 lines, a 1328-byte stack frame). It initializes a large local struct (zeroing several fields on the
# stack), checks obj for null, and (if non-null) checks a flag bit (obj->12, bit 6). It then makes a series of
# calls into sub_82CAF6C8 (a getter / accessor, called repeatedly) and sub_82CAB630 / sub_82CAB770 (SDK / runtime
# functions), building a buffer (Buffer_Append_82CAE410, Buffer_PushByte_82CAE378) and calling many other sub
# _82CA* SDK / runtime functions (sub_82CA2B88, sub_82CA3C68, sub_82CA5DC0, sub_82CAACC0, sub_82CB6A88, ...). So
# it's a "build a buffer / serialize the data" routine (a large serialization / encoding routine that assembles
# a buffer from the object's data, calling the SDK / runtime to read the fields). 1793 lines, 13 callees (Buffer
# _Append_82CAE410, Buffer_PushByte_82CAE378, GetGlobalPtr3_82240578, and 10 sub_82C* SDK / runtime functions).
# 7 callers (sub_82286918, Func_82CA71C8, sub_82CA2A78, ...). The object's class (a large object with a flag at
# ->12) and the buffer's format (a serialized / network / save format) aren't resolvable, so the name states the
# mechanism: build a buffer (serialize the data).
[entrypoint.functions."0x8223F990"]
name = "BuildBuffer_8223F990"

# A vcall-sequence dispatch (a sibling of the DispatchVcallSequence family): make a series of vcall calls
# (via the object's vtable, at slots ->32, ->44, ->48, and others).

# Args: obj (r3, ->4 = a sub-object), ctx (r4). Makes a series of vcall calls: it calls the vtable method at
# obj->4->0->32 (passing obj->4), then the method at obj->4->0->44, then the method at obj->4->0->48, and more
# (6 vcall dispatches total, all via the object's vtable). So it's a "dispatch a sequence of virtual calls"
# routine (a vcall-sequence dispatcher, a sibling of DispatchVcallSequence_82B25628 and the DispatchVcall
# Sequence2-6 family). 215 lines, 6 vcall dispatches, no direct callees (all vtable calls). 1 caller (sub_82BA
# FCE0). The object's class (a sub-object at ->4, with a vtable at ->4->0) and the vcall slots' meanings (->32,
# ->44, ->48 = particular virtual methods) aren't resolvable, so the name states the mechanism: dispatch a
# sequence of virtual calls. The "7" distinguishes it from DispatchVcallSequence_82B25628, DispatchVcall
# Sequence2_82284538, DispatchVcallSequence3_8243D0B8, DispatchVcallSequence4_82C25F18, DispatchVcallSequence5
# _82B8D250, and DispatchVcallSequence6_82C26AF0.
[entrypoint.functions."0x82BB0148"]
name = "DispatchVcallSequence7_82BB0148"

# A serialization wrapper: call the SDK / runtime functions (sub_82CAB770, sub_82CAB630, sub_82CAB7E0) and the
# buffer-builder (BuildBuffer_8223F990), and set an error / result code (obj->0 = 22) on a failure path.

# Args: obj (r3, ->0 = a result / error code), (r4), (r5), (more, r6-r10 saved to the stack). This is a common
# entry point (many callers). It saves several arguments (r6-r10) to the stack, and (if r5 is null) it calls sub
# _82CAB770 (an SDK / runtime function), sets obj->0 to 22 (an error / failure code, 0x16), and calls sub_82CAB630
# (another SDK / runtime function) to handle the error. In the main path (r5 non-null) it processes the data,
# calling sub_82CAB7E0 (an SDK / runtime function) and BuildBuffer_8223F990 (sub_8223F990, the buffer-builder /
# serialization routine). So it's a "serialize / encode the data (via the SDK / runtime and the buffer-builder)
# and report a result code" routine (a serialization entry point that wraps the SDK / runtime). 156 lines, 4
# callees (BuildBuffer_8223F990, sub_82CAB630, sub_82CAB770, sub_82CAB7E0). Many callers (sub_82C86D20, sub
# _82BB39F8, sub_826CAFA8, ...). The data's type and the serialization format aren't resolvable, so the name
# states the mechanism: serialize the data (via the SDK / runtime).
[entrypoint.functions."0x82CA3EB8"]
name = "Serialize_82CA3EB8"

# A dispose / destroy (with an optional free) -- the sibling of Dispose4_82A146C0, Dispose5_82A14720, and
# Dispose6_82A14780 (a different type pointer, 7264): set a vtable / type pointer, run the 20-byte destructor
# (Destruct20Byte_832156E0), and free the object (Free_SizeBucketed_Thunk_8221BE68) if a flag is set.

# Args: obj (r3, ->0 = a vtable / type pointer), flag (r4, bit 0 = an "owned / free" flag). Sets obj->0 to a
# fixed global (a vtable / type pointer, 0x82CCC8C0 + 7264 -- a different type than Dispose4_82A146C0's 7072,
# Dispose5_82A14720's 7096, and Dispose6_82A14780's 7120). It then runs the 20-byte destructor (Destruct20Byte
# _832156E0, sub_832156E0). If the flag's bit 0 is set (the object is owned), it frees the object (Free_Size
# Bucketed_Thunk_8221BE68, sub_8221BE68); otherwise it leaves the memory (a destruct-but-don't-free path). So
# it's a "dispose the object (set the type, run the destructor, free if owned)" routine. 56 lines, 2 callees
# (Destruct20Byte_832156E0, Free_SizeBucketed_Thunk_8221BE68). 0 direct callers (reached via a dispatch / vcall,
# likely a type's destructor / dispose method). A structural sibling of Dispose4_82A146C0 (sub_82A146C0), Dispose
# 5_82A14720 (sub_82A14720), and Dispose6_82A14780 (sub_82A14780) -- same shape, different type pointer (7264 vs
# 7072 / 7096 / 7120). The object's class (a 20-byte object, ->0 = a vtable) and the free flag's meaning aren't
# resolvable, so the name states the mechanism: dispose the object (with an optional free). The "7" distinguishes
# it from Dispose_82B39A30, Dispose2_8233DD70, Dispose3_8233E380, Dispose4_82A146C0, Dispose5_82A14720, and
# Dispose6_82A14780.
[entrypoint.functions."0x82A149C0"]
name = "Dispose7_82A149C0"

# A count process: zero the object's fields (obj->4, ->8, ->12), compute a count from a size difference
# ((src->8 - src->4) / 12), and process it (sub_8282EFD8, with a refcounted copy-assign).

# Args: obj (r3, ->4, ->8, ->12 = result fields), src (r4, ->4 = a start, ->8 = an end), (more). Zeros the
# object's result fields (obj->4, obj->8, obj->12). It then computes a count: (src->8 - src->4) / 12 (the size
# difference of the source, divided by 12 -- an element size of 12 bytes). If the count is non-zero, it calls sub
# _8282EFD8 (a sibling in the 0x8282 cluster, with the object) to process it, and (in a path) CopyAssign_Ref
# Counted_821F0108 (sub_821F0108, a refcounted copy-assign). So it's a "compute a count from a size difference
# (a 12-byte element count) and process it" routine. 123 lines, 2 callees (sub_8282EFD8, CopyAssign_RefCounted
# _821F0108). 2 callers (sub_82B722E8, sub_82B82C68). The object's class (->4, ->8, ->12 = result fields) and
# the source's meaning (a 12-byte-element range) aren't resolvable, so the name states the mechanism: process a
# count.
[entrypoint.functions."0x82B728D8"]
name = "ProcessCount_82B728D8"

# A dispose / destroy (with an optional free) -- the sibling of Dispose4-7 (a different type pointer, 6724):
# set a vtable / type pointer, run the 20-byte destructor (Destruct20Byte_832156E0), and free the object
# (Free_SizeBucketed_Thunk_8221BE68) if a flag is set.

# Args: obj (r3, ->0 = a vtable / type pointer), flag (r4, bit 0 = an "owned / free" flag). Sets obj->0 to a
# fixed global (a vtable / type pointer, 0x82CCC8C0 + 6724 -- a different type than Dispose4_82A146C0's 7072,
# Dispose5_82A14720's 7096, Dispose6_82A14780's 7120, and Dispose7_82A149C0's 7264). It then runs the 20-byte
# destructor (Destruct20Byte_832156E0, sub_832156E0). If the flag's bit 0 is set (the object is owned), it frees
# the object (Free_SizeBucketed_Thunk_8221BE68, sub_8221BE68); otherwise it leaves the memory (a destruct-but-
# don't-free path). So it's a "dispose the object (set the type, run the destructor, free if owned)" routine. 56
# lines, 2 callees (Destruct20Byte_832156E0, Free_SizeBucketed_Thunk_8221BE68). 0 direct callers (reached via a
# dispatch / vcall, likely a type's destructor / dispose method). A structural sibling of Dispose4_82A146C0, Dispose
# 5_82A14720, Dispose6_82A14780, and Dispose7_82A149C0 -- same shape, different type pointer (6724). The object's
# class (a 20-byte object, ->0 = a vtable) and the free flag's meaning aren't resolvable, so the name states the
# mechanism: dispose the object (with an optional free). The "8" distinguishes it from Dispose_82B39A30, Dispose2
# _8233DD70, Dispose3_8233E380, Dispose4_82A146C0, Dispose5_82A14720, Dispose6_82A14780, and Dispose7_82A149C0.
[entrypoint.functions."0x82A12000"]
name = "Dispose8_82A12000"

# A struct copy (an eighth sibling of the CopyStructData family): if src->20 >= 4, copy a variable-length byte
# array from src->12 (byte-by-byte to the stack) and advance src->12, then process further.

# Args: obj (r3), src (r4, ->4 = data, ->12 = a byte-array pointer, ->20 = a size), (r5). If src->20 >= 4, it
# copies (src->20 - 4) bytes from src->12 (a byte-by-byte loop to the stack), advances src->12 by 4, and
# continues processing (calling sub_82A1B480, the sibling in the 0x82A1B cluster that the other CopyStructData*
# variants also call, plus sub_83211D48, a sibling in the 0x83211 cluster). So it's a "copy the struct's data"
# routine. 128 lines, 2 callees (sub_82A1B480, sub_83211D48). 0 direct callers (reached via a dispatch / vcall).
# The struct class and the exact field semantics aren't resolvable, so the name states the mechanism: copy the
# struct's data. The "8" distinguishes this from CopyStructData, CopyStructData2, 3, 4, 5, 6, and 7 (all the same
# shape).
[entrypoint.functions."0x83212620"]
name = "CopyStructData8_83212620"

# A node constructor (the sibling of Construct20ByteNode_83215D98 / Construct20ByteNode2_83215D18 / Construct
# 20ByteNode3_83215CB8): call Construct20Byte_83215640 (the base 20-byte constructor), then set a vtable / type
# pointer (6724 -- the Dispose8_82A12000 type) and a float field from a global.

# Args: obj (r3, ->0 = a vtable / type pointer, ->16 = a float). Calls Construct20Byte_83215640 (sub_83215640,
# the base 20-byte constructor) to allocate and zero the node. It then sets the node's vtable / type pointer
# (obj->0 = a fixed global, 0x82CCC8C0 + 6724 -- the same type pointer Dispose8_82A12000 uses) and a float field
# (obj->16 = a fixed global float). So it's a "construct a 20-byte node and set its type and default float"
# routine (a typed node constructor, part of the 0x83215 cluster). 48 lines, 1 callee (Construct20Byte_83215640).
# 2 callers: sub_83219588, ProcessTreeNode_83218C10 (sub_83218C10). A sibling of Construct20ByteNode_83215D98,
# Construct20ByteNode2_83215D18, and Construct20ByteNode3_83215CB8 -- same base (Construct20Byte), different type
# / field layout (type 6724, a float at ->16). The node's class (a 20-byte typed record) isn't resolvable, so
# the name states the mechanism: construct a 20-byte node. The "4" distinguishes it from Construct20ByteNode_83215
# D98, Construct20ByteNode2_83215D18, and Construct20ByteNode3_83215CB8.
[entrypoint.functions."0x832158B0"]
name = "Construct20ByteNode4_832158B0"

# A 12-byte object constructor: allocate a 12-byte object (Allocate_OrDbgBreak_8221F388), set up its fields
# (from the source and a global), and set a string (SetString_822F2020).

# Args: obj (r3), (r4), src (r5, ->0 = a value), (more). Allocates a 12-byte object (Allocate_OrDbgBreak_8221F388,
# sub_8221F388). If the allocation succeeds, it sets up the object's fields: ->0 (a value from a global), ->4 (a
# value from the global's ->4), and ->8 (src->0, a value from the source). It continues setting up the object
# (copying more fields) and, in a path, sets a string (SetString_822F2020, sub_822F2020) and calls sub_822F1F00
# (an error / setup function), sub_82171810, sub_826C3EF0, and sub_826C3FA8. So it's a "construct a 12-byte
# object (allocate it, set its fields, and set a string)" routine. 127 lines, 6 callees (Allocate_OrDbgBreak
# _8221F388, SetString_822F2020, sub_822F1F00, sub_82171810, sub_826C3EF0, sub_826C3FA8). Many callers (sub
# _82890AF8, sub_82361728, sub_8256C580, ...). The object's class (a 12-byte object with a string) and the
# source's meaning aren't resolvable, so the name states the mechanism: construct a 12-byte object.
[entrypoint.functions."0x825F7B10"]
name = "Construct12Byte_825F7B10"

# A vcall + check: make a vcall (obj->0->4, passing the object and a value), check the result against flags,
# and (in a path) process a float (FloatToIntProcess_82284810).

# Args: obj (r3, ->0 = a sub-object with a vtable at ->0->4), src (r4, ->20 = a value), flag1 (r5), flag2 (r6).
# Loads src->20 (a value). If it is 0, takes an early path. Otherwise it makes a vcall (obj->0->4, passing the
# object and the value). It then checks the result (the low byte of the return) against the flags: if the result
# is 0 and flag2 is 0, it takes one path; otherwise it continues (calling FloatToIntProcess_82284810, sub
# _82284810, in a path). So it's a "dispatch a vcall and check the result (against flags)" routine (a gated vcall
# dispatcher, part of the 0x82BAC cluster). 77 lines, 1 callee (FloatToIntProcess_82284810), 1 vcall dispatch.
# 0 direct callers (reached via a dispatch / vcall). The object's class (a sub-object at ->0, with a vtable at
# ->0->4) and the vcall slot's meaning (->4 = a particular virtual method) aren't resolvable, so the name states
# the mechanism: dispatch a vcall and check the result.
[entrypoint.functions."0x82BACCE0"]
name = "DispatchAndCheck_82BACCE0"

# A vcall + process: load a global value, make a vcall (via the value's vtable, at slot ->32), and process the
# result.

# Args: obj (r3), flag (r4), value (r5), (more). Loads a global value (from a fixed .data address, the ->56
# field of a global struct). If the value argument is null, it calls sub_823FE8F8 (a sibling in the 0x823FE
# cluster) to get a value. It then makes a vcall (via the value's vtable, at slot ->32, i.e. value->0->32,
# passing the value) and processes the result (2 vcall dispatches total, in the various branches). So it's a
# "dispatch a vcall (with a global / looked-up value) and process the result" routine (a vcall processor, part of
# the 0x823F8 cluster). 102 lines, 1 callee (sub_823FE8F8), 2 vcall dispatches. Many callers (sub_823F9398, sub
# _827CE468, sub_823F8F40, ...). The value's class (->0 = a vtable, ->32 = a particular virtual method) and the
# global's meaning aren't resolvable, so the name states the mechanism: make a vcall and process the result.
[entrypoint.functions."0x823F8D30"]
name = "VcallProcess_823F8D30"

# A size-checked allocation: check the size (an overflow check: size != 0 and -1 / size < 12), compute the
# allocation size (size * 12), and allocate (Allocate_OrDbgBreak_8221F388); raise an error (sub_822F1F00) on a
# failure path.

# Args: (r3), size (r4), (more). Checks the size: if size is 0, or -1 / size >= 12 (an overflow check -- the
# allocation size would overflow), it takes an error path. Otherwise it computes the allocation size (size * 3 * 4
# = size * 12) and allocates it (Allocate_OrDbgBreak_8221F388, sub_8221F388). On a failure path it calls sub
# _822F1F00 (an error / setup function, with a string at a fixed .data address). So it's a "check the size (an
# overflow check), compute the allocation size (size * 12), and allocate (with an error on failure)" routine (a
# size-checked allocation of 12-byte elements). 70 lines, 2 callees (Allocate_OrDbgBreak_8221F388, sub_822F1F00).
# Many callers (sub_82A96618, sub_82688FD8, sub_82B420E0, ...). The 12-byte element's meaning (a record / node) and
# the size's source aren't resolvable, so the name states the mechanism: check the size and allocate (size * 12).
[entrypoint.functions."0x8264EFE0"]
name = "AllocateChecked_8264EFE0"

# A size-checked allocation (the sibling of AllocateChecked_8264EFE0, but with a 2x multiplier): check the size
# (an overflow check: size != 0 and -1 / size < 2), compute the allocation size (size * 2), and allocate
# (Allocate_OrDbgBreak_8221F388); raise an error (sub_822F1F00) on a failure path.

# Args: size (r3), (more). Checks the size: if size is 0, or -1 / size >= 2 (an overflow check -- the allocation
# size would overflow), it takes an error path. Otherwise it computes the allocation size (size * 2) and allocates
# it (Allocate_OrDbgBreak_8221F388, sub_8221F388). On a failure path it calls sub_822F1F00 (an error / setup
# function, with a string at a fixed .data address). So it's a "check the size (an overflow check), compute the
# allocation size (size * 2), and allocate (with an error on failure)" routine (a size-checked allocation). 66
# lines, 2 callees (Allocate_OrDbgBreak_8221F388, sub_822F1F00). 4 callers (sub_82C73A58, sub_8217A4F8, sub
# _82C73E00, sub_82B39E78). The element's meaning (a 2x-sized record) and the size's source aren't resolvable, so
# the name states the mechanism: check the size and allocate (size * 2). The "2" distinguishes it from
# AllocateChecked_8264EFE0 (which uses a 12x multiplier).
[entrypoint.functions."0x8217A488"]
name = "AllocateChecked2_8217A488"

# A size-computed allocation (the sibling of AllocateChecked2_8217A488, a grow / resize): round the size up to
# an 8-byte boundary (size | 7), check it against a max and the object's capacity (obj->24), compute the new size,
# and allocate (AllocateChecked2_8217A488); free the old buffer (Free_SizeBucketed_Thunk_8221BE68) on a path.

# Args: obj (r3, ->24 = a capacity), size (r4), (r5), (more). Rounds the size up to an 8-byte boundary (size | 7).
# It then checks the size against a max (0x7FFFFFFE) and the object's capacity (obj->24): if the size exceeds the
# max, or if size / 3 is not less than the capacity (a capacity / overflow check), it uses the original size
# (or the capacity-based size, r11 + (r11 & 0x7FFFFFFF)). It then allocates the new size (AllocateChecked2_8217A4
# 88, sub_8217A488), copies / zeros the data (Buffer_CopyOrZero_Error_82CA3730, sub_82CA3730), and (on a path) frees
# the old buffer (Free_SizeBucketed_Thunk_8221BE68, sub_8221BE68). So it's a "compute the new size (round to 8 bytes,
# check the capacity), allocate it, copy the data, and free the old buffer" routine (a grow / resize allocation,
# a sibling of AllocateChecked2_8217A488). 133 lines, 3 callees (AllocateChecked2_8217A488, Buffer_CopyOrZero_Error
# _82CA3730, Free_SizeBucketed_Thunk_8221BE68). 1 caller: ProcessValue_FlagNonZero_8217A5E0 (sub_8217A5E0). The
# object's class (->24 = a capacity) and the buffer's meaning aren't resolvable, so the name states the mechanism:
# compute the size and allocate (a resize / grow).
[entrypoint.functions."0x8217A4F8"]
name = "AllocateResized_8217A4F8"

# A clamp-and-dispatch: clamp a value (to 32, if it is out of range), and dispatch (DispatchAndAdjustPos_8227BB58
# with the clamped value).

# Args: (r3), obj (r4, ->0, ->8 = a max / bound), value (r5), (more). Compares the value against obj->8 (a max /
# bound) and 32: if value <= obj->8, or value >= 32, it clamps the value to 32. It then dispatches: it calls
# DispatchAndAdjustPos_8227BB58 (sub_8227BB58, a vcall + position-adjust, the state-dispatch cluster) with obj->0
# and the (clamped) value. In a path it also calls sub_82BC9860 (a sibling in the 0x82BC9 cluster). So it's a
# "clamp a value to a bound (32) and dispatch on it" routine (a gated dispatch with a clamped argument, part of
# the 0x82BCD cluster). 81 lines, 2 callees (DispatchAndAdjustPos_8227BB58, sub_82BC9860). 3 callers (sub_822CE098,
# Process2DStrided2_82BDD1E0, sub_82BDD518). The object's class (->0, ->8 = a max / bound) and the value's
# meaning (an index / position) aren't resolvable, so the name states the mechanism: clamp the value and dispatch.
[entrypoint.functions."0x82BCD930"]
name = "ClampAndDispatch_82BCD930"

# A struct copy (a ninth sibling of the CopyStructData family): if src->20 >= 4, copy a variable-length byte
# array from src->12 (byte-by-byte to the stack) and advance src->12, then process further.

# Args: obj (r3), src (r4, ->4 = data, ->12 = a byte-array pointer, ->20 = a size), (r5). If src->20 >= 4, it
# copies (src->20 - 4) bytes from src->12 (a byte-by-byte loop to the stack), advances src->12 by 4, and
# continues processing (calling sub_82A1B480, the sibling in the 0x82A1B cluster that the other CopyStructData*
# variants also call, plus sub_83211D48, a sibling in the 0x83211 cluster). So it's a "copy the struct's data"
# routine. 269 lines, 2 callees (sub_82A1B480, sub_83211D48). 0 direct callers (reached via a dispatch / vcall).
# The struct class and the exact field semantics aren't resolvable, so the name states the mechanism: copy the
# struct's data. The "9" distinguishes this from CopyStructData, CopyStructData2, 3, 4, 5, 6, 7, and 8 (all the
# same shape).
[entrypoint.functions."0x83213958"]
name = "CopyStructData9_83213958"

# A node constructor (the sibling of Construct20ByteNode_83215D98 / Construct20ByteNode2_83215D18 / Construct
# 20ByteNode3_83215CB8 / Construct20ByteNode4_832158B0): call Construct20Byte_83215640 (the base 20-byte
# constructor), then set a vtable / type pointer (7264 -- the Dispose7_82A149C0 type) and two float fields from
# globals.

# Args: obj (r3, ->0 = a vtable / type pointer, ->16 = a float, ->20 = a float, ->24 = a flag). Calls
# Construct20Byte_83215640 (sub_83215640, the base 20-byte constructor) to allocate and zero the node. It then
# sets the node's vtable / type pointer (obj->0 = a fixed global, 0x82CCC8C0 + 7264 -- the same type pointer
# Dispose7_82A149C0 uses), two float fields (obj->16 and obj->20 = a fixed global float), and a flag (obj->24 = 0).
# So it's a "construct a 20-byte node and set its type, default floats, and flag" routine (a typed node
# constructor, part of the 0x83215 cluster). 55 lines, 1 callee (Construct20Byte_83215640). 3 callers: sub
# _83219588, ProcessTreeNode_83218C10 (sub_83218C10), sub_83218630. A sibling of Construct20ByteNode_83215D98,
# Construct20ByteNode2_83215D18, Construct20ByteNode3_83215CB8, and Construct20ByteNode4_832158B0 -- same base
# (Construct20Byte), different type / field layout (type 7264, floats at ->16 and ->20, a flag at ->24). The
# node's class (a 20-byte typed record) isn't resolvable, so the name states the mechanism: construct a 20-byte
# node. The "5" distinguishes it from Construct20ByteNode_83215D98, 2, 3, and 4.
[entrypoint.functions."0x83215A80"]
name = "Construct20ByteNode5_83215A80"

# A trivial global getter: return a fixed global (0x82CCC8C0 + 19812) -- a sibling of the GetGlobalPtr family.

# Args: (none -- it ignores its arguments). Returns a fixed global (0x82CCC8C0 + 19812), i.e. the address of a
# particular global variable / structure. 3 lines, no callees. 0 direct callers (reached via a dispatch / vcall,
# likely a type's global / singleton getter). The global's meaning (a particular global variable / structure) isn't
# resolvable, so the name states the mechanism: get a global pointer. The "6" distinguishes it from GetGlobalPtr
# _82C27FA0, GetGlobalPtr2_82C23D00, GetGlobalPtr3_82240578, GetGlobalPtr4_82C21AD0, and GetGlobalPtr5 (an earlier
# entry).
[entrypoint.functions."0x82C26870"]
name = "GetGlobalPtr6_82C26870"

# An element-list process: clear a flag (src->32->5, the low 2 bits), then loop over a list of elements
# (src->8, count src->40) and dispatch on each (StateDispatch_82BCA7D8).

# Args: obj (r3), src (r4, ->8 = an element list, ->32 = a state object, ->40 = a count), (more). If src->32 is
# non-null, it clears a flag in the state object (src->32->5 &= ~3, clearing the low 2 bits of a flag byte). It
# then loops over the element list (src->8, count src->40): for each element (src->8 + i), it checks the element's
# ->4 field (a size / type, must be >= 4), loads the element's ->0 field, and (in a path) calls StateDispatch
# _82BCA7D8 (sub_82BCA7D8, a state dispatch) to process it. So it's a "clear a state flag and process a list of
# elements (dispatching on each)" routine (an element-list processor, part of the 0x82BCA cluster). 222 lines, 1
# callee (StateDispatch_82BCA7D8). 1 caller: StateDispatch_82BCB0A8 (sub_82BCB0A8). The element's class (->0, ->4
# = a size / type) and the state object's meaning (->5 = a flag byte) aren't resolvable, so the name states the
# mechanism: process a list of elements. Distinguished from ProcessElements_82BDCF20 (a different element
# processor in the 0x82BCD cluster).
[entrypoint.functions."0x82BCAC98"]
name = "ProcessElementList_82BCAC98"

# An atomic counter increment: atomically increment a counter (obj->8) using a load-linked / store-conditional
# loop (lwarx / stwcx) under the global lock, and return the new value.

# Args: obj (r3, ->8 = a counter). Atomically increments the counter at obj->8: it takes the global lock (REX
# _ENTER_GLOBAL_LOCK, disabling interrupts via the MSR), reads the counter (lwarx), increments it, and writes it
# back (stwcx, a compare-and-swap); if the store fails (another thread modified the counter), it retries (a
# load-linked / store-conditional loop). It releases the global lock and returns the new value. So it's an "atomic
# increment of a shared counter" routine (a thread-safe counter increment, part of the 0x822C cluster). 37 lines,
# no callees (a pure atomic operation, using the global lock). 1 caller (sub_82B4F368). The counter's meaning
# (a shared counter at obj->8, a frame / resource / allocation counter) isn't resolvable, so the name states the
# mechanism: atomic increment.
[entrypoint.functions."0x822CBEB8"]
name = "AtomicIncrement_822CBEB8"

# A dispose / destroy (with an optional free) -- the sibling of Dispose4-8 (a different type pointer, 7192):
# set a vtable / type pointer, run the 20-byte destructor (Destruct20Byte_832156E0), and free the object
# (Free_SizeBucketed_Thunk_8221BE68) if a flag is set.

# Args: obj (r3, ->0 = a vtable / type pointer), flag (r4, bit 0 = an "owned / free" flag). Sets obj->0 to a
# fixed global (a vtable / type pointer, 0x82CCC8C0 + 7192 -- a different type than Dispose4_82A146C0's 7072,
# Dispose5_82A14720's 7096, Dispose6_82A14780's 7120, Dispose7_82A149C0's 7264, and Dispose8_82A12000's 6724).
# It then runs the 20-byte destructor (Destruct20Byte_832156E0, sub_832156E0). If the flag's bit 0 is set (the
# object is owned), it frees the object (Free_SizeBucketed_Thunk_8221BE68, sub_8221BE68); otherwise it leaves the
# memory (a destruct-but-don't-free path). So it's a "dispose the object (set the type, run the destructor, free if
# owned)" routine. 56 lines, 2 callees (Destruct20Byte_832156E0, Free_SizeBucketed_Thunk_8221BE68). 0 direct
# callers (reached via a dispatch / vcall, likely a type's destructor / dispose method). A structural sibling of
# Dispose4_82A146C0, Dispose5_82A14720, Dispose6_82A14780, Dispose7_82A149C0, and Dispose8_82A12000 -- same shape,
# different type pointer (7192). The object's class (a 20-byte object, ->0 = a vtable) and the free flag's meaning
# aren't resolvable, so the name states the mechanism: dispose the object (with an optional free). The "9"
# distinguishes it from Dispose_82B39A30, Dispose2_8233DD70, Dispose3_8233E380, Dispose4_82A146C0, Dispose5
# _82A14720, Dispose6_82A14780, Dispose7_82A149C0, and Dispose8_82A12000.
[entrypoint.functions."0x82A148A0"]
name = "Dispose9_82A148A0"

# A struct copy (a tenth sibling of the CopyStructData family): if src->20 >= 4, copy a variable-length byte
# array from src->12 (byte-by-byte to the stack) and advance src->12, then process further.

# Args: obj (r3), src (r4, ->4 = data, ->12 = a byte-array pointer, ->20 = a size), (r5). If src->20 >= 4, it
# copies (src->20 - 4) bytes from src->12 (a byte-by-byte loop to the stack), advances src->12 by 4, and
# continues processing (calling sub_82A1B480, the sibling in the 0x82A1B cluster that the other CopyStructData*
# variants also call, plus sub_83211D48, a sibling in the 0x83211 cluster). So it's a "copy the struct's data"
# routine. 121 lines, 2 callees (sub_82A1B480, sub_83211D48). 0 direct callers (reached via a dispatch / vcall).
# The struct class and the exact field semantics aren't resolvable, so the name states the mechanism: copy the
# struct's data. The "10" distinguishes this from CopyStructData, CopyStructData2, 3, 4, 5, 6, 7, 8, and 9 (all
# the same shape).
[entrypoint.functions."0x832141C0"]
name = "CopyStructData10_832141C0"

# A dispose / destroy (with an optional free) -- the sibling of Dispose4-9 (a different type pointer, 7216):
# set a vtable / type pointer, run the 20-byte destructor (Destruct20Byte_832156E0), and free the object
# (Free_SizeBucketed_Thunk_8221BE68) if a flag is set.

# Args: obj (r3, ->0 = a vtable / type pointer), flag (r4, bit 0 = an "owned / free" flag). Sets obj->0 to a
# fixed global (a vtable / type pointer, 0x82CCC8C0 + 7216 -- a different type than Dispose4_82A146C0's 7072,
# Dispose5_82A14720's 7096, Dispose6_82A14780's 7120, Dispose7_82A149C0's 7264, Dispose8_82A12000's 6724, and
# Dispose9_82A148A0's 7192). It then runs the 20-byte destructor (Destruct20Byte_832156E0, sub_832156E0). If the
# flag's bit 0 is set (the object is owned), it frees the object (Free_SizeBucketed_Thunk_8221BE68, sub_8221BE68);
# otherwise it leaves the memory (a destruct-but-don't-free path). So it's a "dispose the object (set the type, run
# the destructor, free if owned)" routine. 56 lines, 2 callees (Destruct20Byte_832156E0, Free_SizeBucketed_Thunk
# _8221BE68). 0 direct callers (reached via a dispatch / vcall, likely a type's destructor / dispose method). A
# structural sibling of Dispose4_82A146C0, Dispose5_82A14720, Dispose6_82A14780, Dispose7_82A149C0, Dispose8
# _82A12000, and Dispose9_82A148A0 -- same shape, different type pointer (7216). The object's class (a 20-byte
# object, ->0 = a vtable) and the free flag's meaning aren't resolvable, so the name states the mechanism: dispose
# the object (with an optional free). The "10" distinguishes it from Dispose_82B39A30, Dispose2_8233DD70, Dispose3
# _8233E380, Dispose4_82A146C0, Dispose5_82A14720, Dispose6_82A14780, Dispose7_82A149C0, Dispose8_82A12000, and
# Dispose9_82A148A0.
[entrypoint.functions."0x82A14900"]
name = "Dispose10_82A14900"

# A struct copy (an eleventh sibling of the CopyStructData family): if src->20 >= 4, copy a variable-length
# byte array from src->12 (byte-by-byte to the stack) and advance src->12, then process further.

# Args: obj (r3), src (r4, ->4 = data, ->12 = a byte-array pointer, ->20 = a size), (r5). If src->20 >= 4, it
# copies (src->20 - 4) bytes from src->12 (a byte-by-byte loop to the stack), advances src->12 by 4, and
# continues processing (calling sub_82A1B480, the sibling in the 0x82A1B cluster that the other CopyStructData*
# variants also call, plus sub_83211D48, a sibling in the 0x83211 cluster). So it's a "copy the struct's data"
# routine. 121 lines, 2 callees (sub_82A1B480, sub_83211D48). 0 direct callers (reached via a dispatch / vcall).
# The struct class and the exact field semantics aren't resolvable, so the name states the mechanism: copy the
# struct's data. The "11" distinguishes this from CopyStructData, CopyStructData2, 3, 4, 5, 6, 7, 8, 9, and 10 (all
# the same shape).
[entrypoint.functions."0x83213F68"]
name = "CopyStructData11_83213F68"

# A dispose / destroy (with an optional free) -- the sibling of Dispose4-10 (a different type pointer, 7240):
# set a vtable / type pointer, run the 20-byte destructor (Destruct20Byte_832156E0), and free the object
# (Free_SizeBucketed_Thunk_8221BE68) if a flag is set.

# Args: obj (r3, ->0 = a vtable / type pointer), flag (r4, bit 0 = an "owned / free" flag). Sets obj->0 to a
# fixed global (a vtable / type pointer, 0x82CCC8C0 + 7240 -- a different type than Dispose4_82A146C0's 7072,
# Dispose5_82A14720's 7096, Dispose6_82A14780's 7120, Dispose7_82A149C0's 7264, Dispose8_82A12000's 6724,
# Dispose9_82A148A0's 7192, and Dispose10_82A14900's 7216). It then runs the 20-byte destructor (Destruct20Byte
# _832156E0, sub_832156E0). If the flag's bit 0 is set (the object is owned), it frees the object (Free_Size
# Bucketed_Thunk_8221BE68, sub_8221BE68); otherwise it leaves the memory (a destruct-but-don't-free path). So it's
# a "dispose the object (set the type, run the destructor, free if owned)" routine. 56 lines, 2 callees
# (Destruct20Byte_832156E0, Free_SizeBucketed_Thunk_8221BE68). 0 direct callers (reached via a dispatch / vcall,
# likely a type's destructor / dispose method). A structural sibling of Dispose4-10 -- same shape, different type
# pointer (7240). The object's class (a 20-byte object, ->0 = a vtable) and the free flag's meaning aren't
# resolvable, so the name states the mechanism: dispose the object (with an optional free). The "11" distinguishes
# it from Dispose_82B39A30, Dispose2_8233DD70, Dispose3_8233E380, and Dispose4-10.
[entrypoint.functions."0x82A14960"]
name = "Dispose11_82A14960"

# A struct copy (a twelfth sibling of the CopyStructData family): if src->20 >= 4, copy a variable-length byte
# array from src->12 (byte-by-byte to the stack) and advance src->12, then process further.

# Args: obj (r3), src (r4, ->4 = data, ->12 = a byte-array pointer, ->20 = a size), (r5). If src->20 >= 4, it
# copies (src->20 - 4) bytes from src->12 (a byte-by-byte loop to the stack), advances src->12 by 4, and
# continues processing (calling sub_82A1B480, the sibling in the 0x82A1B cluster that the other CopyStructData*
# variants also call, plus sub_83211D48, a sibling in the 0x83211 cluster). So it's a "copy the struct's data"
# routine. 121 lines, 2 callees (sub_82A1B480, sub_83211D48). 0 direct callers (reached via a dispatch / vcall).
# The struct class and the exact field semantics aren't resolvable, so the name states the mechanism: copy the
# struct's data. The "12" distinguishes this from CopyStructData, CopyStructData2, 3, 4, 5, 6, 7, 8, 9, 10, and 11
# (all the same shape).
[entrypoint.functions."0x83213D10"]
name = "CopyStructData12_83213D10"

# A chain / tree traversal and search: walk the chain (a binary-tree search, via node->0, node->8, node->12,
# node->25) and find the node whose ->12 key is at / for the given value (src->0).

# Args: (r3), obj (r4, ->4 = a tree root), src (r5, ->0 = a target key), (more). Loads the tree root (obj->4->4).
# It then walks the tree: for each node, it checks a flag (node->25); if the flag is set, it bails (a found /
# terminal path). Otherwise it compares the node's ->12 key to the target (src->0): if node->12 < target, it
# advances to node->8 (the right child); otherwise it advances to node->0 (the left child). It continues until it
# finds a node (or reaches the end), returning the found node. So it's a "walk the tree to find the node at / for
# the given key" routine (a binary-tree search keyed on node->12, part of the 0x82C34 cluster). 87 lines, no
# callees (pure data manipulation). Many callers (sub_8288D810, sub_827EF058, LookupAndInsertNode_8323A110, ...).
# The node's class (->0 = left, ->8 = right, ->12 = key, ->25 = a flag) and the tree's meaning (a sorted /
# ordered structure) aren't resolvable, so the name states the mechanism: walk the tree to find the node. The "2"
# distinguishes it from Tree_Search (an earlier entry in the same family).
[entrypoint.functions."0x82C343A0"]
name = "Tree_Search2_82C343A0"

# A validate-and-search: zero the result fields (obj->0, obj->4), search the tree (Tree_Search2_82C343A0), and
# validate the result (raising an assertion, trap 22, if it is invalid).

# Args: obj (r3, ->0, ->4 = result fields), src (r4, ->4, ->8), (r5). Zeros the result fields (obj->0, obj->4).
# It then searches the tree: it calls Tree_Search2_82C343A0 (sub_82C343A0, a binary-tree search, passing the
# source). It validates the result: if the result's ->0 field is 0, or differs from src->4 (a source / parent
# pointer), it raises an assertion (twi 31, r0, 22 -- a trap 22, the game's debug-assert instruction). In a path it
# also calls ChainLookup_82550638 (sub_82550638, a chain lookup) and Release_RefCounted_829FF648 (sub_829FF648, a
# refcounted release). So it's a "search the tree (Tree_Search2) and validate the result (assert it is valid)"
# routine (a validated tree search, part of the 0x8323A cluster). 109 lines, 3 callees (Tree_Search2_82C343A0,
# ChainLookup_82550638, Release_RefCounted_829FF648). 4 callers (sub_83225920, sub_832254B8, sub_83225D80, sub
# _832273F8). The tree's class (a binary tree, a node's ->0 / ->4 = parent / source) and the search's meaning
# aren't resolvable, so the name states the mechanism: validate and search the tree.
[entrypoint.functions."0x8323A058"]
name = "ValidateAndSearch_8323A058"

# A dispose / destroy (with an optional free) -- the sibling of Dispose4-11 (a different type pointer, 7168):
# set a vtable / type pointer, run the 20-byte destructor (Destruct20Byte_832156E0), and free the object
# (Free_SizeBucketed_Thunk_8221BE68) if a flag is set.

# Args: obj (r3, ->0 = a vtable / type pointer), flag (r4, bit 0 = an "owned / free" flag). Sets obj->0 to a
# fixed global (a vtable / type pointer, 0x82CCC8C0 + 7168 -- a different type than the other Dispose variants).
# It then runs the 20-byte destructor (Destruct20Byte_832156E0, sub_832156E0). If the flag's bit 0 is set (the
# object is owned), it frees the object (Free_SizeBucketed_Thunk_8221BE68, sub_8221BE68); otherwise it leaves the
# memory (a destruct-but-don't-free path). So it's a "dispose the object (set the type, run the destructor, free if
# owned)" routine. 56 lines, 2 callees (Destruct20Byte_832156E0, Free_SizeBucketed_Thunk_8221BE68). 0 direct
# callers (reached via a dispatch / vcall, likely a type's destructor / dispose method). A structural sibling of
# Dispose4-11 -- same shape, different type pointer (7168). The object's class (a 20-byte object, ->0 = a vtable)
# and the free flag's meaning aren't resolvable, so the name states the mechanism: dispose the object (with an
# optional free). The "12" distinguishes it from Dispose_82B39A30, Dispose2_8233DD70, Dispose3_8233E380, and
# Dispose4-11.
[entrypoint.functions."0x82A14840"]
name = "Dispose12_82A14840"

# A copy-and-process (the sibling of CopyAndProcess_83214828, using 128-bit vector ops): copy the struct data
# (CopyStructData4_82A1BEA8) to the stack, read the copied values with 128-bit vector loads, store them to the
# object (obj->16, plus a float multiply into obj->16+16), and process (sub_83211D48) if a flag is set.

# Args: obj (r3, ->16 = a 128-bit field), (r4), flag (r5, low byte = a "process" flag). Calls CopyStructData4
# _82A1BEA8 (sub_82A1BEA8) to copy the struct data to the stack. It then reads the copied values with 128-bit
# vector loads (lvx128), and stores them to the object: obj->16 = the copied value (a 128-bit store, stvx128), and
# obj->16+16 = the value squared (a 128-bit float multiply, vmulfp128, of the value by itself -- a float4 squared).
# If the flag's low byte is non-zero (a process flag), it processes the object (calling sub_83211D48, a sibling in
# the 0x83211 cluster). So it's a "copy the struct data, store the key value and its square (a float4 and a
# float4-squared, via 128-bit vector ops), and process the object (if flagged)" routine (a copy-and-process for the
# 0x83214 cluster, a sibling of CopyAndProcess_83214828). 66 lines, 2 callees (CopyStructData4_82A1BEA8, sub
# _83211D48). 0 direct callers (reached via a dispatch / vcall). The object's class (->16 = a float4 value) and the
# flag's meaning (a process flag) aren't resolvable, so the name states the mechanism: copy and process the struct
# data. The "2" distinguishes it from CopyAndProcess_83214828 (which uses scalar 32-bit stores instead of 128-bit
# vector ops).
[entrypoint.functions."0x83214418"]
name = "CopyAndProcess2_83214418"

# A node constructor (the sibling of Construct20ByteNode_83215D98 / Construct20ByteNode2_83215D18 / Construct
# 20ByteNode3_83215CB8 / Construct20ByteNode4_832158B0 / Construct20ByteNode5_83215A80): call Construct20Byte
# _83215640 (the base 20-byte constructor), then set a vtable / type pointer (7168 -- the Dispose12_82A14840 type)
# and a 128-bit vector field from a global float.

# Args: obj (r3, ->0 = a vtable / type pointer, ->16 = a 128-bit vector field). Calls Construct20Byte_83215640
# (sub_83215640, the base 20-byte constructor) to allocate and zero the node. It then sets the node's vtable /
# type pointer (obj->0 = a fixed global, 0x82CCC8C0 + 7168 -- the same type pointer Dispose12_82A14840 uses) and a
# 128-bit vector field (obj->16 = a fixed global float, replicated into a 128-bit vector via vspltw and stored with
# stvx128). So it's a "construct a 20-byte node and set its type and default vector field" routine (a typed node
# constructor, part of the 0x83215 cluster). 60 lines, 1 callee (Construct20Byte_83215640). 3 callers: sub_83218630,
# sub_83219588, ProcessTreeNode_83218C10 (sub_83218C10). A sibling of Construct20ByteNode_83215D98, Construct20
# ByteNode2_83215D18, Construct20ByteNode3_83215CB8, Construct20ByteNode4_832158B0, and Construct20ByteNode5
# _83215A80 -- same base (Construct20Byte), different type / field layout (type 7168, a 128-bit vector field at ->16).
# The node's class (a 20-byte typed record) isn't resolvable, so the name states the mechanism: construct a 20-byte
# node. The "6" distinguishes it from Construct20ByteNode_83215D98, 2, 3, 4, and 5.
[entrypoint.functions."0x83215BF0"]
name = "Construct20ByteNode6_83215BF0"

# A vcall + struct-copy (with teardown): make a vcall (src->0->8, passing the source), then copy the source's
# data (CopyStructData4_82A1BEA8), and process / tear it down (DestroyCircularList_82A0F5C0, Free_SizeBucketed
# _Thunk_8221BE68).

# Args: obj (r3), src (r4, ->0 = a vtable, ->4 = data, ->12 = a byte-array pointer, ->20 = a size), (more). Makes
# a vcall (src->0->8, passing the source). If src->20 >= 4, it copies (src->20 - 4) bytes from src->12 (a byte-by-
# byte loop to the stack, advancing src->12 by 4). It then processes / tears down the data: it calls CopyStructData4
# _82A1BEA8 (sub_82A1BEA8, a struct copy), DestroyCircularList_82A0F5C0 (sub_82A0F5C0, a circular-list destroy),
# Free_SizeBucketed_Thunk_8221BE68 (sub_8221BE68, a free), and several sub_82A0* siblings (sub_82A0ED98, sub_82A0EDF0,
# sub_82A0F328, sub_82A0F800) and sub_82A1B480. So it's a "dispatch a vcall, copy the source's data, and process /
# tear it down (a circular list and a free)" routine (a vcall + struct-copy + teardown, part of the 0x82A0E
# cluster). 541 lines -- a large routine (the body has a long tail of processing beyond the initial vcall and copy).
# 8 callees, 2 vcall dispatches. 1 caller (sub_83220548). The source's class (->0 = a vtable, a byte-array at ->12)
# and the processed data's meaning (a circular list) aren't resolvable, so the name states the mechanism: dispatch a
# vcall and copy the struct's data.
[entrypoint.functions."0x82A0E470"]
name = "DispatchAndCopy_82A0E470"

# A 48-byte node constructor: allocate a 48-byte object (Allocate_OrDbgBreak_8221F388), set up self-references
# (result->0 = result, result->4 = result -- a circular-list / self-referential node), and store the result to the
# caller (obj->4 = result, obj->8 = 0).

# Args: obj (r3, ->4 = the new node, ->8 = a value), (more). Allocates a 48-byte object (Allocate_OrDbgBreak
# _8221F388, sub_8221F388). If the allocation succeeds, it sets up the node's self-references: result->0 = result
# and result->4 = result (the node points to itself, a circular-list / self-referential pattern). It then stores
# the node to the caller: obj->4 = result (the new node), and obj->8 = 0 (a value / count). So it's a "construct a
# 48-byte self-referential node (allocate it, set up its self-references, and return it)" routine (a circular-list
# node constructor, part of the 0x82A0E cluster). 56 lines, 1 callee (Allocate_OrDbgBreak_8221F388). 2 callers: sub
# _82A0E470 (DispatchAndCopy_82A0E470), sub_82A0E838. The node's class (a 48-byte self-referential / circular-list
# node, ->0 and ->4 = self-pointers) isn't resolvable, so the name states the mechanism: construct a 48-byte node.
[entrypoint.functions."0x82A0ED98"]
name = "Construct48ByteNode_82A0ED98"

# A 48-byte node constructor (the sibling of Construct48ByteNode_82A0ED98, a larger variant that loops): if
# obj->8 >= 2, allocate 48-byte objects (Allocate_OrDbgBreak_8221F388), set up self-references (result->0 = result,
# result->4 = result), and loop (allocating more), with a circular-list destroy (DestroyCircularList_82A0F5C0) and
# frees (Free_SizeBucketed_Thunk_8221BE68).

# Args: obj (r3, ->8 = a count), (more). If obj->8 < 2, takes an early path. Otherwise it allocates 48-byte objects
# (Allocate_OrDbgBreak_8221F388, sub_8221F388) and sets up their self-references (result->0 = result, result->4 =
# result -- a circular-list / self-referential pattern). It then loops (allocating more 48-byte objects, and calling
# Allocate_SizeBucketed_Thunk_8221F3F0, sub_8221F3F0, for larger allocations). It also destroys a circular list
# (DestroyCircularList_82A0F5C0, sub_82A0F5C0) and frees objects (Free_SizeBucketed_Thunk_8221BE68, sub_8221BE68) in
# the teardown paths, plus several sub_82A0* siblings (sub_82A0F3B8, sub_82A0F528) and sub_82CBBB58. So it's a
# "construct 48-byte self-referential nodes (allocate them, set up self-references, and loop; with teardown)"
# routine (a larger 48-byte node constructor, part of the 0x82A0E cluster). 393 lines, 7 callees (Allocate_OrDbgBreak
# _8221F388, Allocate_SizeBucketed_Thunk_8221F3F0, DestroyCircularList_82A0F5C0, Free_SizeBucketed_Thunk_8221BE68,
# sub_82A0F3B8, sub_82A0F528, sub_82CBBB58). 2 callers: sub_82A0E838, DispatchAndCopy_82A0E470 (sub_82A0E470). The
# node's class (a 48-byte self-referential / circular-list node, ->0 and ->4 = self-pointers) isn't resolvable, so
# the name states the mechanism: construct a 48-byte node. The "2" distinguishes it from Construct48ByteNode_82A0ED98
# (a smaller single-node variant).
[entrypoint.functions."0x82A0EDF0"]
name = "Construct48ByteNode2_82A0EDF0"

# A sub-object dispose: if a condition holds (src->8 <= 1) and obj->0 is non-null, free the sub-object (Free_Size
# Bucketed_Thunk_8221BE68) and zero the field (obj->0 = 0); otherwise take another path (with allocations).

# Args: obj (r3, ->0 = a sub-object), src (r4, ->8 = a count), (more). If src->8 <= 1 (a small count) and obj->0 is
# non-null, it frees the sub-object (Free_SizeBucketed_Thunk_8221BE68, sub_8221BE68) and zeros the field (obj->0 =
# 0). Otherwise it takes another path (a larger count), which (in a path) allocates (Allocate_OrDbgBreak_8221F388,
# sub_8221F388) and calls sub_82A0FCA8 (a sibling in the 0x82A0F cluster). So it's a "dispose a sub-object (free it
# and zero the field, or take a larger path)" routine (a sub-object teardown, part of the 0x82A0F cluster). 203
# lines, 3 callees (Free_SizeBucketed_Thunk_8221BE68, Allocate_OrDbgBreak_8221F388, sub_82A0FCA8). 2 callers: sub
# _82A0E838, DispatchAndCopy_82A0E470 (sub_82A0E470). The sub-object's class (->0 = a sub-object, a 48-byte / 20-byte
# node) and the count's meaning (src->8 = a count) aren't resolvable, so the name states the mechanism: dispose a
# sub-object.
[entrypoint.functions."0x82A0F800"]
name = "DisposeSubObj_82A0F800"

# A build-and-dispatch: build a stack struct (global floats and a 128-bit vector), then dispatch (DispatchAndCopy
# _82A0E470); free a result (Free_SizeBucketed_Thunk_8221BE68) on a path.

# Args: obj (r3), (r4), value (r5), (more). Builds a stack struct: it loads global floats (into stack[84] and
# stack[88]), zeros a field (stack[80] = 0), and loads a 128-bit vector (lvx128, from an indexed global array based
# on the value). It then dispatches: it calls DispatchAndCopy_82A0E470 (sub_82A0E470, a vcall + struct-copy, passing
# the object and the stack struct). In a path it also frees a result (Free_SizeBucketed_Thunk_8221BE68, sub_8221BE68)
# and calls sub_82A0B930 (a sibling in the 0x82A0B cluster). So it's a "build a stack struct (floats and a 128-bit
# vector) and dispatch on it (a vcall + struct-copy)" routine (a build-and-dispatch, part of the 0x83220 cluster).
# 129 lines, 3 callees (DispatchAndCopy_82A0E470, Free_SizeBucketed_Thunk_8221BE68, sub_82A0B930). 3 callers (sub
# _83216BB0, sub_83217688, sub_8323EB40). The stack struct's meaning (a parameter block with floats and a vector)
# and the value's source (an index) aren't resolvable, so the name states the mechanism: build a struct and dispatch.
[entrypoint.functions."0x83220548"]
name = "BuildAndDispatch_83220548"

# An append-to-a-global-array: append a record (5 words) to a global array (count at ->10244, capacity at ->10240),
# with a capacity check (return early if the array is full).

# Args: (r3), a (r4), b (r5), c (r6), (more). Loads a global array (a fixed .data base): the count (->10244) and
# capacity (->10240). If count == capacity (the array is full), it returns early. Otherwise it appends a record: it
# copies 5 words (a, b, c, and 2 more from the stack, saved at stack[32]) to the array at the count's offset (a
# 5-word record, stored word-by-word), and increments the count (count++). So it's a "append a 5-word record to a
# global array (with a capacity check)" routine (a global array appender, part of the 0x82BAA cluster). 56 lines,
# no callees (pure data manipulation). 1 caller: AdvanceAndDispatch_82BAA4A8 (sub_82BAA4A8). The array's meaning
# (a global list of 5-word records, a log / registry / event list) and the record's fields (a, b, c) aren't
# resolvable, so the name states the mechanism: append to a global array.
[entrypoint.functions."0x82BAA688"]
name = "AppendToGlobalArray_82BAA688"

# A process (with a chain lookup): branch on a condition (src->8), and process the data (sub_82370060, with chain
# lookups via FindChainRoot3_82A962B0 / FindChainRoot4_828836E8).

# Args: obj (r3), src (r4, ->4, ->8 = a condition), flag (r5), value (r6), (more). Branches on src->8: if it is 0,
# it calls sub_82370060 (sub_82370060, a sibling in the 0x8237 cluster, with src->4, a constant, and the value). In
# the other path (src->8 non-zero), it processes the data, calling sub_8236FF20 (a sibling in the 0x8236 cluster)
# and the chain-root finders (FindChainRoot3_82A962B0, sub_82A962B0, and FindChainRoot4_828836E8, sub_828836E8) to
# look up a chain root. So it's a "process the data (dispatch on a condition, with a chain lookup)" routine (a
# process with a chain lookup, part of the 0x8236F cluster). 298 lines, 4 callees (sub_82370060, sub_8236FF20,
# FindChainRoot3_82A962B0, FindChainRoot4_828836E8). 2 callers (sub_82371958, sub_8236E4A0). The object's class
# (->4, ->8 = a condition) and the data's meaning (a chain / tree) aren't resolvable, so the name states the
# mechanism: process the data (with a chain lookup).
[entrypoint.functions."0x8236F398"]
name = "ProcessChainLookup_8236F398"

# A validation + error: check a value (src->8 against a threshold), and (if it is invalid) build an error string
# (SetString_822F2020 + a format, sub_826C3EF0) and raise an error (sub_822F1F00); splice a list (List_SpliceNode2
# _8292CC88) in a path.

# Args: obj (r3), src (r4, ->8 = a value), value (r5), (r6), (r7), (more). Checks the value (src->8) against a
# threshold (0x20000000 | 52427): if src->8 < the threshold, it takes one path; otherwise it builds an error string
# (SetString_822F2020, sub_822F2020, with a string at a fixed .data address, and a format via sub_826C3EF0, sub
# _826C3EF0) and raises an error (sub_822F1F00, an error / raise function). In a path it also splices a list (List
# _SpliceNode2_8292CC88, sub_8292CC88) and calls sub_82171810, sub_8234D600, sub_82370EF8, and sub_826C3FA8. So it's
# a "validate a value (check it against a threshold) and raise an error (build an error string and raise) if it is
# invalid" routine (a validated operation with an error path, part of the 0x82370 cluster). 329 lines, 8 callees
# (SetString_822F2020, sub_822F1F00, List_SpliceNode2_8292CC88, sub_826C3EF0, sub_826C3FA8, sub_82171810, sub
# _8234D600, sub_82370EF8). 2 callers (sub_8236F398, sub_8236FF20). The value's meaning (src->8 = a value, an index
# / count) and the error's meaning (a particular error / assertion) aren't resolvable, so the name states the
# mechanism: raise an error if the value is invalid.
[entrypoint.functions."0x82370060"]
name = "RaiseIfInvalid_82370060"

# A 36-byte object constructor: allocate a 36-byte object (Allocate_OrDbgBreak_8221F388) and set up its fields
# (a, b, c, src->0, a sub-value via SetValue2_8236EC48, and zero flags at ->32 and ->33).

# Args: (r3), a (r4), b (r5), c (r6), src (r7, ->0 = a value), (more). Allocates a 36-byte object (Allocate_OrDbg
# Break_8221F388, sub_8221F388). If the allocation succeeds, it sets up the object's fields: ->0 = a, ->4 = b, ->8
# = c, ->12 = src->0 (a value from the source), ->16 = a sub-value (SetValue2_8236EC48, sub_8236EC48, with the
# object+16 and src+4), and zero flags (->32 = 0, ->33 = 0). So it's a "construct a 36-byte object (allocate it
# and set its fields)" routine. 66 lines, 2 callees (Allocate_OrDbgBreak_8221F388, SetValue2_8236EC48). 1 caller:
# sub_82370060 (RaiseIfInvalid_82370060). The object's class (a 36-byte object, a record with a, b, c, a sub-value,
# and flags) and the source's meaning aren't resolvable, so the name states the mechanism: construct a 36-byte
# object.
[entrypoint.functions."0x82370EF8"]
name = "Construct36Byte_82370EF8"

# A block-size computation: clamp a value (against obj->12), then compute a size (integer arithmetic, shifts,
# divisions, and squares), with traps (twllei) on a zero-division.

# Args: obj (r3, ->8, ->12, ->16, ->28 = parameters), value (r4), (more). Clamps the value: if value < obj->12,
# it uses obj->12 (a minimum). It then computes a size: it reads the object's parameters (obj->8, obj->16, obj->28,
# a shift amount), and performs a sequence of integer operations (shifts, multiplications/squares, subtractions, and
# divisions). It traps (twllei r8, 0 and twllei r6, 0) on a zero-division (a guard against dividing by zero). So it's
# a "compute a block size (clamp the value, and compute the size via integer arithmetic)" routine (a size /
# layout computation, part of the 0x829FD cluster). 65 lines, no callees (pure integer arithmetic). 2 callers: sub
# _83231240, Heap_CarveBlock_83231058 (sub_83231058, a heap block carver). The object's class (->8, ->12, ->16, ->28
# = parameters, a shift amount) and the size's meaning (a heap block size / layout) aren't resolvable, so the name
# states the mechanism: compute a block size.
[entrypoint.functions."0x829FD1C8"]
name = "ComputeBlockSize_829FD1C8"

# A size-checked allocation (the sibling of AllocateChecked_8264EFE0 / AllocateChecked2_8217A488, with a 4x
# multiplier): check the size (an overflow check: size != 0 and -1 / size < 4), compute the allocation size (size *
# 4), and allocate (Allocate_OrDbgBreak_8221F388); raise an error (sub_822F1F00) on a failure path.

# Args: (r3), size (r4), (more). Checks the size: if size is 0, or -1 / size >= 4 (an overflow check -- the
# allocation size would overflow), it takes an error path. Otherwise it computes the allocation size (size * 4) and
# allocates it (Allocate_OrDbgBreak_8221F388, sub_8221F388). On a failure path it calls sub_822F1F00 (an error /
# setup function, with a string at a fixed .data address). So it's a "check the size (an overflow check), compute
# the allocation size (size * 4), and allocate (with an error on failure)" routine (a size-checked allocation of 4-
# byte elements). 66 lines, 2 callees (Allocate_OrDbgBreak_8221F388, sub_822F1F00). Many callers (sub_82AAFB08, sub
# _82C09F00, sub_8247D908, ...). The 4-byte element's meaning (a record / node) and the size's source aren't
# resolvable, so the name states the mechanism: check the size and allocate (size * 4). The "3" distinguishes it
# from AllocateChecked_8264EFE0 (12x) and AllocateChecked2_8217A488 (2x).
[entrypoint.functions."0x82333F78"]
name = "AllocateChecked3_82333F78"

# A value store (the sibling of SetValue_8236FCE8 / SetValue2_8236EC48): compute counts ((obj->12 - obj->4) >> 2,
# a capacity, and (obj->8 - obj->4) >> 2, a used count), and (if there is room) store a value (a float, obj->8->0 =
# the value) and advance the write pointer (obj->8 += 4).

# Args: obj (r3, ->4 = a base, ->8 = a write pointer, ->12 = an end), value (r4, a float source), (more). Computes
# the capacity ((obj->12 - obj->4) >> 2, a count of 4-byte elements) and the used count ((obj->8 - obj->4) >> 2).
# If the used count is less than the capacity (there is room), it stores the value: obj->8->0 = the value (a float,
# loaded from the source), and advances the write pointer (obj->8 += 4). If there is no room, it takes another path
# (a grow / full condition, calling sub_82A0A040, a sibling in the 0x82A0A cluster). So it's a "store a value into a
# 4-byte-stride array (with a bounds check, advancing the write pointer)" routine (a value setter for a growing /
# ring buffer, part of the 0x82A09 cluster). 87 lines, 1 callee (sub_82A0A040). 2 callers (sub_832402B8, sub
# _83221378). The buffer's class (->4 = a base, ->8 = a write pointer, ->12 = an end, a 4-byte-stride array of
# floats) and the value's meaning aren't resolvable, so the name states the mechanism: store a value (with a bounds
# check). The "3" distinguishes it from SetValue_8236FCE8 and SetValue2_8236EC48.
[entrypoint.functions."0x82A09FB0"]
name = "SetValue3_82A09FB0"

# A trivial offset getter: return (obj->12 + index * 8) -- a base plus an index into an 8-byte-stride array.

# Args: obj (r3, ->12 = a base), index (r4), (more). Returns (obj->12 + (index << 3)) = (obj->12 + index * 8) -- a
# base (obj->12) plus an index into an 8-byte-stride array. 3 lines, no callees. A sibling of GetOffset732_82BAA148
# and GetOffset684_82BAA150 (which compute (index + constant) * 16 from a base); this one computes (base + index * 8)
# (an 8-byte stride). It is registered (in the function table, fable_2_init.cpp / fable_2_register.cpp) but has no
# direct callers in the recompiled code (a registered-but-uncalled getter, likely invoked only from code outside the
# traced region, or unused). The array's meaning (an 8-byte-stride array) and the base (obj->12) aren't resolvable,
# so the name states the mechanism: get an offset (base + index * 8). The "8" distinguishes it from the other
# GetOffset* getters.
[entrypoint.functions."0x82BABC60"]
name = "GetOffset8_82BABC60"

# A store-with-grow (the sibling of SetValue3_82A09FB0, the grow path it calls): compute a count ((obj->12 - obj->4)
# >> 2), store values (a float, into a 4-byte-stride array), and (on a grow / full path) reallocate (Allocate
# Checked3_82333F78) and copy the data (CheckedCopy_82CA3808 / MemCpy_Aligned_82CA30E8), freeing the old buffer (Free
# _SizeBucketed_Thunk_8221BE68).

# Args: obj (r3, ->4 = a base, ->8 = a write pointer, ->12 = an end), (r4), count (r5), float (r6), (more). Computes
# the count ((obj->12 - obj->4) >> 2, a count of 4-byte elements). It stores values (a float, into the 4-byte-stride
# array, advancing the write pointer). On a grow / full path (when there is no room), it reallocates (Allocate
# Checked3_82333F78, sub_82333F78, a size-checked allocation of 4-byte elements) and copies the data (CheckedCopy
# _82CA3808, sub_82CA3808, and MemCpy_Aligned_82CA30E8, sub_82CA30E8), freeing the old buffer (Free_SizeBucketed
# _Thunk_8221BE68, sub_8221BE68). It also calls sub_82A97648 (a sibling in the 0x82A97 cluster). So it's a "store
# values into a 4-byte-stride array (with a grow / reallocate path: allocate a larger buffer and copy the data)"
# routine (a store with a grow, part of the 0x82A0A cluster, the grow path of SetValue3_82A09FB0). 394 lines, 5
# callees (AllocateChecked3_82333F78, CheckedCopy_82CA3808, MemCpy_Aligned_82CA30E8, Free_SizeBucketed_Thunk_8221BE
# 68, sub_82A97648). 2 callers: sub_82AB7168, SetValue3_82A09FB0 (sub_82A09FB0). The buffer's class (->4 = a base,
# ->8 = a write pointer, ->12 = an end, a 4-byte-stride array of floats) and the values' meaning aren't resolvable,
# so the name states the mechanism: store values (with a grow / reallocate).
[entrypoint.functions."0x82A0A040"]
name = "StoreValueGrow_82A0A040"

# A size-checked allocation (the sibling of AllocateChecked3_82333F78, with an 8x multiplier): check the size (an
# overflow check: size != 0 and -1 / size < 8), compute the allocation size (size * 8), and allocate (Allocate_OrDbg
# Break_8221F388); raise an error (sub_822F1F00) on a failure path.

# Args: (r3), size (r4), (more). Checks the size: if size is 0, or -1 / size >= 8 (an overflow check -- the
# allocation size would overflow), it takes an error path. Otherwise it computes the allocation size (size * 8) and
# allocates it (Allocate_OrDbgBreak_8221F388, sub_8221F388). On a failure path it calls sub_822F1F00 (an error /
# setup function, with a string at a fixed .data address). So it's a "check the size (an overflow check), compute
# the allocation size (size * 8), and allocate (with an error on failure)" routine (a size-checked allocation of 8-
# byte elements). 66 lines, 2 callees (Allocate_OrDbgBreak_8221F388, sub_822F1F00). Many callers (sub_82A58C68, sub
# _82343560, sub_8260B278, ...). The 8-byte element's meaning (a record / node / pointer) and the size's source
# aren't resolvable, so the name states the mechanism: check the size and allocate (size * 8). The "4" distinguishes
# it from AllocateChecked_8264EFE0 (12x), AllocateChecked2_8217A488 (2x), and AllocateChecked3_82333F78 (4x).
[entrypoint.functions."0x82A87220"]
name = "AllocateChecked4_82A87220"

# An atomic increment (the sibling of AtomicIncrement_822CBEB8): atomically increment a value at (ptr + 4), using an
# lwarx/stwcx. loop under the global lock (mfmsr/mtmsrd to disable interrupts), and return the new value.

# Args: ptr (r3, the target is ptr + 4), (more). Atomically increments the 32-bit value at (ptr + 4): it enters the
# global lock (REX_ENTER_GLOBAL_LOCK, with mfmsr/mtmsrd to disable interrupts), does a load-and-reserve / store-
# conditional (lwarx/stwcx.) to read the value, increment it by 1, and write it back (retrying on a store-conditional
# failure), leaves the global lock, and returns the new value (r3 = the incremented value). So it's an "atomically
# increment a value (at ptr + 4, under the global lock) and return the new value" routine (an atomic increment, the
# sibling of AtomicIncrement_822CBEB8, sub_822CBEB8). 24 lines (to the next function), no callees (pure atomic
# arithmetic). It is registered (in the function table, fable_2_init.cpp / fable_2_register.cpp) but has no direct
# callers in the recompiled code (a registered-but-uncalled atomic increment, likely invoked only from code outside
# the traced region, or unused). The value's meaning (a counter at ptr + 4, a reference count / global counter) isn't
# resolvable, so the name states the mechanism: atomically increment a value (at ptr + 4). The "2" distinguishes it
# from AtomicIncrement_822CBEB8.
[entrypoint.functions."0x82375F28"]
name = "AtomicIncrement2_82375F28"

# A virtual call: load a function pointer from a vtable (obj->8 -> ->24) and call it (bctr), passing the object
# (r3 = obj->8, the vtable-derived this) and the other arguments.

# Args: obj (r3, ->8 = a vtable), (r4), (more). Loads the vtable (obj->8), loads a function pointer from it (vtable->
# 24), sets the count register (ctr = the function pointer), and calls it (bctr -- a virtual call). It passes the
# object (r3 = obj->8, the vtable-derived this) and the other arguments (r4, ...). So it's a "virtual call (dispatch
# via a vtable, calling a virtual method at vtable->24)" routine (a virtual-call thunk, part of the 0x826C0 cluster).
# 4 lines (to the next function), no callees (a single indirect call). It is registered (in the function table, fable
# _2_init.cpp / fable_2_register.cpp) but has no direct callers in the recompiled code (a registered-but-uncalled
# virtual-call thunk, likely invoked only from code outside the traced region, or unused). The vtable's meaning (a
# class, a method at vtable->24) isn't resolvable, so the name states the mechanism: a virtual call (dispatch via a
# vtable). The address suffix distinguishes it from other VirtualCall* thunks.
[entrypoint.functions."0x826C0388"]
name = "VirtualCall_826C0388"

# A trivial field getter: return (obj->16).

# Args: obj (r3, ->16 = a value), (more). Returns (obj->16) -- a field at offset 16. 2 lines (to the next function),
# no callees (pure data access). A sibling of the GetField*/GetGlobalPtr* trivial getters (e.g., GetField12). It is
# registered (in the function table, fable_2_init.cpp / fable_2_register.cpp) but has no direct callers in the
# recompiled code (a registered-but-uncalled getter, likely invoked only from code outside the traced region, or
# unused). The field's meaning (obj->16, a sub-object / value / pointer) isn't resolvable, so the name states the
# mechanism: get a field (obj->16). The "13" distinguishes it from GetField12 and other GetField* getters.
[entrypoint.functions."0x82B495F8"]
name = "GetField13_82B495F8"

# A double-virtual-call: load a vtable (obj->16), call two virtual methods (vtable->24 and vtable->28, via bctrl),
# and return their difference.

# Args: obj (r3, ->16 = a vtable), (more). Loads the vtable (obj->16). It then makes two virtual calls (via bctrl):
# first, it calls the virtual method at vtable->24 (saving the result); second, it calls the virtual method at
# vtable->28 (with the object as the this). It returns the difference (result1 - result2). So it's a "call two
# virtual methods (vtable->24 and vtable->28) and return their difference" routine (a double-virtual-call, part of
# the 0x82C01 cluster). 59 lines, no direct callees (two indirect calls via bctrl, which the callee scan does not
# count). Many callers (sub_82C17AE8, sub_82C18550, sub_82C2B430, ... -- all in the 0x82C1/0x82C2 region). The
# vtable's meaning (a class, two virtual methods at vtable->24 and vtable->28) and the difference's meaning (a
# time / length / size difference) aren't resolvable, so the name states the mechanism: call two virtual methods
# (and return their difference).
[entrypoint.functions."0x82C01D60"]
name = "VirtualCallPair_82C01D60"

# A value store (the sibling of SetValue3_82A09FB0, with an 8-byte stride): compute counts ((obj->12 - obj->4) >> 3,
# a capacity, and (obj->8 - obj->4) >> 3, a used count, in 8-byte units), and (if there is room) store a value (8
# bytes, obj->8->0 and obj->8->4 = the value) and advance the write pointer (obj->8 += 8).

# Args: obj (r3, ->4 = a base, ->8 = a write pointer, ->12 = an end), value (r4, ->0 and ->4 = the value), (more).
# Computes the capacity ((obj->12 - obj->4) >> 3, a count of 8-byte elements) and the used count ((obj->8 - obj->4)
# >> 3). If the used count is less than the capacity (there is room), it stores the value: obj->8->0 = value->0 and
# obj->8->4 = value->4 (an 8-byte value, two words), and advances the write pointer (obj->8 += 8). If there is no
# room, it takes another path (a grow / full condition, calling sub_8260B008, a sibling in the 0x8260B cluster --
# the grow path, as in SetValue3_82A09FB0 which calls StoreValueGrow_82A0A040). So it's a "store an 8-byte value
# into an 8-byte-stride array (with a bounds check, advancing the write pointer)" routine (a value setter for a
# growing buffer, part of the 0x829FD cluster). 122 lines, 1 callee (sub_8260B008). Many callers (sub_8322C450, sub
# _82A08EB0, sub_83221548, ...). The buffer's class (->4 = a base, ->8 = a write pointer, ->12 = an end, an 8-byte-
# stride array) and the value's meaning (an 8-byte record / pointer / pair) aren't resolvable, so the name states
# the mechanism: store a value (8 bytes, with a bounds check). The "4" distinguishes it from SetValue3_82A09FB0
# (4 bytes).
[entrypoint.functions."0x829FD7B8"]
name = "SetValue4_829FD7B8"

# A store-with-grow (the sibling of StoreValueGrow_82A0A040, the 8-byte grow path of SetValue4_829FD7B8): atomically
# increment a counter (src->4, an lwarx/stwcx loop under the global lock), then process (grow the buffer: allocate
# via AllocateChecked4_82A87220, copy the data, and manage refcounts via Release_RefCounted_829FF648 / Copy
# RefcountedArray_827F2010), freeing the old buffer (Free_SizeBucketed_Thunk_8221BE68).

# Args: obj (r3), (r4), src (r6, ->0, ->4 = a counter), (more). If src->4 (a counter) is non-zero, it atomically
# increments it (an lwarx/stwcx loop under the global lock, with mfmsr/mtmsrd to disable interrupts). It then
# processes the data (a grow / reallocate): it allocates (AllocateChecked4_82A87220, sub_82A87220, a size-checked
# allocation of 8-byte elements), copies the data, and manages refcounts (Release_RefCounted_829FF648, sub_829FF648,
# and CopyRefcountedArray_827F2010, sub_827F2010), freeing the old buffer (Free_SizeBucketed_Thunk_8221BE68, sub
# _8221BE68). It also calls sub_8284B558, sub_829FA6A0, and sub_82A97648 (siblings in the 0x8284B/0x829FA/0x82A97
# clusters). So it's a "store values into an 8-byte-stride array (with a grow / reallocate path: atomically update a
# counter, allocate a larger buffer, and manage refcounts)" routine (a store with a grow, part of the 0x8260B cluster,
# the 8-byte grow path of SetValue4_829FD7B8). 369 lines, 7 callees (AllocateChecked4_82A87220, Release_RefCounted
# _829FF648, CopyRefcountedArray_827F2010, Free_SizeBucketed_Thunk_8221BE68, sub_8284B558, sub_829FA6A0, sub_82A97648
# ). Many callers (sub_8234FBE0, sub_82607260, sub_82769838, ...). The buffer's class (->4 = a base, ->8 = a write
# pointer, an 8-byte-stride array) and the counter's meaning (src->4 = a refcount / counter) aren't resolvable, so
# the name states the mechanism: store values (with a grow / reallocate, atomically updating a counter). The "2"
# distinguishes it from StoreValueGrow_82A0A040 (the 4-byte version).
[entrypoint.functions."0x8260B008"]
name = "StoreValueGrow2_8260B008"

# A copy-with-refcount-increments (the sibling of CopyRefcountedArray_827F2010): loop (count times), copy 8-byte
# values (src->0 and src->4 -> dst), and (if the value's refcount, src->4, is non-zero) atomically increment it (an
# lwarx/stwcx loop under the global lock).

# Args: (r3), dst (r4, a write pointer), count (r5), src (r6, ->0 and ->4 = a value with a refcount), (more). Loops
# (count times): it copies an 8-byte value (src->0 -> dst->0 and src->4 -> dst->4), and advances the write pointer
# (dst += 8). If the value's refcount (src->4) is non-zero, it atomically increments it (an lwarx/stwcx loop under
# the global lock, with mfmsr/mtmsrd to disable interrupts). If the write pointer (dst) is null, it breaks (a
# bounds check). So it's a "copy 8-byte values (with a refcount increment: atomically increment the refcount of each
# copied value)" routine (a copy with refcount increments, part of the 0x829FA cluster). 69 lines, no direct callees
# (the atomic increment is inline, which the callee scan does not count). 5 callers (sub_8260B008, sub_827F1AB0, sub
# _82B63FC0, sub_824E02A8, sub_82925A20). The value's class (an 8-byte value, ->0 = a pointer, ->4 = a refcount)
# and the source/destination's meaning (arrays of refcounted values) aren't resolvable, so the name states the
# mechanism: copy values (with refcount increments).
[entrypoint.functions."0x829FA6A0"]
name = "CopyRefcountedValues_829FA6A0"

# A floating-point computation (a periodic / wrapped value): compute a wrapped value from a float (fabs, an integer
# conversion (fctidz/fcfid), the fractional part, and a series of fsel selects, against two float constants).

# Args: (r3), (r4), value (f1), (more). Computes a wrapped value from the input (value, in f1): it converts the value
# to an integer and back (fctidz/fcfid, an integer part / floor), computes the absolute value (fabs), the fractional
# part (value - floor), and two constants (loaded from fixed .data addresses, 0x82...3248 and 0x82...3288). It then
# applies a series of fsel (floating-point select) operations (picking a result based on the sign of intermediate
# values), and returns the result in f1. So it's a "compute a wrapped value (a periodic / wrapped floating-point
# value, using fabs, the fractional part, and selects against two constants)" routine (a floating-point math helper,
# part of the 0x8222C cluster). 35 lines, no callees (pure floating-point arithmetic). Many callers (sub_8249A300,
# sub_82F11B70, sub_82F7F4A8, ... -- many in the 0x82F/0x830 rendering / math region). The exact computation (the two
# float constants at 0x82...3248 and 0x82...3288, and the wrap period) aren't resolvable from the code alone, so the
# name states the mechanism: compute a wrapped value (a periodic / wrapped float).
[entrypoint.functions."0x8222C3E8"]
name = "ComputeWrapped_8222C3E8"

# A call-and-free: call sub_83232128 (with the object and a flag), and (if the flag's low bit is set) free a
# resource (Free_SizeBucketed_Thunk_8221BE68).

# Args: obj (r3), flag (r4), (more). Calls sub_83232128 (a sibling in the 0x83232 cluster, with the object and the
# flag). If the flag's low bit is set (flag & 1), it frees a resource (Free_SizeBucketed_Thunk_8221BE68, sub_8221BE68,
# a size-bucketed free). So it's a "call a function (sub_83232128, with the object and a flag) and (if the flag's low
# bit is set) free a resource" routine (a call-and-free, part of the 0x82A13 cluster). 45 lines (to the next function
# ), 2 callees (sub_83232128, Free_SizeBucketed_Thunk_8221BE68). It is registered (in the function table, fable
# _2_init.cpp / fable_2_register.cpp) but has no direct callers in the recompiled code (a registered-but-uncalled
# call-and-free, likely invoked only from code outside the traced region, or unused). The object's class and the
# flag's meaning (a free-after-call flag) aren't resolvable, so the name states the mechanism: call a function (and
# free a resource conditionally).
[entrypoint.functions."0x82A13C50"]
name = "CallAndFree_82A13C50"

# A search-and-assign (the sibling of LookupAndAssign_8323A1E8): call ValidateAndSearch_8323A058 (a validated tree
# search), and (if the found value differs from the object's current value) release the old value (Release_RefCounted
# _829FF648) and assign the new one (obj->0 and obj->4 = the found value).

# Args: obj (r3, ->0 and ->4 = a refcounted value), value (r4), (more). Zeros the object (obj->0 = 0, obj->4 = 0),
# then calls ValidateAndSearch_8323A058 (sub_8323A058, a validated tree search with a trap 22 assertion, with a stack
# buffer and the value). It compares the found value (obj->4, from the search) against the object's current value
# (stack[84]): if they differ, it releases the old value (Release_RefCounted_829FF648, sub_829FF648, a refcounted
# release), and assigns the new value (obj->0 = the found pointer, obj->4 = the found refcount). So it's a "search
# for a value (a validated tree search) and assign it (a refcounted assign: release the old value and assign the new
# one)" routine (a search-and-assign, part of the 0x83225 cluster). 75 lines, 2 callees (ValidateAndSearch_8323A058,
# Release_RefCounted_829FF648). 2 callers (sub_83225E80, sub_83225F18). The object's class (->0 = a refcounted value,
# ->4 = a refcount) and the tree's meaning (a searched tree) aren't resolvable, so the name states the mechanism:
# search for a value (and assign it, a refcounted assign). A sibling of LookupAndAssign_8323A1E8 (a chain-lookup +
# refcounted assign).
[entrypoint.functions."0x83225D80"]
name = "SearchAndAssign_83225D80"

# A process (with a search-and-assign): copy a struct (CopyStructData5_83217EC8), search and assign a refcounted
# value (SearchAndAssign_83225D80), and (depending on the result) release a value (Release_RefCounted_829FF648) and
# call sibling operations (sub_83218B40, sub_823BE9C0).

# Args: obj (r3, ->0 = a refcounted value), value (r4), flag (r5), (more). It copies a struct (CopyStructData5_83217
# EC8, sub_83217EC8, a byte-by-byte variable-length copy, with the value and the flag), then searches and assigns a
# refcounted value (SearchAndAssign_83225D80, sub_83225D80, a validated tree search + refcounted assign, with the
# object and the copied struct). If obj->0 (the assigned value) is non-null, it takes one path; otherwise it releases
# a value (Release_RefCounted_829FF648, sub_829FF648, a refcounted release) and calls sibling operations (sub_83218B
# 40 and sub_823BE9C0). So it's a "process (copy a struct, search and assign a refcounted value, and handle the
# result: release a value and call sibling operations)" routine (a process with a search-and-assign, part of the
# 0x83225 cluster). 91 lines, 5 callees (CopyStructData5_83217EC8, SearchAndAssign_83225D80, Release_RefCounted_829
# FF648, sub_83218B40, sub_823BE9C0). 1 caller: sub_83232200. The object's class (->0 = a refcounted value) and the
# value's meaning (a searched tree) aren't resolvable, so the name states the mechanism: process (copy a struct,
# search and assign a refcounted value, and handle the result).
[entrypoint.functions."0x83225E80"]
name = "ProcessSearchAndAssign_83225E80"

# A virtual call (the sibling of VirtualCall_826C0388): load a function pointer from a vtable (obj->20 -> ->20) and
# call it (bctr), passing the object (r3 = obj->20, the vtable-derived this) and the other arguments.

# Args: obj (r3, ->20 = a vtable), (more). Loads the vtable (obj->20), loads a function pointer from it (vtable->20),
# sets the count register (ctr = the function pointer), and calls it (bctr -- a virtual call). It passes the object
# (r3 = obj->20, the vtable-derived this) and the other arguments. So it's a "virtual call (dispatch via a vtable,
# calling a virtual method at vtable->20)" routine (a virtual-call thunk, part of the 0x83231 cluster). 4 lines (to
# the next function), no callees (a single indirect call). It is registered (in the function table, fable_2_init.cpp
# / fable_2_register.cpp) but has no direct callers in the recompiled code (a registered-but-uncalled virtual-call
# thunk, likely invoked only from code outside the traced region, or unused). The vtable's meaning (a class, a method
# at vtable->20) isn't resolvable, so the name states the mechanism: a virtual call (dispatch via a vtable). The "2"
# distinguishes it from VirtualCall_826C0388.
[entrypoint.functions."0x83231E88"]
name = "VirtualCall2_83231E88"

# A destructor (a tree owner): set a type pointer (obj->0 = a constant), destroy the tree nodes (DestroyTreeNodes_82
# 859340), zero the object's fields (obj->12->4, obj->16, and the circular self-references at obj->12->0 and ->8),
# and release the refcounted value (Release_RefCounted_829FF648, with a dispose via Dispose2_8233DD70).

# Args: obj (r3, ->0 = a type pointer, ->12 = a tree, ->16, ->20 = a refcounted value), (more). Sets the type pointer
# (obj->0 = a constant, a vtable / type pointer at a fixed .data address). It then destroys the tree nodes (Destroy
# TreeNodes_82859340, sub_82859340, with the tree's root). It zeros the object's fields (obj->12->4 = 0, obj->16 =
# 0, and the circular self-references at obj->12->0 and obj->12->8), and releases the refcounted value (Release_Ref
# Counted_829FF648, sub_829FF648, a refcounted release, with the object+20). It also disposes a value (Dispose2_82
# 33DD70, sub_8233DD70, a 20-byte node dispose). So it's a "destructor (set a type pointer, destroy the tree nodes,
# zero the fields, and release the refcounted value)" routine (a destructor for a tree-owning object, part of the
# 0x83232 cluster). 74 lines, 3 callees (DestroyTreeNodes_82859340, Release_RefCounted_829FF648, Dispose2_8233DD70).
# 1 caller: sub_82A13C50 (CallAndFree_82A13C50). The object's class (->0 = a type pointer, ->12 = a tree, ->20 = a
# refcounted value, a tree-owning object) and the tree's meaning aren't resolvable, so the name states the mechanism:
# a destructor (destroys tree nodes and releases a refcounted value).
[entrypoint.functions."0x83232128"]
name = "DestructorTree_83232128"

# A process (with a copy and search): do a virtual call (src->0 -> ->8), and (if src->20 >= 4) copy a struct (byte-by
# byte, from src->12), then search and assign (ProcessSearchAndAssign_83225E80), releasing a value (Release_Ref
# Counted_829FF648).

# Args: obj (r3), src (r4, ->0, ->4, ->12, ->20 = a count), (r5), (more). It does a virtual call (via bctrl, calling
# the virtual method at src->0->8, with src as the this). If src->20 is >= 4 (a count), it copies a struct (byte-by
# byte, from src->12 into a stack buffer, a variable-length copy, calling sub_82A1B480, a copy helper). It then
# searches and assigns a refcounted value (ProcessSearchAndAssign_83225E80, sub_83225E80, a copy + validated tree
# search + refcounted assign). It also releases a value (Release_RefCounted_829FF648, sub_829FF648, a refcounted
# release). So it's a "process (do a virtual call, copy a struct, and search and assign a refcounted value, releasing
# a value)" routine (a process with a copy and search, part of the 0x83232 cluster). 390 lines, 3 callees (Process
# SearchAndAssign_83225E80, Release_RefCounted_829FF648, sub_82A1B480). It is registered (in the function table, fable
# _2_init.cpp / fable_2_register.cpp) but has no direct callers in the recompiled code (a registered-but-uncalled
# process, likely invoked only from code outside the traced region, or unused). The object's class (->0, ->12, ->20
# = a count, a struct with a virtual method and a variable-length payload) and the source's meaning aren't
# resolvable, so the name states the mechanism: process (a virtual call, copy a struct, and search and assign).
[entrypoint.functions."0x83232200"]
name = "ProcessCopyAndSearch_83232200"

# A bubble sort (the sibling of BubbleSortPointers_82C3C...): sort an array (a bubble-sort-like loop, comparing
# adjacent elements and swapping them if they are out of order).

# Args: a (r3, an array), end (r4), (more). If a == end (an empty array), it returns (nothing to sort). Otherwise it
# does a bubble-sort-like sort: it walks the array (from a to end, in 4-byte strides), comparing adjacent elements
# (a->0 and a+4->0, and on a tie, a+4->4 and a->4, a two-key comparison), and swaps them (a bubble-sort swap) if
# they are out of order. It also calls Process2DRegion_82C3C010 (sub_82C3C010, a 2D-region processor, in a path). So
# it's a "sort an array (a bubble sort, comparing adjacent elements by two keys and swapping them if out of order)"
# routine (a bubble sort, part of the 0x82C28 cluster). 135 lines, 1 callee (Process2DRegion_82C3C010). 1 caller: sub
# _82C28D60 (the next function in the TODO). The array's element type (a two-key record, ->0 = a primary key, ->4 = a
# secondary key) and the sort's meaning (a sorted array of records) aren't resolvable, so the name states the
# mechanism: a bubble sort (comparing adjacent elements by two keys and swapping them). The "2" distinguishes it from
# BubbleSortPointers_82C3C... (an earlier bubble sort in the 0x82C3C cluster).
[entrypoint.functions."0x82C28900"]
name = "BubbleSort2_82C28900"

# A quick sort (the caller of BubbleSort2_82C28900): if the array is small ((end - a) >> 2 <= 32 elements), use a
# bubble sort (BubbleSort2_82C28900); otherwise do a quicksort-like partition (a loop, count times, calling sub
# _82C19F30 for a partition, and recursing).

# Args: a (r3, an array), end (r4), count (r5), (r6), (more). If the array is small ((end - a) >> 2 <= 32 elements,
# a 32-element threshold), it uses a bubble sort (BubbleSort2_82C28900, sub_82C28900, a two-key bubble sort). If the
# array is large, it does a quicksort-like partition: it loops (count times, a depth / recursion limit), calling sub
# _82C19F30 (a partition function, with the array, the range, and a stack buffer), and recurses (a recursive call to
# itself, sub_82C28D60). It also calls sub_82C1A2D0 and sub_82C28B48 (siblings in the 0x82C1A/0x82C28 clusters). So
# it's a "sort an array (a quicksort: partition and recurse, with a bubble sort for small arrays)" routine (a quick
# sort, part of the 0x82C28 cluster). 176 lines, 5 callees (sub_82C19F30, BubbleSort2_82C28900, sub_82C1A2D0, sub
# _82C28B48, and a recursive call to itself). 2 callers (sub_82C28EF0, sub_82C28E80). The array's element type (a two
# -key record, a sorted array of records) and the count's meaning (a depth / recursion limit) aren't resolvable, so
# the name states the mechanism: a quicksort (partition and recurse, with a bubble sort for small arrays). The "2"
# distinguishes it from an earlier quicksort (if any).
[entrypoint.functions."0x82C28D60"]
name = "QuickSort2_82C28D60"

# A partition sort (the callee of the QuickSort2 partition function, sub_82C19F30): if the array is large (a 40-
# element threshold), compute a partition size ((count + 1) / 8, with offsets), and sort a partition (SortPointers_82
# C280C0).

# Args: a (r3, an array), end (r4), (r5 = a range end), (r6), (more). If the array is small ((r5 - a) >> 2 <= 40
# elements, a 40-element threshold), it takes one path (a small partition). If the array is large, it computes a
# partition size ((count + 1) / 8, with offsets at 8-byte strides), and sorts a partition (SortPointers_82C280C0, sub
# _82C280C0, a pointer sort, with the array, a partition range, and the offsets). So it's a "sort a partition (compute
# a partition size and sort it, via SortPointers)" routine (a partition sort, part of the 0x82C17 cluster, the callee
# of the QuickSort2 partition function sub_82C19F30). 105 lines, 1 callee (SortPointers_82C280C0). 1 caller: sub
# _82C19F30 (the QuickSort2 partition function, which is called by QuickSort2_82C28D60). The array's element type (a
# pointer array, a sorted array of pointers) and the partition's meaning (a quicksort partition) aren't resolvable,
# so the name states the mechanism: sort a partition (compute a partition size and sort it, via SortPointers).
[entrypoint.functions."0x82C170D0"]
name = "SortPartition_82C170D0"

# A quicksort partition (the callee of QuickSort2_82C28D60): compute a midpoint, sort a partition (SortPartition_82
# C170D0), and do a 3-way partition (a loop, comparing and moving elements around a pivot).

# Args: a (r3, an array), start (r4), end (r5), (r6), (more). It computes a midpoint ((end - start) / 4 / 2, a
# midpoint offset, with 8-byte strides), and sorts a partition (SortPartition_82C170D0, sub_82C170D0, a partition sort
# via SortPointers_82C280C0, with the array, the start, the midpoint, and the offsets). It then does a 3-way
# partition: it loops (comparing elements against a pivot, and moving elements to the left, right, or keeping them in
# the middle, a 3-way quicksort partition). So it's a "partition (compute a midpoint, sort a partition, and do a 3-way
# quicksort partition)" routine (a quicksort partition, part of the 0x82C19 cluster, the callee of QuickSort2_82C28D
# 60). 398 lines, 1 callee (SortPartition_82C170D0). 1 caller: sub_82C28D60 (QuickSort2_82C28D60, the quicksort that
# calls this partition function). The array's element type (a pointer / two-key record array) and the pivot's meaning
# aren't resolvable, so the name states the mechanism: a quicksort partition (compute a midpoint, sort a partition, and
# do a 3-way partition). The "2" distinguishes it from an earlier partition function (if any).
[entrypoint.functions."0x82C19F30"]
name = "Partition2_82C19F30"

# A validate-and-dispatch: validate (a trap 22 debug assertion: obj->4 <= obj->8, checked twice), build a stack
# struct (obj, obj->8, obj->4), and dispatch (sub_82BFA510).

# Args: obj (r3, ->4, ->8), (more). Validates: it checks that obj->4 <= obj->8 (a debug assertion, a trap 22 (ppc_trap
# (ctx, base, 22)) if the assertion fails -- a bounds / index check). It builds a stack struct (obj, obj->8, obj->4,
# saved at stack[80], stack[88], and stack[92]), re-validates (another trap 22), and dispatches: it calls sub_82BFA5
# 10 (a sibling in the 0x82BFA cluster, with the stack struct). So it's a "validate (a trap 22 debug assertion: obj->
# 4 <= obj->8) and dispatch (a call with a stack struct)" routine (a validated dispatch, part of the 0x8236F cluster).
# 58 lines, 1 callee (sub_82BFA510). 4 callers (sub_825C4EE8, sub_823722E0, sub_8236ED00, sub_82B1BF08). The object's
# class (->4 = an index, ->8 = a count / capacity, an object with a bounds check) and the dispatch's meaning (a
# particular operation) aren't resolvable, so the name states the mechanism: validate (a trap 22 assertion) and
# dispatch (a call). A sibling of ValidateAndSearch_8323A058 (a validated tree search with a trap 22 assertion).
[entrypoint.functions."0x8236FC88"]
name = "ValidateAndDispatch_8236FC88"

# A store-with-grow (the sibling of StoreValueGrow_82A0A040 / StoreValueGrow2_8260B008): compute a count ((obj->12 -
# obj->4) >> 2), store values (a value, into a 4-byte-stride array), and (on a grow / full path) reallocate (Construct
# Array4_823093F8) and copy the data (CheckedCopy_82CA3808), freeing the old buffer (Free_SizeBucketed_Thunk_8221BE68
# ).

# Args: obj (r3, ->4 = a base, ->8 = a write pointer, ->12 = an end), (r4), count (r5), value (r6, ->0 = a value),
# (more). Computes the count ((obj->12 - obj->4) >> 2, a count of 4-byte elements). It stores values (a value, into
# the 4-byte-stride array, advancing the write pointer). On a grow / full path (when there is no room), it
# reallocates (ConstructArray4_823093F8, sub_823093F8, a 4-byte array constructor) and copies the data (CheckedCopy
# _82CA3808, sub_82CA3808), freeing the old buffer (Free_SizeBucketed_Thunk_8221BE68, sub_8221BE68). It also calls
# sub_82684B38 (a sibling in the 0x82684 cluster). So it's a "store values into a 4-byte-stride array (with a grow /
# reallocate path: allocate a larger buffer and copy the data)" routine (a store with a grow, part of the 0x82370
# cluster). 443 lines, 4 callees (ConstructArray4_823093F8, CheckedCopy_82CA3808, Free_SizeBucketed_Thunk_8221BE68,
# sub_82684B38). 2 callers (sub_8236FBA8, sub_823723F0). The buffer's class (->4 = a base, ->8 = a write pointer,
# ->12 = an end, a 4-byte-stride array) and the values' meaning aren't resolvable, so the name states the mechanism:
# store values (with a grow / reallocate). The "3" distinguishes it from StoreValueGrow_82A0A040 and StoreValueGrow2
# _8260B008.
[entrypoint.functions."0x82370688"]
name = "StoreValueGrow3_82370688"

# A grow-and-copy (the callee of SetValue2_8236EC48): compute a count ((b - c) >> 2, a count of 4-byte elements), and
# (if non-zero) copy the data (CheckedCopy_82CA3808, a size-checked copy) to the new location.

# Args: obj (r3), a (r4, a source), b (r5, a write pointer), c (r6, a base), (more). Computes the count ((b - c) >> 2
# , a count of 4-byte elements). If the count is non-zero, it copies the data (CheckedCopy_82CA3808, sub_82CA3808, a
# size-checked copy, with the base, the count * 4, and the new location). So it's a "grow (compute a count) and copy
# the data (a size-checked copy to the new location)" routine (a grow-and-copy, part of the 0x82371 cluster, the
# callee of SetValue2_8236EC48 -- the grow path that copies the data to a new, larger buffer). 55 lines, 1 callee
# (CheckedCopy_82CA3808). 1 caller: SetValue2_8236EC48 (sub_8236EC48, a value setter, which calls this to copy the
# data on a grow). The buffer's class (a 4-byte-stride array, a source and a base) and the copy's meaning (a grow /
# reallocate copy) aren't resolvable, so the name states the mechanism: grow (compute a count) and copy the data (a
# size-checked copy).
[entrypoint.functions."0x82371378"]
name = "GrowAndCopy_82371378"

# A process (with a validate-and-dispatch and a vcall): call ValidateAndDispatch_8236FC88 (a validated dispatch), and
# (if obj->4 != 0) do a vcall (SubObject_VcallSlot3_GatedStore32_82C842C8) and store the result (obj->4 = the result);
# (if the dispatch's result is non-null) process (a bit check on obj->5).

# Args: (r3), obj (r4, ->4 = a flag, ->5 = a bit field), value (r5), (more). It calls ValidateAndDispatch_8236FC88
# (sub_8236FC88, a validated dispatch with a trap 22 assertion, with the value). If obj->4 (a flag) is non-zero, it
# does a vcall (SubObject_VcallSlot3_GatedStore32_82C842C8, sub_82C842C8, a virtual call with a gated 32-bit store,
# with the object and a stack buffer), and stores the result (obj->4 = the result). If the dispatch's result (stack
# [80]) is non-null, it processes: it checks a bit (obj->5, a bit field, with a count-leading-zeros (cntlzw) and a bit
# test), and calls sibling operations (sub_8236EED8 and sub_823723F0). So it's a "process (a validate-and-dispatch, a
# vcall (a gated store), and a bit check)" routine (a process with a validate-and-dispatch and a vcall, part of the
# 0x82372 cluster). 159 lines, 4 callees (ValidateAndDispatch_8236FC88, SubObject_VcallSlot3_GatedStore32_82C842C8,
# sub_8236EED8, sub_823723F0). 2 callers (sub_82C67878, sub_82371958). The object's class (->4 = a flag, ->5 = a bit
# field, an object with a gated store and a bit field) and the value's meaning aren't resolvable, so the name states
# the mechanism: process (a validate-and-dispatch, a vcall, and a bit check).
[entrypoint.functions."0x823722E0"]
name = "ProcessDispatchAndVcall_823722E0"

# A value store (the sibling of SetValue3_82A09FB0 / SetValue4_829FD7B8, with an index and a grow): compute a count
# ((obj->8 - obj->4) >> 2) and an offset ((obj->4 - obj->8) >> 2), and store a value (at the offset, into a 4-byte-
# stride array), with a grow (StoreValueGrow3_82370688) on a full path.

# Args: obj (r3, ->4, ->8 = a 4-byte-stride array), index (r4), value (r5), (more). Computes the count ((obj->8 -
# obj->4) >> 2, a count of 4-byte elements). If the count is less than the index (out of range), it takes a grow /
# error path. Otherwise it computes an offset ((obj->4 - obj->8) >> 2) and stores the value (at the offset, into the
# 4-byte-stride array). On a grow / full path, it calls StoreValueGrow3_82370688 (sub_82370688, a store with a grow,
# which reallocates and copies the data). It also calls sub_82370A48 and sub_82BFA510 (siblings in the 0x82370/0x82BFA
# clusters). So it's a "store a value (with an index, at an offset, into a 4-byte-stride array, with a grow)" routine
# (a value setter for a growing array, part of the 0x82372 cluster). 164 lines, 3 callees (StoreValueGrow3_82370688,
# sub_82370A48, sub_82BFA510). 2 callers: sub_823722E0 (ProcessDispatchAndVcall_823722E0), sub_827BFCD8. The
# buffer's class (->4, ->8 = a 4-byte-stride array) and the value's meaning aren't resolvable, so the name states the
# mechanism: store a value (with an index, at an offset, with a grow). The "5" distinguishes it from SetValue3_82A09
# FB0 (4 bytes) and SetValue4_829FD7B8 (8 bytes).
[entrypoint.functions."0x823723F0"]
name = "StoreValue5_823723F0"

# A validate-and-copy: validate (a trap 22 debug assertion: count == 0 or count == index), and (if the source differs
# from the destination) copy the data (CheckedCopy_82CA3808, a size-checked copy).

# Args: a (r3, a source), obj (r4, ->8), count (r5), index (r6), (more). Validates: it checks that count is 0, or
# count == index (a debug assertion, a trap 22 (ppc_trap (ctx, base, 22)) if the assertion fails -- a bounds / index
# check). If the source (a) differs from the destination (r5), it computes a count ((obj->8 - r5) >> 2, a count of
# 4-byte elements), and copies the data (CheckedCopy_82CA3808, sub_82CA3808, a size-checked copy, with the source, the
# count * 4, and the destination). So it's a "validate (a trap 22 debug assertion: count == 0 or count == index) and
# copy the data (a size-checked copy)" routine (a validated copy, part of the 0x82BFA cluster). 80 lines, 1 callee
# (CheckedCopy_82CA3808). 4 callers (sub_8236FC88, sub_823723F0, sub_82BF96E8, sub_82628158). The source/destination's
# meaning (a 4-byte-stride array, a copy) and the count/index's meaning (a bounds check) aren't resolvable, so the
# name states the mechanism: validate (a trap 22 assertion) and copy the data (a size-checked copy). A sibling of
# ValidateAndDispatch_8236FC88 (a validated dispatch with a trap 22 assertion).
[entrypoint.functions."0x82BFA510"]
name = "ValidateAndCopy_82BFA510"

# An atomic increment (the sibling of AtomicIncrement_822CBEB8 / AtomicIncrement2_82375F28): zero a field (obj->0 =
# 0), and atomically increment a global (an lwarx/stwcx loop under the global lock).

# Args: obj (r3, ->0 = a field), (more). It zeros a field (obj->0 = 0). It then atomically increments a global (a
# fixed .data address, 0x82...28344): it enters the global lock (REX_ENTER_GLOBAL_LOCK, with mfmsr/mtmsrd to disable
# interrupts), does a load-and-reserve / store-conditional (lwarx/stwcx.) to read the global, increment it by 1, and
# write it back (retrying on a store-conditional failure), and leaves the global lock. So it's a "zero a field (obj->0
# = 0) and atomically increment a global (under the global lock)" routine (an atomic increment with a field zeroing,
# part of the 0x821F7 cluster). 41 lines (to the next function), no direct callees (the atomic increment is inline,
# which the callee scan does not count). Many callers (DispatchVcallSequence3_82C1F380, sub_82BFE700, Dispatch_Vcall
# Sequence2_82C1E360, ...). The field's meaning (obj->0 = a field, a reset / cleared flag) and the global's meaning (a
# global counter, a count of an operation) aren't resolvable, so the name states the mechanism: zero a field (and
# atomically increment a global). The "3" distinguishes it from AtomicIncrement_822CBEB8 and AtomicIncrement2_82375F
# 28.
[entrypoint.functions."0x821F7798"]
name = "AtomicIncrement3_821F7798"

# A process (with a dispatch): call DispatchValue_821CF230 (a value dispatch), zero a field (src->6 = 0), and (if the
# result matches a constant) process (a constant check on value->4, and sibling operations sub_82BC9860 / sub_82BCA
# 3E8).

# Args: obj (r3), src (r4, ->6 = a flag), value (r5, ->4 = a value), (more). It calls DispatchValue_821CF230 (sub
# _821CF230, a value dispatch, with the source and the value), and zeros a field (src->6 = 0, a flag / state byte).
# If the dispatch's result matches a constant (a specific value, at a fixed .data address), it processes: it checks a
# constant (value->4, against 0 and 3, a small integer), and calls sibling operations (sub_82BC9860 and sub_82BCA3E8
# , in the 0x82BC9/0x82BCA cluster -- state-dispatch siblings). So it's a "process (a value dispatch, zero a flag,
# and (if the result matches a constant) a constant check and sibling operations)" routine (a process with a dispatch
# , part of the 0x8229A cluster). 91 lines, 3 callees (DispatchValue_821CF230, sub_82BC9860, sub_82BCA3E8). 5 callers
# (sub_82BC9FD0, sub_822A5E98, sub_82BCA3E8, sub_82BE1C40, sub_8229A518). The object's class (->6 = a flag, ->4 = a
# small integer, an object with a dispatch and a flag) and the value's meaning aren't resolvable, so the name states
# the mechanism: process (a value dispatch, zero a flag, and a constant check).
[entrypoint.functions."0x8229A488"]
name = "ProcessDispatchValue_8229A488"

# A list splice (the sibling of List_SpliceNode2_8292CC88): splice / insert a node into a doubly-linked list
# (manipulating the ->4, ->8, and ->33 pointers, with comparisons to place the node correctly).

# Args: list (r3, ->4 = a node), node (r4, ->0, ->4, ->8, ->33 = a flag), (more). It splices / inserts the node into a
# doubly-linked list: it reads the list's nodes (list->4, a node), reads the node's fields (node->0, a next pointer,
# node->4 and node->8, the doubly-linked list pointers, and node->33, a flag byte), and manipulates the pointers
# (with comparisons, placing the node correctly in the list). If a flag (node->33) is zero, it does one splice; if a
# next pointer matches, it does another. So it's a "splice / insert a node into a doubly-linked list (manipulating the
# ->4, ->8, and ->33 pointers)" routine (a list splice, part of the 0x8234D cluster). 67 lines, no callees (pure data
# manipulation). Many callers (sub_82B5FEB0, sub_824DFEA0, sub_8234C9E0, ConstructNodeValue_82B4C1C0, ...). The list's
# element type (a doubly-linked list node, ->4 and ->8 = the next/prev pointers, ->33 = a flag) and the list's
# meaning (a particular list) aren't resolvable, so the name states the mechanism: a list splice (insert a node into a
# doubly-linked list). The "3" distinguishes it from List_SpliceNode2_8292CC88.
[entrypoint.functions."0x8234D600"]
name = "List_SpliceNode3_8234D600"

# A complex computation (a clamped / min-max computation): compute clamped values from the object's fields (obj->0,
# obj->20), a, and b (subtractions, andc, additions, and min/max comparisons), and store the results (out->0 and
# out2->0).

# Args: obj (r3, ->0, ->20), a (r4), b (r5), out (r6, ->0), out2 (r7, ->0), (r8), (more). It computes clamped values
# from the object's fields (obj->0, a count, and obj->20, a base), a, and b: it performs a sequence of subtractions
# (obj->0 - 1, b - obj->0, a + obj->0 - 1), andc (bitwise-and-not, a clamp / mask), additions (obj->20 + obj->0 - 1,
# a + ...), and min/max comparisons (comparing the computed values and storing the min / max to out->0 and out2->0).
# So it's a "compute clamped values (a complex min-max computation, with subtractions, andc, and additions) and store
# the results" routine (a clamped computation, part of the 0x83230 cluster). 70 lines, no callees (pure integer
# arithmetic). 2 callers (sub_83230C88, sub_83230D00). The object's class (->0 = a count, ->20 = a base, an object
# with a count and a base) and the values' meaning (clamped / min-max values, an index / range computation) aren't
# resolvable, so the name states the mechanism: a clamped computation (min-max, with subtractions, andc, and
# additions).
[entrypoint.functions."0x83230A48"]
name = "ComputeClamped_83230A48"

# A 16-byte object constructor: if the source (src) is non-null and *src is non-zero, allocate a 16-byte object
# (Allocate_OrDbgBreak_8221F388), zero it (result->0, ->4, ->8 = 0), and process (a loop, with a reassign via Reassign
# Buffer_822088B0 and a release via Release_RefCounted_821C67D8).

# Args: obj (r3), src (r4, ->0 = a count), (more). If src is null, or *src (a count) is zero, it takes an early path
# (no allocation). Otherwise it allocates a 16-byte object (Allocate_OrDbgBreak_8221F388, sub_8221F388), zeros it
# (result->0, result->4, result->8 = 0), and processes (a loop, over the source, with a reassign via ReassignBuffer
# _822088B0, sub_822088B0, a buffer reassign, and a release via Release_RefCounted_821C67D8, sub_821C67D8, a
# refcounted release). So it's a "construct a 16-byte object (allocate it, zero it, and process a source, with a
# reassign and a release)" routine (a 16-byte object constructor, part of the 0x82275 cluster). 105 lines, 3 callees
# (Allocate_OrDbgBreak_8221F388, ReassignBuffer_822088B0, Release_RefCounted_821C67D8). Many callers (sub_822BA5F8,
# sub_82341D48, sub_82991220, ...). The object's class (a 16-byte object, ->0, ->4, ->8 = three 32-bit fields) and the
# source's meaning (a count, a source to process) aren't resolvable, so the name states the mechanism: construct a 16-
# byte object (allocate it, zero it, and process a source).
[entrypoint.functions."0x82275368"]
name = "Construct16Byte_82275368"

# A state dispatch (the sibling of StateDispatch_82BCA7D8 / StateDispatch2_82BCAA58): call SwitchDispatch_82BC9B28
# (a switch dispatch), and (depending on the result) process (a loop, decrementing value->20, with a mutual recursion
# via ProcessDispatchValue_8229A488).

# Args: obj (r3), value (r4, ->16, ->20), count (r5), (more). It calls SwitchDispatch_82BC9B28 (sub_82BC9B28, a switch
# dispatch, with the value and the count). If the result's ->4 is 0 and the result matches a constant (a specific
# value, at a fixed .data address), it takes one path. Otherwise, if value->20 > value->16 (a range check), it loops
# (decrementing value->20 by 20, a 20-byte stride). It also calls sibling operations (sub_82BCA180, in the 0x82BCA
# cluster) and has a mutual recursion with ProcessDispatchValue_8229A488 (sub_8229A488 -- they call each other). So
# it's a "process (a switch dispatch, and (depending on the result) a loop (decrementing value->20) and sibling
# operations)" routine (a state dispatch, part of the 0x82BCA cluster). 233 lines, 3 callees (SwitchDispatch_82BC9B28,
# ProcessDispatchValue_8229A488, sub_82BCA180). 6 callers (sub_82BCCFF8, ProcessDispatchValue_8229A488, sub_82BCA588,
# sub_822DF280, sub_82BC9FD0, sub_82BCA5F8). The object's class (->16, ->20 = a range, an object with a switch dispatch
# and a range) and the value's meaning (a state / event) aren't resolvable, so the name states the mechanism: a state
# dispatch (a switch dispatch, and a loop (decrementing value->20)). The "3" distinguishes it from StateDispatch_82
# BCA7D8 and StateDispatch2_82BCAA58.
[entrypoint.functions."0x82BCA3E8"]
name = "StateDispatch3_82BCA3E8"

# A constant stub: return the constant 8192 (0x2000, a power of 2 -- a capacity / size / max).

# Args: (none). Returns the constant 8192 (li r3, 8192; blr). 2 lines (to the next function), no callees (a constant
# return). A sibling of the ReturnConstant* constant stubs (e.g., ReturnConstant2/8/16/64/128/256/512/0x8000) and a
# likely virtual "capacity / size / max" getter (a method that returns a fixed constant, 8192). It is registered (in
# the function table, fable_2_init.cpp / fable_2_register.cpp) but has no direct callers in the recompiled code (a
# registered-but-uncalled constant stub, likely invoked only from code outside the traced region, or unused). The
# constant's meaning (8192, a capacity / size / max -- a power of 2, a buffer size / block count / max count) isn't
# resolvable from the code alone, so the name states the mechanism: return the constant 8192. The "8192" distinguishes
# it from the other ReturnConstant* stubs.
[entrypoint.functions."0x82A145F8"]
name = "ReturnConstant8192_82A145F8"

# A pointer getter (a branch on a field): if (obj->24 >= 8), return (obj->4); otherwise return (obj + 4).

# Args: obj (r3, ->4 = a pointer, ->24 = a flag / count), (more). Branches on obj->24: if it is >= 8 (a threshold), it
# returns (obj->4, a pointer field); otherwise it returns (obj + 4, a pointer to a fixed offset within the object). So
# it's a "get a pointer (branch on a field: obj->24 >= 8 -> obj->4, else obj + 4)" routine (a pointer getter, part of
# the 0x822F5 cluster). 19 lines (to the next function), no callees (pure data access). 2 callers (sub_82EE2A48, sub
# _822F6480). The object's class (->4 = a pointer, ->24 = a flag / count, an object with a pointer field and a flag)
# and the pointer's meaning (a sub-object / value / a fixed offset) aren't resolvable, so the name states the
# mechanism: get a pointer (branch on a field).
[entrypoint.functions."0x822F5458"]
name = "GetPointer_822F5458"

# A virtual call (the sibling of VirtualCall_826C0388 / VirtualCall2_83231E88): load a function pointer from a vtable
# (obj->0 -> ->60) and call it (bctr), passing the object (the vtable-derived this) and the other arguments.

# Args: obj (r3, ->0 = a vtable), (more). Loads the vtable (obj->0), loads a function pointer from it (vtable->60),
# sets the count register (ctr = the function pointer), and calls it (bctr -- a virtual call). It passes the object
# (the vtable-derived this) and the other arguments. So it's a "virtual call (dispatch via a vtable, calling a virtual
# method at vtable->60)" routine (a virtual-call thunk, part of the 0x82214 cluster). 4 lines (to the next function),
# no direct callees (a single indirect call via bctr, which the callee scan does not count). 1 caller: sub_82C14D70.
# The vtable's meaning (a class, a method at vtable->60) isn't resolvable, so the name states the mechanism: a virtual
# call (dispatch via a vtable). The "3" distinguishes it from VirtualCall_826C0388 and VirtualCall2_83231E88.
[entrypoint.functions."0x822142C0"]
name = "VirtualCall3_822142C0"

# A process (with a virtual call and VirtualCallPair): do a virtual call (obj->0 -> ->32, with a stack buffer), and
# (if the result's low byte is non-zero) call VirtualCallPair_82C01D60 (a double-virtual-call) and process (a
# comparison, with allocations via Allocate_OrDbgBreak_8221F388 / Construct16Byte_82275368 and frees via Free_Thunk
# _824FE010).

# Args: obj (r3, ->0 = a vtable), value (r4), (more). It does a virtual call (via bctrl, calling the virtual method at
# obj->0->32, with a stack buffer). If the result's low byte is non-zero (a flag / state), it calls VirtualCallPair_8
# 2C01D60 (sub_82C01D60, a double-virtual-call, with the object) and processes: it compares the stack buffer's value
# against the result, and (if a condition holds) calls VirtualCallPair_82C01D60 again, and processes further (with
# allocations via Allocate_OrDbgBreak_8221F388, sub_8221F388, and Construct16Byte_82275368, sub_82275368, a 16-byte
# object constructor, and frees via Free_Thunk_824FE010, sub_824FE010). It also calls sub_82BFF7D8 and sub_82BFF848
# (siblings in the 0x82BFF cluster). So it's a "process (a virtual call, and (if a flag) VirtualCallPair and further
# processing (allocations and frees))" routine (a process with a virtual call, part of the 0x82C02 cluster). 204 lines
# , 6 callees (VirtualCallPair_82C01D60, Allocate_OrDbgBreak_8221F388, Construct16Byte_82275368, Free_Thunk_824FE010
# , sub_82BFF7D8, sub_82BFF848). It is registered (in the function table, fable_2_init.cpp / fable_2_register.cpp) but
# has no direct callers in the recompiled code (a registered-but-uncalled process, likely invoked only from code
# outside the traced region, or unused). The object's class (->0 = a vtable, an object with a virtual method at
# vtable->32) and the value's meaning aren't resolvable, so the name states the mechanism: process (a virtual call, and
# (if a flag) VirtualCallPair and further processing).
[entrypoint.functions."0x82C02250"]
name = "ProcessVirtualCall_82C02250"

# A process (with a virtual-call sequence): do a virtual call (obj->0 -> ->24), and (if the result's low byte is non-
# zero) call VirtualCall3_822142C0 (a virtual call, with the object and a value), and (if the result's low byte is non
# -zero again) do another virtual call (obj->0 -> ->28).

# Args: obj (r3, ->0 = a vtable), (r4), value (r5), (more). It does a virtual call (via bctrl, calling the virtual
# method at obj->0->24, with the object). If the result's low byte is non-zero (a flag / state), it calls VirtualCall3
# _822142C0 (sub_822142C0, a virtual call, with the object and the value). If the result's low byte is again non-zero,
# it does another virtual call (via bctrl, calling the virtual method at obj->0->28, with the object). So it's a
# "process (a virtual-call sequence: call obj->0->24, and (if a flag) VirtualCall3 and another virtual call (obj->0->
# 28))" routine (a process with a virtual-call sequence, part of the 0x82C14 cluster). 86 lines, 1 callee (VirtualCall
# 3_822142C0). Many callers (Dispatch_VcallSequence_82C15100, Dispatch_VcallSequence2_82C1E360, sub_82C30C30, ... --
# many in the 0x82C1/0x82C3 region). The object's class (->0 = a vtable, an object with virtual methods at vtable->24
# and vtable->28) and the value's meaning aren't resolvable, so the name states the mechanism: a process (a virtual-
# call sequence, calling obj->0->24, VirtualCall3, and obj->0->28). The "3" distinguishes it from an earlier virtual-
# call sequence process (if any).
[entrypoint.functions."0x82C14D70"]
name = "ProcessVcallSequence3_82C14D70"

# A copy loop (the sibling of the CopyStructData family): loop (count times), copying 32-byte records (8 words, a
# 32-byte stride) from src to dst (advancing both pointers by 32 bytes each iteration).

# Args: (r3), dst (r4, a write pointer), count (r5), src (r6, a read pointer), (more). If count is 0, it takes an early
# path (no copy). Otherwise it loops (count times): it copies a 32-byte record (8 words, from src to dst -- src->0,
# src->4, ..., src->32 -> dst->-24, dst->-20, ..., dst->0, dst->4, dst->8, a 32-byte record, including a float at
# src->32), and advances both pointers (dst += 32, src += 32). So it's a "copy 32-byte records (a loop, copying 8-word
# records from src to dst, advancing both pointers)" routine (a copy loop, part of the 0x82A13 cluster, a sibling of
# the CopyStructData family). 87 lines, no callees (pure data manipulation). 2 callers (sub_82A13D30, sub_8323FF48).
# The record's meaning (a 32-byte record, 8 words, a data record / node / value) and the source/destination's meaning
# (arrays of 32-byte records) aren't resolvable, so the name states the mechanism: copy 32-byte records (a loop,
# copying 8-word records).
[entrypoint.functions."0x82A13CA0"]
name = "CopyRecords_82A13CA0"

# A complex computation (with 128-bit vector ops): compute a value (loading global floats, 128-bit vector operations
# -- vspltisw, vcfux, lvlx, vspltw, stvx128, a SIMD computation), and store the result (a float, to out->0).

# Args: out (r3, ->0 = a float), (more). It computes a value: it loads global floats (from fixed .data addresses,
# 0x82...-27456 and 0x82...-12, constants), and performs a sequence of 128-bit vector operations (vspltisw, vcfux, lvlx
# , vspltw, stvx128 -- a SIMD computation, with a 128-bit load/store and a splat / shuffle). It stores the result (a
# float, to out->0). So it's a "compute a value (a complex SIMD computation, loading global floats and 128-bit vector
# operations) and store the result (a float)" routine (a complex computation with vector ops, part of the 0x83211
# cluster). 173 lines, no direct callees (pure computation, 128-bit vector ops which the callee scan does not count).
# 1 caller: sub_832351B0. The value's meaning (a computed float, a particular math / transformation) and the global
# floats' meaning (constants, a particular computation) aren't resolvable from the code alone, so the name states the
# mechanism: a complex computation (with 128-bit vector ops) and store the result.
[entrypoint.functions."0x832119D8"]
name = "ComputeVector_832119D8"

# A complex float computation (one of the largest routines, ~2020 lines): compute a value (a large sequence of float
# operations, with a 432-byte stack frame and a global flag check -- if a global flag is set, it calls sub_823D2790,
# a setup / initialization function), releasing a refcounted value (Release_RefCounted_829FF648) in a path.

# Args: obj (r3), (r4), a (r5), b (r6), c (r7), value (f1), (more). It checks a global flag (a fixed .data address,
# 0x82...13760): if the flag's low bit is set, it sets the flag (a one-time initialization) and calls sub_823D2790 (a
# setup / initialization function, with a string at a fixed .data address). It then performs a large sequence of float
# operations (a complex computation, with a 432-byte stack frame, saving and restoring f29/f30/f31, and a value (f1,
# a float, in f29)). In a path it releases a refcounted value (Release_RefCounted_829FF648, sub_829FF648, a refcounted
# release) and calls sibling operations (sub_825BF630 and sub_82CA3700, in the 0x825BF/0x82CA3 clusters). So it's a
# "compute a value (a large, complex float computation, with a one-time initialization (a global flag check) and a
# refcounted release)" routine (a complex float computation, part of the 0x83234 cluster). ~2020 lines (at the end of
# the file), 4 callees (Release_RefCounted_829FF648, sub_823D2790, sub_825BF630, sub_82CA3700). 1 caller: sub_82A13220
# . The object's class (a float computation, a particular math / transformation) and the value's meaning (a computed
# float, a particular result) aren't resolvable from the code alone, so the name states the mechanism: a complex float
# computation (with a one-time initialization and a refcounted release).
[entrypoint.functions."0x83234418"]
name = "ProcessFloat_83234418"

# A size-checked allocation (the sibling of the AllocateChecked family, with a 40x multiplier): check the size (an
# overflow check: size != 0 and -1 / size < 40), compute the allocation size (size * 40, via (size * 5) << 3), and
# allocate (Allocate_OrDbgBreak_8221F388); raise an error (sub_822F1F00) on a failure path.

# Args: (r3), size (r4), (more). Checks the size: if size is 0, or -1 / size >= 40 (an overflow check -- the
# allocation size would overflow), it takes an error path. Otherwise it computes the allocation size ((size * 5) << 3
# = size * 40) and allocates it (Allocate_OrDbgBreak_8221F388, sub_8221F388). On a failure path it calls sub_822F1F00
# (an error / setup function, with a string at a fixed .data address). So it's a "check the size (an overflow check),
# compute the allocation size (size * 40), and allocate (with an error on failure)" routine (a size-checked allocation
# of 40-byte elements). 70 lines, 2 callees (Allocate_OrDbgBreak_8221F388, sub_822F1F00). 4 callers (sub_829175B8,
# sub_829184D8, sub_8246F6B8, sub_82A13D30). The 40-byte element's meaning (a record / node) and the size's source
# aren't resolvable, so the name states the mechanism: check the size and allocate (size * 40). The "5" distinguishes
# it from AllocateChecked_8264EFE0 (12x), AllocateChecked2_8217A488 (2x), AllocateChecked3_82333F78 (4x), and
# AllocateChecked4_82A87220 (8x).
[entrypoint.functions."0x824709C0"]
name = "AllocateChecked5_824709C0"

# A process (with a copy of records): copy a 32-byte record (from src to a stack buffer), and (depending on obj->4)
# process (copy records via CopyRecords_82A13CA0 / CopyArray24_82A13FE0, grow via AllocateChecked5_824709C0, and free
# via Free_SizeBucketed_Thunk_8221BE68).

# Args: obj (r3, ->4 = a count), (r4), src (r6, a 32-byte record: ->0, ->4, ->8, ->12, ->16, ->24, ->32, ->36), (more)
# . It copies a 32-byte record (from src to a stack buffer, at stack[80], copying src->0, src->4, src->8, src->12, src
# ->16, src->24, src->32, src->36, a 32-byte record, including two floats). If obj->4 (a count) is 0, it takes one
# path (an empty state). Otherwise it processes: it copies records (CopyRecords_82A13CA0, sub_82A13CA0, a copy loop
# for 32-byte records, and CopyArray24_82A13FE0, sub_82A13FE0, a 24-byte array copy), grows (AllocateChecked5_824709C
# 0, sub_824709C0, a size-checked allocation of 40-byte elements), and frees (Free_SizeBucketed_Thunk_8221BE68, sub
# _8221BE68, a size-bucketed free). It also calls sub_82A97648 (a sibling in the 0x82A97 cluster). So it's a "process
# (copy a 32-byte record, and (depending on the count) copy records, grow, and free)" routine (a process with a copy
# of records, part of the 0x82A13 cluster). 391 lines, 5 callees (CopyRecords_82A13CA0, CopyArray24_82A13FE0, Allocat
# eChecked5_824709C0, Free_SizeBucketed_Thunk_8221BE68, sub_82A97648). 1 caller: sub_8323FF48. The object's class
# (->4 = a count, an object with a count) and the record's meaning (a 32-byte record, a data record / node / value)
# aren't resolvable, so the name states the mechanism: process (copy a record, and (depending on the count) copy
# records, grow, and free).
[entrypoint.functions."0x82A13D30"]
name = "ProcessCopyRecords_82A13D30"

# A copy loop (the sibling of CopyRecords_82A13CA0 / CopyArray24_82A13FE0): loop (while a != b), copying 20-byte
# records (a word, two floats, and four bytes) from a to dst (advancing both pointers by 20 bytes each iteration).

# Args: (r3), a (r4, a read pointer), b (r5, an end), dst (r6, a write pointer), (more). If a == b (an empty range),
# it returns (nothing to copy). Otherwise it loops (while a != b): it copies a 20-byte record (from a to dst -- a
# word (a->-8), two floats (a->-4 and a->0, stored with a scaled index (stfsx)), and four bytes (a->8, a->9, a->10,
# a->11), a 20-byte record), and advances both pointers (a += 20, dst += 20). So it's a "copy 20-byte records (a
# loop, copying a word, two floats, and four bytes from a to dst, advancing both pointers)" routine (a copy loop,
# part of the 0x82A13 cluster, a sibling of CopyRecords_82A13CA0 (32-byte) and CopyArray24_82A13FE0 (24-byte)). 76
# lines, no callees (pure data manipulation). 1 caller: sub_82A12EC0. The record's meaning (a 20-byte record, a word,
# two floats, and four bytes, a data record / node / value) and the source/destination's meaning (arrays of 20-byte
# records) aren't resolvable, so the name states the mechanism: copy 20-byte records (a loop, copying a word, two
# floats, and four bytes). The "20" distinguishes it from CopyRecords_82A13CA0 (32-byte) and CopyArray24_82A13FE0
# (24-byte).
[entrypoint.functions."0x82A13138"]
name = "CopyRecords20_82A13138"

# A sorted list insert (the callee of Heap_CarveBlock_83231058): walk a sorted doubly-linked list (a two-key
# comparison: node->0 and node->4 against the list's nodes, following the ->12 next pointers), and insert the node
# (r8->12 = node and node->8 = r8, or at the head if no match).

# Args: list (r3, ->0 = a head), node (r4, ->0 and ->4 = two keys, ->8 and ->12 = the doubly-linked list pointers),
# (more). It walks the sorted doubly-linked list (from the head, following the ->12 next pointers): it compares the
# node's keys (node->0 and node->4, a primary and a secondary key) against the list's nodes (r11->0 and r11->4), and
# walks to the next node (r11->12) until it finds the right place (a node where the node's keys are <= the node's
# keys, or the end). It then inserts the node: if it found a place (r8, the found node), it sets r8->12 = node and
# node->8 = r8 (splicing the node after the found node); otherwise (no match), it inserts at the head (list->0 = node
# and node->8 = r8). So it's a "insert a node into a sorted doubly-linked list (walk the list by a two-key comparison
# and insert the node, or at the head if no match)" routine (a sorted list insert, part of the 0x829FD cluster). 65
# lines, no callees (pure data manipulation). 2 callers: Heap_CarveBlock_83231058 (sub_83231058, a heap block carver)
# , sub_83231240. The list's element type (a sorted doubly-linked list node, ->0 and ->4 = two keys, ->8 and ->12 =
# the next/prev pointers) and the list's meaning (a sorted list of blocks / nodes, a free list / sorted index) aren't
# resolvable, so the name states the mechanism: insert a node into a sorted doubly-linked list (by a two-key
# comparison). The "Sorted" distinguishes it from an unsorted list insert (if any).
[entrypoint.functions."0x829FD158"]
name = "List_InsertSorted_829FD158"

# A float value parse (the callee of ParseNumberValue_82B47AB0): store a constant (out->4 = a constant), and (if
# count >= 4) loop (processing bytes, with a float computation, parsing a floating-point number from a string).

# Args: out (r3, ->4 = a float), (r4, a pointer), count (r5), (more). It stores a constant (out->4 = a constant, a
# float, from a fixed .data address). If count is < 4 (a short value), it takes one path (a small number). If count
# is >= 4, it loops (processing bytes, reading from a string (r4, a pointer), with a float computation -- a
# floating-point number parse, accumulating a value from the bytes, with a loop over the digits, and a float
# conversion). So it's a "parse a floating-point number (store a constant, and (if the value is long) loop over the
# digits, accumulating a float)" routine (a float value parse, part of the 0x82B47 cluster, the callee of ParseNumber
# Value_82B47AB0 -- a number parser). 439 lines, no direct callees (pure computation, float ops and a loop which the
# callee scan does not count). 1 caller: ParseNumberValue_82B47AB0 (sub_82B47AB0, a number parser, which calls this to
# parse a floating-point number). The value's meaning (a floating-point number, a parsed float) and the string's
# meaning (a number string, a decimal number with a fractional part) aren't resolvable, so the name states the
# mechanism: parse a floating-point number (store a constant, and (if the value is long) loop over the digits,
# accumulating a float).
[entrypoint.functions."0x82B47F98"]
name = "ParseFloatValue_82B47F98"

# A bubble sort (the sibling of BubbleSort2_82C28900): compare adjacent elements (a->4 and b->4, a single-key
# comparison), and (if out of order) swap them (copying 12 bytes, a bubble-sort swap).

# Args: a (r3, ->0, ->4, ->8), b (r4, ->0, ->4, ->8), (more). If a == b (the same element), it takes one path. If a->4
# (a key) is > b->4 (a key), it swaps them (a bubble-sort swap): it copies a 12-byte record (a->0, a->4, a->8) to a
# temporary, and copies b's record (b->0, b->4, b->8) to a, and the temporary to b (a 12-byte swap). If a->4 <= b->4
# (in order), it does not swap. So it's a "sort (a bubble sort: compare adjacent elements by a single key (a->4 and
# b->4) and swap them (copying 12 bytes) if out of order)" routine (a bubble sort, part of the 0x82C16 cluster). 95
# lines, 2 callees (sub_82C15CE0, sub_82C21C90 -- siblings in the 0x82C15/0x82C21 clusters). Many callers (sub_82C1
# B180, sub_82C4B0F0, sub_82C33140, ... -- many in the 0x82C1/0x82C2/0x82C3/0x82C4 region). The element's class (a 12
# -byte record, ->0, ->4, ->8, a key at ->4) and the sort's meaning (a sorted array of 12-byte records) aren't
# resolvable, so the name states the mechanism: a bubble sort (compare adjacent elements by a single key and swap
# them (copying 12 bytes) if out of order). The "3" distinguishes it from BubbleSort2_82C28900 (a two-key bubble
# sort).
[entrypoint.functions."0x82C16C78"]
name = "BubbleSort3_82C16C78"

# A chain search (walking a chain by a comparison): load a node (obj->4->4), and (if the node's flag, node->21, is
# zero) walk the chain (comparing node->12 against a value, and following the node's next pointers, node->8 / node->0
# ), searching for a match.

# Args: obj (r3, ->4 = a node), value (r4, ->0 = a value), (more). It loads a node (obj->4->4, a node in a chain). If
# the node's flag (node->21, a byte) is zero, it takes one path (reading a value from r4->0). It then walks the chain
# (a loop): it compares the node's field (node->12, a value) against the target value (r7, from r4->0), and (depending
# on the comparison) follows the node's next pointer (node->8, a next) or the node's head (node->0, a head / prev),
# walking to the next node (node->4->21, a flag). So it's a "search a chain (walk the chain by a comparison, node->12
# against a value, following the next pointers)" routine (a chain search, part of the 0x82B3D cluster). 148 lines, 1
# callee (sub_824AEBB8). Many callers (sub_82670E90, sub_82B3D6F0, sub_83223FB0, sub_8281A568, sub_83223E18, sub
# _82B3D680, sub_83224FD0, sub_825F1D78, sub_83224818). The chain's element type (a node, ->4 = a next, ->8 = a next,
# ->12 = a value, ->21 = a flag, a doubly-linked / circular chain) and the value's meaning (a searched value, a
# particular match) aren't resolvable, so the name states the mechanism: a chain search (walk the chain by a
# comparison, node->12 against a value).
[entrypoint.functions."0x82B3D880"]
name = "ChainSearch_82B3D880"

# A dispose (the sibling of Dispose8_82A12000 / Dispose12_82A14840): call a destructor (sub_83218A40), and (if the
# flag's low bit is set) free the object (Free_SizeBucketed_Thunk_8221BE68).

# Args: obj (r3), flag (r4), (more). It calls a destructor (sub_83218A40, a sibling in the 0x83218 cluster, a 20-byte
# node destructor, with the object). If the flag's low bit is set (flag & 1, a free flag), it frees the object (Free
# _SizeBucketed_Thunk_8221BE68, sub_8221BE68, a size-bucketed free). So it's a "dispose an object (call a destructor,
# and (if a flag is set) free the object)" routine (a dispose, part of the 0x82A12 cluster, a sibling of the Dispose
# family). 45 lines (to the next function), 2 callees (sub_83218A40, Free_SizeBucketed_Thunk_8221BE68). It is
# registered (in the function table, fable_2_init.cpp / fable_2_register.cpp) but has no direct callers in the
# recompiled code (a registered-but-uncalled dispose, likely invoked only from code outside the traced region, or
# unused). The object's class (a 20-byte node, a refcounted object with a destructor) and the flag's meaning (a free
# flag) aren't resolvable, so the name states the mechanism: dispose an object (call a destructor and free
# conditionally). The "13" distinguishes it from the other Dispose* variants (Dispose through Dispose12).
[entrypoint.functions."0x82A12060"]
name = "Dispose13_82A12060"

# A dispose (the sibling of Dispose13_82A12060 / Dispose12_82A14840): destroy a list (DestroyList_Recursive_82443440,
# on obj->4->4), then zero out the object's fields (self-referential: node->4 = node, node->0 = node, node->8 = node
# , and obj->8 = 0).

# Args: obj (r3, ->4 = a node, ->8 = a field), (more). It destroys a list (DestroyList_Recursive_82443440, sub
# _82443440, a recursive list destroy, on obj->4->4 -- the object's nested list). It then zeros out the object's
# fields (self-referential: node->4 = node, node->0 = node, node->8 = node, and obj->8 = 0, a zero -- a cleanup / free
# -marking, so the object's fields point to itself and are zeroed). So it's a "dispose an object (destroy a nested
# list, and zero out the object's fields (self-referential))" routine (a dispose, part of the 0x82A12 cluster, a
# sibling of the Dispose family). 40 lines (to the next function), 1 callee (DestroyList_Recursive_82443440). 1
# caller: sub_82A12580. The object's class (a node, ->4 = a nested list, ->8 = a field, a refcounted object with a
# nested list) and the list's meaning (a nested list, a particular sub-structure) aren't resolvable, so the name
# states the mechanism: dispose an object (destroy a nested list and zero out the fields (self-referential)). The
# "14" distinguishes it from the other Dispose* variants (Dispose through Dispose13).
[entrypoint.functions."0x82A121D8"]
name = "Dispose14_82A121D8"

# A process (with a validation and a detach): validate (child != 0 and child != parent, a trap if invalid), and (if a
# match, r8 == parent->4->0) detach (a chain-root find via FindChainRoot2_823D4F20 and a dispose via Dispose14_82A121
# D8).

# Args: obj (r3, r28), parent (r4, r31, ->4 = a node), child (r5), (r6), (more). It validates: it checks that the
# child is non-zero (child != 0) and that the child is not the parent (child != parent); if either fails, it raises
# an error (a trap, 22). If the match (r8, a value, == parent->4->0, the parent's node's field), it processes: it
# finds a chain root (FindChainRoot2_823D4F20, sub_823D4F20, a chain-root finder, on the parent's node) and disposes
# (Dispose14_82A121D8, sub_82A121D8, a dispose, on the child). It also calls sub_8247E028 (a sibling in the 0x8247E
# cluster). So it's a "process (validate a child, and (if a match) detach it: find a chain root and dispose it)"
# routine (a process with a validation and a detach, part of the 0x82A12 cluster). 137 lines, 3 callees (FindChainRoo
# t2_823D4F20, sub_8247E028, Dispose14_82A121D8). 1 caller: sub_82A12668. The object's class (obj, r28, a node) and
# the parent's class (parent, r31, ->4 = a node, an object with a nested node) and the child's meaning (a child
# object, a node to detach) aren't resolvable, so the name states the mechanism: process (validate a child and (if a
# match) detach it: find a chain root and dispose it).
[entrypoint.functions."0x82A12580"]
name = "ProcessDetach_82A12580"

# A dispose (the sibling of Dispose14_82A121D8 / Dispose13_82A12060): detach (ProcessDetach_82A12580, on the object
# and its fields), free the object's field (Free_SizeBucketed_Thunk_8221BE68, on obj->4), and zero out the fields
# (obj->4 = 0 and obj->8 = 0).

# Args: obj (r3, ->4 = a field, ->8 = a field), (more). It detaches (ProcessDetach_82A12580, sub_82A12580, a process
# with a validation and a detach, on the object and its fields -- the object's nested node). It frees the object's
# field (Free_SizeBucketed_Thunk_8221BE68, sub_8221BE68, a size-bucketed free, on obj->4). It then zeros out the
# object's fields (obj->4 = 0 and obj->8 = 0, a cleanup). So it's a "dispose an object (detach it: validate and
# detach, free the object's field, and zero out the fields)" routine (a dispose, part of the 0x82A12 cluster, a
# sibling of the Dispose family). 50 lines (to the next function), 2 callees (ProcessDetach_82A12580, Free_SizeBucke
# ted_Thunk_8221BE68). 1 caller: sub_83218A40 (a sibling in the 0x83218 cluster, a destructor -- the callee of
# Dispose13_82A12060). The object's class (obj, ->4 = a field, ->8 = a field, a refcounted object with a field) and
# the field's meaning (a field, a particular sub-structure) aren't resolvable, so the name states the mechanism:
# dispose an object (detach it, free the field, and zero out the fields). The "15" distinguishes it from the other
# Dispose* variants (Dispose through Dispose14).
[entrypoint.functions."0x82A12668"]
name = "Dispose15_82A12668"

# A 28-byte constructor (the sibling of Construct20ByteNode_82A12F00 / Construct16Byte_82275368): allocate 28 bytes
# (Allocate_OrDbgBreak_8221F388), zero out the object's fields (->0, ->4, ->8 = 0, and ->24 = 1, ->25 = 0), and store
# the new object into obj->4.

# Args: obj (r3, ->4 = a field), (more). It allocates 28 bytes (Allocate_OrDbgBreak_8221F388, sub_8221F388, a
# size-bucketed allocation, of 28 bytes). It then zeros out the object's fields (->0, ->4, ->8 = 0, three words, and
# ->24 = 1, a one, and ->25 = 0, a zero -- a byte flag). It then stores the new object into obj->4 (obj->4 = the new
# 28-byte object). So it's a "construct a 28-byte object (allocate 28 bytes, zero out the fields (and set ->24 = 1,
# ->25 = 0), and store it into obj->4)" routine (a 28-byte constructor, part of the 0x82A12 cluster). 83 lines, 1
# callee (Allocate_OrDbgBreak_8221F388). Many callers (sub_82893C28, sub_82C340B0, sub_83290210, sub_82C660F8, sub
# _8288E310, sub_82892198, sub_82891B48, sub_8288E478, sub_828919F0, sub_83218AE8 -- many in the 0x8288E/0x82891/0x
# 82892/0x82893 region). The object's class (a 28-byte object, ->0, ->4, ->8 = words, ->24 = a flag, ->25 = a flag,
# an object with a flag) and the parent's class (obj, ->4 = a field, an object with a 28-byte object) aren't
# resolvable, so the name states the mechanism: a 28-byte constructor (allocate 28 bytes and zero out the fields).
# The "28" distinguishes it from the other constructor sizes (Construct16Byte_82275368, Construct20ByteNode_82A12F0
# 0, Construct36Byte_82370EF8, Construct48ByteNode_82A0ED98).
[entrypoint.functions."0x82A12828"]
name = "Construct28Byte_82A12828"

# A dispose (a complex destructor, the sibling of Dispose14_82A121D8 / Dispose13_82A12060): store a constant
# (obj->0 = a constant, a class id / a marker), destroy a list (DestroyList_Recursive_82443440) and a tree
# (DestroyTreeNodes_82859340), zero out the object's fields (self-referential), and dispose nested objects
# (Dispose15_82A12668 and Dispose2_8233DD70).

# Args: obj (r3, ->0 = a field, ->16 = a node, ->20 = a field, ->28 = a field, ->32 = a node), (more). It stores a
# constant (obj->0 = a constant, a class id / a marker, from a fixed .data address -- a cleanup marking). It destroys
# a list (DestroyList_Recursive_82443440, sub_82443440, a recursive list destroy, on obj->16->4 -- the object's nested
# list) and a tree (DestroyTreeNodes_82859340, sub_82859340, a tree-node destroy, on the object's nested tree). It
# zeros out the object's fields (self-referential: node->4 = node, node->0 = node, node->8 = node, and obj->20 = 0,
# a zero). It then disposes nested objects (Dispose15_82A12668, sub_82A12668, a dispose, on the object's nested
# object, and Dispose2_8233DD70, sub_8233DD70, a dispose, on another nested object). So it's a "dispose an object (a
# complex destructor: store a constant, destroy a list and a tree, zero out the fields, and dispose nested objects)"
# routine (a complex destructor, part of the 0x83218 cluster). 95 lines, 4 callees (DestroyList_Recursive_82443440,
# DestroyTreeNodes_82859340, Dispose15_82A12668, Dispose2_8233DD70). 1 caller: Dispose13_82A12060 (sub_82A12060, a
# dispose, which calls this to destroy the object's fields). The object's class (obj, ->0 = a field, ->16 = a node,
# ->20 = a field, ->28 = a field, ->32 = a node, an object with a nested list, a tree, and nested objects) isn't
# resolvable, so the name states the mechanism: a complex destructor (store a constant, destroy a list and a tree,
# zero out the fields, and dispose nested objects). The "16" distinguishes it from the other Dispose* variants
# (Dispose through Dispose15).
[entrypoint.functions."0x83218A40"]
name = "Dispose16_83218A40"

# A node constructor (the sibling of Construct20ByteNode_82A12F00 / Construct28Byte_82A12828): initialize the
# object's fields (obj->4 = a value, obj->8 = 0, obj->0 = a constant), then construct a 28-byte object
# (Construct28Byte_82A12828, on obj+12) and a 20-byte node (ConstructNode20_82A12B08, on obj+28).

# Args: obj (r3, ->0 = a field, ->4 = a value, ->8 = a field, +12, +28), value (r4), (more). It initializes the
# object's fields (obj->4 = a value, obj->8 = 0, a zero, and obj->0 = a constant, a class id / a marker, from a fixed
# .data address). It then constructs a 28-byte object (Construct28Byte_82A12828, sub_82A12828, a 28-byte constructor,
# on obj+12 -- the object's embedded 28-byte object) and a 20-byte node (ConstructNode20_82A12B08, sub_82A12B08, a
# 20-byte node constructor, on obj+28 -- the object's embedded 20-byte node). So it's a "construct a node (initialize
# the fields, then construct an embedded 28-byte object and an embedded 20-byte node)" routine (a node constructor,
# part of the 0x83218 cluster). 40 lines (to the next function), 2 callees (Construct28Byte_82A12828,
# ConstructNode20_82A12B08). 1 caller: sub_83218B40 (a sibling in the 0x83218 cluster). The object's class (obj, ->0
# = a field, ->4 = a value, ->8 = a field, +12 = an embedded 28-byte object, +28 = an embedded 20-byte node, an
# object with embedded objects) and the value's meaning (a value, a particular field) aren't resolvable, so the name
# states the mechanism: construct a node (initialize the fields and construct embedded objects). The "28" refers to
# the 28-byte embedded object.
[entrypoint.functions."0x83218AE8"]
name = "ConstructNode28_83218AE8"

# A node constructor (with a chain find): find a chain root (WalkChainFind_83239FA8), and (depending on the result)
# construct a node (ConstructNode28_83218AE8) or allocate 40 bytes (Allocate_OrDbgBreak_8221F388), wrapping a
# refcounted value (SetRefcountedWrapper_8238A848).

# Args: obj (r3, ->0 = a field, ->4 = a field), value (r4), (more). It finds a chain root (WalkChainFind_83239FA8,
# sub_83239FA8, a chain finder, on the value and a global). If the result's low byte is zero (result & 0xFF == 0, an
# empty / not-found), it takes one path (allocating 40 bytes, Allocate_OrDbgBreak_8221F388, and constructing a node,
# ConstructNode28_83218AE8, sub_83218AE8, a node constructor). Otherwise (a found value), it takes another path
# (releasing a refcounted value, Release_RefCounted_829FF648, sub_829FF648, a refcounted release). It also wraps a
# refcounted value (SetRefcountedWrapper_8238A848, sub_8238A848, a refcounted wrapper) and calls sub_83225E00 (a
# sibling in the 0x83225 cluster). So it's a "construct a node (find a chain root, and (depending on the result)
# construct a new node or wrap a found refcounted value)" routine (a node constructor with a chain find, part of the
# 0x83218 cluster). 137 lines, 6 callees (WalkChainFind_83239FA8, ConstructNode28_83218AE8, Allocate_OrDbgBreak_8221
# F388, Release_RefCounted_829FF648, SetRefcountedWrapper_8238A848, sub_83225E00). 3 callers: ProcessSearchAndAssign
# _83225E80 (sub_83225E80, a search-and-assign process), sub_83225F18, sub_83225FD0 (siblings in the 0x83225 cluster
# ). The object's class (obj, ->0 = a field, ->4 = a field, a refcounted wrapper object) and the value's meaning (a
# value, a particular key) aren't resolvable, so the name states the mechanism: construct a node (find a chain root
# and (depending on the result) construct a new node or wrap a found value).
[entrypoint.functions."0x83218B40"]
name = "ConstructWithChain_83218B40"

# A process (a refcounted assign, the sibling of SearchAndAssign_83225D80 / ProcessSearchAndAssign_83225E80): if
# obj->4 is non-zero, release a refcounted value (Release_RefCounted_829FF648) and increment a counter (obj->4->0 +=
# 1), then look up and assign (LookupAndAssign_8323A1E8).

# Args: obj (r3, ->0 = a value, ->4 = a node), (more). If obj->4 (a node) is non-zero, it releases a refcounted value
# (Release_RefCounted_829FF648, sub_829FF648, a refcounted release, on the node) and increments a counter (obj->4->0
# += 1, a use count). It then looks up and assigns (LookupAndAssign_8323A1E8, sub_8323A1E8, a lookup-and-assign,
# on the value and a global). So it's a "process (release a refcounted value, increment a counter, and look up and
# assign)" routine (a refcounted assign, part of the 0x83225 cluster, a sibling of SearchAndAssign_83225D80). 73
# lines, 2 callees (Release_RefCounted_829FF648, LookupAndAssign_8323A1E8). 1 caller: sub_83218B40 (ConstructWithCh
# ain_83218B40, a node constructor, which calls this to look up and assign). The object's class (obj, ->0 = a value,
# ->4 = a node, a refcounted wrapper object) and the value's meaning (a value, a particular key) aren't resolvable,
# so the name states the mechanism: a refcounted assign (release a refcounted value, increment a counter, and look up
# and assign).
[entrypoint.functions."0x83225E00"]
name = "ProcessRefAssign_83225E00"

# A double virtual call (the sibling of VirtualCallPair_82C01D60): if value->0 is non-zero, do a virtual call (on
# value->0->0->4, a vtable entry); and if obj->0 is non-zero, do another virtual call (on obj->0->0->8, a vtable
# entry).

# Args: obj (r3, ->0 = a node), value (r4, ->0 = a node), (more). If value->0 (a node) is non-zero, it does a virtual
# call (a vtable dispatch: r3 = value->0, r11 = r3->0, r10 = r11->4, a vtable entry, mtctr r10, bctrl -- a virtual
# call on value->0->0->4, vtable->4). If obj->0 (a node) is non-zero, it does another virtual call (a vtable dispatch:
# r3 = obj->0, r11 = r3->0, r10 = r11->8, a vtable entry, mtctr r10, bctrl -- a virtual call on obj->0->0->8,
# vtable->8). So it's a "double virtual call (if value->0 is non-zero, call a virtual method (vtable->4) on it; and if
# obj->0 is non-zero, call another virtual method (vtable->8) on it)" routine (a double virtual call, part of the
# 0x8260C cluster, a sibling of VirtualCallPair_82C01D60). 71 lines, no direct callees (pure virtual calls via
# REX_CALL_INDIRECT_FUNC). Many callers (sub_82B5D3A0, sub_82B68128, sub_82C6DE30, sub_82B43E30, sub_822F7B70, sub
# _8260B9E0, sub_82C630E0, sub_822F7870, sub_82C638C0, sub_82369958 -- many across the codebase). The node's class (a
# node, ->0 = a node, ->4 and ->8 = vtable entries, an object with virtual methods) and the vtable's meaning (a
# virtual method, a particular operation) aren't resolvable, so the name states the mechanism: a double virtual call
# (call a virtual method on two nodes). The "4" distinguishes it from the other VirtualCall* variants (VirtualCall_
# 826C0388, VirtualCall2_83231E88, VirtualCall3_822142C0).
[entrypoint.functions."0x8260C130"]
name = "VirtualCall4_8260C130"

# A tree/list destructor (a recursive destructor, the sibling of DestructorTree_83232128): loop (while node->21 is
# zero), recursively call itself (sub_82367AE8, on obj and node->8), free the node (Free_SizeBucketed_Thunk_8221BE68),
# and walk to the next node (node->0).

# Args: obj (r3, r30), node (r4, r31, ->0 = a next, ->8 = a field, ->21 = a flag), (more). If node->21 (a flag, a
# byte) is non-zero, it takes one path (a leaf / a done node). If node->21 is zero, it loops: it recursively calls
# itself (sub_82367AE8, on obj and node->8 -- a recursive destroy of the node's nested node), frees the node (Free
# _SizeBucketed_Thunk_8221BE68, sub_8221BE68, a size-bucketed free, on node->0), and walks to the next node (node->0
# , a next pointer), continuing the loop (while the next node's flag, node->21, is zero). So it's a "destroy a tree
# / list (loop over the nodes, recursively destroy each node's nested node, free the node, and walk to the next
# node)" routine (a recursive tree/list destructor, part of the 0x82367 cluster, a sibling of DestructorTree_832321
# 28). 50 lines (to the next function), 2 callees (sub_82367AE8 -- itself, a recursive call, Free_SizeBucketed_Thunk
# _8221BE68). Many callers (sub_826728C0, sub_8267ED70, sub_827C3598, sub_824686C8, sub_823F4A40, sub_82B7CD18, sub
# _8246E8B0, sub_824A7D48, sub_826B16B0, sub_826842E8 -- many across the codebase). The node's class (a node, ->0 =
# a next, ->8 = a field, ->21 = a flag, a tree/list node) and the tree's meaning (a tree/list of nodes, a particular
# structure) aren't resolvable, so the name states the mechanism: a recursive tree/list destructor (loop over the
# nodes, recursively destroy and free each). The "2" distinguishes it from DestructorTree_83232128.
[entrypoint.functions."0x82367AE8"]
name = "DestructorTree2_82367AE8"

# A process (a divide-and-conquer, with a recursive call): compute a range ((b - a) / 12, the number of 12-byte
# records), and (if the range is large, > 32) loop (recursively calling itself, sub_82B42A80, a divide-and-conquer,
# plus sub_82B42E28 and sub_82B43348, sibling operations).

# Args: a (r3, r31, a start pointer), b (r4, r26, an end pointer), count (r5, r30, a count), (more). It computes the
# range ((b - a) / 12, the number of 12-byte records in the range [a, b)). If the range is <= 32 (a small range), it
# takes one path (a base case, a linear process). If the range is large (> 32), it loops (a divide-and-conquer): it
# recursively calls itself (sub_82B42A80, on sub-ranges, a divide-and-conquer), and calls sibling operations (sub
# _82B42E28, sub_82B43348, in the 0x82B42 cluster). So it's a "process (a divide-and-conquer: compute the range, and
# (if large) recursively process sub-ranges)" routine (a divide-and-conquer, part of the 0x82B42 cluster). 340 lines,
# 3 callees (sub_82B42A80 -- itself, a recursive call, sub_82B42E28, sub_82B43348). 1 caller: sub_82B41738. The
# record's class (a 12-byte record, a data record / node) and the range's meaning (a range of 12-byte records, a
# particular collection) aren't resolvable, so the name states the mechanism: a divide-and-conquer (compute the range
# and (if large) recursively process sub-ranges).
[entrypoint.functions."0x82B42A80"]
name = "ProcessDivideConquer_82B42A80"

# A process (a 2D strided operation, the sibling of Process2DStrided_82BDD000): call a 2D strided operation
# (Process2DStrided_82BDD000, on the object, with constants r6 = 4 and r5 = 1), and (if the result is negative, an
# error path) do virtual calls (sub_82BC8870 and sub_82BCCB88, on the object's field, obj->0, with obj->12 and a
# constant).

# Args: obj (r3, ->0 = a field, ->12 = a field), (more). It calls a 2D strided operation (Process2DStrided_82BDD000,
# sub_82BDD000, a 2D strided operation, on the object, with constants r6 = 4 and r5 = 1, and a stack buffer at r4 =
# stack[112]). If the result is negative (an error / a negative value), it does virtual calls (sub_82BC8870, on the
# object's field (obj->0), with obj->12 and a constant; and sub_82BCCB88, on the object's field (obj->0), with a
# constant (3)). So it's a "process (a 2D strided operation, and (if the result is negative) do virtual calls on the
# object's field)" routine (a 2D strided process, part of the 0x82BDD cluster, a sibling of Process2DStrided_82BDD000
# ). 65 lines, 3 callees (Process2DStrided_82BDD000, sub_82BC8870, sub_82BCCB88). 3 callers: sub_82BDD518, sub
# _82BDD258, sub_82BDD8A8 (siblings in the 0x82BDD cluster). The object's class (obj, ->0 = a field, ->12 = a field,
# an object with a field) and the 2D operation's meaning (a 2D strided operation, a particular transform / filter)
# aren't resolvable, so the name states the mechanism: a 2D strided process (call a 2D strided operation and (if the
# result is negative) do virtual calls). The "2" distinguishes it from Process2DStrided_82BDD000.
[entrypoint.functions."0x82BDD170"]
name = "Process2DStrided2_82BDD170"

# A process (a divide-and-conquer helper, the sibling of ProcessDivideConquer_82B42A80): compute a range ((b - a) / 12
# , the number of 12-byte records), then perform a complex computation (computing midpoints, with a loop, a divide-and
# -conquer helper, calling SortRecords_82B43280 to sort the records).

# Args: out (r3, r24, an output pointer), a (r4, r26, a start pointer), b (r5, r25, an end pointer), (more). It
# computes the range ((b - a) / 12, the number of 12-byte records in the range [a, b)). It then performs a complex
# computation (a divide-and-conquer helper, computing midpoints, with a loop, a complex computation over the range,
# with a 40-element threshold). In a path it calls SortRecords_82B43280, sub_82B43280, a records sort, on the
# records. So it's a "process (a divide-and-conquer helper: compute the range, perform a complex computation
# (computing midpoints, with a loop), and sort the records)" routine (a divide-and-conquer helper, part of the
# 0x82B42 cluster, a sibling of ProcessDivideConquer_82B42A80). 459 lines, 1 callee (SortRecords_82B43280). 1 caller:
# ProcessDivideConquer_82B42A80 (sub_82B42A80, a divide-and-conquer, which calls this as a helper). The record's
# class (a 12-byte record, a data record / node) and the computation's meaning (a complex computation over the range,
# a particular divide-and-conquer) aren't resolvable, so the name states the mechanism: a divide-and-conquer helper
# (compute the range, perform a complex computation (computing midpoints, with a loop), and sort the records). The
# "2" distinguishes it from ProcessDivideConquer_82B42A80.
[entrypoint.functions."0x82B42E28"]
name = "ProcessDivideConquer2_82B42E28"

# A copy loop (the sibling of CopyRecords20_82A13138 / CopyRecords_82A13CA0): loop (while c != 0), copying 12-byte
# records (a float and a refcounted value, via CopyAssign_RefCounted_821F0108), advancing the pointers by 12 bytes
# each iteration.

# Args: a (r3, a read pointer), b (r4, r28, an end), c (r5, r30, a write pointer), (more). If a == b (an empty
# range), it takes one path. Otherwise it loops (while c != 0): it copies a 12-byte record (from a to c -- a float
# (a->-8, stored to c->0) and a refcounted value (via CopyAssign_RefCounted_821F0108, sub_821F0108, a refcounted
# copy-assign, on the record's refcounted field, a 12-byte record)), and advances the pointers (a += 12, c += 12).
# So it's a "copy 12-byte records (a loop, copying a float and a refcounted value (via a refcounted copy-assign) from
# a to c, advancing the pointers)" routine (a copy loop, part of the 0x82519 cluster, a sibling of CopyRecords20_82A
# 13138 and CopyRecords_82A13CA0). 72 lines, 1 callee (CopyAssign_RefCounted_821F0108). 4 callers: sub_821E3B38, sub
# _821F01B8, sub_829EABB8, sub_826EEC00. The record's meaning (a 12-byte record, a float and a refcounted value, a
# data record / node / value) and the source/destination's meaning (arrays of 12-byte records) aren't resolvable, so
# the name states the mechanism: copy 12-byte records (a loop, copying a float and a refcounted value (via a
# refcounted copy-assign)). The "12" distinguishes it from CopyRecords20_82A13138 (20-byte) and CopyRecords_82A13CA
# 0 (32-byte).
[entrypoint.functions."0x82519F28"]
name = "CopyRecords12_82519F28"

# A process (a hash table operation, with a validation): validate a size (r29->8 >= 0x00800002, a minimum-size check
# ; if too small, raise an error with a string, via SetString_822F2020, and a trap), and (on the normal path)
# allocate (Allocate_OrDbgBreak_8221F388) and perform a complex computation (a hash table operation, part of the
# 0x829FC cluster).

# Args: obj (r3, r24), (r4, a global, ->8 = a size), a (r5, r26), b (r6, r31), c (r7, r28), (more). It validates a
# size (r29->8, a size, from r4->8, a global): if the size is < 0x00800002 (a minimum size, a too-small check), it
# raises an error (SetString_822F2020, sub_822F2020, a string setter, with a message, and a call to sub_826C3EF0,
# and a trap). On the normal path (size >= 0x00800002), it allocates (Allocate_OrDbgBreak_8221F388, sub_8221F388, a
# size-bucketed allocation, for the hash table) and performs a complex computation (a hash table operation, with a
# large stack frame (224 bytes), and calls to sub_82171810, sub_822F1F00, sub_826C3EF0, sub_826C3FA8, sub_82C239D0,
# sub_82C23A48 -- siblings in the 0x82171/0x822F1/0x826C3/0x82C23 clusters). So it's a "process (a hash table
# operation: validate a size, and (on the normal path) allocate and perform a complex computation)" routine (a hash
# table process, part of the 0x829FC cluster). 344 lines, 8 callees (Allocate_OrDbgBreak_8221F388, SetString_822F2020
# , sub_82171810, sub_822F1F00, sub_826C3EF0, sub_826C3FA8, sub_82C239D0, sub_82C23A48). 7 callers (sub_824EDE70, sub
# _824AEBB8, sub_82366FB0, sub_8246E758, sub_82419280, sub_823660D0, sub_829FC180). The object's class (obj, r24, an
# object with a hash table) and the size's meaning (a size, a minimum-size check) aren't resolvable, so the name
# states the mechanism: a hash table process (validate a size and (on the normal path) allocate and perform a complex
# computation).
[entrypoint.functions."0x829FC3C0"]
name = "ProcessHashTable_829FC3C0"

# A chain/list operation (with a validation): validate obj->0 is non-zero (a trap if zero), and (depending on obj->4
# ->21, a flag) process the chain (walk the chain by obj->4->8 and obj->4->0, with a ->21 flag, and a trap on an
# invalid state).

# Args: obj (r3, ->0 = a field, ->4 = a node, ->4->0 = a head, ->4->8 = a next, ->4->21 = a flag), (more). It
# validates obj->0 is non-zero (obj->0 != 0, a trap, 22, if zero -- an invalid state). It then checks the flag (obj
# ->4->21, a byte): if it is zero, it walks the chain (obj->4->0, the head, and its ->21 flag and ->8 next, a
# chain/list walk); if it is non-zero, it processes obj->4->8 (a next) and (if that's also in a valid state, ->21 is
# zero) returns, else raises a trap (an invalid state). So it's a "process a chain (validate obj->0, and (depending
# on a flag) walk the chain by the next pointers, with a trap on an invalid state)" routine (a chain/list operation,
# part of the 0x8250A cluster). 112 lines, no direct callees (pure data manipulation, with traps). Many callers (sub
# _82AB7620, sub_82AA7F40, sub_8267EAD0, sub_82C82EA8, sub_8218E328, sub_8246E758, sub_82858CA0, sub_82344470, sub
# _8267F630, sub_823775B8 -- many across the codebase). The object's class (obj, ->0 = a field, ->4 = a node, an
# object with a node) and the chain's element type (a node, ->0 = a head, ->8 = a next, ->21 = a flag, a chain/list
# node) aren't resolvable, so the name states the mechanism: a chain/list operation (validate and process the chain
# by the next pointers). The "2" distinguishes it from a first chain process (if any).
[entrypoint.functions."0x8250AF60"]
name = "ProcessChain2_8250AF60"

# A process (a vector/SIMD computation): load a float constant (from a fixed .data address), perform a vector/SIMD
# computation (many vector operations, lvlx, a vector load, and float stores to the stack), and return a computed
# value (a vector result, stored to the stack).

# Args: (r3, a pointer), (many -- a large number of float and vector arguments), (more). It loads a float constant
# (from a fixed .data address, a magic constant, a particular value) and performs a vector/SIMD computation (many
# vector operations, lvlx, a vector load, and float stores to the stack -- a complex vector computation, with a
# 16-byte vector, a 128-bit SIMD operation, and float arithmetic). It stores the computed value (a vector result, a
# float, to the stack, a return value). So it's a "process (a vector/SIMD computation: load a float constant, perform
# a vector computation (many vector operations), and return the computed value)" routine (a vector/SIMD computation,
# part of the 0x82A3B cluster). 118 lines, no direct callees (pure vector/SIMD computation, which the callee scan does
# not count). 1 caller: sub_82A3B330 (a sibling in the 0x82A3B cluster). The value's meaning (a vector result, a
# computed float / vector, a particular value) and the constant's meaning (a magic constant, a particular value) are
# n't resolvable, so the name states the mechanism: a vector/SIMD computation (load a float constant, perform a vector
# computation, and return the computed value).
[entrypoint.functions."0x82A3B1E8"]
name = "ProcessVector_82A3B1E8"

# A list splice (the sibling of List_SpliceNode3_8234D600 / List_SpliceNode2_8292CC88): splice a node into a list
# (using the node's ->0, ->4, and ->8 pointers, with a ->21 flag, and a comparison to place the node in the right
# position).

# Args: obj (r3, ->4 = a node), node (r4, ->0 = a head, ->4 = a next, ->8 = a next, ->21 = a flag), (more). It
# splices the node into a list: it loads a node (r11 = r4->8, a next), loads its head (r10 = r11->0, a head), and
# updates r4->8 (r4->8 = r10). If the head's flag (r10->21, a byte) is zero, it sets r10->4 = r4 (a splice). It then
# updates the next pointers (r11->4 = r4->4, a next) and, (depending on a comparison, obj->4->4 == r4), performs a
# splice (r10 = obj->4, r10->4 = r11, r11->0 = r4, r4->4 = r11 -- a splice of the node into the list). So it's a
# "splice a node into a list (using the node's ->0, ->4, and ->8 pointers, with a ->21 flag, and a comparison to
# place the node in the right position)" routine (a list splice, part of the 0x82C23 cluster, a sibling of List_Splic
# eNode3_8234D600). 67 lines, no direct callees (pure data manipulation). Many callers (sub_82344608, sub_8246FFC8, sub
# _8218E0B0, sub_82C06478, sub_8233DE80, sub_82A42F70, sub_825D8968, ProcessHashTable_829FC3C0, sub_82496FB0, sub
# _82C82A80 -- many across the codebase, including a hash table process). The node's class (a node, ->0 = a head, ->
# 4 = a next, ->8 = a next, ->21 = a flag, a doubly-linked list node) and the list's meaning (a list of nodes, a
# particular structure) aren't resolvable, so the name states the mechanism: a list splice (splice a node into a list
# by the ->0, ->4, and ->8 pointers). The "4" distinguishes it from List_SpliceNode3_8234D600 and List_SpliceNode2_
# 8292CC88.
[entrypoint.functions."0x82C239D0"]
name = "List_SpliceNode4_82C239D0"

# A dispose (a destructor with an atomic decrement, the sibling of the Dispose family): atomically decrement a
# counter (obj->4->4, via lwarx/stwcx and a global lock, a lock-free atomic decrement), and (if the result is zero,
# the last reference) process a destructor (on obj->0, the object's field).

# Args: obj (r3, ->0 = a field, ->4 = a node), (more). It atomically decrements a counter (obj->4->4, a refcount /
# use count): it enters a global lock (REX_ENTER_GLOBAL_LOCK, a thread fence, disabling interrupts), does a lock-free
# atomic decrement (lwarx/stwcx, a compare-and-swap, on obj->4->4, decrementing it by one, and retrying on a CAS
# failure), and exits the global lock (REX_LEAVE_GLOBAL_LOCK). If the result (the decremented counter) is zero (the
# last reference), it processes a destructor (on obj->0, the object's field, a cleanup). So it's a "dispose an object
# (atomically decrement a counter, and (if it is zero, the last reference) process a destructor on the object's
# field)" routine (a destructor with an atomic decrement, part of the 0x82B6A cluster, a sibling of the Dispose
# family). 80 lines, no direct callees (pure data manipulation, with a lock-free atomic decrement and a global lock --
# the callee scan does not count the atomic / lock operations). Many callers (sub_82420D50, sub_82A580D8, sub_8233DE8
# 0, sub_82B5D930, sub_8223F1E0, sub_8218E5F0, sub_82B827D8, sub_82B83808, sub_82A5A310, sub_8233E2A0 -- many across
# the codebase). The object's class (obj, ->0 = a field, ->4 = a node, a refcounted object) and the field's meaning
# (a field, a particular sub-structure) aren't resolvable, so the name states the mechanism: a destructor with an
# atomic decrement (atomically decrement a counter and (if zero) process a destructor). The "17" distinguishes it
# from the other Dispose* variants (Dispose through Dispose16).
[entrypoint.functions."0x82B6AB20"]
name = "Dispose17_82B6AB20"

# A virtual call (a vtable dispatch, the sibling of VirtualCall4_8260C130 / VirtualCallPair_82C01D60): do a virtual
# call (on obj->0->4, a vtable entry), then perform an indexed load (obj->4[index * 4]->0, a field of the
# object's array, indexed by a count).

# Args: (r3, a node), obj (r4, ->0 = a node, ->4 = an array), index (r5, a count), (more). It does a virtual call (a
# vtable dispatch: r11 = obj->0, a node, r9 = r11->4, a vtable entry, mtctr r9, bctrl -- a virtual call on obj->0->
# 4, vtable->4). It then performs an indexed load (r8 = r3->4, a node's array, r7 = index * 4, a scaled index, r6 =
# r8[r7], an indexed load, r3 = r6->0, a field of the indexed element). So it's a "virtual call (call a virtual
# method (vtable->4) on obj->0, and load a field from the object's array, indexed by a count)" routine (a virtual
# call with an indexed load, part of the 0x82C26 cluster, a sibling of VirtualCall4_8260C130). 45 lines (to the next
# function), no direct callees (a pure virtual call via REX_CALL_INDIRECT_FUNC, which the callee scan does not count
# ). It is registered (in the function table, fable_2_init.cpp / fable_2_register.cpp) but has no direct callers in
# the recompiled code (a registered-but-uncalled virtual call, likely invoked only from code outside the traced
# region, or unused). The node's class (a node, ->0 = a node, ->4 = an array, an object with a virtual method and an
# array) and the vtable's meaning (a virtual method, a particular operation) aren't resolvable, so the name states
# the mechanism: a virtual call (call a virtual method on a node and load a field from the object's array). The "5"
# distinguishes it from the other VirtualCall* variants (VirtualCall_826C0388, VirtualCall2_83231E88, VirtualCall3_
# 822142C0, VirtualCall4_8260C130).
[entrypoint.functions."0x82C26818"]
name = "VirtualCall5_82C26818"

# A process (a vcall sequence, the sibling of ProcessVcallSequence3_82C14D70): call a vcall triple
# (VcallTriple_AllSuccess_82C011E8, on the object and a constant), and (if the result's low byte is non-zero, a
# condition) call another vcall sequence (ProcessVcallSequence3_82C14D70, on obj+4 and a constant).

# Args: obj (r3, r31, +4 = a field), value (r4, r30), (more). It calls a vcall triple (VcallTriple_AllSuccess_82C011
# E8, sub_82C011E8, a vcall triple, on the object (r5 = obj) and a constant (r4, a vtable / a descriptor, from a fixed
# .data address), with the value (r3 = value)). If the result's low byte is non-zero (result & 0xFF != 0, a condition
# / a success flag), it calls another vcall sequence (ProcessVcallSequence3_82C14D70, sub_82C14D70, a vcall sequence,
# on obj+4 (r5 = obj + 4) and a constant (r4, a vtable / a descriptor), with the value (r3 = value)). So it's a
# "process (a vcall sequence: call a vcall triple, and (if a condition) call another vcall sequence)" routine (a vcall
# sequence, part of the 0x82C26 cluster, a sibling of ProcessVcallSequence3_82C14D70). 109 lines, 2 callees (VcallTri
# ple_AllSuccess_82C011E8, ProcessVcallSequence3_82C14D70). 1 caller: sub_82C26940 (a sibling in the 0x82C26 cluster).
# The object's class (obj, r31, +4 = a field, an object with a field) and the value's meaning (a value, a particular
# argument) aren't resolvable, so the name states the mechanism: a vcall sequence (call a vcall triple and (if a
# condition) call another vcall sequence). The "4" distinguishes it from ProcessVcallSequence3_82C14D70.
[entrypoint.functions."0x82C26880"]
name = "ProcessVcallSequence4_82C26880"

# A process (a vcall sequence, the sibling of ProcessVcallSequence4_82C26880 / ProcessVcallSequence3_82C14D70):
# do a virtual call (on obj->0->24, a vtable entry), and (if the result's low byte is non-zero, a condition) call a
# vcall sequence (ProcessVcallSequence4_82C26880, on the object and a value) and do another virtual call (on obj->0->
# 28, a vtable entry).

# Args: obj (r3, r31, ->0 = a node, ->0->24 and ->0->28 = vtable entries), (r4), value (r5, r30), (more). It does a
# virtual call (a vtable dispatch: r11 = obj->0, a node, r10 = r11->24, a vtable entry, mtctr r10, bctrl -- a virtual
# call on obj->0->24, vtable->24). If the result's low byte is non-zero (result & 0xFF != 0, a condition / a success
# flag), it calls a vcall sequence (ProcessVcallSequence4_82C26880, sub_82C26880, a vcall sequence, on the object
# (r4 = obj) and a value (r3 = value)), and (if that also succeeds) does another virtual call (a vtable dispatch: r11
# = obj->0, r10 = r11->28, a vtable entry, mtctr r10, bctrl -- a virtual call on obj->0->28, vtable->28). So it's a
# "process (a vcall sequence: do a virtual call (vtable->24), and (if a condition) call a vcall sequence and do another
# virtual call (vtable->28))" routine (a vcall sequence, part of the 0x82C26 cluster, a sibling of ProcessVcallSequenc
# e4_82C26880). 86 lines, 1 direct callee (ProcessVcallSequence4_82C26880 -- the virtual calls via REX_CALL_INDIRECT_
# FUNC are not counted). 1 caller: sub_82C26FB8 (a sibling in the 0x82C26 cluster). The node's class (a node, ->0 = a
# node, ->24 and ->28 = vtable entries, an object with virtual methods) and the vtable's meaning (a virtual method, a
# particular operation) aren't resolvable, so the name states the mechanism: a vcall sequence (do a virtual call and
# (if a condition) call a vcall sequence and do another virtual call). The "5" distinguishes it from ProcessVcallSeq
# uence4_82C26880 and ProcessVcallSequence3_82C14D70.
[entrypoint.functions."0x82C26940"]
name = "ProcessVcallSequence5_82C26940"

# A queue/ring-buffer push (with a size check): compute the capacity ((obj->12 - obj->4) >> 2, in 4-byte units) and
# the current size ((obj->8 - obj->4) >> 2, in 4-byte units), and (if the current size is in range, less than the
# capacity) store a value (obj->8->0 = value->0, a 4-byte store) and advance the write pointer (obj->8 += 4).

# Args: obj (r3, ->4 = a base, ->8 = a write pointer, ->12 = a capacity), value (r4, r6, ->0 = a value), (more). It
# computes the capacity ((obj->12 - obj->4) >> 2, the number of 4-byte slots in the buffer, from the base (obj->4)
# to the capacity (obj->12)) and the current size ((obj->8 - obj->4) >> 2, the number of 4-byte slots used, from the
# base (obj->4) to the write pointer (obj->8)). If the current size (r8) is less than the capacity (r9, an in-range
# check), it stores a value (obj->8->0 = value->0, a 4-byte store, a push) and advances the write pointer (obj->8 +=
# 4, a write pointer advance). If the current size is >= the capacity (an overflow), it takes one path (an error / a
# full buffer, calling sub_822F7530, a sibling in the 0x822F7 cluster). So it's a "push to a queue / ring buffer (compute
# the capacity and the current size, and (if in range) store a value and advance the write pointer)" routine (a queue
# / ring-buffer push, part of the 0x822F5 cluster). 83 lines, 1 callee (sub_822F7530). Many callers (sub_827D4588, sub
# _82545C30, sub_82A72590, sub_824489A0, sub_82B27418, sub_82A6CEF0, sub_82A83420, sub_82363628, sub_823419E0, sub
# _8235F430 -- many across the codebase). The object's class (obj, ->4 = a base, ->8 = a write pointer, ->12 = a
# capacity, a queue / ring buffer with a base, a write pointer, and a capacity) and the value's meaning (a value, a 4
# -byte slot, a particular data item) aren't resolvable, so the name states the mechanism: a queue / ring-buffer push
# (compute the capacity and the current size, and (if in range) store a value and advance the write pointer).
[entrypoint.functions."0x822F5C68"]
name = "QueuePush_822F5C68"

# A process (a clamped computation, the sibling of ComputeClamped_83230A48 / the callee of Heap_CarveBlock_83231058
# ): compute a clamped value (ComputeClamped_83230A48, on a value, with a constant), and (if the result is non-zero)
# process (a computation with obj->0 and constants, 0x10000 and 0x1000, a size / a block-size computation).

# Args: obj (r3, r31, ->0 = a value), (r4), (r5), value (r6, r8), (more). It computes a clamped value (ComputeClamped
# _83230A48, sub_83230A48, a clamped computation, on the value (r6 = value) and a constant (r7 = stack[80], a
# min/max), with a result at stack[80]). If the result (stack[80]) is zero (an empty / a zero), it takes one path. If
# the result is non-zero, it processes: it loads a value (r10 = obj->0, a value), compares it against constants (0x100
# 00 = 1 << 16, and 0x1000 = 1 << 12, a size / a block-size check), and (depending on the comparison) computes a value
# (a size, a block-size computation, with a 4-byte stride, r6 = 4). So it's a "process (a clamped computation: compute
# a clamped value, and (if the result is non-zero) compute a size / a block size (with obj->0 and constants))" routine
# (a clamped computation, part of the 0x83230 cluster, a sibling of ComputeClamped_83230A48, a callee of
# Heap_CarveBlock_83231058). 92 lines, 2 callees (ComputeClamped_83230A48, sub_82CC0CA0). 1 caller: Heap_CarveBlock_8
# 3231058 (sub_83231058, a heap block carver, which calls this to compute a clamped value / a block size). The
# object's class (obj, r31, ->0 = a value, an object with a value) and the value's meaning (a value, a size / a block
# size, a particular dimension) aren't resolvable, so the name states the mechanism: a clamped computation (compute a
# clamped value and (if the result is non-zero) compute a size / a block size). The "2" distinguishes it from Compute
# Clamped_83230A48.
[entrypoint.functions."0x83230D00"]
name = "ComputeClamped2_83230D00"

# A heap allocation (the callee of Allocate_SizeBucketed_83231D20 / PoolAllocate_8240DAA8): clamp the size (min(size,
# obj->16, a max size), default to 16 if zero), align it to 16 bytes ((size + 31) & ~0xF), and (if a condition, obj
# ->24 is zero) carve a block (Heap_CarveBlock_83231058) and perform a heap operation (sub_83230DA0).

# Args: obj (r3, r31, ->16 = a max size, ->24 = a field), size (r4), (more). It clamps the size (min(size, obj->16,
# a max size): if size >= obj->16, it sets size = obj->16). If size is zero, it defaults to 16 (a minimum size). It
# aligns the size to 16 bytes ((size + 31) & ~0xF, a 16-byte alignment, a block alignment). If obj->24 (a field, a
# free-list pointer) is zero (an empty state), it performs a heap operation (sub_83230DA0, a heap operation, on the
# object) and (if the result is non-zero) carves a block (Heap_CarveBlock_83231058, sub_83231058, a heap block
# carver, on the object, with the aligned size). So it's a "allocate from a heap (clamp and align the size, and (if a
# condition) carve a block and perform a heap operation)" routine (a heap allocation, part of the 0x83231 cluster, a
# callee of Allocate_SizeBucketed_83231D20 and PoolAllocate_8240DAA8). 196 lines, 2 callees (Heap_CarveBlock_83231058
# , sub_83230DA0). 4 callers: sub_82A20528, Allocate_SizeBucketed_83231D20 (sub_83231D20, a size-bucketed
# allocation), PoolAllocate_8240DAA8 (sub_8240DAA8, a pool allocation), sub_828456C8. The object's class (obj, r31, ->
# 16 = a max size, ->24 = a field, a heap object with a max size and a free-list pointer) and the size's meaning (a
# size, a particular allocation size) aren't resolvable, so the name states the mechanism: a heap allocation (clamp
# and align the size, and (if a condition) carve a block and perform a heap operation).
[entrypoint.functions."0x83231590"]
name = "HeapAllocate_83231590"

# A copy (a simple 8-byte copy): copy two words (src->124 and src->128, an 8-byte value) to dst->0 and dst->4 (a
# simple 8-byte copy, from an offset in the source to the start of the destination).

# Args: dst (r3, ->0 and ->4, a destination), src (r4, ->124 and ->128, a source), (more). It copies two words (src
# ->124 to dst->0, and src->128 to dst->4, an 8-byte copy, from an offset (124) in the source to the start (0) of the
# destination). So it's a "copy 8 bytes (copy two words, src->124 and src->128, to dst->0 and dst->4)" routine (a
# simple 8-byte copy, part of the 0x82B56 cluster). 10 lines, no callees (pure data manipulation). It is registered (in
# the function table, fable_2_init.cpp / fable_2_register.cpp) but has no direct callers in the recompiled code (a
# registered-but-uncalled copy, likely invoked only from code outside the traced region, or unused). The source's class
# (src, ->124 and ->128, an object with an 8-byte value at an offset) and the destination's class (dst, ->0 and ->4,
# a destination for the 8-byte value) aren't resolvable, so the name states the mechanism: a simple 8-byte copy (copy
# two words, src->124 and src->128, to dst->0 and dst->4).
[entrypoint.functions."0x82B56820"]
name = "Copy8Bytes_82B56820"

# A 24-byte constructor (the sibling of Construct28Byte_82A12828 / Construct16Byte_82275368): allocate 24 bytes
# (Allocate_OrDbgBreak_8221F388), zero out the object's fields (->0, ->4, ->8 = 0, and ->21 = 0, a zero), and set a
# flag (->20 = 1, a one, a byte flag).

# Args: obj (r3, r31, a parent), (more). It allocates 24 bytes (Allocate_OrDbgBreak_8221F388, sub_8221F388, a
# size-bucketed allocation, of 24 bytes). It then zeros out the object's fields (->0, ->4, ->8 = 0, three words, and
# ->21 = 0, a zero -- a byte flag, and ->20 = 1, a one -- a byte flag). It then (presumably) stores the new object into
# the parent (obj, r31). So it's a "construct a 24-byte object (allocate 24 bytes, zero out the fields (and set ->20 =
# 1, ->21 = 0), and store it into the parent)" routine (a 24-byte constructor, part of the 0x8246C cluster). 87 lines,
# 1 callee (Allocate_OrDbgBreak_8221F388). Many callers (sub_823F9C10, sub_82B722E8, sub_827ED030, sub_8235E7C0, sub
# _8324E200, sub_8249C690, sub_825CC450, sub_823FF7E8, sub_8322C840, sub_825C4E80 -- many across the codebase). The
# object's class (a 24-byte object, ->0, ->4, ->8 = words, ->20 = a flag, ->21 = a flag, an object with flags) and the
# parent's class (obj, r31, an object with a 24-byte object) aren't resolvable, so the name states the mechanism: a 24
# -byte constructor (allocate 24 bytes and zero out the fields). The "24" distinguishes it from the other constructor
# sizes (Construct16Byte_82275368, Construct20ByteNode_82A12F00, Construct28Byte_82A12828, Construct36Byte_82370EF8,
# Construct48ByteNode_82A0ED98).
[entrypoint.functions."0x8246C600"]
name = "Construct24Byte_8246C600"

# A copy (a 20-byte copy, the sibling of Copy8Bytes_82B56820 / CopyRecords20_82A13138): copy 20 bytes (four words
# (src->0, ->4, ->8, ->12) and a float (src->16)) from src to dst, then call a process (ProcessCount_82B728D8, on dst
# + 20 and src + 20, the objects' tails).

# Args: dst (r3, r31, +20 = a tail), src (r4, r11, +20 = a tail), (more). It copies 20 bytes (from src to dst -- four
# words: src->0 to dst->0, src->4 to dst->4, src->8 to dst->8, src->12 to dst->12, and a float: src->16 to dst->16, a
# 20-byte copy). It then calls a process (ProcessCount_82B728D8, sub_82B728D8, a count process, on the objects' tails
# (dst + 20 and src + 20, the 20-byte objects' tails)). So it's a "copy a 20-byte object (copy four words and a float
# from src to dst, and process the objects' tails)" routine (a 20-byte copy, part of the 0x82B82 cluster, a sibling of
# Copy8Bytes_82B56820). 50 lines (to the next function), 1 callee (ProcessCount_82B728D8). 1 caller: sub_82B828C8 (a
# sibling in the 0x82B82 cluster). The object's class (a 20-byte object, ->0, ->4, ->8, ->12 = words, ->16 = a float, ->
# 20 = a tail, an object with a head and a tail) and the source/destination's meaning (a 20-byte object, a data object
# / node / value) aren't resolvable, so the name states the mechanism: a 20-byte copy (copy four words and a float from
# src to dst, and process the objects' tails). The "20" distinguishes it from Copy8Bytes_82B56820 (8-byte).
[entrypoint.functions."0x82B82C68"]
name = "Copy20Bytes_82B82C68"

# A process (a critical section operation, the sibling of the 0x82B5C cluster): enter a critical section
# (CriticalSection_EnterOrTry_82200688), call a process (sub_82B5D548, on obj->0 and a constant, with a result at
# stack[96]), validate the result (stack[96] is non-zero and != a constant, a trap / an error if invalid), and (on a
# success path) perform further processing (sub_82B5D690, a sibling in the 0x82B5D cluster).

# Args: obj (r3, r31, ->0 = a value), value (r4, r30), (more). It enters a critical section (CriticalSection_EnterOrT
# ry_82200688, sub_82200688, a critical section enter-or-try, on a global, with a constant). It then calls a process
# (sub_82B5D548, a process, on obj->0 (stack[80], a value, copied from obj->0) and a constant (stack[96], a result
# buffer), with the value (r30)). It validates the result (stack[96], a value): if it is zero or equal to a constant (a
# fixed .data address, a sentinel), it takes an error path (a trap / an error). On a success path (stack[96] is
# non-zero and != the sentinel), it performs further processing (sub_82B5D690, a sibling in the 0x82B5D cluster). So
# it's a "process (a critical section operation: enter a critical section, call a process, validate the result, and
# (on success) perform further processing)" routine (a critical section process, part of the 0x82B5C cluster). 218
# lines, 3 callees (CriticalSection_EnterOrTry_82200688, sub_82B5D548, sub_82B5D690). 1 caller: sub_82B82248 (a
# sibling in the 0x82B82 cluster). The object's class (obj, r31, ->0 = a value, an object with a value) and the value
# 's meaning (a value, a particular argument) aren't resolvable, so the name states the mechanism: a critical section
# process (enter a critical section, call a process, validate the result, and (on success) perform further processing
# ).
[entrypoint.functions."0x82B5C758"]
name = "ProcessCriticalSection_82B5C758"

# A chain search (the sibling of ChainSearch_82B3D880): walk a chain (a two-key comparison: node->12 against a
# value, following the node's next pointers, node->8 / node->0, with a ->21 flag), searching for a match, and process
# the found node (with the list).

# Args: (r3), value (r4, ->0 = a value), (more). It loads a list (a global, a fixed .data address, a list of nodes),
# and walks the chain (from the list's node, following the ->4, ->8, and ->0 pointers): it compares the node's field
# (node->12, a value) against the target value (r7, from r4->0), and (depending on the comparison) follows the node's
# next pointer (node->8, a next) or the node's head (node->0, a head / prev), walking to the next node (node->4->21,
# a flag). If the node's flag (node->21, a byte) is zero, it continues the walk; if it is non-zero, it stops (a found
# node / a match). It then processes the found node (r9, the found node) and the list (r8, the list), storing them to
# the stack (a result, for the caller). So it's a "search a chain (walk the chain by a comparison, node->12 against a
# value, following the next pointers, and process the found node)" routine (a chain search, part of the 0x82B5D
# cluster, a sibling of ChainSearch_82B3D880). 91 lines, no direct callees (pure data manipulation). 4 callers: sub
# _827F0358, ProcessCriticalSection_82B5C758 (sub_82B5C758, a critical section process), sub_82B80C18, sub_822001F8
# . The chain's element type (a node, ->4 = a next, ->8 = a next, ->12 = a value, ->21 = a flag, a chain/list node)
# and the value's meaning (a searched value, a particular match) aren't resolvable, so the name states the mechanism:
# a chain search (walk the chain by a comparison and process the found node). The "2" distinguishes it from ChainSear
# ch_82B3D880.
[entrypoint.functions."0x82B5D548"]
name = "ChainSearch2_82B5D548"

# A chain search (the sibling of ChainSearch2_82B5D548 / ChainSearch_82B3D880): walk a chain (a two-key comparison:
# node->12 against a value, following the node's next pointers, node->8 / node->0, with a ->21 flag), searching for a
# match, and (in a path) dispose (Dispose17_82B6AB20) and call a sibling (sub_82B5E330).

# Args: (r3), value (r4, ->0 = a value), (more). It loads a list (a global, a fixed .data address, a list of nodes),
# and walks the chain (from the list's node, following the ->4, ->8, and ->0 pointers): it compares the node's field
# (node->12, a value) against the target value (r7, from r4->0), and (depending on the comparison) follows the node's
# next pointer (node->8, a next) or the node's head (node->0, a head / prev), walking to the next node (node->4->21,
# a flag). If the node's flag (node->21, a byte) is zero, it continues the walk; if it is non-zero, it stops (a found
# node / a match). In a path it disposes (Dispose17_82B6AB20, sub_82B6AB20, a destructor with an atomic decrement) and
# calls a sibling (sub_82B5E330, a sibling in the 0x82B5E cluster). So it's a "search a chain (walk the chain by a
# comparison, node->12 against a value, following the next pointers, and (in a path) dispose and call a sibling)"
# routine (a chain search, part of the 0x82B5D cluster, a sibling of ChainSearch2_82B5D548). 135 lines, 2 callees
# (Dispose17_82B6AB20, sub_82B5E330). 1 caller: ProcessCriticalSection_82B5C758 (sub_82B5C758, a critical section
# process). The chain's element type (a node, ->4 = a next, ->8 = a next, ->12 = a value, ->21 = a flag, a chain/list
# node) and the value's meaning (a searched value, a particular match) aren't resolvable, so the name states the
# mechanism: a chain search (walk the chain by a comparison and (in a path) dispose). The "3" distinguishes it from
# ChainSearch2_82B5D548 and ChainSearch_82B3D880.
[entrypoint.functions."0x82B5D690"]
name = "ChainSearch3_82B5D690"

# A process (a chain/list operation, the sibling of ProcessChain2_8250AF60 / the callee of ChainSearch3_82B5D690):
# (if a list's field, list->8, is zero) call a process (sub_82B5E558, on the list, with a flag), and (otherwise)
# process the list (call ProcessChain2_8250AF60 and sibling operations, sub_82B5D7F0 and sub_8273F9D8).

# Args: obj (r3, r30), (r4), a (r5, r26), b (r6, r29), (more). It loads a list (a global, a fixed .data address, a
# list of nodes). If the list's field (list->8, a field, a free-list pointer / a head) is zero (an empty state), it
# calls a process (sub_82B5E558, on the list (r4 = list), with the list's field (r6 = list->4), a value (r7 = b), and
# a flag (r5 = 1), a list initialization / a setup). Otherwise (the list is non-empty), it processes the list (call
# ProcessChain2_8250AF60, sub_8250AF60, a chain operation, and sibling operations, sub_82B5D7F0 and sub_8273F9D8 --
# siblings in the 0x82B5D/0x8273F clusters). So it's a "process (a chain/list operation: (if the list is empty) call a
# process, and (otherwise) process the list (call a chain operation and siblings))" routine (a chain/list process,
# part of the 0x82B5E cluster, a callee of ChainSearch3_82B5D690). 311 lines, 4 callees (ProcessChain2_8250AF60,
# sub_82B5D7F0, sub_82B5E558, sub_8273F9D8). 1 caller: ChainSearch3_82B5D690 (sub_82B5D690, a chain search, which
# calls this to process the found list). The list's element type (a node, a list of nodes, a particular structure)
# and the value's meaning (a value, a particular argument) aren't resolvable, so the name states the mechanism: a
# chain/list process ((if the list is empty) call a process, and (otherwise) process the list). The "3" distinguishes
# it from ProcessChain2_8250AF60.
[entrypoint.functions."0x82B5E330"]
name = "ProcessChain3_82B5E330"

# A process (a chain/list operation, the sibling of ProcessChain3_82B5E330 / the callee of ProcessChain3_82B5E330):
# validate a size (list->8 >= 0x00800002, a minimum-size check; if too small, raise an error with a string, via
# SetString_822F2020, and a trap), and (on the normal path) allocate (Allocate_OrDbgBreak_8221F388) and perform a
# complex computation (a chain/list operation, part of the 0x82B5E cluster).

# Args: list (r3, r24, a list), (r4), a (r5, r26), b (r6, r31), c (r7, r28), (more). It validates a size (list->8, a
# size, from a global, a fixed .data address): if the size is < 0x00800002 (a minimum size, a too-small check), it
# raises an error (SetString_822F2020, sub_822F2020, a string setter, with a message, and a call to sub_826C3EF0, and
# a trap). On the normal path (size >= 0x00800002), it allocates (Allocate_OrDbgBreak_8221F388, sub_8221F388, a
# size-bucketed allocation, for the list) and performs a complex computation (a chain/list operation, with a large
# stack frame (224 bytes), and calls to sub_82171810, sub_822F1F00, sub_826C3EF0, sub_826C3FA8, sub_82B5E7F8,
# sub_82B5E878 -- siblings in the 0x82171/0x822F1/0x826C3/0x82B5E clusters). So it's a "process (a chain/list
# operation: validate a size, and (on the normal path) allocate and perform a complex computation)" routine (a chain
# / list process, part of the 0x82B5E cluster, a sibling of ProcessChain3_82B5E330). 382 lines, 8 callees (Allocat
# e_OrDbgBreak_8221F388, SetString_822F2020, sub_82171810, sub_822F1F00, sub_826C3EF0, sub_826C3FA8, sub_82B5E7F8, sub
# _82B5E878). 2 callers: ProcessChain3_82B5E330 (sub_82B5E330, a chain process, which calls this when the list is
# empty), sub_82B5D7F0 (a sibling in the 0x82B5D cluster). The list's element type (a node, a list of nodes, a
# particular structure) and the size's meaning (a size, a minimum-size check) aren't resolvable, so the name states the
# mechanism: a chain/list process (validate a size and (on the normal path) allocate and perform a complex computatio
# n). The "4" distinguishes it from ProcessChain3_82B5E330 and ProcessChain2_8250AF60.
[entrypoint.functions."0x82B5E558"]
name = "ProcessChain4_82B5E558"

# A 32-byte constructor (the sibling of Construct24Byte_8246C600 / Construct28Byte_82A12828): initialize the
# object's fields (obj->4 = 1, obj->8/->12/->16/->20/->24 = 0, obj->0 = a constant, obj->28 = value->0), and (if
# value->0 is non-zero) loop (a complex float computation, calling Construct24Byte_8246C600 and ProcessCount_82B728D8
# ).

# Args: obj (r3, r31, ->0 = a field, ->4 = a count, ->8/->12/->16/->20/->24 = fields, ->28 = a field), value (r4, ->0
# = a value), a (r5, r29), float (f1), (more). It initializes the object's fields (obj->4 = 1, a count / a one, obj
# ->8/->12/->16/->20/->24 = 0, five zeros, obj->0 = a constant, a class id / a marker, from a fixed .data address, and
# obj->28 = value->0, a value). If value->0 (a value) is zero (an empty state), it takes one path. If value->0 is
# non-zero, it loops (a complex float computation, with a float (f1, a float, in f31), and calls to Construct24Byte
# _8246C600, sub_8246C600, a 24-byte constructor, and ProcessCount_82B728D8, sub_82B728D8, a count process). So it's a
# "construct a 32-byte object (initialize the fields, and (if a value is non-zero) loop (a complex float computation,
# constructing nested 24-byte objects and processing counts))" routine (a 32-byte constructor, part of the 0x82B72
# cluster). 117 lines, 2 callees (Construct24Byte_8246C600, ProcessCount_82B728D8). 3 callers: sub_82515540, sub
# _82B82248, sub_82B5C8B0. The object's class (obj, ->0 = a field, ->4 = a count, ->8/->12/->16/->20/->24 = fields, ->
# 28 = a field, a 32-byte object with nested objects) and the value's meaning (a value, a particular argument) aren't
# resolvable, so the name states the mechanism: a 32-byte constructor (initialize the fields and (if a value is non-
# zero) loop (a complex float computation, constructing nested objects)). The "32" distinguishes it from the other
# constructor sizes (Construct16Byte_82275368, Construct20ByteNode_82A12F00, Construct24Byte_8246C600, Construct28B
# yte_82A12828, Construct36Byte_82370EF8, Construct48ByteNode_82A0ED98).
[entrypoint.functions."0x82B722E8"]
name = "Construct32Byte_82B722E8"

# A process (an atomic increment, the sibling of AtomicIncrement3_821F7798 / AtomicIncrement2_82375F28): load a float
# constant (from a fixed .data address), and atomically increment a global counter (via lwarx/stwcx and a global lock,
# a lock-free atomic increment), and (in a path) destruct a refcounted value (DestructRefCounted_82214F08) and call
# sibling operations.

# Args: obj (r3, r31), (r4, a value), (more). It loads a float constant (from a fixed .data address, a magic constant,
# a particular value). It then atomically increments a global counter (a fixed .data address, a counter): it enters a
# global lock (REX_ENTER_GLOBAL_LOCK, a thread fence, disabling interrupts), does a lock-free atomic increment (lwarx/
# stwcx, a compare-and-swap, on the global counter, incrementing it by one, and retrying on a CAS failure), and exits
# the global lock (REX_LEAVE_GLOBAL_LOCK). In a path it destructs a refcounted value (DestructRefCounted_82214F08, sub
# _82214F08, a refcounted destruct, on a value) and calls sibling operations (sub_821E3B38, sub_82518D30, sub_829EAE38
# -- siblings in the 0x821E3/0x82518/0x829EA clusters). So it's a "process (an atomic increment: load a float constant
# , and atomically increment a global counter (via a global lock), and (in a path) destruct a refcounted value)" routin
# e (an atomic increment, part of the 0x82B82 cluster, a sibling of AtomicIncrement3_821F7798). 180 lines, 4 callees
# (DestructRefCounted_82214F08, sub_821E3B38, sub_82518D30, sub_829EAE38). 1 caller: sub_82B80D70 (a sibling in the 0x
# 82B80 cluster). The counter's meaning (a global counter, a particular count / a statistic) and the value's meaning
# (a value, a particular argument) aren't resolvable, so the name states the mechanism: an atomic increment (load a
# float constant, atomically increment a global counter, and (in a path) destruct a refcounted value). The "4"
# distinguishes it from the other AtomicIncrement* variants (AtomicIncrement_822CBEB8, AtomicIncrement2_82375F28,
# AtomicIncrement3_821F7798).
[entrypoint.functions."0x82B82440"]
name = "AtomicIncrement4_82B82440"

# A chain search (the sibling of ChainSearch3_82B5D690 / ChainSearch2_82B5D548 / ChainSearch_82B3D880): walk a chain
# (a two-key comparison: node->12 against a value, following the node's next pointers, node->8 / node->0, with a ->29
# flag), searching for a match, and (in a path) call sibling operations (sub_823074B8, sub_82308C28, sub_8235E278,
# sub_8264EDF8).

# Args: obj (r3, r31, ->4 = a node), value (r4, r30, ->0 = a value), (more). It loads a node (obj->4->4, a node in a
# chain). If the node's flag (node->29, a byte) is zero, it takes one path (reading a value from r30->0). It then walks
# the chain (a loop): it compares the node's field (node->12, a value) against the target value (r9, from r30->0), and
# (depending on the comparison) follows the node's next pointer (node->8, a next) or the node's head (node->0, a head / 
# prev), walking to the next node (node->4->29, a flag). In a path it calls sibling operations (sub_823074B8, sub
# _82308C28, sub_8235E278, sub_8264EDF8 -- siblings in the 0x82307/0x82308/0x8235E/0x8264E clusters). So it's a "search a
# chain (walk the chain by a comparison, node->12 against a value, following the next pointers, and (in a path) call
# sibling operations)" routine (a chain search, part of the 0x82306 cluster, a sibling of ChainSearch3_82B5D690). 158
# lines, 4 callees (sub_823074B8, sub_82308C28, sub_8235E278, sub_8264EDF8). 1 caller: sub_8281A568. The chain's
# element type (a node, ->4 = a next, ->8 = a next, ->12 = a value, ->29 = a flag, a chain/list node) and the value's
# meaning (a searched value, a particular match) aren't resolvable, so the name states the mechanism: a chain search
# (walk the chain by a comparison and (in a path) call sibling operations). The "4" distinguishes it from ChainSearch
# 3_82B5D690, ChainSearch2_82B5D548, and ChainSearch_82B3D880.
[entrypoint.functions."0x82306B80"]
name = "ChainSearch4_82306B80"

# A process (a recursive construct, with a complex computation): construct a 12-byte object (Construct12Byte_825F7B10
# , on a value and value->4), load a float constant (from a fixed .data address), and perform a complex computation (a
# large stack frame, storing constants and floats, calling GetSingleton_82BA8718, GrowRegion_823F9AE0,
# ProcessIndexedRegion_82BA9068, Serialize_82CA3EB8, VcallProcess_823F8D30, and recursively calling itself).

# Args: obj (r3, r31), a (r4, r28), value (r5, r29, ->4 = a field), (more). It constructs a 12-byte object
# (Construct12Byte_825F7B10, sub_825F7B10, a 12-byte constructor, on the value (r3 = value) and a field (r4 = value->
# 4)). It loads a float constant (from a fixed .data address, a magic constant, a particular value). It then performs a
# complex computation (a large stack frame (192 bytes), storing constants and floats, and calls to GetSingleton_82BA8
# 718, sub_82BA8718, a singleton getter; GrowRegion_823F9AE0, sub_823F9AE0, a region grower; ProcessIndexedRegion_82B
# A9068, sub_82BA9068, an indexed region process; Serialize_82CA3EB8, sub_82CA3EB8, a serialize operation;
# VcallProcess_823F8D30, sub_823F8D30, a vcall process; and sub_823F8F40 -- itself, a recursive call). So it's a
# "process (a recursive construct: construct a 12-byte object, load a float constant, and perform a complex computatio
# n (calling a singleton getter, a region grower, an indexed region process, a serialize operation, a vcall process,
# and recursively calling itself))" routine (a recursive construct, part of the 0x823F8 cluster). 148 lines, 7 callees
# (Construct12Byte_825F7B10, GetSingleton_82BA8718, GrowRegion_823F9AE0, ProcessIndexedRegion_82BA9068,
# Serialize_82CA3EB8, VcallProcess_823F8D30, sub_823F8F40 -- itself, a recursive call). 1 caller: sub_8281A568. The
# object's class (obj, r31, an object with a value) and the value's meaning (a value, a particular argument) aren't
# resolvable, so the name states the mechanism: a recursive construct (construct a 12-byte object, load a float
# constant, and perform a complex computation (recursively calling itself)). The "Recursive" refers to the self-call.
[entrypoint.functions."0x823F8F40"]
name = "ProcessRecursive_823F8F40"

# A process (a hash table operation, the sibling of ProcessHashTable_829FC3C0 / the callee of ChainSearch_82B3D880):
# (if a list's field, list->8, is zero) call a hash table operation (ProcessHashTable_829FC3C0, on the list's field
# (list->4) and a value, with a flag), and (otherwise) process the list (call ProcessChain2_8250AF60 and sibling
# operations, sub_8246E758 and sub_8273F9D8).

# Args: obj (r3, r29), list (r4, r30, ->4 = a field, ->8 = a field), a (r5, r31), b (r6, r28), (more). It checks the
# list's field (list->8, a field, a free-list pointer / a head): if it is zero (an empty state), it calls a hash table
# operation (ProcessHashTable_829FC3C0, sub_829FC3C0, a hash table process, on the list's field (r6 = list->4), a
# value (r7 = b), and a flag (r5 = 1), a hash table initialization / a setup). Otherwise (the list is non-empty), it
# processes the list (call ProcessChain2_8250AF60, sub_8250AF60, a chain operation, and sibling operations, sub
# _8246E758 and sub_8273F9D8 -- siblings in the 0x8246E/0x8273F clusters). So it's a "process (a hash table operation:
# (if the list is empty) call a hash table operation, and (otherwise) process the list (call a chain operation and
# siblings))" routine (a hash table process, part of the 0x824AE cluster, a sibling of ProcessHashTable_829FC3C0, a
# callee of ChainSearch_82B3D880). 372 lines, 4 callees (ProcessHashTable_829FC3C0, ProcessChain2_8250AF60, sub
# _8246E758, sub_8273F9D8). 2 callers: ChainSearch_82B3D880 (sub_82B3D880, a chain search), sub_824AE2E8 (a sibling in
# the 0x824AE cluster). The list's element type (a node, a list of nodes, a hash table / a particular structure) and
# the value's meaning (a value, a particular argument) aren't resolvable, so the name states the mechanism: a hash
# table process ((if the list is empty) call a hash table operation, and (otherwise) process the list). The "2"
# distinguishes it from ProcessHashTable_829FC3C0.
[entrypoint.functions."0x824AEBB8"]
name = "ProcessHashTable2_824AEBB8"

# A process (a hash table lookup, with a construct and a comparison, and a recursive call): construct a refcounted
# value (ConstructRefCounted_8222CF18), call a hash lookup (HashLookup_823F74D0, on a value and obj->20), destruct the
# refcounted value (DestructRefCounted_82214F08), compare strings (CompareString_8229AD78), and (in a path) perform a
# complex computation (a large stack frame, calling GetSingleton_82BA8718, GrowRegion_823F9AE0, ProcessIndexedRegion
# _82BA9068, Serialize_82CA3EB8, VcallProcess_823F8D30, and recursively calling itself).

# Args: obj (r3, r29, ->20 = a field), value (r4, r31, a value), (more). It constructs a refcounted value
# (ConstructRefCounted_8222CF18, sub_8222CF18, a refcounted constructor, on a key (r5 = -1, a key, a hash), and a
# string (r4, a constant, from a fixed .data address, a particular key)). It calls a hash lookup (HashLookup_823F74D0
# , sub_823F74D0, a hash table lookup, on the refcounted value (r6), a field (r5 = obj->20), and the value (r4 = value
# ), storing the result to the stack (r3)). It destructs the refcounted value (DestructRefCounted_82214F08, sub
# _82214F08, a refcounted destruct, on the value). It compares strings (CompareString_8229AD78, sub_8229AD78, a string
# comparison, on a constant (r4) and the result (r3), comparing the looked-up string against a particular key). In a
# path it performs a complex computation (a large stack frame (192 bytes), and calls to GetSingleton_82BA8718, sub
# _82BA8718, a singleton getter; GrowRegion_823F9AE0, sub_823F9AE0, a region grower; ProcessIndexedRegion_82BA9068,
# sub_82BA9068, an indexed region process; Serialize_82CA3EB8, sub_82CA3EB8, a serialize operation; VcallProcess
# _823F8D30, sub_823F8D30, a vcall process; and sub_826CAFA8 -- itself, a recursive call). So it's a "process (a hash
# table lookup: construct a refcounted value, call a hash lookup, destruct the value, compare strings, and (in a path)
# perform a complex computation (recursively calling itself))" routine (a hash lookup process, part of the 0x826CA
# cluster). 262 lines, 16 callees (ConstructRefCounted_8222CF18, HashLookup_823F74D0, DestructRefCounted_82214F08,
# CompareString_8229AD78, Allocate_OrDbgBreak_8221F388, GetSingleton_82BA8718, GrowRegion_823F9AE0, ProcessIndexedReg
# ion_82BA9068, Serialize_82CA3EB8, VcallProcess_823F8D30, Release_RefCounted_821C67D8, Release_RefCounted_829FF648,
# SetValue4_829FD7B8, sub_8254C780, sub_826CAFA8 -- itself, a recursive call, sub_828C9450). 4 callers: sub_826C69B0,
# sub_82378FA0, sub_826C63D8, sub_826CAF30 (siblings in the 0x826CA/0x826C6/0x82378 clusters). The value's meaning (a
# value, a particular lookup key) and the key's meaning (a key, a particular string) aren't resolvable, so the name
# states the mechanism: a hash lookup process (construct a refcounted value, call a hash lookup, destruct the value,
# compare strings, and (in a path) perform a complex computation (recursively calling itself)).
[entrypoint.functions."0x826CAFA8"]
name = "ProcessHashLookup_826CAFA8"

# A process (a vector/SIMD computation, the sibling of ProcessVector_82A3B1E8): load a float constant (from a fixed
# .data address), and perform a vector/SIMD computation (vector loads and stores, a vector AND (vand), and other
# vector operations, on a value and a vector constant).

# Args: a (r3, a value), (more). It loads a float constant (from a fixed .data address, a magic constant, a particular
# value). It loads a vector constant (lvx128 v0, from a fixed .data address, a vector of constants, a SIMD constant).
# It then performs a vector/SIMD computation (lvlx v13, a vector load, on a value, from the stack; vand v12, a vector
# AND, combining the vector value (v13) and the vector constant (v0); stvx128 v12, a vector store, storing the result
# to r3, a value). So it's a "process (a vector/SIMD computation: load a float constant and a vector constant, and
# perform a vector/SIMD computation (a vector AND and a vector store))" routine (a vector/SIMD process, part of the
# 0x8223B cluster, a sibling of ProcessVector_82A3B1E8). 106 lines, no direct callees (pure data manipulation). 10+
# callers: sub_821B5848, sub_8248B500, sub_822961A0, sub_82A5D430, sub_82881778, sub_82A3A420, sub_826B7D70, sub
# _826E9AB8, sub_8243FAE8, sub_826CD3A8 (a widely-called vector process, part of the game's vector/SIMD subsystem).
# The value's meaning (a value, a SIMD value, a particular vector) and the vector constant's meaning (a vector
# constant, a particular SIMD constant) aren't resolvable, so the name states the mechanism: a vector/SIMD process
# (load a float constant and a vector constant, and perform a vector/SIMD computation). The "2" distinguishes it from
# ProcessVector_82A3B1E8.
[entrypoint.functions."0x8223BD98"]
name = "ProcessVector2_8223BD98"

# A process (a heap allocation operation, the callee of Pool_AcquireNode_83230688 / the sibling of Heap_CarveBlock
# _83231058): call a process (sub_83236230, on obj->20, a size), and (if the result is non-zero) perform a size
# computation (a divide, rounding up to a multiple, a heap allocation size), storing the result to a value.

# Args: obj (r3, r31, ->4 = a field, ->8 = a size, ->12 = a size, ->20 = a size), (more). It calls a process (sub
# _83236230, on the object's field (obj->20, a size), a pool / a bucket). If the result (r3) is zero (an empty state),
# it returns. If the result is non-zero, it performs a size computation (a divide: it loads the object's field (obj
# ->12, a size, a block size / a stride), adds 15 to it (a rounding, a + 15, for alignment), divides (obj->12 + 15)
# / obj->12, rounding up to a multiple of obj->12, a block count, and stores the result (the block count * the block
# size + the offset, a heap allocation size) to the value (r3->0). It then loads the object's fields (obj->4, a field
# , and obj->8, a size, a count) and continues (a heap allocation, carving a block from the pool, part of the 0x83230
# heap cluster). So it's a "process (a heap allocation: call a process (on a size), and (if the result is non-zero)
# perform a size computation (a divide, rounding up to a multiple, a heap allocation size))" routine (a heap
# allocation process, part of the 0x83230 heap cluster, a callee of Pool_AcquireNode_83230688, a sibling of Heap
# _CarveBlock_83231058). 157 lines, 1 callee (sub_83236230, sub_83236230, a pool / a bucket process). 1 caller: Pool
# _AcquireNode_83230688 (sub_83230688, a pool node acquirer). The object's class (obj, ->4 = a field, ->8 = a size,
# ->12 = a block size, ->20 = a size, a pool / a heap object) and the size's meaning (a size, a heap allocation size)
# aren't resolvable, so the name states the mechanism: a heap allocation process (call a process (on a size) and (if
# the result is non-zero) perform a size computation (a divide, rounding up to a multiple, a heap allocation size)).
[entrypoint.functions."0x83230568"]
name = "ProcessHeapAllocate_83230568"

# A process (a pool/bucket acquisition, with a spinlock, the callee of ProcessHeapAllocate_83230568 / the sibling of
# Pool_AcquireNode_83230688): acquire a spinlock (SpinLock_Acquire_822D7408, on obj->32), and (if obj->12 is zero) call
# a process (sub_8322FF90, on obj->16), and (otherwise) process the pool (increment obj->8, a count, and call sibling
# operations, sub_829FD240 and sub_8322FB30, on a global pool).

# Args: obj (r3, r31, ->8 = a count, ->12 = a field, ->16 = a field, ->32 = a spinlock), (more). It acquires a
# spinlock (SpinLock_Acquire_822D7408, sub_822D7408, a spinlock acquisition, on the object's field (obj->32, a
# spinlock), serializing access to the pool). If the object's field (obj->12, a field, a free-list pointer / a head)
# is zero (an empty state), it calls a process (sub_8322FF90, on the object's field (obj->16, a field, a pool / a
# bucket), a pool allocation / a bucket acquisition); if the result is zero (a failure), it returns; otherwise it
# continues. If obj->12 is non-zero (a non-empty state), it processes the pool (increments the object's field (obj->8,
# a count, a block count), and calls sibling operations (sub_829FD240, sub_8322FB30 -- siblings in the 0x829FD/0x8322F
# clusters, on a global pool, a fixed .data address, a pool / a free list). So it's a "process (a pool/bucket
# acquisition: acquire a spinlock, and (if obj->12 is zero) call a process, and (otherwise) process the pool (increment
# a count and call sibling operations, on a global pool))" routine (a pool acquisition process, part of the 0x83236
# heap cluster, a callee of ProcessHeapAllocate_83230568, a sibling of Pool_AcquireNode_83230688). 156 lines, 4
# callees (SpinLock_Acquire_822D7408, sub_829FD240, sub_8322FB30, sub_8322FF90). 1 caller: ProcessHeapAllocate
# _83230568 (sub_83230568, a heap allocation process, which calls this to acquire a pool node). The object's class
# (obj, ->8 = a count, ->12 = a field, ->16 = a field, ->32 = a spinlock, a pool / a heap object) and the field's
# meaning (a field, a particular pool state) aren't resolvable, so the name states the mechanism: a pool acquisition
# process (acquire a spinlock and (if obj->12 is zero) call a process, and (otherwise) process the pool (increment a
# count and call sibling operations)).
[entrypoint.functions."0x83236230"]
name = "ProcessPoolAcquire_83236230"

# A process (a double-vcall, the sibling of VirtualCall4_8260C130): (if a value is non-zero) perform a double-vcall
# (call obj->4->0->32, a virtual method, and then obj->4->0->76, another virtual method, on a node), and (depending on
# a flag, r3 & 0xFF) take a path.

# Args: obj (r3, r31, ->4 = a node, ->24 = a field), value (r4, r30, a value), a (r5, r29), b (r6, r28), (more). It
# checks the value (r30, a value): if it is zero (an empty state), it takes one path. If it is non-zero, it performs a
# double-vcall (call obj->4->0->32, a virtual method, on the node (obj->4, a node, a refcounted object with a vtable at
# ->0), and then obj->4->0->76, another virtual method, on the node). It then checks a flag (r3 & 0xFF, a low-byte
# flag, a state / a marker): if it is zero (a state), it takes one path; if it is non-zero, it continues (a complex
# computation, with a large stack frame (128 bytes), and a call to ComputeClamped2_83230D00, sub_83230D00, a clamp
# computation). So it's a "process (a double-vcall: (if a value is non-zero) perform a double-vcall (call two virtual
# methods on a node), and (depending on a flag) take a path)" routine (a double-vcall process, part of the 0x82BB0
# cluster, a sibling of VirtualCall4_8260C130). 121 lines, no direct callees (pure data manipulation, with indirect
# vcalls). 0 callers: it is registered in the function table but not called from any recompiled guest function (a
# registered-but-uncalled function, part of the 0x82BB0 cluster, a double-vcall operation). The node's class (obj->4,
# a node, a refcounted object with a vtable at ->0, ->32 and ->76 = virtual methods) and the value's meaning (a value,
# a particular argument) aren't resolvable, so the name states the mechanism: a double-vcall process ((if a value is
# non-zero) perform a double-vcall (call two virtual methods on a node) and (depending on a flag) take a path).
[entrypoint.functions."0x82BB06D8"]
name = "ProcessDoubleVcall_82BB06D8"

# A 32-byte node constructor (the sibling of Construct32Byte_82B722E8 / ConstructNode28_83218AE8): grow the object
# (obj->84 += 32, a 32-byte allocation, carving a new node from the object's pool), initialize the new node's fields (a
# 32-byte object: node->8/->12/->16/->20/->24 = 0, node->28 = 0 (a 16-bit), node->31 = 0 (a byte), node->0 = a, node
# ->4 = b, node->30 = c (a byte)), and (if a value is non-zero) continue (a complex computation, with the value's
# field, value->20).

# Args: obj (r3, r30, ->84 = a pointer, a pool / a free list), value (r4, ->20 = a field), a (r5), b (r6), c (r8),
# (more). It grows the object (obj->84 += 32, a 32-byte allocation, carving a new node from the object's pool, a fixed
# 32-byte node size). It then initializes the new node's fields (the node at obj->84 - 32, a 32-byte object: node->8/
# ->12/->16/->20/->24 = 0, five zeros, node->28 = 0 (a 16-bit), node->31 = 0 (a byte), node->0 = a (r5, a value, a
# field), node->4 = b (r6, a value, a field), node->30 = c (r8, a byte, a flag / a marker)). If the value (r4, a value
# ) is non-zero, it continues (a complex computation, with the value's field (value->20, a field, a size / a count),
# and (if it is non-zero) further processing). So it's a "construct a 32-byte node (grow the object, obj->84 += 32, a
# 32-byte allocation, and initialize the new node's fields, and (if a value is non-zero) continue (a complex
# computation))" routine (a 32-byte node constructor, part of the 0x82BAC cluster, a sibling of Construct32Byte_82B722
# E8). 136 lines, no direct callees (pure data manipulation). 1 caller: sub_82BC1968. The object's class (obj, ->84 =
# a pointer, a pool / a free list of 32-byte nodes) and the value's meaning (a value, a particular argument) aren't
# resolvable, so the name states the mechanism: a 32-byte node constructor (grow the object and initialize the new
# node's fields). The "32" distinguishes it from the other node sizes (Construct16Byte_82275368, Construct20ByteNod
# e_82A12F00, Construct24Byte_8246C600, Construct28Byte_82A12828, ConstructNode28_83218AE8, Construct36Byte_82370EF8
# , Construct48ByteNode_82A0ED98, Construct32Byte_82B722E8).
[entrypoint.functions."0x82BACFB0"]
name = "ConstructNode32_82BACFB0"

# A process (a line parse, with an atomic increment and a call, the caller of ParseLineValue_82B46D40 / the sibling of
# ParseNumberValue_82B470B8 / ParseFloatValue_82B47F98): atomically increment a global counter (via lwarx/stwcx and a
# global lock, a lock-free atomic increment), and call a line parse (ParseLineValue_82B46D40, on a stack value and an
# object), and (in a path) construct and destruct a refcounted value.

# Args: obj (r3, r31, an object), (more). It atomically increments a global counter (a fixed .data address, a counter,
# a line / a value count): it enters a global lock (REX_ENTER_GLOBAL_LOCK, a thread fence, disabling interrupts), does
# a lock-free atomic increment (lwarx/stwcx, a compare-and-swap, on the global counter, incrementing it by one, and
# retrying on a CAS failure), and exits the global lock (REX_LEAVE_GLOBAL_LOCK). It calls a line parse (ParseLineValue
# _82B46D40, sub_82B46D40, a line value parser, on a stack value (r4, a stack buffer, a parsed value) and the object
# (r3, an object, a line / a value object), parsing a line and storing the result to the stack). In a path it
# constructs a refcounted value (ConstructRefCounted_8222CF18, sub_8222CF18, a refcounted constructor) and destructs a
# refcounted value (DestructRefCounted_82214F08, sub_82214F08, a refcounted destruct). So it's a "process (a line
# parse: atomically increment a global counter, call a line parse, and (in a path) construct and destruct a refcounted
# value)" routine (a line parse process, part of the 0x82B47 line-reader cluster, a caller of ParseLineValue_82B46D40,
# a sibling of ParseNumberValue_82B470B8). 130 lines, 4 callees (ConstructRefCounted_8222CF18, DestructRefCounted
# _82214F08, ParseLineValue_82B46D40, sub_82B47E40). 8 callers: sub_82466098, sub_82691A70, sub_825F5238, sub
# _824E3FA0, sub_828152D0, sub_82359BE8, sub_82817D28, sub_8281A568 (a widely-called line parse process, part of the
# game's line-reader subsystem). The object's class (obj, an object, a line / a value object, a particular structure)
# and the counter's meaning (a global counter, a line / a value count) aren't resolvable, so the name states the
# mechanism: a line parse process (atomically increment a global counter, call a line parse, and (in a path) construct
# and destruct a refcounted value).
[entrypoint.functions."0x82B47148"]
name = "ProcessLineParse_82B47148"

# A constant stub: return a constant (0x00010000, 65536).

# Args: none. It loads a constant (0x00010000, 65536, a power of two, a particular size / a count / a flag) into r3
# (the return value register) and returns (blr, a return, returning the constant). So it's a "return a constant (0x00
# 010000, 65536)" routine (a constant stub, part of the 0x82A14 cluster, a sibling of ReturnConstant8192_82B5C730
# / ReturnConstant0x8000_82364B18). 4 lines, no direct callees (a trivial constant stub). 0 callers: it is registered
# in the function table but not called from any recompiled guest function (a registered-but-uncalled function, part of
# the 0x82A14 cluster, a constant stub). The constant's meaning (0x00010000, 65536, a particular size / a count / a
# flag) isn't resolvable, so the name states the mechanism: a constant stub returning 0x00010000. The "0x10000"
# distinguishes it from the other constant stubs (ReturnConstant2_822490B8, ReturnConstant8_82A141C8,
# ReturnConstant16_82C1D8E0, ReturnConstant64_83233840, ReturnConstant128_83233860, ReturnConstant256_83233880,
# ReturnConstant0x8000_82364B18, ReturnConstant8192_82B5C730).
[entrypoint.functions."0x82A14660"]
name = "ReturnConstant_0x10000_82A14660"

# A getter (a string length, the UTF-16 variant of StrLen_822F1DE0 / the sibling of StrLen_822F1DE0): scan a UTF-16
# string (a 2-byte character loop, looking for a null terminator), and return the length (in characters, minus 1).

# Args: a string (r3, a pointer to a UTF-16 string, a 2-byte character array). It scans the string (a loop: it reads a
# 16-bit value (a UTF-16 character, lhz, a 16-bit load) from the string, advances by 2 bytes (a UTF-16 character
# stride, addi r11, r11, 2), and continues until it finds a null terminator (a 16-bit zero, a UTF-16 null character)).
# It then computes the length (in bytes, r11 - r3, the difference between the end and the start, a length in bytes),
# divides by 2 (a length in characters, srawi r11, r11, 1, a right shift by 1, a division by 2, converting bytes to
# UTF-16 characters), and returns the length minus 1 (addi r3, r11, -1, a length minus 1, a particular convention, a
# one-based / a zero-based offset). So it's a "get a string length (scan a UTF-16 string (a 2-byte character loop,
# looking for a null terminator) and return the length (in characters, minus 1))" routine (a string length getter, the
# UTF-16 variant of StrLen_822F1DE0, part of the 0x8217A cluster, a sibling of StrLen_822F1DE0). ~20 lines, no direct
# callees (pure data manipulation). 10+ callers: sub_82C72640, sub_82BFD228, sub_82BF1CA0, sub_82B39260, sub_82C75340,
# sub_82B4DA88, sub_822F8620, sub_82CBD5A0, sub_82C7F458, sub_82CFA1B0 (a widely-called string length getter, part of
# the game's string subsystem). The string's meaning (a string, a particular UTF-16 string, a particular text) isn't
# resolvable, so the name states the mechanism: a string length getter (scan a UTF-16 string (a 2-byte character loop,
# looking for a null terminator) and return the length (in characters, minus 1)). The "UTF16" refers to the 2-byte
# character stride (a UTF-16 string, as opposed to StrLen_822F1DE0's byte-wise scan, a UTF-8 / a byte string).
[entrypoint.functions."0x8217AB30"]
name = "StrLenUTF16_8217AB30"

# A 16-byte constructor (the sibling of Construct16Byte_82275368): initialize the object's fields (obj->4/->8/->12 = 0
# , three zeros, a 16-byte object), compute a count ((value->8 - value->4) / 12, a range divided by 12, a block count
# / a stride), and (if the count is non-zero) call a process (sub_8282EFD8, on the object, a setup / a grow).

# Args: obj (r3, r31, ->4 = a field, ->8 = a field, ->12 = a field), value (r4, r29, ->4 = a field, ->8 = a field),
# (more). It initializes the object's fields (obj->4 = 0, a field, a count / a size, obj->8 = 0, a field, a count / a
# size, obj->12 = 0, a field, a count / a size, three zeros, a 16-byte object). It computes a count (a range (value
# ->8 - value->4, a range, a size), divided by 12 (a stride, a 12-byte element size), a block count / a stride). If the
# count is zero (an empty state), it returns zero. If the count is non-zero, it calls a process (sub_8282EFD8, on the
# object, r3 = obj, a setup / a grow, part of the 0x8282E cluster). So it's a "construct a 16-byte object (initialize
# the fields, obj->4/->8/->12 = 0, compute a count, (value->8 - value->4) / 12, and (if the count is non-zero) call a
# process (on the object, a setup / a grow))" routine (a 16-byte constructor, part of the 0x82A11 cluster, a sibling
# of Construct16Byte_82275368). 117 lines, 1 callee (sub_8282EFD8, sub_8282EFD8, a setup / a grow process). 2 callers:
# sub_82A10DA8, sub_82A10E80 (siblings in the 0x82A10 cluster). The object's class (obj, ->4 = a field, ->8 = a field,
# ->12 = a field, a 16-byte object, a particular structure) and the value's meaning (a value, a range, a particular
# argument) aren't resolvable, so the name states the mechanism: a 16-byte constructor (initialize the fields, compute
# a count, and (if the count is non-zero) call a process). The "2" distinguishes it from Construct16Byte_82275368.
[entrypoint.functions."0x82A11180"]
name = "Construct16Byte2_82A11180"

# A thunk (a string comparison, the callee of StrCmpIgnoreCase_82CA6320): dereference b->0 (and if it is zero, use a
# default, a constant from a fixed .data address, a default string), and dereference a->0, then call a string
# comparison (StrCmpIgnoreCase_82CA6320, on the dereferenced values, a case-insensitive string comparison).

# Args: (r3), a (r4, r9, ->0 = a value), b (r5, ->0 = a value), (more). It dereferences b->0 (a value, a pointer): if
# b->0 is non-zero, it reads b->0->0 (a dereference, a string, a particular string); if b->0 is zero (a null state),
# it uses a default (a constant, from a fixed .data address, a default string, a particular string). It dereferences
# a->0 (a value, a pointer): if a->0 is non-zero, it reads a->0->0 (a dereference, a string, a particular string); if
# a->0 is zero, it uses the default. It calls a string comparison (StrCmpIgnoreCase_82CA6320, sub_82CA6320, a case-
# insensitive string comparison, on the dereferenced values, comparing the two strings, case-insensitively). So it's a
# "thunk (a string comparison: dereference b->0 (and if it is zero, use a default), dereference a->0, and call a string
# comparison (on the dereferenced values, a case-insensitive string comparison))" routine (a string comparison thunk,
# part of the 0x82C1D cluster, a callee of StrCmpIgnoreCase_82CA6320). ~30 lines, 1 callee (StrCmpIgnoreCase_82CA6320
# , sub_82CA6320, a case-insensitive string comparison). 0 callers: it is registered in the function table but not
# called from any recompiled guest function (a registered-but-uncalled function, part of the 0x82C1D cluster, a string
# comparison thunk). The values' meaning (a value, a string, a particular string, a key / a value) aren't resolvable,
# so the name states the mechanism: a string comparison thunk (dereference both values (using a default if zero) and
# call a string comparison (a case-insensitive string comparison)).
[entrypoint.functions."0x82C1DC98"]
name = "StrCmpThunk_82C1DC98"

# A compute (a math function, a special-function approximation, the sibling of the math cluster): take the magnitude
# (fabs, a float absolute value, of the input), load a table of float constants (from a fixed .data address, a table of
# polynomial coefficients, a set of 12 constants), and perform a polynomial evaluation (a Horner's method, a series of
# float multiplications and additions, with float-to-int conversions, a special-function approximation, a math
# function, computing a particular value from the input).

# Args: a float (f1, a value, a particular input). It takes the magnitude (fabs f0, f1, a float absolute value, a
# magnitude, handling both positive and negative inputs, a particular convention). It loads a table of float constants
# (from a fixed .data address, a table of polynomial coefficients, 12 constants: f13, f11, f10, f9, f8, f7, f6, f5, f4,
# f3, f2, f1, a set of coefficients, a particular set of magic constants). It performs a polynomial evaluation (a
# Horner's method: it computes f12 = f13 + f0, a coefficient (a float), and f13 = table[8] * f12, a multiplication (a
# float), and f13 = fctid(f13), a conversion (a float to int, a rounding), and f11 = f13 - f11, a subtraction (a float),
# and f13 = fctidz(f13), a conversion (a float to int, a rounding toward zero), a series of float operations, a
# polynomial evaluation, a special-function approximation, computing a particular value from the input). So it's a
# "compute (a math function: take the magnitude, load a table of float constants, and perform a polynomial evaluation
# (a Horner's method, a special-function approximation, computing a particular value from the input))" routine (a math
# function, a special-function approximation, part of the 0x82239 cluster, a sibling of the math cluster). 125 lines,
# no direct callees (pure data manipulation). 10+ callers: sub_826E2A28, sub_821DAF50, sub_82A27648, sub_82F51298,
# sub_82FD34B8, sub_82AA5FC8, sub_82FB79E8, sub_82A77DC8, sub_823997D0, sub_82BD1290 (a widely-called math function,
# part of the game's math subsystem). The input's meaning (a float, a value, a particular input, a particular quantity)
# and the output's meaning (a float, a value, a particular output, a particular quantity) aren't resolvable, so the
# name states the mechanism: a math function (take the magnitude, load a table of float constants, and perform a
# polynomial evaluation (a Horner's method, a special-function approximation)).
[entrypoint.functions."0x82239E88"]
name = "ComputeSpecialFunction_82239E88"

# A compute (a math function, a special-function approximation, the sibling of ComputeSpecialFunction_82239E88): take
# the magnitude (fabs, a float absolute value, of the input), load a table of float constants (from a fixed .data
# address, a table of polynomial coefficients, a set of constants), and perform a polynomial evaluation (a Horner's
# method, a series of float multiplications and additions, with float-to-int conversions, a special-function
# approximation, a math function, computing a particular value from the input).

# Args: a float (f1, a value, a particular input). It takes the magnitude (fabs f0, f1, a float absolute value, a
# magnitude, handling both positive and negative inputs, a particular convention). It loads a table of float constants
# (from a fixed .data address, a table of polynomial coefficients, a set of constants: f30, f11, f12, f10, f9, f8, f7,
# f6, f5, f4, f3, f2, f31, a set of coefficients, a particular set of magic constants). It performs a polynomial
# evaluation (a Horner's method: it computes f13 = f11 * f0, a multiplication (a float), and f11 = fctid(f13), a
# conversion (a float to int, a rounding), and continues (a series of float operations, a polynomial evaluation, a
# special-function approximation, computing a particular value from the input). So it's a "compute (a math function:
# take the magnitude, load a table of float constants, and perform a polynomial evaluation (a Horner's method, a
# special-function approximation, computing a particular value from the input))" routine (a math function, a
# special-function approximation, part of the 0x82239 cluster, a sibling of ComputeSpecialFunction_82239E88). 124
# lines, no direct callees (pure data manipulation). 10+ callers: sub_8322AF80, sub_824D8638, sub_82FD3088, sub
# _82DBB8D0, sub_821BE068, sub_829C1A80, sub_827ECDA0, sub_8294C460, sub_8322AFE8, sub_82ED1E88 (a widely-called math
# function, part of the game's math subsystem). The input's meaning (a float, a value, a particular input, a particular
# quantity) and the output's meaning (a float, a value, a particular output, a particular quantity) aren't resolvable,
# so the name states the mechanism: a math function (take the magnitude, load a table of float constants, and perform
# a polynomial evaluation (a Horner's method, a special-function approximation)). The "2" distinguishes it from
# ComputeSpecialFunction_82239E88.
[entrypoint.functions."0x82239F68"]
name = "ComputeSpecialFunction2_82239F68"

# A chain search (the sibling of ChainSearch4_82306B80 / ChainSearch3_82B5D690 / ChainSearch2_82B5D548 / ChainSearch
# _82B3D880): walk a chain (a two-key comparison: node->12 against a value, following the node's next pointers, node->8
# / node->0, with a ->33 flag), searching for a match, and (in a path) free (Free_SizeBucketed_Thunk_8221BE68) and call
# sibling operations (ProcessChainLookup_8236F398, SetValue2_8236EC48).

# Args: obj (r3, r30, ->4 = a node), value (r4, ->0 = a value), (more). It loads a node (obj->4->4, a node in a chain).
# If the node's flag (node->33, a byte) is zero, it takes one path (reading a value from r4->0). It then walks the chain
# (a loop): it compares the node's field (node->12, a value) against the target value (r9, from r4->0), and (depending
# on the comparison) follows the node's next pointer (node->8, a next) or the node's head (node->0, a head / prev),
# walking to the next node (node->4->33, a flag). In a path it frees (Free_SizeBucketed_Thunk_8221BE68, sub_8221BE68, a
# size-bucketed free, on a value) and calls sibling operations (ProcessChainLookup_8236F398, sub_8236F398, a chain
# lookup, and SetValue2_8236EC48, sub_8236EC48, a value setter). So it's a "search a chain (walk the chain by a
# comparison, node->12 against a value, following the next pointers, and (in a path) free and call sibling operations)
# " routine (a chain search, part of the 0x8236E cluster, a sibling of ChainSearch4_82306B80). 181 lines, 3 callees
# (Free_SizeBucketed_Thunk_8221BE68, ProcessChainLookup_8236F398, SetValue2_8236EC48). 1 caller: sub_8236DD48. The
# chain's element type (a node, ->4 = a next, ->8 = a next, ->12 = a value, ->33 = a flag, a chain/list node) and the
# value's meaning (a searched value, a particular match) aren't resolvable, so the name states the mechanism: a chain
# search (walk the chain by a comparison and (in a path) free and call sibling operations). The "5" distinguishes it
# from ChainSearch4_82306B80, ChainSearch3_82B5D690, ChainSearch2_82B5D548, and ChainSearch_82B3D880.
[entrypoint.functions."0x8236E4A0"]
name = "ChainSearch5_8236E4A0"

# A process (a compare-and-dispatch, with a size computation, the sibling of DispatchAndCopy_82A0E128 / the caller of
# ValidateAndDispatch_8236FC88): compare two values (a and b, r3 and r4), and (if they differ) perform a size
# computation (a range, b->8 - b->4, divided by 4, a count, a 4-byte element count), and call a validation/dispatch
# (ValidateAndDispatch_8236FC88, on a), and (in paths) copy (CheckedCopy_82CA3808), free (Free_SizeBucketed_Thunk
# _8221BE68), and set a value (SetValue_8236FCE8).

# Args: a (r3, r29, ->4 = a field), b (r4, r30, ->4 = a pointer, ->8 = a pointer), (more). It compares a (r3, r29) and
# b (r4, r30): if they are equal (a match, a same-value check), it takes one path (a no-op / a return, a particular
# state). If they differ, it performs a size computation (a range (b->8 - b->4, a range, a size), divided by 4 (a 4-byte
# element count, srawi r10, r11, 2, a right shift by 2, a division by 4), a count, a particular size / a count). If the
# count is non-zero, it calls a validation/dispatch (ValidateAndDispatch_8236FC88, sub_8236FC88, a validation and
# dispatch, on a (r3, r29), a value, a particular state), and (in paths) copies (CheckedCopy_82CA3808, sub_82CA3808, a
# checked copy), frees (Free_SizeBucketed_Thunk_8221BE68, sub_8221BE68, a size-bucketed free), and sets a value
# (SetValue_8236FCE8, sub_8236FCE8, a value setter). So it's a "process (a compare-and-dispatch: compare two values (a
# and b), and (if they differ) perform a size computation (a range divided by 4, a count), and call a validation
# /dispatch (on a), and (in paths) copy, free, and set a value)" routine (a compare-and-dispatch process, part of the
# 0x8236E cluster, a sibling of DispatchAndCopy_82A0E128, a caller of ValidateAndDispatch_8236FC88). 283 lines, 5
# callees (CheckedCopy_82CA3808, Free_SizeBucketed_Thunk_8221BE68, SetValue_8236FCE8, ValidateAndDispatch_8236FC88, sub
# _822F8488). 3 callers: sub_82C673D8, sub_8236DD48, sub_82C66F20 (siblings in the 0x82C67/0x8236D/0x82C66 clusters).
# The values' meaning (a and b, a and b, particular values, a state / a particular pair) and the size's meaning (a
# size, a count, a particular size) aren't resolvable, so the name states the mechanism: a compare-and-dispatch
# process (compare two values (a and b) and (if they differ) perform a size computation (a range divided by 4, a count)
# and call a validation/dispatch (on a), and (in paths) copy, free, and set a value).
[entrypoint.functions."0x8236ED00"]
name = "ProcessCompareDispatch_8236ED00"

# A process (a dispatch, with a range check, the callee of ComputeHash_8227BA30 / the sibling of DispatchAndCheck
# _82BCD5D0): (if a value, value + 1, is <= -19, a range check, a below-threshold check) call a process (sub_82BC9860
# , on a constant, a particular value), and call a dispatch (DispatchAndAdjustPos_8227BB58, on an object, with two
# zeros and a value (value + 17, a particular offset)), and (in a path) copy (MemCpy_SdkRuntime_82CA2C60, a memory
# copy).

# Args: obj (r3, r29, an object), a (r4, r26, a value), value (r5, r30, a value), b (r6, r28, a value), (more). It
# checks a range (value + 1, a value, against -19, a threshold, a below-threshold check): if it is <= -19 (below the
# threshold), it calls a process (sub_82BC9860, on a constant, r4, a particular value, a setup / a particular
# operation). It calls a dispatch (DispatchAndAdjustPos_8227BB58, sub_8227BB58, a dispatch and adjust-position, on the
# object (r3, r29), with two zeros (r5 = 0, r4 = 0, two particular arguments), and a value (r6 = value + 17, a
# particular offset, a particular position), a dispatch, a state transition, storing the result to r31, a value). In a
# path it copies (MemCpy_SdkRuntime_82CA2C60, sub_82CA2C60, a memory copy, on a value, a particular copy). So it's a
# "process (a dispatch: (if a value is <= -19, a range check) call a process (on a constant), call a dispatch (on an
# object, with two zeros and a value, a particular offset), and (in a path) copy)" routine (a dispatch process, part
# of the 0x82BCD cluster, a callee of ComputeHash_8227BA30, a sibling of DispatchAndCheck_82BCD5D0). 139 lines, 4
# callees (DispatchAndAdjustPos_8227BB58, MemCpy_SdkRuntime_82CA2C60, sub_82BC9860, sub_82BCD698). 1 caller:
# ComputeHash_8227BA30 (sub_8227BA30, a hash computation, which calls this to dispatch a state / a position). The
# object's class (obj, an object, a particular structure, a state / a value object) and the value's meaning (a value, a
# particular argument, a position) aren't resolvable, so the name states the mechanism: a dispatch process ((if a
# value is <= -19, a range check) call a process (on a constant), call a dispatch (on an object, with two zeros and a
# value, a particular offset), and (in a path) copy). The "2" distinguishes it from the other dispatch variants.
[entrypoint.functions."0x82BCD7B0"]
name = "ProcessDispatch2_82BCD7B0"

# A getter (a pointer/index getter, with a bounds check and special values, the sibling of GetPointer_82C23858 /
# GetField13_82375C28): (if a value is positive) compute a pointer (obj->12 + value * 8, an 8-byte element stride, with
# a bounds check, obj->8, a limit, and a particular offset (minus 8)), and (if the value is in a special range,
# -10000 / -10001 / -10002, a set of special values) compute a pointer (obj->8 + value * 8, a negative offset, a
# particular pointer), returning the pointer.

# Args: obj (r3, r11, ->8 = a limit, ->12 = a base, a pointer), value (r4, a value, a particular index / a particular
# value), (more). It checks the value (r4, a value, a particular index / a particular value): if it is positive (a
# positive index, a forward offset), it computes a pointer (obj->12 + value * 8, a base (r9, obj->12) plus a stride (r1
# 0, value * 8, an 8-byte element stride, rlwinm r10, r4, 3, 0, 28, a left shift by 3, a multiplication by 8, a 8-byte
# element stride), a pointer, a particular element), and performs a bounds check (obj->8, a limit, a particular bound
# , and a particular offset (r3 = r11 - 8, a pointer, minus 8, a particular adjustment), if the pointer is below the
# limit, it returns a constant (from a fixed .data address, a particular default, a null / a particular value)). If the
# value is in a special range (-10000 / -10001 / -10002, a set of special values, a particular set of negative offsets)
# , it computes a pointer (obj->8 + value * 8, a base (r11, obj->8) plus a stride (r10, value * 8, a negative offset, a
# particular pointer), a pointer, a particular element). So it's a "get a pointer/index (a pointer/index getter: (if a
# value is positive) compute a pointer (obj->12 + value * 8, an 8-byte element stride, with a bounds check), and (if
# the value is in a special range, a set of special values) compute a pointer (obj->8 + value * 8, a negative offset))"
# routine (a pointer/index getter, part of the 0x82BC5 cluster, a sibling of GetPointer_82C23858). 111 lines, no direct
# callees (pure data manipulation). 10+ callers: sub_822A5E98, sub_82BC68F0, sub_82BD2768, sub_82BC59D0, sub_821E1548,
# sub_821931E8, sub_82BC6718, sub_82BC5C88, sub_82BC5FF0, sub_82BC5B30 (a widely-called pointer/index getter, part of
# the game's data subsystem). The object's class (obj, ->8 = a limit, ->12 = a base, a pointer, a particular structure
# , a table / a region) and the value's meaning (a value, a particular index / a particular value) aren't resolvable, so
# the name states the mechanism: a pointer/index getter ((if a value is positive) compute a pointer (obj->12 + value * 8
# , an 8-byte element stride, with a bounds check), and (if the value is in a special range) compute a pointer (obj->8
# + value * 8, a negative offset)).
[entrypoint.functions."0x82BC5448"]
name = "GetIndexedPointer_82BC5448"

# A process (a conversion, with a range check and a byte manipulation, the callee of sub_82BCA180 / the sibling of
# ParseFloatValue_82B47F98): (if a flag, obj->4, is 3, a type / a flag, a particular type) perform a float-to-int
# conversion (with a range check, 0 < the value <= 1024, a 0-1023 range, a particular range), and perform a byte
# manipulation (a loop, with byte operations, a particular conversion, an 8-byte stride, a byte packing / a byte
# extraction).

# Args: obj (r3, ->0 = a float, ->4 = a type / a flag, a particular value), (more). It checks the flag (obj->4, a type
# / a flag, a particular type): if it is 3 (a particular type, a float type / a particular value type), it performs a
# float-to-int conversion (it reads the float (obj->0, a float, a particular value), converts it to an int (fctiwz
# f13, f0, a float-to-int conversion, a round toward zero, a particular conversion), and performs a range check (0 <
# the value <= 1024, a 0-1023 range, a particular range, a below/above-threshold check)). It then performs a byte
# manipulation (a loop, with byte operations, a particular conversion, an 8-byte stride, rlwinm r11, r11, 24, 8, 31, a
# byte extraction, a byte packing, a particular conversion, converting the int to a particular byte representation). If
# the flag is not 3, it takes one path (a no-op / a return, a particular state). So it's a "process (a conversion:
# (if a flag is 3, a particular type) perform a float-to-int conversion (with a range check, 0 < the value <= 1024, a
# 0-1023 range), and perform a byte manipulation (a loop, with byte operations, a particular conversion, an 8-byte
# stride, a byte packing / a byte extraction))" routine (a conversion process, part of the 0x82BC9 cluster, a callee
# of sub_82BCA180, a sibling of ParseFloatValue_82B47F98). 90 lines, no direct callees (pure data manipulation). 1
# caller: sub_82BCA180. The object's class (obj, ->0 = a float, ->4 = a type / a flag, a particular value, a float /
# int pair, a particular structure) and the flag's meaning (a flag, a type, a particular type, a float type) aren't
# resolvable, so the name states the mechanism: a conversion process ((if a flag is 3, a particular type) perform a
# float-to-int conversion (with a range check, 0 < the value <= 1024, a 0-1023 range), and perform a byte manipulation
# (a loop, with byte operations, a particular conversion, an 8-byte stride, a byte packing / a byte extraction)).
[entrypoint.functions."0x82BC9D70"]
name = "ProcessConversion_82BC9D70"

# A process (a range operation, with bounds checks and a complex computation, a grow, the sibling of GrowAndCopy_82
# 370550 / the caller of CompareU16_822F85C8): (if a value, c, is non-zero) perform a range check (b against obj->20,
# a limit, a below/above-threshold check), and (if b is in range) return b, a value; (otherwise) perform a complex
# computation (a grow, a range check, a 16-byte element, obj->24, a size / a count, and a comparison, CompareU16_822F85
# C8, a 16-bit comparison).

# Args: obj (r3, r11, ->4 = a base, ->20 = a limit, ->24 = a size / a count, a 16-byte element), a (r4, r25, a value)
# , b (r5, a value), c (r6, r28, a value), (more). It checks a value (c, r28, a value, a particular argument): if it
# is non-zero (a non-empty state), it performs a range check (b, r5, a value, against obj->20, a limit, a particular
# bound, a below/above-threshold check). If b is in range (b <= obj->20, a below-threshold check), it returns b (r3 =
# b, a value, a particular result). If b is above the limit (b > obj->20, an above-threshold check), it performs a
# complex computation (a grow, a range check, a 16-byte element, obj->24, a size / a count, a particular element, and
# a comparison, CompareU16_822F85C8, sub_822F85C8, a 16-bit comparison, on a value, a particular comparison). If c is
# zero (an empty state), it takes one path (a no-op / a return, a particular state). So it's a "process (a range
# operation: (if a value is non-zero) perform a range check (b against obj->20, a limit), and (if b is in range)
# return b, a value; (otherwise) perform a complex computation (a grow, a range check, a 16-byte element, obj->24, a
# size / a count, and a comparison))" routine (a range/grow process, part of the 0x8217A allocation cluster, a
# sibling of GrowAndCopy_82370550, a caller of CompareU16_822F85C8). 169 lines, 1 callee (CompareU16_822F85C8, sub
# _822F85C8, a 16-bit comparison). 2 callers: sub_822F2238, sub_8217AA20 (siblings in the 0x822F2/0x8217A clusters).
# The object's class (obj, ->4 = a base, ->20 = a limit, ->24 = a size / a count, a 16-byte element, a particular
# structure, a range / a grow object) and the values' meaning (a, b, c, particular values, a particular range) aren't
# resolvable, so the name states the mechanism: a range/grow process ((if a value is non-zero) perform a range check
# (b against obj->20, a limit), and (if b is in range) return b, a value; (otherwise) perform a complex computation
# (a grow, a range check, a 16-byte element, obj->24, a size / a count, and a comparison)).
[entrypoint.functions."0x8217A858"]
name = "ProcessRangeGrow_8217A858"

# A getter (a pointer offset getter, the sibling of GetOffset8_82BABC60 / GetPointer_82C23858): return a pointer (a +
# 36, a 36-byte offset, a particular field / a particular element, a pointer offset, a particular offset into an object
# ).

# Args: a (r3, a pointer, a particular object / a particular structure). It returns a pointer (r3 + 36, a 36-byte
# offset, a particular field / a particular element, a pointer offset, a particular offset into the object, a specific
# field / a specific element). So it's a "get a pointer offset (a pointer offset getter: return a pointer (a + 36, a
# 36-byte offset, a particular field / a particular element))" routine (a pointer offset getter, part of the 0x82295
# cluster, a sibling of GetOffset8_82BABC60). ~5 lines, no direct callees (a trivial pointer offset getter). 3 callers:
# sub_82C6ABE8, sub_82C6B640, sub_82C6AAB8 (siblings in the 0x82C6A/0x82C6B clusters). The object's class (a pointer,
# a particular object / a particular structure, a particular layout, a 36-byte offset to a particular field) and the
# offset's meaning (a 36-byte offset, a particular field / a particular element) aren't resolvable, so the name states
# the mechanism: a pointer offset getter (return a pointer (a + 36, a 36-byte offset, a particular field / a
# particular element)). The "36" refers to the 36-byte offset (a particular field / a particular element, a specific
# offset into the object).
[entrypoint.functions."0x822957E8"]
name = "GetOffset36_822957E8"

# A process (a range/element access, with a special value and a bounds check, the caller of sub_8217A9A8 / the sibling
# of GetIndexedPointer_82BC5448): (if a value, obj->0, is -2, a special value, a particular state) take one path, and
# (if it is 0, a null state) raise a trap (trap 22, a particular error, a null / a particular invalid state), and
# (otherwise) perform a range/element access (a 2-byte element stride, index * 2, with a bounds check, obj->4 and a
# base, a particular range, a 2-byte element, a particular element).

# Args: obj (r3, ->0 = a value, ->4 = a base, a pointer), index (r4, an index, a particular element), (more). It checks
# a value (obj->0, a value, a particular state): if it is -2 (a special value, a particular state, a below-threshold / a
# particular flag), it takes one path (a particular state, a particular result). If it is 0 (a null state, a particular
# invalid state), it raises a trap (ppc_trap 22, a particular error, a null / a particular invalid state, a particular
# assertion). Otherwise (a valid state), it performs a range/element access (a 2-byte element stride, rlwinm r9, r4, 1,
# 0, 30, a left shift by 1, a multiplication by 2, a 2-byte element stride, an index (r4, an index, a particular
# element), with a bounds check (obj->4, a base, a particular bound, and a base (r7, obj->4 or obj + 4, a base, a
# particular offset), a particular range, a 2-byte element, a particular element). So it's a "process (a range/element
# access: (if a value is -2, a special value) take one path, and (if it is 0, a null state) raise a trap (trap 22, a
# particular error), and (otherwise) perform a range/element access (a 2-byte element stride, index * 2, with a bounds
# check, obj->4 and a base, a particular range))" routine (a range/element access process, part of the 0x8217A
# allocation cluster, a caller of sub_8217A9A8, a sibling of GetIndexedPointer_82BC5448). 79 lines, no direct callees
# (pure data manipulation). 1 caller: sub_8217A9A8 (a sibling in the 0x8217A cluster). The object's class (obj, ->0 = a
# value, ->4 = a base, a pointer, a particular structure, a range / a table object) and the index's meaning (an index,
# a particular element, a particular offset) aren't resolvable, so the name states the mechanism: a range/element
# access process ((if a value is -2, a special value) take one path, and (if it is 0, a null state) raise a trap (trap
# 22), and (otherwise) perform a range/element access (a 2-byte element stride, index * 2, with a bounds check)).
[entrypoint.functions."0x8217A400"]
name = "ProcessElementAccess_8217A400"

# A process (a validation, with a bounds check and a call to ValidateIndex_822F6EE8, the sibling of ProcessElementAcce
# ss_8217A400): compute a pointer (a base (value->4, or value + 4, a base, a particular offset), plus a 2-byte element
# stride (value->20 * 2, a size, a 2-byte element, a particular range)), and call a validation (ValidateIndex_822F6EE8
# , on the pointer and an object, a particular index / a particular validation).

# Args: obj (r3, r31, an object), value (r4, r5, ->4 = a base, ->20 = a size, ->24 = a size, a particular structure),
# (more). It checks a size (value->24, a size, a particular bound): if it is >= 8 (a particular threshold, a particular
# size), it reads a base (value->4, a base, a particular offset); otherwise it uses a base (value + 4, a base, a
# particular offset, a relative offset). It computes a pointer (a base (r11, a base, a particular offset) plus a 2-byte
# element stride (r10, value->20 * 2, a size (r10, value->20, a size, a particular range), a 2-byte element, rlwinm r1
# 0, r10, 1, 0, 30, a left shift by 1, a multiplication by 2, a 2-byte element stride, a particular range), a pointer,
# a particular element). It calls a validation (ValidateIndex_822F6EE8, sub_822F6EE8, an index validation, on the
# pointer (r4, a pointer, a particular element) and the object (r3, r31, an object, a particular structure), a
# particular index / a particular validation). So it's a "process (a validation: compute a pointer (a base, plus a 2-
# byte element stride, value->20 * 2, a size, a particular range), and call a validation (ValidateIndex_822F6EE8, on
# the pointer and an object, a particular index / a particular validation))" routine (a validation process, part of the
# 0x8217A allocation cluster, a sibling of ProcessElementAccess_8217A400). 56 lines, 1 callee (ValidateIndex_822F6EE8,
# sub_822F6EE8, an index validation). 2 callers: sub_822F2238, sub_8217AA20 (siblings in the 0x822F2/0x8217A
# clusters). The object's class (obj, an object, a particular structure, a range / a table object) and the value's
# meaning (a value, a base, a size, a particular structure, a particular range) aren't resolvable, so the name states
# the mechanism: a validation process (compute a pointer (a base, plus a 2-byte element stride, value->20 * 2, a size,
# a particular range), and call a validation (ValidateIndex_822F6EE8, on the pointer and an object)).
[entrypoint.functions."0x8217A7F8"]
name = "ValidateElementAccess_8217A7F8"

# A process (a vcall loop, with a 16-bit element, the caller of ProcessElementAccess_8217A400 / the sibling of
# ProcessVcallSequence5_82C26940): compute a count (a range, b - a, divided by 2, a 16-bit element count, a particular
# count), call a validation (ProcessElementAccess_8217A400, on the object, the base, and the count, a particular
# validation), and loop (a vcall loop, calling a virtual method (d, r7, a vcall, a particular virtual method), on each
# 16-bit element (a, a 16-bit value, lhz, a 16-bit load), and storing the result (a 16-bit value, sthx, a 16-bit
# store)).

# Args: obj (r3, r27, an object), a (r4, r31, a base, a 16-bit element array), b (r5, r30, a limit, a particular end),
# c (r6, a value), d (r7, r29, a vcall, a particular virtual method), (more). It computes a count (a range (b - a, a
# range, a size), divided by 2 (srawi r4, r11, 1, a right shift by 1, a division by 2, a 16-bit element count, a
# particular count), a count, a particular number of 16-bit elements). It calls a validation (ProcessElementAccess
# _8217A400, sub_8217A400, a range/element access, on the object (r3, r27, an object), the base (r31, a base, a
# particular offset), and the count (r4, a count, a particular count), a particular validation). It loops (a vcall
# loop: it reads a 16-bit element (lhz r3, a, a 16-bit value, a particular element), calls a virtual method (bctrl d, r
# 7, a vcall, a particular virtual method, on the 16-bit value, a particular operation), and stores the result (st hx
# r3, r28, a, a 16-bit store, a particular element, a particular result), advancing to the next 16-bit element, a
# particular loop). So it's a "process (a vcall loop: compute a count (a range, b - a, divided by 2, a 16-bit element
# count), call a validation (ProcessElementAccess_8217A400, on the object, the base, and the count), and loop (a vcall
# loop, calling a virtual method (d, a vcall), on each 16-bit element (a, a 16-bit value), and storing the result))"
# routine (a vcall loop process, part of the 0x8217A allocation cluster, a caller of ProcessElementAccess_8217A400, a
# sibling of ProcessVcallSequence5_82C26940). 72 lines, 1 callee (ProcessElementAccess_8217A400, sub_8217A400, a
# range/element access). 2 callers: sub_822F2238, sub_8217AA20 (siblings in the 0x822F2/0x8217A clusters). The
# object's class (obj, an object, a particular structure, a range / a table object) and the 16-bit element's meaning (a
# 16-bit value, a particular element, a particular quantity) aren't resolvable, so the name states the mechanism: a
# vcall loop process (compute a count (a range, b - a, divided by 2, a 16-bit element count), call a validation
# (ProcessElementAccess_8217A400), and loop (a vcall loop, calling a virtual method (d, a vcall), on each 16-bit
# element, and storing the result)).
[entrypoint.functions."0x8217A9A8"]
name = "ProcessVcallLoop_8217A9A8"

# A validation (a bounds check, with a 4-byte element stride and traps, the sibling of ValidateIndex_822F6EE8 / the
# caller of sub_82BEC540): (if obj->0 is 0, a null state) raise a trap (trap 22, a particular error, a null / a
# particular invalid state), and (otherwise) perform a bounds check (a 4-byte element stride, index * 4, with a base
# (obj->0->4, a base, a particular offset) and a limit (obj->0->8, a limit, a particular bound), and (if the pointer is
# below the base or above the limit, or above the limit, a below/above-threshold check) raise a trap (trap 22, a
# particular error, an out-of-range / a particular invalid state)).

# Args: obj (r3, ->0 = a value, a 64-bit, a pointer), index (r4, an index, a particular element), (more). It checks a
# value (obj->0, a value, a 64-bit, a pointer, a particular state): if it is 0 (a null state, a particular invalid
# state), it raises a trap (ppc_trap 22, a particular error, a null / a particular invalid state, a particular
# assertion). Otherwise (a valid state), it performs a bounds check (a 4-byte element stride, rlwinm r10, r4, 2, 0, 29
# , a left shift by 2, a multiplication by 4, a 4-byte element stride, an index (r4, an index, a particular element),
# with a base (r9, obj->0->4, a base, a particular offset) and a limit (r11, obj->0->8, a limit, a particular bound), a
# particular range, a 4-byte element, a particular element). It computes a pointer (r3 = r10 + r9, a pointer, a
# particular element) and checks it (if it is above the limit (r3 > r11, an above-threshold check), or below the base
# (r3 < obj->0->4, a below-threshold check), or above the limit (r3 >= r11, an above-threshold check), it raises a trap
# (ppc_trap 22, a particular error, an out-of-range / a particular invalid state, a particular assertion)). So it's a
# "validate (a bounds check: (if obj->0 is 0, a null state) raise a trap (trap 22), and (otherwise) perform a bounds
# check (a 4-byte element stride, index * 4, with a base (obj->0->4) and a limit (obj->0->8), and (if the pointer is
# below the base or above the limit) raise a trap (trap 22)))" routine (a bounds check validation, part of the 0x82BEC
# cluster, a sibling of ValidateIndex_822F6EE8). ~45 lines, no direct callees (pure data manipulation, with traps). 10
# + callers: sub_82BEC540, sub_82BF1A40, sub_82BEF360, sub_82BEC8B0, sub_82BF07C0, sub_82BEDC10, sub_82BF06F8, sub
# _82BECFA0, sub_82BED548, sub_82BF1268 (a widely-called bounds check validation, part of the game's data validation
# subsystem). The object's class (obj, ->0 = a value, a 64-bit, a pointer, a particular structure, a range / a table
# object) and the index's meaning (an index, a particular element, a particular offset) aren't resolvable, so the name
# states the mechanism: a bounds check validation ((if obj->0 is 0, a null state) raise a trap (trap 22), and
# (otherwise) perform a bounds check (a 4-byte element stride, index * 4, with a base (obj->0->4) and a limit (obj
# ->0->8), and (if the pointer is below the base or above the limit) raise a trap (trap 22))).
[entrypoint.functions."0x82BEC300"]
name = "ValidateBounds4_82BEC300"

# A process (a dispatch, with a type check, the caller of DispatchValue_821CF230 / HashTable_Find3_821CF1D0): (if a
# flag, value->4, is 5, a type / a flag, a particular type) call a dispatch (DispatchValue_821CF230, on a value and
# value->0, a particular dispatch), and (in paths) call a hash lookup (HashTable_Find3_821CF1D0, on a value, a
# particular lookup) and sibling operations (sub_82BC9640, sub_82BC9860, sub_82BCBF50, sub_82BCD5E0 -- siblings in the
# 0x82BC9/0x82BCB/0x82BCD clusters).

# Args: obj (r3, r28, an object), value (r4, r29, ->0 = a value, ->4 = a type / a flag, a particular value), a (r5, r2
# 6, a value), b (r6, r23, a value), (more). It checks a flag (value->4, a type / a flag, a particular type): if it is
# 5 (a particular type, a particular value type), it calls a dispatch (DispatchValue_821CF230, sub_821CF230, a value
# dispatch, on a value (r4, a, a particular argument) and the value (r3, value->0, a particular value), a particular
# dispatch, storing the result to r30, a value). If the result's flag (r30->4, a flag, a particular state) is zero (a
# particular state), it takes one path (a process, with value->0->8, a particular value). In paths it calls a hash
# lookup (HashTable_Find3_821CF1D0, sub_821CF1D0, a hash table lookup, on a value, a particular lookup) and sibling
# operations (sub_82BC9640, sub_82BC9860, sub_82BCBF50, sub_82BCD5E0 -- siblings in the 0x82BC9/0x82BCB/0x82BCD
# clusters). So it's a "process (a dispatch: (if a flag is 5, a particular type) call a dispatch (DispatchValue
# _821CF230, on a value and value->0), and (in paths) call a hash lookup (HashTable_Find3_821CF1D0, on a value) and
# sibling operations)" routine (a dispatch process, part of the 0x821CF cluster, a caller of DispatchValue_821CF230 and
# HashTable_Find3_821CF1D0). 202 lines, 6 callees (DispatchValue_821CF230, HashTable_Find3_821CF1D0, sub_82BC9640, sub
# _82BC9860, sub_82BCBF50, sub_82BCD5E0). 4 callers: sub_82BDA2C8, sub_822DF280, sub_82BD3FD0, sub_821E1408 (siblings
# in the 0x82BDA/0x822DF/0x82BD3/0x821E1 clusters). The object's class (obj, an object, a particular structure, a
# dispatch / a value object) and the value's meaning (a value, a particular argument, a particular value) aren't
# resolvable, so the name states the mechanism: a dispatch process ((if a flag is 5, a particular type) call a dispatch
# (DispatchValue_821CF230, on a value and value->0), and (in paths) call a hash lookup (HashTable_Find3_821CF1D0, on a
# value) and sibling operations). The "Process" suffix distinguishes it from ProcessDispatchValue_82BCD308 (a different
# dispatch, in the 0x82BCD cluster).
[entrypoint.functions."0x821CF320"]
name = "DispatchValueProcess_821CF320"

# A getter (a size-bucket search, with a loop, the sibling of HeapAllocate_83231590 / the callee of PoolAllocate_8240D
# AA8 and Free_SizeBucketed_83231BE8): (if a size is 0, a null state) take one path, and (otherwise) search for a size
# bucket (a loop, over a bucket list (obj->24, a bucket list, a set of size buckets), comparing the size against the
# buckets, finding the first bucket that is large enough, a particular size bucket, a particular pool / a particular
# allocation bucket).

# Args: obj (r3, ->4 = a base, ->24 = a bucket list, a set of size buckets), size (r4, a size, a particular size / a
# particular allocation), (more). It checks the size (r4, a size, a particular size / a particular allocation): if it
# is 0 (a null state, a particular invalid state), it takes one path (a no-op / a return, a particular state). Otherwise
# (a valid size), it searches for a size bucket (a loop, over a bucket list (obj->24, a bucket list, a set of size
# buckets, a particular set of size buckets): it reads a bucket (r5->0, a size, a particular size bucket, a particular
# bucket), compares the size (r4, a size, a particular size) against the bucket (r5->0, a size, a particular size
# bucket, a particular bucket), and (if the size is large enough, a size >= the bucket, a particular size / a
# particular allocation) finds the bucket (a particular size bucket, a particular pool / a particular allocation bucket
# , returning the bucket, a particular result). If the size is not large enough (a size < the bucket, a particular size
# / a particular allocation), it advances to the next bucket (r5 = r5->8, a next, a particular bucket), and continues
# the loop (a particular search, a particular size bucket search). If the bucket list is exhausted (r5 = 0, a null
# state, a particular end), it returns 0 (a not-found, a particular state, a particular result). So it's a "get a size
# bucket (a size-bucket search: (if a size is 0, a null state) take one path, and (otherwise) search for a size bucket
# (a loop, over a bucket list (obj->24, a bucket list, a set of size buckets), comparing the size against the buckets,
# finding the first bucket that is large enough, a particular size bucket))" routine (a size-bucket search, part of the
# 0x83231 heap cluster, a sibling of HeapAllocate_83231590, a callee of PoolAllocate_8240DAA8 and Free_SizeBucketed
# _83231BE8). ~50 lines, no direct callees (pure data manipulation). 3 callers: PoolAllocate_8240DAA8 (sub_8240DAA8, a
# pool allocation), Free_SizeBucketed_83231BE8 (sub_83231BE8, a size-bucketed free), sub_828456C8 (a sibling in the
# 0x82845 cluster). The object's class (obj, ->4 = a base, ->24 = a bucket list, a set of size buckets, a particular
# structure, a heap / a pool object) and the size's meaning (a size, a particular size / a particular allocation)
# aren't resolvable, so the name states the mechanism: a size-bucket search ((if a size is 0, a null state) take one
# path, and (otherwise) search for a size bucket (a loop, over a bucket list (obj->24, a bucket list, a set of size
# buckets), comparing the size against the buckets, finding the first bucket that is large enough)).
[entrypoint.functions."0x83231518"]
name = "FindSizeBucket_83231518"

# A process (a vector hash/computation, with vector operations and constants, the sibling of ProcessVector2_8223BD98
# / the caller of sub_8223BB38): load vector constants (from a fixed .data address, a set of vector constants, a
# particular set of magic constants, a vector of constants), and perform a vector hash/computation (vector permutes,
# a vector XOR, a SIMD computation, a particular hash / a particular comparison, a particular vector operation).

# Args: obj (r3, an object), a (r4, a value), b (r5, a value), c (v2, a vector, a particular value), (more). It loads
# vector constants (from a fixed .data address, a set of vector constants, a particular set of magic constants, a
# vector of constants: lvx128 v0, r11, a vector constant, and vpermwi128 v6, v0, 228, a vector permute, a particular
# permutation, and vpermwi128 v5, v0, 177, a vector permute, a particular permutation, and vpermwi128 v4, v0, 78, a
# vector permute, a particular permutation). It loads another vector constant (lvx128 v0, r8, a vector constant, a
# particular value). It performs a vector hash/computation (a vector XOR, vxor v2, v6, v0, a vector XOR, a particular
# operation, a particular hash / a particular comparison, and vxor v31, v5, v0, a vector XOR, a particular operation, a
# particular hash / a particular comparison, and vxor v30, v4, v0, a vector XOR, a particular operation, a particular
# hash / a particular comparison, a SIMD computation, a particular vector operation). So it's a "process (a vector
# hash/computation: load vector constants (from a fixed .data address, a set of vector constants, a particular set of
# magic constants, a vector of constants), and perform a vector hash/computation (vector permutes, a vector XOR, a
# SIMD computation, a particular hash / a particular comparison, a particular vector operation))" routine (a vector
# hash/computation, part of the 0x8222E cluster, a sibling of ProcessVector2_8223BD98). 150 lines, no direct callees
# (pure data manipulation, with vector operations). 10+ callers: sub_8223BB38, sub_829BEB18, sub_82A0B288, sub
# _826EBE68, sub_826E8078, sub_821D3000, sub_82A9C2F8, sub_829BEFA8, sub_8223D148, sub_822AA498 (a widely-called
# vector hash/computation, part of the game's vector/SIMD subsystem). The object's class (obj, an object, a particular
# structure, a hash / a value object) and the vector's meaning (a vector, a particular value, a particular quantity)
# aren't resolvable, so the name states the mechanism: a vector hash/computation (load vector constants (from a fixed
# .data address, a set of vector constants, a particular set of magic constants, a vector of constants), and perform a
# vector hash/computation (vector permutes, a vector XOR, a SIMD computation, a particular hash / a particular
# comparison)).
[entrypoint.functions."0x8222E6C0"]
name = "ComputeVectorHash_8222E6C0"

# A process (a size computation, with a special case, the caller of sub_822F1F00 / the sibling of ComputeClamped_83230
# A48): (if a size is 0, a null state) take one path, and (otherwise) perform a special case check (a divide, -1 / size
# , a particular value, a below/above-threshold check, >= 24, a particular threshold), and (in a path) call a process
# (sub_822F1F00, on a constant and a stack value, a particular operation), and compute a size (size * 24, a 24x size,
# a particular size, a block size / a particular allocation size).

# Args: obj (r3, an object), size (r4, r31, a size, a particular size / a particular count), (more). It checks the size
# (r4, r31, a size, a particular size / a particular count): if it is 0 (a null state, a particular invalid state), it
# takes one path (a no-op / a return, a particular state). Otherwise (a valid size), it performs a special case check
# (a divide, -1 / size (divwu r10, r11, r31, a divide, -1 (r11, a constant, a particular value) / the size (r31, a
# size, a particular size), a particular value, a particular quotient), and (if the quotient is >= 24 (a particular
# threshold, a particular size, a particular count), it takes one path (a particular state, a particular result)). In a
# path it calls a process (sub_822F1F00, on a constant (r9, a constant, a particular value, from a fixed .data address,
# a particular string / a particular key) and a stack value (r3, a stack buffer, a particular value), a particular
# operation, a string / a key operation). It computes a size (size * 24, a 24x size, a particular size, a block size /
# a particular allocation size: rlwinm r11, r31, 1, 0, 30, a left shift by 1, a multiplication by 2, a particular
# stride, and add r11, r31, r11, a size * 3, a 3x size, and rlwinm r3, r11, 3, 0, 28, a left shift by 3, a multiplication
# by 8, a 8x size, a 24x size, a particular size, a block size / a particular allocation size). So it's a "process (a
# size computation: (if a size is 0, a null state) take one path, and (otherwise) perform a special case check (a
# divide, -1 / size, a particular value, >= 24, a particular threshold), and (in a path) call a process (sub_822F1F00,
# on a constant and a stack value), and compute a size (size * 24, a 24x size, a particular size, a block size / a
# particular allocation size))" routine (a size computation process, part of the 0x825BB cluster, a sibling of
# ComputeClamped_83230A48). 70 lines, 2 callees (Allocate_OrDbgBreak_8221F388, sub_8221F388, a size-bucketed allocation
# ; sub_822F1F00, a string / a key operation). 10+ callers: sub_82B134D8, sub_821BA6D0, sub_82A04C38, sub_82668848,
# sub_82344EC0, sub_825F7BE8, sub_82917988, sub_823FE080, sub_82A12EC0, sub_82A7FBC0 (a widely-called size computation
# process, part of the game's size / allocation subsystem). The object's class (obj, an object, a particular structure,
# a size / a value object) and the size's meaning (a size, a particular size / a particular count) aren't resolvable, so
# the name states the mechanism: a size computation process ((if a size is 0, a null state) take one path, and
# (otherwise) perform a special case check (a divide, -1 / size, a particular value, >= 24, a particular threshold),
# and (in a path) call a process (sub_822F1F00, on a constant and a stack value), and compute a size (size * 24, a 24x
# size, a particular size, a block size / a particular allocation size)).
[entrypoint.functions."0x825BBAD0"]
name = "ProcessSizeCompute_825BBAD0"

# A process (a vector hash, with a vector operation and a counter increment, the caller of ComputeVectorHash_8222E6C0
# ): load vector constants (a vector constant, from the stack, and a vector constant, from obj->80, a particular value
# ), call a vector hash (ComputeVectorHash_8222E6C0, on the vectors, a SIMD computation, a particular hash / a
# particular comparison), and increment a counter (obj->172, a counter, a particular count / a particular statistic).

# Args: obj (r3, r31, ->80 = a vector, a particular value, ->172 = a counter, a particular count / a particular
# statistic), a (r4, a value), b (r5, a value), (more). It loads a float constant (from a fixed .data address, a magic
# constant, a particular value). It loads a vector constant (from the stack, a vector constant, a particular value: lvl
# x v0, r10, a vector constant, from the stack, and vspltw v2, v0, 0, a vector splat, a particular value). It loads a
# vector constant (from obj->80, a vector constant, a particular value: lvx128 v1, r31 + 80, a vector constant, from obj
# , a particular value). It calls a vector hash (ComputeVectorHash_8222E6C0, sub_8222E6C0, a vector hash/computation,
# on the vectors (v2, a vector constant, from the stack, and v1, a vector constant, from obj), a SIMD computation, a
# particular hash / a particular comparison, a particular vector operation). It increments a counter (obj->172, a
# counter, a particular count / a particular statistic: it reads the counter (r11, obj->172, a counter, a particular
# count), and stores it (r11 + 1, an increment, a particular count, a particular statistic)). So it's a "process (a
# vector hash: load vector constants (a vector constant, from the stack, and a vector constant, from obj->80, a
# particular value), call a vector hash (ComputeVectorHash_8222E6C0, on the vectors, a SIMD computation, a particular
# hash / a particular comparison), and increment a counter (obj->172, a counter, a particular count / a particular
# statistic))" routine (a vector hash process, part of the 0x82A0B cluster, a caller of ComputeVectorHash_8222E6C0). 74
# lines, 1 callee (sub_8222E6C0, ComputeVectorHash_8222E6C0, a vector hash/computation). 4 callers: sub_8321ED18, sub
# _8321F668, sub_8321F790, sub_8321FC38 (siblings in the 0x8321E/0x8321F clusters). The object's class (obj, ->80 = a
# vector, a particular value, ->172 = a counter, a particular count / a particular statistic, a hash / a value object)
# and the value's meaning (a value, a particular argument) aren't resolvable, so the name states the mechanism: a
# vector hash process (load vector constants (a vector constant, from the stack, and a vector constant, from obj->80, a
# particular value), call a vector hash (ComputeVectorHash_8222E6C0, on the vectors), and increment a counter (obj
# ->172, a counter, a particular count / a particular statistic)).
[entrypoint.functions."0x82A0B288"]
name = "ProcessVectorHash_82A0B288"

# A process (a copy, with a 24-byte element stride, the callee of ProcessFloat_83234418 / the sibling of CopyRecords
# 20_82A13138): (if a size, obj->4, is 0, a null state) take one path, and (otherwise) compute a count (a 24-byte
# element stride, (obj->12 - obj->4) / 24, a size, a 24-byte element, a particular count), and (if a count (obj->8 -
# obj->4) / 24 is >= the first count, a below/above-threshold check) take one path, and (otherwise) copy a 24-byte
# element (r6->0, a value, to obj->8, a base, a 24-byte element, a particular copy).

# Args: obj (r3, ->4 = a size, ->8 = a base, ->12 = a base, a particular structure), index (r4, r6, ->0 = a value, ->4
# = a float, a 24-byte element, a particular value), (more). It checks a size (obj->4, a size, a particular size / a
# particular count): if it is 0 (a null state, a particular invalid state), it takes one path (a no-op / a return, a
# particular state). Otherwise (a valid size), it computes a count (a 24-byte element stride, (obj->12 - obj->4, a
# size, a 24-byte element, a particular count), divided by 24 (divw r9, r9, r8, a divide, a 24-byte element stride, a
# particular count), a count, a particular number of 24-byte elements). It computes another count (a 24-byte element
# stride, (obj->8 - obj->4, a size, a 24-byte element, a particular count), divided by 24 (divw r8, r10, r8, a divide,
# a 24-byte element stride, a particular count), a count, a particular number of 24-byte elements). If the second count
# is >= the first count (a below/above-threshold check, a particular range, a particular size), it takes one path (a
# particular state, a particular result). Otherwise (a valid range, a particular size), it copies a 24-byte element (r6
# ->0, a value, a 24-byte element, a particular value, to obj->8, a base, a 24-byte element, a particular copy: stw r10
# , 0, r11, a store, a 24-byte element, a particular copy, and lfs f0, 4, r6, a float, a 24-byte element, a particular
# value, and a store, a 24-byte element, a particular copy). So it's a "process (a copy: (if a size, obj->4, is 0, a
# null state) take one path, and (otherwise) compute a count (a 24-byte element stride, (obj->12 - obj->4) / 24, a
# size, a 24-byte element, a particular count), and (if a count (obj->8 - obj->4) / 24 is >= the first count, a
# below/above-threshold check) take one path, and (otherwise) copy a 24-byte element (r6->0, a value, to obj->8, a
# base, a 24-byte element, a particular copy))" routine (a copy process, part of the 0x82A12 cluster, a callee of
# ProcessFloat_83234418, a sibling of CopyRecords20_82A13138). 128 lines, 1 callee (sub_82A12EC0, a sibling in the
# 0x82A12 cluster). 3 callers: ProcessFloat_83234418 (sub_83234418, a float process), sub_83233268, sub_83233B40
# (siblings in the 0x83233 cluster). The object's class (obj, ->4 = a size, ->8 = a base, ->12 = a base, a particular
# structure, a range / a copy object) and the 24-byte element's meaning (a 24-byte element, a particular value, a
# particular quantity) aren't resolvable, so the name states the mechanism: a copy process ((if a size, obj->4, is 0, a
# null state) take one path, and (otherwise) compute a count (a 24-byte element stride, (obj->12 - obj->4) / 24, a size
# , a 24-byte element, a particular count), and (if a count (obj->8 - obj->4) / 24 is >= the first count) take one
# path, and (otherwise) copy a 24-byte element (r6->0, a value, to obj->8, a base, a 24-byte element, a particular
# copy)).
[entrypoint.functions."0x82A12D68"]
name = "ProcessCopyElement_82A12D68"

# A process (a copy loop, with a 20-byte element, the callee of sub_82A12EC0 / the sibling of CopyRecords20_82A13138
# ): (if a count is 0, a null state) take one path, and (otherwise) loop (a copy loop, with a counter, a 20-byte
# element count, copying a 20-byte element (a value, a float, a float, a float, four bytes, a particular 20-byte
# element) from a source (r6, a source, a 20-byte element, a particular value) to a base (a, a base, a 20-byte element,
# a particular target), advancing to the next 20-byte element, a particular loop).

# Args: obj (r3, an object), a (r4, r11, a base, a 20-byte element, a particular target), count (r5, a count, a 20-byte
# element count, a particular count), source (r6, ->0 = a value, ->4 = a float, ->8 = a float, ->12 = a float, ->16 = a
# byte, ->17 = a byte, ->18 = a byte, ->19 = a byte, a 20-byte element, a particular source), (more). It checks a count
# (r5, a count, a 20-byte element count, a particular count): if it is 0 (a null state, a particular invalid state), it
# takes one path (a no-op / a return, a particular state). Otherwise (a valid count), it loops (a copy loop, with a
# counter, a 20-byte element count: it computes a counter (addic. r10, r11, -8, a counter, a 20-byte element count, a
# particular count), and (if the counter is 0, a particular end, a particular state) takes one path (a no-op / a
# return, a particular state). Otherwise (a valid count, a particular count), it copies a 20-byte element (r6->0, a
# value, a 20-byte element, a particular value, to a, a base, a 20-byte element, a particular target: stw r10, -8, r11
# , a store, a 20-byte element, a particular copy, and lfs f0, 4, r6, a float, a 20-byte element, a particular value,
# and a store, a 20-byte element, a particular copy, and lfs f13, 8, r6, a float, a 20-byte element, a particular
# value, and a store, a 20-byte element, a particular copy, and lfs f12, 12, r6, a float, a 20-byte element, a
# particular value, and a store, a 20-byte element, a particular copy, and lbz r9, 16, r6, a byte, a 20-byte element,
# a particular value, and a store, a 20-byte element, a particular copy, and lbz r8, 17, r6, a byte, a 20-byte element,
# a particular value, and a store, a 20-byte element, a particular copy, and lbz r7, 18, r6, a byte, a 20-byte element
# , a particular value, and a store, a 20-byte element, a particular copy, and lbz r3, 19, r6, a byte, a 20-byte
# element, a particular value, and a store, a 20-byte element, a particular copy), advancing to the next 20-byte
# element, a particular loop). So it's a "process (a copy loop: (if a count is 0, a null state) take one path, and
# (otherwise) loop (a copy loop, with a counter, a 20-byte element count, copying a 20-byte element (a value, a float,
# a float, a float, four bytes, a particular 20-byte element) from a source (r6, a source, a 20-byte element, a
# particular value) to a base (a, a base, a 20-byte element, a particular target), advancing to the next 20-byte
# element))" routine (a copy loop process, part of the 0x82A12 cluster, a callee of sub_82A12EC0, a sibling of
# CopyRecords20_82A13138). 80 lines, no direct callees (pure data manipulation). 1 caller: sub_82A12EC0 (a sibling in
# the 0x82A12 cluster, a callee of ProcessCopyElement_82A12D68, sub_82A12D68, a copy process, which calls this to copy
# the elements). The object's class (obj, an object, a particular structure, a copy / a range object) and the 20-byte
# element's meaning (a 20-byte element, a particular value, a particular quantity) aren't resolvable, so the name
# states the mechanism: a copy loop process ((if a count is 0, a null state) take one path, and (otherwise) loop (a copy
# loop, with a counter, a 20-byte element count, copying a 20-byte element (a value, a float, a float, a float, four
# bytes, a particular 20-byte element) from a source (r6, a source, a 20-byte element, a particular value) to a base (a
# , a base, a 20-byte element, a particular target), advancing to the next 20-byte element)).
[entrypoint.functions."0x82A12E40"]
name = "CopyElement20_82A12E40"

# A process (a copy/grow, with a 20-byte element and a 24-byte stride, the callee of ProcessCopyElement_82A12D68 / the
# caller of CopyElement20_82A12E40): copy a 20-byte element (a value, a float, a float, a float, four bytes, a
# particular 20-byte element, from a source (r6, a source, a 20-byte element, a particular value) to the stack, a
# particular copy), and perform a complex computation (a 24-byte element stride, a grow, calling CopyElement20
# _82A12E40, a copy loop; CopyRecords20_82A13138, a copy; Free_SizeBucketed_Thunk_8221BE68, a free; ProcessSizeCompute
# _825BBAD0, a size computation; sub_82A131B8, sub_82A97648 -- siblings in the 0x82A13/0x82A97 clusters).

# Args: obj (r3, r31, ->4 = a size, a particular structure), a (r4, a value), source (r6, ->0 = a value, ->4 = a float,
# ->8 = a float, ->12 = a float, ->16 = a byte, ->17 = a byte, ->18 = a byte, ->19 = a byte, ->20 = a byte, a 20-byte
# element, a particular source), (more). It copies a 20-byte element (a value (r10, r6->0, a value, a 20-byte element,
# a particular value), a float (f0, r6->4, a float, a 20-byte element, a particular value), a float (f13, r6->8, a
# float, a 20-byte element, a particular value), a float (f12, r6->12, a float, a 20-byte element, a particular value)
# , four bytes (r9, r6->16, a byte, a 20-byte element, a particular value; r8, r6->17, a byte, a 20-byte element, a
# particular value; r7, r6->18, a byte, a 20-byte element, a particular value; r5, r6->19, a byte, a 20-byte element, a
# particular value; r4, r6->20, a byte, a 20-byte element, a particular value), from a source (r6, a source, a 20-byte
# element, a particular value) to the stack (a particular copy: stw r10, 80, r1, a store, a 20-byte element, a
# particular copy, and stfs f0, 84, r1, a store, a 20-byte element, a particular copy, and stfs f13, 88, r1, a store, a
# 20-byte element, a particular copy, and stfs f12, 92, r1, a store, a 20-byte element, a particular copy, and stb r9,
# 96, r1, a store, a 20-byte element, a particular copy). It performs a complex computation (a 24-byte element stride, a
# grow, a particular size / a particular allocation, calling CopyElement20_82A12E40, sub_82A12E40, a copy loop; CopyRe
# cords20_82A13138, sub_82A13138, a copy; Free_SizeBucketed_Thunk_8221BE68, sub_8221BE68, a free; ProcessSizeCompute
# _825BBAD0, sub_825BBAD0, a size computation; sub_82A131B8, sub_82A97648 -- siblings in the 0x82A13/0x82A97 clusters
# , a particular copy/grow, a particular size / a particular allocation). So it's a "process (a copy/grow: copy a 20-
# byte element (a value, a float, a float, a float, four bytes, a particular 20-byte element, from a source (r6, a
# source, a 20-byte element, a particular value) to the stack, a particular copy), and perform a complex computation
# (a 24-byte element stride, a grow, calling CopyElement20_82A12E40, a copy loop; CopyRecords20_82A13138, a copy;
# Free_SizeBucketed_Thunk_8221BE68, a free; ProcessSizeCompute_825BBAD0, a size computation; sub_82A131B8, sub
# _82A97648))" routine (a copy/grow process, part of the 0x82A12 cluster, a callee of ProcessCopyElement_82A12D68, a
# caller of CopyElement20_82A12E40). 365 lines, 6 callees (CopyElement20_82A12E40, CopyRecords20_82A13138,
# Free_SizeBucketed_Thunk_8221BE68, ProcessSizeCompute_825BBAD0, sub_82A131B8, sub_82A97648). 1 caller:
# ProcessCopyElement_82A12D68 (sub_82A12D68, a copy process, which calls this to copy and grow the elements). The
# object's class (obj, ->4 = a size, a particular structure, a range / a copy object) and the 20-byte element's meaning
# (a 20-byte element, a particular value, a particular quantity) aren't resolvable, so the name states the mechanism:
# a copy/grow process (copy a 20-byte element (a value, a float, a float, a float, four bytes, a particular 20-byte
# element, from a source (r6, a source, a 20-byte element, a particular value) to the stack, a particular copy), and
# perform a complex computation (a 24-byte element stride, a grow, calling CopyElement20_82A12E40, a copy loop;
# CopyRecords20_82A13138, a copy; Free_SizeBucketed_Thunk_8221BE68, a free; ProcessSizeCompute_825BBAD0, a size
# computation)).
[entrypoint.functions."0x82A12EC0"]
name = "ProcessCopyGrow_82A12EC0"

# A process (a grow/append, with a 24-byte element stride and a copy, the caller of sub_8323FF48 / the sibling of
# ProcessCopyGrow_82A12EC0): (if a size, obj->100, is >= 4, a particular threshold, a particular size) return 0, a
# failure / a particular state, and (otherwise) grow/append (a 24-byte element stride, a copy, a particular size / a
# particular allocation, copying a 24-byte element (a value, a float, a float, a float, four bytes, a particular 20-byte
# element) from a source (r4, a source, a 20-byte element, a particular value) to a base (obj, a base, a 24-byte element
# , a particular target), advancing to the next 24-byte element, a particular grow/append).

# Args: obj (r3, r10, ->100 = a size, a particular structure, a range / a grow object), source (r4, ->0 = a value, ->4
# = a float, ->8 = a float, ->12 = a float, ->16 = a byte, a 20-byte element, a particular source), (more). It checks a
# size (obj->100, a size, a particular size / a particular count): if it is >= 4 (a particular threshold, a particular
# size, a particular count, a particular state), it returns 0 (a failure / a particular state, a particular result). If
# it is < 4 (a particular threshold, a particular size, a particular count, a particular state), it grows/appends (a 24
# -byte element stride, a copy, a particular size / a particular allocation: it computes a size (r9, obj->100 * 2, a
# size, a 2-byte element stride, a particular count), and a base (r7, obj->100 + r9, a size, a 3x size, a particular
# count), and a pointer (r11, r7 * 8, a size, a 24-byte element stride, a particular count, a base, a particular target
# ), and (r11 + r10, a base, a particular target), and copies a 24-byte element (r4->0, a value, a 20-byte element, a
# particular value, to obj, a base, a 24-byte element, a particular target: stw r8, 4, r11, a store, a 24-byte element
# , a particular copy, and lfs f0, 4, r4, a float, a 20-byte element, a particular value, and a store, a 24-byte
# element, a particular copy, and lfs f13, 8, r4, a float, a 20-byte element, a particular value, and a store, a 24-byte
# element, a particular copy, and lfs f12, 12, r4, a float, a 20-byte element, a particular value, and a store, a 24-byte
# element, a particular copy, and lbz r6, 16, r4, a byte, a 20-byte element, a particular value, and a store, a 24-byte
# element, a particular copy), advancing to the next 24-byte element, a particular grow/append). So it's a "process (a
# grow/append: (if a size, obj->100, is >= 4, a particular threshold, a particular size) return 0, a failure / a
# particular state, and (otherwise) grow/append (a 24-byte element stride, a copy, a particular size / a particular
# allocation, copying a 24-byte element (a value, a float, a float, a float, four bytes, a particular 20-byte element)
# from a source (r4, a source, a 20-byte element, a particular value) to a base (obj, a base, a 24-byte element, a
# particular target), advancing to the next 24-byte element))" routine (a grow/append process, part of the 0x82BE4
# cluster, a sibling of ProcessCopyGrow_82A12EC0). 81 lines, no direct callees (pure data manipulation). 3 callers: sub
# _8323FF48, sub_8323FA78, sub_8323FCA8 (siblings in the 0x8323F cluster). The object's class (obj, ->100 = a size, a
# particular structure, a range / a grow object) and the 20-byte element's meaning (a 20-byte element, a particular
# value, a particular quantity) aren't resolvable, so the name states the mechanism: a grow/append process ((if a size,
# obj->100, is >= 4, a particular threshold, a particular size) return 0, a failure / a particular state, and
# (otherwise) grow/append (a 24-byte element stride, a copy, a particular size / a particular allocation, copying a 24-
# byte element (a value, a float, a float, a float, four bytes, a particular 20-byte element) from a source (r4, a
# source, a 20-byte element, a particular value) to a base (obj, a base, a 24-byte element, a particular target),
# advancing to the next 24-byte element)).
[entrypoint.functions."0x82BE4AA8"]
name = "ProcessGrowAppend_82BE4AA8"

# A process (a grow/append, with a byte-by-byte copy and a 4-byte element, the sibling of ProcessGrowAppend_82BE4AA8
# / the caller of sub_82A1B480): (if a size, obj->20, is >= 4, a particular threshold, a particular size) grow/append
# (a byte-by-byte copy, a 4-byte element, a particular size / a particular allocation, copying a 4-byte element (four
# bytes, a particular 4-byte element) from a base (obj->12, a base, a 4-byte element, a particular source) to a stack
# (a particular target), advancing to the next 4-byte element, a particular grow/append), and (in a path) call a
# process (sub_82A1B480, on a value, a particular operation).

# Args: obj (r3, r31, ->4 = a base, ->12 = a base, ->20 = a size, a particular structure, a range / a grow object), value
# (r4, r30, a value, a particular argument), (more). It checks a size (obj->20, a size, a particular size / a particular
# count): if it is >= 4 (a particular threshold, a particular size, a particular count, a particular state), it
# grows/appends (a byte-by-byte copy, a 4-byte element, a particular size / a particular allocation: it computes a size
# (r9, obj->20 - 4, a size, a 4-byte element, a particular count), and a base (r11, obj->12, a base, a 4-byte element,
# a particular source), and (r7, r11 + 4, a base, a 4-byte element, a particular source), and copies a 4-byte element
# (four bytes, a particular 4-byte element: lbz r5, 0, r11, a byte, a 4-byte element, a particular value, and stb r5, 0,
# r8, a store, a 4-byte element, a particular copy, and lbz r4, 1, r11, a byte, a 4-byte element, a particular value, and
# stb r4, 1, r8, a store, a 4-byte element, a particular copy, and lbz r3, 2, r11, a byte, a 4-byte element, a
# particular value, and stb r3, 2, r8, a store, a 4-byte element, a particular copy, and lbz r11, 3, r11, a byte, a 4-byte
# element, a particular value, and stb r11, 3, r8, a store, a 4-byte element, a particular copy), advancing to the next
# 4-byte element, a particular grow/append). If it is < 4 (a particular threshold, a particular size, a particular count
# , a particular state), it takes one path (a no-op / a return, a particular state). In a path it calls a process (sub
# _82A1B480, on a value, a particular operation, a particular grow/append, a particular size / a particular allocation).
# So it's a "process (a grow/append: (if a size, obj->20, is >= 4, a particular threshold, a particular size) grow
# /append (a byte-by-byte copy, a 4-byte element, a particular size / a particular allocation, copying a 4-byte element
# (four bytes, a particular 4-byte element) from a base (obj->12, a base, a 4-byte element, a particular source) to a
# stack (a particular target), advancing to the next 4-byte element), and (in a path) call a process (sub_82A1B480, on a
# value))" routine (a grow/append process, part of the 0x8321F cluster, a sibling of ProcessGrowAppend_82BE4AA8, a
# caller of sub_82A1B480). 290 lines, 1 callee (sub_82A1B480, a sibling in the 0x82A1B cluster). 3 callers: sub
# _8321F548, sub_83225760, sub_83225600 (siblings in the 0x8321F/0x83225 clusters). The object's class (obj, ->4 = a
# base, ->12 = a base, ->20 = a size, a particular structure, a range / a grow object) and the 4-byte element's meaning
# (a 4-byte element, a particular value, a particular quantity) aren't resolvable, so the name states the mechanism: a
# grow/append process ((if a size, obj->20, is >= 4, a particular threshold, a particular size) grow/append (a byte-by-
# byte copy, a 4-byte element, a particular size / a particular allocation, copying a 4-byte element (four bytes, a
# particular 4-byte element) from a base (obj->12, a base, a 4-byte element, a particular source) to a stack (a
# particular target), advancing to the next 4-byte element), and (in a path) call a process (sub_82A1B480, on a value)
# ). The "2" distinguishes it from ProcessGrowAppend_82BE4AA8.
[entrypoint.functions."0x8321F328"]
name = "ProcessGrowAppend2_8321F328"

# A 32-byte constructor (the sibling of ConstructNode32_82BACFB0 / Construct32Byte_82B722E8, with a vector operation):
# initialize the object's fields (obj->4 = value, obj->8 = 0, obj->0 = a constant, a 32-byte object), and perform a
# vector operation (with a constant, a SIMD computation, calling ProcessVector2_8223BD98, a vector process, a
# particular vector operation).

# Args: obj (r3, r31, ->0 = a field, a constant, a class id / a marker, ->4 = a field, a value, ->8 = a field, a count / a
# size, a 32-byte object), value (r4, a value, a particular argument), (more). It initializes the object's fields (obj
# ->4 = value, a field, a value, obj->8 = 0, a field, a count / a size, a zero, obj->0 = a constant, a class id / a
# marker, from a fixed .data address, a particular class / a particular marker). It performs a vector operation (with a
# constant, a SIMD computation: it loads a vector constant (lvx128 v0, r9, a vector constant, from a fixed .data
# address, a particular vector constant), and a float constant (f0, a constant, from a fixed .data address, a
# particular value), and (a vector operation, with a constant, a SIMD computation, calling ProcessVector2_8223BD98, sub
# _8223BD98, a vector process, a particular vector operation). So it's a "construct a 32-byte object (initialize the
# fields, obj->4 = value, obj->8 = 0, obj->0 = a constant, a 32-byte object, and perform a vector operation (with a
# constant, a SIMD computation, calling ProcessVector2_8223BD98, a vector process, a particular vector operation))"
# routine (a 32-byte constructor, with a vector operation, part of the 0x8321F cluster, a sibling of ConstructNode32
# _82BACFB0). 132 lines, 1 callee (ProcessVector2_8223BD98, sub_8223BD98, a vector process). 9 callers: sub_8323C7F0,
# sub_8323BC80, sub_8323AA90, sub_83216720, sub_83217228, sub_8323BCC8, sub_8323B220, sub_8323F1B8, sub_83220478
# (siblings in the 0x8323C/0x8323B/0x8323A/0x83216/0x83217/0x8323F/0x83220 clusters). The object's class (obj, ->0 = a
# field, a constant, a class id / a marker, ->4 = a field, a value, ->8 = a field, a count / a size, a 32-byte object, a
# particular structure) and the value's meaning (a value, a particular argument) aren't resolvable, so the name states
# the mechanism: a 32-byte constructor (with a vector operation) (initialize the fields, obj->4 = value, obj->8 = 0,
# obj->0 = a constant, a 32-byte object, and perform a vector operation (with a constant, a SIMD computation, calling
# ProcessVector2_8223BD98, a vector process)). The "Vector" refers to the vector operation (a SIMD computation, a
# particular vector operation).
[entrypoint.functions."0x8321FB58"]
name = "Construct32ByteVector_8321FB58"

# A process (a grow/append, with a byte-by-byte copy and a 4-byte element, the sibling of ProcessGrowAppend2_8321F328
# / ProcessGrowAppend_82BE4AA8, the caller of ProcessVectorHash_82A0B288 / CopyStructData_83235608 / CopyStructData4_8
# 2A1BEA8): (if a size, value->20, is >= 4, a particular threshold, a particular size) grow/append (a byte-by-byte
# copy, a 4-byte element, a particular size / a particular allocation, copying a 4-byte element (four bytes, a
# particular 4-byte element) from a base (value->12, a base, a 4-byte element, a particular source) to a stack (a
# particular target), advancing to the next 4-byte element, a particular grow/append), and (in paths) call a vector
# hash (ProcessVectorHash_82A0B288, on a value, a SIMD computation, a particular hash / a particular comparison) and
# copy (CopyStructData_83235608, CopyStructData4_82A1BEA8, a struct data copy, a particular copy).

# Args: obj (r3, r30, an object), value (r4, r31, ->4 = a base, ->12 = a base, ->20 = a size, a particular structure, a
# range / a grow object), a (r5, r28, a value), b (r6, r27, a value), (more). It checks a size (value->20, a size, a
# particular size / a particular count): if it is >= 4 (a particular threshold, a particular size, a particular count, a
# particular state), it grows/appends (a byte-by-byte copy, a 4-byte element, a particular size / a particular
# allocation: it computes a size (r9, value->20 - 4, a size, a 4-byte element, a particular count), and a base (r11,
# value->12, a base, a 4-byte element, a particular source), and (r7, r11 + 4, a base, a 4-byte element, a particular
# source), and copies a 4-byte element (four bytes, a particular 4-byte element: lbz r5, 0, r11, a byte, a 4-byte
# element, a particular value, and stb r5, 0, r8, a store, a 4-byte element, a particular copy, and lbz r4, 1, r11, a
# byte, a 4-byte element, a particular value, and stb r4, 1, r8, a store, a 4-byte element, a particular copy, and lbz
# r3, 2, r11, a byte, a 4-byte element, a particular value, and stb r3, 2, r8, a store, a 4-byte element, a particular
# copy, and lbz r11, 3, r11, a byte, a 4-byte element, a particular value, and stb r11, 3, r8, a store, a 4-byte
# element, a particular copy), advancing to the next 4-byte element, a particular grow/append). In paths it calls a
# vector hash (ProcessVectorHash_82A0B288, sub_82A0B288, a vector hash, on a value, a SIMD computation, a particular
# hash / a particular comparison, a particular vector operation) and copies (CopyStructData_83235608, sub_83235608, a
# struct data copy, on a value, a particular copy, and CopyStructData4_82A1BEA8, sub_82A1BEA8, a struct data copy, on a
# value, a particular copy, a particular size / a particular allocation). So it's a "process (a grow/append: (if a
# size, value->20, is >= 4, a particular threshold, a particular size) grow/append (a byte-by-byte copy, a 4-byte
# element, a particular size / a particular allocation, copying a 4-byte element (four bytes, a particular 4-byte
# element) from a base (value->12, a base, a 4-byte element, a particular source) to a stack (a particular target),
# advancing to the next 4-byte element), and (in paths) call a vector hash (ProcessVectorHash_82A0B288, on a value, a
# SIMD computation) and copy (CopyStructData_83235608, CopyStructData4_82A1BEA8, a struct data copy, a particular
# copy))" routine (a grow/append process, part of the 0x8321F cluster, a sibling of ProcessGrowAppend2_8321F328, a
# caller of ProcessVectorHash_82A0B288, CopyStructData_83235608, and CopyStructData4_82A1BEA8). 815 lines, 4 callees
# (CopyStructData4_82A1BEA8, CopyStructData_83235608, ProcessVectorHash_82A0B288, sub_82A1B480). 7 callers: sub
# _8323F5B0, sub_8323C0C0, sub_8323CB98, sub_83216BB0, sub_83217688, sub_8323AE00, sub_8323B650 (siblings in the
# 0x8323F/0x8323C/0x83216/0x83217/0x8323A/0x8323B clusters). The object's class (obj, an object, a particular
# structure, a range / a grow object) and the 4-byte element's meaning (a 4-byte element, a particular value, a
# particular quantity) aren't resolvable, so the name states the mechanism: a grow/append process ((if a size, value
# ->20, is >= 4, a particular threshold, a particular size) grow/append (a byte-by-byte copy, a 4-byte element, a
# particular size / a particular allocation, copying a 4-byte element (four bytes, a particular 4-byte element) from a
# base (value->12, a base, a 4-byte element, a particular source) to a stack (a particular target), advancing to the
# next 4-byte element), and (in paths) call a vector hash (ProcessVectorHash_82A0B288, on a value, a SIMD computation)
# and copy (CopyStructData_83235608, CopyStructData4_82A1BEA8, a struct data copy, a particular copy)). The "3"
# distinguishes it from ProcessGrowAppend2_8321F328 and ProcessGrowAppend_82BE4AA8.
[entrypoint.functions."0x8321FC38"]
name = "ProcessGrowAppend3_8321FC38"

# A process (a state dispatch, with a switch, the sibling of StateDispatch3_82BCD9E8 / StateDispatch2_82BCD7B0):
# initialize the object's fields (obj->0/->4 = 0, two zeros, a particular initialization), and (if a state is <= 9, a
# particular range, a particular state) perform a state dispatch (a switch, over 0-9 states, a particular state
# dispatch, a particular state / a particular transition), and (in paths) call sibling operations (sub_823BE9C0, sub
# _82A0C2E0, sub_82A0C960, sub_82A0CFC8, sub_82A0D630, sub_832167D0, sub_832172A8, sub_83220478, sub_8323B270, sub
# _8323BCC8, sub_8323E260, sub_8323F238 -- siblings in the 0x823BE/0x82A0C/0x82A0D/0x83216/0x83217/0x83220/0x8323B/0
# x8323E/0x8323F clusters, a particular state / a particular transition).

# Args: obj (r3, r31, ->0 = a field, a particular value, ->4 = a field, a particular value, a particular structure),
# state (r4, r11, a state, a particular state / a particular value), a (r5, a value, a particular argument), (more). It
# initializes the object's fields (obj->0 = 0, a field, a particular value, obj->4 = 0, a field, a particular value, two
# zeros, a particular initialization). It checks a state (r4, r11, a state, a particular state / a particular value): if
# it is <= 9 (a particular range, a particular state, a particular value), it performs a state dispatch (a switch, over
# 0-9 states, a particular state dispatch, a particular state / a particular transition: case 0, a particular state, a
# particular transition; case 1, a particular state, a particular transition; case 2, a particular state, a particular
# transition; case 3, a particular state, a particular transition; case 4, a particular state, a particular transition;
# case 5, a particular state, a particular transition; case 6, a particular state, a particular transition; case 7, a
# particular state, a particular transition; case 8, a particular state, a particular transition; case 9, a particular
# state, a particular transition). If the state is > 9 (a particular range, a particular state, a particular value), it
# takes one path (a no-op / a return, a particular state). In paths it calls sibling operations (sub_823BE9C0, sub
# _82A0C2E0, sub_82A0C960, sub_82A0CFC8, sub_82A0D630, sub_832167D0, sub_832172A8, sub_83220478, sub_8323B270, sub
# _8323BCC8, sub_8323E260, sub_8323F238 -- siblings in the 0x823BE/0x82A0C/0x82A0D/0x83216/0x83217/0x83220/0x8323B/0
# x8323E/0x8323F clusters, a particular state / a particular transition, a particular operation). So it's a "process (a
# state dispatch: initialize the object's fields (obj->0/->4 = 0, two zeros, a particular initialization), and (if a
# state is <= 9, a particular range, a particular state) perform a state dispatch (a switch, over 0-9 states, a
# particular state dispatch, a particular state / a particular transition), and (in paths) call sibling operations)"
# routine (a state dispatch process, part of the 0x83220 cluster, a sibling of StateDispatch3_82BCD9E8). 599 lines, 13
# callees (Release_RefCounted_829FF648, sub_823BE9C0, sub_82A0C2E0, sub_82A0C960, sub_82A0CFC8, sub_82A0D630, sub
# _832167D0, sub_832172A8, sub_83220478, sub_8323B270, sub_8323BCC8, sub_8323E260, sub_8323F238). 3 callers: sub
# _83225760, sub_832256A0, sub_83225600 (siblings in the 0x83225 cluster). The object's class (obj, ->0 = a field, a
# particular value, ->4 = a field, a particular value, a particular structure, a state / a value object) and the
# state's meaning (a state, a particular state / a particular value) aren't resolvable, so the name states the
# mechanism: a state dispatch process (initialize the object's fields (obj->0/->4 = 0, two zeros, a particular
# initialization), and (if a state is <= 9, a particular range, a particular state) perform a state dispatch (a switch,
# over 0-9 states, a particular state dispatch, a particular state / a particular transition), and (in paths) call
# sibling operations). The "4" distinguishes it from StateDispatch3_82BCD9E8 and StateDispatch2_82BCD7B0.
[entrypoint.functions."0x83220888"]
name = "StateDispatch4_83220888"

# A process (a reference count, with a release and an increment, the caller of Release_RefCounted_829FF648 /
# LookupAndAssign_8323A1E8): (if a value, obj->4, is non-zero, a particular value) release a refcounted value
# (Release_RefCounted_829FF648, on a value, a particular release), and increment a reference count (a value (obj->4, a
# value, a particular value), a reference count, a particular count), and (otherwise) take one path, and call a lookup
# (LookupAndAssign_8323A1E8, on a value and a constant, a particular lookup).

# Args: obj (r3, ->0 = a value, ->4 = a value, a reference count, a particular structure), (more). It checks a value
# (obj->4, a value, a reference count, a particular value): if it is non-zero (a particular value, a particular state),
# it releases a refcounted value (Release_RefCounted_829FF648, sub_829FF648, a refcounted release, on a value (stack
# [80], a value, a particular value), a particular release, a particular count), and increments a reference count (a
# value (r31, obj->4, a value, a reference count, a particular value), a reference count, a particular count: it reads
# the count (r11, r31->0, a reference count, a particular count), and stores it (r11 + 1, an increment, a particular
# count, a particular reference count). If it is zero (a null state, a particular state), it takes one path (a no-op /
# a return, a particular state). It calls a lookup (LookupAndAssign_8323A1E8, sub_8323A1E8, a lookup and assign, on a
# value (stack[80], a value, a particular value) and a constant (a constant, from a fixed .data address, a particular
# value, a particular key), a particular lookup, a particular assignment). So it's a "process (a reference count: (if a
# value, obj->4, is non-zero, a particular value) release a refcounted value (Release_RefCounted_829FF648, on a value,
# a particular release), and increment a reference count (a value (obj->4, a value, a reference count, a particular
# value), a reference count, a particular count), and (otherwise) take one path, and call a lookup (LookupAndAssign
# _8323A1E8, on a value and a constant, a particular lookup))" routine (a reference count process, part of the 0x83225
# cluster, a caller of Release_RefCounted_829FF648 and LookupAndAssign_8323A1E8). 73 lines, 2 callees
# (LookupAndAssign_8323A1E8, Release_RefCounted_829FF648). 10+ callers: sub_8323BCC8, sub_8323B270, sub_82A0C2E0, sub
# _832172A8, sub_82A0CFC8, sub_8323E260, sub_82A0C960, sub_832167D0, sub_8323F238, sub_82A0D630 (a widely-called
# reference count process, part of the game's reference counting subsystem). The object's class (obj, ->0 = a value, ->
# 4 = a value, a reference count, a particular structure, a reference counted object) and the value's meaning (a value,
# a reference count, a particular value, a particular count) aren't resolvable, so the name states the mechanism: a
# reference count process ((if a value, obj->4, is non-zero, a particular value) release a refcounted value
# (Release_RefCounted_829FF648, on a value, a particular release), and increment a reference count (a value (obj->4, a
# value, a reference count, a particular value), a reference count, a particular count), and (otherwise) take one path,
# and call a lookup (LookupAndAssign_8323A1E8, on a value and a constant, a particular lookup)).
[entrypoint.functions."0x83225538"]
name = "ProcessRefCount_83225538"

# A getter (with a check, the sibling of GetSingleton_829F9090 / GetObjectAtOffset_821782D8): read a global (from a
# fixed .data address, a particular value, a particular singleton / a particular flag), and (if it is non-zero, a
# particular state) return another global (a flag, from a fixed .data address, a particular value, a particular flag /
# a particular state), and (otherwise) return 0, a zero / a particular state.

# No args. It reads a global (r11, from a fixed .data address, a particular value, a particular singleton / a
# particular flag). It checks the value: if it is non-zero (a particular state, a particular value), it returns another
# global (r3, from a fixed .data address, a particular value, a particular flag / a particular state, a particular
# value); if it is zero (a null state, a particular state), it returns 0 (a zero / a particular state, a particular
# value). So it's a "get a flag (with a check: read a global, and (if it is non-zero, a particular state) return
# another global (a flag, from a fixed .data address, a particular value, a particular flag / a particular state), and
# (otherwise) return 0)" routine (a getter, with a check, part of the 0x82226 cluster). 23 lines, no direct callees
# (pure data manipulation). 8 callers: sub_822FAD38, sub_82184D38, sub_8230C328, sub_82815E00, sub_8230BB78, sub
# _8217D1F8, sub_822D1410, sub_822C5390 (a widely-called getter, part of the game's subsystems). The global's meaning
# (a global, from a fixed .data address, a particular value, a particular singleton / a particular flag, a particular
# flag / a particular state) and the flag's meaning (a flag, from a fixed .data address, a particular value, a
# particular flag / a particular state) aren't resolvable, so the name states the mechanism: a getter (with a check) (a
# flag, from a fixed .data address, a particular value, a particular flag / a particular state, with a check, a
# particular state).
[entrypoint.functions."0x82226ED8"]
name = "GetFlag_82226ED8"

# A process (a dispatch, with a loop and a vcall, the caller of ProcessDispatchValue_8229A488 / HashTable_Find3_821CF1
# D0): loop over a list (a list, r27, a particular list / a particular sequence, a dispatch object), and for each item
# (a type, list->4, a type, a particular value, a particular item), (if the type is 5, a particular type, a particular
# state) dispatch a value (ProcessDispatchValue_8229A488, on a value, a particular dispatch, a particular value /
# a particular operation), and (if the result's type is non-zero, a particular state) take one path (a no-op / a return
# , a particular state), and (in paths) call a hash table lookup (HashTable_Find3_821CF1D0, on a value and a constant, a
# particular lookup, a particular key).

# Args: obj (r3, r31, an object), list (r4, r27, ->0 = a value, ->4 = a type, a particular list / a particular
# sequence, a dispatch object), a (r5, r26, a value, a particular argument), b (r6, r24, a value, a particular
# argument), (more). It loops over a list (r27, a list, a particular list / a particular sequence, a dispatch object, a
# particular count / a particular size): for each item (r11, list->4, a type, a particular value, a particular item), it
# checks the type: if it is 5 (a particular type, a particular state, a particular value), it dispatches a value
# (ProcessDispatchValue_8229A488, sub_8229A488, a dispatch, on a value (obj, r31, an object, a particular argument, a
# dispatch object), and a (r26, a value, a particular argument), and r29, list->0, a value, a particular argument, a
# particular dispatch, a particular value / a particular operation). If the result's type (r28, result, a type, a
# particular value, a particular state) is non-zero (a particular state, a particular value), it takes one path (a no-op
# / a return, a particular state). In paths it calls a hash table lookup (HashTable_Find3_821CF1D0, sub_821CF1D0, a
# hash table lookup, on a value and a constant, a particular lookup, a particular key, a particular value / a particular
# operation). So it's a "process (a dispatch, with a loop and a vcall: loop over a list (a list, r27, a particular list
# / a particular sequence, a dispatch object), and for each item (a type, list->4, a type, a particular value, a
# particular item), (if the type is 5, a particular type, a particular state) dispatch a value (ProcessDispatchValue
# _8229A488, on a value, a particular dispatch, a particular value / a particular operation), and (in paths) call a hash
# table lookup (HashTable_Find3_821CF1D0, on a value and a constant, a particular lookup))" routine (a dispatch loop
# process, part of the 0x8229A cluster, a caller of ProcessDispatchValue_8229A488 and HashTable_Find3_821CF1D0). 312
# lines, 7 callees (HashTable_Find3_821CF1D0, ProcessDispatchValue_8229A488, sub_8219EE00, sub_82BC9640, sub_82BC9860,
# sub_82BCCD58, sub_82BCD5E0). 10 callers: sub_82BBB3A8, sub_82BC7328, sub_82BB46A8, sub_82BB5C90, sub_82BC5E60, sub
# _82BC0210, sub_82BBFA28, sub_82BC5EB0, sub_82BC0A60, sub_822DF280 (siblings in the 0x82BBB/0x82BC7/0x82BB4/0x82BB5/0
# x82BC5/0x82BC0/0x82BBF/0x82BC5/0x82BC0/0x822DF clusters). The object's class (obj, an object, a particular structure,
# a dispatch object) and the list's meaning (a list, r27, a particular list / a particular sequence, a dispatch object)
# aren't resolvable, so the name states the mechanism: a dispatch loop process (loop over a list (a list, r27, a
# particular list / a particular sequence, a dispatch object), and for each item (a type, list->4, a type, a
# particular value, a particular item), (if the type is 5, a particular type, a particular state) dispatch a value
# (ProcessDispatchValue_8229A488, on a value, a particular dispatch, a particular value / a particular operation), and
# (in paths) call a hash table lookup (HashTable_Find3_821CF1D0, on a value and a constant, a particular lookup)).
[entrypoint.functions."0x8229A518"]
name = "ProcessDispatchLoop_8229A518"

# A process (a vcall, with a construct and a complex computation, the caller of ConstructRefCounted_8222CF18):
# perform a vcall (a method call, on a value, a particular vcall / a particular operation), construct a refcounted
# value (ConstructRefCounted_8222CF18, on a value, a particular construct / a particular allocation), and (in paths)
# perform a complex computation (a reference count, a particular count / a particular state, a particular size / a
# particular allocation).

# Args: obj (r3, r30, ->0 = a value, a particular structure, a refcounted object), a (r4, r31, ->0 = a vtable, ->28 = a
# value, a particular vcall object, a particular operation object), (more). It performs a vcall (a method call, on a
# value (a, r31, a particular vcall object, a particular operation object), a particular vcall / a particular operation
# : it loads a vtable (r11, a->0, a vtable, a particular vtable), and a method (r10, r11->8, a method, a particular
# method), and calls it (bctrl, a vcall, a method call, a particular vcall / a particular operation, a particular value
# / a particular operation)). It constructs a refcounted value (ConstructRefCounted_8222CF18, sub_8222CF18, a
# refcounted construct, on a value (obj, r30, a particular structure, a refcounted object), and a + result, a particular
# value / a particular allocation, and -1, a constant, a particular value, a particular construct / a particular
# allocation). In paths it performs a complex computation (a reference count, a particular count / a particular state,
# a particular size / a particular allocation: it reads a value (r11, obj->0, a value, a particular value), and (if it
# is non-zero, a particular state) reads another value (r11->4, a value, a particular value), and increments it (r29,
# r11 + 1, an increment, a particular count / a particular state, a particular reference count). So it's a "process (a
# vcall, with a construct and a complex computation: perform a vcall (a method call, on a value, a particular vcall /
# a particular operation), construct a refcounted value (ConstructRefCounted_8222CF18, on a value, a particular
# construct / a particular allocation), and (in paths) perform a complex computation (a reference count, a particular
# count / a particular state))" routine (a vcall+construct process, part of the 0x82A1D cluster, a caller of
# ConstructRefCounted_8222CF18). 98 lines, 1 callee (ConstructRefCounted_8222CF18, sub_8222CF18, a refcounted
# construct). 3 callers: sub_82B80D70, sub_82AAAC20, sub_82B28398 (siblings in the 0x82B80/0x82AAA/0x82B28 clusters)
# . The object's class (obj, r30, ->0 = a value, a particular structure, a refcounted object, a particular vcall
# object) and the value's meaning (a, r31, ->0 = a vtable, ->28 = a value, a particular vcall object, a particular
# operation object) aren't resolvable, so the name states the mechanism: a vcall+construct process (perform a vcall (a
# method call, on a value, a particular vcall / a particular operation), construct a refcounted value
# (ConstructRefCounted_8222CF18, on a value, a particular construct / a particular allocation), and (in paths) perform
# a complex computation (a reference count, a particular count / a particular state)).
[entrypoint.functions."0x82A1D5C8"]
name = "ProcessVcallConstruct_82A1D5C8"

# A process (a vcall, with a complex computation, the caller of BinarySearch_4Stride_82C40EC0): perform a vcall (a
# method call, on a value, a particular vcall / a particular operation), and (if the result is non-zero, a particular
# state) perform a complex computation (a binary search, a particular search / a particular lookup, calling
# BinarySearch_4Stride_82C40EC0, on a value and a constant, a particular search, a particular key), and (otherwise)
# return 0, a zero / a particular state.

# Args: a (r3, a value), obj (r4, ->0 = a vtable, a particular vcall object, a particular operation object), b (r5, a
# value), (more). It performs a vcall (a method call, on a value (obj, r4, a particular vcall object, a particular
# operation object), a particular vcall / a particular operation: it loads a vtable (r11, obj->0, a vtable, a
# particular vtable), and a method (r9, r11->4, a method, a particular method), and calls it (bctrl, a vcall, a
# method call, a particular vcall / a particular operation, a particular value / a particular operation)). If the
# result (r3, result, a value, a particular value, a particular state) is non-zero (a particular state, a particular
# value), it performs a complex computation (a binary search, a particular search / a particular lookup, calling
# BinarySearch_4Stride_82C40EC0, sub_82C40EC0, a binary search, on a value (r31, result + 4, a value, a particular
# value, a particular base) and a constant (a constant, from a fixed .data address, a particular value, a particular
# key), a particular search, a particular key, a particular size / a particular allocation). If the result is zero (a
# null state, a particular state), it returns 0 (a zero / a particular state, a particular value). So it's a "process
# (a vcall, with a complex computation: perform a vcall (a method call, on a value, a particular vcall / a particular
# operation), and (if the result is non-zero, a particular state) perform a complex computation (a binary search, a
# particular search / a particular lookup, calling BinarySearch_4Stride_82C40EC0, on a value and a constant, a
# particular search, a particular key), and (otherwise) return 0)" routine (a vcall process, part of the 0x82C26
# cluster, a caller of BinarySearch_4Stride_82C40EC0). 83 lines, 1 callee (BinarySearch_4Stride_82C40EC0, sub
# _82C40EC0, a binary search). 2 callers: sub_82C27538, sub_82C265A8 (siblings in the 0x82C27/0x82C26 clusters). The
# object's class (obj, r4, ->0 = a vtable, a particular vcall object, a particular operation object, a particular
# structure) and the value's meaning (a, r3, a value; b, r5, a value, a particular argument) aren't resolvable, so the
# name states the mechanism: a vcall process (perform a vcall (a method call, on a value, a particular vcall / a
# particular operation), and (if the result is non-zero, a particular state) perform a complex computation (a binary
# search, a particular search / a particular lookup, calling BinarySearch_4Stride_82C40EC0, on a value and a constant,
# a particular search, a particular key), and (otherwise) return 0).
[entrypoint.functions."0x82C26518"]
name = "ProcessVcallBinary_82C26518"

# A process (a vcall, with a complex computation, the sibling of ProcessVcallBinary_82C26518 / the caller of
# ProcessVcallBinary_82C26518): perform a vcall (a method call, on a value, a particular vcall / a particular
# operation, calling ProcessVcallBinary_82C26518, a vcall process, a particular vcall), and (in paths) perform a
# complex computation (a binary search, a particular search / a particular lookup, calling sub_82BFF710, sub_82BFF750,
# sub_82BFF7D8, sub_82BFF848, sub_82BFFAA8 -- siblings in the 0x82BFF cluster, a particular search / a particular
# lookup, a particular value / a particular operation).

# Args: a (r3, a value), obj (r4, r31, ->0 = a value, a particular vcall object, a particular operation object), b (r5,
# r30, a value), (more). It performs a vcall (a method call, on a value (obj, r4, r31, a particular vcall object, a
# particular operation object), a particular vcall / a particular operation, calling ProcessVcallBinary_82C26518, sub
# _82C26518, a vcall process, a particular vcall, a particular value / a particular operation). In paths it performs a
# complex computation (a binary search, a particular search / a particular lookup: it calls sub_82BFF710, on a value
# (stack[80], a value, a particular value, a particular stack), a particular operation; sub_82BFF7D8, on a value
# (stack[80], a value, a particular value, a particular stack) and a constant (a constant, from a fixed .data address,
# a particular value, a particular key), a particular operation; sub_82BFF848, on a value (stack[80], a value, a
# particular value, a particular stack) and b (r30, a value, a particular argument), a particular operation; sub
# _82BFF7D8, on a value (stack[80], a value, a particular value, a particular stack) and a constant (a constant, from a
# fixed .data address, a particular value, a particular key), a particular operation; sub_82BFF750, sub_82BFFAA8 --
# siblings in the 0x82BFF cluster, a particular search / a particular lookup, a particular value / a particular
# operation). So it's a "process (a vcall, with a complex computation: perform a vcall (a method call, on a value, a
# particular vcall / a particular operation, calling ProcessVcallBinary_82C26518, a vcall process, a particular
# vcall), and (in paths) perform a complex computation (a binary search, a particular search / a particular lookup,
# calling sub_82BFF710, sub_82BFF750, sub_82BFF7D8, sub_82BFF848, sub_82BFFAA8 -- siblings in the 0x82BFF cluster, a
# particular search / a particular lookup))" routine (a vcall process, part of the 0x82C26 cluster, a sibling of
# ProcessVcallBinary_82C26518, a caller of ProcessVcallBinary_82C26518). 97 lines, 6 callees (sub_82BFF710, sub
# _82BFF750, sub_82BFF7D8, sub_82BFF848, sub_82BFFAA8, sub_82C26518). No direct callers (it's called via a vtable / it
# is registered-but-uncalled, part of the game's subsystems). The object's class (obj, r4, r31, ->0 = a value, a
# particular vcall object, a particular operation object, a particular structure) and the value's meaning (a, r3, a
# value; b, r5, r30, a value, a particular argument) aren't resolvable, so the name states the mechanism: a vcall
# process (perform a vcall (a method call, on a value, a particular vcall / a particular operation, calling
# ProcessVcallBinary_82C26518, a vcall process, a particular vcall), and (in paths) perform a complex computation (a
# binary search, a particular search / a particular lookup, calling sub_82BFF710, sub_82BFF750, sub_82BFF7D8, sub
# _82BFF848, sub_82BFFAA8 -- siblings in the 0x82BFF cluster, a particular search / a particular lookup)). The "2"
# distinguishes it from ProcessVcallBinary_82C26518.
[entrypoint.functions."0x82C265A8"]
name = "ProcessVcallBinary2_82C265A8"

# A dispatch (a vcall, with a switch, the sibling of StateDispatch4_83220888 / StateDispatch3_82BCD9E8): (if a value,
# obj->0, is non-zero, a particular value) perform a vcall (a method call, on a value, a particular vcall / a
# particular operation), and (otherwise) perform a dispatch (a switch, over 6-7 states, a particular state dispatch, a
# particular state / a particular transition, calling a vcall (a method call, on a value, a particular vcall / a
# particular operation, based on a type (obj->10, a type, a particular value, a particular state))).

# Args: a (r3, a value), obj (r4, ->0 = a value, ->4 = a value, ->10 = a type, a particular dispatch object, a
# particular vcall object), (more). It checks a value (obj->0, a value, a particular value): if it is non-zero (a
# particular value, a particular state), it performs a vcall (a method call, on a value (r3, obj->0, a value, a
# particular value, a particular vcall object), a particular vcall / a particular operation: it loads a vtable (r11,
# r3->0, a vtable, a particular vtable), and a method (r10, r11->12, a method, a particular method), and calls it
# (bctrl, a vcall, a method call, a particular vcall / a particular operation, a particular value / a particular
# operation)). If it is zero (a null state, a particular state), it performs a dispatch (a switch, over 6-7 states, a
# particular state dispatch, a particular state / a particular transition: it checks a type (r11, obj->10, a type, a
# particular value, a particular state): if it is 6 (a particular state, a particular value, a particular type), it
# performs a vcall (a method call, on a value (r4, obj->4, a value, a particular value, a particular vcall object), a
# particular vcall / a particular operation); if it is 7 (a particular state, a particular value, a particular type), it
# performs another vcall (a method call, on a value, a particular vcall / a particular operation); otherwise it returns
# (a no-op / a return, a particular state). So it's a "dispatch (a vcall, with a switch: (if a value, obj->0, is non
# -zero, a particular value) perform a vcall (a method call, on a value, a particular vcall / a particular operation),
# and (otherwise) perform a dispatch (a switch, over 6-7 states, a particular state dispatch, a particular state / a
# particular transition, calling a vcall (a method call, on a value, a particular vcall / a particular operation, based
# on a type (obj->10, a type, a particular value, a particular state))))" routine (a vcall dispatch, part of the
# 0x82188 cluster, a sibling of StateDispatch4_83220888). 87 lines, no direct callees (pure vcall dispatch, using bctrl
# ). No direct callers (it's called via a vtable / it is registered-but-uncalled, part of the game's subsystems). The
# object's class (obj, r4, ->0 = a value, ->4 = a value, ->10 = a type, a particular dispatch object, a particular
# vcall object, a particular structure) and the value's meaning (a, r3, a value, a particular argument) aren't
# resolvable, so the name states the mechanism: a vcall dispatch ((if a value, obj->0, is non-zero, a particular value)
# perform a vcall (a method call, on a value, a particular vcall / a particular operation), and (otherwise) perform a
# dispatch (a switch, over 6-7 states, a particular state dispatch, a particular state / a particular transition,
# calling a vcall (a method call, on a value, a particular vcall / a particular operation, based on a type (obj->10, a
# type, a particular value, a particular state)))).
[entrypoint.functions."0x82188680"]
name = "DispatchVcallType_82188680"

# A compute (a complex computation, with a count and a constant, the sibling of DispatchVcallType_82188680):
# (if a count, obj->9, is zero, a particular count / a particular state) return (a no-op / a return, a particular
# state), and (otherwise) compute a complex computation (a count, obj->9, a count, a particular count / a particular
# value, a constant, from a fixed .data address, a particular value, a particular result, a particular size / a
# particular allocation, using a base (obj->4, a base, a particular value, a particular source), and a byte (obj->4->31
# , a byte, a particular value, a particular state, a particular flag)).

# Args: a (r3, a value), obj (r4, ->4 = a base, ->9 = a count, a particular structure, a particular compute object),
# (more). It checks a count (r11, obj->9, a count, a particular count / a particular value): if it is zero (a
# particular count / a particular state, a particular value), it returns (a no-op / a return, a particular state, a
# particular result). If it is non-zero (a particular count / a particular state, a particular value), it computes a
# complex computation (a count, obj->9, a count, a particular count / a particular value, a constant, from a fixed
# .data address, a particular value, a particular result, a particular size / a particular allocation: it reads a base
# (r19, obj->4, a base, a particular value, a particular source), and a byte (r11, r19->31, a byte, a particular value
# , a particular state, a particular flag), and (if the byte is non-zero, a particular state, a particular flag), it
# loads a constant (f13, a constant, from a fixed .data address, a particular value, a particular constant), and
# (r22, a constant, from a fixed .data address, a particular value, a particular constant), and (r23, a constant, from a
# fixed .data address, a particular value, a particular constant), and (r24, a constant, from a fixed .data address, a
# particular value, a particular constant), and performs a complex computation (a count, obj->9, a count, a particular
# count / a particular value, a constant, from a fixed .data address, a particular value, a particular result, a
# particular size / a particular allocation, using a base (r19, obj->4, a base, a particular value, a particular
# source), and a byte (r11, r19->31, a byte, a particular value, a particular state, a particular flag))). So it's a
# "compute (a complex computation, with a count and a constant: (if a count, obj->9, is zero, a particular count / a
# particular state) return (a no-op / a return, a particular state), and (otherwise) compute a complex computation (a
# count, obj->9, a count, a particular count / a particular value, a constant, from a fixed .data address, a particular
# value, a particular result, a particular size / a particular allocation, using a base (obj->4, a base, a particular
# value, a particular source), and a byte (obj->4->31, a byte, a particular value, a particular state, a particular
# flag)))" routine (a compute, part of the 0x82188 cluster, a sibling of DispatchVcallType_82188680). 409 lines, no
# direct callees (pure computation, using constants from fixed .data addresses). No direct callers (it's called via a
# vtable / it is registered-but-uncalled, part of the game's subsystems). The object's class (obj, r4, ->4 = a base,
# ->9 = a count, a particular structure, a particular compute object, a particular range / a particular source) and the
# count's meaning (a count, obj->9, a particular count / a particular value) aren't resolvable, so the name states the
# mechanism: a complex computation (with a count and a constant) ((if a count, obj->9, is zero, a particular count / a
# particular state) return (a no-op / a return, a particular state), and (otherwise) compute a complex computation (a
# count, obj->9, a count, a particular count / a particular value, a constant, from a fixed .data address, a particular
# value, a particular result, a particular size / a particular allocation, using a base (obj->4, a base, a particular
# value, a particular source), and a byte (obj->4->31, a byte, a particular value, a particular state, a particular
# flag))).
[entrypoint.functions."0x821888C0"]
name = "ComputeFromCount_821888C0"

# A process (a complex computation, with a range and a divide, the sibling of ProcessRangeGrow_8217A858, the caller of
# sub_82A4E348 / sub_82A10E08): (if an index is <= a count (a range (obj->8 - obj->4, a size, a 12-byte element, a
# particular size), a count, a particular count), a particular threshold, a particular size) take one path (a no-op /
# a return, a particular state), and (otherwise) compute a complex computation (a range (obj->8 - obj->4, a size, a
# 12-byte element, a particular size), a count, a particular count, a divide, a particular size / a particular
# allocation, an offset, a particular value / a particular allocation, calling sub_82A4E348, a complex computation, a
# particular operation).

# Args: obj (r3, ->4 = a size, ->8 = a size, a particular structure, a range / a compute object), index (r4, r10, an
# index, a particular value, a particular count), (more). It checks an index (r4, r10, an index, a particular value, a
# particular count) against a count (r7, (obj->8 - obj->4) / 12, a size, a 12-byte element, a particular size, a count,
# a particular count, a particular threshold): if the index is <= the count (a particular threshold, a particular size,
# a particular count, a particular state), it takes one path (a no-op / a return, a particular state, a particular
# result). If the index is > the count (a particular threshold, a particular size, a particular count, a particular
# state), it computes a complex computation (a range (obj->8 - obj->4, a size, a 12-byte element, a particular size), a
# count, a particular count, a divide, a particular size / a particular allocation, an offset, a particular value / a
# particular allocation: it computes a range (r7, obj->8 - obj->4, a size, a 12-byte element, a particular size), and a
# count (r5, r7 / 12, a count, a 12-byte element, a particular count), and an offset (r5, r10 - r5, an offset, a
# particular value / a particular allocation), and calls a complex computation (sub_82A4E348, on a value (a, a value,
# a particular value, a particular offset), and a (b, a value, a particular value, a particular size), a particular
# operation, a particular size / a particular allocation, a particular range / a particular allocation). So it's a
# "process (a complex computation, with a range and a divide: (if an index is <= a count (a range (obj->8 - obj->4, a
# size, a 12-byte element, a particular size), a count, a particular count), a particular threshold, a particular
# size) take one path (a no-op / a return, a particular state), and (otherwise) compute a complex computation (a range
# (obj->8 - obj->4, a size, a 12-byte element, a particular size), a count, a particular count, a divide, a particular
# size / a particular allocation, an offset, a particular value / a particular allocation, calling sub_82A4E348, a
# complex computation, a particular operation))" routine (a range computation process, part of the 0x82A10 cluster, a
# sibling of ProcessRangeGrow_8217A858, a caller of sub_82A4E348 and sub_82A10E08). 109 lines, 2 callees
# (sub_82A10E08, sub_82A4E348). 2 callers: sub_83238600, sub_83238090 (siblings in the 0x83238 cluster). The object's
# class (obj, ->4 = a size, ->8 = a size, a particular structure, a range / a compute object, a particular range / a
# particular source) and the index's meaning (an index, a particular value, a particular count) aren't resolvable, so
# the name states the mechanism: a range computation process ((if an index is <= a count (a range (obj->8 - obj->4, a
# size, a 12-byte element, a particular size), a count, a particular count), a particular threshold, a particular
# size) take one path (a no-op / a return, a particular state), and (otherwise) compute a complex computation (a range
# (obj->8 - obj->4, a size, a 12-byte element, a particular size), a count, a particular count, a divide, a particular
# size / a particular allocation, an offset, a particular value / a particular allocation, calling sub_82A4E348, a
# complex computation, a particular operation)).
[entrypoint.functions."0x82A10BC8"]
name = "ProcessRangeCompute_82A10BC8"

# A process (a complex computation, with a range and a divide, a 12-byte element, the sibling of
# ProcessRangeCompute_82A10BC8, the caller of AllocateChecked_8264EFE0 / Free_SizeBucketed_Thunk_8221BE68):
# (if a size (obj->4, a size, a particular value) is non-zero, a particular state) compute a count (a range (obj->12 -
# obj->4, a size, a 12-byte element, a particular size), a count, a particular count, a divide, a particular size / a
# particular allocation), and (otherwise) compute a count (0, a zero / a particular state, a particular count), and
# (in paths) allocate (AllocateChecked_8264EFE0, on a value, a particular allocation, a particular size / a particular
# allocation), and free (Free_SizeBucketed_Thunk_8221BE68, on a value, a particular free, a particular size / a
# particular allocation).

# Args: obj (r3, r30, ->4 = a size, ->8 = a size, ->12 = a size, a particular structure, a range / a compute object), a
# (r4, a value), b (r5, r31, a value), c (r6, ->0 = a value, ->4 = a value, ->8 = a value, a particular source), (more
# ). It checks a size (r11, obj->4, a size, a particular value): if it is non-zero (a particular state, a particular
# value), it computes a count (r9, (obj->12 - obj->4) / 12, a size, a 12-byte element, a particular size, a count, a
# particular count, a divide, a particular size / a particular allocation, a particular range / a particular
# allocation); if it is zero (a null state, a particular state), it computes a count (0, a zero / a particular state,
# a particular count, a particular size / a particular allocation). In paths it allocates (AllocateChecked_8264EFE0, sub
# _8264EFE0, an allocation, on a value (a, a value, a particular value, a particular size), a particular allocation, a
# particular size / a particular allocation, a particular range / a particular allocation) and frees (Free_SizeBucketed
# _Thunk_8221BE68, sub_8221BE68, a free, on a value (a, a value, a particular value, a particular size), a particular
# free, a particular size / a particular allocation, a particular range / a particular allocation). So it's a "process
# (a complex computation, with a range and a divide, a 12-byte element: (if a size (obj->4, a size, a particular
# value) is non-zero, a particular state) compute a count (a range (obj->12 - obj->4, a size, a 12-byte element, a
# particular size), a count, a particular count, a divide, a particular size / a particular allocation), and
# (otherwise) compute a count (0, a zero / a particular state, a particular count), and (in paths) allocate
# (AllocateChecked_8264EFE0, on a value, a particular allocation, a particular size / a particular allocation), and
# free (Free_SizeBucketed_Thunk_8221BE68, on a value, a particular free, a particular size / a particular
# allocation))" routine (a range computation process, part of the 0x82A4E cluster, a sibling of ProcessRangeCompute
# _82A10BC8, a caller of AllocateChecked_8264EFE0 and Free_SizeBucketed_Thunk_8221BE68). 477 lines, 3 callees
# (AllocateChecked_8264EFE0, Free_SizeBucketed_Thunk_8221BE68, sub_82A97648). 2 callers: sub_82A4D6C8, sub_82A10BC8
# (ProcessRangeCompute, a range computation process, which calls this to compute the range / a count, a particular
# size / a particular allocation). The object's class (obj, ->4 = a size, ->8 = a size, ->12 = a size, a particular
# structure, a range / a compute object, a particular range / a particular source) and the value's meaning (a value, a
# particular value, a particular size) aren't resolvable, so the name states the mechanism: a range computation
# process ((if a size (obj->4, a size, a particular value) is non-zero, a particular state) compute a count (a range
# (obj->12 - obj->4, a size, a 12-byte element, a particular size), a count, a particular count, a divide, a particular
# size / a particular allocation), and (otherwise) compute a count (0, a zero / a particular state, a particular
# count), and (in paths) allocate (AllocateChecked_8264EFE0, on a value, a particular allocation, a particular size / a
# particular allocation), and free (Free_SizeBucketed_Thunk_8221BE68, on a value, a particular free, a particular size
# / a particular allocation)). The "2" distinguishes it from ProcessRangeCompute_82A10BC8.
[entrypoint.functions."0x82A4E348"]
name = "ProcessRangeCompute2_82A4E348"

# A process (a complex computation, with a range and a divide, a 20-byte element, the sibling of
# ProcessRangeCompute_82A10BC8 / ProcessRangeCompute2_82A4E348, the caller of SetValue2_8236EC48 / sub_82C676C8):
# (if a size (obj->4, a size, a particular value) is non-zero, a particular state) compute a count (a range (obj->8 -
# obj->4, a size, a 20-byte element, a particular size), a count, a particular count, a divide, a particular size / a
# particular allocation), and (otherwise) compute a count (0, a zero / a particular state, a particular count), and
# (if the count is < another count (a range (obj->12 - obj->4, a size, a 20-byte element, a particular size), a count
# , a particular count, a divide, a particular size / a particular allocation), a particular threshold, a particular
# size) take one path (a no-op / a return, a particular state), and (in paths) set a value (SetValue2_8236EC48, on a
# value, a particular set, a particular value / a particular state).

# Args: obj (r3, r31, ->4 = a size, ->8 = a size, ->12 = a size, a particular structure, a range / a compute object), a
# (r4, a value), (more). It checks a size (r11, obj->4, a size, a particular value): if it is non-zero (a particular
# state, a particular value), it computes a count (r10, (obj->8 - obj->4) / 20, a size, a 20-byte element, a particular
# size, a count, a particular count, a divide, a particular size / a particular allocation, a particular range / a
# particular allocation); if it is zero (a null state, a particular state), it computes a count (0, a zero / a
# particular state, a particular count, a particular size / a particular allocation). It computes another count (r5,
# (obj->12 - obj->4) / 20, a size, a 20-byte element, a particular size, a count, a particular count, a divide, a
# particular size / a particular allocation, a particular range / a particular allocation). If the count (r10) is < the
# other count (r5) (a particular threshold, a particular size, a particular count, a particular state), it takes one
# path (a no-op / a return, a particular state, a particular result). In paths it sets a value (SetValue2_8236EC48, sub
# _8236EC48, a set, on a value (a, a value, a particular value, a particular state), a particular set, a particular
# value / a particular state, a particular size / a particular allocation). So it's a "process (a complex computation,
# with a range and a divide, a 20-byte element: (if a size (obj->4, a size, a particular value) is non-zero, a
# particular state) compute a count (a range (obj->8 - obj->4, a size, a 20-byte element, a particular size), a count,
# a particular count, a divide, a particular size / a particular allocation), and (otherwise) compute a count (0, a
# zero / a particular state, a particular count), and (if the count is < another count (a range (obj->12 - obj->4, a
# size, a 20-byte element, a particular size), a count, a particular count, a divide, a particular size / a particular
# allocation), a particular threshold, a particular size) take one path (a no-op / a return, a particular state), and
# (in paths) set a value (SetValue2_8236EC48, on a value, a particular set, a particular value / a particular
# state))" routine (a range computation process, part of the 0x82C67 cluster, a sibling of ProcessRangeCompute
# _82A10BC8 and ProcessRangeCompute2_82A4E348, a caller of SetValue2_8236EC48 and sub_82C676C8). 115 lines, 2 callees
# (SetValue2_8236EC48, sub_82C676C8). 1 caller: sub_82C67878 (a sibling in the 0x82C67 cluster, a range computation
# process, which calls this to compute the range / a count, a particular size / a particular allocation). The
# object's class (obj, ->4 = a size, ->8 = a size, ->12 = a size, a particular structure, a range / a compute object, a
# particular range / a particular source) and the value's meaning (a value, a particular value, a particular size)
# aren't resolvable, so the name states the mechanism: a range computation process ((if a size (obj->4, a size, a
# particular value) is non-zero, a particular state) compute a count (a range (obj->8 - obj->4, a size, a 20-byte
# element, a particular size), a count, a particular count, a divide, a particular size / a particular allocation), and
# (otherwise) compute a count (0, a zero / a particular state, a particular count), and (if the count is < another
# count (a range (obj->12 - obj->4, a size, a 20-byte element, a particular size), a count, a particular count, a
# divide, a particular size / a particular allocation), a particular threshold, a particular size) take one path (a no
# -op / a return, a particular state), and (in paths) set a value (SetValue2_8236EC48, on a value, a particular set, a
# particular value / a particular state)). The "3" distinguishes it from ProcessRangeCompute_82A10BC8 and
# ProcessRangeCompute2_82A4E348.
[entrypoint.functions."0x82C677B0"]
name = "ProcessRangeCompute3_82C677B0"

# An atomic decrement (the sibling of AtomicIncrement_822CBEB8 / AtomicIncrement2_82375F28 / AtomicIncrement3_821F7798
# / AtomicIncrement4_82B82440, with a global lock and a complex computation): perform an atomic decrement (a global
# lock, a particular lock / a particular state, using lwarx/stwcx, a particular atomic operation, on a value (obj->8,
# a value, a reference count, a particular count), decrementing it by 1, a particular count / a particular state), and
# (in paths) perform a complex computation (a reference count, a particular count / a particular state, a particular
# size / a particular allocation).

# Args: obj (r3, ->0 = a value, ->8 = a reference count, a particular structure, a reference counted object), (more). It
# performs an atomic decrement (a global lock, a particular lock / a particular state, using lwarx/stwcx, a particular
# atomic operation, on a value (obj->8, a value, a reference count, a particular count), decrementing it by 1, a
# particular count / a particular state: it saves the msr (mfmsr r9, a particular state, a particular lock), and
# disables interrupts (mtmsrd r13,1, a particular state, a particular lock), and atomically loads the count (lwarx r10
# ,0,r11, a value, a reference count, a particular count, a particular atomic operation), and decrements it (r10 - 1,
# a decrement, a particular count / a particular state), and atomically stores it (stwcx. r10,0,r11, a value, a
# reference count, a particular count, a particular atomic operation), and restores the msr (mtmsrd r9,1, a
# particular state, a particular lock), and (if the store failed, a particular state, a particular lock) retries the
# operation (goto loop, a retry, a particular state / a particular operation, a particular atomic operation)). In paths
# it performs a complex computation (a reference count, a particular count / a particular state, a particular size / a
# particular allocation: it reads a value (r3->0, a value, a particular value), and (if the count is zero, a
# particular state, a particular count), it takes one path (a no-op / a return, a particular state, a particular
# result)). So it's a "perform an atomic decrement (a global lock, a particular lock / a particular state, using
# lwarx/stwcx, a particular atomic operation, on a value (obj->8, a value, a reference count, a particular count),
# decrementing it by 1, a particular count / a particular state), and (in paths) perform a complex computation (a
# reference count, a particular count / a particular state, a particular size / a particular allocation))" routine (an
# atomic decrement, part of the 0x822C7 cluster, a sibling of AtomicIncrement_822CBEB8). 77 lines, no direct callees
# (pure atomic operation, using lwarx/stwcx with a global lock, a particular atomic operation). 1 caller: sub
# _82B4F370 (a sibling in the 0x82B4F cluster, a process / a destructor, which calls this to decrement the reference
# count, a particular count / a particular state, a particular size / a particular allocation). The object's class
# (obj, ->0 = a value, ->8 = a reference count, a particular structure, a reference counted object, a particular value
# / a particular count) and the value's meaning (a value, a reference count, a particular count) aren't resolvable, so
# the name states the mechanism: an atomic decrement (a global lock, a particular lock / a particular state, using
# lwarx/stwcx, a particular atomic operation, on a value (obj->8, a value, a reference count, a particular count),
# decrementing it by 1, a particular count / a particular state).
[entrypoint.functions."0x822C72E0"]
name = "AtomicDecrement_822C72E0"

# A process (a loop, with a state dispatch and a complex computation, the caller of StateDispatch_82BCBB40):
# loop (a state dispatch, a particular state dispatch / a particular state, a complex computation, a particular
# computation / a particular operation, a particular size / a particular allocation, calling StateDispatch_82BCBB40, a
# state dispatch, a particular state / a particular transition), and (in paths) perform a complex computation (a count
# , a particular count / a particular value, a size, a particular size / a particular allocation, a particular
# operation).

# Args: obj (r3, r29, ->16 = a value, a particular structure, a dispatch object), (more). It loops (a state dispatch,
# a particular state dispatch / a particular state, a complex computation, a particular computation / a particular
# operation, a particular size / a particular allocation): it reads a value (r31, obj->16, a value, a particular value
# , a dispatch object), and computes a count (r11, r31->84, a count, a particular count / a particular value), and a
# size (r10, r11 * 4, a size, a 4-byte element, a particular size), and a size (r11, r11 + r10, a size, a 3x size, a
# particular size), and a size (r30, r11 * 2, a size, a 2-byte element, a particular size), and (if the size is non
# -zero, a particular state, a particular size), it takes one path (a particular state / a particular operation); if it
# is zero (a null state, a particular state), it loads a constant (r30, a constant, from a fixed .data address, a
# particular value, a particular size, a particular state). It reads a value (r11, r31->64, a value, a particular
# value), and a value (r9, r31->68, a value, a particular value), and a value (r10, r31->76, a value, a particular
# value), and computes a size (r11, r9 - r11, a size, a particular size), and a size (r8, r11 + r10, a size, a
# particular size), and stores it (r31->76, a store, a particular size, a particular value / a particular state). In
# each iteration it performs a state dispatch (StateDispatch_82BCBB40, sub_82BCBB40, a state dispatch, on a value (obj
# , r29, a particular structure, a dispatch object), a particular state / a particular transition, a particular
# operation). So it's a "process (a loop, with a state dispatch and a complex computation: loop (a state dispatch, a
# particular state dispatch / a particular state, a complex computation, a particular computation / a particular
# operation, a particular size / a particular allocation, calling StateDispatch_82BCBB40, a state dispatch, a
# particular state / a particular transition), and (in paths) perform a complex computation (a count, a particular
# count / a particular value, a size, a particular size / a particular allocation, a particular operation))" routine (a
# loop process, part of the 0x8227B cluster, a caller of StateDispatch_82BCBB40). 121 lines, 1 callee
# (StateDispatch_82BCBB40, sub_82BCBB40, a state dispatch). 10 callers: sub_82BDA8F0, sub_82306698, sub_82BD8990, sub
# _82BC71C0, sub_82BD7570, sub_8219CDA8, sub_82BC5A88, sub_82BB5FD0, sub_82BC05C8, sub_82BB8790 (siblings in the
# 0x82BDA/0x82306/0x82BD8/0x82BC7/0x82BD7/0x8219C/0x82BC5/0x82BB5/0x82BC0/0x82BB8 clusters). The object's class (obj,
# r29, ->16 = a value, a particular structure, a dispatch object, a particular state / a particular value) and the
# value's meaning (a value, a particular value, a dispatch object) aren't resolvable, so the name states the
# mechanism: a loop process (with a state dispatch and a complex computation) (loop (a state dispatch, a particular
# state dispatch / a particular state, a complex computation, a particular computation / a particular operation, a
# particular size / a particular allocation, calling StateDispatch_82BCBB40, a state dispatch, a particular state / a
# particular transition), and (in paths) perform a complex computation (a count, a particular count / a particular
# value, a size, a particular size / a particular allocation, a particular operation)).
[entrypoint.functions."0x8227B8B8"]
name = "ProcessLoopDispatch_8227B8B8"

# A find (with a spin lock and a list traversal, the sibling of HashTable_Find3_821CF1D0 / the caller of
# SpinLock_Acquire_822D7408, the callee of Pool_FreeNode_83230320): acquire a spin lock (SpinLock_Acquire_822D7408, on
# a value (obj + 32, a value, a particular lock, a particular state, a particular size / a particular allocation), a
# particular lock / a particular state, a particular size / a particular allocation), and (if a list (obj->12, a list,
# a particular list / a particular sequence, a particular size / a particular allocation) is non-zero, a particular
# state) traverse the list (a list traversal, a particular traversal / a particular sequence, a particular size / a
# particular allocation: loop over a list (r30, a list, a particular list / a particular sequence, a particular size /
# a particular allocation), and for each node (r30, a node, a particular node / a particular value, a particular size
# / a particular allocation), (if the node's key (r30->0, a key, a particular value, a particular key) matches a masked
# key (r29 & ~(obj->4 - 1), a masked key, a particular key / a particular value, a particular mask, a particular state
# ), a particular match / a particular state), return the node (a found, a particular match / a particular state, a
# particular node / a particular value), and (otherwise) continue to the next node (r30->8, a next, a particular
# node / a particular value, a particular size / a particular allocation)), and (otherwise) take one path (a no-op / a
# return, a particular state, a particular result).

# Args: obj (r3, r31, ->4 = a size, ->12 = a list, a particular structure, a list / a hash object), key (r4, r29, a
# key, a particular value, a particular key), (more). It acquires a spin lock (SpinLock_Acquire_822D7408, sub
# _822D7408, a spin lock, on a value (obj + 32, a value, a particular lock, a particular state, a particular size / a
# particular allocation), a particular lock / a particular state, a particular size / a particular allocation, a
# particular state / a particular operation). It checks a list (r30, obj->12, a list, a particular list / a particular
# sequence, a particular size / a particular allocation): if it is non-zero (a particular state, a particular list / a
# particular sequence), it traverses the list (a list traversal, a particular traversal / a particular sequence, a
# particular size / a particular allocation: it computes a mask (r11, obj->4 - 1, a mask, a particular mask / a
# particular value, a particular state, a particular size / a particular allocation), and a masked key (r28, r29 &
# ~r11, a masked key, a particular key / a particular value, a particular mask, a particular state, a particular size
# / a particular allocation), and loops over a list (r30, a list, a particular list / a particular sequence, a
# particular size / a particular allocation): for each node (r30, a node, a particular node / a particular value, a
# particular size / a particular allocation), it checks the node's key (r11, r30->0, a key, a particular value, a
# particular key) against the masked key (r28, a masked key, a particular key / a particular value, a particular mask,
# a particular state): if it matches (a particular match / a particular state, a particular key / a particular value,
# a particular mask, a particular size / a particular allocation), it returns the node (a found, a particular match /
# a particular state, a particular node / a particular value, a particular size / a particular allocation); if it
# doesn't match (a particular miss / a particular state, a particular key / a particular value, a particular mask, a
# particular size / a particular allocation), it continues to the next node (r30, r30->8, a next, a particular node /
# a particular value, a particular size / a particular allocation, a particular state / a particular operation). If the
# list is zero (a null state, a particular state, a particular list / a particular sequence), it takes one path (a no
# -op / a return, a particular state, a particular result, a particular size / a particular allocation). So it's a
# "find (with a spin lock and a list traversal: acquire a spin lock (SpinLock_Acquire_822D7408, on a value (obj + 32, a
# value, a particular lock, a particular state, a particular size / a particular allocation), a particular lock / a
# particular state, a particular size / a particular allocation), and (if a list (obj->12, a list, a particular list /
# a particular sequence, a particular size / a particular allocation) is non-zero, a particular state) traverse the
# list (a list traversal, a particular traversal / a particular sequence, a particular size / a particular allocation:
# loop over a list (r30, a list, a particular list / a particular sequence, a particular size / a particular
# allocation), and for each node (r30, a node, a particular node / a particular value, a particular size / a
# particular allocation), (if the node's key (r30->0, a key, a particular value, a particular key) matches a masked
# key (r29 & ~(obj->4 - 1), a masked key, a particular key / a particular value, a particular mask, a particular
# state), a particular match / a particular state), return the node (a found, a particular match / a particular state,
# a particular node / a particular value), and (otherwise) continue to the next node (r30->8, a next, a particular
# node / a particular value, a particular size / a particular allocation)), and (otherwise) take one path (a no-op / a
# return, a particular state, a particular result))" routine (a find, with a spin lock and a list traversal, part of
# the 0x83235 cluster, a sibling of HashTable_Find3_821CF1D0, a caller of SpinLock_Acquire_822D7408, a callee of
# Pool_FreeNode_83230320). 217 lines, 4 callees (SpinLock_Acquire_822D7408, sub_829FD240, sub_8322FB30, sub_8322FDB0)
# . 1 caller: Pool_FreeNode_83230320 (sub_83230320, a pool free, which calls this to find a node in the list, a
# particular match / a particular state, a particular node / a particular value, a particular size / a particular
# allocation). The object's class (obj, ->4 = a size, ->12 = a list, a particular structure, a list / a hash object, a
# particular list / a particular sequence) and the key's meaning (a key, a particular value, a particular key) aren't
# resolvable, so the name states the mechanism: a find (with a spin lock and a list traversal) (acquire a spin lock
# (SpinLock_Acquire_822D7408, on a value (obj + 32, a value, a particular lock, a particular state, a particular size
# / a particular allocation), a particular lock / a particular state, a particular size / a particular allocation), and
# (if a list (obj->12, a list, a particular list / a particular sequence, a particular size / a particular allocation)
# is non-zero, a particular state) traverse the list (a list traversal, a particular traversal / a particular
# sequence, a particular size / a particular allocation: loop over a list (r30, a list, a particular list / a
# particular sequence, a particular size / a particular allocation), and for each node (r30, a node, a particular node
# / a particular value, a particular size / a particular allocation), (if the node's key (r30->0, a key, a particular
# value, a particular key) matches a masked key (r29 & ~(obj->4 - 1), a masked key, a particular key / a particular
# value, a particular mask, a particular state), a particular match / a particular state), return the node (a found, a
# particular match / a particular state, a particular node / a particular value), and (otherwise) continue to the next
# node (r30->8, a next, a particular node / a particular value, a particular size / a particular allocation)), and
# (otherwise) take one path (a no-op / a return, a particular state, a particular result)).
[entrypoint.functions."0x83235FC0"]
name = "FindNodeInList_83235FC0"

# A validate (a bounds check, with a 16-byte element and a complex computation, the sibling of ValidateBounds4_82BEC30
# 0, the caller of sub_82CD12C8): (if a size (obj->20, a size, a particular value) is < a value (a, a value, a
# particular value), a particular threshold, a particular size) call a complex computation (sub_82CD12C8, a complex
# computation, a particular computation / a particular operation), and (otherwise) compute a bounds check (a size
# (obj->20 - a, a size, a particular size), a count, a particular count, a particular threshold, a particular size, a
# particular size / a particular allocation, a 16-byte element, a particular size / a particular allocation, a
# particular operation).

# Args: obj (r3, r31, ->20 = a size, ->24 = a size, a particular structure, a range / a bounds object), a (r4, r30, a
# value, a particular value, a particular size / a particular allocation), b (r5, r29, a value, a particular value, a
# particular size / a particular allocation), c (r6, r28, a value, a particular value, a particular size / a
# particular allocation), d (r7, r27, a value, a particular value, a particular size / a particular allocation), (more
# ). It checks a size (r11, obj->20, a size, a particular value, a particular size / a particular allocation) against a
# value (a, r30, a value, a particular value, a particular size / a particular allocation): if the size is < the value
# (a particular threshold, a particular size, a particular value, a particular state, a particular size / a particular
# allocation), it calls a complex computation (sub_82CD12C8, a complex computation, a particular computation / a
# particular operation, a particular size / a particular allocation); if the size is >= the value (a particular
# threshold, a particular size, a particular value, a particular state, a particular size / a particular allocation), it
# takes one path (a no-op / a continue, a particular state, a particular result, a particular size / a particular
# allocation). It computes a bounds check (a size (obj->20 - a, a size, a particular size, a particular size / a
# particular allocation), a count, a particular count, a particular threshold, a particular size, a particular size / a
# particular allocation, a 16-byte element, a particular size / a particular allocation: it computes a size (r11, obj
# ->20 - r30, a size, a particular size, a particular size / a particular allocation), and (if the size is < b (r29, a
# value, a particular value, a particular size / a particular allocation), a particular threshold, a particular size, a
# particular value, a particular state, a particular size / a particular allocation), it stores it (r29, a value, a
# particular value, a particular size / a particular allocation, a particular state / a particular operation); if the
# size is >= b (a particular threshold, a particular size, a particular value, a particular state, a particular size / a
# particular allocation), it takes one path (a no-op / a continue, a particular state, a particular result, a
# particular size / a particular allocation). It checks a size (r29, a value, a particular value, a particular size / a
# particular allocation) against d (r27, a value, a particular value, a particular size / a particular allocation): if
# it is < d (a particular threshold, a particular size, a particular value, a particular state, a particular size / a
# particular allocation), it stores it (r8, a value, a particular value, a particular size / a particular allocation, a
# particular state / a particular operation); if it is >= d (a particular threshold, a particular size, a particular
# value, a particular state, a particular size / a particular allocation), it stores d (r8, d, a value, a particular
# value, a particular size / a particular allocation, a particular state / a particular operation). So it's a "validate
# (a bounds check, with a 16-byte element and a complex computation: (if a size (obj->20, a size, a particular value)
# is < a value (a, a value, a particular value), a particular threshold, a particular size) call a complex computation
# (sub_82CD12C8, a complex computation, a particular computation / a particular operation), and (otherwise) compute a
# bounds check (a size (obj->20 - a, a size, a particular size), a count, a particular count, a particular threshold, a
# particular size, a particular size / a particular allocation, a 16-byte element, a particular size / a particular
# allocation, a particular operation))" routine (a bounds check, with a 16-byte element and a complex computation, part
# of the 0x82BEB cluster, a sibling of ValidateBounds4_82BEC300, a caller of sub_82CD12C8). 128 lines, 1 callee (sub
# _82CD12C8, a complex computation). 8 callers: sub_82BEFC70, sub_82BF1518, sub_82BF04F8, sub_82BEC778, sub_82BF16E0,
# sub_82BEC8B0, sub_82BEC640, sub_82BF1268 (siblings in the 0x82BEC/0x82BEF/0x82BF1/0x82BF0 clusters). The object's
# class (obj, ->20 = a size, ->24 = a size, a particular structure, a range / a bounds object, a particular size / a
# particular allocation) and the value's meaning (a value, a particular value, a particular size / a particular
# allocation) aren't resolvable, so the name states the mechanism: a bounds check (with a 16-byte element and a complex
# computation) ((if a size (obj->20, a size, a particular value) is < a value (a, a value, a particular value), a
# particular threshold, a particular size) call a complex computation (sub_82CD12C8, a complex computation, a
# particular computation / a particular operation), and (otherwise) compute a bounds check (a size (obj->20 - a, a size,
# a particular size), a count, a particular count, a particular threshold, a particular size, a particular size / a
# particular allocation, a 16-byte element, a particular size / a particular allocation, a particular operation)). The
# "16" refers to the 16-byte element (a 16-byte element, a particular size / a particular allocation).
[entrypoint.functions."0x82BEBD20"]
name = "ValidateBounds16_82BEBD20"

# A 24-byte constructor (the sibling of Construct24Byte_82B5D768 / Construct32Byte_82B722E8, with a copy assign and a
# complex computation, the caller of CopyAssign_RefCounted_821F0108 / AllocateChecked_8264EFE0 / CopyRecords12_82519F2
# 8 / DestructRefCounted_82214F08 / Free_SizeBucketed_Thunk_8221BE68): initialize the object's fields (a 24-byte
# object, a particular structure, a particular size / a particular allocation, copying a 24-byte element (a float, a
# value, a value, a particular 24-byte element) from a source (c, a source, a 24-byte element, a particular value) to a
# stack (a particular target), a particular copy / a particular allocation), and perform a complex computation (a
# count, obj->4, a count, a particular count / a particular value, a particular size / a particular allocation, a
# particular operation, calling CopyAssign_RefCounted_821F0108, a copy assign, a particular copy / a particular
# allocation, AllocateChecked_8264EFE0, an allocation, a particular allocation / a particular size, CopyRecords12_8251
# 9F28, a copy, a particular copy / a particular size, DestructRefCounted_82214F08, a destruct, a particular destruct
# / a particular size, Free_SizeBucketed_Thunk_8221BE68, a free, a particular free / a particular size).

# Args: obj (r3, r31, ->4 = a count, a particular structure, a 24-byte object, a particular size / a particular
# allocation), a (r4, a value), b (r5, r30, a value), c (r6, r29, ->0 = a float, ->4 = a value, ->8 = a value, a
# 24-byte element, a particular source, a particular size / a particular allocation), (more). It initializes the
# object's fields (a 24-byte object, a particular structure, a particular size / a particular allocation: it copies a
# 24-byte element (a float (f0, c->0, a float, a 24-byte element, a particular value), a value (r4, c + 4, a value, a
# 24-byte element, a particular value), a value (r5, c + 8, a value, a 24-byte element, a particular value), a
# particular 24-byte element) from a source (c, r29, a 24-byte element, a particular value, a particular source, a
# particular size / a particular allocation) to a stack (stack[80], a value, a 24-byte element, a particular value, a
# particular target, a particular copy / a particular allocation), and calls a copy assign (CopyAssign_RefCounted
# _821F0108, sub_821F0108, a copy assign, on a value (stack[84], a value, a 24-byte element, a particular value, a
# particular target), and c + 4, a value, a 24-byte element, a particular value, a particular source), a particular
# copy / a particular allocation, a particular size / a particular allocation), and (CopyAssign_RefCounted_821F0108,
# sub_821F0108, a copy assign, on a value (stack[88], a value, a 24-byte element, a particular value, a particular
# target), and c + 8, a value, a 24-byte element, a particular value, a particular source), a particular copy / a
# particular allocation, a particular size / a particular allocation)). It performs a complex computation (a count
# (obj->4, a count, a particular count / a particular value, a particular size / a particular allocation, a particular
# operation): it checks a count (r11, obj->4, a count, a particular count / a particular value, a particular size / a
# particular allocation): if it is non-zero (a particular state, a particular count / a particular value, a particular
# size / a particular allocation), it takes one path (a no-op / a continue, a particular state, a particular result, a
# particular size / a particular allocation); if it is zero (a null state, a particular state, a particular count / a
# particular value, a particular size / a particular allocation), it takes another path (a no-op / a continue, a
# particular state, a particular result, a particular size / a particular allocation). In paths it allocates
# (AllocateChecked_8264EFE0, sub_8264EFE0, an allocation, on a value (a, a value, a particular value, a particular
# size, a particular size / a particular allocation), a particular allocation / a particular size, a particular size /
# a particular allocation), and copies (CopyRecords12_82519F28, sub_82519F28, a copy, on a value (a, a value, a
# particular value, a particular size, a particular size / a particular allocation), a particular copy / a particular
# size, a particular size / a particular allocation), and destructs (DestructRefCounted_82214F08, sub_82214F08, a
# destruct, on a value (a, a value, a particular value, a particular size, a particular size / a particular
# allocation), a particular destruct / a particular size, a particular size / a particular allocation), and frees (Free
# _SizeBucketed_Thunk_8221BE68, sub_8221BE68, a free, on a value (a, a value, a particular value, a particular size, a
# particular size / a particular allocation), a particular free / a particular size, a particular size / a particular
# allocation). So it's a "construct a 24-byte object (initialize the fields, a 24-byte object, a particular structure,
# a particular size / a particular allocation, copying a 24-byte element (a float, a value, a value, a particular 24-
# byte element) from a source (c, a source, a 24-byte element, a particular value) to a stack (a particular target), a
# particular copy / a particular allocation), and perform a complex computation (a count, obj->4, a count, a
# particular count / a particular value, a particular size / a particular allocation, a particular operation, calling
# CopyAssign_RefCounted_821F0108, a copy assign, a particular copy / a particular allocation, AllocateChecked
# _8264EFE0, an allocation, a particular allocation / a particular size, CopyRecords12_82519F28, a copy, a particular
# copy / a particular size, DestructRefCounted_82214F08, a destruct, a particular destruct / a particular size, Free
# _SizeBucketed_Thunk_8221BE68, a free, a particular free / a particular size))" routine (a 24-byte constructor, with a
# copy assign and a complex computation, part of the 0x821E3 cluster, a sibling of Construct24Byte_82B5D768, a caller
# of CopyAssign_RefCounted_821F0108, AllocateChecked_8264EFE0, CopyRecords12_82519F28, DestructRefCounted_82214F08, and
# Free_SizeBucketed_Thunk_8221BE68). 313 lines, 10 callees (AllocateChecked_8264EFE0, CopyAssign_RefCounted_821F0108,
# CopyRecords12_82519F28, DestructRefCounted_82214F08, Free_SizeBucketed_Thunk_8221BE68, sub_82518D30, sub_82518DC8, sub
# _82518E28, sub_829EAB40, sub_82A97648). 2 callers: sub_821A0EF8, AtomicIncrement4_82B82440 (sub_82B82440, an atomic
# increment, which calls this to construct a 24-byte object, a particular size / a particular allocation, a particular
# operation). The object's class (obj, ->4 = a count, a particular structure, a 24-byte object, a particular size / a
# particular allocation, a particular value / a particular count) and the source's meaning (a 24-byte element, a
# particular value, a particular source, a particular size / a particular allocation) aren't resolvable, so the name
# states the mechanism: a 24-byte constructor (with a copy assign and a complex computation) (initialize the fields, a
# 24-byte object, a particular structure, a particular size / a particular allocation, copying a 24-byte element (a
# float, a value, a value, a particular 24-byte element) from a source (c, a source, a 24-byte element, a particular
# value) to a stack (a particular target), a particular copy / a particular allocation), and perform a complex
# computation (a count, obj->4, a count, a particular count / a particular value, a particular size / a particular
# allocation, a particular operation)). The "Copy" refers to the copy assign (a copy assign, a particular copy / a
# particular allocation, a particular size / a particular allocation).
[entrypoint.functions."0x821E3B38"]
name = "Construct24ByteCopy_821E3B38"

# A destruct (a range destruct, with a release and an atomic decrement, the sibling of DestructRefCounted_82214F08, the
# caller of Release_RefCounted_821C67D8): (if a start (a value, a particular value, a particular pointer / a particular
# state) is == an end (a value, a particular value, a particular pointer / a particular state), a particular match / a
# particular state) return (a no-op / a return, a particular state, a particular result, a particular size / a
# particular allocation), and (otherwise) destruct a range (a range, a particular range / a particular allocation, a
# release, a particular release / a particular count, an atomic decrement, a particular decrement / a particular
# count, a particular size / a particular allocation, a particular operation, calling Release_RefCounted_821C67D8, a
# release, a particular release / a particular count, a particular size / a particular allocation).

# Args: start (r3, a value, a particular value, a particular pointer / a particular state), end (r4, r28, a value, a
# particular value, a particular pointer / a particular state), (more). It checks a start (r3, a value, a particular
# value, a particular pointer / a particular state) against an end (r28, a value, a particular value, a particular
# pointer / a particular state): if the start is == the end (a particular match / a particular state, a particular
# value, a particular pointer / a particular state), it returns (a no-op / a return, a particular state, a particular
# result, a particular size / a particular allocation); if the start is != the end (a particular miss / a particular
# state, a particular value, a particular pointer / a particular state), it destructs a range (a range, a particular
# range / a particular allocation, a release, a particular release / a particular count, an atomic decrement, a
# particular decrement / a particular count, a particular size / a particular allocation, a particular operation: it
# computes a pointer (r31, start + 4, a pointer, a particular value, a particular pointer / a particular state), and a
# constant (r30, a constant, from a fixed .data address, a particular value, a particular count, a particular pointer /
# a particular state), and loops (a range, a particular range / a particular allocation, a release, a particular
# release / a particular count, an atomic decrement, a particular decrement / a particular count, a particular size / a
# particular allocation, a particular operation: it releases a refcounted value (Release_RefCounted_821C67D8, sub
# _821C67D8, a release, on a value (r31 + 4, a pointer, a particular value, a particular pointer / a particular
# state), a particular release / a particular count, a particular size / a particular allocation), and performs an
# atomic decrement (a global lock, a particular lock / a particular state, using lwarx/stwcx, a particular atomic
# operation, on a value (r30, a constant, from a fixed .data address, a particular value, a particular count, a
# particular pointer / a particular state), decrementing it by 1, a particular decrement / a particular count, a
# particular size / a particular allocation, a particular operation). So it's a "destruct a range (a range, a
# particular range / a particular allocation, a release, a particular release / a particular count, an atomic
# decrement, a particular decrement / a particular count, a particular size / a particular allocation, a particular
# operation, calling Release_RefCounted_821C67D8, a release, a particular release / a particular count, a particular
# size / a particular allocation)" routine (a range destructor, part of the 0x82518 cluster, a sibling of
# DestructRefCounted_82214F08, a caller of Release_RefCounted_821C67D8). 113 lines, 1 callee (Release_RefCounted
# _821C67D8, sub_821C67D8, a release). 10 callers: AtomicIncrement4_82B82440, sub_829E9CD0, sub_82200B28, sub_828BFFF8,
# sub_821F03D8, sub_82B82860, sub_8269F528, sub_829EABB8, sub_821F01B8, sub_82200988 (a widely-called range
# destructor, part of the game's reference counting subsystem). The start's and end's meaning (a value, a particular
# value, a particular pointer / a particular state, a particular range / a particular allocation) aren't resolvable, so
# the name states the mechanism: a range destructor (with a release and an atomic decrement) ((if a start (a value, a
# particular value, a particular pointer / a particular state) is == an end (a value, a particular value, a particular
# pointer / a particular state), a particular match / a particular state) return (a no-op / a return, a particular
# state, a particular result, a particular size / a particular allocation), and (otherwise) destruct a range (a range,
# a particular range / a particular allocation, a release, a particular release / a particular count, an atomic
# decrement, a particular decrement / a particular count, a particular size / a particular allocation, a particular
# operation, calling Release_RefCounted_821C67D8, a release, a particular release / a particular count, a particular
# size / a particular allocation)).
[entrypoint.functions."0x82518D30"]
name = "DestructRange_82518D30"

# A process (a grow/allocate, with a size check and a complex computation, the sibling of ProcessSizeCompute_825BBAD0
# / the caller of AllocateChecked_8264EFE0): (if a size (a value, a particular value, a particular size / a particular
# allocation) is > a threshold (a constant, from a fixed .data address, a particular value, a particular threshold, a
# particular size / a particular allocation), a particular threshold, a particular size) call a complex computation
# (sub_82A97648, a complex computation, a particular computation / a particular operation, a particular size / a
# particular allocation), and (otherwise) grow/allocate (a size, a particular size / a particular allocation, a
# particular size / a particular allocation, a particular operation, calling AllocateChecked_8264EFE0, an allocation, a
# particular allocation / a particular size, a particular size / a particular allocation), and store the result (a
# value, a particular value, a particular size / a particular allocation, a particular operation, obj->4, obj->8, a
# store, a particular value / a particular state, a particular size / a particular allocation, obj->12, a size, a
# particular size / a particular allocation, a particular value / a particular state).

# Args: obj (r3, r31, ->4 = a value, ->8 = a value, ->12 = a size, a particular structure, a grow / a range object, a
# particular size / a particular allocation), size (r4, r30, a value, a particular value, a particular size / a
# particular allocation), (more). It checks a size (r30, a value, a particular value, a particular size / a particular
# allocation) against a threshold (r10, a constant, from a fixed .data address, a particular value, a particular
# threshold, a particular size / a particular allocation): if the size is > the threshold (a particular threshold, a
# particular size, a particular value, a particular state, a particular size / a particular allocation), it calls a
# complex computation (sub_82A97648, a complex computation, a particular computation / a particular operation, a
# particular size / a particular allocation, a particular state / a particular result); if the size is <= the
# threshold (a particular threshold, a particular size, a particular value, a particular state, a particular size / a
# particular allocation), it grows/allocates (a size, a particular size / a particular allocation, a particular size /
# a particular allocation, a particular operation: it calls an allocation (AllocateChecked_8264EFE0, sub_8264EFE0, an
# allocation, on a value (obj, r31, a particular structure, a grow / a range object, a particular size / a particular
# allocation), and a (r30, a value, a particular value, a particular size / a particular allocation), a particular
# allocation / a particular size, a particular size / a particular allocation), and computes a size (r10, a value, a
# particular value, a particular size / a particular allocation, a particular size / a particular allocation), and a
# size (r9, a value, a particular value, a particular size / a particular allocation, a particular size / a
# particular allocation), and stores the result (obj->4, a store, a particular value / a particular state, a
# particular size / a particular allocation, obj->8, a store, a particular value / a particular state, a particular
# size / a particular allocation, obj->12, a size, a particular size / a particular allocation, a particular value / a
# particular state). So it's a "process (a grow/allocate, with a size check and a complex computation: (if a size (a
# value, a particular value, a particular size / a particular allocation) is > a threshold (a constant, from a fixed
# .data address, a particular value, a particular threshold, a particular size / a particular allocation), a
# particular threshold, a particular size) call a complex computation (sub_82A97648, a complex computation, a
# particular computation / a particular operation, a particular size / a particular allocation), and (otherwise)
# grow/allocate (a size, a particular size / a particular allocation, a particular size / a particular allocation, a
# particular operation, calling AllocateChecked_8264EFE0, an allocation, a particular allocation / a particular size, a
# particular size / a particular allocation), and store the result (a value, a particular value, a particular size / a
# particular allocation, a particular operation, obj->4, obj->8, a store, a particular value / a particular state, a
# particular size / a particular allocation, obj->12, a size, a particular size / a particular allocation, a
# particular value / a particular state))" routine (a grow/allocate process, part of the 0x8282E cluster, a sibling of
# ProcessSizeCompute_825BBAD0, a caller of AllocateChecked_8264EFE0). 73 lines, 2 callees (AllocateChecked_8264EFE0,
# sub_82A97648). 10 callers: sub_82B13410, ProcessCount_82B728D8, sub_82B5EC80, sub_82832020, Construct16Byte2
# _82A11180, sub_8217A1E0, sub_82A112B8, sub_821F01B8, sub_82214528, sub_82B13BB0 (siblings in the 0x82B13/0x82B72/0
# x82B5E/0x82832/0x82A11/0x8217A/0x82A11/0x821F0/0x82214/0x82B13 clusters). The object's class (obj, ->4 = a value, ->
# 8 = a value, ->12 = a size, a particular structure, a grow / a range object, a particular size / a particular
# allocation, a particular value / a particular state) and the size's meaning (a value, a particular value, a
# particular size / a particular allocation) aren't resolvable, so the name states the mechanism: a grow/allocate
# process ((if a size (a value, a particular value, a particular size / a particular allocation) is > a threshold (a
# constant, from a fixed .data address, a particular value, a particular threshold, a particular size / a particular
# allocation), a particular threshold, a particular size) call a complex computation (sub_82A97648, a complex
# computation, a particular computation / a particular operation, a particular size / a particular allocation), and
# (otherwise) grow/allocate (a size, a particular size / a particular allocation, a particular size / a particular
# allocation, a particular operation, calling AllocateChecked_8264EFE0, an allocation, a particular allocation / a
# particular size, a particular size / a particular allocation), and store the result (a value, a particular value, a
# particular size / a particular allocation, a particular operation, obj->4, obj->8, a store, a particular value / a
# particular state, a particular size / a particular allocation, obj->12, a size, a particular size / a particular
# allocation, a particular value / a particular state)).
[entrypoint.functions."0x8282EFD8"]
name = "ProcessGrowAllocate_8282EFD8"

# A copy (with a 24-byte element and a loop, the sibling of CopyRecords12_82519F28 / CopyRecords20_82A12E40, the
# caller of CopyAssign_RefCounted_821F0108): loop over a range (a range, a particular range / a particular allocation,
# a count, a particular count / a particular value, a particular size / a particular allocation), and for each item (a
# 24-byte element, a particular value, a particular size / a particular allocation, a particular state / a particular
# operation), (if a base (a value, a particular value, a particular base / a particular state) is non-zero, a
# particular state) copy a 24-byte element (a float, a value, a value, a particular 24-byte element) from a source (c,
# a source, a 24-byte element, a particular value, a particular size / a particular allocation) to a base (b, a base,
# a 24-byte element, a particular value, a particular target, a particular size / a particular allocation), calling a
# copy assign (CopyAssign_RefCounted_821F0108, a copy assign, a particular copy / a particular allocation, a
# particular size / a particular allocation, a particular operation).

# Args: a (r3, a value), b (r4, r31, a base, a 24-byte element, a particular value, a particular target, a particular
# size / a particular allocation), count (r5, r28, a count, a particular count / a particular value, a particular size
# / a particular allocation), c (r6, r29, ->0 = a float, ->4 = a value, ->8 = a value, a source, a 24-byte element, a
# particular value, a particular size / a particular allocation), (more). It checks a count (r28, a count, a
# particular count / a particular value, a particular size / a particular allocation): if it is non-zero (a particular
# state, a particular count / a particular value, a particular size / a particular allocation), it loops over a range
# (a range, a particular range / a particular allocation, a count, a particular count / a particular value, a
# particular size / a particular allocation, a particular state / a particular operation): for each item (a 24-byte
# element, a particular value, a particular size / a particular allocation, a particular state / a particular
# operation), it checks a base (r31, b, a base, a 24-byte element, a particular value, a particular target, a
# particular size / a particular allocation): if it is non-zero (a particular state, a particular value, a particular
# base / a particular state, a particular size / a particular allocation), it copies a 24-byte element (a float (f0, c
# ->0, a float, a 24-byte element, a particular value), a value (r4, c + 4, a value, a 24-byte element, a particular
# value), a value (r5, c + 8, a value, a 24-byte element, a particular value), a particular 24-byte element) from a
# source (c, r29, a source, a 24-byte element, a particular value, a particular size / a particular allocation) to a
# base (b, r31, a base, a 24-byte element, a particular value, a particular target, a particular size / a particular
# allocation, a particular copy / a particular allocation), calling a copy assign (CopyAssign_RefCounted_821F0108, sub
# _821F0108, a copy assign, on a value (b + 4, a base, a 24-byte element, a particular value, a particular target, a
# particular size / a particular allocation), and c + 4, a source, a 24-byte element, a particular value, a particular
# size / a particular allocation), a particular copy / a particular allocation, a particular size / a particular
# allocation, a particular operation, and (CopyAssign_RefCounted_821F0108, sub_821F0108, a copy assign, on a value (b
# + 8, a base, a 24-byte element, a particular value, a particular target, a particular size / a particular
# allocation), and c + 8, a source, a 24-byte element, a particular value, a particular size / a particular
# allocation), a particular copy / a particular allocation, a particular size / a particular allocation, a particular
# operation). If the count is zero (a null state, a particular state, a particular count / a particular value, a
# particular size / a particular allocation), it takes one path (a no-op / a return, a particular state, a particular
# result, a particular size / a particular allocation). So it's a "copy (with a 24-byte element and a loop: loop over a
# range (a range, a particular range / a particular allocation, a count, a particular count / a particular value, a
# particular size / a particular allocation), and for each item (a 24-byte element, a particular value, a particular
# size / a particular allocation, a particular state / a particular operation), (if a base (a value, a particular
# value, a particular base / a particular state) is non-zero, a particular state) copy a 24-byte element (a float, a
# value, a value, a particular 24-byte element) from a source (c, a source, a 24-byte element, a particular value, a
# particular size / a particular allocation) to a base (b, a base, a 24-byte element, a particular value, a
# particular target, a particular size / a particular allocation), calling a copy assign (CopyAssign_RefCounted
# _821F0108, a copy assign, a particular copy / a particular allocation, a particular size / a particular allocation, a
# particular operation))" routine (a copy process, part of the 0x829EA cluster, a sibling of CopyRecords12_82519F28
# and CopyRecords20_82A12E40, a caller of CopyAssign_RefCounted_821F0108). 80 lines, 1 callee
# (CopyAssign_RefCounted_821F0108, sub_821F0108, a copy assign). 3 callers: sub_829EABB8, sub_821E3B38
# (Construct24ByteCopy, a 24-byte constructor, which calls this to copy the 24-byte elements, a particular copy / a
# particular allocation, a particular size / a particular allocation), sub_82B5EC80 (a sibling in the 0x82B5E cluster
# , a process / a destructor, which calls this to copy the 24-byte elements, a particular copy / a particular
# allocation, a particular size / a particular allocation). The base's class (b, a base, a 24-byte element, a
# particular value, a particular target, a particular size / a particular allocation, a particular value / a
# particular state) and the source's meaning (a source, a 24-byte element, a particular value, a particular size / a
# particular allocation, a particular value / a particular state) aren't resolvable, so the name states the
# mechanism: a copy process (with a 24-byte element and a loop) (loop over a range (a range, a particular range / a
# particular allocation, a count, a particular count / a particular value, a particular size / a particular
# allocation), and for each item (a 24-byte element, a particular value, a particular size / a particular allocation,
# a particular state / a particular operation), (if a base (a value, a particular value, a particular base / a
# particular state) is non-zero, a particular state) copy a 24-byte element (a float, a value, a value, a particular
# 24-byte element) from a source (c, a source, a 24-byte element, a particular value, a particular size / a particular
# allocation) to a base (b, a base, a 24-byte element, a particular value, a particular target, a particular size / a
# particular allocation), calling a copy assign (CopyAssign_RefCounted_821F0108, a copy assign, a particular copy / a
# particular allocation, a particular size / a particular allocation, a particular operation)). The "24" refers to the
# 24-byte element (a 24-byte element, a particular value, a particular size / a particular allocation).
[entrypoint.functions."0x829EAB40"]
name = "CopyRecords24_829EAB40"

# A process (a construct, with a float-to-int conversion and a complex computation, the caller of sub_82BBF648):
# (if a value (obj->40, a value, a particular value) is == a value (obj->36, a value, a particular value), a particular
# match / a particular state) perform a construct (a float-to-int conversion, a particular conversion / a particular
# operation, a complex computation, a particular computation / a particular operation, a particular size / a
# particular allocation, calling sub_82BBF648, a complex computation, a particular computation / a particular
# operation, a particular size / a particular allocation), and (otherwise) take one path (a no-op / a return, a
# particular state, a particular result, a particular size / a particular allocation).

# Args: obj (r3, r31, ->36 = a value, ->40 = a value, ->44 = a float, a particular structure, a construct object, a
# particular size / a particular allocation), a (r4, r30, a value), (more). It checks a value (r10, obj->40, a value,
# a particular value) against a value (r11, obj->36, a value, a particular value): if the value is == the other value
# (a particular match / a particular state, a particular value, a particular state / a particular operation), it
# performs a construct (a float-to-int conversion, a particular conversion / a particular operation, a complex
# computation, a particular computation / a particular operation, a particular size / a particular allocation: it
# reads a value (r11, obj->36, a value, a particular value, a count, a particular count / a particular value), and a
# float (f0, obj->44, a float, a particular value, a particular value / a particular state), and performs a float-to
# -int conversion (a float-to-int conversion, a particular conversion / a particular operation, a particular value / a
# particular state, a particular size / a particular allocation: it converts a float (f0, a float, a particular value
# , a particular value / a particular state), and a count (f13, r11, a count, a particular count / a particular
# value), and a float (f10, f0 * f11, a float, a particular value, a particular value / a particular state), and an
# int (f9, fctiwz(f10), a float-to-int conversion, a particular value / a particular state, a particular size / a
# particular allocation, a particular operation), and (in paths) calls a complex computation (sub_82BBF648, a complex
# computation, a particular computation / a particular operation, a particular size / a particular allocation, a
# particular state / a particular result). If the value is != the other value (a particular miss / a particular state,
# a particular value, a particular state / a particular operation), it takes one path (a no-op / a return, a
# particular state, a particular result, a particular size / a particular allocation). So it's a "process (a
# construct, with a float-to-int conversion and a complex computation: (if a value (obj->40, a value, a particular
# value) is == a value (obj->36, a value, a particular value), a particular match / a particular state) perform a
# construct (a float-to-int conversion, a particular conversion / a particular operation, a complex computation, a
# particular computation / a particular operation, a particular size / a particular allocation, calling sub_82BBF648,
# a complex computation, a particular computation / a particular operation, a particular size / a particular
# allocation), and (otherwise) take one path (a no-op / a return, a particular state, a particular result, a
# particular size / a particular allocation))" routine (a construct process, part of the 0x82BAE cluster, a caller of
# sub_82BBF648). 94 lines, 1 callee (sub_82BBF648, a complex computation). 10 callers: sub_82BC4040, sub_822D8AA0, sub
# _82BB3E40, sub_82BAD9D8, sub_82BBF3F0, sub_82BAECF0, sub_82BC4D98, sub_82BC37B8, sub_82BAD1D8, sub_82BAE500
# (siblings in the 0x82BC4/0x822D8/0x82BB3/0x82BAD/0x82BBF/0x82BAE/0x82BC4/0x82BC3/0x82BAD/0x82BAE clusters). The
# object's class (obj, ->36 = a value, ->40 = a value, ->44 = a float, a particular structure, a construct object, a
# particular size / a particular allocation, a particular value / a particular state) and the value's meaning (a
# value, a particular value, a particular size / a particular allocation) aren't resolvable, so the name states the
# mechanism: a construct process (with a float-to-int conversion and a complex computation) ((if a value (obj->40, a
# value, a particular value) is == a value (obj->36, a value, a particular value), a particular match / a particular
# state) perform a construct (a float-to-int conversion, a particular conversion / a particular operation, a complex
# computation, a particular computation / a particular operation, a particular size / a particular allocation, calling
# sub_82BBF648, a complex computation, a particular computation / a particular operation, a particular size / a
# particular allocation), and (otherwise) take one path (a no-op / a return, a particular state, a particular result,
# a particular size / a particular allocation)). The "FloatToInt" refers to the float-to-int conversion (a
# float-to-int conversion, a particular conversion / a particular operation, a particular value / a particular state,
# a particular size / a particular allocation).
[entrypoint.functions."0x82BAE368"]
name = "ProcessFloatToInt_82BAE368"

# An atomic decrement (the sibling of AtomicDecrement_822C72E0 / AtomicIncrement_822CBEB8, with a global lock and a
# complex computation): perform an atomic decrement (a global lock, a particular lock / a particular state, using
# lwarx/stwcx, a particular atomic operation, on a value (obj->4, a value, a reference count, a particular count),
# decrementing it by 1, a particular count / a particular state), and (in paths) perform a complex computation (a
# reference count, a particular count / a particular state, a particular size / a particular allocation).

# Args: obj (r3, ->0 = a value, ->4 = a reference count, a particular structure, a reference counted object), (more). It
# performs an atomic decrement (a global lock, a particular lock / a particular state, using lwarx/stwcx, a particular
# atomic operation, on a value (obj->4, a value, a reference count, a particular count), decrementing it by 1, a
# particular count / a particular state: it saves the msr (mfmsr r9, a particular state, a particular lock), and
# disables interrupts (mtmsrd r13,1, a particular state, a particular lock), and atomically loads the count (lwarx r10
# ,0,r11, a value, a reference count, a particular count, a particular atomic operation), and decrements it (r10 - 1,
# a decrement, a particular count / a particular state), and atomically stores it (stwcx. r10,0,r11, a value, a
# reference count, a particular count, a particular atomic operation), and restores the msr (mtmsrd r9,1, a
# particular state, a particular lock), and (if the store failed, a particular state, a particular lock) retries the
# operation (goto loop, a retry, a particular state / a particular operation, a particular atomic operation)). In paths
# it performs a complex computation (a reference count, a particular count / a particular state, a particular size / a
# particular allocation: it reads a value (r3->0, a value, a particular value), and (if the count is zero, a
# particular state, a particular count), it takes one path (a no-op / a return, a particular state, a particular
# result)). So it's a "perform an atomic decrement (a global lock, a particular lock / a particular state, using
# lwarx/stwcx, a particular atomic operation, on a value (obj->4, a value, a reference count, a particular count),
# decrementing it by 1, a particular count / a particular state), and (in paths) perform a complex computation (a
# reference count, a particular count / a particular state, a particular size / a particular allocation))" routine (an
# atomic decrement, part of the 0x826C0 cluster, a sibling of AtomicDecrement_822C72E0). 77 lines, no direct callees
# (pure atomic operation, using lwarx/stwcx with a global lock, a particular atomic operation). No direct callers (it's
# called via a vtable / it is registered-but-uncalled, part of the game's subsystems). The object's class (obj, ->0 =
# a value, ->4 = a reference count, a particular structure, a reference counted object, a particular value / a
# particular count) and the value's meaning (a value, a reference count, a particular count) aren't resolvable, so the
# name states the mechanism: an atomic decrement (a global lock, a particular lock / a particular state, using
# lwarx/stwcx, a particular atomic operation, on a value (obj->4, a value, a reference count, a particular count),
# decrementing it by 1, a particular count / a particular state). The "2" distinguishes it from AtomicDecrement
# _822C72E0.
[entrypoint.functions."0x826C0B30"]
name = "AtomicDecrement2_826C0B30"

# A 32-byte constructor (the sibling of Construct32ByteVector_8321FB58 / ConstructNode32_82BACFB0, with a vector
# operation, the caller of Construct32ByteVector_8321FB58): perform a 32-byte constructor (with a vector operation, a
# SIMD computation, calling Construct32ByteVector_8321FB58, a 32-byte constructor, a particular 32-byte object / a
# particular size, a particular size / a particular allocation), and initialize the object's fields (obj->0 = a
# constant, from a fixed .data address, a class id / a marker, a particular class / a particular marker, obj->192 = 0, a
# field, a count / a size, a zero, obj->196 = -1, a field, a value, a particular value, obj->216 = a constant, from a
# fixed .data address, a float, a particular value, obj->221 = 0, a field, a count / a size, a zero, a 32-byte object,
# a particular structure, a particular size / a particular allocation).

# Args: obj (r3, r31, ->0 = a field, a constant, a class id / a marker, ->192 = a field, a count / a size, ->196 = a
# field, a value, ->216 = a field, a float, ->221 = a field, a count / a size, a 32-byte object), (more). It performs a
# 32-byte constructor (with a vector operation, a SIMD computation, calling Construct32ByteVector_8321FB58, sub
# _8321FB58, a 32-byte constructor, on a value (obj, r31, a 32-byte object, a particular structure, a particular size
# / a particular allocation), a particular 32-byte object / a particular size, a particular size / a particular
# allocation, a particular operation). It initializes the object's fields (obj->0 = a constant, from a fixed .data
# address, a class id / a marker, a particular class / a particular marker, obj->192 = 0, a field, a count / a size, a
# zero, obj->196 = -1, a field, a value, a particular value, obj->216 = a constant, from a fixed .data address, a
# float, a particular value, obj->221 = 0, a field, a count / a size, a zero, a 32-byte object, a particular
# structure, a particular size / a particular allocation, a particular initialization). So it's a "construct a 32-byte
# object (perform a 32-byte constructor (with a vector operation, a SIMD computation, calling Construct32ByteVector
# _8321FB58, a 32-byte constructor, a particular 32-byte object / a particular size, a particular size / a particular
# allocation), and initialize the object's fields (obj->0 = a constant, from a fixed .data address, a class id / a
# marker, a particular class / a particular marker, obj->192 = 0, a field, a count / a size, a zero, obj->196 = -1, a
# field, a value, a particular value, obj->216 = a constant, from a fixed .data address, a float, a particular value,
# obj->221 = 0, a field, a count / a size, a zero, a 32-byte object, a particular structure, a particular size / a
# particular allocation))" routine (a 32-byte constructor, with a vector operation, part of the 0x8323C cluster, a
# sibling of Construct32ByteVector_8321FB58, a caller of Construct32ByteVector_8321FB58). 83 lines, 1 callee
# (Construct32ByteVector_8321FB58, sub_8321FB58, a 32-byte constructor). 8 callers: sub_82A0ECC8, sub_82A0ED30, sub
# _82A0C2E0, sub_82A0D7F8, sub_8323EAE8, sub_82A0D630, sub_8323DB30, sub_82A0C4A8 (siblings in the 0x82A0E/0x82A0C/0
# x82A0D/0x8323E/0x8323D clusters). The object's class (obj, ->0 = a field, a constant, a class id / a marker, ->192 =
# a field, a count / a size, ->196 = a field, a value, ->216 = a field, a float, ->221 = a field, a count / a size, a
# 32-byte object, a particular structure, a particular size / a particular allocation) and the value's meaning (a
# value, a particular value, a particular size / a particular allocation) aren't resolvable, so the name states the
# mechanism: a 32-byte constructor (with a vector operation) (perform a 32-byte constructor (with a vector operation, a
# SIMD computation, calling Construct32ByteVector_8321FB58, a 32-byte constructor, a particular 32-byte object / a
# particular size, a particular size / a particular allocation), and initialize the object's fields (obj->0 = a
# constant, from a fixed .data address, a class id / a marker, a particular class / a particular marker, obj->192 = 0,
# a field, a count / a size, a zero, obj->196 = -1, a field, a value, a particular value, obj->216 = a constant, from a
# fixed .data address, a float, a particular value, obj->221 = 0, a field, a count / a size, a zero, a 32-byte object,
# a particular structure, a particular size / a particular allocation)). The "2" distinguishes it from
# Construct32ByteVector_8321FB58.
[entrypoint.functions."0x8323C7F0"]
name = "Construct32ByteVector2_8323C7F0"

# A process (a grow/append, with a byte copy and a 1-byte element, the sibling of ProcessGrowAppend_82BE4AA8 /
# ProcessGrowAppend2_8321F328 / ProcessGrowAppend3_8321FC38, the caller of ProcessFloatData_832206E8 /
# ProcessGrowAppend3_8321FC38 / sub_82A1B480): (if a count (a->20, a count, a particular count / a particular value)
# is >= 1, a particular threshold, a particular count) grow/append (a byte copy, a 1-byte element, a particular size /
# a particular allocation, copying a byte (a byte, a particular value, a 1-byte element, a particular value) from a
# base (a->12, a base, a 1-byte element, a particular value, a particular source) to a stack (a particular target), a
# particular copy / a particular allocation), and (in paths) call a process (ProcessFloatData_832206E8, on a value, a
# particular process, a particular operation), and a grow/append (ProcessGrowAppend3_8321FC38, on a value, a
# particular grow/append, a particular size / a particular allocation).

# Args: obj (r3, r30, an object), a (r4, r31, ->4 = a base, ->12 = a base, ->20 = a count, a particular structure, a
# range / a grow object), b (r5, r28, a value), c (r6, r27, a value), (more). It checks a count (r11, a->20, a count, a
# particular count / a particular value): if it is >= 1 (a particular threshold, a particular count, a particular
# value, a particular state), it grows/appends (a byte copy, a 1-byte element, a particular size / a particular
# allocation: it computes a count (r9, a->20 - 1, a count, a 1-byte element, a particular count / a particular value),
# and a base (r11, a->12, a base, a 1-byte element, a particular value, a particular source), and (r7, r11 + 1, a
# base, a 1-byte element, a particular value, a particular source), and copies a byte (r5, r11->0, a byte, a 1-byte
# element, a particular value, a particular source, to a stack (r8, a stack, a particular value, a particular target,
# a particular copy / a particular allocation), a particular copy / a particular allocation, advancing to the next 1-byte
# element, a particular grow/append, a particular size / a particular allocation). If the count is < 1 (a particular
# threshold, a particular count, a particular value, a particular state), it takes one path (a no-op / a return, a
# particular state, a particular result, a particular size / a particular allocation). In paths it calls a process
# (ProcessFloatData_832206E8, sub_832206E8, a process, on a value (a, a value, a particular value, a particular
# argument), a particular process, a particular operation, a particular size / a particular allocation), and a
# grow/append (ProcessGrowAppend3_8321FC38, sub_8321FC38, a grow/append, on a value (a, a value, a particular value, a
# particular argument), a particular grow/append, a particular size / a particular allocation, a particular operation).
# So it's a "process (a grow/append, with a byte copy and a 1-byte element: (if a count (a->20, a count, a particular
# count / a particular value) is >= 1, a particular threshold, a particular count) grow/append (a byte copy, a 1-byte
# element, a particular size / a particular allocation, copying a byte (a byte, a particular value, a 1-byte element, a
# particular value) from a base (a->12, a base, a 1-byte element, a particular value, a particular source) to a stack
# (a particular target), a particular copy / a particular allocation), and (in paths) call a process (ProcessFloatData
# _832206E8, on a value, a particular process, a particular operation), and a grow/append (ProcessGrowAppend3
# _8321FC38, on a value, a particular grow/append, a particular size / a particular allocation))" routine (a
# grow/append process, part of the 0x8323C cluster, a sibling of ProcessGrowAppend_82BE4AA8, a caller of
# ProcessFloatData_832206E8 and ProcessGrowAppend3_8321FC38). 808 lines, 3 callees (ProcessFloatData_832206E8,
# ProcessGrowAppend3_8321FC38, sub_82A1B480). 2 callers: sub_8323EB40, sub_8323DBB0 (siblings in the 0x8323E/0x8323D
# clusters, a range computation process, which calls this to grow/append, a particular size / a particular allocation,
# a particular operation). The object's class (obj, r30, an object, a particular structure, a range / a grow object, a
# particular size / a particular allocation) and the value's meaning (a value, a particular value, a particular size /
# a particular allocation) aren't resolvable, so the name states the mechanism: a grow/append process ((if a count
# (a->20, a count, a particular count / a particular value) is >= 1, a particular threshold, a particular count) grow
# /append (a byte copy, a 1-byte element, a particular size / a particular allocation, copying a byte (a byte, a
# particular value, a 1-byte element, a particular value) from a base (a->12, a base, a 1-byte element, a particular
# value, a particular source) to a stack (a particular target), a particular copy / a particular allocation), and (in
# paths) call a process (ProcessFloatData_832206E8, on a value, a particular process, a particular operation), and a
# grow/append (ProcessGrowAppend3_8321FC38, on a value, a particular grow/append, a particular size / a particular
# allocation)). The "4" distinguishes it from ProcessGrowAppend_82BE4AA8, ProcessGrowAppend2_8321F328, and
# ProcessGrowAppend3_8321FC38.
[entrypoint.functions."0x8323CB98"]
name = "ProcessGrowAppend4_8323CB98"

# A process (an element access, with a bounds check and a complex computation, a 2-byte element, the sibling of
# ProcessElementAccess_8217A400 / ValidateElementAccess_8217A7F8, the caller of ProcessValue_FlagNonZero_8217A5E0 /
# ClampAndUpdate_822F6380 / Buffer_CopyOrZero_Error_82CA3730): (if a size (obj->24, a size, a particular value) is < 8,
# a particular threshold, a particular size) take one path (a no-op / a return, a particular state, a particular
# result, a particular size / a particular allocation), and (otherwise) perform an element access (a bounds check, a
# particular check / a particular state, a complex computation, a particular computation / a particular operation, a
# 2-byte element, a particular size / a particular allocation, a particular value / a particular operation), and (in
# paths) call a process (ProcessValue_FlagNonZero_8217A5E0, on a value, a particular process, a particular operation, a
# particular size / a particular allocation), and clamp/update (ClampAndUpdate_822F6380, on a value, a particular
# clamp/update, a particular size / a particular allocation, a particular operation).

# Args: obj (r3, r31, ->4 = a base, ->20 = a size, ->24 = a size, a particular structure, a range / a bounds object),
# index (r4, r28, an index, a particular value, a particular count), a (r5, r27, a value), (more). It checks a size (r9
# , obj->24, a size, a particular value, a particular size / a particular allocation): if it is < 8 (a particular
# threshold, a particular size, a particular value, a particular state, a particular size / a particular allocation), it
# takes one path (a no-op / a return, a particular state, a particular result, a particular size / a particular
# allocation); if it is >= 8 (a particular threshold, a particular size, a particular value, a particular state, a
# particular size / a particular allocation), it performs an element access (a bounds check, a particular check / a
# particular state, a complex computation, a particular computation / a particular operation, a 2-byte element, a
# particular size / a particular allocation, a particular value / a particular operation: it checks an index (r28, an
# index, a particular value, a particular count) against a base (r11, obj->4, a base, a particular value, a particular
# base / a particular state, a particular size / a particular allocation): if the index is < the base (a particular
# threshold, a particular size, a particular value, a particular state, a particular size / a particular allocation), it
# takes one path (a no-op / a return, a particular state, a particular result, a particular size / a particular
# allocation); if the index is >= the base (a particular threshold, a particular size, a particular value, a particular
# state, a particular size / a particular allocation), it performs an element access (a complex computation, a
# particular computation / a particular operation, a 2-byte element, a particular size / a particular allocation, a
# particular value / a particular operation: it computes a size (r10, obj->20, a size, a particular value, a particular
# size / a particular allocation), and a size (r10 * 2, a size, a 2-byte element, a particular size / a particular
# allocation), and a pointer (r8, r10 + r11, a pointer, a particular value, a particular pointer / a particular state,
# a particular size / a particular allocation), and (in paths) calls a process (ProcessValue_FlagNonZero_8217A5E0, sub
# _8217A5E0, a process, on a value (a, a value, a particular value, a particular size / a particular allocation), a
# particular process, a particular operation, a particular size / a particular allocation), and clamps/updates
# (ClampAndUpdate_822F6380, sub_822F6380, a clamp/update, on a value (a, a value, a particular value, a particular size
# / a particular allocation), a particular clamp/update, a particular size / a particular allocation, a particular
# operation). So it's a "process (an element access, with a bounds check and a complex computation, a 2-byte element:
# (if a size (obj->24, a size, a particular value) is < 8, a particular threshold, a particular size) take one path (a
# no-op / a return, a particular state, a particular result, a particular size / a particular allocation), and
# (otherwise) perform an element access (a bounds check, a particular check / a particular state, a complex
# computation, a particular computation / a particular operation, a 2-byte element, a particular size / a particular
# allocation, a particular value / a particular operation), and (in paths) call a process (ProcessValue_FlagNonZero
# _8217A5E0, on a value, a particular process, a particular operation, a particular size / a particular allocation), and
# clamp/update (ClampAndUpdate_822F6380, on a value, a particular clamp/update, a particular size / a particular
# allocation, a particular operation))" routine (an element access process, part of the 0x8217A cluster, a sibling of
# ProcessElementAccess_8217A400 and ValidateElementAccess_8217A7F8, a caller of ProcessValue_FlagNonZero_8217A5E0, a
# particular process, a particular operation). 182 lines, 3 callees (Buffer_CopyOrZero_Error_82CA3730, ClampAndUpdate
# _822F6380, ProcessValue_FlagNonZero_8217A5E0). 10 callers: sub_82C72640, sub_822F21B8, sub_82C68B50, sub_82C809C8, sub
# _82C66A58, sub_82C7F458, sub_82C7C778, sub_82C7FB28, sub_82BFD228, sub_82C7A220 (siblings in the 0x82C72/0x822F2/0
# x82C68/0x82C80/0x82C66/0x82C7F/0x82C7C/0x82C7F/0x82BFD/0x82C7A clusters). The object's class (obj, ->4 = a base, ->
# 20 = a size, ->24 = a size, a particular structure, a range / a bounds object, a particular size / a particular
# allocation, a particular value / a particular state) and the index's meaning (an index, a particular value, a
# particular count) aren't resolvable, so the name states the mechanism: an element access process (with a bounds check
# and a complex computation, a 2-byte element) ((if a size (obj->24, a size, a particular value) is < 8, a particular
# threshold, a particular size) take one path (a no-op / a return, a particular state, a particular result, a
# particular size / a particular allocation), and (otherwise) perform an element access (a bounds check, a particular
# check / a particular state, a complex computation, a particular computation / a particular operation, a 2-byte
# element, a particular size / a particular allocation, a particular value / a particular operation), and (in paths) call
# a process (ProcessValue_FlagNonZero_8217A5E0, on a value, a particular process, a particular operation, a particular
# size / a particular allocation), and clamp/update (ClampAndUpdate_822F6380, on a value, a particular clamp/update, a
# particular size / a particular allocation, a particular operation)). The "2" distinguishes it from
# ProcessElementAccess_8217A400.
[entrypoint.functions."0x8217A6C0"]
name = "ProcessElementAccess2_8217A6C0"

# A process (a float data process, with a dispatch and a complex computation, the caller of ProcessFloatData_832206E8
# / BuildAndDispatch_83220548 / ProcessGrowAppend4_8323CB98): perform a float data process (a float, a particular
# value, a particular size / a particular allocation, a particular operation, calling ProcessFloatData_832206E8, on a
# value and a constant (4, a constant, a particular value, a particular index / a particular state), a particular
# process, a particular operation, a particular size / a particular allocation), and (in paths) build and dispatch (a
# build, a particular build / a particular operation, a dispatch, a particular dispatch / a particular state, calling
# BuildAndDispatch_83220548, on a value and a constant (0, a constant, a particular value, a particular index / a
# particular state), a particular build, a particular operation, a particular size / a particular allocation), and a
# grow/append (a byte copy, a 1-byte element, a particular size / a particular allocation, calling ProcessGrowAppend4
# _8323CB98, on a value, a particular grow/append, a particular size / a particular allocation, a particular
# operation).

# Args: obj (r3, r30, an object, a float data object, a particular structure, a particular size / a particular
# allocation), a (r4, r31, a value, a float data, a particular value, a particular size / a particular allocation), b
# (r5, r28, a value), c (r6, r27, a value), d (r7, r29, a value), (more). It performs a float data process (a float, a
# particular value, a particular size / a particular allocation, a particular operation, calling ProcessFloatData
# _832206E8, sub_832206E8, a process, on a value (obj, r30, an object, a float data object, a particular structure, a
# particular size / a particular allocation), and a (r31, a value, a float data, a particular value, a particular size
# / a particular allocation), and a constant (4, a constant, a particular value, a particular index / a particular
# state), a particular process, a particular operation, a particular size / a particular allocation, and (ProcessFloat
# Data_832206E8, sub_832206E8, a process, on a value (obj, r30, an object, a float data object, a particular structure,
# a particular size / a particular allocation), and a (r31, a value, a float data, a particular value, a particular
# size / a particular allocation), and a constant (5, a constant, a particular value, a particular index / a
# particular state), a particular process, a particular operation, a particular size / a particular allocation)). In
# paths it builds and dispatches (a build, a particular build / a particular operation, a dispatch, a particular
# dispatch / a particular state, calling BuildAndDispatch_83220548, sub_83220548, a build and dispatch, on a value
# (obj, r30, an object, a float data object, a particular structure, a particular size / a particular allocation), and
# a (r31, a value, a float data, a particular value, a particular size / a particular allocation), and a constant (0, a
# constant, a particular value, a particular index / a particular state), a particular build, a particular operation, a
# particular size / a particular allocation), and grows/appends (a byte copy, a 1-byte element, a particular size / a
# particular allocation, calling ProcessGrowAppend4_8323CB98, sub_8323CB98, a grow/append, on a value (a, a value, a
# particular value, a particular size / a particular allocation), a particular grow/append, a particular size / a
# particular allocation, a particular operation). So it's a "process (a float data process, with a dispatch and a
# complex computation: perform a float data process (a float, a particular value, a particular size / a particular
# allocation, a particular operation, calling ProcessFloatData_832206E8, on a value and a constant (4, a constant, a
# particular value, a particular index / a particular state), a particular process, a particular operation, a
# particular size / a particular allocation), and (in paths) build and dispatch (a build, a particular build / a
# particular operation, a dispatch, a particular dispatch / a particular state, calling BuildAndDispatch_83220548, on a
# value and a constant (0, a constant, a particular value, a particular index / a particular state), a particular
# build, a particular operation, a particular size / a particular allocation), and a grow/append (a byte copy, a 1-byte
# element, a particular size / a particular allocation, calling ProcessGrowAppend4_8323CB98, on a value, a particular
# grow/append, a particular size / a particular allocation, a particular operation))" routine (a float data process,
# part of the 0x8323E cluster, a caller of ProcessFloatData_832206E8, BuildAndDispatch_83220548, and
# ProcessGrowAppend4_8323CB98). 362 lines, 4 callees (BuildAndDispatch_83220548, ProcessFloatData_832206E8,
# ProcessGrowAppend4_8323CB98, sub_82A1B480). 4 callers: sub_82A0D1D0, sub_82A0CB68, sub_82A0D878, sub_82A0C528
# (siblings in the 0x82A0D/0x82A0C clusters, a process, which calls this to process the float data, a particular size
# / a particular allocation, a particular operation). The object's class (obj, r30, an object, a float data object, a
# particular structure, a particular size / a particular allocation, a particular value / a particular state) and the
# value's meaning (a, r31, a value, a float data, a particular value, a particular size / a particular allocation)
# aren't resolvable, so the name states the mechanism: a float data process (with a dispatch and a complex computation)
# (perform a float data process (a float, a particular value, a particular size / a particular allocation, a
# particular operation, calling ProcessFloatData_832206E8, on a value and a constant (4, a constant, a particular
# value, a particular index / a particular state), a particular process, a particular operation, a particular size / a
# particular allocation), and (in paths) build and dispatch (a build, a particular build / a particular operation, a
# dispatch, a particular dispatch / a particular state, calling BuildAndDispatch_83220548, on a value and a constant
# (0, a constant, a particular value, a particular index / a particular state), a particular build, a particular
# operation, a particular size / a particular allocation), and a grow/append (a byte copy, a 1-byte element, a
# particular size / a particular allocation, calling ProcessGrowAppend4_8323CB98, on a value, a particular
# grow/append, a particular size / a particular allocation, a particular operation)). The "Dispatch" refers to the
# dispatch (a dispatch, a particular dispatch / a particular state, a particular operation, a particular size / a
# particular allocation).
[entrypoint.functions."0x8323EB40"]
name = "ProcessFloatDataDispatch_8323EB40"

# A destruct (a destructor, with a free and a complex computation, the sibling of Destruct20Byte_832156E0, the caller
# of Destruct20Byte_832156E0 / Free_SizeBucketed_Thunk_8221BE68): initialize the object's fields (obj->0 = a constant
# , from a fixed .data address, a class id / a marker, a particular class / a particular marker, a 20-byte object, a
# particular structure, a particular size / a particular allocation), and perform a destructor (a free, a particular
# free / a particular count, a complex computation, a particular computation / a particular operation, a particular
# size / a particular allocation, calling Destruct20Byte_832156E0, a destructor, a particular destruct / a particular
# size, a particular size / a particular allocation, and Free_SizeBucketed_Thunk_8221BE68, a free, a particular free /
# a particular count, a particular size / a particular allocation).

# Args: obj (r3, r31, ->0 = a field, a constant, a class id / a marker, a 20-byte object, a particular structure, a
# particular size / a particular allocation), a (r4, r30, a value, a flag, a particular value, a particular size / a
# particular allocation), (more). It initializes the object's fields (obj->0 = a constant, from a fixed .data address,
# a class id / a marker, a particular class / a particular marker, a 20-byte object, a particular structure, a
# particular size / a particular allocation, a particular initialization). It performs a destructor (a free, a
# particular free / a particular count, a complex computation, a particular computation / a particular operation, a
# particular size / a particular allocation: it calls a destructor (Destruct20Byte_832156E0, sub_832156E0, a
# destructor, on a value (obj, r31, a 20-byte object, a particular structure, a particular size / a particular
# allocation), a particular destruct / a particular size, a particular size / a particular allocation, a particular
# operation), and checks a flag (r9, a & 1, a flag, a particular value, a particular state, a particular size / a
# particular allocation): if the flag is non-zero (a particular state, a particular value, a particular size / a
# particular allocation), it frees (Free_SizeBucketed_Thunk_8221BE68, sub_8221BE68, a free, on a value (obj, r31, a 20
# -byte object, a particular structure, a particular size / a particular allocation), a particular free / a particular
# count, a particular size / a particular allocation, a particular operation); if the flag is zero (a null state, a
# particular state, a particular size / a particular allocation), it takes one path (a no-op / a return, a particular
# state, a particular result, a particular size / a particular allocation). So it's a "destruct (a destructor, with a
# free and a complex computation: initialize the object's fields (obj->0 = a constant, from a fixed .data address, a
# class id / a marker, a particular class / a particular marker, a 20-byte object, a particular structure, a
# particular size / a particular allocation), and perform a destructor (a free, a particular free / a particular
# count, a complex computation, a particular computation / a particular operation, a particular size / a particular
# allocation, calling Destruct20Byte_832156E0, a destructor, a particular destruct / a particular size, a particular
# size / a particular allocation, and Free_SizeBucketed_Thunk_8221BE68, a free, a particular free / a particular
# count, a particular size / a particular allocation))" routine (a destructor, part of the 0x82A11 cluster, a sibling
# of Destruct20Byte_832156E0, a caller of Destruct20Byte_832156E0 and Free_SizeBucketed_Thunk_8221BE68). 56 lines, 2
# callees (Destruct20Byte_832156E0, Free_SizeBucketed_Thunk_8221BE68). No direct callers (it's called via a vtable / it
# is registered-but-uncalled, part of the game's subsystems). The object's class (obj, ->0 = a field, a constant, a
# class id / a marker, a 20-byte object, a particular structure, a particular size / a particular allocation, a
# particular value / a particular state) and the value's meaning (a, r30, a value, a flag, a particular value, a
# particular size / a particular allocation) aren't resolvable, so the name states the mechanism: a destructor (with a
# free and a complex computation) (initialize the object's fields (obj->0 = a constant, from a fixed .data address, a
# class id / a marker, a particular class / a particular marker, a 20-byte object, a particular structure, a
# particular size / a particular allocation), and perform a destructor (a free, a particular free / a particular
# count, a complex computation, a particular computation / a particular operation, a particular size / a particular
# allocation, calling Destruct20Byte_832156E0, a destructor, a particular destruct / a particular size, a particular
# size / a particular allocation, and Free_SizeBucketed_Thunk_8221BE68, a free, a particular free / a particular
# count, a particular size / a particular allocation)). The "20ByteFree" refers to the 20-byte destructor (a
# destructor, a particular destruct / a particular size, a particular size / a particular allocation) and the free (a
# free, a particular free / a particular count, a particular size / a particular allocation).
[entrypoint.functions."0x82A11F90"]
name = "Destruct20ByteFree_82A11F90"

# A process (a grow/append, with a byte-by-byte copy and a 4-byte element, the sibling of ProcessGrowAppend2_8321F328
# / ProcessGrowAppend_82BE4AA8, the caller of CopyStructData2_82A1BD30 / sub_82A1B480 / sub_83211D48): (if a count
# (a->20, a count, a particular count / a particular value) is >= 4, a particular threshold, a particular count) grow
# /append (a byte-by-byte copy, a 4-byte element, a particular size / a particular allocation, copying a 4-byte
# element (four bytes, a particular 4-byte element, a 4-byte element, a particular value) from a base (a->12, a base,
# a 4-byte element, a particular value, a particular source) to a stack (a particular target), a particular copy / a
# particular allocation), and (in paths) copy (CopyStructData2_82A1BD30, on a value, a particular copy / a particular
# size, a particular size / a particular allocation, a particular operation).

# Args: obj (r3, r30, an object), a (r4, r31, ->4 = a base, ->12 = a base, ->20 = a count, a particular structure, a
# range / a grow object), b (r5, r29, a value), (more). It checks a count (r11, a->20, a count, a particular count /
# a particular value): if it is >= 4 (a particular threshold, a particular count, a particular value, a particular
# state), it grows/appends (a byte-by-byte copy, a 4-byte element, a particular size / a particular allocation: it
# computes a count (r9, a->20 - 4, a count, a 4-byte element, a particular count / a particular value), and a base
# (r11, a->12, a base, a 4-byte element, a particular value, a particular source), and (r7, r11 + 4, a base, a 4-byte
# element, a particular value, a particular source), and copies a 4-byte element (four bytes, a particular 4-byte
# element, a 4-byte element, a particular value: lbz r5, 0, r11, a byte, a 4-byte element, a particular value, and stb
# r5, 0, r8, a store, a 4-byte element, a particular copy / a particular allocation, and lbz r4, 1, r11, a byte, a 4
# -byte element, a particular value, and stb r4, 1, r8, a store, a 4-byte element, a particular copy / a particular
# allocation, and lbz r3, 2, r11, a byte, a 4-byte element, a particular value, and stb r3, 2, r8, a store, a 4-byte
# element, a particular copy / a particular allocation, and lbz r11, 3, r11, a byte, a 4-byte element, a particular
# value, and stb r11, 3, r8, a store, a 4-byte element, a particular copy / a particular allocation), advancing to the
# next 4-byte element, a particular grow/append, a particular size / a particular allocation). If the count is < 4 (a
# particular threshold, a particular count, a particular value, a particular state), it takes one path (a no-op / a
# return, a particular state, a particular result, a particular size / a particular allocation). In paths it copies
# (CopyStructData2_82A1BD30, sub_82A1BD30, a copy, on a value (a, a value, a particular value, a particular size / a
# particular allocation), a particular copy / a particular size, a particular size / a particular allocation, a
# particular operation). So it's a "process (a grow/append, with a byte-by-byte copy and a 4-byte element: (if a count
# (a->20, a count, a particular count / a particular value) is >= 4, a particular threshold, a particular count) grow
# /append (a byte-by-byte copy, a 4-byte element, a particular size / a particular allocation, copying a 4-byte
# element (four bytes, a particular 4-byte element, a 4-byte element, a particular value) from a base (a->12, a base,
# a 4-byte element, a particular value, a particular source) to a stack (a particular target), a particular copy / a
# particular allocation), and (in paths) copy (CopyStructData2_82A1BD30, on a value, a particular copy / a particular
# size, a particular size / a particular allocation, a particular operation))" routine (a grow/append process, part of
# the 0x83212 cluster, a sibling of ProcessGrowAppend2_8321F328, a caller of CopyStructData2_82A1BD30). 211 lines, 3
# callees (CopyStructData2_82A1BD30, sub_82A1B480, sub_83211D48). No direct callers (it's called via a vtable / it is
# registered-but-uncalled, part of the game's subsystems). The object's class (obj, r30, an object, a particular
# structure, a range / a grow object, a particular size / a particular allocation) and the value's meaning (a, r31, ->
# 4 = a base, ->12 = a base, ->20 = a count, a particular structure, a range / a grow object, a particular size / a
# particular allocation) aren't resolvable, so the name states the mechanism: a grow/append process ((if a count
# (a->20, a count, a particular count / a particular value) is >= 4, a particular threshold, a particular count) grow
# /append (a byte-by-byte copy, a 4-byte element, a particular size / a particular allocation, copying a 4-byte
# element (four bytes, a particular 4-byte element, a 4-byte element, a particular value) from a base (a->12, a base,
# a 4-byte element, a particular value, a particular source) to a stack (a particular target), a particular copy / a
# particular allocation), and (in paths) copy (CopyStructData2_82A1BD30, on a value, a particular copy / a particular
# size, a particular size / a particular allocation, a particular operation)). The "5" distinguishes it from
# ProcessGrowAppend_82BE4AA8, ProcessGrowAppend2_8321F328, ProcessGrowAppend3_8321FC38, and ProcessGrowAppend4
# _8323CB98.
[entrypoint.functions."0x83212DC0"]
name = "ProcessGrowAppend5_83212DC0"

# A 20-byte constructor (the sibling of Construct20Byte_83215640, with a constant and a complex computation, the
# caller of Construct20Byte_83215640): perform a 20-byte constructor (a 20-byte object, a particular structure, a
# particular size / a particular allocation, a particular operation, calling Construct20Byte_83215640, a 20-byte
# constructor, a particular 20-byte object / a particular size, a particular size / a particular allocation), and
# initialize the object's fields (obj->0 = a constant, from a fixed .data address, a class id / a marker, a
# particular class / a particular marker, obj->16 = a constant, from a fixed .data address, a float, a particular
# value, obj->20 = a constant, from a fixed .data address, a float, a particular value, obj->24 = a constant, from a
# fixed .data address, a float, a particular value, obj->28 = a constant, from a fixed .data address, a float, a
# particular value, obj->32 = a constant, from a fixed .data address, a float, a particular value, a 20-byte object,
# a particular structure, a particular size / a particular allocation).

# Args: obj (r3, r31, ->0 = a field, a constant, a class id / a marker, ->16 = a field, a float, ->20 = a field, a
# float, ->24 = a field, a float, ->28 = a field, a float, ->32 = a field, a float, a 20-byte object), (more). It
# performs a 20-byte constructor (a 20-byte object, a particular structure, a particular size / a particular
# allocation, a particular operation, calling Construct20Byte_83215640, sub_83215640, a 20-byte constructor, on a
# value (obj, r31, a 20-byte object, a particular structure, a particular size / a particular allocation), a
# particular 20-byte object / a particular size, a particular size / a particular allocation, a particular operation)
# . It initializes the object's fields (obj->0 = a constant, from a fixed .data address, a class id / a marker, a
# particular class / a particular marker, obj->16 = a constant, from a fixed .data address, a float, a particular
# value, obj->20 = a constant, from a fixed .data address, a float, a particular value, obj->24 = a constant, from a
# fixed .data address, a float, a particular value, obj->28 = a constant, from a fixed .data address, a float, a
# particular value, obj->32 = a constant, from a fixed .data address, a float, a particular value, a 20-byte object,
# a particular structure, a particular size / a particular allocation, a particular initialization). So it's a
# "construct a 20-byte object (perform a 20-byte constructor (a 20-byte object, a particular structure, a particular
# size / a particular allocation, a particular operation, calling Construct20Byte_83215640, a 20-byte constructor, a
# particular 20-byte object / a particular size, a particular size / a particular allocation), and initialize the
# object's fields (obj->0 = a constant, from a fixed .data address, a class id / a marker, a particular class / a
# particular marker, obj->16 = a constant, from a fixed .data address, a float, a particular value, obj->20 = a
# constant, from a fixed .data address, a float, a particular value, obj->24 = a constant, from a fixed .data address,
# a float, a particular value, obj->28 = a constant, from a fixed .data address, a float, a particular value, obj->32
# = a constant, from a fixed .data address, a float, a particular value, a 20-byte object, a particular structure, a
# particular size / a particular allocation))" routine (a 20-byte constructor, with a constant and a complex
# computation, part of the 0x83215 cluster, a sibling of Construct20Byte_83215640, a caller of Construct20Byte
# _83215640). 69 lines, 1 callee (Construct20Byte_83215640, sub_83215640, a 20-byte constructor). 2 callers:
# ProcessTreeNode_83218C10, sub_83219588 (siblings in the 0x83218/0x83219 clusters, a process, which calls this to
# construct a 20-byte object, a particular size / a particular allocation, a particular operation). The object's class
# (obj, ->0 = a field, a constant, a class id / a marker, ->16 = a field, a float, ->20 = a field, a float, ->24 = a
# field, a float, ->28 = a field, a float, ->32 = a field, a float, a 20-byte object, a particular structure, a
# particular size / a particular allocation, a particular value / a particular state) and the value's meaning (a
# value, a particular value, a particular size / a particular allocation) aren't resolvable, so the name states the
# mechanism: a 20-byte constructor (with a constant and a complex computation) (perform a 20-byte constructor (a 20-
# byte object, a particular structure, a particular size / a particular allocation, a particular operation, calling
# Construct20Byte_83215640, a 20-byte constructor, a particular 20-byte object / a particular size, a particular size
# / a particular allocation), and initialize the object's fields (obj->0 = a constant, from a fixed .data address, a
# class id / a marker, a particular class / a particular marker, obj->16 = a constant, from a fixed .data address, a
# float, a particular value, obj->20 = a constant, from a fixed .data address, a float, a particular value, obj->24 = a
# constant, from a fixed .data address, a float, a particular value, obj->28 = a constant, from a fixed .data address,
# a float, a particular value, obj->32 = a constant, from a fixed .data address, a float, a particular value, a 20-byte
# object, a particular structure, a particular size / a particular allocation)). The "2" distinguishes it from
# Construct20Byte_83215640.
[entrypoint.functions."0x832158F8"]
name = "Construct20Byte2_832158F8"

# A destruct (a destructor, with a release and a free and a complex computation, the sibling of DestructRange_82518D30
# / Destruct20ByteFree_82A11F90, the caller of Release_RefCounted_829FF648 / Free_SizeBucketed_Thunk_8221BE68):
# (if a base (obj->4, a base, a particular value, a particular pointer / a particular state) is non-zero, a particular
# state) loop over a range (a range, a particular range / a particular allocation, a base, a particular value, a
# particular pointer / a particular state, a limit, obj->8, a limit, a particular value, a particular size / a
# particular allocation, a particular state / a particular operation), and for each item (a value, a particular value
# , a particular size / a particular allocation, a particular state / a particular operation), release a refcounted
# value (Release_RefCounted_829FF648, on a value (r31, a value, a particular value, a particular pointer / a
# particular state), a particular release / a particular count, a particular size / a particular allocation, a
# particular operation), and free the array (Free_SizeBucketed_Thunk_8221BE68, on a value (obj->4, a base, a
# particular value, a particular pointer / a particular state), a particular free / a particular count, a particular
# size / a particular allocation, a particular operation), and (otherwise) take one path (a no-op / a return, a
# particular state, a particular result, a particular size / a particular allocation), and clear the array's fields
# (obj->4 = 0, obj->8 = 0, obj->12 = 0, three zeros, a particular initialization, a particular size / a particular
# allocation).

# Args: obj (r3, r29, ->4 = a base, ->8 = a limit, ->12 = a size, a particular structure, an array / a range object, a
# particular size / a particular allocation), (more). It checks a base (r31, obj->4, a base, a particular value, a
# particular pointer / a particular state): if it is non-zero (a particular state, a particular value, a particular
# pointer / a particular state), it loops over a range (a range, a particular range / a particular allocation, a base
# (r31, obj->4, a base, a particular value, a particular pointer / a particular state), a limit (r30, obj->8, a limit,
# a particular value, a particular size / a particular allocation), a particular state / a particular operation: for
# each item (a value (r31, a value, a particular value, a particular pointer / a particular state), a particular size
# / a particular allocation, a particular state / a particular operation), it releases a refcounted value
# (Release_RefCounted_829FF648, sub_829FF648, a release, on a value (r31, a value, a particular value, a particular
# pointer / a particular state), a particular release / a particular count, a particular size / a particular
# allocation, a particular operation), and advances to the next item (r31, r31 + 8, a pointer, a particular value, a
# particular pointer / a particular state, a particular state / a particular operation); when the loop is done (a
# particular state / a particular result, a particular size / a particular allocation), it frees the array (Free_Size
# Bucketed_Thunk_8221BE68, sub_8221BE68, a free, on a value (obj->4, a base, a particular value, a particular pointer /
# a particular state), a particular free / a particular count, a particular size / a particular allocation, a
# particular operation). If the base is zero (a null state, a particular state, a particular value, a particular
# pointer / a particular state), it takes one path (a no-op / a return, a particular state, a particular result, a
# particular size / a particular allocation). It clears the array's fields (obj->4 = 0, a base, a particular value, a
# particular pointer / a particular state, obj->8 = 0, a limit, a particular value, a particular size / a particular
# allocation, obj->12 = 0, a size, a particular value, a particular size / a particular allocation, three zeros, a
# particular initialization, a particular size / a particular allocation). So it's a "destruct (a destructor, with a
# release and a free and a complex computation: (if a base (obj->4, a base, a particular value, a particular pointer /
# a particular state) is non-zero, a particular state) loop over a range (a range, a particular range / a particular
# allocation, a base, a particular value, a particular pointer / a particular state, a limit, obj->8, a limit, a
# particular value, a particular size / a particular allocation, a particular state / a particular operation), and for
# each item (a value, a particular value, a particular size / a particular allocation, a particular state / a
# particular operation), release a refcounted value (Release_RefCounted_829FF648, on a value (r31, a value, a
# particular value, a particular pointer / a particular state), a particular release / a particular count, a
# particular size / a particular allocation, a particular operation), and free the array (Free_SizeBucketed_Thunk
# _8221BE68, on a value (obj->4, a base, a particular value, a particular pointer / a particular state), a particular
# free / a particular count, a particular size / a particular allocation, a particular operation), and (otherwise) take
# one path (a no-op / a return, a particular state, a particular result, a particular size / a particular allocation),
# and clear the array's fields (obj->4 = 0, obj->8 = 0, obj->12 = 0, three zeros, a particular initialization, a
# particular size / a particular allocation))" routine (a destructor, part of the 0x82356 cluster, a sibling of
# DestructRange_82518D30 and Destruct20ByteFree_82A11F90, a caller of Release_RefCounted_829FF648 and
# Free_SizeBucketed_Thunk_8221BE68). 60 lines, 2 callees (Free_SizeBucketed_Thunk_8221BE68, Release_RefCounted
# _829FF648). 10 callers: sub_82808AC0, sub_82922CE0, sub_82923448, sub_82723598, sub_82494378, sub_82581968, sub
# _82919220, sub_82B63A10, sub_82925D20, sub_82808888 (a widely-called destructor, part of the game's subsystems). The
# object's class (obj, ->4 = a base, ->8 = a limit, ->12 = a size, a particular structure, an array / a range object, a
# particular size / a particular allocation, a particular value / a particular state) and the value's meaning (a
# value, a particular value, a particular size / a particular allocation) aren't resolvable, so the name states the
# mechanism: a destructor (with a release and a free and a complex computation) ((if a base (obj->4, a base, a
# particular value, a particular pointer / a particular state) is non-zero, a particular state) loop over a range (a
# range, a particular range / a particular allocation, a base, a particular value, a particular pointer / a particular
# state, a limit, obj->8, a limit, a particular value, a particular size / a particular allocation, a particular state
# / a particular operation), and for each item (a value, a particular value, a particular size / a particular
# allocation, a particular state / a particular operation), release a refcounted value (Release_RefCounted_829FF648, on
# a value (r31, a value, a particular value, a particular pointer / a particular state), a particular release / a
# particular count, a particular size / a particular allocation, a particular operation), and free the array
# (Free_SizeBucketed_Thunk_8221BE68, on a value (obj->4, a base, a particular value, a particular pointer / a
# particular state), a particular free / a particular count, a particular size / a particular allocation, a
# particular operation), and (otherwise) take one path (a no-op / a return, a particular state, a particular result, a
# particular size / a particular allocation), and clear the array's fields (obj->4 = 0, obj->8 = 0, obj->12 = 0, three
# zeros, a particular initialization, a particular size / a particular allocation)).
[entrypoint.functions."0x82356698"]
name = "DestructArray_82356698"

# A process (a grow/append, with a byte-by-byte copy and a 4-byte element, the sibling of ProcessGrowAppend2_8321F328
# / ProcessGrowAppend5_83212DC0): (if a count (a->20, a count, a particular count / a particular value) is >= 4, a
# particular threshold, a particular count) grow/append (a byte-by-byte copy, a 4-byte element, a particular size / a
# particular allocation, copying a 4-byte element (four bytes, a particular 4-byte element, a 4-byte element, a
# particular value) from a base (a->12, a base, a 4-byte element, a particular value, a particular source) to a stack
# (a particular target), a particular copy / a particular allocation), and (otherwise) take one path (a no-op / a
# return, a particular state, a particular result, a particular size / a particular allocation), and (in paths) call a
# process (sub_82A1B480, on a value, a particular process, a particular operation, a particular size / a particular
# allocation).

# Args: obj (r3, r28, an object), a (r4, r31, ->4 = a base, ->12 = a base, ->20 = a count, a particular structure, a
# range / a grow object), (more). It checks a count (r11, a->20, a count, a particular count / a particular value): if
# it is >= 4 (a particular threshold, a particular count, a particular value, a particular state), it grows/appends (a
# byte-by-byte copy, a 4-byte element, a particular size / a particular allocation: it computes a count (r9, a->20 - 4
# , a count, a 4-byte element, a particular count / a particular value), and a base (r11, a->12, a base, a 4-byte
# element, a particular value, a particular source), and (r7, r11 + 4, a base, a 4-byte element, a particular value, a
# particular source), and copies a 4-byte element (four bytes, a particular 4-byte element, a 4-byte element, a
# particular value: lbz r5, 0, r11, a byte, a 4-byte element, a particular value, and stb r5, 0, r8, a store, a 4-byte
# element, a particular copy / a particular allocation, and lbz r4, 1, r11, a byte, a 4-byte element, a particular
# value, and stb r4, 1, r8, a store, a 4-byte element, a particular copy / a particular allocation, and lbz r3, 2, r11
# , a byte, a 4-byte element, a particular value, and stb r3, 2, r8, a store, a 4-byte element, a particular copy / a
# particular allocation, and lbz r11, 3, r11, a byte, a 4-byte element, a particular value, and stb r11, 3, r8, a
# store, a 4-byte element, a particular copy / a particular allocation), advancing to the next 4-byte element, a
# particular grow/append, a particular size / a particular allocation). If the count is < 4 (a particular threshold, a
# particular count, a particular value, a particular state), it takes one path (a no-op / a return, a particular
# state, a particular result, a particular size / a particular allocation). In paths it calls a process (sub_82A1B480,
# on a value (a, a value, a particular value, a particular size / a particular allocation), a particular process, a
# particular operation, a particular size / a particular allocation). So it's a "process (a grow/append, with a
# byte-by-byte copy and a 4-byte element: (if a count (a->20, a count, a particular count / a particular value) is >=
# 4, a particular threshold, a particular count) grow/append (a byte-by-byte copy, a 4-byte element, a particular size
# / a particular allocation, copying a 4-byte element (four bytes, a particular 4-byte element, a 4-byte element, a
# particular value) from a base (a->12, a base, a 4-byte element, a particular value, a particular source) to a stack
# (a particular target), a particular copy / a particular allocation), and (otherwise) take one path (a no-op / a
# return, a particular state, a particular result, a particular size / a particular allocation), and (in paths) call a
# process (sub_82A1B480, on a value, a particular process, a particular operation, a particular size / a particular
# allocation))" routine (a grow/append process, part of the 0x83216 cluster, a sibling of ProcessGrowAppend2_8321F328
# and ProcessGrowAppend5_83212DC0). 368 lines, 1 callee (sub_82A1B480, a sibling in the 0x82A1B cluster). 1 caller: sub
# _83221548 (a sibling in the 0x83221 cluster, a process, which calls this to grow/append, a particular size / a
# particular allocation, a particular operation). The object's class (obj, r28, an object, a particular structure, a
# range / a grow object, a particular size / a particular allocation) and the value's meaning (a, r31, ->4 = a base,
# ->12 = a base, ->20 = a count, a particular structure, a range / a grow object, a particular size / a particular
# allocation) aren't resolvable, so the name states the mechanism: a grow/append process ((if a count (a->20, a count,
# a particular count / a particular value) is >= 4, a particular threshold, a particular count) grow/append (a
# byte-by-byte copy, a 4-byte element, a particular size / a particular allocation, copying a 4-byte element (four
# bytes, a particular 4-byte element, a 4-byte element, a particular value) from a base (a->12, a base, a 4-byte
# element, a particular value, a particular source) to a stack (a particular target), a particular copy / a particular
# allocation), and (otherwise) take one path (a no-op / a return, a particular state, a particular result, a
# particular size / a particular allocation), and (in paths) call a process (sub_82A1B480, on a value, a particular
# process, a particular operation, a particular size / a particular allocation)). The "6" distinguishes it from
# ProcessGrowAppend_82BE4AA8, ProcessGrowAppend2_8321F328, ProcessGrowAppend3_8321FC38, ProcessGrowAppend4_8323CB98,
# and ProcessGrowAppend5_83212DC0.
[entrypoint.functions."0x83216060"]
name = "ProcessGrowAppend6_83216060"

# A process (a grow/append, with a byte-by-byte copy and a 4-byte element, the sibling of ProcessGrowAppend2_8321F328
# / ProcessGrowAppend5_83212DC0 / ProcessGrowAppend6_83216060): (if a count (obj->20, a count, a particular count /
# a particular value) is >= 4, a particular threshold, a particular count) grow/append (a byte-by-byte copy, a 4-byte
# element, a particular size / a particular allocation, copying a 4-byte element (four bytes, a particular 4-byte
# element, a 4-byte element, a particular value) from a base (obj->12, a base, a 4-byte element, a particular value, a
# particular source) to a stack (a particular target), a particular copy / a particular allocation), and (otherwise)
# take one path (a no-op / a return, a particular state, a particular result, a particular size / a particular
# allocation), and (in paths) call a process (sub_82A1B480, on a value, a particular process, a particular operation, a
# particular size / a particular allocation).

# Args: obj (r3, r31, ->4 = a base, ->12 = a base, ->20 = a count, a particular structure, a range / a grow object), a
# (r4, r30, a value), (more). It checks a count (r10, obj->20, a count, a particular count / a particular value): if it
# is >= 4 (a particular threshold, a particular count, a particular value, a particular state), it grows/appends (a
# byte-by-byte copy, a 4-byte element, a particular size / a particular allocation: it computes a count (r7, obj->20 -
# 4, a count, a 4-byte element, a particular count / a particular value), and a base (r11, obj->12, a base, a 4-byte
# element, a particular value, a particular source), and (r6, r11 + 4, a base, a 4-byte element, a particular value, a
# particular source), and copies a 4-byte element (four bytes, a particular 4-byte element, a 4-byte element, a
# particular value: lbz r4, 0, r11, a byte, a 4-byte element, a particular value, and stb r4, 0, r8, a store, a 4-byte
# element, a particular copy / a particular allocation, and lbz r3, 1, r11, a byte, a 4-byte element, a particular
# value, and stb r3, 1, r8, a store, a 4-byte element, a particular copy / a particular allocation, and lbz r10, 2,
# r11, a byte, a 4-byte element, a particular value, and stb r10, 2, r8, a store, a 4-byte element, a particular copy /
# a particular allocation, and lbz r9, 3, r11, a byte, a 4-byte element, a particular value, and stb r9, 3, r8, a
# store, a 4-byte element, a particular copy / a particular allocation), advancing to the next 4-byte element, a
# particular grow/append, a particular size / a particular allocation). If the count is < 4 (a particular threshold, a
# particular count, a particular value, a particular state), it takes one path (a no-op / a return, a particular
# state, a particular result, a particular size / a particular allocation). In paths it calls a process (sub_82A1B480,
# on a value (a, a value, a particular value, a particular size / a particular allocation), a particular process, a
# particular operation, a particular size / a particular allocation). So it's a "process (a grow/append, with a
# byte-by-byte copy and a 4-byte element: (if a count (obj->20, a count, a particular count / a particular value) is >=
# 4, a particular threshold, a particular count) grow/append (a byte-by-byte copy, a 4-byte element, a particular size
# / a particular allocation, copying a 4-byte element (four bytes, a particular 4-byte element, a 4-byte element, a
# particular value) from a base (obj->12, a base, a 4-byte element, a particular value, a particular source) to a
# stack (a particular target), a particular copy / a particular allocation), and (otherwise) take one path (a no-op / a
# return, a particular state, a particular result, a particular size / a particular allocation), and (in paths) call a
# process (sub_82A1B480, on a value, a particular process, a particular operation, a particular size / a particular
# allocation))" routine (a grow/append process, part of the 0x83221 cluster, a sibling of ProcessGrowAppend2
# _8321F328, ProcessGrowAppend5_83212DC0, and ProcessGrowAppend6_83216060). 122 lines, 1 callee (sub_82A1B480, a
# sibling in the 0x82A1B cluster). 3 callers: sub_83225A20, sub_832212D0, sub_83225BC0 (siblings in the 0x83225/0
# x83221 clusters, a process, which calls this to grow/append, a particular size / a particular allocation, a
# particular operation). The object's class (obj, ->4 = a base, ->12 = a base, ->20 = a count, a particular
# structure, a range / a grow object, a particular size / a particular allocation) and the value's meaning (a, r30, a
# value, a particular size / a particular allocation) aren't resolvable, so the name states the mechanism: a
# grow/append process ((if a count (obj->20, a count, a particular count / a particular value) is >= 4, a particular
# threshold, a particular count) grow/append (a byte-by-byte copy, a 4-byte element, a particular size / a particular
# allocation, copying a 4-byte element (four bytes, a particular 4-byte element, a 4-byte element, a particular value)
# from a base (obj->12, a base, a 4-byte element, a particular value, a particular source) to a stack (a particular
# target), a particular copy / a particular allocation), and (otherwise) take one path (a no-op / a return, a
# particular state, a particular result, a particular size / a particular allocation), and (in paths) call a process
# (sub_82A1B480, on a value, a particular process, a particular operation, a particular size / a particular
# allocation)). The "7" distinguishes it from ProcessGrowAppend_82BE4AA8, ProcessGrowAppend2_8321F328,
# ProcessGrowAppend3_8321FC38, ProcessGrowAppend4_8323CB98, ProcessGrowAppend5_83212DC0, and ProcessGrowAppend6
# _83216060.
[entrypoint.functions."0x83221138"]
name = "ProcessGrowAppend7_83221138"

# A process (a grow/append, with a complex computation and a constant, the sibling of ProcessGrowAppend2_8321F328 / the
# caller of Allocate_OrDbgBreak_8221F388 / ComputeWrapped_8222C3E8 / SetValue3_82A09FB0 / sub_83221320 / sub_832351B0 /
# sub_82AACDE0): (if a size (a->8 - a->4, a size, a 8-byte element, a particular size) is non-zero, a particular
# state) grow/append (a complex computation, a particular computation / a particular operation, a constant, from a
# fixed .data address, a particular value, a particular size / a particular allocation, a particular operation, calling
# Allocate_OrDbgBreak_8221F388, an allocation, a particular allocation / a particular size, a particular size / a
# particular allocation, and ComputeWrapped_8222C3E8, a compute, a particular computation / a particular operation, a
# particular size / a particular allocation, and SetValue3_82A09FB0, a set, a particular set / a particular value, a
# particular size / a particular allocation), and (otherwise) take one path (a no-op / a return, a particular state, a
# particular result, a particular size / a particular allocation).

# Args: obj (r3, r29, ->12 = a base, ->28 = a value, ->32 = a value, ->36 = a value, a particular structure, a grow /
# a range object), a (r4, r28, ->4 = a base, ->8 = a limit, a particular structure, a range / a grow object), (more). It
# checks a size (r8, a->8 - a->4, a size, a 8-byte element, a particular size, a particular size / a particular
# allocation): if it is non-zero (a particular state, a particular size, a particular value, a particular size / a
# particular allocation), it grows/appends (a complex computation, a particular computation / a particular operation, a
# constant, from a fixed .data address, a particular value, a particular size / a particular allocation, a particular
# operation: it calls a complex computation (sub_83221320, a complex computation, a particular computation / a
# particular operation, a particular size / a particular allocation, a particular state / a particular result), and a
# complex computation (sub_832351B0, on a value (a, a value, a particular value, a particular size / a particular
# allocation), and obj->36, a value, a particular value, a particular size / a particular allocation, and obj->32, a
# value, a particular value, a particular size / a particular allocation), a particular computation / a particular
# operation, a particular size / a particular allocation, a particular state / a particular result), and a complex
# computation (sub_82AACDE0, on a value (obj + 12, a base, a particular value, a particular size / a particular
# allocation), a particular computation / a particular operation, a particular size / a particular allocation, a
# particular state / a particular result), and (in paths) allocates (Allocate_OrDbgBreak_8221F388, sub_8221F388, an
# allocation, on a value (a, a value, a particular value, a particular size, a particular size / a particular
# allocation), a particular allocation / a particular size, a particular size / a particular allocation, a particular
# state / a particular result), and computes (ComputeWrapped_8222C3E8, sub_8222C3E8, a compute, on a value (a, a
# value, a particular value, a particular size, a particular size / a particular allocation), a particular
# computation / a particular operation, a particular size / a particular allocation, a particular state / a particular
# result), and sets (SetValue3_82A09FB0, sub_82A09FB0, a set, on a value (a, a value, a particular value, a
# particular size, a particular size / a particular allocation), a particular set / a particular value, a particular
# size / a particular allocation, a particular state / a particular result). If the size is zero (a null state, a
# particular state, a particular size, a particular value, a particular size / a particular allocation), it takes one
# path (a no-op / a return, a particular state, a particular result, a particular size / a particular allocation). So
# it's a "process (a grow/append, with a complex computation and a constant: (if a size (a->8 - a->4, a size, a 8-byte
# element, a particular size) is non-zero, a particular state) grow/append (a complex computation, a particular
# computation / a particular operation, a constant, from a fixed .data address, a particular value, a particular size /
# a particular allocation, a particular operation, calling Allocate_OrDbgBreak_8221F388, an allocation, a particular
# allocation / a particular size, a particular size / a particular allocation, and ComputeWrapped_8222C3E8, a compute,
# a particular computation / a particular operation, a particular size / a particular allocation, and SetValue3
# _82A09FB0, a set, a particular set / a particular value, a particular size / a particular allocation), and
# (otherwise) take one path (a no-op / a return, a particular state, a particular result, a particular size / a
# particular allocation))" routine (a grow/append process, part of the 0x83221 cluster, a sibling of
# ProcessGrowAppend2_8321F328, a caller of Allocate_OrDbgBreak_8221F388, ComputeWrapped_8222C3E8, and SetValue3
# _82A09FB0). 277 lines, 10 callees (Allocate_OrDbgBreak_8221F388, ComputeWrapped_8222C3E8, SetValue3_82A09FB0, sub
# _82AACDE0, sub_83221320, sub_832351B0, sub_8323FA78, sub_8323FCA8, sub_8323FF48, sub_832402B8). 2 callers: sub
# _832217E8, sub_83221548 (siblings in the 0x83221 cluster, a process, which calls this to grow/append, a particular
# size / a particular allocation, a particular operation). The object's class (obj, ->12 = a base, ->28 = a value, ->
# 32 = a value, ->36 = a value, a particular structure, a grow / a range object, a particular size / a particular
# allocation) and the value's meaning (a, r28, ->4 = a base, ->8 = a limit, a particular structure, a range / a grow
# object, a particular size / a particular allocation) aren't resolvable, so the name states the mechanism: a
# grow/append process ((if a size (a->8 - a->4, a size, a 8-byte element, a particular size) is non-zero, a particular
# state) grow/append (a complex computation, a particular computation / a particular operation, a constant, from a
# fixed .data address, a particular value, a particular size / a particular allocation, a particular operation, calling
# Allocate_OrDbgBreak_8221F388, an allocation, a particular allocation / a particular size, a particular size / a
# particular allocation, and ComputeWrapped_8222C3E8, a compute, a particular computation / a particular operation, a
# particular size / a particular allocation, and SetValue3_82A09FB0, a set, a particular set / a particular value, a
# particular size / a particular allocation), and (otherwise) take one path (a no-op / a return, a particular state, a
# particular result, a particular size / a particular allocation)). The "8" distinguishes it from ProcessGrowAppend
# _82BE4AA8, ProcessGrowAppend2_8321F328, ProcessGrowAppend3_8321FC38, ProcessGrowAppend4_8323CB98, ProcessGrowAppend
# 5_83212DC0, ProcessGrowAppend6_83216060, and ProcessGrowAppend7_83221138.
[entrypoint.functions."0x83221378"]
name = "ProcessGrowAppend8_83221378"

# A process (a grow/append, with a byte-by-byte copy and a 4-byte element, the sibling of ProcessGrowAppend2_8321F328
# / ProcessGrowAppend6_83216060, the caller of ProcessGrowAppend6_83216060 / ProcessGrowAppend8_83221378 / Allocate
# _OrDbgBreak_8221F388 / ConstructNode20_82A12B08 / CopyStructData_83235608): (if a count (a->20, a count, a
# particular count / a particular value) is >= 4, a particular threshold, a particular count) grow/append (a
# byte-by-byte copy, a 4-byte element, a particular size / a particular allocation, copying a 4-byte element (four
# bytes, a particular 4-byte element, a 4-byte element, a particular value) from a base (a->12, a base, a 4-byte
# element, a particular value, a particular source) to a stack (a particular target), a particular copy / a
# particular allocation), and (otherwise) take one path (a no-op / a return, a particular state, a particular result, a
# particular size / a particular allocation), and (in paths) call a grow/append (ProcessGrowAppend6_83216060, on a
# value, a particular grow/append, a particular size / a particular allocation, a particular operation), and a
# grow/append (ProcessGrowAppend8_83221378, on a value, a particular grow/append, a particular size / a particular
# allocation, a particular operation), and allocate (Allocate_OrDbgBreak_8221F388, on a value, a particular
# allocation / a particular size, a particular size / a particular allocation, a particular operation), and construct
# (ConstructNode20_82A12B08, on a value, a particular construct / a particular size, a particular size / a particular
# allocation, a particular operation).

# Args: obj (r3, r22, an object), a (r4, r30, ->4 = a base, ->12 = a base, ->20 = a count, a particular structure, a
# range / a grow object), b (r5, r24, a value), c (r6, r23, a value), (more). It checks a count (r11, a->20, a count, a
# particular count / a particular value): if it is >= 4 (a particular threshold, a particular count, a particular
# value, a particular state), it grows/appends (a byte-by-byte copy, a 4-byte element, a particular size / a
# particular allocation: it computes a count (r9, a->20 - 4, a count, a 4-byte element, a particular count / a
# particular value), and a base (r11, a->12, a base, a 4-byte element, a particular value, a particular source), and
# (r7, r11 + 4, a base, a 4-byte element, a particular value, a particular source), and copies a 4-byte element (four
# bytes, a particular 4-byte element, a 4-byte element, a particular value: lbz r5, 0, r11, a byte, a 4-byte element,
# a particular value, and stb r5, 0, r8, a store, a 4-byte element, a particular copy / a particular allocation, and
# lbz r4, 1, r11, a byte, a 4-byte element, a particular value, and stb r4, 1, r8, a store, a 4-byte element, a
# particular copy / a particular allocation, and lbz r3, 2, r11, a byte, a 4-byte element, a particular value, and stb
# r3, 2, r8, a store, a 4-byte element, a particular copy / a particular allocation, and lbz r11, 3, r11, a byte, a 4
# -byte element, a particular value, and stb r11, 3, r8, a store, a 4-byte element, a particular copy / a particular
# allocation), advancing to the next 4-byte element, a particular grow/append, a particular size / a particular
# allocation). If the count is < 4 (a particular threshold, a particular count, a particular value, a particular
# state), it takes one path (a no-op / a return, a particular state, a particular result, a particular size / a
# particular allocation). In paths it calls a grow/append (ProcessGrowAppend6_83216060, sub_83216060, a grow/append,
# on a value (a, a value, a particular value, a particular size / a particular allocation), a particular grow/append,
# a particular size / a particular allocation, a particular operation), and a grow/append (ProcessGrowAppend8
# _83221378, sub_83221378, a grow/append, on a value (a, a value, a particular value, a particular size / a
# particular allocation), a particular grow/append, a particular size / a particular allocation, a particular
# operation), and allocates (Allocate_OrDbgBreak_8221F388, sub_8221F388, an allocation, on a value (a, a value, a
# particular value, a particular size, a particular size / a particular allocation), a particular allocation / a
# particular size, a particular size / a particular allocation, a particular operation), and constructs (ConstructNode
# 20_82A12B08, sub_82A12B08, a construct, on a value (a, a value, a particular value, a particular size, a particular
# size / a particular allocation), a particular construct / a particular size, a particular size / a particular
# allocation, a particular operation). So it's a "process (a grow/append, with a byte-by-byte copy and a 4-byte
# element: (if a count (a->20, a count, a particular count / a particular value) is >= 4, a particular threshold, a
# particular count) grow/append (a byte-by-byte copy, a 4-byte element, a particular size / a particular allocation,
# copying a 4-byte element (four bytes, a particular 4-byte element, a 4-byte element, a particular value) from a base
# (a->12, a base, a 4-byte element, a particular value, a particular source) to a stack (a particular target), a
# particular copy / a particular allocation), and (otherwise) take one path (a no-op / a return, a particular state, a
# particular result, a particular size / a particular allocation), and (in paths) call a grow/append
# (ProcessGrowAppend6_83216060, on a value, a particular grow/append, a particular size / a particular allocation, a
# particular operation), and a grow/append (ProcessGrowAppend8_83221378, on a value, a particular grow/append, a
# particular size / a particular allocation, a particular operation), and allocate (Allocate_OrDbgBreak_8221F388, on a
# value, a particular allocation / a particular size, a particular size / a particular allocation, a particular
# operation), and construct (ConstructNode20_82A12B08, on a value, a particular construct / a particular size, a
# particular size / a particular allocation, a particular operation))" routine (a grow/append process, part of the
# 0x83221 cluster, a sibling of ProcessGrowAppend2_8321F328, a caller of ProcessGrowAppend6_83216060 and
# ProcessGrowAppend8_83221378). 373 lines, 11 callees (Allocate_OrDbgBreak_8221F388, Allocate_SizeBucketed_Thunk
# _8221F3F0, ConstructNode20_82A12B08, CopyStructData_83235608, Release_RefCounted_829FF648, SetValue4_829FD7B8, sub
# _82356698, sub_82A1B480, sub_82CBBB58, sub_83216060, sub_83221378). No direct callers (it's called via a vtable / it
# is registered-but-uncalled, part of the game's subsystems). The object's class (obj, r22, an object, a particular
# structure, a range / a grow object, a particular size / a particular allocation) and the value's meaning (a, r30, ->
# 4 = a base, ->12 = a base, ->20 = a count, a particular structure, a range / a grow object, a particular size / a
# particular allocation) aren't resolvable, so the name states the mechanism: a grow/append process ((if a count
# (a->20, a count, a particular count / a particular value) is >= 4, a particular threshold, a particular count) grow
# /append (a byte-by-byte copy, a 4-byte element, a particular size / a particular allocation, copying a 4-byte
# element (four bytes, a particular 4-byte element, a 4-byte element, a particular value) from a base (a->12, a base,
# a 4-byte element, a particular value, a particular source) to a stack (a particular target), a particular copy / a
# particular allocation), and (otherwise) take one path (a no-op / a return, a particular state, a particular result, a
# particular size / a particular allocation), and (in paths) call a grow/append (ProcessGrowAppend6_83216060, on a
# value, a particular grow/append, a particular size / a particular allocation, a particular operation), and a
# grow/append (ProcessGrowAppend8_83221378, on a value, a particular grow/append, a particular size / a particular
# allocation, a particular operation), and allocate (Allocate_OrDbgBreak_8221F388, on a value, a particular
# allocation / a particular size, a particular size / a particular allocation, a particular operation), and construct
# (ConstructNode20_82A12B08, on a value, a particular construct / a particular size, a particular size / a particular
# allocation, a particular operation)). The "9" distinguishes it from ProcessGrowAppend_82BE4AA8, ProcessGrowAppend2
# _8321F328, ProcessGrowAppend3_8321FC38, ProcessGrowAppend4_8323CB98, ProcessGrowAppend5_83212DC0, ProcessGrowAppend
# 6_83216060, ProcessGrowAppend7_83221138, and ProcessGrowAppend8_83221378.
[entrypoint.functions."0x83221548"]
name = "ProcessGrowAppend9_83221548"

# A 32-byte constructor (the sibling of Construct32Byte_82B722E8 / Construct32ByteVector_8321FB58, with a constant and
# a complex computation, the caller of Allocate_OrDbgBreak_8221F388 / SetRefcountedWrapper_8238A848): initialize the
# object's fields (a 32-byte object, a particular structure, a particular size / a particular allocation, obj->4 = a
# value, a field, a particular value, obj->8 = 0, a field, a count / a size, a zero, obj->0 = a constant, from a fixed
# .data address, a class id / a marker, a particular class / a particular marker, obj->16 = 0, a field, a count / a
# size, a zero, obj->20 = 0, a field, a count / a size, a zero, obj->24 = 0, a field, a count / a size, a zero, obj->32
# = 1, a field, a value, a one, obj->36 = 0, a field, a count / a size, a zero, obj->40 = 0, a field, a count / a size
# , a zero, obj->44 = a constant, from a fixed .data address, a float, a particular value, obj->48 = a constant, from a
# fixed .data address, a float, a particular value, a 32-byte object, a particular structure, a particular size / a
# particular allocation), and (in paths) allocate (Allocate_OrDbgBreak_8221F388, on a value, a particular allocation /
# a particular size, a particular size / a particular allocation, a particular operation), and set a refcounted
# wrapper (SetRefcountedWrapper_8238A848, on a value, a particular set / a particular wrapper, a particular size / a
# particular allocation, a particular operation).

# Args: obj (r3, r31, ->0 = a field, a constant, a class id / a marker, ->4 = a field, a value, ->8 = a field, a count
# / a size, ->16 = a field, a count / a size, ->20 = a field, a count / a size, ->24 = a field, a count / a size, ->32
# = a field, a value, ->36 = a field, a count / a size, ->40 = a field, a count / a size, ->44 = a field, a float, ->
# 48 = a field, a float, a 32-byte object), a (r4, a value), (more). It initializes the object's fields (a 32-byte
# object, a particular structure, a particular size / a particular allocation: obj->4 = a, a value, a particular
# value, obj->8 = 0, a field, a count / a size, a zero, obj->0 = a constant, from a fixed .data address, a class id /
# a marker, a particular class / a particular marker, obj->16 = 0, a field, a count / a size, a zero, obj->20 = 0, a
# field, a count / a size, a zero, obj->24 = 0, a field, a count / a size, a zero, obj->32 = 1, a field, a value, a
# one, obj->36 = 0, a field, a count / a size, a zero, obj->40 = 0, a field, a count / a size, a zero, obj->44 = a
# constant, from a fixed .data address, a float, a particular value, obj->48 = a constant, from a fixed .data address,
# a float, a particular value, a 32-byte object, a particular structure, a particular size / a particular
# allocation, a particular initialization). In paths it allocates (Allocate_OrDbgBreak_8221F388, sub_8221F388, an
# allocation, on a value (a, a value, a particular value, a particular size, a particular size / a particular
# allocation), a particular allocation / a particular size, a particular size / a particular allocation, a particular
# operation, a particular state / a particular result), and sets a refcounted wrapper (SetRefcountedWrapper_8238A848,
# sub_8238A848, a set, on a value (a, a value, a particular value, a particular size, a particular size / a
# particular allocation), a particular set / a particular wrapper, a particular size / a particular allocation, a
# particular operation, a particular state / a particular result). So it's a "construct a 32-byte object (initialize
# the object's fields (a 32-byte object, a particular structure, a particular size / a particular allocation, obj->4 =
# a value, a field, a particular value, obj->8 = 0, a field, a count / a size, a zero, obj->0 = a constant, from a
# fixed .data address, a class id / a marker, a particular class / a particular marker, obj->16 = 0, a field, a count
# / a size, a zero, obj->20 = 0, a field, a count / a size, a zero, obj->24 = 0, a field, a count / a size, a zero,
# obj->32 = 1, a field, a value, a one, obj->36 = 0, a field, a count / a size, a zero, obj->40 = 0, a field, a count
# / a size, a zero, obj->44 = a constant, from a fixed .data address, a float, a particular value, obj->48 = a
# constant, from a fixed .data address, a float, a particular value, a 32-byte object, a particular structure, a
# particular size / a particular allocation), and (in paths) allocate (Allocate_OrDbgBreak_8221F388, on a value, a
# particular allocation / a particular size, a particular size / a particular allocation, a particular operation), and
# set a refcounted wrapper (SetRefcountedWrapper_8238A848, on a value, a particular set / a particular wrapper, a
# particular size / a particular allocation, a particular operation))" routine (a 32-byte constructor, with a
# constant and a complex computation, part of the 0x83221 cluster, a sibling of Construct32Byte_82B722E8 and
# Construct32ByteVector_8321FB58, a caller of Allocate_OrDbgBreak_8221F388 and SetRefcountedWrapper_8238A848). 117
# lines, 2 callees (Allocate_OrDbgBreak_8221F388, SetRefcountedWrapper_8238A848). 1 caller: sub_83221A60 (a sibling in
# the 0x83221 cluster, a process, which calls this to construct a 32-byte object, a particular size / a particular
# allocation, a particular operation). The object's class (obj, ->0 = a field, a constant, a class id / a marker, ->
# 4 = a field, a value, ->8 = a field, a count / a size, ->16 = a field, a count / a size, ->20 = a field, a count /
# a size, ->24 = a field, a count / a size, ->32 = a field, a value, ->36 = a field, a count / a size, ->40 = a
# field, a count / a size, ->44 = a field, a float, ->48 = a field, a float, a 32-byte object, a particular
# structure, a particular size / a particular allocation, a particular value / a particular state) and the value's
# meaning (a, a value, a particular value, a particular size / a particular allocation) aren't resolvable, so the
# name states the mechanism: a 32-byte constructor (with a constant and a complex computation) (initialize the
# object's fields (a 32-byte object, a particular structure, a particular size / a particular allocation, obj->4 = a
# value, a field, a particular value, obj->8 = 0, a field, a count / a size, a zero, obj->0 = a constant, from a
# fixed .data address, a class id / a marker, a particular class / a particular marker, obj->16 = 0, a field, a count
# / a size, a zero, obj->20 = 0, a field, a count / a size, a zero, obj->24 = 0, a field, a count / a size, a zero,
# obj->32 = 1, a field, a value, a one, obj->36 = 0, a field, a count / a size, a zero, obj->40 = 0, a field, a count
# / a size, a zero, obj->44 = a constant, from a fixed .data address, a float, a particular value, obj->48 = a
# constant, from a fixed .data address, a float, a particular value, a 32-byte object, a particular structure, a
# particular size / a particular allocation), and (in paths) allocate (Allocate_OrDbgBreak_8221F388, on a value, a
# particular allocation / a particular size, a particular size / a particular allocation, a particular operation), and
# set a refcounted wrapper (SetRefcountedWrapper_8238A848, on a value, a particular set / a particular wrapper, a
# particular size / a particular allocation, a particular operation)). The "2" distinguishes it from Construct32Byte
# _82B722E8 and Construct32ByteVector_8321FB58.
[entrypoint.functions."0x83221998"]
name = "Construct32Byte2_83221998"

# A process (a lookup, with a release and a complex computation, the caller of WalkChainFind_83239FA8 /
# Construct32Byte2_83221998 / Allocate_OrDbgBreak_8221F388 / SetRefcountedWrapper_8238A848 / Release_RefCounted_829FF
# 648): perform a lookup (a lookup, a particular lookup / a particular search, a particular size / a particular
# allocation, a particular operation, calling WalkChainFind_83239FA8, a lookup, a particular lookup / a particular
# search, a particular size / a particular allocation, a particular operation), and (if a flag (r3 & 0xFF, a flag, a
# particular value, a particular state, a particular size / a particular allocation) is non-zero, a particular state)
# release (Release_RefCounted_829FF648, on a value, a particular release / a particular count, a particular size / a
# particular allocation, a particular operation), and clear the object's fields (obj->0 = 0, obj->4 = 0, two zeros, a
# particular initialization, a particular size / a particular allocation, a particular state / a particular result),
# and (otherwise) construct (Construct32Byte2_83221998, on a value, a particular construct / a particular size, a
# particular size / a particular allocation, a particular operation), and allocate (Allocate_OrDbgBreak_8221F388, on a
# value, a particular allocation / a particular size, a particular size / a particular allocation, a particular
# operation), and set a refcounted wrapper (SetRefcountedWrapper_8238A848, on a value, a particular set / a
# particular wrapper, a particular size / a particular allocation, a particular operation).

# Args: obj (r3, r31, ->0 = a field, a value, ->4 = a field, a value, a particular structure, a lookup object, a
# particular size / a particular allocation), a (r4, r29, a value), (more). It performs a lookup (a lookup, a
# particular lookup / a particular search, a particular size / a particular allocation, a particular operation, calling
# WalkChainFind_83239FA8, sub_83239FA8, a lookup, on a value (stack[80], a value, a particular value, a particular
# size / a particular allocation, a particular target), and a constant (a constant, from a fixed .data address, a
# particular value, a particular key, a particular size / a particular allocation), a particular lookup / a
# particular search, a particular size / a particular allocation, a particular operation, a particular state / a
# particular result). It checks a flag (r10, r3 & 0xFF, a flag, a particular value, a particular state, a particular
# size / a particular allocation): if the flag is non-zero (a particular state, a particular value, a particular size
# / a particular allocation), it releases (Release_RefCounted_829FF648, sub_829FF648, a release, on a value (stack[80]
# , a value, a particular value, a particular size / a particular allocation, a particular target), a particular
# release / a particular count, a particular size / a particular allocation, a particular operation, a particular
# state / a particular result), and clears the object's fields (obj->0 = 0, a field, a value, a zero, obj->4 = 0, a
# field, a value, a zero, two zeros, a particular initialization, a particular size / a particular allocation, a
# particular state / a particular result); if the flag is zero (a null state, a particular state, a particular size /
# a particular allocation), it constructs (Construct32Byte2_83221998, sub_83221998, a 32-byte constructor, on a value
# (a, a value, a particular value, a particular size, a particular size / a particular allocation), a particular
# construct / a particular size, a particular size / a particular allocation, a particular operation, a particular
# state / a particular result), and allocates (Allocate_OrDbgBreak_8221F388, sub_8221F388, an allocation, on a value
# (a, a value, a particular value, a particular size, a particular size / a particular allocation), a particular
# allocation / a particular size, a particular size / a particular allocation, a particular operation, a particular
# state / a particular result), and sets a refcounted wrapper (SetRefcountedWrapper_8238A848, sub_8238A848, a set, on
# a value (a, a value, a particular value, a particular size, a particular size / a particular allocation), a
# particular set / a particular wrapper, a particular size / a particular allocation, a particular operation, a
# particular state / a particular result). So it's a "process (a lookup, with a release and a complex computation: perform
# a lookup (a lookup, a particular lookup / a particular search, a particular size / a particular allocation, a
# particular operation, calling WalkChainFind_83239FA8, a lookup, a particular lookup / a particular search, a
# particular size / a particular allocation, a particular operation), and (if a flag (r3 & 0xFF, a flag, a
# particular value, a particular state, a particular size / a particular allocation) is non-zero, a particular state)
# release (Release_RefCounted_829FF648, on a value, a particular release / a particular count, a particular size / a
# particular allocation, a particular operation), and clear the object's fields (obj->0 = 0, obj->4 = 0, two zeros, a
# particular initialization, a particular size / a particular allocation, a particular state / a particular result),
# and (otherwise) construct (Construct32Byte2_83221998, on a value, a particular construct / a particular size, a
# particular size / a particular allocation, a particular operation), and allocate (Allocate_OrDbgBreak_8221F388, on a
# value, a particular allocation / a particular size, a particular size / a particular allocation, a particular
# operation), and set a refcounted wrapper (SetRefcountedWrapper_8238A848, on a value, a particular set / a
# particular wrapper, a particular size / a particular allocation, a particular operation))" routine (a lookup process
# , part of the 0x83221 cluster, a sibling of Construct32Byte2_83221998, a caller of WalkChainFind_83239FA8,
# Construct32Byte2_83221998, Allocate_OrDbgBreak_8221F388, and SetRefcountedWrapper_8238A848). 137 lines, 6 callees
# (Allocate_OrDbgBreak_8221F388, Construct32Byte2_83221998, Release_RefCounted_829FF648, SetRefcountedWrapper_8238A848
# , WalkChainFind_83239FA8, sub_832259A0). 3 callers: sub_83225AE0, sub_83225A20, sub_83225BC0 (siblings in the
# 0x83225 cluster, a process, which calls this to perform the lookup, a particular size / a particular allocation, a
# particular operation). The object's class (obj, ->0 = a field, a value, ->4 = a field, a value, a particular
# structure, a lookup object, a particular size / a particular allocation, a particular value / a particular state) and
# the value's meaning (a, r29, a value, a particular size / a particular allocation) aren't resolvable, so the name
# states the mechanism: a lookup process (with a release and a complex computation) (perform a lookup (a lookup, a
# particular lookup / a particular search, a particular size / a particular allocation, a particular operation, calling
# WalkChainFind_83239FA8, a lookup, a particular lookup / a particular search, a particular size / a particular
# allocation, a particular operation), and (if a flag (r3 & 0xFF, a flag, a particular value, a particular state, a
# particular size / a particular allocation) is non-zero, a particular state) release (Release_RefCounted_829FF648, on
# a value, a particular release / a particular count, a particular size / a particular allocation, a particular
# operation), and clear the object's fields (obj->0 = 0, obj->4 = 0, two zeros, a particular initialization, a
# particular size / a particular allocation, a particular state / a particular result), and (otherwise) construct
# (Construct32Byte2_83221998, on a value, a particular construct / a particular size, a particular size / a
# particular allocation, a particular operation), and allocate (Allocate_OrDbgBreak_8221F388, on a value, a
# particular allocation / a particular size, a particular size / a particular allocation, a particular operation), and
# set a refcounted wrapper (SetRefcountedWrapper_8238A848, on a value, a particular set / a particular wrapper, a
# particular size / a particular allocation, a particular operation)). The "LookupConstruct" refers to the lookup (a
# lookup, a particular lookup / a particular search, a particular size / a particular allocation, a particular
# operation) and the construct (a construct, a particular construct / a particular size, a particular size / a
# particular allocation, a particular operation).
[entrypoint.functions."0x83221A60"]
name = "ProcessLookupConstruct_83221A60"

# A process (a reference count, with a release and an increment, the sibling of ProcessRefCount_83225538, the caller
# of Release_RefCounted_829FF648 / LookupAndAssign_8323A1E8): (if a value (obj->4, a value, a reference count, a
# particular value) is non-zero, a particular value) release a refcounted value (Release_RefCounted_829FF648, on a
# value, a particular release, a particular count), and increment a reference count (a value (obj->4, a value, a
# reference count, a particular value), a reference count, a particular count), and (otherwise) take one path, and
# call a lookup (LookupAndAssign_8323A1E8, on a value and a constant, a particular lookup).

# Args: obj (r3, ->0 = a value, ->4 = a value, a reference count, a particular structure, a reference counted object),
# (more). It checks a value (obj->4, a value, a reference count, a particular value): if it is non-zero (a
# particular value, a particular state), it releases a refcounted value (Release_RefCounted_829FF648, sub_829FF648,
# a refcounted release, on a value (stack[80], a value, a particular value), a particular release, a particular count
# ), and increments a reference count (a value (r31, obj->4, a value, a reference count, a particular value), a
# reference count, a particular count: it reads the count (r11, r31->0, a reference count, a particular count), and
# stores it (r11 + 1, an increment, a particular count, a particular reference count). If it is zero (a null state, a
# particular state), it takes one path (a no-op / a return, a particular state). It calls a lookup (LookupAndAssign
# _8323A1E8, sub_8323A1E8, a lookup and assign, on a value (stack[80], a value, a particular value) and a constant (a
# constant, from a fixed .data address, a particular value, a particular key), a particular lookup, a particular
# assignment). So it's a "process (a reference count: (if a value, obj->4, is non-zero, a particular value) release a
# refcounted value (Release_RefCounted_829FF648, on a value, a particular release), and increment a reference count
# (a value (obj->4, a value, a reference count, a particular value), a reference count, a particular count), and
# (otherwise) take one path, and call a lookup (LookupAndAssign_8323A1E8, on a value and a constant, a particular
# lookup))" routine (a reference count process, part of the 0x83225 cluster, a sibling of ProcessRefCount_83225538, a
# caller of Release_RefCounted_829FF648 and LookupAndAssign_8323A1E8). 73 lines, 2 callees (LookupAndAssign_8323A1E8,
# Release_RefCounted_829FF648). 1 caller: sub_83221A60 (ProcessLookupConstruct, a lookup process, which calls this to
# process the reference count, a particular count / a particular state, a particular size / a particular allocation).
# The object's class (obj, ->0 = a value, ->4 = a value, a reference count, a particular structure, a reference
# counted object) and the value's meaning (a value, a reference count, a particular value, a particular count) aren't
# resolvable, so the name states the mechanism: a reference count process ((if a value, obj->4, is non-zero, a
# particular value) release a refcounted value (Release_RefCounted_829FF648, on a value, a particular release), and
# increment a reference count (a value (obj->4, a value, a reference count, a particular value), a reference count, a
# particular count), and (otherwise) take one path, and call a lookup (LookupAndAssign_8323A1E8, on a value and a
# constant, a particular lookup)). The "2" distinguishes it from ProcessRefCount_83225538.
[entrypoint.functions."0x832259A0"]
name = "ProcessRefCount2_832259A0"

# A process (an allocation, with a check and a call, the caller of Allocate_OrDbgBreak_8221F388 / sub_822F1F00): (if a
# size (a size, a particular size, a particular value, a particular size / a particular allocation) is non-zero, a
# particular value) perform a check (a check, a particular check / a particular state, a particular size / a
# particular allocation, a particular operation, calling sub_822F1F00, on a value (stack[80], a value, a particular
# value, a particular size / a particular allocation, a particular target), and a constant (a constant, from a fixed
# .data address, a particular value, a particular size / a particular allocation, a particular state), a particular
# check / a particular state, a particular size / a particular allocation, a particular operation, a particular state
# / a particular result), and (otherwise) take one path (a no-op / a return, a particular state, a particular result,
# a particular size / a particular allocation), and allocate (Allocate_OrDbgBreak_8221F388, on a value (a, a value, a
# particular value, a particular size, a particular size / a particular allocation), a particular allocation / a
# particular size, a particular size / a particular allocation, a particular operation, a particular state / a
# particular result).

# Args: a (r3, a value), size (r4, r31, a size, a particular size, a particular value, a particular size / a
# particular allocation), (more). It checks a size (r31, a, a size, a particular size, a particular value, a
# particular size / a particular allocation): if it is non-zero (a particular value, a particular state, a particular
# size / a particular allocation), it performs a check (a check, a particular check / a particular state, a
# particular size / a particular allocation, a particular operation, calling sub_822F1F00, sub_822F1F00, on a value
# (stack[80], a value, a particular value, a particular size / a particular allocation, a particular target), and a
# constant (a constant, from a fixed .data address, a particular value, a particular size / a particular allocation, a
# particular state), a particular check / a particular state, a particular size / a particular allocation, a
# particular operation, a particular state / a particular result); if it is zero (a null state, a particular state, a
# particular size / a particular allocation), it takes one path (a no-op / a return, a particular state, a
# particular result, a particular size / a particular allocation). It allocates (Allocate_OrDbgBreak_8221F388,
# sub_8221F388, an allocation, on a value (a, a value, a particular value, a particular size, a particular size / a
# particular allocation), a particular allocation / a particular size, a particular size / a particular allocation, a
# particular operation, a particular state / a particular result). So it's a "process (an allocation, with a check and
# a call: (if a size (a size, a particular size, a particular value, a particular size / a particular allocation) is
# non-zero, a particular value) perform a check (a check, a particular check / a particular state, a particular size /
# a particular allocation, a particular operation, calling sub_822F1F00, on a value (stack[80], a value, a
# particular value, a particular size / a particular allocation, a particular target), and a constant (a constant,
# from a fixed .data address, a particular value, a particular size / a particular allocation, a particular state), a
# particular check / a particular state, a particular size / a particular allocation, a particular operation, a
# particular state / a particular result), and (otherwise) take one path (a no-op / a return, a particular state, a
# particular result, a particular size / a particular allocation), and allocate (Allocate_OrDbgBreak_8221F388, on a
# value (a, a value, a particular value, a particular size, a particular size / a particular allocation), a
# particular allocation / a particular size, a particular size / a particular allocation, a particular operation, a
# particular state / a particular result))" routine (an allocation check process, part of the 0x82301 cluster, a
# caller of Allocate_OrDbgBreak_8221F388 and sub_822F1F00). 66 lines, 2 callees (Allocate_OrDbgBreak_8221F388, sub
# _822F1F00). 4 callers: sub_82301240, sub_823012B0, sub_82B28F48, sub_82301878 (siblings in the 0x82301/0x82B28
# clusters, a process, which calls this to perform the allocation check, a particular size / a particular allocation,
# a particular operation). The value's meaning (a, a value, a particular value, a particular size / a particular
# allocation) isn't resolvable, so the name states the mechanism: an allocation check process (with a check and a
# call) ((if a size (a size, a particular size, a particular value, a particular size / a particular allocation) is
# non-zero, a particular value) perform a check (a check, a particular check / a particular state, a particular size /
# a particular allocation, a particular operation, calling sub_822F1F00, on a value (stack[80], a value, a
# particular value, a particular size / a particular allocation, a particular target), and a constant (a constant,
# from a fixed .data address, a particular value, a particular size / a particular allocation, a particular state), a
# particular check / a particular state, a particular size / a particular allocation, a particular operation, a
# particular state / a particular result), and (otherwise) take one path (a no-op / a return, a particular state, a
# particular result, a particular size / a particular allocation), and allocate (Allocate_OrDbgBreak_8221F388, on a
# value (a, a value, a particular value, a particular size, a particular size / a particular allocation), a
# particular allocation / a particular size, a particular size / a particular allocation, a particular operation, a
# particular state / a particular result)). The "AllocateCheck" refers to the allocation (an allocation, a
# particular allocation / a particular size, a particular size / a particular allocation, a particular operation) and
# the check (a check, a particular check / a particular state, a particular size / a particular allocation, a
# particular operation).
[entrypoint.functions."0x823014D0"]
name = "ProcessAllocateCheck_823014D0"

# A process (a grow/copy, with a range check and a call, the caller of sub_823012B0 / CheckedCopy_82CA3808, the
# sibling of ProcessAllocateCheck_823014D0): (if a value (a, a value, a particular value, a particular size / a
# particular allocation) is <= a size (obj->8 - obj->4, a size, a particular size, a particular size / a particular
# allocation), a particular size) perform a range check (a range check, a particular range / a particular check, a
# particular size / a particular allocation, a particular operation, a particular state / a particular result), and
# (otherwise) grow/copy (a grow/copy, a particular grow / a particular copy, a particular size / a particular
# allocation, a particular operation, a particular state / a particular result, calling sub_823012B0, on a value
# (stack[80], a value, a particular value, a particular size / a particular allocation, a particular target), and a
# value (stack[84], a value, a particular value, a particular size / a particular allocation, a particular source),
# and a value (a, a value, a particular value, a particular size / a particular allocation, a particular size / a
# particular allocation), a particular grow / a particular copy, a particular size / a particular allocation, a
# particular operation, a particular state / a particular result), and (in paths) copy (CheckedCopy_82CA3808, on a
# value, a particular copy, a particular size / a particular allocation, a particular operation, a particular state /
# a particular result).

# Args: obj (r3, r31, ->4 = a base, ->8 = a limit, a particular structure, a grow object), a (r4, a value, a
# particular value, a particular size / a particular allocation), b (r5, r6, a value, a particular value, a
# particular size / a particular allocation), (more). It checks a size (r9, obj->8 - obj->4, a size, a particular
# size, a particular size / a particular allocation): if a value (r4, a, a value, a particular value, a particular
# size / a particular allocation) is <= the size (a particular size, a particular state, a particular size / a
# particular allocation), it performs a range check (a range check, a particular range / a particular check, a
# particular size / a particular allocation, a particular operation, a particular state / a particular result); if
# the value is > the size (a particular size, a particular state, a particular size / a particular allocation), it
# grows/copies (a grow/copy, a particular grow / a particular copy, a particular size / a particular allocation, a
# particular operation, a particular state / a particular result, calling sub_823012B0, sub_823012B0, a grow/copy, on
# a value (stack[80], a value, a particular value, a particular size / a particular allocation, a particular
# target), and a value (stack[84], a value, a particular value, a particular size / a particular allocation, a
# particular source), and a value (a, a value, a particular value, a particular size / a particular allocation, a
# particular size / a particular allocation), a particular grow / a particular copy, a particular size / a
# particular allocation, a particular operation, a particular state / a particular result). In paths it copies
# (CheckedCopy_82CA3808, sub_82CA3808, a checked copy, on a value (a, a value, a particular value, a particular size,
# a particular size / a particular allocation), a particular copy, a particular size / a particular allocation, a
# particular operation, a particular state / a particular result). So it's a "process (a grow/copy, with a range check
# and a call: (if a value (a, a value, a particular value, a particular size / a particular allocation) is <= a size
# (obj->8 - obj->4, a size, a particular size, a particular size / a particular allocation), a particular size)
# perform a range check (a range check, a particular range / a particular check, a particular size / a particular
# allocation, a particular operation, a particular state / a particular result), and (otherwise) grow/copy (a
# grow/copy, a particular grow / a particular copy, a particular size / a particular allocation, a particular
# operation, a particular state / a particular result, calling sub_823012B0, on a value (stack[80], a value, a
# particular value, a particular size / a particular allocation, a particular target), and a value (stack[84], a
# value, a particular value, a particular size / a particular allocation, a particular source), and a value (a, a
# value, a particular value, a particular size / a particular allocation, a particular size / a particular
# allocation), a particular grow / a particular copy, a particular size / a particular allocation, a particular
# operation, a particular state / a particular result), and (in paths) copy (CheckedCopy_82CA3808, on a value, a
# particular copy, a particular size / a particular allocation, a particular operation, a particular state / a
# particular result))" routine (a grow/copy process, part of the 0x82300 cluster, a sibling of
# ProcessAllocateCheck_823014D0, a caller of sub_823012B0 and CheckedCopy_82CA3808). 87 lines, 2 callees (sub
# _823012B0, CheckedCopy_82CA3808). 10+ callers: sub_82A855A8, sub_823C2148, sub_822FDF78, sub_82886658, sub_82B31CA0
# , sub_82B4A230, sub_82B88670, sub_82A4AE70, sub_824E7A50, sub_82B4A4F8 (processes, which call this to grow/copy, a
# particular size / a particular allocation, a particular operation). The object's class (obj, ->4 = a base, ->8 = a
# limit, a particular structure, a grow object, a particular size / a particular allocation, a particular value /
# a particular state) and the value's meaning (a, a value, a particular value, a particular size / a particular
# allocation, a particular size / a particular allocation) aren't resolvable, so the name states the mechanism: a
# grow/copy process (with a range check and a call) ((if a value (a, a value, a particular value, a particular size /
# a particular allocation) is <= a size (obj->8 - obj->4, a size, a particular size, a particular size / a
# particular allocation), a particular size) perform a range check (a range check, a particular range / a
# particular check, a particular size / a particular allocation, a particular operation, a particular state / a
# particular result), and (otherwise) grow/copy (a grow/copy, a particular grow / a particular copy, a
# particular size / a particular allocation, a particular operation, a particular state / a particular result, calling
# sub_823012B0, on a value (stack[80], a value, a particular value, a particular size / a particular allocation, a
# particular target), and a value (stack[84], a value, a particular value, a particular size / a particular
# allocation, a particular source), and a value (a, a value, a particular value, a particular size / a
# particular allocation, a particular size / a particular allocation), a particular grow / a particular copy, a
# particular size / a particular allocation, a particular operation, a particular state / a particular result), and
# (in paths) copy (CheckedCopy_82CA3808, on a value, a particular copy, a particular size / a particular allocation, a
# particular operation, a particular state / a particular result)). The "GrowCopy" refers to the grow/copy (a
# grow/copy, a particular grow / a particular copy, a particular size / a particular allocation, a
# particular operation, a particular state / a particular result).
[entrypoint.functions."0x82300648"]
name = "ProcessGrowCopy_82300648"

# A process (a grow/append, with a range check and a copy, the sibling of ProcessGrowAppend_82BE4AA8 /
# ProcessGrowAppend2_8321F328 / ProcessGrowCopy_82300648, the caller of sub_82A97648 / CheckedCopy_82CA3808 / Free
# _SizeBucketed_Thunk_8221BE68 / ProcessAllocateCheck_823014D0): (if a base (obj->4, a base, a particular value, a
# particular size / a particular allocation) is non-zero, a particular value) compute a size (obj->12 - obj->4, a
# size, a particular size, a particular size / a particular allocation, a particular operation), and (if a count
# (obj->8 - obj->4 - 1, a count, a particular count, a particular size / a particular allocation) is >= a value
# (r29, a count, a particular count, a particular size / a particular allocation), a particular count) perform a
# range check (a range check, a particular range / a particular check, a particular size / a particular allocation, a
# particular operation, a particular state / a particular result), and (otherwise) grow/append (a grow/append, a
# particular grow / a particular append, a particular size / a particular allocation, a particular operation, a
# particular state / a particular result, calling sub_82A97648, on a value, a particular grow / a particular
# append, a particular size / a particular allocation, a particular operation, a particular state / a
# particular result), and (in paths) copy (CheckedCopy_82CA3808, on a value, a particular copy, a particular size /
# a particular allocation, a particular operation, a particular state / a particular result), and free
# (Free_SizeBucketed_Thunk_8221BE68, on a value, a particular free / a particular size, a particular size / a
# particular allocation, a particular operation, a particular state / a particular result), and allocate
# (ProcessAllocateCheck_823014D0, on a value, a particular allocation / a particular check, a particular size / a
# particular allocation, a particular operation, a particular state / a particular result).

# Args: obj (r3, r28, ->4 = a base, ->8 = a limit, ->12 = a base, a particular structure, a grow object), a (r4, a
# value), count (r5, r29, a count, a particular count, a particular size / a particular allocation), b (r6, a value,
# a particular value, a particular size / a particular allocation), (more). It checks a base (r11, obj->4, a base, a
# particular value, a particular size / a particular allocation): if it is non-zero (a particular value, a
# particular state, a particular size / a particular allocation), it computes a size (r10, obj->12 - obj->4, a size, a
# particular size, a particular size / a particular allocation, a particular operation). It checks a count (r29, a
# count, a particular count, a particular size / a particular allocation): if it is zero (a null state, a
# particular state, a particular size / a particular allocation), it takes one path (a no-op / a return, a
# particular state, a particular result, a particular size / a particular allocation). If it is non-zero (a
# particular value, a particular state, a particular size / a particular allocation), it checks a count (r9, obj->8
# - obj->4 - 1, a count, a particular count, a particular size / a particular allocation): if it is >= the count (a
# particular count, a particular state, a particular size / a particular allocation), it performs a range check (a
# range check, a particular range / a particular check, a particular size / a particular allocation, a particular
# operation, a particular state / a particular result); if it is < the count (a particular count, a particular
# state, a particular size / a particular allocation), it grows/appends (a grow/append, a particular grow / a
# particular append, a particular size / a particular allocation, a particular operation, a particular state / a
# particular result, calling sub_82A97648, sub_82A97648, a grow/append, on a value (a, a value, a particular value,
# a particular size, a particular size / a particular allocation), a particular grow / a particular append, a
# particular size / a particular allocation, a particular operation, a particular state / a particular result). In
# paths it copies (CheckedCopy_82CA3808, sub_82CA3808, a checked copy, on a value (a, a value, a particular value, a
# particular size, a particular size / a particular allocation), a particular copy, a particular size / a
# particular allocation, a particular operation, a particular state / a particular result), frees
# (Free_SizeBucketed_Thunk_8221BE68, sub_8221BE68, a free, on a value (a, a value, a particular value, a particular
# size, a particular size / a particular allocation), a particular free / a particular size, a particular size / a
# particular allocation, a particular operation, a particular state / a particular result), and allocates
# (ProcessAllocateCheck_823014D0, sub_823014D0, an allocation, on a value (a, a value, a particular value, a
# particular size, a particular size / a particular allocation), a particular allocation / a particular check, a
# particular size / a particular allocation, a particular operation, a particular state / a particular result). So
# it's a "process (a grow/append, with a range check and a copy: (if a base (obj->4, a base, a particular value, a
# particular size / a particular allocation) is non-zero, a particular value) compute a size (obj->12 - obj->4, a
# size, a particular size, a particular size / a particular allocation, a particular operation), and (if a count
# (obj->8 - obj->4 - 1, a count, a particular count, a particular size / a particular allocation) is >= a value
# (r29, a count, a particular count, a particular size / a particular allocation), a particular count) perform a
# range check (a range check, a particular range / a particular check, a particular size / a particular allocation, a
# particular operation, a particular state / a particular result), and (otherwise) grow/append (a grow/append, a
# particular grow / a particular append, a particular size / a particular allocation, a particular operation, a
# particular state / a particular result, calling sub_82A97648, on a value, a particular grow / a
# particular append, a particular size / a particular allocation, a particular operation, a particular state / a
# particular result), and (in paths) copy (CheckedCopy_82CA3808, on a value, a particular copy, a particular size /
# a particular allocation, a particular operation, a particular state / a particular result), and free
# (Free_SizeBucketed_Thunk_8221BE68, on a value, a particular free / a particular size, a particular size / a
# particular allocation, a particular operation, a particular state / a particular result), and allocate
# (ProcessAllocateCheck_823014D0, on a value, a particular allocation / a particular check, a particular size / a
# particular allocation, a particular operation, a particular state / a particular result))" routine (a grow/append
# process, part of the 0x82301 cluster, a sibling of ProcessGrowCopy_82300648, a caller of CheckedCopy_82CA3808,
# Free_SizeBucketed_Thunk_8221BE68, and ProcessAllocateCheck_823014D0). 324 lines, 4 callees (CheckedCopy_82CA3808,
# Free_SizeBucketed_Thunk_8221BE68, sub_823014D0, sub_82A97648). 2 callers: sub_82B29020, sub_82300648 (ProcessGrow
# Copy, a grow/copy process, which calls this to grow/append, a particular size / a particular allocation, a
# particular operation). The object's class (obj, ->4 = a base, ->8 = a limit, ->12 = a base, a particular
# structure, a grow object, a particular size / a particular allocation, a particular value / a
# particular state) and the value's meaning (a, a value, a particular value, a particular size / a
# particular allocation) aren't resolvable, so the name states the mechanism: a grow/append process (with a range
# check and a copy) ((if a base (obj->4, a base, a particular value, a particular size / a particular allocation) is
# non-zero, a particular value) compute a size (obj->12 - obj->4, a size, a particular size, a
# particular size / a particular allocation, a particular operation), and (if a count (obj->8 - obj->4 - 1, a
# count, a particular count, a particular size / a particular allocation) is >= a value (r29, a count, a
# particular count, a particular size / a particular allocation), a particular count) perform a range check (a
# range check, a particular range / a particular check, a particular size / a particular allocation, a
# particular operation, a particular state / a particular result), and (otherwise) grow/append (a grow/append, a
# particular grow / a particular append, a particular size / a particular allocation, a particular operation, a
# particular state / a particular result, calling sub_82A97648, on a value, a particular grow / a
# particular append, a particular size / a particular allocation, a particular operation, a particular state / a
# particular result), and (in paths) copy (CheckedCopy_82CA3808, on a value, a particular copy, a
# particular size / a particular allocation, a particular operation, a particular state / a particular result), and
# free (Free_SizeBucketed_Thunk_8221BE68, on a value, a particular free / a particular size, a
# particular size / a particular allocation, a particular operation, a particular state / a
# particular result), and allocate (ProcessAllocateCheck_823014D0, on a value, a
# particular allocation / a particular check, a particular size / a particular allocation, a
# particular operation, a particular state / a particular result)). The "10" distinguishes it from ProcessGrowAppend
# _82BE4AA8, ProcessGrowAppend2_8321F328, ProcessGrowAppend3_8321FC38, ProcessGrowAppend4_8323CB98, ProcessGrowAppend
# 5_83212DC0, ProcessGrowAppend6_83216060, ProcessGrowAppend7_83221138, ProcessGrowAppend8_83221378, and
# ProcessGrowAppend9_83221548.
[entrypoint.functions."0x823012B0"]
name = "ProcessGrowAppend10_823012B0"

# A process (an increment, with a copy, the caller of CheckedCopy_82CA3808, the sibling of ProcessGrowAppend10_82301
# 2B0): (if a base (obj->4, a base, a particular value, a particular size / a particular allocation) is != a limit
# (obj->8, a limit, a particular value, a particular size / a particular allocation), a particular value) perform an
# increment (an increment, a particular increment / a particular count, a particular size / a particular allocation, a
# particular operation, a particular state / a particular result), and copy (CheckedCopy_82CA3808, on a value, a
# particular copy, a particular size / a particular allocation, a particular operation, a particular state / a
# particular result), and store the new limit (obj->8 = r30, a store, a limit, a particular value, a particular size /
# a particular allocation, a particular state / a particular result), and (otherwise) take one path (a no-op / a
# return, a particular state, a particular result, a particular size / a particular allocation).

# Args: obj (r3, r31, ->4 = a base, ->8 = a limit, a particular structure, a grow object), (more). It checks a base
# (r3, obj->4, a base, a particular value, a particular size / a particular allocation): if it is != a limit (r5,
# obj->8, a limit, a particular value, a particular size / a particular allocation, a particular state), it performs an
# increment (an increment, a particular increment / a particular count, a particular size / a particular allocation, a
# particular operation, a particular state / a particular result), and copies (CheckedCopy_82CA3808, sub_82CA3808, a
# checked copy, on a value (r6, a zero, a particular value, a particular size / a particular allocation, a
# particular source), a particular copy, a particular size / a particular allocation, a particular operation, a
# particular state / a particular result), and stores the new limit (obj->8 = r30, a store, a limit, a particular
# value, a particular size / a particular allocation, a particular state / a particular result). If it is == the
# limit (a particular state, a particular value, a particular size / a particular allocation), it takes one path (a
# no-op / a return, a particular state, a particular result, a particular size / a particular allocation). So it's a
# "process (an increment, with a copy: (if a base (obj->4, a base, a particular value, a particular size / a
# particular allocation) is != a limit (obj->8, a limit, a particular value, a particular size / a particular
# allocation), a particular value) perform an increment (an increment, a particular increment / a
# particular count, a particular size / a particular allocation, a particular operation, a particular state / a
# particular result), and copy (CheckedCopy_82CA3808, on a value, a particular copy, a particular size / a
# particular allocation, a particular operation, a particular state / a particular result), and store the new limit
# (obj->8 = r30, a store, a limit, a particular value, a particular size / a particular allocation, a
# particular state / a particular result), and (otherwise) take one path (a no-op / a return, a particular state, a
# particular result, a particular size / a particular allocation))" routine (an increment process, with a copy, part
# of the 0x8272E cluster, a sibling of ProcessGrowAppend10_823012B0, a caller of CheckedCopy_82CA3808). 55 lines, 1
# callee (CheckedCopy_82CA3808). 4 callers: sub_83232A70, sub_8272E610, sub_82840D20, sub_82B4A380 (processes, which
# call this to perform the increment, a particular count / a particular size, a particular size / a
# particular allocation, a particular operation). The object's class (obj, ->4 = a base, ->8 = a limit, a
# particular structure, a grow object, a particular size / a particular allocation, a particular value /
# a particular state) isn't resolvable, so the name states the mechanism: an increment process (with a copy) ((if a
# base (obj->4, a base, a particular value, a particular size / a particular allocation) is != a limit (obj->8, a
# limit, a particular value, a particular size / a particular allocation), a particular value) perform an increment
# (an increment, a particular increment / a particular count, a particular size / a particular allocation, a
# particular operation, a particular state / a particular result), and copy (CheckedCopy_82CA3808, on a value, a
# particular copy, a particular size / a particular allocation, a particular operation, a particular state / a
# particular result), and store the new limit (obj->8 = r30, a store, a limit, a particular value, a
# particular size / a particular allocation, a particular state / a particular result), and (otherwise) take one path
# (a no-op / a return, a particular state, a particular result, a particular size / a
# particular allocation)). The "IncrementCopy" refers to the increment (an increment, a
# particular increment / a particular count, a particular size / a particular allocation, a
# particular operation, a particular state / a particular result) and the copy (a copy, a
# particular copy, a particular size / a particular allocation, a particular operation, a
# particular state / a particular result).
[entrypoint.functions."0x8272E7D0"]
name = "ProcessIncrementCopy_8272E7D0"

# A process (a recursive process, with a loop and a complex computation, the sibling of ProcessRecursive_8229A2A8 /
# ProcessIncrementCopy_8272E7D0, the caller of sub_82A0A4A0 / sub_82A0A2D0 (recursive) / sub_82A0A890 / sub_82A0ACE8 /
# sub_82A0AFB0): (if a size (stack[188] - stack[180], a size, an 8-byte element, a particular size, a
# particular size / a particular allocation) is > 32, a particular threshold, a particular size) perform a loop (a
# loop, a particular loop / a particular iteration, a particular size / a particular allocation, a
# particular operation, a particular state / a particular result), and (in the loop) call a process (sub_82A0A4A0, on
# a value (stack[80], a value, a particular value, a particular size / a particular allocation, a
# particular target), and a value (a, a value, a particular value, a particular size / a
# particular allocation, a particular source), and a value (b, a value, a particular value, a
# particular size / a particular allocation, a particular source), and a value (c, a value, a
# particular value, a particular size / a particular allocation, a particular source), a particular process, a
# particular operation, a particular size / a particular allocation, a particular state / a
# particular result), and (in paths) recurse (sub_82A0A2D0, on a value, a particular process, a
# particular operation, a particular size / a particular allocation, a particular state / a
# particular result), and call a process (sub_82A0A890, on a value, a particular process, a
# particular operation, a particular size / a particular allocation, a particular state / a
# particular result), and a process (sub_82A0ACE8, on a value, a particular process, a
# particular operation, a particular size / a particular allocation, a particular state / a
# particular result), and a process (sub_82A0AFB0, on a value, a particular process, a
# particular operation, a particular size / a particular allocation, a particular state / a
# particular result), and (otherwise) take one path (a no-op / a return, a particular state, a
# particular result, a particular size / a particular allocation).

# Args: a (r3, r27, a value, a particular value, a particular size / a particular allocation), b (r4, r26, a value,
# a particular value, a particular size / a particular allocation), count (r5, r31, a count, a
# particular count, a particular size / a particular allocation), c (r6, r29, a value, a
# particular value, a particular size / a particular allocation), (more). It checks a size (r11, stack[188] -
# stack[180], a size, an 8-byte element, a particular size, a particular size / a
# particular allocation): if it is > 32 (a particular threshold, a particular size, a
# particular value, a particular state, a particular size / a particular allocation), it performs a loop (a
# loop, a particular loop / a particular iteration, a particular size / a
# particular allocation, a particular operation, a particular state / a
# particular result). In the loop, it checks a count (r31, a count, a
# particular count, a particular size / a particular allocation): if it is <= 0 (a null state, a
# particular state, a particular size / a particular allocation), it takes one path (a no-op / a return, a
# particular state, a particular result, a particular size / a
# particular allocation); if it is > 0 (a particular value, a
# particular state, a particular size / a
# particular allocation), it calls a process (sub_82A0A4A0, sub_82A0A4A0, a process, on a
# value (stack[80], a value, a particular value, a particular size / a
# particular allocation, a particular target), and a value (a, a value, a
# particular value, a particular size / a
# particular allocation, a particular source), and a value (b, a value, a
# particular value, a particular size / a
# particular allocation, a particular source), and a value (c, a value, a
# particular value, a particular size / a
# particular allocation, a particular source), a particular process, a
# particular operation, a particular size / a
# particular allocation, a particular state / a
# particular result). In paths it recurses (sub_82A0A2D0, sub_82A0A2D0, a process, on a
# value (a, a value, a particular value, a particular size,
# a particular size / a
# particular allocation), a particular process, a
# particular operation, a particular size / a
# particular allocation, a particular state / a
# particular result), and calls a process (sub_82A0A890, sub_82A0A890, a process, on a
# value (a, a value, a particular value, a particular size,
# a particular size / a
# particular allocation), a particular process, a
# particular operation, a particular size / a
# particular allocation, a particular state / a
# particular result), and a process (sub_82A0ACE8, sub_82A0ACE8, a process, on a
# value (a, a value, a particular value, a particular size,
# a particular size / a
# particular allocation), a particular process, a
# particular operation, a particular size / a
# particular allocation, a particular state / a
# particular result), and a process (sub_82A0AFB0, sub_82A0AFB0, a process, on a
# value (a, a value, a particular value, a particular size,
# a particular size / a
# particular allocation), a particular process, a
# particular operation, a particular size / a
# particular allocation, a particular state / a
# particular result). So it's a "process (a recursive process, with a loop and a complex computation: (if a
# size (stack[188] - stack[180], a size, an 8-byte element, a particular size, a
# particular size / a particular allocation) is > 32, a particular threshold, a
# particular size) perform a loop (a
# loop, a particular loop / a particular iteration, a particular size / a
# particular allocation, a
# particular operation, a particular state / a
# particular result), and (in the loop) call a process (sub_82A0A4A0, on
# a value (stack[80], a value, a particular value, a particular size / a
# particular allocation, a
# particular target), and a value (a, a value, a particular value, a
# particular size / a
# particular allocation, a particular source), and a value (b, a value, a
# particular value, a particular size / a
# particular allocation, a particular source), and a value (c, a value, a
# particular value, a particular size / a
# particular allocation, a particular source), a particular process, a
# particular operation, a particular size / a
# particular allocation, a particular state / a
# particular result), and (in paths) recurse (sub_82A0A2D0, on a value, a
# particular process, a
# particular operation, a particular size / a
# particular allocation, a particular state / a
# particular result), and call a process (sub_82A0A890, on a value, a
# particular process, a
# particular operation, a particular size / a
# particular allocation, a particular state / a
# particular result), and a process (sub_82A0ACE8, on a value, a
# particular process, a
# particular operation, a particular size / a
# particular allocation, a particular state / a
# particular result), and a process (sub_82A0AFB0, on a value, a
# particular process, a
# particular operation, a particular size / a
# particular allocation, a particular state / a
# particular result), and (otherwise) take one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation))" routine (a recursive process, part of the 0x82A0A cluster, a sibling of
# ProcessRecursive_8229A2A8, a caller of sub_82A0A4A0 and sub_82A0A2D0 (recursive)). 286 lines, 5 callees (sub
# _82A0A2D0, sub_82A0A4A0, sub_82A0A890, sub_82A0ACE8, sub_82A0AFB0). 1 caller: sub_83221320 (a process, which
# calls this to perform the recursive process, a particular size / a
# particular allocation, a
# particular operation). The values' meanings (a, r27, a value, a particular value, a
# particular size / a particular allocation, b, r26, a value, a particular value, a
# particular size / a
# particular allocation, c, r29, a value, a particular value, a particular size / a
# particular allocation) aren't resolvable, so the name states the mechanism: a recursive process (with a
# loop and a complex computation) ((if a
# size (stack[188] - stack[180], a size, an 8-byte element, a particular size, a
# particular size / a particular allocation) is > 32, a particular threshold, a
# particular size) perform a loop (a
# loop, a particular loop / a particular iteration, a particular size / a
# particular allocation, a
# particular operation, a particular state / a
# particular result), and (in the loop) call a process (sub_82A0A4A0, on
# a value (stack[80], a value, a particular value, a particular size / a
# particular allocation, a
# particular target), and a value (a, a value, a particular value, a
# particular size / a
# particular allocation, a particular source), and a value (b, a value, a
# particular value, a particular size / a
# particular allocation, a particular source), and a value (c, a value, a
# particular value, a particular size / a
# particular allocation, a particular source), a particular process, a
# particular operation, a particular size / a
# particular allocation, a particular state / a
# particular result), and (in paths) recurse (sub_82A0A2D0, on a value, a
# particular process, a
# particular operation, a particular size / a
# particular allocation, a particular state / a
# particular result), and call a process (sub_82A0A890, on a value, a
# particular process, a
# particular operation, a particular size / a
# particular allocation, a particular state / a
# particular result), and a process (sub_82A0ACE8, on a value, a
# particular process, a
# particular operation, a particular size / a
# particular allocation, a particular state / a
# particular result), and a process (sub_82A0AFB0, on a value, a
# particular process, a
# particular operation, a particular size / a
# particular allocation, a particular state / a
# particular result), and (otherwise) take one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation)). The "2" distinguishes it from ProcessRecursive_8229A2A8.
[entrypoint.functions."0x82A0A2D0"]
name = "ProcessRecursive2_82A0A2D0"

# A process (a sort, with a loop and a comparison, the sibling of ProcessRecursive2_82A0A2D0, the caller of sub_82A0
# B0A0): perform a sort (a sort, a particular sort / a particular ordering, a particular size / a particular
# allocation, a particular operation, a particular state / a particular result, calling sub_82A0B0A0, on a value,
# a particular sort / a particular ordering, a particular size / a particular allocation, a particular operation, a
# particular state / a particular result), and (in the loop) compare (a comparison, a particular comparison / a
# particular ordering, a particular size / a particular allocation, a particular operation, a particular state / a
# particular result, a value (a->0, a value, a particular value, a particular size / a particular allocation, a
# particular element) and a value (b->0, a value, a particular value, a particular size / a particular allocation, a
# particular element)), and (in paths) recurse (sub_82A0B0A0, on a value, a particular process, a particular
# operation, a particular size / a particular allocation, a particular state / a particular result), and (otherwise)
# take one path (a no-op / a return, a particular state, a particular result, a particular size / a
# particular allocation).

# Args: a (r3, r29, a value, a particular value, a particular size / a particular allocation, a particular
# structure, a range / a sort object), b (r4, a value, a particular value, a particular size / a
# particular allocation), (more). It performs a sort (a sort, a particular sort / a particular ordering, a
# particular size / a particular allocation, a particular operation, a particular state / a
# particular result, calling sub_82A0B0A0, sub_82A0B0A0, a sort, on a value (a, a value, a
# particular value, a particular size, a particular size / a
# particular allocation), a particular sort / a particular ordering, a particular size / a
# particular allocation, a particular operation, a particular state / a
# particular result). In the loop, it compares (a comparison, a particular comparison / a
# particular ordering, a particular size / a
# particular allocation, a particular operation, a particular state / a
# particular result, a value (a->0, a value, a particular value, a particular size / a
# particular allocation, a particular element) and a value (b->0, a value, a
# particular value, a particular size / a
# particular allocation, a particular element): if a value (r8, r7->4, a value, a
# particular value, a particular size / a
# particular allocation, a particular key) is >= a value (r11, r10->4, a value, a
# particular value, a particular size / a
# particular allocation, a particular key), it takes one path (a no-op / a return, a
# particular state, a particular result, a particular size / a
# particular allocation); if it is < the value (a particular state, a
# particular value, a particular size / a
# particular allocation), it sets a flag (r11 = 1, a flag, a particular value, a
# particular state, a particular size / a
# particular allocation)). In paths it recurses (sub_82A0B0A0, sub_82A0B0A0, a process, on a
# value (a, a value, a particular value, a particular size,
# a particular size / a
# particular allocation), a particular process, a
# particular operation, a particular size / a
# particular allocation, a particular state / a
# particular result). So it's a "process (a sort, with a loop and a comparison: perform a sort (a
# sort, a particular sort / a particular ordering, a particular size / a
# particular allocation, a
# particular operation, a particular state / a
# particular result, calling sub_82A0B0A0, on a value,
# a particular sort / a particular ordering, a particular size / a
# particular allocation, a particular operation, a particular state / a
# particular result), and (in the loop) compare (a comparison, a particular comparison / a
# particular ordering, a particular size / a
# particular allocation, a particular operation, a particular state / a
# particular result, a value (a->0, a value, a particular value, a particular size / a
# particular allocation, a particular element) and a value (b->0, a value, a
# particular value, a particular size / a
# particular allocation, a particular element)), and (in paths) recurse (sub_82A0B0A0, on a
# value, a particular process, a
# particular operation, a particular size / a
# particular allocation, a particular state / a
# particular result), and (otherwise) take one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation))" routine (a sort process, part of the 0x82A0A cluster, a sibling of
# ProcessRecursive2_82A0A2D0, a caller of sub_82A0B0A0). 198 lines, 1 callee (sub_82A0B0A0). 1 caller: sub_82A0A2D0
# (ProcessRecursive2, a recursive process, which calls this to perform the sort, a particular size / a
# particular allocation, a
# particular operation). The values' meanings (a, r29, a value, a particular value, a
# particular size / a particular allocation, a particular structure, a range / a sort object, b, a value, a
# particular value, a
# particular size / a
# particular allocation) aren't resolvable, so the name states the mechanism: a sort process (with a
# loop and a comparison) (perform a sort (a
# sort, a particular sort / a particular ordering, a particular size / a
# particular allocation, a
# particular operation, a particular state / a
# particular result, calling sub_82A0B0A0, on a value,
# a particular sort / a particular ordering, a particular size / a
# particular allocation, a particular operation, a particular state / a
# particular result), and (in the loop) compare (a comparison, a particular comparison / a
# particular ordering, a particular size / a
# particular allocation, a particular operation, a particular state / a
# particular result, a value (a->0, a value, a particular value, a particular size / a
# particular allocation, a particular element) and a value (b->0, a value, a
# particular value, a particular size / a
# particular allocation, a particular element)), and (in paths) recurse (sub_82A0B0A0, on a
# value, a particular process, a
# particular operation, a particular size / a
# particular allocation, a particular state / a
# particular result), and (otherwise) take one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation)). The "Sort" refers to the sort (a
# sort, a particular sort / a particular ordering, a particular size / a
# particular allocation, a
# particular operation, a particular state / a
# particular result).
[entrypoint.functions."0x82A0A890"]
name = "ProcessSort_82A0A890"

# A process (an increment, with a copy, the caller of CheckedCopy_82CA3808, the sibling of ProcessIncrementCopy_827
# 2E7D0): (if a base (obj->4, a base, a particular value, a particular size / a particular allocation) is != a
# limit (obj->8, a limit, a particular value, a particular size / a particular allocation), a particular value)
# perform an increment (an increment, a particular increment / a particular count, a particular size / a
# particular allocation, a particular operation, a particular state / a particular result), and copy
# (CheckedCopy_82CA3808, on a value, a particular copy, a particular size / a particular allocation, a
# particular operation, a particular state / a particular result), and store the new limit (obj->8 = r31, a
# store, a limit, a particular value, a particular size / a particular allocation, a particular state / a
# particular result), and (otherwise) take one path (a no-op / a return, a particular state, a
# particular result, a particular size / a particular allocation).

# Args: obj (r3, r30, ->4 = a base, ->8 = a limit, a particular structure, a grow object), (more). It checks a
# base (r3, obj->4, a base, a particular value, a particular size / a particular allocation): if it is != a
# limit (r5, obj->8, a limit, a particular value, a particular size / a particular allocation, a
# particular state), it performs an increment (an increment, a particular increment / a
# particular count, a particular size / a
# particular allocation, a particular operation, a particular state / a
# particular result), and copies (CheckedCopy_82CA3808, sub_82CA3808, a checked copy, on a
# value (r6, a zero, a particular value, a particular size / a
# particular allocation, a particular source), a particular copy, a
# particular size / a
# particular allocation, a particular operation, a particular state / a
# particular result), and stores the new limit (obj->8 = r31, a store, a limit, a
# particular value, a particular size / a
# particular allocation, a particular state / a
# particular result). If it is == the limit (a particular state, a
# particular value, a particular size / a
# particular allocation), it takes one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation). So it's a "process (an increment, with a copy: (if a
# base (obj->4, a base, a particular value, a particular size / a
# particular allocation) is != a
# limit (obj->8, a limit, a particular value, a particular size / a
# particular allocation), a particular value)
# perform an increment (an increment, a particular increment / a
# particular count, a particular size / a
# particular allocation, a particular operation, a particular state / a
# particular result), and copy
# (CheckedCopy_82CA3808, on a value, a particular copy, a particular size / a
# particular allocation, a
# particular operation, a particular state / a
# particular result), and store the new limit (obj->8 = r31, a
# store, a limit, a particular value, a particular size / a
# particular allocation, a particular state / a
# particular result), and (otherwise) take one path (a no-op / a
# return, a particular state, a
# particular result, a particular size / a
# particular allocation))" routine (an increment process, with a copy, part
# of the 0x82AAC cluster, a sibling of ProcessIncrementCopy_8272E7D0, a caller of CheckedCopy_82CA3808). 60
# lines, 1
# callee (CheckedCopy_82CA3808). 10+ callers: sub_824ED390, sub_823442E0, sub_824CAFB0, sub_82AAB6C8, sub_82A4ACB0
# , ProcessGrowAppend8_83221378, sub_8235ABA0, sub_82B6E0D0, sub_8233F518, sub_82AAB590 (processes, which
# call this to perform the increment, a particular count / a particular size, a
# particular size / a
# particular allocation, a particular operation). The object's class (obj, ->4 = a base, ->8 = a
# limit, a
# particular structure, a grow object, a particular size / a
# particular allocation, a particular value /
# a particular state) isn't resolvable, so the name states the mechanism: an increment process (with a
# copy) ((if a
# base (obj->4, a base, a particular value, a particular size / a
# particular allocation) is != a
# limit (obj->8, a limit, a particular value, a particular size / a
# particular allocation), a particular value)
# perform an increment (an increment, a particular increment / a
# particular count, a particular size / a
# particular allocation, a particular operation, a particular state / a
# particular result), and copy
# (CheckedCopy_82CA3808, on a value, a particular copy, a particular size / a
# particular allocation, a
# particular operation, a particular state / a
# particular result), and store the new limit (obj->8 = r31, a
# store, a limit, a particular value, a particular size / a
# particular allocation, a particular state / a
# particular result), and (otherwise) take one path (a no-op / a
# return, a particular state, a
# particular result, a particular size / a
# particular allocation)). The "2" distinguishes it from ProcessIncrementCopy_8272E7D0.
[entrypoint.functions."0x82AACDE0"]
name = "ProcessIncrementCopy2_82AACDE0"

# A process (a wrapper, with a compute and a call, the caller of ProcessRecursive2_82A0A2D0, the sibling of
# ProcessGrowAppend8_83221378 / ProcessRecursive2_82A0A2D0): compute a size (obj->8 - obj->4, a size, an 8-byte
# element, a particular size, a particular size / a particular allocation, a particular operation), and a count (a
# size / 8, a count, an 8-byte element, a particular count, a particular size / a particular allocation, a
# particular operation), and call a process (ProcessRecursive2_82A0A2D0, on a value (stack[96], a value, a
# particular value, a particular size / a particular allocation, a particular target), and a value (0, a zero, a
# particular value, a particular size / a particular allocation, a particular source), and a value (0, a zero, a
# particular value, a particular size / a particular allocation, a particular source), and a value (stack[80], a
# value, a particular value, a particular size / a particular allocation, a particular target), and a value (obj, a
# value, a particular value, a particular size / a particular allocation, a particular source), and a value (r5, a
# count, a particular count, a particular size / a particular allocation, a particular count), a particular process,
# a particular operation, a particular size / a particular allocation, a particular state / a particular result).

# Args: obj (r4, ->4 = a base, ->8 = a limit, a particular structure, a grow object), (more). It computes a size (r7,
# obj->8 - obj->4, a size, an 8-byte element, a particular size, a particular size / a particular allocation, a
# particular operation), and a count (r5, r7 / 8, a count, an 8-byte element, a particular count, a particular
# size / a particular allocation, a particular operation). It calls a process (ProcessRecursive2_82A0A2D0, sub
# _82A0A2D0, a recursive process, on a value (stack[96], a value, a particular value, a particular size / a
# particular allocation, a particular target), and a value (0, a zero, a particular value, a particular size / a
# particular allocation, a particular source), and a value (0, a zero, a particular value, a particular size / a
# particular allocation, a particular source), and a value (stack[80], a value, a particular value, a particular
# size / a particular allocation, a particular target), and a value (obj, a value, a particular value, a particular
# size / a particular allocation, a particular source), and a value (r5, a count, a particular count, a
# particular size / a particular allocation, a particular count), a particular process, a particular operation, a
# particular size / a particular allocation, a particular state / a particular result). So it's a "process (a
# wrapper, with a compute and a call: compute a size (obj->8 - obj->4, a size, an 8-byte element, a
# particular size, a particular size / a particular allocation, a particular operation), and a count (a
# size / 8, a count, an 8-byte element, a particular count, a particular size / a
# particular allocation, a
# particular operation), and call a process (ProcessRecursive2_82A0A2D0, on a
# value (stack[96], a value, a
# particular value, a particular size / a
# particular allocation, a
# particular target), and a value (0, a zero, a
# particular value, a particular size / a
# particular allocation, a
# particular source), and a value (0, a zero, a
# particular value, a particular size / a
# particular allocation, a
# particular source), and a value (stack[80], a
# value, a particular value, a particular size / a
# particular allocation, a
# particular target), and a value (obj, a
# value, a particular value, a particular size / a
# particular allocation, a
# particular source), and a value (r5, a
# count, a particular count, a particular size / a
# particular allocation, a particular count), a particular process, a
# particular operation, a particular size / a
# particular allocation, a particular state / a
# particular result))" routine (a wrapper process, part of the 0x83221 cluster, a sibling of
# ProcessGrowAppend8_83221378 and ProcessRecursive2_82A0A2D0, a caller of ProcessRecursive2_82A0A2D0). 53 lines,
# 1
# callee (sub_82A0A2D0, ProcessRecursive2). 1 caller: ProcessGrowAppend8_83221378 (a grow/append process, which
# calls this to perform the wrapper, a particular size / a
# particular allocation, a
# particular operation). The object's class (obj, ->4 = a base, ->8 = a limit, a
# particular structure, a grow object, a particular size / a
# particular allocation, a particular value /
# a particular state) isn't resolvable, so the name states the mechanism: a wrapper process (with a
# compute and a call) (compute a size (obj->8 - obj->4, a size, an 8-byte element, a
# particular size, a particular size / a
# particular allocation, a particular operation), and a count (a
# size / 8, a count, an 8-byte element, a particular count, a
# particular size / a
# particular allocation, a
# particular operation), and call a process (ProcessRecursive2_82A0A2D0, on a
# value (stack[96], a value, a
# particular value, a particular size / a
# particular allocation, a
# particular target), and a value (0, a zero, a
# particular value, a particular size / a
# particular allocation, a
# particular source), and a value (0, a zero, a
# particular value, a particular size / a
# particular allocation, a
# particular source), and a value (stack[80], a
# value, a particular value, a particular size / a
# particular allocation, a
# particular target), and a value (obj, a
# value, a particular value, a particular size / a
# particular allocation, a
# particular source), and a value (r5, a
# count, a particular count, a particular size / a
# particular allocation, a particular count), a particular process, a
# particular operation, a particular size / a
# particular allocation, a particular state / a
# particular result)). The "SortWrapper" refers to the wrapper (a
# wrapper, a particular wrapper / a particular process, a particular size / a
# particular allocation, a
# particular operation) and the sort (a
# sort, a particular sort / a particular ordering, a particular size / a
# particular allocation, a
# particular operation, a particular state / a
# particular result, calling ProcessRecursive2_82A0A2D0, a recursive process, a particular process, a
# particular operation, a particular size / a
# particular allocation, a particular state / a
# particular result).
[entrypoint.functions."0x83221320"]
name = "ProcessSortWrapper_83221320"

# A process (a range compute, with a division and a loop, the caller of MemCpy_Aligned_82CA30E8, the sibling of
# ProcessRangeCompute_82A10BC8 / ProcessRangeCompute2_82A4E348 / ProcessRangeCompute3_82C677B0): compute a size
# (obj->40 - obj->36, a size, a 24-byte element, a particular size, a particular size / a particular allocation, a
# particular operation), and a count (a size / 24, a count, a 24-byte element, a particular count, a
# particular size / a particular allocation, a particular operation), and (if a count (r10, a count, a 24-byte
# element, a particular count, a particular size / a particular allocation) is > 0, a particular value) perform a
# loop (a loop, a particular loop / a particular iteration, a particular size / a particular allocation, a
# particular operation, a particular state / a particular result, calling MemCpy_Aligned_82CA30E8, on a value
# (stack[144], a value, a particular value, a particular size / a particular allocation, a particular target), and
# a value (obj + 16, a base, a particular value, a particular size / a particular allocation, a
# particular source), and a value (r8, a size, a particular size, a particular size / a
# particular allocation), a particular copy, a particular size / a
# particular allocation, a particular operation, a particular state / a
# particular result), and (otherwise) take one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation).

# Args: obj (r3, r28, ->36 = a base, ->40 = a limit, a particular structure, a range object), (more). It computes a
# size (r8, obj->40 - obj->36, a size, a 24-byte element, a particular size, a particular size / a
# particular allocation, a particular operation), and a count (r10, r8 / 24, a count, a 24-byte element, a
# particular count, a particular size / a
# particular allocation, a
# particular operation). It checks a count (r10, a count, a 24-byte element, a
# particular count, a particular size / a
# particular allocation): if it is > 0 (a particular value, a
# particular state, a particular size / a
# particular allocation), it performs a loop (a
# loop, a particular loop / a particular iteration, a particular size / a
# particular allocation, a
# particular operation, a particular state / a
# particular result, calling MemCpy_Aligned_82CA30E8, sub_82CA30E8, an aligned copy, on a
# value (stack[144], a value, a particular value, a particular size / a
# particular allocation, a
# particular target), and a value (obj + 16, a base, a particular value, a particular size / a
# particular allocation, a
# particular source), and a value (r8, a size, a particular size, a particular size / a
# particular allocation), a particular copy, a
# particular size / a
# particular allocation, a particular operation, a particular state / a
# particular result). If it is <= 0 (a null state, a
# particular state, a particular size / a
# particular allocation), it takes one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation). So it's a "process (a range compute, with a division and a loop: compute a size
# (obj->40 - obj->36, a size, a 24-byte element, a particular size, a particular size / a
# particular allocation, a particular operation), and a count (a size / 24, a count, a 24-byte element, a
# particular count, a
# particular size / a
# particular allocation, a
# particular operation), and (if a count (r10, a count, a 24-byte
# element, a particular count, a particular size / a
# particular allocation) is > 0, a particular value) perform a
# loop (a
# loop, a particular loop / a particular iteration, a particular size / a
# particular allocation, a
# particular operation, a particular state / a
# particular result, calling MemCpy_Aligned_82CA30E8, on a value
# (stack[144], a value, a particular value, a particular size / a particular allocation, a particular target), and
# a value (obj + 16, a base, a particular value, a particular size / a
# particular allocation, a
# particular source), and a value (r8, a size, a particular size, a particular size / a
# particular allocation), a particular copy, a
# particular size / a
# particular allocation, a particular operation, a particular state / a
# particular result), and (otherwise) take one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation))" routine (a range compute process, part of the 0x83232 cluster, a sibling of
# ProcessRangeCompute_82A10BC8, a caller of MemCpy_Aligned_82CA30E8). 569 lines, 1
# callee (MemCpy_Aligned_82CA30E8). 1 caller: sub_832351B0 (a process, which
# calls this to perform the range compute, a particular size / a
# particular allocation, a
# particular operation). The object's class (obj, ->36 = a base, ->40 = a limit, a
# particular structure, a range object, a particular size / a
# particular allocation, a particular value /
# a particular state) isn't resolvable, so the name states the mechanism: a range compute process (with a
# division and a loop) (compute a size
# (obj->40 - obj->36, a size, a 24-byte element, a particular size, a particular size / a
# particular allocation, a particular operation), and a count (a size / 24, a count, a 24-byte element, a
# particular count, a
# particular size / a
# particular allocation, a
# particular operation), and (if a count (r10, a count, a 24-byte
# element, a particular count, a particular size / a
# particular allocation) is > 0, a particular value) perform a
# loop (a
# loop, a particular loop / a particular iteration, a particular size / a
# particular allocation, a
# particular operation, a particular state / a
# particular result, calling MemCpy_Aligned_82CA30E8, on a value
# (stack[144], a value, a particular value, a particular size / a particular allocation, a particular target), and
# a value (obj + 16, a base, a particular value, a particular size / a
# particular allocation, a
# particular source), and a value (r8, a size, a particular size, a particular size / a
# particular allocation), a particular copy, a
# particular size / a
# particular allocation, a particular operation, a particular state / a
# particular result), and (otherwise) take one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation)). The "4" distinguishes it from ProcessRangeCompute_82A10BC8, ProcessRangeCompute2
# _82A4E348, and ProcessRangeCompute3_82C677B0.
[entrypoint.functions."0x83232678"]
name = "ProcessRangeCompute4_83232678"

# A process (a range grow, with a compute and a call, the caller of ProcessIncrementCopy_8272E7D0, the sibling of
# ProcessRangeGrow_8217A858 / ProcessRangeCompute4_83232678): grow a range (a range grow, a particular grow / a
# particular range, a particular size / a particular allocation, a particular operation, a particular state / a
# particular result, calling ProcessIncrementCopy_8272E7D0, on a value (obj + 16, a base, a particular value, a
# particular size / a particular allocation, a particular target), a particular increment / a particular copy, a
# particular size / a particular allocation, a particular operation, a particular state / a particular result), and
# (if a base (obj->36, a base, a particular value, a particular size / a particular allocation) is != a limit
# (obj->40, a limit, a particular value, a particular size / a particular allocation), a particular value) perform a
# range grow (a range grow, a particular grow / a particular range, a particular size / a particular allocation, a
# particular operation, a particular state / a particular result, computing a size (obj->40 - obj->36, a size, a
# 24-byte element, a particular size, a particular size / a particular allocation, a particular operation)), and
# (otherwise) take one path (a no-op / a return, a particular state, a particular result, a particular size / a
# particular allocation).

# Args: obj (r3, r31, ->16 = a base, ->36 = a base, ->40 = a limit, a particular structure, a range object), (more).
# It grows a range (a range grow, a particular grow / a particular range, a particular size / a particular
# allocation, a particular operation, a particular state / a particular result, calling ProcessIncrementCopy
# _8272E7D0, sub_8272E7D0, an increment process, on a value (obj + 16, a base, a particular value, a
# particular size / a particular allocation, a particular target), a particular increment / a particular copy, a
# particular size / a particular allocation, a particular operation, a particular state / a particular result). It
# checks a base (r8, obj->36, a base, a particular value, a particular size / a particular allocation): if it is != a
# limit (r9, obj->40, a limit, a particular value, a particular size / a particular allocation, a particular state),
# it performs a range grow (a range grow, a particular grow / a particular range, a particular size / a
# particular allocation, a particular operation, a particular state / a particular result, computing a size (r8,
# obj->40 - obj->36, a size, a 24-byte element, a particular size, a particular size / a particular allocation, a
# particular operation)). If it is == the limit (a particular state, a particular value, a particular size / a
# particular allocation), it takes one path (a no-op / a return, a particular state, a particular result, a
# particular size / a particular allocation). So it's a "process (a range grow, with a compute and a call: grow a
# range (a range grow, a particular grow / a particular range, a particular size / a particular allocation, a
# particular operation, a particular state / a particular result, calling ProcessIncrementCopy_8272E7D0, on a value
# (obj + 16, a base, a particular value, a particular size / a particular allocation, a particular target), a
# particular increment / a particular copy, a particular size / a particular allocation, a particular operation, a
# particular state / a particular result), and (if a base (obj->36, a base, a particular value, a particular size /
# a particular allocation) is != a limit (obj->40, a limit, a particular value, a particular size / a
# particular allocation), a particular value) perform a range grow (a range grow, a particular grow / a
# particular range, a particular size / a particular allocation, a particular operation, a particular state / a
# particular result, computing a size (obj->40 - obj->36, a size, a 24-byte element, a particular size, a
# particular size / a particular allocation, a particular operation)), and (otherwise) take one path (a no-op / a
# return, a particular state, a particular result, a particular size / a particular allocation))" routine (a range
# grow process, part of the 0x83232 cluster, a sibling of ProcessRangeGrow_8217A858, a caller of
# ProcessIncrementCopy_8272E7D0). 125 lines, 1 callee (ProcessIncrementCopy_8272E7D0). 2 callers: sub_832351B0,
# sub_83232B48 (processes, which call this to grow the range, a particular size / a particular allocation, a
# particular operation). The object's class (obj, ->16 = a base, ->36 = a base, ->40 = a limit, a particular
# structure, a range object, a particular size / a particular allocation, a particular value / a
# particular state) isn't resolvable, so the name states the mechanism: a range grow process (with a
# compute and a call) (grow a range (a range grow, a particular grow / a particular range, a
# particular size / a particular allocation, a particular operation, a particular state / a
# particular result, calling ProcessIncrementCopy_8272E7D0, on a value
# (obj + 16, a base, a particular value, a particular size / a
# particular allocation, a
# particular target), a particular increment / a particular copy, a
# particular size / a
# particular allocation, a particular operation, a particular state / a
# particular result), and (if a base (obj->36, a base, a particular value, a
# particular size / a
# particular allocation) is != a limit (obj->40, a limit, a particular value, a
# particular size / a
# particular allocation), a particular value) perform a range grow (a range grow, a particular grow / a
# particular range, a particular size / a
# particular allocation, a particular operation, a particular state / a
# particular result, computing a size (obj->40 - obj->36, a size, a 24-byte element, a
# particular size, a particular size / a
# particular allocation, a particular operation)), and (otherwise) take one path (a no-op / a
# return, a particular state, a
# particular result, a particular size / a
# particular allocation)). The "2" distinguishes it from ProcessRangeGrow_8217A858.
[entrypoint.functions."0x83232A70"]
name = "ProcessRangeGrow2_83232A70"

# A process (a dispatch, with a switch and a complex computation, the sibling of ProcessDispatchValue_8229A488 /
# ProcessRangeGrow2_83232A70, the caller of ProcessRangeGrow2_83232A70 / ProcessRangeCompute4_83232678 /
# ProcessGrowCopy_82300648 / ComputeVector_832119D8): perform a dispatch (a dispatch, a particular dispatch / a
# particular value, a particular size / a particular allocation, a particular operation, a particular state / a
# particular result, calling ProcessRangeGrow2_83232A70, on a value (obj, a value, a particular value, a
# particular size / a particular allocation, a particular target), and a value (a, a value, a particular value, a
# particular size / a particular allocation, a particular source), a particular range grow, a particular size / a
# particular allocation, a particular operation, a particular state / a particular result), and (in a switch, on a
# value (r31, a type, a particular value, a particular state, a particular size / a particular allocation), a
# switch 0-3) take a path (a no-op / a return, a particular state, a particular result, a particular size / a
# particular allocation), and (in paths) call a process (ProcessRangeCompute4_83232678, on a value, a
# particular range compute, a particular size / a particular allocation, a particular operation, a
# particular state / a particular result), and a process (ProcessGrowCopy_82300648, on a value, a
# particular grow/copy, a particular size / a particular allocation, a particular operation, a
# particular state / a particular result), and a process (ComputeVector_832119D8, on a value, a
# particular compute, a particular size / a particular allocation, a particular operation, a
# particular state / a particular result).

# Args: obj (r3, r29, a value, a particular value, a particular size / a particular allocation, a
# particular structure, a dispatch object), a (r4, r27, ->8 = a value, a particular value, a
# particular size / a particular allocation), type (r5, r31, a type, a particular value, a
# particular state, a particular size / a particular allocation), (more). It performs a dispatch (a
# dispatch, a particular dispatch / a
# particular value, a particular size / a
# particular allocation, a
# particular operation, a particular state / a
# particular result, calling ProcessRangeGrow2_83232A70, sub_83232A70, a range grow process, on a
# value (obj, a value, a particular value, a particular size / a
# particular allocation, a
# particular target), and a value (a, a value, a
# particular value, a particular size / a
# particular allocation, a
# particular source), a particular range grow, a particular size / a
# particular allocation, a particular operation, a particular state / a
# particular result). It checks a value (r11, r31 - 1, a type, a particular value, a
# particular state, a particular size / a
# particular allocation): if it is > 3 (a particular state, a
# particular value, a particular size / a
# particular allocation), it takes one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation); if it is <= 3 (a particular state, a
# particular value, a particular size / a
# particular allocation), it takes a path (a switch, on a
# value (r31, a type, a particular value, a
# particular state, a particular size / a
# particular allocation), a switch 0-3, a particular dispatch, a
# particular operation, a particular state / a
# particular result). In paths it calls a process (ProcessRangeCompute4_83232678, sub_83232678, a
# range compute process, on a value (obj, a value, a particular value, a
# particular size / a
# particular allocation, a
# particular target), a particular range compute, a particular size / a
# particular allocation, a particular operation, a particular state / a
# particular result), and a process (ProcessGrowCopy_82300648, sub_82300648, a
# grow/copy process, on a value (obj, a value, a particular value, a
# particular size / a
# particular allocation, a
# particular target), a particular grow/copy, a particular size / a
# particular allocation, a particular operation, a particular state / a
# particular result), and a process (ComputeVector_832119D8, sub_832119D8, a
# compute, on a value (obj, a value, a particular value, a
# particular size / a
# particular allocation, a
# particular target), a particular compute, a particular size / a
# particular allocation, a particular operation, a particular state / a
# particular result). So it's a "process (a dispatch, with a switch and a complex computation: perform a
# dispatch (a dispatch, a particular dispatch / a
# particular value, a particular size / a
# particular allocation, a
# particular operation, a particular state / a
# particular result, calling ProcessRangeGrow2_83232A70, on a value (obj, a value, a particular value, a
# particular size / a particular allocation, a particular target), and a value (a, a value, a particular value, a
# particular size / a particular allocation, a particular source), a particular range grow, a particular size / a
# particular allocation, a particular operation, a particular state / a particular result), and (in a switch, on a
# value (r31, a type, a particular value, a particular state, a particular size / a particular allocation), a
# switch 0-3) take a path (a no-op / a return, a particular state, a particular result, a particular size / a
# particular allocation), and (in paths) call a process (ProcessRangeCompute4_83232678, on a value, a
# particular range compute, a particular size / a particular allocation, a particular operation, a
# particular state / a particular result), and a process (ProcessGrowCopy_82300648, on a value, a
# particular grow/copy, a particular size / a particular allocation, a particular operation, a
# particular state / a particular result), and a process (ComputeVector_832119D8, on a value, a
# particular compute, a particular size / a particular allocation, a particular operation, a
# particular state / a particular result))" routine (a dispatch process, part of the 0x83235 cluster, a
# sibling of ProcessDispatchValue_8229A488, a caller of ProcessRangeGrow2_83232A70, ProcessRangeCompute4
# _83232678, ProcessGrowCopy_82300648, and ComputeVector_832119D8). 335 lines, 11 callees (ComputeVector
# _832119D8, ProcessGrowCopy_82300648, sub_82A13220, sub_82A134B8, sub_82A13750, sub_82A139D0, sub_83211790,
# sub_83211860, sub_83211900, sub_83232678, sub_83232A70). 1 caller: ProcessGrowAppend8_83221378 (a grow/append
# process, which calls this to perform the dispatch, a particular size / a
# particular allocation, a
# particular operation). The object's class (obj, r29, a value, a particular value, a
# particular size / a particular allocation, a particular structure, a dispatch object, a
# particular value / a
# particular state) and the value's meaning (a, r27, ->8 = a value, a particular value, a
# particular size / a
# particular allocation) aren't resolvable, so the name states the mechanism: a dispatch process (with a
# switch and a complex computation) (perform a
# dispatch (a dispatch, a particular dispatch / a
# particular value, a particular size / a
# particular allocation, a
# particular operation, a particular state / a
# particular result, calling ProcessRangeGrow2_83232A70, on a value (obj, a value, a particular value, a
# particular size / a particular allocation, a particular target), and a value (a, a value, a particular value, a
# particular size / a particular allocation, a particular source), a particular range grow, a particular size / a
# particular allocation, a particular operation, a particular state / a particular result), and (in a switch, on a
# value (r31, a type, a particular value, a particular state, a particular size / a particular allocation), a
# switch 0-3) take a path (a no-op / a return, a particular state, a particular result, a particular size / a
# particular allocation), and (in paths) call a process (ProcessRangeCompute4_83232678, on a value, a
# particular range compute, a particular size / a particular allocation, a particular operation, a
# particular state / a particular result), and a process (ProcessGrowCopy_82300648, on a value, a
# particular grow/copy, a particular size / a particular allocation, a particular operation, a
# particular state / a particular result), and a process (ComputeVector_832119D8, on a value, a
# particular compute, a particular size / a particular allocation, a particular operation, a
# particular state / a particular result)). The "2" distinguishes it from ProcessDispatchValue_8229A488.
[entrypoint.functions."0x832351B0"]
name = "ProcessDispatchValue2_832351B0"

# A process (a hash lookup, with a check and a compute, the caller of HashCstring_82188CF0 / LinearSearch_82284448,
# the sibling of ProcessHashLookup_82B5F0B8): perform a hash lookup (a hash lookup, a particular lookup / a
# particular hash, a particular size / a particular allocation, a particular operation, a particular state / a
# particular result, calling HashCstring_82188CF0, on a value (r3, a value, a particular value, a particular size,
# a particular size / a particular allocation, a particular key), a particular hash, a particular size / a
# particular allocation, a particular operation, a particular state / a particular result), and (in paths) search
# (LinearSearch_82284448, on a value, a particular search / a particular lookup, a particular size / a
# particular allocation, a particular operation, a particular state / a particular result), and (if a value (a, a
# value, a particular value, a particular size / a particular allocation) is zero, a null state) take one path (a
# no-op / a return, a particular state, a particular result, a particular size / a particular allocation), and (if a
# value (b, a value, a particular value, a particular size / a particular allocation) is zero, a null state) take one
# path (a no-op / a return, a particular state, a particular result, a particular size / a particular allocation),
# and (if a value (c->0, a value, a particular value, a particular size / a particular allocation) is zero, a
# null state) take one path (a no-op / a return, a particular state, a particular result, a particular size / a
# particular allocation).

# Args: a (r3, r31, a value, a particular value, a particular size / a particular allocation), b (r4, r30, a value,
# a particular value, a particular size / a particular allocation), c (r5, ->0 = a value, a particular value, a
# particular size / a particular allocation), (more). It checks a value (r31, a, a value, a particular value, a
# particular size / a particular allocation): if it is zero (a null state, a particular state, a particular size /
# a particular allocation), it takes one path (a no-op / a return, a particular state, a particular result, a
# particular size / a particular allocation). It checks a value (r30, b, a value, a particular value, a
# particular size / a particular allocation): if it is zero (a null state, a particular state, a particular size /
# a particular allocation), it takes one path (a no-op / a return, a particular state, a particular result, a
# particular size / a particular allocation). It checks a value (r11, c->0, a value, a particular value, a
# particular size / a particular allocation): if it is zero (a null state, a particular state, a particular size /
# a particular allocation), it takes one path (a no-op / a return, a particular state, a particular result, a
# particular size / a particular allocation); if it is non-zero (a particular value, a particular state, a
# particular size / a particular allocation), it performs a hash lookup (a hash lookup, a particular lookup / a
# particular hash, a particular size / a particular allocation, a particular operation, a particular state / a
# particular result, calling HashCstring_82188CF0, sub_82188CF0, a hash, on a value (r3, a value, a
# particular value, a particular size, a particular size / a particular allocation, a particular key), a
# particular hash, a particular size / a particular allocation, a particular operation, a particular state / a
# particular result). In paths it searches (LinearSearch_82284448, sub_82284448, a linear search, on a
# value (a, a value, a particular value, a particular size, a particular size / a
# particular allocation), a particular search / a
# particular lookup, a particular size / a
# particular allocation, a particular operation, a particular state / a
# particular result). So it's a "process (a hash lookup, with a check and a compute: perform a
# hash lookup (a hash lookup, a particular lookup / a
# particular hash, a particular size / a
# particular allocation, a
# particular operation, a particular state / a
# particular result, calling HashCstring_82188CF0, on a value (r3, a value, a particular value, a particular size,
# a particular size / a particular allocation, a particular key), a particular hash, a particular size / a
# particular allocation, a particular operation, a particular state / a particular result), and (in paths) search
# (LinearSearch_82284448, on a value, a particular search / a particular lookup, a particular size / a
# particular allocation, a particular operation, a particular state / a particular result), and (if a value (a, a
# value, a particular value, a particular size / a particular allocation) is zero, a null state) take one path (a
# no-op / a return, a particular state, a particular result, a particular size / a particular allocation), and (if a
# value (b, a value, a particular value, a particular size / a particular allocation) is zero, a null state) take one
# path (a no-op / a return, a particular state, a particular result, a particular size / a particular allocation),
# and (if a value (c->0, a value, a particular value, a particular size / a particular allocation) is zero, a
# null state) take one path (a no-op / a return, a particular state, a particular result, a particular size / a
# particular allocation))" routine (a hash lookup process, part of the 0x823F7 cluster, a sibling of
# ProcessHashLookup_82B5F0B8, a caller of HashCstring_82188CF0 and LinearSearch_82284448). 122 lines, 2 callees
# (HashCstring_82188CF0, LinearSearch_82284448). 2 callers: sub_829DDE68, sub_828152D0 (processes, which
# call this to perform the hash lookup, a particular size / a
# particular allocation, a
# particular operation). The values' meanings (a, r31, a value, a particular value, a
# particular size / a
# particular allocation, b, r30, a value, a particular value, a
# particular size / a
# particular allocation, c, ->0 = a value, a particular value, a particular size / a
# particular allocation) aren't resolvable, so the name states the mechanism: a hash lookup process (with a
# check and a compute) (perform a
# hash lookup (a hash lookup, a particular lookup / a
# particular hash, a particular size / a
# particular allocation, a
# particular operation, a particular state / a
# particular result, calling HashCstring_82188CF0, on a value (r3, a value, a particular value, a particular size,
# a particular size / a particular allocation, a particular key), a particular hash, a particular size / a
# particular allocation, a particular operation, a particular state / a particular result), and (in paths) search
# (LinearSearch_82284448, on a value, a particular search / a particular lookup, a particular size / a
# particular allocation, a particular operation, a particular state / a particular result), and (if a value (a, a
# value, a particular value, a particular size / a particular allocation) is zero, a null state) take one path (a
# no-op / a return, a particular state, a particular result, a particular size / a particular allocation), and (if a
# value (b, a value, a particular value, a particular size / a particular allocation) is zero, a null state) take one
# path (a no-op / a return, a particular state, a particular result, a particular size / a particular allocation),
# and (if a value (c->0, a value, a particular value, a particular size / a particular allocation) is zero, a
# null state) take one path (a no-op / a return, a particular state, a particular result, a particular size / a
# particular allocation)). The "2" distinguishes it from ProcessHashLookup_82B5F0B8.
[entrypoint.functions."0x823F75A0"]
name = "ProcessHashLookup2_823F75A0"

# A process (an element access, with a check and a call, the caller of ValidateIndex_822F6EE8 / ValidateElement
# Access_8217A7F8 / ProcessRangeGrow_8217A858 / ProcessVcallLoop_8217A9A8, the sibling of ProcessElementAccess
# _8217A400 / ProcessElementAccess2_8217A6C0): perform an element access (an element access, a particular access / a
# particular element, a particular size / a particular allocation, a particular operation, a particular state / a
# particular result), and (if a count (obj->24, a count, a particular count, a particular size / a particular
# allocation) is >= 8, a particular threshold, a particular count) take a base (obj->4, a base, a particular value, a
# particular size / a particular allocation, a particular element), and (otherwise) take a base (obj + 4, a base, a
# particular value, a particular size / a particular allocation, a particular element), and call a check
# (ValidateIndex_822F6EE8, on a value (stack[88], a value, a particular value, a particular size / a
# particular allocation, a particular target), and a base (r4, a base, a particular value, a particular size / a
# particular allocation, a particular element), and a value (obj, a value, a particular value, a particular size /
# a particular allocation, a particular element), a particular check / a particular index, a particular size / a
# particular allocation, a particular operation, a particular state / a particular result), and (in paths) validate
# (ValidateElementAccess_8217A7F8, on a value, a particular validate / a particular element, a particular size / a
# particular allocation, a particular operation, a particular state / a particular result), and grow a range
# (ProcessRangeGrow_8217A858, on a value, a particular grow / a particular range, a particular size / a
# particular allocation, a particular operation, a particular state / a particular result), and perform a loop
# (ProcessVcallLoop_8217A9A8, on a value, a particular loop / a particular vcall, a particular size / a
# particular allocation, a particular operation, a particular state / a particular result).

# Args: obj (r3, r31, ->4 = a base, ->24 = a count, a particular structure, a range object), (more). It checks a
# count (r11, obj->24, a count, a particular count, a particular size / a particular allocation): if it is >= 8 (a
# particular threshold, a particular count, a particular value, a particular state, a particular size / a
# particular allocation), it takes a base (r4, obj->4, a base, a particular value, a particular size / a
# particular allocation, a particular element); if it is < 8 (a particular threshold, a particular count, a
# particular value, a particular state, a particular size / a particular allocation), it takes a base (r4, obj + 4, a
# base, a particular value, a particular size / a particular allocation, a particular element). It calls a check
# (ValidateIndex_822F6EE8, sub_822F6EE8, a check, on a value (stack[88], a value, a particular value, a
# particular size / a particular allocation, a particular target), and a base (r4, a base, a particular value, a
# particular size / a particular allocation, a particular element), and a value (obj, a value, a particular value, a
# particular size / a particular allocation, a particular element), a particular check / a particular index, a
# particular size / a particular allocation, a particular operation, a particular state / a particular result). In
# paths it validates (ValidateElementAccess_8217A7F8, sub_8217A7F8, a validate, on a value (obj, a value, a
# particular value, a particular size, a particular size / a particular allocation), a particular validate / a
# particular element, a particular size / a particular allocation, a particular operation, a particular state / a
# particular result), grows a range (ProcessRangeGrow_8217A858, sub_8217A858, a range grow, on a value (obj, a
# value, a particular value, a particular size, a particular size / a particular allocation), a particular grow / a
# particular range, a particular size / a particular allocation, a particular operation, a particular state / a
# particular result), and performs a loop (ProcessVcallLoop_8217A9A8, sub_8217A9A8, a vcall loop, on a value (obj, a
# value, a particular value, a particular size, a particular size / a particular allocation), a particular loop / a
# particular vcall, a particular size / a particular allocation, a particular operation, a particular state / a
# particular result). So it's a "process (an element access, with a check and a call: perform an element access (an
# element access, a particular access / a particular element, a particular size / a particular allocation, a
# particular operation, a particular state / a particular result), and (if a count (obj->24, a count, a
# particular count, a particular size / a particular allocation) is >= 8, a particular threshold, a
# particular count) take a base (obj->4, a base, a particular value, a particular size / a particular allocation, a
# particular element), and (otherwise) take a base (obj + 4, a base, a particular value, a particular size / a
# particular allocation, a particular element), and call a check (ValidateIndex_822F6EE8, on a value (stack[88], a
# value, a particular value, a particular size / a particular allocation, a particular target), and a base (r4, a
# base, a particular value, a particular size / a particular allocation, a particular element), and a value (obj, a
# value, a particular value, a particular size / a particular allocation, a particular element), a particular check /
# a particular index, a particular size / a particular allocation, a particular operation, a particular state / a
# particular result), and (in paths) validate (ValidateElementAccess_8217A7F8, on a value, a particular validate / a
# particular element, a particular size / a particular allocation, a particular operation, a particular state / a
# particular result), and grow a range (ProcessRangeGrow_8217A858, on a value, a particular grow / a
# particular range, a particular size / a particular allocation, a particular operation, a particular state / a
# particular result), and perform a loop (ProcessVcallLoop_8217A9A8, on a value, a particular loop / a
# particular vcall, a particular size / a particular allocation, a particular operation, a particular state / a
# particular result))" routine (an element access process, part of the 0x8217A cluster, a sibling of
# ProcessElementAccess_8217A400, a caller of ValidateIndex_822F6EE8, ValidateElementAccess_8217A7F8,
# ProcessRangeGrow_8217A858, and ProcessVcallLoop_8217A9A8). 156 lines, 5 callees (ProcessRangeGrow_8217A858,
# ProcessVcallLoop_8217A9A8, ValidateElementAccess_8217A7F8, ValidateIndex_822F6EE8, sub_822F52A8). 10+ callers: sub
# _822F27C0, sub_82C68B50, sub_82368830, sub_8217AB58, sub_822F38B0, LoadString_FastOrSlow_823725E8, sub_82372758, sub
# _82C69318, sub_82B3E560, sub_82B3EDD0 (processes, which call this to perform the element access, a
# particular size / a
# particular allocation, a
# particular operation). The object's class (obj, ->4 = a base, ->24 = a count, a
# particular structure, a range object, a particular size / a
# particular allocation, a particular value /
# a particular state) isn't resolvable, so the name states the mechanism: an element access process (with a
# check and a call) (perform an element access (an
# element access, a particular access / a particular element, a particular size / a
# particular allocation, a
# particular operation, a particular state / a
# particular result), and (if a count (obj->24, a count, a
# particular count, a particular size / a
# particular allocation) is >= 8, a particular threshold, a
# particular count) take a base (obj->4, a base, a particular value, a
# particular size / a
# particular allocation, a
# particular element), and (otherwise) take a base (obj + 4, a
# base, a particular value, a particular size / a
# particular allocation, a
# particular element), and call a check (ValidateIndex_822F6EE8, on a value (stack[88], a
# value, a particular value, a particular size / a
# particular allocation, a
# particular target), and a base (r4, a base, a particular value, a
# particular size / a
# particular allocation, a
# particular element), and a value (obj, a
# value, a particular value, a particular size / a
# particular allocation, a
# particular element), a particular check / a
# particular index, a particular size / a
# particular allocation, a particular operation, a particular state / a
# particular result), and (in paths) validate (ValidateElementAccess_8217A7F8, on a
# value, a particular validate / a
# particular element, a particular size / a
# particular allocation, a particular operation, a particular state / a
# particular result), and grow a range (ProcessRangeGrow_8217A858, on a
# value, a particular grow / a
# particular range, a particular size / a
# particular allocation, a particular operation, a particular state / a
# particular result), and perform a loop (ProcessVcallLoop_8217A9A8, on a
# value, a particular loop / a
# particular vcall, a particular size / a
# particular allocation, a particular operation, a particular state / a
# particular result)). The "3" distinguishes it from ProcessElementAccess_8217A400 and ProcessElementAccess2
# _8217A6C0.
[entrypoint.functions."0x8217AA20"]
name = "ProcessElementAccess3_8217AA20"

# A process (a lookup, with a check and a comparison, the caller of LinearSearch_82284448 / FloatToIntProcess_8228
# 4810, the sibling of LinearSearch_82284448): perform a lookup (a lookup, a particular lookup / a
# particular search, a particular size / a particular allocation, a particular operation, a particular state / a
# particular result, calling LinearSearch_82284448, on a value (a, a value, a particular value, a
# particular size / a particular allocation, a particular target), and a value (b, a value, a
# particular value, a particular size / a particular allocation, a particular source), and a value (c, a
# value, a particular value, a particular size / a particular allocation, a particular key), a particular
# lookup / a particular search, a particular size / a particular allocation, a particular operation, a
# particular state / a particular result), and (if a result (r29, a value, a particular value, a
# particular size / a particular allocation, a particular result) is non-zero, a particular value) check a value
# (r29->4, a value, a particular value, a particular size / a particular allocation, a particular key): if it is != a
# value (c, a value, a particular value, a particular size / a particular allocation, a particular key), take one
# path (a no-op / a return, a particular state, a particular result, a particular size / a
# particular allocation); if it is == the value (a particular state, a particular value, a
# particular size / a
# particular allocation), check a flag (r29->0 & 0x1C, a flag, a particular value, a
# particular state, a particular size / a
# particular allocation): if it is != 20 (a particular state, a
# particular value, a particular size / a
# particular allocation), take one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation); if it is == 20 (a particular state, a
# particular value, a particular size / a
# particular allocation), take a path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation), and (otherwise) take one path (a no-op / a
# return, a particular state, a
# particular result, a particular size / a
# particular allocation), and (in paths) process (FloatToIntProcess_82284810, on a value, a
# particular process / a
# particular float-to-int, a particular size / a
# particular allocation, a particular operation, a particular state / a
# particular result).

# Args: obj (r3, r31, an object), a (r4, r28, ->28 = a value, a particular value, a
# particular size / a
# particular allocation), b (r5, a value, a particular value, a
# particular size / a
# particular allocation), c (r6, r27, a value, a particular value, a
# particular size / a
# particular allocation), (more). It performs a lookup (a
# lookup, a particular lookup / a
# particular search, a particular size / a
# particular allocation, a
# particular operation, a particular state / a
# particular result, calling LinearSearch_82284448, sub_82284448, a linear search, on a
# value (a, a value, a particular value, a particular size / a
# particular allocation, a
# particular target), and a value (b, a value, a
# particular value, a particular size / a
# particular allocation, a
# particular source), and a value (c, a
# value, a particular value, a particular size / a
# particular allocation, a
# particular key), a particular
# lookup / a
# particular search, a particular size / a
# particular allocation, a particular operation, a particular state / a
# particular result). It checks a result (r29, a value, a
# particular value, a particular size / a
# particular allocation, a
# particular result): if it is non-zero (a particular value, a
# particular state, a particular size / a
# particular allocation), it checks a value (r11, r29->4, a
# value, a particular value, a particular size / a
# particular allocation, a
# particular key): if it is != a value (r27, c, a
# value, a particular value, a particular size / a
# particular allocation, a
# particular key), it takes one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation); if it is == the value (a particular state, a
# particular value, a particular size / a
# particular allocation), it checks a flag (r10, r29->0 & 0x1C, a
# flag, a particular value, a
# particular state, a particular size / a
# particular allocation): if it is != 20 (a particular state, a
# particular value, a particular size / a
# particular allocation), it takes one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation); if it is == 20 (a particular state, a
# particular value, a particular size / a
# particular allocation), it takes a path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation). If the result is zero (a null state, a
# particular state, a particular size / a
# particular allocation), it takes one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation). In paths it processes (FloatToIntProcess_82284810, sub_82284810, a
# float-to-int process, on a value (a, a value, a
# particular value, a particular size, a particular size / a
# particular allocation), a particular process / a
# particular float-to-int, a particular size / a
# particular allocation, a particular operation, a particular state / a
# particular result). So it's a "process (a lookup, with a check and a comparison: perform a
# lookup (a lookup, a particular lookup / a
# particular search, a particular size / a
# particular allocation, a
# particular operation, a particular state / a
# particular result, calling LinearSearch_82284448, on a value (a, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular target), and a value (b, a value, a
# particular value, a particular size / a
# particular allocation, a
# particular source), and a value (c, a
# value, a particular value, a particular size / a
# particular allocation, a
# particular key), a particular
# lookup / a
# particular search, a particular size / a
# particular allocation, a particular operation, a particular state / a
# particular result), and (if a result (r29, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular result) is non-zero, a particular value) check a value
# (r29->4, a value, a particular value, a particular size / a
# particular allocation, a particular key): if it is != a
# value (c, a value, a particular value, a particular size / a
# particular allocation, a
# particular key), take one
# path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation); if it is == the value (a particular state, a
# particular value, a
# particular size / a
# particular allocation), check a flag (r29->0 & 0x1C, a flag, a
# particular value, a
# particular state, a particular size / a
# particular allocation): if it is != 20 (a particular state, a
# particular value, a particular size / a
# particular allocation), take one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation); if it is == 20 (a particular state, a
# particular value, a particular size / a
# particular allocation), take a path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation), and (otherwise) take one path (a no-op / a
# return, a particular state, a
# particular result, a particular size / a
# particular allocation), and (in paths) process (FloatToIntProcess_82284810, on a value, a
# particular process / a
# particular float-to-int, a particular size / a
# particular allocation, a particular operation, a particular state / a
# particular result))" routine (a lookup process, with a check and a comparison, part
# of the 0x82284 cluster, a sibling of LinearSearch_82284448, a caller of LinearSearch_82284448 and
# FloatToIntProcess_82284810). 184 lines, 2 callees (FloatToIntProcess_82284810, LinearSearch_82284448). No
# direct callers (it's called via a vtable / it is registered-but-uncalled, part of the game's subsystems). The
# object's class (obj, r31, an object, a
# particular structure, a lookup object, a particular size / a
# particular allocation, a
# particular value /
# a particular state) and the value's meaning (a, r28, ->28 = a value, a
# particular value, a
# particular size / a
# particular allocation, b, a value, a particular value, a
# particular size / a
# particular allocation, c, r27, a value, a particular value, a
# particular size / a
# particular allocation) aren't resolvable, so the name states the mechanism: a lookup process (with a
# check and a comparison) (perform a
# lookup (a lookup, a particular lookup / a
# particular search, a particular size / a
# particular allocation, a
# particular operation, a particular state / a
# particular result, calling LinearSearch_82284448, on a value (a, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular target), and a value (b, a value, a
# particular value, a particular size / a
# particular allocation, a
# particular source), and a value (c, a
# value, a particular value, a particular size / a
# particular allocation, a
# particular key), a particular
# lookup / a
# particular search, a particular size / a
# particular allocation, a particular operation, a particular state / a
# particular result), and (if a result (r29, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular result) is non-zero, a particular value) check a value
# (r29->4, a value, a particular value, a particular size / a
# particular allocation, a particular key): if it is != a
# value (c, a value, a particular value, a particular size / a
# particular allocation, a
# particular key), take one
# path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation); if it is == the value (a particular state, a
# particular value, a
# particular size / a
# particular allocation), check a flag (r29->0 & 0x1C, a flag, a
# particular value, a
# particular state, a particular size / a
# particular allocation): if it is != 20 (a particular state, a
# particular value, a particular size / a
# particular allocation), take one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation); if it is == 20 (a particular state, a
# particular value, a particular size / a
# particular allocation), take a path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation), and (otherwise) take one path (a no-op / a
# return, a particular state, a
# particular result, a particular size / a
# particular allocation), and (in paths) process (FloatToIntProcess_82284810, on a value, a
# particular process / a
# particular float-to-int, a particular size / a
# particular allocation, a particular operation, a particular state / a
# particular result)). The "LookupCheck" refers to the lookup (a
# lookup, a particular lookup / a
# particular search, a particular size / a
# particular allocation, a
# particular operation, a particular state / a
# particular result) and the check (a
# check, a particular check / a
# particular comparison, a particular size / a
# particular allocation, a
# particular operation, a particular state / a
# particular result).
[entrypoint.functions."0x82284300"]
name = "ProcessLookupCheck_82284300"

# A count (a count, with a loop and a comparison, the sibling of ProcessLookupCheck_82284300): perform a count (a
# count, a particular count / a particular number, a particular size / a particular allocation, a particular
# operation, a particular state / a particular result), and (in a loop) check a value (base->0, a value, a
# particular value, a particular size / a particular allocation, a 2-byte element, a particular element): if it is
# zero (a null state, a particular state, a particular size / a particular allocation), take one path (a no-op / a
# return, a particular state, a particular result, a particular size / a particular allocation); if it is non-zero (a
# particular value, a particular state, a particular size / a particular allocation), increment a count (r11, a
# count, a particular count, a particular size / a particular allocation, a particular number), and advance a base
# (r3, a base, a particular value, a particular size / a particular allocation, a 2-byte element, a
# particular source), and (if a count (r11, a count, a particular count, a particular size / a
# particular allocation, a
# particular number) is < a value (r4, a count, a particular count, a particular size / a
# particular allocation, a
# particular number), a particular count) continue the loop (a loop, a particular loop / a
# particular iteration, a particular size / a
# particular allocation, a
# particular operation, a particular state / a
# particular result), and (otherwise) take one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation), and return the count (r11, a count, a particular count, a
# particular size / a
# particular allocation, a
# particular number).

# Args: base (r3, a value, a particular value, a particular size / a particular allocation, a 2-byte element, a
# particular source), count (r4, a count, a particular count, a particular size / a
# particular allocation, a
# particular number), (more). It performs a count (a
# count, a particular count / a
# particular number, a particular size / a
# particular allocation, a
# particular operation, a particular state / a
# particular result). In the loop, it checks a value (r10, base->0, a
# value, a particular value, a particular size / a
# particular allocation, a 2-byte element, a
# particular element): if it is zero (a null state, a
# particular state, a particular size / a
# particular allocation), it takes one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation); if it is non-zero (a particular value, a
# particular state, a particular size / a
# particular allocation), it increments a count (r11, r11 + 1, a
# count, a particular count, a particular size / a
# particular allocation, a
# particular number), and advances a base (r3, r3 + 2, a
# base, a particular value, a particular size / a
# particular allocation, a 2-byte element, a
# particular source). It checks a count (r11, a count, a
# particular count, a particular size / a
# particular allocation, a
# particular number): if it is < a value (r4, a count, a
# particular count, a particular size / a
# particular allocation, a
# particular number), it continues the loop (a
# loop, a particular loop / a
# particular iteration, a particular size / a
# particular allocation, a
# particular operation, a particular state / a
# particular result); if it is >= the value (a particular state, a
# particular value, a particular size / a
# particular allocation), it takes one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation). It returns the count (r11, a count, a
# particular count, a particular size / a
# particular allocation, a
# particular number). So it's a "count (a count, with a loop and a comparison: perform a
# count (a
# count, a particular count / a
# particular number, a particular size / a
# particular allocation, a
# particular operation, a particular state / a
# particular result), and (in a loop) check a value (base->0, a value, a
# particular value, a particular size / a
# particular allocation, a 2-byte element, a
# particular element): if it is
# zero (a null state, a particular state, a particular size / a
# particular allocation), take one path (a no-op / a
# return, a particular state, a
# particular result, a particular size / a
# particular allocation); if it is non-zero (a
# particular value, a
# particular state, a particular size / a
# particular allocation), increment a count (r11, a
# count, a particular count, a particular size / a
# particular allocation, a
# particular number), and advance a base
# (r3, a base, a particular value, a particular size / a
# particular allocation, a 2-byte element, a
# particular source), and (if a count (r11, a count, a
# particular count, a particular size / a
# particular allocation, a
# particular number) is < a value (r4, a count, a
# particular count, a particular size / a
# particular allocation, a
# particular number), a particular count) continue the loop (a loop, a particular loop / a
# particular iteration, a particular size / a
# particular allocation, a
# particular operation, a particular state / a
# particular result), and (otherwise) take one path (a no-op / a
# return, a particular state, a
# particular result, a particular size / a
# particular allocation), and return the count (r11, a count, a
# particular count, a
# particular size / a
# particular allocation, a
# particular number))" routine (a count routine, part of the 0x822CA cluster, a sibling of
# ProcessLookupCheck_82284300). 30 lines, 0 callees. 2 callers: sub_822CA580, sub_82CA67D0 (processes, which
# call this to perform the count, a particular count / a
# particular number, a
# particular size / a
# particular allocation, a
# particular operation). The values' meanings (base, r3, a value, a
# particular value, a particular size / a
# particular allocation, a 2-byte element, a
# particular source, count, r4, a count, a
# particular count, a particular size / a
# particular allocation, a
# particular number) aren't resolvable, so the name states the mechanism: a count (with a
# loop and a comparison) (perform a
# count (a
# count, a particular count / a
# particular number, a particular size / a
# particular allocation, a
# particular operation, a particular state / a
# particular result), and (in a loop) check a value (base->0, a value, a
# particular value, a particular size / a
# particular allocation, a 2-byte element, a
# particular element): if it is
# zero (a null state, a particular state, a particular size / a
# particular allocation), take one path (a no-op / a
# return, a particular state, a
# particular result, a particular size / a
# particular allocation); if it is non-zero (a
# particular value, a
# particular state, a particular size / a
# particular allocation), increment a count (r11, a
# count, a particular count, a particular size / a
# particular allocation, a
# particular number), and advance a base
# (r3, a base, a particular value, a particular size / a
# particular allocation, a 2-byte element, a
# particular source), and (if a count (r11, a count, a
# particular count, a particular size / a
# particular allocation, a
# particular number) is < a value (r4, a count, a
# particular count, a particular size / a
# particular allocation, a
# particular number), a particular count) continue the loop (a loop, a particular loop / a
# particular iteration, a particular size / a
# particular allocation, a
# particular operation, a particular state / a
# particular result), and (otherwise) take one path (a no-op / a
# return, a particular state, a
# particular result, a particular size / a
# particular allocation), and return the count (r11, a count, a
# particular count, a
# particular size / a
# particular allocation, a
# particular number)). The "NonZero" refers to the count (a
# count, a particular count / a
# particular number, a particular size / a
# particular allocation, a
# particular operation, a particular state / a
# particular result) of non-zero values (a
# value, a particular value, a particular size / a
# particular allocation, a 2-byte element, a
# particular element, a
# particular value, a
# particular state).
[entrypoint.functions."0x822CA680"]
name = "CountNonZero_822CA680"

# A 32-byte constructor (the sibling of Construct32Byte_82B722E8 / Construct32Byte2_83221998, with a constant and
# a complex computation): initialize the object's fields (a 32-byte object, a particular structure, a
# particular size / a particular allocation, obj->0 = a constant, from a fixed .data address, a class id / a
# marker, a particular class / a particular marker, obj->4 = 0, a field, a count / a size, a zero, obj->20 = 1, a
# field, a value, a one, obj->8 = a constant, from a fixed .data address, a float, a particular value, obj->21 = 1, a
# field, a value, a one, obj->12 = a constant, from a fixed .data address, a float, a particular value, obj->22 = 0, a
# field, a count / a size, a zero, obj->16 = a constant, from a fixed .data address, a float, a particular value,
# obj->23 = 0, a field, a count / a size, a zero, obj->32 = a constant, from a fixed .data address, a float, a
# particular value, obj->24 = 0, a field, a count / a size, a zero, obj->36 = a constant, from a fixed .data address,
# a float, a particular value, obj->28 = 0, a field, a count / a size, a zero, obj->40 = a constant, from a fixed
# .data address, a float, a particular value, obj->44 = 1, a field, a value, a one, obj->56 = a constant, from a
# fixed .data address, a float, a particular value, a 32-byte object, a particular structure, a
# particular size / a
# particular allocation, a particular initialization).

# Args: obj (r3, ->0 = a field, a constant, a class id / a marker, ->4 = a field, a count / a size, ->8 = a field,
# a float, ->12 = a field, a float, ->16 = a field, a float, ->20 = a field, a value, ->21 = a field, a
# value, ->22 = a field, a count / a size, ->23 = a field, a count / a size, ->24 = a field, a
# count / a size, ->28 = a field, a count / a size, ->32 = a field, a float, ->36 = a field, a
# float, ->40 = a field, a float, ->44 = a field, a value, ->56 = a field, a float, a
# 32-byte object), (more). It initializes the object's fields (a 32-byte object, a
# particular structure, a
# particular size / a
# particular allocation: obj->0 = a constant, from a fixed .data address, a class id / a
# marker, a particular class / a
# particular marker, obj->4 = 0, a field, a count / a size, a zero, obj->20 = 1, a
# field, a value, a one, obj->8 = a constant, from a fixed .data address, a float, a
# particular value, obj->21 = 1, a
# field, a value, a one, obj->12 = a constant, from a fixed .data address, a float, a
# particular value, obj->22 = 0, a
# field, a count / a size, a zero, obj->16 = a constant, from a fixed .data address, a float, a
# particular value, obj->23 = 0, a
# field, a count / a size, a zero, obj->32 = a constant, from a fixed .data address, a float, a
# particular value, obj->24 = 0, a
# field, a count / a size, a zero, obj->36 = a constant, from a fixed .data address, a float, a
# particular value, obj->28 = 0, a
# field, a count / a size, a zero, obj->40 = a constant, from a fixed .data address, a float, a
# particular value, obj->44 = 1, a
# field, a value, a one, obj->56 = a constant, from a fixed .data address, a float, a
# particular value, a 32-byte object, a
# particular structure, a
# particular size / a
# particular allocation, a
# particular initialization). So it's a "construct a 32-byte object (initialize the object's fields (a
# 32-byte object, a
# particular structure, a
# particular size / a
# particular allocation, obj->0 = a constant, from a fixed .data address, a class id / a
# marker, a particular class / a
# particular marker, obj->4 = 0, a field, a count / a size, a zero, obj->20 = 1, a
# field, a value, a one, obj->8 = a constant, from a fixed .data address, a float, a
# particular value, obj->21 = 1, a
# field, a value, a one, obj->12 = a constant, from a fixed .data address, a float, a
# particular value, obj->22 = 0, a
# field, a count / a size, a zero, obj->16 = a constant, from a fixed .data address, a float, a
# particular value, obj->23 = 0, a
# field, a count / a size, a zero, obj->32 = a constant, from a fixed .data address, a float, a
# particular value, obj->24 = 0, a
# field, a count / a size, a zero, obj->36 = a constant, from a fixed .data address, a float, a
# particular value, obj->28 = 0, a
# field, a count / a size, a zero, obj->40 = a constant, from a fixed .data address, a float, a
# particular value, obj->44 = 1, a
# field, a value, a one, obj->56 = a constant, from a fixed .data address, a float, a
# particular value, a 32-byte object, a
# particular structure, a
# particular size / a
# particular allocation))" routine (a 32-byte constructor, with a
# constant and a
# complex computation, part
# of the 0x82BE4 cluster, a sibling of Construct32Byte_82B722E8 and Construct32Byte2_83221998). 109
# lines, 0 callees. 3 callers: sub_8323FA78, sub_8323FCA8, sub_8323FF48 (siblings in the 0x8323F cluster, a
# process, which calls this to construct a 32-byte object, a
# particular size / a
# particular allocation, a
# particular operation). The object's class (obj, ->0 = a field, a constant, a class id / a
# marker, ->4 = a field, a count / a size, ->8 = a field, a float, ->12 = a field, a
# float, ->16 = a field, a float, ->20 = a field, a value, ->21 = a field, a
# value, ->22 = a field, a count / a size, ->23 = a field, a count / a size, ->24 = a field, a
# count / a size, ->28 = a field, a count / a size, ->32 = a field, a float, ->36 = a field, a
# float, ->40 = a field, a float, ->44 = a field, a value, ->56 = a field, a float, a
# 32-byte object, a
# particular structure, a
# particular size / a
# particular allocation, a
# particular value / a
# particular state) isn't resolvable, so the name states the mechanism: a 32-byte constructor (with a
# constant and a
# complex computation) (initialize the object's fields (a
# 32-byte object, a
# particular structure, a
# particular size / a
# particular allocation, obj->0 = a constant, from a fixed .data address, a class id / a
# marker, a particular class / a
# particular marker, obj->4 = 0, a field, a count / a size, a zero, obj->20 = 1, a
# field, a value, a one, obj->8 = a constant, from a fixed .data address, a float, a
# particular value, obj->21 = 1, a
# field, a value, a one, obj->12 = a constant, from a fixed .data address, a float, a
# particular value, obj->22 = 0, a
# field, a count / a size, a zero, obj->16 = a constant, from a fixed .data address, a float, a
# particular value, obj->23 = 0, a
# field, a count / a size, a zero, obj->32 = a constant, from a fixed .data address, a float, a
# particular value, obj->24 = 0, a
# field, a count / a size, a zero, obj->36 = a constant, from a fixed .data address, a float, a
# particular value, obj->28 = 0, a
# field, a count / a size, a zero, obj->40 = a constant, from a fixed .data address, a float, a
# particular value, obj->44 = 1, a
# field, a value, a one, obj->56 = a constant, from a fixed .data address, a float, a
# particular value, a 32-byte object, a
# particular structure, a
# particular size / a
# particular allocation)). The "3" distinguishes it from Construct32Byte_82B722E8 and Construct32Byte2_83221998.
[entrypoint.functions."0x82BE49F0"]
name = "Construct32Byte3_82BE49F0"

# An atomic decrement (with a release, the sibling of AtomicDecrement_822C72E0 / AtomicDecrement2_826C0B30): perform an
# atomic decrement (an atomic decrement, a particular decrement / a particular count, a particular size / a
# particular allocation, a particular operation, a particular state / a particular result), and (if a reference
# count (obj->4, a reference count, a particular count, a particular size / a particular allocation) is zero, a
# null state, a particular state) release a value (a value, a particular value, a particular size / a
# particular allocation, a particular operation, a particular state / a particular result), and (otherwise) take
# one path (a no-op / a return, a particular state, a particular result, a particular size / a
# particular allocation).

# Args: obj (r3, ->0 = a value, a particular value, a particular size / a particular allocation, ->4 = a reference
# count, a particular count, a particular size / a particular allocation, a reference counted object), (more). It
# performs an atomic decrement (an atomic decrement, a particular decrement / a particular count, a
# particular size / a
# particular allocation, a
# particular operation, a particular state / a
# particular result: it reads a reference count (r10, obj->4, a reference count, a
# particular count, a particular size / a
# particular allocation, a
# particular value), and stores it (r10 - 1, an atomic decrement, a
# particular count, a particular size / a
# particular allocation, a
# particular value)). It checks a reference count (r31, obj->4, a
# reference count, a
# particular count, a particular size / a
# particular allocation): if it is zero (a null state, a
# particular state, a particular size / a
# particular allocation), it releases a value (a
# value, a particular value, a particular size / a
# particular allocation, a
# particular operation, a particular state / a
# particular result); if it is non-zero (a particular value, a
# particular state, a particular size / a
# particular allocation), it takes one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation). So it's an "atomic decrement (with a release: perform an
# atomic decrement (an atomic decrement, a particular decrement / a
# particular count, a
# particular size / a
# particular allocation, a
# particular operation, a particular state / a
# particular result), and (if a reference
# count (obj->4, a reference count, a particular count, a particular size / a
# particular allocation) is zero, a
# null state, a particular state) release a value (a value, a particular value, a particular size / a
# particular allocation, a
# particular operation, a particular state / a
# particular result), and (otherwise) take
# one path (a no-op / a return, a particular state, a particular result, a particular size / a
# particular allocation))" routine (an atomic decrement, with a
# release, part of the 0x82375 cluster, a sibling of AtomicDecrement_822C72E0 and
# AtomicDecrement2_826C0B30). 77 lines, 0 callees. No
# direct callers (it's called via a vtable / it is registered-but-uncalled, part of the game's subsystems). The
# object's class (obj, ->0 = a value, a particular value, a particular size / a
# particular allocation, ->4 = a reference
# count, a
# particular count, a particular size / a
# particular allocation, a reference counted object, a
# particular value / a
# particular state) isn't resolvable, so the name states the mechanism: an atomic decrement (with a
# release) (perform an
# atomic decrement (an atomic decrement, a particular decrement / a
# particular count, a
# particular size / a
# particular allocation, a
# particular operation, a particular state / a
# particular result), and (if a reference
# count (obj->4, a reference count, a particular count, a particular size / a
# particular allocation) is zero, a
# null state, a particular state) release a value (a value, a particular value, a particular size / a
# particular allocation, a
# particular operation, a particular state / a
# particular result), and (otherwise) take
# one path (a no-op / a return, a particular state, a particular result, a particular size / a
# particular allocation)). The "3" distinguishes it from AtomicDecrement_822C72E0 and AtomicDecrement2_826C0B30.
[entrypoint.functions."0x82375F50"]
name = "AtomicDecrement3_82375F50"

# A process (a dispatch, with a switch and a call, the sibling of AtomicDecrement3_82375F50 / ProcessDispatch
# _82BCD7B0, the caller of sub_82815CB0 / sub_82188B88 / sub_82815E00): perform a dispatch (a dispatch, a
# particular dispatch / a particular value, a particular size / a particular allocation, a particular operation, a
# particular state / a particular result), and (in a switch, on a value (a->10, a type, a particular value, a
# particular state, a particular size / a particular allocation), a switch on a type) take a path (a no-op / a
# return, a particular state, a particular result, a particular size / a particular allocation), and (in paths) call
# a process (sub_82815CB0, on a value, a particular process, a particular operation, a particular size / a
# particular allocation, a particular state / a particular result), and a process (sub_82188B88, on a value, a
# particular process, a particular operation, a particular size / a particular allocation, a particular state / a
# particular result), and a process (sub_82815E00, on a value, a particular process, a particular operation, a
# particular size / a particular allocation, a particular state / a particular result).

# Args: obj (r3, a value), a (r4, ->10 = a type, a particular value, a particular state, a particular size / a
# particular allocation, a dispatch object), (more). It performs a dispatch (a dispatch, a particular dispatch / a
# particular value, a particular size / a particular allocation, a particular operation, a particular state / a
# particular result). In the switch, on a value (r11, a->10, a type, a particular value, a particular state, a
# particular size / a particular allocation), it takes a path: if the type is 6 (a particular state, a
# particular value, a particular size / a particular allocation), it calls a process (sub_82815CB0, sub_82815CB0, a
# process, on a value (obj, a value, a particular value, a particular size, a particular size / a
# particular allocation), a particular process, a particular operation, a particular size / a
# particular allocation, a particular state / a particular result); if the type is 2 (a particular state, a
# particular value, a particular size / a particular allocation), it calls a process (sub_82188B88, sub_82188B88, a
# process, on a value (obj, a value, a particular value, a particular size, a particular size / a
# particular allocation), a particular process, a particular operation, a particular size / a
# particular allocation, a particular state / a particular result); if the type is 7 (a particular state, a
# particular value, a particular size / a particular allocation), it calls a process (sub_82815E00, sub_82815E00, a
# process, on a value (obj, a value, a particular value, a particular size, a particular size / a
# particular allocation), a particular process, a particular operation, a particular size / a
# particular allocation, a particular state / a particular result); if the type is other (a particular state, a
# particular value, a particular size / a particular allocation), it takes one path (a no-op / a return, a
# particular state, a particular result, a particular size / a particular allocation). So it's a "process (a
# dispatch, with a switch and a call: perform a dispatch (a dispatch, a particular dispatch / a
# particular value, a particular size / a particular allocation, a particular operation, a
# particular state / a
# particular result), and (in a switch, on a value (a->10, a type, a
# particular value, a
# particular state, a particular size / a
# particular allocation), a switch on a type) take a path (a no-op / a
# return, a particular state, a
# particular result, a particular size / a
# particular allocation), and (in paths) call
# a process (sub_82815CB0, on a value, a
# particular process, a
# particular operation, a particular size / a
# particular allocation, a particular state / a
# particular result), and a process (sub_82188B88, on a
# value, a particular process, a
# particular operation, a particular size / a
# particular allocation, a particular state / a
# particular result), and a process (sub_82815E00, on a
# value, a particular process, a
# particular operation, a particular size / a
# particular allocation, a particular state / a
# particular result))" routine (a dispatch process, part of the 0x82815 cluster, a sibling of
# AtomicDecrement3_82375F50 and ProcessDispatch_82BCD7B0, a caller of sub_82815CB0, sub_82188B88, and
# sub_82815E00). 29 lines, 3 callees (sub_82188B88, sub_82815CB0, sub_82815E00). No
# direct callers (it's called via a vtable / it is registered-but-uncalled, part of the game's subsystems). The
# object's class (obj, r3, a value, a particular value, a particular size / a
# particular allocation, a
# particular structure, a dispatch object, a
# particular value /
# a particular state) and the value's meaning (a, r4, ->10 = a type, a
# particular value, a
# particular state, a
# particular size / a
# particular allocation, a
# dispatch object) aren't resolvable, so the name states the mechanism: a dispatch process (with a
# switch and a call) (perform a dispatch (a dispatch, a particular dispatch / a
# particular value, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a switch, on a value (a->10, a type, a
# particular value, a
# particular state, a
# particular size / a
# particular allocation), a switch on a type) take a path (a no-op / a
# return, a particular state, a
# particular result, a particular size / a
# particular allocation), and (in paths) call
# a process (sub_82815CB0, on a value, a
# particular process, a
# particular operation, a particular size / a
# particular allocation, a particular state / a
# particular result), and a process (sub_82188B88, on a
# value, a particular process, a
# particular operation, a particular size / a
# particular allocation, a particular state / a
# particular result), and a process (sub_82815E00, on a
# value, a particular process, a
# particular operation, a particular size / a
# particular allocation, a particular state / a
# particular result)). The "3" distinguishes it from ProcessDispatch_82BCD7B0 and ProcessDispatch2_82BCD9E8.
[entrypoint.functions."0x82815C28"]
name = "ProcessDispatch3_82815C28"

# A process (a lookup, with a check and a compare, the sibling of ProcessLookupCheck_82284300 / ProcessDispatch3
# _82815C28, the caller of no functions): perform a lookup (a lookup, a particular lookup / a
# particular search, a particular size / a particular allocation, a particular operation, a
# particular state / a
# particular result), and (in a loop) check a value (r9, a value, a particular value, a
# particular size / a
# particular allocation, a particular element): if it is zero (a null state, a
# particular state, a
# particular size / a
# particular allocation), take one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation); if it is == a value (r4, a value, a particular value, a
# particular size / a
# particular allocation, a
# particular key), take a path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation), and (otherwise) trap (a trap, a particular trap / a
# particular state, a
# particular size / a
# particular allocation, a
# particular operation, a particular state / a
# particular result), and (in paths) compare (a comparison, a particular comparison / a
# particular check, a particular size / a
# particular allocation, a
# particular operation, a particular state / a
# particular result, a value (r11, a value, a particular value, a
# particular size / a
# particular allocation, a
# particular element) and a value (r7, a value, a particular value, a
# particular size / a
# particular allocation, a
# particular element)).

# Args: obj (r4, ->4 = a value, a particular value, a
# particular size / a
# particular allocation, a
# particular structure, a lookup object), (more). It performs a lookup (a
# lookup, a particular lookup / a
# particular search, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). In the loop, it checks a value (r9, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular element): if it is zero (a null state, a
# particular state, a
# particular size / a
# particular allocation), it takes one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation); if it is == a value (r4, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular key), it takes a path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation); if it is other (a particular state, a
# particular value, a
# particular size / a
# particular allocation), it traps (a trap, a particular trap / a
# particular state, a
# particular size / a
# particular allocation, a
# particular operation, a particular state / a
# particular result). In paths it compares (a
# comparison, a
# particular comparison / a
# particular check, a particular size / a
# particular allocation, a
# particular operation, a particular state / a
# particular result, a value (r11, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular element) and a value (r7, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular element)). So it's a "process (a lookup, with a check and a compare: perform a
# lookup (a lookup, a particular lookup / a
# particular search, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a loop) check a value (r9, a value, a
# particular value, a
# particular size / a
# particular allocation, a particular element): if it is zero (a null state, a
# particular state, a
# particular size / a
# particular allocation), take one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation); if it is == a value (r4, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular key), take a path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation), and (otherwise) trap (a trap, a particular trap / a
# particular state, a
# particular size / a
# particular allocation, a
# particular operation, a particular state / a
# particular result), and (in paths) compare (a comparison, a particular comparison / a
# particular check, a particular size / a
# particular allocation, a
# particular operation, a particular state / a
# particular result, a value (r11, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular element) and a value (r7, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular element))" routine (a lookup process, with a
# check and a
# compare, part
# of the 0x82A0F cluster, a sibling of ProcessLookupCheck_82284300 and ProcessDispatch3_82815C28). 167
# lines, 0 callees. 1 caller: ReassignBlock_82A0F6B8 (a process, which
# calls this to perform the lookup, a
# particular size / a
# particular allocation, a
# particular operation). The object's class (obj, r4, ->4 = a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular structure, a lookup object, a
# particular value /
# a particular state) isn't resolvable, so the name states the mechanism: a lookup process (with a
# check and a
# compare) (perform a
# lookup (a lookup, a particular lookup / a
# particular search, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a loop) check a value (r9, a value, a
# particular value, a
# particular size / a
# particular allocation, a particular element): if it is zero (a null state, a
# particular state, a
# particular size / a
# particular allocation), take one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation); if it is == a value (r4, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular key), take a path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation), and (otherwise) trap (a trap, a particular trap / a
# particular state, a
# particular size / a
# particular allocation, a
# particular operation, a particular state / a
# particular result), and (in paths) compare (a comparison, a particular comparison / a
# particular check, a particular size / a
# particular allocation, a
# particular operation, a particular state / a
# particular result, a value (r11, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular element) and a value (r7, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular element)). The "2" distinguishes it from ProcessLookupCheck_82284300 and ProcessLookup_82B5E330.
[entrypoint.functions."0x82A0FB80"]
name = "ProcessLookup2_82A0FB80"

# A process (a count, with a check and a call, the caller of CountNonZero_822CA680 / sub_822F5150 / sub_822F6480, the
# sibling of CountNonZero_822CA680 / ProcessLookup2_82A0FB80): perform a count (a count, a particular count / a
# particular number, a particular size / a particular allocation, a particular operation, a particular state / a
# particular result, calling CountNonZero_822CA680, on a value (obj, a value, a particular value, a
# particular size / a particular allocation, a particular source), and a value (2048, a constant, a
# particular value, a particular size / a particular allocation, a particular count), a particular count, a
# particular size / a particular allocation, a particular operation, a particular state / a
# particular result), and (if a count (r29, a count, a particular count, a particular size / a
# particular allocation, a
# particular number) is < 40, a particular threshold, a
# particular count) take one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation), and (otherwise) call a process (sub_822F5150, on a value (stack[96], a
# value, a
# particular value, a particular size / a
# particular allocation, a
# particular target), and a value (obj, a
# value, a particular value, a
# particular size / a
# particular allocation, a
# particular source), a particular process, a
# particular operation, a particular size / a
# particular allocation, a particular state / a
# particular result), and a process (sub_822F6480, on a
# value (stack[96], a
# value, a
# particular value, a particular size / a
# particular allocation, a
# particular target), and a value (stack[80], a
# value, a
# particular value, a particular size / a
# particular allocation, a
# particular source), a particular process, a
# particular operation, a particular size / a
# particular allocation, a particular state / a
# particular result), and (in paths) call a process (sub_822F20F0, on a
# value, a particular process, a
# particular operation, a particular size / a
# particular allocation, a particular state / a
# particular result), and a process (sub_822F5208, on a
# value, a particular process, a
# particular operation, a particular size / a
# particular allocation, a particular state / a
# particular result).

# Args: obj (r3, r31, a value, a particular value, a particular size / a
# particular allocation, a
# particular structure, a count object), (more). It checks a value (r31, obj, a
# value, a
# particular value, a
# particular size / a
# particular allocation): if it is zero (a null state, a
# particular state, a
# particular size / a
# particular allocation), it takes one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation). If it is non-zero (a particular value, a
# particular state, a
# particular size / a
# particular allocation), it performs a count (a
# count, a particular count / a
# particular number, a particular size / a
# particular allocation, a
# particular operation, a particular state / a
# particular result, calling CountNonZero_822CA680, sub_822CA680, a count, on a
# value (obj, a value, a particular value, a
# particular size / a
# particular allocation, a
# particular source), and a value (2048, a constant, a
# particular value, a particular size / a
# particular allocation, a
# particular count), a particular count, a
# particular size / a
# particular allocation, a particular operation, a particular state / a
# particular result). It checks a count (r29, a count, a
# particular count, a particular size / a
# particular allocation, a
# particular number): if it is < 40 (a particular threshold, a
# particular count, a
# particular value, a
# particular state, a
# particular size / a
# particular allocation), it takes one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation); if it is >= 40 (a particular threshold, a
# particular count, a
# particular value, a
# particular state, a
# particular size / a
# particular allocation), it calls a process (sub_822F5150, sub_822F5150, a
# process, on a
# value (stack[96], a
# value, a
# particular value, a particular size / a
# particular allocation, a
# particular target), and a value (obj, a
# value, a particular value, a
# particular size / a
# particular allocation, a
# particular source), a particular process, a
# particular operation, a particular size / a
# particular allocation, a particular state / a
# particular result), and a process (sub_822F6480, sub_822F6480, a
# process, on a
# value (stack[96], a
# value, a
# particular value, a particular size / a
# particular allocation, a
# particular target), and a value (stack[80], a
# value, a
# particular value, a particular size / a
# particular allocation, a
# particular source), a particular process, a
# particular operation, a particular size / a
# particular allocation, a particular state / a
# particular result). In paths it calls a process (sub_822F20F0, sub_822F20F0, a
# process, on a
# value (obj, a value, a particular value, a
# particular size, a
# particular size / a
# particular allocation), a particular process, a
# particular operation, a particular size / a
# particular allocation, a particular state / a
# particular result), and a process (sub_822F5208, sub_822F5208, a
# process, on a
# value (obj, a value, a particular value, a
# particular size, a
# particular size / a
# particular allocation), a particular process, a
# particular operation, a particular size / a
# particular allocation, a particular state / a
# particular result). So it's a "process (a count, with a check and a call: perform a
# count (a
# count, a particular count / a
# particular number, a particular size / a
# particular allocation, a
# particular operation, a particular state / a
# particular result, calling CountNonZero_822CA680, on a value (obj, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular source), and a value (2048, a constant, a
# particular value, a particular size / a
# particular allocation, a
# particular count), a particular count, a
# particular size / a
# particular allocation, a particular operation, a particular state / a
# particular result), and (if a count (r29, a count, a
# particular count, a particular size / a
# particular allocation, a
# particular number) is < 40, a particular threshold, a
# particular count) take one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation), and (otherwise) call a process (sub_822F5150, on a value (stack[96], a
# value, a
# particular value, a particular size / a
# particular allocation, a
# particular target), and a value (obj, a
# value, a particular value, a
# particular size / a
# particular allocation, a
# particular source), a particular process, a
# particular operation, a particular size / a
# particular allocation, a particular state / a
# particular result), and a process (sub_822F6480, on a
# value (stack[96], a
# value, a
# particular value, a particular size / a
# particular allocation, a
# particular target), and a value (stack[80], a
# value, a
# particular value, a particular size / a
# particular allocation, a
# particular source), a particular process, a
# particular operation, a particular size / a
# particular allocation, a particular state / a
# particular result), and (in paths) call a process (sub_822F20F0, on a
# value, a particular process, a
# particular operation, a particular size / a
# particular allocation, a particular state / a
# particular result), and a process (sub_822F5208, on a
# value, a particular process, a
# particular operation, a particular size / a
# particular allocation, a particular state / a
# particular result))" routine (a count process, part of the 0x822CA cluster, a sibling of
# CountNonZero_822CA680, a caller of CountNonZero_822CA680, sub_822F5150, and sub_822F6480). 151
# lines, 5 callees (CountNonZero_822CA680, sub_822F20F0, sub_822F5150, sub_822F5208, sub_822F6480). 3
# callers: LazyGetData_822F2088, sub_82C66CC8, sub_82B4B2B8 (processes, which
# call this to perform the count, a
# particular count / a
# particular number, a
# particular size / a
# particular allocation, a
# particular operation). The object's class (obj, r31, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular structure, a count object, a
# particular value /
# a particular state) isn't resolvable, so the name states the mechanism: a count process (with a
# check and a
# call) (perform a
# count (a
# count, a particular count / a
# particular number, a particular size / a
# particular allocation, a
# particular operation, a particular state / a
# particular result, calling CountNonZero_822CA680, on a value (obj, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular source), and a value (2048, a constant, a
# particular value, a particular size / a
# particular allocation, a
# particular count), a particular count, a
# particular size / a
# particular allocation, a particular operation, a particular state / a
# particular result), and (if a count (r29, a count, a
# particular count, a particular size / a
# particular allocation, a
# particular number) is < 40, a particular threshold, a
# particular count) take one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation), and (otherwise) call a process (sub_822F5150, on a value (stack[96], a
# value, a
# particular value, a particular size / a
# particular allocation, a
# particular target), and a value (obj, a
# value, a particular value, a
# particular size / a
# particular allocation, a
# particular source), a particular process, a
# particular operation, a particular size / a
# particular allocation, a particular state / a
# particular result), and a process (sub_822F6480, on a
# value (stack[96], a
# value, a
# particular value, a particular size / a
# particular allocation, a
# particular target), and a value (stack[80], a
# value, a
# particular value, a particular size / a
# particular allocation, a
# particular source), a particular process, a
# particular operation, a particular size / a
# particular allocation, a particular state / a
# particular result), and (in paths) call a process (sub_822F20F0, on a
# value, a particular process, a
# particular operation, a particular size / a
# particular allocation, a particular state / a
# particular result), and a process (sub_822F5208, on a
# value, a particular process, a
# particular operation, a particular size / a
# particular allocation, a particular state / a
# particular result)). The "CountDispatch" refers to the count (a
# count, a particular count / a
# particular number, a particular size / a
# particular allocation, a
# particular operation, a particular state / a
# particular result) and the dispatch (a
# dispatch, a
# particular dispatch / a
# particular value, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, calling sub_822F5150, a
# process, a
# particular process, a
# particular operation, a particular size / a
# particular allocation, a particular state / a
# particular result, and sub_822F6480, a
# process, a
# particular process, a
# particular operation, a particular size / a
# particular allocation, a particular state / a
# particular result).
[entrypoint.functions."0x822CA580"]
name = "ProcessCountDispatch_822CA580"

# An atomic increment (with a check and a compare, the sibling of AtomicIncrement_822CBEB8 / AtomicIncrement2_8237
# 5F28 / AtomicIncrement3_821F7798 / AtomicIncrement4_82B82440, the caller of Allocate_OrDbgBreak_8221F388 /
# ReassignBuffer_822088B0 / Release_RefCounted_821C67D8): perform an atomic increment (an atomic increment, a
# particular increment / a particular count, a particular size / a particular allocation, a particular operation, a
# particular state / a particular result), and (if a value (obj->0, a value, a particular value, a particular size /
# a particular allocation) is zero, a null state, a particular state) take one path (a no-op / a return, a
# particular state, a particular result, a particular size / a particular allocation), and (otherwise) compute a
# base (obj->0 + 12, a base, a particular value, a particular size / a particular allocation, a reference count,
# a particular element), and (in a loop) increment a reference count (a reference count, a particular count, a
# particular size / a particular allocation, a particular value), and (if a reference count (r11, a reference
# count, a particular count, a particular size / a particular allocation, a particular value) is == 2, a
# particular value, a particular state) call a process (Allocate_OrDbgBreak_8221F388, on a value, a
# particular allocation, a particular size / a particular allocation, a particular operation, a
# particular state / a particular result), and (in paths) reassign a buffer (ReassignBuffer_822088B0, on a
# value, a particular reassign / a particular buffer, a particular size / a particular allocation, a
# particular operation, a particular state / a particular result), and release a value (Release_RefCounted
# _821C67D8, on a value, a particular release, a particular count, a particular size / a
# particular allocation, a particular operation, a particular state / a particular result).

# Args: obj (r3, r29, ->0 = a value, a particular value, a particular size / a particular allocation, a
# reference counted object), (more). It checks a value (r11, obj->0, a value, a particular value, a
# particular size / a
# particular allocation): if it is zero (a null state, a
# particular state, a
# particular size / a
# particular allocation), it takes one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation). If it is non-zero (a particular value, a
# particular state, a
# particular size / a
# particular allocation), it computes a base (r11, obj->0 + 12, a
# base, a particular value, a particular size / a
# particular allocation, a reference count,
# a particular element). In the loop, it performs an atomic increment (an atomic increment, a
# particular increment / a
# particular count, a
# particular size / a
# particular allocation, a
# particular operation, a particular state / a
# particular result: it reads a reference count (r10, r11->0, a reference count, a
# particular count, a
# particular size / a
# particular allocation, a
# particular value), and stores it (r10 + 1, an atomic increment, a
# particular count, a
# particular size / a
# particular allocation, a
# particular value)). It checks a reference count (r11, a reference count, a
# particular count, a
# particular size / a
# particular allocation, a
# particular value): if it is == 2 (a particular value, a
# particular state, a
# particular size / a
# particular allocation), it calls a process (Allocate_OrDbgBreak_8221F388, sub_8221F388, an
# allocation, on a value (a, a value, a
# particular value, a
# particular size, a
# particular size / a
# particular allocation), a particular allocation, a
# particular size / a
# particular allocation, a
# particular operation, a particular state / a
# particular result). In paths it reassigns a buffer (ReassignBuffer_822088B0, sub_822088B0, a
# reassign, on a
# value (a, a value, a
# particular value, a
# particular size, a
# particular size / a
# particular allocation), a particular reassign / a
# particular buffer, a
# particular size / a
# particular allocation, a
# particular operation, a particular state / a
# particular result), and releases a value (Release_RefCounted_821C67D8, sub_821C67D8, a
# release, on a
# value (a, a value, a
# particular value, a
# particular size, a
# particular size / a
# particular allocation), a particular release, a
# particular count, a
# particular size / a
# particular allocation, a
# particular operation, a particular state / a
# particular result). So it's an "atomic increment (with a check and a compare: perform an
# atomic increment (an atomic increment, a
# particular increment / a
# particular count, a
# particular size / a
# particular allocation, a
# particular operation, a particular state / a
# particular result), and (if a value (obj->0, a value, a
# particular value, a
# particular size / a
# particular allocation) is zero, a
# null state, a particular state) take one path (a no-op / a
# return, a particular state, a
# particular result, a particular size / a
# particular allocation), and (otherwise) compute a
# base (obj->0 + 12, a base, a particular value, a
# particular size / a
# particular allocation, a reference count,
# a particular element), and (in a loop) increment a reference count (a reference count, a
# particular count, a
# particular size / a
# particular allocation, a particular value), and (if a reference count (r11, a reference
# count, a particular count, a particular size / a
# particular allocation, a particular value) is == 2, a
# particular value, a particular state) call a process (Allocate_OrDbgBreak_8221F388, on a value, a
# particular allocation, a particular size / a particular allocation, a particular operation, a
# particular state / a particular result), and (in paths) reassign a buffer (ReassignBuffer_822088B0, on a
# value, a particular reassign / a particular buffer, a particular size / a particular allocation, a
# particular operation, a particular state / a particular result), and release a value (Release_RefCounted
# _821C67D8, on a value, a particular release, a particular count, a particular size / a
# particular allocation, a particular operation, a particular state / a particular result))" routine (an atomic
# increment, with a
# check and a
# compare, part
# of the 0x8222E cluster, a sibling of AtomicIncrement_822CBEB8, AtomicIncrement2_82375F28,
# AtomicIncrement3_821F7798, and AtomicIncrement4_82B82440, a caller of Allocate_OrDbgBreak_8221F388,
# ReassignBuffer_822088B0, and Release_RefCounted_821C67D8). 195 lines, 3 callees (Allocate_OrDbgBreak_8221F388,
# ReassignBuffer_822088B0, Release_RefCounted_821C67D8). 10+ callers: sub_82B3B468, sub_821DA550, sub_82B3A230, sub
# _82B3A1B8, sub_821EE858, sub_82B40708, sub_823F8A38, sub_82197488, sub_821EEA00, sub_821EC668 (processes, which
# call this to perform the atomic increment, a
# particular count / a
# particular size, a
# particular size / a
# particular allocation, a
# particular operation). The object's class (obj, r29, ->0 = a value, a
# particular value, a
# particular size / a
# particular allocation, a
# reference counted object, a
# particular value /
# a particular state) isn't resolvable, so the name states the mechanism: an atomic increment (with a
# check and a
# compare) (perform an
# atomic increment (an atomic increment, a
# particular increment / a
# particular count, a
# particular size / a
# particular allocation, a
# particular operation, a particular state / a
# particular result), and (if a value (obj->0, a value, a
# particular value, a
# particular size / a
# particular allocation) is zero, a
# null state, a particular state) take one path (a no-op / a
# return, a particular state, a
# particular result, a particular size / a
# particular allocation), and (otherwise) compute a
# base (obj->0 + 12, a base, a particular value, a
# particular size / a
# particular allocation, a reference count,
# a particular element), and (in a loop) increment a reference count (a reference count, a
# particular count, a
# particular size / a
# particular allocation, a particular value), and (if a reference count (r11, a reference
# count, a particular count, a particular size / a
# particular allocation, a particular value) is == 2, a
# particular value, a particular state) call a process (Allocate_OrDbgBreak_8221F388, on a value, a
# particular allocation, a particular size / a particular allocation, a particular operation, a
# particular state / a particular result), and (in paths) reassign a buffer (ReassignBuffer_822088B0, on a
# value, a particular reassign / a particular buffer, a particular size / a particular allocation, a
# particular operation, a particular state / a particular result), and release a value (Release_RefCounted
# _821C67D8, on a value, a particular release, a particular count, a particular size / a
# particular allocation, a particular operation, a particular state / a particular result)). The "5" distinguishes
# it from AtomicIncrement_822CBEB8, AtomicIncrement2_82375F28, AtomicIncrement3_821F7798, and
# AtomicIncrement4_82B82440.
[entrypoint.functions."0x8222E840"]
name = "AtomicIncrement5_8222E840"

# A process (a float range, with a loop and a compute, the caller of ProcessFloat_83234418, the sibling of Process
# DispatchValue2_832351B0 / ProcessFloatDataDispatch_8323EB40): perform a float range (a float range, a
# particular range / a particular float, a particular size / a particular allocation, a particular operation, a
# particular state / a particular result), and (in a loop) compute a float (a float, a particular float / a
# particular value, a particular size / a particular allocation, a particular operation, a particular state / a
# particular result, calling ProcessFloat_83234418, on a value, a particular float / a particular compute, a
# particular size / a particular allocation, a particular operation, a particular state / a particular result), and
# (if a count (a->8 - a->4, a count, an 8-byte element, a particular count, a particular size / a
# particular allocation) is > 0, a particular value) continue the loop (a loop, a particular loop / a
# particular iteration, a particular size / a particular allocation, a particular operation, a
# particular state / a
# particular result), and (otherwise) take one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation).

# Args: obj (r3, r30, a value, a particular value, a particular size / a
# particular allocation, a
# particular structure, a range object), a (r4, r29, ->4 = a base, ->8 = a limit, a
# particular value, a
# particular size / a
# particular allocation), b (r5, r27, a value, a
# particular value, a
# particular size / a
# particular allocation), (more). It performs a float range (a
# float range, a
# particular range / a
# particular float, a particular size / a
# particular allocation, a
# particular operation, a particular state / a
# particular result). In the loop, it computes a float (a
# float, a
# particular float / a
# particular value, a particular size / a
# particular allocation, a
# particular operation, a particular state / a
# particular result, calling ProcessFloat_83234418, sub_83234418, a float, on a
# value (a, a value, a
# particular value, a
# particular size, a
# particular size / a
# particular allocation), a particular float / a
# particular compute, a
# particular size / a
# particular allocation, a particular operation, a particular state / a
# particular result). It checks a count (r28, a->8 - a->4, a
# count, an 8-byte element, a particular count, a
# particular size / a
# particular allocation): if it is > 0 (a particular value, a
# particular state, a
# particular size / a
# particular allocation), it continues the loop (a
# loop, a particular loop / a
# particular iteration, a particular size / a
# particular allocation, a
# particular operation, a particular state / a
# particular result); if it is <= 0 (a null state, a
# particular state, a
# particular size / a
# particular allocation), it takes one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation). So it's a "process (a float range, with a loop and a compute: perform a
# float range (a
# float range, a
# particular range / a
# particular float, a particular size / a
# particular allocation, a
# particular operation, a particular state / a
# particular result), and (in a loop) compute a float (a
# float, a
# particular float / a
# particular value, a particular size / a
# particular allocation, a
# particular operation, a particular state / a
# particular result, calling ProcessFloat_83234418, on a value, a
# particular float / a
# particular compute, a
# particular size / a
# particular allocation, a particular operation, a particular state / a
# particular result), and (if a count (a->8 - a->4, a
# count, an 8-byte element, a particular count, a particular size / a
# particular allocation) is > 0, a particular value) continue the loop (a loop, a particular loop / a
# particular iteration, a particular size / a particular allocation, a particular operation, a
# particular state / a
# particular result), and (otherwise) take one path (a no-op / a
# return, a particular state, a
# particular result, a particular size / a
# particular allocation))" routine (a float range process, part of the 0x82A13 cluster, a sibling of
# ProcessDispatchValue2_832351B0, a caller of ProcessFloat_83234418). 377 lines, 1
# callee (ProcessFloat_83234418). 1 caller: ProcessDispatchValue2_832351B0 (a
# dispatch process, which
# calls this to perform the float range, a
# particular size / a
# particular allocation, a
# particular operation). The object's class (obj, r30, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular structure, a range object, a
# particular value /
# a particular state) and the value's meaning (a, r29, ->4 = a base, ->8 = a limit, a
# particular value, a
# particular size / a
# particular allocation, b, r27, a value, a
# particular value, a
# particular size / a
# particular allocation) aren't resolvable, so the name states the mechanism: a float range process (with a
# loop and a
# compute) (perform a
# float range (a
# float range, a
# particular range / a
# particular float, a particular size / a
# particular allocation, a
# particular operation, a particular state / a
# particular result), and (in a loop) compute a float (a
# float, a
# particular float / a
# particular value, a particular size / a
# particular allocation, a
# particular operation, a particular state / a
# particular result, calling ProcessFloat_83234418, on a value, a
# particular float / a
# particular compute, a
# particular size / a
# particular allocation, a particular operation, a particular state / a
# particular result), and (if a count (a->8 - a->4, a
# count, an 8-byte element, a particular count, a particular size / a
# particular allocation) is > 0, a particular value) continue the loop (a loop, a particular loop / a
# particular iteration, a particular size / a particular allocation, a particular operation, a
# particular state / a
# particular result), and (otherwise) take one path (a no-op / a
# return, a particular state, a
# particular result, a particular size / a
# particular allocation)). The "FloatRange" refers to the float range (a
# float range, a
# particular range / a
# particular float, a particular size / a
# particular allocation, a
# particular operation, a particular state / a
# particular result) and the loop (a
# loop, a particular loop / a
# particular iteration, a particular size / a
# particular allocation, a
# particular operation, a particular state / a
# particular result).
[entrypoint.functions."0x82A13220"]
name = "ProcessFloatRange_82A13220"

# A 32-byte constructor (the sibling of Construct32Byte_82B722E8 / Construct32Byte2_83221998 / Construct32Byte3_82
# BE49F0, with a constant and a complex computation, the caller of Construct32Byte3_82BE49F0 / CopyRecords_82A13CA
# 0 / ProcessCopyRecords_82A13D30 / ProcessGrowAppend_82BE4AA8): initialize the object's fields (a 32-byte object,
# a particular structure, a particular size / a particular allocation, obj->0 = a constant, from a fixed .data
# address, a class id / a marker, a particular class / a particular marker, obj->8 = 0, a field, a count / a size,
# a zero, obj->12 = 0, a field, a count / a size, a zero, obj->16 = 0, a field, a count / a size, a zero, obj->32 =
# 0, a field, a count / a size, a zero, obj->36 = 1, a field, a value, a one, a 32-byte object, a
# particular structure, a
# particular size / a
# particular allocation, a particular initialization), and (in paths) construct (Construct32Byte3_82BE49F0, on a
# value, a particular construct / a particular size, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and copy records (CopyRecords_82A13CA0, on a value, a
# particular copy / a particular record, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and copy records (ProcessCopyRecords_82A13D30, on a
# value, a particular copy / a particular record, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and grow/append (ProcessGrowAppend_82BE4AA8, on a
# value, a particular grow / a particular append, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result).

# Args: obj (r3, r31, ->0 = a field, a constant, a class id / a marker, ->8 = a field, a
# count / a size, ->12 = a field, a count / a size, ->16 = a field, a count / a size, ->32 = a field, a
# count / a size, ->36 = a field, a value, a 32-byte object), a (r4, r25, a value, a
# particular value, a
# particular size / a
# particular allocation), (more). It initializes the object's fields (a 32-byte object, a
# particular structure, a
# particular size / a
# particular allocation: obj->0 = a constant, from a fixed .data address, a class id / a
# marker, a particular class / a
# particular marker, obj->8 = 0, a field, a count / a size, a zero, obj->12 = 0, a
# field, a count / a size, a zero, obj->16 = 0, a
# field, a count / a size, a zero, obj->32 = 0, a
# field, a count / a size, a zero, obj->36 = 1, a
# field, a value, a one, a 32-byte object, a
# particular structure, a
# particular size / a
# particular allocation, a
# particular initialization). In paths it constructs (Construct32Byte3_82BE49F0, sub_82BE49F0, a
# 32-byte constructor, on a value (a, a value, a
# particular value, a
# particular size, a
# particular size / a
# particular allocation), a particular construct / a
# particular size, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), copies records (CopyRecords_82A13CA0, sub_82A13CA0, a copy, on a
# value (a, a value, a
# particular value, a
# particular size, a
# particular size / a
# particular allocation), a particular copy / a
# particular record, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), copies records (ProcessCopyRecords_82A13D30, sub_82A13D30, a
# copy, on a
# value (a, a value, a
# particular value, a
# particular size, a
# particular size / a
# particular allocation), a particular copy / a
# particular record, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and grows/appends (ProcessGrowAppend_82BE4AA8, sub_82BE4AA8, a
# grow/append, on a
# value (a, a value, a
# particular value, a
# particular size, a
# particular size / a
# particular allocation), a particular grow / a
# particular append, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). So it's a "construct a 32-byte object (initialize the object's fields (a
# 32-byte object,
# a
# particular structure, a
# particular size / a
# particular allocation, obj->0 = a constant, from a fixed .data address, a class id / a
# marker, a particular class / a
# particular marker, obj->8 = 0, a field, a count / a size, a zero, obj->12 = 0, a
# field, a count / a size, a zero, obj->16 = 0, a
# field, a count / a size, a zero, obj->32 = 0, a
# field, a count / a size, a zero, obj->36 = 1, a
# field, a value, a one, a 32-byte object, a
# particular structure, a
# particular size / a
# particular allocation), and (in paths) construct (Construct32Byte3_82BE49F0, on a
# value, a particular construct / a
# particular size, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and copy records (CopyRecords_82A13CA0, on a value, a
# particular copy / a particular record, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and copy records (ProcessCopyRecords_82A13D30, on a
# value, a particular copy / a particular record, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and grow/append (ProcessGrowAppend_82BE4AA8, on a
# value, a particular grow / a particular append, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result))" routine (a 32-byte constructor, with a
# constant and a
# complex computation, part
# of the 0x8323F cluster, a sibling of Construct32Byte_82B722E8, Construct32Byte2_83221998, and
# Construct32Byte3_82BE49F0, a caller of Construct32Byte3_82BE49F0, CopyRecords_82A13CA0,
# ProcessCopyRecords_82A13D30, and ProcessGrowAppend_82BE4AA8). 526 lines, 4 callees (Construct32Byte3_82BE49F0,
# CopyRecords_82A13CA0, ProcessCopyRecords_82A13D30, ProcessGrowAppend_82BE4AA8). 1 caller: ProcessGrowAppend8
# _83221378 (a grow/append process, which
# calls this to construct a 32-byte object, a
# particular size / a
# particular allocation, a
# particular operation). The object's class (obj, ->0 = a field, a constant, a class id / a
# marker, ->8 = a field, a
# count / a size, ->12 = a field, a count / a size, ->16 = a field, a count / a size, ->32 = a field, a
# count / a size, ->36 = a field, a value, a 32-byte object, a
# particular structure, a
# particular size / a
# particular allocation, a
# particular value /
# a particular state) and the value's meaning (a, r25, a value, a
# particular value, a
# particular size / a
# particular allocation) aren't resolvable, so the name states the mechanism: a 32-byte constructor (with a
# constant and a
# complex computation) (initialize the object's fields (a
# 32-byte object,
# a
# particular structure, a
# particular size / a
# particular allocation, obj->0 = a constant, from a fixed .data address, a class id / a
# marker, a particular class / a
# particular marker, obj->8 = 0, a field, a count / a size, a zero, obj->12 = 0, a
# field, a count / a size, a zero, obj->16 = 0, a
# field, a count / a size, a zero, obj->32 = 0, a
# field, a count / a size, a zero, obj->36 = 1, a
# field, a value, a one, a 32-byte object, a
# particular structure, a
# particular size / a
# particular allocation), and (in paths) construct (Construct32Byte3_82BE49F0, on a
# value, a particular construct / a
# particular size, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and copy records (CopyRecords_82A13CA0, on a value, a
# particular copy / a particular record, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and copy records (ProcessCopyRecords_82A13D30, on a
# value, a particular copy / a particular record, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and grow/append (ProcessGrowAppend_82BE4AA8, on a
# value, a particular grow / a particular append, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result)). The "4" distinguishes it from Construct32Byte_82B722E8, Construct32Byte2_83221998, and
# Construct32Byte3_82BE49F0.
[entrypoint.functions."0x8323FF48"]
name = "Construct32Byte4_8323FF48"

# A trampoline (a trampoline, with a call, the caller of sub_822D7068, the sibling of ProcessElementAccess3_8217AA
# 20 / ProcessVcallLoop_8217A9A8): forward a call (a trampoline, a particular forward / a
# particular call, a particular size / a particular allocation, a particular operation, a
# particular state / a
# particular result, calling sub_822D7068, on a value (a, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular target), and a value (b, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular source), a particular process, a
# particular operation, a particular size / a
# particular allocation, a particular state / a
# particular result), and return the result (a
# result, a
# particular result, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result).

# Args: a (r3, a value, a particular value, a particular size / a
# particular allocation, a
# particular target), b (r4, a value, a particular value, a
# particular size / a
# particular allocation, a
# particular source), (more). It forwards a call (a
# trampoline, a particular forward / a
# particular call, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, calling sub_822D7068, sub_822D7068, a process, on a
# value (a, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular target), and a value (b, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular source), a particular process, a
# particular operation, a particular size / a
# particular allocation, a particular state / a
# particular result). It returns the result (a
# result, a
# particular result, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result). So it's a "trampoline (a
# trampoline, with a
# call: forward a call (a
# trampoline, a particular forward / a
# particular call, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, calling sub_822D7068, on a value (a, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular target), and a value (b, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular source), a particular process, a
# particular operation, a particular size / a
# particular allocation, a particular state / a
# particular result), and return the result (a
# result, a
# particular result, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result))" routine (a trampoline, part of the 0x8217A cluster, a sibling of
# ProcessElementAccess3_8217AA20 and ProcessVcallLoop_8217A9A8, a caller of sub_822D7068). 9
# lines, 1
# callee (sub_822D7068). 5 callers: sub_822F21B8, sub_8217AB58, sub_82BFD228, sub_82B3EB30, sub_82C68B50
# (processes, which
# call this to forward the call, a
# particular size / a
# particular allocation, a
# particular operation). The values' meanings (a, r3, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular target, b, r4, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular source) aren't resolvable, so the name states the mechanism: a trampoline (with a
# call) (forward a call (a
# trampoline, a particular forward / a
# particular call, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, calling sub_822D7068, on a value (a, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular target), and a value (b, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular source), a particular process, a
# particular operation, a particular size / a
# particular allocation, a particular state / a
# particular result), and return the result (a
# result, a
# particular result, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result)). The "Trampoline" refers to the trampoline (a
# trampoline, a particular forward / a
# particular call, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result).
[entrypoint.functions."0x8217A978"]
name = "Trampoline_8217A978"

# A process (a set, with a check and a call, the caller of sub_82CAB770 / sub_82CAB630, the sibling of Trampoline
# _8217A978): perform a set (a set, a particular set / a particular value, a particular size / a particular
# allocation, a particular operation, a particular state / a particular result), and (if a value (obj, a value, a
# particular value, a particular size / a particular allocation) is zero, a null state, a particular state) take
# one path (a no-op / a return, a particular state, a particular result, a particular size / a
# particular allocation), and (if a value (b, a value, a particular value, a particular size / a
# particular allocation) is zero, a null state, a particular state) take one path (a no-op / a
# return, a particular state, a
# particular result, a particular size / a
# particular allocation), and (if a value (a, a value, a particular value, a
# particular size / a
# particular allocation) is zero, a null state, a particular state) call a process (sub_82CAB770, on a
# value, a particular process, a
# particular operation, a particular size / a
# particular allocation, a particular state / a
# particular result), and set a flag (r3->0 = 22, a flag, a particular value, a
# particular state, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and call a process (sub_82CAB630, on a
# value, a particular process, a
# particular operation, a particular size / a
# particular allocation, a particular state / a
# particular result), and (otherwise) take one path (a no-op / a
# return, a particular state, a
# particular result, a particular size / a
# particular allocation).

# Args: obj (r3, r11, ->0 = a value, a particular value, a particular size / a
# particular allocation, a
# particular structure, a set object), a (r4, a value, a
# particular value, a
# particular size / a
# particular allocation), b (r5, a value, a
# particular value, a
# particular size / a
# particular allocation), (more). It checks a value (r11, obj, a
# value, a
# particular value, a
# particular size / a
# particular allocation): if it is zero (a null state, a
# particular state, a
# particular size / a
# particular allocation), it takes one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation). If it is non-zero (a particular value, a
# particular state, a
# particular size / a
# particular allocation), it checks a value (r5, b, a
# value, a
# particular value, a
# particular size / a
# particular allocation): if it is zero (a null state, a
# particular state, a
# particular size / a
# particular allocation), it takes one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation); if it is non-zero (a particular value, a
# particular state, a
# particular size / a
# particular allocation), it sets a value (obj->0 = 0, a store, a zero, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result). It checks a value (r4, a, a
# value, a
# particular value, a
# particular size / a
# particular allocation): if it is zero (a null state, a
# particular state, a
# particular size / a
# particular allocation), it calls a process (sub_82CAB770, sub_82CAB770, a
# process, on a
# value (a, a value, a
# particular value, a
# particular size, a
# particular size / a
# particular allocation), a particular process, a
# particular operation, a particular size / a
# particular allocation, a particular state / a
# particular result), and sets a flag (r3->0 = 22, a flag, a
# particular value, a
# particular state, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and calls a process (sub_82CAB630, sub_82CAB630, a
# process, on a
# value (a, a value, a
# particular value, a
# particular size, a
# particular size / a
# particular allocation), a particular process, a
# particular operation, a particular size / a
# particular allocation, a particular state / a
# particular result). If it is non-zero (a particular value, a
# particular state, a
# particular size / a
# particular allocation), it takes one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation). So it's a "process (a set, with a check and a call: perform a
# set (a set, a particular set / a
# particular value, a particular size / a
# particular allocation, a
# particular operation, a particular state / a
# particular result), and (if a value (obj, a value, a
# particular value, a
# particular size / a
# particular allocation) is zero, a null state, a particular state) take
# one path (a no-op / a return, a particular state, a
# particular result, a particular size / a
# particular allocation), and (if a value (b, a value, a
# particular value, a particular size / a
# particular allocation) is zero, a null state, a particular state) take one path (a no-op / a
# return, a particular state, a
# particular result, a particular size / a
# particular allocation), and (if a value (a, a value, a
# particular value, a
# particular size / a
# particular allocation) is zero, a null state, a particular state) call a process (sub_82CAB770, on a
# value, a particular process, a
# particular operation, a particular size / a
# particular allocation, a particular state / a
# particular result), and set a flag (r3->0 = 22, a flag, a particular value, a
# particular state, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and call a process (sub_82CAB630, on a
# value, a particular process, a
# particular operation, a particular size / a
# particular allocation, a particular state / a
# particular result), and (otherwise) take one path (a no-op / a
# return, a particular state, a
# particular result, a particular size / a
# particular allocation))" routine (a set process, part of the 0x822D6 cluster, a sibling of
# Trampoline_8217A978, a caller of sub_82CAB770 and sub_82CAB630). 122 lines, 2 callees (sub_82CAB630,
# sub_82CAB770). 2 callers: sub_822D7068, sub_82CA3970 (processes, which
# call this to perform the set, a
# particular size / a
# particular allocation, a
# particular operation). The object's class (obj, r11, ->0 = a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular structure, a set object, a
# particular value /
# a particular state) and the value's meaning (a, r4, a value, a
# particular value, a
# particular size / a
# particular allocation, b, r5, a value, a
# particular value, a
# particular size / a
# particular allocation) aren't resolvable, so the name states the mechanism: a set process (with a
# check and a
# call) (perform a
# set (a set, a particular set / a
# particular value, a particular size / a
# particular allocation, a
# particular operation, a particular state / a
# particular result), and (if a value (obj, a value, a
# particular value, a
# particular size / a
# particular allocation) is zero, a null state, a particular state) take
# one path (a no-op / a return, a particular state, a
# particular result, a particular size / a
# particular allocation), and (if a value (b, a value, a
# particular value, a particular size / a
# particular allocation) is zero, a null state, a particular state) take one path (a no-op / a
# return, a particular state, a
# particular result, a particular size / a
# particular allocation), and (if a value (a, a value, a
# particular value, a
# particular size / a
# particular allocation) is zero, a null state, a particular state) call a process (sub_82CAB770, on a
# value, a particular process, a
# particular operation, a particular size / a
# particular allocation, a particular state / a
# particular result), and set a flag (r3->0 = 22, a flag, a particular value, a
# particular state, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and call a process (sub_82CAB630, on a
# value, a particular process, a
# particular operation, a particular size / a
# particular allocation, a particular state / a
# particular result), and (otherwise) take one path (a no-op / a
# return, a particular state, a
# particular result, a particular size / a
# particular allocation)). The "SetError" refers to the set (a
# set, a particular set / a
# particular value, a particular size / a
# particular allocation, a
# particular operation, a particular state / a
# particular result) and the error (a
# set, a particular set / a
# particular value, a particular size / a
# particular allocation, a
# particular operation, a particular state / a
# particular result, setting a flag (r3->0 = 22, a flag, a
# particular value, a
# particular state, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result)).
[entrypoint.functions."0x822D6F90"]
name = "ProcessSetError_822D6F90"

# A process (a set, with a check and a call, the caller of ProcessSetError_822D6F90 / sub_82CAB770 / sub_82CAB630, the
# sibling of ProcessSetError_822D6F90 / Trampoline_8217A978): perform a set (a set, a particular set / a
# particular value, a particular size / a particular allocation, a particular operation, a particular state / a
# particular result), and (if a value (a, a value, a particular value, a particular size / a
# particular allocation) is zero, a null state, a particular state) check a value (b, a value, a
# particular value, a particular size / a particular allocation): if it is zero (a null state, a
# particular state, a
# particular size / a
# particular allocation), take one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation); if it is non-zero (a particular value, a
# particular state, a
# particular size / a
# particular allocation), take a path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation), and (otherwise) check a value (b, a value, a
# particular value, a
# particular size / a
# particular allocation): if it is zero (a null state, a
# particular state, a
# particular size / a
# particular allocation), take a path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation); if it is non-zero (a particular value, a
# particular state, a
# particular size / a
# particular allocation), take one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation), and (in paths) call a process (ProcessSetError_822D6F90, on a
# value, a particular set / a
# particular error, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and a process (sub_82CAB770, on a
# value, a particular process, a
# particular operation, a particular size / a
# particular allocation, a particular state / a
# particular result), and a process (sub_82CAB630, on a
# value, a particular process, a
# particular operation, a particular size / a
# particular allocation, a particular state / a
# particular result).

# Args: obj (r3, r27, a value, a particular value, a particular size / a
# particular allocation, a
# particular structure, a set object), a (r4, r31, a value, a
# particular value, a
# particular size / a
# particular allocation), b (r5, r30, a value, a
# particular value, a
# particular size / a
# particular allocation), c (r6, r29, a value, a
# particular value, a
# particular size / a
# particular allocation), (more). It performs a set (a
# set, a particular set / a
# particular value, a particular size / a
# particular allocation, a
# particular operation, a particular state / a
# particular result). It checks a value (r31, a, a
# value, a
# particular value, a
# particular size / a
# particular allocation): if it is zero (a null state, a
# particular state, a
# particular size / a
# particular allocation), it checks a value (r30, b, a
# value, a
# particular value, a
# particular size / a
# particular allocation): if it is zero (a null state, a
# particular state, a
# particular size / a
# particular allocation), it takes one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation); if it is non-zero (a particular value, a
# particular state, a
# particular size / a
# particular allocation), it takes a path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation). If it is non-zero (a particular value, a
# particular state, a
# particular size / a
# particular allocation), it checks a value (r30, b, a
# value, a
# particular value, a
# particular size / a
# particular allocation): if it is zero (a null state, a
# particular state, a
# particular size / a
# particular allocation), it takes a path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation); if it is non-zero (a particular value, a
# particular state, a
# particular size / a
# particular allocation), it takes one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation). In paths it calls a process (ProcessSetError_822D6F90, sub_822D6F90, a
# set process, on a
# value (a, a value, a
# particular value, a
# particular size, a
# particular size / a
# particular allocation), a particular set / a
# particular error, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and a process (sub_82CAB770, sub_82CAB770, a
# process, on a
# value (a, a value, a
# particular value, a
# particular size, a
# particular size / a
# particular allocation), a particular process, a
# particular operation, a particular size / a
# particular allocation, a particular state / a
# particular result), and a process (sub_82CAB630, sub_82CAB630, a
# process, on a
# value (a, a value, a
# particular value, a
# particular size, a
# particular size / a
# particular allocation), a particular process, a
# particular operation, a particular size / a
# particular allocation, a particular state / a
# particular result). So it's a "process (a set, with a check and a call: perform a
# set (a set, a particular set / a
# particular value, a particular size / a
# particular allocation, a
# particular operation, a particular state / a
# particular result), and (if a value (a, a value, a
# particular value, a
# particular size / a
# particular allocation) is zero, a null state, a particular state) check a value (b, a value, a
# particular value, a particular size / a
# particular allocation): if it is zero (a null state, a
# particular state, a
# particular size / a
# particular allocation), take one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation); if it is non-zero (a particular value, a
# particular state, a
# particular size / a
# particular allocation), take a path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation), and (otherwise) check a value (b, a value, a
# particular value, a
# particular size / a
# particular allocation): if it is zero (a null state, a
# particular state, a
# particular size / a
# particular allocation), take a path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation); if it is non-zero (a particular value, a
# particular state, a
# particular size / a
# particular allocation), take one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation), and (in paths) call a process (ProcessSetError_822D6F90, on a
# value, a particular set / a
# particular error, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and a process (sub_82CAB770, on a
# value, a particular process, a
# particular operation, a particular size / a
# particular allocation, a particular state / a
# particular result), and a process (sub_82CAB630, on a
# value, a particular process, a
# particular operation, a particular size / a
# particular allocation, a particular state / a
# particular result))" routine (a set process, part of the 0x822D7 cluster, a sibling of
# ProcessSetError_822D6F90, a caller of ProcessSetError_822D6F90, sub_82CAB770, and sub_82CAB630). 184
# lines, 3 callees (sub_822D6F90, sub_82CAB630, sub_82CAB770). 1 caller: Trampoline_8217A978 (a
# trampoline, which
# calls this to perform the set, a
# particular size / a
# particular allocation, a
# particular operation). The object's class (obj, r27, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular structure, a set object, a
# particular value /
# a particular state) and the value's meaning (a, r31, a value, a
# particular value, a
# particular size / a
# particular allocation, b, r30, a value, a
# particular value, a
# particular size / a
# particular allocation, c, r29, a value, a
# particular value, a
# particular size / a
# particular allocation) aren't resolvable, so the name states the mechanism: a set process (with a
# check and a
# call) (perform a
# set (a set, a particular set / a
# particular value, a particular size / a
# particular allocation, a
# particular operation, a particular state / a
# particular result), and (if a value (a, a value, a
# particular value, a
# particular size / a
# particular allocation) is zero, a null state, a particular state) check a value (b, a value, a
# particular value, a particular size / a
# particular allocation): if it is zero (a null state, a
# particular state, a
# particular size / a
# particular allocation), take one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation); if it is non-zero (a particular value, a
# particular state, a
# particular size / a
# particular allocation), take a path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation), and (otherwise) check a value (b, a value, a
# particular value, a
# particular size / a
# particular allocation): if it is zero (a null state, a
# particular state, a
# particular size / a
# particular allocation), take a path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation); if it is non-zero (a particular value, a
# particular state, a
# particular size / a
# particular allocation), take one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation), and (in paths) call a process (ProcessSetError_822D6F90, on a
# value, a particular set / a
# particular error, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and a process (sub_82CAB770, on a
# value, a particular process, a
# particular operation, a particular size / a
# particular allocation, a particular state / a
# particular result), and a process (sub_82CAB630, on a
# value, a particular process, a
# particular operation, a particular size / a
# particular allocation, a particular state / a
# particular result)). The "2" distinguishes it from ProcessSetError_822D6F90.
[entrypoint.functions."0x822D7068"]
name = "ProcessSetError2_822D7068"

# A process (a sort compare, with a swap and a loop, the sibling of ProcessSetError2_822D7068 / ProcessSort_82A0A890):
# perform a sort compare (a sort compare, a particular compare / a particular ordering, a particular size / a
# particular allocation, a particular operation, a particular state / a particular result), and (in a loop) compare
# (a comparison, a particular comparison / a particular ordering, a particular size / a particular allocation, a
# particular operation, a particular state / a particular result, a value (a->0, a value, a particular value, a
# particular size / a particular allocation, a particular element) and a value (b->0, a value, a particular value, a
# particular size / a particular allocation, a particular element)), and (if a value (r9, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular key) is > a value (r8, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular key), a particular key) swap (a swap, a particular swap / a
# particular ordering, a particular size / a
# particular allocation, a
# particular operation, a particular state / a
# particular result, a value (b->0, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular element) and a value (a->0, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular element)), and (otherwise) take one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation), and (in paths) compare (a comparison, a
# particular comparison / a
# particular ordering, a particular size / a
# particular allocation, a
# particular operation, a particular state / a
# particular result, a value (b->0, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular element) and a value (c->0, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular element)).

# Args: a (r3, ->0 = a value, a particular value, a particular size / a
# particular allocation, a
# particular structure, a compare object), b (r4, ->0 = a value, a
# particular value, a
# particular size / a
# particular allocation), c (r5, ->0 = a value, a
# particular value, a
# particular size / a
# particular allocation), (more). It performs a sort compare (a
# sort compare, a
# particular compare / a
# particular ordering, a particular size / a
# particular allocation, a
# particular operation, a particular state / a
# particular result). In the loop, it compares (a
# comparison, a
# particular comparison / a
# particular ordering, a particular size / a
# particular allocation, a
# particular operation, a particular state / a
# particular result, a value (a->0, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular element) and a value (b->0, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular element): if a value (r9, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular key) is > a value (r8, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular key), it swaps (a
# swap, a
# particular swap / a
# particular ordering, a particular size / a
# particular allocation, a
# particular operation, a particular state / a
# particular result, a value (b->0, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular element) and a value (a->0, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular element)). If a value (r9, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular key) is == a value (r8, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular key), it checks a value (r9, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular key): if it is >= a value (r8, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular key), it takes one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation); if it is < the value (a particular state, a
# particular value, a
# particular size / a
# particular allocation), it swaps (a
# swap, a
# particular swap / a
# particular ordering, a particular size / a
# particular allocation, a
# particular operation, a particular state / a
# particular result, a value (b->0, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular element) and a value (a->0, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular element)). In paths it compares (a
# comparison, a
# particular comparison / a
# particular ordering, a particular size / a
# particular allocation, a
# particular operation, a particular state / a
# particular result, a value (b->0, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular element) and a value (c->0, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular element)). So it's a "process (a sort compare, with a swap and a loop: perform a
# sort compare (a
# sort compare, a
# particular compare / a
# particular ordering, a particular size / a
# particular allocation, a
# particular operation, a particular state / a
# particular result), and (in a loop) compare
# (a comparison, a particular comparison / a particular ordering, a particular size / a
# particular allocation, a
# particular operation, a particular state / a
# particular result, a value (a->0, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular element) and a value (b->0, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular element)), and (if a value (r9, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular key) is > a value (r8, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular key), a particular key) swap (a swap, a particular swap / a
# particular ordering, a particular size / a
# particular allocation, a
# particular operation, a particular state / a
# particular result, a value (b->0, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular element) and a value (a->0, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular element)), and (otherwise) take one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation), and (in paths) compare (a comparison, a
# particular comparison / a
# particular ordering, a particular size / a
# particular allocation, a
# particular operation, a particular state / a
# particular result, a value (b->0, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular element) and a value (c->0, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular element))" routine (a sort compare process, part of the 0x82C22 cluster, a sibling of
# ProcessSetError2_822D7068 and ProcessSort_82A0A890). 89
# lines, 0 callees. 1 caller: sub_82C222F0 (a process, which
# calls this to perform the sort compare, a
# particular size / a
# particular allocation, a
# particular operation). The object's class (obj, ->0 = a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular structure, a compare object, a
# particular value /
# a particular state) and the value's meaning (a, r3, ->0 = a value, a
# particular value, a
# particular size / a
# particular allocation, b, r4, ->0 = a value, a
# particular value, a
# particular size / a
# particular allocation, c, r5, ->0 = a value, a
# particular value, a
# particular size / a
# particular allocation) aren't resolvable, so the name states the mechanism: a sort compare process (with a
# swap and a
# loop) (perform a
# sort compare (a
# sort compare, a
# particular compare / a
# particular ordering, a particular size / a
# particular allocation, a
# particular operation, a particular state / a
# particular result), and (in a loop) compare
# (a comparison, a particular comparison / a particular ordering, a particular size / a
# particular allocation, a
# particular operation, a particular state / a
# particular result, a value (a->0, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular element) and a value (b->0, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular element)), and (if a value (r9, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular key) is > a value (r8, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular key), a particular key) swap (a swap, a particular swap / a
# particular ordering, a particular size / a
# particular allocation, a
# particular operation, a particular state / a
# particular result, a value (b->0, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular element) and a value (a->0, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular element)), and (otherwise) take one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation), and (in paths) compare (a comparison, a
# particular comparison / a
# particular ordering, a particular size / a
# particular allocation, a
# particular operation, a particular state / a
# particular result, a value (b->0, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular element) and a value (c->0, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular element)). The "SortCompare" refers to the sort compare (a
# sort compare, a
# particular compare / a
# particular ordering, a particular size / a
# particular allocation, a
# particular operation, a particular state / a
# particular result) and the swap (a
# swap, a
# particular swap / a
# particular ordering, a particular size / a
# particular allocation, a
# particular operation, a particular state / a
# particular result).
[entrypoint.functions."0x82C22148"]
name = "ProcessSortCompare_82C22148"

# A lookup (a lookup, with a compute and a mask, the sibling of ProcessSortCompare_82C22148 / ProcessLookup2_82A0
# FB80): perform a lookup (a lookup, a particular lookup / a
# particular value, a particular size / a
# particular allocation, a
# particular operation, a particular state / a
# particular result), and (in a compute) compute an offset (an offset, a
# particular offset / a
# particular index, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, (index << 1) & 0x1FE, a
# compute, a
# particular compute / a
# particular operation, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (in a mask) mask a value (a value, a
# particular value, a
# particular size / a
# particular allocation, a
# 2-byte element, a
# particular element, & 0xFFFF8000, a
# mask, a
# particular mask / a
# particular value, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and return the value (a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# 2-byte element, a
# particular element, a
# particular state / a
# particular result).

# Args: index (r3, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular index), (more). It performs a lookup (a
# lookup, a particular lookup / a
# particular value, a particular size / a
# particular allocation, a
# particular operation, a particular state / a
# particular result). In the compute, it computes an offset (r10, (r3 << 1) & 0x1FE, an
# offset, a
# particular offset / a
# particular index, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, a
# compute, a
# particular compute / a
# particular operation, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result). It masks a value (r11, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# 2-byte element, a
# particular element, & 0xFFFF8000, a
# mask, a
# particular mask / a
# particular value, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). It returns the value (a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# 2-byte element, a
# particular element, a
# particular state / a
# particular result). So it's a "lookup (a
# lookup, with a
# compute and a
# mask: perform a lookup (a
# lookup, a particular lookup / a
# particular value, a particular size / a
# particular allocation, a
# particular operation, a particular state / a
# particular result), and (in a compute) compute an offset (an offset, a
# particular offset / a
# particular index, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, (index << 1) & 0x1FE, a
# compute, a
# particular compute / a
# particular operation, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (in a mask) mask a value (a value, a
# particular value, a
# particular size / a
# particular allocation, a
# 2-byte element, a
# particular element, & 0xFFFF8000, a
# mask, a
# particular mask / a
# particular value, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and return the value (a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# 2-byte element, a
# particular element, a
# particular state / a
# particular result))" routine (a lookup routine, part of the 0x8223F cluster, a sibling of
# ProcessSortCompare_82C22148 and ProcessLookup2_82A0FB80). 18
# lines, 0 callees. 9 callers: sub_82CB7B90, sub_82CAE4A8, sub_82CB7688, sub_830043D0, BuildBuffer_8223F990, sub
# _82CACCC0, sub_82CB3260, sub_82CB2038, sub_82CB1438 (processes, which
# call this to perform the lookup, a
# particular size / a
# particular allocation, a
# particular operation). The value's meaning (index, r3, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular index) isn't resolvable, so the name states the mechanism: a lookup (with a
# compute and a
# mask) (perform a lookup (a
# lookup, a particular lookup / a
# particular value, a particular size / a
# particular allocation, a
# particular operation, a particular state / a
# particular result), and (in a compute) compute an offset (an offset, a
# particular offset / a
# particular index, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, (index << 1) & 0x1FE, a
# compute, a
# particular compute / a
# particular operation, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (in a mask) mask a value (a value, a
# particular value, a
# particular size / a
# particular allocation, a
# 2-byte element, a
# particular element, & 0xFFFF8000, a
# mask, a
# particular mask / a
# particular value, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and return the value (a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# 2-byte element, a
# particular element, a
# particular state / a
# particular result)). The "Masked" refers to the lookup (a
# lookup, a particular lookup / a
# particular value, a particular size / a
# particular allocation, a
# particular operation, a particular state / a
# particular result) and the mask (a
# mask, a
# particular mask / a
# particular value, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result).
[entrypoint.functions."0x8223F970"]
name = "LookupMasked_8223F970"

# A destruct (a destruct, with a check and a free, the caller of Destruct20Byte_832156E0 / Free_SizeBucketed_Thunk
# _8221BE68, the sibling of Destruct20ByteFree_82A11F90): destruct a 20-byte object (a destruct, a
# particular destruct / a
# particular release, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, calling Destruct20Byte_832156E0, on a
# value (obj, a value, a
# particular value, a
# particular size, a
# particular size / a
# particular allocation), a particular destruct / a
# particular release, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (if a flag (a & 0x1, a flag, a
# particular value, a
# particular state, a
# particular size / a
# particular allocation) is non-zero, a
# particular value) free a value (Free_SizeBucketed_Thunk_8221BE68, on a
# value (obj, a value, a
# particular value, a
# particular size, a
# particular size / a
# particular allocation), a particular free / a
# particular size, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (otherwise) take one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation).

# Args: obj (r3, r31, ->0 = a value, a particular value, a particular size / a
# particular allocation, a
# particular structure, a destruct object), a (r4, r30, a value, a
# particular value, a
# particular size / a
# particular allocation), (more). It destructs a 20-byte object (a
# destruct, a
# particular destruct / a
# particular release, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, calling Destruct20Byte_832156E0, sub_832156E0, a destruct, on a
# value (obj, a value, a
# particular value, a
# particular size, a
# particular size / a
# particular allocation), a particular destruct / a
# particular release, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). It checks a flag (r9, a & 0x1, a
# flag, a
# particular value, a
# particular state, a
# particular size / a
# particular allocation): if it is non-zero (a particular value, a
# particular state, a
# particular size / a
# particular allocation), it frees a value (Free_SizeBucketed_Thunk_8221BE68, sub_8221BE68, a
# free, on a
# value (obj, a value, a
# particular value, a
# particular size, a
# particular size / a
# particular allocation), a particular free / a
# particular size, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result); if it is zero (a null state, a
# particular state, a
# particular size / a
# particular allocation), it takes one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation). So it's a "destruct a 20-byte object (a
# destruct, with a
# check and a
# free: destruct a 20-byte object (a
# destruct, a
# particular destruct / a
# particular release, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, calling Destruct20Byte_832156E0, on a
# value (obj, a value, a
# particular value, a
# particular size, a
# particular size / a
# particular allocation), a particular destruct / a
# particular release, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (if a flag (a & 0x1, a flag, a
# particular value, a
# particular state, a
# particular size / a
# particular allocation) is non-zero, a
# particular value) free a value (Free_SizeBucketed_Thunk_8221BE68, on a
# value (obj, a value, a
# particular value, a
# particular size, a
# particular size / a
# particular allocation), a particular free / a
# particular size, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (otherwise) take one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation))" routine (a destruct routine, part of the 0x82A14 cluster, a sibling of
# Destruct20ByteFree_82A11F90, a caller of Destruct20Byte_832156E0 and Free_SizeBucketed_Thunk_8221BE68). 56
# lines, 2 callees (Destruct20Byte_832156E0, Free_SizeBucketed_Thunk_8221BE68). No
# direct callers (it's called via a vtable / it is registered-but-uncalled, part of the game's subsystems). The
# object's class (obj, r31, ->0 = a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular structure, a destruct object, a
# particular value /
# a particular state) and the value's meaning (a, r30, a value, a
# particular value, a
# particular size / a
# particular allocation) aren't resolvable, so the name states the mechanism: a destruct (with a
# check and a
# free) (destruct a 20-byte object (a
# destruct, a
# particular destruct / a
# particular release, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, calling Destruct20Byte_832156E0, on a
# value (obj, a value, a
# particular value, a
# particular size, a
# particular size / a
# particular allocation), a particular destruct / a
# particular release, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (if a flag (a & 0x1, a flag, a
# particular value, a
# particular state, a
# particular size / a
# particular allocation) is non-zero, a
# particular value) free a value (Free_SizeBucketed_Thunk_8221BE68, on a
# value (obj, a value, a
# particular value, a
# particular size, a
# particular size / a
# particular allocation), a particular free / a
# particular size, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (otherwise) take one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation)). The "2" distinguishes it from Destruct20ByteFree_82A11F90.
[entrypoint.functions."0x82A14A20"]
name = "Destruct20ByteFree2_82A14A20"

# A 20-byte constructor (the sibling of Construct20Byte_82A11180 / Construct20Byte2_832158F8, with a copy and a check,
# the caller of CopyStructData2_82A1BD30 / sub_83211D48): construct a 20-byte object (a construct, a
# particular construct / a
# particular size, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a copy) copy struct data (a copy, a
# particular copy / a
# particular struct, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, calling CopyStructData2_82A1BD30, on a
# value (stack[80], a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular target), a particular copy / a
# particular struct, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and store values (obj->16 = r10, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, obj->20 = r9, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (if a flag (b & 0xFF, a flag, a
# particular value, a
# particular state, a
# particular size / a
# particular allocation) is non-zero, a
# particular value) call a process (sub_83211D48, on a
# value (obj, a value, a
# particular value, a
# particular size, a
# particular size / a
# particular allocation), a particular process, a
# particular operation, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (otherwise) take one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation).

# Args: obj (r3, r31, ->16 = a value, a particular value, a particular size / a
# particular allocation, ->20 = a value, a
# particular value, a
# particular size / a
# particular allocation, a
# 20-byte object), a (r4, a value, a
# particular value, a
# particular size / a
# particular allocation), b (r5, r30, a value, a
# particular value, a
# particular size / a
# particular allocation), (more). It constructs a 20-byte object (a
# construct, a
# particular construct / a
# particular size, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). In the copy, it copies struct data (a
# copy, a
# particular copy / a
# particular struct, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, calling CopyStructData2_82A1BD30, sub_82A1BD30, a copy, on a
# value (stack[80], a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular target), a particular copy / a
# particular struct, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). It stores values (obj->16 = r10, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, obj->20 = r9, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result). It checks a flag (r11, b & 0xFF, a
# flag, a
# particular value, a
# particular state, a
# particular size / a
# particular allocation): if it is non-zero (a particular value, a
# particular state, a
# particular size / a
# particular allocation), it calls a process (sub_83211D48, sub_83211D48, a
# process, on a
# value (obj, a value, a
# particular value, a
# particular size, a
# particular size / a
# particular allocation), a particular process, a
# particular operation, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result); if it is zero (a null state, a
# particular state, a
# particular size / a
# particular allocation), it takes one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation). So it's a "construct a 20-byte object (a
# construct, with a
# copy and a
# check: construct a 20-byte object (a
# construct, a
# particular construct / a
# particular size, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a copy) copy struct data (a copy, a
# particular copy / a
# particular struct, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, calling CopyStructData2_82A1BD30, on a
# value (stack[80], a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular target), a particular copy / a
# particular struct, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and store values (obj->16 = r10, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, obj->20 = r9, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (if a flag (b & 0xFF, a flag, a
# particular value, a
# particular state, a
# particular size / a
# particular allocation) is non-zero, a
# particular value) call a process (sub_83211D48, on a
# value (obj, a value, a
# particular value, a
# particular size, a
# particular size / a
# particular allocation), a particular process, a
# particular operation, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (otherwise) take one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation))" routine (a 20-byte constructor, with a
# copy and a
# check, part
# of the 0x83212 cluster, a sibling of Construct20Byte_82A11180 and Construct20Byte2_832158F8, a caller of
# CopyStructData2_82A1BD30 and sub_83211D48). 58 lines, 2 callees (CopyStructData2_82A1BD30, sub_83211D48). No
# direct callers (it's called via a vtable / it is registered-but-uncalled, part of the game's subsystems). The
# object's class (obj, ->16 = a value, a particular value, a particular size / a
# particular allocation, ->20 = a value, a
# particular value, a
# particular size / a
# particular allocation, a
# 20-byte object, a
# particular structure, a
# particular size / a
# particular allocation, a
# particular value /
# a particular state) and the value's meaning (a, r4, a value, a
# particular value, a
# particular size / a
# particular allocation, b, r30, a value, a
# particular value, a
# particular size / a
# particular allocation) aren't resolvable, so the name states the mechanism: a 20-byte constructor (with a
# copy and a
# check) (construct a 20-byte object (a
# construct, a
# particular construct / a
# particular size, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a copy) copy struct data (a copy, a
# particular copy / a
# particular struct, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, calling CopyStructData2_82A1BD30, on a
# value (stack[80], a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular target), a particular copy / a
# particular struct, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and store values (obj->16 = r10, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, obj->20 = r9, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (if a flag (b & 0xFF, a flag, a
# particular value, a
# particular state, a
# particular size / a
# particular allocation) is non-zero, a
# particular value) call a process (sub_83211D48, on a
# value (obj, a value, a
# particular value, a
# particular size, a
# particular size / a
# particular allocation), a particular process, a
# particular operation, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (otherwise) take one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation)). The "3" distinguishes it from Construct20Byte_82A11180 and Construct20Byte2_832158F8.
[entrypoint.functions."0x83212058"]
name = "Construct20Byte3_83212058"

# A 20-byte constructor (the sibling of Construct20Byte_82A11180 / Construct20Byte2_832158F8 / Construct20Byte3_83
# 212058, with a constant and a copy, the caller of Construct20Byte_83215640): construct a 20-byte object (a
# construct, a particular construct / a
# particular size, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a copy) copy a constant (a copy, a
# particular copy / a
# particular constant, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, calling Construct20Byte_83215640, on a
# value (obj, a value, a
# particular value, a
# particular size, a
# particular size / a
# particular allocation), a particular construct / a
# particular size, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and store values (obj->0 = a constant, from a fixed .data address, a class id / a
# marker, a particular class / a
# particular marker, a
# store, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, obj->16 = a constant, from a fixed .data address, a float, a
# particular value, a
# store, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, obj->20 = a constant, from a fixed .data address, a float, a
# particular value, a
# store, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result).

# Args: obj (r3, r31, ->0 = a field, a constant, a class id / a marker, ->16 = a field, a
# float, ->20 = a field, a float, a
# 20-byte object), (more). It constructs a 20-byte object (a
# construct, a
# particular construct / a
# particular size, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). In the copy, it copies a constant (a
# copy, a
# particular copy / a
# particular constant, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, calling Construct20Byte_83215640, sub_83215640, a construct, on a
# value (obj, a value, a
# particular value, a
# particular size, a
# particular size / a
# particular allocation), a particular construct / a
# particular size, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). It stores values (obj->0 = a constant, from a fixed .data address, a class id / a
# marker, a particular class / a
# particular marker, a
# store, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, obj->16 = a constant, from a fixed .data address, a float, a
# particular value, a
# store, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, obj->20 = a constant, from a fixed .data address, a float, a
# particular value, a
# store, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result). So it's a "construct a 20-byte object (a
# construct, with a
# constant and a
# copy: construct a 20-byte object (a
# construct, a
# particular construct / a
# particular size, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a copy) copy a constant (a copy, a
# particular copy / a
# particular constant, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, calling Construct20Byte_83215640, on a
# value (obj, a value, a
# particular value, a
# particular size, a
# particular size / a
# particular allocation), a particular construct / a
# particular size, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and store values (obj->0 = a constant, from a fixed .data address, a class id / a
# marker, a particular class / a
# particular marker, a
# store, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, obj->16 = a constant, from a fixed .data address, a float, a
# particular value, a
# store, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, obj->20 = a constant, from a fixed .data address, a float, a
# particular value, a
# store, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result))" routine (a 20-byte constructor, with a
# constant and a
# copy, part
# of the 0x83215 cluster, a sibling of Construct20Byte_82A11180, Construct20Byte2_832158F8, and
# Construct20Byte3_83212058, a caller of Construct20Byte_83215640). 51 lines, 1
# callee (Construct20Byte_83215640). 2 callers: sub_83219588, ProcessTreeNode_83218C10 (processes, which
# call this to construct a 20-byte object, a
# particular size / a
# particular allocation, a
# particular operation). The object's class (obj, ->0 = a field, a constant, a class id / a
# marker, ->16 = a field, a
# float, ->20 = a field, a float, a
# 20-byte object, a
# particular structure, a
# particular size / a
# particular allocation, a
# particular value /
# a particular state) isn't resolvable, so the name states the mechanism: a 20-byte constructor (with a
# constant and a
# copy) (construct a 20-byte object (a
# construct, a
# particular construct / a
# particular size, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a copy) copy a constant (a copy, a
# particular copy / a
# particular constant, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, calling Construct20Byte_83215640, on a
# value (obj, a value, a
# particular value, a
# particular size, a
# particular size / a
# particular allocation), a particular construct / a
# particular size, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and store values (obj->0 = a constant, from a fixed .data address, a class id / a
# marker, a particular class / a
# particular marker, a
# store, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, obj->16 = a constant, from a fixed .data address, a float, a
# particular value, a
# store, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, obj->20 = a constant, from a fixed .data address, a float, a
# particular value, a
# store, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result)). The "4" distinguishes it from Construct20Byte_82A11180, Construct20Byte2_832158F8, and
# Construct20Byte3_83212058.
[entrypoint.functions."0x83215808"]
name = "Construct20Byte4_83215808"

# A convert (a convert, with a check and a compute, the sibling of ToLower_SdkRuntime / Construct20Byte4_83215808):
# convert a character (a convert, a particular convert / a
# particular character, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (if a character (c, a character, a
# particular value, a
# particular size / a
# particular allocation) is < 97 (a lowercase 'a', a
# particular value, a
# particular size / a
# particular allocation), a particular value) take one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation), and (if a character (c, a character, a
# particular value, a
# particular size / a
# particular allocation) is > 122 (a lowercase 'z', a
# particular value, a
# particular size / a
# particular allocation), a particular value) take one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation), and (otherwise) convert to uppercase (a
# convert, a
# particular convert / a
# particular character, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, c - 32, a
# compute, a
# particular compute / a
# particular operation, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result).

# Args: c (r3, a character, a
# particular value, a
# particular size / a
# particular allocation), (more). It checks a character (r3, c, a
# character, a
# particular value, a
# particular size / a
# particular allocation): if it is < 97 (a lowercase 'a', a
# particular value, a
# particular size / a
# particular allocation, a
# particular state), it takes one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation); if it is > 122 (a lowercase 'z', a
# particular value, a
# particular size / a
# particular allocation, a
# particular state), it takes one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation). If it is in the range [97, 122] (a lowercase 'a' to a lowercase 'z', a
# particular value, a
# particular size / a
# particular allocation, a
# particular state), it converts to uppercase (a
# convert, a
# particular convert / a
# particular character, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, c - 32, a
# compute, a
# particular compute / a
# particular operation, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result). So it's a "convert (a
# convert, with a
# check and a
# compute: convert a character (a convert, a
# particular convert / a
# particular character, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (if a character (c, a character, a
# particular value, a
# particular size / a
# particular allocation) is < 97 (a lowercase 'a', a
# particular value, a
# particular size / a
# particular allocation), a particular value) take one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation), and (if a character (c, a character, a
# particular value, a
# particular size / a
# particular allocation) is > 122 (a lowercase 'z', a
# particular value, a
# particular size / a
# particular allocation), a particular value) take one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation), and (otherwise) convert to uppercase (a
# convert, a
# particular convert / a
# particular character, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, c - 32, a
# compute, a
# particular compute / a
# particular operation, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result))" routine (a convert routine, part of the 0x821EE cluster, a sibling of
# ToLower_SdkRuntime and Construct20Byte4_83215808). 16
# lines, 0 callees. 10+ callers: sub_82F12280, sub_82BD2300, sub_821C0848, sub_827C3FD0, sub_82A249E8, sub
# _82C5C340, sub_822F3D20, sub_830253B8, sub_821EEA90, sub_82548870 (processes, which
# call this to convert the character, a
# particular size / a
# particular allocation, a
# particular operation). The character's meaning (c, r3, a character, a
# particular value, a
# particular size / a
# particular allocation) isn't resolvable, so the name states the mechanism: a convert (with a
# check and a
# compute) (convert a character (a convert, a
# particular convert / a
# particular character, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (if a character (c, a character, a
# particular value, a
# particular size / a
# particular allocation) is < 97 (a lowercase 'a', a
# particular value, a
# particular size / a
# particular allocation), a particular value) take one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation), and (if a character (c, a character, a
# particular value, a
# particular size / a
# particular allocation) is > 122 (a lowercase 'z', a
# particular value, a
# particular size / a
# particular allocation), a particular value) take one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation), and (otherwise) convert to uppercase (a
# convert, a
# particular convert / a
# particular character, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, c - 32, a
# compute, a
# particular compute / a
# particular operation, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result)). The "_SdkRuntime" suffix keeps the guest symbol distinct from the host libc's toupper.
[entrypoint.functions."0x821EEB10"]
name = "ToUpper_SdkRuntime_821EEB10"

# A constant return (a constant return, with a compute, the sibling of ReturnConstant_0x10000_82A14660 / ToUpper
# _SdkRuntime_821EEB10): return a constant (a constant return, a particular return / a
# particular value, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, 1024, a
# constant, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result).

# Args: (more). It returns a constant (a
# constant return, a
# particular return / a
# particular value, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, 1024, a
# constant, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result). So it's a "constant return (a
# constant return, with a
# compute: return a constant (a constant return, a
# particular return / a
# particular value, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, 1024, a
# constant, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result))" routine (a constant return routine, part of the 0x82A11 cluster, a sibling of
# ReturnConstant_0x10000_82A14660 and ToUpper_SdkRuntime_821EEB10). 8
# lines, 0 callees. No
# direct callers (it's called via a vtable / it is registered-but-uncalled, part of the game's subsystems). The
# constant's value (1024, a
# constant, a
# particular value, a
# particular size / a
# particular allocation) isn't resolvable beyond its value, so the name states the mechanism: a constant return (with a
# compute) (return a constant (a constant return, a
# particular return / a
# particular value, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, 1024, a
# constant, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result)). The "1024" refers to the constant (1024, a
# constant, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result).
[entrypoint.functions."0x82A11E60"]
name = "ReturnConstant_1024_82A11E60"

# A compute (a compute, with a check and a call, the sibling of ProcessClamped_83231358 / ReturnConstant_1024_82A11
# E60, the caller of ComputeClamped_83230A48 / sub_82CC0CF0): do a compute (a compute, a particular compute / a
# particular operation, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, stack[80], a
# particular value, a
# particular state / a
# particular result), and (if a value (stack[80], a value, a
# particular value, a
# particular size / a
# particular allocation) is non-zero, a
# particular value) call a process (sub_82CC0CF0, on a
# value (stack[84], a value, a
# particular value, a
# particular size / a
# particular allocation, r30, a
# particular value, 16384, a
# particular value / a
# particular size), a particular process, a
# particular operation, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and compute a value (obj->32 = obj->32 - r30, a
# compute, a
# particular compute / a
# particular operation, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (otherwise) take one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation).

# Args: obj (r3, r31, ->32 = a value, a particular value, a particular size / a
# particular allocation, a
# particular object), a (r4, a value, a
# particular value, a
# particular size / a
# particular allocation), b (r5, a value, a
# particular value, a
# particular size / a
# particular allocation), c (r6, r8, a value, a
# particular value, a
# particular size / a
# particular allocation), (more). It does a compute (a
# compute, a
# particular compute / a
# particular operation, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). In the check, it checks a value (a
# value, a
# particular value, a
# particular size / a
# particular allocation, stack[80], a
# particular value, a
# particular state / a
# particular result). If it is non-zero (a particular value, a
# particular state, a
# particular size / a
# particular allocation), it calls a process (sub_82CC0CF0, sub_82CC0CF0, a
# process, on a
# value (stack[84], a value, a
# particular value, a
# particular size / a
# particular allocation, r30, a
# particular value, 16384, a
# particular value / a
# particular size), a particular process, a
# particular operation, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and computes a value (obj->32 = obj->32 - r30, a
# compute, a
# particular compute / a
# particular operation, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result); if it is zero (a null state, a
# particular state, a
# particular size / a
# particular allocation), it takes one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation). So it's a "compute (a
# compute, with a
# check and a
# call: do a compute (a compute, a
# particular compute / a
# particular operation, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, stack[80], a
# particular value, a
# particular state / a
# particular result), and (if a value (stack[80], a value, a
# particular value, a
# particular size / a
# particular allocation) is non-zero, a
# particular value) call a process (sub_82CC0CF0, on a
# value (stack[84], a value, a
# particular value, a
# particular size / a
# particular allocation, r30, a
# particular value, 16384, a
# particular value / a
# particular size), a particular process, a
# particular operation, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and compute a value (obj->32 = obj->32 - r30, a
# compute, a
# particular compute / a
# particular operation, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (otherwise) take one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation))" routine (a compute routine, part of the 0x83230 cluster, a sibling of
# ProcessClamped_83231358 and ReturnConstant_1024_82A11E60, a caller of ComputeClamped_83230A48 and sub_82CC0CF0). 68
# lines, 2 callees (ComputeClamped_83230A48, sub_82CC0CF0). 1 caller: sub_83231240 (a process, which calls this to do
# the compute, a
# particular size / a
# particular allocation, a
# particular operation). The object's class (obj, r31, ->32 = a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular object, a
# particular value /
# a particular state) and the value's meaning (a, r4, a value, a
# particular value, a
# particular size / a
# particular allocation, b, r5, a value, a
# particular value, a
# particular size / a
# particular allocation, c, r8, a value, a
# particular value, a
# particular size / a
# particular allocation) aren't resolvable, so the name states the mechanism: a compute (with a
# check and a
# call) (do a compute (a compute, a
# particular compute / a
# particular operation, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, stack[80], a
# particular value, a
# particular state / a
# particular result), and (if a value (stack[80], a value, a
# particular value, a
# particular size / a
# particular allocation) is non-zero, a
# particular value) call a process (sub_82CC0CF0, on a
# value (stack[84], a value, a
# particular value, a
# particular size / a
# particular allocation, r30, a
# particular value, 16384, a
# particular value / a
# particular size), a particular process, a
# particular operation, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and compute a value (obj->32 = obj->32 - r30, a
# compute, a
# particular compute / a
# particular operation, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (otherwise) take one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation)). The "83230C88" refers to the address (a
# particular address, a
# particular size / a
# particular allocation).
[entrypoint.functions."0x83230C88"]
name = "ProcessClamped_83230C88"

# A process (a process, with a compute and a check and an insert, the sibling of ProcessClamped_83230C88 / Process
# Insert_83231358, the caller of ComputeBlockSize_829FD1C8 / List_InsertSorted_829FD158 / ProcessClamped_83230C88): do
# a process (a process, a particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a compute) compute a value (a value, a
# particular value, a
# particular size / a
# particular allocation, obj->36 = obj->36 - a->12, a
# compute, a
# particular compute / a
# particular operation, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, r24, a
# particular value, r8, a
# particular value, a
# particular state / a
# particular result), and (if a value (r24, a value, a
# particular value, a
# particular size / a
# particular allocation) is < a value (r8, a value, a
# particular value, a
# particular size / a
# particular allocation), a particular value) take one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation), and (otherwise) insert a value (List_InsertSorted_829FD158, on a
# value (obj, a value, a
# particular value, a
# particular size, a
# particular size / a
# particular allocation, a->12, a
# particular value, a
# particular size / a
# particular allocation), a particular insert / a
# particular value, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result).

# Args: obj (r3, r30, ->36 = a value, a particular value, a particular size / a
# particular allocation, ->4 = a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular object), a (r4, r29, ->12 = a value, a particular value, a particular size / a
# particular allocation, ->8 = a value, a
# particular value, a
# particular size / a
# particular allocation, ->16 = a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular object), b (r5, r25, ->0 = a value, a particular value, a particular size / a
# particular allocation, a
# particular object), (more). It does a process (a
# process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). In the compute, it computes a value (a
# value, a
# particular value, a
# particular size / a
# particular allocation, obj->36 = obj->36 - a->12, a
# compute, a
# particular compute / a
# particular operation, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result). In the check, it checks a value (a
# value, a
# particular value, a
# particular size / a
# particular allocation, r24, a
# particular value, r8, a
# particular value, a
# particular state / a
# particular result). If it is < a value (r24, a
# value, a
# particular value, a
# particular size / a
# particular allocation, r8, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state), it takes one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation); otherwise, it inserts a value (List_InsertSorted_829FD158, sub_829FD158, an
# insert, on a
# value (obj, a value, a
# particular value, a
# particular size, a
# particular size / a
# particular allocation, a->12, a
# particular value, a
# particular size / a
# particular allocation), a particular insert / a
# particular value, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). So it's a "process (a
# process, with a
# compute and a
# check and an
# insert: do a process (a process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a compute) compute a value (a value, a
# particular value, a
# particular size / a
# particular allocation, obj->36 = obj->36 - a->12, a
# compute, a
# particular compute / a
# particular operation, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, r24, a
# particular value, r8, a
# particular value, a
# particular state / a
# particular result), and (if a value (r24, a value, a
# particular value, a
# particular size / a
# particular allocation) is < a value (r8, a value, a
# particular value, a
# particular size / a
# particular allocation), a particular value) take one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation), and (otherwise) insert a value (List_InsertSorted_829FD158, on a
# value (obj, a value, a
# particular value, a
# particular size, a
# particular size / a
# particular allocation, a->12, a
# particular value, a
# particular size / a
# particular allocation), a particular insert / a
# particular value, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result))" routine (a process routine, part of the 0x83231 cluster, a sibling of
# ProcessClamped_83230C88 and ProcessInsert_83231358, a caller of ComputeBlockSize_829FD1C8, List_InsertSorted_829FD158,
# and ProcessClamped_83230C88). 405 lines, 5
# callees (ComputeBlockSize_829FD1C8, List_InsertSorted_829FD158, sub_83230BC0, sub_83230C18, ProcessClamped_83230C88).
# 1 caller: FindSizeBucket_83231518 (a process, which calls this to do the process, a
# particular size / a
# particular allocation, a
# particular operation). The object's class (obj, r30, ->36 = a value, a
# particular value, a
# particular size / a
# particular allocation, ->4 = a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular object, a
# particular value /
# a particular state) and the value's meaning (a, r29, ->12 = a value, a
# particular value, a
# particular size / a
# particular allocation, ->8 = a value, a
# particular value, a
# particular size / a
# particular allocation, ->16 = a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular object, b, r25, ->0 = a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular object) aren't resolvable, so the name states the mechanism: a process (with a
# compute and a
# check and an
# insert) (do a process (a process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a compute) compute a value (a value, a
# particular value, a
# particular size / a
# particular allocation, obj->36 = obj->36 - a->12, a
# compute, a
# particular compute / a
# particular operation, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, r24, a
# particular value, r8, a
# particular value, a
# particular state / a
# particular result), and (if a value (r24, a value, a
# particular value, a
# particular size / a
# particular allocation) is < a value (r8, a value, a
# particular value, a
# particular size / a
# particular allocation), a particular value) take one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation), and (otherwise) insert a value (List_InsertSorted_829FD158, on a
# value (obj, a value, a
# particular value, a
# particular size, a
# particular size / a
# particular allocation, a->12, a
# particular value, a
# particular size / a
# particular allocation), a particular insert / a
# particular value, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result)). The "83231240" refers to the address (a
# particular address, a
# particular size / a
# particular allocation).
[entrypoint.functions."0x83231240"]
name = "ProcessInsert_83231240"

# A return address (a return address, with a constant, the sibling of ReturnPointer_82C3C648 / ProcessInsert_83231
# 240): return a pointer (a return address, a particular return / a
# particular pointer, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, from a fixed .data address + 19932, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result).

# Args: (more). It returns a pointer (a
# return address, a
# particular return / a
# particular pointer, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, from a fixed .data address + 19932, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result). So it's a "return address (a
# return address, with a
# constant: return a pointer (a return address, a
# particular return / a
# particular pointer, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, from a fixed .data address + 19932, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result))" routine (a return address routine, part of the 0x82C3B cluster, a sibling of
# ReturnPointer_82C3C648 and ProcessInsert_83231240). 10
# lines, 0 callees. No
# direct callers (it's called via a vtable / it is registered-but-uncalled, part of the game's subsystems). The
# pointer's meaning (r3, a pointer, a
# particular value, a
# particular size / a
# particular allocation, from a fixed .data address + 19932, a
# particular value) isn't resolvable, so the name states the mechanism: a return address (with a
# constant) (return a pointer (a return address, a
# particular return / a
# particular pointer, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, from a fixed .data address + 19932, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result)). The "82C3BE78" refers to the address (a
# particular address, a
# particular size / a
# particular allocation).
[entrypoint.functions."0x82C3BE78"]
name = "ReturnPointer_82C3BE78"

# A process (a process, with a vcall and a check, the sibling of ProcessVcall_82C22258 / ProcessSortCompare_82C221
# 48, the caller of VcallTriple_AllSuccess_82C011E8 / AtomicIncrement3_821F7798 / DestructRefCounted_82214F08):
# do a process (a process, a particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a vcall) do a vcall (a vcall, a particular vcall / a
# particular method, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, on b->0->20, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, r3 & 0xFF, a
# particular value, a
# particular state / a
# particular result), and (if a value (r3 & 0xFF, a value, a
# particular value, a
# particular size / a
# particular allocation) is non-zero, a
# particular value) do another vcall (a vcall, a
# particular vcall / a
# particular method, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, calling VcallTriple_AllSuccess_82C011E8, on a
# value (b, a value, a
# particular value, a
# particular size, a
# particular size / a
# particular allocation, a + 4, a
# particular value), a particular vcall / a
# particular method, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (otherwise) take one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation).

# Args: a (r3, r30, a value, a
# particular value, a
# particular size / a
# particular allocation), b (r4, r31, ->0 = a value, a particular value, a particular size / a
# particular allocation, a
# particular object), (more). It does a process (a
# process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). In the vcall, it does a vcall (a
# vcall, a
# particular vcall / a
# particular method, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, on b->0->20, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result). In the check, it checks a value (a
# value, a
# particular value, a
# particular size / a
# particular allocation, r3 & 0xFF, a
# particular value, a
# particular state / a
# particular result). If it is non-zero (a particular value, a
# particular state, a
# particular size / a
# particular allocation), it does another vcall (a
# vcall, a
# particular vcall / a
# particular method, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, calling VcallTriple_AllSuccess_82C011E8, sub_82C011E8, a vcall, on a
# value (b, a value, a
# particular value, a
# particular size, a
# particular size / a
# particular allocation, a + 4, a
# particular value), a particular vcall / a
# particular method, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result); if it is zero (a null state, a
# particular state, a
# particular size / a
# particular allocation), it takes one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation). So it's a "process (a
# process, with a
# vcall and a
# check: do a process (a process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a vcall) do a vcall (a vcall, a
# particular vcall / a
# particular method, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, on b->0->20, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, r3 & 0xFF, a
# particular value, a
# particular state / a
# particular result), and (if a value (r3 & 0xFF, a value, a
# particular value, a
# particular size / a
# particular allocation) is non-zero, a
# particular value) do another vcall (a vcall, a
# particular vcall / a
# particular method, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, calling VcallTriple_AllSuccess_82C011E8, on a
# value (b, a value, a
# particular value, a
# particular size, a
# particular size / a
# particular allocation, a + 4, a
# particular value), a particular vcall / a
# particular method, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (otherwise) take one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation))" routine (a process routine, part of the 0x82C21 cluster, a sibling of
# ProcessVcall_82C22258 and ProcessSortCompare_82C22148, a caller of VcallTriple_AllSuccess_82C011E8, AtomicIncrement3_821F7798,
# and DestructRefCounted_82214F08). 302 lines, 5
# callees (AtomicIncrement3_821F7798, DestructRefCounted_82214F08, ProcessVcallSequence3_82C14D70, VcallTriple_AllSuccess_82C011E8,
# sub_8228A4C0). 1 caller: sub_82C22258 (a process, which calls this to do the process, a
# particular size / a
# particular allocation, a
# particular operation). The object's class (b, r31, ->0 = a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular object, a
# particular value /
# a particular state) and the value's meaning (a, r30, a value, a
# particular value, a
# particular size / a
# particular allocation) aren't resolvable, so the name states the mechanism: a process (with a
# vcall and a
# check) (do a process (a process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a vcall) do a vcall (a vcall, a
# particular vcall / a
# particular method, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, on b->0->20, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, r3 & 0xFF, a
# particular value, a
# particular state / a
# particular result), and (if a value (r3 & 0xFF, a value, a
# particular value, a
# particular size / a
# particular allocation) is non-zero, a
# particular value) do another vcall (a vcall, a
# particular vcall / a
# particular method, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, calling VcallTriple_AllSuccess_82C011E8, on a
# value (b, a value, a
# particular value, a
# particular size, a
# particular size / a
# particular allocation, a + 4, a
# particular value), a particular vcall / a
# particular method, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (otherwise) take one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation)). The "82C21F38" refers to the address (a
# particular address, a
# particular size / a
# particular allocation).
[entrypoint.functions."0x82C21F38"]
name = "ProcessVcall_82C21F38"

# A process (a process, with a vcall and a check, the sibling of ProcessVcall_82C21F38 / ProcessVcall3_82C223E8, the
# caller of ProcessVcall_82C21F38): do a process (a process, a particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a vcall) do a vcall (a vcall, a particular vcall / a
# particular method, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, on obj->0->24, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, r3 & 0xFF, a
# particular value, a
# particular state / a
# particular result), and (if a value (r3 & 0xFF, a value, a
# particular value, a
# particular size / a
# particular allocation) is non-zero, a
# particular value) call a process (ProcessVcall_82C21F38, on a
# value (b, a value, a
# particular value, a
# particular size, a
# particular size / a
# particular allocation, obj, a
# particular value, a
# particular size / a
# particular allocation), a particular process, a
# particular operation, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (otherwise) take one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation).

# Args: obj (r3, r31, ->0 = a value, a particular value, a particular size / a
# particular allocation, a
# particular object), a (r4, a value, a
# particular value, a
# particular size / a
# particular allocation), b (r5, r30, a value, a
# particular value, a
# particular size / a
# particular allocation), (more). It does a process (a
# process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). In the vcall, it does a vcall (a
# vcall, a
# particular vcall / a
# particular method, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, on obj->0->24, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result). In the check, it checks a value (a
# value, a
# particular value, a
# particular size / a
# particular allocation, r3 & 0xFF, a
# particular value, a
# particular state / a
# particular result). If it is non-zero (a particular value, a
# particular state, a
# particular size / a
# particular allocation), it calls a process (ProcessVcall_82C21F38, sub_82C21F38, a
# process, on a
# value (b, a value, a
# particular value, a
# particular size, a
# particular size / a
# particular allocation, obj, a
# particular value, a
# particular size / a
# particular allocation), a particular process, a
# particular operation, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result); if it is zero (a null state, a
# particular state, a
# particular size / a
# particular allocation), it takes one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation). So it's a "process (a
# process, with a
# vcall and a
# check: do a process (a process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a vcall) do a vcall (a vcall, a
# particular vcall / a
# particular method, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, on obj->0->24, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, r3 & 0xFF, a
# particular value, a
# particular state / a
# particular result), and (if a value (r3 & 0xFF, a value, a
# particular value, a
# particular size / a
# particular allocation) is non-zero, a
# particular value) call a process (ProcessVcall_82C21F38, on a
# value (b, a value, a
# particular value, a
# particular size, a
# particular size / a
# particular allocation, obj, a
# particular value, a
# particular size / a
# particular allocation), a particular process, a
# particular operation, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (otherwise) take one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation))" routine (a process routine, part of the 0x82C22 cluster, a sibling of
# ProcessVcall_82C21F38 and ProcessVcall3_82C223E8, a caller of ProcessVcall_82C21F38). 86 lines, 1
# callee (ProcessVcall_82C21F38). 1 caller: sub_82C223E8 (a process, which calls this to do the process, a
# particular size / a
# particular allocation, a
# particular operation). The object's class (obj, r31, ->0 = a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular object, a
# particular value /
# a particular state) and the value's meaning (a, r4, a value, a
# particular value, a
# particular size / a
# particular allocation, b, r30, a value, a
# particular value, a
# particular size / a
# particular allocation) aren't resolvable, so the name states the mechanism: a process (with a
# vcall and a
# check) (do a process (a process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a vcall) do a vcall (a vcall, a
# particular vcall / a
# particular method, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, on obj->0->24, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, r3 & 0xFF, a
# particular value, a
# particular state / a
# particular result), and (if a value (r3 & 0xFF, a value, a
# particular value, a
# particular size / a
# particular allocation) is non-zero, a
# particular value) call a process (ProcessVcall_82C21F38, on a
# value (b, a value, a
# particular value, a
# particular size, a
# particular size / a
# particular allocation, obj, a
# particular value, a
# particular size / a
# particular allocation), a particular process, a
# particular operation, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (otherwise) take one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation)). The "2" distinguishes it from ProcessVcall_82C21F38.
[entrypoint.functions."0x82C22258"]
name = "ProcessVcall2_82C22258"

# A 32-byte copy constructor (the sibling of Construct24ByteCopy_821E3B38 / Construct32Byte_82B722E8, with a copy and a check,
# the caller of Allocate_OrDbgBreak_8221F388): construct a 32-byte object (a construct, a
# particular construct / a
# particular size, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a copy) copy a 32-byte object (a copy, a
# particular copy / a
# particular struct, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, from src, a value, a
# particular value, a
# particular size, a
# particular size / a
# particular allocation: obj->0 = 0, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, obj->4 = src->4, a float, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, obj->8 = src->8, a float, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, obj->16 = src->16, a vector, 16 bytes, a vector load / a vector store, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (if a value (src->0, a value, a
# particular value, a
# particular size / a
# particular allocation) is non-zero, a
# particular value) allocate a value (Allocate_OrDbgBreak_8221F388, 160 bytes, a size, a
# particular value, a
# particular size / a
# particular allocation, on a
# value (160, a value, a
# particular value, a
# particular size / a
# particular allocation), a particular allocate / a
# particular size, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (otherwise) take one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation).

# Args: obj (r3, r31, ->0 = a field, a value, ->4 = a field, a float, ->8 = a field, a
# float, ->16 = a field, a vector, a
# 32-byte object), src (r4, r30, ->0 = a field, a value, ->4 = a field, a float, ->8 = a
# field, a float, ->16 = a field, a vector, a
# 32-byte object), (more). It constructs a 32-byte object (a
# construct, a
# particular construct / a
# particular size, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). In the copy, it copies a 32-byte object (a
# copy, a
# particular copy / a
# particular struct, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, from src, a value, a
# particular value, a
# particular size, a
# particular size / a
# particular allocation: obj->0 = 0, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, obj->4 = src->4, a float, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, obj->8 = src->8, a float, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, obj->16 = src->16, a vector, 16 bytes, a vector load / a vector store, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result). It checks a value (r10, src->0, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state): if it is non-zero (a particular value, a
# particular state, a
# particular size / a
# particular allocation), it allocates a value (Allocate_OrDbgBreak_8221F388, sub_8221F388, an
# allocate, 160 bytes, a size, a
# particular value, a
# particular size / a
# particular allocation, on a
# value (160, a value, a
# particular value, a
# particular size / a
# particular allocation), a particular allocate / a
# particular size, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result); if it is zero (a null state, a
# particular state, a
# particular size / a
# particular allocation), it takes one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation). So it's a "construct a 32-byte object (a
# construct, with a
# copy and a
# check: construct a 32-byte object (a
# construct, a
# particular construct / a
# particular size, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a copy) copy a 32-byte object (a copy, a
# particular copy / a
# particular struct, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, from src, a value, a
# particular value, a
# particular size, a
# particular size / a
# particular allocation: obj->0 = 0, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, obj->4 = src->4, a float, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, obj->8 = src->8, a float, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, obj->16 = src->16, a vector, 16 bytes, a vector load / a vector store, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (if a value (src->0, a value, a
# particular value, a
# particular size / a
# particular allocation) is non-zero, a
# particular value) allocate a value (Allocate_OrDbgBreak_8221F388, 160 bytes, a size, a
# particular value, a
# particular size / a
# particular allocation, on a
# value (160, a value, a
# particular value, a
# particular size / a
# particular allocation), a particular allocate / a
# particular size, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (otherwise) take one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation))" routine (a 32-byte copy constructor, with a
# copy and a
# check, part
# of the 0x82A0C cluster, a sibling of Construct24ByteCopy_821E3B38 and Construct32Byte_82B722E8, a caller of
# Allocate_OrDbgBreak_8221F388). 188 lines, 1
# callee (Allocate_OrDbgBreak_8221F388). 4 callers: sub_82A0BDA0, sub_82A0B668, sub_8321F968, sub_82A0B930 (processes,
# which call this to construct the 32-byte object, a
# particular size / a
# particular allocation, a
# particular operation). The object's class (obj, r31, ->0 = a field, a value, ->4 = a field, a
# float, ->8 = a field, a
# float, ->16 = a field, a vector, a
# 32-byte object, a
# particular structure, a
# particular size / a
# particular allocation, a
# particular value /
# a particular state) isn't resolvable, so the name states the mechanism: a 32-byte copy constructor (with a
# copy and a
# check) (construct a 32-byte object (a
# construct, a
# particular construct / a
# particular size, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a copy) copy a 32-byte object (a copy, a
# particular copy / a
# particular struct, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, from src, a value, a
# particular value, a
# particular size, a
# particular size / a
# particular allocation: obj->0 = 0, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, obj->4 = src->4, a float, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, obj->8 = src->8, a float, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, obj->16 = src->16, a vector, 16 bytes, a vector load / a vector store, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (if a value (src->0, a value, a
# particular value, a
# particular size / a
# particular allocation) is non-zero, a
# particular value) allocate a value (Allocate_OrDbgBreak_8221F388, 160 bytes, a size, a
# particular value, a
# particular size / a
# particular allocation, on a
# value (160, a value, a
# particular value, a
# particular size / a
# particular allocation), a particular allocate / a
# particular size, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (otherwise) take one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation)). The "Copy" refers to the copy (a
# copy, a
# particular copy / a
# particular struct, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, from src, a value, a
# particular value, a
# particular size, a
# particular size / a
# particular allocation).
[entrypoint.functions."0x82A0C1B8"]
name = "Construct32ByteCopy_82A0C1B8"

# A process (a process, with a loop and a compute and a recursion, the sibling of ProcessRecursive_8229A2A8 / Process
# Recursive2_82A0A2D0, the caller of sub_82C202A0 / sub_82C20680): do a process (a process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a loop) loop over a range (a loop, a particular loop / a
# particular range, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over n, a count, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (in a compute) compute a value (a value, a
# particular value, a
# particular size / a
# particular allocation, (src - dst) >> 2, a
# compute, a
# particular compute / a
# particular operation, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (if a value ((src - dst) >> 2, a value, a
# particular value, a
# particular size / a
# particular allocation) is > 32, a
# particular value) call a process (sub_82C202A0, on a
# value (stack[80], a value, a
# particular value, a
# particular size / a
# particular allocation, dst, a
# particular value, a
# particular size / a
# particular allocation, src, a
# particular value, a
# particular size / a
# particular allocation, a, a
# particular value), a particular process, a
# particular operation, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (otherwise) take one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation).

# Args: dst (r3, r30, a value, a
# particular value, a
# particular size / a
# particular allocation, a destination), src (r4, r31, a value, a
# particular value, a
# particular size / a
# particular allocation, a source), n (r5, r29, a count, a
# particular value, a
# particular size / a
# particular allocation), a (r6, r28, a value, a
# particular value, a
# particular size / a
# particular allocation), (more). It does a process (a
# process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). In the loop, it loops over a range (a
# loop, a
# particular loop / a
# particular range, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over n, a count, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result). In the compute, it computes a value (a
# value, a
# particular value, a
# particular size / a
# particular allocation, (src - dst) >> 2, a
# compute, a
# particular compute / a
# particular operation, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result). If it is > 32 (a particular value, a
# particular state, a
# particular size / a
# particular allocation), it calls a process (sub_82C202A0, sub_82C202A0, a
# process, on a
# value (stack[80], a value, a
# particular value, a
# particular size / a
# particular allocation, dst, a
# particular value, a
# particular size / a
# particular allocation, src, a
# particular value, a
# particular size / a
# particular allocation, a, a
# particular value), a particular process, a
# particular operation, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result); if it is <= 32 (a particular value, a
# particular state, a
# particular size / a
# particular allocation), it takes one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation). So it's a "process (a
# process, with a
# loop and a
# compute and a
# recursion: do a process (a process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a loop) loop over a range (a loop, a
# particular loop / a
# particular range, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over n, a count, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (in a compute) compute a value (a value, a
# particular value, a
# particular size / a
# particular allocation, (src - dst) >> 2, a
# compute, a
# particular compute / a
# particular operation, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (if a value ((src - dst) >> 2, a value, a
# particular value, a
# particular size / a
# particular allocation) is > 32, a
# particular value) call a process (sub_82C202A0, on a
# value (stack[80], a value, a
# particular value, a
# particular size / a
# particular allocation, dst, a
# particular value, a
# particular size / a
# particular allocation, src, a
# particular value, a
# particular size / a
# particular allocation, a, a
# particular value), a particular process, a
# particular operation, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (otherwise) take one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation))" routine (a process routine, part of the 0x82C20 cluster, a sibling of
# ProcessRecursive_8229A2A8 and ProcessRecursive2_82A0A2D0, a caller of sub_82C202A0 and sub_82C20680 (self-recursive)). 176
# lines, 5 callees (sub_82C1FE90, sub_82C202A0, sub_82C204F0, sub_82C20618, sub_82C20680 (self-recursive)). 2
# callers: sub_82C208E8, sub_82C20878 (processes, which
# call this to do the process, a
# particular size / a
# particular allocation, a
# particular operation). The object's class (dst, r30, a value, a
# particular value, a
# particular size / a
# particular allocation, a destination, a
# particular value /
# a particular state) and the value's meaning (src, r31, a value, a
# particular value, a
# particular size / a
# particular allocation, a source, n, r29, a count, a
# particular value, a
# particular size / a
# particular allocation, a, r28, a value, a
# particular value, a
# particular size / a
# particular allocation) aren't resolvable, so the name states the mechanism: a process (with a
# loop and a
# compute and a
# recursion) (do a process (a process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a loop) loop over a range (a loop, a
# particular loop / a
# particular range, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over n, a count, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (in a compute) compute a value (a value, a
# particular value, a
# particular size / a
# particular allocation, (src - dst) >> 2, a
# compute, a
# particular compute / a
# particular operation, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (if a value ((src - dst) >> 2, a value, a
# particular value, a
# particular size / a
# particular allocation) is > 32, a
# particular value) call a process (sub_82C202A0, on a
# value (stack[80], a value, a
# particular value, a
# particular size / a
# particular allocation, dst, a
# particular value, a
# particular size / a
# particular allocation, src, a
# particular value, a
# particular size / a
# particular allocation, a, a
# particular value), a particular process, a
# particular operation, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (otherwise) take one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation)). The "3" distinguishes it from ProcessRecursive_8229A2A8 and ProcessRecursive2_82A0A2D0.
[entrypoint.functions."0x82C20680"]
name = "ProcessRecursive3_82C20680"

# A process (a process, with an atomic increment and a check, the sibling of ProcessAtomicIncrement_821E6408 / Process
# Recursive3_82C20680, the caller of AllocateString_822079D8 / Release_RefCounted_821C67D8): do a process (a
# process, a particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in an atomic increment) do an atomic increment (a
# process, a particular atomic increment / a
# particular counter, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, on a global counter, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, lwarx/stwcx, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, a->0, a
# particular value, a
# particular state / a
# particular result), and (if a value (a->0, a value, a
# particular value, a
# particular size / a
# particular allocation) is non-zero, a
# particular value) call a process (Release_RefCounted_821C67D8, on a
# value (a, a value, a
# particular value, a
# particular size, a
# particular size / a
# particular allocation), a particular process, a
# particular operation, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (otherwise) take one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation).

# Args: obj (r3, r31, ->0 = a field, a value, a particular value, a particular size / a
# particular allocation, a
# particular object), a (r4, ->0 = a field, a value, a particular value, a particular size / a
# particular allocation, a
# particular object), (more). It does a process (a
# process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). In the atomic increment, it does an atomic increment (a
# process, a
# particular atomic increment / a
# particular counter, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, on a global counter, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, lwarx/stwcx, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). In the check, it checks a value (a
# value, a
# particular value, a
# particular size / a
# particular allocation, a->0, a
# particular value, a
# particular state / a
# particular result). If it is non-zero (a particular value, a
# particular state, a
# particular size / a
# particular allocation), it calls a process (Release_RefCounted_821C67D8, sub_821C67D8, a
# process, on a
# value (a, a value, a
# particular value, a
# particular size, a
# particular size / a
# particular allocation), a particular process, a
# particular operation, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result); if it is zero (a null state, a
# particular state, a
# particular size / a
# particular allocation), it takes one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation). So it's a "process (a
# process, with an
# atomic increment and a
# check: do a process (a process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in an atomic increment) do an atomic increment (a
# process, a particular atomic increment / a
# particular counter, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, on a global counter, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, lwarx/stwcx, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, a->0, a
# particular value, a
# particular state / a
# particular result), and (if a value (a->0, a value, a
# particular value, a
# particular size / a
# particular allocation) is non-zero, a
# particular value) call a process (Release_RefCounted_821C67D8, on a
# value (a, a value, a
# particular value, a
# particular size, a
# particular size / a
# particular allocation), a particular process, a
# particular operation, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (otherwise) take one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation))" routine (a process routine, part of the 0x821E3 cluster, a sibling of
# ProcessAtomicIncrement_821E6408 and ProcessRecursive3_82C20680, a caller of AllocateString_822079D8 and Release_RefCounted_821C67D8). 123
# lines, 2 callees (AllocateString_822079D8, Release_RefCounted_821C67D8). 10+ callers: sub_821F03D8, sub_8285D078, sub
# _82A6DC38, sub_822F38B0, sub_82414F40, sub_828BFFF8, sub_82C02FC8, sub_82265500, sub_82281EE0, sub_821E6408
# (processes, which call this to do the process, a
# particular size / a
# particular allocation, a
# particular operation). The object's class (obj, r31, ->0 = a field, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular object, a
# particular value /
# a particular state) and the value's meaning (a, r4, ->0 = a field, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular object) aren't resolvable, so the name states the mechanism: a process (with an
# atomic increment and a
# check) (do a process (a process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in an atomic increment) do an atomic increment (a
# process, a
# particular atomic increment / a
# particular counter, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, on a global counter, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, lwarx/stwcx, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, a->0, a
# particular value, a
# particular state / a
# particular result), and (if a value (a->0, a value, a
# particular value, a
# particular size / a
# particular allocation) is non-zero, a
# particular value) call a process (Release_RefCounted_821C67D8, on a
# value (a, a value, a
# particular value, a
# particular size, a
# particular size / a
# particular allocation), a particular process, a
# particular operation, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (otherwise) take one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation)). The "821E3950" refers to the address (a
# particular address, a
# particular size / a
# particular allocation).
[entrypoint.functions."0x821E3950"]
name = "ProcessAtomicIncrement_821E3950"

# A process (a process, with a loop and a comparison, the sibling of ProcessLoop_82C202A0 / ProcessAtomicIncrement_821
# E3950, the caller of Process2DRegion_82C3C010): do a process (a process, a particular process, a particular size /
# a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a loop) loop over a range (a loop, a particular loop / a
# particular range, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over a, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (in a comparison) compare a value (a value, a
# particular value, a
# particular size / a
# particular allocation, r9, a
# particular value, r10, a
# particular value, a
# particular state / a
# particular result), and (if a value (r9, a value, a
# particular value, a
# particular size / a
# particular allocation) is < a value (r10, a value, a
# particular value, a
# particular size / a
# particular allocation), a particular value) continue the loop (a
# particular state, a
# particular result, a particular size / a
# particular allocation), and (otherwise) call a process (Process2DRegion_82C3C010, on a
# value (a, a value, a
# particular value, a
# particular size, a
# particular size / a
# particular allocation, b, a
# particular value, a
# particular size / a
# particular allocation), a particular process, a
# particular operation, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result).

# Args: a (r3, r29, a value, a
# particular value, a
# particular size / a
# particular allocation), b (r4, r28, a value, a
# particular value, a
# particular size / a
# particular allocation), (more). It does a process (a
# process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). In the loop, it loops over a range (a
# loop, a
# particular loop / a
# particular range, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over a, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result). In the comparison, it compares a value (a
# value, a
# particular value, a
# particular size / a
# particular allocation, r9, a
# particular value, r10, a
# particular value, a
# particular state / a
# particular result). If it is < a value (r9, a
# value, a
# particular value, a
# particular size / a
# particular allocation, r10, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state), it continues the loop (a
# particular state, a
# particular result, a particular size / a
# particular allocation); otherwise, it calls a process (Process2DRegion_82C3C010, sub_82C3C010, a
# process, on a
# value (a, a value, a
# particular value, a
# particular size, a
# particular size / a
# particular allocation, b, a
# particular value, a
# particular size / a
# particular allocation), a particular process, a
# particular operation, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result). So it's a "process (a
# process, with a
# loop and a
# comparison: do a process (a process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a loop) loop over a range (a loop, a
# particular loop / a
# particular range, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over a, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (in a comparison) compare a value (a value, a
# particular value, a
# particular size / a
# particular allocation, r9, a
# particular value, r10, a
# particular value, a
# particular state / a
# particular result), and (if a value (r9, a value, a
# particular value, a
# particular size / a
# particular allocation) is < a value (r10, a value, a
# particular value, a
# particular size / a
# particular allocation), a particular value) continue the loop (a
# particular state, a
# particular result, a particular size / a
# particular allocation), and (otherwise) call a process (Process2DRegion_82C3C010, on a
# value (a, a value, a
# particular value, a
# particular size, a
# particular size / a
# particular allocation, b, a
# particular value, a
# particular size / a
# particular allocation), a particular process, a
# particular operation, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result))" routine (a process routine, part of the 0x82C20 cluster, a sibling of
# ProcessLoop_82C202A0 and ProcessAtomicIncrement_821E3950, a caller of Process2DRegion_82C3C010). 115
# lines, 1 callee (Process2DRegion_82C3C010). 1 caller: sub_82C20680 (ProcessRecursive3_82C20680, a
# process, which calls this to do the process, a
# particular size / a
# particular allocation, a
# particular operation). The object's class (a, r29, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular value /
# a particular state) and the value's meaning (b, r28, a value, a
# particular value, a
# particular size / a
# particular allocation) aren't resolvable, so the name states the mechanism: a process (with a
# loop and a
# comparison) (do a process (a process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a loop) loop over a range (a loop, a
# particular loop / a
# particular range, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over a, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (in a comparison) compare a value (a value, a
# particular value, a
# particular size / a
# particular allocation, r9, a
# particular value, r10, a
# particular value, a
# particular state / a
# particular result), and (if a value (r9, a value, a
# particular value, a
# particular size / a
# particular allocation) is < a value (r10, a value, a
# particular value, a
# particular size / a
# particular allocation), a particular value) continue the loop (a
# particular state, a
# particular result, a particular size / a
# particular allocation), and (otherwise) call a process (Process2DRegion_82C3C010, on a
# value (a, a value, a
# particular value, a
# particular size, a
# particular size / a
# particular allocation, b, a
# particular value, a
# particular size / a
# particular allocation), a particular process, a
# particular operation, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result)). The "82C204F0" refers to the address (a
# particular address, a
# particular size / a
# particular allocation).
[entrypoint.functions."0x82C204F0"]
name = "ProcessLoop_82C204F0"

# A construct (a construct, with a copy and a check, the sibling of ConstructString_821E6408 / ProcessLoop_82C204F0, the
# caller of StrLenUTF16_8217AB30 / ProcessElementAccess2_8217A6C0 / ProcessElementAccess3_8217AA20): construct a string (a
# construct, a particular construct / a
# particular string, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a copy) copy a string (a copy, a
# particular copy / a
# particular string, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, from src, a value, a
# particular value, a
# particular size, a
# particular size / a
# particular allocation: obj->20 = 0, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, obj->24 = 7, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, obj->4 = 0, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, obj->28 = 0, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, obj->32 = 0, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (in a compute) compute a string length (a
# compute, a
# particular compute / a
# particular string, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, calling StrLenUTF16_8217AB30, on a
# value (stack[80], a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular string), a particular compute / a
# particular string, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result).

# Args: obj (r3, r31, ->20 = a field, a value, ->24 = a field, a value, ->4 = a field, a
# value, ->28 = a field, a value, ->32 = a field, a value, a
# particular object), src (r4, r6, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular string), (more). It constructs a string (a
# construct, a
# particular construct / a
# particular string, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). In the copy, it copies a string (a
# copy, a
# particular copy / a
# particular string, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, from src, a value, a
# particular value, a
# particular size, a
# particular size / a
# particular allocation: obj->20 = 0, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, obj->24 = 7, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, obj->4 = 0, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, obj->28 = 0, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, obj->32 = 0, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result). In the compute, it computes a string length (a
# compute, a
# particular compute / a
# particular string, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, calling StrLenUTF16_8217AB30, sub_8217AB30, a compute, on a
# value (stack[80], a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular string), a particular compute / a
# particular string, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). So it's a "construct a string (a
# construct, with a
# copy and a
# check: construct a string (a
# construct, a
# particular construct / a
# particular string, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a copy) copy a string (a copy, a
# particular copy / a
# particular string, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, from src, a value, a
# particular value, a
# particular size, a
# particular size / a
# particular allocation: obj->20 = 0, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, obj->24 = 7, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, obj->4 = 0, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, obj->28 = 0, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, obj->32 = 0, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (in a compute) compute a string length (a
# compute, a
# particular compute / a
# particular string, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, calling StrLenUTF16_8217AB30, on a
# value (stack[80], a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular string), a particular compute / a
# particular string, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result))" routine (a construct routine, part of the 0x8217A cluster, a sibling of
# ConstructString_821E6408 and ProcessLoop_82C204F0, a caller of StrLenUTF16_8217AB30, ProcessElementAccess2_8217A6C0, and
# ProcessElementAccess3_8217AA20). 77 lines, 4
# callees (ProcessElementAccess2_8217A6C0, ProcessElementAccess3_8217AA20, StrLenUTF16_8217AB30, Trampoline_8217A978). 10+
# callers: sub_8233A8D8, sub_822F4368, sub_822F3D20, sub_82BFD600, sub_825B27D0, sub_82C67C70, sub_824E93A8, sub_82C65310,
# sub_82C07D80, sub_82374240 (processes, which
# call this to construct the string, a
# particular size / a
# particular allocation, a
# particular operation). The object's class (obj, r31, ->20 = a field, a value, ->24 = a field, a value, ->4 = a field, a
# value, ->28 = a field, a value, ->32 = a field, a value, a
# particular object, a
# particular value /
# a particular state) and the value's meaning (src, r6, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular string) aren't resolvable, so the name states the mechanism: a construct (with a
# copy and a
# check) (construct a string (a
# construct, a
# particular construct / a
# particular string, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a copy) copy a string (a copy, a
# particular copy / a
# particular string, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, from src, a value, a
# particular value, a
# particular size, a
# particular size / a
# particular allocation: obj->20 = 0, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, obj->24 = 7, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, obj->4 = 0, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, obj->28 = 0, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, obj->32 = 0, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (in a compute) compute a string length (a
# compute, a
# particular compute / a
# particular string, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, calling StrLenUTF16_8217AB30, on a
# value (stack[80], a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular string), a particular compute / a
# particular string, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result)). The "8217AB58" refers to the address (a
# particular address, a
# particular size / a
# particular allocation).
[entrypoint.functions."0x8217AB58"]
name = "ConstructString_8217AB58"

# A return address (a return address, with a constant, the sibling of ReturnPointer_82C3BE78 / ConstructString_8217AB58):
# return a pointer (a return address, a particular return / a
# particular pointer, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, from a fixed .data address + 19868, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result).

# Args: (more). It returns a pointer (a
# return address, a
# particular return / a
# particular pointer, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, from a fixed .data address + 19868, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result). So it's a "return address (a
# return address, with a
# constant: return a pointer (a return address, a
# particular return / a
# particular pointer, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, from a fixed .data address + 19868, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result))" routine (a return address routine, part of the 0x82C2E cluster, a sibling of
# ReturnPointer_82C3BE78 and ConstructString_8217AB58). 10
# lines, 0 callees. No
# direct callers (it's called via a vtable / it is registered-but-uncalled, part of the game's subsystems). The
# pointer's meaning (r3, a pointer, a
# particular value, a
# particular size / a
# particular allocation, from a fixed .data address + 19868, a
# particular value) isn't resolvable, so the name states the mechanism: a return address (with a
# constant) (return a pointer (a return address, a
# particular return / a
# particular pointer, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, from a fixed .data address + 19868, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result)). The "2" distinguishes it from ReturnPointer_82C3BE78.
[entrypoint.functions."0x82C2EDE8"]
name = "ReturnPointer2_82C2EDE8"

# A process (a process, with a copy and a loop, the sibling of ProcessToLower_821EEA90 / ReturnPointer2_82C2EDE8, the
# caller of CopyAssign_RefCounted_821F0108 / AtomicIncrement5_8222E840 / ToLower_Ascii_821EE9E8): do a process (a
# process, a particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a copy) copy a refcounted object (a copy, a
# particular copy / a
# particular refcounted object, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, calling CopyAssign_RefCounted_821F0108, on a
# value (obj, a value, a
# particular value, a
# particular size, a
# particular size / a
# particular allocation), a particular copy / a
# particular refcounted object, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a loop) loop over a range (a loop, a particular loop / a
# particular range, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over a string, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (in a to-lower) convert to lowercase (a
# convert, a
# particular convert / a
# particular character, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, calling ToLower_Ascii_821EE9E8, on a
# value (a byte, a value, a
# particular value, a
# particular size / a
# particular allocation), a particular convert / a
# particular character, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result).

# Args: obj (r3, r30, ->0 = a field, a value, a particular value, a particular size / a
# particular allocation, a
# particular object), (more). It does a process (a
# process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). In the copy, it copies a refcounted object (a
# copy, a
# particular copy / a
# particular refcounted object, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, calling CopyAssign_RefCounted_821F0108, sub_821F0108, a copy, on a
# value (obj, a value, a
# particular value, a
# particular size, a
# particular size / a
# particular allocation), a particular copy / a
# particular refcounted object, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). In the loop, it loops over a range (a
# loop, a
# particular loop / a
# particular range, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over a string, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result). In the to-lower, it converts to lowercase (a
# convert, a
# particular convert / a
# particular character, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, calling ToLower_Ascii_821EE9E8, sub_821EE9E8, a convert, on a
# value (a byte, a value, a
# particular value, a
# particular size / a
# particular allocation), a particular convert / a
# particular character, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). So it's a "process (a
# process, with a
# copy and a
# loop: do a process (a process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a copy) copy a refcounted object (a copy, a
# particular copy / a
# particular refcounted object, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, calling CopyAssign_RefCounted_821F0108, on a
# value (obj, a value, a
# particular value, a
# particular size, a
# particular size / a
# particular allocation), a particular copy / a
# particular refcounted object, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a loop) loop over a range (a loop, a
# particular loop / a
# particular range, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over a string, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (in a to-lower) convert to lowercase (a
# convert, a
# particular convert / a
# particular character, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, calling ToLower_Ascii_821EE9E8, on a
# value (a byte, a value, a
# particular value, a
# particular size / a
# particular allocation), a particular convert / a
# particular character, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result))" routine (a process routine, part of the 0x821EE cluster, a sibling of
# ProcessToLower_821EEA90 and ReturnPointer2_82C2EDE8, a caller of CopyAssign_RefCounted_821F0108, AtomicIncrement5_8222E840,
# and ToLower_Ascii_821EE9E8). 77 lines, 4
# callees (AtomicIncrement5_8222E840, CopyAssign_RefCounted_821F0108, GetSubObjField4_821EEA78, ToLower_Ascii_821EE9E8). 10+
# callers: sub_82835508, sub_824C7FB8, sub_82699F18, sub_8271E7E0, sub_82871858, sub_82C635D8, sub_83243AC0, sub_82612F38,
# sub_82A93090, sub_83242C90 (processes, which
# call this to do the process, a
# particular size / a
# particular allocation, a
# particular operation). The object's class (obj, r30, ->0 = a field, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular object, a
# particular value /
# a particular state) isn't resolvable, so the name states the mechanism: a process (with a
# copy and a
# loop) (do a process (a process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a copy) copy a refcounted object (a copy, a
# particular copy / a
# particular refcounted object, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, calling CopyAssign_RefCounted_821F0108, on a
# value (obj, a value, a
# particular value, a
# particular size, a
# particular size / a
# particular allocation), a particular copy / a
# particular refcounted object, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a loop) loop over a range (a loop, a
# particular loop / a
# particular range, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over a string, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (in a to-lower) convert to lowercase (a
# convert, a
# particular convert / a
# particular character, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, calling ToLower_Ascii_821EE9E8, on a
# value (a byte, a value, a
# particular value, a
# particular size / a
# particular allocation), a particular convert / a
# particular character, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result)). The "821EEA00" refers to the address (a
# particular address, a
# particular size / a
# particular allocation).
[entrypoint.functions."0x821EEA00"]
name = "ProcessToLower_821EEA00"

# A chain search (a process, with a lookup and a loop, the sibling of ChainSearch_82B3D880 / ChainSearch2_82B5D548 / Chain
# Search5_8236E4A0): do a chain search (a process, a particular process / a
# particular lookup, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a lookup) do a lookup (a lookup, a
# particular lookup / a
# particular value, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over a, a value, a
# particular value, a
# particular size / a
# particular allocation, a->4, a
# particular value, a
# particular state / a
# particular result), and (in a loop) loop over a chain (a loop, a particular loop / a
# particular chain, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over a chain, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (if a value (r11, a value, a
# particular value, a
# particular size / a
# particular allocation) is != a value (r9, a value, a
# particular value, a
# particular size / a
# particular allocation), a particular value) continue the loop (a
# particular state, a
# particular result, a particular size / a
# particular allocation), and (otherwise) return a value (a value, a
# particular value, a
# particular size / a
# particular allocation, r10, a
# particular value, a
# particular state / a
# particular result).

# Args: a (r3, ->4 = a field, a value, ->0 = a field, a value, a
# particular object), b (r4, ->4 = a field, a value, ->8 = a field, a
# value, a
# particular object), (more). It does a chain search (a
# process, a
# particular process / a
# particular lookup, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). In the lookup, it does a lookup (a
# lookup, a
# particular lookup / a
# particular value, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over a, a value, a
# particular value, a
# particular size / a
# particular allocation, a->4, a
# particular value, a
# particular state / a
# particular result). In the loop, it loops over a chain (a
# loop, a
# particular loop / a
# particular chain, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over a chain, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result). If a value (r11, a
# value, a
# particular value, a
# particular size / a
# particular allocation) is != a value (r9, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state), it continues the loop (a
# particular state, a
# particular result, a particular size / a
# particular allocation); otherwise, it returns a value (a
# value, a
# particular value, a
# particular size / a
# particular allocation, r10, a
# particular value, a
# particular state / a
# particular result). So it's a "chain search (a
# process, with a
# lookup and a
# loop: do a chain search (a process, a
# particular process / a
# particular lookup, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a lookup) do a lookup (a lookup, a
# particular lookup / a
# particular value, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over a, a value, a
# particular value, a
# particular size / a
# particular allocation, a->4, a
# particular value, a
# particular state / a
# particular result), and (in a loop) loop over a chain (a loop, a
# particular loop / a
# particular chain, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over a chain, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (if a value (r11, a value, a
# particular value, a
# particular size / a
# particular allocation) is != a value (r9, a value, a
# particular value, a
# particular size / a
# particular allocation), a particular value) continue the loop (a
# particular state, a
# particular result, a particular size / a
# particular allocation), and (otherwise) return a value (a value, a
# particular value, a
# particular size / a
# particular allocation, r10, a
# particular value, a
# particular state / a
# particular result))" routine (a chain search routine, part of the 0x82C46 cluster, a sibling of
# ChainSearch_82B3D880, ChainSearch2_82B5D548, and ChainSearch5_8236E4A0). 65 lines, 0
# callees. 2 callers: sub_82C46F80, Tree_ProcessNode_82C47318 (processes, which
# call this to do the chain search, a
# particular size / a
# particular allocation, a
# particular operation). The object's class (a, r3, ->4 = a field, a value, ->0 = a field, a value, a
# particular object, a
# particular value /
# a particular state) and the value's meaning (b, r4, ->4 = a field, a value, ->8 = a field, a
# value, a
# particular object) aren't resolvable, so the name states the mechanism: a chain search (with a
# lookup and a
# loop) (do a chain search (a process, a
# particular process / a
# particular lookup, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a lookup) do a lookup (a lookup, a
# particular lookup / a
# particular value, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over a, a value, a
# particular value, a
# particular size / a
# particular allocation, a->4, a
# particular value, a
# particular state / a
# particular result), and (in a loop) loop over a chain (a loop, a
# particular loop / a
# particular chain, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over a chain, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (if a value (r11, a value, a
# particular value, a
# particular size / a
# particular allocation) is != a value (r9, a value, a
# particular value, a
# particular size / a
# particular allocation), a particular value) continue the loop (a
# particular state, a
# particular result, a particular size / a
# particular allocation), and (otherwise) return a value (a value, a
# particular value, a
# particular size / a
# particular allocation, r10, a
# particular value, a
# particular state / a
# particular result)). The "6" distinguishes it from ChainSearch_82B3D880, ChainSearch2_82B5D548, and ChainSearch5_8236E4A0.
[entrypoint.functions."0x82C46BA0"]
name = "ChainSearch6_82C46BA0"

# A process (a process, with a vcall and a chain search, the sibling of ProcessVcall_82C21F38 / ProcessVcall2_82C22258,
# the caller of ChainSearch6_82C46BA0): do a process (a process, a particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a vcall) do a vcall (a vcall, a particular vcall / a
# particular method, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, on a->0->16, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (in a chain search) do a chain search (a
# process, a particular process / a
# particular lookup, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, calling ChainSearch6_82C46BA0, on a
# value (obj, a value, a
# particular value, a
# particular size, a
# particular size / a
# particular allocation, r3 + r30, a
# particular value), a particular process / a
# particular lookup, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result).

# Args: obj (r3, r31, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular object), a (r4, ->0 = a field, a value, a particular value, a particular size / a
# particular allocation, a
# particular object), b (r5, r30, a value, a
# particular value, a
# particular size / a
# particular allocation), (more). It does a process (a
# process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). In the vcall, it does a vcall (a
# vcall, a
# particular vcall / a
# particular method, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, on a->0->16, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result). In the chain search, it does a chain search (a
# process, a
# particular process / a
# particular lookup, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, calling ChainSearch6_82C46BA0, sub_82C46BA0, a process, on a
# value (obj, a value, a
# particular value, a
# particular size, a
# particular size / a
# particular allocation, r3 + r30, a
# particular value), a particular process / a
# particular lookup, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). So it's a "process (a
# process, with a
# vcall and a
# chain search: do a process (a process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a vcall) do a vcall (a vcall, a
# particular vcall / a
# particular method, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, on a->0->16, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (in a chain search) do a chain search (a
# process, a particular process / a
# particular lookup, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, calling ChainSearch6_82C46BA0, on a
# value (obj, a value, a
# particular value, a
# particular size, a
# particular size / a
# particular allocation, r3 + r30, a
# particular value), a particular process / a
# particular lookup, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result))" routine (a process routine, part of the 0x82C46 cluster, a sibling of
# ProcessVcall_82C21F38 and ProcessVcall2_82C22258, a caller of ChainSearch6_82C46BA0). 53 lines, 1
# callee (ChainSearch6_82C46BA0). 10+ callers: sub_82C13628, sub_82C55AD8, sub_82C50130, sub_82C136C0, sub_82C50198, sub
# _82C1BBA0, sub_82C1BE80, sub_82C0AF58, sub_82C52A80, sub_82C55BC8 (processes, which
# call this to do the process, a
# particular size / a
# particular allocation, a
# particular operation). The object's class (obj, r31, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular object, a
# particular value /
# a particular state) and the value's meaning (a, r4, ->0 = a field, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular object, b, r30, a value, a
# particular value, a
# particular size / a
# particular allocation) aren't resolvable, so the name states the mechanism: a process (with a
# vcall and a
# chain search) (do a process (a process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a vcall) do a vcall (a vcall, a
# particular vcall / a
# particular method, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, on a->0->16, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (in a chain search) do a chain search (a
# process, a
# particular process / a
# particular lookup, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, calling ChainSearch6_82C46BA0, on a
# value (obj, a value, a
# particular value, a
# particular size, a
# particular size / a
# particular allocation, r3 + r30, a
# particular value), a particular process / a
# particular lookup, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result)). The "3" distinguishes it from ProcessVcall_82C21F38 and ProcessVcall2_82C22258.
[entrypoint.functions."0x82C46F80"]
name = "ProcessVcall3_82C46F80"

# A process (a process, with a loop and a comparison and a heap insert, the sibling of ProcessHeapInsert_82C202A0 / Process
# Vcall3_82C46F80): do a process (a process, a particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a loop) loop over a range (a loop, a particular loop / a
# particular range, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over a, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (in a comparison) compare a value (a value, a
# particular value, a
# particular size / a
# particular allocation, r6, a
# particular value, r8, a
# particular value, a
# particular state / a
# particular result), and (if a value (r6, a value, a
# particular value, a
# particular size / a
# particular allocation) is < a value (r8, a value, a
# particular value, a
# particular size / a
# particular allocation), a particular value) insert a value (a store, a
# particular store / a
# particular value, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, a->r11 = r9, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, a->0 = r10, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (otherwise) continue the loop (a
# particular state, a
# particular result, a particular size / a
# particular allocation).

# Args: a (r3, ->0 = a field, a value, ->r11 = a field, a value, ->r7 = a field, a
# value, a
# particular object), b (r4, a value, a
# particular value, a
# particular size / a
# particular allocation), c (r5, a value, a
# particular value, a
# particular size / a
# particular allocation), (more). It does a process (a
# process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). In the loop, it loops over a range (a
# loop, a
# particular loop / a
# particular range, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over a, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result). In the comparison, it compares a value (a
# value, a
# particular value, a
# particular size / a
# particular allocation, r6, a
# particular value, r8, a
# particular value, a
# particular state / a
# particular result). If it is < a value (r6, a
# value, a
# particular value, a
# particular size / a
# particular allocation, r8, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state), it inserts a value (a
# store, a
# particular store / a
# particular value, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, a->r11 = r9, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, a->0 = r10, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result); otherwise, it continues the loop (a
# particular state, a
# particular result, a particular size / a
# particular allocation). So it's a "process (a
# process, with a
# loop and a
# comparison and a
# heap insert: do a process (a process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a loop) loop over a range (a loop, a
# particular loop / a
# particular range, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over a, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (in a comparison) compare a value (a value, a
# particular value, a
# particular size / a
# particular allocation, r6, a
# particular value, r8, a
# particular value, a
# particular state / a
# particular result), and (if a value (r6, a value, a
# particular value, a
# particular size / a
# particular allocation) is < a value (r8, a value, a
# particular value, a
# particular size / a
# particular allocation), a particular value) insert a value (a store, a
# particular store / a
# particular value, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, a->r11 = r9, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, a->0 = r10, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (otherwise) continue the loop (a
# particular state, a
# particular result, a particular size / a
# particular allocation))" routine (a process routine, part of the 0x82C1F cluster, a sibling of
# ProcessHeapInsert_82C202A0 and ProcessVcall3_82C46F80). 298 lines, 0
# callees. 1 caller: sub_82C202A0 (a process, which calls this to do the process, a
# particular size / a
# particular allocation, a
# particular operation). The object's class (a, r3, ->0 = a field, a value, ->r11 = a field, a value, ->r7 = a field, a
# value, a
# particular object, a
# particular value /
# a particular state) and the value's meaning (b, r4, a value, a
# particular value, a
# particular size / a
# particular allocation, c, r5, a value, a
# particular value, a
# particular size / a
# particular allocation) aren't resolvable, so the name states the mechanism: a process (with a
# loop and a
# comparison and a
# heap insert) (do a process (a process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a loop) loop over a range (a loop, a
# particular loop / a
# particular range, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over a, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (in a comparison) compare a value (a value, a
# particular value, a
# particular size / a
# particular allocation, r6, a
# particular value, r8, a
# particular value, a
# particular state / a
# particular result), and (if a value (r6, a value, a
# particular value, a
# particular size / a
# particular allocation) is < a value (r8, a value, a
# particular value, a
# particular size / a
# particular allocation), a particular value) insert a value (a store, a
# particular store / a
# particular value, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, a->r11 = r9, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, a->0 = r10, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (otherwise) continue the loop (a
# particular state, a
# particular result, a particular size / a
# particular allocation)). The "82C1FC68" refers to the address (a
# particular address, a
# particular size / a
# particular allocation).
[entrypoint.functions."0x82C1FC68"]
name = "ProcessHeapInsert_82C1FC68"

# A process (a process, with a heap insert and a loop, the sibling of ProcessHeapInsert_82C1FC68 / ProcessHeap_82C20330,
# the caller of ProcessHeapInsert_82C1FC68): do a process (a process, a particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a heap insert) do a heap insert (a
# process, a particular process / a
# particular insert, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, calling ProcessHeapInsert_82C1FC68, on a
# value (r3, a value, a
# particular value, a
# particular size / a
# particular allocation, r31, a
# particular value), a particular process / a
# particular insert, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a loop) loop over a range (a loop, a particular loop / a
# particular range, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over a range, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (if a value (r30, a value, a
# particular value, a
# particular size / a
# particular allocation) is < a value (r31, a value, a
# particular value, a
# particular size / a
# particular allocation), a particular value) continue the loop (a
# particular state, a
# particular result, a particular size / a
# particular allocation), and (otherwise) take one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation).

# Args: obj (r3, r28, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular object), dst (r4, r30, a value, a
# particular value, a
# particular size / a
# particular allocation, a destination), src (r5, r29, a value, a
# particular value, a
# particular size / a
# particular allocation, a source), (more). It does a process (a
# process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). In the heap insert, it does a heap insert (a
# process, a
# particular process / a
# particular insert, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, calling ProcessHeapInsert_82C1FC68, sub_82C1FC68, a process, on a
# value (r3, a value, a
# particular value, a
# particular size / a
# particular allocation, r31, a
# particular value), a particular process / a
# particular insert, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). In the loop, it loops over a range (a
# loop, a
# particular loop / a
# particular range, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over a range, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result). If a value (r30, a
# value, a
# particular value, a
# particular size / a
# particular allocation) is < a value (r31, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state), it continues the loop (a
# particular state, a
# particular result, a particular size / a
# particular allocation); otherwise, it takes one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation). So it's a "process (a
# process, with a
# heap insert and a
# loop: do a process (a process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a heap insert) do a heap insert (a
# process, a particular process / a
# particular insert, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, calling ProcessHeapInsert_82C1FC68, on a
# value (r3, a value, a
# particular value, a
# particular size / a
# particular allocation, r31, a
# particular value), a particular process / a
# particular insert, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a loop) loop over a range (a loop, a
# particular loop / a
# particular range, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over a range, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (if a value (r30, a value, a
# particular value, a
# particular size / a
# particular allocation) is < a value (r31, a value, a
# particular value, a
# particular size / a
# particular allocation), a particular value) continue the loop (a
# particular state, a
# particular result, a particular size / a
# particular allocation), and (otherwise) take one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation))" routine (a process routine, part of the 0x82C20 cluster, a sibling of
# ProcessHeapInsert_82C1FC68 and ProcessHeap_82C20330, a caller of ProcessHeapInsert_82C1FC68). 326 lines, 1
# callee (ProcessHeapInsert_82C1FC68). 1 caller: ProcessRecursive3_82C20680 (a
# process, which calls this to do the process, a
# particular size / a
# particular allocation, a
# particular operation). The object's class (obj, r28, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular object, a
# particular value /
# a particular state) and the value's meaning (dst, r30, a value, a
# particular value, a
# particular size / a
# particular allocation, a destination, src, r29, a value, a
# particular value, a
# particular size / a
# particular allocation, a source) aren't resolvable, so the name states the mechanism: a process (with a
# heap insert and a
# loop) (do a process (a process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a heap insert) do a heap insert (a
# process, a
# particular process / a
# particular insert, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, calling ProcessHeapInsert_82C1FC68, on a
# value (r3, a value, a
# particular value, a
# particular size / a
# particular allocation, r31, a
# particular value), a particular process / a
# particular insert, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a loop) loop over a range (a loop, a
# particular loop / a
# particular range, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over a range, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (if a value (r30, a value, a
# particular value, a
# particular size / a
# particular allocation) is < a value (r31, a value, a
# particular value, a
# particular size / a
# particular allocation), a particular value) continue the loop (a
# particular state, a
# particular result, a particular size / a
# particular allocation), and (otherwise) take one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation)). The "82C202A0" refers to the address (a
# particular address, a
# particular size / a
# particular allocation).
[entrypoint.functions."0x82C202A0"]
name = "ProcessHeap_82C202A0"

# A constant return (a constant return, with a constant, the sibling of ReturnConstant_1024_82A11E60 / ReturnConstant
# _0x10000_82A14660): return a constant (a constant return, a particular return / a
# particular value, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, 131072, a
# constant, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result).

# Args: (more). It returns a constant (a
# constant return, a
# particular return / a
# particular value, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, 131072, a
# constant, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result). So it's a "constant return (a
# constant return, with a
# constant: return a constant (a constant return, a
# particular return / a
# particular value, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, 131072, a
# constant, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result))" routine (a constant return routine, part of the 0x82A14 cluster, a sibling of
# ReturnConstant_1024_82A11E60 and ReturnConstant_0x10000_82A14660). 8
# lines, 0 callees. No
# direct callers (it's called via a vtable / it is registered-but-uncalled, part of the game's subsystems). The
# constant's value (131072, a
# constant, a
# particular value, a
# particular size / a
# particular allocation) isn't resolvable beyond its value, so the name states the mechanism: a constant return (with a
# constant) (return a constant (a constant return, a
# particular return / a
# particular value, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, 131072, a
# constant, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result)). The "131072" refers to the constant (131072, a
# constant, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result).
[entrypoint.functions."0x82A14668"]
name = "ReturnConstant_131072_82A14668"

# A destruct (a destruct, with a check and a free, the sibling of Destruct_822C97D0 / Destruct2_82B11990 / Destruct
# 20ByteFree2_82A14A20, the caller of Free_SizeBucketed_Thunk_8221BE68): destruct an object (a destruct, a
# particular destruct / a
# particular release, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, obj->24, a
# particular value, a
# particular state / a
# particular result), and (if a value (obj->24, a value, a
# particular value, a
# particular size / a
# particular allocation) is < 8, a
# particular value) free a value (Free_SizeBucketed_Thunk_8221BE68, on a
# value (obj->4, a value, a
# particular value, a
# particular size / a
# particular allocation), a particular free / a
# particular size, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (otherwise) take one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation), and store values (obj->20 = 0, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, obj->24 = 7, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, obj->4 = 0, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result).

# Args: obj (r3, r31, ->24 = a field, a value, ->4 = a field, a value, ->20 = a field, a
# value, a
# particular object), (more). It destructs an object (a
# destruct, a
# particular destruct / a
# particular release, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). In the check, it checks a value (a
# value, a
# particular value, a
# particular size / a
# particular allocation, obj->24, a
# particular value, a
# particular state / a
# particular result). If it is < 8 (a particular value, a
# particular state, a
# particular size / a
# particular allocation), it frees a value (Free_SizeBucketed_Thunk_8221BE68, sub_8221BE68, a
# free, on a
# value (obj->4, a value, a
# particular value, a
# particular size / a
# particular allocation), a particular free / a
# particular size, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result); if it is >= 8 (a particular value, a
# particular state, a
# particular size / a
# particular allocation), it takes one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation). It stores values (obj->20 = 0, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, obj->24 = 7, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, obj->4 = 0, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result). So it's a "destruct an object (a
# destruct, with a
# check and a
# free: destruct an object (a destruct, a
# particular destruct / a
# particular release, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, obj->24, a
# particular value, a
# particular state / a
# particular result), and (if a value (obj->24, a value, a
# particular value, a
# particular size / a
# particular allocation) is < 8, a
# particular value) free a value (Free_SizeBucketed_Thunk_8221BE68, on a
# value (obj->4, a value, a
# particular value, a
# particular size / a
# particular allocation), a particular free / a
# particular size, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (otherwise) take one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation), and store values (obj->20 = 0, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, obj->24 = 7, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, obj->4 = 0, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result))" routine (a destruct routine, part of the 0x822F5 cluster, a sibling of
# Destruct_822C97D0, Destruct2_82B11990, and Destruct20ByteFree2_82A14A20, a caller of Free_SizeBucketed_Thunk_8221BE68). 49
# lines, 1 callee (Free_SizeBucketed_Thunk_8221BE68). 10+ callers: sub_82374808, sub_823728E0, sub_82C638C0, sub_82C7AC28,
# sub_82C68B50, sub_82C71050, sub_82C70A00, sub_82EE2A48, sub_824EA150, sub_82C07AB0 (processes, which
# call this to destruct the object, a
# particular size / a
# particular allocation, a
# particular operation). The object's class (obj, r31, ->24 = a field, a value, ->4 = a field, a value, ->20 = a field, a
# value, a
# particular object, a
# particular value /
# a particular state) isn't resolvable, so the name states the mechanism: a destruct (with a
# check and a
# free) (destruct an object (a destruct, a
# particular destruct / a
# particular release, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, obj->24, a
# particular value, a
# particular state / a
# particular result), and (if a value (obj->24, a value, a
# particular value, a
# particular size / a
# particular allocation) is < 8, a
# particular value) free a value (Free_SizeBucketed_Thunk_8221BE68, on a
# value (obj->4, a value, a
# particular value, a
# particular size / a
# particular allocation), a particular free / a
# particular size, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (otherwise) take one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation), and store values (obj->20 = 0, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, obj->24 = 7, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, obj->4 = 0, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result)). The "3" distinguishes it from Destruct_822C97D0 and Destruct2_82B11990.
[entrypoint.functions."0x822F5208"]
name = "Destruct3_822F5208"

# A process (a process, with a check and an allocate, the sibling of ProcessAllocate_83230900 / Destruct3_822F5208, the
# caller of sub_82CC0CA0): do a process (a process, a particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, obj->8, a
# particular value, a
# particular state / a
# particular result), and (if a value (obj->8, a value, a
# particular value, a
# particular size / a
# particular allocation) is zero, a
# particular value) allocate a value (sub_82CC0CA0, on a
# value (0, a value, a
# particular value, a
# particular size / a
# particular allocation, 4096, a
# particular value, 4, a
# particular value, 128 | 4096, a
# particular value), a particular allocate / a
# particular size, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (otherwise) take one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation).

# Args: obj (r3, r31, ->8 = a field, a value, ->0 = a field, a value, a
# particular object), (more). It does a process (a
# process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). In the check, it checks a value (a
# value, a
# particular value, a
# particular size / a
# particular allocation, obj->8, a
# particular value, a
# particular state / a
# particular result). If it is zero (a null state, a
# particular state, a
# particular size / a
# particular allocation), it allocates a value (sub_82CC0CA0, sub_82CC0CA0, an
# allocate, on a
# value (0, a value, a
# particular value, a
# particular size / a
# particular allocation, 4096, a
# particular value, 4, a
# particular value, 128 | 4096, a
# particular value), a particular allocate / a
# particular size, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result); if it is non-zero (a particular value, a
# particular state, a
# particular size / a
# particular allocation), it takes one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation). So it's a "process (a
# process, with a
# check and an
# allocate: do a process (a process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, obj->8, a
# particular value, a
# particular state / a
# particular result), and (if a value (obj->8, a value, a
# particular value, a
# particular size / a
# particular allocation) is zero, a
# particular value) allocate a value (sub_82CC0CA0, on a
# value (0, a value, a
# particular value, a
# particular size / a
# particular allocation, 4096, a
# particular value, 4, a
# particular value, 128 | 4096, a
# particular value), a particular allocate / a
# particular size, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (otherwise) take one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation))" routine (a process routine, part of the 0x8322F cluster, a sibling of
# ProcessAllocate_83230900 and Destruct3_822F5208, a caller of sub_82CC0CA0). 145 lines, 1
# callee (sub_82CC0CA0). 4 callers: sub_83230900, ProcessPoolAcquire_83236230, sub_83230C18, FindNodeInList_83235FC0
# (processes, which call this to do the process, a
# particular size / a
# particular allocation, a
# particular operation). The object's class (obj, r31, ->8 = a field, a value, ->0 = a field, a value, a
# particular object, a
# particular value /
# a particular state) isn't resolvable, so the name states the mechanism: a process (with a
# check and an
# allocate) (do a process (a process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, obj->8, a
# particular value, a
# particular state / a
# particular result), and (if a value (obj->8, a value, a
# particular value, a
# particular size / a
# particular allocation) is zero, a
# particular value) allocate a value (sub_82CC0CA0, on a
# value (0, a value, a
# particular value, a
# particular size / a
# particular allocation, 4096, a
# particular value, 4, a
# particular value, 128 | 4096, a
# particular value), a particular allocate / a
# particular size, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (otherwise) take one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation)). The "8322FB30" refers to the address (a
# particular address, a
# particular size / a
# particular allocation).
[entrypoint.functions."0x8322FB30"]
name = "ProcessAllocate_8322FB30"

# A process (a process, with a decrement and a lookup, the sibling of ProcessDecrement_83230BC0 / ProcessAllocate_8322
# FB30, the caller of sub_82CC0CF0): do a process (a process, a particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a decrement) do a decrement (a
# process, a particular process / a
# particular decrement, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, r11->8 = r11->8 - 1, a
# store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (in a lookup) do a lookup (a lookup, a
# particular lookup / a
# particular value, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over obj->8, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (if a value (r11->8, a value, a
# particular value, a
# particular size / a
# particular allocation) is zero, a
# particular value) unlink a value (a store, a
# particular store / a
# particular value, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, r9->4 = r11->4, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, obj->8 = r11->4, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and tail-call a process (sub_82CC0CF0, on a
# value (r11, a value, a
# particular value, a
# particular size / a
# particular allocation, 0, a
# particular value, 32768, a
# particular value), a particular process, a
# particular operation, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result).

# Args: obj (r3, ->8 = a field, a value, a particular value, a particular size / a
# particular allocation, a
# particular object), a (r4, a value, a
# particular value, a
# particular size / a
# particular allocation), (more). It does a process (a
# process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). In the decrement, it does a decrement (a
# process, a
# particular process / a
# particular decrement, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, r11->8 = r11->8 - 1, a
# store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result). In the lookup, it does a lookup (a
# lookup, a
# particular lookup / a
# particular value, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over obj->8, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result). If a value (r11->8, a
# value, a
# particular value, a
# particular size / a
# particular allocation) is zero (a null state, a
# particular state, a
# particular size / a
# particular allocation), it unlinks a value (a
# store, a
# particular store / a
# particular value, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, r9->4 = r11->4, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, obj->8 = r11->4, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and tail-calls a process (sub_82CC0CF0, sub_82CC0CF0, a
# process, on a
# value (r11, a value, a
# particular value, a
# particular size / a
# particular allocation, 0, a
# particular value, 32768, a
# particular value), a particular process, a
# particular operation, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result). So it's a "process (a
# process, with a
# decrement and a
# lookup: do a process (a process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a decrement) do a decrement (a
# process, a particular process / a
# particular decrement, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, r11->8 = r11->8 - 1, a
# store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (in a lookup) do a lookup (a lookup, a
# particular lookup / a
# particular value, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over obj->8, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (if a value (r11->8, a value, a
# particular value, a
# particular size / a
# particular allocation) is zero, a
# particular value) unlink a value (a store, a
# particular store / a
# particular value, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, r9->4 = r11->4, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, obj->8 = r11->4, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and tail-call a process (sub_82CC0CF0, on a
# value (r11, a value, a
# particular value, a
# particular size / a
# particular allocation, 0, a
# particular value, 32768, a
# particular value), a particular process, a
# particular operation, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result))" routine (a process routine, part of the 0x8322F cluster, a sibling of
# ProcessDecrement_83230BC0 and ProcessAllocate_8322FB30, a caller of sub_82CC0CF0). 84 lines, 1
# callee (sub_82CC0CF0). 4 callers: sub_83230BC0, sub_829FD240, sub_83230FA0, sub_83230848 (processes, which
# call this to do the process, a
# particular size / a
# particular allocation, a
# particular operation). The object's class (obj, r3, ->8 = a field, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular object, a
# particular value /
# a particular state) and the value's meaning (a, r4, a value, a
# particular value, a
# particular size / a
# particular allocation) aren't resolvable, so the name states the mechanism: a process (with a
# decrement and a
# lookup) (do a process (a process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a decrement) do a decrement (a
# process, a
# particular process / a
# particular decrement, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, r11->8 = r11->8 - 1, a
# store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (in a lookup) do a lookup (a lookup, a
# particular lookup / a
# particular value, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over obj->8, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (if a value (r11->8, a value, a
# particular value, a
# particular size / a
# particular allocation) is zero, a
# particular value) unlink a value (a store, a
# particular store / a
# particular value, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, r9->4 = r11->4, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, obj->8 = r11->4, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and tail-call a process (sub_82CC0CF0, on a
# value (r11, a value, a
# particular value, a
# particular size / a
# particular allocation, 0, a
# particular value, 32768, a
# particular value), a particular process, a
# particular operation, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result)). The "8322FAA0" refers to the address (a
# particular address, a
# particular size / a
# particular allocation).
[entrypoint.functions."0x8322FAA0"]
name = "ProcessDecrement_8322FAA0"

# A process (a process, with a switch and a lookup, the sibling of ProcessDispatch_82BCD7B0 / ProcessDispatch3_82815C28,
# the caller of ConstructRefCounted_8222CF18 / DestructRefCounted_82214F08 / ProcessToLower_821EEA00): do a process (a
# process, a particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a switch) switch on a type (a switch, a
# particular switch / a
# particular type, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over type, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (in a lookup) do a lookup (a lookup, a
# particular lookup / a
# particular value, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over a table, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and store a value (obj->524 = r9->r7, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result).

# Args: obj (r3, r31, ->524 = a field, a value, a particular value, a particular size / a
# particular allocation, a
# particular object), type (r4, a value, a
# particular value, a
# particular size / a
# particular allocation), (more). It does a process (a
# process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). In the switch, it switches on a type (a
# switch, a
# particular switch / a
# particular type, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over type, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result). In the lookup, it does a lookup (a
# lookup, a
# particular lookup / a
# particular value, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over a table, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result). It stores a value (obj->524 = r9->r7, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result). So it's a "process (a
# process, with a
# switch and a
# lookup: do a process (a process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a switch) switch on a type (a switch, a
# particular switch / a
# particular type, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over type, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (in a lookup) do a lookup (a lookup, a
# particular lookup / a
# particular value, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over a table, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and store a value (obj->524 = r9->r7, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result))" routine (a process routine, part of the 0x82816 cluster, a sibling of
# ProcessDispatch_82BCD7B0 and ProcessDispatch3_82815C28, a caller of ConstructRefCounted_8222CF18, DestructRefCounted_82214F08,
# and ProcessToLower_821EEA00). 172 lines, 5
# callees (ConstructRefCounted_8222CF18, DestructRefCounted_82214F08, ProcessToLower_821EEA00, sub_821EC668, sub_82BFD780).
# 2 callers: sub_82188B88, sub_828152D0 (processes, which
# call this to do the process, a
# particular size / a
# particular allocation, a
# particular operation). The object's class (obj, r31, ->524 = a field, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular object, a
# particular value /
# a particular state) and the value's meaning (type, r4, a value, a
# particular value, a
# particular size / a
# particular allocation) aren't resolvable, so the name states the mechanism: a process (with a
# switch and a
# lookup) (do a process (a process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a switch) switch on a type (a switch, a
# particular switch / a
# particular type, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over type, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (in a lookup) do a lookup (a lookup, a
# particular lookup / a
# particular value, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over a table, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and store a value (obj->524 = r9->r7, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result)). The "4" distinguishes it from ProcessDispatch_82BCD7B0 and ProcessDispatch3_82815C28.
[entrypoint.functions."0x828164E8"]
name = "ProcessDispatch4_828164E8"

# A constant return (a constant return, with a constant, the sibling of ReturnConstant_1024_82A11E60 / ReturnConstant
# _131072_82A14668): return a constant (a constant return, a particular return / a
# particular value, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, 4096, a
# constant, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result).

# Args: (more). It returns a constant (a
# constant return, a
# particular return / a
# particular value, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, 4096, a
# constant, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result). So it's a "constant return (a
# constant return, with a
# constant: return a constant (a constant return, a
# particular return / a
# particular value, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, 4096, a
# constant, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result))" routine (a constant return routine, part of the 0x82A11 cluster, a sibling of
# ReturnConstant_1024_82A11E60 and ReturnConstant_131072_82A14668). 8
# lines, 0 callees. No
# direct callers (it's called via a vtable / it is registered-but-uncalled, part of the game's subsystems). The
# constant's value (4096, a
# constant, a
# particular value, a
# particular size / a
# particular allocation) isn't resolvable beyond its value, so the name states the mechanism: a constant return (with a
# constant) (return a constant (a constant return, a
# particular return / a
# particular value, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, 4096, a
# constant, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result)). The "4096" refers to the constant (4096, a
# constant, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result).
[entrypoint.functions."0x82A11F28"]
name = "ReturnConstant_4096_82A11F28"

# A trampoline (a trampoline, with a single call, the sibling of Trampoline_8217A978 / ReturnConstant_4096_82A11F28, the
# caller of LazyGetData_822F2088): do a trampoline (a trampoline, a particular trampoline, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a call) call a process (LazyGetData_822F2088, on a
# value (r3, a value, a
# particular value, a
# particular size / a
# particular allocation), a particular process, a
# particular operation, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result).

# Args: (r3, a value, a
# particular value, a
# particular size / a
# particular allocation), (more). It does a trampoline (a
# trampoline, a
# particular trampoline, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). In the call, it calls a process (LazyGetData_822F2088, sub_822F2088, a
# process, on a
# value (r3, a value, a
# particular value, a
# particular size / a
# particular allocation), a particular process, a
# particular operation, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result). So it's a "trampoline (a
# trampoline, with a
# single call: do a trampoline (a trampoline, a
# particular trampoline, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a call) call a process (LazyGetData_822F2088, on a
# value (r3, a value, a
# particular value, a
# particular size / a
# particular allocation), a particular process, a
# particular operation, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result))" routine (a trampoline routine, part of the 0x822C8 cluster, a sibling of
# Trampoline_8217A978 and ReturnConstant_4096_82A11F28, a caller of LazyGetData_822F2088). 7
# lines, 1 callee (LazyGetData_822F2088). 4 callers: sub_82C651D8, sub_82C65270, sub_82C65310, sub_82BFD780 (processes,
# which call this to do the trampoline, a
# particular size / a
# particular allocation, a
# particular operation). The value's meaning (r3, a value, a
# particular value, a
# particular size / a
# particular allocation) isn't resolvable, so the name states the mechanism: a trampoline (with a
# single call) (do a trampoline (a trampoline, a
# particular trampoline, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a call) call a process (LazyGetData_822F2088, on a
# value (r3, a value, a
# particular value, a
# particular size / a
# particular allocation), a particular process, a
# particular operation, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result)). The "2" distinguishes it from Trampoline_8217A978.
[entrypoint.functions."0x822C88A8"]
name = "Trampoline2_822C88A8"

# A process (a process, with a check and a construct and a free, the sibling of ProcessConstructString_82C651D8 / Tramp
# oline2_822C88A8, the caller of ConstructString_8217AB58 / Trampoline2_822C88A8 / Free_Thunk_824FE010): do a process (a
# process, a particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, obj->0, a
# particular value, a
# particular state / a
# particular result), and (in a construct) construct a string (a
# construct, a particular construct / a
# particular string, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, calling ConstructString_8217AB58, on a
# value (stack[80], a value, a
# particular value, a
# particular size / a
# particular allocation, r4, a
# particular value), a particular construct / a
# particular string, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a free) free a value (Free_Thunk_824FE010, on a
# value (stack[84], a value, a
# particular value, a
# particular size / a
# particular allocation), a particular free / a
# particular size, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result).

# Args: obj (r3, ->0 = a field, a value, a particular value, a particular size / a
# particular allocation, a
# particular object), (more). It does a process (a
# process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). In the check, it checks a value (a
# value, a
# particular value, a
# particular size / a
# particular allocation, obj->0, a
# particular value, a
# particular state / a
# particular result). In the construct, it constructs a string (a
# construct, a
# particular construct / a
# particular string, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, calling ConstructString_8217AB58, sub_8217AB58, a construct, on a
# value (stack[80], a value, a
# particular value, a
# particular size / a
# particular allocation, r4, a
# particular value), a particular construct / a
# particular string, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). In the free, it frees a value (Free_Thunk_824FE010, sub_824FE010, a
# free, on a
# value (stack[84], a value, a
# particular value, a
# particular size / a
# particular allocation), a particular free / a
# particular size, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). So it's a "process (a
# process, with a
# check and a
# construct and a
# free: do a process (a process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, obj->0, a
# particular value, a
# particular state / a
# particular result), and (in a construct) construct a string (a
# construct, a particular construct / a
# particular string, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, calling ConstructString_8217AB58, on a
# value (stack[80], a value, a
# particular value, a
# particular size / a
# particular allocation, r4, a
# particular value), a particular construct / a
# particular string, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a free) free a value (Free_Thunk_824FE010, on a
# value (stack[84], a value, a
# particular value, a
# particular size / a
# particular allocation), a particular free / a
# particular size, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result))" routine (a process routine, part of the 0x82BFD cluster, a sibling of
# ProcessConstructString_82C651D8 and Trampoline2_822C88A8, a caller of ConstructString_8217AB58, Trampoline2_822C88A8, and
# Free_Thunk_824FE010). 67 lines, 3
# callees (ConstructString_8217AB58, Free_Thunk_824FE010, sub_822C88A8). 10+ callers: sub_82557DE8, sub_82AAA530, sub
# _828815D8, sub_83260198, sub_83249CD0, sub_83249E50, sub_8240BA80, sub_82A1C7F0, sub_82819E70, sub_8235C3C0 (processes,
# which call this to do the process, a
# particular size / a
# particular allocation, a
# particular operation). The object's class (obj, r3, ->0 = a field, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular object, a
# particular value /
# a particular state) isn't resolvable, so the name states the mechanism: a process (with a
# check and a
# construct and a
# free) (do a process (a process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, obj->0, a
# particular value, a
# particular state / a
# particular result), and (in a construct) construct a string (a
# construct, a
# particular construct / a
# particular string, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, calling ConstructString_8217AB58, on a
# value (stack[80], a value, a
# particular value, a
# particular size / a
# particular allocation, r4, a
# particular value), a particular construct / a
# particular string, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a free) free a value (Free_Thunk_824FE010, on a
# value (stack[84], a value, a
# particular value, a
# particular size / a
# particular allocation), a particular free / a
# particular size, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result)). The "82BFD780" refers to the address (a
# particular address, a
# particular size / a
# particular allocation).
[entrypoint.functions."0x82BFD780"]
name = "ProcessConstructString_82BFD780"

# A process (a process, with a check and a string operation, the sibling of ProcessString_826C3EF0 / ProcessConstruct
# String_82BFD780, the caller of SetString_822F2020 / Allocate_OrDbgBreak_8221F388 / CopyAssign_RefCounted_821F0108):
# do a process (a process, a particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, a->8, a
# particular value, a
# particular state / a
# particular result), and (if a value (a->8, a value, a
# particular value, a
# particular size / a
# particular allocation) is >= a constant (r9, a
# particular value, a
# particular size / a
# particular allocation), a particular value) do a string operation (a
# string operation, a particular string operation, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, calling SetString_822F2020, on a
# value (stack[80], a value, a
# particular value, a
# particular size / a
# particular allocation, a constant, a
# particular value), a particular string operation, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (otherwise) take one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation).

# Args: obj (r3, r24, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular object), a (r4, r29, ->8 = a field, a value, a particular value, a particular size / a
# particular allocation, a
# particular object), b (r5, r26, a value, a
# particular value, a
# particular size / a
# particular allocation), c (r6, r31, a value, a
# particular value, a
# particular size / a
# particular allocation), d (r7, r28, a value, a
# particular value, a
# particular size / a
# particular allocation), (more). It does a process (a
# process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). In the check, it checks a value (a
# value, a
# particular value, a
# particular size / a
# particular allocation, a->8, a
# particular value, a
# particular state / a
# particular result). If it is >= a constant (a->8, a
# value, a
# particular value, a
# particular size / a
# particular allocation, r9, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state), it does a string operation (a
# string operation, a
# particular string operation, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, calling SetString_822F2020, sub_822F2020, a string operation, on a
# value (stack[80], a value, a
# particular value, a
# particular size / a
# particular allocation, a constant, a
# particular value), a particular string operation, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result); if it is < a constant (a particular value, a
# particular state, a
# particular size / a
# particular allocation), it takes one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation). So it's a "process (a
# process, with a
# check and a
# string operation: do a process (a process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, a->8, a
# particular value, a
# particular state / a
# particular result), and (if a value (a->8, a value, a
# particular value, a
# particular size / a
# particular allocation) is >= a constant (r9, a
# particular value, a
# particular size / a
# particular allocation), a particular value) do a string operation (a
# string operation, a particular string operation, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, calling SetString_822F2020, on a
# value (stack[80], a value, a
# particular value, a
# particular size / a
# particular allocation, a constant, a
# particular value), a particular string operation, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (otherwise) take one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation))" routine (a process routine, part of the 0x8246C cluster, a sibling of
# ProcessString_826C3EF0 and ProcessConstructString_82BFD780, a caller of SetString_822F2020, Allocate_OrDbgBreak_8221F388,
# and CopyAssign_RefCounted_821F0108). 343 lines, 9
# callees (Allocate_OrDbgBreak_8221F388, CopyAssign_RefCounted_821F0108, SetString_822F2020, sub_82171810, sub_822F1F00,
# sub_82301AE0, sub_8234D268, sub_826C3EF0, sub_826C3FA8). 1 caller: LookupString2_8246C010 (a
# process, which calls this to do the process, a
# particular size / a
# particular allocation, a
# particular operation). The object's class (obj, r24, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular object, a
# particular value /
# a particular state) and the value's meaning (a, r29, ->8 = a field, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular object, b, r26, a value, a
# particular value, a
# particular size / a
# particular allocation, c, r31, a value, a
# particular value, a
# particular size / a
# particular allocation, d, r28, a value, a
# particular value, a
# particular size / a
# particular allocation) aren't resolvable, so the name states the mechanism: a process (with a
# check and a
# string operation) (do a process (a process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, a->8, a
# particular value, a
# particular state / a
# particular result), and (if a value (a->8, a value, a
# particular value, a
# particular size / a
# particular allocation) is >= a constant (r9, a
# particular value, a
# particular size / a
# particular allocation), a particular value) do a string operation (a
# string operation, a
# particular string operation, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, calling SetString_822F2020, on a
# value (stack[80], a value, a
# particular value, a
# particular size / a
# particular allocation, a constant, a
# particular value), a particular string operation, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (otherwise) take one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation)). The "8246CF80" refers to the address (a
# particular address, a
# particular size / a
# particular allocation).
[entrypoint.functions."0x8246CF80"]
name = "ProcessString_8246CF80"

# A hash lookup (a process, with a hash and a check, the sibling of HashLookup_82B49320 / ProcessString_8246CF80): do a
# hash lookup (a process, a particular process / a
# particular lookup, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a hash) do a hash (a hash, a
# particular hash / a
# particular value, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, obj->36 ^ key, a
# compute, a
# particular compute / a
# particular operation, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, r11->60, a
# particular value, a
# particular state / a
# particular result), and (if a value (r11->60, a value, a
# particular value, a
# particular size / a
# particular allocation) is != 1, a
# particular value) take one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation), and (otherwise) do a lookup (a lookup, a
# particular lookup / a
# particular value, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over a table, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result).

# Args: obj (r3, ->36 = a field, a value, ->8 = a field, a value, a
# particular object), key (r4, a value, a
# particular value, a
# particular size / a
# particular allocation), (more). It does a hash lookup (a
# process, a
# particular process / a
# particular lookup, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). In the hash, it does a hash (a
# hash, a
# particular hash / a
# particular value, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, obj->36 ^ key, a
# compute, a
# particular compute / a
# particular operation, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result). In the check, it checks a value (a
# value, a
# particular value, a
# particular size / a
# particular allocation, r11->60, a
# particular value, a
# particular state / a
# particular result). If it is != 1 (a particular value, a
# particular state, a
# particular size / a
# particular allocation), it takes one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation); otherwise, it does a lookup (a
# lookup, a
# particular lookup / a
# particular value, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over a table, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result). So it's a "hash lookup (a
# process, with a
# hash and a
# check: do a hash lookup (a process, a
# particular process / a
# particular lookup, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a hash) do a hash (a hash, a
# particular hash / a
# particular value, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, obj->36 ^ key, a
# compute, a
# particular compute / a
# particular operation, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, r11->60, a
# particular value, a
# particular state / a
# particular result), and (if a value (r11->60, a value, a
# particular value, a
# particular size / a
# particular allocation) is != 1, a
# particular value) take one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation), and (otherwise) do a lookup (a lookup, a
# particular lookup / a
# particular value, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over a table, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result))" routine (a hash lookup routine, part of the 0x82B49 cluster, a sibling of
# HashLookup_82B49320 and ProcessString_8246CF80). 100 lines, 0
# callees. 6 callers: sub_82B48FE0, sub_82B497B8, sub_82B49600, sub_82B49320, sub_82B49730,
# ProcessChunk_TableDispatch_82B49110 (processes, which
# call this to do the hash lookup, a
# particular size / a
# particular allocation, a
# particular operation). The object's class (obj, r3, ->36 = a field, a value, ->8 = a field, a value, a
# particular object, a
# particular value /
# a particular state) and the value's meaning (key, r4, a value, a
# particular value, a
# particular size / a
# particular allocation) aren't resolvable, so the name states the mechanism: a hash lookup (with a
# hash and a
# check) (do a hash lookup (a process, a
# particular process / a
# particular lookup, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a hash) do a hash (a hash, a
# particular hash / a
# particular value, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, obj->36 ^ key, a
# compute, a
# particular compute / a
# particular operation, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, r11->60, a
# particular value, a
# particular state / a
# particular result), and (if a value (r11->60, a value, a
# particular value, a
# particular size / a
# particular allocation) is != 1, a
# particular value) take one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation), and (otherwise) do a lookup (a lookup, a
# particular lookup / a
# particular value, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over a table, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result)). The "82B49678" refers to the address (a
# particular address, a
# particular size / a
# particular allocation).
[entrypoint.functions."0x82B49678"]
name = "HashLookup_82B49678"

# A constant return (a constant return, with a constant, the sibling of ReturnConstant_1024_82A11E60 / ReturnConstant
# _4096_82A11F28): return a constant (a constant return, a particular return / a
# particular value, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, 16384, a
# constant, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result).

# Args: (more). It returns a constant (a
# constant return, a
# particular return / a
# particular value, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, 16384, a
# constant, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result). So it's a "constant return (a
# constant return, with a
# constant: return a constant (a constant return, a
# particular return / a
# particular value, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, 16384, a
# constant, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result))" routine (a constant return routine, part of the 0x82A14 cluster, a sibling of
# ReturnConstant_1024_82A11E60 and ReturnConstant_4096_82A11F28). 8
# lines, 0 callees. No
# direct callers (it's called via a vtable / it is registered-but-uncalled, part of the game's subsystems). The
# constant's value (16384, a
# constant, a
# particular value, a
# particular size / a
# particular allocation) isn't resolvable beyond its value, so the name states the mechanism: a constant return (with a
# constant) (return a constant (a constant return, a
# particular return / a
# particular value, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, 16384, a
# constant, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result)). The "16384" refers to the constant (16384, a
# constant, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result).
[entrypoint.functions."0x82A145F0"]
name = "ReturnConstant_16384_82A145F0"

# A process (a process, with a check and a call, the sibling of Process_82227934 / ReturnConstant_16384_82A145F0):
# do a process (a process, a particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, b, a
# particular value, d, a
# particular value, a
# particular state / a
# particular result), and (if a value (b, a value, a
# particular value, a
# particular size / a
# particular allocation) is non-zero, a
# particular value) or (if a value (d, a value, a
# particular value, a
# particular size / a
# particular allocation) is non-zero, a
# particular value) call a process (sub_828456C8, on a
# value (r30->27520, a value, a
# particular value, a
# particular size / a
# particular allocation, b, a
# particular value, a
# particular size / a
# particular allocation, c, a
# particular value, a
# particular size / a
# particular allocation, d, a
# particular value, a
# particular size / a
# particular allocation), a particular process, a
# particular operation, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (otherwise) take one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation).

# Args: a (r3, a value, a
# particular value, a
# particular size / a
# particular allocation), b (r4, r28, a value, a
# particular value, a
# particular size / a
# particular allocation), c (r5, r27, a value, a
# particular value, a
# particular size / a
# particular allocation), d (r6, r29, a value, a
# particular value, a
# particular size / a
# particular allocation), (more). It does a process (a
# process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). In the check, it checks a value (a
# value, a
# particular value, a
# particular size / a
# particular allocation, b, a
# particular value, d, a
# particular value, a
# particular state / a
# particular result). If a value (b, a
# value, a
# particular value, a
# particular size / a
# particular allocation) is non-zero (a particular value, a
# particular state, a
# particular size / a
# particular allocation) or a value (d, a
# value, a
# particular value, a
# particular size / a
# particular allocation) is non-zero (a particular value, a
# particular state, a
# particular size / a
# particular allocation), it calls a process (sub_828456C8, sub_828456C8, a
# process, on a
# value (r30->27520, a value, a
# particular value, a
# particular size / a
# particular allocation, b, a
# particular value, a
# particular size / a
# particular allocation, c, a
# particular value, a
# particular size / a
# particular allocation, d, a
# particular value, a
# particular size / a
# particular allocation), a particular process, a
# particular operation, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result); otherwise, it takes one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation). So it's a "process (a
# process, with a
# check and a
# call: do a process (a process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, b, a
# particular value, d, a
# particular value, a
# particular state / a
# particular result), and (if a value (b, a value, a
# particular value, a
# particular size / a
# particular allocation) is non-zero, a
# particular value) or (if a value (d, a value, a
# particular value, a
# particular size / a
# particular allocation) is non-zero, a
# particular value) call a process (sub_828456C8, on a
# value (r30->27520, a value, a
# particular value, a
# particular size / a
# particular allocation, b, a
# particular value, a
# particular size / a
# particular allocation, c, a
# particular value, a
# particular size / a
# particular allocation, d, a
# particular value, a
# particular size / a
# particular allocation), a particular process, a
# particular operation, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (otherwise) take one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation))" routine (a process routine, part of the 0x82227 cluster, a sibling of
# Process_82227934 and ReturnConstant_16384_82A145F0). 127 lines, 5
# callees (sub_82195930, sub_824149C8, sub_828456C8, sub_828457F0, sub_82CBBB58). No
# direct callers (it's called via a vtable / it is registered-but-uncalled, part of the game's subsystems). The
# value's meaning (a, r3, a value, a
# particular value, a
# particular size / a
# particular allocation, b, r28, a value, a
# particular value, a
# particular size / a
# particular allocation, c, r27, a value, a
# particular value, a
# particular size / a
# particular allocation, d, r29, a value, a
# particular value, a
# particular size / a
# particular allocation) isn't resolvable, so the name states the mechanism: a process (with a
# check and a
# call) (do a process (a process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, b, a
# particular value, d, a
# particular value, a
# particular state / a
# particular result), and (if a value (b, a value, a
# particular value, a
# particular size / a
# particular allocation) is non-zero, a
# particular value) or (if a value (d, a value, a
# particular value, a
# particular size / a
# particular allocation) is non-zero, a
# particular value) call a process (sub_828456C8, on a
# value (r30->27520, a value, a
# particular value, a
# particular size / a
# particular allocation, b, a
# particular value, a
# particular size / a
# particular allocation, c, a
# particular value, a
# particular size / a
# particular allocation, d, a
# particular value, a
# particular size / a
# particular allocation), a particular process, a
# particular operation, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (otherwise) take one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation)). The "82227868" refers to the address (a
# particular address, a
# particular size / a
# particular allocation).
[entrypoint.functions."0x82227868"]
name = "Process_82227868"

# A process (a process, with a check and an acquire, the sibling of PoolAllocate_828457F0 / Process_82227868, the caller
# of Pool_AcquireNode_83230688 / HeapAllocate_83231590 / FindSizeBucket_83231518): do a process (a process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, size, a
# particular value, a
# particular state / a
# particular result), and (if a value (size, a value, a
# particular value, a
# particular size / a
# particular allocation) is zero, a
# particular value) take one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation), and (otherwise) do an acquire (a
# process, a particular process / a
# particular acquire, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, calling Pool_AcquireNode_83230688, on a
# value (r10->r9, a value, a
# particular value, a
# particular size / a
# particular allocation), a particular process / a
# particular acquire, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result).

# Args: a (r3, a value, a
# particular value, a
# particular size / a
# particular allocation), b (r4, r28, a value, a
# particular value, a
# particular size / a
# particular allocation), c (r5, r27, a value, a
# particular value, a
# particular size / a
# particular allocation), size (r6, r31, a value, a
# particular value, a
# particular size / a
# particular allocation), (more). It does a process (a
# process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). In the check, it checks a value (a
# value, a
# particular value, a
# particular size / a
# particular allocation, size, a
# particular value, a
# particular state / a
# particular result). If it is zero (a null state, a
# particular state, a
# particular size / a
# particular allocation), it takes one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation); otherwise, it does an acquire (a
# process, a
# particular process / a
# particular acquire, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, calling Pool_AcquireNode_83230688, sub_83230688, a process, on a
# value (r10->r9, a value, a
# particular value, a
# particular size / a
# particular allocation), a particular process / a
# particular acquire, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). So it's a "process (a
# process, with a
# check and an
# acquire: do a process (a process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, size, a
# particular value, a
# particular state / a
# particular result), and (if a value (size, a value, a
# particular value, a
# particular size / a
# particular allocation) is zero, a
# particular value) take one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation), and (otherwise) do an acquire (a
# process, a particular process / a
# particular acquire, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, calling Pool_AcquireNode_83230688, on a
# value (r10->r9, a value, a
# particular value, a
# particular size / a
# particular allocation), a particular process / a
# particular acquire, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result))" routine (a process routine, part of the 0x82845 cluster, a sibling of
# PoolAllocate_828457F0 and Process_82227868, a caller of Pool_AcquireNode_83230688, HeapAllocate_83231590, and
# FindSizeBucket_83231518). 171 lines, 5
# callees (FindSizeBucket_83231518, HeapAllocate_83231590, MemCpy_SdkRuntime_82CA2C60, Pool_AcquireNode_83230688,
# Pool_FreeByAddress_832304E8). 1 caller: sub_82227868 (Process_82227868, a
# process, which calls this to do the process, a
# particular size / a
# particular allocation, a
# particular operation). The value's meaning (a, r3, a value, a
# particular value, a
# particular size / a
# particular allocation, b, r28, a value, a
# particular value, a
# particular size / a
# particular allocation, c, r27, a value, a
# particular value, a
# particular size / a
# particular allocation, size, r31, a value, a
# particular value, a
# particular size / a
# particular allocation) isn't resolvable, so the name states the mechanism: a process (with a
# check and an
# acquire) (do a process (a process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, size, a
# particular value, a
# particular state / a
# particular result), and (if a value (size, a value, a
# particular value, a
# particular size / a
# particular allocation) is zero, a
# particular value) take one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation), and (otherwise) do an acquire (a
# process, a
# particular process / a
# particular acquire, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, calling Pool_AcquireNode_83230688, on a
# value (r10->r9, a value, a
# particular value, a
# particular size / a
# particular allocation), a particular process / a
# particular acquire, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result)). The "828456C8" refers to the address (a
# particular address, a
# particular size / a
# particular allocation).
[entrypoint.functions."0x828456C8"]
name = "PoolAllocate_828456C8"

# A process (a process, with a loop and a check, the sibling of ProcessString_8246CF80 / PoolAllocate_828456C8, the
# caller of GetSubObjField4_821EEA78 / DestructRefCounted_82214F08 / ProcessAtomicIncrement_821E3950): do a process (a
# process, a particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a loop) loop over a range (a loop, a particular loop / a
# particular range, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over a string, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, r11, a
# particular value, a
# particular state / a
# particular result), and (if a value (r11, a value, a
# particular value, a
# particular size / a
# particular allocation) is == 32, a
# particular value) or (if a value (r11, a value, a
# particular value, a
# particular size / a
# particular allocation) is == 9, a
# particular value) or (if a value (r11, a value, a
# particular value, a
# particular size / a
# particular allocation) is == 13, a
# particular value) take one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation), and (otherwise) continue the loop (a
# particular state, a
# particular result, a particular size / a
# particular allocation).

# Args: obj (r3, r30, ->0 = a field, a value, a particular value, a particular size / a
# particular allocation, a
# particular object), (more). It does a process (a
# process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). In the loop, it loops over a range (a
# loop, a
# particular loop / a
# particular range, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over a string, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result). In the check, it checks a value (a
# value, a
# particular value, a
# particular size / a
# particular allocation, r11, a
# particular value, a
# particular state / a
# particular result). If it is == 32 (a particular value, a
# particular state, a
# particular size / a
# particular allocation) or == 9 (a particular value, a
# particular state, a
# particular size / a
# particular allocation) or == 13 (a particular value, a
# particular state, a
# particular size / a
# particular allocation), it takes one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation); otherwise, it continues the loop (a
# particular state, a
# particular result, a particular size / a
# particular allocation). So it's a "process (a
# process, with a
# loop and a
# check: do a process (a process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a loop) loop over a range (a loop, a
# particular loop / a
# particular range, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over a string, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, r11, a
# particular value, a
# particular state / a
# particular result), and (if a value (r11, a value, a
# particular value, a
# particular size / a
# particular allocation) is == 32, a
# particular value) or (if a value (r11, a value, a
# particular value, a
# particular size / a
# particular allocation) is == 9, a
# particular value) or (if a value (r11, a value, a
# particular value, a
# particular size / a
# particular allocation) is == 13, a
# particular value) take one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation), and (otherwise) continue the loop (a
# particular state, a
# particular result, a particular size / a
# particular allocation))" routine (a process routine, part of the 0x82B3A cluster, a sibling of
# ProcessString_8246CF80 and PoolAllocate_828456C8, a caller of GetSubObjField4_821EEA78, DestructRefCounted_82214F08,
# and ProcessAtomicIncrement_821E3950). 169 lines, 4
# callees (DestructRefCounted_82214F08, GetSubObjField4_821EEA78, ProcessAtomicIncrement_821E3950, Release_RefCounted_821C67D8). 9
# callers: sub_825F45A8, sub_82359BE8, sub_8281A568, sub_82355C08, sub_825F5238, sub_82607260, sub_82816618, sub_826935F0,
# sub_82817D28 (processes, which call this to do the process, a
# particular size / a
# particular allocation, a
# particular operation). The object's class (obj, r30, ->0 = a field, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular object, a
# particular value /
# a particular state) isn't resolvable, so the name states the mechanism: a process (with a
# loop and a
# check) (do a process (a process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a loop) loop over a range (a loop, a
# particular loop / a
# particular range, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over a string, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, r11, a
# particular value, a
# particular state / a
# particular result), and (if a value (r11, a value, a
# particular value, a
# particular size / a
# particular allocation) is == 32, a
# particular value) or (if a value (r11, a value, a
# particular value, a
# particular size / a
# particular allocation) is == 9, a
# particular value) or (if a value (r11, a value, a
# particular value, a
# particular size / a
# particular allocation) is == 13, a
# particular value) take one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation), and (otherwise) continue the loop (a
# particular state, a
# particular result, a particular size / a
# particular allocation)). The "2" distinguishes it from ProcessString_8246CF80.
[entrypoint.functions."0x82B3A448"]
name = "ProcessString2_82B3A448"

# A process (a process, with a loop and a check, the sibling of ProcessString_8246CF80 / ProcessString2_82B3A448, the
# caller of DestructRefCounted_82214F08 / ProcessAtomicIncrement_821E3950 / Release_RefCounted_821C67D8): do a process (a
# process, a particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a loop) loop over a range (a loop, a particular loop / a
# particular range, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over a string, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, r11, a
# particular value, a
# particular state / a
# particular result), and (if a value (r11, a value, a
# particular value, a
# particular size / a
# particular allocation) is == 32, a
# particular value) or (if a value (r11, a value, a
# particular value, a
# particular size / a
# particular allocation) is == 9, a
# particular value) or (if a value (r11, a value, a
# particular value, a
# particular size / a
# particular allocation) is == 13, a
# particular value) or (if a value (r11, a value, a
# particular value, a
# particular size / a
# particular allocation) is == 10, a
# particular value) take one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation), and (otherwise) continue the loop (a
# particular state, a
# particular result, a particular size / a
# particular allocation).

# Args: obj (r3, r31, ->0 = a field, a value, a particular value, a particular size / a
# particular allocation, a
# particular object), (more). It does a process (a
# process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). In the loop, it loops over a range (a
# loop, a
# particular loop / a
# particular range, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over a string, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result). In the check, it checks a value (a
# value, a
# particular value, a
# particular size / a
# particular allocation, r11, a
# particular value, a
# particular state / a
# particular result). If it is == 32 (a particular value, a
# particular state, a
# particular size / a
# particular allocation) or == 9 (a particular value, a
# particular state, a
# particular size / a
# particular allocation) or == 13 (a particular value, a
# particular state, a
# particular size / a
# particular allocation) or == 10 (a particular value, a
# particular state, a
# particular size / a
# particular allocation), it takes one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation); otherwise, it continues the loop (a
# particular state, a
# particular result, a particular size / a
# particular allocation). So it's a "process (a
# process, with a
# loop and a
# check: do a process (a process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a loop) loop over a range (a loop, a
# particular loop / a
# particular range, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over a string, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, r11, a
# particular value, a
# particular state / a
# particular result), and (if a value (r11, a value, a
# particular value, a
# particular size / a
# particular allocation) is == 32, a
# particular value) or (if a value (r11, a value, a
# particular value, a
# particular size / a
# particular allocation) is == 9, a
# particular value) or (if a value (r11, a value, a
# particular value, a
# particular size / a
# particular allocation) is == 13, a
# particular value) or (if a value (r11, a value, a
# particular value, a
# particular size / a
# particular allocation) is == 10, a
# particular value) take one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation), and (otherwise) continue the loop (a
# particular state, a
# particular result, a particular size / a
# particular allocation))" routine (a process routine, part of the 0x82B3A cluster, a sibling of
# ProcessString_8246CF80 and ProcessString2_82B3A448, a caller of DestructRefCounted_82214F08, ProcessAtomicIncrement_821E3950,
# and Release_RefCounted_821C67D8). 158 lines, 3
# callees (DestructRefCounted_82214F08, ProcessAtomicIncrement_821E3950, Release_RefCounted_821C67D8). 10 callers: sub_82817D28,
# sub_825F5238, sub_82355C08, sub_825F45A8, sub_82C47BC0, sub_826935F0, sub_8281A568, sub_82607260, sub_82359BE8,
# sub_82816618 (processes, which call this to do the process, a
# particular size / a
# particular allocation, a
# particular operation). The object's class (obj, r31, ->0 = a field, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular object, a
# particular value /
# a particular state) isn't resolvable, so the name states the mechanism: a process (with a
# loop and a
# check) (do a process (a process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a loop) loop over a range (a loop, a
# particular loop / a
# particular range, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over a string, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, r11, a
# particular value, a
# particular state / a
# particular result), and (if a value (r11, a value, a
# particular value, a
# particular size / a
# particular allocation) is == 32, a
# particular value) or (if a value (r11, a value, a
# particular value, a
# particular size / a
# particular allocation) is == 9, a
# particular value) or (if a value (r11, a value, a
# particular value, a
# particular size / a
# particular allocation) is == 13, a
# particular value) or (if a value (r11, a value, a
# particular value, a
# particular size / a
# particular allocation) is == 10, a
# particular value) take one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation), and (otherwise) continue the loop (a
# particular state, a
# particular result, a particular size / a
# particular allocation)). The "3" distinguishes it from ProcessString_8246CF80 and ProcessString2_82B3A448.
[entrypoint.functions."0x82B3A560"]
name = "ProcessString3_82B3A560"

# A process (a process, with a scan and a construct, the sibling of ProcessLine_82B47128 / ProcessString3_82B3A560, the
# caller of ScanLineLength_82B477F0 / ConstructRefCounted_8222CF18): do a process (a process, a particular process, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a scan) do a scan (a
# scan, a particular scan / a
# particular line, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, calling ScanLineLength_82B477F0, on a
# value (a, a value, a
# particular value, a
# particular size, a
# particular size / a
# particular allocation, r4, a
# particular value, a
# particular size / a
# particular allocation), a particular scan / a
# particular line, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a construct) construct a refcounted object (a
# construct, a particular construct / a
# particular refcounted object, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, calling ConstructRefCounted_8222CF18, on a
# value (obj, a value, a
# particular value, a
# particular size, a
# particular size / a
# particular allocation, r4, a
# particular value, a
# particular size / a
# particular allocation, r5, a
# particular value, a
# particular size / a
# particular allocation), a particular construct / a
# particular refcounted object, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result).

# Args: obj (r3, r31, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular object), a (r4, r5, ->0 = a field, a value, ->36 = a field, a value, a
# particular object), (more). It does a process (a
# process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). In the scan, it does a scan (a
# scan, a
# particular scan / a
# particular line, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, calling ScanLineLength_82B477F0, sub_82B477F0, a scan, on a
# value (a, a value, a
# particular value, a
# particular size, a
# particular size / a
# particular allocation, r4, a
# particular value, a
# particular size / a
# particular allocation), a particular scan / a
# particular line, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). In the construct, it constructs a refcounted object (a
# construct, a
# particular construct / a
# particular refcounted object, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, calling ConstructRefCounted_8222CF18, sub_8222CF18, a construct, on a
# value (obj, a value, a
# particular value, a
# particular size, a
# particular size / a
# particular allocation, r4, a
# particular value, a
# particular size / a
# particular allocation, r5, a
# particular value, a
# particular size / a
# particular allocation), a particular construct / a
# particular refcounted object, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). So it's a "process (a
# process, with a
# scan and a
# construct: do a process (a process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a scan) do a scan (a
# scan, a particular scan / a
# particular line, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, calling ScanLineLength_82B477F0, on a
# value (a, a value, a
# particular value, a
# particular size, a
# particular size / a
# particular allocation, r4, a
# particular value, a
# particular size / a
# particular allocation), a particular scan / a
# particular line, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a construct) construct a refcounted object (a
# construct, a particular construct / a
# particular refcounted object, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, calling ConstructRefCounted_8222CF18, on a
# value (obj, a value, a
# particular value, a
# particular size, a
# particular size / a
# particular allocation, r4, a
# particular value, a
# particular size / a
# particular allocation, r5, a
# particular value, a
# particular size / a
# particular allocation), a particular construct / a
# particular refcounted object, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result))" routine (a process routine, part of the 0x82B47 cluster, a sibling of
# ProcessLine_82B47128 and ProcessString3_82B3A560, a caller of ScanLineLength_82B477F0 and ConstructRefCounted_8222CF18). 69
# lines, 2 callees (ConstructRefCounted_8222CF18, ScanLineLength_82B477F0). 8 callers: sub_82A18998, sub_82607260, sub
# _828152D0, sub_825F5238, sub_826935F0, sub_82817D28, sub_82359BE8, sub_82355C08 (processes, which
# call this to do the process, a
# particular size / a
# particular allocation, a
# particular operation). The object's class (obj, r31, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular object, a
# particular value /
# a particular state) and the value's meaning (a, r5, ->0 = a field, a value, ->36 = a field, a value, a
# particular object) aren't resolvable, so the name states the mechanism: a process (with a
# scan and a
# construct) (do a process (a process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a scan) do a scan (a
# scan, a
# particular scan / a
# particular line, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, calling ScanLineLength_82B477F0, on a
# value (a, a value, a
# particular value, a
# particular size, a
# particular size / a
# particular allocation, r4, a
# particular value, a
# particular size / a
# particular allocation), a particular scan / a
# particular line, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a construct) construct a refcounted object (a
# construct, a
# particular construct / a
# particular refcounted object, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, calling ConstructRefCounted_8222CF18, on a
# value (obj, a value, a
# particular value, a
# particular size, a
# particular size / a
# particular allocation, r4, a
# particular value, a
# particular size / a
# particular allocation, r5, a
# particular value, a
# particular size / a
# particular allocation), a particular construct / a
# particular refcounted object, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result)). The "82B470D0" refers to the address (a
# particular address, a
# particular size / a
# particular allocation).
[entrypoint.functions."0x82B470D0"]
name = "ProcessLine_82B470D0"

# A process (a process, with a check and a list manipulation, the sibling of ListUnlink_82B5D930 / ProcessLine_82B470D0):
# do a process (a process, a particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, r10->21, a
# particular value, a
# particular state / a
# particular result), and (in a list manipulation) do a list manipulation (a
# list manipulation, a particular list manipulation, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, node->8 = r11->0, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, r10->4 = node, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, r11->4 = node->4, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, r10->4 = r11, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, r11->0 = node, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, node->4 = r11, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result).

# Args: a (r3, a value, a
# particular value, a
# particular size / a
# particular allocation), node (r4, ->8 = a field, a value, ->4 = a field, a
# value, a
# particular object), (more). It does a process (a
# process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). In the check, it checks a value (a
# value, a
# particular value, a
# particular size / a
# particular allocation, r10->21, a
# particular value, a
# particular state / a
# particular result). In the list manipulation, it does a list manipulation (a
# list manipulation, a
# particular list manipulation, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, node->8 = r11->0, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, r10->4 = node, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, r11->4 = node->4, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, r10->4 = r11, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, r11->0 = node, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, node->4 = r11, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result). So it's a "process (a
# process, with a
# check and a
# list manipulation: do a process (a process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, r10->21, a
# particular value, a
# particular state / a
# particular result), and (in a list manipulation) do a list manipulation (a
# list manipulation, a particular list manipulation, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, node->8 = r11->0, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, r10->4 = node, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, r11->4 = node->4, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, r10->4 = r11, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, r11->0 = node, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, node->4 = r11, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result))" routine (a list manipulation routine, part of the 0x82B5E cluster, a sibling of
# ListUnlink_82B5D930 and ProcessLine_82B470D0). 71 lines, 0
# callees. 2 callers: sub_82B5D930, ProcessChain4_82B5E558 (processes, which
# call this to do the list manipulation, a
# particular size / a
# particular allocation, a
# particular operation). The object's class (node, r4, ->8 = a field, a value, ->4 = a field, a
# value, a
# particular object, a
# particular value /
# a particular state) and the value's meaning (a, r3, a value, a
# particular value, a
# particular size / a
# particular allocation) aren't resolvable, so the name states the mechanism: a process (with a
# check and a
# list manipulation) (do a process (a process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, r10->21, a
# particular value, a
# particular state / a
# particular result), and (in a list manipulation) do a list manipulation (a
# list manipulation, a
# particular list manipulation, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, node->8 = r11->0, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, r10->4 = node, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, r11->4 = node->4, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, r10->4 = r11, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, r11->0 = node, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, node->4 = r11, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result)). The "82B5E7F8" refers to the address (a
# particular address, a
# particular size / a
# particular allocation).
[entrypoint.functions."0x82B5E7F8"]
name = "ListUnlink_82B5E7F8"

# A destruct (a destruct, with a loop and a free, the sibling of Destruct_822C97D0 / Destruct3_822F5208 / ListUnlink
# _82B5E7F8, the caller of Free_SizeBucketed_Thunk_8221BE68): destruct an object (a destruct, a
# particular destruct / a
# particular release, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a loop) loop over a range (a loop, a particular loop / a
# particular range, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over obj->4, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (in a free) free a value (Free_SizeBucketed_Thunk_8221BE68, on a
# value (r3->0, a value, a
# particular value, a
# particular size / a
# particular allocation), a particular free / a
# particular size, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and store values (obj->8 = 0, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, obj->4 = 0, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result).

# Args: obj (r3, r31, ->4 = a field, a value, ->8 = a field, a value, a
# particular object), (more). It destructs an object (a
# destruct, a
# particular destruct / a
# particular release, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). In the loop, it loops over a range (a
# loop, a
# particular loop / a
# particular range, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over obj->4, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result). In the free, it frees a value (Free_SizeBucketed_Thunk_8221BE68, sub_8221BE68, a
# free, on a
# value (r3->0, a value, a
# particular value, a
# particular size / a
# particular allocation), a particular free / a
# particular size, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). It stores values (obj->8 = 0, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, obj->4 = 0, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result). So it's a "destruct an object (a
# destruct, with a
# loop and a
# free: destruct an object (a destruct, a
# particular destruct / a
# particular release, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a loop) loop over a range (a loop, a
# particular loop / a
# particular range, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over obj->4, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (in a free) free a value (Free_SizeBucketed_Thunk_8221BE68, on a
# value (r3->0, a value, a
# particular value, a
# particular size / a
# particular allocation), a particular free / a
# particular size, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and store values (obj->8 = 0, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, obj->4 = 0, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result))" routine (a destruct routine, part of the 0x8264E cluster, a sibling of
# Destruct_822C97D0, Destruct3_822F5208, and ListUnlink_82B5E7F8, a caller of Free_SizeBucketed_Thunk_8221BE68). 63
# lines, 1 callee (Free_SizeBucketed_Thunk_8221BE68). 10 callers: sub_826928F8, sub_82B3BAC0, sub_832ABDC0, sub_82817D28,
# sub_82768B70, sub_82A45F58, sub_8268A9A0, sub_82647328, sub_82A46EA8, sub_82359BE8 (processes, which
# call this to destruct the object, a
# particular size / a
# particular allocation, a
# particular operation). The object's class (obj, r31, ->4 = a field, a value, ->8 = a field, a value, a
# particular object, a
# particular value /
# a particular state) isn't resolvable, so the name states the mechanism: a destruct (with a
# loop and a
# free) (destruct an object (a destruct, a
# particular destruct / a
# particular release, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a loop) loop over a range (a loop, a
# particular loop / a
# particular range, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over obj->4, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (in a free) free a value (Free_SizeBucketed_Thunk_8221BE68, on a
# value (r3->0, a value, a
# particular value, a
# particular size / a
# particular allocation), a particular free / a
# particular size, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and store values (obj->8 = 0, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, obj->4 = 0, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result)). The "4" distinguishes it from Destruct_822C97D0 and Destruct3_822F5208.
[entrypoint.functions."0x8264EDF8"]
name = "Destruct4_8264EDF8"

# A return address (a return address, with a constant, the sibling of ReturnPointer_82C3BE78 / ReturnPointer2_82C2EDE8):
# return a pointer (a return address, a particular return / a
# particular pointer, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, from a fixed .data address + 19784, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result).

# Args: (more). It returns a pointer (a
# return address, a
# particular return / a
# particular pointer, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, from a fixed .data address + 19784, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result). So it's a "return address (a
# return address, with a
# constant: return a pointer (a return address, a
# particular return / a
# particular pointer, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, from a fixed .data address + 19784, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result))" routine (a return address routine, part of the 0x82C21 cluster, a sibling of
# ReturnPointer_82C3BE78 and ReturnPointer2_82C2EDE8). 10
# lines, 0 callees. No
# direct callers (it's called via a vtable / it is registered-but-uncalled, part of the game's subsystems). The
# pointer's meaning (r3, a pointer, a
# particular value, a
# particular size / a
# particular allocation, from a fixed .data address + 19784, a
# particular value) isn't resolvable, so the name states the mechanism: a return address (with a
# constant) (return a pointer (a return address, a
# particular return / a
# particular pointer, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, from a fixed .data address + 19784, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result)). The "3" distinguishes it from ReturnPointer_82C3BE78 and ReturnPointer2_82C2EDE8.
[entrypoint.functions."0x82C21F28"]
name = "ReturnPointer3_82C21F28"

# A process (a process, with a loop and a check, the sibling of ProcessAtomicIncrement_821E3950 / ReturnPointer3
# _82C21F28, the caller of Allocate_OrDbgBreak_8221F388 / CopyAssign_RefCounted2_82265160): do a process (a process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a loop) loop over a range (a loop, a particular loop / a
# particular range, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over r7->0, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, r7->0, a
# particular value, a
# particular state / a
# particular result), and (in an atomic increment) increment a value (r7->0, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, via a global lock, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result).

# Args: obj (r3, r31, ->8 = a field, a value, a
# particular object), a (r4, r28, a value, a
# particular value, a
# particular size / a
# particular allocation), b (r6, r26, a value, a
# particular value, a
# particular size / a
# particular allocation), (more). It does a process (a
# process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). In the loop, it loops over a range (a
# loop, a
# particular loop / a
# particular range, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over r7->0, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result). In the check, it checks a value (a
# value, a
# particular value, a
# particular size / a
# particular allocation, r7->0, a
# particular value, a
# particular state / a
# particular result). In the atomic increment, it increments a value (r7->0, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, via a global lock, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). So it's a "process (a
# process, with a
# loop and a
# check: do a process (a process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a loop) loop over a range (a loop, a
# particular loop / a
# particular range, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over r7->0, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, r7->0, a
# particular value, a
# particular state / a
# particular result), and (in an atomic increment) increment a value (r7->0, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, via a global lock, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result))" routine (a process routine, part of the 0x82354 cluster, a sibling of
# ProcessAtomicIncrement_821E3950 and ReturnPointer3_82C21F28, a caller of Allocate_OrDbgBreak_8221F388 and
# CopyAssign_RefCounted2_82265160). 168 lines, 2
# callees (Allocate_OrDbgBreak_8221F388, CopyAssign_RefCounted2_82265160). 10 callers: sub_82A185C8, sub_82B3BAC0,
# sub_826928F8, sub_828152D0, sub_8281A568, sub_82A18998, sub_825F45A8, sub_826939A0, sub_82466098, sub_824E3FA0 (processes,
# which call this to do the process, a
# particular size / a
# particular allocation, a
# particular operation). The object's class (obj, r31, ->8 = a field, a value, a
# particular object, a
# particular value /
# a particular state) isn't resolvable, so the name states the mechanism: a process (with a
# loop and a
# check) (do a process (a process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a loop) loop over a range (a loop, a
# particular loop / a
# particular range, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over r7->0, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, r7->0, a
# particular value, a
# particular state / a
# particular result), and (in an atomic increment) increment a value (r7->0, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, via a global lock, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result)). The "2" distinguishes it from ProcessAtomicIncrement_821E3950.
[entrypoint.functions."0x82354880"]
name = "ProcessAtomic2_82354880"

# A process (a process, with a loop and a check, the sibling of Process12Byte_82601D80 / ProcessAtomic2_82354880, the
# caller of sub_82601D80): do a process (a process, a particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a loop) loop over a range (a loop, a particular loop / a
# particular range, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over a 12-byte element, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, r8, a
# particular value, a
# particular state / a
# particular result), and (in a store) store a value (a
# value, a
# particular value, a
# particular size / a
# particular allocation, a->8->0 = b->0, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, a->8->4 = b->4, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result).

# Args: a (r3, ->4 = a field, a value, ->8 = a field, a value, ->12 = a field, a
# value, a
# particular object), b (r4, r6, ->0 = a field, a value, ->4 = a field, a
# value, a
# particular object), (more). It does a process (a
# process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). In the loop, it loops over a range (a
# loop, a
# particular loop / a
# particular range, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over a 12-byte element, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result). In the check, it checks a value (a
# value, a
# particular value, a
# particular size / a
# particular allocation, r8, a
# particular value, a
# particular state / a
# particular result). In the store, it stores a value (a
# value, a
# particular value, a
# particular size / a
# particular allocation, a->8->0 = b->0, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, a->8->4 = b->4, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result). So it's a "process (a
# process, with a
# loop and a
# check: do a process (a process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a loop) loop over a range (a loop, a
# particular loop / a
# particular range, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over a 12-byte element, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, r8, a
# particular value, a
# particular state / a
# particular result), and (in a store) store a value (a
# value, a
# particular value, a
# particular size / a
# particular allocation, a->8->0 = b->0, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, a->8->4 = b->4, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result))" routine (a process routine, part of the 0x82601 cluster, a sibling of
# Process12Byte_82601D80 and ProcessAtomic2_82354880, a caller of sub_82601D80). 126 lines, 1
# callee (sub_82601D80). 10 callers: sub_829CABD0, sub_82364EB8, sub_8281AE60, sub_82365458, sub_825F86A8, sub_82363150,
# sub_825F5238, sub_8226BE28, sub_8281A568, sub_82921518 (processes, which
# call this to do the process, a
# particular size / a
# particular allocation, a
# particular operation). The object's class (a, r3, ->4 = a field, a value, ->8 = a field, a value, ->12 = a field, a
# value, a
# particular object, a
# particular value /
# a particular state) and the value's meaning (b, r6, ->0 = a field, a value, ->4 = a field, a value, a
# particular object) aren't resolvable, so the name states the mechanism: a process (with a
# loop and a
# check) (do a process (a process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a loop) loop over a range (a loop, a
# particular loop / a
# particular range, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over a 12-byte element, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, r8, a
# particular value, a
# particular state / a
# particular result), and (in a store) store a value (a
# value, a
# particular value, a
# particular size / a
# particular allocation, a->8->0 = b->0, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, a->8->4 = b->4, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result)). The "826016B0" refers to the address (a
# particular address, a
# particular size / a
# particular allocation).
[entrypoint.functions."0x826016B0"]
name = "Process12Byte_826016B0"

# A process (a process, with a loop and a check, the sibling of ProcessSize_82BC9860 / Process12Byte_826016B0, the
# caller of DispatchAndAdjustPos_8227BB58 / sub_82BC9860): do a process (a process, a particular process, a particular
# size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a loop) loop over a range (a loop, a particular loop / a
# particular range, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over r11, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, c, a
# particular value, a
# particular state / a
# particular result), and (in a store) store a value (a
# value, a
# particular value, a
# particular size / a
# particular allocation, b->16 = r10, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result).

# Args: a (r3, r28, a value, a
# particular value, a
# particular size / a
# particular allocation), b (r4, r29, ->16 = a field, a value, a
# particular object), c (r5, r30, a value, a
# particular value, a
# particular size / a
# particular allocation), (more). It does a process (a
# process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). In the loop, it loops over a range (a
# loop, a
# particular loop / a
# particular range, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over r11, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result). In the check, it checks a value (a
# value, a
# particular value, a
# particular size / a
# particular allocation, c, a
# particular value, a
# particular state / a
# particular result). In the store, it stores a value (a
# value, a
# particular value, a
# particular size / a
# particular allocation, b->16 = r10, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result). So it's a "process (a
# process, with a
# loop and a
# check: do a process (a process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a loop) loop over a range (a loop, a
# particular loop / a
# particular range, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over r11, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, c, a
# particular value, a
# particular state / a
# particular result), and (in a store) store a value (a
# value, a
# particular value, a
# particular size / a
# particular allocation, b->16 = r10, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result))" routine (a process routine, part of the 0x82BC9 cluster, a sibling of
# ProcessSize_82BC9860 and Process12Byte_826016B0, a caller of DispatchAndAdjustPos_8227BB58 and sub_82BC9860). 172
# lines, 2 callees (DispatchAndAdjustPos_8227BB58, sub_82BC9860). 2 callers: sub_82BC9FD0, sub_82BCA340 (processes,
# which call this to do the process, a
# particular size / a
# particular allocation, a
# particular operation). The object's class (b, r29, ->16 = a field, a value, a
# particular object, a
# particular value /
# a particular state) and the value's meaning (a, r28, a value, a
# particular value, a
# particular size / a
# particular allocation; c, r30, a value, a
# particular value, a
# particular size / a
# particular allocation) aren't resolvable, so the name states the mechanism: a process (with a
# loop and a
# check) (do a process (a process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a loop) loop over a range (a loop, a
# particular loop / a
# particular range, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over r11, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, c, a
# particular value, a
# particular state / a
# particular result), and (in a store) store a value (a
# value, a
# particular value, a
# particular size / a
# particular allocation, b->16 = r10, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result)). The "82BC9EA0" refers to the address (a
# particular address, a
# particular size / a
# particular allocation).
[entrypoint.functions."0x82BC9EA0"]
name = "ProcessSize_82BC9EA0"

# A process (a process, with a check and a call, the sibling of ProcessFloat_82BC4328 / ProcessSize_82BC9EA0, the
# caller of MemCpy_SdkRuntime_82CA2C60 / sub_82BC4328): do a process (a process, a particular process, a particular
# size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, a->40, a
# particular value, a
# particular state / a
# particular result), and (in a call) call a process (sub_82BC4328, on a
# value (a, a value, a
# particular value, a
# particular size / a
# particular allocation, r4, a
# particular value, a
# particular size / a
# particular allocation, b, a
# particular value, a
# particular size / a
# particular allocation, f0, a
# particular value, a
# particular size / a
# particular allocation), a particular call / a
# particular process, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a compute) do a compute (a
# compute, a
# particular compute / a
# particular float, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, f10 = f0 * f11, a float multiply, a
# value, a
# particular value, a
# particular size / a
# particular allocation).

# Args: a (r3, r30, ->36 = a field, a value, ->40 = a field, a value, ->44 = a float, a
# particular object), b (r4, r29, a value, a
# particular value, a
# particular size / a
# particular allocation), (more). It does a process (a
# process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). In the check, it checks a value (a
# value, a
# particular value, a
# particular size / a
# particular allocation, a->40, a
# particular value, a
# particular state / a
# particular result). In the call, it calls a process (sub_82BC4328, on a
# value (a, a value, a
# particular value, a
# particular size / a
# particular allocation, r4, a
# particular value, a
# particular size / a
# particular allocation, b, a
# particular value, a
# particular size / a
# particular allocation, f0, a
# particular value, a
# particular size / a
# particular allocation), a particular call / a
# particular process, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). In the compute, it does a compute (a
# compute, a
# particular compute / a
# particular float, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, f10 = f0 * f11, a float multiply, a
# value, a
# particular value, a
# particular size / a
# particular allocation). So it's a "process (a
# process, with a
# check and a
# call: do a process (a process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, a->40, a
# particular value, a
# particular state / a
# particular result), and (in a call) call a process (sub_82BC4328, on a
# value (a, a value, a
# particular value, a
# particular size / a
# particular allocation, r4, a
# particular value, a
# particular size / a
# particular allocation, b, a
# particular value, a
# particular size / a
# particular allocation, f0, a
# particular value, a
# particular size / a
# particular allocation), a particular call / a
# particular process, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a compute) do a compute (a
# compute, a
# particular compute / a
# particular float, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, f10 = f0 * f11, a float multiply, a
# value, a
# particular value, a
# particular size / a
# particular allocation))" routine (a process routine, part of the 0x8229D cluster, a sibling of
# ProcessFloat_82BC4328 and ProcessSize_82BC9EA0, a caller of MemCpy_SdkRuntime_82CA2C60 and sub_82BC4328). 122
# lines, 2 callees (MemCpy_SdkRuntime_82CA2C60, sub_82BC4328). 2 callers: sub_8229DEE0, sub_82BC3248 (processes,
# which call this to do the process, a
# particular size / a
# particular allocation, a
# particular operation). The object's class (a, r30, ->36 = a field, a value, ->40 = a field, a value, ->44 = a float, a
# particular object, a
# particular value /
# a particular state) and the value's meaning (b, r29, a value, a
# particular value, a
# particular size / a
# particular allocation) aren't resolvable, so the name states the mechanism: a process (with a
# check and a
# call) (do a process (a process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, a->40, a
# particular value, a
# particular state / a
# particular result), and (in a call) call a process (sub_82BC4328, on a
# value (a, a value, a
# particular value, a
# particular size / a
# particular allocation, r4, a
# particular value, a
# particular size / a
# particular allocation, b, a
# particular value, a
# particular size / a
# particular allocation, f0, a
# particular value, a
# particular size / a
# particular allocation), a particular call / a
# particular process, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a compute) do a compute (a
# compute, a
# particular compute / a
# particular float, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, f10 = f0 * f11, a float multiply, a
# value, a
# particular value, a
# particular size / a
# particular allocation)). The "8229DE10" refers to the address (a
# particular address, a
# particular size / a
# particular allocation).
[entrypoint.functions."0x8229DE10"]
name = "ProcessFloat_8229DE10"

# A process (a process, with a loop and a check, the sibling of ProcessSpecial_82239E88 / ProcessFloat_8229DE10, the
# caller of ComputeSpecialFunction_82239E88 / ComputeSpecialFunction2_82239F68): do a process (a process, a particular
# process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a loop) loop over a range (a loop, a particular loop / a
# particular range, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over a value (r9, a value, a
# particular value, a
# particular size / a
# particular allocation), a particular loop / a
# particular range, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, r3, a
# particular value, a
# particular state / a
# particular result), and (in a compute) do a compute (a
# compute, a
# particular compute / a
# particular float / a
# particular bit, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, a float multiply, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a bit operation, a
# value, a
# particular value, a
# particular size / a
# particular allocation).

# Args: a (r3, a value, a
# particular value, a
# particular size / a
# particular allocation), b (r4, a value, a
# particular value, a
# particular size / a
# particular allocation), c (r5, a value, a
# particular value, a
# particular size / a
# particular allocation), d (r6, r31, a value, a
# particular value, a
# particular size / a
# particular allocation), (more). It does a process (a
# process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). In the loop, it loops over a range (a
# loop, a
# particular loop / a
# particular range, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over a value (r9, a value, a
# particular value, a
# particular size / a
# particular allocation), a particular loop / a
# particular range, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). In the check, it checks a value (a
# value, a
# particular value, a
# particular size / a
# particular allocation, r3, a
# particular value, a
# particular state / a
# particular result). In the compute, it does a compute (a
# compute, a
# particular compute / a
# particular float / a
# particular bit, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, a float multiply, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a bit operation, a
# value, a
# particular value, a
# particular size / a
# particular allocation). So it's a "process (a
# process, with a
# loop and a
# check: do a process (a process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a loop) loop over a range (a loop, a
# particular loop / a
# particular range, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over a value (r9, a value, a
# particular value, a
# particular size / a
# particular allocation), a particular loop / a
# particular range, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, r3, a
# particular value, a
# particular state / a
# particular result), and (in a compute) do a compute (a
# compute, a
# particular compute / a
# particular float / a
# particular bit, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, a float multiply, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a bit operation, a
# value, a
# particular value, a
# particular size / a
# particular allocation))" routine (a process routine, part of the 0x821E4 cluster, a sibling of
# ProcessSpecial_82239E88 and ProcessFloat_8229DE10, a caller of ComputeSpecialFunction_82239E88 and
# ComputeSpecialFunction2_82239F68). 412 lines, 2
# callees (ComputeSpecialFunction_82239E88, ComputeSpecialFunction2_82239F68). 1 caller: sub_822D8FF8 (a
# process, which calls this to do the process, a
# particular size / a
# particular allocation, a
# particular operation). The object's class (a, r3, a value, a
# particular value, a
# particular size / a
# particular allocation; b, r4, a value, a
# particular value, a
# particular size / a
# particular allocation; c, r5, a value, a
# particular value, a
# particular size / a
# particular allocation; d, r31, a value, a
# particular value, a
# particular size / a
# particular allocation) isn't resolvable, so the name states the mechanism: a process (with a
# loop and a
# check) (do a process (a process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a loop) loop over a range (a loop, a
# particular loop / a
# particular range, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over a value (r9, a value, a
# particular value, a
# particular size / a
# particular allocation), a particular loop / a
# particular range, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, r3, a
# particular value, a
# particular state / a
# particular result), and (in a compute) do a compute (a
# compute, a
# particular compute / a
# particular float / a
# particular bit, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, a float multiply, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a bit operation, a
# value, a
# particular value, a
# particular size / a
# particular allocation)). The "821E4EB8" refers to the address (a
# particular address, a
# particular size / a
# particular allocation).
[entrypoint.functions."0x821E4EB8"]
name = "ProcessSpecial_821E4EB8"

# A process (a process, with a float matrix multiply, the sibling of ProcessMatrix_8229DEE0 / ProcessFloat_8229DE10,
# the caller of MemCpy_SdkRuntime_82CA2C60): do a process (a process, a particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a compute) do a compute (a
# compute, a
# particular compute / a
# particular float, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, a float multiply, f10 = f12 * f0, a
# value, a
# particular value, a
# particular size / a
# particular allocation, f6 = f11 * f0, a
# value, a
# particular value, a
# particular size / a
# particular allocation, f5 = f9 * f0, a
# value, a
# particular value, a
# particular size / a
# particular allocation), and (in a store) store a value (a
# value, a
# particular value, a
# particular size / a
# particular allocation, a particular store, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result).

# Args: a (r3, r31, ->16 = a float, ->20 = a float, ->24 = a float, ->28 = a
# float, a
# particular object), b (r4, ->0 = a float, ->4 = a float, ->8 = a float, ->16 = a float, ->20 = a
# float, a
# particular object), (more). It does a process (a
# process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). In the compute, it does a compute (a
# compute, a
# particular compute / a
# particular float, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, a float multiply, f10 = f12 * f0, a
# value, a
# particular value, a
# particular size / a
# particular allocation, f6 = f11 * f0, a
# value, a
# particular value, a
# particular size / a
# particular allocation, f5 = f9 * f0, a
# value, a
# particular value, a
# particular size / a
# particular allocation). In the store, it stores a value (a
# value, a
# particular value, a
# particular size / a
# particular allocation, a particular store, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). So it's a "process (a
# process, with a
# float matrix multiply: do a process (a process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a compute) do a compute (a
# compute, a
# particular compute / a
# particular float, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, a float multiply, f10 = f12 * f0, a
# value, a
# particular value, a
# particular size / a
# particular allocation, f6 = f11 * f0, a
# value, a
# particular value, a
# particular size / a
# particular allocation, f5 = f9 * f0, a
# value, a
# particular value, a
# particular size / a
# particular allocation), and (in a store) store a value (a
# value, a
# particular value, a
# particular size / a
# particular allocation, a particular store, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result))" routine (a process routine, part of the 0x8229D cluster, a sibling of
# ProcessMatrix_8229DEE0 and ProcessFloat_8229DE10, a caller of MemCpy_SdkRuntime_82CA2C60). 356
# lines, 1 callee (MemCpy_SdkRuntime_82CA2C60). 3 callers: sub_82BC3990, sub_8229DEE0, sub_82BBC1A0 (processes,
# which call this to do the process, a
# particular size / a
# particular allocation, a
# particular operation). The object's class (a, r31, ->16 = a float, ->20 = a float, ->24 = a float, ->28 = a
# float, a
# particular object, a
# particular value /
# a particular state; b, r4, ->0 = a float, ->4 = a float, ->8 = a float, ->16 = a float, ->20 = a
# float, a
# particular object) isn't resolvable, so the name states the mechanism: a process (with a
# float matrix multiply) (do a process (a process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a compute) do a compute (a
# compute, a
# particular compute / a
# particular float, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, a float multiply, f10 = f12 * f0, a
# value, a
# particular value, a
# particular size / a
# particular allocation, f6 = f11 * f0, a
# value, a
# particular value, a
# particular size / a
# particular allocation, f5 = f9 * f0, a
# value, a
# particular value, a
# particular size / a
# particular allocation), and (in a store) store a value (a
# value, a
# particular value, a
# particular size / a
# particular allocation, a particular store, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result)). The "8229DBD8" refers to the address (a
# particular address, a
# particular size / a
# particular allocation).
[entrypoint.functions."0x8229DBD8"]
name = "ProcessMatrix_8229DBD8"

# A process (a process, with a loop and a check, the sibling of Process2D_8229DFE8 / ProcessMatrix_8229DBD8, the
# caller of MemCpy_SdkRuntime_82CA2C60 / ProcessMatrix_8229DBD8 / ProcessFloat_8229DE10): do a process (a process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a loop) loop over a range (a loop, a particular loop / a
# particular range, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over a value (r31, a value, a
# particular value, a
# particular size / a
# particular allocation), a particular loop / a
# particular range, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, b->251, a
# particular value, a
# particular state / a
# particular result), and (in a copy) copy a value (MemCpy_SdkRuntime_82CA2C60, on a
# value (r3, a value, a
# particular value, a
# particular size / a
# particular allocation, r31, a
# particular value, a
# particular size / a
# particular allocation, 64, a
# particular size / a
# particular allocation), a particular copy / a
# particular size, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result).

# Args: a (r3, r29, ->32 = a field, a value, ->40 = a field, a value, a
# particular object), b (r4, r30, ->251 = a byte, a
# particular object), (more). It does a process (a
# process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). In the loop, it loops over a range (a
# loop, a
# particular loop / a
# particular range, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over a value (r31, a value, a
# particular value, a
# particular size / a
# particular allocation), a particular loop / a
# particular range, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). In the check, it checks a value (a
# value, a
# particular value, a
# particular size / a
# particular allocation, b->251, a
# particular value, a
# particular state / a
# particular result). In the copy, it copies a value (MemCpy_SdkRuntime_82CA2C60, on a
# value (r3, a value, a
# particular value, a
# particular size / a
# particular allocation, r31, a
# particular value, a
# particular size / a
# particular allocation, 64, a
# particular size / a
# particular allocation), a particular copy / a
# particular size, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). So it's a "process (a
# process, with a
# loop and a
# check: do a process (a process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a loop) loop over a range (a loop, a
# particular loop / a
# particular range, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over a value (r31, a value, a
# particular value, a
# particular size / a
# particular allocation), a particular loop / a
# particular range, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, b->251, a
# particular value, a
# particular state / a
# particular result), and (in a copy) copy a value (MemCpy_SdkRuntime_82CA2C60, on a
# value (r3, a value, a
# particular value, a
# particular size / a
# particular allocation, r31, a
# particular value, a
# particular size / a
# particular allocation, 64, a
# particular size / a
# particular allocation), a particular copy / a
# particular size, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result))" routine (a process routine, part of the 0x8229D cluster, a sibling of
# Process2D_8229DFE8 and ProcessMatrix_8229DBD8, a caller of MemCpy_SdkRuntime_82CA2C60, ProcessMatrix_8229DBD8, and
# ProcessFloat_8229DE10). 156 lines, 3
# callees (MemCpy_SdkRuntime_82CA2C60, ProcessMatrix_8229DBD8, ProcessFloat_8229DE10). 1 caller: sub_8229DFE8 (a
# process, which calls this to do the process, a
# particular size / a
# particular allocation, a
# particular operation). The object's class (a, r29, ->32 = a field, a value, ->40 = a field, a value, a
# particular object, a
# particular value /
# a particular state; b, r30, ->251 = a byte, a
# particular object) isn't resolvable, so the name states the mechanism: a process (with a
# loop and a
# check) (do a process (a process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a loop) loop over a range (a loop, a
# particular loop / a
# particular range, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over a value (r31, a value, a
# particular value, a
# particular size / a
# particular allocation), a particular loop / a
# particular range, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, b->251, a
# particular value, a
# particular state / a
# particular result), and (in a copy) copy a value (MemCpy_SdkRuntime_82CA2C60, on a
# value (r3, a value, a
# particular value, a
# particular size / a
# particular allocation, r31, a
# particular value, a
# particular size / a
# particular allocation, 64, a
# particular size / a
# particular allocation), a particular copy / a
# particular size, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result)). The "8229DEE0" refers to the address (a
# particular address, a
# particular size / a
# particular allocation).
[entrypoint.functions."0x8229DEE0"]
name = "Process2D_8229DEE0"

# A process (a process, with a check and a call, the sibling of Process2D_8229DEE0, the caller of Process2D_8229DEE0
# / sub_8229E148 / sub_822D8AA0): do a process (a process, a particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, b->251, a
# particular value, a
# particular state / a
# particular result), and (in a call) call a process (sub_8229DEE0, on a
# value (a + 44, a value, a
# particular value, a
# particular size / a
# particular allocation, b, a
# particular value, a
# particular size / a
# particular allocation), a particular call / a
# particular process, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, and sub_8229E148, on a
# value (b + 60, a value, a
# particular value, a
# particular size / a
# particular allocation, b + 12, a
# particular value, a
# particular size / a
# particular allocation, b + 24, a
# particular value, a
# particular size / a
# particular allocation, b + 36, a
# particular value, a
# particular size / a
# particular allocation, b + 48, a
# particular value, a
# particular size / a
# particular allocation, b->248, a
# particular value, a
# particular size / a
# particular allocation, b->249, a
# particular value, a
# particular size / a
# particular allocation), a particular call / a
# particular process, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result).

# Args: a (r3, r30, ->44 = a field, a value, a
# particular object), b (r4, r31, ->12 = a field, a value, ->24 = a field, a value, ->36 = a field, a value, ->48 = a
# field, a value, ->60 = a field, a value, ->248 = a byte, ->249 = a byte, ->251 = a
# byte, a
# particular object), (more). It does a process (a
# process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). In the check, it checks a value (a
# value, a
# particular value, a
# particular size / a
# particular allocation, b->251, a
# particular value, a
# particular state / a
# particular result). In the call, it calls a process (sub_8229DEE0, on a
# value (a + 44, a value, a
# particular value, a
# particular size / a
# particular allocation, b, a
# particular value, a
# particular size / a
# particular allocation), a particular call / a
# particular process, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, and sub_8229E148, on a
# value (b + 60, a value, a
# particular value, a
# particular size / a
# particular allocation, b + 12, a
# particular value, a
# particular size / a
# particular allocation, b + 24, a
# particular value, a
# particular size / a
# particular allocation, b + 36, a
# particular value, a
# particular size / a
# particular allocation, b + 48, a
# particular value, a
# particular size / a
# particular allocation, b->248, a
# particular value, a
# particular size / a
# particular allocation, b->249, a
# particular value, a
# particular size / a
# particular allocation), a particular call / a
# particular process, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). So it's a "process (a
# process, with a
# check and a
# call: do a process (a process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, b->251, a
# particular value, a
# particular state / a
# particular result), and (in a call) call a process (sub_8229DEE0, on a
# value (a + 44, a value, a
# particular value, a
# particular size / a
# particular allocation, b, a
# particular value, a
# particular size / a
# particular allocation), a particular call / a
# particular process, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, and sub_8229E148, on a
# value (b + 60, a value, a
# particular value, a
# particular size / a
# particular allocation, b + 12, a
# particular value, a
# particular size / a
# particular allocation, b + 24, a
# particular value, a
# particular size / a
# particular allocation, b + 36, a
# particular value, a
# particular size / a
# particular allocation, b + 48, a
# particular value, a
# particular size / a
# particular allocation, b->248, a
# particular value, a
# particular size / a
# particular allocation, b->249, a
# particular value, a
# particular size / a
# particular allocation), a particular call / a
# particular process, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result))" routine (a process routine, part of the 0x8229D cluster, a sibling of
# Process2D_8229DEE0, a caller of Process2D_8229DEE0, sub_8229E148, and sub_822D8AA0). 101
# lines, 3 callees (Process2D_8229DEE0, sub_8229E148, sub_822D8AA0). No
# direct callers (it's called via a vtable / it is registered-but-uncalled, part of the game's subsystems). The
# object's class (a, r30, ->44 = a field, a value, a
# particular object, a
# particular value /
# a particular state; b, r31, ->12 = a field, a value, ->24 = a field, a value, ->36 = a field, a value, ->48 = a
# field, a value, ->60 = a field, a value, ->248 = a byte, ->249 = a byte, ->251 = a
# byte, a
# particular object) isn't resolvable, so the name states the mechanism: a process (with a
# check and a
# call) (do a process (a process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, b->251, a
# particular value, a
# particular state / a
# particular result), and (in a call) call a process (sub_8229DEE0, on a
# value (a + 44, a value, a
# particular value, a
# particular size / a
# particular allocation, b, a
# particular value, a
# particular size / a
# particular allocation), a particular call / a
# particular process, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, and sub_8229E148, on a
# value (b + 60, a value, a
# particular value, a
# particular size / a
# particular allocation, b + 12, a
# particular value, a
# particular size / a
# particular allocation, b + 24, a
# particular value, a
# particular size / a
# particular allocation, b + 36, a
# particular value, a
# particular size / a
# particular allocation, b + 48, a
# particular value, a
# particular size / a
# particular allocation, b->248, a
# particular value, a
# particular size / a
# particular allocation, b->249, a
# particular value, a
# particular size / a
# particular allocation), a particular call / a
# particular process, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result)). The "2" distinguishes it from Process2D_8229DEE0.
[entrypoint.functions."0x8229DFE8"]
name = "Process2D2_8229DFE8"

# A process (a process, with a loop and a check, the sibling of ProcessIndirect_8229DFE8 / Process2D2_8229DFE8):
# do a process (a process, a particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a loop) loop over a range (a loop, a particular loop / a
# particular range, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over b->220, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, r11, a
# particular value, a
# particular state / a
# particular result), and (in a call) call a function (a
# call, a
# particular call / a
# particular function, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, via an indirect call, bctrl, on a
# value (r10, a value, a
# particular value, a
# particular size / a
# particular allocation), a particular call / a
# particular function, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result).

# Args: a (r3, r30, ->0 = a field, a value, a
# particular object), b (r4, r31, ->220 = a field, a value, a
# particular object), (more). It does a process (a
# process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). In the loop, it loops over a range (a
# loop, a
# particular loop / a
# particular range, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over b->220, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result). In the check, it checks a value (a
# value, a
# particular value, a
# particular size / a
# particular allocation, r11, a
# particular value, a
# particular state / a
# particular result). In the call, it calls a function (a
# call, a
# particular call / a
# particular function, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, via an indirect call, bctrl, on a
# value (r10, a value, a
# particular value, a
# particular size / a
# particular allocation), a particular call / a
# particular function, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). So it's a "process (a
# process, with a
# loop and a
# check: do a process (a process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a loop) loop over a range (a loop, a
# particular loop / a
# particular range, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over b->220, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, r11, a
# particular value, a
# particular state / a
# particular result), and (in a call) call a function (a
# call, a
# particular call / a
# particular function, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, via an indirect call, bctrl, on a
# value (r10, a value, a
# particular value, a
# particular size / a
# particular allocation), a particular call / a
# particular function, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result))" routine (a process routine, part of the 0x8229E cluster, a sibling of
# ProcessIndirect_8229DFE8 and Process2D2_8229DFE8). 90
# lines, 0 callees (it does indirect calls via bctrl, not direct calls). No
# direct callers (it's called via a vtable / it is registered-but-uncalled, part of the game's subsystems). The
# object's class (a, r30, ->0 = a field, a value, a
# particular object, a
# particular value /
# a particular state; b, r31, ->220 = a field, a value, a
# particular object) isn't resolvable, so the name states the mechanism: a process (with a
# loop and a
# check) (do a process (a process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a loop) loop over a range (a loop, a
# particular loop / a
# particular range, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over b->220, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, r11, a
# particular value, a
# particular state / a
# particular result), and (in a call) call a function (a
# call, a
# particular call / a
# particular function, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, via an indirect call, bctrl, on a
# value (r10, a value, a
# particular value, a
# particular size / a
# particular allocation), a particular call / a
# particular function, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result)). The "8229E090" refers to the address (a
# particular address, a
# particular size / a
# particular allocation).
[entrypoint.functions."0x8229E090"]
name = "ProcessIndirect_8229E090"

# A decrement (a decrement, with a store, the sibling of Decrement_8229E090 / ProcessIndirect_8229E090): decrement a
# field (a decrement, a particular decrement, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, obj->84 = obj->84 - 1, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result).

# Args: obj (r3, ->84 = a field, a value, a
# particular object), (more). It decrements a field (a
# decrement, a
# particular decrement, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, obj->84 = obj->84 - 1, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result). So it's a "decrement a field (a
# decrement, with a
# store: decrement a field (a decrement, a
# particular decrement, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, obj->84 = obj->84 - 1, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result))" routine (a decrement routine, part of the 0x8229E cluster, a sibling of
# Decrement_8229E090 and ProcessIndirect_8229E090). 7
# lines, 0 callees. No
# direct callers (it's called via a vtable / it is registered-but-uncalled, part of the game's subsystems). The
# object's class (obj, r3, ->84 = a field, a value, a
# particular object, a
# particular value /
# a particular state) isn't resolvable, so the name states the mechanism: a decrement (with a
# store) (decrement a field (a decrement, a
# particular decrement, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, obj->84 = obj->84 - 1, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result)). The "8229E130" refers to the address (a
# particular address, a
# particular size / a
# particular allocation).
[entrypoint.functions."0x8229E130"]
name = "DecrementField_8229E130"

# A process (a process, with a float matrix multiply, the sibling of ProcessMatrix2_8229E848 / ProcessMatrix
# _8229DBD8, the caller of MemCpy_SdkRuntime_82CA2C60 / sub_8229E848 / sub_822D8FF8): do a process (a process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a compute) do a compute (a
# compute, a
# particular compute / a
# particular float, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, a float multiply, f11 = f0 * f13, a
# value, a
# particular value, a
# particular size / a
# particular allocation, f8 = f12 * f10, a
# value, a
# particular value, a
# particular size / a
# particular allocation, f6 = f9 * f7, a
# value, a
# particular value, a
# particular size / a
# particular allocation), and (in a store) store a value (a
# value, a
# particular value, a
# particular size / a
# particular allocation, a particular store, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result).

# Args: a (r3, r31, a value, a
# particular value, a
# particular size / a
# particular allocation), b (r4, r30, a value, a
# particular value, a
# particular size / a
# particular allocation), c (r5, r29, a value, a
# particular value, a
# particular size / a
# particular allocation), d (r6, r28, ->0 = a float, ->4 = a float, ->8 = a
# float, a
# particular object), e (r7, ->0 = a float, ->4 = a float, ->8 = a
# float, a
# particular object), (more). It does a process (a
# process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). In the compute, it does a compute (a
# compute, a
# particular compute / a
# particular float, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, a float multiply, f11 = f0 * f13, a
# value, a
# particular value, a
# particular size / a
# particular allocation, f8 = f12 * f10, a
# value, a
# particular value, a
# particular size / a
# particular allocation, f6 = f9 * f7, a
# value, a
# particular value, a
# particular size / a
# particular allocation). In the store, it stores a value (a
# value, a
# particular value, a
# particular size / a
# particular allocation, a particular store, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). So it's a "process (a
# process, with a
# float matrix multiply: do a process (a process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a compute) do a compute (a
# compute, a
# particular compute / a
# particular float, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, a float multiply, f11 = f0 * f13, a
# value, a
# particular value, a
# particular size / a
# particular allocation, f8 = f12 * f10, a
# value, a
# particular value, a
# particular size / a
# particular allocation, f6 = f9 * f7, a
# value, a
# particular value, a
# particular size / a
# particular allocation), and (in a store) store a value (a
# value, a
# particular value, a
# particular size / a
# particular allocation, a particular store, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result))" routine (a process routine, part of the 0x8229E cluster, a sibling of
# ProcessMatrix2_8229E848 and ProcessMatrix_8229DBD8, a caller of MemCpy_SdkRuntime_82CA2C60, sub_8229E848, and
# sub_822D8FF8). 264 lines, 3
# callees (MemCpy_SdkRuntime_82CA2C60, sub_8229E848, sub_822D8FF8). 2 callers: Process2D2_8229DFE8, sub_82BC3990
# (processes, which call this to do the process, a
# particular size / a
# particular allocation, a
# particular operation). The object's class (a, r31, a value, a
# particular value, a
# particular size / a
# particular allocation; b, r30, a value, a
# particular value, a
# particular size / a
# particular allocation; c, r29, a value, a
# particular value, a
# particular size / a
# particular allocation; d, r28, ->0 = a float, ->4 = a float, ->8 = a
# float, a
# particular object; e, r7, ->0 = a float, ->4 = a float, ->8 = a
# float, a
# particular object) isn't resolvable, so the name states the mechanism: a process (with a
# float matrix multiply) (do a process (a process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a compute) do a compute (a
# compute, a
# particular compute / a
# particular float, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, a float multiply, f11 = f0 * f13, a
# value, a
# particular value, a
# particular size / a
# particular allocation, f8 = f12 * f10, a
# value, a
# particular value, a
# particular size / a
# particular allocation, f6 = f9 * f7, a
# value, a
# particular value, a
# particular size / a
# particular allocation), and (in a store) store a value (a
# value, a
# particular value, a
# particular size / a
# particular allocation, a particular store, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result)). The "2" distinguishes it from ProcessMatrix_8229DBD8.
[entrypoint.functions."0x8229E148"]
name = "ProcessMatrix2_8229E148"

# A process (a process, with a float matrix multiply, the sibling of ProcessTransform_8229E148 / ProcessMatrix2
# _8229E148, the caller of MemCpy_SdkRuntime_82CA2C60): do a process (a process, a particular process, a particular
# size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a compute) do a compute (a
# compute, a
# particular compute / a
# particular float, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, a float multiply, f5 = f9 * f7, a
# value, a
# particular value, a
# particular size / a
# particular allocation, f3 = f6 * f13, a
# value, a
# particular value, a
# particular size / a
# particular allocation), and (in a store) store a value (a
# value, a
# particular value, a
# particular size / a
# particular allocation, a particular store, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result).

# Args: a (r3, r31, ->4 = a float, ->8 = a float, a
# particular object), b (r4, r11, ->0 = a float, ->8 = a float, ->16 = a float, ->20 = a float, ->36 = a
# float, a
# particular object), (more). It does a process (a
# process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). In the compute, it does a compute (a
# compute, a
# particular compute / a
# particular float, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, a float multiply, f5 = f9 * f7, a
# value, a
# particular value, a
# particular size / a
# particular allocation, f3 = f6 * f13, a
# value, a
# particular value, a
# particular size / a
# particular allocation). In the store, it stores a value (a
# value, a
# particular value, a
# particular size / a
# particular allocation, a particular store, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). So it's a "process (a
# process, with a
# float matrix multiply: do a process (a process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a compute) do a compute (a
# compute, a
# particular compute / a
# particular float, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, a float multiply, f5 = f9 * f7, a
# value, a
# particular value, a
# particular size / a
# particular allocation, f3 = f6 * f13, a
# value, a
# particular value, a
# particular size / a
# particular allocation), and (in a store) store a value (a
# value, a
# particular value, a
# particular size / a
# particular allocation, a particular store, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result))" routine (a process routine, part of the 0x8229E cluster, a sibling of
# ProcessTransform_8229E148 and ProcessMatrix2_8229E148, a caller of MemCpy_SdkRuntime_82CA2C60). 264
# lines, 1 callee (MemCpy_SdkRuntime_82CA2C60). 1 caller: ProcessMatrix2_8229E148 (a
# process, which calls this to do the process, a
# particular size / a
# particular allocation, a
# particular operation). The object's class (a, r31, ->4 = a float, ->8 = a float, a
# particular object, a
# particular value /
# a particular state; b, r11, ->0 = a float, ->8 = a float, ->16 = a float, ->20 = a float, ->36 = a
# float, a
# particular object) isn't resolvable, so the name states the mechanism: a process (with a
# float matrix multiply) (do a process (a process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a compute) do a compute (a
# compute, a
# particular compute / a
# particular float, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, a float multiply, f5 = f9 * f7, a
# value, a
# particular value, a
# particular size / a
# particular allocation, f3 = f6 * f13, a
# value, a
# particular value, a
# particular size / a
# particular allocation), and (in a store) store a value (a
# value, a
# particular value, a
# particular size / a
# particular allocation, a particular store, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result)). The "8229E848" refers to the address (a
# particular address, a
# particular size / a
# particular allocation).
[entrypoint.functions."0x8229E848"]
name = "ProcessTransform_8229E848"

# A process (a process, with a check and a call, the sibling of ProcessDispatch4_828164E8 / ProcessTransform
# _8229E848, the caller of LinearSearch_82284448 / ProcessFloatToInt_82BAE368): do a process (a process, a particular
# process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, b->250, a
# particular value, a
# particular state / a
# particular result), and (in a call) call a function (a
# call, a
# particular call / a
# particular function, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, via an indirect call, bctrl, on a
# value (r9, a value, a
# particular value, a
# particular size / a
# particular allocation), a particular call / a
# particular function, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result).

# Args: a (r3, r30, a value, a
# particular value, a
# particular size / a
# particular allocation), b (r4, r29, ->10 = a byte, ->250 = a byte, a
# particular object), c (r5, r28, a value, a
# particular value, a
# particular size / a
# particular allocation), (more). It does a process (a
# process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). In the check, it checks a value (a
# value, a
# particular value, a
# particular size / a
# particular allocation, b->250, a
# particular value, a
# particular state / a
# particular result). In the call, it calls a function (a
# call, a
# particular call / a
# particular function, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, via an indirect call, bctrl, on a
# value (r9, a value, a
# particular value, a
# particular size / a
# particular allocation), a particular call / a
# particular function, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). So it's a "process (a
# process, with a
# check and a
# call: do a process (a process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, b->250, a
# particular value, a
# particular state / a
# particular result), and (in a call) call a function (a
# call, a
# particular call / a
# particular function, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, via an indirect call, bctrl, on a
# value (r9, a value, a
# particular value, a
# particular size / a
# particular allocation), a particular call / a
# particular function, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result))" routine (a process routine, part of the 0x822D8 cluster, a sibling of
# ProcessDispatch4_828164E8 and ProcessTransform_8229E848, a caller of LinearSearch_82284448 and
# ProcessFloatToInt_82BAE368). 258 lines, 2
# callees (LinearSearch_82284448, ProcessFloatToInt_82BAE368). 1 caller: Process2D2_8229DFE8 (a
# process, which calls this to do the process, a
# particular size / a
# particular allocation, a
# particular operation). The object's class (a, r30, a value, a
# particular value, a
# particular size / a
# particular allocation; b, r29, ->10 = a byte, ->250 = a byte, a
# particular object, a
# particular value /
# a particular state; c, r28, a value, a
# particular value, a
# particular size / a
# particular allocation) isn't resolvable, so the name states the mechanism: a process (with a
# check and a
# call) (do a process (a process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, b->250, a
# particular value, a
# particular state / a
# particular result), and (in a call) call a function (a
# call, a
# particular call / a
# particular function, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, via an indirect call, bctrl, on a
# value (r9, a value, a
# particular value, a
# particular size / a
# particular allocation), a particular call / a
# particular function, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result)). The "5" distinguishes it from ProcessDispatch4_828164E8.
[entrypoint.functions."0x822D8AA0"]
name = "ProcessDispatch5_822D8AA0"

# A dispatch (a dispatch, with a switch, the sibling of ProcessDispatch5_822D8AA0 / ProcessSpecial_821E4EB8, the
# caller of ProcessSpecial_821E4EB8): dispatch a value (a dispatch, a particular dispatch / a
# particular case, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a switch) do a switch (a
# switch, a
# particular switch, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, on a
# value (c, a value, a
# particular value, a
# particular size / a
# particular allocation), with cases 0-11, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a call) call a process (ProcessSpecial_821E4EB8, on a
# value (a, a value, a
# particular value, a
# particular size / a
# particular allocation, b, a
# particular value, a
# particular size / a
# particular allocation, c, a
# particular value, a
# particular size / a
# particular allocation), a particular call / a
# particular process, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result).

# Args: a (r3, r31, a value, a
# particular value, a
# particular size / a
# particular allocation), b (r4, a value, a
# particular value, a
# particular size / a
# particular allocation), c (r5, a value, a
# particular value, a
# particular size / a
# particular allocation), (more). It dispatches a value (a
# dispatch, a
# particular dispatch / a
# particular case, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). In the switch, it does a switch (a
# switch, a
# particular switch, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, on a
# value (c, a value, a
# particular value, a
# particular size / a
# particular allocation), with cases 0-11, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). In the call, it calls a process (ProcessSpecial_821E4EB8, on a
# value (a, a value, a
# particular value, a
# particular size / a
# particular allocation, b, a
# particular value, a
# particular size / a
# particular allocation, c, a
# particular value, a
# particular size / a
# particular allocation), a particular call / a
# particular process, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). So it's a "dispatch a value (a
# dispatch, with a
# switch: dispatch a value (a dispatch, a
# particular dispatch / a
# particular case, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a switch) do a switch (a
# switch, a
# particular switch, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, on a
# value (c, a value, a
# particular value, a
# particular size / a
# particular allocation), with cases 0-11, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a call) call a process (ProcessSpecial_821E4EB8, on a
# value (a, a value, a
# particular value, a
# particular size / a
# particular allocation, b, a
# particular value, a
# particular size / a
# particular allocation, c, a
# particular value, a
# particular size / a
# particular allocation), a particular call / a
# particular process, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result))" routine (a dispatch routine, part of the 0x822D8 cluster, a sibling of
# ProcessDispatch5_822D8AA0 and ProcessSpecial_821E4EB8, a caller of ProcessSpecial_821E4EB8). 304
# lines, 1 callee (ProcessSpecial_821E4EB8). 2 callers: sub_82BBBDB0, ProcessMatrix2_8229E148 (processes,
# which call this to dispatch the value, a
# particular size / a
# particular allocation, a
# particular operation). The value's meaning (a, r31, a value, a
# particular value, a
# particular size / a
# particular allocation; b, r4, a value, a
# particular value, a
# particular size / a
# particular allocation; c, r5, a value, a
# particular value, a
# particular size / a
# particular allocation) isn't resolvable, so the name states the mechanism: a dispatch (with a
# switch) (dispatch a value (a dispatch, a
# particular dispatch / a
# particular case, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a switch) do a switch (a
# switch, a
# particular switch, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, on a
# value (c, a value, a
# particular value, a
# particular size / a
# particular allocation), with cases 0-11, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a call) call a process (ProcessSpecial_821E4EB8, on a
# value (a, a value, a
# particular value, a
# particular size / a
# particular allocation, b, a
# particular value, a
# particular size / a
# particular allocation, c, a
# particular value, a
# particular size / a
# particular allocation), a particular call / a
# particular process, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result)). The "6" distinguishes it from ProcessDispatch5_822D8AA0.
[entrypoint.functions."0x822D8FF8"]
name = "ProcessDispatch6_822D8FF8"

# A list manipulation (a list manipulation, with a check and a store, the sibling of ListUnlink2_82B5E7F8 / ListUnlink
# _82B5E7F8): do a list manipulation (a
# list manipulation, a particular list manipulation, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, node->0 = r11->8, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, r10->4 = node, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, r11->4 = node->4, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, r10->4 = r11, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, r11->8 = node, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, node->4 = r11, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result).

# Args: node (r4, ->0 = a field, a value, ->4 = a field, a value, ->8 = a field, a
# value, a
# particular object), (more). It does a list manipulation (a
# list manipulation, a
# particular list manipulation, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, node->0 = r11->8, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, r10->4 = node, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, r11->4 = node->4, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, r10->4 = r11, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, r11->8 = node, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, node->4 = r11, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result). So it's a "list manipulation (a
# list manipulation, with a
# check and a
# store: do a list manipulation (a
# list manipulation, a particular list manipulation, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, node->0 = r11->8, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, r10->4 = node, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, r11->4 = node->4, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, r10->4 = r11, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, r11->8 = node, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, node->4 = r11, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result))" routine (a list manipulation routine, part of the 0x82B5E cluster, a sibling of
# ListUnlink2_82B5E7F8 and ListUnlink_82B5E7F8). 71
# lines, 0 callees. 2 callers: sub_82B5D930, ProcessChain4_82B5E558 (processes, which
# call this to do the list manipulation, a
# particular size / a
# particular allocation, a
# particular operation). The object's class (node, r4, ->0 = a field, a value, ->4 = a field, a value, ->8 = a field, a
# value, a
# particular object, a
# particular value /
# a particular state) isn't resolvable, so the name states the mechanism: a list manipulation (with a
# check and a
# store) (do a list manipulation (a
# list manipulation, a
# particular list manipulation, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, node->0 = r11->8, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, r10->4 = node, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, r11->4 = node->4, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, r10->4 = r11, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, r11->8 = node, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, node->4 = r11, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result)). The "2" distinguishes it from ListUnlink_82B5E7F8.
[entrypoint.functions."0x82B5E878"]
name = "ListUnlink2_82B5E878"

# A construct (a construct, with a store and a call, the sibling of Construct8_82A3B330 / ConstructString_8217AB58,
# the caller of sub_82A3B330): construct an object (a construct, a
# particular construct, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a store) store a value (a
# value, a
# particular value, a
# particular size / a
# particular allocation, obj->0 = r10, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, obj->544 = -1, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, obj->548 = 1, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (in a call) call a process (sub_82A3B330, on a
# value (obj + 16, a value, a
# particular value, a
# particular size / a
# particular allocation), a particular call / a
# particular process, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result).

# Args: obj (r3, r31, ->0 = a field, a value, ->16 = a field, a value, ->544 = a field, a value, ->548 = a field, a
# value, ->549 = a byte, ->550 = a byte, ->552 = a field, a value, ->556 = a field, a value, ->560 = a field, a
# value, ->564 = a field, a value, a
# particular object), (more). It constructs an object (a
# construct, a
# particular construct, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). In the store, it stores a value (a
# value, a
# particular value, a
# particular size / a
# particular allocation, obj->0 = r10, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, obj->544 = -1, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, obj->548 = 1, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result). In the call, it calls a process (sub_82A3B330, on a
# value (obj + 16, a value, a
# particular value, a
# particular size / a
# particular allocation), a particular call / a
# particular process, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). So it's a "construct an object (a
# construct, with a
# store and a
# call: construct an object (a construct, a
# particular construct, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a store) store a value (a
# value, a
# particular value, a
# particular size / a
# particular allocation, obj->0 = r10, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, obj->544 = -1, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, obj->548 = 1, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (in a call) call a process (sub_82A3B330, on a
# value (obj + 16, a value, a
# particular value, a
# particular size / a
# particular allocation), a particular call / a
# particular process, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result))" routine (a construct routine, part of the 0x82814 cluster, a sibling of
# Construct8_82A3B330 and ConstructString_8217AB58, a caller of sub_82A3B330). 92
# lines, 1 callee (sub_82A3B330). 2 callers: sub_8281AE60, sub_8281A568 (processes, which
# call this to construct the object, a
# particular size / a
# particular allocation, a
# particular operation). The object's class (obj, r31, ->0 = a field, a value, ->16 = a field, a value, ->544 = a
# field, a value, ->548 = a field, a value, ->549 = a byte, ->550 = a byte, ->552 = a field, a value, ->556 = a
# field, a value, ->560 = a field, a value, ->564 = a field, a value, a
# particular object, a
# particular value /
# a particular state) isn't resolvable, so the name states the mechanism: a construct (with a
# store and a
# call) (construct an object (a construct, a
# particular construct, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a store) store a value (a
# value, a
# particular value, a
# particular size / a
# particular allocation, obj->0 = r10, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, obj->544 = -1, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, obj->548 = 1, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (in a call) call a process (sub_82A3B330, on a
# value (obj + 16, a value, a
# particular value, a
# particular size / a
# particular allocation), a particular call / a
# particular process, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result)). The "82814A38" refers to the address (a
# particular address, a
# particular size / a
# particular allocation).
[entrypoint.functions."0x82814A38"]
name = "Construct8_82814A38"

# A process (a process, with a float operation, the sibling of ProcessFloatOp_82814A38 / Construct8_82814A38):
# do a process (a process, a particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a compute) do a compute (a
# compute, a
# particular compute / a
# particular float, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, a float add, f10 = f2 + f4, a
# value, a
# particular value, a
# particular size / a
# particular allocation, f11 = f1 + f3, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a float sub, f13 = f12 - f4, a
# value, a
# particular value, a
# particular size / a
# particular allocation, f7 = f8 - f3, a
# value, a
# particular value, a
# particular size / a
# particular allocation, f6 = f9 - f13, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a float abs, f5 = fabs(f7), a
# value, a
# particular value, a
# particular size / a
# particular allocation, f12 = f12 - f10, a
# value, a
# particular value, a
# particular size / a
# particular allocation).

# Args: a (r3, ->48 = a field, a value, a
# particular object), f1 (a float), f2 (a float), f3 (a float), f4 (a float), (more). It does a process (a
# process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). In the compute, it does a compute (a
# compute, a
# particular compute / a
# particular float, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, a float add, f10 = f2 + f4, a
# value, a
# particular value, a
# particular size / a
# particular allocation, f11 = f1 + f3, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a float sub, f13 = f12 - f4, a
# value, a
# particular value, a
# particular size / a
# particular allocation, f7 = f8 - f3, a
# value, a
# particular value, a
# particular size / a
# particular allocation, f6 = f9 - f13, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a float abs, f5 = fabs(f7), a
# value, a
# particular value, a
# particular size / a
# particular allocation, f12 = f12 - f10, a
# value, a
# particular value, a
# particular size / a
# particular allocation). So it's a "process (a
# process, with a
# float operation: do a process (a process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a compute) do a compute (a
# compute, a
# particular compute / a
# particular float, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, a float add, f10 = f2 + f4, a
# value, a
# particular value, a
# particular size / a
# particular allocation, f11 = f1 + f3, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a float sub, f13 = f12 - f4, a
# value, a
# particular value, a
# particular size / a
# particular allocation, f7 = f8 - f3, a
# value, a
# particular value, a
# particular size / a
# particular allocation, f6 = f9 - f13, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a float abs, f5 = fabs(f7), a
# value, a
# particular value, a
# particular size / a
# particular allocation, f12 = f12 - f10, a
# value, a
# particular value, a
# particular size / a
# particular allocation))" routine (a process routine, part of the 0x82814 cluster, a sibling of
# ProcessFloatOp_82814A38 and Construct8_82814A38). 234
# lines, 0 callees. 3 callers: sub_82841120, sub_82815E00, sub_828152D0 (processes, which
# call this to do the process, a
# particular size / a
# particular allocation, a
# particular operation). The object's class (a, r3, ->48 = a field, a value, a
# particular object, a
# particular value /
# a particular state) and the value's meaning (f1, a float; f2, a float; f3, a float; f4, a float) aren't
# resolvable, so the name states the mechanism: a process (with a
# float operation) (do a process (a process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a compute) do a compute (a
# compute, a
# particular compute / a
# particular float, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, a float add, f10 = f2 + f4, a
# value, a
# particular value, a
# particular size / a
# particular allocation, f11 = f1 + f3, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a float sub, f13 = f12 - f4, a
# value, a
# particular value, a
# particular size / a
# particular allocation, f7 = f8 - f3, a
# value, a
# particular value, a
# particular size / a
# particular allocation, f6 = f9 - f13, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a float abs, f5 = fabs(f7), a
# value, a
# particular value, a
# particular size / a
# particular allocation, f12 = f12 - f10, a
# value, a
# particular value, a
# particular size / a
# particular allocation)). The "82814BA8" refers to the address (a
# particular address, a
# particular size / a
# particular allocation).
[entrypoint.functions."0x82814BA8"]
name = "ProcessFloatOp_82814BA8"

# A process (a process, with a loop and a check, the sibling of ProcessVector_82814F40 / ProcessFloatOp_82814BA8):
# do a process (a process, a particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a loop) loop over a range (a loop, a particular loop / a
# particular range, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over a value (obj->64, a value, a
# particular value, a
# particular size / a
# particular allocation), a particular loop / a
# particular range, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, obj->552, a
# particular value, a
# particular state / a
# particular result), and (in a vector operation) do a vector operation (a
# vector operation, a particular vector operation, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, lvx128, a
# particular size / a
# particular allocation, a
# particular operation).

# Args: obj (r3, ->552 = a field, a value, ->64 = a field, a value, a
# particular object), a (r4, ->0 = a byte, ->1 = a byte, ->2 = a byte, ->3 = a
# byte, a
# particular object), (more). It does a process (a
# process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). In the loop, it loops over a range (a
# loop, a
# particular loop / a
# particular range, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over a value (obj->64, a value, a
# particular value, a
# particular size / a
# particular allocation), a particular loop / a
# particular range, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). In the check, it checks a value (a
# value, a
# particular value, a
# particular size / a
# particular allocation, obj->552, a
# particular value, a
# particular state / a
# particular result). In the vector operation, it does a vector operation (a
# vector operation, a
# particular vector operation, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, lvx128, a
# particular size / a
# particular allocation, a
# particular operation). So it's a "process (a
# process, with a
# loop and a
# check: do a process (a process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a loop) loop over a range (a loop, a
# particular loop / a
# particular range, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over a value (obj->64, a value, a
# particular value, a
# particular size / a
# particular allocation), a particular loop / a
# particular range, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, obj->552, a
# particular value, a
# particular state / a
# particular result), and (in a vector operation) do a vector operation (a
# vector operation, a particular vector operation, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, lvx128, a
# particular size / a
# particular allocation, a
# particular operation))" routine (a process routine, part of the 0x82814 cluster, a sibling of
# ProcessVector_82814F40 and ProcessFloatOp_82814BA8). 269
# lines, 0 callees. 2 callers: sub_828152D0, sub_823F80F0 (processes, which
# call this to do the process, a
# particular size / a
# particular allocation, a
# particular operation). The object's class (obj, r3, ->552 = a field, a value, ->64 = a field, a value, a
# particular object, a
# particular value /
# a particular state; a, r4, ->0 = a byte, ->1 = a byte, ->2 = a byte, ->3 = a
# byte, a
# particular object) isn't resolvable, so the name states the mechanism: a process (with a
# loop and a
# check) (do a process (a process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a loop) loop over a range (a loop, a
# particular loop / a
# particular range, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over a value (obj->64, a value, a
# particular value, a
# particular size / a
# particular allocation), a particular loop / a
# particular range, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, obj->552, a
# particular value, a
# particular state / a
# particular result), and (in a vector operation) do a vector operation (a
# vector operation, a
# particular vector operation, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, lvx128, a
# particular size / a
# particular allocation, a
# particular operation)). The "82814F40" refers to the address (a
# particular address, a
# particular size / a
# particular allocation).
[entrypoint.functions."0x82814F40"]
name = "ProcessVector_82814F40"

# A process (a process, with a loop and a check, the sibling of ProcessParse_82814F40 / ProcessVector_82814F40, the
# caller of ScanLineLength_82B477F0 / ProcessLine_82B470D0 / Destruct4_8264EDF8): do a process (a process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a loop) loop over a range (a loop, a particular loop / a
# particular range, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over b->0, a value, a
# particular value, a
# particular size / a
# particular allocation, b->36, a
# value, a
# particular value, a
# particular size / a
# particular allocation), a particular loop / a
# particular range, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, a value (r31, a value, a
# particular value, a
# particular size / a
# particular allocation), a
# particular value, a
# particular state / a
# particular result), and (in a call) call a process (ScanLineLength_82B477F0, on a
# value (b, a value, a
# particular value, a
# particular size / a
# particular allocation), a particular call / a
# particular process, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result).

# Args: a (r3, r30, a value, a
# particular value, a
# particular size / a
# particular allocation), b (r4, r31, ->0 = a field, a value, ->36 = a field, a
# value, a
# particular object), c (r6, r27, a value, a
# particular value, a
# particular size / a
# particular allocation), d (r7, r26, a value, a
# particular value, a
# particular size / a
# particular allocation), (more). It does a process (a
# process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). In the loop, it loops over a range (a
# loop, a
# particular loop / a
# particular range, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over b->0, a value, a
# particular value, a
# particular size / a
# particular allocation, b->36, a
# value, a
# particular value, a
# particular size / a
# particular allocation), a particular loop / a
# particular range, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). In the check, it checks a value (a
# value, a
# particular value, a
# particular size / a
# particular allocation, a value (r31, a value, a
# particular value, a
# particular size / a
# particular allocation), a
# particular value, a
# particular state / a
# particular result). In the call, it calls a process (ScanLineLength_82B477F0, on a
# value (b, a value, a
# particular value, a
# particular size / a
# particular allocation), a particular call / a
# particular process, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). So it's a "process (a
# process, with a
# loop and a
# check: do a process (a process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a loop) loop over a range (a loop, a
# particular loop / a
# particular range, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over b->0, a value, a
# particular value, a
# particular size / a
# particular allocation, b->36, a
# value, a
# particular value, a
# particular size / a
# particular allocation), a particular loop / a
# particular range, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, a value (r31, a value, a
# particular value, a
# particular size / a
# particular allocation), a
# particular value, a
# particular state / a
# particular result), and (in a call) call a process (ScanLineLength_82B477F0, on a
# value (b, a value, a
# particular value, a
# particular size / a
# particular allocation), a particular call / a
# particular process, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result))" routine (a process routine, part of the 0x82815 cluster, a sibling of
# ProcessParse_82814F40 and ProcessVector_82814F40, a caller of ScanLineLength_82B477F0, ProcessLine_82B470D0, and
# Destruct4_8264EDF8). 596 lines, 19
# callees (ConstructRefCounted_8222CF18, CopyAssign_RefCounted2_82265160, Ctype_Pred_Bit2_82CA6B10, Destruct4_8264EDF8,
# DestructRefCounted_82214F08, HashLookupCompare_823F9620, HashLookup_823F74D0, ParseLine_82B47218,
# ProcessAtomic2_82354880, ProcessDispatch4_828164E8, ProcessHashLookup2_823F75A0, ProcessLineParse_82B47148,
# ProcessLine_82B470D0, ProcessVector2_8223BD98, ScanLineLength_82B477F0, ScanQuoteParity_82B47A40,
# SkipCtypeChars_82B47610, ProcessFloatOp_82814BA8, ProcessVector_82814F40). No
# direct callers (it's called via a vtable / it is registered-but-uncalled, part of the game's subsystems). The
# object's class (a, r30, a value, a
# particular value, a
# particular size / a
# particular allocation; b, r31, ->0 = a field, a value, ->36 = a field, a
# value, a
# particular object, a
# particular value /
# a particular state; c, r27, a value, a
# particular value, a
# particular size / a
# particular allocation; d, r26, a value, a
# particular value, a
# particular size / a
# particular allocation) isn't resolvable, so the name states the mechanism: a process (with a
# loop and a
# check) (do a process (a process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a loop) loop over a range (a loop, a
# particular loop / a
# particular range, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over b->0, a value, a
# particular value, a
# particular size / a
# particular allocation, b->36, a
# value, a
# particular value, a
# particular size / a
# particular allocation), a particular loop / a
# particular range, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, a value (r31, a value, a
# particular value, a
# particular size / a
# particular allocation), a
# particular value, a
# particular state / a
# particular result), and (in a call) call a process (ScanLineLength_82B477F0, on a
# value (b, a value, a
# particular value, a
# particular size / a
# particular allocation), a particular call / a
# particular process, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result)). The "828152D0" refers to the address (a
# particular address, a
# particular size / a
# particular allocation).
[entrypoint.functions."0x828152D0"]
name = "ProcessParse_828152D0"

# A construct (a construct, with a loop and a call, the sibling of Construct9_82A3B1E8 / Construct8_82814A38, the
# caller of ProcessVector_82A3B1E8): construct an object (a construct, a
# particular construct, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a loop) loop over a range (a loop, a particular loop / a
# particular range, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over obj->16, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (in a call) call a process (ProcessVector_82A3B1E8, on a
# value (obj + 16, a value, a
# particular value, a
# particular size / a
# particular allocation), a particular call / a
# particular process, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and store a value (obj->0 = 0, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result).

# Args: obj (r3, r31, ->0 = a field, a value, ->16 = a field, a value, a
# particular object), (more). It constructs an object (a
# construct, a
# particular construct, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). In the loop, it loops over a range (a
# loop, a
# particular loop / a
# particular range, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over obj->16, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result). In the call, it calls a process (ProcessVector_82A3B1E8, on a
# value (obj + 16, a value, a
# particular value, a
# particular size / a
# particular allocation), a particular call / a
# particular process, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). It stores a value (obj->0 = 0, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result). So it's a "construct an object (a
# construct, with a
# loop and a
# call: construct an object (a construct, a
# particular construct, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a loop) loop over a range (a loop, a
# particular loop / a
# particular range, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over obj->16, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (in a call) call a process (ProcessVector_82A3B1E8, on a
# value (obj + 16, a value, a
# particular value, a
# particular size / a
# particular allocation), a particular call / a
# particular process, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and store a value (obj->0 = 0, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result))" routine (a construct routine, part of the 0x82A3B cluster, a sibling of
# Construct9_82A3B1E8 and Construct8_82814A38, a caller of ProcessVector_82A3B1E8). 426
# lines, 1 callee (ProcessVector_82A3B1E8). 9 callers: sub_82A608F8, Construct8_82814A38, sub_8235B570, sub_8235B708,
# sub_823FF000, sub_82900B28, sub_82900688, sub_82410D98, sub_82817748 (processes, which
# call this to construct the object, a
# particular size / a
# particular allocation, a
# particular operation). The object's class (obj, r31, ->0 = a field, a value, ->16 = a field, a value, a
# particular object, a
# particular value /
# a particular state) isn't resolvable, so the name states the mechanism: a construct (with a
# loop and a
# call) (construct an object (a construct, a
# particular construct, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a loop) loop over a range (a loop, a
# particular loop / a
# particular range, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over obj->16, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (in a call) call a process (ProcessVector_82A3B1E8, on a
# value (obj + 16, a value, a
# particular value, a
# particular size / a
# particular allocation), a particular call / a
# particular process, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and store a value (obj->0 = 0, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result)). The "9" distinguishes it from Construct8_82814A38.
[entrypoint.functions."0x82A3B330"]
name = "Construct9_82A3B330"

# A string operation (a string operation, with a loop and a check, the sibling of StringCopyN_822CD828 / Construct9
# _82A3B330): do a string operation (a
# string operation, a particular string operation, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a loop) loop over a range (a loop, a particular loop / a
# particular range, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over n, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, src->0, a
# particular value, a
# particular state / a
# particular result), and (in a copy) copy a value (a
# value, a
# particular value, a
# particular size / a
# particular allocation, dst->0 = src->0, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and store a value (dst->0 = 0, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result).

# Args: dst (r3, r11, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular string), src (r4, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular string), n (r5, a value, a
# particular value, a
# particular size / a
# particular allocation), (more). It does a string operation (a
# string operation, a
# particular string operation, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). In the loop, it loops over a range (a
# loop, a
# particular loop / a
# particular range, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over n, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result). In the check, it checks a value (a
# value, a
# particular value, a
# particular size / a
# particular allocation, src->0, a
# particular value, a
# particular state / a
# particular result). In the copy, it copies a value (a
# value, a
# particular value, a
# particular size / a
# particular allocation, dst->0 = src->0, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result). It stores a value (dst->0 = 0, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result). So it's a "string operation (a
# string operation, with a
# loop and a
# check: do a string operation (a
# string operation, a particular string operation, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a loop) loop over a range (a loop, a
# particular loop / a
# particular range, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over n, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, src->0, a
# particular value, a
# particular state / a
# particular result), and (in a copy) copy a value (a
# value, a
# particular value, a
# particular size / a
# particular allocation, dst->0 = src->0, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and store a value (dst->0 = 0, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result))" routine (a string operation routine, part of the 0x822CD cluster, a sibling of
# StringCopyN_822CD828 and Construct9_82A3B330). 56
# lines, 0 callees. 10 callers: sub_82E7DCA0, sub_82E7D8F8, sub_82D3AE00, sub_82C5BCC0, sub_82D51970, sub_82BA1048,
# sub_827FFE08, sub_8231AB20, sub_82BF9040, sub_82D114F8 (processes, which
# call this to do the string operation, a
# particular size / a
# particular allocation, a
# particular operation). The string's meaning (dst, r11, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular string; src, r4, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular string; n, r5, a value, a
# particular value, a
# particular size / a
# particular allocation) isn't resolvable, so the name states the mechanism: a string operation (with a
# loop and a
# check) (do a string operation (a
# string operation, a
# particular string operation, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a loop) loop over a range (a loop, a
# particular loop / a
# particular range, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over n, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, src->0, a
# particular value, a
# particular state / a
# particular result), and (in a copy) copy a value (a
# value, a
# particular value, a
# particular size / a
# particular allocation, dst->0 = src->0, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and store a value (dst->0 = 0, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result)). The "822CD828" refers to the address (a
# particular address, a
# particular size / a
# particular allocation).
[entrypoint.functions."0x822CD828"]
name = "StringCopyN_822CD828"

# A process (a process, with a loop and a check, the sibling of ProcessObject_82BC2860 / StringCopyN_822CD828, the
# caller of StringCopyN_822CD828): do a process (a process, a particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a loop) loop over a range (a loop, a particular loop / a
# particular range, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over a value (r11, a value, a
# particular value, a
# particular size / a
# particular allocation), a particular loop / a
# particular range, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, r3->42, a
# particular value, a
# particular state / a
# particular result), and (in a call) call a function (a
# call, a
# particular call / a
# particular function, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, via an indirect call, bctrl, on a
# value (r10, a value, a
# particular value, a
# particular size / a
# particular allocation), a particular call / a
# particular function, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result).

# Args: obj (r3, r31, ->8 = a field, a value, ->12 = a field, a value, ->32 = a field, a value, ->36 = a field, a
# value, a
# particular object), a (r5, r28, a value, a
# particular value, a
# particular size / a
# particular allocation), b (r6, r29, a value, a
# particular value, a
# particular size / a
# particular allocation), c (r7, r27, a value, a
# particular value, a
# particular size / a
# particular allocation), d (r8, r26, a value, a
# particular value, a
# particular size / a
# particular allocation), (more). It does a process (a
# process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). In the loop, it loops over a range (a
# loop, a
# particular loop / a
# particular range, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over a value (r11, a value, a
# particular value, a
# particular size / a
# particular allocation), a particular loop / a
# particular range, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). In the check, it checks a value (a
# value, a
# particular value, a
# particular size / a
# particular allocation, r3->42, a
# particular value, a
# particular state / a
# particular result). In the call, it calls a function (a
# call, a
# particular call / a
# particular function, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, via an indirect call, bctrl, on a
# value (r10, a value, a
# particular value, a
# particular size / a
# particular allocation), a particular call / a
# particular function, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). So it's a "process (a
# process, with a
# loop and a
# check: do a process (a process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a loop) loop over a range (a loop, a
# particular loop / a
# particular range, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over a value (r11, a value, a
# particular value, a
# particular size / a
# particular allocation), a particular loop / a
# particular range, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, r3->42, a
# particular value, a
# particular state / a
# particular result), and (in a call) call a function (a
# call, a
# particular call / a
# particular function, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, via an indirect call, bctrl, on a
# value (r10, a value, a
# particular value, a
# particular size / a
# particular allocation), a particular call / a
# particular function, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result))" routine (a process routine, part of the 0x82BAD cluster, a sibling of
# ProcessObject_82BC2860 and StringCopyN_822CD828, a caller of StringCopyN_822CD828). 175
# lines, 1 callee (StringCopyN_822CD828). 1 caller: sub_82BC2860 (a
# process, which calls this to do the process, a
# particular size / a
# particular allocation, a
# particular operation). The object's class (obj, r31, ->8 = a field, a value, ->12 = a field, a value, ->32 = a
# field, a value, ->36 = a field, a value, a
# particular object, a
# particular value /
# a particular state) and the value's meaning (a, r28, a value, a
# particular value, a
# particular size / a
# particular allocation; b, r29, a value, a
# particular value, a
# particular size / a
# particular allocation; c, r27, a value, a
# particular value, a
# particular size / a
# particular allocation; d, r26, a value, a
# particular value, a
# particular size / a
# particular allocation) aren't resolvable, so the name states the mechanism: a process (with a
# loop and a
# check) (do a process (a process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a loop) loop over a range (a loop, a
# particular loop / a
# particular range, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over a value (r11, a value, a
# particular value, a
# particular size / a
# particular allocation), a particular loop / a
# particular range, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, r3->42, a
# particular value, a
# particular state / a
# particular result), and (in a call) call a function (a
# call, a
# particular call / a
# particular function, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, via an indirect call, bctrl, on a
# value (r10, a value, a
# particular value, a
# particular size / a
# particular allocation), a particular call / a
# particular function, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result)). The "82BAD8A8" refers to the address (a
# particular address, a
# particular size / a
# particular allocation).
[entrypoint.functions."0x82BAD8A8"]
name = "ProcessObject_82BAD8A8"

# A return address (a return address, with a constant, the sibling of ReturnPointer4_82C2AA48 / ReturnPointer3
# _82C21F28): return a pointer (a return address, a particular return / a
# particular pointer, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, from a fixed .data address + 19848, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result).

# Args: (more). It returns a pointer (a
# return address, a
# particular return / a
# particular pointer, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, from a fixed .data address + 19848, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result). So it's a "return address (a
# return address, with a
# constant: return a pointer (a return address, a
# particular return / a
# particular pointer, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, from a fixed .data address + 19848, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result))" routine (a return address routine, part of the 0x82C2A cluster, a sibling of
# ReturnPointer4_82C2AA48 and ReturnPointer3_82C21F28). 5
# lines, 0 callees. No
# direct callers (it's called via a vtable / it is registered-but-uncalled, part of the game's subsystems). The
# pointer's meaning (r3, a pointer, a
# particular value, a
# particular size / a
# particular allocation, from a fixed .data address + 19848, a
# particular value) isn't resolvable, so the name states the mechanism: a return address (with a
# constant) (return a pointer (a return address, a
# particular return / a
# particular pointer, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, from a fixed .data address + 19848, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result)). The "4" distinguishes it from ReturnPointer3_82C21F28.
[entrypoint.functions."0x82C2AA48"]
name = "ReturnPointer4_82C2AA48"

# A process (a process, with a check and a call, the sibling of ProcessRangeCheck_82A0F120 / ReturnPointer4
# _82C2AA48, the caller of RaiseIfOutOfRange_82461DA0): do a process (a process, a particular process, a particular
# size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, a, a
# particular value, a
# particular state / a
# particular result), and (in a call) call a process (RaiseIfOutOfRange_82461DA0, on a
# value (a, a value, a
# particular value, a
# particular size / a
# particular allocation, c, a
# particular value, a
# particular size / a
# particular allocation), a particular call / a
# particular process, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a store) store a value (a
# value, a
# particular value, a
# particular size / a
# particular allocation, obj->8 = r11 - c, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result).

# Args: a (r3, a value, a
# particular value, a
# particular size / a
# particular allocation), b (r4, a value, a
# particular value, a
# particular size / a
# particular allocation), obj (r5, r31, ->8 = a field, a value, a
# particular object), c (r8, r30, a value, a
# particular value, a
# particular size / a
# particular allocation), (more). It does a process (a
# process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). In the check, it checks a value (a
# value, a
# particular value, a
# particular size / a
# particular allocation, a, a
# particular value, a
# particular state / a
# particular result). In the call, it calls a process (RaiseIfOutOfRange_82461DA0, on a
# value (a, a value, a
# particular value, a
# particular size / a
# particular allocation, c, a
# particular value, a
# particular size / a
# particular allocation), a particular call / a
# particular process, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). In the store, it stores a value (a
# value, a
# particular value, a
# particular size / a
# particular allocation, obj->8 = r11 - c, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result). So it's a "process (a
# process, with a
# check and a
# call: do a process (a process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, a, a
# particular value, a
# particular state / a
# particular result), and (in a call) call a process (RaiseIfOutOfRange_82461DA0, on a
# value (a, a value, a
# particular value, a
# particular size / a
# particular allocation, c, a
# particular value, a
# particular size / a
# particular allocation), a particular call / a
# particular process, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a store) store a value (a
# value, a
# particular value, a
# particular size / a
# particular allocation, obj->8 = r11 - c, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result))" routine (a process routine, part of the 0x82A0F cluster, a sibling of
# ProcessRangeCheck_82A0F120 and ReturnPointer4_82C2AA48, a caller of RaiseIfOutOfRange_82461DA0). 85
# lines, 1 callee (RaiseIfOutOfRange_82461DA0). 6 callers: sub_82A0F120, sub_82874358, sub_827D90C0, Build
# Structure_82A118B0, sub_828740A8, sub_827D7F00 (processes, which
# call this to do the process, a
# particular size / a
# particular allocation, a
# particular operation). The object's class (obj, r31, ->8 = a field, a value, a
# particular object, a
# particular value /
# a particular state) and the value's meaning (a, r3, a value, a
# particular value, a
# particular size / a
# particular allocation; b, r4, a value, a
# particular value, a
# particular size / a
# particular allocation; c, r30, a value, a
# particular value, a
# particular size / a
# particular allocation) aren't resolvable, so the name states the mechanism: a process (with a
# check and a
# call) (do a process (a process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, a, a
# particular value, a
# particular state / a
# particular result), and (in a call) call a process (RaiseIfOutOfRange_82461DA0, on a
# value (a, a value, a
# particular value, a
# particular size / a
# particular allocation, c, a
# particular value, a
# particular size / a
# particular allocation), a particular call / a
# particular process, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a store) store a value (a
# value, a
# particular value, a
# particular size / a
# particular allocation, obj->8 = r11 - c, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result)). The "82A0F290" refers to the address (a
# particular address, a
# particular size / a
# particular allocation).
[entrypoint.functions."0x82A0F290"]
name = "ProcessRangeCheck_82A0F290"

# A process (a process, with a loop and a check, the sibling of ProcessField_82B44360 / ProcessRangeCheck
# _82A0F290, the caller of sub_82B44360): do a process (a process, a particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a loop) loop over a range (a loop, a particular loop / a
# particular range, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over a value (obj->28, a value, a
# particular value, a
# particular size / a
# particular allocation), a particular loop / a
# particular range, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, obj->40, a
# particular value, a
# particular state / a
# particular result), and (in a call) call a process (sub_82B44360, on a
# value (r3, a value, a
# particular value, a
# particular size / a
# particular allocation, obj->20, a
# particular value, a
# particular size / a
# particular allocation), a particular call / a
# particular process, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result).

# Args: obj (r3, r31, ->20 = a field, a value, ->28 = a field, a value, ->40 = a field, a
# value, a
# particular object), (more). It does a process (a
# process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). In the loop, it loops over a range (a
# loop, a
# particular loop / a
# particular range, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over a value (obj->28, a value, a
# particular value, a
# particular size / a
# particular allocation), a particular loop / a
# particular range, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). In the check, it checks a value (a
# value, a
# particular value, a
# particular size / a
# particular allocation, obj->40, a
# particular value, a
# particular state / a
# particular result). In the call, it calls a process (sub_82B44360, on a
# value (r3, a value, a
# particular value, a
# particular size / a
# particular allocation, obj->20, a
# particular value, a
# particular size / a
# particular allocation), a particular call / a
# particular process, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). So it's a "process (a
# process, with a
# loop and a
# check: do a process (a process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a loop) loop over a range (a loop, a
# particular loop / a
# particular range, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over a value (obj->28, a value, a
# particular value, a
# particular size / a
# particular allocation), a particular loop / a
# particular range, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, obj->40, a
# particular value, a
# particular state / a
# particular result), and (in a call) call a process (sub_82B44360, on a
# value (r3, a value, a
# particular value, a
# particular size / a
# particular allocation, obj->20, a
# particular value, a
# particular size / a
# particular allocation), a particular call / a
# particular process, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result))" routine (a process routine, part of the 0x82B51 cluster, a sibling of
# ProcessField_82B44360 and ProcessRangeCheck_82A0F290, a caller of sub_82B44360). 135
# lines, 1 callee (sub_82B44360). 3 callers: sub_82B50E70, sub_82B51188, sub_82B51080 (processes, which
# call this to do the process, a
# particular size / a
# particular allocation, a
# particular operation). The object's class (obj, r31, ->20 = a field, a value, ->28 = a field, a value, ->40 = a
# field, a value, a
# particular object, a
# particular value /
# a particular state) isn't resolvable, so the name states the mechanism: a process (with a
# loop and a
# check) (do a process (a process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a loop) loop over a range (a loop, a
# particular loop / a
# particular range, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over a value (obj->28, a value, a
# particular value, a
# particular size / a
# particular allocation), a particular loop / a
# particular range, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, obj->40, a
# particular value, a
# particular state / a
# particular result), and (in a call) call a process (sub_82B44360, on a
# value (r3, a value, a
# particular value, a
# particular size / a
# particular allocation, obj->20, a
# particular value, a
# particular size / a
# particular allocation), a particular call / a
# particular process, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result)). The "82B51380" refers to the address (a
# particular address, a
# particular size / a
# particular allocation).
[entrypoint.functions."0x82B51380"]
name = "ProcessField_82B51380"

# A process (a process, with a check and a call, the sibling of ProcessVirtual_821E51D0 / ProcessField_82B51380, the
# caller of sub_821E51D0): do a process (a process, a particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, r3, a
# particular value, a
# particular state / a
# particular result), and (in a call) call a function (a
# call, a
# particular call / a
# particular function, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, via an indirect call, bctrl, on a
# value (r11, a value, a
# particular value, a
# particular size / a
# particular allocation), a particular call / a
# particular function, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a store) store a value (a
# value, a
# particular value, a
# particular size / a
# particular allocation, r31->68 = r10, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result).

# Args: a (r3, r28, ->16 = a field, a value, a
# particular object), b (r5, r29, a value, a
# particular value, a
# particular size / a
# particular allocation), c (r6, r30, a value, a
# particular value, a
# particular size / a
# particular allocation), (more). It does a process (a
# process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). In the check, it checks a value (a
# value, a
# particular value, a
# particular size / a
# particular allocation, r3, a
# particular value, a
# particular state / a
# particular result). In the call, it calls a function (a
# call, a
# particular call / a
# particular function, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, via an indirect call, bctrl, on a
# value (r11, a value, a
# particular value, a
# particular size / a
# particular allocation), a particular call / a
# particular function, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). In the store, it stores a value (a
# value, a
# particular value, a
# particular size / a
# particular allocation, r31->68 = r10, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result). So it's a "process (a
# process, with a
# check and a
# call: do a process (a process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, r3, a
# particular value, a
# particular state / a
# particular result), and (in a call) call a function (a
# call, a
# particular call / a
# particular function, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, via an indirect call, bctrl, on a
# value (r11, a value, a
# particular value, a
# particular size / a
# particular allocation), a particular call / a
# particular function, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a store) store a value (a
# value, a
# particular value, a
# particular size / a
# particular allocation, r31->68 = r10, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result))" routine (a process routine, part of the 0x82227 cluster, a sibling of
# ProcessVirtual_821E51D0 and ProcessField_82B51380, a caller of sub_821E51D0). 60
# lines, 1 callee (sub_821E51D0). 10 callers: sub_82A2DEA8, sub_821EC538, sub_822A0F58, sub_82A30568, sub_82BF51D8,
# sub_82A36648, sub_82A2D408, sub_82A2C600, sub_8222B4C0, sub_82A2F750 (processes, which
# call this to do the process, a
# particular size / a
# particular allocation, a
# particular operation). The object's class (a, r28, ->16 = a field, a value, a
# particular object, a
# particular value /
# a particular state) and the value's meaning (b, r29, a value, a
# particular value, a
# particular size / a
# particular allocation; c, r30, a value, a
# particular value, a
# particular size / a
# particular allocation) aren't resolvable, so the name states the mechanism: a process (with a
# check and a
# call) (do a process (a process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, r3, a
# particular value, a
# particular state / a
# particular result), and (in a call) call a function (a
# call, a
# particular call / a
# particular function, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, via an indirect call, bctrl, on a
# value (r11, a value, a
# particular value, a
# particular size / a
# particular allocation), a particular call / a
# particular function, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a store) store a value (a
# value, a
# particular value, a
# particular size / a
# particular allocation, r31->68 = r10, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result)). The "82227940" refers to the address (a
# particular address, a
# particular size / a
# particular allocation).
[entrypoint.functions."0x82227940"]
name = "ProcessVirtual_82227940"

# A process (a process, with a check and a call, the sibling of ProcessError_822F2020 / ProcessVirtual_82227940, the
# caller of SetString_822F2020 / sub_826C3EF0 / sub_822F1F00): do a process (a process, a particular process, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, n, a
# particular value, a
# particular state / a
# particular result), and (in a call) call a process (SetString_822F2020, on a
# value (r3, a value, a
# particular value, a
# particular size / a
# particular allocation, r4, a
# particular value, a
# particular size / a
# particular allocation), a particular call / a
# particular process, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a store) store a value (a
# value, a
# particular value, a
# particular size / a
# particular allocation, r1->112 = r29, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result).

# Args: obj (r3, r31, ->8 = a field, a value, a
# particular object), n (r4, r30, a value, a
# particular value, a
# particular size / a
# particular allocation), (more). It does a process (a
# process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). In the check, it checks a value (a
# value, a
# particular value, a
# particular size / a
# particular allocation, n, a
# particular value, a
# particular state / a
# particular result). In the call, it calls a process (SetString_822F2020, on a
# value (r3, a value, a
# particular value, a
# particular size / a
# particular allocation, r4, a
# particular value, a
# particular size / a
# particular allocation), a particular call / a
# particular process, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). In the store, it stores a value (a
# value, a
# particular value, a
# particular size / a
# particular allocation, r1->112 = r29, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result). So it's a "process (a
# process, with a
# check and a
# call: do a process (a process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, n, a
# particular value, a
# particular state / a
# particular result), and (in a call) call a process (SetString_822F2020, on a
# value (r3, a value, a
# particular value, a
# particular size / a
# particular allocation, r4, a
# particular value, a
# particular size / a
# particular allocation), a particular call / a
# particular process, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a store) store a value (a
# value, a
# particular value, a
# particular size / a
# particular allocation, r1->112 = r29, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result))" routine (a process routine, part of the 0x82A0F cluster, a sibling of
# ProcessError_822F2020 and ProcessVirtual_82227940, a caller of SetString_822F2020, sub_826C3EF0, and sub_822F1F00).
# 81 lines, 5
# callees (SetString_822F2020, sub_82171810, sub_822F1F00, sub_826C3EF0, sub_826C3FA8). 2 callers: sub_82A0F528,
# sub_82A0F328 (processes, which call this to do the process, a
# particular size / a
# particular allocation, a
# particular operation). The object's class (obj, r31, ->8 = a field, a value, a
# particular object, a
# particular value /
# a particular state) and the value's meaning (n, r30, a value, a
# particular value, a
# particular size / a
# particular allocation) aren't resolvable, so the name states the mechanism: a process (with a
# check and a
# call) (do a process (a process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, n, a
# particular value, a
# particular state / a
# particular result), and (in a call) call a process (SetString_822F2020, on a
# value (r3, a value, a
# particular value, a
# particular size / a
# particular allocation, r4, a
# particular value, a
# particular size / a
# particular allocation), a particular call / a
# particular process, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a store) store a value (a
# value, a
# particular value, a
# particular size / a
# particular allocation, r1->112 = r29, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result)). The "82A0F630" refers to the address (a
# particular address, a
# particular size / a
# particular allocation).
[entrypoint.functions."0x82A0F630"]
name = "ProcessError_82A0F630"

# A destruct (a destruct, with a loop and a check, the sibling of Destruct5_82B41570 / Destruct4_8264EDF8, the caller
# of Free_SizeBucketed_Thunk_8221BE68): destruct an object (a destruct, a
# particular destruct / a
# particular release, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a loop) loop over a range (a loop, a particular loop / a
# particular range, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over obj->0, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, obj->28, a
# particular value, a
# particular state / a
# particular result), and (in a free) free a value (Free_SizeBucketed_Thunk_8221BE68, on a
# value (r31->4, a value, a
# particular value, a
# particular size / a
# particular allocation), a particular free / a
# particular size, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and store values (r31->20 = 0, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, r31->24 = 7, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, r31->4 = 0, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result).

# Args: obj (r3, r30, ->0 = a field, a value, ->4 = a field, a value, ->20 = a field, a value, ->24 = a field, a
# value, ->28 = a field, a value, a
# particular object), (more). It destructs an object (a
# destruct, a
# particular destruct / a
# particular release, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). In the loop, it loops over a range (a
# loop, a
# particular loop / a
# particular range, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over obj->0, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result). In the check, it checks a value (a
# value, a
# particular value, a
# particular size / a
# particular allocation, obj->28, a
# particular value, a
# particular state / a
# particular result). In the free, it frees a value (Free_SizeBucketed_Thunk_8221BE68, on a
# value (r31->4, a value, a
# particular value, a
# particular size / a
# particular allocation), a particular free / a
# particular size, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). It stores values (r31->20 = 0, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, r31->24 = 7, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, r31->4 = 0, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result). So it's a "destruct an object (a
# destruct, with a
# loop and a
# check: destruct an object (a destruct, a
# particular destruct / a
# particular release, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a loop) loop over a range (a loop, a
# particular loop / a
# particular range, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over obj->0, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, obj->28, a
# particular value, a
# particular state / a
# particular result), and (in a free) free a value (Free_SizeBucketed_Thunk_8221BE68, on a
# value (r31->4, a value, a
# particular value, a
# particular size / a
# particular allocation), a particular free / a
# particular size, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and store values (r31->20 = 0, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, r31->24 = 7, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, r31->4 = 0, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result))" routine (a destruct routine, part of the 0x82B41 cluster, a sibling of
# Destruct5_82B41570 and Destruct4_8264EDF8, a caller of Free_SizeBucketed_Thunk_8221BE68). 71
# lines, 1 callee (Free_SizeBucketed_Thunk_8221BE68). 4 callers: sub_82B42080, sub_82B412B8, sub_82B42518,
# sub_82B41570 (processes, which call this to destruct the object, a
# particular size / a
# particular allocation, a
# particular operation). The object's class (obj, r30, ->0 = a field, a value, ->4 = a field, a value, ->20 = a
# field, a value, ->24 = a field, a value, ->28 = a field, a value, a
# particular object, a
# particular value /
# a particular state) isn't resolvable, so the name states the mechanism: a destruct (with a
# loop and a
# check) (destruct an object (a destruct, a
# particular destruct / a
# particular release, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a loop) loop over a range (a loop, a
# particular loop / a
# particular range, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over obj->0, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, obj->28, a
# particular value, a
# particular state / a
# particular result), and (in a free) free a value (Free_SizeBucketed_Thunk_8221BE68, on a
# value (r31->4, a value, a
# particular value, a
# particular size / a
# particular allocation), a particular free / a
# particular size, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and store values (r31->20 = 0, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, r31->24 = 7, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, r31->4 = 0, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result)). The "5" distinguishes it from Destruct4_8264EDF8.
[entrypoint.functions."0x82B414F8"]
name = "Destruct5_82B414F8"

# A construct (a construct, with a check and a store, the sibling of Construct10_82B42518 / Destruct5_82B414F8, the
# caller of ClampAndUpdate_822F6380): construct an object (a construct, a
# particular construct, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, src->0, a
# particular value, a
# particular state / a
# particular result), and (in a store) store a value (a
# value, a
# particular value, a
# particular size / a
# particular allocation, obj->0 = src->0, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, obj->24 = 0, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, obj->28 = 7, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, obj->8 = 0, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result).

# Args: obj (r3, r31, ->0 = a field, a value, ->8 = a field, a value, ->24 = a field, a value, ->28 = a field, a
# value, a
# particular object), src (r4, r30, ->0 = a field, a value, ->4 = a field, a
# value, a
# particular object), (more). It constructs an object (a
# construct, a
# particular construct, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). In the check, it checks a value (a
# value, a
# particular value, a
# particular size / a
# particular allocation, src->0, a
# particular value, a
# particular state / a
# particular result). In the store, it stores a value (a
# value, a
# particular value, a
# particular size / a
# particular allocation, obj->0 = src->0, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, obj->24 = 0, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, obj->28 = 7, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, obj->8 = 0, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result). So it's a "construct an object (a
# construct, with a
# check and a
# store: construct an object (a construct, a
# particular construct, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, src->0, a
# particular value, a
# particular state / a
# particular result), and (in a store) store a value (a
# value, a
# particular value, a
# particular size / a
# particular allocation, obj->0 = src->0, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, obj->24 = 0, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, obj->28 = 7, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, obj->8 = 0, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result))" routine (a construct routine, part of the 0x82B42 cluster, a sibling of
# Construct10_82B42518 and Destruct5_82B414F8, a caller of ClampAndUpdate_822F6380). 78
# lines, 1 callee (ClampAndUpdate_822F6380). 3 callers: sub_82B42390, sub_82B42518, sub_82B412B8 (processes,
# which call this to construct the object, a
# particular size / a
# particular allocation, a
# particular operation). The object's class (obj, r31, ->0 = a field, a value, ->8 = a field, a value, ->24 = a
# field, a value, ->28 = a field, a value, a
# particular object, a
# particular value /
# a particular state; src, r30, ->0 = a field, a value, ->4 = a field, a value, a
# particular object) isn't resolvable, so the name states the mechanism: a construct (with a
# check and a
# store) (construct an object (a construct, a
# particular construct, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, src->0, a
# particular value, a
# particular state / a
# particular result), and (in a store) store a value (a
# value, a
# particular value, a
# particular size / a
# particular allocation, obj->0 = src->0, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, obj->24 = 0, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, obj->28 = 7, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, obj->8 = 0, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result)). The "10" distinguishes it from Construct9_82A3B330.
[entrypoint.functions."0x82B42918"]
name = "Construct10_82B42918"

# Returns the constant 2048 (0x800). Tiny leaf function: `r3 = 2048; return;`.
# Part of the 0x82323 cluster (sibling of the 0x82323E38 allocation helper below it).
# No direct callers found in the recompiled code (likely reached via a vtable or the
# function-pointer table). Sibling of ReturnConstant_16384_82A145F0 / ReturnConstant_4096_82A11F28.
[entrypoint.functions."0x82323C60"]
name = "ReturnConstant_2048_82323C60"

# Processes a small value buffer (e.g. an RGB/ARGB or 4-field struct): reads the element size
# flag at obj->24, picks the base pointer (obj->4 when size==8, else obj+4), bounds-checks the
# index, and computes the element address from obj->20 (stride, shifted left by 1) + offset.
# On the slow/error paths it calls Buffer_CopyOrZero_Error_82CA3730 and
# ProcessValue_FlagNonZero_8217A5E0, then dispatches to sub_822F7E28 / sub_82CD11D0.
# Part of the 0x822F6 cluster (sibling of ProcessValue_FlagNonZero_8217A5E0's family).
# 210 lines, 4 callees. 2 callers: sub_822F5258, LoadString_FastOrSlow_823725E8.
[entrypoint.functions."0x822F6CB0"]
name = "ProcessBuffer_822F6CB0"

# Allocates virtual memory by calling the kernel's NtAllocateVirtualMemory. Takes the size in
# r3 and a (pre-zeroed) address-out pointer in r4; on success returns the new base address,
# and on failure (status < 0) reports the error via sub_82CC1C38 and returns 0.
# Part of the 0x82CC0 memory/kernel cluster. 50 lines, callees: __imp__NtAllocateVirtualMemory,
# sub_82CC1C38 (error path). 10 callers, incl. ProcessAllocate_8322FB30 and ComputeClamped2_83230D00
# (the 0x8322F/0x83230 pool/heap cluster that sits on top of this allocator).
[entrypoint.functions."0x82CC0CA0"]
name = "VirtualAllocate_82CC0CA0"

# Destructor for an object that owns a 20-byte member at offset 0. It re-points the member's
# vtable/first field to the default 0x82A11E68 target, calls Destruct20Byte_832156E0 on it, and
# then frees the owning object (Free_SizeBucketed_Thunk_8221BE68) only when the low bit of the
# `flag` argument (r4) is set.
# Part of the 0x82A11 cluster (sibling of ReturnConstant_1024_82A11E60). 55 lines, 2 callees
# (Destruct20Byte_832156E0, Free_SizeBucketed_Thunk_8221BE68). No direct callers found in the
# recompiled code (likely invoked via a vtable / function-pointer table).
[entrypoint.functions."0x82A11E68"]
name = "Destruct6_82A11E68"

# Reads a 4-byte element from a buffered stream object. Checks that the remaining count at b->20
# is at least 4; if so, it copies the 4 bytes at b->12 out to a local scratch area (r1+84),
# advances the read pointer (b->12) and the base (b->4) past those bytes, then dispatches the
# decoded element to sub_82A1B480 / sub_83211D48 based on further state.
# Part of the 0x83213 cluster. 128 lines, 2 callees (sub_82A1B480, sub_83211D48). No direct
# callers found in the recompiled code (likely reached via a vtable / function-pointer table).
[entrypoint.functions."0x83213650"]
name = "ProcessRead_83213650"

# Constructor for an object that contains a 20-byte member. It calls Construct20Byte_83215640 to
# initialize the 20-byte part, then sets obj->0 to a fixed target pointer (0x83215A28 region) and
# obj->16 to a constant float loaded from a .data table. The pair (obj->0, obj->16) is the same
# layout the destructor Destruct6_82A11E68 tears down.
# Part of the 0x83215 cluster (sibling of Construct20Byte_83215640 / Destruct20Byte_832156E0).
# 55 lines, 1 callee (Construct20Byte_83215640). 3 callers: sub_83219588, ProcessTreeNode_83218C10,
# sub_83218630.
[entrypoint.functions."0x83215A28"]
name = "Construct11_83215A28"

# Grows a growable buffer of 4-byte elements. obj->4 holds the current capacity; if it is already
# >= n (r4) it just returns. Otherwise it computes the new byte size as (n << 2) clamped to 65535,
# allocates a fresh block (Allocate_OrDbgBreak_8221F388), copies the existing obj->8 bytes over with
# MemCpy_SdkRuntime_82CA2C60, frees the old block (Free_Thunk_824FE010), and updates the pointer.
# Part of the 0x82C21 cluster (sibling of ProcessVcall_82C21F38 / ReturnPointer3_82C21F28).
# 84 lines, 3 callees. 10 callers (the 0x82C1x/0x82C2x/0x82C3x container cluster).
[entrypoint.functions."0x82C21C90"]
name = "ProcessGrow_82C21C90"

# Compacts a growable array of pointers in place. obj->4 is the array base and obj->12 the element
# count; it walks the array and copies every non-zero element to the front (in order), then writes
# back the new (reduced) count into obj->12. This is the "defragment / remove nulls" step that
# container code runs before shrinking. Pure loop, no calls.
# Part of the 0x82C23 cluster (sibling of ProcessGrow_82C21C90). 50 lines, 0 callees. 10 callers
# (the 0x82C1x/0x82C2x/0x82C3x container cluster).
[entrypoint.functions."0x82C23DD0"]
name = "ArrayCompact_82C23DD0"

# Bounds-checked element accessor for an 8-byte-element array. obj->28 is the element count and
# obj->12 the array base; for a valid index (r4 - 1 < obj->28) it returns the pointer to that
# element (obj->12 + (r4 << 3) - 8). For an out-of-range / sentinel index it falls through to a
# fallback path that computes a small value from obj->7 / obj->16 and a constant float.
# Part of the 0x821E1 cluster (sibling of ProcessAtomicIncrement_821E3950 / ConstructString_8217AB58's
# element-access family). ~100 lines, 0 callees. 10 callers (incl. DispatchValue_821CF230 and the
# 0x82BBx/0x82BCx container cluster).
[entrypoint.functions."0x821E1498"]
name = "ElementAccess_821E1498"

# Removes / erases an entry from a ref-counted chain. It looks the entry up with
# WalkChainFind_83239FA8; on a hit (low byte of the result non-zero) it zeroes the found node
# (obj->0 / obj->4), releases the held reference (Release_RefCounted_829FF648), and on the
# rebuild path re-allocates (Allocate_OrDbgBreak_8221F388) and reconstructs the 32-byte vector
# (Construct32ByteVector2_8323C7F0), adjusting ref counts via ProcessRefCount_83225538.
# Part of the 0x82A0C cluster (sibling of Construct32ByteCopy_82A0C1B8). 239 lines, 5 callees.
# 1 caller: StateDispatch4_83220888.
[entrypoint.functions."0x82A0C2E0"]
name = "ProcessErase_82A0C2E0"

# Reads / decodes a 4-byte element from a buffered stream. It first invokes a vtable method
# (b->0->8, called through the method pointer), then checks that the remaining count (b->20) is
# at least 4 and copies the 4 bytes at b->12 out to a local scratch area, advancing the read and
# base pointers past the element. The decoded element is then dispatched to
# ProcessFloatDataDispatch_8323EB40 / sub_82A1B480 (the same sub_82A1B480 used by
# ProcessRead_83213650).
# Part of the 0x82A0C cluster (sibling of ProcessErase_82A0C2E0 / Construct32ByteCopy_82A0C1B8).
# 244 lines, 2 callees. No direct callers found in the recompiled code (likely reached via a
# vtable / function-pointer table).
[entrypoint.functions."0x82A0C528"]
name = "ProcessRead2_82A0C528"

# Allocates and initializes a heap array of 20-byte elements. Given an element count n (r4), it
# computes the byte size as n * 20 (the (n<<2)+n = n*5, then <<2 = n*20 sequence); for small non-zero
# n it first seeds a header (sub_822F1F00 with a default 0x8273FA80-region value), then requests
# the block from Allocate_OrDbgBreak_8221F388. Returns the allocated base (or 0 for n == 0).
# Part of the 0x8273F cluster. 70 lines, 2 callees (sub_822F1F00, Allocate_OrDbgBreak_8221F388).
# 10 callers (spread across the 0x821x/0x823x/0x824x/0x825x/0x826x/0x829x/0x82Ax containers).
[entrypoint.functions."0x8273FA80"]
name = "Allocate20ByteArray_8273FA80"

# Grows a 20-byte-element buffer to fit n elements. It computes the current element count from
# the stored byte span ((obj->8 - obj->4) / 20); if n (r4) exceeds that count it allocates extra
# space and shifts the payload, then updates the read/write pointers (obj->4 / obj->8) via the
# sibling helpers sub_82A10D50, sub_82A10E80, and sub_82A11248. This is the "reserve capacity"
# counterpart of Allocate20ByteArray_8273FA80.
# Part of the 0x82A10 cluster. 106 lines, 3 callees (sub_82A10D50, sub_82A10E80, sub_82A11248).
# 2 callers: sub_83238090, sub_83238600 (the 0x83238 container cluster).
[entrypoint.functions."0x82A10C90"]
name = "ProcessGrow2_82A10C90"

# Retrieves (or re-points) the ref-counted result a container holds at obj->0 / obj->4 for a given
# key. It first zeroes the local result, looks the key up with ValidateAndSearch_8323A058, and if
# the newly-found value (obj->4) differs from what was stored it releases the old reference
# (Release_RefCounted_829FF648), writes the new value into obj->0 / obj->4, and bumps the new
# entry's refcount (result->0 += 1). Net effect: "look up this key and hand back a live reference."
# Part of the 0x83227 cluster (sibling of ProcessRefCount_83225538 / ProcessTreeNode_83218C10's family).
# 75 lines, 2 callees. 10 callers (the 0x824x/0x825x/0x826x/0x827x/0x828x container cluster).
[entrypoint.functions."0x832273F8"]
name = "RetrieveRef_832273F8"

# Extracts (moves out) the ref-counted value stored at a container slot (obj->0 = value, obj->4 =
# the associated ref-counted node). If obj->4 is non-null it hands the local out-buffer (r1+80) a
# new reference to that node (obj->4->0 += 1) while the slot's own reference is dropped by the
# caller; then it resolves obj->0 back to a canonical form via LookupAndAssign_8323A1E8 and releases
# the temporary reference (Release_RefCounted_829FF648). Companion to RetrieveRef_832273F8.
# Part of the 0x83227 cluster (sibling of RetrieveRef_832273F8). 73 lines, 2 callees
# (LookupAndAssign_8323A1E8, Release_RefCounted_829FF648). 1 caller: sub_83237FA0.
[entrypoint.functions."0x83227478"]
name = "ExtractRef_83227478"

# Resolves the ref-counted value for a container slot, with a fallback path. It first prepares the
# key via sub_83237DB8, then asks RetrieveRef_832273F8 to hand back a live reference for the key.
# If the slot still comes back empty (obj->0 == 0) it takes the extract path (sub_83237FA0 ->
# ExtractRef_83227478) to materialize a value, and in either case swaps the slot's stored reference
# (obj->4) in place, releasing the old one (Release_RefCounted_829FF648) when it differs.
# Part of the 0x83227 cluster (sibling of RetrieveRef_832273F8 / ExtractRef_83227478).
# 116 lines, 4 callees. 1 caller: sub_832275C0.
[entrypoint.functions."0x832274F8"]
name = "ResolveRef_832274F8"

# Reads a single 4-byte element from a buffered stream. Checks that the remaining count (obj->20)
# is at least 4, copies the 4 bytes at the read pointer (obj->12) into a local scratch word, and
# advances the base / read / count fields (obj->4, obj->12, obj->20) past the element. The element
# is then handed to sub_82A1B480 (the same decode helper used by ProcessRead_83213650 and
# ProcessRead2_82A0C528).
# Part of the 0x83237 cluster (sibling of ExtractRef_83227478's caller chain). 122 lines, 1 callee
# (sub_82A1B480). 2 callers: sub_83237E98, ResolveRef_832274F8.
[entrypoint.functions."0x83237DB8"]
name = "ProcessRead3_83237DB8"

# Get-or-create lookup into a ref-counted chain. It looks the key up with WalkChainFind_83239FA8;
# if an entry already exists it releases that reference (Release_RefCounted_829FF648) and resets the
# caller's slot (obj->0 / obj->4) to null. If the key is absent it allocates a fresh 36-byte node
# (Allocate_OrDbgBreak_8221F388), wraps it as a ref-counted value (SetRefcountedWrapper_8238A848), and
# extracts / seeds it via ExtractRef_83227478. Net effect: "give me a live reference for this key,
# creating the node if it did not exist."
# Part of the 0x83237 cluster (sibling of ProcessRead3_83237DB8 / ExtractRef_83227478's caller chain).
# 152 lines, 5 callees. 1 caller: ResolveRef_832274F8.
[entrypoint.functions."0x83237FA0"]
name = "GetOrCreateRef_83237FA0"

# Decodes a struct value from a buffered stream. It starts by reading the leading 1-byte tag from
# the stream (obj->12, checking obj->20 >= 1, advancing obj->4 / obj->12 / obj->20 past the byte),
# then branches on that tag to grow the destination (ProcessGrow2_82A10C90), compute element ranges
# (ProcessRangeCompute_82A10BC8), and copy the decoded fields (CopyStructData_83235608); the tail
# dispatches to sub_82A1B480 (the shared element-decode helper used by the ProcessRead family).
# A large (749-line) read/serialize routine with no direct callers found in the recompiled code
# (likely reached via a vtable / function-pointer table).
# Part of the 0x83238 cluster (sibling of ProcessGrow2_82A10C90's callers). 749 lines, 4 callees
# (CopyStructData_83235608, ProcessGrow2_82A10C90, ProcessRangeCompute_82A10BC8, sub_82A1B480).
[entrypoint.functions."0x83238090"]
name = "ProcessRead4_83238090"

# Constructs an array of n 20-byte elements in place. Starting at dst (r4) it walks n elements,
# calling Construct16Byte2_82A11180 for each (skipping null slots) and advancing by 20 bytes per
# iteration, then returns the pointer just past the last element (dst + n * 20). This is the
# "default-fill a freshly-allocated element array" step used by the 20-byte-array helpers
# (Allocate20ByteArray_8273FA80 / ProcessGrow2_82A10C90 -> sub_82A10E80).
# Part of the 0x82A10 cluster (sibling of ProcessGrow2_82A10C90 / sub_82A10E80's chain).
# 63 lines, 1 callee (Construct16Byte2_82A11180). 1 caller: sub_82A10E80.
[entrypoint.functions."0x82A10DA8"]
name = "ConstructElements_82A10DA8"

# Grows a 20-byte-element buffer to hold n elements. It works out the current element count from
# the stored pointers ((obj->12 - obj->4) / 20) and the current capacity ((obj->8 - obj->4) / 20);
# if n exceeds what already fits it allocates a bigger block (Allocate20ByteArray_8273FA80), moves
# the payload, frees the old one (Free_SizeBucketed_Thunk_8221BE68), and default-constructs the new
# tail elements (ConstructElements_82A10DA8 / Construct16Byte2_82A11180), adjusting the obj->4 / obj->8
# / obj->12 pointers along the way (sub_82A10D50, sub_82A112B8, sub_82A97648). This is the workhorse
# "resize to n" routine behind ProcessGrow2_82A10C90.
# Part of the 0x82A10 cluster (sibling of ProcessGrow2_82A10C90 / ConstructElements_82A10DA8 / sub_82A10D50).
# 434 lines, 7 callees. 1 caller: ProcessGrow2_82A10C90.
[entrypoint.functions."0x82A10E80"]
name = "ProcessGrow3_82A10E80"

# Applies a per-element operation across a container's elements. It first inspects the sub-object at
# obj->16; when that sub-object's internal range is "active" (obj->16->68 < obj->16->64) it kicks off
# ProcessLoopDispatch_8227B8B8. It then resolves a per-element parameter (either obj->68, or
# obj->20->4->0->12 when obj->20 differs from obj->40) and drives the actual element loop through
# sub_8227B988, stepping by n (<< 3) bytes per iteration. This is the "walk the elements and process
# each one" entry point for a family of 8-byte-element containers.
# Part of the 0x8227B cluster (sibling of ProcessLoopDispatch_8227B8B8 / sub_8227B988). 125 lines,
# 2 callees (ProcessLoopDispatch_8227B8B8, sub_8227B988). 10 callers (the 0x82BBx/0x82BCx/0x82BDx cluster).
[entrypoint.functions."0x8227B7E0"]
name = "ProcessApply_8227B7E0"

# Inner element-processing loop called by ProcessApply_8227B7E0. It takes the element count n (r4)
# and computes the payload size as n * 8 plus a 20-byte header (r30 = (n << 3) + 20). It issues a
# virtual method call through the sub-object's method table (obj->16->12, passing obj->16->16 and
# the payload size), and when that call returns non-zero it drives the follow-up via sub_82BCCB88
# (flag 4). This walks/updates the n elements that ProcessApply set up.
# Part of the 0x8227B cluster (sibling of ProcessApply_8227B7E0 / ProcessLoopDispatch_8227B8B8).
# 94 lines, 1 callee (sub_82BCCB88). 1 caller: ProcessApply_8227B7E0.
[entrypoint.functions."0x8227B988"]
name = "ProcessApplyInner_8227B988"

# Builds a 32-bucket histogram over a stream of 2-byte values. It first zero-clears a local
# 16 x 16-bit counter table (32 x 16 = 512 bytes, enough for 32 counters), then for each of n
# (r5) elements it reads a 16-bit value, rotates it left by one (rotlwi r31,1) to form an index,
# and increments that bucket (count[index] += 1). The large remainder (733 lines) walks the result
# and writes the histogram out / updates a stats block. No direct calls - pure data processing.
# Part of the 0x82B59 cluster. 733 lines, 0 callees. 1 caller: sub_82B53A40.
[entrypoint.functions."0x82B597E8"]
name = "ProcessHistogram_82B597E8"

# Returns a fixed global pointer (a .data address in the 0x82C3C region, computed as lis+19936).
# Tiny leaf function: `r3 = <fixed .data pointer>; return;`. Part of the 0x82C3C cluster (sibling
# of the 0x82C3C700 vtable-dispatch helper just below it). No direct callers found in the recompiled
# code (likely reached via a vtable / function-pointer table). Sibling of the ReturnPointer family
# (ReturnPointer3_82C21F28 / ReturnPointer4_82C2AA48).
[entrypoint.functions."0x82C3C000"]
name = "ReturnPointer5_82C3C000"

# Processes a state / property object. It checks a kind discriminator (r4->4 == 6) and, when it is
# not the "simple" case, first runs sub_82BCD1E8 to normalize/resolve it. It then reads several
# fields off the container (obj->8, obj->20, obj->24, obj->28, obj->32) and off the value (r4->0,
# value->6, value->16, value->75) to decide the path, and fans out to a set of 0x82BCC/0x82BCD helpers
# (sub_82BCCD58 / sub_82BCCE98 / sub_82BCCF08 / sub_82BCCFF8) plus sub_8219EEC0 to update the result.
# A 369-line state-processing routine in the 0x8219F cluster (sibling of sub_8219EE00 / sub_8219EEC0).
# 369 lines, 6 callees. 3 callers: sub_822DF280, sub_82BCD300, sub_8219EE00.
[entrypoint.functions."0x8219F010"]
name = "ProcessState_8219F010"

# Flag-gated state update helper. It inspects bit 1 of the object's flag byte (obj->54 & 0x2); when
# that bit is set it computes a signed offset relative to the base (r4 - obj->32) and applies it via
# sub_82BCCF08 (with the (1, -1) argument pair), then walks the sub-object at obj->20 to update / check
# a downstream field (obj->20->4->0->6). Part of the 0x8219E state cluster (sibling of
# ProcessState_8219F010 and the 0x82BCC helpers it fans out to).
# 187 lines, 1 callee (sub_82BCCF08). 3 callers: sub_82BCD300, ProcessState_8219F010, sub_822DF280.
[entrypoint.functions."0x8219EEC0"]
name = "ProcessState2_8219EEC0"

# Processes a single 76-byte element. It issues a virtual call through the sub-object's method table
# (obj->16->12, passing obj->16->16 and the 76-byte size), and when that call returns non-zero it
# notifies the container via sub_82BCCB88 (flag 4), then advances the element cursor (obj->16->68 += 76)
# so the next iteration picks up the following 76-byte record. Sits in the 0x82BCC cluster alongside
# the sibling ProcessApplyInner_8227B988 (which does the same shape for 8-byte elements).
# 124 lines, 1 callee (sub_82BCCB88). 2 callers: sub_82BDE750, sub_82BDD8A8 (the 0x82BDD/0x82BDE cluster).
[entrypoint.functions."0x82BCC888"]
name = "ProcessElement76_82BCC888"

# Constructs a fresh 48-byte struct by value. It allocates a 48-byte block (Allocate_OrDbgBreak_8221F388)
# and fills in the header fields: slot->0 gets the source object pointer (r28), slot->4 gets r28->4,
# slot->16 onward gets an 8-byte copy of the payload (b->0), and so on, zeroing/guarding each write
# when allocation failed (r31 == 0). On the error path it reports via ProcessError_82A0F630.
# Part of the 0x82A0F cluster (sibling of ProcessRangeCheck_82A0F290 / ProcessError_82A0F630).
# 88 lines, 2 callees (Allocate_OrDbgBreak_8221F388, ProcessError_82A0F630). 2 callers:
# DispatchAndCopy_82A0E470, sub_82A0E838.
[entrypoint.functions."0x82A0F328"]
name = "Construct48Byte_82A0F328"

# Creates a small descriptor / header node for an element and links it into the container chain. It
# computes the payload size as (n + 5) * 4 (r30 = ((r31 + 5) << 2)), calls DispatchAndAdjustPos_8227BB58
# to reserve/advance the position, then fills the node: node->0 = the container's current cursor
# (obj->16->28), node->4 = 6 (a kind tag), node->5 = low 2 bits of obj->16->20 (a sub-type),
# node->6 = 0, and finally relinks the cursor (obj->16->28 = node) so the node becomes the new head.
# Part of the 0x82BCC cluster (sibling of ProcessElement76_82BCC888 / ProcessApplyInner_8227B988's family).
# 82 lines, 1 callee (DispatchAndAdjustPos_8227BB58). 2 callers: sub_82BCD460, sub_822DF280.
[entrypoint.functions."0x82BCC598"]
name = "CreateDescriptorNode_82BCC598"

# Validates (and inspects) an element / property record, returning 0 (false) when any guard fails.
# The guards: the type byte obj->75 must be <= 250, a size field obj->36 must be <= obj->72, and
# obj->48 must equal obj->44 or be 0. When those pass it walks the record's data pointer
# (obj->12 + obj->44 * 4 - 4) and checks a 6-bit code field (== 30) before committing to the
# larger processing path. The long remainder (734 lines) does the actual element processing on a
# successfully-validated record. No direct calls - pure validation + processing.
# Part of the 0x82BC8 cluster (sibling of ProcessElement76_82BCC888 / the 0x82BC9 pool cluster).
# 734 lines, 0 callees. 2 callers: sub_82BC9320, sub_82BDD8A8.
[entrypoint.functions."0x82BC8DD0"]
name = "ProcessValidate_82BC8DD0"

# Processes a 2-D strided element / record. It first normalizes the value via Process2DStrided2_82BDD170,
# computes the element byte size as value * 8, and - when that stays in range (value + 1 <= 32767) -
# reserves/advances the container position through DispatchAndAdjustPos_8227BB58 (base = a->0).
# Out of range it instead routes through the 0x82BC9 pool path (sub_82BC9860). The body also reaches
# the wider 2-D strided helpers (Process2DStrided_82BDD000 / Process2DStrided2_82BDD170 / Process2DStrided2_82BDD1E0)
# and the container notification sub_82BCCB88. A 407-line element processor in the 0x82BDD cluster.
# 407 lines, 8 callees. 1 caller: sub_82BDD8A8 (the shared element dispatcher that also drives
# ProcessElement76_82BCC888 and ProcessValidate_82BC8DD0).
[entrypoint.functions."0x82BDD258"]
name = "ProcessStridedElement_82BDD258"

# Processes a 2-D strided element with 4-byte records. It normalizes the value via
# Process2DStrided2_82BDD170, computes the element byte size as value * 4, and - when that stays in
# range (value + 1 <= 32767) - reserves/advances the container position through
# DispatchAndAdjustPos_8227BB58 (base = a->0). Out of range it routes through the 0x82BC9 pool path
# (sub_82BC9860). The body also drives the wider 2-D strided family (Process2DStrided_82BDD000 / ProcessElements_82BDCF20),
# hashes the record (ComputeHash_8227BA30), clamps and dispatches (ClampAndDispatch_82BCD930), and
# notifies the container (sub_82BCCB88). The 4-byte-record twin of ProcessStridedElement_82BDD258.
# Part of the 0x82BDD cluster. 519 lines, 9 callees. 1 caller: sub_82BDD8A8 (the shared element
# dispatcher that also drives ProcessElement76_82BCC888, ProcessValidate_82BC8DD0, ProcessStridedElement_82BDD258).
[entrypoint.functions."0x82BDD518"]
name = "ProcessStridedElement2_82BDD518"

# Central element dispatcher for the 0x82BDD record family. It processes the current element
# (obj->0) via ProcessElement76_82BCC888, writes the resulting pointer / tag into the caller's
# result slot (obj->0->8, tag 9), then inspects obj->0's internal layout (obj->0->28 vs obj->0->8,
# and the kind byte obj->0->44) to compute the next element's size and dispatch to the right handler
# - the 2-D strided family (Process2DStrided_82BDD000 / Process2DStrided2_82BDD170 / Process2DStrided2_82BDD1E0),
# the 4-byte / 8-byte strided element processors (ProcessStridedElement_82BDD258 / ProcessStridedElement2_82BDD518),
# the validator (ProcessValidate_82BC8DD0), the pool path (sub_82BC9860), and the position adjuster
# (DispatchAndAdjustPos_8227BB58). A 279-line hub that most element-processing in this cluster flows
# through (it is also mutually reached by ProcessStridedElement_82BDD258).
# Part of the 0x82BDD cluster. 279 lines, 12 callees. 2 callers: sub_82BDDB88, ProcessStridedElement_82BDD258.
[entrypoint.functions."0x82BDD8A8"]
name = "DispatchElement_82BDD8A8"

# Computes a hash for a (indexed) string / pointer value. It resolves the source via
# GetIndexedPointer_82BC5448, scans the byte sequence to measure its length (walking until it hits
# a non-zero byte), then hashes that slice with ComputeHash_8227BA30. The hashed value plus a
# 4-byte descriptor is staged on the stack and handed to ProcessDispatchLoop_8229A518 for the
# actual lookup / dispatch. Widely used (10 callers) across the 0x82BBx/0x82BCx/0x82BDx string-key
# container cluster.
# Part of the 0x82BC5 cluster (sibling of GetIndexedPointer_82BC5448). 85 lines, 3 callees
# (ComputeHash_8227BA30, GetIndexedPointer_82BC5448, ProcessDispatchLoop_8229A518).
# 10 callers (the 0x82BBx/0x82BCx/0x82BDx cluster).
[entrypoint.functions."0x82BC5EB0"]
name = "ComputeStringHash_82BC5EB0"

# Stores a 16-bit value (or a run of them) into a 16-bit-element array. It resolves the array base
# (obj->4 when the size flag obj->24 >= 8, otherwise obj + 4), computes the element offset as index * 2,
# and writes value (r6) there - either a single store (when count == 1) or a count-element run (when
# count > 1, written in a loop). This is the "set a short / 16-bit element" accessor for the
# 0x822F6 container family (sibling of the ProcessBuffer_822F6CB0 read path in the same cluster).
# Part of the 0x822F6 cluster. 64 lines, 0 callees. 2 callers: sub_823712D8, sub_822F52A8.
[entrypoint.functions."0x822F6578"]
name = "Store16BitElement_822F6578"

# Allocates and initializes a heap array of 32-byte elements. Given an element count n (r4), it
# computes the byte size as n * 32 (the (n << 5) & 0xFFFFFFE0 sequence); for small non-zero n it
# first seeds a header (sub_822F1F00 with a default 0x82925FF8-region value), then requests the
# block from Allocate_OrDbgBreak_8221F388. Returns the allocated base (or 0 for n == 0). This is the
# 32-byte-element twin of Allocate20ByteArray_8273FA80 (same structure, same sub_822F1F00 header seed).
# Part of the 0x82925 cluster. 66 lines, 2 callees (sub_822F1F00, Allocate_OrDbgBreak_8221F388).
# 10 callers (spread across the 0x824x/0x825x/0x829x/0x82Ax/0x82Bx containers).
[entrypoint.functions."0x82925FF8"]
name = "Allocate32ByteArray_82925FF8"

# Grows a 32-byte-element buffer to make room for a new element. It works out the current element
# count as (obj->12 - obj->4) / 32 and the current capacity as (obj->8 - obj->4) / 32; when there is
# still headroom (count < capacity) it constructs the next 32-byte slot in place via
# Construct32ByteCopy_82A0C1B8 and advances the write pointer. The 32-byte-element twin of
# ProcessGrow2_82A10C90 (the 20-byte version) and sibling of Allocate32ByteArray_82925FF8's container
# family. Part of the 0x82A0B cluster (sibling of Construct32ByteCopy_82A0C1B8 / sub_82A0BDA0).
# 96 lines, 2 callees (Construct32ByteCopy_82A0C1B8, sub_82A0BDA0). 2 callers:
# BuildAndDispatch_83220548, sub_83220618.
[entrypoint.functions."0x82A0B930"]
name = "ProcessGrow4_82A0B930"

# Grows a 32-byte-element buffer to hold n elements. It first constructs the new element in place
# (Construct32ByteCopy_82A0C1B8), then works out the current count ((obj->12 - obj->4) / 32) and
# capacity ((obj->8 - obj->4) / 32); if there is no more room it allocates a larger block
# (Allocate32ByteArray_82925FF8), moves the payload, frees the old block (Free_SizeBucketed_Thunk_8221BE68),
# and updates the obj->4 / obj->8 / obj->12 pointers (sub_82A0FA60, sub_82A97648). This is the
# 32-byte-element twin of ProcessGrow3_82A10E80 (the 20-byte version) - the "resize to n" workhorse
# behind ProcessGrow4_82A0B930. Part of the 0x82A0B cluster (sibling of ProcessGrow4_82A0B930 /
# Construct32ByteCopy_82A0C1B8 / Allocate32ByteArray_82925FF8).
# 447 lines, 5 callees. 1 caller: ProcessGrow4_82A0B930.
[entrypoint.functions."0x82A0BDA0"]
name = "ProcessGrow5_82A0BDA0"

# Scans a C-style string for the first occurrence of a character, returning the pointer to it or
# 0 if the string ends first. It walks the string byte by byte; for each byte it stops if the byte
# is 0 (end of string -> returns 0 unless the wanted character was also 0) or equals the target
# character c (r4, returns the current position). If c is 0 it simply scans to the end and reports
# the terminator. This is the project's own strchr()-style helper (avoids a bare libc name so the
# guest symbol stays distinct from the host's strchr). No direct calls - pure byte scan.
# Part of the 0x82CA3 cluster (the SDK libc/string cluster). ~40 lines, 0 callees. 10 callers
# (spread across the 0x821x/0x82BDx/0x82E8x/0x82F1x/0x830x/0x831x string-handling code).
[entrypoint.functions."0x82CA3980"]
name = "FindChar_82CA3980"

# Dispatches on a 7-value type discriminator. It takes the kind from b->4 (minus 4, so valid kinds
# are 4..10 -> 0..6) and does a jump-table indirect branch (bctr through a 7-entry table) to the
# matching case. Each case processes the structure differently - reserving/advancing the container
# position (DispatchAndAdjustPos_8227BB58) and fanning out to the 0x82BCC/0x82BC7 structure helpers
# (sub_82BC7BB8 / sub_82BCC6F0 / sub_82BCC970). This is the per-kind switch inside the recursive
# structure processor (its single caller, ProcessStructure_Recursive_82BCB610).
# Part of the 0x82BCB cluster (sibling of ProcessStructure_Recursive_82BCB610). 228 lines, 4 callees.
# 1 caller: ProcessStructure_Recursive_82BCB610.
[entrypoint.functions."0x82BCB480"]
name = "DispatchOnKind7_82BCB480"

# Returns its input pointer advanced by a fixed 620-byte offset (r3 = r3 + 620; return). A tiny
# leaf accessor (5 lines) with no calls of its own; the constant 620 suggests it is a "get the field
# at offset 620" helper for a large struct. No direct callers found in the recompiled code (likely
# reached via a vtable / function-pointer table). Part of the 0x82BAA cluster (sibling of
# AdvanceAndDispatch_82BAA4A8 / AdvanceRanges_82BAA6F0).
[entrypoint.functions."0x82BAA140"]
name = "PointerOffset620_82BAA140"

# Thread-safe decrement of a global counter. It resolves the target global (a fixed .data pointer),
# acquires the associated spin lock (SpinLock_Acquire_822D7408), performs the decrement through
# ProcessDecrement_8322FAA0, issues an lwsync memory barrier, then clears the lock (global->12 = 0)
# to release it. This is the "decrement this shared counter atomically" primitive - a sibling of the
# ProcessAtomic family (ProcessAtomic2_82354880 / ProcessAtomicIncrement_821E3950) that use explicit
# lwarx/stwcx instead of a spin lock.
# Part of the 0x829FD cluster. 54 lines, 2 callees (SpinLock_Acquire_822D7408, ProcessDecrement_8322FAA0).
# 2 callers: ProcessPoolAcquire_83236230, FindNodeInList_83235FC0.
[entrypoint.functions."0x829FD240"]
name = "AtomicDecrement_829FD240"

# Destructor for an object that owns a 20-byte member at offset 0. It re-points the member's first
# field to a fixed default target (a 0x82A14A80-region value), calls Destruct20Byte_832156E0 on it,
# and then frees the owning object (Free_SizeBucketed_Thunk_8221BE68) only when the low bit of the
# `flag` argument (r4) is set. The 20-byte-member destructor counterpart of Construct11_83215A28 -
# a near-twin of Destruct6_82A11E68 (same Destruct20Byte + conditional-free shape).
# Part of the 0x82A14 cluster. 55 lines, 2 callees (Destruct20Byte_832156E0, Free_SizeBucketed_Thunk_8221BE68).
# No direct callers found in the recompiled code (likely invoked via a vtable / function-pointer table).
[entrypoint.functions."0x82A14A80"]
name = "Destruct7_82A14A80"

# Reads a single 4-byte element from a buffered stream, then decodes / dispatches it. It checks that
# the remaining count (obj->20) is at least 4, copies the 4 bytes at the read pointer (obj->12) into a
# local scratch word (r1+84), and advances the base / read / count fields (obj->4, obj->12, obj->20)
# past the element. The decoded element is then handed to sub_82A1B480 (the shared element-decode
# helper used by the whole ProcessRead family). The 4-byte-stream reader twin of ProcessRead_83213650 /
# ProcessRead2_82A0C528 / ProcessRead3_83237DB8.
# Part of the 0x82A1C cluster. 396 lines, 1 callee (sub_82A1B480). 1 caller: sub_83211E68.
[entrypoint.functions."0x82A1C428"]
name = "ProcessRead5_82A1C428"

# Reads a struct element out of a buffered stream into an object. It calls ProcessRead5_82A1C428 to
# pull the next 4-byte tag + payload into a local scratch (r1+80), then writes two 8-byte fields into
# the destination (obj->16 = scratch->0, obj->24 = scratch->8). When the low byte of the flag argument
# (r5) is non-zero it additionally runs sub_83211D48 on the object to finish the decode. This is the
# higher-level "read one element into this object" driver sitting on top of ProcessRead5.
# Part of the 0x83211 cluster (sibling of ProcessRead5_82A1C428 / sub_83211D48). 60 lines, 2 callees
# (ProcessRead5_82A1C428, sub_83211D48). No direct callers found in the recompiled code (likely
# reached via a vtable / function-pointer table).
[entrypoint.functions."0x83211E68"]
name = "ProcessRead6_83211E68"

# Constructor for an object containing a 20-byte member plus a small vector/float header. It first
# initializes the 20-byte part (Construct20Byte_83215640), then stages two constant floats on the
# stack (loaded from a .data table at 0x83215768 region), performs a 16-byte vector load (lvlx) into
# a local SIMD scratch, and points the object's first field at a fixed target (r5 + 7360 - the same
# 0x82A14/0x83215 target value the destructor Destruct7_82A14A80 / Destruct6_82A11E68 reset to). This
# is the "build a fresh vector-carrying node" constructor in the 0x83215 cluster.
# Part of the 0x83215 cluster (sibling of Construct20Byte_83215640 / Destruct20Byte_832156E0 / Construct11_83215A28).
# 93 lines, 1 callee (Construct20Byte_83215640). 2 callers: sub_83219588, ProcessTreeNode_83218C10.
[entrypoint.functions."0x83215768"]
name = "Construct12_83215768"

# Get-or-create lookup into a ref-counted chain that allocates 256-byte nodes. It looks the key up
# with WalkChainFind_83239FA8; if an entry already exists it releases that reference
# (Release_RefCounted_829FF648) and resets the caller's slot (obj->0 / obj->4) to null. If the key is
# absent it allocates a fresh 256-byte node (Allocate_OrDbgBreak_8221F388) and seeds it (sub_82A0ECC8),
# adjusting ref counts via ProcessRefCount_83225538. Net effect: "give me a live 256-byte reference
# for this key, creating the node if it did not exist." The 256-byte-node twin of
# GetOrCreateRef_83237FA0 (which allocates 36-byte nodes).
# Part of the 0x82A0C cluster (sibling of ProcessErase_82A0C2E0 / Construct32ByteCopy_82A0C1B8).
# 211 lines, 5 callees. 1 caller: StateDispatch4_83220888.
[entrypoint.functions."0x82A0C960"]
name = "GetOrCreateRef2_82A0C960"

# Reads / decodes a 4-byte element from a buffered stream after a vtable call. It first invokes the
# method at b->0->8 (passed b), then checks that the remaining count (b->20) is at least 4 and copies
# the 4 bytes at b->12 out to a local scratch area (r1+84), advancing the read and base pointers
# (b->4 / b->12 / b->20) past the element. The decoded element is dispatched to
# ProcessFloatDataDispatch_8323EB40 / sub_82A1B480 (the shared element-decode helper) and, in one
# branch, sub_8321DC20. The vtable-call variant of the ProcessRead family - the twin of
# ProcessRead2_82A0C528 (same b->0->8 dispatch + 4-byte read shape).
# Part of the 0x82A0C cluster (sibling of ProcessRead2_82A0C528 / GetOrCreateRef2_82A0C960).
# 251 lines, 3 callees (ProcessFloatDataDispatch_8323EB40, sub_82A1B480, sub_8321DC20). No direct
# callers found in the recompiled code (likely reached via a vtable / function-pointer table).
[entrypoint.functions."0x82A0CB68"]
name = "ProcessRead7_82A0CB68"

# Constructs a 256-byte node used by the 0x82A0C chain / ref-counted container family. It first
# initializes the 32-byte vector member at the base (Construct32ByteVector2_8323C7F0), then fills in
# the node header: obj->0 = a fixed target pointer (a 0x82A0ECC8-region value), obj->228 and obj->240
# = constant floats loaded from .data tables, and obj->224/obj->225/obj->244/obj->245 = 0 (clear
# bytes). This is the "fresh node" builder that GetOrCreateRef2_82A0C960 seeds when it allocates a new
# 256-byte entry. Part of the 0x82A0E cluster (sibling of GetOrCreateRef2_82A0C960's caller chain).
# 66 lines, 1 callee (Construct32ByteVector2_8323C7F0). 2 callers: GetOrCreateRef2_82A0C960, sub_82A0CB10.
[entrypoint.functions."0x82A0ECC8"]
name = "Construct256ByteNode_82A0ECC8"

# Reads a single 4-byte element from a buffered stream, then decodes it. It checks that the remaining
# count (obj->20) is at least 4, copies the 4 bytes at the read pointer (obj->12) into a local scratch
# word (r1+88), and advances the base / read / count fields (obj->4, obj->12, obj->20) past the element.
# The decoded element is handed to sub_82A1B480 (the shared element-decode helper used by the whole
# ProcessRead family). Part of the 0x8321D cluster; it is the element-reader branch that
# ProcessRead7_82A0CB68 reaches (its only caller).
# 226 lines, 1 callee (sub_82A1B480). 1 caller: ProcessRead7_82A0CB68.
[entrypoint.functions."0x8321DC20"]
name = "ProcessRead8_8321DC20"

# Reads a shared sub-object under a critical section. It acquires the lock (CriticalSection_EnterOrTry_82200688),
# then - if obj->36 is non-null - issues a vtable call through the sub-object (obj->36->0->16), and on
# success copies the resulting 8-byte value into obj->12 (zeroing it when the call reports an error).
# The lock (obj->40 region) guards the sub-object read so concurrent threads see a consistent value.
# Widely used (10 callers) as the safe "peek at this container's shared member" accessor in the
# 0x82B44 / 0x82C63 / 0x82C66 cluster.
# Part of the 0x82B44 cluster (sibling of ProcessField_82B51380's callee chain). 94 lines, 2 callees
# (CriticalSection_EnterOrTry_82200688, sub_82C6F8E8). 10 callers (the 0x8236x/0x824Ex/0x8292x/0x82B4x/0x82C6x cluster).
[entrypoint.functions."0x82B44360"]
name = "LockedReadSubobject_82B44360"

# Advances a 64-bit cursor field (obj->16) by n, guarded by a vtable call. It issues the method at
# obj->8->0->12 (a validation / "can I advance" probe passing obj->16, obj->24, and n); when the call
# returns a non-negative result it bumps obj->16 by the low 32 bits of n (obj->16 += n) and returns 0
# (success), otherwise it returns a negative status. This is the "skip n units forward in this
# stream/container" operation. Part of the 0x82C66 cluster (sibling of the 0x82C63/0x82C66 container
# cluster that also drives LockedReadSubobject_82B44360).
# 72 lines, 0 direct callees (only the indirect vtable call). No direct callers found in the
# recompiled code (likely reached via a vtable / function-pointer table).
[entrypoint.functions."0x82C66478"]
name = "AdvanceCursor64_82C66478"

# Validates a size/count and raises a descriptive error when it is out of range. It checks the value
# at b->8 against a 32766 upper bound; when it exceeds that it builds an error message (SetString_822F2020
# with a "range" string from a .data table), assembles an error struct (sub_826C3EF0 / sub_826C3FA8), and
# dispatches the failure (sub_823702B8 / sub_82171810 / the 0x82C70 log helpers), seeding the payload
# through sub_822F1F00. This is the "size too big" guard for the 0x8236F/0x82371 size-handling cluster.
# Part of the 0x8236F cluster (sibling of ProcessSize_82BC9EA0's error-raise family). 329 lines, 8 callees.
# 2 callers: sub_8236E5E0, sub_82371CD8.
[entrypoint.functions."0x8236F5A8"]
name = "RaiseRangeError_8236F5A8"

# Builds a 48-byte error/exception info struct. It allocates the block (Allocate_OrDbgBreak_8221F388) and
# on success packs the four incoming values into the first 12 bytes (info->0, info->4, info->8, info->12
# = d->0) and marks info->40 = 7 (a status / kind tag), zeroing the rest of the payload. The struct is then
# handed back to RaiseRangeError_8236F5A8 (its only caller) as the failure descriptor. A small
# "make an error record" helper in the 0x82370 error cluster.
# Part of the 0x82370 cluster (sibling of RaiseRangeError_8236F5A8). 78 lines, 2 callees
# (Allocate_OrDbgBreak_8221F388, ClampAndUpdate_822F6380). 1 caller: RaiseRangeError_8236F5A8.
[entrypoint.functions."0x823702B8"]
name = "ConstructErrorInfo_823702B8"

# Validates a size/length field and raises a range error when it is zero (or otherwise invalid). When
# b->8 is 0 it calls RaiseRangeError_8236F5A8 (flag=1) to report the bad size; when b->8 is non-zero it
# goes on to inspect the payload (b->4 / the value c) and route through the rest of the 0x82371 size
# pipeline (sub_82370FD8 / sub_8236E5E0) or the 0x82C70 log helpers. This is the "check the declared size
# then proceed or bail" entry point that pairs with RaiseRangeError.
# Part of the 0x82371 cluster (sibling of RaiseRangeError_8236F5A8 / ConstructErrorInfo_823702B8).
# 298 lines, 4 callees (sub_8236E5E0, RaiseRangeError_8236F5A8, sub_82370FD8, sub_82C705D8). 2 callers:
# sub_823714A0, sub_82371AA0.
[entrypoint.functions."0x82371CD8"]
name = "ValidateSizeOrRaise_82371CD8"

# Finds and re-caches the tail node of a linked list. It asserts obj->0 is non-null (debug trap
# otherwise), then walks the ->0 "next" chain starting at obj->4, stepping through nodes while the byte
# flag at offset 45 (a valid/occupied marker) on each node's target is zero. When it reaches a node
# whose flag is set, it stores that node back into obj->4 as the cached tail. Widely used (10 callers)
# to keep a list's tail pointer up to date before appending. Part of the 0x82C70 list/exception cluster.
# 93 lines, 0 direct callees (pure linked-list walk with debug asserts). 10 callers (the
# 0x822Ex/0x822Fx/0x8237x/0x8249x/0x82C6x/0x82C7x clusters).
[entrypoint.functions."0x82C705D8"]
name = "FindListTail_82C705D8"

# Inserts a node into a doubly-linked list. It reads the insertion anchor (b->8), advances it past the
# target node (b->8 = node->0), and splices the node b into the list between its predecessor (b->4) and
# the following node - fixing up the ->0 (next) and ->4 (prev) links on the neighbors to keep the list
# consistent. The boundary branches (where b equals the head or tail neighbor) handle insert-at-start /\n# insert-at-end. This is the generic "link a node in here" operation for the game's doubly-linked lists,
# the mirror image of the unlink helpers (ListUnlink_82B5E7F8 / ListUnlink2_82B5E878). Part of the
# 0x8234D cluster. 67 lines, 0 direct callees (pure pointer bookkeeping). 7 callers: sub_82BFBE00,
# sub_82C7E138, sub_82BFC278, sub_826A4C60, sub_82858358, sub_8282ED70, ProcessString_8246CF80.
[entrypoint.functions."0x8234D268"]
name = "ListNodeInsert_8234D268"

# Sorts a buffer of 4-byte elements and then binary-searches it for a key. It first bubble-sorts the
# element array (BubbleSort3_82C16C78); when the count (obj->12) is non-zero it computes the search
# range (base = (obj+4)->0, end = base + count*4) and runs the binary-search comparator
# (BinarySearch_Cmp_82C39988). When a new entry is being added it may grow the array (ProcessGrow_82C21C90)
# and release the old one (Free_Thunk_824FE010). This is the "keep this sorted set ordered and look the
# key up" driver for the 0x82C1-0x82C4 container cluster (10 callers in that range).
# Part of the 0x82C1B cluster (sibling of BubbleSort3_82C16C78 / BinarySearch_Cmp_82C39988 / ProcessGrow_82C21C90).
# 290 lines, 4 callees. 10 callers (the 0x82C2-0x82C4 cluster).
[entrypoint.functions."0x82C1B998"]
name = "SortedFind_82C1B998"

# Computes / processes a named string entry. It first tests a byte flag (b->8): when zero it delegates
# to the sibling path (sub_82814B38); otherwise it pulls the string-descriptor at b->4, checks that it
# is valid (->31 > 0, ->28 = the 16-bit payload/length), and hashes the string (HashCstring_82188CF0),
# using a constant string from a .data table as the seed/key. Along the way it manages ref-counted state
# (ConstructRefCounted_8222CF18 / DestructRefCounted_82214F08) and dispatches into the 0x82816 processor
# (ProcessDispatch4_828164E8 / sub_82814D30 / sub_82816618). This is the "hash a named value" step in the
# 0x82188 / 0x82814-0x82816 dispatch cluster.
# Part of the 0x82188 cluster (sibling of HashCstring_82188CF0 / ProcessDispatch3_82815C28). 208 lines,
# 7 callees. 1 caller: ProcessDispatch3_82815C28.
[entrypoint.functions."0x82188B88"]
name = "HashNamedEntry_82188B88"

# Processes a named-value payload. It reads the descriptor at b->4 and bails early when it is null or
# its validity byte (->31) is zero; otherwise it loads a set of constant keys / a float from .data tables
# and walks the payload starting at (b->4) + 32 (r29), hashing the name through HashCstring_82188CF0 and
# routing the decoded fields through the 0x82816 sub-procedures (sub_82815F28 / sub_82816098 / sub_82816208
# / sub_82816378). It is the heavier sibling of HashNamedEntry_82188B88 - both are driven by
# ProcessDispatch3_82815C28 as the two branches of "process this named value" (name-hash vs. full payload
# decode). Part of the 0x82815 dispatch cluster. 201 lines, 5 callees. 1 caller: ProcessDispatch3_82815C28.
[entrypoint.functions."0x82815CB0"]
name = "ProcessNamedValue_82815CB0"

# Processes a named-value payload (the lighter twin of ProcessNamedValue_82815CB0). It reads the
# descriptor at b->4 and bails when it is null or its validity count (->31) is not positive; otherwise
# it reads the 16-bit field at ->28, tests its sign bit (r28 = value & 1), loads a small block of
# constants from a .data table, and walks the payload starting at (b->4) + 32, routing values through
# GetFlag_82226ED8 and ProcessFloatOp_82814BA8 (plus sub_823F8158). This is the second "decode this
# named value" branch driven by ProcessDispatch3_82815C28.
# Part of the 0x82815 dispatch cluster (sibling of ProcessNamedValue_82815CB0 / ProcessDispatch3_82815C28).
# 174 lines, 3 callees (GetFlag_82226ED8, ProcessFloatOp_82814BA8, sub_823F8158). 1 caller:
# ProcessDispatch3_82815C28.
[entrypoint.functions."0x82815E00"]
name = "ProcessNamedValue2_82815E00"

# Tests whether a float value is within a tolerance of a reference, and flags the result. It stages the
# incoming float (f1) on the stack, does a 16-byte vector copy of the payload at obj+64, then computes
# the absolute difference abs(stored_float - f1) and compares it against a tolerance constant loaded from
# a .data table. When the difference is below the tolerance it marks obj->548 = 1 (an "equal within
# epsilon" flag); otherwise the comparison path returns a 0/1 result. This is the epsilon-compare step in
# the named-value decode pipeline (its only caller is ProcessNamedValue_82815CB0). Part of the 0x82816
# cluster. 229 lines, 0 direct callees (pure float / vector math). 1 caller: ProcessNamedValue_82815CB0.
[entrypoint.functions."0x82816098"]
name = "FloatToleranceTest_82816098"

# The twin of FloatToleranceTest_82816098 - an epsilon-compare of a float value against a reference,
# reading the reference from a different stack slot (r1-12 instead of r1-16). Same shape: stage the
# incoming float (f1), copy the 16-byte payload at obj+64, compute abs(reference - f1), compare it to the
# tolerance constant from the .data table, and when the difference is below the tolerance set obj->548 = 1
# (an "equal within epsilon" flag). Both variants are called from ProcessNamedValue_82815CB0 to test two
# different fields of a named value. Part of the 0x82816 cluster.
# 229 lines, 0 direct callees (pure float / vector math). 1 caller: ProcessNamedValue_82815CB0.
[entrypoint.functions."0x82816208"]
name = "FloatToleranceTest2_82816208"

# The third member of the float epsilon-compare trio (FloatToleranceTest_82816098 / _2_82816208 /
# _3_82816378), reading the reference float from the third stack slot (r1-8). Same body as its siblings:
# stage f1, copy the 16-byte payload at obj+64, compute abs(reference - f1), compare against the
# tolerance constant, and set obj->548 = 1 when the difference is within epsilon. ProcessNamedValue_82815CB0
# calls all three to test three separate fields of a named value.
# Part of the 0x82816 cluster (sibling of FloatToleranceTest_82816098 / FloatToleranceTest2_82816208).
# 229 lines, 0 direct callees (pure float / vector math). 1 caller: ProcessNamedValue_82815CB0.
[entrypoint.functions."0x82816378"]
name = "FloatToleranceTest3_82816378"

# The second 256-byte get-or-create lookup - an exact twin of GetOrCreateRef2_82A0C960. It looks the
# key up with WalkChainFind_83239FA8; on a hit it releases the found reference (Release_RefCounted_829FF648)
# and resets the caller's slot (obj->0 / obj->4) to null, and on a miss it allocates a fresh 256-byte node
# (Allocate_OrDbgBreak_8221F388) and seeds it with sub_82A0ED30, adjusting counts via ProcessRefCount_83225538.
# Same "give me a live 256-byte reference for this key" contract as its sibling; the two are distinguished
# only by the node-seed routine they call (sub_82A0ED30 here vs. Construct256ByteNode_82A0ECC8 there). Part
# of the 0x82A0C cluster.
# 211 lines, 5 callees (Allocate_OrDbgBreak_8221F388, ProcessRefCount_83225538, Release_RefCounted_829FF648,
# WalkChainFind_83239FA8, sub_82A0ED30). 1 caller: StateDispatch4_83220888.
[entrypoint.functions."0x82A0CFC8"]
name = "GetOrCreateRef3_82A0CFC8"

# The second vtable-dispatched 4-byte element reader - the twin of ProcessRead7_82A0CB68. It invokes
# the method at b->0->8 (passed b), then checks the remaining count (b->20) is at least 4 and copies the
# 4 bytes at b->12 into a local scratch (r1+84), advancing the read / base / count pointers (b->4, b->12,
# b->20) past the element. The decoded element is dispatched to ProcessFloatDataDispatch_8323EB40 /
# sub_82A1B480 (the shared decode helper) and, in one branch, to sub_8321D6B0. Reached only via a
# vtable / function-pointer table (no direct callers in the recompiled code). Part of the 0x82A0D cluster.
# 251 lines, 3 callees (ProcessFloatDataDispatch_8323EB40, sub_82A1B480, sub_8321D6B0). No direct callers
# found in the recompiled code (likely reached via a vtable / function-pointer table).
[entrypoint.functions."0x82A0D1D0"]
name = "ProcessRead9_82A0D1D0"

# The twin of Construct256ByteNode_82A0ECC8 - builds a fresh 256-byte node. It initializes the 32-byte
# vector member (Construct32ByteVector2_8323C7F0), then writes the node header: obj->0 = a fixed target
# pointer (a different 0x82A0E target than its sibling - r9 + 20856 here vs. r9 + 19240 there), obj->228
# and obj->240 = constant floats from .data tables, and obj->224/225/244/245 = 0. It is the node-seed
# routine that GetOrCreateRef3_82A0CFC8 calls when it allocates a new 256-byte entry. Part of the 0x82A0E
# cluster (sibling of Construct256ByteNode_82A0ECC8).
# 66 lines, 1 callee (Construct32ByteVector2_8323C7F0). 2 callers: GetOrCreateRef3_82A0CFC8, sub_82A0D178.
[entrypoint.functions."0x82A0ED30"]
name = "Construct256ByteNode2_82A0ED30"

# The second buffered-stream 4-byte element reader - the twin of ProcessRead8_8321DC20. It checks that
# the remaining count (b->20) is at least 4, copies the 4 bytes at the read pointer (b->12) into a local
# scratch word (r1+88), and advances the base / read / count fields (b->4, b->12, b->20) past the element.
# The decoded element is handed to sub_82A1B480 (the shared decode helper used by the whole ProcessRead
# family). It is the element-reader branch that ProcessRead9_82A0D1D0 reaches (its only caller). Part of
# the 0x8321D cluster. 226 lines, 1 callee (sub_82A1B480). 1 caller: ProcessRead9_82A0D1D0.
[entrypoint.functions."0x8321D6B0"]
name = "ProcessRead10_8321D6B0"

# Appends / processes a block of elements onto a container with a bounds check. It first verifies that
# the incoming count (n) does not exceed the container's remaining capacity (obj->20); when it would
# overflow it raises an error (sub_82CD11D0). For a non-zero count it computes the new end position
# (obj->20 + n) and processes the values through ProcessValue_FlagNonZero_8217A5E0, storing a 16-bit
# element (Store16BitElement_822F6578) along the way. This is the "grow this container by n, validating
# the size" step in the 0x82371 size-handling cluster (sibling of ValidateSizeOrRaise_82371CD8).
# Part of the 0x82371 cluster. 97 lines, 3 callees (ProcessValue_FlagNonZero_8217A5E0, Store16BitElement_822F6578,
# sub_82CD11D0). 4 callers: sub_82371240, sub_826C3908, sub_82C77A88, sub_82C774F8.
[entrypoint.functions."0x823712D8"]
name = "ProcessAppend_823712D8"

# Processes / skips over n 8-byte elements at a position, adjusting the position pointer. It first
# validates that the count (n + 1) does not exceed a hard limit; when it would overflow it logs an error
# (sub_82BC9860 with a message from a .data table) and returns 0. Otherwise it converts the element count
# to bytes (n * 8 and obj->28 * 8) and hands the pair to DispatchAndAdjustPos_8227BB58 to advance the
# position, then writes the new position back to obj->12. This is the "advance n 8-byte slots" step in the
# 0x82BC9 element/size cluster (sibling of ProcessSize_82BC9EA0).
# Part of the 0x82BC9 cluster (sibling of ProcessSize_82BC9EA0 / DispatchAndAdjustPos_8227BB58).
# 89 lines, 2 callees (DispatchAndAdjustPos_8227BB58, sub_82BC9860). 2 callers: sub_82BCA340, sub_82BC9FD0.
[entrypoint.functions."0x82BC9E10"]
name = "ProcessElements8_82BC9E10"

# Inserts a 4-byte element into a bounded element buffer. It reads the buffer state (a->4 = start,
# a->8 = current end, a->12 = capacity), computes the used and remaining element counts (each = (offset -
# start) / 4), and - when there is still room - writes the incoming value (b->0) at the current end and
# advances the end by 4. When the buffer is full or the geometry is invalid it bails (via sub_8238EB88).
# This is the "append a 4-byte entry if space allows" operation for the 0x8228A element-buffer family,
# used by 10 callers across the 0x8227-0x8292 range.
# Part of the 0x8228A cluster (sibling of the 0x8227B/0x8228A buffer/insert family). 98 lines, 1 callee
# (sub_8238EB88). 10 callers (the 0x8227-0x8292 cluster).
[entrypoint.functions."0x8228A900"]
name = "InsertElement4_8228A900"

# Inserts / processes a hash-table entry. It tests a field on the entry (b->8); when it is zero it goes
# straight to ProcessHashTable_829FC3C0 (flag=1) to perform the table operation, and when it is non-zero
# it first inspects the payload (b->4 / the value c) and routes through the rest of the table logic
# (sub_8273F9D8 / sub_823660D0), also reaching the chain walker (ProcessChain2_8250AF60). This is the
# heavier "insert or update this keyed entry" driver, called by Tree_Insert_82A00620 and sub_824191A0.
# Part of the 0x82419 hash-table / tree cluster (sibling of ProcessHashTable_829FC3C0 / Tree_Insert_82A00620).
# 306 lines, 4 callees (ProcessChain2_8250AF60, ProcessHashTable_829FC3C0, sub_823660D0, sub_8273F9D8).
# 2 callers: sub_824191A0, Tree_Insert_82A00620.
[entrypoint.functions."0x82419280"]
name = "ProcessHashTable2_82419280"

# Inserts a node into a doubly-linked list - the twin of ListNodeInsert_8234D268, but for the node family
# that carries its valid/occupied flag at offset 45 (matching the FindListTail_82C705D8 list). It reads the
# insertion anchor (b->8), advances it past the target node (b->8 = node->0), and splices node b between its
# predecessor (b->4) and the following node, fixing up the ->0 (next) / ->4 (prev) links on both neighbors.
# The boundary branches handle insert-at-head / insert-at-tail. Widely used (10 callers, including
# RaiseRangeError_8236F5A8) to link nodes into the 0x82C70 / 0x82497 / 0x822F7 list family.
# Part of the 0x82C70 cluster (sibling of ListNodeInsert_8234D268 / FindListTail_82C705D8). 67 lines, 0
# direct callees (pure pointer bookkeeping). 10 callers (the 0x822F7/0x8236x/0x82497/0x82C6x/0x82C7x clusters).
[entrypoint.functions."0x82C70680"]
name = "ListNodeInsert2_82C70680"

# Allocates an object from a global pool under a spin lock. It loads a global pool object (from a .data
# table at 0x83230C18), acquires the pool's lock (SpinLock_Acquire_822D7408 on the pool + 12 field), and
# requests an allocation (ProcessAllocate_8322FB30). A load-store barrier (lwsync) then guarantees the
# pool state is visible; when the allocation returns null it returns 0 (nothing to hand back), otherwise
# it zeroes the new object's first fields (->8, ->12) before returning it. This is the thread-safe
# "pull one fresh node off the global pool" allocator (sibling of PoolAllocate_828456C8). Part of the
# 0x83230 pool-allocation cluster. 65 lines, 2 callees (ProcessAllocate_8322FB30, SpinLock_Acquire_822D7408).
# 2 callers: ProcessInsert_83231240, sub_83230DA0.
[entrypoint.functions."0x83230C18"]
name = "PoolAllocateLock_83230C18"

# Frees a block of virtual memory - the companion to VirtualAllocate_82CC0CA0. It takes a base address
# (r3), a size (r4), and a flags word (r5). When the MEM_RELEASE-style flag (bit 15, 0x8000) is set and the
# size is non-zero it takes the error path (sub_82CC1C38 - the same error handler the allocator uses, with a
# status code 0x80000000 | 13) and returns 0; otherwise it releases the region through the
# __imp__NtFreeVirtualMemory syscall, passing the base and size (staged on the stack) and returning the
# NT status. Used by 9 callers (the 0x8322F/0x83230 allocation / clamped-access cluster) to release memory
# previously obtained from the pool allocator.
# Part of the 0x82CC0 virtual-memory cluster (sibling of VirtualAllocate_82CC0CA0). 63 lines, 1 callee
# (sub_82CC1C38 - the shared error handler). 9 callers (the 0x82B4D/0x8322F/0x83230 cluster).
[entrypoint.functions."0x82CC0CF0"]
name = "VirtualFree_82CC0CF0"

# Reads a string / element by index with bounds validation. It looks at the index (obj->24): when the
# value is at least 8 it dereferences the base pointer at obj->4 (the "large / indirect" layout), otherwise
# it uses the inlined buffer at (obj + 4) (the "small / inline" layout). Each access is bounds-checked with
# ValidateIndex_822F6EE8 and the range is grown on demand (ProcessRangeGrow_8217A858); the element is then
# accessed through ValidateElementAccess_8217A7F8 / ProcessVcallLoop_8217A9A8 and the 0x822F5 element helpers.
# This is the "get the Nth string" accessor for the 0x822F2 string cluster (sibling of SetString_822F2020),
# widely used (10 callers) as the safe read path.
# Part of the 0x822F2 string cluster (sibling of SetString_822F2020 / ValidateIndex_822F6EE8). 203 lines, 6
# callees. 10 callers (the 0x822F2/0x8236x/0x8237x/0x826C0/0x82B3D/0x82C63 cluster).
[entrypoint.functions."0x822F2238"]
name = "ReadStringElement_822F2238"

# Destructor for a 20-byte object - the twin of Destruct6_82A11E68. It first points the object's first
# field at a fixed target (r11 + 6676, a 0x82A11 target value), then tears down the 20-byte member
# (Destruct20Byte_832156E0). When the low bit of the flag argument (r4) is set it also releases the object
# itself (Free_SizeBucketed_Thunk_8221BE68); otherwise the object memory is left for the caller. Reached
# only via a vtable / function-pointer table (no direct callers in the recompiled code). Part of the
# 0x82A11 destruct cluster (sibling of Destruct6_82A11E68 / ReturnConstant_1024_82A11E60).
# 56 lines, 2 callees (Destruct20Byte_832156E0, Free_SizeBucketed_Thunk_8221BE68). No direct callers found
# in the recompiled code (likely reached via a vtable / function-pointer table).
[entrypoint.functions."0x82A11F30"]
name = "Destruct8_82A11F30"

# Another buffered-stream 4-byte element reader in the ProcessRead family (sibling of
# ProcessRead8_8321DC20 / ProcessRead10_8321D6B0 / ProcessRead_83213650). It checks the remaining count
# (b->20) is at least 4, copies the 4 bytes at the read pointer (b->12) into a local scratch word (r1+92),
# and advances the base / read / count fields (b->4, b->12, b->20) past the element. The decoded element
# is handed to sub_82A1B480 (the shared decode helper) and, in one branch, sub_83211D48 (a decode used by
# ProcessRead_83213650). Reached only via a vtable / function-pointer table (no direct callers in the
# recompiled code). Part of the 0x83213 buffer-read cluster.
# 280 lines, 2 callees (sub_82A1B480, sub_83211D48). No direct callers found in the recompiled code
# (likely reached via a vtable / function-pointer table).
[entrypoint.functions."0x83213150"]
name = "ProcessRead11_83213150"

# Constructs an object that holds a lowercased string. It initializes the 20-byte member
# (Construct20Byte_83215640) and points the object's first field at a fixed target (r11 + 6676 - the same
# 0x83215/0x82A11 target value Destruct8_82A11F30 resets to). It then builds a ref-counted string from a
# constant (ConstructRefCounted_8222CF18), lower-cases it (ProcessToLower_821EEA00), normalizes it into a
# string object (ProcessConstructString_82BFD780), and stores the result at obj->16, finally tearing down
# the temporary (DestructRefCounted_82214F08). This is the "build a node carrying a normalized/lowercased
# key" constructor in the 0x83215 cluster, called by ProcessTreeNode_83218C10 and sub_83219588.
# Part of the 0x83215 cluster (sibling of Construct20Byte_83215640 / Construct12_83215768 / Construct11_83215A28).
# 77 lines, 5 callees. 2 callers: sub_83219588, ProcessTreeNode_83218C10.
[entrypoint.functions."0x83215960"]
name = "Construct13_83215960"

# Issues a vtable call and then reads an element from a 4-byte array. It dispatches the method at
# obj->0->4 (passed obj plus a constant string from a .data table as arguments), then computes the offset
# of the n-th element in the 4-byte array at obj->4 (offset = n * 4) and returns the first field of that
# element. This is a "call this virtual routine, then fetch the Nth slot's first member" helper. Reached
# only via a vtable / function-pointer table (no direct callers in the recompiled code). Part of the
# 0x82C21 element-access cluster (sibling of ReturnPointer3_82C21F28 / ProcessGrow_82C21C90).
# 50 lines, 0 direct callees (only the indirect vtable call). No direct callers found in the recompiled
# code (likely reached via a vtable / function-pointer table).
[entrypoint.functions."0x82C21650"]
name = "VtableElementAccess_82C21650"

# Constructs a small fixed-layout struct by writing a set of constant fields, then takes a reference. It
# seeds obj->0 = 0, obj->4 = -1, obj->8 = 0, obj->12 = 2, obj->16 = 0, and the two byte fields obj->20 /
# obj->21 = 1, and finally atomically increments the counter at obj->24 (AtomicIncrement3_821F7798) - the
# "take a reference" step. The result is a freshly-initialized node (the constant 2 at ->12 and the -1 at
# ->4 look like a type tag / invalid-index marker). Called only by sub_82C22770. Part of the 0x82C21
# element/struct cluster (sibling of VtableElementAccess_82C21650 / ReturnPointer3_82C21F28).
# 56 lines, 1 callee (AtomicIncrement3_821F7798). 1 caller: sub_82C22770.
[entrypoint.functions."0x82C21A70"]
name = "ConstructSmallStruct_82C21A70"

# Populates an object's fields one by one through vtable calls. It walks the 4-byte-aligned fields of the
# object (obj+0, obj+4, obj+8, ...) and for each issues a vcall-triple (VcallTriple_AllSuccess_82C011E8)
# that takes the object, a constant string/key from a .data table, and the destination field slot, so the
# named value is written into that field. The longer runs go through VcallChain_AllSuccess_82C14F38 and
# ProcessVcallSequence3_82C14D70. Net effect: "fill this record with its per-field string values." Called
# only by sub_82C21E90. Part of the 0x82C21 vcall / element cluster (sibling of ConstructSmallStruct_82C21A70
# / VcallTriple_AllSuccess_82C011E8).
# 115 lines, 3 callees (ProcessVcallSequence3_82C14D70, VcallChain_AllSuccess_82C14F38,
# VcallTriple_AllSuccess_82C011E8). 1 caller: sub_82C21E90.
[entrypoint.functions."0x82C21AE0"]
name = "PopulateViaVcalls_82C21AE0"

# Drives the vtable-based population of an object. It first dispatches the method at obj->0->24 (a
# "prepare / begin" call); when that returns non-zero (success) it hands the object to PopulateViaVcalls_82C21AE0
# to fill in its per-field string values. If the population step also succeeds it proceeds to the next
# virtual stage (obj->0->28) to continue the multi-step object build. This is the outer "populate this
# record" driver that calls PopulateViaVcalls_82C21AE0 (its only callee). Part of the 0x82C21 vcall cluster
# (sibling of PopulateViaVcalls_82C21AE0 / ConstructSmallStruct_82C21A70). 86 lines, 1 callee
# (PopulateViaVcalls_82C21AE0). 1 caller: sub_82C22770.
[entrypoint.functions."0x82C21E90"]
name = "ProcessPopulate_82C21E90"

# Releases / decrements an object back to a global pool under a spin lock - the mirror image of
# PoolAllocateLock_83230C18. It loads the same global pool object (from the .data table at 0x83230BC0),
# acquires the pool's lock (SpinLock_Acquire_822D7408 on the pool + 12 field), and decrements/releases the
# count (ProcessDecrement_8322FAA0). A load-store barrier (lwsync) guarantees visibility, then the pool's
# +12 field is reset to 0 before returning. This is the thread-safe "put this object back on the pool"
# release path, driven by ProcessInsert_83231240 and Heap_CarveBlock_83231058. Part of the 0x83230
# pool-allocation cluster (sibling of PoolAllocateLock_83230C18 / ProcessDecrement_8322FAA0).
# 52 lines, 2 callees (ProcessDecrement_8322FAA0, SpinLock_Acquire_822D7408). 2 callers: ProcessInsert_83231240,
# Heap_CarveBlock_83231058.
[entrypoint.functions."0x83230BC0"]
name = "PoolReleaseLock_83230BC0"

# Looks up an entry in a hash table and reports whether it is present. It hashes the key (combining it
# with the table's salt at obj->36) to select a 32-byte bucket, and when the bucket's 8-byte value field
# matches the expected value it returns 1 (found, fast path). On a mismatch it falls back to the full table
# walk (HashLookup_82B49678) plus the 0x82C6F/0x8260C helpers to confirm or deny the entry, returning 1 if
# present and 0 otherwise. This is the "does the table contain this key/value?" query - the value-checking
# sibling of HashLookup_82B49678. Part of the 0x82B49 hash-table cluster.
# 191 lines, 3 callees (HashLookup_82B49678, VirtualCall4_8260C130, sub_82C6F8E8). 2 callers: sub_82B49908,
# sub_82B49730.
[entrypoint.functions."0x82B497B8"]
name = "HashLookup2_82B497B8"

# Another buffered-stream 4-byte element reader in the ProcessRead family (sibling of ProcessRead11_83213150
# / ProcessRead8_8321DC20 / ProcessRead10_8321D6B0). It checks the remaining count (b->20) is at least 4,
# copies the 4 bytes at the read pointer (b->12) into a local scratch word (r1+88), and advances the base /
# read / count fields (b->4, b->12, b->20) past the element. The decoded element is dispatched to
# CopyStructData4_82A1BEA8 (a 4-byte struct copy), sub_82A1B480 (the shared decode helper), and, in one
# branch, sub_83211D48. Reached only via a vtable / function-pointer table (no direct callers in the
# recompiled code). Part of the 0x83212 buffer-read cluster. 335 lines, 3 callees (CopyStructData4_82A1BEA8,
# sub_82A1B480, sub_83211D48). No direct callers found in the recompiled code (likely reached via a
# vtable / function-pointer table).
[entrypoint.functions."0x832128D0"]
name = "ProcessRead12_832128D0"

# Constructor for an object containing a 20-byte member plus a small vector/float header - the twin of
# Construct12_83215768. It initializes the 20-byte part (Construct20Byte_83215640), stages two constant
# floats on the stack (from a .data table near the 0x83215 cluster), performs a 16-byte vector load (lvlx)
# into a local SIMD scratch, and points a field at a fixed target (r5 + 7288). It is the "build a fresh
# vector-carrying node" constructor used by ProcessTreeNode_83218C10 and sub_83219588 (the same two callers
# as its sibling Construct12). Part of the 0x83215 construct cluster.
# 107 lines, 1 callee (Construct20Byte_83215640). 2 callers: sub_83219588, ProcessTreeNode_83218C10.
[entrypoint.functions."0x83215DF0"]
name = "Construct14_83215DF0"

# Performs a vtable-dispatched binary search. It invokes the method at obj->0->4 (passed obj plus a
# constant string from a .data table); when that returns non-zero it sets up a search range (base at +4,
# an offset block at r1+80, and a key staged at r1+148) and runs the 4-stride binary search
# (BinarySearch_4Stride_82C40EC0), returning whether the key was found. When the vcall reports not-ready /\n# zero it returns 0 immediately. This is the "look the key up in this sorted table" operation, driven by
# sub_82C214E8 and sub_82C217B8. Part of the 0x82C21 search/element cluster (sibling of
# VtableElementAccess_82C21650 / BinarySearch_4Stride_82C40EC0). 83 lines, 1 callee (BinarySearch_4Stride_82C40EC0).
# 2 callers: sub_82C214E8, sub_82C217B8.
[entrypoint.functions."0x82C21458"]
name = "ProcessBinarySearch_82C21458"

# Looks up a key in a sorted table and processes the result. It first runs the vtable-driven binary
# search (ProcessBinarySearch_82C21458); when the key is found (non-zero) it reads the matched node
# (obj->0) and routes the value through the 0x82BFF handler family (sub_82BFF710 to initialize the output,
# sub_82BFF7D8 to write per-field values from .data constants, sub_82BFF848 / sub_82BFF750 / sub_82BFFAA8
# to finish the decode). This is the "find this key and materialize its value" operation, the consumer side
# of ProcessBinarySearch. Reached only via a vtable / function-pointer table (no direct callers in the
# recompiled code). Part of the 0x82C21 lookup cluster (sibling of ProcessBinarySearch_82C21458).
# 160 lines, 6 callees. No direct callers found in the recompiled code (likely reached via a vtable /
# function-pointer table).
[entrypoint.functions."0x82C214E8"]
name = "ProcessLookup_82C214E8"

# Inserts / copies a 16-byte element into a bounded element buffer - the 16-byte sibling of
# InsertElement4_8228A900. It reads the buffer geometry (obj->4 = start, obj->8 = end, obj->12 = capacity),
# computes the free element count in 16-byte units ((obj->12 - obj->4) / 16), and writes the incoming 16-byte
# vector (loaded from r6) at the insertion point, advancing the end. When the buffer needs to grow it
# reallocates through ConstructArray16_82512640 and releases the old storage (Free_SizeBucketed_Thunk_8221BE68),
# with sub_82A97648 handling an overflow / error path. Used by 3 callers across the 0x82442/0x827C5/0x82A8B
# range. Part of the 0x82A8B element-buffer cluster.
# 446 lines, 3 callees (ConstructArray16_82512640, Free_SizeBucketed_Thunk_8221BE68, sub_82A97648). 3 callers:
# sub_82442B68, sub_82A8BBD0, sub_827C55C8.
[entrypoint.functions."0x82A8BFE8"]
name = "InsertElement16_82A8BFE8"

# Recursively binary-searches a 4-byte-element array. It takes the search range [a, b), a count (n), and a
# comparison context (c); it computes the range size in element units ((b - a) / 4) and, when the range is
# large, recurses on the halves (sub_82C27A00 calls itself) while narrowing with the midpoint helpers
# (sub_82C22AB0 to compare a candidate, sub_82C22D80 / sub_82C22E68 / sub_82C23250 for the split / bound
# arithmetic). The count is halved each level (n >> 1, then >> 1 again) as the search converges. This is the
# low-level "find the target in a sorted 4-byte array" routine (a plain-bounds twin of
# ProcessBinarySearch_82C21458). Part of the 0x82C27 search cluster. 176 lines, 5 callees (sub_82C22AB0,
# sub_82C22D80, sub_82C22E68, sub_82C23250, and itself). 1 caller: sub_82C27B20.
[entrypoint.functions."0x82C27A00"]
name = "BinarySearch_82C27A00"

# Compares two adjacent pointers in a 4-byte element array and swaps them if they are out of order - the
# sort step that keeps the array sorted. It dereferences both entries (a->0 and b->0, each pointing at a
# small struct) and orders them by the ->4 field, using the ->8 field as a tie-breaker; when the right one
# is smaller than the left it exchanges the two array slots (and mirrors the swap via
# Process2DRegion_82C3C010 for the associated bookkeeping). This is the low-level "bubble one pair into
# place" routine driven by BinarySearch_82C27A00 (its only caller). Part of the 0x82C22 sort / compare
# cluster. 135 lines, 1 callee (Process2DRegion_82C3C010). 1 caller: BinarySearch_82C27A00.
[entrypoint.functions."0x82C22D80"]
name = "CompareSwapAdjacent_82C22D80"

# Sorts a buffer of 4-byte elements and then binary-searches it - the lighter twin of SortedFind_82C1B998.
# It bubble-sorts the element array (BubbleSort3_82C16C78) over (a + 4), and when the count (obj->8) is
# non-zero it computes the search range (base = obj->0, end = base + count*4) and runs the binary-search
# comparator (BinarySearch_Cmp_82C39988). Unlike its sibling it does not grow or free storage here; it is
# the pure "sort this set and look the key up" step. Widely used (10 callers) as the ordered-set lookup for
# the 0x82C1-0x82C4 container cluster. Part of the 0x82C3A cluster (sibling of SortedFind_82C1B998 /
# BubbleSort3_82C16C78 / BinarySearch_Cmp_82C39988). 65 lines, 2 callees. 10 callers (the 0x82C1-0x82C4
# cluster).
[entrypoint.functions."0x82C3ADF0"]
name = "SortedFind2_82C3ADF0"

# Assigns a ref-counted pointer with proper ref-counting - the standard "operator=" for a smart-pointer
# slot. Given a source pointer (a) and a destination slot (obj), it first increments the reference count on
# the source (vcall through a->0->4), then releases the destination's current occupant (vcall through
# obj->0->0->8, only when the slot is non-null), and finally stores the new pointer (obj->0 = a). The two
# vtable calls are the add-ref / release pair that keeps the ref count balanced across the swap. Widely
# used (10 callers) as the safe pointer-assignment primitive for the 0x822F5 smart-pointer cluster.
# 69 lines, 0 direct callees (only the two indirect vtable calls). 10 callers (the 0x822Fx/0x8236x/0x8237x/
# 0x8260B/0x82B4E/0x82B50/0x82C6x clusters).
[entrypoint.functions."0x822F54C8"]
name = "AssignRefCounted_822F54C8"

# Returns a fixed pointer - a one-line accessor that loads a constant 0x82C2A-region address (from a .data
# table) and hands it back. It is the kind of trampoline used to expose a global / singleton (likely a
# vtable, type descriptor, or static object) through a callable entry. Reached only via a vtable /
# function-pointer table (no direct callers in the recompiled code). Part of the 0x82C2A constant-return
# cluster (sibling of ReturnConstant_2048_82323C60 / ReturnPointer_82C3BE78).
# 6 lines, 0 direct callees. No direct callers found in the recompiled code (likely reached via a vtable /
# function-pointer table).
[entrypoint.functions."0x82C2AC78"]
name = "ReturnConstant_82C2AC78"

# Copies a 28-byte struct (four 32-bit values followed by three 32-bit floats) from src to dst. It first
# tests whether the source and the size argument are equal and bails if so (an empty / no-op copy), then
# walks the struct field-by-field: dst->0, dst->4, dst->8, dst->12 are the four word members, and dst->16 /
# dst->20 / dst->24 are the three float members (loaded with lfs and stored with stfs). The struct looks
# like a small transform / material descriptor (3 ints + 1 float, then 3 more floats). Called only by
# sub_82A142D0. Part of the 0x82A14 struct-copy cluster. 70 lines, 0 direct callees (pure field copy).
# 1 caller: sub_82A142D0.
[entrypoint.functions."0x82A141A0"]
name = "Copy28ByteStruct_82A141A0"

# Dispatches on a value's type tag and processes the payload accordingly. It reads the tag at obj->4 and
# branches: a few inline cases (obj->4 == 3 converts the float at obj->0 to an integer and stores it; the
# default and other cases just pass through), and the remaining kinds are routed to sub_82228570 /
# SwitchDispatch_82A2F3E8 / SwitchDispatch_82BC8260 / sub_82229260 for their type-specific handling. This is
# the "interpret / coerce this typed value" switch in the 0x82228 value-dispatch cluster, used by 9 callers
# across the 0x821C/0x8222/0x822D/0x82A2 range. Part of the 0x82228 typed-value cluster.
# 141 lines, 4 callees (SwitchDispatch_82A2F3E8, SwitchDispatch_82BC8260, sub_82228570, sub_82229260). 9
# callers (the 0x821C/0x8222/0x82A2 cluster).
[entrypoint.functions."0x822285D8"]
name = "DispatchValueType_822285D8"

# Processes a value while adjusting its position. It first calls DispatchAndAdjustPos_8227BB58 with a fixed
# 32-byte payload to advance / reposition the internal state, then reads a sub-pointer (a->16) and routes
# further work through ProcessSize_82BC9EA0 (a size query) and ProcessElements8_82BC9E10 (an element walk).
# The result pointer is stored into the caller's out-slot (a->28) and a low-bit mask (a byte & 3) is used
# for a small branch. This is the "handle one element and fix up the cursor" step for the 0x82BCA
# container-processing cluster, used by 9 callers. Part of the 0x82BCA cluster (sibling of
# DispatchAndAdjustPos_8227BB58 / ProcessSize_82BC9EA0 / ProcessElements8_82BC9E10).
# 94 lines, 3 callees (DispatchAndAdjustPos_8227BB58, ProcessSize_82BC9EA0, ProcessElements8_82BC9E10). 9
# callers (the 0x822D/0x82BC/0x82BD cluster).
[entrypoint.functions."0x82BCA340"]
name = "ProcessAdjustPosition_82BCA340"

# Looks up a key in a hash table by walking the matching bucket's chain. It derives the bucket index from
# the key (a->8) and the table mask (b->8): ((a->8 - 1) & b->8), then indexes the bucket array (a->20) to
# the chain head. It then walks the linked list of nodes (each node's ->16 points at the next) with a
# dcbt prefetch, testing each node's type tag (->12) and key (->8); it returns the node when ->8 matches the
# target key, or falls off to a constant "not found" pointer (a .data address) at the end of the chain.
# This is the linear-probing / chained hash-table search in the 0x82228 value cluster, called by 9 sites
# (including DispatchValueType_822285D8). Part of the 0x82228 hash-lookup cluster.
# 56 lines, 0 direct callees (pure linked-list walk). 9 callers (the 0x821C/0x8222/0x82A2/0x82A3 cluster).
[entrypoint.functions."0x82228570"]
name = "HashLookupByKey_82228570"

# Appends a 16-byte element to the end of a bounded element buffer, growing the buffer if there is no room.
# It reads the buffer geometry (obj->4 = base, obj->8 = end, obj->12 = capacity) and computes the used and
# free element counts in 16-byte units. When the end is past the capacity it falls through to the growth
# path (InsertElement16_82A8BFE8), otherwise it does an in-place 128-bit vector copy (lvx/stvx) of the
# incoming element (src) to obj->8 and advances the end by 16. This is the "push one 16-byte element onto
# the buffer" primitive for the 0x82442 element-buffer cluster, used by 10 callers (the 0x82442/0x82683/
# 0x827D3/0x82884/0x82966/0x8299E/0x83228/0x83236/0x8323F range). Part of the 0x82442 element-buffer
# cluster (sibling of InsertElement16_82A8BFE8).
# 90 lines, 1 callee (InsertElement16_82A8BFE8). 10 callers (the 0x82442/0x82683/0x827D3/0x82884/0x82966/
# 0x8299E/0x83228/0x83236/0x8323F cluster).
[entrypoint.functions."0x82442B68"]
name = "AppendElement16_82442B68"

# Performs a split / midpoint computation for a 4-byte element array and drives the sort-and-compare step.
# It takes the range endpoints (a, b) and a bound (c, d), computes the range size in element units
# ((c - a) / 4), and - when the range is wide (> 40 elements) - derives an 8-element stride
# ((size + 1) / 8, then *4 and *8) to set up sub-range pointers. It then hands the sub-range to
# ProcessSortCompare_82C22148 to compare / partition the elements. This is the "divide a block and sort the
# halves" step in the 0x82C22 sort cluster, called only by sub_82C22AB0. Part of the 0x82C22 sort / compare
# cluster (sibling of ProcessSortCompare_82C22148).
# 105 lines, 1 callee (ProcessSortCompare_82C22148). 1 caller: sub_82C22AB0.
[entrypoint.functions."0x82C222F0"]
name = "BinarySearchInsert_82C222F0"

# The core of the 4-byte element binary search. It takes the search range [b, c) and a target (a), computes
# the range size in element units ((c - b) / 4) and a midpoint offset ((size / 8) * 4), then partitions the
# block by calling BinarySearchInsert_82C222F0 on the midpoint. It continues narrowing (reading the
# candidate at the midpoint and recursing into the correct half) until the target position is found, then
# returns the insertion index. This is the workhorse the higher-level BinarySearch_82C27A00 calls, and it
# also serves as the generic "find / bisect a 4-byte sorted array" routine (used from the 0x82C22 sort /
# compare family). Part of the 0x82C22 sort / compare cluster. 398 lines, 1 callee
# (BinarySearchInsert_82C222F0). 1 caller: BinarySearch_82C27A00.
[entrypoint.functions."0x82C22AB0"]
name = "BinarySearchCore_82C22AB0"

# Decrements an object's reference count - the "release" half of the ref-counting pair. It takes the object
# pointer, adjusts back by 4 (r3 - 4) to reach the reference-count field that precedes the vtable pointer,
# and forwards to AtomicIncrement_822CBEB8 to perform the atomic decrement (which, when the count reaches
# zero, is expected to free the object). This is the trampoline a caller uses to drop a reference to a
# ref-counted object. Reached only via a vtable / function-pointer table (no direct callers in the
# recompiled code). Part of the 0x82B4F ref-count-trampoline cluster (sibling of
# AtomicIncrement_822CBEB8 / the 0x82B4F9 LazyGet_Field40 group).
# 5 lines, 1 callee (AtomicIncrement_822CBEB8). No direct callers found in the recompiled code (likely
# reached via a vtable / function-pointer table).
[entrypoint.functions."0x82B4F368"]
name = "DecrementRefCount_82B4F368"

# Destructor for a 20-byte object that also frees the memory when asked. It first rewrites the object's
# vtable pointer (obj->0) with a "being-deleted" / base-class vtable constant, then calls the 20-byte
# destructor (Destruct20Byte_832156E0) to run the member teardown. If the second argument's low bit is set
# (a & 1), it then frees the object's storage through Free_SizeBucketed_Thunk_8221BE68; otherwise the
# caller owns the memory and keeps it. This is the "operator delete / ~T" entry for the 0x82A14
# 20-byte-object cluster (sibling of Construct20Byte_83215640 / Destruct20Byte_832156E0). Reached only via
# a vtable / function-pointer table (no direct callers in the recompiled code).
# 56 lines, 2 callees (Destruct20Byte_832156E0, Free_SizeBucketed_Thunk_8221BE68). No direct callers found
# in the recompiled code (likely reached via a vtable / function-pointer table).
[entrypoint.functions."0x82A14600"]
name = "DestructAndFree_82A14600"

# Inserts an element at a specific position in an 8-byte-element buffer, growing the buffer first if needed.
# It reads the buffer's current count (obj->28), a flag byte (obj->7), and a base pointer (obj->16). When
# the requested index (n) is past the end it grows via ProcessElements8_82BC9E10; it then sizes the write
# through ProcessSize_82BC9EA0, and for an in-range index it shifts the tail elements up by 8 bytes, stores
# the new element (a) at the slot, bumps the count, and runs the element's per-type handling through
# ElementAccess_821E1498 / ProcessDispatchValue_8229A488 / StateDispatch3_82BCA3E8, with
# DispatchAndAdjustPos_8227BB58 / sub_82BC9860 for the position bookkeeping. This is the "insert at index"
# routine for the 0x82BC9 element-buffer cluster (sibling of ProcessElements8_82BC9E10 /
# ProcessSize_82BC9EA0). 253 lines, 7 callees. 2 callers: sub_822DF280, sub_82BCA180.
[entrypoint.functions."0x82BC9FD0"]
name = "InsertElementAt_82BC9FD0"

# Reads a 4-byte element out of a cursor-tracked buffer and advances the read position. It takes a buffer
# descriptor (a) and an output object (obj). It first checks that at least 4 bytes remain (a->20 >= 4);
# when so it peeks the element at the current cursor (a->12), copies the 4 bytes byte-by-byte into a
# scratch slot (r1 + 84), then advances both the data cursor (a->12 += 4) and the logical offset (a->4 +=
# 4). The rest of the body (308 lines) handles the element's type-specific follow-up, routing through
# sub_82A1B480 and sub_83211D48 (a 0x83211 buffer-read helper) depending on the element kind. This is the
# "pull one 4-byte element off the buffer" step in the 0x83212 buffer-read cluster (sibling of the
# ProcessRead family: ProcessRead11_83213150 / ProcessRead12_832128D0 / sub_83211D48).
# 308 lines, 2 callees (sub_82A1B480, sub_83211D48). No direct callers found in the recompiled code (likely
# reached via a vtable / function-pointer table).
[entrypoint.functions."0x83212218"]
name = "ProcessRead13_83212218"

# Constructs a 20-byte object with a vtable and pre-initialized float fields. It first runs the base 20-byte
# constructor (Construct20Byte_83215640) to zero / lay out the object, then installs the object's vtable
# (obj->0) from a .data constant, and seeds three float members (obj->16, obj->20, obj->24) with constant
# values loaded from .data (two of the slots get the same constant). This is the "make a default 20-byte
# float-backed object" factory for the 0x83215 construct cluster (sibling of Construct20Byte_83215640 /
# Construct12_83215768 / Construct13_83215960). Called by ProcessTreeNode_83218C10 and sub_83219588.
# 56 lines, 1 callee (Construct20Byte_83215640). 2 callers: ProcessTreeNode_83218C10, sub_83219588.
[entrypoint.functions."0x83215858"]
name = "ConstructFloatObject_83215858"

# Converts / re-emits a buffer of elements by walking each one and dispatching its type-specific handler.
# It first zero-clears a 108-byte scratch table (27 entries of 4 bytes) on the stack, reads the element
# count (obj->28), and loops over the indices. For each element it computes a per-element scratch slot, then
# routes the value through ProcessConversion_82BC9D70 (a type conversion) and writes the result back via
# InsertElementAt_82BC9FD0 (an in-place insert). This is the "map a buffer through a conversion and rebuild
# it" routine for the 0x82BCA element-buffer cluster. Called only by StateDispatch3_82BCA3E8. Part of the
# 0x82BCA cluster (sibling of ProcessConversion_82BC9D70 / InsertElementAt_82BC9FD0). 260 lines, 2 callees.
# 1 caller: StateDispatch3_82BCA3E8.
[entrypoint.functions."0x82BCA180"]
name = "ProcessConvertElements_82BCA180"

# Returns a field from a global / thread-local structure. It loads a base pointer from the thread-context
# register at offset 256 (r13 + 256), then returns the 32-bit field at offset 332 of that structure
# ((r13 + 256) + 332). This is the trivial one-field getter used to expose a global value (likely a
# per-thread state word or a global object pointer) to the 10 callers scattered across the 0x821C/0x8221/
# 0x82A1/0x82A9/0x82B6/0x82C0/0x82EF/0x8300 range. Part of the 0x8221E global-accessor cluster (sibling of
# the Allocate_SizeBucketed thunks nearby). 5 lines, 0 direct callees (pure 2-load accessor). 10 callers
# (the 0x821C/0x8221/0x82A1/0x82A9/0x82B6/0x82C0/0x82EF/0x8300 cluster).
[entrypoint.functions."0x8221EE38"]
name = "GetGlobalField_8221EE38"

# Dispatches on a value's type tag through a jump table. It reads the tag byte at obj->4, normalizes it
# (obj->4 - 4), and range-checks it (out-of-range jumps to a default handler at loc_82A2D578). The valid
# range (0..6) selects one of seven case handlers, each routed through a method-pointer table (a .data
# jump table indexed by tag) with a bctr indirect call; the inline cases then forward to the per-type
# processors (ProcessVirtual_82227940 / sub_8222B4C0 / sub_82A20130 / sub_82A2DEA8). This is the
# "interpret one typed value by kind" dispatcher for the 0x82A2D value cluster, called only by
# sub_82A2D590. Part of the 0x82A2D type-dispatch cluster (sibling of SwitchDispatch_82A2F3E8).
# 224 lines, 4 callees (ProcessVirtual_82227940, sub_8222B4C0, sub_82A20130, sub_82A2DEA8). 1 caller:
# sub_82A2D590.
[entrypoint.functions."0x82A2D408"]
name = "DispatchTypeTag_82A2D408"

# Returns a fixed pointer - a one-line accessor that loads a constant 0x82C27-region address (from a .data
# table) and hands it back. It is the kind of trampoline used to expose a global / singleton (likely a
# vtable, type descriptor, or static object) through a callable entry. This is the 0x82C27 twin of
# ReturnConstant_82C2AC78 (which returns a neighboring .data pointer). Reached only via a vtable /
# function-pointer table (no direct callers in the recompiled code). Part of the 0x82C27 constant-return
# cluster (sibling of ReturnConstant_82C2AC78 / ReturnConstant_2048_82323C60 / ReturnPointer_82C3BE78).
# 6 lines, 0 direct callees. No direct callers found in the recompiled code (likely reached via a vtable /
# function-pointer table).
[entrypoint.functions."0x82C27F90"]
name = "ReturnConstant2_82C27F90"

# Dispatches on a value's type tag through a jump table - the 0x822D8 twin of DispatchTypeTag_82A2D408. It
# reads the tag word at obj->4, range-checks it (out-of-range and the "no-op" tags 1, 2, 4, 6 all jump to a
# common default at loc_822D83A0). The valid tags (0..7) select one of the case handlers, each routed
# through a method-pointer table (a .data jump table indexed by tag) with a bctr indirect call, then to the
# per-type handlers (sub_82BCBF50 / sub_82BCC108). This is the "interpret one typed value by kind"
# dispatcher for the 0x822D8 value cluster, used by 2 callers (sub_822DF280 / sub_82BC5940). Part of the
# 0x822D8 type-dispatch cluster (sibling of DispatchTypeTag_82A2D408 / SwitchDispatch_82A2F3E8).
# 175 lines, 2 callees (sub_82BCBF50, sub_82BCC108). 2 callers: sub_822DF280, sub_82BC5940.
[entrypoint.functions."0x822D8298"]
name = "DispatchTypeTag2_822D8298"

# Walks a singly-linked node list, dispatching each node and accumulating the results. It starts at the
# head node (obj) and reads the next-pointer at offset 36; while that pointer is non-null it calls
# StateDispatch_82BCB0A8 on the current node, adds the node's return value into an accumulator (r31 +=
# result), and advances to the next node (node = node->36). When the list ends (node->36 == 0) it returns
# the accumulated total (0 for an empty list). This is the "reduce a node chain by dispatching each node"
# routine for the 0x82BCB node-list cluster, called only by sub_82BCB8D0. Part of the 0x82BCB cluster
# (sibling of StateDispatch_82BCB0A8). 56 lines, 1 callee (StateDispatch_82BCB0A8). 1 caller: sub_82BCB8D0.
[entrypoint.functions."0x82BCB2A0"]
name = "ProcessNodeList_82BCB2A0"

# Splices a node into (or out of) a doubly-linked list by rewiring the neighbor pointers. It takes a list
# head (obj) and a node (r11), advances the head's pointer (obj->0) to the node's successor (r11->8), and
# then patches the cross-links: when the node's ->17 flag is clear it links the node as the head's ->4, and
# it rewrites the ->4 / ->8 next- and back-pointers on both the node and its predecessor (a->4 / r10->4 /
# r10->8) so the chain stays consistent. This is the low-level "relink one list node" primitive for the
# 0x82301 linked-list cluster, used by 7 callers across the 0x8230/0x8246/0x826A/0x8282/0x8285/0x82BF/0x82C7
# range. Part of the 0x82301 linked-list cluster (sibling of ListNodeInsert_8234D268). 67 lines, 0 direct
# callees (pure pointer rewiring). 7 callers (the 0x8230/0x8246/0x826A/0x8282/0x8285/0x82BF/0x82C7 cluster).
[entrypoint.functions."0x82301AE0"]
name = "SpliceListNode_82301AE0"

# Returns a global 32-bit value. It loads a .data address and reads the 32-bit word stored at (r11 - 1572),
# returning it in r3. This is the trivial one-field getter used to expose a global value to the 10 callers
# scattered across the 0x82BC/0x82BD/0x82CD range. Part of the 0x82CB7 global-accessor cluster (sibling of
# GetGlobalField_8221EE38). 6 lines, 0 direct callees (pure 2-load accessor). 10 callers (the 0x82BC/0x82BD/
# 0x82CD cluster).
[entrypoint.functions."0x82CB7D90"]
name = "GetGlobalValue_82CB7D90"

# Returns a pointer to a field at a fixed offset from a base object, with a null guard. It takes a base
# pointer (r3), checks the field at offset 492 (r3 + 492): if that field is non-zero it returns the pointer
# to the field at offset 568 (r3 + 568), otherwise it returns null. The addic. / bnelr sequence makes this a
# single conditional-return trampoline. This is a "get a member pointer, or null if the owning sub-object is
# absent" accessor for the 0x82173 object cluster. Reached only via a vtable / function-pointer table (no
# direct callers in the recompiled code). Part of the 0x82173 field-accessor cluster. 11 lines, 0 direct
# callees (pure pointer arithmetic + null guard). No direct callers found in the recompiled code (likely
# reached via a vtable / function-pointer table).
[entrypoint.functions."0x82173CC0"]
name = "GetField568_82173CC0"

# Transforms a packed bit-field value and writes the result back. It takes a base object (obj) and a set
# of packed parameters (a..e). It first makes a vtable call (obj->0->4) to run the object's pre-handler, then
# reinterprets the base as a larger enclosing struct (obj - 76) and works on a sub-record at (obj - 76) + 48.
# It unpacks the bit fields (r28 << 1 to align, then r27 << 5 to expand a 3-bit field into an 8-bit value,
# a low-bit test r5 & 1), clears the target words (store 0 to r9->0 / r9->8), and repacks the transformed
# fields. This is a "decode / re-encode a bit-packed descriptor" routine for the 0x82BAF bit-transform
# cluster. Reached only via a vtable / function-pointer table (no direct callers in the recompiled code).
# 87 lines, 0 direct callees (one indirect vtable call). No direct callers found in the recompiled code
# (likely reached via a vtable / function-pointer table).
[entrypoint.functions."0x82BAFA00"]
name = "ProcessBitTransform_82BAFA00"

# Sets a single flag bit on a byte field, guarded by a vtable pre-handler. It takes a base object (obj) and
# a value (a). It first runs the object's handler (vcall through obj->0->4), then reads the flag byte at
# (obj + 48) + 16, sets bit 2 (value 4) when the low byte of (a) is non-zero (a & 0xFF, then OR'd in as
# r8 << 2 masked to just bit 2), and writes the byte back. This is the "toggle on one bit in a packed
# descriptor" routine, the lighter sibling of ProcessBitTransform_82BAFA00 in the same 0x82BAF bit-field
# cluster. Reached only via a vtable / function-pointer table (no direct callers in the recompiled code).
# 33 lines, 0 direct callees (one indirect vtable call). No direct callers found in the recompiled code
# (likely reached via a vtable / function-pointer table).
[entrypoint.functions."0x82BAFA98"]
name = "SetFlagBit_82BAFA98"

# Processes a nested object through a pair of vtable calls. It takes a base object (obj) and parameters
# (a, b). It first dereferences the sub-object at obj->4 and runs its method at offset 32 (vcall through
# obj->4->0->32), then reads a further nested pointer (obj->4->0) and runs the method at offset 36 (vcall
# through ->0->36). The rest of the 320-line body walks the returned structure (reading fields at +76 and
# other offsets) and forwards the work to DispatchVcallSequence7_82BB0148 for the per-type handler chain.
# This is the "dispatch a nested object through its two primary vtable slots" routine for the 0x82BAF
# object-processing cluster (sibling of ProcessBitTransform_82BAFA00 / SetFlagBit_82BAFA98 / DispatchVcall
# Sequence7_82BB0148). Reached only via a vtable / function-pointer table (no direct callers in the
# recompiled code).
# 320 lines, 1 callee (DispatchVcallSequence7_82BB0148). No direct callers found in the recompiled code
# (likely reached via a vtable / function-pointer table).
[entrypoint.functions."0x82BAFCE0"]
name = "ProcessVcallObject_82BAFCE0"

# Processes one element of a 16-byte array after a flag check. It takes a base object (obj) and an index
# (a). It computes the element address (obj->8 + index * 16), reads the element's header word, and tests
# bit 30 (a "present / enabled" flag); if clear it bails early (loc_82BB0A2C). When set it re-derives the
# element's payload offset (a *3 then *8 scaling of the element's offset field, plus obj->16 as a base)
# and dispatches the element through a vtable (vcall through obj->4->0->32). The remaining ~150 lines handle
# the per-element follow-up. This is the "visit one entry of a 16-byte element table" routine for the 0x82BB0
# array-processing cluster. Reached only via a vtable / function-pointer table (no direct callers in the
# recompiled code). 181 lines, 0 direct callees (one indirect vtable call). No direct callers found in the
# recompiled code (likely reached via a vtable / function-pointer table).
[entrypoint.functions."0x82BB0910"]
name = "ProcessArrayElement_82BB0910"

# Computes the address of an element in a 2D / 3D table of 8-byte entries and performs a cache prefetch.
# It takes a base object (obj), an index descriptor (a), and a float (f1). It derives the row offset from
# a->12 (scaled by *3 then *8 to land on the 8-byte stride) and adds the table base (obj->16); it then
# derives a secondary offset from a->8 (the same *3 *8 scaling) to position within the element. When the
# row count is small (< 2) it falls through to a different indexing mode (reading a->8 as the primary
# index). It issues a dcbt (data cache block touch) to warm the cache before the access, and the bulk of
# the 356-line body then reads / transforms the element (forwarding to sub_82BB07A8 for a per-element
# handler). This is the "index into a 2D/3D 8-byte element grid and prefetch" routine for the 0x82283
# table-access cluster. 356 lines, 1 callee (sub_82BB07A8). 1 caller: sub_82284218.
[entrypoint.functions."0x82283FB0"]
name = "Access2DElement_82283FB0"

# Returns the field at offset 24 from an object - a one-line getter. It loads the 32-bit word at obj->24
# and returns it in r3. This is the trivial accessor used to expose a single object member to the (vtable /
# function-pointer-reached) call site. Part of the 0x82284 field-accessor cluster (sibling of
# GetField568_82173CC0). 5 lines, 0 direct callees (pure one-load accessor). No direct callers found in the
# recompiled code (likely reached via a vtable / function-pointer table).
[entrypoint.functions."0x82284200"]
name = "GetField24_82284200"

# Performs a lock-protected 2D table lookup. It enters a global critical section (RtlEnterCriticalSection
# on a .data section), then reads a key word from the input (a->0): the low byte is shifted left by 2 to
# form a row index (r9 << 2) and the full word shifted by 2 and masked forms a column offset (r8 << 2).
# It walks a global row table (a .data pointer at r10 + 22620) to fetch the row, dereferences the row's
# sub-pointer (->8), and indexes into it by the column offset, returning the address just past the matched
# entry (result + 4). It then leaves the critical section (RtlLeaveCriticalSection). This is the
# "locked 2D array / hash lookup by packed key" primitive for the 0x82C81 table cluster, used by 10
# callers (the 0x82361/0x82A16/0x82BF7/0x82C86/0x82C87 range). Part of the 0x82C81 locked-lookup cluster
# (sibling of LockedReadSubobject_82B44360). 67 lines, 0 direct callees (only the two imported
# RtlEnter/LeaveCriticalSection calls). 10 callers.
[entrypoint.functions."0x82C81E58"]
name = "LockedTableLookup_82C81E58"

# Dispatches an object through a type switch and adjusts a running size. It first calls
# SwitchDispatch_82A2F3E8 with (obj, b) to run the type-specific handler, capturing the returned pointer.
# It then null-checks the result (result->4) against a .data sentinel; when the size fields allow it
# (obj->24 > obj->20) it decrements the running offset (obj->24 -= 20) and continues processing, routing
# further work through sub_82228D98 and sub_82A2FA28 for the per-type follow-up. This is the "dispatch one
# object and account for its size" routine for the 0x82A2F dispatch cluster (sibling of
# SwitchDispatch_82A2F3E8). 233 lines, 3 callees (SwitchDispatch_82A2F3E8, sub_82228D98, sub_82A2FA28). 4
# callers (the 0x82228/0x8223B/0x82A2F/0x82A30 cluster).
[entrypoint.functions."0x82A2FBE0"]
name = "ProcessDispatchSize_82A2FBE0"

# Destructor for a 20-byte object that also frees the memory when asked - the 0x82A11 twin of
# DestructAndFree_82A14600. It rewrites the object's vtable pointer (obj->0) with a "being-deleted" /
# base-class vtable constant, runs the 20-byte member teardown (Destruct20Byte_832156E0), and - when the
# second argument's low bit is set (a & 1) - frees the storage through Free_SizeBucketed_Thunk_8221BE68.
# The difference from its sibling is only the specific vtable constant installed (a different 0x82A11-region
# object type). This is the "operator delete / ~T" entry for the 0x82A11 20-byte-object cluster (sibling of
# Destruct6_82A11E68 / Destruct8_82A11F30 / DestructAndFree_82A14600). Reached only via a vtable /
# function-pointer table (no direct callers in the recompiled code). 56 lines, 2 callees. No direct callers
# found in the recompiled code (likely reached via a vtable / function-pointer table).
[entrypoint.functions."0x82A11EC8"]
name = "DestructAndFree2_82A11EC8"

# Reads a 4-byte element out of a cursor-tracked buffer and advances the read position - the 0x83213 twin of
# ProcessRead13_83212218. It takes a buffer descriptor (a) and an output object (obj). It first checks that
# at least 4 bytes remain (a->20 >= 4); when so it copies the 4 bytes at the current cursor (a->12)
# byte-by-byte into a scratch slot (r1 + 84), then advances the data cursor (a->12 += 4) and decrements the
# remaining count (a->20 -= 4). The element's type-specific follow-up is routed through sub_82A1B480 and
# sub_83211D48 (a 0x83211 buffer-read helper) depending on the element kind. This is the "pull one 4-byte
# element off the buffer" step in the 0x83213 buffer-read cluster (sibling of ProcessRead13_83212218 /
# ProcessRead11_83213150 / ProcessRead12_832128D0 / sub_83211D48).
# 121 lines, 2 callees (sub_82A1B480, sub_83211D48). No direct callers found in the recompiled code (likely
# reached via a vtable / function-pointer table).
[entrypoint.functions."0x83213408"]
name = "ProcessRead14_83213408"

# Returns a fixed pointer - a one-line accessor that loads a constant 0x82C24-region address (from a .data
# table) and hands it back. It is the kind of trampoline used to expose a global / singleton (likely a
# vtable, type descriptor, or static object) through a callable entry. This is the 0x82C24 twin of
# ReturnConstant_82C2AC78 / ReturnConstant2_82C27F90 (which return neighboring .data pointers). Reached only
# via a vtable / function-pointer table (no direct callers in the recompiled code). Part of the 0x82C24
# constant-return cluster (sibling of ReturnConstant_82C2AC78 / ReturnConstant2_82C27F90 /
# ReturnConstant_2048_82323C60 / ReturnPointer_82C3BE78).
# 6 lines, 0 direct callees. No direct callers found in the recompiled code (likely reached via a vtable /
# function-pointer table).
[entrypoint.functions."0x82C240A8"]
name = "ReturnConstant3_82C240A8"

# Returns a fixed pointer - a one-line accessor that loads a constant 0x82C26-region address (from a .data
# table) and hands it back. It is the kind of trampoline used to expose a global / singleton (likely a
# vtable, type descriptor, or static object) through a callable entry. This is the 0x82C26 twin of
# ReturnConstant_82C2AC78 / ReturnConstant2_82C27F90 / ReturnConstant3_82C240A8 (which return neighboring
# .data pointers). Reached only via a vtable / function-pointer table (no direct callers in the recompiled
# code). Part of the 0x82C26 constant-return cluster (sibling of ReturnConstant_82C2AC78 /
# ReturnConstant2_82C27F90 / ReturnConstant3_82C240A8 / ReturnPointer_82C3BE78).
# 6 lines, 0 direct callees. No direct callers found in the recompiled code (likely reached via a vtable /
# function-pointer table).
[entrypoint.functions."0x82C269D8"]
name = "ReturnConstant4_82C269D8"

# Processes a typed value by dispatching on its kind and post-checking the result. It first runs the value
# through DispatchValueType_822285D8 (the type-tag interpreter), clearing a status byte (a->6 = 0) first.
# It then compares the returned pointer against a .data sentinel (r11 - 26784); on the "expected" result it
# inspects the second value's kind (b->4) and branches on it (0, 3, and other cases each route to different
# follow-up handlers), otherwise it jumps to a default path. The remaining work is forwarded to
# sub_82A2C520 and ProcessDispatchSize_82A2FBE0. This is the "interpret one value and react to its kind"
# routine for the 0x82228 typed-value cluster (sibling of DispatchValueType_822285D8). 91 lines, 3 callees
# (DispatchValueType_822285D8, sub_82A2C520, ProcessDispatchSize_82A2FBE0). 5 callers (the 0x82228/0x82A1E/
# 0x82A2F/0x82A37 cluster).
[entrypoint.functions."0x82228D98"]
name = "ProcessDispatchValue_82228D98"

# Returns a field from a typed value, dispatching on the value's kind. It first resolves the value's base
# pointer via GetIndexedPointer_82BC5448, then reads the value's type tag (->4) and branches: when the tag
# is 2 it returns the value's payload directly (->0), when the tag is 7 it returns a pointer offset into the
# payload (->0 + 24), and for any other tag it returns null (0). This is the "extract the relevant field
# from a value whose layout depends on its type" getter for the 0x822AF typed-value cluster, used by 10
# callers (the 0x82BB1/0x82BB7/0x82BB8/0x82BB9/0x82BBF/0x82BC0/0x82BD6/0x82BD8 range). Part of the 0x822AF
# typed-value cluster (sibling of GetIndexedPointer_82BC5448 / ProcessDispatchValue_82228D98). 56 lines, 1
# callee (GetIndexedPointer_82BC5448). 10 callers.
[entrypoint.functions."0x822AF338"]
name = "GetTypedField_822AF338"

# Returns the field at offset 24 from an object - a one-line getter (the 0x82B56 twin of GetField24_82284200).
# It loads the 32-bit word at obj->24 and returns it in r3. This is the trivial accessor used to expose a
# single object member to its call sites, here reached from 10 callers in the 0x82B5/0x82EE/0x8301/0x8302/
# 0x8304/0x8305 range (including several Func_8304x thunk entries). Part of the 0x82B56 field-accessor
# cluster (sibling of GetField24_82284200 / GetTypedField_822AF338). 5 lines, 0 direct callees (pure
# one-load accessor). 10 callers.
[entrypoint.functions."0x82B56750"]
name = "GetField24_82B56750"

# Runs an element-processing step against a temporary ref-counted object, then returns the result. It
# first constructs a scratch ref-counted object on the stack (ConstructRefCounted_8222CF18 at r1 + 80 with
# a -1 size hint), then drives the element walk through ProcessElements_Vcall4_82C46C10, passing
# (obj + 8) as the element source and the scratch object as both the accumulator and context. It destructs
# the scratch object (DestructRefCounted_82214F08) and, when the walk returned a non-null result (r31),
# returns the result's ->4 field; otherwise it returns 0. This is the "process a value into a temporary and
# extract its outcome" routine for the 0x82C02 temp-ref cluster. 61 lines, 3 callees (ConstructRefCounted_
# 8222CF18, ProcessElements_Vcall4_82C46C10, DestructRefCounted_82214F08). 1 caller: sub_82C155B8.
[entrypoint.functions."0x82C025A8"]
name = "ProcessWithTempRef_82C025A8"

# Processes an object by building a temporary reference, dispatching it, and copy-assigning the outcome.
# It first runs ProcessWithTempRef_82C025A8 to materialize a temporary ref-counted result for the input (a),
# then inspects the result (result->4->76) to decide whether the target's state actually changed. When it
# did, it vtable-dispatches the owner (a->0->20), copies the new value into the destination via
# CopyAssign_RefCounted_821F0108, and tears down the temporary (DestructRefCounted_82214F08), balancing
# the reference with AtomicIncrement3_821F7798. The per-element value writes go through the 0x82BFF handler
# family (sub_82BFF7A0 / sub_82BFF7D8) and ProcessVcallSequence3_82C14D70. This is the "compute a new
# state, diff it, and assign it" routine for the 0x82C15 state-update cluster. 340 lines, 7 callees. 1
# caller: sub_82C16740.
[entrypoint.functions."0x82C155B8"]
name = "ProcessAssignRef_82C155B8"

# Dispatches an object's update handler and, if it reports a change, conditionally assigns the new value.
# It runs the object's "compute update" vtable slot (obj->0->24) with the input (a); if that returns a
# non-zero low byte (a change flag) it calls ProcessAssignRef_82C155B8 to copy the new state into the
# destination. If that also reports a change it then runs the object's follow-up vtable slot (obj->0->28)
# to finalize. This is the "ask the object for its new state, and commit it only if it changed" routine for
# the 0x82C16 state-update cluster (sibling of ProcessAssignRef_82C155B8). 86 lines, 1 callee
# (ProcessAssignRef_82C155B8). 2 callers: sub_82C17AE8, sub_82C17788.
[entrypoint.functions."0x82C16740"]
name = "ProcessConditionalAssign_82C16740"

# Forwards a value to a handler only when the owning object has a valid sub-object; otherwise returns null.
# It takes an owning object (obj) and a value (a). It dereferences obj->4 and reads the sub-object's
# validity word (obj->4->76); when that is zero it simply returns 0 (no sub-object present). When present it
# loads the sub-object's payload pointer (obj->4->76->0) and forwards the value to the handler
# (sub_82C1DE98), returning that handler's result. This is the "run the sub-object's value handler if the
# sub-object exists" trampoline for the 0x82C14 value-forwarding cluster. Reached only via a vtable /
# function-pointer table (no direct callers in the recompiled code). 24 lines, 1 callee (sub_82C1DE98). No
# direct callers found in the recompiled code (likely reached via a vtable / function-pointer table).
[entrypoint.functions."0x82C14CF0"]
name = "ForwardIfPresent_82C14CF0"

# Processes a value against an element and, if it resolves, forwards it to a vtable handler. It takes a
# base object (obj) and a value (a). It runs ProcessElements_Vcall4_82C46C10 on the element at obj + 28
# (passing the value as both the accumulator and context); when the walk returns a non-null result it
# dereferences the result's payload (result->0) and dispatches through the handler vtable (vcall through
# result->0->16), returning the handler's outcome. When the walk finds nothing it returns 0. This is the
# "resolve one element and dispatch its value handler" routine for the 0x82C1D element-forwarding cluster
# (sibling of ProcessElements_Vcall4_82C46C10). Called only by ForwardIfPresent_82C14CF0. 62 lines, 1
# callee (ProcessElements_Vcall4_82C46C10). 1 caller: ForwardIfPresent_82C14CF0.
[entrypoint.functions."0x82C1DE98"]
name = "ProcessForwardElement_82C1DE98"

# Returns one of two fixed pointers depending on a condition. It takes a value (r4); when it is non-zero it
# returns a constant 0x82C2C-region address (r11 + 19856), otherwise it returns a neighboring constant (r11 +
# 19860). This is a "select between two related singletons / vtable descriptors by a flag" accessor - the
# two addresses are adjacent in .data, so they are almost certainly a matched pair (e.g. a type descriptor
# and its alternate, or two states of the same object). Reached only via a vtable / function-pointer table
# (no direct callers in the recompiled code). Part of the 0x82C2C constant-return cluster (sibling of
# ReturnConstant_82C2AC78 / ReturnConstant2_82C27F90 / ReturnConstant3_82C240A8). 22 lines, 0 direct callees.
# No direct callers found in the recompiled code (likely reached via a vtable / function-pointer table).
[entrypoint.functions."0x82C2CDD8"]
name = "ReturnConditionalConst_82C2CDD8"

# Compares two bounded byte strings and returns the first non-zero signed difference (0 when they match).
# It takes two pointers (a, b) and a length (n). When n <= 0 it returns 0 immediately. Otherwise it loops up
# to n bytes, loading a[i] and b[i] as unsigned bytes, computing the signed difference (a[i] - b[i]), and
# stopping early as soon as the difference is non-zero and a[i] is non-zero (an actual mismatch rather than
# a terminator) - returning that difference. If the loop runs to the end without a mismatch it returns 0.
# This is the bounded, strcmp-style "compare up to n bytes, return the sign of the first difference" routine
# for the 0x82CA3 string-compare cluster, used by 10 callers across the 0x821D/0x82319/0x824E3/0x825F3/0x8269E/
# 0x82844/0x82AAA/0x82B73/0x82BF7/0x8301D range. Part of the 0x82CA3 string cluster. 56 lines, 0 direct
# callees (pure byte-compare loop). 10 callers.
[entrypoint.functions."0x82CA3920"]
name = "StringCompare_82CA3920"

# Inserts an 8-byte entry into a hash table, growing the table first when it is full. It takes a table
# object (obj) and two values (a, b). It reads the table's geometry (obj->16 -> a table descriptor with
# capacity at +68 and used at +64); when the used count is below the capacity it runs ProcessLoopDispatch_
# 8227B8B8 to re-bucket / grow as needed. It then computes the hash through ComputeHash_8227BA30 (obj, a,
# b), writes the resulting key word into the entry slot (obj->8 -> 0) with a size word of 4 (obj->8 -> 4),
# and advances the table's insert cursor (obj->8 += 8). This is the "hash and insert one 8-byte entry" step
# for the 0x8219C hash-table cluster, used by 10 callers (the 0x82BB0/0x82BB1/0x82BB9/0x82BBA/0x82BBF/
# 0x82BDB/0x82BD4/0x82BD7/0x82BDA range). Part of the 0x8219C hash cluster (sibling of ProcessHashTable_
# 829FC3C0 / ProcessHashTable2_82419280 / ComputeHash_8227BA30). 62 lines, 2 callees. 10 callers.
[entrypoint.functions."0x8219CDA8"]
name = "InsertHashEntry_8219CDA8"

# Computes the address of an 8-byte element by index, with bounds checking and special-case handling. It
# takes a container (obj) and an index (a). For a positive index it computes the element address (obj->12 +
# index * 8), then tests whether the slot's back-pointer (address - 8) is before the container's start
# (obj->8) - an out-of-bounds / invalid slot that makes it return a .data sentinel pointer (r11 - 26784).
# For an index in the range (-10000, 0] it returns the address directly (obj->8 + index * 8), and a handful
# of sentinel negative indices (-10000, -10001, -10002) each map to a distinct fixed address. This is the
# "index into an 8-byte element array, with an out-of-range guard and named sentinels" accessor for the
# 0x82A1E element-access cluster, used by 10 callers (the 0x82228/0x8239F/0x823A5/0x824F7/0x825ED/0x8277C/
# 0x8290A/0x82A18/0x82A1E/0x82A29 range). Part of the 0x82A1E element-access cluster (sibling of
# ElementAccess_821E1498 / Access2DElement_82283FB0). 111 lines, 0 direct callees (pure address math +
# sentinel selection). 10 callers.
[entrypoint.functions."0x82A1E620"]
name = "AccessElement8_82A1E620"

# Validates a bounds condition and, when the value is in range, allocates and processes a result. It takes
# a result slot (obj) and a cluster of parameters (a..f). It forms a candidate position from the low 32
# bits of (d) plus (c) (r11 + c) and compares it against the container's limit (a->28). When the candidate is
# out of range it stores 0 into obj->0 and returns (a no-op). When in range it allocates a result buffer
# (Allocate_OrDbgBreak_8221F388), takes a lock (CriticalSection_EnterOrTry_82200688), and drives the
# per-element processing through VirtualCall4_8260C130 / sub_82B50E70 / sub_82C6F8E8, writing the result
# into obj. This is the "range-check a value, then build and populate its result" routine for the 0x82B43
# bounds-processing cluster. Reached only via a vtable / function-pointer table (no direct callers in the
# recompiled code). 262 lines, 5 callees. No direct callers found in the recompiled code (likely reached
# via a vtable / function-pointer table).
[entrypoint.functions."0x82B43E30"]
name = "ProcessBoundsCheck_82B43E30"

# Constructs an object with a vtable and seed fields, then processes a source value into it. It takes a
# destination (obj) and a set of parameters (a..f). It installs the object's vtable (obj->0) and several
# .data constant pointers / zeros into the leading fields (obj->0, obj->4, obj->8, obj->12) - a fixed
# template for the object type. When the source (b) is non-null it reads b->0 and drives the per-field
# population: the sub-object is read under a lock (LockedReadSubobject_82B44360) and the individual fields
# are processed through ProcessField_82B51380. This is the "build a templated object and fill it from a
# source" routine for the 0x82B50 construct/process cluster. Called only by ProcessBoundsCheck_82B43E30.
# Part of the 0x82B50 cluster (sibling of ProcessField_82B51380 / LockedReadSubobject_82B44360). 155
# lines, 2 callees. 1 caller: ProcessBoundsCheck_82B43E30.
[entrypoint.functions."0x82B50E70"]
name = "ConstructProcessObject_82B50E70"

# Compares two objects (or their sub-objects) for equality. It takes two objects (a, b). It first bails
# early when the pointers are identical (a == b). Otherwise it dereferences each object's sub-object pointer
# (a->4 and b->4), loads the sub-objects' leading values (a->4->0 and b->4->0), and compares them, walking
# the remaining fields as needed to decide equality. The comparison bookkeeping is kept in a stack frame
# (the r1 + 80..108 slots hold the two sub-objects and their current fields). When the comparison reaches a
# range it forwards to ProcessRangeCheck_82A0F290. This is the "are these two values equal" predicate for
# the 0x82A0F object-compare cluster, called only by BuildStructure_82A118B0. Part of the 0x82A0F cluster
# (sibling of ProcessRangeCheck_82A0F290). 210 lines, 1 callee (ProcessRangeCheck_82A0F290). 1 caller:
# BuildStructure_82A118B0.
[entrypoint.functions."0x82A0F120"]
name = "CompareObjects_82A0F120"

# Clears (destructs) an element array, freeing each element. It takes an array container (obj). It rewrites
# the container's vtable (obj->0) with a "cleared" descriptor, then - when the element count (obj->12) is
# non-zero - loops over every element, reading the element pointer from the element table (obj->4 + i * 4)
# and freeing it (Free_Thunk_824FE010). When the loop finishes it zeroes the count (obj->12 = 0), leaving
# the container empty. This is the "drop all elements of this array (freeing each)" teardown routine for the
# 0x82C2D array-destruct cluster, used by 10 callers (the 0x82C1C/0x82C25/0x82C29/0x82C2A/0x82C2C/0x82C2E/
# 0x82C34/0x82C38/0x82C40/0x82C43 range). Part of the 0x82C2D array cluster. 80 lines, 1 callee
# (Free_Thunk_824FE010). 10 callers (the 0x82C1C-0x82C43 container cluster).
[entrypoint.functions."0x82C2D228"]
name = "ClearElementArray_82C2D228"

# Compacts then fully clears (destructs) an element array, freeing each element. It takes an array container
# (obj). It rewrites the container's vtable (obj->0) with a "cleared" descriptor, runs ArrayCompact_82C23DD0
# to pack any surviving entries, then - when the element count (obj->12) is non-zero - loops over every
# element, reading the element pointer from the element table (obj->4 + i * 4) and freeing it
# (Free_Thunk_824FE010). When the loop finishes it zeroes the count (obj->12 = 0), leaving the container
# empty. This is the "compact, then drop every element (freeing each)" teardown for the 0x82C39 array cluster.
# It is the compact-first sibling of ClearElementArray_82C2D228. Used by 10 callers (the 0x82C1B/0x82C1C/
# 0x82C27/0x82C2A/0x82C2E/0x82C34/0x82C38/0x82C3B/0x82C41 range). 83 lines, 2 callees (ArrayCompact_82C23DD0,
# Free_Thunk_824FE010). 10 callers (the 0x82C1B-0x82C41 container cluster).
[entrypoint.functions."0x82C39298"]
name = "CompactClearArray_82C39298"

# Walks a linked list to its tail and processes the end, allocating a fresh node when needed. It takes a
# list object (obj). It starts at the list's head (obj->12) and follows the chain (each node's ->12 is the
# next node) until it reaches a node whose ->8 field is set (a marker) or the list ends (node == 0). When it
# reaches the end it calls sub_8322FE78(obj) to materialize / acquire the terminal node (which in turn may
# pull fresh memory via VirtualAllocate_82CC0CA0); when that produces a node it returns it, otherwise it
# returns 0. This is the "descend to the list tail and produce its next node" routine for the 0x8322F pool
# cluster, called only by ProcessPoolAcquire_83236230. Part of the 0x8322F pool/list cluster (sibling of
# VirtualAllocate_82CC0CA0 / ProcessPoolAcquire_83236230). 266 lines, 2 callees (VirtualAllocate_82CC0CA0,
# sub_8322FE78). 1 caller: ProcessPoolAcquire_83236230.
[entrypoint.functions."0x8322FF90"]
name = "ProcessListTail_8322FF90"

# Grows a bit array and allocates the next bit, returning the index. It takes a bit-array object (obj) and
# a requested size (a). It forms a target size (a + 4) aligned up to a 4-byte boundary, then compares it
# against the array's current capacity (obj->8 >> 1). When the request fits it allocates the next bit index
# directly (the low bit of obj->8 distinguishes the two allocation directions); when it does not fit it
# grows the backing buffer (re-allocating via Allocate_OrDbgBreak_8221F388, copying the existing bits with
# MemCpy_Vectorized_822085D0, zeroing the fresh region with MemSet_SdkRuntime_82CA3190, and dropping the old
# buffer via Free_SizeBucketed_Thunk_8221BE68) before returning the new index. This is the "ensure the bit
# array is large enough and hand out a bit" allocator for the 0x821F1 bit-set cluster, used by 3 callers
# (sub_82197400 / sub_82B3AB48 / sub_82B3ABF0). Part of the 0x821F1 bit-set cluster. 201 lines, 4 callees.
# 3 callers.
[entrypoint.functions."0x821F1248"]
name = "GrowBitArray_821F1248"

# Sizes a bit array to hold a string and installs the string's bytes into it. It takes a bit-array object
# (obj) and a source byte string (a). It first scans (a) to measure its length (the run of non-zero bytes,
# counting up to the first zero). It then grows the bit array to accommodate obj->4 + length bytes through
# GrowBitArray_821F1248, copies the string bytes into the new region (MemCpy_Vectorized_822085D0), and
# zero-fills any remainder (MemSet_SdkRuntime_82CA3190). This is the "reserve space in the bit/pool array for
# this string and copy it in" routine for the 0x82B3A bit-pool cluster, used by 3 callers (sub_821DA550 /
# sub_821EC668 / sub_82B3ABF0). It is the string-sizing sibling of GrowBitArray_821F1248. 98 lines, 3
# callees (GrowBitArray_821F1248, MemCpy_Vectorized_822085D0, MemSet_SdkRuntime_82CA3190). 3 callers.
[entrypoint.functions."0x82B3AB48"]
name = "GrowBitArrayForString_82B3AB48"

# Validates a size and constructs a sized descriptor / allocation for it. It takes a size (a). When a is
# non-zero and fits the constraint that 0xFFFFFFFF / a < 36 (i.e. a is large enough that a * 9 cannot
# overflow a 32-bit value), it builds a small on-stack descriptor (r1 + 80 / + 84 holding a .data template
# and a zero) and drives the construction through sub_822F1F00, then allocates the final backing storage
# (Allocate_OrDbgBreak_8221F388). The tail always computes the aligned size as a + a * 8 (= a * 9) rounded
# up to 4 bytes. This is the "check the size is safe to grow and build the descriptor" step for the 0x82377
# construct cluster, used by 3 callers (sub_82376200 / sub_82376A90 / sub_82B41E28). Part of the 0x82377
# construct cluster (sibling of sub_822F1F00). 70 lines, 2 callees (Allocate_OrDbgBreak_8221F388,
# sub_822F1F00). 3 callers.
[entrypoint.functions."0x82377F60"]
name = "ValidateAndConstruct_82377F60"

# A large transform routine that reads a 16-byte descriptor, derives a value from an object's fields, and
# reconstructs the resulting array. It takes an object (a), a context (b), and a pointer to a 16-byte
# descriptor (c). It copies the descriptor into a stack-local copy (r1 + 80, four words). It reads a->4;
# when non-zero it computes (a->12 - a->4) >> 4 (a count of 16-byte elements) and otherwise uses 0. It then
# drives the reconstruction: building the 16-byte element array (ConstructArray16_82512640), releasing the
# old storage (Free_SizeBucketed_Thunk_8221BE68), and populating the per-element data through sub_82A97648.
# This is the "apply a descriptor to rebuild this object's element array" transform for the 0x826FA
# transform cluster, used by 2 callers (sub_826FA0D8 / sub_832402B8). Part of the 0x826FA transform cluster.
# 520 lines, 3 callees (ConstructArray16_82512640, Free_SizeBucketed_Thunk_8221BE68, sub_82A97648). 2
# callers.
[entrypoint.functions."0x826FAFD0"]
name = "ProcessTransform_826FAFD0"

# Constructs a refcounted object that carries a float value and a lowercased string. It takes a
# destination (obj). It seeds obj->0 with a .data float constant, then builds the refcounted payload:
# the float sub-value is created through ConstructRefCounted_8222CF18, a key string is lowercased
# (ProcessToLower_821EEA00) and materialized (ProcessConstructString_82BFD780), and the resulting
# string pointer is stored into obj->4. On the teardown path the refcounted members are released via
# DestructRefCounted_82214F08. This is the "build a refcounted {float, lowercased-string} record" constructor
# for the 0x83211 construct cluster, called only by ProcessDispatchValue2_832351B0. Part of the 0x83211
# construct cluster (sibling of ProcessConstructString_82BFD780 / ProcessToLower_821EEA00). 132 lines, 4
# callees. 1 caller: ProcessDispatchValue2_832351B0.
[entrypoint.functions."0x83211790"]
name = "ConstructFloatString_83211790"

# A flag-guarded one-time subsystem initialization. It takes several parameters (a..e). It reads a global
# control word (a fixed .data address, +13712); when its low bit is already set the routine has already run
# and it returns early. On first run it sets that bit (so subsequent calls are no-ops) and then performs the
# full initialization sequence: it drives the subsystem setup through sub_823D2790 / sub_82CA3700 / sub_
# 825BF630 / sub_832130E8, and populates the global chain/tree state through FindChainRoot_82A596F0 and
# Tree_FindNode2_82A12230, releasing stale refcounted entries with Release_RefCounted_829FF648. This is the
# big "initialize the game's shared subsystems exactly once" entry point for the 0x83232 init cluster,
# called only by sub_82A139D0. Part of the 0x83232 init cluster. 1056 lines, 7 callees. 1 caller: sub_82A139D0.
[entrypoint.functions."0x83232BB8"]
name = "InitializeSubsystems_83232BB8"

# Constructs a string-carrying object, seeding fixed header fields and processing the string payload. It
# takes a destination (obj) and a source (a). It writes the object's fixed fields (obj->4 = 0, obj->20 = 0,
# obj->24 = 7) and a flag word of 2049, then prepares a stack buffer (r1 + 80) through Trampoline_8217A978,
# measures the string length (StrLenUTF16_8217AB30), and drives the element construction through
# ProcessElementAccess2_8217A6C0 and ReadStringElement_822F2238. This is the "build a {string} object"
# constructor for the 0x822F2 string-object cluster, used by 7 callers (sub_822F27C0 / sub_823728E0 /
# sub_823735B0 / sub_824E93A8 / sub_824EA150 / sub_825F2C98 / sub_8260B9E0). Part of the 0x822F2 string-
# object cluster (sibling of ReadStringElement_822F2238 / SetString_822F2020). 73 lines, 4 callees. 7 callers.
[entrypoint.functions."0x822F21B8"]
name = "ConstructStringObject_822F21B8"

# Computes a 32-bit hash over a word array. It takes an output slot (a), a source buffer (b), and a byte
# length (n). It folds a fixed hash prime (a .data constant OR'd with 0xE8D5, giving the game's FNV-style
# mixing constant) into a running hash: for each 4-byte word of the buffer it multiplies by the prime,
# rotates one byte to the high end (value << 8), XORs the byte back in, multiplies again, and XORs the
# result into the accumulator (a classic Jenkins / FNV-multiply-mix). When the length is a multiple of four
# the whole buffer is processed; the leftover trailing bytes are handled through sub_82A2DCC8. This is the
# "hash a byte buffer into a 32-bit key" routine for the 0x82227 hash cluster, used by 10 callers (the
# 0x82A1E/0x82A20/0x82A24/0x82A28/0x82A2B/0x82A2E/0x82A36/0x82BF4 range). Part of the 0x82227 hash cluster
# (sibling of ComputeHash_8227BA30 / HashNamedEntry_82188B88). 217 lines, 1 callee (sub_82A2DCC8). 10
# callers.
[entrypoint.functions."0x82227EA0"]
name = "ComputeStringHash_82227EA0"

# Destructs (clears) a range of 36-byte elements, freeing any that carry a heap payload. It takes a start
# (a) and an end (b) that delimit a contiguous array of 36-byte elements. It walks the range element by
# element: when an element's size field (element->24) is at least 8 it had an allocated buffer, so that
# buffer (element->4) is released (Free_SizeBucketed_Thunk_8221BE68). Every element is then reset to its
# empty state (element->24 = 7, element->20 = 0, element->4 = 0). This is the "teardown a block of
# {36-byte element} array" destructor for the 0x82378 element-range cluster, used by 5 callers (the
# 0x82376/0x82377/0x82B41 range). Part of the 0x82378 element-range cluster (sibling of
# ClearElementArray_82C2D228). 57 lines, 1 callee (Free_SizeBucketed_Thunk_8221BE68). 5 callers.
[entrypoint.functions."0x82378088"]
name = "DestructElementRange_82378088"

# Searches a tree down to the leaf that matches a key. It takes a tree root handle (a), a search context
# (b), and a key (c). It descends from the tree's first node (b->4 -> 4), at each node comparing the
# node's key field (node->12) against the requested key (c->0): when the node's key is at least the target
# it moves to the left child (node->0), otherwise it climbs to the parent / right side (node->8). A node's
# flag byte (node->21) marks the terminal / matching node. Once the matching leaf is located it records the
# path (the parent and node into the stack) and continues the per-leaf processing. This is the "find the
# tree leaf owning this key" descent for the 0x825CB tree cluster, used by 10 callers (the 0x821F5/0x823C1/
# 0x82431/0x8257C/0x82593/0x828C6/0x82A00/0x82C54 range). Part of the 0x825CB tree cluster (sibling of
# Tree_FindNode2_82A12230 / FindChainRoot_82A596F0). 87 lines, 0 direct callees (pure tree walk). 10 callers.
[entrypoint.functions."0x825CB448"]
name = "FindTreeLeaf_825CB448"

# A thin wrapper that measures a UTF-16 string and hands it to the buffer processor. It takes a destination
# (a) and a source string (b). It computes the string's length (StrLenUTF16_8217AB30) and then drives the
# copy/processing through ProcessBuffer_822F6CB0(a, b, length). This is the "measure-and-process this
# string into the buffer" helper for the 0x822F5 string-buffer cluster, called only by ReadStringElement_
# 822F2238. Part of the 0x822F5 string-buffer cluster (sibling of ProcessBuffer_822F6CB0 / StrLenUTF16_
# 8217AB30). 47 lines, 2 callees (StrLenUTF16_8217AB30, ProcessBuffer_822F6CB0). 1 caller:
# ReadStringElement_822F2238.
[entrypoint.functions."0x822F5258"]
name = "ProcessStringBuffer_822F5258"

# Validates a range of 36-byte elements, trapping when the range is corrupt. It takes a context (a), a
# range descriptor (b), and a value (c). It reads the range's start (b->4) and end (b->8). When the start
# is null or (b->8 - b->4) / 36 rounds to zero there is nothing to validate, so it returns early. When the
# start pointer is past the end pointer (b->4 > b->8) the range is corrupt and it raises a trap
# (ppc_trap 22) - a deliberate abort on bad memory. Otherwise it validates the element count and drives the
# per-element processing through sub_82376A90. This is the "make sure this {36-byte element} range is sane,
# else trap" validator for the 0x82376 element-range cluster, called only by Array_PushElement_823758C0.
# Part of the 0x82376 element-range cluster (sibling of DestructElementRange_82378088). 136 lines, 1
# callee (sub_82376A90). 1 caller: Array_PushElement_823758C0.
[entrypoint.functions."0x82376118"]
name = "ValidateElementRange_82376118"

# Bulk-loads an array of 36-byte elements from a source. It takes a context (a), a destination range (b), a
# count (c), and a source (d). When the count is non-zero it walks the destination element by element
# (each 36 bytes apart); for every non-null destination element it pulls the corresponding data from the
# source through LoadFrom_Source_822F2398(b, d). When the loop finishes it returns the aligned total size
# of the loaded block (count * 9, rounded up to 4 bytes) plus the base. This is the "load this range of
# {36-byte element} entries from the source" bulk loader for the 0x82376 element-range cluster, called only
# by sub_82376A90. Part of the 0x82376 element-range cluster (sibling of DestructElementRange_82378088 /
# ValidateElementRange_82376118). 63 lines, 1 callee (LoadFrom_Source_822F2398). 1 caller: sub_82376A90.
[entrypoint.functions."0x823762F8"]
name = "LoadElementRange_823762F8"

# A large element-range processor that loads, validates, reconstructs, or frees a block of 36-byte
# elements. It takes a range object (a) and a set of parameters (b..d). It loads the range's header from
# the source (LoadFrom_Source_822F2398), computes the element count as (a->12 - a->4) / 36, and then drives
# the requested operation: growing / reconstructing the range (ValidateAndConstruct_82377F60,
# LoadElementRange_823762F8, sub_82377F00, sub_823780E0, sub_82684B38) or tearing it down
# (DestructElementRange_82378088, Free_SizeBucketed_Thunk_8221BE68). This is the "operate on this block of
# {36-byte element} entries" workhorse for the 0x82376 element-range cluster, called only by
# ValidateElementRange_82376118. Part of the 0x82376 element-range cluster (sibling of LoadElementRange_
# 823762F8 / DestructElementRange_82378088 / ValidateAndConstruct_82377F60). 454 lines, 7 callees. 1
# caller: ValidateElementRange_82376118.
[entrypoint.functions."0x82376A90"]
name = "ProcessElementRange_82376A90"

# Releases a globally-referenced object by decrementing its global refcount. It takes a handle (obj). It
# first runs the object's own release (Release_RefCounted_821C67D8), then - under a global lock - atomically
# decrements a global reference counter (a fixed .data address, +28340), retrying the load-modify-store until
# it lands. When the counter is finally zeroed it clears the handle (obj->0 = 0). This is the "drop one
# reference to a globally-shared singleton" release for the 0x821C6 global-ref cluster, used by 10 callers
# (the 0x821AD/0x82303/0x823B6/0x82416/0x8260E/0x82692/0x82696/0x826CA/0x82825/0x828E4 range). Part of the
# 0x821C6 global-ref cluster (sibling of Release_RefCounted_821C67D8). 64 lines, 1 callee
# (Release_RefCounted_821C67D8). 10 callers.
[entrypoint.functions."0x821C6868"]
name = "ReleaseGlobalRef_821C6868"

# Searches a chain of nodes for one whose key matches a value, then dispatches on the result. It takes an
# object (obj) and a target key (a). It walks the chain from obj->84 (each node's ->0 is the next node),
# comparing the node's key field (node->8) against (a): a smaller key aborts the walk (the chain is ordered
# and the target is not present), an exact match jumps to the found path (loc_82BCC6BC), and a larger key
# continues the walk. When the walk ends without a match it dispatches the default path through
# DispatchAndAdjustPos_8227BB58. This is the "walk this ordered node chain looking for the key, then
# dispatch" search for the 0x82BCC chain-search cluster, called only by sub_822DF280. Part of the 0x82BCC
# chain cluster (sibling of DispatchAndAdjustPos_8227BB58 / ProcessAdjustPosition_82C1C6A0). 117 lines, 1
# callee (DispatchAndAdjustPos_8227BB58). 1 caller: sub_822DF280.
[entrypoint.functions."0x82BCC620"]
name = "ChainSearchDispatch_82BCC620"

# Constructs a string element, seeding fixed header fields and processing the string payload. It takes a
# destination (obj) and a source string (a). It writes the element's fixed header fields (obj->4 = 0,
# obj->20 = 0, obj->24 = 7), measures the string length (StrLenUTF16_8217AB30), and drives the element
# construction through ProcessElementAccess2_8217A6C0(obj, a, length). This is the "build a {string}
# element" constructor for the 0x822F5 string-element cluster, used by 10 callers (the 0x822F2/0x822F3/
# 0x82372/0x82374/0x82B4A/0x82B5A/0x82C71/0x82C72/0x82C80 range). It is the string-element sibling of
# ConstructStringObject_822F21B8. Part of the 0x822F5 string-element cluster (sibling of
# ProcessStringBuffer_822F5258). 59 lines, 2 callees (StrLenUTF16_8217AB30, ProcessElementAccess2_8217A6C0).
# 10 callers.
[entrypoint.functions."0x822F5150"]
name = "ConstructStringElement_822F5150"

# Returns one of two fixed pointers depending on a condition. It takes a value (r4); when it is non-zero it
# returns a constant 0x82C26-region address (r11 + 19808), otherwise it returns a neighboring constant (r11 +
# 19812). This is a "select between two related singletons / type descriptors by a flag" accessor - the two
# addresses are adjacent in .data, so they are a matched pair (e.g. a type descriptor and its alternate, or
# two states of the same object). Reached only via a vtable / function-pointer table (no direct callers in
# the recompiled code). Part of the 0x82C26 constant-return cluster (sibling of ReturnConditionalConst_
# 82C2CDD8 / ReturnConstant_82C2AC78 / ReturnConstant2_82C27F90 / ReturnConstant3_82C240A8 / ReturnConstant4_
# 82C20150). 21 lines, 0 direct callees. No direct callers found in the recompiled code (likely reached via
# a vtable / function-pointer table).
[entrypoint.functions."0x82C263D0"]
name = "ReturnConditionalConst2_82C263D0"

# Returns a fixed global pointer. It loads a single word from a fixed .data address (r11 + 6784, a global
# object pointer) and returns it. This is a one-line "get the global singleton at this address" accessor
# for the 0x82239 global-getter cluster, used by 10 callers (the 0x82238/0x82239/0x82CA3/0x82CA5/0x82CA8/
# 0x82CAB/0x82CAE/0x82CBB/0x82CAF range). Part of the 0x82239 global-getter cluster (sibling of GetGlobalField_
# 82810540 / GetGlobalValue_82C2AC08). 10 lines, 0 direct callees (pure load + return). 10 callers.
[entrypoint.functions."0x82239798"]
name = "GetGlobalPointer_82239798"

# Conditionally records a value into a global object's field. It takes a value (a) and reads the current
# thread's context object (r13). When the context's "already set" marker (r13->336) is non-zero the routine
# is a no-op. Otherwise it writes (a) into the global object's slot (r13->256 -> 352). This is the "record
# this value into the global, but only if one has not already been recorded" setter for the 0x82CC1 global-
# setter cluster, used by 6 callers (the 0x82CC0/0x82CFA/0x82CBC range). Part of the 0x82CC1 global-setter
# cluster. 16 lines, 0 direct callees (pure flag-check + store). 6 callers.
[entrypoint.functions."0x82CC1C20"]
name = "SetGlobalFieldIfNeeded_82CC1C20"

# Matches two strings, applying a default when either is absent. It takes two pointers (a, b). When both are
# non-null it walks them byte by byte (a->0, b->0, ...) performing the string comparison. When either is
# null it takes the default path: it runs sub_82CAB770 (an error/trace sink), writes a sentinel code (22)
# into a->0, and drives the default handling through sub_82CAB630 (returning 0xFFFF07FF as the result). This
# is the "compare these two strings, or emit the default failure code" matcher for the 0x82CAA string-match
# cluster, called only by sub_82CAAF08. Part of the 0x82CAA string-match cluster (sibling of StringCompare_
# 82CA3920). 81 lines, 2 callees (sub_82CAB630, sub_82CAB770). 1 caller: sub_82CAAF08.
[entrypoint.functions."0x82CAAE80"]
name = "MatchString_82CAAE80"

# A thin wrapper around the string matcher. It takes two pointers (a, b), clears the third argument (r5 = 0),
# and forwards directly to MatchString_82CAAE80. This is the "match these two strings" convenience entry
# for the 0x82CAA string-match cluster, used by 5 callers (the 0x82A30/0x82C82/0x82C86/0x82C88 range). Part
# of the 0x82CAA string-match cluster (sibling of MatchString_82CAAE80). 9 lines, 1 callee (MatchString_
# 82CAAE80). 5 callers.
[entrypoint.functions."0x82CAAF08"]
name = "MatchStringWrapper_82CAAF08"

# Checks a growable counter and expands its backing block when it is about to overflow. It takes an object
# (obj) and a context (a). It reads the object's counter block (obj->40 -> current count at +4, limit at +8).
# When current + 1 is still within the limit there is room and the routine proceeds normally. When it would
# overflow it checks the limit against a hard maximum (0x7FFF0FFE); if below the maximum it grows the block
# (sub_82BCDAF8, doubling it to limit * 2 + 1), then continues with the new size. On the dispatch path it
# adjusts positions through DispatchAndAdjustPos_8227BB58 / sub_82BC9860. This is the "make sure the
# growable counter has room, doubling the block when full" allocator for the 0x82BCD counter-grow cluster,
# used by 8 callers (the 0x82BC9/0x82BCE range, including sub_82BCDAF8 itself). Part of the 0x82BCD counter
# cluster. 104 lines, 3 callees (DispatchAndAdjustPos_8227BB58, sub_82BC9860, sub_82BCDAF8). 8 callers.
[entrypoint.functions."0x82BCD9B8"]
name = "GrowCounterIfNeeded_82BCD9B8"

# Recursively destructs a nested object tree, freeing each node's heap payload. It takes a context (a) and a
# node (b). It first checks the node's "already torn down" flag (b->45); when set the walk is complete. It
# then recurses into the node's first child (sub_822F8380(a, b->8)), advancing through the sibling chain.
# When a node's size field (b->40) is at least 8 it carried an allocated buffer, so that buffer (b + 16 -> 4)
# is released (Free_SizeBucketed_Thunk_8221BE68) and the node's header is reset (b + 16 -> 24 = 7, the empty
# marker). This is the "walk and tear down this nested {36-byte node} tree, freeing each node's buffer"
# recursive destructor for the 0x822F8 cluster, used by 2 callers (sub_822F6F58 / sub_82371AA0). Part of the
# 0x822F8 destruct cluster (sibling of DestructAndFree_82C23A50 / DestructAndFree2_82C247A8). 79 lines, 2
# callees (Free_SizeBucketed_Thunk_8221BE68, sub_822F8380 [self-recursive]). 2 callers.
[entrypoint.functions."0x822F8380"]
name = "DestructNested_822F8380"

# Checks a status flag on a node and, depending on it, dispatches or increments a counter. It takes a value
# (a), a node (b), and a counter slot (c). It inspects the node's status word (b->12): when bit 0x40 is clear
# the node is "ready" and it runs the dispatch (sub_82CB7DA0), then - unless (a & 0xFFFF) is 0xFFFF (an
# error / not-found sentinel) - writes -1 as the result. When bit 0x40 is set (or b->8 is null) it instead
# increments the counter (c->0 += 1) and writes that back. This is the "test the node's ready flag, dispatch
# if ready, else bump the pending counter" handler for the 0x82CAC flag-cluster, used by 2 callers
# (sub_82CACCC0 / sub_82CB1438). Part of the 0x82CAC flag cluster. 62 lines, 1 callee (sub_82CB7DA0). 2
# callers.
[entrypoint.functions."0x82CACC58"]
name = "CheckFlagAndIncrement_82CACC58"

# A large node dispatcher that checks bounds and status, then drives the core per-node handler. It takes a
# value (a) and a node (b). It caches (a) on the stack, fetches the current global context
# (GetGlobalPtr3_82240578), and bails out (loc_82CB8064) when (b) points at one of the context's reserved
# slots (+32 or +64). It inspects the node's status word (b->12); when bit 0x40 is clear the node is ready
# and it drives the core handler (sub_82CAF6C8), otherwise it takes the not-ready path (loc_82CB7E50).
# The remaining body handles the various result states (found / not found / error) through sub_82CAB7E0 /
# sub_82CB6A88 / sub_82CBB100 / sub_82CC1798. This is the "validate the node, then run its core handler"
# dispatcher for the 0x82CB7 node-dispatch cluster, used by 3 callers (sub_82CACC58 / Func_830F0914 /
# sub_830F0880). Part of the 0x82CB7 node-dispatch cluster. 434 lines, 6 callees. 3 callers.
[entrypoint.functions."0x82CB7DA0"]
name = "ProcessNodeDispatch_82CB7DA0"

# Processes a flag and removes / adjusts an element in a container. It takes a context (a), a sub-object
# (b), and a container (c). When the sub-object's flag (b->4) is set it drives a gated 32-bit store through
# SubObject_VcallSlot3_GatedStore32_82C842C8 and records the result back into b->4. It then reads the
# current position (r1 + 80) and the container's limit (c->20): when (position - 1) exceeds the limit it
# takes the out-of-range path, otherwise it removes / compacts the element through sub_822F6E18(c, position
# - 1, -1) and the per-element cleanup through ProcessAppend_823712D8 / sub_823713D0. This is the "handle
# the sub-object's flag, then remove this element from the container" step for the 0x82371 element cluster,
# used by 2 callers (sub_823710A0 / sub_82371AA0). Part of the 0x82371 element cluster (sibling of
# ProcessAppend_823712D8). 92 lines, 4 callees. 2 callers.
[entrypoint.functions."0x82371240"]
name = "ProcessRemoveElement_82371240"

# Processes a fixed number of elements, gated by a bit-manipulation guard. It takes an object (a), a value
# (b), and a count (c). It reads a flag byte (a->5), runs a leading-zero-count / bit test (cntlzw + rotate +
# AND) to derive a mask; when that mask is zero the routine is a no-op. Otherwise it loops (c) times, each
# iteration loading the object's element pointer (a->0), decrementing the count, and processing the element
# through the object's fields (a->0 -> 12 and following). This is the "process this many elements, if the
# guard allows" loop for the 0x82371 element cluster, called only by ProcessRemoveElement_82371240. Part of
# the 0x82371 element cluster (sibling of ProcessRemoveElement_82371240 / ProcessAppend_823712D8). 119 lines,
# 0 direct callees (inline element processing). 1 caller: ProcessRemoveElement_82371240.
[entrypoint.functions."0x823713D0"]
name = "ProcessElementLoop_823713D0"

# Searches a tree down to the node matching a key, then updates it. It takes a tree root handle (a) and a
# key (b). It descends from the tree's first node (a->4 -> 4), at each node comparing the node's key field
# (node->12) against the requested key (b->0): a smaller key moves to the left child (node->0), a larger key
# climbs to the parent / right side (node->8), and a node's flag byte (node->45) marks the terminal / matching
# node. Once the matching node is located it updates it: clamping / adjusting the value (ClampAndUpdate_
# 822F6380), validating the size and raising on overflow (ValidateSizeOrRaise_82371CD8), and releasing the old
# buffer (Free_SizeBucketed_Thunk_8221BE68). This is the "find the tree node for this key and update its
# value" operation for the 0x82371 tree cluster, called only by sub_8236DD48. Part of the 0x82371 tree cluster
# (sibling of FindTreeLeaf_825CB448). 185 lines, 3 callees. 1 caller: sub_8236DD48.
[entrypoint.functions."0x823714A0"]
name = "FindTreeNode_823714A0"

# Constructs a sorted array object, seeding the header and sorting the elements. It takes a destination (obj)
# and an element source (a). It writes the object's vtable (obj->0) and zeroes the count fields (obj->4, obj->8,
# obj->12 = 0), then runs the element sort (BubbleSort3_82C16C78(a, obj + 4)) to order the elements in place.
# After the sort it rewrites the vtable to a final "sorted" descriptor (obj->0). This is the "build a {36-byte
# element} array and sort it" constructor for the 0x82C3D array cluster, used by 10 callers (the 0x82C1C/
# 0x82C1D/0x82C23/0x82C25/0x82C2E/0x82C31/0x82C38/0x82C40/0x82C41/0x82C43 range). Part of the 0x82C3D array
# cluster (sibling of BubbleSort3_82C16C78). 58 lines, 1 callee (BubbleSort3_82C16C78). 10 callers.
[entrypoint.functions."0x82C3D028"]
name = "ConstructSortedArray_82C3D028"

# Decrements a reference count and, when it reaches zero, processes the result. It takes a handle (a). It
# enters a global lock, atomically decrements the handle's reference count (a->4), and retries the load-
# modify-store until it lands. When the count reaches zero (and a is non-null) it reads the handle's payload
# (a->0) and drives the final processing (the release / cleanup path). This is the "drop one reference and
# finalize when the last is gone" decrement for the 0x82375 refcount cluster, reached only via a vtable /
# function-pointer table (no direct callers in the recompiled code). Part of the 0x82375 refcount cluster
# (sibling of ReleaseGlobalRef_821C6868 / DecrementRefCount_82C23B48). 77 lines, 0 direct callees (inline
# atomic decrement). No direct callers found in the recompiled code (likely reached via a vtable / function-
# pointer table).
[entrypoint.functions."0x82375980"]
name = "DecrementRefAndProcess_82375980"

# Returns one of two fixed pointers depending on a condition. It takes a value (r4); when it is non-zero it
# returns a constant 0x82C29-region address (r11 + 19832), otherwise it returns a neighboring constant (r11 +
# 19836). This is a "select between two related singletons / type descriptors by a flag" accessor - the two
# addresses are adjacent in .data, so they are a matched pair (e.g. a type descriptor and its alternate, or
# two states of the same object). Reached only via a vtable / function-pointer table (no direct callers in
# the recompiled code). Part of the 0x82C29 constant-return cluster (sibling of ReturnConditionalConst_
# 82C2CDD8 / ReturnConditionalConst2_82C263D0 / ReturnConstant_82C2AC78). 21 lines, 0 direct callees. No
# direct callers found in the recompiled code (likely reached via a vtable / function-pointer table).
[entrypoint.functions."0x82C29280"]
name = "ReturnConditionalConst3_82C29280"

# Searches a chain for a node matching a key and returns that node's value. It takes a chain handle (a). It
# reads the chain's first node (a->0) and the target key (a->4). It walks the chain (each node's ->0 is the
# next node), comparing the node's key field against the target: when a node's key equals the target it
# returns that node's value (node->16), otherwise it continues the walk. When the walk reaches the key's
# sentinel (or the chain ends without a match) it returns 0. This is the "look up this key in the chain and
# hand back its value" finder for the 0x82C46 chain cluster, used by 10 callers (the 0x82C02/0x82C0A/0x82C0C/
# 0x82C13/0x82C29/0x82C2E/0x82C50/0x82C55 range). Part of the 0x82C46 chain cluster (sibling of FindChainRoot_
# 82A596F0 / ChainSearch_82B3D880). 39 lines, 0 direct callees (pure chain walk). 10 callers.
[entrypoint.functions."0x82C469F0"]
name = "FindChainValue_82C469F0"

# Increments a counter and grows a bit array to accommodate a value. It takes a source (a) and a value slot
# (b). When the source's payload (a->0) is non-null it atomically increments a reference count
# (AtomicIncrement5_8222E840). It then reads the value slot (b->0): when the slot is empty it grows the bit
# array to hold a default constant (a .data address, +63), and when the slot holds a value it grows the bit
# array to hold that value's first word (b->0->0) - both through GrowBitArrayForString_82B3AB48. This is the
# "bump the refcount, then reserve bit-array space for this value" step for the 0x821EC bit-array cluster,
# used by 10 callers (the 0x821BD/0x82357/0x82427/0x8242E/0x824BE/0x825AD/0x825AE/0x82755/0x8287C/0x82B7C
# range). Part of the 0x821EC bit-array cluster (sibling of GrowBitArray_821F1248 / GrowBitArrayForString_
# 82B3AB48). 113 lines, 2 callees. 10 callers.
[entrypoint.functions."0x821EC668"]
name = "ProcessGrowBitArray_821EC668"

# Allocates and constructs a 16-byte object, seeding its header fields. It takes a context (a). It allocates
# a 16-byte block (Allocate_OrDbgBreak_8221F388); when the allocation succeeds it seeds the object's header
# (obj->0 = 2, obj->4 = 0, obj->8 = 0) and allocates a secondary zero-length buffer (Allocate_OrDbgBreak_
# 8221F388(0)) stored at obj->12, then records the object pointer into a local slot (r1 + 80). On allocation
# failure it stores a null. It then drives the element insertion through InsertElement4_8228A900. This is the
# "allocate and build a {16-byte} object with its secondary buffer" constructor for the 0x8236F construct
# cluster, used by 3 callers (sub_8236D4E0 / sub_823715E8 / sub_82371BF0). Part of the 0x8236F construct
# cluster (sibling of ConstructAllocArray_82376200). 76 lines, 2 callees. 3 callers.
[entrypoint.functions."0x8236F070"]
name = "ConstructAllocObject_8236F070"

# Processes a value and, when a flag is set, frees the buffer. It takes a buffer (a) and a value (b). It
# first drives the processing through sub_82B50FD0(a, b). It then inspects the low bit of the value (b & 0x1):
# when that bit is set it releases the buffer (Free_SizeBucketed_Thunk_8221BE68(a)), and when the bit is clear
# it leaves the buffer alone. This is the "process this value, then free its buffer if the result flagged it"
# step for the 0x82B50 process cluster, reached only via a vtable / function-pointer table (no direct callers
# in the recompiled code). Part of the 0x82B50 process cluster (sibling of sub_82B50FD0 /
# ConstructProcessObject_82B50E70). 50 lines, 2 callees (sub_82B50FD0, Free_SizeBucketed_Thunk_8221BE68). No
# direct callers found in the recompiled code (likely reached via a vtable / function-pointer table).
[entrypoint.functions."0x82B50F80"]
name = "ProcessAndFreeIfFlagged_82B50F80"

# Constructs an object with a vtable, then dispatches a vcall on each present sub-object. It takes a
# destination (a). It installs the object's vtable (a->0) and a .data constant (a->4), then - when a sub-object
# is present - dispatches a virtual call through it: for a->20 (if non-null) it calls a->20->0->8, and for
# a->16 (if non-null) it calls a->16->0->8 (bctrl through each sub-object's vtable slot +8). This is the
# "build the templated object and fire its sub-object vcall handlers" routine for the 0x82B50 construct cluster,
# called only by ProcessAndFreeIfFlagged_82B50F80. Part of the 0x82B50 construct cluster (sibling of
# ConstructProcessObject_82B50E70). 99 lines, 0 direct callees (vtable-reached via bctrl). 1 caller:
# ProcessAndFreeIfFlagged_82B50F80.
[entrypoint.functions."0x82B50FD0"]
name = "ConstructDispatchVcalls_82B50FD0"

# A locked field processor that takes a critical section, dispatches a vcall, and processes the result. It
# takes an object (a). It acquires the object's critical section (CriticalSection_EnterOrTry_82200688 on a +
# 52, with a stack lock slot at r1 + 80), then - when a sub-object is present (a->12 non-null) - dispatches a
# virtual call through it (a->12->0->12). It checks the result flag (r1 + 84); when set it releases the
# critical section (RtlLeaveCriticalSection) and drives the per-field processing through ProcessField_82B51380
# and sub_82C6F8E8. This is the "lock, dispatch the handler vcall, unlock, then process the field" routine for
# the 0x82B51 locked-field cluster, reached only via a vtable / function-pointer table (no direct callers in
# the recompiled code). Part of the 0x82B51 cluster (sibling of ProcessField_82B51380). 161 lines, 3 callees.
# No direct callers found in the recompiled code (likely reached via a vtable / function-pointer table).
[entrypoint.functions."0x82B51080"]
name = "LockedProcessField_82B51080"

# A locked double-vcall processor that takes a critical section, dispatches two vcalls, and processes the
# result. It takes an object (a). It acquires the object's critical section (CriticalSection_EnterOrTry_
# 82200688 on a + 52, with a stack lock slot at r1 + 80), then - when a sub-object is present (a->12 non-null)
# - dispatches two virtual calls through it: first a->12->0->36 (a handler with argument 2), then
# a->12->0->16. It checks the result flag (r1 + 84); when set it releases the critical section and drives the
# per-field processing through ProcessField_82B51380 and ByteValueCheck_82CBC678. This is the "lock, dispatch
# two handler vcalls, unlock, then process the field" routine for the 0x82B51 locked-field cluster, reached
# only via a vtable / function-pointer table (no direct callers in the recompiled code). Part of the 0x82B51
# cluster (sibling of LockedProcessField_82B51080 / ProcessField_82B51380). 173 lines, 3 callees. No direct
# callers found in the recompiled code (likely reached via a vtable / function-pointer table).
[entrypoint.functions."0x82B51188"]
name = "LockedDoubleVcall_82B51188"

# Claims a flag on an object, succeeding only when it is not already claimed. It takes an object (a). It
# reads the object's claim marker (a->24): when that marker is non-zero the object is already claimed and the
# routine returns -1 (failure). When the marker is zero it sets the claim flag (a->44 = 1) and returns 0
# (success). This is the "set the claimed bit if free, else fail" predicate for the 0x82B51 flag cluster,
# reached only via a vtable / function-pointer table (no direct callers in the recompiled code). Part of the
# 0x82B51 flag cluster (sibling of ProcessField_82B51380 / SetFlagBit_82C24860). 25 lines, 0 direct callees
# (pure flag check + set). No direct callers found in the recompiled code (likely reached via a vtable /
# function-pointer table).
[entrypoint.functions."0x82B51310"]
name = "ClaimFlagIfNeeded_82B51310"

# Copies a struct, deep-copying its variable-length payload. It takes a destination (a) and a source (b). It
# copies the source's header fields (b->0, b->4, b->8 -> a->0, a->4, a->8). It selects the payload size: when
# b->0 == 1 it uses b->4, otherwise b->8. It allocates a fresh payload buffer of that size
# (Allocate_OrDbgBreak_8221F388), copies the source's payload (b->12) into it (MemCpy_SdkRuntime_82CA2C60),
# and stores the new buffer pointer at a->12. This is the "shallow-copy the header, deep-copy the payload"
# struct copier for the 0x8236F struct cluster, called only by sub_8236DFC0. Part of the 0x8236F struct cluster
# (sibling of Copy28ByteStruct_82C2AC08 / ConstructAllocObject_8236F070). 60 lines, 2 callees
# (Allocate_OrDbgBreak_8221F388, MemCpy_SdkRuntime_82CA2C60). 1 caller: sub_8236DFC0.
[entrypoint.functions."0x8236FD78"]
name = "CopyStructWithPayload_8236FD78"

# Sets a synchronization event, returning success or failure. It takes an event handle (a). It calls the
# SDK's NtSetEvent (a kernel import, with a zeroed attributes word); when the call succeeds (a non-negative
# status) it returns 1, and when it fails (a negative NTSTATUS) it routes the error through sub_82CC1C38
# (an error/log sink) and returns 0. This is the "signal this event, report ok / fail" wrapper for the 0x822C9
# SDK-event cluster, used by 10 callers (the 0x821E2/0x8223E/0x8235A/0x8236C/0x8262D/0x82A89/0x82B45/0x82EF4
# range). Part of the 0x822C9 SDK-event cluster (sibling of the Nt* import thunks). 41 lines, 1 callee
# (sub_82CC1C38). 10 callers.
[entrypoint.functions."0x822C9380"]
name = "SetEvent_SdkRuntime_822C9380"

# Destructs an element array, freeing its elements and backing buffer, then clears the object. It takes an
# element-array object (a). When the array's element table (a->4) is non-null it walks the range from a->4 to
# a->8, freeing each element's payload (DestructElementRange_82378088), and then releases the element table
# itself (Free_SizeBucketed_Thunk_8221BE68). Finally it zeroes the object's array fields (a->4, a->8, a->12 =
# 0), leaving the object empty. This is the "teardown this element array (free each element, free the table,
# clear the fields)" destructor for the 0x82376 element-array cluster, used by 2 callers (sub_823750A0 /
# sub_82375AB8). Part of the 0x82376 element-array cluster (sibling of ClearElementArray_82C2D228 /
# DestructElementRange_82378088). 56 lines, 2 callees. 2 callers.
[entrypoint.functions."0x82376298"]
name = "DestructElementArray_82376298"

# Stores a value into an object, dispatching a vcall on it when it is non-null. It takes an object (a) and a
# value (b). It records the value in the object (a->0 = b), then - when b is non-null - dispatches a virtual
# call through it (b->0->4, bctrl through the value's vtable slot +4). This is the "assign this value and
# fire its vcall handler" step for the 0x82366 set/dispatch cluster, used by 10 callers (the 0x822F4/0x826C0/
# 0x82B44/0x82B48/0x82B4F/0x82B55/0x82BFC/0x82C65/0x82C67/0x82C68 range). Part of the 0x82366 set/dispatch
# cluster (sibling of SetGlobalFieldIfNeeded_82CC1C20). 47 lines, 0 direct callees (vtable-reached via
# bctrl). 10 callers.
[entrypoint.functions."0x82366210"]
name = "SetAndDispatch_82366210"

# Constructs a 28-byte-per-element array, validating the count and initializing the owning object. It takes an
# object (a) and a count (b). It validates the count: when b is zero or so large that -1 / b overflows the
# 28-byte stride (b * 28 would wrap), it skips the initialization. Otherwise it stages two .data pointers on a
# stack slot (r1 + 80 / r1 + 84) and drives the object initialization through sub_822F1F00. In all cases it
# allocates the array storage (Allocate_OrDbgBreak_8221F388(count * 28)). This is the "build this element array
# of 28-byte records, guarding the allocation size" constructor for the 0x829FE array cluster, used by 10
# callers (the 0x82171/0x82407/0x82911/0x82912/0x82B24/0x82C66 range). Part of the 0x829FE array cluster
# (sibling of ConstructElementRange_823762F8). 66 lines, 2 callees (Allocate_OrDbgBreak_8221F388, sub_822F1F00).
# 10 callers.
[entrypoint.functions."0x829FED20"]
name = "ConstructElementArray_829FED20"

# Copies a value from one object into another, dispatching a vcall on it when it is non-null. It takes a
# destination (a) and a source (b). It copies the source's value into the destination (a->0 = b->0), then -
# when that value (b->0) is non-null - dispatches a virtual call through it (b->0->0->4, bctrl through the
# value's vtable slot +4). This is the "transfer this value into the destination and fire its vcall handler"
# step for the 0x822F5 set/dispatch cluster, used by 10 callers (the 0x82200/0x822F5/0x82375/0x8266F/0x82B40/
# 0x82B4E/0x82B55/0x82B67/0x82C65/0x82C68 range). Part of the 0x822F5 set/dispatch cluster (sibling of
# SetAndDispatch_82366210 / ConstructStringElement_822F5150). 47 lines, 0 direct callees (vtable-reached via
# bctrl). 10 callers.
[entrypoint.functions."0x822F5478"]
name = "CopyAndDispatch_822F5478"

# A float-distance threshold checker that measures a value against a reference, caches a vector, and sets a
# flag. It takes an object (a) and a floating-point value (f1). It caches a 128-bit vector from a->64
# (lvx/stvx128 through a stack slot) and a working float on the stack, then computes the absolute difference
# between a reference float (f0, a .data constant / prior value) and the incoming value f1
# (fabs(ref - value)). When that distance is within the .data threshold it leaves a "matched" flag set;
# otherwise it clears it. Finally - when the flag is set - it records a->548 = 1 (a matched / in-range byte).
# This is the "measure this float against the reference, cache the vector, flag it as in-range" routine for
# the 0x82815 float-distance cluster, called only by ProcessNamedValue_82815CB0. Part of the 0x82815
# float-distance cluster (sibling of ProcessNamedValue_82815CB0 / ProcessValueRange_82828E58). 229 lines, 0
# direct callees (pure float + vector math). 1 caller: ProcessNamedValue_82815CB0.
[entrypoint.functions."0x82815F28"]
name = "ProcessFloatDistance_82815F28"

# Validates and processes a linked structure, trapping on an invalid pointer. It takes an object (a) and a
# linked node (c). It sets up a working frame on the stack, then walks the linked node (through c->4):
# when a node's pointer is null or self-referential (r9 == 0 or r9 == c) it raises a trap (twi 31, r0, 22),
# and when a node's payload (r11) does not match the expected payload (r7) it keeps advancing the structure.
# This is the "walk this linked node, sanity-check each pointer, trap on corruption" routine for the 0x82A0F
# linked-structure cluster, called only by DisposeSubObj_82A0F800. Part of the 0x82A0F cluster (sibling of
# DisposeSubObj_82A0F800 / CompareObjects_82A0F120). 189 lines, 0 direct callees (pure pointer walk). 1
# caller: DisposeSubObj_82A0F800.
[entrypoint.functions."0x82A0FCA8"]
name = "ProcessLinkedStructure_82A0FCA8"

# Returns a fixed .data constant pointer (a getter). It takes no arguments. It loads a .data base constant
# and adds a fixed offset (+19924) to compute the address of a specific .data object, then returns it as the
# function's result. This is the "hand out this well-known .data constant address" getter for the 0x82C39
# constant-pointer cluster, reached only via a vtable / function-pointer table (no direct callers in the
# recompiled code). Part of the 0x82C39 cluster (sibling of CompactClearArray_82C39298). 10 lines, 0 direct
# callees (pure .data pointer arithmetic). No direct callers found in the recompiled code (likely reached
# via a vtable / function-pointer table).
[entrypoint.functions."0x82C39178"]
name = "GetConstantPointer_82C39178"

# Looks up a key in a hash table and, when it is absent, sets a flag bit on the entry. It takes an entry (a),
# a bit index (b), and a lookup key (c). It runs the lookup (HashTable_Find3_821CF1D0(a, b, c)); when the
# entry's found-marker (a->4) is zero (the key was not present) it raises the indexed flag bit:
# a->6 = a->6 | (1 << b). This is the "look this key up and mark it seen (set bit b)" routine for the 0x82BCD
# flag-cluster, used by 5 callers (sub_82BCC108 / sub_82BCB6E0 / ProcessDispatchLoop_8229A518 /
# StateDispatch2_82BCAA58 / DispatchValueProcess_821CF320). Part of the 0x82BCD cluster (sibling of
# GrowCounterIfNeeded_82BCD9B8). 53 lines, 1 callee. 5 callers.
[entrypoint.functions."0x82BCD5E0"]
name = "FindAndSetFlagBit_82BCD5E0"

# Atomically decrements a refcount field located just before a struct. It takes a pointer (a) that points to
# the start of a payload; it computes a - 4 (the refcount sits at offset -4, immediately ahead of the
# payload) and atomically decrements that count (AtomicDecrement_822C72E0(a - 4)). This is the "release one
# reference on this payload (via its leading refcount)" step for the 0x82B4F refcount cluster, reached only
# via a vtable / function-pointer table (no direct callers in the recompiled code). Part of the 0x82B4F
# refcount cluster (sibling of DecrementRefAndProcess_82375980). 7 lines, 1 callee
# (AtomicDecrement_822C72E0). No direct callers found in the recompiled code (likely reached via a vtable /
# function-pointer table).
[entrypoint.functions."0x82B4F370"]
name = "DecrementRefAtOffset_82B4F370"

# Copies a run of 8-byte refcounted elements from a source to a destination. It takes a destination base (a),
# a destination pointer (b), a count (c), and a source pointer (d). When c is zero it is a no-op. Otherwise it
# loops c times: for each element it copies the source's head word (d->0 -> b->0) and drives the refcounted
# copy-assign of the element's body through CopyAssign_RefCounted_821F0108(b + 4, d + 4), advancing both
# pointers by 8 bytes each iteration. After the loop it computes the end of the written region
# (dest_base + c * 8) to return. This is the "refcounted deep-copy this array of 8-byte elements" routine for
# the 0x82343 array cluster, used by 4 callers (sub_82343F28 / sub_8243B2A0 / sub_82B5F910 / sub_82AA3760).
# Part of the 0x82343 array cluster (sibling of CopyAssign_RefCounted_821F0108). 63 lines, 1 callee. 4
# callers.
[entrypoint.functions."0x82343C08"]
name = "CopyArrayRefcounted_82343C08"

# Processes a refcounted buffer: atomically bumps a global counter, then grows or shrinks the buffer to a
# requested size, and finally cleans up a temporary. It takes an object (a) carrying a start offset (a->4) and
# an end offset (a->8), plus a requested size (b). First it atomically increments a .data global counter
# (an lwarx/stwcx. loop guarded by the machine-state lock, mfmsr/mtmsrd). It then measures the buffer's
# current element count ((a->8 - a->4) / 8) and compares it to the requested size b: when b is larger it
# extends the buffer (sub_82B5F910), and when b is smaller it compacts it (sub_82B60740 + sub_8243B230,
# updating a->8 to the new end). Finally it destructs a temporary refcounted value on the stack
# (DestructRefCounted_82214F08). This is the "bump the global counter and resize this refcounted buffer to b"
# routine for the 0x82B5E buffer cluster, used by 4 callers (sub_82B5C8B0 / sub_82B714A0 / sub_82B725A8 /
# sub_82B80D70). Part of the 0x82B5E buffer cluster (sibling of GrowCounterIfNeeded_82BCD9B8). 134 lines, 4
# callees. 4 callers.
[entrypoint.functions."0x82B5E0A0"]
name = "ProcessRefCountedBuffer_82B5E0A0"

# Grows (extends) a refcounted buffer to a requested size, copying the elements into the new storage. It takes
# an object (a) carrying the buffer's start (a->4), end (a->8), and capacity (a->12), a requested count (c),
# and a copy source (d). It copies the buffer's refcounted header (CopyAssign_RefCounted_821F0108), computes
# the current element count ((a->12 - a->4) / 8 when a->4 is set), and - when a new count (c) is requested -
# allocates the larger backing storage (AllocateChecked4_82A87220), deep-copies the existing elements across
# (CopyArrayRefcounted_82343C08 / CopyAssign_RefCounted2_82265160 / sub_8243B230 / sub_82A97648), then frees the
# old storage (Free_SizeBucketed_Thunk_8221BE68) and cleans up temporaries (DestructRefCounted_82214F08). This
# is the "extend this refcounted buffer, carrying its elements over" routine for the 0x82B5F buffer cluster,
# used by 2 callers (sub_82B6D718 / ProcessRefCountedBuffer_82B5E0A0). Part of the 0x82B5F buffer cluster
# (sibling of ProcessRefCountedBuffer_82B5E0A0). 464 lines, 8 callees. 2 callers.
[entrypoint.functions."0x82B5F910"]
name = "GrowRefCountedBuffer_82B5F910"

# Copies a run of 28-byte float-structures (four ints + three floats) from a source to a destination. It
# takes a destination base (a), a destination offset pointer (b), a count (c), and a source pointer (d). When
# c is zero it is a no-op; otherwise it walks the source, copying each 28-byte record (d->0, d->4, d->8, d->12
# as ints and d->16, d->20, d->24 as floats) into the destination at (b + 20), advancing 28 bytes per element
# until the count is reached. This is the "memcpy this run of 28-byte float records" routine for the 0x82A14
# float-struct cluster, called only by sub_82A142D0. Part of the 0x82A14 float-struct cluster (sibling of
# ProcessFloatDistance_82815F28). 73 lines, 0 direct callees (pure element copy). 1 caller: sub_82A142D0.
[entrypoint.functions."0x82A14130"]
name = "CopyFloatStructs_82A14130"

# Constructs (or inserts into) a container of 28-byte float-structures, allocating and growing as needed. It
# takes an object (a), a payload value (b), a key (c), and a 28-byte source record (d: four ints + three
# floats). It stages the source record on the stack (r1 + 80, the 28-byte element), checks the container's
# current size (a->4), and drives the construction: it builds / grows the element array (ConstructElementArray_
# 829FED20), copies the 28-byte records into place (Copy28ByteStruct_82A141A0 / sub_82A14130 =
# CopyFloatStructs), handles overflow via sub_82A97648, and releases scratch storage
# (Free_SizeBucketed_Thunk_8221BE68). This is the "build this 28-byte float-struct container, growing the
# backing array as it fills" constructor for the 0x82A14 container cluster, used by 2 callers (sub_8323FA78 /
# sub_8323FCA8). Part of the 0x82A14 container cluster (sibling of CopyFloatStructs_82A14130 /
# ConstructElementArray_829FED20). 381 lines, 5 callees. 2 callers.
[entrypoint.functions."0x82A142D0"]
name = "ConstructFloatStructContainer_82A142D0"

# Constructs a 24-byte object with a vtable, storing the pointer into a slot on the caller. It takes an
# object (a). It allocates a 24-byte block (Allocate_OrDbgBreak_8221F388(24)); on success it seeds the block
# with a .data vtable pointer (blk->0 = a .data constant) and zeroes the remaining fields (blk->4, blk->12,
# blk->16, blk->20 = 0). It records the block pointer in the caller's slot (a->0 = blk). When the allocation
# fails it stores a null. This is the "allocate and initialize this 24-byte vtable object" constructor for the
# 0x82375 object cluster, used by 6 callers (sub_82B3E370 / sub_82B41100 / sub_82B4B0E0 / sub_82375838 /
# sub_82C67C70 / sub_82C68B50). Part of the 0x82375 object cluster (sibling of ConstructAllocObject_8236F070).
# 76 lines, 1 callee (Allocate_OrDbgBreak_8221F388). 6 callers.
[entrypoint.functions."0x82375020"]
name = "Construct24ByteObject_82375020"

# Destructs a container object, clearing its element array and freeing the object when flagged. It takes an
# object (a) and a flags word (b). It reinstalls a .data vtable pointer on the object (a->0 = a .data
# constant), then tears down the element array embedded at a + 8 (DestructElementArray_82376298). It inspects
# the low bit of the flags word (b & 0x1): when that bit is set it releases the object itself
# (Free_SizeBucketed_Thunk_8221BE68(a)), and when the bit is clear it leaves the object for the caller to free.
# This is the "dispose this container (clear its array, free the shell if we own it)" destructor for the
# 0x82375 container cluster, reached only via a vtable / function-pointer table (no direct callers in the
# recompiled code). Part of the 0x82375 container cluster (sibling of Construct24ByteObject_82375020 /
# DestructElementArray_82376298). 58 lines, 2 callees. No direct callers found in the recompiled code (likely
# reached via a vtable / function-pointer table).
[entrypoint.functions."0x823750A0"]
name = "DestructContainer_823750A0"

# A large field/offset/bit processor that reads many fields of two structures and computes derived offsets and
# bit flags. It takes an object (a) and a value (b). It reads a->28 (a sub-object), then a batch of fields from
# a (a->0, a->4, a->12, a->16) and from the sub-object (->40, ->44, ->48, ->52, ->56, ->60, ->84, ->88),
# computing a set of derived quantities: element-index offsets (index - 1, index + base, index - 5, value - b
# plus an index) and two bit flags (1 << sub->84 and 1 << sub->88). It then drives a large per-field processing
# body over these computed offsets and flags. This is the "read this pair of structures, derive their offsets
# and flags, and process each field" routine for the 0x82B59 field cluster, called only by sub_82B53A40. Part
# of the 0x82B59 field cluster (sibling of ProcessField_82B51380 / ProcessNodeDispatch_82CB7DA0). 642 lines, 0
# direct callees (pure field read + offset/bit math). 1 caller: sub_82B53A40.
[entrypoint.functions."0x82B59D28"]
name = "ProcessStructFields_82B59D28"

# A large object-field processor: dispatches a vcall on the object, then walks its fields, copying values and
# computing offsets. It takes an object (a) and a source struct (b: ->0, ->4, ->12, ->20). It first dispatches a
# virtual call through the source (b->0->8). When the source carries at least 4 elements (b->20 >= 4) it
# advances into the element area (b->12 + 4, b->4 + 4, offset b->20 - 4) and processes each field: copying the
# element header byte (b->12->0) onto a stack slot and driving the per-field body. It uses
# CopyStructData4_82A1BEA8, sub_827C55C8, and sub_82A1B480 for the field-level work. This is the "dispatch the
# handler vcall, then copy/process this source struct's fields into the object" routine for the 0x83236
# object-field cluster, reached only via a vtable / function-pointer table (no direct callers in the recompiled
# code). Part of the 0x83236 cluster (sibling of ProcessListTail_8322FF90 / CopyStructData4_82A1BEA8). 764
# lines, 3 callees. No direct callers found in the recompiled code (likely reached via a vtable / function-
# pointer table).
[entrypoint.functions."0x83236C28"]
name = "ProcessObjectFields_83236C28"

# Atomically decrements a refcount and, when it reaches zero, destroys the object. It takes an object (a) whose
# refcount sits at a + 4. It loops an lwarx/stwcx. compare-and-swap (guarded by the machine-state lock,
# mfmsr/mtmsrd) decrementing a + 4 until the store succeeds, capturing the new count. When the new count is
# non-zero it returns (the object is still referenced). When the count reaches zero it reads the object's head
# (a->0) and drives the destruction of the now-last-referenced object. This is the "release one reference; when
# it is the last, dispose the object" routine for the 0x8237A refcount cluster, reached only via a vtable /
# function-pointer table (no direct callers in the recompiled code). Part of the 0x8237A refcount cluster
# (sibling of DecrementRefAndProcess_82375980). 77 lines, 0 direct callees (pure atomic refcount + destroy
# path). No direct callers found in the recompiled code (likely reached via a vtable / function-pointer table).
[entrypoint.functions."0x8237AF30"]
name = "DecrementRefAndDestroy_8237AF30"

# Computes a FNV-style hash over the non-NUL prefix of a string. It takes a string pointer (a). It first scans
# forward to the first zero byte to find the hashable prefix length, and bails early if the prefix is empty. It
# then folds the prefix into a running hash: starting from a .data seed, for each byte it multiplies by the
# 0x01000193 (16777619) FNV prime and XORs in the byte (hash = (hash * prime) ^ byte), iterating over the
# prefix. The resulting hash is returned in r3. This is the "hash this string's bytes with an FNV-style
# multiply-xor fold" routine for the 0x82C81 hash cluster, called only by sub_82C822D8. Part of the 0x82C81
# hash cluster (sibling of ComputeStringHash_82227EA0 / StringCompare_82CA3920). 46 lines, 0 direct callees
# (pure hash math). 1 caller: sub_82C822D8.
[entrypoint.functions."0x82C81DF8"]
name = "ComputeFNVHash_82C81DF8"

# Looks up (or inserts) a string key in a power-of-two hash table. It takes a table (a: size at a->4, bucket
# array at a->8) and a key string (b). It hashes the key (ComputeFNVHash_82C81DF8), reduces the hash to a
# bucket index (hash & (size - 1)) and scales it to a 4-byte stride (index << 2). It walks the bucket's chain:
# loading each entry (a->8[index]), string-matching it against the key (MatchStringWrapper_82CAAF08), and
# continuing the chain on a miss. On a full miss it drives the insert / allocation path (sub_82C82220 /
# sub_82C87A50) and copies the key in (MemCpy_SdkRuntime_82CA2C60). This is the "find this string key in the
# hash table (hashing, bucket lookup, chain walk, insert-on-miss)" routine for the 0x82C82 hash-table cluster,
# called only by sub_82C823D8. Part of the 0x82C82 hash-table cluster (sibling of ComputeFNVHash_82C81DF8 /
# InsertHashEntry_8219CDA8). 148 lines, 5 callees. 1 caller: sub_82C823D8.
[entrypoint.functions."0x82C822D8"]
name = "HashTableLookup_82C822D8"

# A locked processor that reads a global structure's last element, then drives a hash-table lookup on it. It
# takes a context (a) and a value (b). It acquires a .data critical section (RtlEnterCriticalSection on a
# .data lock), then reads a .data global structure (at .data + 22620): it inspects the element count
# (glob->8) and, when non-zero, takes the last element (index count - 1, 4-byte stride) and reads its fields
# (entry->4, entry->12), deriving a size via (entry->4 & ~1) / 3. It then drives the element's processing
# through a hash-table lookup (HashTableLookup_82C822D8) plus the supporting helpers (sub_82C81D90 /
# sub_82C820B0 / sub_82C87A50), and releases the critical section before returning. This is the "lock the
# global table, read its newest entry, and process it (hash lookup / insert)" routine for the 0x82C82
# global-struct cluster, used by 10 callers (the 0x82361/0x82931/0x82A16/0x82BF7/0x82C82/0x82C86/0x82C87
# range). Part of the 0x82C82 global-struct cluster (sibling of HashTableLookup_82C822D8). 149 lines, 4
# callees. 10 callers.
[entrypoint.functions."0x82C823D8"]
name = "ProcessGlobalStruct_82C823D8"

# A locked container processor that takes a critical section, then clamps/constructs/destructs elements under
# the lock. It takes an object (a), a count (b), a key (c), and two additional values (d, e). It acquires the
# object's critical section (CriticalSection_EnterOrTry_82200688 on a + 8, with a stack lock slot at r1 + 80).
# When the count (b) is zero it skips straight to the error path (leaving the lock if held and returning an
# error status); otherwise it processes the elements under the lock - clamping values
# (ClampAndUpdate_822F6380), constructing entries (Construct10_82B42918), and destructing freed entries
# (Destruct5_82B414F8) - with per-element work through sub_82B41738 / sub_82B422A8. This is the "lock the
# container, build / clamp / dispose its elements, unlock" routine for the 0x82B41 container cluster, called
# only by sub_82C65428. Part of the 0x82B41 container cluster (sibling of Destruct5_82B414F8 /
# Construct10_82B42918). 327 lines, 6 callees. 1 caller: sub_82C65428.
[entrypoint.functions."0x82B412B8"]
name = "LockedProcessContainer_82B412B8"

# A locked sorted-array processor that takes a critical section, then searches and inserts elements in a
# 12-stride sorted array. It takes an object (a: a state flag at a->68), a count (b), a key (c), and two
# values (d, e). It acquires the object's critical section (CriticalSection_EnterOrTry_82200688 on a + 8,
# with a stack lock slot at r1 + 144). When the state flag (a->68) is clear it drives the build path
# (sub_82B41C90), then processes the elements under the lock: searching the sorted array
# (BinarySearch_12Stride_Simple_82B429A0), inserting new elements (Array_InsertOne_8236FDD8), dividing-and-
# conquering the range (ProcessDivideConquer_82B42A80), lazy-loading associated data (LazyGetData_822F2088) and
# strings (LoadString_FastOrSlow_823725E8), and releasing freed entries (Free_SizeBucketed_Thunk_8221BE68) via
# the per-element helpers sub_82B420E0 / sub_82B423F0 / sub_82B42CD8. It leaves the lock before returning.
# This is the "lock the array, binary-search and insert the sorted elements, unlock" routine for the 0x82B41
# sorted-array cluster, used by 2 callers (sub_82B41C90 / LockedProcessContainer_82B412B8). Part of the 0x82B41
# cluster (sibling of LockedProcessContainer_82B412B8 / BinarySearch_12Stride_Simple_82B429A0). 764 lines, 11
# callees. 2 callers.
[entrypoint.functions."0x82B41738"]
name = "LockedSortedInsert_82B41738"

# Grows (ensures capacity of) a 12-byte-per-element buffer. It takes an object (a: start at a->4, end at a->12)
# and a desired element count (b). When the desired count (b) exceeds a large .data limit it routes the request
# through sub_82684B38 (an error / fallback path) and returns. Otherwise it computes the current element count
# as (a->12 - a->4) / 12 (the 12-byte stride). When the current count is already at least b it is a no-op;
# when more capacity is needed it allocates a larger backing block (AllocateChecked_8264EFE0), carries the
# existing elements across, and frees the old storage (Free_SizeBucketed_Thunk_8221BE68). This is the "make
# sure this element buffer can hold b records, growing it if short" routine for the 0x82B42 buffer cluster,
# called only by LockedSortedInsert_82B41738. Part of the 0x82B42 buffer cluster (sibling of
# LockedSortedInsert_82B41738 / ProcessRefCountedBuffer_82B5E0A0). 167 lines, 3 callees. 1 caller:
# LockedSortedInsert_82B41738.
[entrypoint.functions."0x82B420E0"]
name = "GrowElementBuffer_82B420E0"

# Constructs a struct whose float fields are seeded from .data constants (with a vector-unit setup). It takes a
# destination (a). It stages a vector of ones (vspltisw 1 / vcfux -> 1.0f) and reads a set of .data float
# constants, then seeds the destination's float fields from them (a->0, a->20, a->24, a->28 = the staged
# constants, plus a->5 as a small int). This is the "build this float-structured record, initializing its
# components from well-known .data constants" constructor for the 0x83211 float-struct cluster, called only by
# ProcessDispatchValue2_832351B0. Part of the 0x83211 float-struct cluster (sibling of ConstructFloatString_
# 83211790 / CopyFloatStructs_82A14130). 138 lines, 0 direct callees (pure float/vector constant seeding). 1
# caller: ProcessDispatchValue2_832351B0.
[entrypoint.functions."0x83211900"]
name = "ConstructFloatStruct_83211900"

# A one-time initializer that, on first call, builds a tree / chain data structure guarded by a .data flag. It
# takes an object (a), a value (b), a count (c), a pointer (d), and a float (e, f1). It reads a .data
# initialized-flag (at .data + 13744) and inspects its low bit; when already set it skips straight to the
# "already built" path. On the first call it raises the flag (.data + 13744 |= 1) and runs the build: it seeds
# a .data base (sub_823D2790) and assembles the structure - finding tree nodes (Tree_FindNode2_82A12230),
# walking chain roots (FindChainRoot_82A596F0), copying elements (ProcessCopyElement_82A12D68), releasing
# refcounted temporaries (Release_RefCounted_829FF648), and driving the per-node assembly through sub_825BF630 /
# sub_82A12C70 / sub_82CA3700. This is the "lazily build this tree/chain once, then fast-path afterward"
# one-time initializer for the 0x83233 init cluster, called only by sub_82A134B8. Part of the 0x83233 init
# cluster (sibling of InitializeSubsystems_83232BB8). Large function, 8 callees. 1 caller: sub_82A134B8.
[entrypoint.functions."0x83233B40"]
name = "OneTimeInitTree_83233B40"

# Returns one of two .data constant pointers, selected by a small index. It takes an unused context (a) and a
# selector (b). When b == 1 it returns the first .data constant (.data base + 19796); for any other value (b ==
# 0 or b > 1) it returns the second .data constant (.data base + 19800). This is the "pick one of these two
# well-known .data object addresses by index" selector for the 0x82C23 constant cluster, reached only via a
# vtable / function-pointer table (no direct callers in the recompiled code). Part of the 0x82C23 constant
# cluster (sibling of GetConstantPointer_82C39178). 24 lines, 0 direct callees (pure .data pointer select). No
# direct callers found in the recompiled code (likely reached via a vtable / function-pointer table).
[entrypoint.functions."0x82C23858"]
name = "SelectConstantPointer_82C23858"

# Stores a value into an object, dispatches on a byte flag, and records the result flag. It takes an object
# (a), a value (b), and a flags word (c). It records the value (a->0 = b), then inspects the low byte of the
# flags (c & 0xFF): when that byte is non-zero it drives one handler (sub_82B4F390(b)), and when it is zero it
# drives the other (sub_82217E38(b)). It then examines the low byte of the processed value (b & 0xFF) to set a
# result flag on the object (a->4 = 0 when that byte is zero, a->4 = 1 otherwise). This is the "store this
# value, run the handler selected by the flag byte, and stamp the success byte" routine for the 0x82217
# dispatch cluster, used by 2 callers (sub_82B456C8 / sub_82B458C0). Part of the 0x82217 dispatch cluster
# (sibling of ProcessAndDispatch_82366210). 64 lines, 2 callees. 2 callers.
[entrypoint.functions."0x82217DD0"]
name = "ProcessAndDispatchByte_82217DD0"

# A global-counter processor: checks a global field, increments a counter when the value matches, and handles
# the empty case atomically. It takes an object (a: a value at a->0, a counter at a->4, a bound at a->8). When
# a->0 is non-null it resolves the global field (GetGlobalField_8221EE38) and - when the bound (a->8) equals
# the value (a->0) - increments the counter (a->4 + 1). When a->0 is null it enters the machine-state lock
# (mfmsr/mtmsrd) and performs an atomic lwarx/stwcx. update on the slot, retrying on contention. This is the
# "check this global field, bump the counter on a match, and atomically fill the empty slot" routine for the
# 0x82217 counter cluster, called only by ProcessAndDispatchByte_82217DD0. Part of the 0x82217 counter cluster
# (sibling of ProcessAndDispatchByte_82217DD0 / GetGlobalField_8221EE38). 112 lines, 1 callee. 1 caller:
# ProcessAndDispatchByte_82217DD0.
[entrypoint.functions."0x82217E38"]
name = "ProcessGlobalCounter_82217E38"

# Validates a global object and decrements its counter, trapping on corruption. It takes an object (a: a
# value at a->0, a present flag at a->4). When the flag (a->4) is clear it skips the work. Otherwise it
# resolves the global field (GetGlobalField_8221EE38) and validates the object: it traps (twi 31, r0, 22) when
# the object's bound (obj->8) does not equal the value (a->0), and again when the object's head (obj->0) is
# null. On a valid object it decrements the counter (obj->4 - 1) and, when the counter underflows, drives the
# cleanup path. This is the "check this global object, sanity-check it (trap on corruption), and release one
# reference (decrement)" routine for the 0x82222 global-validation cluster, used by 2 callers (sub_82B456C8 /
# sub_82B458C0). Part of the 0x82222 cluster (sibling of ProcessGlobalCounter_82217E38 / GetGlobalField_
# 8221EE38). 103 lines, 1 callee. 2 callers.
[entrypoint.functions."0x82222D68"]
name = "DecrementAndValidate_82222D68"

# A process that checks a state flag, dispatches a handler vcall, and signals an event on the outcome. It
# takes an object (a: a state flag at a->152) and a value (b: ->0). When the state flag (a->152) is already set
# it short-circuits; otherwise it resolves an offset (a - 20, falling back to null when a is below the object
# header) and dispatches a handler virtual call through the value (b->0->32). It checks the handler's result:
# when it is non-negative it continues the process (sub_82B456C8) and, depending on the outcome, signals a
# synchronization event (SetEvent_SdkRuntime_822C9380); when the handler fails (a negative result) it skips the
# event. This is the "check this state flag, run the handler vcall, and signal the event on success" routine
# for the 0x82B45 process cluster, reached only via a vtable / function-pointer table (no direct callers in the
# recompiled code). Part of the 0x82B45 process cluster (sibling of sub_82B456C8 / SetEvent_SdkRuntime_
# 822C9380). 116 lines, 2 callees. No direct callers found in the recompiled code (likely reached via a vtable
# / function-pointer table).
[entrypoint.functions."0x82B45358"]
name = "ProcessAndSetEvent_82B45358"

# Constructs a 16-byte node and atomically inserts it into a linked list, with a byte-flag dispatch. It takes an
# object (a: a list head at a + 16, a flag slot at a + 20) and a value (b). It first drives the byte-flag
# dispatch (sub_82217DD0 = ProcessAndDispatchByte, using a + 20). It allocates a 16-byte node
# (Allocate_OrDbgBreak_8221F388(16)), seeds it (node->0 = b, node->8 = 0, node->12 = 0), and - with an lwsync
# barrier - performs an atomic lwarx/stwcx. loop (guarded by the machine-state lock, mfmsr/mtmsrd) to spin the
# new node in front of the list at a + 16. On the path it releases the prior holder (sub_82222D68 =
# DecrementAndValidate). This is the "dispatch the flag, build a 16-byte node, and atomically push it onto the
# list" routine for the 0x82B45 node-insert cluster, used by 6 callers (sub_82B44400 / sub_82B45358 /
# sub_82B454C0 / sub_82C45128 / sub_82C491B8 / sub_82C49388). Part of the 0x82B45 cluster (sibling of
# ProcessAndSetEvent_82B45358). 334 lines, 3 callees. 6 callers.
[entrypoint.functions."0x82B456C8"]
name = "ConstructAndInsertNode_82B456C8"

# A processor that lazy-loads two values, and when they differ, builds and pushes an element onto an array. It
# takes an object (a: a field at a + 12), a first source (b), and a second source (c). It resolves both
# sources' data lazily (LazyGetData_822F2088(b) and LazyGetData_822F2088(c)) and compares the two results. When
# they are equal it bails out (no change). When they differ it drives the update: it prepares a new element
# (sub_82375AB8), allocates its backing block (Allocate_OrDbgBreak_8221F388), finds the tree leaf for the
# object (FindTreeLeaf_825CB448), loads from the source (LoadFrom_Source_822F2398), and pushes the element onto
# the object's array (Array_PushElement_823758C0), cleaning up a temporary (Destruct3_822F5208). This is the
# "compare the two sources, and on a change, construct and push the updated element" routine for the 0x82375
# array-push cluster, called only by sub_823728E0. Part of the 0x82375 array-push cluster (sibling of
# Array_PushElement_823758C0 / DestructElementArray_82376298). 218 lines, 7 callees. 1 caller: sub_823728E0.
[entrypoint.functions."0x82375270"]
name = "ProcessAndPushIfChanged_82375270"

# Constructs a 24-byte object and pushes it onto the owning array. It takes an array owner (a: a header at
# a->0) and a destination slot (b). It first constructs the 24-byte object in the slot (Construct24ByteObject_
# 82375020(b)), then reads the array's header (a->0) and pushes the new object's payload (at b + 8) onto the
# array (Array_PushElement_823758C0(a->0 + 8, b + 8)). This is the "build this 24-byte element and append it to
# the array" routine for the 0x82375 construct/push cluster, reached only via a vtable / function-pointer table
# (no direct callers in the recompiled code). Part of the 0x82375 construct/push cluster (sibling of
# Construct24ByteObject_82375020 / Array_PushElement_823758C0). 47 lines, 2 callees. No direct callers found in
# the recompiled code (likely reached via a vtable / function-pointer table).
[entrypoint.functions."0x82375838"]
name = "ConstructAndPush_82375838"

# Searches a tree / list structure for a value's range, walking the nodes to locate the matching branch. It
# takes an object (a: a pointer at a->4) and a value (b: a bound at b->0). It walks the structure (through
# a->4 -> 4), reading each node's flag byte (node->33); when a node's bound (node->12) is at least the search
# value (b->0) it descends (node->0), otherwise it follows the next pointer (node->8), continuing until it
# finds the branch whose flag is set (or exhausts the chain). Once the branch is located it drives the
# follow-up element work - reconstructing the element array (DestructElementArray_82376298) and processing the
# found range (sub_82376358 / sub_823769E8). This is the "find this value's branch in the tree, then rebuild
# its element range" search routine for the 0x82375 tree-search cluster, called only by
# ProcessAndPushIfChanged_82375270. Part of the 0x82375 tree-search cluster (sibling of FindTreeLeaf_825CB448 /
# FindTreeNode_823714A0). 155 lines, 3 callees. 1 caller: ProcessAndPushIfChanged_82375270.
[entrypoint.functions."0x82375AB8"]
name = "SearchTreeRange_82375AB8"

# A processor that resolves a value, then searches a 28-byte element array for a matching entry. It takes an
# index (a), an array object (b: a base at b->12, a length at b->28), and a context (c). It first drives the
# per-value processing (sub_82BC9C50(a)). When the index (a + 1) is within the array length (b->28) it walks
# the element array from a + 1 forward: each element is a 28-byte record (base + (index << 3) + 4), and it
# scans for the first non-zero entry (element->0 != 0), advancing 8 bytes per probe up to the length bound.
# This is the "process this index, then linearly scan its trailing 28-byte element array for a hit" routine
# for the 0x822CD element-array cluster, used by 4 callers (sub_82188508 / sub_82BB9B60 / sub_82BDBDD8 /
# sub_82BDBF78). Part of the 0x822CD element-array cluster (sibling of SearchTreeRange_82375AB8 /
# ProcessAndPushIfChanged_82375270). 186 lines, 1 callee. 4 callers.
[entrypoint.functions."0x822CDF48"]
name = "SearchElementArray_822CDF48"

# A typed-value processor: inspects a value's type tag, converts the value to the requested form, and
# dispatches on the type. It takes a context (a), a value (b), and a value descriptor (c: a payload at c->0,
# a type tag at c->4). When the type tag (c->4) is zero it returns -1 (a failure). When the tag selects the
# float form (c->4 == 3) it reads the float payload (c->0) and performs a float -> int -> float round-trip
# (fctiwz, sign-extend, fcfid) to normalize it. It then drives the type-specific processing through the
# switch dispatchers (SwitchDispatch_82BC8260 / SwitchDispatch_82BC9B28) and a per-type handler
# (sub_82BC9860). This is the "inspect this value's type, convert it to the requested form, and dispatch the
# per-type handler" routine for the 0x82BC9 typed-value cluster, called only by SearchElementArray_822CDF48.
# Part of the 0x82BC9 typed-value cluster (sibling of SwitchDispatch_82BC8260 / SwitchDispatch_82BC9B28). 165
# lines, 3 callees. 1 caller: SearchElementArray_822CDF48.
[entrypoint.functions."0x82BC9C50"]
name = "ProcessTypedValue_82BC9C50"

# A processor that atomically increments a global counter, then processes a value (allocating, releasing, and
# handling it). It takes an object (a: a result slot at a->0) and a value (b: a type/tag at b->0). It first
# stores 0 into the result slot (a->0 = 0), then performs an atomic lwarx/stwcx. increment on a .data global
# counter (guarded by the machine-state lock, mfmsr/mtmsrd), retrying on contention. When the value (b) is
# non-null it reads its type word (b->0, a halfword) and drives the per-value processing: allocating a result
# buffer (Allocate_OrDbgBreak_8221F388), releasing a refcounted reference (Release_RefCounted_821C67D8), and
# handling the value through sub_82B39DD8. This is the "bump the global counter, then build and store the
# processed value" routine for the 0x822D6 counter cluster, used by 10 callers (the 0x822D6/0x82303/0x82431/
# 0x825A0/0x82698/0x826C7/0x8288D/0x828ED/0x83260/0x83261 range). Part of the 0x822D6 counter cluster
# (sibling of ProcessAndIncrement_82CACC58 / GrowCounterIfNeeded_82BCD9B8). 141 lines, 3 callees. 10 callers.
[entrypoint.functions."0x822D6B40"]
name = "ProcessAndIncrementCounter_822D6B40"

# A processor that inserts (or removes) an element from a 16-byte-per-element array based on an index. It
# takes an array object (a: a start at a->4, an end at a->8), an index (b), and a value (c). It computes the
# current element count as (a->8 - a->4) / 16 (the 16-byte stride). When the index (b) is beyond the current
# count (an append) it grows / inserts the trailing element (InsertElement16_82A8BFE8). When the index (b) is
# within the array (an interior edit) it locates the element (base + (b << 4)) and updates it in place.
# This is the "insert / update this 16-byte element at index b, growing the array on append" routine for the
# 0x827C5 element-array cluster, used by 10 callers (the 0x82549/0x827C4/0x827F2/0x82884/0x82930/0x83228/
# 0x83236/0x83237 range, incl. ProcessObjectFields_83236C28). Part of the 0x827C5 element-array cluster
# (sibling of InsertElement16_82A8BFE8 / ProcessObjectFields_83236C28). 102 lines, 1 callee. 10 callers.
[entrypoint.functions."0x827C55C8"]
name = "ProcessElementArray_827C55C8"

# Constructs a struct (seeding fields from .data constants) and appends a 16-byte element to it. It takes a
# destination (a). It seeds the struct's head with a .data pointer (a->0 = .data base + 7028) and zeroes the
# following fields (a->8, a->12, a->16 = 0), then caches a working vector on the stack (stvx128 through r1 - 48
# / r1 + 80). It reads a .data float constant and seeds a->24 with it (a->24 = the .data float). It then
# appends a 16-byte element to the struct (AppendElement16_82442B68). This is the "build this struct from the
# .data constants and append its first 16-byte element" constructor for the 0x83236 construct cluster, called
# only by sub_8323F1B8. Part of the 0x83236 construct cluster (sibling of ProcessObjectFields_83236C28 /
# AppendElement16_82442B68). 107 lines, 1 callee. 1 caller: sub_8323F1B8.
[entrypoint.functions."0x832369C0"]
name = "ConstructAndAppend_832369C0"

# Constructs a vector-bearing struct: builds a 32-byte vector, seeds the struct's fields, and appends it. It
# takes a destination (a), and two vector arguments (b, c) passed in FP/vector registers (v1, v2). It stages
# the vectors on the stack (stvx128 through r1 - 48 / r1 - 32), builds a 32-byte vector from them
# (Construct32ByteVector_8321FB58), and restores the vector registers. It seeds the struct's head with a .data
# pointer (a->0 = .data base + 6288) and clears a flag byte (a->192 = 0). It then drives the struct
# construction and the first 16-byte element append through sub_832369C0 (= ConstructAndAppend). This is the
# "build this 32-byte vector, seed the struct header, and append the first element" constructor for the
# 0x8323F vector-struct cluster, called only by sub_8323F238. Part of the 0x8323F vector-struct cluster
# (sibling of ConstructAndAppend_832369C0 / Construct32ByteVector_8321FB58). 75 lines, 2 callees. 1 caller:
# sub_8323F238.
[entrypoint.functions."0x8323F1B8"]
name = "ConstructVectorStruct_8323F1B8"

# A processor that walks a chain to resolve an object, then either releases the existing refcount or
# allocates and constructs a fresh 240-byte object. It takes an object (a: fields at a->0, a->4) and a value
# (b). It resolves the target by walking a .data chain (WalkChainFind_83239FA8 on a .data base - 7288). When
# the walk's low byte is non-zero (the object was found) it clears the object's payload fields (a->0 = 0,
# a->4 = 0) and releases the existing refcount (Release_RefCounted_829FF648). When the walk finds nothing it
# allocates a fresh 240-byte object (Allocate_OrDbgBreak_8221F388(240)) and constructs its contents - seeding
# the refcount (ProcessRefCount_83225538 / SetRefcountedWrapper_8238A848) and building the vector payload
# (sub_8323F1B8 = ConstructVectorStruct). This is the "resolve this object via the chain, and swap in a fresh
# 240-byte one when it is not already present" routine for the 0x8323F realloc cluster, called only by
# StateDispatch4_83220888. Part of the 0x8323F realloc cluster (sibling of ConstructVectorStruct_8323F1B8 /
# WalkChainFind_83239FA8). 181 lines, 6 callees. 1 caller: StateDispatch4_83220888.
[entrypoint.functions."0x8323F238"]
name = "ProcessAndReallocObject_8323F238"

# A processor that updates a structure's fields based on a count, then processes its float data and grows the
# buffer. It takes a context (a), a structure (b: a head index at b->4, an element pointer at b->12, a count
# at b->20), a value (c), and another value (d). When the count (b->20) is at least 1 it steps the structure
# forward - incrementing the element pointer (b->12 + 1), decrementing the count (b->20 - 1), and bumping the
# head index (b->4 + 1) - and copies the current element's leading byte (b->12->0) onto a stack slot. When the
# count is exhausted it initializes the slot with a default byte. It then drives the per-element float-data
# processing (ProcessFloatData_832206E8), growing / appending as needed (ProcessGrowAppend3_8321FC38) with
# supporting work (sub_82A1B480). This is the "step through this structure's elements, process each float
# record, and grow the buffer" routine for the 0x8323F float-struct cluster, reached only via a vtable /
# function-pointer table (no direct callers in the recompiled code). Part of the 0x8323F float-struct cluster
# (sibling of ProcessFloatData_832206E8 / ProcessAndReallocObject_8323F238). 122 lines, 3 callees. No direct
# callers found in the recompiled code (likely reached via a vtable / function-pointer table).
[entrypoint.functions."0x8323F5B0"]
name = "ProcessGrowFloatStruct_8323F5B0"

# Searches a linked list of ranges for the range that contains a value, and frees the rest of the list. It
# takes a container (a: a base at a->4, a range-chain head at a->12) and a value (b). It walks the chain of
# range records (each record: a start at ->0, a length at ->8, a next at ->12): for each record it tests
# whether b falls within the range (b >= start and b < start + length), advancing through the linked records
# (->12) until one matches or the chain is exhausted. When a range matches it processes it (reading its
# start/length); when no range contains b it walks the remaining records and frees them (VirtualFree_82CC0CF0).
# This is the "find the range in this linked chain that contains b, and discard the rest" routine for the
# 0x8322F range-list cluster, called only by FindNodeInList_83235FC0. Part of the 0x8322F range-list cluster
# (sibling of FindNodeInList_83235FC0 / VirtualFree_82CC0CF0). 113 lines, 1 callee. 1 caller:
# FindNodeInList_83235FC0.
[entrypoint.functions."0x8322FDB0"]
name = "FindRangeInList_8322FDB0"

# Constructs (or processes) an object, dispatching on the supplied arguments. It takes a slot (a: a state at
# a->0), a value (b), and a descriptor (c: a payload at c->0). When the descriptor (c) is null or the payload
# (c->0) is empty it drives the fresh-construction path - calling sub_82CAB770 to prepare, seeding the state
# word (a->0 = 22), and calling sub_82CAB630 with zeroed fields to complete the default construction. When b
# is null it falls back to that same default path. When the object is already populated (a non-zero, with a
# non-empty payload) it instead processes the existing object (sub_82CAB770 / sub_82CAB630 over the live
# fields). This is the "construct this object fresh when it is empty, otherwise process the existing one"
# routine for the 0x82CA6 construct cluster, used by 2 callers (sub_82BF1E58 / sub_82BF2348). Part of the
# 0x82CA6 construct cluster (sibling of sub_82CAB630 / sub_82CAB770). 181 lines, 2 callees. 2 callers.
[entrypoint.functions."0x82CA6890"]
name = "ProcessAndConstructObject_82CA6890"

# A recursive processor that walks a tree of nodes, processing each node up to a bound and recursing into its
# children. It takes a context (a: a header at a->16), a node (b: a child pointer at b->0), and a depth bound
# (c). It reads the header's tag byte (a->16->20) and walks the node's child chain (b->0), stepping through
# each element up to the depth bound (c, decremented each iteration). For each child whose type byte is the
# container marker (b->0->4 == 8) it recurses (sub_82A2D590 over the child's nested node at + 88). It dispatches
# each node's type through DispatchTypeTag_82A2D408. This is the "walk this tree, process each node up to the
# depth bound, and recurse into container children" routine for the 0x82A2D tree cluster, used by 2 callers
# (sub_82A202F8 / sub_82A2DAC0). Part of the 0x82A2D tree cluster (sibling of DispatchTypeTag_82A2D408 / Find
# 0x82A2D family). 119 lines, 1 external callee + 1 recursive self-call. 2 callers.
[entrypoint.functions."0x82A2D590"]
name = "ProcessTreeRecursive_82A2D590"

# Constructs a node: allocates it, sets a flag, and wires it into a self-referential linked structure. It
# takes a container (a: a node slot at a->4, a tail at a->8). It allocates the node (sub_82344E68) and stores
# the pointer into the slot (a->4). It sets a flag byte in the node (node->21 = 1). It then wires the node
# into a self-referential 3-pointer structure - copying the node pointer into three offsets of the node itself
# (node->0, node->4, node->8 all point at the node) - and clears the tail (a->8 = 0). This is the "allocate
# this node, flag it, and link it into the self-referential chain" constructor for the 0x82344 node cluster,
# used by 10 callers (the 0x8233E/0x82364/0x8236D/0x82375/0x823F9/0x82B48/0x82C67/0x82C68 range). Part of the
# 0x82344 node cluster (sibling of sub_82344E68 / CopyArrayRefcounted_82343C08). 35 lines, 1 callee. 10
# callers.
[entrypoint.functions."0x823445B0"]
name = "ConstructLinkedNode_823445B0"

# Allocates and zero-inits a 24-byte node. It calls the generic allocator (Allocate_OrDbgBreak_8221F388(24))
# to obtain a 24-byte block, then clears its three 32-bit pointer / value fields (node->0, node->4, node->8
# all = 0), and seeds two flag bytes (node->20 = 1, node->21 = 0). This is the "allocate a fresh 24-byte node
# with its pointers cleared and its flags initialized" allocator for the 0x82344 node cluster, used by 2
# callers (sub_823445B0 = ConstructLinkedNode and sub_8325A980). Part of the 0x82344 node cluster (sibling of
# ConstructLinkedNode_823445B0 / CopyArrayRefcounted_82343C08). 35 lines, 1 callee. 2 callers.
[entrypoint.functions."0x82344E68"]
name = "AllocateZeroedNode_82344E68"

# A getter that returns the 32-bit field at offset 72 of a structure. It takes a structure (a) and returns
# a->72. It is a trivial accessor - a single 32-bit load from the object, with no side effects. It is reached
# only via a vtable / function-pointer table (no direct callers in the recompiled code), indicating it is a
# virtual "get field 72" accessor for the 0x82B44 structure family (adjacent to the destruct / free-family
# sub_82B44A00 at 0x82B44A00, which frees the object when its low bit is set). Part of the 0x82B44
# accessor / free cluster (sibling of sub_82B44A00). 6 lines, 0 callees. No direct callers found in the
# recompiled code (likely reached via a vtable / function-pointer table).
[entrypoint.functions."0x82B44358"]
name = "GetField_72_82B44358"

# Finds the index at which two null-terminated 16-bit arrays first diverge (or where the shorter one ends).
# It takes two arrays (a, b), each a run of 16-bit halfwords ending in a zero. It walks both in lock-step,
# reading the current 16-bit element of each: when the two elements match it advances to the next element of
# the first array (a + 2) and keeps comparing; when they differ (or the first array is exhausted) it advances
# the second array (b + 2). The inner loops bound each array's scan by its terminating zero. It finally returns
# (position - a) / 2 - the number of matching 16-bit elements, i.e. the length of the common prefix. This is
# the "compare these two 16-bit arrays and return how long their common prefix is" routine for the 0x82CAA
# array cluster, used by 4 callers (the 0x82B3D/0x82B3E/0x82B40 range). Part of the 0x82CAA array cluster
# (sibling of ProcessAndConstructObject_82CA6890 / sub_82CAAAxx family). 40 lines, 0 callees. 4 callers.
[entrypoint.functions."0x82CAAA68"]
name = "FindCommonPrefix16_82CAAA68"

# A processor that dispatches on a type tag and processes the object with type-specific constants. It takes a
# context (a), a tagged object (b: a type tag at b->4, a payload at b + 8), and four additional parameters
# (c, d, e, f). It reads the type tag (b->4): when it is 2 it takes the default path; when it is 1 it first
# processes the payload through sub_82B52500(b + 8) before falling through. In the fall-through path it drives
# the main processing (sub_82B537C0) passing the payload (b + 8), the literal constants 47 and 56, and a .data
# descriptor pointer, then continues with further per-type handling (sub_82B53A40). This is the "dispatch on
# this object's type tag and run the matching processor (with the built-in 47/56 constants)" routine for the
# 0x82B49 type-dispatch cluster, used by 3 callers (sub_82371150 / sub_82C033D8 / sub_82C6FCB8). Part of the
# 0x82B49 type-dispatch cluster (sibling of sub_82B52500 / sub_82B537C0 / sub_82B53A40). 116 lines, 3
# callees. 3 callers.
[entrypoint.functions."0x82B49D50"]
name = "ProcessTypeDispatch_82B49D50"

# A processor that lazily computes a cached flag and stores it back into the structure. It takes a structure
# (a: a sub-object at a->28, a virtual method at a->32, a target at a->40) and a value (b). It checks the
# cached flag slot (a->28->52): when it is already populated it takes the populated path. When it is empty it
# reads the bit index (a->28->36), forms a bit mask (1 << bit index), and performs a virtual call through the
# method pointer (a->32) passing (a->40, 1, the mask) to compute the flag. It stores the computed value back
# into the cache slot (a->28->52). This is the "compute this flag via the virtual method if it is not already
# cached, then store it" routine for the 0x82B53 flag cluster, called only by sub_82B53A40 (a sibling of
# ProcessTypeDispatch_82B49D50). Part of the 0x82B53 flag cluster (sibling of sub_82B52500 / sub_82B537C0 /
# sub_82B53A40). 192 lines, 1 callee. 1 caller: sub_82B53A40.
[entrypoint.functions."0x82B538F0"]
name = "ComputeAndCacheFlag_82B538F0"

# A large processor that validates a structure, then processes its fields, copying / computing histograms and
# caching flags along the way. It takes a structure (a: a state at a->0, a secondary at a->4, a sub-object at
# a->12, a parent at a->28) and a value (b). It validates the structure - bailing out early when any of a, a->28,
# a->12 is null (and on a->0 / a->4 being in the unexpected state). When a->28->0 is the "dirty" state (11) it
# promotes it to the "processed" state (12). It then drives the per-field processing - copying payloads
# (MemCpy_SdkRuntime_82CA2C60), building histograms (ProcessHistogram_82B597E8), processing struct fields
# (ProcessStructFields_82B59D28), computing and caching flags (sub_82B538F0 = ComputeAndCacheFlag), and the
# supporting field operations (sub_82B57438 / sub_82B57748 / sub_82B57760). This is the "validate this
# structure and run the full field-processing pipeline" routine for the 0x82B53 structure cluster, used by 3
# callers (ProcessTypeDispatch_82B49D50 / sub_82EF9AA0 / sub_82F289A8). Part of the 0x82B53 structure cluster
# (sibling of ProcessTypeDispatch_82B49D50 / ComputeAndCacheFlag_82B538F0 / ProcessStructFields_82B59D28). 3158
# lines, 7 callees. 3 callers.
[entrypoint.functions."0x82B53A40"]
name = "ProcessStructureValidation_82B53A40"

# A destructor that tears down the node's tree, then resets the node to an empty self-referential state. It
# takes a container (a: a node at a->4, a tail at a->8). It first destructs the node's tree payload
# (DestructorTree2_82367AE8 on a->4->4). It then resets the node: clearing the tail (a->8 = 0) and rewiring
# the node into a self-referential empty list - setting the node's three fields (a->4->0, a->4->4, a->4->8)
# to point back at the node itself. This is the "destroy this node's tree and reset the node to empty" routine
# for the 0x82367 destruct cluster, used by 7 callers (the 0x823CA/0x824A2/0x82592/0x827ED/0x8288E range). Part
# of the 0x82367 destruct cluster (sibling of DestructorTree2_82367AE8 / ConstructLinkedNode_823445B0). 37
# lines, 1 callee. 7 callers.
[entrypoint.functions."0x82367A90"]
name = "DestructAndResetNode_82367A90"

# A destructor that processes a node, frees its payload, and clears the pointer. It takes a container (a: a
# payload pointer at a->4, a secondary at a->8). It first runs the per-node processing (sub_823FB338) over the
# node's fields (reading a->4 and a->4->0). It then frees the payload (Free_SizeBucketed_Thunk_8221BE68(a->4))
# and clears both container fields (a->4 = 0, a->8 = 0). This is the "process this node, free its payload, and
# reset the container to empty" destructor for the 0x823FB node cluster, used by 10 callers (the 0x8235E/0x823BC
# /0x823C D/0x823FA/0x82592/0x825CB/0x827ED/0x8288D/0x832AF range). Part of the 0x823FB node cluster (sibling
# of DestructAndResetNode_82367A90 / Free_SizeBucketed_Thunk_8221BE68). 61 lines, 2 callees. 10 callers.
[entrypoint.functions."0x823FB150"]
name = "DestructNodePayload_823FB150"

# A processor that validates its pointer arguments, then processes the node. It takes a context (a), an object
# (b: a sub-object at b->4), and several pointers (c, d, f) that must match the object's fields. It validates
# the arguments - trapping (ppc_trap 31) when c is null or does not match the object (b), and routing to an
# error path when f does not match the sub-object's head (b->4->0). When the pointers are consistent it drives
# the per-node processing - resetting the node (sub_82367A90 = DestructAndResetNode) and the supporting field
# operations (sub_8246FFC8 / sub_8273F9D8). This is the "validate these pointers against this object, then run
# the node processing" routine for the 0x823FB node cluster, used by 5 callers (the 0x8236F/0x823FB/0x82B5B/
# 0x82C08/0x82C54 range). Part of the 0x823FB node cluster (sibling of DestructNodePayload_823FB150 /
# DestructAndResetNode_82367A90). 137 lines, 3 callees. 5 callers.
[entrypoint.functions."0x823FB338"]
name = "ValidateAndProcessNode_823FB338"

# A processor that dispatches on a state, then walks a range processing each element. It takes a context (a)
# and a structure (b: a base at b->8, a range head at b->20, a range tail at b->40, a descriptor at b->68, a
# state at b->72). It first checks the state: when b->72 is below 4 and the descriptor's low bits (b->68->5 & 3)
# are non-zero it drives the state dispatch (StateDispatch_82BCA7D8). It then walks the element range from the
# tail (b->20) up to the head (b->40), processing each element (reading element->8 and dispatching through
# sub_82BCCD58 / sub_82BCCDE0). This is the "dispatch on this structure's state, then process the full element
# range" routine for the 0x82BCA state/range cluster, called only by StateDispatch_82BCB0A8. Part of the 0x82BCA
# state/range cluster (sibling of StateDispatch_82BCA7D8 / StateDispatch_82BCB0A8). 208 lines, 3 callees. 1
# caller: StateDispatch_82BCB0A8.
[entrypoint.functions."0x82BCAF40"]
name = "ProcessStateAndRange_82BCAF40"

# A processor that finds the object's position in a circular linked list, removes it, and destructs the
# payload. It takes a container (a: a list pointer at a->4). It walks the circular list (advancing through each
# node's a->4 link) to locate the object's position, trapping (ppc_trap 31) when the search fails or the list
# is malformed (a null node, or a node that does not match the expected object). When found, it unlinks the
# node from the circular list (rewiring the predecessor's link to the successor) and destructs the node's
# payload (sub_823FB150 = DestructNodePayload) with supporting handling (sub_8273F9D8). This is the "find this
# object in the circular list, remove it, and free its payload" routine for the 0x8235E list cluster, used by 10
# callers (the 0x82364/0x82365/0x82371/0x82A4A/0x82C67/0x82C83 range). Part of the 0x8235E list cluster (sibling
# of DestructNodePayload_823FB150 / ConstructLinkedNode_823445B0). 107 lines, 2 callees. 10 callers.
[entrypoint.functions."0x8235E680"]
name = "FindAndRemoveFromList_8235E680"

# A selector that returns one of two .data pointer constants based on a condition. It takes a value (b). When
# b is zero it returns the first .data pointer (a .data base + 19788); when b is non-zero it returns the second
# (.data base + 19784). The two pointers differ by only 4 bytes, so this is a "pick one of two adjacent .data
# descriptors" selector - a variant of the constant-pointer select family. It is reached only via a vtable /
# function-pointer table (no direct callers in the recompiled code), indicating it is a virtual "get one of two
# descriptors" accessor. Part of the 0x82C20 constant-pointer cluster (sibling of SelectConstantPointer_82C23858
# / GetConstantPointer_82C39178). 16 lines, 0 callees. No direct callers found in the recompiled code (likely
# reached via a vtable / function-pointer table).
[entrypoint.functions."0x82C20CA0"]
name = "SelectConstantPointer2_82C20CA0"

# A processor that handles a virtual object, checking a bound and driving the per-virtual processing. It takes
# a context (a), a value (b), a bound (c), and another value (d). It first checks the bound: when c + 1 is below
# the threshold (c + 1 <= -19) it skips the pre-pass. Otherwise it runs the pre-pass (sub_82A2C520) using a .data
# descriptor (a .data base + 11760). It then computes the working index (c + 17) and drives the main
# per-virtual processing (ProcessVirtual_82227940(a, 0, 0, c + 17)), copying payloads (MemCpy_SdkRuntime_82CA2C60)
# and running the supporting virtual operations (sub_822A0F58 / sub_82A2C520). This is the "check this bound,
# run the pre-pass, and process the virtual object" routine for the 0x82A2D virtual cluster, called only by
# ComputeStringHash_82227EA0. Part of the 0x82A2D virtual cluster (sibling of ProcessTreeRecursive_82A2D590 /
# ProcessVirtual_82227940). 139 lines, 4 callees. 1 caller: ComputeStringHash_82227EA0.
[entrypoint.functions."0x82A2DCC8"]
name = "ProcessVirtualBound_82A2DCC8"

# A constructor that allocates a 68-byte element and appends it to a container, clearing its fields. It takes a
# container (a: a tail pointer at a->84) and several parameters (b, h, ...). It advances the container's tail by
# a 68-byte stride (a->84 + 68) and stores the new tail back. It then clears the new element's fields - a run of
# 32-bit, 16-bit, and 8-bit stores from element->8 through element->45, zeroing each slot. This is the "grow the
# container by one 68-byte element and zero the new element" constructor for the 0x82BAD construct cluster, called
# only by sub_82BC1968. Part of the 0x82BAD construct cluster (sibling of the element-append family). 167 lines,
# 0 callees. 1 caller: sub_82BC1968.
[entrypoint.functions."0x82BAD0A8"]
name = "ConstructAndAppend68Byte_82BAD0A8"

# A processor that increments a counter, triggering the corresponding processing when specific thresholds are
# reached. It takes a structure (a: a counter at a->52), a value (b), and another value (c). It increments the
# 16-bit counter (a->52 + 1, wrapped to 16 bits) and stores it back. When the counter (a->52) is below 200 it
# continues normally. When it reaches 200 it runs a one-time processing pass (sub_82BC9860 with a .data
# descriptor); when it reaches 225 or beyond it runs the higher-threshold handling. It then drives the per-step
# processing - dispatching the loop (ProcessLoopDispatch_8227B8B8), handling the state (ProcessState_8219F010),
# and the supporting operations (sub_822DF280 / sub_82BCCB88). This is the "bump this counter and run the
# processing for whatever threshold was just crossed" routine for the 0x8219E counter cluster, used by 10 callers
# (the 0x82BB9/0x82BC8/0x82BC9/0x82BD0/0x82BD3/0x82BD6/0x82BDA/0x82BDB range). Part of the 0x8219E counter
# cluster (sibling of ProcessState_8219F010 / ProcessLoopDispatch_8227B8B8). 112 lines, 5 callees. 10 callers.
[entrypoint.functions."0x8219EE00"]
name = "IncrementAndCheckThreshold_8219EE00"

# A processor that stores a value into a structure, selecting the field and the arithmetic based on the
# structure's type bits. It takes a structure (a: a head at a->0, a stride at a->4, an output at a->8, a
# fallback at a->12, a type at a->16) and a value (b). It inspects the type byte (a->16): when bit 2 (a->16 & 4)
# is clear it takes the no-op path; when bit 4 (a->16 & 0x10) is set it stores b into the fallback field (a->12
# = b). Otherwise, based on bit 1 (a->16 & 2) it either stores a->0 - b into a->8, or stores (a->4 << 1) - a->0 -
# b into a->8 (a strided, doubled-offset variant). This is the "store this value into this structure using the
# field and arithmetic selected by the type bits" routine for the 0x82284 typed-store cluster, used by 2 callers
# (sub_822848B8 / sub_822AC668). Part of the 0x82284 typed-store cluster (sibling of sub_822848B8). 290 lines, 0
# callees. 2 callers.
[entrypoint.functions."0x82284960"]
name = "ProcessAndStoreTypedValue_82284960"

# A processor that drives a virtual call, stores the typed value, then dispatches on the result. It takes a
# context (a: a vtable pointer at a->0, a working object at a + 48, a descriptor at a - 76), a value (b), and a
# second value (c). It first invokes a virtual method through a->0 (reading the method at a->0->4 and bctrl-ing
# into it). It then stores the typed value into the working object (ProcessAndStoreTypedValue_82284960(a + 48,
# c)). It reads the low byte of the result: when it is non-zero it loads a second descriptor (a - 76 -> 76) and
# invokes another virtual method through it. This is the "call this virtual method, store the result as a typed
# value, and dispatch on it" routine for the 0x82284 value-dispatch cluster, reached only via a vtable /
# function-pointer table (no direct callers in the recompiled code). Part of the 0x82284 value-dispatch cluster
# (sibling of ProcessAndStoreTypedValue_82284960). 98 lines, 1 callee. No direct callers found in the recompiled
# code (likely reached via a vtable / function-pointer table).
[entrypoint.functions."0x822848B8"]
name = "ProcessAndDispatchValue_822848B8"

# A processor that invokes a virtual method, checks the result against a count, and processes the value. It
# takes a context (a: a vtable pointer at a->0, a descriptor at a - 72), a value (b), a count (c), and a flag
# (d). It invokes a virtual method through a->0 (reading the method at a->0->4 and bctrl-ing into it). It then
# validates the count: when c is below 1, or when c does not exceed the result's capacity byte (result->42), it
# bails out. When the flag (d & 0xFF) is non-zero it loads a nested descriptor (a - 72 -> 0 -> 116) and processes
# the value through it (driving FloatToIntProcess_82284810 for the float / int conversion). This is the "call
# this virtual method, validate the count against the result's capacity, and process the value" routine for the
# 0x82BAF value-check cluster, reached only via a vtable / function-pointer table (no direct callers in the
# recompiled code). Part of the 0x82BAF value-check cluster (sibling of FloatToIntProcess_82284810 /
# ProcessAndDispatchValue_822848B8). 208 lines, 1 callee. No direct callers found in the recompiled code (likely
# reached via a vtable / function-pointer table).
[entrypoint.functions."0x82BAF540"]
name = "ProcessAndCheckCount_82BAF540"

# A processor that invokes a virtual method, then walks an array of 16-bit elements. It takes a context (a: a
# vtable chain at a->4, an element-base at a->8) and an index (b). It invokes a virtual method through a->4 (reading
# the method at a->4->0->44 and bctrl-ing into it). It then forms the element pointer (base + (index << 5) - the
# 32-byte stride), reads the element's bounds (element->16) and payload head (element->28). When the bounds are
# non-zero it walks the payload - a run of 16-bit pairs (element->4, element->6), each pair carrying a sign bit and
# a value - accumulating / processing them in a loop. This is the "call this virtual method and walk this index's
# 16-bit element array" routine for the 0x82BB0 array cluster, reached only via a vtable / function-pointer table
# (no direct callers in the recompiled code). Part of the 0x82BB0 array cluster (sibling of ProcessAndCheckCount
# _82BAF540). 146 lines, 0 callees. No direct callers found in the recompiled code (likely reached via a vtable /
# function-pointer table).
[entrypoint.functions."0x82BB0028"]
name = "ProcessAndWalkArray_82BB0028"

# A processor that grows a buffer to fit a requested range, then processes the elements. It takes a destination
# buffer (a: a capacity at a->20), a source buffer (b: a capacity at b->20), an offset (c), and a length (d). It
# first checks the source capacity (b->20) against the offset (c): when the source is too small it grows it
# (sub_82CD12C8). It clamps the length (d) to the remaining source capacity (b->20 - c). It then checks the
# destination capacity (a->20) against the range (offset + length), growing the destination when it would
# overflow (overflow checked by add-with-carry). It copies / zeroes the requested range into the destination
# (Buffer_CopyOrZero_Error_82CA3730), processing the elements (ProcessValue_FlagNonZero_8217A5E0) with
# supporting buffer operations (sub_82CD11D0 / sub_82CD12C8). This is the "grow the buffers as needed and copy
# this offset / length range" routine for the 0x822F7 buffer cluster, used by 3 callers (ProcessBuffer_822F6CB0 /
# sub_82372510 / sub_82C77A88). Part of the 0x822F7 buffer cluster (sibling of ProcessBuffer_822F6CB0 /
# Buffer_CopyOrZero_Error_82CA3730). 165 lines, 4 callees. 3 callers.
[entrypoint.functions."0x822F7E28"]
name = "GrowBufferForRange_822F7E28"

# A constructor that builds a buffer from a source: sets up the header, clamps / updates it, grows it for the
# requested range, and reads a string element. It takes a context (a), a value (b), and a source (c). It stages a
# buffer header on the stack (zeroing the flags at r1 + 100 / r1 + 104 / r1 + 84) and clamps / updates it
# (ClampAndUpdate_822F6380). It grows the buffer to fit the source range (sub_822F7E28 = GrowBufferForRange),
# reading the string element (ReadStringElement_822F2238). When done it frees the temporary (Free_SizeBucketed
# _Thunk_8221BE68). This is the "build this buffer from the source, growing and reading it as needed" constructor
# for the 0x82372 buffer cluster, used by 5 callers (the 0x8226C/0x82372/0x82B3D/0x82B3E/0x82B4F range). Part of
# the 0x82372 buffer cluster (sibling of GrowBufferForRange_822F7E28 / ClampAndUpdate_822F6380 /
# ReadStringElement_822F2238). 126 lines, 4 callees. 5 callers.
[entrypoint.functions."0x82372510"]
name = "ConstructBufferFromSource_82372510"

# A processor that clamps a requested range against a buffer's capacity, then operates on the clamped range. It
# takes a buffer (a: a capacity at a->20, a payload at a->24, a head at a + 4), a value (b), a requested start (c),
# and a requested length (d). It clamps the range against the capacity (a->20): when the requested start (c) is
# beyond the capacity it returns the capacity; when the start + length (c + d) would exceed the capacity it
# truncates the length. It then operates on the clamped range (a + 4 + start .. length), comparing 16-bit elements
# (CompareU16_822F85C8) and resolving pointers (GetPointer_822F5458). This is the "clamp this start / length range
# to this buffer's capacity and operate on the result" routine for the 0x822F6 range cluster, called only by
# ProcessCountDispatch_822CA580. Part of the 0x822F6 range cluster (sibling of ClampAndUpdate_822F6380 /
# CompareU16_822F85C8 / GetPointer_822F5458). 148 lines, 2 callees. 1 caller: ProcessCountDispatch_822CA580.
[entrypoint.functions."0x822F6480"]
name = "ClampRange_822F6480"

# A processor that computes a hash index, then performs a table lookup (trying up to two probes). It takes a hash
# table (a: a stride / bucket size at a->32) and a key (b). It computes the index from the key and the bucket size
# (key / bucket, then key - (key / bucket) * bucket - the remainder), forming the probe address (a->32 +
# remainder). It performs the first lookup (HashLookup2_82B497B8(a, 0, probe)); when the result's low byte is
# non-zero (a hit) it performs a second lookup (HashLookup2_82B497B8(a, 1, probe)) to complete the resolution.
# This is the "compute this key's hash index and look it up in the table (with a follow-up probe)" routine for the
# 0x82B49 hash cluster, used by 2 callers (sub_82B48EB8 / sub_82B49530). Part of the 0x82B49 hash cluster (sibling
# of HashLookup2_82B497B8 / ProcessTypeDispatch_82B49D50). 79 lines, 1 callee. 2 callers.
[entrypoint.functions."0x82B49908"]
name = "HashLookupWithIndex_82B49908"

# A destructor that removes an object from its list, then invokes its virtual destructor. It takes an object
# (a: a payload at a->0, a list at a + 8). It first removes the object from the circular list
# (FindAndRemoveFromList_8235E680(a + 8)). It then checks the payload (a->0): when it is non-null it invokes the
# payload's virtual destructor (reading the method at a->0->0->8 and bctrl-ing into it). This is the "remove this
# object from its list and run its virtual destructor" routine for the 0x82364 destruct cluster, used by 6 callers
# (the 0x8236D/0x82A4A/0x82B48/0x82C68 range). Part of the 0x82364 destruct cluster (sibling of
# FindAndRemoveFromList_8235E680 / DestructAndResetNode_82367A90). 48 lines, 1 callee. 6 callers.
[entrypoint.functions."0x82364420"]
name = "RemoveAndDestructObject_82364420"

# A validator that checks whether a value is a valid, in-range integer-valued float, returning its byte count. It
# takes a value descriptor (a: a payload at a->0, a type tag at a->4). It requires the type tag to be the float
# marker (a->4 == 3). It then performs a float -> int -> float round-trip (fctiwz, sign-extend, fcfid, frsp) on the
# payload (a->0) and requires the round-trip to be lossless (the converted value equals the original) - i.e. the
# float holds an exact integer. It then checks the integer is in range (0 < value <= 1024) and counts the number of
# bytes needed to represent it (shifting the value right in 24-bit chunks). This is the "validate this float is an
# exact integer in the allowed range and compute its byte count" routine for the 0x82A2F validate cluster, called
# only by sub_82A2FA28. Part of the 0x82A2F validate cluster (sibling of sub_82A2FA28). 90 lines, 0 callees. 1
# caller: sub_82A2FA28.
[entrypoint.functions."0x82A2F620"]
name = "ValidateFloatValue_82A2F620"

# A constructor that builds a 104-byte object from a source, or clears the slot when the source is empty. It takes
# a destination slot (a: a pointer at a->0), a source (b), and two additional parameters (c, d). When the source (b)
# is null it simply clears the slot (a->0 = 0). When the source is non-null it allocates a fresh 104-byte object
# (Allocate_OrDbgBreak_8221F388(104)) and initializes it from the source (sub_82B48EB8(b, c, d)), storing the new
# pointer into the slot. This is the "build this 104-byte object from the source, or clear it when empty" routine
# for the 0x82B48 construct cluster, used by 10 callers (the 0x82363/0x82364/0x824E9/0x82B48/0x82BF C/0x82C02/0x82C62
# range). Part of the 0x82B48 construct cluster (sibling of sub_82B48EB8 / Allocate_OrDbgBreak_8221F388). 83 lines,
# 2 callees. 10 callers.
[entrypoint.functions."0x82B48E30"]
name = "Construct104ByteObject_82B48E30"

# An initializer that seeds a 104-byte object's fields and drives its per-object setup. It takes a freshly
# allocated object (a: a vtable / .data pointer at a->0, a flag at a->4, a source at a->8, a payload at a->16, a
# tail at a->24, a field at a->32, a body at a + 40), a source (b), a value (c), and another value (d). It seeds
# the object's head with a .data pointer (a->0 = a .data base - 28916) and clears the flag (a->4 = 0). It stores the
# source (a->8 = b) and, when the source is non-null, invokes a virtual method through it (b->0->4). It then stores
# the payload fields (a->16 = c, a->24 = 0, a->32 = d) and drives the body initialization (a + 40), allocating
# (Allocate_OrDbgBreak_8221F388) and looking up references (HashLookupWithIndex_82B49908) as needed. This is the
# "seed this 104-byte object's fields and run its setup" initializer for the 0x82B48 construct cluster, called only
# by Construct104ByteObject_82B48E30. Part of the 0x82B48 construct cluster (sibling of Construct104ByteObject
# _82B48E30 / HashLookupWithIndex_82B49908). 123 lines, 2 callees. 1 caller: Construct104ByteObject_82B48E30.
[entrypoint.functions."0x82B48EB8"]
name = "Initialize104ByteObject_82B48EB8"

# A destructor that processes an object, then frees it when its flag bit is set. It takes an object (a) and a
# value (b). It first drives the per-object processing (sub_82B48FE0(a, b)). It then checks the low bit of the
# value (b & 1): when it is set it frees the value (Free_SizeBucketed_Thunk_8221BE68(b)); when it is clear it
# leaves it in place. This is the "process this object and free the associated value when the flag bit is set"
# destructor for the 0x82B48 destruct cluster, reached only via a vtable / function-pointer table (no direct
# callers in the recompiled code). Part of the 0x82B48 destruct cluster (sibling of sub_82B48FE0 /
# Initialize104ByteObject_82B48EB8). 50 lines, 2 callees. No direct callers found in the recompiled code (likely
# reached via a vtable / function-pointer table).
[entrypoint.functions."0x82B48F90"]
name = "DestructAndFreeIfFlagged_82B48F90"

# A destructor that tears down a 104-byte object's fields, walking its elements and freeing them. It takes an
# object (a: a vtable / .data pointer at a->0, an element index at a->36) and a value (b). It re-seeds the
# object's head with a .data pointer (a->0 = a .data base - 28916) and computes the element pointer (a + 40 +
# (a->36 << 5) - the 32-byte stride). When the element's payload (element->16) is non-null it invokes a virtual
# method through it (element->16->16) to destruct the payload. It then frees / resolves the remaining references
# (Free_SizeBucketed_Thunk_8221BE68 / HashLookup_82B49678 / sub_82C6F8E8). This is the "tear down this 104-byte
# object's fields, destructing and freeing its elements" routine for the 0x82B48 destruct cluster, called only by
# DestructAndFreeIfFlagged_82B48F90. Part of the 0x82B48 destruct cluster (sibling of DestructAndFreeIfFlagged
# _82B48F90 / Initialize104ByteObject_82B48EB8). 176 lines, 3 callees. 1 caller: DestructAndFreeIfFlagged
# _82B48F90.
[entrypoint.functions."0x82B48FE0"]
name = "DestructObjectFields_82B48FE0"

# A processor that inspects a value's type and dispatches it through the value handler. It takes a context (a),
# a value descriptor (b: a payload at b->0, a type tag at b->4), a target (c), and another value (d). It loads two
# .data descriptors (r22, r23) as working references. It inspects the type tag (b->4): when it is the matching
# marker (5) it drives the per-value dispatch (ProcessDispatchValue_82228D98(a, b->0, c)) and continues with the
# result; otherwise it takes the other type branches. It resolves the value against the hash table
# (HashLookupByKey_82228570) and runs the supporting operations (sub_82228518 / sub_82228C70 / sub_82A2C300 /
# sub_82A2C520 / sub_82A2E140). This is the "inspect this value's type and dispatch it through the matching
# handler" routine for the 0x82228 dispatch cluster, used by 10 callers (the 0x821FC/0x8246B/0x82570/0x8261C/0x827A5
# /0x82844/0x82852/0x8290B/0x82953 range). Part of the 0x82228 dispatch cluster (sibling of
# ProcessDispatchValue_82228D98 / HashLookupByKey_82228570). 312 lines, 7 callees. 10 callers.
[entrypoint.functions."0x82228E28"]
name = "ProcessDispatchByType_82228E28"

# A constructor that allocates a 20-byte node, clears its fields, and wires it into a self-referential list. It
# takes a container (a: a node slot at a->4). It allocates the node (Allocate_OrDbgBreak_8221F388(20)) and, when the
# allocation succeeds, clears its four fields (node->0, node->4, node->8, node->16 all = 0). It stores the node into
# the slot (a->4 = node) and wires it into the self-referential structure - setting node->8 to point back at the
# node and copying the container's link (a->4->8) into node->4. This is the "allocate this 20-byte node, clear it,
# and link it into the self-referential chain" constructor for the 0x82C47 node cluster, used by 10 callers (the
# 0x82C02/0x82C0B/0x82C0F/0x82C14/0x82C1D/0x82C34/0x82C37/0x82C3D/0x82C53/0x82C55 range). Part of the 0x82C47 node
# cluster (sibling of ConstructLinkedNode_823445B0 / Allocate_OrDbgBreak_8221F388). 135 lines, 1 callee. 10
# callers.
[entrypoint.functions."0x82C474E0"]
name = "Construct20ByteNode_82C474E0"

# A destructor that frees the two pointer fields of a structure. It takes a structure (a: a first pointer at a->0,
# a second pointer at a->4). It frees the second field (a->4) when it is non-null (Free_Thunk_824FE010), then frees
# the first field (a->0) when it is non-null. This is the "free this structure's two pointer fields" destructor for
# the 0x82C47 node cluster (the paired destructor of Construct20ByteNode_82C474E0), used by 10 callers (the 0x82C02
# /0x82C0B/0x82C14/0x82C17/0x82C1D/0x82C1E/0x82C2E/0x82C55 range). Part of the 0x82C47 node cluster (sibling of
# Construct20ByteNode_82C474E0 / Free_Thunk_824FE010). 47 lines, 1 callee. 10 callers.
[entrypoint.functions."0x82C475D8"]
name = "Destruct20ByteNode_82C475D8"

# A destructor that runs a virtual call on a pointer field, then clears it. It takes a structure (a: a pointer
# field at a->0). When the field (a->0) is non-null it invokes a virtual method through it (reading the method at
# a->0->0->8 and bctrl-ing into it) and then clears the field (a->0 = 0). When the field is already null it does
# nothing. This is the "destruct this pointer field (virtual call) and clear it" routine for the 0x82C6F destruct
# cluster, used by 10 callers (ProcessBoundsCheck_82B43E30 / HashLookup2_82B497B8 / DestructObjectFields_82B48FE0 /
# CsGuarded_HandlerDispatch_82B43CF0 / LockedReadSubobject_82B44360, and the 0x8218C/0x821BC/0x8260B/0x8280B/0x82C6F
# range). Part of the 0x82C6F destruct cluster (sibling of ProcessBoundsCheck_82B43E30 / DestructObjectFields
# _82B48FE0). 47 lines, 0 callees. 10 callers.
[entrypoint.functions."0x82C6F8E8"]
name = "DestructAndClearField_82C6F8E8"

# A processor that resolves two indexed pointers and compares them. It takes a container (a), a value (b), and an
# index (c). It resolves two pointers from the container (GetIndexedPointer_82BC5448(a, -1) and
# GetIndexedPointer_82BC5448(a, c)). It then compares the two resolved pointers: when either matches the .data
# sentinel (a .data base + 1876), or when their low fields (->4) differ, it takes the mismatch path. When the two
# pointers agree it continues with the matching handling (driving DispatchTypeTag2_822D8298 for the per-type
# processing). This is the "resolve these two indices and compare the resulting pointers" routine for the 0x82BC5
# compare cluster, used by 2 callers (sub_82BB8F50 / sub_82BB9B60). Part of the 0x82BC5 compare cluster (sibling of
# GetIndexedPointer_82BC5448 / DispatchTypeTag2_822D8298). 84 lines, 2 callees. 2 callers.
[entrypoint.functions."0x82BC5940"]
name = "CompareIndexedPointers_82BC5940"

# A processor that drives a series of virtual calls, checking the result after each. It takes a structure (a) and
# a value (b). It invokes a sequence of virtual calls through VcallTriple_AllSuccess_82C011E8, each passing the
# value (b), a .data descriptor pointer, and a field offset into the structure (a, then a + 4, and so on). After
# each call it reads the result's low byte: when it is zero (the call failed / the field is empty) it bails out
# early; when it is non-zero it continues to the next field. This is the "drive this sequence of virtual field
# dispatches, stopping when a field is empty" routine for the 0x82C2A dispatch cluster, called only by sub_82C2AEC0.
# Part of the 0x82C2A dispatch cluster (sibling of VcallTriple_AllSuccess_82C011E8 / sub_82C2AEC0). 109 lines, 1
# callee. 1 caller: sub_82C2AEC0.
[entrypoint.functions."0x82C2AC88"]
name = "ProcessAndDispatchVirtual_82C2AC88"

# A processor that drives a virtual call, checks the result, and dispatches the sub-processing. It takes a
# structure (a: a vtable chain at a->0), a value (b), and a target (c). It invokes a virtual method through a->0
# (reading the method at a->0->24 and bctrl-ing into it). It reads the result's low byte: when it is zero (the call
# failed) it bails out. When it succeeds it drives the per-field sub-dispatch (sub_82C2AC88 =
# ProcessAndDispatchVirtual(c, a)), again checking the result's low byte and bailing out on failure. When both
# succeed it continues with further virtual handling (a->0->28). This is the "invoke this virtual method, and when
# it succeeds dispatch the sub-processing" routine for the 0x82C2A dispatch cluster, called only by sub_82C2B118.
# Part of the 0x82C2A dispatch cluster (sibling of ProcessAndDispatchVirtual_82C2AC88 / sub_82C2B118). 86 lines, 1
# callee. 1 caller: sub_82C2B118.
[entrypoint.functions."0x82C2AEC0"]
name = "ProcessAndDispatchSubVirtual_82C2AEC0"

# An SDK event creator: builds and issues an event through the SDK, handling the result. It takes a context (a),
# a value (b), a value (c), and an optional descriptor (d). When the descriptor (d) is non-null it first stages it
# (sub_82CC1DF0). It then computes a bit from the value (count-leading-zeros of b, shifted right - selecting a
# bit from the value's magnitude) and an attribute from c's low byte, and issues the event creation
# (NtCreateEvent through r1 + 80). It checks the result's sign (status < 0) and drives the follow-up handling
# (SetGlobalFieldIfNeeded_82CC1C20 / sub_82CC1C38). This is the "build this event and create it through the SDK"
# routine for the 0x82CBC event cluster, used by 10 callers (the 0x82321/0x82359/0x8236B/0x82B44/0x82B49/0x82B56/0x82BA2
# /0x82D01/0x832B8 range). Part of the 0x82CBC event cluster (sibling of SetGlobalFieldIfNeeded_82CC1C20 /
# SetEvent_SdkRuntime_822C9380). 94 lines, 3 callees. 10 callers.
[entrypoint.functions."0x82CBC4B8"]
name = "CreateEvent_SdkRuntime_82CBC4B8"

# A processor that measures a null-terminated string, hashes it, and sets up the result. It takes a context (a),
# a value (b), and a byte string (c). It first accesses the element (AccessElement8_82A1E620). It then scans the
# string (c), advancing byte-by-byte until it finds the terminating null, computing the length (the distance to
# the null, minus one). It hashes the string (ComputeStringHash_82227EA0(a, c, length)) and stages the result (a
# 4-byte descriptor at r1 + 80 / r1 + 84). It then drives the per-type dispatch (ProcessDispatchByType_82228E28).
# This is the "measure this string, compute its hash, and dispatch on it" routine for the 0x82A1E string cluster,
# used by 10 callers (the 0x823B1/0x823F6/0x824B1/0x824EF/0x8253E/0x82690/0x82790/0x828BB/0x828BC/0x8296A range).
# Part of the 0x82A1E string cluster (sibling of ComputeStringHash_82227EA0 / AccessElement8_82A1E620 / Process
# DispatchByType_82228E28). 85 lines, 3 callees. 10 callers.
[entrypoint.functions."0x82A1EC08"]
name = "ProcessAndHashString_82A1EC08"

# A processor that validates a chunk's state, then writes it - constructing, freeing, and gating the fields. It
# takes a chunk (a: a vtable chain at a->8, a state at a->12, a count at a->16, a length at a->20, a flag at a->24,
# an index at a->32, an end at a->36). It clears the flag (a->24 = 0) and validates the state: when the state (a->12)
# is below 1 or above 1 it takes the error paths; when the index (a->36) does not match the end (a->32) it routes to
# the mismatch path. When valid it drives the per-chunk write - invoking a virtual method through a->8 (a->8->0->24),
# constructing a node (ConstructLinkedNode_823445B0), allocating (Allocate_OrDbgBreak_8221F388), freeing the
# temporary (Free_SizeBucketed_Thunk_8221BE68), and gating the 32-bit store (SubObject_VcallSlot3_GatedStore32
# _82C842C8). This is the "validate this chunk's state and write it" routine for the 0x82B48 chunk cluster, called
# only by WriteChunk_TableDispatch_82B486D0. Part of the 0x82B48 chunk cluster (sibling of
# WriteChunk_TableDispatch_82B486D0 / ConstructLinkedNode_823445B0). 408 lines, 5 callees. 1 caller:
# WriteChunk_TableDispatch_82B486D0.
[entrypoint.functions."0x82B48A40"]
name = "ProcessAndWriteChunk_82B48A40"

# A grow that extends a buffer to fit a given number of 8-byte elements, zeroing the newly added space. It takes
# a buffer (a: a current end at a->8, a capacity at a->12) and an element count (b). When the requested end
# (a->12 + (b << 3)) is already at or beyond the current end, it skips the zeroing. Otherwise it walks the new
# space, zeroing each 8-byte slot (the low 32 bits), advancing the end pointer by 8 each time. It then stores the
# final end (a->8 = a->12 + (b << 3)). This is the "grow this buffer to hold b 8-byte elements, zero-filling the
# new space" routine for the 0x82BC5 grow cluster, used by 10 callers (the 0x82306/0x8244F/0x824FA/0x825B7/0x82792/0x82A29
# /0x82BF3/0x82BF5/0x82BF7 range). Part of the 0x82BC5 grow cluster (sibling of GrowBitArray_821F1B58 / Grow
# ElementBuffer_82B420E0). 65 lines, 0 callees. 10 callers.
[entrypoint.functions."0x82BC55B8"]
name = "GrowAndZeroBuffer_82BC55B8"

# A processor that runs a destruct step, then frees the value when its flag is set. It takes a value (a) and a
# flag (b). It first runs the per-value destruct step (sub_82B439E0(a, b)). It then reads the flag's low bit (b &
# 1): when it is set, it frees the value (Free_SizeBucketed_Thunk_8221BE68(a)); when the bit is clear it skips the
# free. This is the "run the destruct step, and free the value when its flag is set" routine for the 0x82B43
# destruct cluster, referenced by no other function. Part of the 0x82B43 destruct cluster (sibling of
# ProcessAndFreeIfFlagged_82B50F80 / DestructAndFreeIfFlagged_82B48F90 / Free_SizeBucketed_Thunk_8221BE68). 50
# lines, 2 callees. 0 callers (vtable / external).
[entrypoint.functions."0x82B437B0"]
name = "ProcessAndFreeIfFlagged3_82B437B0"

# A processor that runs under a critical section, driving a series of virtual calls when the value is present. It
# takes a value (a: a descriptor at a->0, a sub-object at a->36, a payload at a->40). It stores a descriptor pointer
# into a->0. It then enters the critical section (CriticalSection_EnterOrTry_82200688). When the sub-object (a->36)
# is present it drives a sequence of virtual methods through it (reading the methods at a->36->0->32 and a->36->0->16
# and bctrl-ing into them). It then releases the section. This is the "lock, drive these virtual calls, and unlock"
# routine for the 0x82B43 locked-process cluster, called only by ProcessAndFreeIfFlagged3_82B437B0. Part of the
# 0x82B43 locked-process cluster (sibling of ProcessAndFreeIfFlagged3_82B437B0 / CriticalSection_EnterOrTry
# _82200688). 173 lines, 2 callees. 1 caller: ProcessAndFreeIfFlagged3_82B437B0.
[entrypoint.functions."0x82B439E0"]
name = "ProcessAndDispatchLocked_82B439E0"

# A comparator that compares two strings in a signed, case-sensitive manner. It takes two string holders (a and b,
# each with the string at ->0). It first compares the two string pointers: when they are identical it returns equal
# (0). When the pointers differ it handles the null cases - when one string is null it is ordered before the other
# (returning 1 or 0). When both are present it performs the signed comparison (StrCmp_Signed_8226D7A8(a->0, b->0))
# and returns the sign (a negative, zero, or positive result from a leading-zero count). This is the "compare these
# two strings, signed" routine for the 0x82C16 string-compare cluster, used by 6 callers (sub_82C17240 / sub_82C172D0
# / sub_82C1A200 / sub_82C1A3D8 / sub_82C1AA30 / sub_82C1D380). Part of the 0x82C16 string-compare cluster (sibling
# of StringCompare_82CA3358 / StrCmp_Signed_8226D7A8). 131 lines, 1 callee. 6 callers.
[entrypoint.functions."0x82C16660"]
name = "CompareStringsSigned_82C16660"

# A refcount decrement: atomically decrements a structure's reference count, cleaning it up when it reaches zero. It
# takes a structure (a: a vtable at a->0, a refcount at a->4). It performs an atomic read-modify-write on a->4 (a
# load-and-reserve (lwarx) and store-conditional (stwcx) pair, looping until it succeeds), decrementing the count
# by one. When the decremented count is non-zero (other references remain) it returns. When it reaches zero and the
# structure (a) is non-null it proceeds to clean it up. This is the "release one reference, freeing when the last one
# goes" routine for the 0x82BFB refcount cluster, referenced by no other function. Part of the 0x82BFB refcount
# cluster (sibling of DecrementRefAndDestroy_82B4F5A8 / DecrementRefAtOffset_82B4F370). 77 lines, 0 callees. 0
# callers (vtable / external).
[entrypoint.functions."0x82BFB5B0"]
name = "DecrementRefAndClean_82BFB5B0"

# A processor that drives a virtual call and, when the result is valid, dispatches the sub-processing. It takes a
# structure (a: a sub-object at a->16), an element count (b), and a flag (c). It computes an 8-byte offset from the
# count (b << 3). It then invokes a virtual method through the sub-object (reading the method at a->16->12 and
# bctrl-ing into it, passing a->16->16). When the call's result is non-zero or the computed size is zero it skips
# the sub-dispatch. Otherwise it drives the per-element sub-processing (sub_821E51D0(a, 4, ...)). This is the
# "invoke this virtual method and, when it succeeds, dispatch the per-element processing" routine for the 0x8229B
# dispatch cluster, used by 2 callers (sub_8219AB28 / sub_82A1ED58). Part of the 0x8229B dispatch cluster (sibling of
# ProcessAndDispatchSubVirtual_82C2AEC0 / sub_821E51D0). 94 lines, 1 callee. 2 callers.
[entrypoint.functions."0x8229B748"]
name = "ProcessAndDispatchVirtual2_8229B748"

# A processor that, when a new size exceeds the current, expands and clears the indexed region. It takes a
# structure (a: a current size at a->36, a pointer at a->40) and a new size (b). When the new size (b) is at or
# below the current size (a->36) it does nothing. Otherwise it computes a 4-byte size (b << 2), obtains the
# singleton (GetSingleton_82BA8718), processes the indexed region (ProcessIndexedRegion2_82BA8F18 with a tag of 141),
# and clears the newly added memory (MemSet_SdkRuntime_82CA3190, zero-filling). This is the "grow this indexed
# region to the new size and zero the new space" routine for the 0x82BBF region cluster, used by 10 callers (the
# 0x82BAE/0x82BAF/0x82BB2/0x82BB3/0x82BBC/0x82BBD/0x82BC4/0x82BE7 range). Part of the 0x82BBF region cluster
# (sibling of ProcessIndexedRegion_82BA9068 / ProcessIndexedRegion2_82BA8F18). 88 lines, 5 callees. 10 callers.
[entrypoint.functions."0x82BBF648"]
name = "ProcessAndClearIndexedRegion_82BBF648"

# A processor that pulls an element, stores it into an 8-byte slot, and reads back the typed field. It takes a
# structure (a: a buffer end at a->8, a source at a->16). It reads the source's element (a->16->92) and accesses the
# first element (ElementAccess_821E1498(..., 1)). It copies the element's two 32-bit words into the buffer (a->8->0
# and a->8->4) and advances the buffer end by 8 bytes. It then reads back the newly stored value as a typed field
# (GetTypedField_822AF338(a, -1)) and steps the buffer end back. This is the "store this element into the buffer and
# read it back typed" routine for the 0x82BB1 element-store cluster, used by 10 callers (sub_821D2C70 and the
# 0x82BB1/0x82BB2/0x82BB3/0x82BB6/0x82BB7/0x82BB9 range). Part of the 0x82BB1 element-store cluster (sibling of
# ElementAccess_821E1498 / GetTypedField_822AF338). 67 lines, 2 callees. 10 callers.
[entrypoint.functions."0x82BB1E58"]
name = "ProcessAndStoreElement_82BB1E58"

# A processor that validates a bound, then resolves a typed field and dispatches on it. It takes a structure (a:
# a source at a->16, a node at a->20, a end at a->40, a flag at a->72), a value (b), and a flag (c). It first reads
# the source's count fields (a->16->68 and a->16->64): when the count (a->16->68) is below the low bound (a->16->64)
# it runs the correction step (sub_82179350). It then resolves the target node: when the node (a->20) matches the
# end (a->40) it uses the flag (a->72); otherwise it walks to a->20->4->0 and reads the field at a->20->4->0->12.
# It then drives the per-element dispatch (ProcessAndDispatchVirtual2_8229B748). This is the "check this bound,
# resolve the typed field, and dispatch" routine for the 0x8219A field-resolve cluster, used by 10 callers (the
# 0x824F5/0x825E8/0x82679/0x8267A/0x826FB/0x82773/0x82774/0x827AB/0x828B5/0x8296A range). Part of the 0x8219A
# field-resolve cluster (sibling of ProcessAndDispatchVirtual2_8229B748 / sub_82179350). 125 lines, 2 callees. 10
# callers.
[entrypoint.functions."0x8219AB28"]
name = "ProcessAndResolveTypedField_8219AB28"

# A processor that resolves an indexed pointer, measures a null-terminated string, and hashes it. It takes a
# container (a), an index (b), and a byte string (c). It first resolves the indexed pointer (GetIndexedPointer
# _82BC5448(a, b)). It then scans the string (c), advancing byte-by-byte until it finds the terminating null,
# computing the length (the distance to the null, minus one). It hashes the string (ComputeHash_8227BA30(a, c,
# length)) and stages the result (a 4-byte descriptor at r1 + 80 / r1 + 84). It then drives the per-value dispatch
# (DispatchValueProcess_821CF320). This is the "resolve this index, measure the string, and hash it" routine for the
# 0x821E1 string cluster, used by 10 callers (the 0x82BB1/0x82BC0/0x82BBF/0x82BD1/0x82BD7/0x82BDA/0x82BDB range).
# Part of the 0x821E1 string cluster (sibling of ProcessAndHashString_82A1EC08 / ComputeHash_8227BA30 / GetIndexed
# Pointer_82BC5448). 83 lines, 3 callees. 10 callers.
[entrypoint.functions."0x821E1408"]
name = "ProcessAndHashString2_821E1408"

# A destructor that walks an array of refcounted objects, destructing and freeing each element. It takes a
# container (a: a descriptor at a->0, an element array at a->4, an element count at a->12). It stores a descriptor
# pointer into a->0. When the count (a->12) is non-positive it skips the loop. Otherwise it walks the element array
# (a->4), and for each non-null element it runs the refcounted destruct (DestructRefCounted_82214F08(element + 4))
# and then frees the element (Free_Thunk_824FE010(element)). This is the "destruct and free every element in this
# array" routine for the 0x82C2A destruct cluster, used by 8 callers (sub_82C1C6B8 and the 0x82C27/0x82C2A/0x82C2C/
# 0x82C2E/0x82C30/0x82C31/0x82C3F range). Part of the 0x82C2A destruct cluster (sibling of DestructElementArray
# _82C2D1A8 / DestructRefCounted_82214F08 / Free_Thunk_824FE010). 92 lines, 2 callees. 8 callers.
[entrypoint.functions."0x82C2AAF8"]
name = "DestructElementArray2_82C2AAF8"

# A destructor that compacts an array of refcounted objects, then destructs and frees each element. It takes a
# container (a: a descriptor at a->0, an element array at a->4, an element count at a->12). It stores a descriptor
# pointer into a->0. It first compacts the array (ArrayCompact_82C23DD0(a)). When the count (a->12) is non-positive
# it skips the loop. Otherwise it walks the element array (a->4), and for each non-null element it runs the
# refcounted destruct (DestructRefCounted_82214F08(element + 4)) and then frees the element (Free_Thunk_824FE010
# (element)). This is the "compact this array, then destruct and free every element" routine for the 0x82C2F
# destruct cluster, used by 10 callers (sub_82C1B180 and the 0x82C1C/0x82C27/0x82C28/0x82C2C/0x82C31/0x82C3D/0x82C3F
# range). Part of the 0x82C2F destruct cluster (sibling of DestructElementArray2_82C2AAF8 / ArrayCompact_82C23DD0
# / DestructRefCounted_82214F08). 95 lines, 3 callees. 10 callers.
[entrypoint.functions."0x82C2FDC0"]
name = "CompactAndDestructArray_82C2FDC0"

# A constructor that zeroes a structure and runs a bubble sort over it. It takes a structure (a: a descriptor at
# a->0, a start at a->4, a end at a->8, a count at a->12) and a value (b). It stores a descriptor pointer into a->0,
# zeroes the start / end / count fields (a->4, a->8, a->12 all to 0). It then runs the bubble sort (BubbleSort3
# _82C16C78(b, a + 4)), operating on the structure's body. When the sort completes it updates the descriptor pointer
# (a->0). This is the "construct this structure, zeroing it, and bubble-sort it" routine for the 0x82C31 construct
# cluster, used by 7 callers (sub_82C1C6B8 and the 0x82C27/0x82C2C/0x82C2E/0x82C30/0x82C31/0x82C3F range). Part of
# the 0x82C31 construct cluster (sibling of BubbleSort3_82C16C78 / ConstructElementArray_829FED20). 58 lines, 1
# callee. 7 callers.
[entrypoint.functions."0x82C31B48"]
name = "ConstructAndBubbleSort_82C31B48"

# A one-time initializer: ensures a global is set up only once, then drives a virtual call on it. It takes a value
# (a). It first reads a one-shot flag from a .data slot: when the flag's low bit is already set, the global is
# already initialized and it skips straight to the virtual call. When the flag is clear it sets the flag (so this
# runs only once), stores the global's descriptor pointer (r3->0), and proceeds. It then invokes a virtual method
# through the global (reading the method at the global->4 and bctrl-ing into it). This is the "initialize this
# global exactly once, then make its virtual call" routine for the 0x82B4D one-time-init cluster, used by 10
# callers (the 0x8260B/0x82B55/0x82C44/0x82C45/0x82C46/0x82C64/0x82C65/0x82C66 range). Part of the 0x82B4D
# one-time-init cluster (sibling of OneTimeInitTree_83233B40). 68 lines, 0 callees. 10 callers.
[entrypoint.functions."0x82B4D518"]
name = "OneTimeInitAndVcall_82B4D518"

# A constructor that stores a descriptor pointer and atomically increments a counter. It takes a structure (a: a
# descriptor at a->0, a counter at a->4). It stores a descriptor pointer into a->0. It then atomically increments
# the counter (AtomicIncrement3_821F7798(a + 4)). This is the "set this structure's descriptor and bump its
# counter" routine for the 0x82BFF construct cluster, used by 10 callers (sub_82C21238 and the 0x82C2F/0x82C40/
# 0x82C41/0x82C42/0x82C43/0x82C50 range). Part of the 0x82BFF construct cluster (sibling of
# AtomicIncrement3_821F7798 / IncrementAndCheckThreshold_8219EE00). 40 lines, 1 callee. 10 callers.
[entrypoint.functions."0x82BFF710"]
name = "ConstructAndIncrementCounter_82BFF710"

# A destructor that sets a descriptor pointer and runs the refcounted destruct. It takes a structure (a: a
# descriptor at a->0, a payload at a->4). It stores a descriptor pointer into a->0. It then runs the refcounted
# destruct on the payload (DestructRefCounted_82214F08(a + 4)). This is the "set this structure's descriptor and
# run its refcounted destruct" routine for the 0x82BFF destruct cluster, used by 10 callers (sub_82C21238 and the
# 0x82C21/0x82C27/0x82C2F/0x82C30/0x82C41/0x82C42/0x82C43 range). Part of the 0x82BFF destruct cluster (sibling of
# DestructRefCounted_82214F08 / ConstructAndIncrementCounter_82BFF710). 17 lines, 1 callee. 10 callers.
[entrypoint.functions."0x82BFF750"]
name = "DestructAndSetDescriptor_82BFF750"

# A comparator that compares two 12-byte structures, memcmp-style, with a signed length tail. It takes two
# structures (a and b: each with a 12-byte body and a tail field at ->12). It compares the two bodies byte-by-byte
# (looping up to 12 bytes), computing the first difference (a[i] - b[i]). When the bodies differ it returns 0 (not
# equal). When all 12 bytes match it compares the tail fields (a->12 and b->12), computing their difference and
# returning the sign (a leading-zero count shifted right - negative, zero, or positive). This is the "compare these
# two 12-byte structures" routine for the 0x82C00 compare cluster, called only by sub_82C02FC8. Part of the 0x82C00
# compare cluster (sibling of CompareStringsSigned_82C16660 / StringCompare_82CA3358). 50 lines, 0 callees. 1
# caller: sub_82C02FC8.
[entrypoint.functions."0x82C00EE0"]
name = "Compare12ByteStructs_82C00EE0"

# A processor that drives a sequence of virtual calls, gating on the result, then increments a counter. It takes a
# value (a: a vtable chain at a->0) and a flag (b). It invokes a virtual method through a->0 (reading the method at
# a->0->0 and bctrl-ing into it): when the result is below 1 it takes the failure path; when it is exactly 1 it
# continues, otherwise it takes the mismatch path. When it matches it drives a second virtual call (a->0->20), again
# gating on the result being exactly 1. When both match it atomically increments the counter (AtomicIncrement3
# _821F7798) and proceeds with the per-value handling (sub_82BFF7A0 / sub_82BFF7D8 / sub_82C00FA8 / sub_82C00FF8),
# destructing the temporary (DestructRefCounted_82214F08). This is the "run these gated virtual calls and, when they
# agree, increment the counter and process the value" routine for the 0x82C01 dispatch cluster, called only by
# sub_82C021B8. Part of the 0x82C01 dispatch cluster (sibling of ProcessAndDispatchVirtual2_8229B748 /
# AtomicIncrement3_821F7798). 324 lines, 6 callees. 1 caller: sub_82C021B8.
[entrypoint.functions."0x82C01ED0"]
name = "ProcessAndDispatchVirtual3_82C01ED0"

# A processor that drives a virtual call, checks the result, and dispatches the sub-processing. It takes a
# structure (a: a vtable chain at a->0), a value (b), and a target (c). It invokes a virtual method through a->0
# (reading the method at a->0->24 and bctrl-ing into it). It reads the result's low byte: when it is zero (the call
# failed) it bails out. When it succeeds it drives the per-field sub-dispatch (sub_82C01ED0 =
# ProcessAndDispatchVirtual3(a, c)), again checking the result's low byte and bailing out on failure. When both
# succeed it continues with further virtual handling (a->0->28). This is the "invoke this virtual method, and when
# it succeeds dispatch the sub-processing" routine for the 0x82C02 dispatch cluster, used by 3 callers (sub_82C02FC8
# / sub_82C033D8 / sub_82C02E88). Part of the 0x82C02 dispatch cluster (sibling of ProcessAndDispatchSubVirtual
# _82C2AEC0 / ProcessAndDispatchVirtual3_82C01ED0). 86 lines, 1 callee. 3 callers.
[entrypoint.functions."0x82C021B8"]
name = "ProcessAndDispatchSubVirtual2_82C021B8"

# A processor that finds a chain value, then walks the resulting tree and processes each node. It takes a
# structure (a: a descriptor at a->0, a start at a->4, a chain at a->8). It stores a descriptor pointer into a->0.
# It then finds the chain value (FindChainValue_82C469F0(a + 8)). When the result is non-null it walks the tree:
# for each node it drives the tree processing (Tree_Process_Wrapper_82C47470) and, when the node has a child, the
# no-op stub (NoOp_Stub_829CE870). When a node is done it destructs it (Destruct20ByteNode_82C475D8), runs the
# refcounted destruct (DestructRefCounted_82214F08), and frees it (Free_Thunk_824FE010). This is the "find this
# chain value, walk the tree, and process each node" routine for the 0x82C02 tree-walk cluster, used by 4 callers
# (sub_82C02AD0 / sub_82C02B20 / sub_82C02F68 / sub_82C03350). Part of the 0x82C02 tree-walk cluster (sibling of
# FindChainValue_82C469F0 / Tree_Process_Wrapper_82C47470 / Destruct20ByteNode_82C475D8). 99 lines, 6 callees. 4
# callers.
[entrypoint.functions."0x82C02478"]
name = "ProcessAndWalkTree_82C02478"

# A constructor that builds a node, sets its descriptors, and processes it. It takes a node (a: a descriptor at
# a->0, a link at a->4, a field at a->16), a value (b), and a flag (c). It stores the descriptor pointers into a->0
# and a->4. It then constructs the 20-byte node (Construct20ByteNode_82C474E0). It updates the descriptors again
# (a->0 / a->4 to the final .data pointers), stores the field (a->16 = b), and increments the counter
# (ConstructAndIncrementCounter_82BFF710). It then drives the sub-dispatch (sub_82C021B8 =
# ProcessAndDispatchSubVirtual2). This is the "construct this node, set its descriptors, and process it" routine for
# the 0x82C02 construct cluster, used by 4 callers (sub_82BFCEF8 and the 0x82C02/0x82C03/0x82C4B range). Part of the
# 0x82C02 construct cluster (sibling of Construct20ByteNode_82C474E0 / ProcessAndWalkTree_82C02478). 129 lines, 3
# callees. 4 callers.
[entrypoint.functions."0x82C02E88"]
name = "ConstructAndProcessNode_82C02E88"

# A destructor that destructs a descriptor, drives a virtual call, and walks the tree. It takes a structure (a: a
# descriptor at a->0, a child at a->16, a payload at a->20). It stores a descriptor pointer into a->0. It then
# destructs the payload's descriptor (DestructAndSetDescriptor_82BFF750(a + 20)). When the child (a->16) is present it
# drives a virtual method through it (reading the method at a->16->0->8 and bctrl-ing into it). It then walks the
# tree (sub_82C02478 = ProcessAndWalkTree(a)). This is the "destruct this structure's descriptor, run the virtual
# call, and walk the tree" routine for the 0x82C02 destruct cluster, used by 4 callers (sub_82BFCEF8 and the
# 0x82C02/0x82C03/0x82C4B range). Part of the 0x82C02 destruct cluster (sibling of DestructAndSetDescriptor
# _82BFF750 / ProcessAndWalkTree_82C02478 / ConstructAndProcessNode_82C02E88). 59 lines, 2 callees. 4 callers.
[entrypoint.functions."0x82C02F68"]
name = "DestructAndWalkTree_82C02F68"

# A bounds checker that verifies a 12-byte index stays within a range, trapping on violation. It takes a descriptor
# (a: a range struct at a->0) and an index (b). It reads the range struct and asserts it is non-null (trapping when
# null). It computes the 12-byte offset (b * 12, added to the base pointer) and verifies the resulting address falls
# within the valid range [range->4, range->8). When the offset is below the range's start or at or beyond its end it
# traps (a debug assert). This is the "check this 12-byte index is in bounds" routine for the 0x82BEB bounds-check
# cluster, called only by sub_82BED1B0. Part of the 0x82BEB bounds-check cluster (sibling of ProcessBoundsCheck
# _82B43E30 / ProcessVirtualBound_82A2DCC8). 47 lines, 0 callees. 1 caller: sub_82BED1B0.
[entrypoint.functions."0x82BEBF70"]
name = "BoundsCheck12Byte_82BEBF70"

# A processor that, under a critical section, searches and dispatches a value. It takes a structure (a: a vtable
# chain at a->0), a value (b), a flag (c), and an option (d). When the option (d) is zero it first runs a setup step
# (sub_82B3E0A8). It then drives a virtual call through a->0 (reading the method at a->0->0->16 and bctrl-ing into
# it). When the result (r1 + 84) is present it proceeds with the per-value handling: entering the critical section
# (CriticalSection_EnterOrTry_82200688), searching (BinarySearch_12Stride_Simple_82B429A0), finding the common 16-bit
# prefix (FindCommonPrefix16_82CAAA68), lazily getting the data (LazyGetData_822F2088), copying and dispatching
# (CopyAndDispatch_822F5478), and the fourth virtual call (VirtualCall4_8260C130). This is the "search this value and
# dispatch it" routine for the 0x82B40 dispatch cluster, called only by sub_82C65D80. Part of the 0x82B40 dispatch
# cluster (sibling of ProcessTypeDispatch_82B49D50 / BinarySearch_12Stride_Simple_82B429A0). 413 lines, 8 callees. 1
# caller: sub_82C65D80.
[entrypoint.functions."0x82B40BD0"]
name = "ProcessAndSearchDispatch_82B40BD0"

# A processor that resolves an indexed pointer, runs a sub-step, and stores the resulting values. It takes a
# structure (a: a start at a->0, an end at a->4, a context at a->8) and an index (b). It resolves the indexed pointer
# (GetIndexedPointer_82BC5448(a, b)). It then runs the per-value sub-step (sub_82BCA588(a, pointer)). It stores the
# start / end values (a->0 / a->4 from the context's tail fields). It then validates the result: when the tail length
# (a->8->-4) is below 4, or the payload's low type bits (byte 5 & 3) are clear, it skips the remaining handling. When
# valid it continues with the typed-value processing. This is the "resolve this index, run the sub-step, and store the
# result" routine for the 0x82BC5 process cluster, used by 10 callers (the 0x82BB0/0x82BB1/0x82BC7/0x82BD8/0x82BD9/0x82BDB
# /0x82BDC range). Part of the 0x82BC5 process cluster (sibling of GetIndexedPointer_82BC5448 /
# CompareIndexedPointers_82BC5940). 94 lines, 2 callees. 10 callers.
[entrypoint.functions."0x82BC5F48"]
name = "ProcessAndStoreIndexedValue_82BC5F48"

# A processor that accesses an element and, when it is a valid float, converts it. It takes a container (a), a
# structure (b), and an index (c). It first accesses the element (ElementAccess_821E1498(b, c)). When the result does
# not match the expected .data descriptor it takes the non-matching path. When it matches it performs a float
# round-trip (load the double, convert to int, then back to float - a quantize / normalize step) and stages the result
# (a 4-byte float at r1 + 80, with a 3-byte tag at r1 + 84). It then drives the state dispatch (StateDispatch3
# _82BCA3E8). This is the "access this element and, when it is a valid float, convert it" routine for the 0x82BCA
# float-convert cluster, used by 2 callers (ProcessAndStoreIndexedValue_82BC5F48 / sub_82BC8928). Part of the 0x82BCA
# float-convert cluster (sibling of ElementAccess_821E1498 / StateDispatch3_82BCA3E8). 69 lines, 2 callees. 2
# callers.
[entrypoint.functions."0x82BCA588"]
name = "ProcessAndConvertFloat_82BCA588"

# A processor that drives a global virtual call, then a per-value virtual call. It takes a value (a: a vtable chain
# at a->0, a payload at a->204). It first reads a .data function pointer: when it is non-null it invokes the global
# method (bctrl through the .data pointer). It then drives a per-value virtual call through a->0 (reading the method
# at a->0->12 and bctrl-ing into it). When that result is non-zero it continues with the per-value handling
# (sub_82B49B50). This is the "drive this global virtual call, then the per-value virtual call" routine for the
# 0x82B56 dispatch cluster, referenced by no other function. Part of the 0x82B56 dispatch cluster (sibling of
# ProcessDispatchByType_82228E28 / ProcessTypeDispatch_82B49D50). 119 lines, 1 callee. 0 callers (vtable / external).
[entrypoint.functions."0x82B56758"]
name = "ProcessAndDispatchGlobal_82B56758"

# A processor that drives a virtual call, checks a bounds condition, and sets a result. It takes a result (a: a
# value at a->0), a structure (b: a vtable chain at b->0), and a set of values (c, d, e, f, g, h). It invokes a
# virtual method through b (reading the method at b->0->24 and bctrl-ing into it). It then computes a running sum
# (the low 32 bits of e plus d) and checks the bounds: when the sum does not exceed b (the end) it takes the
# out-of-bounds path, clearing the result (a->0 = 0). When it is within bounds it continues with the per-value
# handling (sub_82B56448, allocating with Allocate_OrDbgBreak_8221F388 and dispatching with SetAndDispatch
# _82366210). This is the "drive this virtual call, check the bounds, and set the result" routine for the 0x82B4F
# dispatch cluster, referenced by no other function. Part of the 0x82B4F dispatch cluster (sibling of
# SetAndDispatch_82366210 / DecrementRefAtOffset_82B4F370). 202 lines, 3 callees. 0 callers (vtable / external).
[entrypoint.functions."0x82B4F678"]
name = "ProcessAndDispatchBounds_82B4F678"

# A constructor that builds a structure, sets its descriptors, and processes it - creating an SDK event when valid.
# It takes a structure (a: a descriptor at a->0, a link at a->4, a flag at a->8, a value at a->12) and a value (b),
# plus several more values (c, d, e, f, g). It stores the descriptor pointers into a->0 and a->4, zeroes the flag
# (a->8 = 0), and stores the value (a->12 = b). When the value (b) is present it proceeds with the per-value
# handling: creating the SDK event (CreateEvent_SdkRuntime_82CBC4B8) and driving the follow-up processing
# (sub_82B56D58). This is the "construct this structure, and when the value is present create its event and process
# it" routine for the 0x82B56 construct cluster, called only by ProcessAndDispatchBounds_82B4F678. Part of the
# 0x82B56 construct cluster (sibling of CreateEvent_SdkRuntime_82CBC4B8 / ProcessAndDispatchBounds_82B4F678). 289
# lines, 2 callees. 1 caller: ProcessAndDispatchBounds_82B4F678.
[entrypoint.functions."0x82B56448"]
name = "ConstructAndProcessEvent_82B56448"

# An assign that sets a refcounted field at offset 176 of a structure. It takes a structure (a: a refcounted field
# at a->176). It computes the field address (a + 176) and assigns the refcounted value to it (AssignRefCounted
# _822F54C8(a + 176)). It then returns 0. This is the "assign the refcounted value into this structure's field 176"
# routine for the 0x82B56 field-assign cluster, referenced by no other function. Part of the 0x82B56 field-assign
# cluster (sibling of AssignRefCounted_822F54C8 / ProcessField_82B51380). 28 lines, 1 callee. 0 callers (vtable /
# external).
[entrypoint.functions."0x82B56D30"]
name = "AssignField_176_82B56D30"

# A processor that, when a field is null, zeroes a 24-byte region and stores it into the structure. It takes a
# structure (a: fields at a->96, a->112, a->120) and a value (b: a pointer at b->0). When the value's pointer (b->0)
# is non-null it takes the present path. When it is null it zeroes a 24-byte region on the stack (r1 + 80 through
# r1 + 104) and stores it into the structure's fields (a->96, a->112, a->120). It then continues with the per-structure
# handling, allocating where needed (Allocate_OrDbgBreak_8221F388). This is the "zero this structure's fields when
# the value is absent, and process the structure" routine for the 0x82B56 struct-field cluster, called only by
# ConstructAndProcessEvent_82B56448. Part of the 0x82B56 struct-field cluster (sibling of
# ConstructAndProcessEvent_82B56448 / ProcessStructFields_82B59D28). 375 lines, 1 callee. 1 caller:
# ConstructAndProcessEvent_82B56448.
[entrypoint.functions."0x82B56D58"]
name = "ProcessAndZeroStructFields_82B56D58"

# A processor that validates a condition and, when it fails, runs an abort / trap sequence. It takes a result (a: a
# value at a->0), a value (b), and a value (c). When the result (a) is null and the value (b) is also null it runs the
# abort path: it logs the failure (sub_82CAB770), stores a status code (a->0 = 22), and raises the fault (sub_82CAB630
# with a zeroed argument set), then returns 22. When the result is present but the value (b) is non-zero it handles the
# present-but-invalid case (zeroing the low byte of a). This is the "validate this condition, aborting with status 22
# when it fails" routine for the 0x82170 abort cluster, used by 10 callers (sub_82170DA8 and the 0x822EF/0x822FC/0x82373
# /0x82BF1/0x82BF2/0x82BF3/0x83000/0x83003 range). Part of the 0x82170 abort cluster (sibling of Allocate_OrDbgBreak
# _8221F388 / sub_82CAB630). 134 lines, 2 callees. 10 callers.
[entrypoint.functions."0x82170CC8"]
name = "ProcessAndAbort_82170CC8"

# A processor that toggles a flag, performs a hash lookup, and, when found, performs a second hash lookup. It takes a
# structure (a: a index at a->32, a state at a->36). It first toggles the state bit (a->36 = a->36 ^ 1). It then
# performs the hash lookup (HashLookup_82B49678(a)). When the result's low byte is zero it skips the second lookup.
# When it is present it computes the bucket (the state's upper bits, a->36 >> 5, offset into the structure) and reads
# the bucket's table (r11->40). It then performs the second hash lookup (HashLookup2_82B497B8(bucket + 40, ...)).
# This is the "toggle this flag, hash-lookup, and on a hit do the second lookup" routine for the 0x82B49 hash cluster,
# used by 2 callers (ProcessChunk_TableDispatch_82B49110 / sub_82B49320). Part of the 0x82B49 hash cluster (sibling of
# HashLookup_82B49678 / HashLookup2_82B497B8 / HashLookupWithIndex_82B49908). 79 lines, 2 callees. 2 callers.
[entrypoint.functions."0x82B49730"]
name = "ProcessHashLookup_82B49730"

# A counter increment: atomically increments a global .data counter and stages the result. It takes a result (a: a
# value at a->0), a value (b), and a value (c). It first zeroes the result (a->0 = 0). It then performs an atomic
# read-modify-write on a .data counter (a load-and-reserve (lwarx) and store-conditional (stwcx) pair under the
# global lock, looping until it succeeds), incrementing the counter by one. It then stages the result and drives the
# follow-up handling (copy-assigning a refcounted value (CopyAssign_RefCounted2_82265160) and growing a bit array
# (ProcessGrowBitArray_821EC668)). This is the "atomically bump this global counter and set up the result" routine
# for the 0x821E2 counter cluster, used by 10 callers (the 0x82405/0x82415/0x8247B/0x824E7/0x825AD/0x825F2/0x82617/0x828F5
# /0x828FB/0x8323D range). Part of the 0x821E2 counter cluster (sibling of ProcessAndIncrementCounter_822D6B40 /
# IncrementAndCheckThreshold_8219EE00). 81 lines, 2 callees. 10 callers.
[entrypoint.functions."0x821E2CC8"]
name = "IncrementGlobalCounter2_821E2CC8"

# A setter that stores a value into field 12 of a structure, gated on a condition. It takes a structure (a: a guard
# at a->8, a value at a->12) and a value (b). When the guard (a->8) is null it returns without doing anything. When
# the guard is present it stores the value into the field (a->12 = b). This is the "set this structure's field 12, only
# when its guard is present" routine for the 0x82C62 field-set cluster, referenced by no other function. Part of the
# 0x82C62 field-set cluster (sibling of SetField_72_82B44358 / ProcessField_82B51380). 14 lines, 0 callees. 0 callers
# (vtable / external).
[entrypoint.functions."0x82C62FB0"]
name = "SetField_12_82C62FB0"

# A processor that copies a struct region, recomputing offsets as it goes. It takes a structure (a: a count at a->4,
# a source at a->12, a length at a->20), a destination (b), and an offset (c). When the length (a->20) is
# non-positive it skips the copy. Otherwise it copies the region (MemCpy_SdkRuntime_82CA2C60(b, a->12, a->20)) and
# then recomputes the offsets - advancing the source (a->12 + a->20), the destination (a->20 + b), and the remaining
# length (c - a->20). It recurses for the remaining data (sub_82A1B480). This is the "copy this struct region and
# recompute the offsets, recursing for the rest" routine for the 0x82A1B struct-copy cluster, used by 10 callers
# (CopyStructData3_8322EFB0 / CopyStructData7_83214990 / CopyStructData12_83213D10 and the 0x82A1B/0x82A85/0x82B25/
# 0x8321D/0x8322F range). Part of the 0x82A1B struct-copy cluster (sibling of CopyStructData_83214990 family /
# MemCpy_SdkRuntime_82CA2C60). 216 lines, 2 callees. 10 callers.
[entrypoint.functions."0x82A1B480"]
name = "ProcessAndCopyStructRegion_82A1B480"

# A processor that drives a virtual call, computes a clamped offset, and stores the result. It takes a structure (a:
# a vtable chain at a->0, a position at a->8, a node at a->28, a bound at a->36), a value (b), and a result (c: a
# value at c->0). It invokes a virtual method through a->0 (reading the method at a->0->8 and bctrl-ing into it).
# It then computes the offset (a->8 minus the returned value) and clamps it against the bound (a->36): when the
# offset exceeds the bound it uses the bound instead. It stores the result (c->0 = the clamped offset's source). It
# then continues with the per-node handling (reading the node's fields at a->28->0->20). This is the "drive this
# virtual call, clamp the offset, and store the result" routine for the 0x82A1C offset-clamp cluster, referenced by
# no other function. Part of the 0x82A1C offset-clamp cluster (sibling of ClampRange_822F6480 / ProcessClampOffset
# family). 89 lines, 0 callees. 0 callers (vtable / external).
[entrypoint.functions."0x82A1CF98"]
name = "ProcessAndClampOffset_82A1CF98"

# A comparator that returns 1 when a value is below a bound, else 0. It takes a structure (a: a bound at a->36) and
# a value (b). It reads the bound (a->36) and compares the value (b) against it: when the value (b) is strictly below
# the bound it returns 1; otherwise (b at or above the bound) it returns 0. This is the "is this value below the
# bound" predicate for the 0x82A1D bound-compare cluster, referenced by no other function. Part of the 0x82A1D
# bound-compare cluster (sibling of ProcessAndClampOffset_82A1CF98 / ClampRange_822F6480). 19 lines, 0 callees. 0
# callers (vtable / external).
[entrypoint.functions."0x82A1D030"]
name = "IsBelowBound_82A1D030"

# A constructor that zeroes a structure, sets its flags, and initializes a critical section. It takes a structure (a:
# a descriptor at a->0, a count at a->4, two flags at a->8 / a->9, several fields at a->12 / a->16 / a->20 / a->24 /
# a->36, a critical section at a->40, a flag at a->68) and two values (b, c). It zeroes the fields (a->4, a->12,
# a->16, a->20, a->24, a->36, a->68 all to 0), sets the two flag bytes (a->8 = 1, a->9 = 1), and stores the
# descriptor pointer (a->0). It then initializes the critical section (RtlInitializeCriticalSection(a + 40)) and
# drives the per-structure setup (allocating (Allocate_OrDbgBreak_8221F388), assigning refcounted values (Assign
# RefCounted_822F54C8), the fourth virtual call (VirtualCall4_8260C130), and the follow-up handling). This is the
# "construct this structure, zeroing it and initializing its critical section" routine for the 0x82B43 construct
# cluster, called only by sub_82B43B08. Part of the 0x82B43 construct cluster (sibling of
# CriticalSection_EnterOrTry_82200688 / ProcessAndFreeIfFlagged3_82B437B0). 417 lines, 7 callees. 1 caller:
# sub_82B43B08.
[entrypoint.functions."0x82B434C0"]
name = "ConstructAndInitCritical_82B434C0"

# A constructor that, when a value is present, allocates 80 bytes and constructs the structure. It takes a result (a:
# a pointer at a->0), a value (b), and a value (c). When the value (b) is null it stores a null pointer (a->0 = 0) and
# returns. When it is present it allocates the 80-byte structure (Allocate_OrDbgBreak_8221F388(80)) and, when the
# allocation succeeds, constructs it (sub_82B434C0 = ConstructAndInitCritical(allocated, b, c)). When the allocation
# fails it stores a null pointer. This is the "allocate this 80-byte structure and construct it, or null when absent"
# routine for the 0x82B43 construct cluster, used by 9 callers (the 0x824EA/0x825F3/0x8260B/0x8292E/0x82B28/0x82BFD/0x82C63
# /0x82C65/0x82C68 range). Part of the 0x82B43 construct cluster (sibling of ConstructAndInitCritical_82B434C0 /
# Allocate_OrDbgBreak_8221F388). 81 lines, 2 callees. 9 callers.
[entrypoint.functions."0x82B43B08"]
name = "ConstructAndAllocate_82B43B08"

# A selector that returns one of two .data constant pointers based on a flag. It takes a value (a) and a flag (b).
# When the flag (b) is exactly 1 it returns the "true" constant pointer (.data base + 19936). When the flag is any
# other value (0 or greater than 1) it returns the "false" constant pointer (.data base + 19932). This is the
# "select between these two constant pointers based on the flag" routine for the 0x82C3B constant-select cluster,
# referenced by no other function. Part of the 0x82C3B constant-select cluster (sibling of SelectConstantPointer
# _82C23858 / SelectConstantPointer2_82C20CA0). 25 lines, 0 callees. 0 callers (vtable / external).
[entrypoint.functions."0x82C3B778"]
name = "SelectConstantPointer3_82C3B778"

# A processor that constructs a 12-byte value, then resets the structure's fields. It takes a structure (a: a
# index at a->544, a flag at a->548, a value at a->560). When the index (a->544) is -1 it skips the construction.
# Otherwise it resolves a .data entry and constructs the 12-byte value (Construct12Byte_825F7B10, using the entry's
# offset + 144 and + 148 fields). It then resets the structure's fields (a->544 = -1, a->560 = -1, a->548 = 1). This
# is the "construct this 12-byte value (when the index is valid) and reset the fields" routine for the 0x82814
# construct cluster, used by 7 callers (HashNamedEntry_82188B88 and the 0x821C9/0x82283/0x823FB/0x82814/0x828F6 range).
# Part of the 0x82814 construct cluster (sibling of Construct12Byte_825F7B10 / ConstructVectorStruct_8323F1B8). 65
# lines, 1 callee. 7 callers.
[entrypoint.functions."0x82814B38"]
name = "ProcessAndResetFields_82814B38"

# A processor that sets up an exception handler via setjmp, restoring the values when the jump fires. It takes a
# structure (a: a saved pointer at a->92), a value (b), and a value (c). It saves the three values onto the stack (a
# at r1 + 1492, b at r1 + 1500, c at r1 + 1508). It then swaps a->92 to a temporary buffer and issues a setjmp (the
# exception-establishment point, at r1 + 96). When the setjmp returns non-zero (the jump was taken) it takes the
# exception path; when it returns zero (first time) it restores the three saved values (a, b, c) from the stack and
# continues with the normal processing. This is the "establish this exception handler, restoring the values on a
# jump" routine for the 0x822C0 setjmp cluster, used by 4 callers (sub_822C05F8 and the 0x822DD/0x82BB1/0x82BC7 range).
# Part of the 0x822C0 setjmp cluster (sibling of CsGuarded_HandlerDispatch_82B43CF0). 88 lines, 0 callees. 4
# callers.
[entrypoint.functions."0x822C0568"]
name = "ProcessAndSetjmp_822C0568"

# A processor that dispatches three positions, each from a different field with a different stride. It takes a value
# (a) and a structure (b: fields at b->8, b->12, b->16, b->40, b->44, b->52). It dispatches three positions via
# DispatchAndAdjustPos_8227BB58, each reading a different field pair: the first (b->12 pointer, b->44 index with a
# 4-byte stride, i.e. index << 2), the second (b->16 pointer, b->52 index with a 4-byte stride), and the third (b->8
# pointer, b->40 index with an 8-byte stride, i.e. index << 3). This is the "dispatch these three positions (each
# from its own field/stride)" routine for the 0x82BCC position-dispatch cluster, called only by DispatchOnKind7_82BC
# B480. Part of the 0x82BCC position-dispatch cluster (sibling of DispatchOnKind7_82BCB480 / DispatchAndAdjustPos
# _8227BB58). 124 lines, 1 callee. 1 caller: DispatchOnKind7_82BCB480.
[entrypoint.functions."0x82BCC970"]
name = "ProcessAndDispatchPositions_82BCC970"

# A selector that returns one of two .data constant pointers based on a flag. It takes a value (a) and a flag (b).
# When the flag (b) is zero it returns the "null" constant pointer (.data base + 19824). When the flag is non-zero it
# returns the "set" constant pointer (.data base + 19820). This is the "select between these two constant pointers
# based on the flag" routine for the 0x82C27 constant-select cluster, referenced by no other function. Part of the
# 0x82C27 constant-select cluster (sibling of SelectConstantPointer_82C23858 / SelectConstantPointer3_82C3B778). 21
# lines, 0 callees. 0 callers (vtable / external).
[entrypoint.functions."0x82C27FC0"]
name = "SelectConstantPointer4_82C27FC0"

# A selector that returns one of two .data constant pointers based on a flag. It takes a value (a) and a flag (b).
# When the flag (b) is exactly 1 it returns the "true" constant pointer (.data base + 19844). When the flag is any
# other value (0 or greater than 1) it returns the "false" constant pointer (.data base + 19848). This is the
# "select between these two constant pointers based on the flag" routine for the 0x82C2A constant-select cluster,
# referenced by no other function. Part of the 0x82C2A constant-select cluster (sibling of SelectConstantPointer
# _82C23858 / SelectConstantPointer4_82C27FC0). 25 lines, 0 callees. 0 callers (vtable / external).
[entrypoint.functions."0x82C2A6D0"]
name = "SelectConstantPointer5_82C2A6D0"

# A processor that drives a virtual call and returns a success/failure result. It takes no arguments. It resolves a
# virtual method from a global .data entry (the method at the entry's + 4 field) and invokes it (bctrl). When the
# result is non-negative (success) it returns 1. When the result is negative (failure) it invokes the failure handler
# (sub_82CC1C38) and returns 0. This is the "drive this virtual call, returning 1 on success and 0 (with the failure
# handler) on failure" routine for the 0x82CBB vcall-check cluster, used by 10 callers (the 0x822FA/0x82368/0x82A39/
# 0x82A47/0x82B44/0x82B4F/0x82BA0/0x82CBB/0x83002/0x830C5 range). Part of the 0x82CBB vcall-check cluster (sibling
# of sub_82CC1C38 / the 0x82C2A virtual-call cluster). 47 lines, 1 callee. 10 callers.
[entrypoint.functions."0x82CBBF60"]
name = "ProcessAndCheckSuccess_82CBBF60"

# A processor that establishes an exception guard, then drives the guarded follow-up dispatch. It takes a structure
# (a: a start at a->20, a base at a->32, an end at a->40, a flag at a->52, a byte at a->55, a saved return at a->96),
# a value (b), and a value (c). It saves the return value (a->96 = c) and reads the structure's fields (a->20, a->32,
# a->40, a->52, a->55). It establishes the exception guard (ProcessAndSetjmp_822C0568). When the guarded processing
# succeeds (non-zero result) it computes the target (a->32 + b) and drives the guarded dispatch (sub_82BCC6F0, plus
# the follow-up sub_82BCCAB8 and sub_82BCCDE0); when it fails it takes the exception path. This is the "establish this
# guard, then dispatch the guarded processing" routine for the 0x822C0 guarded-dispatch cluster, used by 10 callers
# (the 0x82193/0x821D2/0x82306/0x82BB1/0x82BBC/0x82BD0/0x82BD6 range). Part of the 0x822C0 guarded-dispatch cluster
# (sibling of ProcessAndSetjmp_822C0568 / CsGuarded_HandlerDispatch_82B43CF0). 114 lines, 4 callees. 10 callers.
[entrypoint.functions."0x822C05F8"]
name = "ProcessAndGuardedDispatch_822C05F8"

# A processor that reports an error, adjusts a counter, and updates a linked list. It takes two values (a and c: a
# counter at c->8), three values (b, d, e), and a value (f). When the two values (a, c) differ it reports the error
# (ProcessError_82A0F630(f)) and adjusts the counter (c->8 -= f). It then updates the linked list (resolving the
# next/previous links at the values' + 4 fields and splicing them together). This is the "report the mismatch, adjust
# the counter, and relink the list" routine for the 0x82A0F counter-adjust cluster, used by 2 callers (Construct48Byte
# Node2_82A0EDF0 and sub_82A0F3B8). Part of the 0x82A0F counter-adjust cluster (sibling of ProcessError_82A0F630 /
# ProcessAndResolvePointer3_82A0F160). 85 lines, 1 callee. 2 callers.
[entrypoint.functions."0x82A0F528"]
name = "ProcessAndAdjustCounter_82A0F528"

# A processor that computes an aligned size, clamps it, and allocates. It takes a value (a), a count (b), and a
# result (c: a pointer at c->0). It computes the aligned size ((b << 1 + 5) rounded up to a multiple of 4, i.e.
# 2*count + 5 header bytes, 4-byte aligned). When the size exceeds the allocation bound (0x7FFFFF | 0xFFFF) it uses
# -1 (allocation failure); otherwise it doubles the size. It then performs the allocation (Allocate_OrDbgBreak
# _8221F388(size)) and stores the result (c->0 = the allocated pointer). This is the "compute this aligned size,
# clamp it, and allocate the buffer" routine for the 0x82B39 allocate cluster, used by 3 callers (the 0x82B393B / 0x82
# B395D / 0x82B39DD range). Part of the 0x82B39 allocate cluster (sibling of ProcessAndAllocate2_82B39110 /
# Allocate_OrDbgBreak_8221F388). 57 lines, 1 callee. 3 callers.
[entrypoint.functions."0x82B39BE8"]
name = "ProcessAndAllocateAligned_82B39BE8"

# A processor that iterates the elements of a collection, driving a virtual call for each. It takes a collection (a:
# an element array at a->0, a count at a->8). When the count (a->8) is 1 or fewer it skips the iteration. Otherwise it
# walks the elements (each with a 4-byte stride from a->0): for each element it resolves the virtual method (the
# method at the element's chain) and invokes it (bctrl), comparing the element's value case-insensitively (StrCmpI
# gnoreCase_82CA6320). It then advances to the next element (the sibling at the previous element's - 4 / + 4 fields).
# This is the "iterate these elements, driving a virtual call for each" routine for the 0x82C15 element-iterate
# cluster, used by 2 callers (sub_82C1B920 and sub_82C1C170). Part of the 0x82C15 element-iterate cluster (sibling
# of StrCmpIgnoreCase_82CA6320). 120 lines, 1 callee. 2 callers.
[entrypoint.functions."0x82C15C10"]
name = "ProcessAndIterateElements_82C15C10"

# A processor that frees the old buffer, reallocates it larger, and updates the size fields. It takes a buffer (a: a
# pointer at a->0, a size at a->4, a capacity at a->8), a value (b), and a count (c). When the buffer is present
# (a->0 non-null) it frees the old buffer (Free_SizeBucketed_Thunk_8221BE68(a->0)) and clears the pointer (a->0 = 0).
# It then reallocates the buffer to the new (larger) aligned size (ProcessAndAllocateAligned_82B39BE8 = sub_82B39BE8),
# copies the existing data over (MemCpy_Vectorized_822085D0), zeroes the new region (MemSet_SdkRuntime_82CA3190), and
# updates the size fields (a->4 = c, a->8 = the new capacity). This is the "free this buffer, reallocate it larger,
# and update the size" routine for the 0x82B39 reallocate cluster, used by 7 callers (ProcessAndIncrementCounter
# _822D6B40 and the 0x82B38/0x82B39 range). Part of the 0x82B39 reallocate cluster (sibling of GrowAndZeroBuffer
# _82BC55B8 / ProcessAndAllocateAligned_82B39BE8). 92 lines, 4 callees. 7 callers.
[entrypoint.functions."0x82B39DD8"]
name = "ProcessAndReallocateBuffer_82B39DD8"

# A processor that leaves the critical section when the lock is held. It takes a lock (a: a critical section at a->0,
# a held flag at a->4). When the held flag (a->4) is zero (lock not held) it does nothing. When it is set it leaves the
# critical section (RtlLeaveCriticalSection(a->0)) and clears the flag (a->4 = 0). This is the "leave this critical
# section (when held) and clear the flag" routine for the 0x821C3 critical-leave cluster, used by 10 callers (UIText
# _FrameRenderIter and the 0x82BF9/0x82C08/0x82C52/0x82C6F/0x82C72/0x82C7F/0x82C80 range). Part of the 0x821C3
# critical-leave cluster (sibling of CriticalSection_EnterOrTry_82200688 / RtlInitializeCriticalSection). 43 lines,
# 0 callees. 10 callers.
[entrypoint.functions."0x821C3628"]
name = "ProcessAndLeaveCritical_821C3628"

# A processor that reads a float value (converting it through double), then processes the elements. It takes a value
# (a), a structure (b: a start at b->4, an end at b->8), and a value (c). It reads the start (b->4) and end (b->8),
# computes the count ((b->8 - b->4) / 8, i.e. 8-byte elements), and reads the element just before the end (b->8 - 8),
# resolving the float value (at the element's + 4 field, converted int -> double -> float). It then, when the count
# is positive, iterates the elements processing each (the per-element handling, and the subsystem initialization
# InitializeSubsystems_83232BB8). This is the "read this float value (with conversion) and process the elements"
# routine for the 0x82A13 float-process cluster, called only by ProcessDispatchValue2_832351B0. Part of the 0x82A13
# float-process cluster (sibling of ProcessDispatchValue2_832351B0 / ProcessAndConvertFloat_82BCA588). 365 lines, 1
# callee. 1 caller: ProcessDispatchValue2_832351B0.
[entrypoint.functions."0x82A139D0"]
name = "ProcessAndConvertFloatElements_82A139D0"

# A processor that acquires a lock, dispatches a guarded search, and releases the lock. It takes a value (a), a
# structure (b: a cached result at b->100, a lock at b + 108), a value (c), and a value (d). It acquires the lock
# (CriticalSection_EnterOrTry_82200688). When the cached result (b->100) is present it returns it; when it is null it
# dispatches the search (ProcessAndSearchDispatch_82B40BD0), performing the tree lookup (Tree_Search2_82C343A0),
# lazy data fetch (LazyGetData_822F2088), and the per-result construction (ConstructAndAllocate_82B43B08) and virtual
# dispatch (VirtualCall4_8260C130), then releases the lock (ProcessAndLeaveCritical_821C3628 = sub_821C3628). This is
# the "acquire the lock, dispatch the guarded search, and release" routine for the 0x82C65 guarded-search cluster,
# referenced by no other function. Part of the 0x82C65 guarded-search cluster (sibling of ProcessAndSearchDispatch
# _82B40BD0 / CriticalSection_EnterOrTry_82200688). 455 lines, 14 callees. 0 callers (vtable / external).
[entrypoint.functions."0x82C65D80"]
name = "ProcessAndGuardedSearch_82C65D80"

# A processor that zeroes the fields, sets a descriptor, and computes the element count. It takes a structure (a: a
# descriptor at a->0, a count at a->4, several fields at a->12 / a->16 / a->20 / a->28 / a->32 / a->36, a flag at a->40)
# and a value (b: a 68-byte element array at b->36 + 16, with a start at + 20 and an end at + 24). It zeroes the fields
# (a->4, a->12, a->16, a->20, a->28, a->32, a->36 all to 0) and sets the descriptor pointer (a->0) and the flag (a->40 = 0).
# It then computes the element count: (end - start) / 68 (the number of 68-byte elements in the array). It also applies
# the transform (ProcessTransform_826FAFD0) and sets the value (SetValue3_82A09FB0). This is the "zero these fields, set
# the descriptor, and compute the element count" routine for the 0x83240 count-compute cluster, called only by
# ProcessGrowAppend8_83221378. Part of the 0x83240 count-compute cluster (sibling of ProcessGrowAppend8_83221378 /
# ProcessTransform_826FAFD0). 183 lines, 2 callees. 1 caller: ProcessGrowAppend8_83221378.
[entrypoint.functions."0x832402B8"]
name = "ProcessAndComputeCount_832402B8"

# A processor that, when a flag is set or the type matches, dispatches the follow-up exception handler with a tag.
# It takes a structure (a: a flags at a->20, a type at a->379), a value (b), and a value (c). When the flag (a->20 &
# 0x40000) is set or the type (a->379) matches the expected value (r3), it dispatches the follow-up exception handling
# (the per-type processing, with the tag 1479, and the zeroing MemSet_SdkRuntime_82CA3190). It also resolves the current
# process type (KeGetCurrentProcessType) and drives the tail-call dispatch (TailCall_82238FCC). This is the "when this
# flag/type matches, dispatch the follow-up exception handler with the tag" routine for the 0x82238 exception-dispatch
# cluster, used by 10 callers (Exception_82CC02FC / Exception_82CC031C / MemFree_82CBFD74 / MemFree_82CBFD9C and the
# 0x82238/0x825AA/0x82CBB/0x82CBC range). Part of the 0x82238 exception-dispatch cluster (sibling of Exception_82CC02FC
# / TailCall_82238FCC). 1150 lines, 5 callees. 10 callers.
[entrypoint.functions."0x82238790"]
name = "ProcessAndDispatchException_82238790"

# An (unresolved) exception-frame return routine called after RtlRaiseException. It takes the saved exception frame
# (the 320-byte frame, passed in r12). It is the continuation of the exception/heap-error path: after the exception
# has been raised (RtlRaiseException), it cleans up and returns through the frame (restoring r1 to the frame top and
# returning the result). Its body is not recovered in this recompile (it is an unresolved bl target, flagged
# REX_FATAL at each call site), so it is named from its call context only. This is the "return through this exception
# frame after raising the exception" routine for the 0x82239 exception-return cluster, called from 3 functions
# (Heap_AllocateBlock_82238828 / ProcessAndDispatchException_82238790 / TailCall_82238FCC) across 5 call sites. Part of
# the 0x82239 exception-return cluster (sibling of Heap_AllocateBlock_82238828 / TailCall_82238FCC / RtlRaiseException).
# Unresolved target, 0 callees. 3 callers (5 call sites).
[entrypoint.functions."0x8223900C"]
name = "Exception_ReturnFrame_8223900C"

# A processor that resolves a global pointer, initializing the subsystems when it is missing. It takes a value (a, a
# bound / flag). When the value (a) is above the bound (-4096) it takes the alternate path. Otherwise it resolves the
# global pointer (GetGlobalPointer_82239798); when the pointer is missing (null) it drives the one-time initialization
# (sub_82CACB00, the 30-argument and 255-argument setup sub_82CACAB8 / sub_82CA95C0, and the follow-up sub_82CAB770 /
# sub_82CACC10), then proceeds. This is the "resolve this global pointer, initializing the subsystems when absent"
# routine for the 0x82CA3 global-resolve cluster, used by 10 callers (the 0x829FC/0x82B57/0x82BF2/0x82CAE/0x82CB6/
# 0x82FE5/0x83000/0x83001/0x83230 range). Part of the 0x82CA3 global-resolve cluster (sibling of GetGlobalPointer
# _82239798 / ProcessAndDispatchException_82238790). 129 lines, 7 callees. 10 callers.
[entrypoint.functions."0x82CA3C68"]
name = "ProcessAndResolveGlobal_82CA3C68"

# An allocation thunk that forwards the size to the allocator. It takes a size (a) and performs the allocation
# (Allocate_OrDbgBreak_8221F388(size)), returning the result. It is a one-line pass-through wrapper around
# Allocate_OrDbgBreak_8221F388 (the debug-break-on-failure allocator). This is the "allocate this size (via the
# debug-break allocator)" thunk for the 0x8217D allocate cluster, used by 10 callers (the 0x82ED0/0x82EE6/0x82ECA/
# 0x8300D/0x8300E/0x83010/0x8304A/0x830EA/0x830EF range). Part of the 0x8217D allocate cluster (sibling of
# Allocate_OrDbgBreak_8221F388). 7 lines, 1 callee. 10 callers.
[entrypoint.functions."0x8217DD10"]
name = "Allocate_Thunk_8217DD10"

# A processor that computes the 8-byte element offset when the index is in range. It takes a collection (a: an element
# base at a->16, a count at a->32, and auxiliary fields at a->8 / a->20) and an index (b). When the index (b - 1) is
# below the count (a->32) it computes the element address (a->16 + b * 8 - 8, the 8-byte-stride element) and returns it.
# When the index is out of range it takes the out-of-bounds path (reading the auxiliary fields and comparing the value
# against a .data float constant). This is the "compute this 8-byte element offset (when in range)" routine for the
# 0x82229 element-offset cluster, used by 10 callers (DispatchValueType_822285D8 and the 0x82229/0x827BF/0x82A21/0x82
# A23/0x82A28/0x82A2F range). Part of the 0x82229 element-offset cluster (sibling of DispatchValueType_822285D8). 104
# lines, 0 callees. 10 callers.
[entrypoint.functions."0x82229260"]
name = "ProcessAndComputeElementOffset_82229260"

# A counter that counts the non-zero bytes, stopping early on a zero. It takes a byte array (a) and a maximum (b).
# It walks the bytes up to the maximum (b), counting the non-zero ones: for each byte, when it is zero it stops early
# (returning the count so far); when it is non-zero it increments the count and advances. It returns the number of
# non-zero bytes counted (up to b, or up to the first zero byte). This is the "count these non-zero bytes (up to the
# max, stopping early on a zero)" routine for the 0x82CA3 byte-count cluster, used by 2 callers (sub_8230BB78 and
# sub_82CA5F20). Part of the 0x82CA3 byte-count cluster (sibling of StrCmpIgnoreCase_82CA6320 / ProcessAndResolve
# Global_82CA3C68). 30 lines, 0 callees. 2 callers.
[entrypoint.functions."0x82CA3BC0"]
name = "CountNonZeroBytes_82CA3BC0"

# A processor that, when the value is null, reports an error (code 22); otherwise counts the non-zero bytes. It takes
# a byte array (a) and a maximum (b). When the array (a) is null it drives the error path (sub_82CAB770, the error
# report sub_82CAB630, storing the code 22) and returns 22. When the array is present it counts the non-zero bytes
# (CountNonZeroBytes_82CA3BC0 = sub_82CA3BC0(a, b)) and returns the count. This is the "count these non-zero bytes, or
# report the error 22 when the array is null" routine for the 0x82CA5 byte-count cluster, called only by sub_82CA5FE0.
# Part of the 0x82CA5 byte-count cluster (sibling of CountNonZeroBytes_82CA3BC0 / StrCmpIgnoreCase_82CA6320). 110
# lines, 3 callees. 1 caller: sub_82CA5FE0.
[entrypoint.functions."0x82CA5F20"]
name = "ProcessAndCountOrError_82CA5F20"

# A thunk that forwards to the count-or-error routine. It takes a byte array (a) and a maximum (b), sets up the
# default argument (r5 from a .data entry), and forwards to ProcessAndCountOrError_82CA5F20 = sub_82CA5F20(a, b, r5),
# returning the result. It is a thin wrapper that adds the default third argument before calling the count-or-error
# processor. This is the "count-or-error thunk (adding the default argument)" routine for the 0x82CA5 byte-count
# cluster, used by 5 callers (the 0x82BEB/0x82BED/0x82BF1/0x82BF3 range). Part of the 0x82CA5 byte-count cluster
# (sibling of ProcessAndCountOrError_82CA5F20 / CountNonZeroBytes_82CA3BC0). 11 lines, 1 callee. 5 callers.
[entrypoint.functions."0x82CA5FE0"]
name = "CountOrError_Thunk_82CA5FE0"

# A processor that drives a virtual call, then reads the indexed element. It takes a value (a), a vtable container (b:
# a vtable at b->0, with the method at b->0->4), and an index (c). It drives the virtual method (bctrl into b->0->4).
# When the result is null it returns. When it is present it reads the element array (the array at the result's + 4
# field) and reads the indexed element (array[c], the 4-byte-stride element at c * 4). This is the "drive this virtual
# call and read the indexed element (when the result is present)" routine for the 0x82C2A vcall cluster, referenced by
# no other function. Part of the 0x82C2A vcall cluster (sibling of SelectConstantPointer5_82C2A6D0 / ProcessAnd
# ReadIndexed family). 65 lines, 0 callees. 0 callers (vtable / external).
[entrypoint.functions."0x82C2A9D8"]
name = "ProcessAndReadIndexed_82C2A9D8"

# A processor that drives a sequence of virtual calls, checking each result. It takes a value (a) and a target (b).
# It drives the first virtual call group (VcallTriple_AllSuccess_82C011E8 = the all-success triple vcall, with a .data
# selector and the value a) and checks the result; when it is non-zero it drives the next sequence (ProcessVcall
# Sequence3_82C14D70, with the value at a + 4) and checks its result. When a check yields zero it takes the failure
# path. This is the "drive this sequence of virtual calls, checking each result" routine for the 0x82C2A vcall-seq
# cluster, called only by sub_82C2ABE0. Part of the 0x82C2A vcall-seq cluster (sibling of VcallTriple_AllSuccess
# _82C011E8 / ProcessVcallSequence3_82C14D70). 92 lines, 2 callees. 1 caller: sub_82C2ABE0.
[entrypoint.functions."0x82C2AA58"]
name = "ProcessAndDispatchVcallSequence_82C2AA58"

# A processor that drives a virtual call, then dispatches the vcall sequence. It takes a vtable container (a: a vtable
# at a->0, with methods at a->0->24 and a->0->28) and a value (b). It drives the first virtual method (bctrl into
# a->0->24) and checks the result; when it is non-zero it dispatches the vcall sequence (ProcessAndDispatchVcall
# Sequence_82C2AA58 = sub_82C2AA58(a, b)) and checks that result. When a check yields zero it takes the failure path;
# when both succeed it drives the follow-up virtual method (bctrl into a->0->28). This is the "drive this virtual call
# and dispatch the vcall sequence (when the result is non-zero)" routine for the 0x82C2A vcall-group cluster, called
# only by sub_82C2B430. Part of the 0x82C2A vcall-group cluster (sibling of ProcessAndDispatchVcallSequence_82C2AA58).
# 86 lines, 1 callee. 1 caller: sub_82C2B430.
[entrypoint.functions."0x82C2ABE0"]
name = "ProcessAndDispatchVcallGroup_82C2ABE0"

# A processor that reads .data values and, when the value is in range, updates the global pointer. It takes a value
# (a). It reads the global state from .data (a count, a base, and a bound at the 18484 / 18488 / 18492 / 18496 fields).
# When the count is present and the value (a) is within the bound it updates the global pointer (advancing the base by
# the value). When the count is absent it resolves the alternate path (the one-time setup via ProcessAndResolveGlobal
# _82CA3C68, plus the follow-up handling). This is the "read the .data state, and when the value is in range, update the
# global pointer (resolving the alternate path otherwise)" routine for the 0x82BF2 global-update cluster, used by 10
# callers (the 0x82BEB/0x82BEC/0x82BEE/0x82BEF/0x82BF0/0x82BF1/0x82BF2 range). Part of the 0x82BF2 global-update
# cluster (sibling of ProcessAndResolveGlobal_82CA3C68 / CountOrError_Thunk_82CA5FE0 callers). 169 lines, 4 callees.
# 10 callers.
[entrypoint.functions."0x82BF21E0"]
name = "ProcessAndUpdateGlobal_82BF21E0"

# A thunk that forwards to the global-field setter. It takes no arguments of its own (forwarding the passed arguments)
# and performs the set (SetGlobalFieldIfNeeded_82CC1C20), returning the result. It is a one-line pass-through wrapper
# around SetGlobalFieldIfNeeded_82CC1C20 (the conditional global-field setter). This is the "set this global field (via
# the conditional setter)" thunk for the 0x82CC0 global-set cluster, used by 10 callers (the 0x82BEB/0x82BF0/0x82BF2/
# 0x82BF3/0x82CBD/0x82CBD/0x82CBDD/0x82CAC/0x82CC1 range). Part of the 0x82CC0 global-set cluster (sibling of
# SetGlobalFieldIfNeeded_82CC1C20). 7 lines, 1 callee. 10 callers.
[entrypoint.functions."0x82CC0750"]
name = "SetGlobalField_Thunk_82CC0750"

# A processor that drives a virtual call, then performs a binary search on the result. It takes a value (a), a vtable
# container (b: a vtable at b->0, with the method at b->0->4), and a value (c). It drives the virtual method (bctrl
# into b->0->4) and checks the result; when it is null it returns 0. When it is present it continues with the result's
# data (the array at the result's + 4 field) and performs the 4-byte-stride binary search (BinarySearch_4Stride
# _82C40EC0) over it. This is the "drive this virtual call and binary-search the result (returning 0 when absent)"
# routine for the 0x82C2A search cluster, used by 2 callers (sub_82C2A860 and sub_82C2C780). Part of the 0x82C2A
# search cluster (sibling of BinarySearch_4Stride_82C40EC0 / ProcessAndReadIndexed_82C2A9D8). 83 lines, 1 callee.
# 2 callers.
[entrypoint.functions."0x82C2A7D0"]
name = "ProcessAndBinarySearch_82C2A7D0"

# A thunk that calls the binary-search routine and converts the result to a boolean. It takes a value (a), a vtable
# container (b), and a value (c). It calls ProcessAndBinarySearch_82C2A7D0 = sub_82C2A7D0(a, b, c) and then converts the
# result to a boolean (inspecting the count-leading-zeros of the result to extract a property bit, then negating it).
# This is the "binary-search and reduce to a boolean" wrapper around ProcessAndBinarySearch_82C2A7D0 for the 0x82C2A
# search cluster, referenced by no other function. Part of the 0x82C2A search cluster (sibling of
# ProcessAndBinarySearch_82C2A7D0 / BinarySearch_4Stride_82C40EC0). 30 lines, 1 callee. 0 callers (vtable / external).
[entrypoint.functions."0x82C2A860"]
name = "ProcessAndBinarySearchBool_82C2A860"

# A selector that returns one of two .data constant pointers based on a flag. It takes a value (a) and a flag (b).
# When the flag (b) is exactly 1 it returns the "true" constant pointer (.data base + 19904). When the flag is any
# other value (0 or greater than 1) it returns the "false" constant pointer (.data base + 19900). This is the
# "select between these two constant pointers based on the flag" routine for the 0x82C34 constant-select cluster,
# referenced by no other function. Part of the 0x82C34 constant-select cluster (sibling of SelectConstantPointer
# _82C23858 / SelectConstantPointer5_82C2A6D0). 25 lines, 0 callees. 0 callers (vtable / external).
[entrypoint.functions."0x82C34D00"]
name = "SelectConstantPointer6_82C34D00"

# A processor that, when the count is below the minimum, calls the grow handler, and clamps the value. It takes a
# structure (a: a count at a->20, a type at a->24), a minimum (b), a bound (c), a value (d), and a cap (e). When the
# count (a->20) is below the minimum (b) it invokes the grow handler (sub_82CD12C8). It then computes the clamped
# value: (a->20 - b), clamped to the bound (c) and the cap (e) - using the minimum of the difference and the bound,
# then the minimum of that and the cap. It then proceeds with the clamped value (checking the type at a->24 for the
# 16-byte path). This is the "grow the count when below the minimum, and clamp the value" routine for the 0x8217C
# clamp-grow cluster, used by 10 callers (the 0x8236A/0x82BF9/0x82C6A/0x82C6B/0x82C6C/0x82C6E/0x82C84 range). Part of
# the 0x8217C clamp-grow cluster (sibling of sub_82CD12C8 / ClampRange_822F6480). 129 lines, 1 callee. 10 callers.
[entrypoint.functions."0x8217CBE8"]
name = "ProcessAndClampGrow_8217CBE8"

# A processor that, when two values match, dispatches a virtual call based on the type (via a switch). It takes a
# structure (a: a type index at a->76), a container (b: a type at b->0), a expected type (c), and a value (d). When the
# two values (b, c) differ it returns -4 (mismatch). When they match it computes the type (b->0 + a->76) and, when the
# type is within range (<= 10), dispatches the virtual call through a .data jump table (bctr into the type's handler).
# This is the "when these two values match, dispatch the virtual call by type" routine for the 0x82C8B type-dispatch
# cluster, referenced by no other function. Part of the 0x82C8B type-dispatch cluster (sibling of ProcessAndDispatchBy
# Type_82228E28 / the 0x82C8A range). 519 lines, 2 callees. 0 callers (vtable / external).
[entrypoint.functions."0x82C8B5F8"]
name = "ProcessAndDispatchByType_82C8B5F8"

# A processor that resolves a 40-byte global, then populates its fields. It takes three structures (a, b, and c: each
# with a value at ->0). It resolves the 40-byte global (ProcessAndResolveGlobal_82CA3C68(40)) and stores the result.
# When the global is present (non-null) it populates the fields (the value at a->0, b->0, and c->0 into the global's
# + 0 / + 20 / + 12 fields, with the + 16 and + 36 fields zeroed). This is the "resolve this 40-byte global and
# populate its fields (from the three structures)" routine for the 0x829FD global-populate cluster, used by 4 callers
# (sub_8240D938 and the 0x83231/0x83236 range). Part of the 0x829FD global-populate cluster (sibling of
# ProcessAndResolveGlobal_82CA3C68 / ProcessAndResolveGlobal2_82CA3C68). 113 lines, 1 callee. 4 callers.
[entrypoint.functions."0x829FD060"]
name = "ProcessAndPopulateGlobal_829FD060"

# A processor that zeroes the fields, then dispatches the state when the flag is set. It takes a structure (a: a
# handler at a->16, with fields at a->16->36 / a->16->40 / a->16->44 / a->16->100, and a state object at
# a->16->100->72 / a->16->100->68). It zeroes the fields (a->16->36, a->16->40, a->16->44 all to 0). It then checks the
# flag (the bits at a->16->100->5 & 0x3); when it is set it dispatches the state (StateDispatch_82BCA7D8(a->16)). It
# then continues with the per-state handling (checking the state's count at a->16->100->72 against 4). This is the
# "zero these fields and dispatch the state (when the flag is set)" routine for the 0x82BCB state-dispatch cluster,
# used by 2 callers (StateDispatch_82BCBB40 and sub_82BC62B8). Part of the 0x82BCB state-dispatch cluster (sibling of
# StateDispatch_82BCA7D8 / ProcessAndDispatchPositions_82BCC970). 134 lines, 1 callee. 2 callers.
[entrypoint.functions."0x82BCB7E8"]
name = "ProcessAndDispatchState_82BCB7E8"

# A processor that performs a binary search over the 8-byte elements, returning the index. It takes a collection (a:
# an element base at a->12, a count at a->28). When the count (a->28) is zero it takes the not-found path. When the
# last element (at a->12 + count * 8 - 4) is non-zero it also takes the not-found path. Otherwise it performs the
# binary search: iterating over the elements (8-byte stride), narrowing the range (high/low) until the target is
# found (the element at the mid-point is zero) or the range is exhausted. It returns the index (or the not-found
# sentinel). This is the "binary-search these 8-byte elements for the zero element, returning the index" routine for
# the 0x82BCA binary-search cluster, used by 2 callers (sub_822DF280 and sub_82BC5B30). Part of the 0x82BCA binary
# -search cluster (sibling of BinarySearch_4Stride_82C40EC0 / ProcessAndBinarySearch_82C2A7D0). 79 lines, 1 callee.
# 2 callers.
[entrypoint.functions."0x82BCA750"]
name = "ProcessAndBinarySearchIndex_82BCA750"

# A comparator that compares two structures field-by-field, returning whether they match. It takes two structures (a
# and b: each with a value at ->0, a float at ->4, and bytes at ->16 / ->17 / ->18 / ->19 / ->20 / ...). It compares the
# float (a->4 vs b->4), the value (a->0 vs b->0), and the bytes (a->16 vs b->16, a->17 vs b->17, a->18 vs b->18, a->19
# vs b->19, and the following bytes). When any field differs it takes the not-equal path (returning 0); when all
# fields match it returns 1. This is the "compare these two structures field-by-field (float, value, and bytes),
# returning whether they match" routine for the 0x82A12 compare cluster, used by 2 callers (OneTimeInitTree_83233B40
# and sub_83233268). Part of the 0x82A12 compare cluster (sibling of Compare12ByteStructs_82C00EE0 / CompareStrings
# Signed_82C16660). 98 lines, 0 callees. 2 callers.
[entrypoint.functions."0x82A12C70"]
name = "CompareStructFields_82A12C70"

# A processor that shifts the elements down, removing the first element. It takes a collection (a: an element array
# starting at a + 8, a count/limit at a->8). When the array is empty (a + 8 >= a->8) it does nothing. Otherwise it
# shifts all the elements down by one (8-byte stride): each element (at offset + 0 and + 4) is copied into the
# previous slot (at - 8 and - 4), advancing through the array. It then decrements the count (a->8 -= 8). This is the
# "shift these elements down and remove the first" routine for the 0x82BC5 remove cluster, used by 8 callers (the
# 0x82BC6/0x82BC7/0x82BCF/0x82BD0/0x82BDA range). Part of the 0x82BC5 remove cluster (sibling of GetIndexedPointer
# _82BC5448 / GrowAndZeroBuffer_82BC55B8). 58 lines, 1 callee. 8 callers.
[entrypoint.functions."0x82BC5630"]
name = "ProcessAndRemoveFirstElement_82BC5630"

# A processor that, when the flag is set, iterates the elements and clears the type flag. It takes a structure (a: a
# flag at a->5, an element base at a->12, a count at a->28, with the element type at + 4 and the element value at + 0).
# When the structure is null it does nothing. When the flag (a->5 & 0x10) is not set or the count (a->28) is zero it
# takes the early path. Otherwise it iterates the elements (8-byte stride, from the last toward the first): for each
# element, when its type (+ 4 field) is 4 it reads the element value (the pointer at the element's + 0 field) and clears
# its low two bits (the type flag, at the value's + 5 field & 0xFFFFFFFC). This is the "when the flag is set, iterate
# the type-4 elements and clear their type flag" routine for the 0x82BCB type-flag cluster, called only by sub_82BCB8D0.
# Part of the 0x82BCB type-flag cluster (sibling of ProcessAndDispatchState_82BCB7E8 / StateDispatch_82BCA7D8). 211
# lines, 0 callees. 1 caller: sub_82BCB8D0.
[entrypoint.functions."0x82BCB300"]
name = "ProcessAndClearTypeFlag_82BCB300"

# A processor that iterates the linked elements, processing the type-4 elements. It takes a structure (a: a handler at
# a->16, with a linked list starting at a->16->120 and a base at a->16 + 104; each element has a flag at ->5, a value
# at ->8, a type at ->8->4, and a pointer at ->8->0). When the list is empty (a->16->120 == a->16 + 104) it does
# nothing. Otherwise it iterates the elements: for each, when the flag (a->16->120->5 & 0x4) is set and the low bits
# (a->16->120->5 & 0x3) are zero, and the element type (a->16->120->8->4) is 4, it processes the element - finding it in
# the hash table (HashTable_Find3_821CF1D0), processing the node list (ProcessNodeList_82BCB2A0), dispatching the state
# (StateDispatch_82BCA7D8), and clearing the type flag (ProcessAndClearTypeFlag_82BCB300). This is the "iterate these
# linked elements, processing the type-4 ones (hash lookup, node list, state dispatch)" routine for the 0x82BCB node
# -list cluster, called only by StateDispatch_82BCBB40. Part of the 0x82BCB node-list cluster (sibling of
# ProcessNodeList_82BCB2A0 / ProcessAndClearTypeFlag_82BCB300). 350 lines, 4 callees. 1 caller: StateDispatch_82BCBB40.
[entrypoint.functions."0x82BCB8D0"]
name = "ProcessAndDispatchNodeList_82BCB8D0"

# A thunk that forwards to the UTF-16 string-length routine. It takes a string (a) and computes its length in UTF-16
# code units (StrLenUTF16_8217AB30(a)), returning the result. It is a one-line pass-through wrapper around
# StrLenUTF16_8217AB30 (the UTF-16 string-length function). This is the "compute this UTF-16 string's length (via the
# UTF-16 strlen)" thunk for the 0x822F8 string cluster, used by 3 callers (sub_82EE5018 and the 0x82BF0BB / 0x82BF0CE
# range). Part of the 0x822F8 string cluster (sibling of StrLenUTF16_8217AB30). 7 lines, 1 callee. 3 callers.
[entrypoint.functions."0x822F8620"]
name = "StrLenUTF16_Thunk_822F8620"

# A large processor that sets up the arguments (constants and floats) and drives the element dispatch. It takes a
# value (a) and a value (b). It stores the value (b) and sets up the constant/float arguments (the .data entries and the
# computed offsets). It then drives the large element dispatch: performing the chain search (ChainSearchDispatch
# _82BCC620), creating descriptor nodes (CreateDescriptorNode_82BCC598), dispatching by type (DispatchTypeTag2_822D8298
# / StateDispatch_82BCA7D8 / StateDispatch3_82BCA3E8), accessing elements (ElementAccess_821E1498 / InsertElementAt
# _82BC9FD0), searching (ProcessAndBinarySearchIndex_82BCA750), adjusting positions (ProcessAdjustPosition_82BCA340), and
# processing the loop (ProcessDispatchLoop_8229A518 / ProcessLoopDispatch_8227B8B8 / ProcessState_8219F010 /
# ProcessState2_8219EEC0). This is the "set up the arguments and drive the element dispatch" routine for the 0x822DF
# dispatch cluster, used by 2 callers (sub_82BCD300 and IncrementAndCheckThreshold_8219EE00). Part of the 0x822DF
# dispatch cluster (sibling of the 0x82BCA/0x82BCC dispatch cluster). 2642 lines, 28 callees. 2 callers.
[entrypoint.functions."0x822DF280"]
name = "ProcessAndDispatchElements_822DF280"

# A processor (second variant) that reads a float value (converting it through double), then processes the elements. It
# takes a value (a), a structure (b: a start at b->4, an end at b->8), and a value (c). It reads the start (b->4) and
# end (b->8), computes the count ((b->8 - b->4) / 8, i.e. 8-byte elements), and reads the element just before the end
# (b->8 - 8), resolving the float value (at the element's + 4 field, converted int -> double -> float). It then, when
# the count is positive, iterates the elements processing each (the per-element handling, and the one-time tree setup
# OneTimeInitTree_83233B40). This is the "read this float value (with conversion) and process the elements (second
# variant)" routine for the 0x82A13 float-process cluster, called only by ProcessDispatchValue2_832351B0. Part of the
# 0x82A13 float-process cluster (sibling of ProcessAndConvertFloatElements_82A139D0 / OneTimeInitTree_83233B40). 377
# lines, 1 callee. 1 caller: ProcessDispatchValue2_832351B0.
[entrypoint.functions."0x82A134B8"]
name = "ProcessAndConvertFloatElements2_82A134B8"

# A processor that sets up the structure, then constructs the float array. It takes a structure (a: a descriptor at
# a->0, a count at a->8, a value at a->12, a flag at a->16, a size at a->32, a type at a->40) and a value (b). It sets
# the descriptor pointer (a->0) and zeroes the fields (a->8, a->12, a->16, a->32 all to 0), setting the type (a->40 = 5).
# It then constructs the float structure (ConstructFloatStructContainer_82A142D0), the 32-byte node (Construct32Byte3
# _82BE49F0), and grows/appends the elements (ProcessGrowAppend_82BE4AA8). This is the "set up this structure and
# construct the float array" routine for the 0x8323F float-array cluster, called only by ProcessGrowAppend8_83221378.
# Part of the 0x8323F float-array cluster (sibling of ConstructFloatStructContainer_82A142D0 / ProcessGrowAppend8
# _83221378). 424 lines, 3 callees. 1 caller: ProcessGrowAppend8_83221378.
[entrypoint.functions."0x8323FCA8"]
name = "ProcessAndConstructFloatArray_8323FCA8"

# A processor that waits on a single object, then maps the result to a status. It takes a handle (a: a wait object at
# a->128) and a value (b, a timeout / flags). It computes the wait flags (checking whether b is a power of two via
# count-leading-zeros) and waits on the object (KeWaitForSingleObject_Guest(a->128, b, flags)). It then maps the result
# to a status: 192 (wait completed) maps to 1 (success), 0 or 128 (time-out) maps to 2 (time-out), and other values
# take the error path. This is the "wait on this single object and map the result to a status (1 = success, 2 =
# timeout)" routine for the 0x82B49 wait-status cluster, used by 10 callers (the 0x8236C/0x82B44/0x82BFE/0x82BFF/
# 0x82C7D/0x82C7E range). Part of the 0x82B49 wait-status cluster (sibling of KeWaitForSingleObject_Guest). 81 lines,
# 1 callee. 10 callers.
[entrypoint.functions."0x82B49B50"]
name = "ProcessAndWaitMapStatus_82B49B50"

# A processor that, when the range is large enough, performs a recursive divide-and-conquer search. It takes a range
# (a, b: the two bounds, with the size (b - a) / 4 in 4-byte units), a count (c), and a value (d). When the range is too
# small (b - a >> 2 <= 32) or the count is non-positive (c <= 0), it takes the base-case path. Otherwise it performs the
# search (sub_82C19978) on the range, then recurses (calling itself and sub_82C1AF58 / sub_82C19800 / sub_82C19ED0) to
# process the halves of the range (computing the mid-point c >> 1). This is the "when the range is large enough, perform
# the recursive divide-and-conquer search" routine for the 0x82C1B recursive-search cluster, used by 2 callers (sub
# _82C1B920 and sub_82C1C170). Part of the 0x82C1B recursive-search cluster (sibling of sub_82C19978 / sub_82C1AF58).
# 176 lines, 5 callees (including a recursive call to itself). 2 callers.
[entrypoint.functions."0x82C1B600"]
name = "ProcessAndRecursiveSearch_82C1B600"

# A processor that performs a series of virtual calls (one per field), returning when one fails. It takes a target (a:
# a structure whose fields at +0, +4, +8, ... are the per-element values) and a value (b). It performs a virtual call for
# each field (VcallTriple_AllSuccess_82C011E8(b, a + offset, constant)) - checking the result after each. When a call
# returns 0 (failure), it stops (goes to the failure path). The constant argument (the .data entry) is the per-call
# selector. It also calls VcallChain_AllSuccess_82C15068 for the chained case. This is the "perform this series of
# virtual calls (one per field, stopping on failure)" routine for the 0x82C35 vcall-cluster, called only by sub_82C35308.
# Part of the 0x82C35 vcall cluster (sibling of VcallTriple_AllSuccess_82C011E8 / VcallChain_AllSuccess_82C15068). 121
# lines, 2 callees. 1 caller: sub_82C35308.
[entrypoint.functions."0x82C35238"]
name = "ProcessAndDispatchVcallChain_82C35238"

# A processor that performs a sequence of virtual calls (a vcall, the vcall chain, then another vcall), returning when
# one fails. It takes a target (a: a structure at a->0, whose method table has the first vcall at ->24 and the last at
# ->28) and a value (b). It performs the first virtual call (a->0->24, the method at the ->24 slot of the ->0
# structure), checking the result. Then it performs the vcall chain (ProcessAndDispatchVcallChain_82C35238(b, a)),
# checking the result. Then it performs the last virtual call (a->0->28). When any step returns 0 (failure), it stops
# (goes to the failure path). This is the "perform this sequence of virtual calls (first vcall, vcall chain, last vcall,
# stopping on failure)" routine for the 0x82C35 vcall cluster, called only by sub_82C358F0. Part of the 0x82C35 vcall
# cluster (sibling of ProcessAndDispatchVcallChain_82C35238). 86 lines, 1 callee. 1 caller: sub_82C358F0.
[entrypoint.functions."0x82C35308"]
name = "ProcessAndDispatchVcallSequence_82C35308"

# A processor that copies a 16-byte data buffer, performs the operation on the data, and updates the count. It takes a
# structure (a: a data buffer at a->4, a size/offset at a->20, a capacity check at a->24), a flag (b), and a new count
# (c). When the flag (b & 0xFF) is zero or the capacity (a->24) is less than 16, it takes the early path. Otherwise it
# copies/zeroes the 16-byte data buffer (Buffer_CopyOrZero_Error_82CA3730(a + 4, a->4, 16, c)) - copying the source
# (a->4) into the destination (a + 4) with the new count. It then performs the operation on the data (sub_82BF2158(a
# ->4)) and updates the count (a->20 = c). This is the "copy this 16-byte data buffer, perform the operation, and update
# the count" routine for the 0x82BEC data cluster, used by 8 callers (the 0x82BEC/0x82BED/0x82BEE/0x82BEF/0x82BF1
# range). Part of the 0x82BEC data cluster (sibling of Buffer_CopyOrZero_Error_82CA3730 / sub_82BF2158). 70 lines, 2
# callees. 8 callers.
[entrypoint.functions."0x82BEC350"]
name = "ProcessAndCopyDataAndSetCount_82BEC350"

# A processor that grows the data buffer (when needed), then copies the data and sets the count. It takes a structure
# (a: a data buffer at a->4, a size at a->20, a capacity at a->24), a size (b), and a flag (c). When the size (b) is
# valid (b > -2), it performs the size adjustment (sub_82CD11D0). When the capacity (a->24) is large enough (a->24 >= b),
# it takes the no-grow path; otherwise it grows the buffer (sub_82BEC470) and re-copies the data. It then, when the flag
# (c & 0xFF) is set and the size (b) is at least 16, performs the data copy (ProcessAndCopyDataAndSetCount_82BEC350).
# This is the "grow this data buffer (when needed), then copy the data and set the count" routine for the 0x82BEC data
# cluster, used by 2 callers (sub_82BED0C8 and sub_82BED440). Part of the 0x82BEC data cluster (sibling of
# ProcessAndCopyDataAndSetCount_82BEC350 / sub_82BEC470). 116 lines, 3 callees. 2 callers.
[entrypoint.functions."0x82BECAE0"]
name = "ProcessAndGrowCopyData_82BECAE0"

# A processor that resolves the data pointer (handling the buffer layout), checks the bounds, and grows the data. It
# takes a structure (a: a data buffer at a->4, a size at a->20, a capacity at a->24), a size (b), and a flag (c). It
# resolves the data pointer based on the buffer layout: when the capacity (a->24) is less than 16 (the inline buffer
# threshold), the data is stored inline (at a + 4); otherwise the data buffer is at a->4. It checks the bounds (b vs
# the resolved size, and a->20 + the offset vs b) and, when the bounds are violated, grows the data (ProcessAndGrowCopy
# Data_82BECAE0 / sub_82BED0C8) and copies it (Buffer_CopyOrZero_Error_82CA3730). This is the "resolve this data pointer
# (handling the inline-vs-heap buffer layout), check the bounds, and grow the data" routine for the 0x82BED data cluster,
# used by 2 callers (sub_82BEE748 and sub_82BF1D00). Part of the 0x82BED data cluster (sibling of
# ProcessAndGrowCopyData_82BECAE0 / sub_82BED0C8). 161 lines, 3 callees. 2 callers.
[entrypoint.functions."0x82BED440"]
name = "ProcessAndGrowDataBySize_82BED440"

# A processor that, when the two objects differ and are non-null, atomically decrements the reference count. It takes
# two object pointers (a: a pointer at a->0, with a reference count at a->0->4; b: a pointer at b->0). When a->0 is
# null or a->0 equals b->0, it does nothing. Otherwise it atomically decrements the reference count (a->0->4 -= 1),
# using a load-word-reserved / store-word-conditional (lwarx / stwcx) pair under the global lock (REX_ENTER_GLOBAL_LOCK
# / REX_CHECK_GLOBAL_LOCK) - retrying the compare-and-swap loop until it succeeds. This is the "when these two object
# pointers differ and are non-null, atomically decrement the reference count" routine for the 0x8218D reference-count
# cluster, used by 10 callers (the 0x8219C/0x822D9/0x824F7/0x82816/0x82818/0x82A5E/0x82A63/0x82A76/0x82A9A/0x82A9F
# range). Part of the 0x8218D reference-count cluster. 131 lines, 0 callees. 10 callers.
[entrypoint.functions."0x8218DFF0"]
name = "ProcessAndDecrementRefCount_8218DFF0"

# A processor that atomically decrements the reference count, disposing the object when it reaches zero. It takes an
# object pointer (a: a pointer at a->0, with the object's data at a->0->0 and the reference count at a->0->4). When
# a->0 is null, it does nothing. Otherwise it atomically decrements the reference count (a->0->4 -= 1), using a
# load-word-reserved / store-word-conditional (lwarx / stwcx) pair under the global lock. When the count reaches 0 (the
# last reference was released), it reads the object's data (a->0->0) and disposes the object (the destruction / free
# path). When the count is non-zero (other references remain), it takes the early path. This is the "atomically
# decrement this reference count and dispose the object when it reaches zero" routine for the 0x821DE reference-count
# cluster, used by 10 callers (the 0x821DD/0x82303/0x82379/0x8237A/0x8244A/0x8245C/0x827C2/0x827DE/0x82816/0x82A9F
# range). Part of the 0x821DE reference-count cluster (sibling of ProcessAndDecrementRefCount_8218DFF0). 80 lines, 0
# callees. 10 callers.
[entrypoint.functions."0x821DE318"]
name = "ProcessAndDecrementDispose_821DE318"

# A processor that, under a lock, finds the tree leaf and validates the result. It takes a value (a), a tree (b: a flag
# at b->4, a lock at b + 12), and a value (c). It enters the critical section (CriticalSection_EnterOrTry_82200688, using
# the lock at b + 12) and finds the tree leaf (FindTreeLeaf_825CB448). It then validates the result: when the found leaf
# is null or is the tree itself, it takes the error path (trap 22). When the leaf is valid, it processes the found node
# (sub_825F2B58 / sub_82B6AC38) and disposes the old value (Dispose17_82B6AB20). This is the "under a lock, find this
# tree leaf, validate the result, and process the found node" routine for the 0x821F5 tree-leaf cluster, used by 10
# callers (the 0x8219F/0x821D5/0x82261/0x822A2/0x822C1/0x82303/0x8235C/0x8244A/0x825B3 range). Part of the 0x821F5 tree
# -leaf cluster (sibling of FindTreeLeaf_825CB448 / Dispose17_82B6AB20). 489 lines, 5 callees. 10 callers.
[entrypoint.functions."0x821F5B90"]
name = "ProcessAndFindTreeLeafLocked_821F5B90"

# A processor that atomically increments a global counter, checks the count, and releases the reference. It takes a
# result (a: an output at a->0, zeroed first) and a count (b). It zeroes the output (a->0 = 0), then atomically
# increments the global counter (the .data entry, via a load-word-reserved / store-word-conditional (lwarx / stwcx)
# pair under the global lock) - retrying the compare-and-swap loop until it succeeds. It then checks the count (b vs
# the new counter) and, when appropriate, releases the reference-counted object (Release_RefCounted_821C67D8). This is
# the "atomically increment this global counter, check the count, and release the reference" routine for the 0x82287
# counter cluster, used by 10 callers (the 0x82317/0x82333/0x82356/0x8245C/0x824F9/0x827DE/0x8282E/0x8283E/0x828D0/
# 0x82B62 range). Part of the 0x82287 counter cluster (sibling of Release_RefCounted_821C67D8). 128 lines, 1 callee.
# 10 callers.
[entrypoint.functions."0x82287710"]
name = "ProcessAndIncrementCounter_82287710"

# A processor that replaces a reference-counted pointer, releasing the old reference and incrementing the new one. It
# takes a target (a: a reference-counted pointer at a->0) and a source (b: a reference-counted pointer at b->0, with a
# reference count at b->0->12). When the target and source are the same (a == b, or a->0 == b->0), it does nothing.
# Otherwise it releases the old reference (Release_RefCounted_821C67D8(a->0), when a->0 is non-null), stores the new
# pointer (a->0 = b->0), and atomically increments the new reference count (b->0->12 += 1, via a load-word-reserved /
# store-word-conditional (lwarx / stwcx) pair under the global lock). This is the "replace this reference-counted
# pointer (release the old reference, store the new pointer, and atomically increment the new reference count)" routine
# for the 0x822B8 refcounted cluster, used by 10 callers (the 0x8219C/0x8245E/0x8247D/0x827A6/0x827CC/0x827D3/0x82817/
# 0x82836/0x828ED/0x828F8 range). Part of the 0x822B8 refcounted cluster (sibling of Release_RefCounted_821C67D8). 92
# lines, 1 callee. 10 callers.
[entrypoint.functions."0x822B85C8"]
name = "ProcessAndReplaceRefCounted_822B85C8"

# A processor that resolves the handler (from the .data entries, handling the different layouts), then finds the tree
# leaf. It takes a value (a) and a value (b). It resolves the handler from the .data entries: when the primary handler
# (the .data entry at the fixed address) is set and its flag is non-zero, it uses that handler (reading the value at
# handler + 12 -> 96); otherwise it uses the alternate handler (the .data base + 8). When the resolved handler is null,
# it takes the error path. It then performs the locked tree-leaf find (ProcessAndFindTreeLeafLocked_821F5B90). This is
# the "resolve this handler (handling the primary-vs-alternate layout) and find the tree leaf" routine for the 0x82816
# handler cluster, used by 3 callers (sub_82816EC0 and the 0x828186/0x828187 range). Part of the 0x82816 handler cluster
# (sibling of ProcessAndFindTreeLeafLocked_821F5B90). 107 lines, 1 callee. 3 callers.
[entrypoint.functions."0x82816E08"]
name = "ProcessAndResolveHandler_82816E08"

# A processor that zeroes a 16-byte structure, calls the virtual query function, and processes the result. It takes a
# target (a: a structure at a->0, whose query method is at a->0->24). It zeroes the 16-byte output structure on the
# stack, then calls the virtual query method (a->0->24, passing the zeroed structure as the output buffer), and checks
# the result (must be at least 16 bytes). When the query succeeds, it processes the result: comparing the 12-byte
# structs (Compare12ByteStructs_82C00EE0), constructing the nodes (ConstructAndProcessNode_82C02E88 /
# ConstructRefCounted_8222CF18 / Construct104ByteObject_82B48E30), copying the reference-counted values (CopyAssign
# _RefCounted2_82265160), incrementing the reference count (ProcessAtomicIncrement_821E3950), comparing the names
# (StrCmpIgnoreCase_82CA6320), dispatching the sub-virtuals (ProcessAndDispatchSubVirtual2_82C021B8), and walking the
# tree (DestructAndWalkTree_82C02F68). This is the "zero this structure, call the virtual query method, and process the
# result (constructing the tree nodes)" routine for the 0x82C02 query cluster, used by 3 callers (sub_82BFCEF8 and the
# 0x82C03/0x82C4B range). Part of the 0x82C02 query cluster (sibling of ConstructAndProcessNode_82C02E88 /
# DestructAndWalkTree_82C02F68). 354 lines, 10 callees. 3 callers.
[entrypoint.functions."0x82C02FC8"]
name = "ProcessAndQueryConstructTree_82C02FC8"

# A finder that searches for a UTF-16 substring within a UTF-16 string. It takes a haystack (a: a UTF-16 string, an
# array of 16-bit code units) and a needle (b: a UTF-16 string, an array of 16-bit code units). When the needle (b) is
# empty, it returns immediately. When the haystack (a) is empty, it returns with no match. Otherwise it performs a
# naive substring search: the outer loop advances through the haystack (a) two bytes (one UTF-16 code unit) at a time,
# and the inner loop compares the characters at a and at the corresponding position in the haystack (a + (a - b)),
# advancing until either the characters mismatch or the end of the substring is reached. It returns the position of the
# first match (or the end / no match). This is the "search for this UTF-16 substring within this UTF-16 string" routine
# for the 0x82CA3 string cluster, used by 6 callers (sub_8238E1E8, sub_8238E4E8, sub_8238E7D8 and the 0x82B38B/0x82B391
# range). Part of the 0x82CA3 string cluster (sibling of CountNonZeroBytes_82CA3BC0). 69 lines, 0 callees. 6 callers.
[entrypoint.functions."0x82CA3BF0"]
name = "FindUTF16Substring_82CA3BF0"

# A processor that computes the buffer size (handling the bounds), reallocates when needed, and stores the 16-bit
# elements. It takes a structure (a: a size at a->20), a base offset (b), a count (c), a value (d), and a flag (e). When
# the current size (a->20) is less than the base offset (b), it grows the buffer (sub_82CD12C8). It then computes the
# available space (a->20 - b), clamping the count (c) to the available space when necessary. It then, when the space is
# insufficient, reallocate (sub_82CD11D0) and stores the 16-bit elements (Store16BitElement_822F6578), performing the
# checked copy (CheckedCopy_82CA3808) and the per-value processing (ProcessValue_FlagNonZero_8217A5E0). This is the
# "compute this buffer size (handling the bounds), reallocate when needed, and store the 16-bit elements" routine for the
# 0x822F5 grow-store cluster, used by 2 callers (ProcessElementAccess3_8217AA20 and ReadStringElement_822F2238). Part of
# the 0x822F5 grow-store cluster (sibling of Store16BitElement_822F6578 / CheckedCopy_82CA3808). 247 lines, 5 callees. 2
# callers.
[entrypoint.functions."0x822F52A8"]
name = "ProcessAndGrowStoreElements_822F52A8"

# A processor that accesses the 8-byte element, computes the offset, and appends the element to the list. It takes a
# structure (a: an element buffer at a->0, a list/append pointer at a->8, with the element pair at a->8->0 and a->8->4),
# an index (b), and a value (c). It accesses the 8-byte element (AccessElement8_82A1E620), computes the element offset
# (ProcessAndComputeElementOffset_82229260), and then appends the element (an 8-byte pair): it copies the two 32-bit
# fields (a->0->0 -> a->8->0, a->0->4 -> a->8->4) and advances the append pointer (a->8 += 8). This is the "access this
# 8-byte element, compute the offset, and append the element (an 8-byte pair) to the list" routine for the 0x82229
# element-append cluster, used by 10 callers (the 0x822A2/0x82322/0x8243C/0x824F6/0x82526/0x82637/0x8273C/0x82792/
# 0x827EF/0x82865 range). Part of the 0x82229 element-append cluster (sibling of AccessElement8_82A1E620 /
# ProcessAndComputeElementOffset_82229260). 53 lines, 2 callees. 10 callers.
[entrypoint.functions."0x82229208"]
name = "ProcessAndAppendElement_82229208"

# A processor that gets the indexed pointer, dispatches the value, and stores the result. It takes a structure (a: a
# result at a->0 and a->4, an element buffer at a->8). It gets the indexed pointer (GetIndexedPointer_82BC5448), reads
# the last element (a->8 - 16, the 16-byte slot before the end), and dispatches the value (ProcessDispatchValue
# _8229A488). It then stores the result (a->0 = the element's + -8 field, a->4 = the element's + -4 field). When the
# element type (a->8 - 4, the field before the end) is less than 4 or the flag (a->8 - 8->5 & 0x3) is zero, it takes the
# early path. This is the "get this indexed pointer, dispatch the value, and store the result" routine for the 0x822A5
# indexed-value cluster, used by 9 callers (sub_821E15A0 and the 0x82BB1/0x82BB8/0x82BD0/0x82BD5/0x82BD7 range). Part of
# the 0x822A5 indexed-value cluster (sibling of GetIndexedPointer_82BC5448 / ProcessDispatchValue_8229A488). 96 lines,
# 2 callees. 9 callers.
[entrypoint.functions."0x822A5E98"]
name = "ProcessAndDispatchIndexedValue_822A5E98"

# A thunk that computes a size (the product of two values), then allocates it. It takes a size factor (b) and a count (c)
# and computes the allocation size (b * c). It then allocates the memory (Allocate_OrDbgBreak_8221F388(b * c)), returning
# the allocated pointer. It is a one-line pass-through wrapper around Allocate_OrDbgBreak_8221F388 (the allocate-or-
# debug-break function), computing the size as the product of the two arguments. This is the "compute this size (the
# product of the two values) and allocate it" thunk for the 0x82B49 allocate cluster, a small helper (0 direct callers in
# the recompiled code - likely called via a function pointer or from a small number of large functions). Part of the
# 0x82B49 allocate cluster (sibling of Allocate_OrDbgBreak_8221F388). 9 lines, 1 callee. 0 direct callers.
[entrypoint.functions."0x82B49BE0"]
name = "Allocate_Sized_Thunk_82B49BE0"

# A thunk that forwards to the size-bucketed free routine. It takes a pointer (b) and frees the memory
# (Free_SizeBucketed_Thunk_8221BE68(b)). It is a one-line pass-through wrapper around Free_SizeBucketed_Thunk_8221BE68
# (the size-bucketed free function) - moving the pointer into r3 and tail-calling the free. This is the "free this
# pointer (via the size-bucketed free)" thunk for the 0x82B49 free cluster, a small helper (0 direct callers in the
# recompiled code - likely called via a function pointer or from a small number of large functions), the free counterpart
# to Allocate_Sized_Thunk_82B49BE0. Part of the 0x82B49 free cluster (sibling of Free_SizeBucketed_Thunk_8221BE68 / 
# Allocate_Sized_Thunk_82B49BE0). 7 lines, 1 callee. 0 direct callers.
[entrypoint.functions."0x82B49BE8"]
name = "Free_SizeBucketed_Thunk2_82B49BE8"

# A thunk that forwards to the character-property check routine. It takes a character (a) and checks its property
# (sub_82CA6BD0(a)), which itself forwards to the character-classification function (sub_82CB1328(a, 8)) with the
# constant 8 (the property / class selector). It is a one-line pass-through wrapper around sub_82CA6BD0 (the character
# -property check function). This is the "check this character's property (via the character-classification function)"
# thunk for the 0x82BF2 character cluster, called only by sub_82BF0BB0 (the UTF-16 string-processing function). Part of
# the 0x82BF2 character cluster (sibling of the 0x82CA6/0x82CB1 character-classification cluster). 5 lines, 1 callee.
# 1 caller: sub_82BF0BB0.
[entrypoint.functions."0x82BF2340"]
name = "CharProperty_Check_Thunk_82BF2340"

# A thunk that forwards to the character-classification function with the constant 8 (the property selector). It takes a
# character (a) and checks its property (sub_82CB1328(a, 8)) - passing the character and the constant 8 (the property /
# class selector) to the character-classification function. It is a one-line pass-through wrapper around sub_82CB1328
# (the character-classification function), setting up the constant argument (8). This is the "check this character's
# property (property 8, via the character-classification function)" thunk for the 0x82CA6 character cluster, used by 10
# callers (the 0x82BF2 and 0x82F71/0x82F75/0x82F82/0x82F83 range). Part of the 0x82CA6 character cluster (sibling of 
# sub_82CB1328 / CharProperty_Check_Thunk_82BF2340). 7 lines, 1 callee. 10 callers.
[entrypoint.functions."0x82CA6BD0"]
name = "CharProperty_Check8_82CA6BD0"

# A lookup that checks a character's property bitfield. It takes a character (a, a 16-bit value) and a property mask (b,
# a 16-bit value). When the character (a) is invalid (a == 0xFFFF, a >= 256, i.e. not a 7-bit ASCII character), it
# returns 0. Otherwise it looks up the character's property bitfield in the lookup table (the .data entry at the fixed
# address, indexed by a * 2), and returns the AND of the property bitfield with the mask (b). This is the "when this
# character is valid (0-255), look up its property bitfield in the table and return the AND with this mask" routine for
# the 0x82CB1 character-property cluster, used by 5 callers (CharProperty_Check8_82CA6BD0 and the 0x82CA6/0x82CD2 range).
# Part of the 0x82CB1 character-property cluster (sibling of CharProperty_Check8_82CA6BD0). 33 lines, 0 callees. 5
# callers.
[entrypoint.functions."0x82CB1328"]
name = "CharProperty_Lookup_82CB1328"

# A processor that gets the indexed pointer, then reads the value (converting the float to int when the type is 3). It
# takes an element (a: a value at a->0, a type at a->4). It gets the indexed pointer (GetIndexedPointer_82BC5448). When
# the type (a->4) is not 3, it resolves the value through the other handler (sub_82BCBD48). When the type (a->4) is 3 (a
# float), it reads the float value (a->0) and converts it to an integer (the float-to-int conversion, fctiwz). This is
# the "get this indexed pointer and read the value (converting the float to int when the type is 3)" routine for the
# 0x82BC5 read-conversion cluster, used by 10 callers (the 0x82BB3/0x82BB9/0x82BC6/0x82BC7/0x82BCF/0x82BD0/0x82BD3/
# 0x82BD8/0x82BD9 range). Part of the 0x82BC5 read-conversion cluster (sibling of GetIndexedPointer_82BC5448 /
# ProcessAndReadIndexed_82C2A9D8). 59 lines, 2 callees. 10 callers.
[entrypoint.functions."0x82BC59D0"]
name = "ProcessAndReadConvertedValue_82BC59D0"

# A processor that, when the type is 5, dispatches the value type, performs the hash lookup, and processes the result.
# It takes a value (a), a structure (b: a pointer at b->0, a type at b->4, with the result at b->0->0, a flag at b->0->4,
# and a value at b->0->12), a value (c), and a value (d). When the type (b->4) is 5, it dispatches the value type
# (DispatchValueType_822285D8), checking the result (the flag at b->0->4). When the flag is zero, it reads the value
# (b->0->12) and processes it (the per-value handling, and the hash lookup HashLookupByKey_82228570, and the element
# access sub_82A2C300 / sub_82A2C520 / sub_82A2FF50). This is the "when this type is 5, dispatch the value type, perform
# the hash lookup, and process the result" routine for the 0x82228 typed-value cluster, used by 10 callers (the
# 0x824EF/0x825A7/0x825AE/0x827F9/0x82A18/0x82A1E/0x82A26/0x82A28 range and GuildChest_ProcessItems_822DE008). Part of
# the 0x82228 typed-value cluster (sibling of DispatchValueType_822285D8 / HashLookupByKey_82228570). 202 lines, 6
# callees. 10 callers.
[entrypoint.functions."0x822286D0"]
name = "ProcessAndDispatchTypedValue_822286D0"

# A processor that sets the descriptor, then iterates the elements, calling the virtual function on each. It takes a
# structure (a: a descriptor at a->0, an element array at a->4, a count at a->12). It sets the descriptor (a->0, the
# .data entry) and, when the count (a->12) is positive, iterates the elements (a->4): for each non-null element, it
# calls the virtual function (the method at the element's ->0->0 slot) with the argument 1 (the "invoke" / "activate"
# operation). When the elements are done, it frees them (Free_Thunk_824FE010). This is the "set this descriptor and
# invoke each element (calling the virtual function with argument 1), then free the elements" routine for the 0x82C15
# element-invoke cluster, used by 4 callers (sub_82C15AD8 and the 0x82C1C/0x82C20 range). Part of the 0x82C15 element
# -invoke cluster (sibling of Free_Thunk_824FE010). 93 lines, 1 callee. 4 callers.
[entrypoint.functions."0x82C15998"]
name = "ProcessAndInvokeElements_82C15998"

# A processor that sets the descriptor, zeroes the fields, and performs the bubble sort. It takes a structure (a: a
# descriptor at a->0, an element array at a->4, a count at a->8, a size at a->12) and a value (b). It sets the
# descriptor (a->0, the .data entry) and zeroes the fields (a->4, a->8, a->12 all to 0). It then performs the bubble
# sort (BubbleSort3_82C16C78(b, a + 4)), sorting the elements (at a + 4) by the comparator (b). It then sets the
# descriptor again (a->0, the .data entry) before returning. This is the "set this descriptor, zero the fields, and
# perform the bubble sort (sorting the elements by the comparator)" routine for the 0x82C1A sort cluster, used by 3
# callers (sub_82C1CBD8, sub_82C1CD68, and sub_82C20AF0). Part of the 0x82C1A sort cluster (sibling of BubbleSort3
# _82C16C78). 58 lines, 1 callee. 3 callers.
[entrypoint.functions."0x82C1AE88"]
name = "ProcessAndSortElements_82C1AE88"

# A processor that sets the descriptor, compacts the array, and invokes each element. It takes a structure (a: a
# descriptor at a->0, an element array at a->4, a count at a->12). It sets the descriptor (a->0, the .data entry) and
# compacts the array (ArrayCompact_82C23DD0, removing the null / empty slots). It then, when the count (a->12) is
# positive, iterates the elements (a->4): for each non-null element, it calls the virtual function (the method at the
# element's ->0->0 slot) with the argument 1 (the "invoke" / "activate" operation). When the elements are done, it frees
# them (Free_Thunk_824FE010). This is the "set this descriptor, compact the array, and invoke each element (calling the
# virtual function with argument 1), then free the elements" routine for the 0x82C1F compact-invoke cluster, used by 6
# callers (sub_82C1B028, sub_82C1CBD8, sub_82C1CD68, sub_82C1FB80, sub_82C207A0, and sub_82C20AF0). Part of the 0x82C1F
# compact-invoke cluster (sibling of ArrayCompact_82C23DD0 / ProcessAndInvokeElements_82C15998). 96 lines, 2 callees. 6
# callers.
[entrypoint.functions."0x82C1FAE0"]
name = "ProcessAndCompactInvoke_82C1FAE0"

# A popper that, when the field is non-zero, pops the value. It takes a structure (b: a pointer at b->0, a value at
# b->4) and an output (c: a value at c->0). When the field (b->4) is zero, it returns 0 (nothing to pop). Otherwise it
# pops the value: it stores the value (c->0 = b->4), returns the pointer (b->0), and zeroes the field (b->4 = 0). This
# is the "when this field is non-zero, pop the value (store the value, return the pointer, and zero the field)" routine
# for the 0x82A1F value cluster, a small helper (0 direct callers in the recompiled code - likely called via a function
# pointer or from a small number of large functions). Part of the 0x82A1F value cluster. 25 lines, 0 callees. 0 direct
# callers.
[entrypoint.functions."0x82A1FF68"]
name = "PopValue_82A1FF68"

# A processor that calls the virtual query, storing the index and returning the byte value. It takes a structure (a: an
# index at a->0, a pointer at a->4, a method at a->8, an argument at a->12, another argument at a->16). It calls the
# virtual query (the method at a->8, passing a->16, a->12, and an output buffer), and checks the result. When the result
# (the pointer) is non-null and the output (r1->80) is non-null, it stores the index (a->0 = the output - 1, a->4 = the
# pointer, and a->4 + 1) and returns the byte value (the byte at the pointer). This is the "call this virtual query, and
# when the result is valid, store the index and return the byte value" routine for the 0x82BCD query-index cluster, used
# by 10 callers (sub_82179420 and the 0x82A2C/0x82A30/0x82A31/0x82BCE/0x82BCD range). Part of the 0x82BCD query-index
# cluster. 75 lines, 0 callees. 10 callers.
[entrypoint.functions."0x82BCD8A8"]
name = "ProcessAndQueryIndex_82BCD8A8"

# A getter that reads the field at offset 12 and returns it. It takes a structure (a) and returns the field at a->12
# (a one-line field accessor). This is the "read this structure's field at offset 12" getter for the 0x83041 field
# -accessor cluster, used by 10 callers (sub_82317970, sub_82317E50, sub_82317F98, sub_82325A78, sub_8232DBB8,
# sub_823971C8, sub_82C51040, sub_82C510C8, sub_82C511C0, and sub_82C80EB8). Part of the 0x83041 field-accessor
# cluster. 5 lines, 0 callees. 10 callers.
[entrypoint.functions."0x830418B0"]
name = "GetField_12_830418B0"

# A finder that searches for a substring within a string (the byte / ASCII version). It takes a haystack (a: a string,
# an array of bytes) and a needle (b: a string, an array of bytes). When the needle (b) is empty, it returns immediately.
# When the haystack (a) is empty, it returns with no match. Otherwise it performs a naive substring search: the outer loop
# advances through the haystack (a) one byte at a time, and the inner loop compares the characters at a and at the
# corresponding position in the haystack (a + (a - b)), advancing until either the characters mismatch or the end of the
# substring is reached. It returns the position of the first match (or the end / no match). This is the "search for this
# substring within this string (the byte / ASCII version)" routine for the 0x821E6 string cluster, used by 10 callers (the
# 0x8234F/0x82500/0x8250B/0x8270A/0x82A18/0x82B3A/0x82B47/0x82BC7/0x82D51/0x82F30 range). Part of the 0x821E6 string
# cluster (the byte counterpart of FindUTF16Substring_82CA3BF0). 74 lines, 0 callees. 10 callers.
[entrypoint.functions."0x821E6388"]
name = "FindSubstring_821E6388"

# A processor that zeroes the tail elements, then performs the hash lookup and dispatch. It takes a structure (a: an
# element pointer at a->8), a structure (b: a count at b->73, a flag at b->74), and a value (c). It reads the count
# (b->73). When the value (c) is less than the count, it iterates the tail elements (from the current position to the
# end, b->73 - c of them), zeroing each element's field (a->8->4 = 0) and advancing the pointer (a->8 += 8). It then, when
# the flag (b->74 & 0x4) is set, performs the hash lookup (ComputeHash_8227BA30 / HashTable_Find3_821CF1D0), accesses the
# elements (ElementAccess_821E1498), adjusts the position (ProcessAdjustPosition_82BCA340), dispatches the loop
# (ProcessLoopDispatch_8227B8B8), and dispatches the state (StateDispatch3_82BCA3E8). This is the "zero this tail's
# elements, then perform the hash lookup and dispatch" routine for the 0x82BCC zero-tail cluster, called only by
# ProcessState_8219F010. Part of the 0x82BCC zero-tail cluster (sibling of the 0x82BCA/0x8227B dispatch cluster). 284
# lines, 6 callees. 1 caller: ProcessState_8219F010.
[entrypoint.functions."0x82BCCFF8"]
name = "ProcessAndZeroTailDispatch_82BCCFF8"

# A processor that reads the data value (when the size is small), stores it, and allocates the buffer. It takes a size
# (a, in bytes / 8). When the size (a) is zero or the size is too large (a * 8 overflows, checked via -1 / a >= 8), it
# takes the allocate-only path. Otherwise it reads the .data value (the constant at the fixed .data address), stores it
# (the value into the buffer, r1->80, r1->84), and calls the buffer setup (sub_822F1F00). It then, in all cases,
# computes the allocation size (a << 3, i.e. a * 8) and allocates the buffer (Allocate_OrDbgBreak_8221F388(a << 3)). This
# is the "read this data value (when the size is small), store it, and allocate the buffer (a * 8 bytes)" routine for the
# 0x823D9 buffer-allocate cluster, used by 5 callers (sub_823D43C8, sub_823D4440, sub_826BAB08, sub_828BAD50, and sub
# _82926068). Part of the 0x823D9 buffer-allocate cluster (sibling of Allocate_OrDbgBreak_8221F388). 66 lines, 2 callees.
# 5 callers.
[entrypoint.functions."0x823D94F8"]
name = "ProcessAndAllocateBuffer_823D94F8"

# A processor that computes the element counts (from the buffer ranges) and inserts the value in the array. It takes a
# structure (a: a base at a->4, an end at a->8, a limit at a->12) and a value (b: a pointer at b->0). It computes the
# element counts (a->8 - a->4 and a->12 - a->4, in 8-byte units). When the counts are valid (a->4 is non-zero, and the
# end count (a->8 - a->4 >> 3) is less than the limit count (a->12 - a->4 >> 3)), it inserts the value (b) into the array
# (the per-element handling, sub_82925940). When the counts are invalid or the range is full, it takes the early path.
# This is the "compute this element count (from the buffer ranges) and insert the value in the array (when the range is
# valid)" routine for the 0x82925 array-insertion cluster, used by 5 callers (sub_826B3E60, sub_828B8F20, sub_82924730,
# sub_829785F8, and sub_82C6CB88). Part of the 0x82925 array-insertion cluster. 107 lines, 1 callee. 5 callers.
[entrypoint.functions."0x829255E8"]
name = "ProcessAndInsertInArray_829255E8"

# A processor that computes the element count, validates the range, and processes the elements. It takes a value (a), a
# structure (b: a base at b->4, an end at b->8), and a value (c). It computes the element count ((b->8 - b->4) / 8, i.e.
# 8-byte elements). When the base (b->4) is zero or the count is zero, it takes the empty path. Otherwise it validates the
# range (b->4 <= b->8, taking the error path (trap 22) when violated) and processes the elements (the per-element
# handling, and the value insertion sub_82926068). This is the "compute this element count, validate the range, and
# process the elements (inserting the value)" routine for the 0x82925 array-insertion cluster, called only by
# ProcessAndInsertInArray_829255E8. Part of the 0x82925 array-insertion cluster (sibling of ProcessAndInsertInArray
# _829255E8 / sub_82926068). 132 lines, 1 callee. 1 caller: ProcessAndInsertInArray_829255E8.
[entrypoint.functions."0x82925940"]
name = "ProcessAndValidateInsert_82925940"

# A processor that computes the element count, and when the count and value are valid, grows the array and inserts the
# value. It takes a structure (a: a base at a->4, an end at a->8, a limit at a->12), a value (b), a value (c), and a
# structure (d: a 64-bit value at d->0). It computes the element count ((a->12 - a->4) / 8, i.e. 8-byte elements). When
# the value (c) is zero or the base (a->4) is zero, it takes the empty path. Otherwise it processes the elements: it
# grows the array (ProcessAndAllocateBuffer_823D94F8, allocating the buffer), frees the old buffer (Free_SizeBucketed
# _Thunk_8221BE68), and performs the per-element handling (sub_82684B38). This is the "compute this element count and,
# when the count and value are valid, grow the array and insert the value" routine for the 0x82926 grow-insert cluster,
# used by 2 callers (sub_828BAED8 and ProcessAndValidateInsert_82925940). Part of the 0x82926 grow-insert cluster
# (sibling of ProcessAndAllocateBuffer_823D94F8 / ProcessAndValidateInsert_82925940). 503 lines, 3 callees. 2 callers.
[entrypoint.functions."0x82926068"]
name = "ProcessAndGrowAndInsert_82926068"

# A processor that, when the condition is met, dispatches the loop, adjusts the position, and appends the node. It takes
# a structure (a: a node pointer at a->8, a structure at a->16, with two values at a->16->64 and a->16->68), a value (b),
# and a value (c). It reads the two values (a->16->68, a->16->64) and, when the condition is met (a->16->68 >= a->16->64),
# dispatches the loop (ProcessLoopDispatch_8227B8B8). It then adjusts the position (ProcessAdjustPosition_82BCA340) and
# appends the node (a->8->0 = a, a->8->4 = 5, the type tag, advancing a->8 += 8). This is the "when this condition is met,
# dispatch the loop, adjust the position, and append the node (with type 5)" routine for the 0x82BC5 append-node cluster,
# used by 10 callers (the 0x82BB0/0x82BB1/0x82BB7/0x82BBF/0x82BC0/0x82BC5/0x82BCF/0x82BD0/0x82BD4/0x82BDB range). Part of
# the 0x82BC5 append-node cluster (sibling of ProcessAdjustPosition_82BCA340 / ProcessLoopDispatch_8227B8B8). 62 lines, 2
# callees. 10 callers.
[entrypoint.functions."0x82BC5CD8"]
name = "ProcessAndAppendNode_82BC5CD8"

# A processor that, under a critical section, allocates and initializes a 416-byte object (when the global is not set).
# It takes a value (a). It gets the object (sub_82B44BC8) and enters the critical section (CriticalSection_EnterOrTry
# _82200688). When the global object (the .data entry) is not set, it allocates a 416-byte object (Allocate_OrDbgBreak
# _8221F388(416)) and initializes it (sub_82B44890), then sets and dispatches (SetAndDispatch_82366210 / sub_82B386B8).
# When the global object is already set, it uses the existing one. This is the "under a critical section, when this global
# object is not set, allocate a 416-byte object and initialize it" routine for the 0x82B44 critical-object cluster, used by
# 9 callers (sub_822F27C0, sub_826C0628, sub_82B3E0A8, sub_82B3E1B0, sub_82B4E708, sub_82C630E0, sub_82C63168, sub
# _82C6C7B0, and ConstructAndInitCritical_82B434C0). Part of the 0x82B44 critical-object cluster (sibling of
# CriticalSection_EnterOrTry_82200688 / ConstructAndInitCritical_82B434C0). 157 lines, 6 callees. 9 callers.
[entrypoint.functions."0x82B44788"]
name = "ProcessAndAllocateCriticalObject_82B44788"

# A one-time initializer that initializes the critical section (when the initialized bit is not set). It takes a value (a)
# and checks the initialized bit (the .data entry's low bit). When the bit is set (already initialized), it returns the
# existing critical section. Otherwise it sets the bit (the .data entry |= 1) and initializes the critical section (Rtl
# InitializeCriticalSection, at the .data base + 8100), performing the one-time setup (sub_82CA3700). It returns the
# critical section pointer. This is the "one-time initialize this critical section (when the bit is not set, set it and
# call RtlInitializeCriticalSection)" routine for the 0x82B44 critical-section cluster, used by 4 callers (sub_82B44788
# = ProcessAndAllocateCriticalObject and the 0x82B44A/0x82B44C range). Part of the 0x82B44 critical-section cluster
# (sibling of ProcessAndAllocateCriticalObject_82B44788 / CriticalSection_EnterOrTry_82200688). 72 lines, 1 callee. 4
# callers.
[entrypoint.functions."0x82B44BC8"]
name = "OneTimeInitCriticalSection_82B44BC8"

# A processor that zeroes the output, calls the virtual query function, and processes the result. It takes a value (a), a
# structure (b: a pointer at b->0, whose query method is at b->0->16), and several values (c, d, e, f, g). It zeroes the
# output fields (three 32-bit fields on the stack), calls the virtual query method (b->0->16, passing the output buffers
# and the values), and checks the result. When the query succeeds, it processes the result: allocating the object
# (Allocate_OrDbgBreak_8221F388), assigning the reference (AssignRefCounted_822F54C8), copying and dispatching (CopyAnd
# Dispatch_822F5478), and allocating the critical object (ProcessAndAllocateCriticalObject_82B44788). This is the "zero
# this output, call the virtual query method, and process the result (allocating the object and assigning the
# reference)" routine for the 0x82B4E query-allocate cluster, used by 4 callers (sub_82C44688, sub_82C46010, sub
# _82C64650, and sub_82C6C7B0). Part of the 0x82B4E query-allocate cluster (sibling of ProcessAndAllocateCriticalObject
# _82B44788 / AssignRefCounted_822F54C8). 494 lines, 4 callees. 4 callers.
[entrypoint.functions."0x82B4E708"]
name = "ProcessAndQueryAllocate_82B4E708"

# A processor that, when the two elements differ, performs the virtual comparison and processes the result. It takes two
# element pointers (a, b). When a equals b, or a + 4 equals b, it takes the equal path. Otherwise it reads the element
# data (a->0, a->0->4, a->0->4->0, a->0->4->0->4) and performs the virtual comparison (the method at the element's ->0
# ->4->0->4 slot, comparing the two elements). It then processes the result: comparing the 2D regions (Process2DRegion
# _82C3C010) and the strings (StrCmpIgnoreCase_82CA6320). This is the "when these two elements differ, perform the virtual
# comparison (comparing the 2D regions and the strings) and process the result" routine for the 0x82C19 compare cluster,
# called only by ProcessAndRecursiveSearch_82C1B600. Part of the 0x82C19 compare cluster (sibling of Process2DRegion
# _82C3C010 / StrCmpIgnoreCase_82CA6320). 215 lines, 2 callees. 1 caller: ProcessAndRecursiveSearch_82C1B600.
[entrypoint.functions."0x82C19800"]
name = "ProcessAndCompareElements_82C19800"

# A processor that, when the index is valid, performs the processing (allocating the buffer and growing the structure).
# It takes a structure (a: a limit at a->0, a base at a->4, a buffer at a->16, a count at a->24) and an index (b). When
# the index (b) is out of bounds (b <= a->0 or a->4 + b >= a->0), it returns 0. Otherwise it performs the processing
# (sub_82C87A50, allocating the buffer), and, when the count (a->24) is zero, it grows the structure (sub_82C820B0).
# This is the "when this index is valid, perform the processing (allocating the buffer and growing the structure)" routine
# for the 0x82C82 hash-table cluster, used by 3 callers (HashTableLookup_82C822D8 and the 0x82C89/0x82CA2 range). Part of
# the 0x82C82 hash-table cluster (sibling of HashTableLookup_82C822D8 / sub_82C87A50). 107 lines, 2 callees. 3 callers.
[entrypoint.functions."0x82C82220"]
name = "ProcessAndGrowHashTable_82C82220"

# A processor that performs the processing and, when the flag (low bit) is set, frees the buffer. It takes a value (a)
# and a value (b, with a low-bit flag). It performs the processing (sub_82B56698) and, when the low bit of b is set
# (b & 1 != 0), it frees the buffer (Free_SizeBucketed_Thunk_8221BE68). This is the "perform this processing and, when the
# flag (low bit) is set, free the buffer" routine for the 0x82B56 free-if-flagged cluster, called only via the vtable (0
# direct callers). Part of the 0x82B56 free-if-flagged cluster (sibling of sub_82B56698 / Free_SizeBucketed_Thunk
# _8221BE68). 50 lines, 2 callees. 0 direct callers (vtable dispatch).
[entrypoint.functions."0x82B56648"]
name = "ProcessAndFreeIfFlagged_82B56648"

# A processor that sets the vtable, checks success, and dispatches the virtual functions on the two sub-objects. It takes
# a structure (a: a vtable at a->0/a->4, sub-objects at a->180 and a->184, a field at a->332) and a value (b). It sets the
# vtable (a->0, a->4, the two .data pointers), checks success (ProcessAndCheckSuccess_82CBBF60, on a->332), and, when the
# sub-objects are non-null, dispatches their virtual functions (a->184->0->8, a->180->0->...). This is the "set this vtable,
# check success, and dispatch the virtual functions (the two sub-objects)" routine for the 0x82B56 dispatch cluster, called
# only by ProcessAndFreeIfFlagged_82B56648. Part of the 0x82B56 dispatch cluster (sibling of ProcessAndFreeIfFlagged
# _82B56648 / ProcessAndCheckSuccess_82CBBF60). 104 lines, 1 callee. 1 caller: ProcessAndFreeIfFlagged_82B56648.
[entrypoint.functions."0x82B56698"]
name = "ProcessAndDispatchSubObjects_82B56698"

# A small copy helper that copies a value (b->24) into the destination (a->0) and zeroes the adjacent field (a->4). It takes
# a destination (a: two 32-bit fields at a->0 and a->4) and a source (b: a value at b->24). It reads the value (b->24),
# zeroes a->4, and copies b->24 into a->0. This is the "copy this value (b->24) into the destination (a->0), zeroing the
# adjacent field (a->4)" routine for the 0x82173 copy cluster, called only via the vtable (0 direct callers). Part of the
# 0x82173 copy cluster. 14 lines, 0 callees. 0 direct callers (vtable dispatch).
[entrypoint.functions."0x82173E60"]
name = "CopyValueAndZeroField_82173E60"

# A processor that compares two byte sequences (the first 16 bytes, then the remainder). It takes a value (a) and a source
# (b: bytes at b->0, with a limit at b->16). It reads a constant byte sequence (the .data reference) and walks the source
# bytes (b->0, advancing up to b->16), comparing each against the constant. When a byte differs, it exits and performs a
# second comparison (the remaining bytes). This is the "compare these two byte sequences (the first 16 bytes, then the
# remainder)" routine for the 0x826C0 compare cluster, called only via the vtable (0 direct callers). Part of the 0x826C0
# compare cluster. 114 lines, 0 callees. 0 direct callers (vtable dispatch).
[entrypoint.functions."0x826C0BA0"]
name = "ProcessAndCompareBytes_826C0BA0"

# A small initializer that calls RtlInitializeCriticalSection on the given critical section. It takes a critical section
# (a) and calls RtlInitializeCriticalSection (a). This is the "initialize this critical section (a)" routine for the 0x82B38
# critical-section cluster, used by 10 callers (the 0x82BF/0x82C0/0x82C5/0x82C6/0x82C8/0x8329 range). Part of the 0x82B38
# critical-section cluster (sibling of OneTimeInitCriticalSection_82B44BC8). 32 lines, 0 callees (external import). 10
# callers.
[entrypoint.functions."0x82B38560"]
name = "InitializeCriticalSection_82B38560"

# A constructor that sets the vtable, sets the flag, copies the values, zeroes the fields, and initializes the critical
# section. It takes a structure (a: a vtable at a->0, a flag at a->9, values at a->12/a->16, fields at a->20/a->24/a->36,
# two copies at a->28/a->32), a source (b: values at b->0/b->4), and several values (c, d, e). It sets the vtable (a->0, the
# .data pointer), sets the flag (a->9 = 1), copies the values (a->12 = b->0, a->16 = b->4), copies c (a->28 = a->32 = c),
# zeroes the fields (a->4, a->8, a->20, a->24, a->36), and initializes the critical section (RtlInitializeCriticalSection).
# This is the "set this vtable, set the flag, copy the values, zero the fields, and initialize the critical section" routine
# for the 0x82B43 construct cluster, called only by sub_82B43B88. Part of the 0x82B43 construct cluster (sibling of
# InitializeCriticalSection_82B38560 / ConstructAndInitCritical_82B434C0). 146 lines, 0 callees (external import). 1 caller.
[entrypoint.functions."0x82B43800"]
name = "ConstructAndInitCriticalSection_82B43800"

# A processor that, when the source value is set, stores it into the destination; otherwise, allocates 80 bytes and
# constructs the critical section. It takes a destination (a: a field at a->0), a source (b: a value at b->0), and several
# values (c, d, e). When b->0 is zero, it stores 0 into a->0 and returns. Otherwise it allocates 80 bytes (Allocate_OrDbg
# Break_8221F388) and constructs the critical section (ConstructAndInitCriticalSection_82B43800), storing the result into
# a->0. This is the "when this source value is set, store it into the destination; otherwise, allocate 80 bytes and
# construct the critical section" routine for the 0x82B43 construct cluster, used by 2 callers (sub_822F4D10 and sub
# _82C66570). Part of the 0x82B43 construct cluster (sibling of ConstructAndInitCriticalSection_82B43800 / Allocate_OrDbg
# Break_8221F388). 87 lines, 2 callees. 2 callers.
[entrypoint.functions."0x82B43B88"]
name = "ProcessAndConstructCriticalSection_82B43B88"

# A small thunk that calls OneTimeInitAndVcall_82B4D518 on the given object. It takes a value (a) and calls OneTimeInitAnd
# Vcall_82B4D518 (a). This is the "one-time initialize and call the virtual function (a)" thunk for the 0x82B55 vcall
# cluster, called only via the vtable (0 direct callers). Part of the 0x82B55 vcall cluster (sibling of OneTimeInitAndVcall
# _82B4D518). 32 lines, 1 callee. 0 direct callers (vtable dispatch).
[entrypoint.functions."0x82B55F48"]
name = "OneTimeInitAndVcallThunk_82B55F48"

# A processor that, when the count is valid, performs the processing (validating the range, computing the offset, and
# constructing the critical section). It takes a destination (a: a field at a->0), a structure (b: values at b->60, b->68,
# and b->72), a count (c), and a value (d). When d is zero, it stores 0 into a->0 and returns. Otherwise it validates the
# range (b->68, b->72; the element count (b->72 - b->68) / 12, taking the error path (trap 22) when c - 1 exceeds it) and
# computes the offset. It then performs the processing: constructing the critical section (ProcessAndConstructCritical
# Section_82B43B88), reading the sub-object (LockedReadSubobject_82B44360), allocating the buffer (Allocate_OrDbgBreak
# _8221F388), assigning the reference (AssignRefCounted_822F54C8), and dispatching (OneTimeInitAndVcall_82B4D518 and
# several others). This is the "when this count is valid, perform the processing (validating the range, computing the
# offset, and constructing the critical section)" routine for the 0x82C66 validate-construct cluster, called only by sub
# _82C66CC8. Part of the 0x82C66 validate-construct cluster (sibling of ProcessAndConstructCriticalSection_82B43B88 /
# LockedReadSubobject_82B44360). 397 lines, 9 callees. 1 caller.
[entrypoint.functions."0x82C66570"]
name = "ProcessAndValidateAndConstruct_82C66570"

# A processor that, when the offset is valid, performs the processing (validating the range, computing the offset, and
# querying and constructing). It takes a destination (a: a field at a->0), a structure (b: a limit at b->12, a base at
# b->16, a value at b->20), and several values (c, d, e, f, g). When the offset (d + e) is out of bounds (d + e <= b->12),
# it stores 0 into a->0 and returns. Otherwise it validates the range (b->20), computes the offset (b + 16), and performs
# the processing: querying and constructing (ProcessAndQueryAllocate_82B4E708), allocating the critical object (ProcessAnd
# AllocateCriticalObject_82B44788), allocating the buffer (Allocate_OrDbgBreak_8221F388), and dispatching (sub_82C6FF50).
# This is the "when this offset is valid, perform the processing (validating the range, computing the offset, and querying
# and constructing)" routine for the 0x82C6C query-construct cluster, called only via the vtable (0 direct callers). Part of
# the 0x82C6C query-construct cluster (sibling of ProcessAndQueryAllocate_82B4E708 / ProcessAndAllocateCriticalObject
# _82B44788). 431 lines, 4 callees. 0 direct callers (vtable dispatch).
[entrypoint.functions."0x82C6C7B0"]
name = "ProcessAndQueryAndConstruct_82C6C7B0"

# A processor that sets the vtable, frees the buffer, zeroes the fields, and dispatches the virtual function. It takes a
# structure (a: a vtable at a->0, a sub-object at a->8, fields at a->16, a buffer at a->20). It sets the vtable (a->0, the
# .data pointer) and, when the buffer (a->20) is non-null, frees it (Free_Thunk_824FE010). It zeroes the fields (a + 16
# ->4, a + 16->8, a + 16->12) and, when the sub-object (a->8) is non-null, dispatches its virtual function (a->8->0->8).
# This is the "set this vtable, free the buffer, zero the fields, and dispatch the virtual function" routine for the 0x82C6C
# destruct cluster, called only by sub_82C6CB38. Part of the 0x82C6C destruct cluster (sibling of Free_Thunk_824FE010 /
# ProcessAndQueryAndConstruct_82C6C7B0). 79 lines, 1 callee. 1 caller.
[entrypoint.functions."0x82C6CAB0"]
name = "ProcessAndDestructAndDispatch_82C6CAB0"

# A processor that performs the destruct (freeing the buffer and zeroing the fields) and, when the flag (low bit) is set,
# frees the buffer. It takes a value (a) and a value (b, with a low-bit flag). It performs the destruct (ProcessAndDestruct
# AndDispatch_82C6CAB0) and, when the low bit of b is set (b & 1 != 0), it frees the buffer (Free_Thunk_824FE010). This is
# the "perform this destruct (freeing the buffer and zeroing the fields) and, when the flag (low bit) is set, free the
# buffer" routine for the 0x82C6C destruct cluster, called only via the vtable (0 direct callers). Part of the 0x82C6C
# destruct cluster (sibling of ProcessAndDestructAndDispatch_82C6CAB0 / Free_Thunk_824FE010). 50 lines, 2 callees. 0
# direct callers (vtable dispatch).
[entrypoint.functions."0x82C6CB38"]
name = "ProcessAndDestructAndFree_82C6CB38"

# A processor that sets the vtable, zeroes the fields, copies the value, and calls the virtual function (when the value
# is non-null). It takes a structure (a: a vtable at a->0, a field at a->4, a value at a->8, a base at a->16, fields at
# a->20/a->24/a->28), a value (b, with a vtable at b->0 and a method at b->0->4), and a value (c). It sets the vtable (a->0,
# the .data pointer), copies b (a->8 = b), zeroes the fields (a->4, a->20, a->24, a->28), and, when b is non-null, calls
# the virtual function (b->0->4, passing b). It also inserts into the array (ProcessAndInsertInArray_829255E8). This is the
# "set this vtable, zero the fields, copy the value, and call the virtual function (when the value is non-null)" routine
# for the 0x82C6C construct cluster, called only by ProcessAndValidateAndConstruct_82C66570. Part of the 0x82C6C construct
# cluster (sibling of ProcessAndValidateAndConstruct_82C66570 / ProcessAndInsertInArray_829255E8). 190 lines, 1 callee. 1
# caller.
[entrypoint.functions."0x82C6CB88"]
name = "ProcessAndConstructAndInsert_82C6CB88"

# A processor that calls the virtual query function, and when the result is zero, performs the byte check and re-queries.
# It takes a structure (a: a pointer at a->0, whose query method is at a->0->12). It calls the virtual query method (a->0
# ->12, passing a). When the result is non-zero, it uses the result. Otherwise it performs the byte check (ByteValueCheck
# _82CBC678) and re-queries (a->0->12, passing a), looping until the result is non-zero. This is the "call this virtual
# query method, and when the result is zero, perform the byte check and re-query" routine for the 0x82C6F query cluster,
# called only via the vtable (0 direct callers). Part of the 0x82C6F query cluster (sibling of ByteValueCheck_82CBC678).
# 75 lines, 1 callee. 0 direct callers (vtable dispatch).
[entrypoint.functions."0x82C6F848"]
name = "ProcessAndQueryWithByteCheck_82C6F848"

# A small virtual-call thunk that calls the method at the structure's ->40->0->32 vtable slot. It takes a structure (a: a
# pointer at a->40, whose vtable is at a->40->0 and method slot is at a->40->0->32). It reads the vtable (a->40->0->32)
# and calls the virtual method (a->40->0->32, passing a). This is the "call this virtual method (a->40->0->32, passing a)"
# thunk for the 0x82C6F vcall cluster, called only via the vtable (0 direct callers). Part of the 0x82C6F vcall cluster.
# 15 lines, 0 callees. 0 direct callers (vtable dispatch).
[entrypoint.functions."0x82C6FC00"]
name = "VcallThunk_82C6FC00"

# A constructor that sets the vtable, zeroes the fields, initializes the critical section, and copies the values. It takes
# a structure (a: a vtable at a->0, a field at a->4, a field at a->8, a critical section at a->12, a value at a->40) and
# several values (b, c, d, e, f, g). It sets the vtable (a->0, a->4, the .data pointers), zeroes a->8, initializes the
# critical section (InitializeCriticalSection_82B38560, at a->12), and copies b (a->40 = b). This is the "set this vtable,
# zero the fields, initialize the critical section, and copy the values" routine for the 0x82C6F construct cluster, called
# only by ProcessAndQueryAndConstruct_82C6C7B0. Part of the 0x82C6F construct cluster (sibling of ProcessAndQueryAnd
# Construct_82C6C7B0 / InitializeCriticalSection_82B38560). 108 lines, 1 callee. 1 caller.
[entrypoint.functions."0x82C6FF50"]
name = "ProcessAndConstructWithCsInit_82C6FF50"

# A processor that, when the condition is met, performs the processing, computes the string hash, and appends the node.
# It takes a structure (a: a node pointer at a->8, a structure at a->16, with two values at a->16->64 and a->16->68), a
# value (b), and a value (c). It reads the two values (a->16->68, a->16->64) and, when the condition is met (a->16->68 >=
# a->16->64), performs the processing (sub_82179350). It then computes the string hash (ComputeStringHash_82227EA0) and
# appends the node (a->8->0 = a, a->8->4 = 4, the type tag, advancing a->8 += 8). This is the "when this condition is met,
# perform the processing, compute the string hash, and append the node (with type 4)" routine for the 0x82A1E append-node
# cluster, used by 10 callers (the 0x8244F/0x825D0/0x82608/0x82759/0x82765/0x828B3/0x82903/0x82A1E/0x82A28/0x82A2B range).
# Part of the 0x82A1E append-node cluster (sibling of ComputeStringHash_82227EA0 / ProcessAndAppendNode_82BC5CD8). 62
# lines, 2 callees. 10 callers.
[entrypoint.functions."0x82A1EA18"]
name = "ProcessAndAppendStringNode_82A1EA18"

# A processor that, when the counter is zero, queries the index, increments the counter, and reads the byte. It takes a
# structure (a: a counter at a->0, a pointer at a->4, whose byte is at a->4->0). When the counter (a->0) is non-zero, it
# uses the existing state. Otherwise it queries the index (ProcessAndQueryIndex_82BCD8A8); when the result is -1, it
# returns. Otherwise it increments the counter (a->0 += 1), decrements a->4 (a->4 -= 1), and reads the byte (a->4->0).
# This is the "when this counter is zero, query the index, increment the counter, and read the byte" routine for the 0x82A2C
# query-advance cluster, used by 2 callers (sub_82A2E630 and sub_82BCD460). Part of the 0x82A2C query-advance cluster
# (sibling of ProcessAndQueryIndex_82BCD8A8). 68 lines, 1 callee. 2 callers.
[entrypoint.functions."0x82A2C6E0"]
name = "ProcessAndQueryAndAdvance_82A2C6E0"

# A processor that compares the two floats, and when the first exceeds the second, copies the three floats from the source.
# It takes a destination (a: a float at a->0, a limit at a->12) and a source (b: three floats at b->0, b->4, and b->8). It
# reads the float (a->0) and the limit (a->12). When a->0 exceeds a->12, it copies the three floats from the source (a->0 =
# b->0, a->4 = b->4, a->8 = b->8) and continues. This is the "compare these two floats, and when the first exceeds the
# second, copy the three floats from the source" routine for the 0x82BB4 float-copy cluster, used by 3 callers (sub_82819130,
# sub_82BB43A0, and sub_82BB7B68). Part of the 0x82BB4 float-copy cluster. 181 lines, 0 callees. 3 callers.
[entrypoint.functions."0x82BB42A0"]
name = "ProcessAndCopyFloats_82BB42A0"

# A processor that, when the condition is met, copies or zeroes the buffer and performs the processing. It takes a
# structure (a: a base at a->4, a field at a->20, a value at a->24), a value (b), and a value (c). When b is zero (b & 0xFF
# == 0) or a->24 is less than 8, it skips the processing. Otherwise it reads a->4 and, when c is non-zero, copies or zeroes
# the buffer (Buffer_CopyOrZero_Error_82CA3730, 16 bytes from a + 4, c << 1 bytes) and performs the processing (sub_82BF2158,
# on a->4). It then stores c into a->20. This is the "when this condition is met, copy or zero the buffer and perform the
# processing" routine for the 0x82BF1 buffer cluster, used by 4 callers (sub_82BF1190, sub_82BF1408, sub_82BF1460, and sub
# _82BF1CA0). Part of the 0x82BF1 buffer cluster (sibling of Buffer_CopyOrZero_Error_82CA3730). 72 lines, 2 callees. 4
# callers.
[entrypoint.functions."0x82BF1048"]
name = "ProcessAndCopyOrZeroBuffer_82BF1048"

# A small search helper that finds the 16-bit value (b & 0xFFFF) in the sequence. It takes a pointer to a 16-bit sequence (a,
# zero-terminated) and a value (b). It walks the 16-bit values (a->0, advancing by 2 bytes), comparing each against b & 0xFFFF.
# When a value matches, or the sequence is exhausted (reaching the zero terminator), it returns 1 when the last value equals
# b & 0xFFFF, and 0 otherwise. This is the "search for this 16-bit value (b & 0xFFFF) in the sequence" routine for the 0x82CA6
# search cluster, used by 3 callers (sub_82B39150, sub_82BF1E58, and sub_82EE4770). Part of the 0x82CA6 search cluster
# (sibling of CharProperty_Check8_82CA6BD0). 38 lines, 0 callees. 3 callers.
[entrypoint.functions."0x82CA64D0"]
name = "SearchFor16BitValue_82CA64D0"

# A processor that, when the source is non-null, performs the conversion; otherwise, performs the default handling. It takes
# a source (a) and a destination (b). When a is non-null, it performs the conversion (CountNonZero_822CA680, on a and b).
# Otherwise it performs the default handling: setting the status (a->0 = 22), calling the initialization (sub_82CAB770), and
# the processing (sub_82CAB630, with the default arguments). This is the "when this source is non-null, perform the
# conversion; otherwise, perform the default handling (setting the status and calling the initialization)" routine for the
# 0x82CA6 convert cluster, called only by sub_82CA6888. Part of the 0x82CA6 convert cluster (sibling of SearchFor16BitValue
# _82CA64D0 / CountNonZero_822CA680). 108 lines, 3 callees. 1 caller.
[entrypoint.functions."0x82CA67D0"]
name = "ProcessAndConvertIfPresent_82CA67D0"

# A small thunk that calls ProcessAndConvertIfPresent_82CA67D0 with the default argument. It takes a source (a) and a
# destination (b), sets the third argument to 0 (the default), and calls ProcessAndConvertIfPresent_82CA67D0 (a, b, 0). This
# is the "perform this conversion (with the default argument)" thunk for the 0x82CA6 convert cluster, called only by sub
# _82BF1E58. Part of the 0x82CA6 convert cluster (sibling of ProcessAndConvertIfPresent_82CA67D0). 9 lines, 1 callee. 1
# caller.
[entrypoint.functions."0x82CA6888"]
name = "ConvertIfPresentThunk_82CA6888"

# A processor that clears the output, and performs the processing (the length computation, the specific case, or the
# conversion). It takes a value (a, with the special value 65001), a value (b), a value (c), a value (d, with the special
# value -1), a value (e), a value (f), and an output (g, with a field at g->0). It clears the output (g->0 = 0, when g is
# non-null). When a is the special value (65001), it performs the specific case (sub_82CC8B50, on c, e, and f). Otherwise,
# when d is -1, it computes the length (StrLenUTF16_8217AB30, on c), and sets the global field (SetGlobalField_Thunk
# _82CC0750). This is the "clear this output, and perform the processing (the length computation, the specific case, or the
# conversion)" routine for the 0x82CBD convert cluster, used by 5 callers (sub_82407A98, sub_82BF1D00, sub_82CA9A60, sub
# _82CFB8C8, and sub_8301DFD8). Part of the 0x82CBD convert cluster (sibling of StrLenUTF16_8217AB30 / SetGlobalField_Thunk
# _82CC0750). 119 lines, 3 callees. 5 callers.
[entrypoint.functions."0x82CBD5A0"]
name = "ProcessAndClearAndConvert_82CBD5A0"

# A processor that constructs the structure (storing the values, zeroing the fields, and computing the offsets) and
# dispatches. It takes a structure (a: a field at a->8), a value (b), a value (c), and a value (d). When d is zero, it uses
# the default value (the .data pointer). It constructs the structure on the stack: storing the values (b, c, a, and the
# default value), zeroing the fields (four 32-bit fields), and computing the offsets (the stack pointers). It then reads a->8
# and performs the processing (ProcessAndGuardedDispatch_822C05F8). This is the "construct this structure (storing the
# values, zeroing the fields, and computing the offsets) and dispatch" routine for the 0x82BC6 construct cluster, used by 6
# callers (sub_821D2C70, sub_82306718, sub_82BC7950, sub_82BD0C20, sub_82BD62F8, and sub_82BD6588). Part of the 0x82BC6
# construct cluster (sibling of ProcessAndGuardedDispatch_822C05F8). 100 lines, 1 callee. 6 callers.
[entrypoint.functions."0x82BC6130"]
name = "ProcessAndConstructAndDispatch_82BC6130"

# A processor that queries the index, dispatches the loop, and selects the .data pointer (based on the type). It takes a
# structure (a: a structure at a->16, with two values at a->16->64 and a->16->68) and a structure (b: a value at b->0).
# It queries the index (ProcessAndQueryAndAdvance_82A2C6E0, on b->0). It reads the two values (a->16->68, a->16->64) and,
# when the condition is met (a->16->68 >= a->16->64), dispatches the loop (ProcessLoopDispatch_8227B8B8, on a). It then
# selects the .data pointer (based on the value b->0, the special value 27, selecting the appropriate pointer) and creates
# the descriptor node (CreateDescriptorNode_82BCC598 and others). This is the "query this index, dispatch the loop, and
# select the .data pointer (based on the type)" routine for the 0x82BCD query-select cluster, called only via the vtable (0
# direct callers). Part of the 0x82BCD query-select cluster (sibling of ProcessAndQueryAndAdvance_82A2C6E0 /
# CreateDescriptorNode_82BCC598). 216 lines, 5 callees. 0 direct callers (vtable dispatch).
[entrypoint.functions."0x82BCD460"]
name = "ProcessAndQueryAndSelect_82BCD460"

# A one-time initializer that initializes the structure (when the initialized bit is not set). It takes a value (a) and
# checks the initialized bit (the .data entry's low bit). When the bit is set (already initialized), it returns the existing
# structure. Otherwise it sets the bit (the .data entry |= 1), zeroes the field (the .data base + 22456 = 0), and performs
# the one-time initialization (sub_82CA3700, on the .data pointer). It returns the structure pointer. This is the "one-time
# initialize this structure (when the bit is not set, set it and perform the initialization)" routine for the 0x82C63 init
# cluster, used by 3 callers (sub_82C634F8, sub_82C638C0, and sub_82C63BE8). Part of the 0x82C63 init cluster (sibling of
# OneTimeInitCriticalSection_82B44BC8). 69 lines, 1 callee. 3 callers.
[entrypoint.functions."0x82C63320"]
name = "OneTimeInitStruct_82C63320"

# A processor that sets the vtable, dispatches the virtual function (when the field is non-null), and zeroes the field. It
# takes a structure (a: a vtable at a->0, a field at a->8). It sets the vtable (a->0, the .data pointer) and, when the field
# (a->8) is non-null, dispatches its virtual function (a->8->0->8, passing a). It then zeroes the field (a + 8->0 = 0) and
# performs the destruct (DestructRefCounted_82214F08). This is the "set this vtable, dispatch the virtual function (when the
# field is non-null), and zero the field" routine for the 0x82C63 destruct cluster, used by 10 callers (the 0x822F3/0x82415/
# 0x824E9/0x824EA/0x824EB/0x825F3/0x825F4/0x82C63 range). Part of the 0x82C63 destruct cluster (sibling of DestructRefCounted
# _82214F08 / OneTimeInitStruct_82C63320). 86 lines, 1 callee. 10 callers.
[entrypoint.functions."0x82C63398"]
name = "ProcessAndDestructAndZero_82C63398"

# A processor that converts the string to lower case, copies the reference, and constructs the reference (the string). It
# takes a structure (a: a field at a->4), a value (b), and a value (c). It reads the buffer, converts it to lower case
# (ProcessToLower_821EEA00), copies the reference (CopyAssign_RefCounted2_82265160), and constructs the reference
# (ConstructRefCounted_8222CF18, on the .data pointer). When the result is non-zero, it uses it; otherwise it performs the
# default handling. It also constructs the string (ConstructString_8217AB58), compares it (StringCompare_82CA3920),
# constructs and allocates (ConstructAndAllocate_82B43B08), atomically increments (ProcessAtomicIncrement_821E3950),
# dispatches the vcall (VirtualCall4_8260C130), and one-time initializes (OneTimeInitStruct_82C63320). This is the
# "convert this string to lower case, copy the reference, and construct the reference (the string)" routine for the 0x82C63
# construct cluster, used by 7 callers (sub_824154F0, sub_824EA150, sub_824EA6F0, sub_824EA7A0, sub_824EB648, sub_824EB910,
# and sub_82C64018). Part of the 0x82C63 construct cluster (sibling of ProcessToLower_821EEA00 / ConstructString_8217AB58).
# 467 lines, 13 callees. 7 callers.
[entrypoint.functions."0x82C638C0"]
name = "ProcessAndConstructLowerString_82C638C0"

# A processor that sets the vtable, atomically increments the counter, zeroes the field, and constructs the lower string.
# It takes a structure (a: a vtable at a->0, a counter at a->4, a field at a->8) and several values (b, c, d). It sets the
# vtable (a->0, the .data pointer), computes the counter pointer (a + 4), atomically increments the counter (AtomicIncrement3
# _821F7798, on a + 4), zeroes the field (a->8 = 0), and constructs the lower string (ProcessAndConstructLowerString
# _82C638C0, on a, b, c, and d). This is the "set this vtable, atomically increment the counter, zero the field, and
# construct the lower string" routine for the 0x82C64 construct cluster, used by 10 callers (the 0x822F3/0x822F4/0x82304/
# 0x8234E/0x82414/0x824E9/0x825F3/0x825F4/0x82932 range). Part of the 0x82C64 construct cluster (sibling of
# ProcessAndConstructLowerString_82C638C0 / AtomicIncrement3_821F7798). 55 lines, 2 callees. 10 callers.
[entrypoint.functions."0x82C64018"]
name = "ProcessAndConstructAndAtomicIncrement_82C64018"

# A processor that looks up the indexed pointer (based on the type) and returns the result. It takes a structure (a: a
# pointer at a->0, a type at a->4, a base at a->16) and a value (b). It gets the indexed pointer (GetIndexedPointer_82BC5448,
# on a). It reads the type (a->4) and, for the special types (5 and 7), it reads the pointer (a->0->8). Otherwise it computes
# the offset ((a->4 + 31) * 4) and reads the pointer (a->16 + offset). When the result is non-zero, it returns it; otherwise
# it returns 0. This is the "look up this indexed pointer (based on the type, the special types 5 and 7, or the general case)
# and return the result" routine for the 0x82BC5 lookup cluster, used by 10 callers (the 0x82BB4/0x82BB5/0x82BBA/0x82BBB/
# 0x82BC5/0x82BCF/0x82BD5/0x82BD7/0x82BDB range). Part of the 0x82BC5 lookup cluster (sibling of GetIndexedPointer_82BC5448).
# 83 lines, 1 callee. 10 callers.
[entrypoint.functions."0x82BC5D40"]
name = "ProcessAndLookupIndexedPointer_82BC5D40"

# A processor that dispatches the virtual function (when the field is non-null) and zeroes the field. It takes a structure
# (a: a field at a->8, with a vtable at a->8->0 and a method at a->8->0->8). It reads the field (a->8). When the field is
# non-null, it dispatches its virtual function (a->8->0->8, passing a). In either case, it zeroes the field (a->8 = 0). This
# is the "dispatch this virtual function (when the field is non-null) and zero the field" routine for the 0x82C62 destruct
# cluster, called only by sub_829FCBE0. Part of the 0x82C62 destruct cluster. 61 lines, 0 callees. 1 caller.
[entrypoint.functions."0x82C62E48"]
name = "ProcessAndDestructAndZeroField_82C62E48"

# A processor that, when the value is non-null, performs the lookup and stores the result. It takes a structure (a: a value
# at a->12), a value (b), and a destination (c: a field at c->0). When a->12 is zero, it returns 0. Otherwise it performs the
# processing (sub_82C88EE8, on a->12). When the result is non-zero, it performs the lookup (ProcessGlobalStruct_82C823D8, on
# the stack pointer) and stores the result into c->0. This is the "when this value is non-null, perform the lookup and store
# the result" routine for the 0x82C86 lookup cluster, used by 4 callers (sub_8235F430, sub_82361968, sub_82361E78, and sub
# _82931B88). Part of the 0x82C86 lookup cluster (sibling of ProcessGlobalStruct_82C823D8). 66 lines, 2 callees. 4 callers.
[entrypoint.functions."0x82C866B8"]
name = "ProcessAndLookupAndStore_82C866B8"

# A processor that searches the table (the linked list), matching the string at each node. It takes a structure (a: a head
# at a->32, with a next at a->32->4) and a string (b). It walks the linked list (a->32, advancing by a->32->4), and at each
# node it performs the table lookup (LockedTableLookup_82C81E58, on a->32 + 8) and matches the string (MatchStringWrapper
# _82CAAF08, comparing the node's string against b). When the string matches, it returns the node; otherwise it returns 0.
# This is the "search this table (the linked list), matching the string at each node" routine for the 0x82C88 search cluster,
# used by 5 callers (sub_82C86508, sub_82C86570, sub_82C865D8, sub_82C86640, and ProcessAndLookupAndStore_82C866B8). Part of
# the 0x82C88 search cluster (sibling of LockedTableLookup_82C81E58 / MatchStringWrapper_82CAAF08). 70 lines, 2 callees. 5
# callers.
[entrypoint.functions."0x82C88EE8"]
name = "ProcessAndSearchTable_82C88EE8"

# A processor that, when the condition is met, performs the processing (the specific case and the hash insertion). It takes
# a structure (a: a limit at a->8, a value at a->12, with a type at a->12->4). It reads a->12 and a->8. When a->12 exceeds
# a->8, or a->12 equals the default value, or a->12->4 is 2, it takes the specific path. Otherwise it performs the processing
# (sub_82BC68F0, with a, 1, and the .data pointer->8), and, for the specific case, it reads the typed field (GetTypedField
# _822AF338) and inserts the hash entry (InsertHashEntry_8219CDA8). This is the "when this condition is met, perform the
# processing (the specific case and the hash insertion)" routine for the 0x82BB6 specific-case cluster, called only via the
# vtable (0 direct callers). Part of the 0x82BB6 specific-case cluster (sibling of GetTypedField_822AF338 /
# InsertHashEntry_8219CDA8). 125 lines, 3 callees. 0 direct callers (vtable dispatch).
[entrypoint.functions."0x82BB6BC0"]
name = "ProcessAndDispatchSpecificCase_82BB6BC0"

# A processor that performs the destruct (dispatching the virtual function and zeroing the field) and, when the flag (low
# bit) is set, frees the buffer. It takes a value (a) and a value (b, with a low-bit flag). It performs the destruct
# (ProcessAndDestructAndZero_82C63398, dispatching the virtual function and zeroing the field) and, when the low bit of b is
# set (b & 1 != 0), it frees the buffer (Free_Thunk_824FE010). This is the "perform this destruct (dispatching the virtual
# function and zeroing the field) and, when the flag (low bit) is set, free the buffer" routine for the 0x82C63 destruct
# cluster, called only via the vtable (0 direct callers). Part of the 0x82C63 destruct cluster (sibling of ProcessAndDestruct
# AndZero_82C63398 / Free_Thunk_824FE010). 50 lines, 2 callees. 0 direct callers (vtable dispatch).
[entrypoint.functions."0x82C63530"]
name = "ProcessAndDestructAndFreeIfFlagged_82C63530"

# A small virtual-call thunk that calls the method at the structure's ->8->0->36 vtable slot. It takes a structure (a: a
# field at a->8, with a vtable at a->8->0 and method slot at a->8->0->36). When the field (a->8) is zero, it returns 0.
# Otherwise it reads the vtable (a->8->0->36) and calls the virtual method (a->8->0->36, passing a->8). This is the "call this
# virtual method (a->8->0->36, passing a->8, when the field is non-null)" thunk for the 0x82C6C vcall cluster, called only
# via the vtable (0 direct callers). Part of the 0x82C6C vcall cluster (sibling of VcallThunk_82C6FC00). 23 lines, 0 callees.
# 0 direct callers (vtable dispatch).
[entrypoint.functions."0x82C6C788"]
name = "VcallThunk_82C6C788"

# A processor that, when the value is non-null, performs the lookup, and when the lookup fails, performs the default handling
# (initializing the structure). It takes a structure (a: a field at a->0). When a is zero, it returns. Otherwise it performs
# the lookup (GetGlobalPointer_82239798, on a, then sub_82239468 with the global pointer, 0, and a). When the lookup fails
# (the result is zero), it performs the default handling: initializing the structure (sub_82CAB770), performing the setup
# (sub_82CC0758 and sub_82CAB700, on the initialized value), and storing the result into a->0. This is the "when this value
# is non-null, perform the lookup, and when the lookup fails, perform the default handling (initializing the structure)"
# routine for the 0x82CA5 lookup cluster, used by 10 callers (the 0x82C59/0x82CA4/0x82CAC/0x82CAE/0x82CB1/0x82CB5/0x83002/
# 0x832B5/0x832B6 range). Part of the 0x82CA5 lookup cluster (sibling of GetGlobalPointer_82239798). 59 lines, 5 callees. 10
# callers.
[entrypoint.functions."0x82CA5DC0"]
name = "ProcessAndLookupWithDefaultInit_82CA5DC0"

# A processor that processes the global structure, resolves the buffer pointer (the inline-vs-heap case), and calls the
# virtual function (writing the range). It takes a value (a), a structure (b: a pointer at b->4, a capacity at b->24), and a
# structure (c: a pointer at c->0, whose method is at c->0->40). It processes the global structure (ProcessGlobalStruct
# _82C823D8, on the stack pointer and the .data pointer). It resolves the buffer pointer: when b->24 is 16 or more, it uses
# b->4 (the heap pointer); otherwise it uses b + 4 (the inline buffer). It then calls the virtual function (c->0->40, passing
# c, the stack pointer, the buffer pointer, and the resolved value). It also performs the table lookup (LockedTableLookup
# _82C81E58) and writes the range (WriteRange_AtPos_821A8F68). This is the "process this global structure, resolve the buffer
# pointer (the inline-vs-heap case), and call the virtual function (writing the range)" routine for the 0x82C69 resolve-write
# cluster, used by 6 callers (sub_82C69570, sub_82C69840, sub_82C6A3A8, sub_82C6B390, sub_82C6B640, and sub_82C6BB68). Part of
# the 0x82C69 resolve-write cluster (sibling of ProcessGlobalStruct_82C823D8 / WriteRange_AtPos_821A8F68). 102 lines, 3
# callees. 6 callers.
[entrypoint.functions."0x82C694C0"]
name = "ProcessAndResolveAndWrite_82C694C0"

# A processor that, when the condition is met, performs the processing, and resolves the value (based on the comparison).
# It takes a structure (a: a structure at a->16, with two values at a->16->64 and a->16->68, a value at a->20, a value at
# a->40, a value at a->72, and a pointer at a->20->4) and a value (b). It reads the two values (a->16->68, a->16->64) and,
# when the condition is met (a->16->68 >= a->16->64), performs the processing (sub_82179350, on a and b). It then reads a->20
# and a->40 and, when they differ, resolves the value (a->20->4->0->12, otherwise a->72). It checks b (the value) and
# performs the further processing (sub_821E51D0 and sub_82A2C520). This is the "when this condition is met, perform the
# processing, and resolve the value (based on the comparison)" routine for the 0x82227 dispatch-resolve cluster, used by 10
# callers (the 0x824C6/0x826FC/0x82775/0x8278F/0x82795/0x827BF/0x8293F/0x82943/0x8295B/0x8296F range). Part of the 0x82227
# dispatch-resolve cluster. 164 lines, 3 callees. 10 callers.
[entrypoint.functions."0x822279A0"]
name = "ProcessAndDispatchAndResolve_822279A0"

# A processor that, when the flag is set, performs the processing (the reference copy, the hash computation, and the element
# append). It takes a structure (a: a pointer at a->0, with a flag at a->0->12 and a value at a->0->8) and several values (b,
# c). When the flag (a->0->12) is zero, it skips the processing. Otherwise it performs the processing (sub_824EF378), and,
# when the result is non-zero, it copies the reference (CopyAssign_RefCounted_821F0108, on a->0 + 8), computes the hash
# (FNV1Hash_821F3C28), appends the element (ProcessAndAppendElement_82229208), and performs the setup (sub_821DA550 and
# others). This is the "when this flag is set, perform the processing (the reference copy, the hash computation, and the
# element append)" routine for the 0x824EF reference-hash cluster, used by 10 callers (the 0x82513/0x82533/0x825E7/0x827CF/
# 0x827D4/0x82875/0x82897/0x828B6/0x82955/0x82957 range). Part of the 0x824EF reference-hash cluster (sibling of
# CopyAssign_RefCounted_821F0108 / FNV1Hash_821F3C28). 235 lines, 9 callees. 10 callers.
[entrypoint.functions."0x824EF160"]
name = "ProcessAndCopyReferenceAndHash_824EF160"

# A processor that computes the string length (the byte length) and appends the string node. It takes a value (a) and a
# structure (b: a field at b->8). When the global value (the .data entry) is set, it takes the alternate path. Otherwise it
# reads the .data pointer (the string base) and walks the bytes (advancing until the zero terminator), computing the byte
# length. It then appends the string node (ProcessAndAppendStringNode_82A1EA18, on b, the length, and the byte length) and
# performs the further processing (ProcessAndDispatchTypedValue_822286D0 and sub_822B9510). This is the "compute this string
# length (the byte length) and append the string node" routine for the 0x824EF string-append cluster, used by 2 callers (sub
# _824EF160 = ProcessAndCopyReferenceAndHash and sub_825B0D28). Part of the 0x824EF string-append cluster (sibling of
# ProcessAndAppendStringNode_82A1EA18 / ProcessAndDispatchTypedValue_822286D0). 138 lines, 3 callees. 2 callers.
[entrypoint.functions."0x824EF378"]
name = "ProcessAndAppendStringWithLength_824EF378"

# A processor that gets the indexed pointer, appends the node (copying the values), and finds the character (the delimiter).
# It takes a structure (a: a value at a->0, a value at a->4, a node pointer at a->8) and several values (b, c, d). It gets the
# indexed pointer (GetIndexedPointer_82BC5448, on a), appends the node (copying a->0 and a->4 into the node, advancing a->8 by
# 8), and finds the character (FindChar_82CA3980, with the delimiter 46). It then computes the hash (ComputeHash_8227BA30),
# dispatches the value (DispatchValue_821CF230), adjusts the position (ProcessAdjustPosition_82BCA340), removes the first
# element (ProcessAndRemoveFirstElement_82BC5630), and dispatches the loop (ProcessDispatchLoop_8229A518 and
# ProcessLoopDispatch_8227B8B8). This is the "get this indexed pointer, append the node (copying the values), and find the
# character (the delimiter)" routine for the 0x82BC7 append-find cluster, used by 3 callers (sub_82BC7060, sub_82BDB558, and
# sub_82BDB990). Part of the 0x82BC7 append-find cluster (sibling of GetIndexedPointer_82BC5448 / FindChar_82CA3980). 319
# lines, 8 callees. 3 callers.
[entrypoint.functions."0x82BC7328"]
name = "ProcessAndAppendAndFindChar_82BC7328"

# A checker that, when the field is non-null, reads the value and returns a bit derived from the leading-zero count of the
# value. It takes a structure (a: a field at a->8). When a->8 is zero, it returns 0. Otherwise it reads the value (LockedRead
# Subobject_82B44360, on the stack pointer), counts the leading zeros of the value (cntlzw), extracts a bit from the leading
# -zeros count, and returns the inverted bit. This is the "when this field is non-null, return a bit derived from the leading
 # -zero count of the value" routine for the 0x82C63 check cluster, used by 9 callers (the 0x82415/0x824EA/0x824EB/0x825F3/
# 0x825F4/0x829FC range). Part of the 0x82C63 check cluster (sibling of LockedReadSubobject_82B44360). 51 lines, 1 callee. 9
# callers.
[entrypoint.functions."0x82C63040"]
name = "CheckAndExtractBit_82C63040"

# A processor that updates the pointer (the linked list) and adjusts the connections (the prev/next pointers). It takes a
# structure (a: a pointer at a->4, with a connection at a->4->4) and a structure (b: a pointer at b->0, with a value at
# b->0->8, a byte at b->0->8->21, a connection at b->0->8->4, and a value at b->4). It updates the pointer (b->0 = b->0->8),
# and, when the byte (b->0->8->21) is zero, it sets the connection (b->0->8->4 = b). It then copies the value (b->0->4 = b->4).
# It checks the value (b vs a->4->4) and, when they differ, it adjusts the connections (updating the prev/next pointers: a->4->
# 4 = b->0, b->0->8 = b, b->4 = b->0, and the alternate path via b->4->8). This is the "update this pointer (the linked list)
# and adjust the connections (the prev/next pointers)" routine for the 0x82C23 linked-list cluster, used by 10 callers (sub
# _8218E0B0, sub_823453F8, sub_82356BE8, sub_823776F8, sub_82377B88, sub_825D8968, sub_825D93B0, sub_82A1AC50, sub_82C06478,
# and ProcessHashTable_829FC3C0). Part of the 0x82C23 linked-list cluster (sibling of ProcessHashTable_829FC3C0). 67 lines, 0
# callees. 10 callers.
[entrypoint.functions."0x82C23A48"]
name = "ProcessAndUpdateLinkedList_82C23A48"

# A search that finds the position of a character in a byte sequence (a string). It takes a byte sequence (a: a string, with
# bytes at a->0, a->1, ...) and a value (b, the character to search for). It reads the first byte (a->0) and checks the value
# (b). When b is zero, it searches for the zero terminator (the end of the string). Otherwise it walks the bytes (advancing
# the pointer until the byte equals b), returning the position of the character. This is the "find the position of this
# character in this byte sequence (a string)" routine for the 0x82CA7 char-search cluster, used by 10 callers (the 0x82B40/
# 0x82BDB/0x82BF3/0x82CAB/0x82CAF/0x82D51/0x82F00/0x82F14/0x8301D/0x831CA range). Part of the 0x82CA7 char-search cluster.
# 59 lines, 0 callees. 10 callers.
[entrypoint.functions."0x82CA70C0"]
name = "SearchStringForChar_82CA70C0"

# A small virtual-call thunk that calls the method at the structure's ->8->0->24 vtable slot. It takes a structure (a: a field
# at a->8, with a vtable at a->8->0 and method slot at a->8->0->24). It reads the vtable (a->8->0->24) and calls the virtual
# method (a->8->0->24, passing a->8). This is the "call this virtual method (a->8->0->24, passing a->8)" thunk for the 0x82C63
# vcall cluster, used by 2 callers (sub_824154F0 and sub_824E9A98). Part of the 0x82C63 vcall cluster (sibling of
# VcallThunk_82C6FC00 / VcallThunk_82C6C788). 34 lines, 0 callees. 2 callers.
[entrypoint.functions."0x82C63008"]
name = "VcallThunk_82C63008"

# A search that finds the offset of a substring in a string. It takes a structure (a: a string pointer at a->0, with a value at
# a->0->0). It reads the substring to search for (the .data pointer) and calls FindSubstring_821E6388, on the string (a->0) and
# the substring. When the substring is found (the result is non-zero), it computes the offset (the position of the substring in
# the string, as the difference between the found position and the string base). This is the "find this substring in this string
# and return the offset" routine for the 0x821E6 substring-search cluster, used by 10 callers (the 0x821BD/0x8223C/0x82265/
# 0x823F8/0x82414/0x82415/0x824E4/0x826E9/0x8285D/0x82C47 range). Part of the 0x821E6 substring-search cluster (sibling of
# FindSubstring_821E6388). 76 lines, 1 callee. 10 callers.
[entrypoint.functions."0x821E6308"]
name = "SearchAndFindSubstringOffset_821E6308"

# A processor that constructs the object and finds the substring offset (computing the offset). It takes a value (a) and
# several values (b, c, d). It constructs the object (ConstructRefCounted_8222CF18, on the stack pointer, a, and -1). It then
# reads the .data pointer (the substring) and finds the substring offset (SearchAndFindSubstringOffset_821E6308, on the stack
# pointer and the substring). When the result is negative, it takes the error path. Otherwise it reads the offset (r1->80, or
# r1->80->4 + 6) and performs the further processing (CopyAssign_RefCounted2_82265160, DestructRefCounted_82214F08, and others).
# This is the "construct this object and find the substring offset (computing the offset)" routine for the 0x82414 construct-
# offset cluster, called only via the vtable (0 direct callers). Part of the 0x82414 construct-offset cluster (sibling of
# ConstructRefCounted_8222CF18 / SearchAndFindSubstringOffset_821E6308). 180 lines, 8 callees. 0 direct callers (vtable
# dispatch).
[entrypoint.functions."0x82414F40"]
name = "ProcessAndConstructAndFindOffset_82414F40"

# A processor that performs two virtual calls (the dispatch and the secondary dispatch). It takes a structure (a: a pointer at
# a->0, with method slots at a->0->8 and a->0->0). It reads the first method slot (a->0->8) and calls the virtual method
# (a->0->8, passing a). It then reads the second method slot (a->0->0) and calls the virtual method (a->0->0, passing a and
# 1). This is the "perform this two virtual calls (the dispatch and the secondary dispatch)" routine for the 0x82415 two-vcall
# cluster, called only via the vtable (0 direct callers). Part of the 0x82415 two-vcall cluster. 49 lines, 0 callees. 0 direct
# callers (vtable dispatch).
[entrypoint.functions."0x82415070"]
name = "ProcessAndDispatchTwoVcalls_82415070"

# A processor that computes the count and calls the virtual function (the dispatch), then constructs and removes the element.
# It takes a structure (a: a value at a->8, a value at a->12) and a value (b). It reads a->8 and a->12, computes the count
# ((a->8 - a->12) >> 3 + 1), and calls the virtual function (the .data pointer, passing b and the stack pointers). When the
# result is non-zero, it performs the further processing: getting the indexed pointer (GetIndexedPointer_82BC5448), constructing
# and dispatching (ProcessAndConstructAndDispatch_82BC6130), removing the first element (ProcessAndRemoveFirstElement_82BC5630),
# and dispatching the loop (ProcessLoopDispatch_8227B8B8). This is the "compute this count and call the virtual function (the
# dispatch), then construct and remove the element" routine for the 0x82BC7 compute-dispatch cluster, used by 4 callers (sub
# _82BB1420, sub_82BD63C8, sub_82BD6698, and sub_82BDA9F0). Part of the 0x82BC7 compute-dispatch cluster (sibling of
# ProcessAndConstructAndDispatch_82BC6130 / GetIndexedPointer_82BC5448). 200 lines, 8 callees. 4 callers.
[entrypoint.functions."0x82BC7950"]
name = "ProcessAndComputeAndDispatch_82BC7950"

# A query that, when the field is non-null, returns the value (read under the lock). It takes a structure (a: a field at a->8).
# When a->8 is zero, it returns 0. Otherwise it reads the value (LockedReadSubobject_82B44360, on the stack pointer) and returns
# the value (r1->80). This is the "when this field is non-null, return the value (read under the lock)" routine for the 0x82C63
# locked-read cluster, used by 6 callers (sub_822F4958, sub_8234EE00, sub_823784A0, sub_82414F40, sub_824154F0, and sub_82A16DA0).
# Part of the 0x82C63 locked-read cluster (sibling of LockedReadSubobject_82B44360 / CheckAndExtractBit_82C63040). 45 lines, 1
# callee. 6 callers.
[entrypoint.functions."0x82C63098"]
name = "QueryLockedSubobject_82C63098"

# A processor that initializes the stack structure and processes the elements. It takes a value (a). It stores the initial
# values (the bytes and the .data pointer) into the stack structure, then processes the elements (ProcessElements_82BDCF20,
# on the .data pointer). It then reads the results (the byte at r1->134, the structure at r1 + 112) and performs the further
# processing (sub_82BC8870 and sub_82BCCB88). This is the "initialize this stack structure and process the elements" routine for
# the 0x82BDD init-process cluster, used by 1 caller (sub_82BDDB88). Part of the 0x82BDD init-process cluster (sibling of
# ProcessElements_82BDCF20). 132 lines, 3 callees. 1 caller.
[entrypoint.functions."0x82BDDA98"]
name = "ProcessAndInitializeAndProcessElements_82BDDA98"

# A processor that checks the type (the byte) and stores the value (based on the type), then computes the hash and dispatches
# the element. It takes a value (a), a value (b), and several other values (f, with a byte at f->0). It reads the byte (f->0)
# and checks the type (the values 64, 61, and 27). Based on the check, it stores the value (the .data pointer, or f, or f + 1)
# into r1->92. It then stores the values (r1->80 = a, r1->84 = b) and processes the elements (ProcessAndInitializeAndProcess
# Elements_82BDDA98). It also computes the hash (ComputeHash_8227BA30) and dispatches the element (DispatchElement_82BDD8A8).
# This is the "check this type (the byte) and store the value (based on the type)" routine for the 0x82BDD type-check cluster,
# called only via the vtable (0 direct callers). Part of the 0x82BDD type-check cluster (sibling of ComputeHash_8227BA30 /
# ProcessAndInitializeAndProcessElements_82BDDA98). 91 lines, 3 callees. 0 direct callers (vtable dispatch).
[entrypoint.functions."0x82BDDB88"]
name = "ProcessAndCheckTypeAndStore_82BDDB88"

# A processor that checks the value and calls the virtual function (the dispatch). It takes a structure (a: a value at a->12,
# a value at a->8), a structure (b: a method slot at b->60), and several values (c, d). It reads a->12 and, when it is zero, it
# performs the processing (sub_82C99808) and checks the value (the low byte of the result). It then reads b->60 and a->8 and
# calls the virtual function (b->60, passing b, a + 12, a->8, d, and the stack pointer). This is the "check this value and call
# the virtual function (the dispatch)" routine for the 0x82C9B check-dispatch cluster, used by 4 callers (sub_82C9B2F8,
# sub_82C9BE58, sub_82C9C3A8, and sub_82C9FB58). Part of the 0x82C9B check-dispatch cluster (sibling of sub_82C99808). 114
# lines, 1 callee. 4 callers.
[entrypoint.functions."0x82C9B1A8"]
name = "ProcessAndCheckAndDispatch_82C9B1A8"

# A processor that checks the value and returns the result (or zero). It takes a structure (a: a value at a->8, a value at
# a->12). It calls ProcessAndCheckAndDispatch_82C9B1A8, on a. When the result is zero, it returns 0. Otherwise it reads a->8
# and a->12 and, when they differ, it takes the alternate path. It then calls sub_82C99808, on a, and checks the value (the low
# byte of the result). This is the "check this value and return the result (or zero)" routine for the 0x82C9B check-return
# cluster, used by 10 callers (the 0x82C9B3/0x82C9BB/0x82C9C3/0x82C9C8/0x82C9C9/0x82C9D C/0x82C9EA/0x82C9FB range). Part of the
# 0x82C9B check-return cluster (sibling of ProcessAndCheckAndDispatch_82C9B1A8). 82 lines, 2 callees. 10 callers.
[entrypoint.functions."0x82C9B2F8"]
name = "ProcessAndCheckAndReturn_82C9B2F8"

# A processor that converts the string to lower case and constructs the reference-counted objects. It takes a value (a). It
# converts the string to lower case (ProcessToLower_821EEA00, on the stack pointer), then constructs the reference-counted
# objects (ConstructRefCounted_8222CF18, on the stack pointers and the .data pointers). It then reads the values (r1->88) and,
# when non-zero, performs the further processing: computing the hash (FNV1Hash_821F3C28), comparing the string (StringCompare
# _82CA3920), searching for the character (SearchStringForChar_82CA70C0), growing the bit array (ProcessGrowBitArray_821EC668),
# and constructing the 16-byte value (Construct16Byte_82275368). This is the "convert this string to lower case and construct the
# reference-counted objects" routine for the 0x82B40 lower-string cluster, used by 3 callers (sub_822F38B0, sub_82B3AEA0, and
# sub_82B3E560). Part of the 0x82B40 lower-string cluster (sibling of ProcessToLower_821EEA00 / ConstructRefCounted_8222CF18).
# 424 lines, 18 callees. 3 callers.
[entrypoint.functions."0x82B40708"]
name = "ProcessAndConstructLowerString_82B40708"

# A small getter that returns the field at offset 8 of the structure. It takes a structure (a: a field at a->8). It reads the
# field (a->8) and returns it. This is the "return this field (a->8)" routine for the 0x821CD field-getter cluster, called only
# via the vtable (0 direct callers). Part of the 0x821CD field-getter cluster. 8 lines, 0 callees. 0 direct callers (vtable
# dispatch).
[entrypoint.functions."0x821CDF88"]
name = "GetFieldAt8_821CDF88"

# A processor that applies the operation (the process) and accesses the element. It takes a structure (a: a pointer at a->4,
# with values at a->4->8, a->4->12, and a->4->16, and a pointer at a->4->16->92) and several values (b, c). It reads a->4->8
# and a->4->12, computes the value ((a->4->8 - a->4->12) >> 3), and applies the operation (ProcessApply_8227B7E0, on a->4 and the
# .data pointer). It then accesses the element (ElementAccess_821E1498, on a->4->16->92) and performs the further processing
# (GetIndexedPointer_82BC5448, GrowAndZeroBuffer_82BC55B8, InsertHashEntry_8219CDA8, and the dispatch). This is the "apply this
# operation (the process) and access the element" routine for the 0x821E1 apply-access cluster, used by 4 callers (sub_821D2BB0,
# sub_821D2C70, sub_822C52B0, and sub_82BB1830). Part of the 0x821E1 apply-access cluster (sibling of ElementAccess_821E1498 /
# ProcessApply_8227B7E0). 252 lines, 8 callees. 4 callers.
[entrypoint.functions."0x821E15A0"]
name = "ProcessAndApplyAndAccessElement_821E15A0"

# A processor that stores the values (the parameters) and calls the virtual function (the dispatch). It takes several values
# (a, b, c, d, e, f, g, h). It stores the values (c through h) into the stack structure, reads the value (r1->80), and calls the
# virtual function (sub_82CA4578, on the stack pointer, b, 260, the stack pointer, and the value). This is the "store this value
# (the parameters) and call the virtual function (the dispatch)" routine for the 0x82BF2 store-dispatch cluster, used by 5
# callers (sub_82BF3010, sub_82BF32C8, sub_82BF3428, sub_82BF3690, and sub_82BF3830). Part of the 0x82BF2 store-dispatch cluster
# (sibling of sub_82CA4578). 48 lines, 1 callee. 5 callers.
[entrypoint.functions."0x82BF2EC8"]
name = "ProcessAndStoreAndDispatch_82BF2EC8"

# A checker that determines whether a byte sequence (a string) contains any non-whitespace characters. It takes a byte sequence
# (a: a string, with bytes at a->0) and a value (b, the count). When b is zero or less, it returns 1. Otherwise it walks the
# bytes (advancing the pointer) and checks each one against the whitespace values (space 32, newline 10, carriage return 13, and
# tab 9). When a non-whitespace byte is found, it returns the result. This is the "check this byte sequence (a string) for any
# non-whitespace characters" routine for the 0x82C87 non-whitespace check cluster, used by 1 caller (sub_82C87E98). Part of the
# 0x82C87 non-whitespace check cluster. 61 lines, 0 callees. 1 caller.
[entrypoint.functions."0x82C87D48"]
name = "CheckForNonWhitespace_82C87D48"

# A processor that looks up the table and computes the length (the byte length). It takes a structure (a: a pointer at a->12,
# a byte at a->16) and several values (b, c). It performs the table lookup (LockedTableLookup_82C81E58, on a + 12). It then walks
# the bytes (advancing the pointer until the zero terminator), computing the byte length. It checks the byte (a->16) and, when it
# is set, performs the further processing (CheckForNonWhitespace_82C87D48, on the value and the length). This is the "look up this
# table and compute the length (the byte length)" routine for the 0x82C87 lookup-length cluster, called only via the vtable (0
# direct callers). Part of the 0x82C87 lookup-length cluster (sibling of LockedTableLookup_82C81E58 / CheckForNonWhitespace_82
# C87D48). 91 lines, 2 callees. 0 direct callers (vtable dispatch).
[entrypoint.functions."0x82C87E98"]
name = "ProcessAndLookupAndComputeLength_82C87E98"

# A processor that, when the condition is met, stores the value (the default) and performs the processing. It takes a structure
# (a: a field at a->0) and several values (b, c, d). When d is zero, it stores the default value (a->0 = 22), performs the
# processing (sub_82CAB770, and sub_82CAB630, on a and the zero values), and returns the result. Otherwise it checks the values
# (c and b) and performs the alternate processing (sub_82CAB7E0). This is the "when this condition is met, store the value (the
# default) and perform the processing" routine for the 0x82CA4 default-process cluster, used by 2 callers (sub_82CA44C8 and
# sub_82CA4588). Part of the 0x82CA4 default-process cluster. 164 lines, 3 callees. 2 callers.
[entrypoint.functions."0x82CA43B0"]
name = "ProcessAndStoreDefaultAndProcess_82CA43B0"

# A processor that checks the values and performs the processing (the default). It takes a value (a), a value (b), a value (c),
# and several other values (f, g). When c is zero, or a is zero, or b is zero, it performs the default processing (sub_82CAB770).
# Otherwise it reads the .data pointer and performs the processing (ProcessAndStoreDefaultAndProcess_82CA43B0, on the .data
# pointer, a, b, and c). This is the "check these values and perform the processing (the default)" routine for the 0x82CA4 check-
# default cluster, used by 2 callers (sub_82CA3230 and sub_82CA4578). Part of the 0x82CA4 check-default cluster (sibling of
# ProcessAndStoreDefaultAndProcess_82CA43B0). 102 lines, 3 callees. 2 callers.
[entrypoint.functions."0x82CA44C8"]
name = "ProcessAndCheckAndProcessDefault_82CA44C8"

# A small thunk that calls the function (sub_82CA44C8, passing the values). It takes several values (a, b, c, d). It calls the
# function (ProcessAndCheckAndProcessDefault_82CA44C8, on a, b, and c). This is the "call this function (sub_82CA44C8, passing the
# values)" thunk for the 0x82CA4 thunk cluster, used by 6 callers (ProcessAndStoreAndDispatch_82BF2EC8, sub_82584D48, sub_82C87
# DC0, sub_82C87F30, sub_82BF8C50, and sub_82EE4808). Part of the 0x82CA4 thunk cluster (sibling of ProcessAndCheckAndProcess
# Default_82CA44C8). 11 lines, 1 callee. 6 callers.
[entrypoint.functions."0x82CA4578"]
name = "Thunk_82CA4578"

# A processor that buffers the data and performs the lookup (the global), then performs the processing. It takes a value (a), a
# value (b), and a value (c). It stores the initial values into the stack structure. When a is zero, it performs the default
# processing (sub_82CAB770, storing 22 into a->0). Otherwise it buffers the data (Buffer_Append_82CAE410 and Buffer_PushByte
# _82CAE378), performs the lookup (LookupMasked_8223F970 and ProcessAndLookupWithDefaultInit_82CA5DC0), resolves the global
# (ProcessAndResolveGlobal_82CA3C68), and performs the further processing. This is the "buffer this data and perform the lookup
# (the global)" routine for the 0x82CAE buffer-lookup cluster, called only via the vtable (0 direct callers). Part of the 0x82CAE
# buffer-lookup cluster (sibling of Buffer_Append_82CAE410 / ProcessAndLookupWithDefaultInit_82CA5DC0). 1805 lines, 13 callees.
# 0 direct callers (vtable dispatch).
[entrypoint.functions."0x82CAE4A8"]
name = "ProcessAndBufferAndLookup_82CAE4A8"

# A processor that constructs the 32-byte vector and initializes the fields (the value and the float). It takes a structure (a:
# a field at a->0, a byte at a->192, a float at a->196). It constructs the 32-byte vector (Construct32ByteVector_8321FB58, on a).
# It then reads the .data pointers and initializes the fields: storing 0 into a->192, the value (the .data pointer) into a->0, and
# the float (the .data pointer) into a->196. This is the "construct this 32-byte vector and initialize the fields (the value and
# the float)" routine for the 0x8323B vector-init cluster, used by 1 caller (sub_8323B270). Part of the 0x8323B vector-init
# cluster (sibling of Construct32ByteVector_8321FB58). 52 lines, 1 callee. 1 caller.
[entrypoint.functions."0x8323B220"]
name = "ProcessAndConstructVectorAndInit_8323B220"

# A processor that walks the chain (finds the value) and releases the reference. It takes a structure (a: a field at a->0, a
# field at a->4) and a value (b). It reads the .data pointer and walks the chain (WalkChainFind_83239FA8, on the .data pointer).
# When the result is non-zero, it stores 0 into a->0 and a->4 and releases the reference (Release_RefCounted_829FF648, on the
# stack pointer). When the result is zero, it allocates the object (Allocate_OrDbgBreak_8221F388), processes the refcount
# (ProcessRefCount_83225538), sets the wrapper (SetRefcountedWrapper_8238A848), and constructs the vector (ProcessAndConstruct
# VectorAndInit_8323B220). This is the "walk this chain (finds the value) and release the reference" routine for the 0x8323B walk-
# chain cluster, used by 1 caller (StateDispatch4_83220888). Part of the 0x8323B walk-chain cluster (sibling of WalkChainFind
# _83239FA8 / Release_RefCounted_829FF648). 177 lines, 6 callees. 1 caller.
[entrypoint.functions."0x8323B270"]
name = "ProcessAndWalkChainAndRelease_8323B270"

# A processor that reads the byte (the element) and updates the counters (the index and the count). It takes a value (a), a
# structure (b: a value at b->4, a pointer at b->12, a value at b->20), and several values (c, d). When b->20 is less than 1, it
# takes the alternate path (r5 = 1). Otherwise it reads b->12 and b->4, updates the counters (b->12 = b->12 + 1, b->20 = b->20 -
# 1, b->4 = b->4 + 1), reads the byte (b->12->0), and stores the byte into the stack pointer. It then performs the further
# processing (ProcessAndCopyStructRegion_82A1B480, ProcessFloatData_832206E8, and ProcessGrowAppend3_8321FC38). This is the
# "read this byte (the element) and update the counters (the index and the count)" routine for the 0x8323B read-element cluster,
# called only via the vtable (0 direct callers). Part of the 0x8323B read-element cluster (sibling of ProcessAndCopyStructRegion
# _82A1B480). 207 lines, 3 callees. 0 direct callers (vtable dispatch).
[entrypoint.functions."0x8323B650"]
name = "ProcessAndReadElementAndGrow_8323B650"

# A processor that copies the values (the node) and increments the refcount (the atomic operation). It takes a value (a), a
# structure (b: a value at b->-4, a value at b->0, a value at b->4), a value (c), and a structure (d: a field at d->0, a field at
# d->8). When b equals c, it returns. Otherwise it copies the values (b->-4, b->0, and b->4) into the node (d->0, the computed
# pointer, and d->8). It then increments the refcount (the atomic operation, lwarx/stwcx, on b->4). This is the "copy this value
# (the node) and increment the refcount (the atomic operation)" routine for the 0x8290F copy-node cluster, used by 4 callers (sub
# _8246E3C8, sub_82601D80, sub_826703C0, and sub_8292F4E0). Part of the 0x8290F copy-node cluster. 75 lines, 0 callees. 4
# callers.
[entrypoint.functions."0x8290F610"]
name = "ProcessAndCopyNodeAndIncrement_8290F610"

# A processor that performs the linear search and, when found, stores the value (the result). It takes a value (a), a value (b),
# a structure (c: a field at c->0), and a structure (d: a field at d->0). It reads c->0 and computes the value (c->0 >> 5). It
# performs the linear search (LinearSearch_82284448, on b and the computed value). When the result is non-zero (found), it stores
# the value (d->0 = r3->4, c->0 = r3->0) and returns 1. Otherwise it returns 0. This is the "perform this linear search and, when
# found, store the value (the result)" routine for the 0x82BAC search-store cluster, called only via the vtable (0 direct callers).
# Part of the 0x82BAC search-store cluster (sibling of LinearSearch_82284448). 64 lines, 1 callee. 0 direct callers (vtable
# dispatch).
[entrypoint.functions."0x82BAC8A8"]
name = "ProcessAndSearchAndStoreResult_82BAC8A8"

# A processor that queries and constructs the tree, and constructs the 104-byte object. It takes a value (a), a value (b), and a
# structure (c: a field at c->8). When c is non-zero, it stores 0 into c->8 and performs the processing (sub_82BFF768, on c and
# the .data pointer). It then queries and constructs the tree (ProcessAndQueryConstructTree_82C02FC8, on b), constructs the
# 104-byte object (Construct104ByteObject_82B48E30, on the stack pointer, b, 0, and 65536), and performs the further processing
# (ConstructAndProcessNode_82C02E88, DestructAndWalkTree_82C02F68, and SetAndDispatch_82366210). This is the "query and construct
# this tree, and construct the 104-byte object" routine for the 0x82BFC tree-construct cluster, used by 2 callers (sub_82BFD228
# and sub_82BFD4A8). Part of the 0x82BFC tree-construct cluster (sibling of ProcessAndQueryConstructTree_82C02FC8 /
# Construct104ByteObject_82B48E30). 314 lines, 12 callees. 2 callers.
[entrypoint.functions."0x82BFCEF8"]
name = "ProcessAndConstructTreeAndObject_82BFCEF8"

# A processor that performs the virtual calls (the dispatch) and checks the results, then grows the array. It takes a structure
# (a: a pointer at a->0, with method slots at a->0->0 and a->0->20) and a structure (b: a value at b->8). It calls the virtual
# function (a->0->0, passing a) and checks the result (it must equal 1). It then calls the virtual function (a->0->20, passing a)
# and checks the result (it must equal 1). It reads b->8 and, when non-zero, performs the further processing: allocating the
# object (Allocate_OrDbgBreak_8221F388), growing the array (ProcessGrow_82C21C90), pushing back (Array_PushBack_82C21E08), and
# performing the virtual calls (VirtualCallPair_82C01D60). This is the "perform this virtual calls (the dispatch) and check the
# results" routine for the 0x82C18 dispatch-grow cluster, used by 1 caller (sub_82C1C9D8). Part of the 0x82C18 dispatch-grow
# cluster (sibling of VirtualCallPair_82C01D60 / Array_PushBack_82C21E08). 471 lines, 8 callees. 1 caller.
[entrypoint.functions."0x82C18C08"]
name = "ProcessAndDispatchAndGrow_82C18C08"

# A processor that sorts the values (the bubble sort) and performs the recursive search. It takes a structure (a: a value at
# a->0, a value at a->8) and a value (b). It sorts the values (BubbleSort3_82C16C78, on a and b + 4). It then reads a->8 and,
# when it is non-zero, it computes the value (a->8 << 2, the size) and performs the recursive search (ProcessAndRecursiveSearch
# _82C1B600, on the stack pointer and the size). It also iterates the elements (ProcessAndIterateElements_82C15C10). This is the
# "sort this values (the bubble sort) and perform the recursive search" routine for the 0x82C1B sort-search cluster, used by 2
# callers (sub_82C1CBD8 and sub_82C1CD68). Part of the 0x82C1B sort-search cluster (sibling of BubbleSort3_82C16C78 /
# ProcessAndRecursiveSearch_82C1B600). 70 lines, 3 callees. 2 callers.
[entrypoint.functions."0x82C1B920"]
name = "ProcessAndSortAndSearch_82C1B920"

# A processor that sorts the values (the bubble sort) and performs the recursive search (the alternate form). It takes a
# structure (a: a value at a->12, a pointer at a->4, with a value at a->4->0) and a value (b). It sorts the values (BubbleSort3
# _82C16C78, on a + 4 and b + 4). It then reads a->12 and, when it is non-zero, it computes the value (a->12 << 2, the size) and
# performs the recursive search (ProcessAndRecursiveSearch_82C1B600, on the stack pointer and the size). It also grows the array
# (ProcessGrow_82C21C90), compares the string (StrCmpIgnoreCase_82CA6320), and frees the thunk (Free_Thunk_824FE010). This is the
# "sort this values (the bubble sort) and perform the recursive search" routine for the 0x82C1C sort-search cluster, used by 2
# callers (sub_82C1CBD8 and sub_82C1CD68). Part of the 0x82C1C sort-search cluster (sibling of ProcessAndSortAndSearch_82C1B920
# / BubbleSort3_82C16C78). 312 lines, 6 callees. 2 callers.
[entrypoint.functions."0x82C1C170"]
name = "ProcessAndSortAndSearch2_82C1C170"

# A processor that performs the virtual calls (the dispatch) and checks the results, then clears the array and finds the value.
# It takes a structure (a: a pointer at a->0, with method slots at a->0->0 and a->0->20) and a structure (b: a value at b->8).
# It calls the virtual function (a->0->0, passing a) and checks the result (it must equal 1). It then calls the virtual function
# (a->0->20, passing a) and checks the result (it must equal 1). It reads b->8 and, when it is non-zero, it performs the further
# processing: clearing the array (ClearElementArray_82C2D228 and CompactClearArray_82C39298), constructing the sorted array
# (ConstructSortedArray_82C3D028), and finding the value (SortedFind_82C1B998 and SortedFind2_82C3ADF0). This is the "perform this
# virtual calls (the dispatch) and check the results" routine for the 0x82C1C dispatch-find cluster, used by 1 caller (sub_82C1D158).
# Part of the 0x82C1C dispatch-find cluster (sibling of ProcessAndDispatchAndGrow_82C18C08 / ConstructSortedArray_82C3D028). 230
# lines, 9 callees. 1 caller.
[entrypoint.functions."0x82C1C9D8"]
name = "ProcessAndDispatchAndFind_82C1C9D8"

# A processor that performs the virtual calls (the dispatch) and checks the results. It takes a structure (a: a pointer at a->0,
# with method slots at a->0->24 and a->0->28) and a value (c). It calls the virtual function (a->0->24, passing a) and checks the
# value (the low byte of the result). When the value is non-zero, it calls the processing (ProcessAndDispatchAndFind_82C1C9D8, on
# a and c) and checks the value again (the low byte of the result). When the value is non-zero, it reads a->0->28 and calls the
# virtual function (a->0->28, passing a). This is the "perform this virtual calls (the dispatch) and check the results" routine for
# the 0x82C1D dispatch-check cluster, used by 1 caller (sub_82C1D970). Part of the 0x82C1D dispatch-check cluster (sibling of
# ProcessAndDispatchAndFind_82C1C9D8). 86 lines, 1 callee. 1 caller.
[entrypoint.functions."0x82C1D158"]
name = "ProcessAndDispatchAndCheck_82C1D158"

# A processor that performs the virtual call (the dispatch) and returns the value (the field). It takes a structure (a: a pointer
# at a->0, with a method slot at a->0->4). It reads a->0 and a->0->4, and calls the virtual function (a->0->4, passing a and the
# .data pointer). When the result is zero, it returns. Otherwise it reads the value (r3->12) and returns it. This is the "perform
# this virtual call (the dispatch) and return the value (the field)" routine for the 0x82C1E dispatch-return cluster, called only
# via the vtable (0 direct callers). Part of the 0x82C1E dispatch-return cluster. 51 lines, 0 callees. 0 direct callers (vtable
# dispatch).
[entrypoint.functions."0x82C1E820"]
name = "ProcessAndDispatchAndReturnField_82C1E820"

# A processor that performs the virtual call (the dispatch) and returns the value (the field, the alternate form). It takes a
# structure (a: a pointer at a->0, with a method slot at a->0->4). It reads a->0 and a->0->4, and calls the virtual function
# (a->0->4, passing a and the .data pointer). When the result is zero, it returns. Otherwise it reads the value (r3->12) and
# returns it. This is the "perform this virtual call (the dispatch) and return the value (the field)" routine for the 0x82C1F
# dispatch-return cluster, called only via the vtable (0 direct callers). Part of the 0x82C1F dispatch-return cluster (sibling of
# ProcessAndDispatchAndReturnField_82C1E820). 51 lines, 0 callees. 0 direct callers (vtable dispatch).
[entrypoint.functions."0x82C1F8B8"]
name = "ProcessAndDispatchAndReturnField2_82C1F8B8"

# A processor that performs the virtual call (the dispatch) and returns the value (the field, the third form). It takes a
# structure (a: a pointer at a->0, with a method slot at a->0->4). It reads a->0 and a->0->4, and calls the virtual function
# (a->0->4, passing a and the .data pointer). When the result is zero, it returns. Otherwise it reads the value (r3->12) and
# returns it. This is the "perform this virtual call (the dispatch) and return the value (the field)" routine for the 0x82C21
# dispatch-return cluster, called only via the vtable (0 direct callers). Part of the 0x82C21 dispatch-return cluster (sibling of
# ProcessAndDispatchAndReturnField_82C1E820 / ProcessAndDispatchAndReturnField2_82C1F8B8). 51 lines, 0 callees. 0 direct callers
# (vtable dispatch).
[entrypoint.functions."0x82C215F8"]
name = "ProcessAndDispatchAndReturnField3_82C215F8"

# A processor that performs the virtual call (the dispatch) and returns the value (the field, the fourth form). It takes a
# structure (a: a pointer at a->0, with a method slot at a->0->4). It reads a->0 and a->0->4, and calls the virtual function
# (a->0->4, passing a and the .data pointer). When the result is zero, it returns. Otherwise it reads the value (r3->12) and
# returns it. This is the "perform this virtual call (the dispatch) and return the value (the field)" routine for the 0x82C23
# dispatch-return cluster, called only via the vtable (0 direct callers). Part of the 0x82C23 dispatch-return cluster (sibling of
# ProcessAndDispatchAndReturnField_82C1E820 / ProcessAndDispatchAndReturnField3_82C215F8). 51 lines, 0 callees. 0 direct callers
# (vtable dispatch).
[entrypoint.functions."0x82C23B20"]
name = "ProcessAndDispatchAndReturnField4_82C23B20"

# A processor that performs the virtual call (the dispatch) and returns the value (the field, the fifth form). It takes a
# structure (a: a pointer at a->0, with a method slot at a->0->4). It reads a->0 and a->0->4, and calls the virtual function
# (a->0->4, passing a and the .data pointer). When the result is zero, it returns. Otherwise it reads the value (r3->12) and
# returns it. This is the "perform this virtual call (the dispatch) and return the value (the field)" routine for the 0x82C26
# dispatch-return cluster, called only via the vtable (0 direct callers). Part of the 0x82C26 dispatch-return cluster (sibling of
# ProcessAndDispatchAndReturnField_82C1E820 / ProcessAndDispatchAndReturnField4_82C23B20). 51 lines, 0 callees. 0 direct callers
# (vtable dispatch).
[entrypoint.functions."0x82C266D8"]
name = "ProcessAndDispatchAndReturnField5_82C266D8"

# A processor that performs the virtual call (the dispatch) and returns the value (the field, the sixth form). It takes a
# structure (a: a pointer at a->0, with a method slot at a->0->4). It reads a->0 and a->0->4, and calls the virtual function
# (a->0->4, passing a and the .data pointer). When the result is zero, it returns. Otherwise it reads the value (r3->12) and
# returns it. This is the "perform this virtual call (the dispatch) and return the value (the field)" routine for the 0x82C28
# dispatch-return cluster, called only via the vtable (0 direct callers). Part of the 0x82C28 dispatch-return cluster (sibling of
# ProcessAndDispatchAndReturnField_82C1E820 / ProcessAndDispatchAndReturnField5_82C266D8). 51 lines, 0 callees. 0 direct callers
# (vtable dispatch).
[entrypoint.functions."0x82C28290"]
name = "ProcessAndDispatchAndReturnField6_82C28290"

# A processor that performs the virtual call (the dispatch) and returns the value (the field, the seventh form). It takes a
# structure (a: a pointer at a->0, with a method slot at a->0->4). It reads a->0 and a->0->4, and calls the virtual function
# (a->0->4, passing a and the .data pointer). When the result is zero, it returns. Otherwise it reads the value (r3->12) and
# returns it. This is the "perform this virtual call (the dispatch) and return the value (the field)" routine for the 0x82C29
# dispatch-return cluster, called only via the vtable (0 direct callers). Part of the 0x82C29 dispatch-return cluster (sibling of
# ProcessAndDispatchAndReturnField_82C1E820 / ProcessAndDispatchAndReturnField6_82C28290). 51 lines, 0 callees. 0 direct callers
# (vtable dispatch).
[entrypoint.functions."0x82C29420"]
name = "ProcessAndDispatchAndReturnField7_82C29420"

# A processor that performs the virtual call (the dispatch) and returns the value (the field, the eighth form). It takes a
# structure (a: a pointer at a->0, with a method slot at a->0->4). It reads a->0 and a->0->4, and calls the virtual function
# (a->0->4, passing a and the .data pointer). When the result is zero, it returns. Otherwise it reads the value (r3->12) and
# returns it. This is the "perform this virtual call (the dispatch) and return the value (the field)" routine for the 0x82C2A
# dispatch-return cluster, called only via the vtable (0 direct callers). Part of the 0x82C2A dispatch-return cluster (sibling of
# ProcessAndDispatchAndReturnField_82C1E820 / ProcessAndDispatchAndReturnField7_82C29420). 51 lines, 0 callees. 0 direct callers
# (vtable dispatch).
[entrypoint.functions."0x82C2A920"]
name = "ProcessAndDispatchAndReturnField8_82C2A920"

# A processor that performs the virtual call (the dispatch) and returns the value (the field, the ninth form). It takes a
# structure (a: a pointer at a->0, with a method slot at a->0->4). It reads a->0 and a->0->4, and calls the virtual function
# (a->0->4, passing a and the .data pointer). When the result is zero, it returns. Otherwise it reads the value (r3->12) and
# returns it. This is the "perform this virtual call (the dispatch) and return the value (the field)" routine for the 0x82C2C
# dispatch-return cluster, called only via the vtable (0 direct callers). Part of the 0x82C2C dispatch-return cluster (sibling of
# ProcessAndDispatchAndReturnField_82C1E820 / ProcessAndDispatchAndReturnField8_82C2A920). 51 lines, 0 callees. 0 direct callers
# (vtable dispatch).
[entrypoint.functions."0x82C2CF20"]
name = "ProcessAndDispatchAndReturnField9_82C2CF20"

# A processor that performs the virtual call (the dispatch) and returns the value (the field, the tenth form). It takes a
# structure (a: a pointer at a->0, with a method slot at a->0->4). It reads a->0 and a->0->4, and calls the virtual function
# (a->0->4, passing a and the .data pointer). When the result is zero, it returns. Otherwise it reads the value (r3->12) and
# returns it. This is the "perform this virtual call (the dispatch) and return the value (the field)" routine for the 0x82C2F
# dispatch-return cluster, called only via the vtable (0 direct callers). Part of the 0x82C2F dispatch-return cluster (sibling of
# ProcessAndDispatchAndReturnField_82C1E820 / ProcessAndDispatchAndReturnField9_82C2CF20). 51 lines, 0 callees. 0 direct callers
# (vtable dispatch).
[entrypoint.functions."0x82C2F6C0"]
name = "ProcessAndDispatchAndReturnField10_82C2F6C0"

# A processor that performs the virtual call (the dispatch) and returns the value (the field, the eleventh form). It takes a
# structure (a: a pointer at a->0, with a method slot at a->0->4). It reads a->0 and a->0->4, and calls the virtual function
# (a->0->4, passing a and the .data pointer). When the result is zero, it returns. Otherwise it reads the value (r3->12) and
# returns it. This is the "perform this virtual call (the dispatch) and return the value (the field)" routine for the 0x82C30
# dispatch-return cluster, called only via the vtable (0 direct callers). Part of the 0x82C30 dispatch-return cluster (sibling of
# ProcessAndDispatchAndReturnField_82C1E820 / ProcessAndDispatchAndReturnField10_82C2F6C0). 51 lines, 0 callees. 0 direct callers
# (vtable dispatch).
[entrypoint.functions."0x82C30958"]
name = "ProcessAndDispatchAndReturnField11_82C30958"

# A processor that performs the virtual call (the dispatch) and returns the value (the field, the twelfth form). It takes a
# structure (a: a pointer at a->0, with a method slot at a->0->4). It reads a->0 and a->0->4, and calls the virtual function
# (a->0->4, passing a and the .data pointer). When the result is zero, it returns. Otherwise it reads the value (r3->12) and
# returns it. This is the "perform this virtual call (the dispatch) and return the value (the field)" routine for the 0x82C32
# dispatch-return cluster, called only via the vtable (0 direct callers). Part of the 0x82C32 dispatch-return cluster (sibling of
# ProcessAndDispatchAndReturnField_82C1E820 / ProcessAndDispatchAndReturnField11_82C30958). 51 lines, 0 callees. 0 direct callers
# (vtable dispatch).
[entrypoint.functions."0x82C32188"]
name = "ProcessAndDispatchAndReturnField12_82C32188"

# A processor that performs the virtual call (the dispatch) and returns the value (the field, the thirteenth form). It takes a
# structure (a: a pointer at a->0, with a method slot at a->0->4). It reads a->0 and a->0->4, and calls the virtual function
# (a->0->4, passing a and the .data pointer). When the result is zero, it returns. Otherwise it reads the value (r3->12) and
# returns it. This is the "perform this virtual call (the dispatch) and return the value (the field)" routine for the 0x82C34
# dispatch-return cluster, called only via the vtable (0 direct callers). Part of the 0x82C34 dispatch-return cluster (sibling of
# ProcessAndDispatchAndReturnField_82C1E820 / ProcessAndDispatchAndReturnField12_82C32188). 51 lines, 0 callees. 0 direct callers
# (vtable dispatch).
[entrypoint.functions."0x82C34D88"]
name = "ProcessAndDispatchAndReturnField13_82C34D88"

# A processor that performs the virtual call (the dispatch) and returns the value (the field, the fourteenth form). It takes a
# structure (a: a pointer at a->0, with a method slot at a->0->4). It reads a->0 and a->0->4, and calls the virtual function
# (a->0->4, passing a and the .data pointer). When the result is zero, it returns. Otherwise it reads the value (r3->12) and
# returns it. This is the "perform this virtual call (the dispatch) and return the value (the field)" routine for the 0x82C39
# dispatch-return cluster, called only via the vtable (0 direct callers). Part of the 0x82C39 dispatch-return cluster (sibling of
# ProcessAndDispatchAndReturnField_82C1E820 / ProcessAndDispatchAndReturnField13_82C34D88). 51 lines, 0 callees. 0 direct callers
# (vtable dispatch).
[entrypoint.functions."0x82C39040"]
name = "ProcessAndDispatchAndReturnField14_82C39040"

# A processor that performs the virtual call (the dispatch) and returns the value (the field, the fifteenth form). It takes a
# structure (a: a pointer at a->0, with a method slot at a->0->4). It reads a->0 and a->0->4, and calls the virtual function
# (a->0->4, passing a and the .data pointer). When the result is zero, it returns. Otherwise it reads the value (r3->12) and
# returns it. This is the "perform this virtual call (the dispatch) and return the value (the field)" routine for the 0x82C3B
# dispatch-return cluster, called only via the vtable (0 direct callers). Part of the 0x82C3B dispatch-return cluster (sibling of
# ProcessAndDispatchAndReturnField_82C1E820 / ProcessAndDispatchAndReturnField14_82C39040). 51 lines, 0 callees. 0 direct callers
# (vtable dispatch).
[entrypoint.functions."0x82C3BCE8"]
name = "ProcessAndDispatchAndReturnField15_82C3BCE8"

# A processor that performs the virtual call (the dispatch) and returns the value (the field, the sixteenth form). It takes a
# structure (a: a pointer at a->0, with a method slot at a->0->4). It reads a->0 and a->0->4, and calls the virtual function
# (a->0->4, passing a and the .data pointer). When the result is zero, it returns. Otherwise it reads the value (r3->12) and
# returns it. This is the "perform this virtual call (the dispatch) and return the value (the field)" routine for the 0x82C40
# dispatch-return cluster, called only via the vtable (0 direct callers). Part of the 0x82C40 dispatch-return cluster (sibling of
# ProcessAndDispatchAndReturnField_82C1E820 / ProcessAndDispatchAndReturnField15_82C3BCE8). 51 lines, 0 callees. 0 direct callers
# (vtable dispatch).
[entrypoint.functions."0x82C40348"]
name = "ProcessAndDispatchAndReturnField16_82C40348"

# A processor that performs the virtual call (the dispatch) and returns the value (the field, the seventeenth form). It takes a
# structure (a: a pointer at a->0, with a method slot at a->0->4). It reads a->0 and a->0->4, and calls the virtual function
# (a->0->4, passing a and the .data pointer). When the result is zero, it returns. Otherwise it reads the value (r3->12) and
# returns it. This is the "perform this virtual call (the dispatch) and return the value (the field)" routine for the 0x82C41
# dispatch-return cluster, called only via the vtable (0 direct callers). Part of the 0x82C41 dispatch-return cluster (sibling of
# ProcessAndDispatchAndReturnField_82C1E820 / ProcessAndDispatchAndReturnField16_82C40348). 51 lines, 0 callees. 0 direct callers
# (vtable dispatch).
[entrypoint.functions."0x82C41288"]
name = "ProcessAndDispatchAndReturnField17_82C41288"

# A processor that performs the virtual call (the dispatch) and returns the value (the field, the eighteenth form). It takes a
# structure (a: a pointer at a->0, with a method slot at a->0->4). It reads a->0 and a->0->4, and calls the virtual function
# (a->0->4, passing a and the .data pointer). When the result is zero, it returns. Otherwise it reads the value (r3->12) and
# returns it. This is the "perform this virtual call (the dispatch) and return the value (the field)" routine for the 0x82C42
# dispatch-return cluster, called only via the vtable (0 direct callers). Part of the 0x82C42 dispatch-return cluster (sibling of
# ProcessAndDispatchAndReturnField_82C1E820 / ProcessAndDispatchAndReturnField17_82C41288). 51 lines, 0 callees. 0 direct callers
# (vtable dispatch).
[entrypoint.functions."0x82C420F8"]
name = "ProcessAndDispatchAndReturnField18_82C420F8"

# A processor that performs the virtual call (the dispatch) and returns the value (the field, the nineteenth form). It takes a
# structure (a: a pointer at a->0, with a method slot at a->0->4). It reads a->0 and a->0->4, and calls the virtual function
# (a->0->4, passing a and the .data pointer). When the result is zero, it returns. Otherwise it reads the value (r3->12) and
# returns it. This is the "perform this virtual call (the dispatch) and return the value (the field)" routine for the 0x82C43
# dispatch-return cluster, called only via the vtable (0 direct callers). Part of the 0x82C43 dispatch-return cluster (sibling of
# ProcessAndDispatchAndReturnField_82C1E820 / ProcessAndDispatchAndReturnField18_82C420F8). 51 lines, 0 callees. 0 direct callers
# (vtable dispatch).
[entrypoint.functions."0x82C432E8"]
name = "ProcessAndDispatchAndReturnField19_82C432E8"

# A processor that formats a string based on a 5-way type tag. It takes a type tag (r7: 1-5), a source string pointer, a flags
# word (r4), a second value (r5), and a flags word (r8) whose individual bits are unpacked into a bitfield. It maps the type tag
# to a format code (1->2, 2->5, 3->1, 4->3, 5->4-or-0 by a flag bit; other values dispatch to sub_82CC1C38 and return -1). It
# initializes an ANSI string over the source buffer (RtlInitAnsiString) and records whether the source ends in a backslash (92).
# It assembles a result struct on the stack (ANSI string, type code, and a bitfield of extracted flags, with bits 0x40/0x1000 set
# for specific flag combinations) and calls the virtual method selected from the global vtable. This is the "format this string
# for this type tag" routine, part of the 0x82C26-0x82CBC string/format cluster. 288 lines. 5 callees (incl __imp__RtlInitAnsiString,
# sub_82CC1C38, SetGlobalField_Thunk_82CC0750). 10 callers.
[entrypoint.functions."0x82CBCC00"]
name = "ProcessAndFormatStringWithType_82CBCC00"

# A setter that assigns a string buffer to an object. It takes an object (a: a buffer pointer at a->0) and a source string (a byte
# pointer). When a->0 is already set, it increments that buffer's refcount (AtomicIncrement5) and grows the bit array for the
# string (GrowBitArrayForString). When a->0 is empty and the source string is non-empty, it allocates a 16-byte buffer descriptor
# (Allocate_OrDbgBreak), zeros it, walks the source to compute its byte length, reassigns the buffer from the source
# (ReassignBuffer), sets the low flag word to 1, and stores the new descriptor at a->0. If a had a previous buffer it releases it
# (Release_RefCounted). This is the "set the string buffer on this object" routine, part of the 0x821DA string-buffer cluster.
# 129 lines. 5 callees (AtomicIncrement5, GrowBitArrayForString, Allocate_OrDbgBreak, ReassignBuffer, Release_RefCounted). 10 callers.
[entrypoint.functions."0x821DA550"]
name = "SetStringBuffer_821DA550"

# A processor that frees/updates a heap block. It takes a heap context (a: a free-bitmap base), a block pointer, a size word, and a
# flag. It walks backward from the block by a 16-byte-aligned size and validates the block size (rejecting blocks over 60KB / 61440).
# When the flag is set, it splices the block into the doubly-linked free list (connecting the previous/next block pointers) and, for
# small blocks (< 128), toggles the corresponding bit in the free bitmap (bit index derived from the block size, XORed with 1). It
# then follows the block's flags to continue processing the surrounding blocks. This is a large leaf routine (no callees) that
# manages the free-list and free-bitmap state for a single heap block, part of the 0x82239 heap-management cluster (called from
# Heap_FreeBlock and the heap lookup). 560 lines. 0 callees. 4 callers.
[entrypoint.functions."0x82239058"]
name = "ProcessAndFreeHeapBlock_82239058"

# A processor that frees/commits heap memory. It takes a heap manager (a: a structure with a state flag at a->20 and a process-type
# tag at a->379) and two parameters. It enters a critical section (RtlEnterCriticalSection) to serialize access, checks the state
# flag (bit 13) and the current process type (KeGetCurrentProcessType) against the manager's tag, and on mismatch logs an error
# (log code 244 / 4414). It then walks the heap's blocks, releasing them (NtFreeVirtualMemory) and updating the free-list/free-bitmap
# state via ProcessAndFreeHeapBlock, and raises a bug check (KeBugCheckEx) on failure. This is the central heap free/commit routine,
# part of the 0x82239 heap-management cluster (called from MemFree and the exception handlers). 357 lines. 12 callees. 11 callers.
[entrypoint.functions."0x82239468"]
name = "ProcessAndFreeHeapMemory_82239468"

# A releaser that conditionally releases the heap's critical section. It takes a heap manager (a: a structure whose critical-section
# pointer is at a->1408) and a flag value. When the flag is non-zero, it loads the critical-section pointer and leaves it
# (RtlLeaveCriticalSection); otherwise it does nothing. This is the "release the heap lock" helper paired with the heap free/commit
# routine, part of the 0x82239 heap-management cluster. 45 lines. 1 callee (__imp__RtlLeaveCriticalSection). 3 callers
# (ProcessAndFreeHeapMemory, Heap_FreeBlock, and the heap tail-call helper).
[entrypoint.functions."0x82239708"]
name = "ReleaseHeapLock_82239708"

# A shrinker that shrinks a buffer down to fit its content. It takes a buffer (a: data pointer at a->0, capacity at a->4, an aligned
# pointer plus a flag bit at a->8) and a target size. When the target size is smaller than the current capacity, it allocates a new
# 4-byte-aligned buffer of (target + 4) bytes, copies the target bytes from the old buffer (MemCpy_Vectorized), null-terminates the
# copy, frees the old buffer (Free_SizeBucketed_Thunk), and updates a->0/a->4/a->8 (storing the new aligned pointer in a->8 while
# preserving its low flag bit). It then zeroes the unused tail of the new buffer (MemSet_SdkRuntime). When the target size is not
# smaller, it does nothing. This is the "shrink the buffer to fit" routine, part of the 0x821B7 buffer cluster. 95 lines. 4 callees
# (Allocate_OrDbgBreak, MemCpy_Vectorized, Free_SizeBucketed_Thunk, MemSet_SdkRuntime). 6 callers (incl ProcessAndConstructLowerString).
[entrypoint.functions."0x821B7330"]
name = "ShrinkBufferToFit_821B7330"

# A processor that shrinks a buffer (or resets it to empty). It takes a structure (a: a buffer pointer at a->0) and a target size. It
# reads the buffer's capacity (a->0->4). When the capacity is larger than the target size, it either constructs a default 16-byte
# buffer (Construct16Byte, when the target size is zero) or increments the buffer's refcount (AtomicIncrement5) and shrinks the buffer
# down to the target size (ShrinkBufferToFit). When the capacity is not larger, it does nothing. This is the "shrink this buffer to
# the target size" wrapper, part of the 0x82B3A buffer cluster. 73 lines. 3 callees (AtomicIncrement5, Construct16Byte,
# ShrinkBufferToFit). 3 callers (incl ProcessAndConstructLowerString).
[entrypoint.functions."0x82B3A1B8"]
name = "ProcessAndShrinkBuffer_82B3A1B8"

# A processor that constructs a lowercased string from a buffer (alternate form). It takes a result slot (a: a string at a->0), a
# buffer (b: a structure whose method table is at b->0->20), and a flag word. When the flag's low byte is set, it calls the buffer's
# virtual method (b->0->20) to extract a string into a stack temporary, converts it to lowercase (ProcessToLower), constructs a new
# string from it (ProcessConstructString), and stores the result at a->0. It then destroys both temporaries (DestructRefCounted).
# This is the "build a lowercase copy of this buffer's string" routine, part of the 0x82A1C string cluster (sibling of
# ProcessAndConstructLowerString_82B40708). 164 lines. 4 callees (ProcessToLower, ProcessConstructString, ProcessAndCopyStructRegion,
# DestructRefCounted). 4 callers.
[entrypoint.functions."0x82A1C7F0"]
name = "ProcessAndConstructLowerString2_82A1C7F0"

# A thunk that forwards to the object constructor. It takes the same arguments and unconditionally tail-calls ProcessAndConstructObject
# (a 3-line forward, no logic of its own). This is the "construct object" thunk, part of the 0x82BF2 construct-object cluster. 3 lines.
# 1 callee (ProcessAndConstructObject). 1 caller (sub_82BF0CE0).
[entrypoint.functions."0x82BF2348"]
name = "ConstructObject_Thunk_82BF2348"

# A processor that queries a hash table. It takes a hash-table container (a: a flag byte at a->4, a bucket count at a->8, a subobject at
# a->16, and a result slot at a->0), a key string (a byte pointer), and a flag. When the table is not yet initialized (a->8 is zero) and
# the flag is set, it initializes the table: it calls the subobject's virtual method (a->16->0, with a size hint of 256) to build the
# table, stores the result at a->0, and sets the flag/count (a->4 = 6, a->8 = 64). It then computes a hash of the key string using a
# multiply-xor loop (multiplier 0xF4213) and derives the bucket index (hash & (bucket_count - 1)), and walks the bucket to resolve the
# entry. This is the central "query this hash table for the key" routine, part of the 0x82C99 hash-table cluster. 580 lines. 1 callee
# (MemSet_SdkRuntime). 9 callers (all within the 0x82C9x cluster).
[entrypoint.functions."0x82C99298"]
name = "ProcessAndQueryHashTable_82C99298"

# A constructor that builds an object. It takes an object (a: a structure of ~200 bytes) and constructs a 32-byte vector inside it
# (Construct32ByteVector), stores a .data pointer at a->0 and zeroes a flag byte at a->192. It then constructs a refcounted temporary
# (ConstructRefCounted) holding a .data string, converts it to lowercase (ProcessToLower), builds a string from it
# (ProcessConstructString), and stores that string at a->196, destroying the temporaries (DestructRefCounted) afterwards. This is the
# "construct this object" routine, part of the 0x83217 construct-object cluster. 77 lines. 5 callees (Construct32ByteVector,
# ConstructRefCounted, ProcessToLower, ProcessConstructString, DestructRefCounted). 1 caller (sub_832172A8).
[entrypoint.functions."0x83217228"]
name = "ConstructObject_83217228"

# A processor that looks up an object in a chain and constructs one if it is not present. It takes a result slot (a: pointers at a->0
# and a->4) and a key value. It searches the chain (WalkChainFind); when an entry is found, it clears the result slot (a->0 = a->4 = 0)
# and releases the stack temporary (Release_RefCounted). When no entry is found, it allocates a 208-byte object (Allocate_OrDbgBreak),
# constructs it (ConstructObject), installs a refcounted wrapper (SetRefcountedWrapper), and atomically increments the shared refcount
# (ProcessRefCount, using the lwarx/stwcx global-lock sequence). This is the "look up this object or create a new one" routine, part of
# the 0x83217 object-lookup cluster. 177 lines. 6 callees (WalkChainFind, Allocate_OrDbgBreak, ConstructObject, SetRefcountedWrapper,
# ProcessRefCount, Release_RefCounted). 1 caller (StateDispatch4).
[entrypoint.functions."0x832172A8"]
name = "ProcessAndLookupOrConstruct_832172A8"

# A processor that shifts an element out of a buffer and processes it. It takes four arguments (a context, a buffer structure b: a
# length at b->4, a read pointer at b->12, and a remaining count at b->20, plus two more values). When the remaining count is at least
# one, it advances the read pointer and decrements the count (shifting one byte out, storing it on the stack); otherwise it copies a
# struct region (ProcessAndCopyStructRegion) to seed the value. It then builds a lowercased string from the value
# (ProcessAndConstructLowerString2), grows/appends the result (ProcessGrowAppend3), handles floating-point data (ProcessFloatData),
# and dispatches the built state (BuildAndDispatch). This is the "shift one element and process it" routine, called only via the
# vtable (0 direct callers), part of the 0x83217 element-processing cluster. 212 lines. 5 callees. 0 direct callers (vtable dispatch).
[entrypoint.functions."0x83217688"]
name = "ProcessAndShiftElement_83217688"

# A thunk that forwards to an unresolved routine. It takes the same arguments and unconditionally tail-calls sub_82CA6BE8 (a 3-line
# forward, no logic of its own; the callee body is not recovered). This is the "forward" thunk, part of the 0x82BF2 cluster. 3 lines.
# 1 callee (sub_82CA6BE8, unresolved). 1 caller (sub_82BF0CE0).
[entrypoint.functions."0x82BF2358"]
name = "Thunk_82BF2358"

# A processor that resolves a value. It takes a result slot (a: a value at a->0) and two further values. When the result slot is null
# or the second value is zero, it calls sub_82CAB770, writes the default value 22 into a->0, calls sub_82CAB630 with all-zero
# arguments, and returns 22. Otherwise it performs the full resolution path over the supplied values (both callee bodies, sub_82CAB770
# and sub_82CAB630, are unrecovered in this project). This is the "resolve this value (falling back to the 22 default)" routine, part
# of the 0x82CA6 resolve cluster (forwarded from Thunk_82BF2358). 162 lines. 2 callees (sub_82CAB770, sub_82CAB630, both unresolved).
# 1 caller (Thunk_82BF2358).
[entrypoint.functions."0x82CA6BE8"]
name = "ProcessAndResolveValue_82CA6BE8"

# A processor that dispatches a value and stores the result. It takes a structure (a: a value at a->0 and a result pointer at a->8).
# It resolves an indexed pointer (GetIndexedPointer), dispatches the value at a->0 (DispatchValue) into a temporary, and copies the
# resulting two-word value back into the memory just before a->8 (at a->8 - 8 and a->8 - 4). This is the "dispatch this value and
# store the two-word result" routine, part of the 0x82BC5 dispatch cluster. 49 lines. 2 callees (GetIndexedPointer, DispatchValue).
# 8 callers (all within the 0x82BBx cluster).
[entrypoint.functions."0x82BC5C88"]
name = "ProcessAndDispatchValue_82BC5C88"

# A processor that validates index/bounds before a lookup. It takes an optional pointer. When the pointer is null it raises an assert
# (sub_82BF2B68 with three .data format strings and code 835). It then loads a bounds table from .data and traps (ppc_trap 22) if the
# high bound (table->8) is not greater than the low bound (table->12). It validates the requested index against the 16-byte and 4-byte
# bound tables (ValidateBounds16, ValidateBounds4) and resolves the matching entry. This is the "validate these bounds" routine, part of
# the 0x82BEC bounds-validation cluster. 179 lines. 3 callees (ValidateBounds16, ValidateBounds4, sub_82BF2B68). 5 callers.
[entrypoint.functions."0x82BEC640"]
name = "ProcessAndValidateBounds_82BEC640"

# A processor that writes an element into a circular buffer. It takes a buffer (a: a count at a->4, a head pointer at a->8, and a tail
# pointer at a->12) and an element (a value pointer). It computes the head position in 4-byte units ((a->8 - a->4) >> 2) and compares
# it against the tail position ((a->12 - a->4) >> 2). When there is room (head < tail), it stores the element at the head and advances
# the head pointer. When the buffer is full, it validates the head (trapping on an out-of-range count) and calls sub_82BF0378 (an
# unresolved helper) to handle the wrap/resize. This is the "write this element into the circular buffer" routine, part of the 0x82BF0
# circular-buffer cluster. 93 lines. 1 callee (sub_82BF0378, unresolved). 7 callers.
[entrypoint.functions."0x82BF0458"]
name = "ProcessAndWriteToBuffer_82BF0458"

# A processor that sorts a set of elements. It takes a structure (a: an element base pointer at a->0 and a count at a->8) and a value.
# It first performs a bubble-sort pass over the elements (BubbleSort3), and then, when the count is non-zero, runs a quick-sort over the
# 4-byte-aligned element array (QuickSort2, using a->0 as the base, the count*4 byte size, a zero flag, and the count). This is the
# "sort these elements" routine, part of the 0x82C28 sort cluster. 65 lines. 2 callees (BubbleSort3, QuickSort2). 2 callers.
[entrypoint.functions."0x82C28E80"]
name = "ProcessAndSortElements_82C28E80"

# A processor that sorts a set of elements (alternate form). It takes a structure (a: an element base at a->0/a->4 and a count at a->12)
# and a value. It first performs a bubble-sort pass over the elements (BubbleSort3), and then, when the count is non-zero, runs a
# quick-sort over the 4-byte-aligned element array (QuickSort2). It also manages the underlying storage, freeing (Free_Thunk) and growing
# (ProcessGrow) the element array as needed. This is the longer "sort these elements" routine, part of the 0x82C28 sort cluster (sibling
# of ProcessAndSortElements_82C28E80, sharing the same two callers). 268 lines. 4 callees (BubbleSort3, QuickSort2, Free_Thunk,
# ProcessGrow). 2 callers.
[entrypoint.functions."0x82C28EF0"]
name = "ProcessAndSortElements2_82C28EF0"

# A processor that validates index/bounds before a lookup (alternate form). It takes an optional pointer. When the pointer is null it
# raises an assert (sub_82BF2B68 with three .data format strings and code 902). It then loads a bounds table from .data and traps
# (ppc_trap 22) if the high bound (table->8) is not greater than the low bound (table->12). It validates the requested index against the
# 16-byte and 4-byte bound tables (ValidateBounds16, ValidateBounds4) and resolves the matching entry. This is the sibling "validate
# these bounds" routine, part of the 0x82BEC bounds-validation cluster (sibling of ProcessAndValidateBounds_82BEC640, differing in the
# assert code and bounds-table offset). 176 lines. 3 callees (ValidateBounds16, ValidateBounds4, sub_82BF2B68). 3 callers.
[entrypoint.functions."0x82BEC8B0"]
name = "ProcessAndValidateBounds2_82BEC8B0"

# A clamping helper that clamps a value and copies the result. It takes two structures (a and b: each with a word at offset 28 and a
# word at offset 32). It calls ClampAndUpdate (passing a, b, 0, and -1) to perform the clamping, then copies the two result words
# (b->28 and b->32) into a (a->28 and a->32). This is the "clamp this value and copy the two result words" routine, part of the
# 0x822F4 clamp cluster. 50 lines. 1 callee (ClampAndUpdate). 1 caller (sub_82B3E560).
[entrypoint.functions."0x822F4100"]
name = "ClampAndCopyFields_822F4100"

# A processor that indexes into an element array. It takes a range (a: a low bound at a->4 and a high bound at a->8) and an index.
# When a->4 is set, it computes the element count as (a->8 - a->4) / 36 and checks the index against it; otherwise it calls sub_8233DB18
# (an unresolved helper). It validates the range (trapping with ppc_trap 22 if the low bound exceeds the high bound) and computes the
# element position (index << 3, a 32-byte stride) for the lookup. This is the "index into this element array" routine, part of the
# 0x8233D index cluster. 110 lines. 1 callee (sub_8233DB18, unresolved). 4 callers.
[entrypoint.functions."0x8233D880"]
name = "ProcessAndIndexElement_8233D880"

# A processor that updates a node in a linked list. It takes five arguments (a context, a node whose flag is at node->8, and three
# further values). It checks the node's flag against a mask (0x1FFFFEFE); when it matches, it sets a string (SetString) and resolves a
# reference (sub_826C3EF0). It then allocates storage (Allocate_OrDbgBreak), splices a node into the list (List_SpliceNode4), and
# updates the linked-list pointers (ProcessAndUpdateLinkedList), along with several further sub-routines. This is a large "update this
# node in the list" routine, part of the 0x82344 node-update cluster. 339 lines. 9 callees (Allocate_OrDbgBreak, List_SpliceNode4,
# ProcessAndUpdateLinkedList, SetString, and five further helpers). 2 callers.
[entrypoint.functions."0x82344608"]
name = "ProcessAndUpdateNode_82344608"

# A constructor that builds a node from a source. It takes a target structure (a) and a source node (plus a few scalar values). It
# initializes a's fields from the supplied values (a->0, a->4, a->8, and a->12 from the source's first word), copy-assigns the
# refcounted sub-object at a->16 from the source (CopyAssign_RefCounted), sets a flag byte at a->20, and zeroes the byte at a->21.
# This is the "construct this node from the source" routine, part of the 0x82345 node-construction cluster. 60 lines. 1 callee
# (CopyAssign_RefCounted). 2 callers (ProcessAndUpdateNode, sub_8240ACB8).
[entrypoint.functions."0x82345910"]
name = "ConstructAndCopyNode_82345910"

# A processor that formats/constructs a string (large routine). It takes a context structure, a value, a source structure (with a data
# pointer at src->4 and a kind tag at src->24), and a further value. It finds the common 16-bit prefix of the source data
# (FindCommonPrefix16), lazily resolves data (LazyGetData), builds a lowercased copy (ProcessAndConstructLowerString) and a
# type-formatted string (ProcessAndFormatStringWithType), allocates and constructs the resulting string elements
# (ConstructRefCounted, ConstructStringElement, ProcessElementAccess3), and clamps/copies the final fields (ClampAndCopyFields),
# releasing and destructing the temporaries (Release_RefCounted, Destruct3, DestructRefCounted, Free_SizeBucketed_Thunk) as it goes.
# This is a large "process and format this string" routine, part of the 0x82B3E string-format cluster. 661 lines. 19 callees. 1 caller
# (sub_826C0868).
[entrypoint.functions."0x82B3E560"]
name = "ProcessAndFormatString_82B3E560"

# A processor that dispatches by a type tag. It takes a structure (a: a type tag at a->4, a value at a->0, a pointer at a->8, and a
# table at a->16). It resolves an indexed pointer (GetIndexedPointer) and reads a value from just before a->8 (falling back to 0 when
# the high word is zero). It then dispatches on the type tag: when the tag is 5 it performs one path, when it is 7 it stores the value
# into a sub-object (a->0->8), and otherwise it computes a 4-byte-stride index ((tag + 31) << 2) into the a->16 table and stores the
# value there. It finally invokes the state dispatcher (StateDispatch) to act on the result. This is the "dispatch this value by its
# type tag" routine, part of the 0x82BC5 dispatch cluster. 166 lines. 2 callees (GetIndexedPointer, StateDispatch). 12 callers.
[entrypoint.functions."0x82BC5FF0"]
name = "ProcessAndDispatchByType_82BC5FF0"

# A processor that resolves a value (alternate form). It takes a result slot (a: a value at a->0) and two further values. When the
# result slot is null or the relevant argument is zero, it calls sub_82CAB770, writes the default value 22 into a->0, and calls
# sub_82CAB630 with all-zero arguments. On the non-trivial path it measures a UTF-16 string length (StrLenUTF16), clears/converts the
# value (ProcessAndClearAndConvert), and resolves it through sub_82CC0758. This is the "resolve this value (falling back to the 22
# default)" routine, part of the 0x82CA9 resolve cluster (sibling of ProcessAndResolveValue_82CA6BE8). 401 lines. 5 callees
# (StrLenUTF16, ProcessAndClearAndConvert, sub_82CAB630, sub_82CAB770, sub_82CC0758). 2 callers.
[entrypoint.functions."0x82CA9A60"]
name = "ProcessAndResolveValue2_82CA9A60"

# A processor that resolves a value (third form). It takes a result structure, an optional byte slot, an optional value slot, and two
# more values. It clears the supplied slots (writing 0 into the byte and value slots when present) and then resolves the value by
# delegating to ProcessAndResolveValue2 (sub_82CA9A60) and the default helpers (sub_82CAB630, sub_82CAB770). This is the "clear the
# slots and resolve this value" routine, part of the 0x82CA9 resolve cluster (sibling of ProcessAndResolveValue2_82CA9A60). 186 lines.
# 3 callees (sub_82CA9A60, sub_82CAB630, sub_82CAB770). 1 caller (sub_82CA9E78).
[entrypoint.functions."0x82CA9D38"]
name = "ProcessAndResolveValue3_82CA9D38"

# A thunk that forwards to the value resolver with a fixed argument. It sets r8 to 0 and unconditionally tail-calls
# ProcessAndResolveValue3 (sub_82CA9D38), passing the first three arguments through unchanged (a 3-line forward, no logic of its own).
# This is the "resolve this value" thunk, part of the 0x82CA9 resolve cluster. 3 lines. 1 callee (ProcessAndResolveValue3). 3 callers
# (incl ProcessAndFormatString).
[entrypoint.functions."0x82CA9E78"]
name = "ResolveValue_Thunk_82CA9E78"

# A copier that copies a bounded UTF-16 string and zero-pads the remainder. It takes a destination, a source, and a maximum count
# (in 16-bit code units). It copies up to the count 16-bit values from the source to the destination, stopping early when it reads a
# null terminator. After the copy, if there is room left (count not exhausted), it zero-fills the remaining destination code units up
# to the count. This is the "copy this bounded UTF-16 string and zero-pad the rest" routine, a leaf helper (no callees) used by the
# string formatter, part of the 0x82CAA string cluster. 58 lines. 0 callees. 1 caller (ProcessAndFormatString).
[entrypoint.functions."0x82CAAAC8"]
name = "CopyAndZeroPadUtf16_82CAAAC8"

# An appender that appends a UTF-16 string. It takes a destination (a null-terminated UTF-16 buffer) and a source (a UTF-16 string).
# It first advances the destination pointer to the end of the existing string (looping over 16-bit code units until a null
# terminator), then copies 16-bit code units from the source to the destination until the source's null terminator. This is the
# "append this UTF-16 string" routine (an wcscat-style helper), a leaf function (no callees) used by the string formatter, part of the
# 0x82CAA string cluster (sibling of CopyAndZeroPadUtf16_82CAAAC8). 35 lines. 0 callees. 1 caller (ProcessAndFormatString).
[entrypoint.functions."0x82CAAB28"]
name = "AppendUtf16String_82CAAB28"

# A comparator that compares two UTF-16 strings (bounded). It takes two UTF-16 strings (a and b) and a maximum count (in 16-bit code
# units). When the count is zero it returns 0. Otherwise it walks both strings in lockstep, up to the count, stopping at the first
# mismatch or a null terminator in the first string. It then returns the difference of the two 16-bit code units at the stopping
# position (0 when they match, negative/positive indicating which is smaller/larger). This is the "compare these two bounded UTF-16
# strings" routine (a wcsncmp-style helper), a leaf function (no callees) used by the string formatter and other routines, part of the
# 0x82CAA string cluster (sibling of CopyAndZeroPadUtf16_82CAAAC8 / AppendUtf16String_82CAAB28). 51 lines. 0 callees. 4 callers.
[entrypoint.functions."0x82CAAB80"]
name = "CompareUtf16String_82CAAB80"

# A querier that queries a file's attributes. It takes a file path (an ANSI string). It initializes a local ANSI string over the path
# (RtlInitAnsiString) and calls the Windows kernel to query the full file attributes (NtQueryFullAttributesFile, with a file-attributes
# mask of 0xFFFFFFFF and a buffer size of 64). On success it returns the file-attribute flags word from the result; on failure it calls
# sub_82CC1C38 (an error helper) and returns -1. This is the "query this file's attributes" routine, part of the 0x82CBD file-query
# cluster. 62 lines. 3 callees (RtlInitAnsiString, NtQueryFullAttributesFile, sub_82CC1C38). 9 callers.
[entrypoint.functions."0x82CBD368"]
name = "QueryFileAttributes_82CBD368"

# A querier that calls a Windows API and returns a value. It sets up a local result struct on the stack (at r1+80 and r1+88) and calls
# a Windows API function (resolved through a .data function pointer) with the result struct, a size of 4, and a query code of 26. When
# the call returns a non-negative status, it returns the value stored in the result struct (r1+80); on failure it calls sub_82CC1C38
# (an error helper) and returns -1. This is the "query this value via the Windows API" wrapper, part of the 0x82CC1 API-wrapper
# cluster. 55 lines. 1 callee (sub_82CC1C38, the shared error helper). 1 caller (ProcessAndFormatString).
[entrypoint.functions."0x82CC1360"]
name = "QueryAndReturnValue_82CC1360"

# A getter that resolves a data pointer and a clamped length. It takes a structure (a: a base at a->184, a total size at a->188, an
# offset at a->216, and a length at a->220) plus two output pointers. It computes the data pointer as (a->184 + a->216) and the
# usable length as the smaller of a->220 and (a->188 - a->216) (i.e. the length clamped so it does not run past the end of the total
# size), stores the pointer to *out1 and the length to *out2, and returns 1 (success). This is the "get the data pointer and its
# clamped length" routine, a leaf function (no callees) called only via the vtable (0 direct callers), part of the 0x832B6
# data-access cluster. ~33 lines. 0 callees. 0 direct callers (vtable dispatch).
[entrypoint.functions."0x832B6A60"]
name = "GetDataPointerAndLength_832B6A60"

# A consumer that advances a read position by a given amount. It takes a structure (a: a total size at a->188, a flag at a->208, an
# extra field at a->212, an offset at a->216, and a remaining length at a->220) and an amount. It advances the offset by the amount
# (wrapping around past the total size when it goes past the end) and decrements the remaining length by the amount. When the remaining
# length reaches zero (and the a->208 and a->212 fields are clear), it performs a follow-up through sub_832B6A98. This is the
# "consume this many bytes and advance the read position" routine, called only via the vtable (0 direct callers), part of the 0x832B6
# data-access cluster. 1 callee (sub_832B6A98). 0 direct callers (vtable dispatch).
[entrypoint.functions."0x832B6C38"]
name = "ConsumeAndAdvance_832B6C38"

# A processor that reads a value (or resolves it). It takes a structure (a) and an index. It first attempts to read a converted value
# (ProcessAndReadConvertedValue); when that yields a non-zero result it returns that value. Otherwise it resolves the indexed pointer
# (GetIndexedPointer), and when the resolved kind is not 3 it attempts a further resolution through sub_82BCBD48 (and sub_82BC68F0).
# This is the "read this value (falling back to resolving it)" routine, part of the 0x82BC6 value-read cluster. 70 lines. 4 callees
# (ProcessAndReadConvertedValue, GetIndexedPointer, sub_82BCBD48, sub_82BC68F0). 12 callers (all within the 0x82BBx/0x82BDx cluster).
[entrypoint.functions."0x82BC6F00"]
name = "ProcessAndReadOrResolveValue_82BC6F00"

# A querier that calls a Windows API and stores a 64-bit value. It takes a 64-bit output pointer. It sets up a local result struct on the
# stack (at r1+80 and r1+96) and calls a Windows API function (resolved through a .data function pointer) with the result struct, a size
# of 56, and a query code of 34. When the call returns a non-negative status, it stores the 64-bit value from the result struct to *out
# and returns 1 (success); on failure it calls sub_82CC1C38 (the shared error helper) and returns 0. This is the "query this 64-bit value
# via the Windows API" wrapper, part of the 0x82CC1 API-wrapper cluster (sibling of QueryAndReturnValue_82CC1360). 65 lines. 1 callee
# (sub_82CC1C38). 2 callers.
[entrypoint.functions."0x82CC1460"]
name = "QueryAndStoreValue_82CC1460"

# A constructor that builds an object. It takes a size (r29). When the size is zero it allocates a fixed 320-byte object (through
# sub_82BF2B68) using a default descriptor; when it is non-zero it uses that size. It then reads a descriptor from a .data table (validating
# that the count field does not exceed the size field, asserting otherwise), and uses ValidateBounds16/ValidateBounds4 and sub_82BF2C28 to
# set up and initialize the object's fields. This is the "construct this object" routine, part of the 0x82BF1 construct cluster. 188 lines.
# 4 callees (sub_82BF2B68, sub_82BF2C28, ValidateBounds16, ValidateBounds4). 5 callers (all within the 0x82BFx cluster).
[entrypoint.functions."0x82BF1268"]
name = "ProcessAndConstructObject_82BF1268"

# A comparator that compares two byte strings, ignoring case. It takes two byte strings (a and b) and a maximum count. It walks both
# strings in lockstep, up to the count, folding any uppercase A-Z byte to lowercase (by adding 32) before each comparison, and stops at
# the first mismatch, a null byte, or when the count is exhausted. It returns the difference of the two bytes at the stopping position
# (0 when they match, negative/positive indicating which is smaller/larger). This is the "compare these two strings ignoring case" routine
# (a strncasecmp-style helper), a leaf function (no callees), part of the 0x82CAB string cluster. 63 lines. 0 callees. 1 caller
# (sub_82CAB438).
[entrypoint.functions."0x82CAB3D0"]
name = "CompareIgnoreCase_82CAB3D0"

# A parser that resolves a value from a string lookup. It takes three values. When the first (or second) value is zero it takes a default
# path: it stores the default value 22 into the result, calls sub_82CAB630 (a zeroing helper) and sub_82CAB770, and returns the sentinel
# 0x7FFFFFFF. Otherwise it performs a case-insensitive string comparison (CompareIgnoreCase_82CAB3D0) and calls sub_82CAB630 / sub_82CAB770
# to resolve and return the value. This is the "parse/resolve this value (with a default)" routine, part of the 0x82CAB value/parse
# cluster (sibling of CompareIgnoreCase_82CAB3D0). 98 lines. 3 callees (CompareIgnoreCase, sub_82CAB630, sub_82CAB770). 12 callers.
[entrypoint.functions."0x82CAB438"]
name = "ProcessAndParseValue_82CAB438"

# A dispatcher that processes an array of elements by type tag. It takes a structure (a: a base pointer at a->12), an extra value, and a
# count. It walks an array of 8-byte elements (indexed into a->12), reads each element's type tag, and dispatches based on it (tags 3 and 4
# take special handling). For the matched elements it computes a hash (ComputeHash), copies data (MemCpy), and invokes the element
# handlers (ClampAndDispatch, sub_82BC9640, sub_82BC9860, sub_82BCBDC8, sub_82BCC038). This is the "process and dispatch these elements"
# routine, part of the 0x822CE/0x82BCx dispatch cluster. 278 lines. 7 callees (ComputeHash, MemCpy, ClampAndDispatch, sub_82BC9640,
# sub_82BC9860, sub_82BCBDC8, sub_82BCC038). 6 callers.
[entrypoint.functions."0x822CE098"]
name = "ProcessAndDispatchElements2_822CE098"

# A processor that compares two element structures and processes the result. It takes two structures (a and b, each with a head pointer
# at ->0 and an extra field at ->4). When a and b are the same pointer it returns early. Otherwise it builds a local descriptor from the
# head pointers and the ->4 fields (storing several of them on the stack), then walks and compares the two element values, and invokes
# ProcessAndAdjustCounter_82A0F528 to adjust counters. This is the "compare these two elements and process the result" routine, part of
# the 0x82A0F element cluster. 210 lines. 1 callee (ProcessAndAdjustCounter_82A0F528). 1 caller (Construct48ByteNode2).
[entrypoint.functions."0x82A0F3B8"]
name = "ProcessAndCompareElements_82A0F3B8"

# A large dispatcher that branches on a running counter. It takes a structure (a: a counter at a->0 and a field pointer at a->40) and a
# value. It writes 0 into *a->40+4, increments a->0, and when a->0 exceeds 127 it jumps to a default path. Otherwise it performs a large
# 127-way switch on a->0, where each case carries out a distinct piece of processing (querying indices, growing counters, finding
# characters, and invoking the sub_82BCx element helpers). This is the "increment this counter and dispatch on its new value" routine,
# part of the 0x82BCx/0x82BCE element cluster. 1339 lines. 14 callees (FindChar, GetGlobalValue, GrowCounterIfNeeded, ProcessAndQueryIndex,
# and several sub_82BCx helpers). 2 callers.
[entrypoint.functions."0x82BCEE60"]
name = "ProcessAndDispatchByCounter_82BCEE60"

# A processor that shifts a structure's fields and dispatches. It takes a structure (a: fields at a->4, a->20, and a->24). When a->20 is
# already 287 it calls ProcessAndDispatchByCounter_82BCEE60 (with a and a+16). Otherwise it shifts the fields down (a->4 to a->8, a->20 to
# a->12, a->24 to a->16) and sets a->20 to 287 (a sentinel marking the shift as done), then returns. This is the "shift these fields and
# dispatch" routine, part of the 0x82BCF/0x82BDx element cluster. 69 lines. 1 callee (ProcessAndDispatchByCounter_82BCEE60). 12 callers
# (all within the 0x82BDx/0x82BEx cluster).
[entrypoint.functions."0x82BCF7D8"]
name = "ProcessAndShiftFields_82BCF7D8"

# A constructor that allocates and initializes a 16-byte node. It takes a value and a type (r4). When the type is 0 it allocates 16 bytes
# (Allocate_OrDbgBreak) and, on success, initializes the node with a vtable pointer (at +0) and three zero words (at +4, +8, +12); when the
# type is 1 it allocates 16 bytes and initializes the node differently; for any other type it returns a null result. This is the
# "allocate and initialize this 16-byte node (by type)" routine, called only via the vtable (0 direct callers), part of the 0x82C23
# node-construct cluster. 95 lines. 1 callee (Allocate_OrDbgBreak). 0 direct callers (vtable dispatch).
[entrypoint.functions."0x82C230F0"]
name = "ProcessAndAllocateNode_82C230F0"

# A constructor that allocates and initializes a 16-byte node (second form). It takes a value and a type (r4). When the type is 0 it
# allocates 16 bytes (Allocate_OrDbgBreak) and, on success, initializes the node with a vtable pointer (at +0) and three zero words (at +4,
# +8, +12); when the type is 1 it allocates 16 bytes and initializes the node differently; for any other type it returns a null result.
# This is the "allocate and initialize this 16-byte node (by type)" routine, a sibling of ProcessAndAllocateNode_82C230F0 (same layout, a
# different vtable pointer), called only via the vtable (0 direct callers), part of the 0x82C24 node-construct cluster. 95 lines. 1 callee
# (Allocate_OrDbgBreak). 0 direct callers (vtable dispatch).
[entrypoint.functions."0x82C24A08"]
name = "ProcessAndAllocateNode2_82C24A08"

# A validator that checks a run of elements is in sorted order. It takes a structure (a: a base at a->0 and a count at a->8). When the
# count is 1 or less it returns 1 (trivially sorted). Otherwise it walks the elements (4 bytes each, starting at a->0+4) in order and
# compares each element's key field (at +4, using the field at +8 as a tiebreaker) against the previous one. When an element compares
# less than its predecessor it returns 0 (not sorted); when the run is fully non-decreasing it returns 1 (sorted). This is the "check
# that these elements are in sorted order" routine, a leaf function (no callees), part of the 0x82C26 sort-check cluster. 58 lines. 0
# callees. 1 caller (sub_82C27B20).
[entrypoint.functions."0x82C26A88"]
name = "ValidateSortedOrder_82C26A88"

# A constructor that allocates and initializes a 16-byte node (third form). It takes a value and a type (r4). When the type is 0 it
# allocates 16 bytes (Allocate_OrDbgBreak) and, on success, initializes the node with a vtable pointer (at +0) and three zero words (at +4,
# +8, +12); when the type is 1 it allocates 16 bytes and initializes the node differently; for any other type it returns a null result.
# This is the "allocate and initialize this 16-byte node (by type)" routine, a sibling of ProcessAndAllocateNode_82C230F0 and
# ProcessAndAllocateNode2_82C24A08 (same layout, a different vtable pointer), called only via the vtable (0 direct callers), part of the
# 0x82C27 node-construct cluster. 95 lines. 1 callee (Allocate_OrDbgBreak). 0 direct callers (vtable dispatch).
[entrypoint.functions."0x82C27300"]
name = "ProcessAndAllocateNode3_82C27300"

# A processor that sorts a run of elements and validates the order. It takes a structure (a: a base at a->0 and a count at a->8). When the
# count is non-zero it performs a binary search over the elements (BinarySearch_82C27A00, with a comparison supplied by
# BinarySearch_Cmp_82C39988). It then validates that the run is in sorted order (ValidateSortedOrder_82C26A88) and acts on the result.
# This is the "sort these elements and validate the sorted order" routine, part of the 0x82C27 sort cluster. 86 lines. 3 callees
# (BinarySearch, BinarySearch_Cmp, ValidateSortedOrder). 2 callers.
[entrypoint.functions."0x82C27B20"]
name = "ProcessAndSortAndValidate_82C27B20"

# A constructor that allocates and initializes a 16-byte node (fourth form). It takes a value and a type (r4). When the type is 0 it
# allocates 16 bytes (Allocate_OrDbgBreak) and, on success, initializes the node with a vtable pointer (at +0) and three zero words (at +4,
# +8, +12); for any other type it allocates 16 bytes and initializes the node differently; on allocation failure it returns a null result.
# This is the "allocate and initialize this 16-byte node (by type)" routine, a sibling of ProcessAndAllocateNode_82C230F0 / 2_82C24A08 /
# 3_82C27300 (same layout, a different vtable pointer), called only via the vtable (0 direct callers), part of the 0x82C28
# node-construct cluster. 89 lines. 1 callee (Allocate_OrDbgBreak). 0 direct callers (vtable dispatch).
[entrypoint.functions."0x82C28AA8"]
name = "ProcessAndAllocateNode4_82C28AA8"

# A constructor that allocates and initializes a 16-byte node (fifth form). It takes a value and a type (r4). When the type is 0 it
# allocates 16 bytes (Allocate_OrDbgBreak) and, on success, initializes the node with a vtable pointer (at +0) and three zero words (at +4,
# +8, +12); when the type is 1 it allocates 16 bytes and initializes the node differently; for any other type it returns a null result.
# This is the "allocate and initialize this 16-byte node (by type)" routine, a sibling of ProcessAndAllocateNode_82C230F0 / 2_82C24A08 /
# 3_82C27300 / 4_82C28AA8 (same layout, a different vtable pointer), called only via the vtable (0 direct callers), part of the 0x82C2B
# node-construct cluster. 95 lines. 1 callee (Allocate_OrDbgBreak). 0 direct callers (vtable dispatch).
[entrypoint.functions."0x82C2BD30"]
name = "ProcessAndAllocateNode5_82C2BD30"

# A constructor that allocates and initializes a 16-byte node (sixth form). It takes a value and a type (r4). When the type is 0 it
# allocates 16 bytes (Allocate_OrDbgBreak) and, on success, initializes the node with a vtable pointer (at +0) and three zero words (at +4,
# +8, +12); when the type is 1 it allocates 16 bytes and initializes the node differently; for any other type it returns a null result.
# This is the "allocate and initialize this 16-byte node (by type)" routine, a sibling of ProcessAndAllocateNode_82C230F0 / 2_82C24A08 /
# 3_82C27300 / 4_82C28AA8 / 5_82C2BD30 (same layout, a different vtable pointer), called only via the vtable (0 direct callers), part of
# the 0x82C3C node-construct cluster. 95 lines. 1 callee (Allocate_OrDbgBreak). 0 direct callers (vtable dispatch).
[entrypoint.functions."0x82C3C4C0"]
name = "ProcessAndAllocateNode6_82C3C4C0"

# A dispatcher that resolves an indexed pointer and dispatches by its type tag. It takes a value. It resolves the indexed pointer
# (GetIndexedPointer) and reads the resolved node's type field (at +4), subtracting 3 to get a case index. It then performs a 5-way switch
# on that index (with a default path), where each case carries out a distinct piece of processing (including a binary search over indices
# via ProcessAndBinarySearchIndex_82BCA750 and sub_82BCBDC8). This is the "resolve this value and dispatch on its type" routine, part of the
# 0x82BC5 value-dispatch cluster. 126 lines. 3 callees (GetIndexedPointer, ProcessAndBinarySearchIndex, sub_82BCBDC8). 11 callers (all
# within the 0x82BDx/0x82BCx cluster).
[entrypoint.functions."0x82BC5B30"]
name = "ProcessAndDispatchByType2_82BC5B30"

# A writer that walks a run of elements and writes a range for each. It takes a start structure (a: a field at a->20), an end pointer, and a
# source structure (b: fields at b->4, b->20, and b->24). It loops over the elements (36 bytes each), advancing from the start until it
# reaches the end, and for each element it calls WriteRangeClamped_82377FD8 (using b's data pointer, which is either b->4 when b->24 is at
# least 8, or b+4 otherwise). When WriteRangeClamped returns 0 it exits early; otherwise it continues. This is the "write a range for each
# element in this run" routine, part of the 0x82C68 range-write cluster. 68 lines. 1 callee (WriteRangeClamped). 1 caller (sub_82C68978).
[entrypoint.functions."0x82C68908"]
name = "ProcessAndWriteRange_82C68908"

# A writer that validates bounds and delegates the range write. It takes four values (a start, a structure (a: a descriptor at a->12), and
# two more values). It reads a descriptor from a->12 (offset by 8) and validates two ordering invariants (asserting, trap 22, that the
# first field does not exceed the second, in both directions). It then sets up a local descriptor and delegates the actual per-element range
# write to ProcessAndWriteRange_82C68908. This is the "validate these bounds and write the range" routine, called only via the vtable (0
# direct callers), part of the 0x82C68 range-write cluster (wrapper of ProcessAndWriteRange_82C68908). 128 lines. 1 callee
# (ProcessAndWriteRange). 0 direct callers (vtable dispatch).
[entrypoint.functions."0x82C68978"]
name = "ProcessAndWriteRange2_82C68978"

# A validator that checks an index against an element count and indexes the element. It takes a result pointer, a structure (a: a
# descriptor at a->12), and two values (an index and an end). It reads the descriptor (a->12) and computes the element count as
# (descriptor+16 - descriptor+12) / 36 (36 bytes per element). When the descriptor is empty or the clamped index (end - 1) is out of
# range it stores 0 into *result and returns; otherwise it delegates the actual element index to ProcessAndIndexElement_8233D880. This is
# the "validate this index against the element count and index the element" routine, called only via the vtable (0 direct callers), part of
# the 0x82C68 element cluster. 84 lines. 1 callee (ProcessAndIndexElement). 0 direct callers (vtable dispatch).
[entrypoint.functions."0x82C68AC0"]
name = "ProcessAndValidateIndex_82C68AC0"

# A thunk that forwards to a threshold check. It takes a structure (a: a counter at a->0 and a threshold at a->4) and unconditionally
# tail-calls IncrementAndCheckThreshold_8219EE00 with (a->0, a->4). This is the "check this threshold" thunk, called only via the vtable (0
# direct callers), part of the 0x82BC6 threshold cluster. 11 lines. 1 callee (IncrementAndCheckThreshold). 0 direct callers (vtable
# dispatch).
[entrypoint.functions."0x82BC6120"]
name = "CheckThreshold_Thunk_82BC6120"

# A constructor that builds an object from fixed descriptors. It takes two values. It sets up two .data descriptors (r27 and r26). When the
# first value is zero it allocates/initializes the object through sub_82BF2B68 with a fixed descriptor of 920; when the second value is zero
# it does the same with a fixed descriptor of 921. It then uses BoundsCheck12Byte_82BEBF70 and ProcessAndValidateBounds2_82BEC8B0 to
# validate the object's bounds and set up its fields. This is the "construct this object (second form)" routine, part of the 0x82BED
# construct cluster. 253 lines. 3 callees (sub_82BF2B68, BoundsCheck12Byte, ProcessAndValidateBounds2). 2 callers.
[entrypoint.functions."0x82BED1B0"]
name = "ProcessAndConstructObject2_82BED1B0"

# A free routine that shrinks a buffer and releases the refcounted elements it frees. It takes a structure (a: a used size at a->4 and a
# total size at a->8). When a->4 equals a->8 it returns early. Otherwise it calls sub_829FF3E8 (a free/shrink) to determine the new size,
# then walks the freed elements (8 bytes each) from the new size back up to the old size and calls Release_RefCounted on each. It finally
# stores the new size back into a->8. This is the "free and shrink this buffer, releasing the refcounted elements" routine, part of the
# 0x823B8 buffer cluster. 60 lines. 2 callees (sub_829FF3E8, Release_RefCounted). 9 callers.
[entrypoint.functions."0x823B8178"]
name = "ProcessAndFreeAndShrink_823B8178"

# A processor that replaces a value and updates related counters. It takes a structure (a: a field at a->16) and a value (b). It replaces a
# refcounted value (ProcessAndReplaceRefCounted_822B85C8), increments several counters (ProcessAndIncrementCounter_82287710 and
# ProcessAndIncrementCounter_822D6B40, with .data descriptors), and manages the associated memory (Allocate_OrDbgBreak, Release_RefCounted,
# ReleaseGlobalRef, SetValue4, and sub_823B8178 / sub_827DEEE0 / sub_827DFA18 / sub_82B38B78). This is the "replace this value and update
# the related counters" routine, part of the 0x827DE value-replace cluster. 163 lines. 11 callees. 4 callers.
[entrypoint.functions."0x827DEAE0"]
name = "ProcessAndReplaceValue_827DEAE0"

# A processor that looks up a value and manages the associated memory. It takes three values. It increments a counter (ProcessAndIncrementCounter_822D6B40
# with a .data descriptor), performs a lookup (sub_82B391D8), and based on the result (checking for a -1 sentinel) it either takes a default path
# or continues processing. It then manages the associated memory and counters (Allocate_OrDbgBreak, ProcessAndIncrementCounter, ProcessAndReplaceRefCounted,
# ReleaseGlobalRef, Release_RefCounted, SetValue4, and several sub_827Dx / sub_82B38x / sub_82B39x helpers). This is the "look up this value and
# manage the associated memory" routine, part of the 0x827DE value-lookup cluster. 577 lines. 14 callees. 1 caller (ProcessAndReplaceValue_827DEAE0).
[entrypoint.functions."0x827DEEE0"]
name = "ProcessAndLookupValue_827DEEE0"

# A constructor that allocates and initializes a 12-byte node. It takes a structure (a: a value at a->0 and a result slot at a->4). It allocates
# 12 bytes (Allocate_OrDbgBreak); when the allocation succeeds it initializes the node with a count of 1 (at +0), a .data descriptor (at +4), and
# the value from a->0 (at +8). It then stores the node pointer (or 0 on failure) into a->4. This is the "allocate and initialize this 12-byte
# node" routine, part of the 0x827DF node-construct cluster. 62 lines. 1 callee (Allocate_OrDbgBreak). 2 callers (ProcessAndLookupValue_827DEEE0,
# ProcessAndReplaceValue_827DEAE0).
[entrypoint.functions."0x827DFA18"]
name = "ProcessAndAllocateNode12Byte_827DFA18"

# A processor that computes with floating-point and vector operations. It takes a value. It loads floating-point constants from .data, sets up
# local vector/float slots on the stack, and performs a series of floating-point and vector (lvlx/stvx) computations. It then resolves a handler
# (ProcessAndResolveHandler_82816E08) and manages the associated refcounts and disposal (ProcessAndDecrementRefCount_8218DFF0,
# ProcessAndDecrementDispose_821DE318, and sub_827DEAE0 / ProcessAndReplaceValue). This is the "compute this vector/floating-point value"
# routine, part of the 0x82816 compute cluster. 417 lines. 4 callees (ProcessAndResolveHandler, ProcessAndDecrementRefCount, ProcessAndDecrementDispose,
# sub_827DEAE0). 2 callers.
[entrypoint.functions."0x82816EC0"]
name = "ProcessAndComputeVector_82816EC0"

# A processor that atomically increments a counter and parses/processes lines. It takes four values. It atomically increments a global
# counter (a lwarx/stwcx. loop with the MSR saved and restored to guard the critical section), then performs a large amount of line and string
# processing (ParseLine_82B47218, ProcessLineParse_82B47148, ProcessLine_82B470D0, ProcessString2_82B3A448, ProcessString3_82B3A560,
# ScanLineLength_82B477F0, HashLookupCompare_823F9620, StrCmp_Signed_8226D7A8) and manages the associated refcounted objects (Construct16Byte,
# ConstructRefCounted, CopyAssign_RefCounted2, Destruct4, DestructRefCounted). This is the "increment the counter and parse/process these lines"
# routine, called only via the vtable (0 direct callers), part of the 0x82817 parse cluster. 554 lines. 16 callees. 0 direct callers (vtable
# dispatch).
[entrypoint.functions."0x82817D28"]
name = "ProcessAndParseLines_82817D28"

# A processor that computes a floating-point value into a structure. It takes a structure (a: fields at a->88, a->92, and a->188) and a value.
# It stores a floating-point constant from .data into a->92 and the value into a->188. When the value is less than 18 it performs a series of
# floating-point computations (sign-extending the value, converting to a double, rounding, and multiplying by another .data constant) and stores
# the result into a->88; otherwise it skips that computation. It then resolves a handler (ProcessAndResolveHandler_82816E08) and manages the
# associated refcounts and disposal (ProcessAndDecrementRefCount_8218DFF0, ProcessAndDecrementDispose_821DE318). This is the "compute this
# floating-point value into the structure" routine, part of the 0x82818 compute cluster. 142 lines. 3 callees (ProcessAndResolveHandler,
# ProcessAndDecrementRefCount, ProcessAndDecrementDispose). 3 callers.
[entrypoint.functions."0x82818638"]
name = "ProcessAndComputeFloat_82818638"

# A processor that atomically increments a counter and processes strings. It takes a structure (a: a field at a->0) and a value. It atomically
# increments a global counter (a lwarx/stwcx. loop with the MSR saved and restored to guard the critical section), then reads a->0 and performs
# a series of string and refcount operations (FindUTF16Substring_82CA3BF0, StrLenUTF16_8217AB30, ProcessAndReplaceRefCounted_822B85C8,
# ProcessAndIncrementCounter_822D6B40, ReleaseGlobalRef_821C6868, Release_RefCounted_821C67D8). This is the "increment the counter and process
# these strings" routine, part of the 0x82B38 string cluster. 259 lines. 8 callees. 12 callers.
[entrypoint.functions."0x82B38B78"]
name = "ProcessAndIncrementAndProcess_82B38B78"

# A processor that replaces a value in a structure. It takes two structures (a: a field at a->0, and b: a field at b->0). When a->0 is
# non-zero it first calls sub_82B394B8 (a prepare/lock step) on a. It then reads b->0; when b->0 is non-zero it calls sub_82B39A88 (a set
# step) with (a->0, b->0->0), and when b->0 is zero it calls sub_82B39A88 with (a->0, a .data descriptor). It finally releases the refcounted
# value (Release_RefCounted_821C67D8). This is the "replace this value in the structure" routine (second form), part of the 0x82B38
# value-replace cluster. 131 lines. 3 callees (sub_82B394B8, sub_82B39A88, Release_RefCounted). 12 callers.
[entrypoint.functions."0x82B38E60"]
name = "ProcessAndReplaceValue2_82B38E60"

# A finder that locates a UTF-16 substring and returns its index. It takes a structure (a: a field at a->0). It sets up a .data descriptor and
# resolves the search start (either a->0->0 when a->0 is non-zero, or the .data descriptor itself). It then calls FindUTF16Substring_82CA3BF0
# to locate the substring. When a match is found it converts the match pointer to a 16-bit index (by subtracting the start and shifting right by
# 1) and returns it; when no match is found it returns -1. This is the "find this UTF-16 substring and return its index" routine, part of the
# 0x82B39 string cluster. 82 lines. 1 callee (FindUTF16Substring). 4 callers.
[entrypoint.functions."0x82B391D8"]
name = "ProcessAndFindSubstring_82B391D8"

# A dispatcher that resolves an indexed pointer and dispatches by its type tag. It takes three values. It resolves the indexed pointer
# (GetIndexedPointer) and reads the resolved node's type field (at +4). When the type is 4 it takes one path; otherwise it calls sub_82BCBDC8
# (a resolve step) and, based on the result, either continues processing (using ProcessLoopDispatch_8227B8B8) or stores 0 into the third
# argument and returns 0. This is the "resolve this value and dispatch on its type" routine (third form), part of the 0x82BC5 value-dispatch
# cluster. 101 lines. 3 callees (GetIndexedPointer, sub_82BCBDC8, ProcessLoopDispatch). 12 callers (all within the 0x82BDx/0x82BCx cluster).
[entrypoint.functions."0x82BC5A88"]
name = "ProcessAndDispatchByType3_82BC5A88"

# A processor that resolves an indexed pointer, checks its type, and processes a string. It takes three values. It resolves the indexed pointer
# (GetIndexedPointer) and reads the resolved node's type field (at +4). When the type is zero or less it takes one path; otherwise it checks
# the third argument and, when non-zero, walks a byte string (looping until a null terminator) and processes it. It then delegates further
# processing to sub_82BC5A88 (a dispatch step) and sub_82BC68F0. This is the "resolve this value, check its type, and process the string"
# routine, part of the 0x82BC6 value cluster. 122 lines. 3 callees (GetIndexedPointer, sub_82BC5A88, sub_82BC68F0). 12 callers (all within the
# 0x82BCx/0x82BDx cluster).
[entrypoint.functions."0x82BC6E30"]
name = "ProcessAndCheckAndProcess_82BC6E30"

# A dispatcher that processes and dispatches a run of elements. It takes a structure (a: a start at a->8 and an end at a->12). It first calls
# ProcessAndCheckAndProcess_82BC6E30 (a check step). It then computes the element count as (a->12 - a->8) / 8 and calls
# ProcessAndComputeAndDispatch_82BC7950 to compute and dispatch the elements. When that returns non-zero it calls sub_82BC9788 (a follow-up
# step) and also checks a threshold (IncrementAndCheckThreshold_8219EE00). This is the "process and dispatch these elements" routine (third
# form), called only via the vtable (0 direct callers), part of the 0x82BD6 element-dispatch cluster. 101 lines. 4 callees
# (ProcessAndCheckAndProcess, ProcessAndComputeAndDispatch, sub_82BC9788, IncrementAndCheckThreshold). 0 direct callers (vtable dispatch).
[entrypoint.functions."0x82BD6698"]
name = "ProcessAndDispatchElements3_82BD6698"

# A reset routine that clears a structure's state. It takes a structure (a: fields at a->8, a->20, a->24, a->28, and a->48). When a or a->28
# is zero it returns early. Otherwise it clears a number of fields to 0 (a->20, a->8, a->24, and many fields of the sub-structure at a->28,
# including a->28->0, a->28->4, a->28->12, a->28->28, and others), sets a flag (a->48) to 1, and stores a->28+1328 into a->28->108. This is
# the "reset this structure's state" routine, a leaf function (no callees), part of the 0x82B53 state cluster. 69 lines. 0 callees. 3 callers.
[entrypoint.functions."0x82B53740"]
name = "ProcessAndResetState_82B53740"

# A processor that checks a string and a value, then processes. It takes a structure (a: fields at a->24, a->32, and a->40), a value, a byte
# string, and another value. When the byte string is null, its first byte is not 49 (the ASCII '1'), or the other value is not 56 it takes a
# default path. When the structure is null it returns -2. Otherwise it reads a->32, clears a->24, and (when a->32 is zero) performs further
# initialization, eventually delegating to ProcessAndResetState_82B53740. This is the "check this string and value and process" routine (second
# form), part of the 0x82B53 state cluster. 168 lines. 1 callee (ProcessAndResetState_82B53740). 3 callers (incl ProcessTypeDispatch).
[entrypoint.functions."0x82B537C0"]
name = "ProcessAndCheckAndProcess2_82B537C0"

# A caller that invokes a method on a structure. It takes a structure (a: a field at a->28, a method pointer at a->36, and a context at a->40).
# When a, a->28, or a->36 is zero it returns early. Otherwise it reads a->28->52; when that is non-zero it first invokes the method (a->36)
# with the context (a->40). It then invokes the method again (a->36) with (a->28, a->40) and acts on the result. This is the "invoke this
# method on the structure" routine, a leaf function (no direct callees, uses indirect calls), part of the 0x82B55 method cluster. 87 lines. 0
# direct callees. 11 callers.
[entrypoint.functions."0x82B55128"]
name = "ProcessAndCallMethod_82B55128"

# A hasher that computes a 32-bit hash from two 16-bit halves. It takes a 32-bit value (two 16-bit halves: high at bits 16-31, low at bits 0-15),
# a value (b), and a mode (c). When the mode is 1 it reads a byte from b, adds it to the low half (modulo 65521), then adds the result to the
# high half (modulo 65521), and returns the combined (high << 16) | low. When b is zero and the mode is not 1 it returns 1. This is the
# "compute this hash (second form)" routine, a leaf function (no callees), part of the 0x82B57 hash cluster (sibling of ComputeHash_8227BA30).
# 424 lines. 0 callees. 2 callers.
[entrypoint.functions."0x82B57438"]
name = "ComputeHash2_82B57438"

# A processor that checks each character of a UTF-16 string and truncates or modifies it. It takes a UTF-16 string. When the string is null it
# returns. Otherwise it walks the string 16-bit code units at a time, checking each character with CharProperty_Check_Thunk_82BF2340. When a
# character fails the check it either truncates the string (storing a null terminator at the failing position, when the failing character is
# already null) or, when the failing character is non-zero, computes the remaining length (StrLenUTF16_Thunk_822F8620) and performs further
# processing (sub_82BF2B68). This is the "check this UTF-16 string's characters and truncate/modify it" routine, part of the 0x82BF0 string
# cluster. 134 lines. 3 callees (CharProperty_Check_Thunk, StrLenUTF16_Thunk, sub_82BF2B68). 1 caller (sub_82BF1E58).
[entrypoint.functions."0x82BF0BB0"]
name = "ProcessAndCheckAndTruncateString_82BF0BB0"

# A processor that checks a string and returns a code. It takes a UTF-16 string. When the string is null it calls sub_82BF2B68 (with .data
# descriptors) and returns the code 87. Otherwise it checks and truncates the string (ProcessAndCheckAndTruncateString_82BF0BB0) and, when the
# first character is now null, returns the code 4306. Otherwise it performs a large amount of string and buffer processing (StrLenUTF16,
# SearchFor16BitValue_82CA64D0, ConvertIfPresentThunk_82CA6888, ProcessAndConstructObject_82CA6890, ProcessAndUpdateGlobal_82BF21E0,
# ProcessAndWriteToBuffer_82BF0458, SetGlobalField_Thunk_82CC0750) and returns a result code. This is the "check this string and return a code"
# routine, called only via the vtable (0 direct callers), part of the 0x82BF1 string cluster. 416 lines. 13 callees. 0 direct callers (vtable
# dispatch).
[entrypoint.functions."0x82BF1E58"]
name = "ProcessAndCheckString_82BF1E58"

# A processor that checks a type and dispatches processing. It takes a structure (a: a field at a->144) and a structure (b: fields at b->4,
# b->8, and b->10). When b->10 is not 5 it takes a default path. When b->10 is 5 it checks b->8; when b->8 is non-zero it reads b->4 and
# a->144 and performs further processing, otherwise it calls sub_82270B68 (a simple step) and returns. The processing involves hashing a string
# (HashCstring_82188CF0), a linear search (LinearSearch_82284448), computing a float (ProcessAndComputeFloat_82818638), and managing the
# associated refcounted objects (ConstructRefCounted_8222CF18, DestructRefCounted_82214F08). This is the "check this type and dispatch
# processing" routine, called only via the vtable (0 direct callers), part of the 0x82818 process cluster. 450 lines. 9 callees. 0 direct
# callers (vtable dispatch).
[entrypoint.functions."0x828180A8"]
name = "ProcessAndDispatch_828180A8"

# A dispatcher that computes an index and dispatches processing. It takes a value, a byte, another value, and a fourth value. When the byte
# equals the third value it returns -1. Otherwise it reads the byte, adds the first value, and reads a field at offset 76 of the result, then
# subtracts 5 to get an index. When the index exceeds 24 it takes a default path; otherwise it dispatches to one of several processing routines
# (sub_82C89638, sub_82C89AB0, sub_82C8A210, sub_82C8A988) based on the index. This is the "compute this index and dispatch" routine, part of
# the 0x82C8A dispatch cluster. 947 lines. 4 callees. 1 caller (ProcessAndDispatchByType_82C8B5F8).
[entrypoint.functions."0x82C8AF48"]
name = "ProcessAndDispatchIndex_82C8AF48"

# A thunk that returns a .data pointer. It unconditionally returns a fixed pointer from the .data section (computed as a constant offset). This
# is the "return this .data pointer" thunk, called only via the vtable (0 direct callers), part of the 0x82C35 thunk cluster. 5 lines. 0
# callees. 0 direct callers (vtable dispatch).
[entrypoint.functions."0x82C35228"]
name = "ReturnDataPointer_Thunk_82C35228"

# A processor that clears a structure's fields and processes. It takes a structure (a: fields at a->4, a->8, and a->12) and a value. It clears
# the three fields to 0. When the value is non-zero it calls sub_82A1DC28 (a process step) with (a, value); when the value is zero it sets the
# result to 0. It then checks the low byte of the result; when it is non-zero it reads a->4 and performs further processing (with different
# paths for the zero and non-zero value cases). This is the "clear these fields and process" routine, part of the 0x82A1D field cluster. 82
# lines. 1 callee (sub_82A1DC28). 2 callers.
[entrypoint.functions."0x82A1D980"]
name = "ProcessAndClearFields_82A1D980"

# A processor that allocates memory and sets up a structure's fields. It takes a structure (a: fields at a->4, a->8, and a->12) and a value.
# When the value is greater than -1 it calls sub_82A97648 (a process step); otherwise it allocates and initializes memory (Allocate_Init_821A9258)
# and stores the result into a->4 and a->8, and the result plus the value into a->12. It returns 1. This is the "allocate and set up these
# fields" routine, part of the 0x82A1D field cluster. 65 lines. 2 callees (Allocate_Init, sub_82A97648). 1 caller (ProcessAndClearFields_82A1D980).
[entrypoint.functions."0x82A1DC28"]
name = "ProcessAndAllocateFields_82A1DC28"

# A processor that computes a size and processes. It takes a value, a structure (a: a field at a->20), and another value. When the second value
# is zero it stores a .data descriptor into a->20 and takes one path. Otherwise it computes a size by right-shifting the second value minus 1
# until it is less than 256 (tracking the shift amount), and uses that to determine the processing. It then performs further processing using
# ProcessVirtual_82227940 and sub_82A2C520. This is the "compute this size and process" routine, part of the 0x82A2F size cluster. 172 lines.
# 2 callees (ProcessVirtual, sub_82A2C520). 2 callers.
[entrypoint.functions."0x82A2F750"]
name = "ProcessAndComputeSize_82A2F750"

# A processor that checks bounds and allocates. It takes a result pointer, a value (b), and two more values (c and d). When b is zero or c
# exceeds d it stores 0 into *result and returns. Otherwise it allocates 32 bytes (Allocate_OrDbgBreak); when the allocation succeeds it
# initializes the block and uses SetAndDispatch_82366210 to set up and dispatch the result, storing the result pointer into *result. This is
# the "check these bounds and allocate" routine, part of the 0x82B4D allocate cluster. 110 lines. 2 callees (Allocate_OrDbgBreak,
# SetAndDispatch). 2 callers.
[entrypoint.functions."0x82B4DB48"]
name = "ProcessAndAllocateAndDispatch_82B4DB48"

# A finder that searches a linked list for a value. It takes a structure (a: a head at a->8), a value to find (b), and a starting node (c).
# When c is non-zero it starts at that node; otherwise it starts at a->8. It first checks the starting node's value field (at +0); when it
# matches b it returns that node. Otherwise it walks the list (following the next pointer at +16, starting from the head's +20), checking
# each node's value field against b. When a match is found it returns the matching node; when the list is exhausted it returns 0. This is the
# "find this value in the linked list" routine, a leaf function (no callees), called only via the vtable (0 direct callers), part of the
# 0x82BAC list cluster. 45 lines. 0 callees. 0 direct callers (vtable dispatch).
[entrypoint.functions."0x82BACC90"]
name = "FindInLinkedList_82BACC90"

# A processor that stores an element. It takes a structure (a: a pointer at a->8) and a value. It resolves the indexed pointer
# (GetIndexedPointer) and accesses the element (ElementAccess_821E1498). It then stores the element's first two fields (at +0 and +4) into
# the structure pointed to by a->8, and advances a->8 by 8 (to point past the stored element). This is the "store this element" routine (second
# form), part of the 0x821E1 element cluster. 53 lines. 2 callees (GetIndexedPointer, ElementAccess). 11 callers (all within the
# 0x82BBx/0x82BDx/0x82BCx cluster).
[entrypoint.functions."0x821E1548"]
name = "ProcessAndStoreElement2_821E1548"

# A processor that invokes a method and checks the result. It takes a structure (a: fields at a->4, a->12, a->20, and a->24). When a->24 or
# a->12 is zero it takes a default path. Otherwise it invokes a method (the function pointer at a->12->0->4) and compares the result against
# a->20. When the result exceeds a->20 it clears a->12 and takes the default path; otherwise it continues processing (using sub_8223E8B0 and
# sub_82CD2658). This is the "invoke this method and check the result" routine, part of the 0x8223E method cluster. 82 lines. 2 callees
# (sub_8223E8B0, sub_82CD2658). 12 callers (all within the 0x82BEx/0x82C4x/0x8228x cluster).
[entrypoint.functions."0x8223E820"]
name = "ProcessAndCheckMethod_8223E820"

# A processor that increments a value and stores it. It takes a structure (a: a pointer at a->0) and a value. It reads the pointer (a->0),
# computes offsets +260 and +256, and reads a counter field (at +260) and a size field (at +256). It increments the counter by the value and
# asserts (trap 22) that the result does not exceed the size field. When the incremented counter equals the value at +256 it stores 0 into the
# counter field; otherwise it stores the incremented counter. It then performs further processing (sub_8223E950). This is the "increment this
# counter and store it" routine, part of the 0x8223E counter cluster. 100 lines. 1 callee (sub_8223E950). 1 caller
# (ProcessAndCheckMethod_8223E820).
[entrypoint.functions."0x8223E8B0"]
name = "ProcessAndIncrementAndStore_8223E8B0"

# A thunk that sets an event. It takes a structure (a: an event handle at a->128), reads the handle, and calls SetEvent_SdkRuntime on it. This
# is the "set this event" thunk, part of the 0x8223E event cluster. 9 lines. 1 callee (SetEvent_SdkRuntime). 12 callers (all within the
# 0x82C7x/0x82C0x/0x82BFDx cluster).
[entrypoint.functions."0x8223E950"]
name = "ProcessEvent_Thunk_8223E950"

# A processor that invokes methods on a structure. It takes a structure (a: fields at a->12, a->20, a->24, and a->28) and a value. When a->24
# is non-zero it computes a pointer to a->28, calls sub_82CD2650 on it, and when a->12 is also non-zero it calls sub_82CD2658, then returns 0.
# When a->24 is zero it checks the low byte of the value against a->20 (using ByteValueCheck_82CBC678) and performs further processing. This is
# the "invoke these methods" routine, part of the 0x8223E method cluster. 131 lines. 3 callees (sub_82CD2650, sub_82CD2658, ByteValueCheck). 12
# callers (all within the 0x82BEx/0x82C4x cluster).
[entrypoint.functions."0x8223E958"]
name = "ProcessAndCallMethods_8223E958"

# A thunk that enters a critical section. It takes a critical section handle and calls RtlEnterCriticalSection on it, blocking until the section
# is available. This is the "enter this critical section" thunk, part of the 0x82CD2 lock cluster. 7 lines. 0 callees (forwards directly to
# RtlEnterCriticalSection). 4 callers.
[entrypoint.functions."0x82CD2650"]
name = "EnterCriticalSection_Thunk_82CD2650"

# A thunk that leaves a critical section. It takes a critical section handle and calls RtlLeaveCriticalSection on it, releasing the lock. This
# is the "leave this critical section" thunk (the companion to EnterCriticalSection_Thunk_82CD2650), part of the 0x82CD2 lock cluster. 7
# lines. 0 callees (forwards directly to RtlLeaveCriticalSection). 5 callers.
[entrypoint.functions."0x82CD2658"]
name = "LeaveCriticalSection_Thunk_82CD2658"

# A function that zeroes 16 bytes. It takes a pointer and stores 0 into the first 16 bytes (four 32-bit words at offsets 0, 4, 8, and 12). This
# is the "zero these 16 bytes" routine, a leaf function (no callees), called only via the vtable (0 direct callers), part of the 0x82BA9 zero
# cluster. 16 lines. 0 callees. 0 direct callers (vtable dispatch).
[entrypoint.functions."0x82BA9280"]
name = "Zero16Bytes_82BA9280"

# A processor that constructs an object, gated on a .data flag. It takes two values. When a .data flag byte is zero it sets a global field to 21
# (via SetGlobalField_Thunk) and returns 0. Otherwise it constructs the object using ProcessAndConstructObject2_82BED1B0, validates bounds
# (ProcessAndValidateBounds_82BEC640 and ValidateBounds4_82BEC300), counts elements (CountOrError_Thunk_82CA5FE0), and aborts on error
# (ProcessAndAbort_82170CC8). This is the "construct this object, gated on this flag" routine, part of the 0x82BED construct cluster. 444 lines.
# 7 callees (SetGlobalField_Thunk, ProcessAndConstructObject2, ProcessAndValidateBounds, ValidateBounds4, CountOrError_Thunk, ProcessAndAbort,
# sub_82BF2B68). 1 caller (sub_82BEDAB0).
[entrypoint.functions."0x82BED7A8"]
name = "ProcessAndConstructWithFlag_82BED7A8"

# A processor that constructs an object, gated on a .data flag. It takes four values. When a .data flag byte is zero it sets a global field to 21
# (via SetGlobalField_Thunk) and returns -1. Otherwise it delegates to ProcessAndConstructWithFlag_82BED7A8 (the object-construction routine) and
# performs further processing (sub_82BF2B68, sub_82BF2D00, sub_82BF2D58, sub_82BF2D60), aborting on error (ProcessAndAbort_82170CC8). This is the
# "construct this object, gated on this flag" routine (second form, a wrapper around ProcessAndConstructWithFlag), part of the 0x82BED construct
# cluster. 202 lines. 7 callees (SetGlobalField_Thunk, ProcessAndConstructWithFlag, sub_82BF2B68, sub_82BF2D00, sub_82BF2D58, sub_82BF2D60,
# ProcessAndAbort). 2 callers.
[entrypoint.functions."0x82BEDAB0"]
name = "ProcessAndConstructWithFlag2_82BEDAB0"

# A processor that constructs an object, gated on a .data flag. It takes two values. When a .data flag byte is zero it returns 21. Otherwise it
# constructs the object using ProcessAndConstructObject_82BF1268, validates bounds (ValidateBounds16_82BEBD20), counts elements
# (CountOrError_Thunk_82CA5FE0), and aborts on error (ProcessAndAbort_82170CC8). This is the "construct this object, gated on this flag" routine
# (third form), part of the 0x82BF1 construct cluster. 260 lines. 6 callees (SetGlobalField_Thunk, ProcessAndConstructObject, ValidateBounds16,
# CountOrError_Thunk, ProcessAndAbort, sub_82BF2B68). 3 callers.
[entrypoint.functions."0x82BF1518"]
name = "ProcessAndConstructWithFlag3_82BF1518"

# A processor that constructs a value. It takes a structure (a: a descriptor at a->0, a data region at a->4, and a flag at a->12). It clears the
# flag (a->12 = 0), stores a .data descriptor into a->0, and copies 8 bytes of initial data from a .data source into a->4
# (Buffer_CopyOrZero_Error_82CA3730). This is the "construct this value" routine, part of the 0x82BF2 value-construct cluster. 52 lines. 1 callee
# (Buffer_CopyOrZero_Error). 1 caller (ProcessAndConstructWithFlag2_82BEDAB0).
[entrypoint.functions."0x82BF2D00"]
name = "ProcessAndConstructValue_82BF2D00"

# A processor that resolves a value from a structure. It takes a structure (a: fields at a->8, a->12, a->16, and a->20) and a value. When a->12
# is non-zero it copies a->16 into a->12 and proceeds. Otherwise it inspects the low byte of the value and the sub-object at a->12; when the low
# byte is non-zero it uses the sub-object's +20 field, and when the low byte is zero it checks a->20. It then resolves the final value (using
# LockedTableLookup_82C81E58 for the fallback path). This is the "resolve this value from the structure" routine, part of the 0x82C86 resolve
# cluster. 84 lines. 1 callee (LockedTableLookup). 2 callers.
[entrypoint.functions."0x82C86728"]
name = "ProcessAndResolveValue_82C86728"

# A thunk that resolves a value. It takes a structure and calls ProcessAndResolveValue_82C86728 on it with a zero value. This is the "resolve this
# value" thunk (passing 0 as the value), part of the 0x82C86 resolve cluster. 9 lines. 1 callee (ProcessAndResolveValue). 3 callers.
[entrypoint.functions."0x82C867E0"]
name = "ResolveValue_Thunk_82C867E0"

# A copier that copies fields from one structure into another. It takes a source structure (a: fields at a->8, a->12, a->16, a->20, a->24, a->28,
# a->40, and a->56) and a destination structure (b: fields at b->0, b->4, b->8, b->12, b->16, b->20, b->24, b->28, and b->44). It copies the
# source's fields into the destination in a reordered layout (a->56 to b->0, a->8 to b->4, a->12 to b->8, and so on), and it copies a trailing
# block of data from a->64 into b->44 (using MemCpy_SdkRuntime_82CA2C60). This is the "copy these fields and block" routine, part of the 0x82CC9
# copy cluster. 88 lines. 1 callee (MemCpy_SdkRuntime). 2 callers.
[entrypoint.functions."0x82CC9E60"]
name = "CopyFieldsAndBlock_82CC9E60"

# A processor that performs linear searches and copies data. It takes two structures (a: a field at a->4, and b: a field at b->4). It reads b->4
# and performs a linear search (LinearSearch_82284448) against a .data table; when the search succeeds it reads the result's +4 field. It then
# checks the low byte of that value and performs a second linear search. It uses the results to copy data (ProcessAndCopyFloats_82BB42A0). This
# is the "search these tables and copy the data" routine, part of the 0x82819 search-and-copy cluster. 334 lines. 2 callees (LinearSearch,
# ProcessAndCopyFloats). 1 caller (ProcessAndDispatch_828180A8).
[entrypoint.functions."0x82819130"]
name = "ProcessAndSearchAndCopy_82819130"

# A processor that resolves a value and accesses an element. It takes a structure (a: fields at a->8, a->12, and a->20). It reads or resolves a
# value (ProcessAndReadOrResolveValue_82BC6F00 with code 2). It then checks a->12 against a->8 (a bounds check); when a->12 is within range and
# not a special sentinel it reads the element's type field (at +4) and checks it against 5. Based on these checks it performs further processing
# (using ElementAccess_821E1498 and sub_82BC68F0). This is the "resolve this value and access the element" routine, called only via the vtable
# (0 direct callers), part of the 0x82BD6 element cluster. 144 lines. 3 callees (ProcessAndReadOrResolveValue, ElementAccess, sub_82BC68F0). 0
# direct callers (vtable dispatch).
[entrypoint.functions."0x82BD60F8"]
name = "ProcessAndResolveAndAccess_82BD60F8"

# A checker that verifies a value is within a range. It takes a structure (a: fields at a->0, a->4, a->20, and a->24) and a value. It reads the
# flag at a->24; when the flag is less than 8 it uses the address a->4, otherwise it reads the value at a->4. It returns 0 when the value is less
# than that address. Otherwise (when a->24 is at least 8) it reads a->0, computes (a->20 * 2) + a->0, and returns 1 when the result exceeds the
# value, else 0. This is the "check whether this value is in range" routine, a leaf function (no callees), part of the 0x82BF0 range-check
# cluster. 48 lines. 0 callees. 1 caller (sub_82BF1B00).
[entrypoint.functions."0x82BF0C90"]
name = "CheckValueInRange_82BF0C90"

# A processor that computes an offset with bounds checking. It takes a structure (a: a field at a->24), a value (b), and another value. It
# computes a mask of (b | 7). When the mask exceeds -2 it uses b directly as the offset; otherwise it reads a->24, computes a->24 / 2 and checks
# that (mask / 3) is less than a->24 / 2 and that a->24 is at most (a->24 / 2) - 2, and when those bounds hold it uses (a->24 / 2) + a->24 as the
# offset. It then computes a scaled size ((offset + 1) * 2) and uses it to copy or zero a buffer (Buffer_CopyOrZero_Error_82CA3730 and
# ProcessAndCopyOrZeroBuffer_82BF1048), updating a global (ProcessAndUpdateGlobal_82BF21E0). This is the "compute this offset and copy the
# buffer" routine, part of the 0x82BF1 buffer cluster. 127 lines. 3 callees (Buffer_CopyOrZero_Error, ProcessAndCopyOrZeroBuffer,
# ProcessAndUpdateGlobal). 1 caller (sub_82BF1460).
[entrypoint.functions."0x82BF1190"]
name = "ProcessAndComputeOffset_82BF1190"

# A processor that computes an offset and copies a buffer. It takes a structure (a: fields at a->20 and a->24), a value (b), and another value.
# When b exceeds -2 it calls sub_82CD11D0 (a process step). It reads a->24; when a->24 is at least b it delegates to
# ProcessAndComputeOffset_82BF1190 (passing a->20 as a size); otherwise it inspects the low byte of the third value and b (including a special
# case for b == 8) and performs further processing (ProcessAndCopyOrZeroBuffer_82BF1048). This is the "compute this offset and copy the buffer"
# routine (second form, the caller of ProcessAndComputeOffset), part of the 0x82BF1 buffer cluster. 116 lines. 3 callees (sub_82CD11D0,
# ProcessAndComputeOffset, ProcessAndCopyOrZeroBuffer). 2 callers.
[entrypoint.functions."0x82BF1460"]
name = "ProcessAndComputeOffset2_82BF1460"

# A processor that checks a value and copies a buffer. It takes a structure (a: fields at a->4 and a->24), a value (b), and another value (c). It
# first checks whether b is in range (CheckValueInRange_82BF0C90). When the check fails it takes a default path. Otherwise it reads a->24; when
# a->24 is at least 8 it reads a->4, otherwise it uses the address a->4. It computes (b - that value) / 2 and delegates to sub_82BF1948 (passing
# it as a size), which copies or zeros the buffer (Buffer_CopyOrZero_Error_82CA3730 and ProcessAndComputeOffset2_82BF1460). This is the "check
# this value and copy the buffer" routine, part of the 0x82BF1 buffer cluster. 124 lines. 4 callees (CheckValueInRange, sub_82BF1948,
# ProcessAndComputeOffset2, Buffer_CopyOrZero_Error). 1 caller (sub_82BF1CA0).
[entrypoint.functions."0x82BF1B00"]
name = "ProcessAndCheckAndCopy_82BF1B00"

# A processor that copies a string. It takes a structure (a) and a UTF-16 string pointer (b). It first copies or zeros a buffer
# (ProcessAndCopyOrZeroBuffer_82BF1048 with zeroed arguments), computes the string's length (StrLenUTF16_8217AB30), and then checks the length and
# copies the string into the structure (ProcessAndCheckAndCopy_82BF1B00, passing the length). This is the "copy this string into the structure"
# routine, part of the 0x82BF1 string cluster. 56 lines. 3 callees (ProcessAndCopyOrZeroBuffer, StrLenUTF16, ProcessAndCheckAndCopy). 1 caller
# (sub_82BF1D00).
[entrypoint.functions."0x82BF1CA0"]
name = "ProcessAndCopyString_82BF1CA0"

# A processor that copies fields and data into a structure. It takes a structure (a: a data region at a->0, a string region at a->28, another
# data region at a->56, and a 16-bit field at a->84) and four more values. It copies or zeros the first data region (ProcessAndCopyDataAndSetCount_82BEC350
# with zeroed arguments), copies a string into a->28 (ProcessAndCopyString_82BF1CA0), copies or zeros the second data region at a->56,
# stores the fifth value into a->84, and performs further processing (ProcessAndGrowDataBySize_82BED440 and ProcessAndClearAndConvert_82CBD5A0). This
# is the "copy these fields and data into the structure" routine, part of the 0x82BF1 field-copy cluster. 192 lines. 5 callees
# (ProcessAndCopyDataAndSetCount, ProcessAndCopyString, ProcessAndGrowDataBySize, ProcessAndClearAndConvert, sub_82BF2B68). 1 caller
# (ProcessAndCheckString_82BF1E58).
[entrypoint.functions."0x82BF1D00"]
name = "ProcessAndCopyFieldsAndData_82BF1D00"

# A function that accesses an element by index. It takes a value, a structure (a: fields at a->40 and a->44), and another structure (b: fields at
# b->4 and b->8). It reads a->40 (a base pointer) and b->4 (an index); when the base is zero or the index is at least (a->44 - a->40) / 36 it asserts
# (trap 22). It then computes a byte offset ((index + (index * 8)) * 4) from the base, loads the element pointer at that offset, and reads its
# fields. This is the "access this element by index" routine, a leaf function (no callees), called only via the vtable (0 direct callers), part of
# the 0x82B40 element-access cluster. 74 lines. 0 callees. 0 direct callers (vtable dispatch).
[entrypoint.functions."0x82B40EB0"]
name = "AccessElementByIndex_82B40EB0"

# A thunk that returns a .data pointer. It unconditionally returns a fixed pointer from the .data section (computed as a constant offset). This is
# the "return this .data pointer" thunk (second form), called only via the vtable (0 direct callers), part of the 0x82C32 thunk cluster. 10 lines.
# 0 callees. 0 direct callers (vtable dispatch).
[entrypoint.functions."0x82C32770"]
name = "ReturnDataPointer2_Thunk_82C32770"

# A processor that searches an element array and adjusts a counter. It takes a structure (a: a pointer at a->8). It resolves the indexed pointer
# (GetIndexedPointer_82BC5448) and searches an element array (SearchElementArray_822CDF48) for the element pointed to by the indexed pointer (with a
# size of a->8 minus 8). When the search succeeds it advances a->8 by 8; when it fails it decrements a->8 by 8. This is the "search this element
# array and adjust the counter" routine, part of the 0x82188 array-search cluster. 59 lines. 2 callees (GetIndexedPointer, SearchElementArray). 1
# caller (sub_82BD5F08).
[entrypoint.functions."0x82188508"]
name = "ProcessAndSearchAndAdjustCounter_82188508"

# A processor that handles a value type. It takes a structure (a: fields at a->8 and a->12). It reads a->8 (a base) and a->12 (an index); when the
# index is out of range (at least a->8) or equals a .data sentinel it takes one path. Otherwise it reads the element's type field (at +4) and when
# it equals 2 it takes another path. It then performs substantial processing including storing elements (ProcessAndStoreElement_82BB1E58), hashing
# strings (HashCstring_82188CF0 and InsertHashEntry_8219CDA8), reading typed fields (GetTypedField_822AF338), and looping and dispatching
# (ProcessLoopDispatch_8227B8B8). This is the "handle this value type" routine, called only via the vtable (0 direct callers), part of the 0x82BB6
# type-handling cluster. 632 lines. 8 callees (sub_82BC68F0, sub_82BC6A18, sub_82BCBDC8, ProcessAndStoreElement, HashCstring, InsertHashEntry,
# GetTypedField, ProcessLoopDispatch). 0 direct callers (vtable dispatch).
[entrypoint.functions."0x82BB6CA0"]
name = "ProcessAndHandleType_82BB6CA0"

# A processor that handles a value type. It takes a structure (a: fields at a->8 and a->12). It reads a->8 (a base) and a->12 (an index); when the
# index is out of range (at least a->8) or equals a .data sentinel it calls sub_82BC68F0 (a processing step) with a .data value. Otherwise it reads
# the element's type field (at +4) and when it equals 5 it performs further processing. It uses the search-and-adjust routine
# (ProcessAndSearchAndAdjustCounter_82188508). This is the "handle this value type" routine (second form), called only via the vtable (0 direct
# callers), part of the 0x82BD5 type-handling cluster. 137 lines. 2 callees (sub_82BC68F0, ProcessAndSearchAndAdjustCounter). 0 direct callers
# (vtable dispatch).
[entrypoint.functions."0x82BD5F08"]
name = "ProcessAndHandleType2_82BD5F08"

# A function that validates a value and stores a pointer. It takes a result pointer, a value (b), and a structure (c: fields at c->4, c->20, and
# c->24). It first stores 0 into *result. When c or b is zero it asserts (trap 22). It reads the flag at c->24; when the flag is at least 16 it
# reads c->4, otherwise it uses the address c->4. It asserts that this address is not greater than b. When the flag is at least 16 it reads the
# value at c->4, adds c->20, and asserts that b does not exceed the sum. When all checks pass it stores c into *result. This is the "validate this
# value and store the pointer" routine, a leaf function (no callees), part of the 0x8236F validate cluster. 61 lines. 0 callees. 3 callers.
[entrypoint.functions."0x8236F008"]
name = "ValidateAndStorePointer_8236F008"

# A processor that initializes a string. It takes a result structure (a: a pointer at a->0 and two 16-bit fields at a->4 and a->6) and a source
# structure (b: a string pointer at b->0 and a length at b->4). It clears a->0, a->4, and a->6. When the length (b->4) is non-zero it reads the
# string pointer (b->0); when the pointer is zero it uses a .data default string. It then inserts the string into the result structure
# (StringInsert_82BA8850). This is the "initialize this string" routine, part of the 0x82BA8 string cluster. 60 lines. 1 callee (StringInsert). 6
# callers.
[entrypoint.functions."0x82BA8798"]
name = "ProcessAndInitializeString_82BA8798"

# A processor that handles a value type. It takes a structure (a: fields at a->8 and a->12). It reads a->8 (a base) and a->12 (an index); when the
# index is out of range (at least a->8) it uses a .data sentinel element. It reads the element's type field (at +4); when the type equals 4 it takes
# one path. Otherwise it processes the element (sub_82BCBDC8) and, when that succeeds, performs substantial processing including storing elements
# (ProcessAndStoreElement_82BB1E58), hashing strings (HashCstring_82188CF0), reading typed fields (GetTypedField_822AF338), processing indexed
# regions (ProcessIndexedRegion_82BA9068 and ProcessIndexedRegion2_82BA8F18), copying strings (StringCopyN_822CD828 and FindChar_82CA3980), and
# looping and dispatching (ProcessLoopDispatch_8227B8B8). This is the "handle this value type" routine (third form), called only via the vtable (0
# direct callers), part of the 0x82BB6 type-handling cluster. 419 lines. 11 callees. 0 direct callers (vtable dispatch).
[entrypoint.functions."0x82BB68D8"]
name = "ProcessAndHandleType3_82BB68D8"

# A processor that iterates a linked list. It takes a structure (a: fields at a->16 and a->84) and a value. It reads a->84 (a head pointer) and
# a->16 (a state structure). When the head is zero it takes a default path. Otherwise it walks the list (following the next pointer at +0), and
# for each node it checks the node's +8 field against the value; when it is less than the value it takes the default path. It inspects the node's
# type bits (at +5) against a flag in a->16 (at +20) and, based on that comparison, dispatches processing (DispatchAndAdjustPos_8227BB58 and
# StateDispatch_82BCA7D8). This is the "iterate this linked list and process each node" routine, part of the 0x82BCC list-iteration cluster. 220
# lines. 2 callees (DispatchAndAdjustPos, StateDispatch). 6 callers.
[entrypoint.functions."0x82BCC6F0"]
name = "ProcessAndIterateLinkedList_82BCC6F0"

# A thunk that invokes a method. It takes a structure (a: a pointer at a->8), reads the sub-object at a->8 (then at +0), reads the method pointer
# at offset +36 of that sub-object, and invokes it indirectly. This is the "invoke this method" thunk, called only via the vtable (0 direct
# callers), part of the 0x826C0 method cluster. 15 lines. 0 callees (single indirect call). 0 direct callers (vtable dispatch).
[entrypoint.functions."0x826C03B8"]
name = "InvokeMethod_Thunk_826C03B8"

# A processor that checks bounds and invokes a method. It takes a result pointer, a structure (a: fields at a->8, a->16, and a->24), and two more
# values. It reads a->16 (a base), adds the second value to it, and adds the low 32 bits of the third value. When the result exceeds a->24 (a
# limit) it stores 0 into *result and returns. Otherwise it reads the sub-object at a->8 (then at +0) and invokes the method at offset +16 of that
# sub-object indirectly. This is the "check these bounds and invoke the method" routine, called only via the vtable (0 direct callers), part of
# the 0x82B4D bounds-check cluster. 68 lines. 0 callees (single indirect call). 0 direct callers (vtable dispatch).
[entrypoint.functions."0x82B4DCB8"]
name = "ProcessAndBoundsCheckCall_82B4DCB8"

# A function that computes the difference between two fields. It takes a structure (a: two 64-bit fields at a->16 and a->24), reads both fields, and
# returns a->24 minus a->16. This is the "compute this field difference" routine, a leaf function (no callees), called only via the vtable (0
# direct callers), part of the 0x82B4D compute cluster. 12 lines. 0 callees. 0 direct callers (vtable dispatch).
[entrypoint.functions."0x82B4DD70"]
name = "ComputeFieldDifference_82B4DD70"

# A processor that computes a value. It takes a structure (a: a pointer at a->0), an index (b), and three more values. When the index is -1 it takes
# a default path. Otherwise it loops: it reads the pointer (a->0), loads the entry at (entry->12 + index * 4), extracts bits 18-32 (a 15-bit field),
# subtracts 0xFFFF from it, and when the result is not -1 it adds the index and computes the result. It uses sub_82BE1958 and sub_82BCDAF8 to
# perform the computation. This is the "compute this value" routine, part of the 0x82BE1 compute cluster. 149 lines. 2 callees (sub_82BE1958,
# sub_82BCDAF8). 5 callers.
[entrypoint.functions."0x82BE1A98"]
name = "ProcessAndComputeValue_82BE1A98"

# A processor that dispatches on a value. It takes a structure (a: a field at a->0). It reads the field, subtracts 6, and uses the result as an
# index (from 0 to 8); when the index exceeds 8 it takes a default path. It performs a table jump based on the index and dispatches to one of
# several paths (using sub_82BE1E98 and sub_82BE3410): indices 0-3 take distinct paths, indices 4-6 take one common path, and indices 7-8 take
# another. This is the "dispatch on this value" routine, part of the 0x82BE1 dispatch cluster. 228 lines. 2 callees (sub_82BE1E98, sub_82BE3410).
# 10 callers (all within the 0x82BE2x/0x82BDFx cluster).
[entrypoint.functions."0x82BE1F00"]
name = "ProcessAndDispatchByValue_82BE1F00"

# A processor that computes and stores a value. It takes a structure (a: fields at a->0, a->24, and a->32), a value (b), and another value. It reads
# a->24 (a count), a->0 (a pointer), and a->32 (an index). It computes a value (ProcessAndComputeValue_82BE1A98, passing 255 as a default and the
# count as a parameter). It then stores -1 into a->32, reads the sub-object at a->0 (at +44 and +12), and when (a->24 + 1) exceeds the sub-object's
# +44 field it takes one path; otherwise it performs further processing (sub_82BCF850). This is the "compute this value and store it" routine,
# part of the 0x82BE3 compute cluster. 128 lines. 2 callees (ProcessAndComputeValue, sub_82BCF850). 12 callers (all within the 0x82BE2x/0x82BDEx
# cluster).
[entrypoint.functions."0x82BE3410"]
name = "ProcessAndComputeAndStore_82BE3410"

# A processor that validates a linked list. It takes a structure (a: fields at a->0 and a->4). It reads a->0 (a header); when the header is zero it
# asserts (trap 22). It reads a->4 (a node) and asserts that the node's flag (at +21) is zero. It then reads the node's +8 field and walks the
# linked list (following the next pointer at +0), checking each node's flag (at +21). When it finds a node with a non-zero flag it stores the
# previous node into a->4 and returns. This is the "validate this linked list" routine, a leaf function (no callees), part of the 0x82BEB
# list-validation cluster. 81 lines. 0 callees. 3 callers.
[entrypoint.functions."0x82BEB9C0"]
name = "ProcessAndValidateList_82BEB9C0"

# A processor that checks a string. It takes a base pointer, a value (b), and another value (c). When b or c is zero it returns 87. It scans the
# string starting at the base + 12 (finding the first null terminator) and computes its length; when the length is zero it returns 5023. It then
# performs further processing including finding characters (FindChar_82CA3980), storing and dispatching (ProcessAndStoreAndDispatch_82BF2EC8),
# querying file attributes (QueryFileAttributes_82CBD368), and setting a global field (SetGlobalField_Thunk_82CC0750). This is the "check this
# string" routine (second form), called only via the vtable (0 direct callers), part of the 0x82BF3 string-check cluster. 237 lines. 7 callees. 0
# direct callers (vtable dispatch).
[entrypoint.functions."0x82BF3690"]
name = "ProcessAndCheckString2_82BF3690"

# A processor that queries data and copies it. It takes a value. It allocates a 328-byte buffer (sub_82CC1FC8); when the allocation fails (a
# negative result) it releases the buffer (sub_82CC1C38) and returns 0. Otherwise it copies the data into the buffer (CopyFieldsAndBlock_82CC9E60)
# and returns 1. This is the "query this data and copy it" routine, part of the 0x82CC0 query cluster. 56 lines. 3 callees (sub_82CC1FC8,
# sub_82CC1C38, CopyFieldsAndBlock). 4 callers.
[entrypoint.functions."0x82CC0BB8"]
name = "ProcessAndQueryAndCopy_82CC0BB8"

# A processor that queries data. It takes a value (a) and another value (b). It sets up a local buffer and invokes a query function indirectly (the
# function pointer at a .data structure's +24 field), passing 0 for several parameters. When the query returns a negative value it returns that
# error code; otherwise it reads the size field from the local buffer (at +96) and returns it. This is the "query this data" routine, a leaf
# function (no callees, single indirect call), part of the 0x82CC1 query cluster. 55 lines. 0 callees (single indirect call). 1 caller
# (ProcessAndQueryAndCopy_82CC0BB8).
[entrypoint.functions."0x82CC1FC8"]
name = "ProcessAndQueryData_82CC1FC8"

# A processor that performs an atomic decrement. It takes a structure (a: fields at a->0 and a->4). It atomically decrements the counter at a->4
# (using lwarx/stwcx. with the global lock and MSR save/restore). When the decremented counter is zero and a is non-zero it reads a->0 and performs
# further processing (passing 1 as a parameter); otherwise it takes a default path. This is the "atomically decrement this counter" routine, a leaf
# function (no callees), called only via the vtable (0 direct callers), part of the 0x82B41 atomic-counter cluster. 77 lines. 0 callees. 0 direct
# callers (vtable dispatch).
[entrypoint.functions."0x82B41F48"]
name = "ProcessAndAtomicDecrement_82B41F48"

# A thunk that adds an offset to a pointer. It takes a pointer and returns the pointer plus 12. This is the "add this offset to the pointer" thunk,
# part of the 0x82BF2 offset cluster. 8 lines. 0 callees. 1 caller (ProcessAndConstructWithFlag2_82BEDAB0).
[entrypoint.functions."0x82BF2D58"]
name = "AddOffset12_Thunk_82BF2D58"

# A function that compares 8 bytes and returns a bit. It takes a pointer; when the pointer is zero it returns (with a zero result). It compares the
# first 8 bytes at the pointer against a .data pattern, byte by byte. When it finds a mismatch it computes the number of leading zero bits in the
# difference (cntlzw) and returns bit 5 of that count ((count >> 5) & 1). This is the "compare these 8 bytes and return a bit" routine, a leaf
# function (no callees), part of the 0x82BF2 compare cluster. 41 lines. 0 callees. 3 callers.
[entrypoint.functions."0x82BF2D60"]
name = "CompareAndReturnBit_82BF2D60"

# A thunk that returns a .data pointer. It unconditionally returns a fixed pointer from the .data section (computed as a constant offset). This is
# the "return this .data pointer" thunk (third form), called only via the vtable (0 direct callers), part of the 0x82C32 thunk cluster. 10 lines.
# 0 callees. 0 direct callers (vtable dispatch).
[entrypoint.functions."0x82C32378"]
name = "ReturnDataPointer3_Thunk_82C32378"

# A processor that checks a range. It takes a value, a structure (a: a field at a->4), and another structure (b: a field at b->0). It reads a->4
# (then at +4) to get a node, and checks the node's flag (at +17); when the flag is non-zero it takes one path. Otherwise it reads b->0 and walks
# the structure (following the next pointer at +0), comparing b->0 against each node's +12 field; when b->0 is less than the field it sets a flag to
# 0, otherwise to 1. It uses FindChainRoot5_824708F8 and sub_8282ED70 to perform the processing. This is the "check this range" routine, part of the
# 0x825BF range-check cluster. 195 lines. 2 callees (FindChainRoot5, sub_8282ED70). 12 callers.
[entrypoint.functions."0x825BF630"]
name = "ProcessAndCheckRange_825BF630"

# A processor that constructs a node. It takes a value, a structure (a: a field at a->8), and three more values. It reads a->8; when the field
# exceeds a threshold (0xFFFF | 65534) it sets a string (SetString_822F2020), processes the data (sub_826C3EF0 and sub_826C3FA8), and stores a .data
# descriptor. It then constructs and inserts a list node (Allocate_OrDbgBreak_8221F388, ListNodeInsert_8234D268, and SpliceListNode_82301AE0),
# performing further processing (sub_82171810 and sub_822F1F00). This is the "construct this node" routine, part of the 0x8282E node-construct
# cluster. 340 lines. 8 callees (SetString, sub_826C3EF0, sub_826C3FA8, Allocate_OrDbgBreak, ListNodeInsert, SpliceListNode, sub_82171810,
# sub_822F1F00). 3 callers.
[entrypoint.functions."0x8282ED70"]
name = "ProcessAndConstructNode_8282ED70"

# A processor that shifts elements and copies them. It takes a structure (a: a pointer at a->8) and a destination pointer. It resolves the indexed
# pointer (GetIndexedPointer_82BC5448). It reads a->8 (an end pointer) and, when it is greater than the destination, walks backwards (following the
# previous pointer at -8), shifting each 8-byte element (two 32-bit words) down by 8 bytes. When the walk completes it copies the final 8-byte
# element (two 32-bit words) into the destination. This is the "shift these elements and copy them" routine, part of the 0x82BC5 element cluster.
# 60 lines. 1 callee (GetIndexedPointer). 12 callers (all within the 0x82BDx/0x82BCx cluster).
[entrypoint.functions."0x82BC5690"]
name = "ProcessAndShiftAndCopy_82BC5690"

# A processor that counts elements and processes them. It takes a value, another value, and a pointer. When the second value is zero it takes a
# default path. Otherwise it walks the array (following the 8-byte elements), counting the number of non-zero elements until it finds a null
# terminator. It then processes the elements (ProcessAndAppendAndFindChar_82BC7328, with -10000 as a parameter and a .data string), performing
# further processing including computing string hashes (ComputeStringHash_82BC5EB0 and ProcessAndHashString2_821E1408), growing and zeroing a buffer
# (GrowAndZeroBuffer_82BC55B8), removing the first element (ProcessAndRemoveFirstElement_82BC5630), shifting and copying (ProcessAndShiftAndCopy_82BC5690),
# and applying (ProcessApply_8227B7E0). This is the "count these elements and process them" routine, part of the 0x82BC7 element cluster. 203 lines.
# 8 callees. 12 callers.
[entrypoint.functions."0x82BC7060"]
name = "ProcessAndCountAndProcess_82BC7060"

# A processor that compacts an array and frees its elements. It takes a structure (a: fields at a->0, a->4, and a->12). It stores a .data
# descriptor into a->0 and compacts the array (ArrayCompact_82C23DD0). It then reads the element count (a->12) and iterates over the array (a->4 is
# the array pointer), freeing each non-zero element (Free_Thunk_824FE010). This is the "compact this array and free its elements" routine, part of
# the 0x82C16 array cluster. 88 lines. 2 callees (ArrayCompact, Free_Thunk). 5 callers.
[entrypoint.functions."0x82C16490"]
name = "ProcessAndCompactAndFree_82C16490"

# A processor that constructs and sorts a structure. It takes a structure (a: fields at a->0, a->4, a->8, and a->12) and a value. It stores a .data
# descriptor into a->0, clears a->4, a->8, and a->12 to 0, and sets a->0 to a .data descriptor. It then sorts the structure (BubbleSort3_82C16C78,
# passing a->4 and the value). Finally it stores another .data descriptor into a->0. This is the "construct and sort this structure" routine, part
# of the 0x82C1A construct cluster. 58 lines. 1 callee (BubbleSort3). 2 callers.
[entrypoint.functions."0x82C1A500"]
name = "ProcessAndConstructAndSort_82C1A500"

# A processor that frees an array's elements. It takes a structure (a: fields at a->0, a->4, and a->12). It stores a .data descriptor into a->0.
# It reads the element count (a->12) and iterates over the array (a->4 is the array pointer), freeing each non-zero element
# (Free_Thunk_824FE010). This is the "free this array's elements" routine, part of the 0x82C1E array cluster. 85 lines. 1 callee (Free_Thunk). 3
# callers.
[entrypoint.functions."0x82C1E680"]
name = "ProcessAndFreeElements_82C1E680"

# A processor that compares strings and constructs values. It takes a pointer. It reads a .data structure (at +4 and +8) and compares the two
# pointers; when they are equal it takes a default path. Otherwise it loops: it reads the pointer's +0 field (a node), reads the node's +0 field
# (or 63 if the node is null), reads the .data structure's +4 field (a second node) and its +0 field (or 63 if null), and compares the two strings
# case-insensitively (StrCmpIgnoreCase_82CA6320). Based on the comparison it constructs strings (ConstructString_8217AB58), parses values
# (ProcessAndParseValue_82CAB438), constructs refcounted objects (ConstructRefCounted_8222CF18), releases refcounts (Release_RefCounted_821C67D8),
# queues (QueuePush_822F5C68), upper-cases (ToUpper_SdkRuntime_821EEB10), and performs further processing (sub_82369EE0, sub_8236A530,
# sub_829F84E0, sub_82BFC9C0, sub_82BFD4A8, sub_822F84D8, ClampAndUpdate_822F6380, and Free_SizeBucketed_Thunk_8221BE68). This is the "compare
# these strings and construct the values" routine, part of the 0x822F3 compare-construct cluster. 596 lines. 15 callees. 2 callers.
[entrypoint.functions."0x822F3D20"]
name = "ProcessAndCompareAndConstruct_822F3D20"

# A processor that computes an index and copies data. It takes a structure (a: fields at a->4, a->8, and a->12), a value, and another structure
# (b: a field at b->0). It computes a capacity index ((a->12 - a->4) >> 2) and an offset (a->8 - a->4) >> 2. When the value is zero it takes a
# default path. Otherwise it processes the data, constructing an array (ConstructArray4_823093F8), copying with bounds checking
# (CheckedCopy_82CA3808), and performing further processing (sub_82A97648), freeing size-bucketed memory (Free_SizeBucketed_Thunk_8221BE68). This
# is the "compute this index and copy the data" routine, part of the 0x822F7 compute-copy cluster. 368 lines. 4 callees (ConstructArray4,
# CheckedCopy, sub_82A97648, Free_SizeBucketed_Thunk). 2 callers.
[entrypoint.functions."0x822F7530"]
name = "ProcessAndComputeAndCopy_822F7530"

# A processor that searches a binary tree and processes the result. It takes a structure (a: a pointer at a->4) and another structure (b: a key at
# b->0). It reads a->4 (then at +4) to get the tree root. When the root's flag (at +21) is zero, it reads b->0 (the key) and walks the binary tree
# (comparing the key against each node's +12 field; going left to +0 or right to +8) until it finds a node with a non-zero flag (at +21). It then
# processes the found node (sub_82343D18), performing refcounted operations (CopyAssign_RefCounted_821F0108 and DestructRefCounted_82214F08). This
# is the "search this binary tree and process the result" routine, part of the 0x82343 binary-tree cluster. 183 lines. 3 callees (sub_82343D18,
# CopyAssign_RefCounted, DestructRefCounted). 11 callers.
[entrypoint.functions."0x82343900"]
name = "ProcessAndSearchAndProcess_82343900"

# A processor that searches a binary tree and inserts a node. It takes a structure (a: a field at a->4), another structure (b: fields at b->4 and
# b->8), and two values. It reads b->8; when it is zero it updates the node (ProcessAndUpdateNode_82344608, with b->4, 1, and the value). Otherwise
# it reads b->4 and the second value; when the second value is zero it takes a path, otherwise it processes the node (sub_82344470 and
# sub_8273F9D8), performing further processing (ProcessChain2_8250AF60). This is the "search this binary tree and insert the node" routine, part of
# the 0x82343 binary-tree cluster (called by ProcessAndSearchAndProcess_82343900). 298 lines. 4 callees (ProcessAndUpdateNode, sub_82344470,
# sub_8273F9D8, ProcessChain2). 1 caller.
[entrypoint.functions."0x82343D18"]
name = "ProcessAndSearchAndInsert_82343D18"

# A processor that copies fields and increments a refcount. It takes a count, a destination pointer, and a source pointer. It loops (count times,
# using the count register): it reads the source's three 32-bit fields (at +0, +4, and +8) and copies them into the destination. When the third
# field is non-zero it atomically increments the refcount at that field (lwarx/stwcx. with the global lock and MSR save/restore). This is the
# "copy these fields and increment the refcount" routine, a leaf function (no callees), part of the 0x82354 refcount cluster. 77 lines. 0 callees.
# 4 callers.
[entrypoint.functions."0x823541A8"]
name = "ProcessAndCopyAndIncrementRef_823541A8"

# A processor that copies and clones a refcounted node. It takes a value, another value, and a structure (a: fields at a->0, a->4, and a->8). It
# reads a->8; when it is non-zero it atomically increments the refcount at that field (lwarx/stwcx. with the global lock and MSR save/restore).
# It then processes the data, allocating checked memory (AllocateChecked_8264EFE0), copying and incrementing a node
# (ProcessAndCopyNodeAndIncrement_8290F610), and copying and incrementing a ref (ProcessAndCopyAndIncrementRef_823541A8), releasing refcounts
# (Release_RefCounted_829FF648), and performing further processing (sub_82367D10 and sub_82A97648), freeing size-bucketed memory
# (Free_SizeBucketed_Thunk_8221BE68). This is the "copy and clone this refcounted node" routine, part of the 0x82601 clone cluster. 380 lines. 7
# callees. 1 caller (Process12Byte_826016B0).
[entrypoint.functions."0x82601D80"]
name = "ProcessAndCopyAndClone_82601D80"

# A processor that clears fields and processes the data. It takes a value and a structure (a: a pointer at a->0). It reads a->0 (a node) and, when
# it is non-zero, reads the node's +4 field. It clears the fields (ProcessAndClearFields_82A1D980, with the node's +4 field + 1 as a parameter) and
# gets a sub-object field (GetSubObjField4_821EEA78). When the result is non-zero it iterates over the elements (reading a->0 as each element),
# constructing refcounted objects (ConstructRefCounted_8222CF18) and freeing size-bucketed memory (Free_SizeBucketed_Thunk_8221BE68). This is the
# "clear these fields and process the data" routine, part of the 0x82B39 process cluster. 125 lines. 4 callees (ProcessAndClearFields,
# GetSubObjField4, ConstructRefCounted, Free_SizeBucketed_Thunk). 12 callers.
[entrypoint.functions."0x82B398A8"]
name = "ProcessAndClearAndProcess_82B398A8"

# A processor that constructs a structure with fields. It takes a structure (a: fields at a->0, a->4, a->8, a->9, a->12, a->16, a->20, a->24, a->28,
# a->32, and a->36), a value, and another value. It initializes a->0 to a .data descriptor, clears a->4, sets a->8 to 1 (byte), clears a->9 (byte),
# and clears a->12, a->16, a->20, and a->24 to 0. It stores the value into a->28 and a->32, and clears a->36 to 0. It then allocates a 40-byte
# block (Allocate_OrDbgBreak_8221F388, at a->40). This is the "construct this structure with its fields" routine, part of the 0x82B43 construct
# cluster. 121 lines. 1 callee (Allocate_OrDbgBreak). 5 callers.
[entrypoint.functions."0x82B43908"]
name = "ProcessAndConstructWithFields_82B43908"

# A processor that allocates and constructs a structure. It takes a pointer (a destination), and two values. It allocates an 80-byte block
# (Allocate_OrDbgBreak_8221F388). When the allocation succeeds it constructs the structure with its fields (ProcessAndConstructWithFields_82B43908).
# It stores the result into the destination. When the result is non-zero it reads the result's +0 field (a vtable), reads the vtable's +4 entry
# (a method), and calls it indirectly (bctrl). This is the "allocate and construct this structure" routine, part of the 0x82B43 construct cluster
# (a sibling of ProcessAndConstructWithFields_82B43908). 65 lines. 2 callees (Allocate_OrDbgBreak, ProcessAndConstructWithFields). 2 callers.
[entrypoint.functions."0x82B43C18"]
name = "ProcessAndAllocateAndConstruct_82B43C18"

# A processor that checks bounds and processes the data. It takes a structure (a: fields at a->9 and a->32), another structure (b: a pointer at
# b->0), and two values. When b->0 is zero or the second value is zero it takes a default path. It reads a->9 (a flag byte); when the flag is non-
# zero it returns an error (-0x7FFF). Otherwise it reads a->32 (a capacity) and, when the first value exceeds the capacity, returns an error.
# Otherwise it processes the data, allocating an 80-byte block (Allocate_OrDbgBreak_8221F388) and freeing size-bucketed memory
# (Free_SizeBucketed_Thunk_8221BE68). This is the "check these bounds and process the data" routine, called only via the vtable (0 direct
# callers), part of the 0x82B43 bounds-check cluster. 299 lines. 2 callees (Allocate_OrDbgBreak, Free_SizeBucketed_Thunk). 0 direct callers
# (vtable dispatch).
[entrypoint.functions."0x82B43FF8"]
name = "ProcessAndCheckBounds_82B43FF8"

# A processor that creates an event. It takes a structure (a: fields at a->0 and a->128), a value (b: a byte at b->0), and another value. It
# computes a flag ((b->0 - 1) clz, bit 5) and an initial-state flag (the value's clz, bit 5, inverted). It clears a->0 and creates an event
# (CreateEvent_SdkRuntime_82CBC4B8, with 0, the computed flags, and 0), storing the resulting handle into a->128. This is the "create this event"
# routine, part of the 0x82B49 event cluster. 56 lines. 1 callee (CreateEvent_SdkRuntime). 8 callers.
[entrypoint.functions."0x82B49990"]
name = "ProcessAndCreateEvent_82B49990"

# A thunk that checks success. It takes a structure (a: a handle at a->128). It reads a->128 (the handle) and checks success
# (ProcessAndCheckSuccess_82CBBF60). This is the "check this handle's success" thunk, part of the 0x82B49 event cluster. 5 lines. 1 callee
# (ProcessAndCheckSuccess). 9 callers.
[entrypoint.functions."0x82B49B48"]
name = "CheckSuccess_Thunk_82B49B48"

# A processor that initializes a structure with descriptors. It takes a structure (a: fields at a->0, a->4, a->40, a->44, and a->48). It stores
# a .data descriptor into a->0, clears a->4 to 0, stores .data descriptors into a->40 and a->44, and clears a->48 to 0. This is the "initialize
# this structure with its descriptors" routine, a leaf function (no callees), part of the 0x82B49 descriptor cluster. 25 lines. 0 callees. 2
# callers.
[entrypoint.functions."0x82B49BF0"]
name = "ProcessAndInitializeWithDescriptors_82B49BF0"

# A processor that dispatches by a type field. It takes a structure (a: fields at a->0 and a->4). It stores a .data descriptor into a->0 and
# reads a->4 (a type field). When the type is 1 it calls sub_82B52500 (passing a->8); when the type is 2 it calls ProcessAndCallMethod_82B55128
# (passing a->8); otherwise it returns immediately. This is the "dispatch by this type field" routine, part of the 0x82B49 type-dispatch cluster.
# 34 lines. 2 callees (sub_82B52500, ProcessAndCallMethod). 2 callers.
[entrypoint.functions."0x82B49C28"]
name = "ProcessAndDispatchByType_82B49C28"

# A processor that dispatches and adjusts a position. It takes a structure (a: a pointer at a->16) and a value (an index). It reads a->16 (a
# node) and reads the node's +21 flag; when the flag is 2 it takes a path. Otherwise it reads the index + 1; when it exceeds a threshold (0xFFFF
# | 65535) it calls sub_82BC9860 (with a .data string), otherwise it calls DispatchAndAdjustPos_8227BB58 (with the index << 2). This is the
# "dispatch and adjust this position" routine, part of the 0x82BCD dispatch cluster. 162 lines. 2 callees (DispatchAndAdjustPos, sub_82BC9860). 3
# callers (including StateDispatch_82BCBB40 and ProcessDispatch2_82BCD7B0).
[entrypoint.functions."0x82BCD698"]
name = "ProcessAndDispatchAndAdjust_82BCD698"

# A processor that calls methods on a vtable. It takes a structure (a: a vtable at a->0) and a value. It reads a->0 (a vtable), reads the
# vtable's +24 entry (a method), and calls it indirectly (bctrl). When the result is non-zero it calls sub_82BFFFB0 (passing the value and the
# structure). When that result is non-zero it reads the vtable's +28 entry (another method) and calls it indirectly. This is the "call these
# methods on the vtable" routine, part of the 0x82BFC method cluster. 86 lines. 1 callee (sub_82BFFFB0). 1 caller (ProcessAndConstructTreeAndObject_82BFCEF8).
[entrypoint.functions."0x82BFCD38"]
name = "ProcessAndCallMethods_82BFCD38"

# A processor that constructs a value, gated by a .data flag. It takes a value and another value. It reads a .data flag byte (at offset 18916);
# when the flag is zero it constructs a 16-byte value (Construct16Byte_82275368, with two .data descriptors) and returns 0. When the flag is non-
# zero it processes the value, constructing a tree and object (ProcessAndConstructTreeAndObject_82BFCEF8), searching and processing
# (ProcessAndSearchAndProcess_82343900), copying and assigning a refcounted object (CopyAssign_RefCounted2_82265160), destructing a refcounted
# object (DestructRefCounted_82214F08), incrementing a counter (ProcessAndIncrementCounter_822D6B40), clearing and processing
# (ProcessAndClearAndProcess_82B398A8), releasing a global ref (ReleaseGlobalRef_821C6868), and performing further processing (sub_82BFF460,
# sub_82BFFFA0, sub_82C00150, sub_82C002A0). This is the "construct this value, gated by the flag" routine, part of the 0x82BFD construct
# cluster. 204 lines. 12 callees. 2 callers (including ProcessAndCompareAndConstruct_822F3D20).
[entrypoint.functions."0x82BFD4A8"]
name = "ProcessAndConstructWithFlag_82BFD4A8"

# A processor that creates an event and processes the data. It takes a value and another value. It reads a .data flag byte (at offset 18960);
# when the flag is non-zero it creates an event (ProcessAndCreateEvent_82B49990, with 1 and 0), calls methods (ProcessAndCallMethods_8223E958,
# with a .data descriptor and 1), and stores a .data descriptor into the result. It then checks the result's success (CheckSuccess_Thunk_82B49B48),
# checks the method (ProcessAndCheckMethod_8223E820), and waits for map status (ProcessAndWaitMapStatus_82B49B50). This is the "create this event
# and process the data" routine, part of the 0x82BFF event cluster. 98 lines. 5 callees (ProcessAndCreateEvent, ProcessAndCallMethods,
# CheckSuccess_Thunk, ProcessAndCheckMethod, ProcessAndWaitMapStatus). 2 callers (including ProcessAndConstructWithFlag_82BFD4A8).
[entrypoint.functions."0x82BFF460"]
name = "ProcessAndCreateEventAndProcess_82BFF460"

# A thunk that stores a field. It takes a structure (a: a pointer at a->4) and a value. It reads a->4 (a node) and stores the value into the
# node's +76 field. This is the "store this field" thunk, a leaf function (no callees), part of the 0x82BFF field-store cluster. 5 lines. 0
# callees. 2 callers (including ProcessAndConstructWithFlag_82BFD4A8).
[entrypoint.functions."0x82BFFFA0"]
name = "StoreField76_Thunk_82BFFFA0"

# A processor that processes a value with a .data descriptor. It takes a value (a: a field at a->4) and another value (b). It uses a .data
# descriptor and processes the values in sequence: it calls sub_82C023A8 (with b, the descriptor, and a), calls sub_82C1D970 (with a->4 and b),
# and calls sub_82C02518 (with b and the descriptor). This is the "process this value" routine, part of the 0x82BFF process cluster. 54 lines. 3
# callees (sub_82C023A8, sub_82C1D970, sub_82C02518). 1 caller (ProcessAndCallMethods_82BFCD38).
[entrypoint.functions."0x82BFFFB0"]
name = "ProcessAndProcess_82BFFFB0"

# A thunk that stores a .data descriptor. It takes a structure (a: a field at a->0). It stores a .data descriptor into a->0. This is the
# "store this .data descriptor" thunk, a leaf function (no callees), part of the 0x82C00 descriptor-store cluster. 5 lines. 0 callees. 12 callers
# (all within the 0x82C0x/0x82C2x/0x82C3x/0x82C4x cluster).
[entrypoint.functions."0x82C00008"]
name = "StoreDescriptor_Thunk_82C00008"

# A processor that allocates and processes a structure. It takes a structure (a: fields at a->0 and a->4). It clears a->4 to 0, stores a .data
# descriptor into a->0, and allocates a 168-byte block (Allocate_OrDbgBreak_8221F388). When the allocation succeeds it processes the block
# (sub_82C1B0B0) and stores the result into a->4; when it fails it stores 0 into a->4. This is the "allocate and process this structure" routine,
# part of the 0x82C00 allocate-process cluster. 61 lines. 2 callees (Allocate_OrDbgBreak, sub_82C1B0B0). 2 callers (including
# ProcessAndConstructWithFlag_82BFD4A8).
[entrypoint.functions."0x82C00150"]
name = "ProcessAndAllocateAndProcess_82C00150"

# A processor that destructs and frees a structure. It takes a structure (a: fields at a->0 and a->4). It stores a .data descriptor into a->0.
# It reads a->4 (a pointer); when the pointer is non-zero it destructs the object (sub_82C1B180) and frees it (Free_Thunk_824FE010). It then
# clears a->4 to 0. This is the "destruct and free this structure" routine, part of the 0x82C00 destruct cluster. 58 lines. 2 callees
# (sub_82C1B180, Free_Thunk). 2 callers (including ProcessAndConstructWithFlag_82BFD4A8).
[entrypoint.functions."0x82C002A0"]
name = "ProcessAndDestructAndFree_82C002A0"

# A processor that compares 12 bytes and returns a bit. It takes two pointers (a: a field at a->12, b: a field at b->12). It compares the first
# 12 bytes of a and b, byte by byte. When it finds a mismatch it computes the number of leading zero bits in the difference (cntlzw) and
# returns bit 5 of that count ((count >> 5) & 1). When all 12 bytes match it reads a->12 and b->12 and performs a further comparison. This is
# the "compare these 12 bytes and return a bit" routine (second form), a leaf function (no callees), part of the 0x82C00 compare cluster. 62
# lines. 0 callees. 1 caller (sub_82C033D8).
[entrypoint.functions."0x82C00F38"]
name = "CompareAndReturnBit2_82C00F38"

# A processor that allocates and constructs a tree. It takes a pointer (a: a field at a->4), a value (b), and another value (c). It allocates a
# 28-byte block (Allocate_OrDbgBreak_8221F388). When the allocation succeeds it increments an atomic counter (AtomicIncrement3_821F7798) and
# resets the tree object (ResetTreeObj_82C474C8, at +8). It then constructs a 16-byte value (Construct16Byte_82275368, with b), stores c into the
# block's +4 field, and processes the data (Tree_GetOrCreate_82C46CF8, with a->4). This is the "allocate and construct this tree" routine, part
# of the 0x82C02 tree cluster. 68 lines. 5 callees (Allocate_OrDbgBreak, AtomicIncrement3, ResetTreeObj, Construct16Byte, Tree_GetOrCreate). 1
# caller (ProcessAndProcess_82BFFFB0).
[entrypoint.functions."0x82C023A8"]
name = "ProcessAndConstructTree_82C023A8"

# A processor that processes elements. It takes a value (a). It constructs a refcounted object (ConstructRefCounted_8222CF18, with -1) and
# processes the elements (ProcessElements_Vcall4_82C46C10, with a + 4 and the refcounted object). It then destructs the refcounted object
# (DestructRefCounted_82214F08) and processes the tree (Tree_Process_Wrapper_82C47470, with a + 4 and the result). When the result is zero it
# frees memory (Free_Thunk_824FE010) and performs a no-op (NoOp_Stub_829CE870). This is the "process these elements" routine, part of the 0x82C02
# element cluster. 87 lines. 6 callees (ConstructRefCounted, ProcessElements_Vcall4, DestructRefCounted, Tree_Process_Wrapper, Free_Thunk,
# NoOp_Stub). 1 caller (ProcessAndProcess_82BFFFB0).
[entrypoint.functions."0x82C02518"]
name = "ProcessAndProcessElements_82C02518"

# A processor that queries and constructs a tree. It takes a value and another value. It queries and constructs a tree (ProcessAndQueryConstructTree_82C02FC8). When the result is 1 or 0 it takes a path; when it is 2 it
# initializes descriptors (ProcessAndInitializeWithDescriptors_82B49BF0), constructs a 104-byte object (Construct104ByteObject_82B48E30, with 0 and 65536),
# and processes the data. It dispatches by type (ProcessAndDispatchByType_82B49C28 and ProcessTypeDispatch_82B49D50), compares (CompareAndReturnBit2_82C00F38),
# constructs and processes a node (ConstructAndProcessNode_82C02E88), destructs and walks the tree (DestructAndWalkTree_82C02F68), performs virtual calls
# (VcallTriple_AllSuccess_82C011E8 and ProcessAndDispatchSubVirtual2_82C021B8), allocates and frees memory (Allocate_OrDbgBreak_8221F388 and
# Free_Thunk_824FE010), and performs further processing (sub_82C02108). This is the "query and construct this tree" routine, part of the 0x82C03
# query-construct cluster. 617 lines. 13 callees. 2 callers (including ProcessAndConstructTreeAndObject_82BFCEF8).
[entrypoint.functions."0x82C033D8"]
name = "ProcessAndQueryAndConstruct_82C033D8"

# A processor that walks a structure and compares strings. It takes a structure (a: fields at a->0 and a->8). It reads a->8 (a count); when the
# count is 1 or less it takes a default path. Otherwise it walks the structure (following a->0, with 4-byte steps), reading each element's +0 field
# (a node) and +4 field, and comparing the strings case-insensitively (StrCmpIgnoreCase_82CA6320, with a .data default string). This is the "walk
# this structure and compare the strings" routine, part of the 0x82C15 walk-compare cluster. 94 lines. 1 callee (StrCmpIgnoreCase). 1 caller
# (sub_82C1B890).
[entrypoint.functions."0x82C15A38"]
name = "ProcessAndWalkAndCompare_82C15A38"

# A processor that destructs and frees an array's elements. It takes a structure (a: fields at a->0, a->4, and a->12). It stores a .data descriptor
# into a->0. It reads the element count (a->12) and iterates over the array (a->4 is the array pointer); when each element is non-zero it destructs
# the refcounted object (DestructRefCounted_82214F08) and frees it (Free_Thunk_824FE010). This is the "destruct and free this array's elements" routine
# (second form), part of the 0x82C15 destruct cluster. 92 lines. 2 callees (DestructRefCounted, Free_Thunk). 2 callers.
[entrypoint.functions."0x82C15B28"]
name = "ProcessAndDestructAndFree2_82C15B28"

# A processor that compacts an array and destructs its elements. It takes a structure (a: fields at a->0, a->4, and a->12). It stores a .data
# descriptor into a->0 and compacts the array (ArrayCompact_82C23DD0). It then reads the element count (a->12) and iterates over the array (a->4 is
# the array pointer); when each element is non-zero it destructs the refcounted object (DestructRefCounted_82214F08) and frees it
# (Free_Thunk_824FE010). This is the "compact this array and destruct its elements" routine, part of the 0x82C16 array cluster. 95 lines. 3 callees
# (ArrayCompact, DestructRefCounted, Free_Thunk). 3 callers.
[entrypoint.functions."0x82C16520"]
name = "ProcessAndCompactAndDestruct_82C16520"

# A processor that calls methods on each array element. It takes a structure (a: fields at a->0, a->4, and a->12). It stores a .data descriptor
# into a->0. It reads the element count (a->12) and iterates over the array (a->4 is the array pointer). When each element is non-zero it reads the
# element's +60 field (a vtable); when the vtable is non-zero it reads the vtable's +4 entry (a method) and calls it indirectly (bctrl), passing the
# element's +56 field. It frees size-bucketed memory (Free_Thunk_824FE010). This is the "call methods on each array element" routine, part of the
# 0x82C16 method cluster. 109 lines. 1 callee (Free_Thunk). 2 callers.
[entrypoint.functions."0x82C167D8"]
name = "ProcessAndCallMethods2_82C167D8"

# A processor that compacts an array and calls methods on each element. It takes a structure (a: fields at a->0, a->4, and a->12). It stores a .data
# descriptor into a->0 and compacts the array (ArrayCompact_82C23DD0). It then reads the element count (a->12) and iterates over the array (a->4 is the
# array pointer). When each element is non-zero it reads the element's +60 field (a vtable); when the vtable is non-zero it reads the vtable's +4 entry
# (a method) and calls it indirectly (bctrl), passing the element's +56 field. It frees size-bucketed memory (Free_Thunk_824FE010). This is the
# "compact this array and call methods on each element" routine, part of the 0x82C17 method cluster. 112 lines. 2 callees (ArrayCompact, Free_Thunk).
# 3 callers.
[entrypoint.functions."0x82C17678"]
name = "ProcessAndCompactAndCallMethods_82C17678"

# A processor that grows and processes an array. It takes a structure (a: a vtable at a->0) and another structure (b: a count at b->8). It reads a->0
# (a vtable) and calls the vtable's +0 entry (a method) indirectly (bctrl). When the result is 1 it calls the vtable's +20 entry (another method)
# indirectly. When that result is 1 it reads b->8 (a count) and processes the array: it grows the array (ProcessGrow_82C21C90), pushes back
# (Array_PushBack_82C21E08), performs a conditional assign (ProcessConditionalAssign_82C16740), performs virtual calls (VirtualCallPair_82C01D60),
# allocates memory (Allocate_OrDbgBreak_8221F388), and performs further processing (sub_82BFF7D8, sub_82BFF848, sub_82C02B98). This is the "grow and
# process this array" routine, part of the 0x82C17 grow cluster. 483 lines. 8 callees. 1 caller (sub_82C1CBD8).
[entrypoint.functions."0x82C17788"]
name = "ProcessAndGrowAndProcess_82C17788"

# A processor that grows and processes an array (second form). It takes a structure (a: a vtable at a->0) and another structure (b: a count at b->8). It
# reads a->0 (a vtable) and calls the vtable's +0 entry (a method) indirectly (bctrl). When the result is 1 it calls the vtable's +20 entry (another
# method) indirectly. When that result is 1 it reads b->8 (a count) and processes the array: it grows the array (ProcessGrow_82C21C90), pushes back
# (Array_PushBack_82C21E08), performs a conditional assign (ProcessConditionalAssign_82C16740), performs virtual calls (VirtualCallPair_82C01D60),
# allocates memory (Allocate_OrDbgBreak_8221F388), and performs further processing (sub_82BFF7D8, sub_82BFF848, sub_82C02B98). This is the "grow and
# process this array" routine (second form, a twin of ProcessAndGrowAndProcess_82C17788), part of the 0x82C17 grow cluster. 483 lines. 8 callees. 1
# caller (sub_82C1CD68).
[entrypoint.functions."0x82C17AE8"]
name = "ProcessAndGrowAndProcess2_82C17AE8"

# A processor that grows and processes an array (third form). It takes a structure (a: a vtable at a->0) and another structure (b: a count at b->8). It
# reads a->0 (a vtable) and calls the vtable's +0 entry (a method) indirectly (bctrl). When the result is 1 it calls the vtable's +20 entry (another
# method) indirectly. When that result is 1 it reads b->8 (a count) and processes the array: it grows the array (ProcessGrow_82C21C90), pushes back
# (Array_PushBack_82C21E08), performs a dispatch (VcallChain_6_Dispatch_7_82C168E0), performs virtual calls (VirtualCallPair_82C01D60), allocates memory
# (Allocate_OrDbgBreak_8221F388), and performs further processing (sub_82BFF7D8 and sub_82BFF848, sub_82C02B98). This is the "grow and process this
# array" routine (third form, a twin of ProcessAndGrowAndProcess_82C17788 and ProcessAndGrowAndProcess2_82C17AE8), part of the 0x82C17 grow cluster.
# 515 lines. 8 callees. 1 caller (sub_82C1C398).
[entrypoint.functions."0x82C17E48"]
name = "ProcessAndGrowAndProcess3_82C17E48"

# A processor that grows and processes an array (fourth form). It takes a structure (a: a vtable at a->0) and another structure (b: a count at b->8). It
# reads a->0 (a vtable) and calls the vtable's +0 entry (a method) indirectly (bctrl). When the result is 1 it calls the vtable's +20 entry (another
# method) indirectly. When that result is 1 it reads b->8 (a count) and processes the array: it grows the array (ProcessGrow_82C21C90), pushes back
# (Array_PushBack_82C21E08), performs virtual calls (VirtualCallPair_82C01D60), allocates memory (Allocate_OrDbgBreak_8221F388), and performs further
# processing (sub_82BFF7D8, sub_82BFF848, sub_82C02B98, sub_82C16978). This is the "grow and process this array" routine (fourth form, a twin of
# ProcessAndGrowAndProcess_82C17788, ProcessAndGrowAndProcess2_82C17AE8, and ProcessAndGrowAndProcess3_82C17E48), part of the 0x82C18 grow cluster. 487
# lines. 8 callees. 1 caller (sub_82C1C528).
[entrypoint.functions."0x82C181E8"]
name = "ProcessAndGrowAndProcess4_82C181E8"

# A processor that grows and processes an array (fifth form). It takes a structure (a: a vtable at a->0) and another structure (b: a count at b->8). It
# reads a->0 (a vtable) and calls the vtable's +0 entry (a method) indirectly (bctrl). When the result is 1 it calls the vtable's +20 entry (another
# method) indirectly. When that result is 1 it reads b->8 (a count) and processes the array: it grows the array (ProcessGrow_82C21C90), pushes back
# (Array_PushBack_82C21E08), increments an atomic counter (AtomicIncrement3_821F7798), performs virtual calls (VirtualCallPair_82C01D60), allocates
# memory (Allocate_OrDbgBreak_8221F388), and performs further processing (sub_82BFF7D8, sub_82BFF848, sub_82C02B98, sub_82C16A10). This is the "grow
# and process this array" routine (fifth form, a twin of ProcessAndGrowAndProcess_82C17788, ProcessAndGrowAndProcess2_82C17AE8,
# ProcessAndGrowAndProcess3_82C17E48, and ProcessAndGrowAndProcess4_82C181E8), part of the 0x82C18 grow cluster. 481 lines. 9 callees. 1 caller
# (sub_82C1C6B8).
[entrypoint.functions."0x82C18550"]
name = "ProcessAndGrowAndProcess5_82C18550"

# A processor that grows and processes an array (sixth form). It takes a structure (a: a vtable at a->0) and another structure (b: a count at b->8). It
# reads a->0 (a vtable) and calls the vtable's +0 entry (a method) indirectly (bctrl). When the result is 1 it calls the vtable's +20 entry (another
# method) indirectly. When that result is 1 it reads b->8 (a count) and processes the array: it grows the array (ProcessGrow_82C21C90), pushes back
# (Array_PushBack_82C21E08), performs a dispatch (VcallChain_6_Triple_7_82C16AA8), performs virtual calls (VirtualCallPair_82C01D60), allocates memory
# (Allocate_OrDbgBreak_8221F388), and performs further processing (sub_82BFF7D8, sub_82BFF848, sub_82C02B98). This is the "grow and process this array"
# routine (sixth form, a twin of ProcessAndGrowAndProcess_82C17788, ProcessAndGrowAndProcess2_82C17AE8, ProcessAndGrowAndProcess3_82C17E48,
# ProcessAndGrowAndProcess4_82C181E8, and ProcessAndGrowAndProcess5_82C18550), part of the 0x82C18 grow cluster. 483 lines. 8 callees. 1 caller
# (sub_82C1D1F0).
[entrypoint.functions."0x82C188A8"]
name = "ProcessAndGrowAndProcess6_82C188A8"

# A processor that grows and processes an array (seventh form). It takes a structure (a: a vtable at a->0) and another structure (b: a count at b->8). It
# reads a->0 (a vtable) and calls the vtable's +0 entry (a method) indirectly (bctrl). When the result is 1 it calls the vtable's +20 entry (another
# method) indirectly. When that result is 1 it reads b->8 (a count) and processes the array (saving float registers): it grows the array
# (ProcessGrow_82C21C90), pushes back (Array_PushBack_82C21E08), increments an atomic counter (AtomicIncrement3_821F7798), performs virtual calls
# (VirtualCallPair_82C01D60), allocates memory (Allocate_OrDbgBreak_8221F388), and performs further processing (sub_82BFF7D8, sub_82BFF848,
# sub_82C02B98, sub_82C14CA0, sub_82C16BE0). This is the "grow and process this array" routine (seventh form, a twin of
# ProcessAndGrowAndProcess_82C17788, ProcessAndGrowAndProcess2_82C17AE8, ProcessAndGrowAndProcess3_82C17E48, ProcessAndGrowAndProcess4_82C181E8,
# ProcessAndGrowAndProcess5_82C18550, and ProcessAndGrowAndProcess6_82C188A8), part of the 0x82C18 grow cluster. 521 lines. 10 callees. 1 caller
# (sub_82C1D618).
[entrypoint.functions."0x82C18F50"]
name = "ProcessAndGrowAndProcess7_82C18F50"

# A processor that constructs and sorts a structure (second form). It takes a structure (a: fields at a->0, a->4, a->8, and a->12) and a value. It stores
# a .data descriptor into a->0, clears a->4, a->8, and a->12 to 0. It then sorts the structure (BubbleSort3_82C16C78, passing a->4 and the value).
# Finally it stores another .data descriptor into a->0. This is the "construct and sort this structure" routine (second form, a twin of
# ProcessAndConstructAndSort_82C1A500), part of the 0x82C1A construct cluster. 58 lines. 1 callee (BubbleSort3). 1 caller (sub_82C1C848).
[entrypoint.functions."0x82C1A568"]
name = "ProcessAndConstructAndSort2_82C1A568"

# A processor that grows and processes an array (eighth form). It takes a structure (a: a vtable at a->0) and another structure (b: a count at b->8). It
# reads a->0 (a vtable) and calls the vtable's +0 entry (a method) indirectly (bctrl). When the result is 1 it calls the vtable's +20 entry (another
# method) indirectly. When that result is 1 it reads b->8 (a count) and processes the array (saving float registers): it grows the array
# (ProcessGrow_82C21C90), pushes back (Array_PushBack_82C21E08), performs virtual calls (VirtualCallPair_82C01D60), allocates memory
# (Allocate_OrDbgBreak_8221F388), and performs further processing (sub_82BFF7D8, sub_82BFF848, sub_82C02B98, sub_82C16158, sub_82C192D8). This is the
# "grow and process this array" routine (eighth form, a twin of ProcessAndGrowAndProcess_82C17788, ProcessAndGrowAndProcess2_82C17AE8,
# ProcessAndGrowAndProcess3_82C17E48, ProcessAndGrowAndProcess4_82C181E8, ProcessAndGrowAndProcess5_82C18550, ProcessAndGrowAndProcess6_82C188A8,
# and ProcessAndGrowAndProcess7_82C18F50), part of the 0x82C1A grow cluster. 586 lines. 9 callees. 1 caller (sub_82C1C848).
[entrypoint.functions."0x82C1A5D0"]
name = "ProcessAndGrowAndProcess8_82C1A5D0"

# A processor that constructs and sorts a structure (third form). It takes a structure (a: fields at a->0, a->4, a->8, and a->12) and a value. It stores
# a .data descriptor into a->0, clears a->4, a->8, and a->12 to 0. It then sorts the structure (BubbleSort3_82C16C78, passing a->4 and the value).
# Finally it stores another .data descriptor into a->0. This is the "construct and sort this structure" routine (third form, a twin of
# ProcessAndConstructAndSort_82C1A500 and ProcessAndConstructAndSort2_82C1A568), part of the 0x82C1A construct cluster. 58 lines. 1 callee
# (BubbleSort3). 1 caller (sub_82C1D618).
[entrypoint.functions."0x82C1A9C8"]
name = "ProcessAndConstructAndSort3_82C1A9C8"

# A processor that constructs a node and processes the data. It takes a structure (a: fields at a->0 and a->8). It constructs a 20-byte node
# (Construct20ByteNode_82C474E0, with .data descriptors). It then reads a->8 (a count) and iterates over the array (a->0 is the array pointer),
# performing a binary search (BinarySearch_82C0BC08), looking up a 24-byte element (Lookup24Element_82C174D8), storing a .data descriptor
# (StoreDescriptor_Thunk_82C00008), and destructing the 20-byte node (Destruct20ByteNode_82C475D8). This is the "construct this node and process the
# data" routine, part of the 0x82C1A node cluster. 242 lines. 5 callees (Construct20ByteNode, BinarySearch, Lookup24Element, StoreDescriptor_Thunk,
# Destruct20ByteNode). 1 caller (sub_82C1D970).
[entrypoint.functions."0x82C1ACD8"]
name = "ProcessAndConstructNodeAndProcess_82C1ACD8"

# A processor that initializes a 168-byte structure. It takes a structure (a: fields at a->0, a->4, a->8, a->12, a->16, a->20, a->24, a->28, a->32, a->36,
# a->40, a->44, a->48, a->52, a->56, a->60, a->64, a->68, a->72, a->76, and beyond). It clears a->0, a->4, and a->8 to 0, stores a .data descriptor into
# a->12, clears a->16, a->20, a->24, and a->28 to 0, stores a .data descriptor into a->32, and clears a->36 through a->76 (and beyond) to 0. This is the
# "initialize this 168-byte structure" routine, a leaf function (no callees), part of the 0x82C1B init cluster. 108 lines. 0 callees. 1 caller
# (ProcessAndAllocateAndProcess_82C00150).
[entrypoint.functions."0x82C1B0B0"]
name = "ProcessAndInitialize168Byte_82C1B0B0"

# A processor that walks and compares (second form). It takes a structure (a: fields at a->0 and a->8). It reads a->8 (a count); when the count is non-
# zero it computes a range (a->0 + (count << 2) to a->0) and calls sub_82C1B4E0 (with a->0, the element count, a byte flag, and the end pointer). It then
# walks the structure and compares (ProcessAndWalkAndCompare_82C15A38, with the structure). When the result is non-zero it performs a binary search
# (BinarySearch_Cmp_82C39988). This is the "walk and compare" routine (second form), part of the 0x82C1B walk-compare cluster. 86 lines. 3 callees
# (sub_82C1B4E0, ProcessAndWalkAndCompare, BinarySearch_Cmp). 1 caller (sub_82C1D970).
[entrypoint.functions."0x82C1B890"]
name = "ProcessAndWalkAndCompare2_82C1B890"

# A processor that finds a chain value and processes the data. It takes a structure (a: fields at a->76 and a->12). It reads a->76 (a node) and the node's
# +0 field, then finds a chain value (FindChainValue_82C469F0, with the node's +0 field + 4). When the result is non-zero it walks the chain (following the
# +36 field of each element), calling a method on each element (bctrl, via the element's +0 field's +28 entry). It then processes the data: it performs a
# sorted find (SortedFind_82C1B998) and a binary search (BinarySearch_Cmp_82C39988), grows the array (ProcessGrow_82C21C90), clears the element array
# (ClearElementArray_82C2D228) and compacts and clears (CompactClearArray_82C39298), sorts (BubbleSort3_82C16C78), copies memory (MemCpy_SdkRuntime_82CA2C60),
# performs a virtual call (ProcessVcall3_82C46F80), allocates and frees memory (Allocate_OrDbgBreak_8221F388 and Free_Thunk_824FE010). This is the "find this
# chain value and process the data" routine, part of the 0x82C1B find cluster. 415 lines. 11 callees. 1 caller (sub_82C1D970).
[entrypoint.functions."0x82C1BBA0"]
name = "ProcessAndFindAndProcess_82C1BBA0"

# A processor that finds a chain value and processes the data (second form). It takes a structure (a: fields at a->76 and a->32). It reads a->76 (a node) and
# the node's +0 field (+ 12), then finds a chain value (FindChainValue_82C469F0, with the node's +0 field + 12 + 4). When the result is non-zero it walks the
# chain (following the +36 field of each element), calling a method on each element (bctrl, via the element's +0 field's +24 entry). It then processes the
# data: it performs a sorted find (SortedFind_82C1B998) and a binary search (BinarySearch_Cmp_82C39988), grows the array (ProcessGrow_82C21C90), clears the
# element array (ClearElementArray_82C2D228) and compacts and clears (CompactClearArray_82C39298), sorts (BubbleSort3_82C16C78), copies memory
# (MemCpy_SdkRuntime_82CA2C60), performs a virtual call (ProcessVcall3_82C46F80), and allocates and frees memory (Allocate_OrDbgBreak_8221F388 and
# Free_Thunk_824FE010). This is the "find this chain value and process the data" routine (second form, a twin of ProcessAndFindAndProcess_82C1BBA0), part
# of the 0x82C1B find cluster. 423 lines. 11 callees. 1 caller (sub_82C1D970).
[entrypoint.functions."0x82C1BE80"]
name = "ProcessAndFindAndProcess2_82C1BE80"

# A processor that grows and processes an array (ninth form). It takes a structure (a: a vtable at a->0) and another structure (b: a count at b->8). It
# reads a->0 (a vtable) and calls the vtable's +0 entry (a method) indirectly (bctrl). When the result is 1 it calls the vtable's +20 entry (another method)
# indirectly. When that result is 1 it reads b->8 (a count) and processes the array: it grows and processes (ProcessAndGrowAndProcess3_82C17E48), constructs
# a sorted array (ConstructSortedArray_82C3D028), performs a sorted find (SortedFind_82C1B998 and SortedFind2_82C3ADF0), clears the element array
# (ClearElementArray_82C2D228) and compacts and clears (CompactClearArray_82C39298), and performs further processing (sub_82BFF7D8, sub_82C3BF18,
# sub_82C40BD0). This is the "grow and process this array" routine (ninth form, a twin of ProcessAndGrowAndProcess_82C17788, ProcessAndGrowAndProcess2_82C17AE8,
# ProcessAndGrowAndProcess3_82C17E48, ProcessAndGrowAndProcess4_82C181E8, ProcessAndGrowAndProcess5_82C18550, ProcessAndGrowAndProcess6_82C188A8,
# ProcessAndGrowAndProcess7_82C18F50, and ProcessAndGrowAndProcess8_82C1A5D0), part of the 0x82C1C grow cluster. 230 lines. 9 callees. 1 caller
# (sub_82C1CEF8).
[entrypoint.functions."0x82C1C398"]
name = "ProcessAndGrowAndProcess9_82C1C398"

# A processor that grows and processes an array (tenth form). It takes a structure (a: a vtable at a->0) and another structure (b: a count at b->8). It
# reads a->0 (a vtable) and calls the vtable's +0 entry (a method) indirectly (bctrl). When the result is 1 it calls the vtable's +20 entry (another method)
# indirectly. When that result is 1 it reads b->8 (a count) and processes the array: it grows and processes (ProcessAndGrowAndProcess4_82C181E8), constructs
# and sorts (ProcessAndConstructAndSort_82C1A500), frees the elements (ProcessAndFreeElements_82C1E680) and compacts and frees (ProcessAndCompactAndFree_82C16490),
# performs a sorted find (SortedFind_82C1B998 and SortedFind2_82C3ADF0), and performs further processing (sub_82BFF7D8, sub_82C3BF18, sub_82C40BD0). This is the
# "grow and process this array" routine (tenth form, a twin of ProcessAndGrowAndProcess_82C17788, ProcessAndGrowAndProcess2_82C17AE8, ProcessAndGrowAndProcess3_82C17E48,
# ProcessAndGrowAndProcess4_82C181E8, ProcessAndGrowAndProcess5_82C18550, ProcessAndGrowAndProcess6_82C188A8, ProcessAndGrowAndProcess7_82C18F50,
# ProcessAndGrowAndProcess8_82C1A5D0, and ProcessAndGrowAndProcess9_82C1C398), part of the 0x82C1C grow cluster. 230 lines. 9 callees. 1 caller (sub_82C1CF90).
[entrypoint.functions."0x82C1C528"]
name = "ProcessAndGrowAndProcess10_82C1C528"

# A processor that grows and processes an array (eleventh form). It takes a structure (a: a vtable at a->0) and another structure (b: a count at b->8). It
# reads a->0 (a vtable) and calls the vtable's +0 entry (a method) indirectly (bctrl). When the result is 1 it calls the vtable's +20 entry (another method)
# indirectly. When that result is 1 it reads b->8 (a count) and processes the array: it grows and processes (ProcessAndGrowAndProcess5_82C18550), constructs
# and bubble-sorts (ConstructAndBubbleSort_82C31B48), destructs the element array (DestructElementArray2_82C2AAF8) and compacts and destructs
# (CompactAndDestructArray_82C2FDC0), performs a sorted find (SortedFind_82C1B998 and SortedFind2_82C3ADF0), and performs further processing (sub_82BFF7D8,
# sub_82C3BF18, sub_82C40BD0). This is the "grow and process this array" routine (eleventh form, a twin of ProcessAndGrowAndProcess_82C17788,
# ProcessAndGrowAndProcess2_82C17AE8, ProcessAndGrowAndProcess3_82C17E48, ProcessAndGrowAndProcess4_82C181E8, ProcessAndGrowAndProcess5_82C18550,
# ProcessAndGrowAndProcess6_82C188A8, ProcessAndGrowAndProcess7_82C18F50, ProcessAndGrowAndProcess8_82C1A5D0, ProcessAndGrowAndProcess9_82C1C398, and
# ProcessAndGrowAndProcess10_82C1C528), part of the 0x82C1C grow cluster. 230 lines. 9 callees. 1 caller (sub_82C1D028).
[entrypoint.functions."0x82C1C6B8"]
name = "ProcessAndGrowAndProcess11_82C1C6B8"

# A processor that grows and processes an array (twelfth form). It takes a structure (a: a vtable at a->0) and another structure (b: a count at b->8). It
# reads a->0 (a vtable) and calls the vtable's +0 entry (a method) indirectly (bctrl). When the result is 1 it calls the vtable's +20 entry (another method)
# indirectly. When that result is 1 it reads b->8 (a count) and processes the array: it grows and processes (ProcessAndGrowAndProcess8_82C1A5D0), constructs
# and sorts (ProcessAndConstructAndSort2_82C1A568), calls methods (ProcessAndCallMethods2_82C167D8) and compacts and calls methods
# (ProcessAndCompactAndCallMethods_82C17678), performs a sorted find (SortedFind_82C1B998 and SortedFind2_82C3ADF0), and performs further processing
# (sub_82BFF7D8, sub_82C3BF18, sub_82C40BD0). This is the "grow and process this array" routine (twelfth form, a twin of ProcessAndGrowAndProcess_82C17788,
# ProcessAndGrowAndProcess2_82C17AE8, ProcessAndGrowAndProcess3_82C17E48, ProcessAndGrowAndProcess4_82C181E8, ProcessAndGrowAndProcess5_82C18550,
# ProcessAndGrowAndProcess6_82C188A8, ProcessAndGrowAndProcess7_82C18F50, ProcessAndGrowAndProcess8_82C1A5D0, ProcessAndGrowAndProcess9_82C1C398,
# ProcessAndGrowAndProcess10_82C1C528, and ProcessAndGrowAndProcess11_82C1C6B8), part of the 0x82C1C grow cluster. 230 lines. 9 callees. 1 caller
# (sub_82C1D0C0).
[entrypoint.functions."0x82C1C848"]
name = "ProcessAndGrowAndProcess12_82C1C848"

# A processor that sorts and processes the data. It takes a structure (a: fields at a->0 and a->8) and a value. It sorts the structure (BubbleSort3_82C16C78,
# passing the structure and the value + 4). It then reads a->8 (a count); when the count is non-zero it computes a range (a->0 + (count << 2) to a->0) and
# calls sub_82C1B770 (with a->0, the element count, a byte flag, and the end pointer). This is the "sort and process this data" routine, part of the 0x82C1C
# sort cluster. 65 lines. 2 callees (BubbleSort3, sub_82C1B770). 1 caller (sub_82C1D618).
[entrypoint.functions."0x82C1CB68"]
name = "ProcessAndSortAndProcess_82C1CB68"

# A processor that grows and processes an array (thirteenth form). It takes a structure (a: a vtable at a->0) and another structure (b: a count at b->8). It
# reads a->0 (a vtable) and calls the vtable's +0 entry (a method) indirectly (bctrl). When the result is 1 it calls the vtable's +20 entry (another method)
# indirectly. When that result is 1 it reads b->8 (a count) and processes the array: it grows and processes (ProcessAndGrowAndProcess_82C17788), invokes the
# elements (ProcessAndInvokeElements_82C15998) and compacts and invokes (ProcessAndCompactInvoke_82C1FAE0), sorts the elements (ProcessAndSortElements_82C1AE88)
# and performs a sort and search (ProcessAndSortAndSearch_82C1B920 and ProcessAndSortAndSearch2_82C1C170), and performs further processing (sub_82BFF7D8,
# sub_82C3BF18, sub_82C40BD0). This is the "grow and process this array" routine (thirteenth form, a twin of ProcessAndGrowAndProcess_82C17788,
# ProcessAndGrowAndProcess2_82C17AE8, ProcessAndGrowAndProcess3_82C17E48, ProcessAndGrowAndProcess4_82C181E8, ProcessAndGrowAndProcess5_82C18550,
# ProcessAndGrowAndProcess6_82C188A8, ProcessAndGrowAndProcess7_82C18F50, ProcessAndGrowAndProcess8_82C1A5D0, ProcessAndGrowAndProcess9_82C1C398,
# ProcessAndGrowAndProcess10_82C1C528, ProcessAndGrowAndProcess11_82C1C6B8, and ProcessAndGrowAndProcess12_82C1C848), part of the 0x82C1C grow cluster.
# 230 lines. 9 callees. 1 caller (sub_82C1D7A8).
[entrypoint.functions."0x82C1CBD8"]
name = "ProcessAndGrowAndProcess13_82C1CBD8"

# A processor that grows and processes an array (fourteenth form). It takes a structure (a: a vtable at a->0) and another structure (b: a count at b->8). It
# reads a->0 (a vtable) and calls the vtable's +0 entry (a method) indirectly (bctrl). When the result is 1 it calls the vtable's +20 entry (another method)
# indirectly. When that result is 1 it reads b->8 (a count) and processes the array: it grows and processes (ProcessAndGrowAndProcess2_82C17AE8), invokes the
# elements (ProcessAndInvokeElements_82C15998) and compacts and invokes (ProcessAndCompactInvoke_82C1FAE0), sorts the elements (ProcessAndSortElements_82C1AE88)
# and performs a sort and search (ProcessAndSortAndSearch_82C1B920 and ProcessAndSortAndSearch2_82C1C170), and performs further processing (sub_82BFF7D8,
# sub_82C3BF18, sub_82C40BD0). This is the "grow and process this array" routine (fourteenth form, a twin of ProcessAndGrowAndProcess_82C17788,
# ProcessAndGrowAndProcess2_82C17AE8, ProcessAndGrowAndProcess3_82C17E48, ProcessAndGrowAndProcess4_82C181E8, ProcessAndGrowAndProcess5_82C18550,
# ProcessAndGrowAndProcess6_82C188A8, ProcessAndGrowAndProcess7_82C18F50, ProcessAndGrowAndProcess8_82C1A5D0, ProcessAndGrowAndProcess9_82C1C398,
# ProcessAndGrowAndProcess10_82C1C528, ProcessAndGrowAndProcess11_82C1C6B8, ProcessAndGrowAndProcess12_82C1C848, and ProcessAndGrowAndProcess13_82C1CBD8),
# part of the 0x82C1C grow cluster. 230 lines. 9 callees. 1 caller (sub_82C1D840).
[entrypoint.functions."0x82C1CD68"]
name = "ProcessAndGrowAndProcess14_82C1CD68"

# A processor that grows and processes an array (fifteenth form). It takes a structure (a: a vtable at a->0) and a structure (b). It reads a->0 (a vtable) and
# calls the vtable's +24 entry (a method) indirectly (bctrl). When the result is non-zero it grows and processes the array (ProcessAndGrowAndProcess9_82C1C398,
# with a and b). When that result is non-zero it calls the vtable's +28 entry (another method) indirectly (bctrl). This is the "grow and process this array"
# routine (fifteenth form, a twin of ProcessAndGrowAndProcess_82C17788, ProcessAndGrowAndProcess2_82C17AE8, ProcessAndGrowAndProcess3_82C17E48,
# ProcessAndGrowAndProcess4_82C181E8, ProcessAndGrowAndProcess5_82C18550, ProcessAndGrowAndProcess6_82C188A8, ProcessAndGrowAndProcess7_82C18F50,
# ProcessAndGrowAndProcess8_82C1A5D0, ProcessAndGrowAndProcess9_82C1C398, ProcessAndGrowAndProcess10_82C1C528, ProcessAndGrowAndProcess11_82C1C6B8,
# ProcessAndGrowAndProcess12_82C1C848, ProcessAndGrowAndProcess13_82C1CBD8, and ProcessAndGrowAndProcess14_82C1CD68), part of the 0x82C1C grow cluster.
# 86 lines. 1 callee (ProcessAndGrowAndProcess9). 1 caller (sub_82C1D970).
[entrypoint.functions."0x82C1CEF8"]
name = "ProcessAndGrowAndProcess15_82C1CEF8"

# A processor that grows and processes an array (sixteenth form). It takes a structure (a: a vtable at a->0) and a structure (b). It reads a->0 (a vtable) and
# calls the vtable's +24 entry (a method) indirectly (bctrl). When the result is non-zero it grows and processes the array
# (ProcessAndGrowAndProcess10_82C1C528, with a and b). When that result is non-zero it calls the vtable's +28 entry (another method) indirectly (bctrl). This
# is the "grow and process this array" routine (sixteenth form, a twin of ProcessAndGrowAndProcess_82C17788, ProcessAndGrowAndProcess2_82C17AE8,
# ProcessAndGrowAndProcess3_82C17E48, ProcessAndGrowAndProcess4_82C181E8, ProcessAndGrowAndProcess5_82C18550, ProcessAndGrowAndProcess6_82C188A8,
# ProcessAndGrowAndProcess7_82C18F50, ProcessAndGrowAndProcess8_82C1A5D0, ProcessAndGrowAndProcess9_82C1C398, ProcessAndGrowAndProcess10_82C1C528,
# ProcessAndGrowAndProcess11_82C1C6B8, ProcessAndGrowAndProcess12_82C1C848, ProcessAndGrowAndProcess13_82C1CBD8, ProcessAndGrowAndProcess14_82C1CD68, and
# ProcessAndGrowAndProcess15_82C1CEF8), part of the 0x82C1C grow cluster. 86 lines. 1 callee (ProcessAndGrowAndProcess10). 1 caller (sub_82C1D970).
[entrypoint.functions."0x82C1CF90"]
name = "ProcessAndGrowAndProcess16_82C1CF90"

# A processor that grows and processes an array (seventeenth form). It takes a structure (a: a vtable at a->0) and a structure (b). It reads a->0 (a vtable) and
# calls the vtable's +24 entry (a method) indirectly (bctrl). When the result is non-zero it grows and processes the array
# (ProcessAndGrowAndProcess11_82C1C6B8, with a and b). When that result is non-zero it calls the vtable's +28 entry (another method) indirectly (bctrl). This is
# the "grow and process this array" routine (seventeenth form, a twin of ProcessAndGrowAndProcess_82C17788, ProcessAndGrowAndProcess2_82C17AE8,
# ProcessAndGrowAndProcess3_82C17E48, ProcessAndGrowAndProcess4_82C181E8, ProcessAndGrowAndProcess5_82C18550, ProcessAndGrowAndProcess6_82C188A8,
# ProcessAndGrowAndProcess7_82C18F50, ProcessAndGrowAndProcess8_82C1A5D0, ProcessAndGrowAndProcess9_82C1C398, ProcessAndGrowAndProcess10_82C1C528,
# ProcessAndGrowAndProcess11_82C1C6B8, ProcessAndGrowAndProcess12_82C1C848, ProcessAndGrowAndProcess13_82C1CBD8, ProcessAndGrowAndProcess14_82C1CD68,
# ProcessAndGrowAndProcess15_82C1CEF8, and ProcessAndGrowAndProcess16_82C1CF90), part of the 0x82C1D grow cluster. 86 lines. 1 callee
# (ProcessAndGrowAndProcess11). 1 caller (sub_82C1D970).
[entrypoint.functions."0x82C1D028"]
name = "ProcessAndGrowAndProcess17_82C1D028"

# A processor that grows and processes an array (eighteenth form). It takes a structure (a: a vtable at a->0) and a structure (b). It reads a->0 (a vtable) and
# calls the vtable's +24 entry (a method) indirectly (bctrl). When the result is non-zero it grows and processes the array
# (ProcessAndGrowAndProcess12_82C1C848, with a and b). When that result is non-zero it calls the vtable's +28 entry (another method) indirectly (bctrl). This is
# the "grow and process this array" routine (eighteenth form, a twin of ProcessAndGrowAndProcess_82C17788, ProcessAndGrowAndProcess2_82C17AE8,
# ProcessAndGrowAndProcess3_82C17E48, ProcessAndGrowAndProcess4_82C181E8, ProcessAndGrowAndProcess5_82C18550, ProcessAndGrowAndProcess6_82C188A8,
# ProcessAndGrowAndProcess7_82C18F50, ProcessAndGrowAndProcess8_82C1A5D0, ProcessAndGrowAndProcess9_82C1C398, ProcessAndGrowAndProcess10_82C1C528,
# ProcessAndGrowAndProcess11_82C1C6B8, ProcessAndGrowAndProcess12_82C1C848, ProcessAndGrowAndProcess13_82C1CBD8, ProcessAndGrowAndProcess14_82C1CD68,
# ProcessAndGrowAndProcess15_82C1CEF8, ProcessAndGrowAndProcess16_82C1CF90, and ProcessAndGrowAndProcess17_82C1D028), part of the 0x82C1D grow cluster.
# 86 lines. 1 callee (ProcessAndGrowAndProcess12). 1 caller (sub_82C1D970).
[entrypoint.functions."0x82C1D0C0"]
name = "ProcessAndGrowAndProcess18_82C1D0C0"

# A processor that grows and processes an array (nineteenth form). It takes a structure (a: a vtable at a->0) and another structure (b: a count at b->8). It reads
# a->0 (a vtable) and calls the vtable's +0 entry (a method) indirectly (bctrl). When the result is 1 it calls the vtable's +20 entry (another method) indirectly.
# When that result is 1 it reads b->8 (a count) and processes the array: it grows and processes (ProcessAndGrowAndProcess6_82C188A8), constructs a sorted array
# (ConstructSortedArray_82C3D028), sorts the elements (ProcessAndSortElements_82C28E80 and ProcessAndSortElements2_82C28EF0), clears the element array
# (ClearElementArray_82C2D228) and compacts and clears (CompactClearArray_82C39298), and performs further processing (sub_82BFF7D8, sub_82C3BF18, sub_82C40BD0).
# This is the "grow and process this array" routine (nineteenth form, a twin of ProcessAndGrowAndProcess_82C17788, ProcessAndGrowAndProcess2_82C17AE8,
# ProcessAndGrowAndProcess3_82C17E48, ProcessAndGrowAndProcess4_82C181E8, ProcessAndGrowAndProcess5_82C18550, ProcessAndGrowAndProcess6_82C188A8,
# ProcessAndGrowAndProcess7_82C18F50, ProcessAndGrowAndProcess8_82C1A5D0, ProcessAndGrowAndProcess9_82C1C398, ProcessAndGrowAndProcess10_82C1C528,
# ProcessAndGrowAndProcess11_82C1C6B8, ProcessAndGrowAndProcess12_82C1C848, ProcessAndGrowAndProcess13_82C1CBD8, ProcessAndGrowAndProcess14_82C1CD68,
# ProcessAndGrowAndProcess15_82C1CEF8, ProcessAndGrowAndProcess16_82C1CF90, and ProcessAndGrowAndProcess17_82C1D028), part of the 0x82C1D grow cluster. 230
# lines. 9 callees. 1 caller (sub_82C1D580).
[entrypoint.functions."0x82C1D1F0"]
name = "ProcessAndGrowAndProcess19_82C1D1F0"

# A processor that sorts and processes the data (second form). It takes a structure (a: fields at a->0, a->4, and a->12) and another structure (b). It sorts the
# data (BubbleSort3_82C16C78, passing b + 4 and a + 4). It then reads a->12 (a count); when the count is non-zero it computes a range (a + 4 -> 0 + (count << 2)
# to a + 4 -> 0) and calls sub_82C1B770 (with a + 4 -> 0, the element count, a byte flag, and the end pointer). It then compares strings (CompareStringsSigned_82C16660)
# and grows the array (ProcessGrow_82C21C90). It frees memory (Free_Thunk_824FE010). This is the "sort and process this data" routine (second form, a twin of
# ProcessAndSortAndProcess_82C1CB68), part of the 0x82C1D sort cluster. 288 lines. 5 callees (BubbleSort3, sub_82C1B770, CompareStringsSigned, ProcessGrow,
# Free_Thunk). 1 caller (sub_82C1D618).
[entrypoint.functions."0x82C1D380"]
name = "ProcessAndSortAndProcess2_82C1D380"

# A processor that grows and processes an array (twentieth form). It takes a structure (a: a vtable at a->0) and a structure (b). It reads a->0 (a vtable) and calls
# the vtable's +24 entry (a method) indirectly (bctrl). When the result is non-zero it grows and processes the array (ProcessAndGrowAndProcess19_82C1D1F0, with a
# and b). When that result is non-zero it calls the vtable's +28 entry (another method) indirectly (bctrl). This is the "grow and process this array" routine
# (twentieth form, a twin of ProcessAndGrowAndProcess_82C17788, ProcessAndGrowAndProcess2_82C17AE8, ProcessAndGrowAndProcess3_82C17E48, ProcessAndGrowAndProcess4_82C181E8,
# ProcessAndGrowAndProcess5_82C18550, ProcessAndGrowAndProcess6_82C188A8, ProcessAndGrowAndProcess7_82C18F50, ProcessAndGrowAndProcess8_82C1A5D0,
# ProcessAndGrowAndProcess9_82C1C398, ProcessAndGrowAndProcess10_82C1C528, ProcessAndGrowAndProcess11_82C1C6B8, ProcessAndGrowAndProcess12_82C1C848,
# ProcessAndGrowAndProcess13_82C1CBD8, ProcessAndGrowAndProcess14_82C1CD68, ProcessAndGrowAndProcess15_82C1CEF8, ProcessAndGrowAndProcess16_82C1CF90,
# ProcessAndGrowAndProcess17_82C1D028, and ProcessAndGrowAndProcess18_82C1D0C0), part of the 0x82C1D grow cluster. 86 lines. 1 callee
# (ProcessAndGrowAndProcess19). 1 caller (sub_82C1D970).
[entrypoint.functions."0x82C1D580"]
name = "ProcessAndGrowAndProcess20_82C1D580"

# A processor that grows and processes an array (twenty-first form). It takes a structure (a: a vtable at a->0) and another structure (b: a count at b->8). It reads
# a->0 (a vtable) and calls the vtable's +0 entry (a method) indirectly (bctrl). When the result is 1 it calls the vtable's +20 entry (another method) indirectly.
# When that result is 1 it reads b->8 (a count) and processes the array: it grows and processes (ProcessAndGrowAndProcess7_82C18F50), constructs and sorts
# (ProcessAndConstructAndSort3_82C1A9C8), sorts and processes (ProcessAndSortAndProcess_82C1CB68 and sub_82C1D380), destructs and frees
# (ProcessAndDestructAndFree2_82C15B28) and compacts and destructs (ProcessAndCompactAndDestruct_82C16520), and performs further processing (sub_82BFF7D8,
# sub_82C3BF18, sub_82C40BD0). This is the "grow and process this array" routine (twenty-first form, a twin of ProcessAndGrowAndProcess_82C17788,
# ProcessAndGrowAndProcess2_82C17AE8, ProcessAndGrowAndProcess3_82C17E48, ProcessAndGrowAndProcess4_82C181E8, ProcessAndGrowAndProcess5_82C18550,
# ProcessAndGrowAndProcess6_82C188A8, ProcessAndGrowAndProcess7_82C18F50, ProcessAndGrowAndProcess8_82C1A5D0, ProcessAndGrowAndProcess9_82C1C398,
# ProcessAndGrowAndProcess10_82C1C528, ProcessAndGrowAndProcess11_82C1C6B8, ProcessAndGrowAndProcess12_82C1C848, ProcessAndGrowAndProcess13_82C1CBD8,
# ProcessAndGrowAndProcess14_82C1CD68, ProcessAndGrowAndProcess15_82C1CEF8, ProcessAndGrowAndProcess16_82C1CF90, ProcessAndGrowAndProcess17_82C1D028,
# ProcessAndGrowAndProcess18_82C1D0C0, and ProcessAndGrowAndProcess19_82C1D1F0), part of the 0x82C1D grow cluster. 230 lines. 9 callees. 1 caller
# (sub_82C1D8D8).
[entrypoint.functions."0x82C1D618"]
name = "ProcessAndGrowAndProcess21_82C1D618"

# A processor that grows and processes an array (twenty-second form). It takes a structure (a: a vtable at a->0) and a structure (b). It reads a->0 (a vtable) and
# calls the vtable's +24 entry (a method) indirectly (bctrl). When the result is non-zero it grows and processes the array (ProcessAndGrowAndProcess13_82C1CBD8,
# with a and b + 4). When that result is non-zero it calls the vtable's +28 entry (another method) indirectly (bctrl). This is the "grow and process this array"
# routine (twenty-second form, a twin of ProcessAndGrowAndProcess_82C17788, ProcessAndGrowAndProcess2_82C17AE8, ProcessAndGrowAndProcess3_82C17E48,
# ProcessAndGrowAndProcess4_82C181E8, ProcessAndGrowAndProcess5_82C18550, ProcessAndGrowAndProcess6_82C188A8, ProcessAndGrowAndProcess7_82C18F50,
# ProcessAndGrowAndProcess8_82C1A5D0, ProcessAndGrowAndProcess9_82C1C398, ProcessAndGrowAndProcess10_82C1C528, ProcessAndGrowAndProcess11_82C1C6B8,
# ProcessAndGrowAndProcess12_82C1C848, ProcessAndGrowAndProcess13_82C1CBD8, ProcessAndGrowAndProcess14_82C1CD68, ProcessAndGrowAndProcess15_82C1CEF8,
# ProcessAndGrowAndProcess16_82C1CF90, ProcessAndGrowAndProcess17_82C1D028, ProcessAndGrowAndProcess18_82C1D0C0, ProcessAndGrowAndProcess19_82C1D1F0,
# ProcessAndGrowAndProcess20_82C1D580, and ProcessAndGrowAndProcess21_82C1D618), part of the 0x82C1D grow cluster. 86 lines. 1 callee
# (ProcessAndGrowAndProcess13). 1 caller (sub_82C1D970).
[entrypoint.functions."0x82C1D7A8"]
name = "ProcessAndGrowAndProcess22_82C1D7A8"

# A processor that grows and processes an array (twenty-third form). It takes a structure (a: a vtable at a->0) and a structure (b). It reads a->0 (a vtable) and
# calls the vtable's +24 entry (a method) indirectly (bctrl). When the result is non-zero it grows and processes the array (ProcessAndGrowAndProcess14_82C1CD68,
# with a and b + 4). When that result is non-zero it calls the vtable's +28 entry (another method) indirectly (bctrl). This is the "grow and process this array"
# routine (twenty-third form, a twin of ProcessAndGrowAndProcess_82C17788, ProcessAndGrowAndProcess2_82C17AE8, ProcessAndGrowAndProcess3_82C17E48,
# ProcessAndGrowAndProcess4_82C181E8, ProcessAndGrowAndProcess5_82C18550, ProcessAndGrowAndProcess6_82C188A8, ProcessAndGrowAndProcess7_82C18F50,
# ProcessAndGrowAndProcess8_82C1A5D0, ProcessAndGrowAndProcess9_82C1C398, ProcessAndGrowAndProcess10_82C1C528, ProcessAndGrowAndProcess11_82C1C6B8,
# ProcessAndGrowAndProcess12_82C1C848, ProcessAndGrowAndProcess13_82C1CBD8, ProcessAndGrowAndProcess14_82C1CD68, ProcessAndGrowAndProcess15_82C1CEF8,
# ProcessAndGrowAndProcess16_82C1CF90, ProcessAndGrowAndProcess17_82C1D028, ProcessAndGrowAndProcess18_82C1D0C0, ProcessAndGrowAndProcess19_82C1D1F0,
# ProcessAndGrowAndProcess20_82C1D580, ProcessAndGrowAndProcess21_82C1D618, and ProcessAndGrowAndProcess22_82C1D7A8), part of the 0x82C1D grow cluster.
# 86 lines. 1 callee (ProcessAndGrowAndProcess14). 1 caller (sub_82C1D970).
[entrypoint.functions."0x82C1D840"]
name = "ProcessAndGrowAndProcess23_82C1D840"

# A processor that grows and processes an array (twenty-fourth form). It takes a structure (a: a vtable at a->0) and a structure (b). It reads a->0 (a vtable) and
# calls the vtable's +24 entry (a method) indirectly (bctrl). When the result is non-zero it grows and processes the array
# (ProcessAndGrowAndProcess21_82C1D618, with a and b). When that result is non-zero it calls the vtable's +28 entry (another method) indirectly (bctrl). This is the
# "grow and process this array" routine (twenty-fourth form, a twin of ProcessAndGrowAndProcess_82C17788, ProcessAndGrowAndProcess2_82C17AE8,
# ProcessAndGrowAndProcess3_82C17E48, ProcessAndGrowAndProcess4_82C181E8, ProcessAndGrowAndProcess5_82C18550, ProcessAndGrowAndProcess6_82C188A8,
# ProcessAndGrowAndProcess7_82C18F50, ProcessAndGrowAndProcess8_82C1A5D0, ProcessAndGrowAndProcess9_82C1C398, ProcessAndGrowAndProcess10_82C1C528,
# ProcessAndGrowAndProcess11_82C1C6B8, ProcessAndGrowAndProcess12_82C1C848, ProcessAndGrowAndProcess13_82C1CBD8, ProcessAndGrowAndProcess14_82C1CD68,
# ProcessAndGrowAndProcess15_82C1CEF8, ProcessAndGrowAndProcess16_82C1CF90, ProcessAndGrowAndProcess17_82C1D028, ProcessAndGrowAndProcess18_82C1D0C0,
# ProcessAndGrowAndProcess19_82C1D1F0, ProcessAndGrowAndProcess20_82C1D580, ProcessAndGrowAndProcess21_82C1D618, ProcessAndGrowAndProcess22_82C1D7A8, and
# ProcessAndGrowAndProcess23_82C1D840), part of the 0x82C1D grow cluster. 86 lines. 1 callee (ProcessAndGrowAndProcess21). 1 caller (sub_82C1D970).
[entrypoint.functions."0x82C1D8D8"]
name = "ProcessAndGrowAndProcess24_82C1D8D8"

# A processor that dispatches to the grow-and-process routines. It takes a structure (a: a field at a->76) and a structure (b). It reads a->76 (a flag); when the flag
# is non-zero it performs a virtual call (VcallTriple_AllSuccess_82C011E8, with b, a .data descriptor, and a + 164). It then dispatches to the grow-and-process wrappers
# in sequence, each with b and a distinct .data descriptor offset into a: ProcessAndGrowAndProcess15_82C1CEF8 (a + 0), ProcessAndGrowAndProcess16_82C1CF90 (a + 80),
# ProcessAndGrowAndProcess17_82C1D028, ProcessAndGrowAndProcess18_82C1D0C0, ProcessAndGrowAndProcess20_82C1D580, and further wrappers (sub_82C1D7A8, sub_82C1D840,
# sub_82C1D8D8). It then calls the find-and-process routines (ProcessAndFindAndProcess_82C1BBA0 and ProcessAndFindAndProcess2_82C1BE80), the walk-and-compare routine
# (ProcessAndWalkAndCompare2_82C1B890), the construct-node-and-process routine (ProcessAndConstructNodeAndProcess_82C1ACD8), the dispatch-and-check routine
# (ProcessAndDispatchAndCheck_82C1D158), and further processing (sub_82BFF7D8). This is the central "dispatch to all the grow-and-process routines" routine, the
# common entry point for the 0x82C1D grow cluster. 326 lines. 15 callees. 1 caller (ProcessAndProcess_82BFFFB0).
[entrypoint.functions."0x82C1D970"]
name = "ProcessAndDispatchGrowAndProcess_82C1D970"

# A processor that locks and processes the data. It takes a structure (a: a field at a->12) and a structure (b: a field at b->0). It enters a critical section
# (CriticalSection_EnterOrTry_82200688, with a stack descriptor and a .data descriptor). It then reads b->0 (a node); when the node is non-zero it reads the node's
# +0 field as a descriptor. It then reads a->12 and calls sub_82BFD600 (with a->12 and the descriptor). When the result is non-zero it performs further processing
# (sub_82BFC9C0) and destructs the refcounted object (DestructRefCounted_82214F08). This is the "lock and process this data" routine, part of the 0x829F8 lock cluster.
# 132 lines. 4 callees (CriticalSection_EnterOrTry, sub_82BFD600, sub_82BFC9C0, DestructRefCounted). 2 callers (ProcessAndCompareAndConstruct_822F3D20, sub_823419E0).
[entrypoint.functions."0x829F84E0"]
name = "ProcessAndLockAndProcess_829F84E0"

# A processor that constructs a string. It takes a value (a) and a value (b). It reads a .data flag (byte); when the flag is zero it constructs a 16-byte object
# (Construct16Byte_82275368) and returns 0. When the flag is non-zero it constructs a 16-byte object (Construct16Byte_82275368) and constructs a string
# (ConstructString_8217AB58, with a stack descriptor and b). It then constructs and allocates (ConstructAndAllocate_82B43B08) and constructs with a flag
# (ProcessAndConstructWithFlag_82BFD4A8). It frees memory (Free_Thunk_824FE010). This is the "construct this string" routine, part of the 0x82BFD construct cluster.
# 220 lines. 5 callees (Construct16Byte, ConstructString, ConstructAndAllocate, ProcessAndConstructWithFlag, Free_Thunk). 1 caller (ProcessAndLockAndProcess_829F84E0).
[entrypoint.functions."0x82BFD600"]
name = "ProcessAndConstructString_82BFD600"

# A checker that checks a value and returns a descriptor. It takes a value (a). When the value is 1 it returns a .data descriptor (a specific object pointer).
# Otherwise it returns a different .data descriptor. This is the "check this value and return the appropriate descriptor" routine, a leaf function (no callees),
# part of the 0x82C32 check cluster. 25 lines. 0 callees. 0 callers (unresolved — body is recovered but no callers found).
[entrypoint.functions."0x82C320B0"]
name = "CheckAndReturnDescriptor_82C320B0"

# A processor that swaps and processes the data. It takes values (a, b, c), a structure (d: a field at d->0), and another structure (e: a field at e->0). It reads
# d->0 and e->0; when d->0 is less than e->0 it swaps the values (storing e->0 into d->0 and d->0 into e->0) and calls sub_82A5A588 (with d + 4 and e + 4). It then
# performs further processing. This is the "swap and process this data" routine, part of the 0x82B83 swap cluster. 113 lines. 1 callee (sub_82A5A588). 1 caller
# (sub_82A5A068).
[entrypoint.functions."0x82B834D0"]
name = "ProcessAndSwapAndProcess_82B834D0"

# A processor that computes a length and processes the data. It takes a structure (a: fields at a->4 and a->6, both halfwords), a pointer (b), and a length (c, or -1).
# When c is -1 it computes the string length of b (scanning for the null terminator). It then reads a->4 (a halfword) and a->6 (a halfword); when a->4 + c is less than or
# equal to a->6 it performs further processing (ProcessIndexedRegion_82BA9068 and ProcessIndexedRegion2_82BA8F18). It gets a singleton (GetSingleton_82BA8718) and copies
# memory (MemCpy_SdkRuntime_82CA2C60). This is the "compute this length and process the data" routine, part of the 0x82BA8 process cluster. 158 lines. 4 callees
# (ProcessIndexedRegion, ProcessIndexedRegion2, GetSingleton, MemCpy_SdkRuntime). 5 callers.
[entrypoint.functions."0x82BA89B0"]
name = "ProcessAndComputeLengthAndProcess_82BA89B0"

# A processor that checks and processes the data. It takes a structure (a: fields at a->8 and a->12). It reads a->12 (a pointer) and a->8 (a limit). It checks the
# pointer + 8 against the limit and a .data descriptor; when the check fails it sets a flag to -1. It then reads the pointer's +4 field (a type); when the type is 5 it
# performs a lookup (ProcessAndLookupIndexedPointer_82C16C78 is not called here; instead sub_82BC67F0, sub_82BC68F0, sub_82BC6A18, sub_82BC6F70) and dispatches by type
# (ProcessAndDispatchByType_82BC5FF0). This is the "check and process this data" routine, part of the 0x82BD5 check cluster. 190 lines. 6 callees. 0 callers (unresolved
# — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x82BD5440"]
name = "ProcessAndCheckAndProcess_82BD5440"

# A processor that checks and processes the data (second form). It takes a structure (a: fields at a->8 and a->12). It reads a->8 (a pointer) and a->12 (a limit).
# It computes a count ((a->8 - a->12) >> 3) and checks the pointer - 8 against a .data descriptor; when the check passes and the pointer's +4 field is zero it stores
# the pointer - 8 into a->8 and returns -1. Otherwise it performs a read (ProcessAndReadConvertedValue_82BC59D0), stores an element (ProcessAndStoreElement2_821E1548)
# and stores an indexed value (ProcessAndStoreIndexedValue_82BC5F48), and dispatches by type (ProcessAndDispatchByType2_82BC5B30). This is the "check and process this
# data" routine (second form), part of the 0x82BC7 check cluster. 124 lines. 4 callees. 3 callers.
[entrypoint.functions."0x82BC77F0"]
name = "ProcessAndCheckAndProcess2_82BC77F0"

# A processor that checks and processes the data (third form). It takes a structure (a: fields at a->0 and a->4) and a structure (b: fields at b->0 and b->4). It
# reads a->4 (a type); when the type is 4 it reads a->0 (a pointer) and calls sub_82BC82E0 (with the pointer + 16 and a stack descriptor). When the result is non-zero it
# reads a float (from the stack descriptor) into b->0, stores 3 into b->4 (a type), and returns b. This is the "check and process this data" routine (third form), part
# of the 0x82BCB check cluster. 78 lines. 1 callee (sub_82BC82E0). 12 callers (including ProcessAndReadOrResolveValue_82BC6F00).
[entrypoint.functions."0x82BCBD48"]
name = "ProcessAndCheckAndProcess3_82BCBD48"

# A processor that gets an indexed pointer and processes the data. It takes a value (a) and a value (b). It gets an indexed pointer (GetIndexedPointer_82BC5448, with a
# and b). It then reads the pointer's +4 field (a type); when the type is 3 it reads a float (from the pointer's +0 field). Otherwise it checks and processes the data
# (ProcessAndCheckAndProcess3_82BCBD48, with the pointer and a stack descriptor); when the result is non-zero it reads a float (from the result's +0 field). It then
# performs further processing (sub_82BC68F0). This is the "get this indexed pointer and process the data" routine, part of the 0x82188 pointer cluster. 119 lines. 3
# callees (GetIndexedPointer, ProcessAndCheckAndProcess3, sub_82BC68F0). 12 callers.
[entrypoint.functions."0x821885C0"]
name = "ProcessAndGetPointerAndProcess_821885C0"

# A processor that processes and clears the fields. It takes a structure (a: fields at a->0, a->4, and a->6, the last two halfwords). It reads a->0 (a pointer); when
# the pointer is non-zero it reads a->6 (a halfword) and adds 1, gets a singleton (GetSingleton_82BA8718, with the incremented value), and processes the indexed region
# (ProcessIndexedRegion_82BA9068, with the pointer and the incremented value). It then clears a->6, a->4, and a->0 to 0. This is the "process and clear these fields"
# routine, part of the 0x82BA8 process cluster. 50 lines. 2 callees (GetSingleton, ProcessIndexedRegion). 7 callers.
[entrypoint.functions."0x82BA8800"]
name = "ProcessAndClearFields_82BA8800"

# A processor that checks and processes the data (fourth form). It takes a structure (a: fields at a->8 and a->12). It reads a->12 (a pointer) and a->8 (a limit); when
# a->12 is less than a->8 and not equal to a .data descriptor, it reads the pointer's +4 field (a type). When the type is not 2 it calls sub_82BC68F0 (with a, 1, and a
# field from a .data descriptor). It then stores the element (ProcessAndStoreElement_82BB1E58), appends a node (ProcessAndAppendNode_82BC5CD8), grows the region
# (GrowRegion_823F9AE0), gets a typed field (GetTypedField_822AF338), processes the indexed region (ProcessIndexedRegion_82BA9068) and stores an indexed value
# (ProcessAndStoreIndexedValue_82BC5F48), and serializes (Serialize_82CA3EB8). It gets a singleton (GetSingleton_82BA8718). This is the "check and process this data" routine
# (fourth form), part of the 0x82BB7 check cluster. 228 lines. 9 callees. 0 callers (unresolved — body is recovered but no callers found; likely a vtable method or
# indirect entry).
[entrypoint.functions."0x82BB7700"]
name = "ProcessAndCheckAndProcess4_82BB7700"

# A processor that constructs and processes the data. It takes a structure (a: fields at a->0, a->4, a->8, and a->16) and a value (b). It clears a->4 to 0 and a->16 to 0.
# When b is non-zero it stores a .data descriptor into a->0, stores b into a->8, and calls a method on b (bctrl, via b->0's +4 entry). It then calls a method on a->8
# (bctrl, via a->8's +0 field's +36 entry). This is the "construct and process this data" routine, a leaf function (no direct callees, only indirect bctrl calls), part
# of the 0x82C66 construct cluster. 68 lines. 0 callees. 1 caller (sub_82C66830).
[entrypoint.functions."0x82C663E8"]
name = "ProcessAndConstructAndProcess_82C663E8"

# A processor that destructs and frees the data (third form). It takes a structure (a: fields at a->0 and a->8) and a value (b). It reads a->8 (a pointer); when the
# pointer is non-zero it calls a method on the pointer (bctrl, via the pointer's +0 field's +8 entry). It then stores a .data descriptor into a->0. When b's low bit is set
# it frees the memory (Free_Thunk_824FE010, with a). This is the "destruct and free this data" routine (third form), part of the 0x82C66 destruct cluster. 69 lines. 1
# callee (Free_Thunk). 0 callers (unresolved — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x82C664F8"]
name = "ProcessAndDestructAndFree3_82C664F8"

# A processor that allocates and constructs the data (second form). It takes a structure (a: a field at a->0) and a value (b). When b is zero it stores 0 into a->0.
# Otherwise it allocates 32 bytes (Allocate_OrDbgBreak_8221F388, with 32 and b); when the allocation succeeds it constructs and processes the data
# (ProcessAndConstructAndProcess_82C663E8, with the allocated pointer and b). It then stores the allocated pointer into a->0. This is the "allocate and construct this
# data" routine (second form), part of the 0x82C66 allocate cluster. 82 lines. 2 callees (Allocate_OrDbgBreak, ProcessAndConstructAndProcess). 2 callers.
[entrypoint.functions."0x82C66830"]
name = "ProcessAndAllocateAndConstruct2_82C66830"

# A processor that accesses and stores the data. It takes a structure (a: fields at a->4, a->8, and a->12), a value (b), a value (c), and a value (d). It computes an
# index ((c >> 2) - 1) and reads a->8 (a pointer); when the pointer + (index << 2) is non-zero it calls sub_83230490 (with the pointer + (index << 2) and d). It then reads
# a->4 (a pointer) and stores the result into a->4 + (b & ~3). This is the "access and store this data" routine, part of the 0x83236 access cluster. 77 lines. 2 callees
# (sub_83230490, ProcessAndPopulateGlobal_829FD060). 2 callers.
[entrypoint.functions."0x832365C0"]
name = "ProcessAndAccessAndStore_832365C0"

# A processor that walks and constructs the data. It takes a structure (a: fields at a->0 and a->4) and a value (b). It walks the chain and finds (WalkChainFind_83239FA8,
# with a .data descriptor and b). When the result is non-zero it stores 0 into a->0 and a->4, releases the refcounted object (Release_RefCounted_829FF648), and returns a.
# Otherwise it allocates 208 bytes (Allocate_OrDbgBreak_8221F388, with 208 and b), constructs a 32-byte vector (Construct32ByteVector_8321FB58), processes the refcount
# (ProcessRefCount_83225538), and sets the refcounted wrapper (SetRefcountedWrapper_8238A848). This is the "walk and construct this data" routine, part of the 0x8323B
# walk-construct cluster. 189 lines. 6 callees. 1 caller (StateDispatch4_83220888).
[entrypoint.functions."0x8323BCC8"]
name = "ProcessAndWalkAndConstruct_8323BCC8"

# A processor that decrements and processes the data. It takes a value (a), a structure (b: fields at b->4, b->12, and b->20), a value (c), and a value (d). It reads
# b->20 (a count); when the count is at least 1 it decrements b->20, increments b->12 and b->4, reads a byte (from b->12's +0 field) into a stack descriptor. It then
# copies the struct region (ProcessAndCopyStructRegion_82A1B480), processes the float data (ProcessFloatData_832206E8), and grows and appends (ProcessGrowAppend3_8321FC38).
# This is the "decrement and process this data" routine, part of the 0x8323C process cluster. 176 lines. 3 callees. 0 callers (unresolved — body is recovered but no
# callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x8323C0C0"]
name = "ProcessAndDecrementAndProcess_8323C0C0"

# A processor that computes and grows the data. It takes a value (a), a structure (b: fields at b->8, b->20, and b->32), a value (c), and a value (d). It reads b->32 (a
# count); when c is greater than the count it calls sub_82A2F6C0 (with a and b). It then computes the size (ProcessAndComputeSize_82A2F750, with a, b, and d). When c is
# less than the count it stores c into b->32, computes an element offset (ProcessAndComputeElementOffset_82229260), dispatches by size (ProcessDispatchSize_82A2FBE0) and
# value (ProcessDispatchValue_82228D98), and performs a virtual call (ProcessVirtual_82227940). It then performs further processing (sub_82A2C520). This is the "compute
# and grow this data" routine, part of the 0x82A2F compute-grow cluster. 249 lines. 7 callees. 2 callers (including GuildChest_ProcessItems_822DE008).
[entrypoint.functions."0x82A2F880"]
name = "ProcessAndComputeAndGrow_82A2F880"

# A processor that zeroes and processes the data. It takes a value (a) and a structure (b: a field at b->32). It zeroes a 27-element array on the stack. It then reads
# b->32 (a count) and iterates over the count: it validates the float value (ValidateFloatValue_82A2F620) and computes and grows the data (ProcessAndComputeAndGrow_82A2F880,
# with a and b). This is the "zero and process this data" routine, part of the 0x82A2F process cluster. 257 lines. 2 callees (ValidateFloatValue, ProcessAndComputeAndGrow).
# 1 caller (ProcessDispatchSize_82A2FBE0).
[entrypoint.functions."0x82A2FA28"]
name = "ProcessAndZeroAndProcess_82A2FA28"

# A processor that checks and processes the data (fifth form). It takes values (a, b, d, e) and a structure (c: a field at c->0). It reads c->0 (a count) and compares it
# against e >> 1 and e. When the count is between e >> 1 and e it calls sub_82BC9860 (with another value, f). It then dispatches and adjusts the position
# (DispatchAndAdjustPos_8227BB58). This is the "check and process this data" routine (fifth form), part of the 0x82BCF check cluster. 106 lines. 2 callees
# (sub_82BC9860, DispatchAndAdjustPos). 5 callers (including ProcessAndComputeAndStore_82BE3410).
[entrypoint.functions."0x82BCF850"]
name = "ProcessAndCheckAndProcess5_82BCF850"

# A processor that calls methods (third form). It takes a value (a) and a value (b). It calls a method (VcallTriple_AllSuccess_82C011E8, with b, a .data descriptor, and
# a). When the result is non-zero it calls another method (VcallTriple_AllSuccess_82C011E8, with b, a different .data descriptor, and a + 4). When that result is non-zero
# it performs further processing. This is the "call these methods" routine (third form), part of the 0x82C32 method cluster. 109 lines. 1 callee (VcallTriple_AllSuccess).
# 1 caller (sub_82C32840).
[entrypoint.functions."0x82C32780"]
name = "ProcessAndCallMethods3_82C32780"

# A processor that calls methods (fourth form). It takes a structure (a: a vtable at a->0) and a value (b). It reads a->0 (a vtable) and calls the vtable's +24 entry (a
# method) indirectly (bctrl). When the result is non-zero it calls methods (ProcessAndCallMethods3_82C32780, with b and a). When that result is non-zero it calls the
# vtable's +28 entry (another method) indirectly (bctrl). This is the "call these methods" routine (fourth form, a twin of ProcessAndCallMethods_82BFCD38,
# ProcessAndCallMethods2_82C167D8, and ProcessAndCallMethods3_82C32780), part of the 0x82C32 method cluster. 86 lines. 1 callee (ProcessAndCallMethods3). 1 caller
# (sub_82C32E28).
[entrypoint.functions."0x82C32840"]
name = "ProcessAndCallMethods4_82C32840"

# A copier that copies fields. It takes a structure (a: fields at a->12 and a->16). It reads a->12 (a pointer), then copies the pointer's +4 field into a->16 and the
# pointer's +16 field into a->12. This is the "copy these fields" routine, a leaf function (no callees), part of the 0x82C86 copy cluster. 16 lines. 0 callees. 4 callers.
[entrypoint.functions."0x82C864F0"]
name = "CopyFields_82C864F0"

# A processor that allocates and processes the data (second form). It takes a structure (a: a field at a->8) and a value (b). It reads a->8 (a pointer); when the pointer
# is zero it allocates 8 bytes (sub_82C88628, with 8 and b), and when the allocation succeeds it calls sub_82C88678 (with the allocated pointer). It stores the allocated
# pointer into a->8. It then reads a->8 (the pointer) and calls sub_82C89270 (with b and the pointer's +4 field). It performs further processing (sub_82C88630,
# sub_82C88D78). This is the "allocate and process this data" routine (second form), part of the 0x82C86 allocate cluster. 99 lines. 5 callees. 7 callers.
[entrypoint.functions."0x82C86988"]
name = "ProcessAndAllocateAndProcess2_82C86988"

# A copier that copies a field. It takes a structure (a: a field at a->12). It reads a->12 (a pointer) and copies the pointer's +16 field into a->12. This is the "copy
# this field" routine, a leaf function (no callees), part of the 0x82C86 copy cluster. 12 lines. 0 callees. 2 callers.
[entrypoint.functions."0x82C86A30"]
name = "CopyField_82C86A30"

# A processor that looks up and processes the data. It takes a structure (a: fields at a->0 and a->12), a value (b), and a value (c). It performs a locked table lookup
# (LockedTableLookup_82C81E58, with a + 12 and b). It then computes the string length (scanning for the null terminator). When the length is zero it calls a method on a
# (bctrl, via a->0's +0 entry). This is the "look up and process this data" routine, part of the 0x82C87 lookup cluster. 67 lines. 1 callee (LockedTableLookup). 0 callers
# (unresolved — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x82C87C30"]
name = "ProcessAndLookupAndProcess_82C87C30"

# A processor that looks up and processes the data (second form). It takes a structure (a: a field at a->12) and a value (b). It performs a locked table lookup
# (LockedTableLookup_82C81E58, with a + 12 and b). It then computes the string length (scanning for the null terminator). When the length is non-zero it processes the
# global struct (ProcessGlobalStruct_82C823D8, with a stack descriptor and b). It then reads a->12's +0 field and performs further processing (sub_82C825A0). This is the
# "look up and process this data" routine (second form), part of the 0x82C87 lookup cluster. 93 lines. 3 callees (LockedTableLookup, ProcessGlobalStruct, sub_82C825A0).
# 0 callers (unresolved — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x82C87CA0"]
name = "ProcessAndLookupAndProcess2_82C87CA0"

# A processor that calls a method and processes the data. It takes a structure (a: a field at a->28) and a value (b). It reads a->28 (a pointer) and calls a method on the
# pointer (bctrl, via the pointer's +0 field's +4 entry). It then reads b->0 (a pointer); when the pointer is non-zero it iterates over the array (b + 4, b + 8, ...), and for
# each non-zero element it processes the global struct (ProcessGlobalStruct_82C823D8, with a stack descriptor and the element). This is the "call this method and process the
# data" routine, part of the 0x82C88 call cluster. 80 lines. 1 callee (ProcessGlobalStruct). 0 callers (unresolved — body is recovered but no callers found; likely a vtable
# method or indirect entry).
[entrypoint.functions."0x82C88520"]
name = "ProcessAndCallAndProcess_82C88520"

# A caller that calls a method. It takes a structure (a: a field at a->28). It reads a->28 (a pointer) and calls the pointer's +12 field as a method (bctrl), with a + 28
# as the argument. This is the "call this method" routine, a leaf function (no direct callees), part of the 0x82C88 call cluster. 15 lines. 0 callees. 0 callers (unresolved
# — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x82C885A8"]
name = "CallMethod_82C885A8"

# A constructor that zeroes and processes the data. It takes a structure (a: fields at a->0 to a->40). It zeroes a->0 and a->4, then processes the sub-object at a + 8
# (ProcessGlobalStruct_82C823D8). It then processes the sub-object at a + 12 (ProcessGlobalStruct_82C823D8, with a .data descriptor). It then zeroes a->16, a->20, a->24,
# a->28, a->32, a->36, and a->40. This is the "construct and zero this data" routine, part of the 0x82C88 construct cluster. 67 lines. 1 callee (ProcessGlobalStruct). 1
# caller (sub_82C89270).
[entrypoint.functions."0x82C88D00"]
name = "ConstructAndZeroAndProcess_82C88D00"

# A processor that links and processes the data. It takes a structure (a: a field at a->32) and a value (b). It reads a->32 (a pointer); when the pointer is non-zero it
# processes the data at a + 32 (a 64-byte structure with fields at +0 and +4). It compares the pointer against the structure's +0 and +4 fields, and when they match it
# updates the structure's fields (with the pointer's +4 and +0 fields). When they don't match it links the pointer into the structure (storing the pointer's +4 field into the
# pointer's +0 field's +4). It performs further processing (sub_82CA29F0). This is the "link and process this data" routine, part of the 0x82C88 link cluster. 94 lines.
# 1 callee (sub_82CA29F0). 2 callers.
[entrypoint.functions."0x82C88F88"]
name = "ProcessAndLinkAndProcess_82C88F88"

# A processor that processes and frees the data. It takes a structure (a: a field at a->0). It reads a->0 (a pointer); when the pointer is non-zero it processes a chain of
# nodes. For each node it reads the node's +32 field (a pointer); when that pointer is non-zero it iterates over the inner chain (the pointer's +4 field): it calls NoOp_Stub
# (with the node and the inner pointer's +4 field) and calls sub_82CA29E8 (with the node), following the inner pointer's +4 field. It then stores zero into the node's +36
# field and processes the node's +20 sub-object (sub_82C87A68). This is the "process and free this data" routine (recursive), part of the 0x82C89 process cluster. 87 lines.
# 3 callees (NoOp_Stub, sub_82C87A68, sub_82CA29E8). 2 callers.
[entrypoint.functions."0x82C89080"]
name = "ProcessAndFreeAndProcess_82C89080"

# A processor that recurses and processes the data. It takes a structure (a: a field at a->20) and a value (b). It reads a->20 (a pointer); when the pointer is non-zero it
# recurses on the pointer (calling itself with the pointer and b). It then processes the sub-object at a + 20 (a 64-byte structure with fields at +0 and +4): it compares the
# pointer against the structure's +0 and +4 fields, and when they don't match it links the pointer into the structure (storing the pointer's +4 field into the pointer's +0
# field's +4). It then calls ProcessAndFreeAndProcess_82C89080 and ProcessAndLinkAndProcess_82C88F88, and performs further processing (NoOp_Stub, sub_82CA29E8). This is the
# "recurse and process this data" routine (recursive), part of the 0x82C89 recurse cluster. 146 lines. 4 external callees (NoOp_Stub, ProcessAndFreeAndProcess,
# ProcessAndLinkAndProcess, sub_82CA29E8) plus self-recursion. 1 external caller (sub_82C89208).
[entrypoint.functions."0x82C89110"]
name = "ProcessAndRecurseAndProcess_82C89110"

# A processor that grows a hash table and constructs the result. It takes a value (a) and a value (b). It grows the hash table (ProcessAndGrowHashTable_82C82220, with b and 44).
# When the growth succeeds (non-zero result) it constructs and zeroes the result (ConstructAndZeroAndProcess_82C88D00, with the result and a). It returns the result. This is
# the "grow the hash table and construct this" routine, part of the 0x82C89 construct cluster. 52 lines. 2 callees (ProcessAndGrowHashTable, ConstructAndZeroAndProcess). 1
# caller (ProcessAndAllocateAndProcess2_82C86988).
[entrypoint.functions."0x82C89270"]
name = "ProcessAndGrowHashTableAndConstruct_82C89270"

# A dispatcher that parses and processes the data. It takes a base pointer (a), an index (b), a limit (c), a structure (d: fields at d->0 and d->12), and other values (e, f).
# It iterates over the data starting at b + 1: for each byte it computes an index (the byte's value plus a base offset's +76 field minus 3) and dispatches on that index via a
# table lookup (bctrl) with 27 cases (0-26). Each case handles a specific token/opcode, updating the output structure (d) and advancing the index. When the computed index
# exceeds 26 it bails out. This is the "parse and process this data" routine (a large switch-based dispatch loop), part of the 0x82C8D dispatch cluster. 413 lines. 0 callees
# (all inline). 0 callers (unresolved — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x82C8D000"]
name = "DispatchAndParseAndProcess_82C8D000"

# A clearer that zeroes the data. It takes a structure (a: fields at a->0, a->4, a->8, a->12, and a->16). It reads a->4 (a pointer); when the pointer is zero it stores a->0
# into a->4. It then walks the chain (following the pointers' +0 fields), updating the structure's +4 field as it goes. It then zeroes a->0, a->8, a->12, and a->16. This is the
# "clear and zero this data" routine, a leaf function (no callees), part of the 0x82C99 clear cluster. 49 lines. 0 callees. 5 callers.
[entrypoint.functions."0x82C99728"]
name = "ClearAndZeroAndProcess_82C99728"

# A processor that queries and processes the data. It takes a structure (a: a field at a->356), values (b, c, f), and a structure (d: a field at d->0). It reads a->356 (a pointer
# to a large sub-object) and queries the hash table (ProcessAndQueryHashTable_82C99298, with the sub-object's +20 field and d->0). When the query fails (zero result) it queries
# the sub-object's +80 field (sub_82C9B268). It then performs a large amount of processing, checking and returning values (ProcessAndCheckAndReturn_82C9B2F8), copying memory
# (MemCpy_SdkRuntime_82CA2C60), and calling various helpers (sub_82C98AD0, sub_82C99808, sub_82C9AAD0, sub_82C9C9C0, sub_82C9D748). This is the "query and process this data"
# routine, part of the 0x82C9D process cluster. 1390 lines. 9 callees. 1 caller (sub_82C9EAC0).
[entrypoint.functions."0x82C9DC30"]
name = "ProcessAndQueryAndProcess_82C9DC30"

# A store routine for the last error code (SDK runtime). It takes an NT status (a) and a global kernel structure (r13: fields at +256, +336, and +352). It converts the NT status
# to a DOS error code (RtlNtStatusToDosError). When the kernel structure's +336 field is zero it reads the +256 field (a per-thread error block) and stores the converted DOS
# error code into its +352 field (the last error code). This is the "store the last error code" routine, a leaf function (no direct callees), part of the 0x82CC1 SDK error
# cluster. 35 lines. 0 callees. 12 callers (VirtualAllocate, VirtualFree, QueryFileAttributes, KeWaitForSingleObject_Guest, etc.).
[entrypoint.functions."0x82CC1C38"]
name = "StoreLastErrorCode_82CC1C38"

# A processor that computes and processes the data (second form). It takes a value (a), a structure (b: fields at b->4 and b->8), and a value (c). It reads b->4 (a limit) and
# b->8 (a start) and computes (b->8 - b->4) >> 2 (a count of 4-byte elements). When the limit is zero it returns zero. When the count is non-zero it processes the array (with the
# limit, start, count, and a), asserting that b->4 <= b->8 when c is non-zero. It then calls sub_8238EC68 (with the limit, start, count, and a). This is the "compute and process
# this data" routine (second form), part of the 0x8238E process cluster. 132 lines. 1 callee (sub_8238EC68). 12 callers.
[entrypoint.functions."0x8238EB88"]
name = "ProcessAndComputeAndProcess2_8238EB88"

# A processor that computes and processes the data (third form). It takes a structure (a: fields at a->4, a->8, and a->12), a value (b), and a structure (c: a field at c->0). It
# reads a->4 (a limit) and computes two values: (a->12 - a->4) >> 2 and (a->8 - a->4) >> 2 (counts of 4-byte elements). It then performs a large amount of processing: it
# allocates checked memory (AllocateChecked3_82333F78), copies checked data (CheckedCopy_82CA3808), frees size-bucketed memory (Free_SizeBucketed_Thunk_8221BE68), and calls
# sub_82684B38. This is the "compute and process this data" routine (third form), part of the 0x8238E process cluster. 412 lines. 4 callees (AllocateChecked3, CheckedCopy,
# Free_SizeBucketed_Thunk, sub_82684B38). 1 caller (ProcessAndComputeAndProcess2_8238EB88).
[entrypoint.functions."0x8238EC68"]
name = "ProcessAndComputeAndProcess3_8238EC68"

# A processor that calls methods (fifth form). It takes a structure (a: a vtable at a->0) and a value (b). It reads a->0 (a vtable) and calls the vtable's +24 entry (a method)
# indirectly (bctrl). When the result is non-zero it calls a method (VcallTriple_AllSuccess_82C011E8, with a, a .data descriptor, and b). When that result is non-zero it
# performs further processing. This is the "call these methods" routine (fifth form, a twin of ProcessAndCallMethods4_82C32840 and ProcessAndGrowAndProcess15_82C1CEF8), part of
# the 0x82C16 method cluster. 90 lines. 1 callee (VcallTriple_AllSuccess). 1 caller (ProcessAndDispatchAndGrow_82C18C08).
[entrypoint.functions."0x82C16B40"]
name = "ProcessAndCallMethods5_82C16B40"

# A processor that clamps and processes the data. It takes a structure (a: a field at a->4) and a structure (b: fields at b->4, b->20, and b->24). It reads a->4 (a pointer) and then
# that pointer's +4 field (another pointer), and reads the byte at the second pointer's +45 field. When that byte is zero it iterates: it reads b->24 (a limit) and b->20 (a value), and
# when the limit is >= 16 it reads the limit-th element of b + 4 (a pointer). It then calls ProcessAndClampGrow_8217CBE8 (with the second pointer's +12 field, 0, that field's +20
# value, the selected element, and b->20). This is the "clamp and process this data" routine, part of the 0x82C6C process cluster. 82 lines. 1 callee (ProcessAndClampGrow). 5
# callers.
[entrypoint.functions."0x82C6CFF0"]
name = "ProcessAndClampAndProcess_82C6CFF0"

# A lookup routine that looks up from a structure. It takes a structure (a: a field at a->12). It reads a->12 (a pointer); when the pointer is zero it returns zero. Otherwise it
# tail-calls LockedTableLookup_82C81E58 with the pointer + 8. This is the "look up from this structure" routine, part of the 0x82C86 lookup cluster. 20 lines. 1 callee
# (LockedTableLookup). 3 callers.
[entrypoint.functions."0x82C86260"]
name = "LookupFromStructure_82C86260"

# A caller that calls from a structure. It takes a structure (a: fields at a->8 and a->12). It reads a->12 (a pointer); when the pointer is zero it returns. Otherwise it reads a->8
# (another pointer) and that pointer's +4 field, and tail-calls sub_82C88E68 (with the first pointer and the second pointer's +4 field). This is the "call from this structure" routine,
# part of the 0x82C86 call cluster. 19 lines. 1 callee (sub_82C88E68). 0 callers (unresolved — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x82C86C50"]
name = "CallFromStructure_82C86C50"

# A processor that looks up and calls a method. It takes a structure (a: a field at a->0), a value (b), and a value (c). It reads a->0 (a pointer to a large sub-object) and performs a
# locked table lookup (LockedTableLookup_82C81E58, with c and the sub-object). It then reads the sub-object's +88 field (a method pointer) and calls it (bctrl) with a, b, and the
# lookup result. It returns 1. This is the "look up and call this method" routine, part of the 0x82C86 lookup cluster. 45 lines. 1 callee (LockedTableLookup). 1 caller
# (sub_82BF7740).
[entrypoint.functions."0x82C86EC0"]
name = "ProcessAndLookupAndCall_82C86EC0"

# An inserter that inserts a node into a list. It takes a structure (a: a list head at a + 20 with fields at a->24) and a node (b). It reads a->24 (the list's last node); when it is
# non-zero it links b in after it (storing b into the last node's +4 field and the last node's prev into b->0). When the list is empty it links b as both the first and last node.
# It zeroes b->4, increments the list's count (a + 20's +8 field), and stores a into b->16 (the container pointer). This is the "insert this node into the list" routine, a leaf
# function (no callees), part of the 0x82C88 insert cluster. 55 lines. 0 callees. 1 caller (ProcessAndAllocateAndProcess2_82C86988).
[entrypoint.functions."0x82C88D78"]
name = "InsertNodeIntoList_82C88D78"

# A constructor that inserts a node. It takes a structure (a: a list head at a + 32 with a field at a->36) and values (b, c, d). It allocates a node (sub_82CA2A08, with b, c, and d).
# It then inserts the node into the doubly-linked list at a + 32 (with fields at +0 for prev, +4 for next, and +8 for count): when the list's +4 field is non-zero it links the node
# after the last node, otherwise it makes the node both the first and last node. It zeroes the node's next field. This is the "construct and insert this node" routine, part of the
# 0x82C88 construct-insert cluster. 76 lines. 1 callee (sub_82CA2A08). 1 caller (sub_82C88E68).
[entrypoint.functions."0x82C88DE0"]
name = "ConstructAndInsertNode_82C88DE0"

# A processor that matches and inserts the data. It takes a structure (a: a field at a->32) and values (b, c, d). It reads a->32 (a pointer); when the pointer is non-zero it iterates
# over the list (following the pointers' +4 fields): for each node it performs a locked table lookup (LockedTableLookup_82C81E58, with the node's +8 field and b) and checks if the
# result matches a string (MatchStringWrapper_82CAAF08, with the node's +8 field and b). When a match is found it inserts the node (ConstructAndInsertNode_82C88DE0) and performs further
# processing (sub_82C825A0). This is the "match and insert this data" routine, part of the 0x82C88 match-insert cluster. 79 lines. 4 callees (LockedTableLookup, MatchStringWrapper,
# sub_82C825A0, ConstructAndInsertNode). 1 caller (CallFromStructure_82C86C50).
[entrypoint.functions."0x82C88E68"]
name = "ProcessAndMatchAndInsert_82C88E68"

# A processor that queries and processes the data (second form). It takes a structure (a: a field at a->356) and values (b, c, d). It reads a->356 (a pointer to a large sub-object) and
# compares the sub-object's +92 field against its +88 field. When they match it queries the sub-object's +80 field (sub_82C99808, with b). When they don't match (or the query fails)
# it processes the sub-object's +92 field (with b, c, and d): it checks and returns values (ProcessAndCheckAndReturn_82C9B2F8) and queries the hash table (ProcessAndQueryHashTable_82C99298).
# This is the "query and process this data" routine (second form), part of the 0x82C9C query cluster. 351 lines. 3 callees (ProcessAndCheckAndReturn, ProcessAndQueryHashTable,
# sub_82C99808). 2 callers (ProcessAndQueryAndProcess_82C9DC30, sub_82C9FB58).
[entrypoint.functions."0x82C9C9C0"]
name = "ProcessAndQueryAndProcess2_82C9C9C0"

# A decrementer that decrements a counter. It takes a value (a) and a structure (b: a field at b->12). When a is zero it returns. Otherwise it reads b->12 (a counter) and decrements
# it (storing b->12 - 1 back into b->12). This is the "decrement this counter" routine, a leaf function (no callees), part of the 0x82CA2 decrement cluster. 16 lines. 0 callees. 1
# caller (ProcessAndLinkAndProcess_82C88F88).
[entrypoint.functions."0x82CA29F0"]
name = "DecrementCounter_82CA29F0"

# A constructor that zeroes and processes the data (second form). It takes values (a, b, c). It grows the hash table (ProcessAndGrowHashTable_82C82220, with c and 16). When the growth
# succeeds (non-zero result) it zeroes the result's +0 and +4 fields, then processes the sub-object at the result + 8 (ProcessGlobalStruct_82C823D8, with a) and the sub-object at the
# result + 12 (ProcessGlobalStruct_82C823D8, with b). It returns the result. This is the "construct and zero this data" routine (second form), part of the 0x82CA2 construct cluster.
# 59 lines. 2 callees (ProcessAndGrowHashTable, ProcessGlobalStruct). 1 caller (ConstructAndInsertNode_82C88DE0).
[entrypoint.functions."0x82CA2A08"]
name = "ConstructAndZeroAndProcess2_82CA2A08"

# A processor that constructs and processes the data (second form). It takes a structure (a: a vtable at a->0) and a structure (b: a field at b->8). It calls a method on a (bctrl, via
# the vtable's +0 entry). When the result is 1 it calls another method (bctrl, via the vtable's +20 entry). When that result is 1 it reads b->8 (a count) and performs a large amount of
# processing: it allocates memory (Allocate_OrDbgBreak_8221F388), pushes to an array (Array_PushBack_82C21E08), dispatches a vcall sequence (DispatchVcallSequence4_82C1FBD0), initializes
# a struct (InitStruct_82C4DAC8), grows (ProcessGrow_82C21C90), and makes virtual calls (VirtualCallPair_82C01D60, sub_82BFF7D8, sub_82BFF848, sub_82C02B98). This is the "construct and
# process this data" routine (second form), part of the 0x82C1F construct cluster. 481 lines. 9 callees. 1 caller (sub_82C20AF0).
[entrypoint.functions."0x82C1FF48"]
name = "ProcessAndConstructAndProcess2_82C1FF48"

# A constructor that constructs a 16-byte object. It allocates 16 bytes (Allocate_OrDbgBreak_8221F388). When the allocation succeeds it initializes the object: it stores a .data descriptor
# into the object's +0 field and zeroes the +4, +8, and +12 fields. When the allocation fails it returns zero. It returns the allocated object. This is the "construct this 16-byte object"
# routine, part of the 0x82C20 construct cluster. 55 lines. 1 callee (Allocate_OrDbgBreak). 0 callers (unresolved — body is recovered but no callers found; likely a vtable method or
# indirect entry).
[entrypoint.functions."0x82C205B8"]
name = "Construct16ByteObject_82C205B8"

# A processor that sorts and recurses. It takes a structure (a: fields at a->0 and a->8) and a structure (b: a field at b->4). It sorts (BubbleSort3_82C16C78, with b + 4 and a). It then
# reads a->8 (a count); when the count is non-zero it processes the array recursively (ProcessRecursive3_82C20680, with a->0, a->0 + (count << 2), count, a stack descriptor, and 0).
# This is the "sort and recurse this data" routine, part of the 0x82C20 sort cluster. 65 lines. 2 callees (BubbleSort3, ProcessRecursive3). 1 caller (sub_82C20AF0).
[entrypoint.functions."0x82C20878"]
name = "ProcessAndSortAndRecurse_82C20878"

# A processor that sorts and recurses (second form). It takes a structure (a: fields at a->0, a->4, and a->12) and a structure (b: a field at b->4). It sorts (BubbleSort3_82C16C78,
# with b + 4 and a + 4). It then reads a->12 (a count); when the count is non-zero it processes the array recursively (ProcessRecursive3_82C20680, with a + 4's +0 field, that field
# + (count << 2), count, a stack descriptor, and 0). It then performs further processing (freeing with Free_Thunk_824FE010 and growing with ProcessGrow_82C21C90). This is the "sort
# and recurse this data" routine (second form), part of the 0x82C20 sort cluster. 290 lines. 4 callees (BubbleSort3, Free_Thunk, ProcessGrow, ProcessRecursive3). 1 caller
# (sub_82C20AF0).
[entrypoint.functions."0x82C208E8"]
name = "ProcessAndSortAndRecurse2_82C208E8"

# A processor that dispatches and processes the data (second form). It takes a structure (a: a vtable at a->0) and a structure (b: a field at b->8). It calls a method on a (bctrl, via
# the vtable's +0 entry). When the result is 1 it calls another method (bctrl, via the vtable's +20 entry). When that result is 1 it reads b->8 (a count) and dispatches to various
# processing routines: it constructs and processes (ProcessAndConstructAndProcess2_82C1FF48), sorts and recurses (ProcessAndSortAndRecurse_82C20878 and
# ProcessAndSortAndRecurse2_82C208E8), compacts and invokes (ProcessAndCompactInvoke_82C1FAE0), invokes elements (ProcessAndInvokeElements_82C15998), sorts elements
# (ProcessAndSortElements_82C1AE88), and performs further processing (sub_82BFF7D8, sub_82C3BF18, sub_82C40BD0). This is the "dispatch and process this data" routine (second form),
# part of the 0x82C20 dispatch cluster. 230 lines. 9 callees. 1 caller (sub_82C20C80).
[entrypoint.functions."0x82C20AF0"]
name = "ProcessAndDispatchAndProcess2_82C20AF0"

# A dispatcher that processes the data. It takes a value (a) and a value (b). It tail-calls ProcessAndDispatchAndProcess2_82C20AF0 with b and a + 4. This is the "dispatch and process
# this data" routine (a simple wrapper), part of the 0x82C20 dispatch cluster. 13 lines. 1 callee (ProcessAndDispatchAndProcess2). 0 callers (unresolved — body is recovered but no
# callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x82C20C80"]
name = "DispatchAndProcess_82C20C80"

# A destructor that destructs and frees an array. It takes a structure (a: fields at a->0, a->4, and a->12). It reads a->12 (a count) and stores a .data descriptor into a->0. When
# the count is non-zero it iterates over the array (a->4's elements, indexed from 0 to count): for each non-zero element it destructs the element (DestructRefCounted_82214F08, with the
# element + 24) and frees it (Free_Thunk_824FE010, with the element). This is the "destruct and free this array" routine, part of the 0x82C21 destructor cluster. 92 lines. 2 callees
# (DestructRefCounted, Free_Thunk). 2 callers.
[entrypoint.functions."0x82C21BA8"]
name = "DestructAndFreeArray_82C21BA8"

# A destructor that destructs and frees an array (second form). It takes a structure (a: fields at a->0, a->4, and a->12). It stores a .data descriptor into a->0, then compacts the
# array (ArrayCompact_82C23DD0, with a). It then reads a->12 (a count); when the count is non-zero it iterates over the array (a->4's elements, indexed from 0 to count): for each
# non-zero element it destructs the element (DestructRefCounted_82214F08, with the element + 24) and frees it (Free_Thunk_824FE010, with the element). This is the "destruct and free
# this array" routine (second form), part of the 0x82C21 destructor cluster. 95 lines. 3 callees (ArrayCompact, DestructRefCounted, Free_Thunk). 3 callers.
[entrypoint.functions."0x82C21D18"]
name = "DestructAndFreeArray2_82C21D18"

# A processor that constructs and processes the data (third form). It takes a structure (a: a vtable at a->0) and a structure (b: a field at b->8). It calls a method on a (bctrl, via
# the vtable's +0 entry). When the result is 1 it calls another method (bctrl, via the vtable's +20 entry). When that result is 1 it reads b->8 (a count) and performs a large amount of
# processing: it allocates memory (Allocate_OrDbgBreak_8221F388), pushes to an array (Array_PushBack_82C21E08), grows (ProcessGrow_82C21C90), makes a vcall (ProcessVcall2_82C22258), and
# makes virtual calls (VirtualCallPair_82C01D60, sub_82BFF7D8, sub_82BFF848, sub_82C02B98). This is the "construct and process this data" routine (third form), part of the 0x82C22
# construct cluster. 503 lines. 8 callees. 1 caller (sub_82C23468).
[entrypoint.functions."0x82C223E8"]
name = "ProcessAndConstructAndProcess3_82C223E8"

# A processor that constructs and processes the data (fourth form). It takes a structure (a: a vtable at a->0) and a structure (b: a field at b->8). It calls a method on a (bctrl, via
# the vtable's +0 entry). When the result is 1 it calls another method (bctrl, via the vtable's +20 entry). When that result is 1 it reads b->8 (a count) and performs a large amount of
# processing: it allocates memory (Allocate_OrDbgBreak_8221F388), pushes to an array (Array_PushBack_82C21E08), constructs a small struct (ConstructSmallStruct_82C21A70), grows
# (ProcessGrow_82C21C90), populates (ProcessPopulate_82C21E90), and makes virtual calls (VirtualCallPair_82C01D60, sub_82BFF7D8, sub_82BFF848, sub_82C02B98). This is the "construct
# and process this data" routine (fourth form), part of the 0x82C22 construct cluster. 469 lines. 9 callees. 1 caller (sub_82C235F8).
[entrypoint.functions."0x82C22770"]
name = "ProcessAndConstructAndProcess4_82C22770"

# A constructor that sorts. It takes a structure (a: fields at a->0, a->4, a->8, and a->12) and a value (b). It initializes a: it stores a .data descriptor into a->0 and zeroes a->4,
# a->8, and a->12. It then sorts (BubbleSort3_82C16C78, with b and a + 4). It then stores a different .data descriptor into a->0. This is the "construct and sort this" routine, part
# of the 0x82C23 construct cluster. 58 lines. 1 callee (BubbleSort3). 1 caller (sub_82C235F8).
[entrypoint.functions."0x82C231E8"]
name = "ConstructAndSort_82C231E8"

# A processor that dispatches and processes the data (third form). It takes a structure (a: a vtable at a->0) and a structure (b: a field at b->8). It calls a method on a (bctrl, via
# the vtable's +0 entry). When the result is 1 it calls another method (bctrl, via the vtable's +20 entry). When that result is 1 it reads b->8 (a count) and dispatches to various
# processing routines: it constructs and processes (ProcessAndConstructAndProcess3_82C223E8), finds in a sorted array (SortedFind_82C1B998 and SortedFind2_82C3ADF0), constructs a sorted
# array (ConstructSortedArray_82C3D028), clears element arrays (ClearElementArray_82C2D228 and CompactClearArray_82C39298), and performs further processing (sub_82BFF7D8, sub_82C3BF18,
# sub_82C40BD0). This is the "dispatch and process this data" routine (third form), part of the 0x82C23 dispatch cluster. 230 lines. 9 callees. 1 caller (sub_82C23788).
[entrypoint.functions."0x82C23468"]
name = "ProcessAndDispatchAndProcess3_82C23468"

# A processor that dispatches and processes the data (fourth form). It takes a structure (a: a vtable at a->0) and a structure (b: a field at b->8). It calls a method on a (bctrl, via
# the vtable's +0 entry). When the result is 1 it calls another method (bctrl, via the vtable's +20 entry). When that result is 1 it reads b->8 (a count) and dispatches to various
# processing routines: it constructs and processes (ProcessAndConstructAndProcess4_82C22770), destructs and frees arrays (DestructAndFreeArray_82C21BA8 and DestructAndFreeArray2_82C21D18),
# constructs and sorts (ConstructAndSort_82C231E8), finds in a sorted array (SortedFind_82C1B998 and SortedFind2_82C3ADF0), and performs further processing (sub_82BFF7D8, sub_82C3BF18,
# sub_82C40BD0). This is the "dispatch and process this data" routine (fourth form), part of the 0x82C23 dispatch cluster. 230 lines. 9 callees. 1 caller (sub_82C23838).
[entrypoint.functions."0x82C235F8"]
name = "ProcessAndDispatchAndProcess4_82C235F8"

# A processor that dispatches and validates the data. It takes a value (a) and a structure (b: a vtable at b->0). It dispatches and processes (ProcessAndDispatchAndProcess3_82C23468,
# with b and a + 4). When the result is non-zero it calls a method on b (bctrl, via the vtable's +20 entry). When that result is zero it sorts and validates (ProcessAndSortAndValidate_82C27B20,
# with a + 4). It performs further processing (sub_82BFF7D8). This is the "dispatch and validate this data" routine, part of the 0x82C23 dispatch cluster. 101 lines. 3 callees
# (ProcessAndDispatchAndProcess3, ProcessAndSortAndValidate, sub_82BFF7D8). 0 callers (unresolved — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x82C23788"]
name = "ProcessAndDispatchAndValidate_82C23788"

# A dispatcher that processes the data (second form). It takes a value (a) and a value (b). It tail-calls ProcessAndDispatchAndProcess4_82C235F8 with b and a + 4. This is the "dispatch
# and process this data" routine (second form, a simple wrapper), part of the 0x82C23 dispatch cluster. 13 lines. 1 callee (ProcessAndDispatchAndProcess4). 0 callers (unresolved — body
# is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x82C23838"]
name = "DispatchAndProcess2_82C23838"

# A destructor that destructs and frees an array (third form). It takes a structure (a: fields at a->0, a->4, and a->12). It reads a->12 (a count) and stores a .data descriptor into a->0.
# When the count is non-zero it iterates over the array (a->4's elements, indexed from 0 to count): for each non-zero element it destructs the element's +8 and +4 fields
# (DestructRefCounted_82214F08, with the element + 8 and the element + 4) and frees the element (Free_Thunk_824FE010). This is the "destruct and free this array" routine (third form),
# part of the 0x82C23 destructor cluster. 97 lines. 2 callees (DestructRefCounted, Free_Thunk). 2 callers.
[entrypoint.functions."0x82C23E28"]
name = "DestructAndFreeArray3_82C23E28"

# A destructor that destructs and frees an array (fourth form). It takes a structure (a: fields at a->0, a->4, and a->12). It stores a .data descriptor into a->0, then compacts the
# array (ArrayCompact_82C23DD0, with a). It then reads a->12 (a count); when the count is non-zero it iterates over the array (a->4's elements, indexed from 0 to count): for each
# non-zero element it destructs the element's +8 and +4 fields (DestructRefCounted_82214F08, with the element + 8 and the element + 4) and frees the element (Free_Thunk_824FE010). This
# is the "destruct and free this array" routine (fourth form), part of the 0x82C23 destructor cluster. 100 lines. 3 callees (ArrayCompact, DestructRefCounted, Free_Thunk). 3 callers.
[entrypoint.functions."0x82C23F18"]
name = "DestructAndFreeArray4_82C23F18"

# A processor that constructs and processes the data (fifth form). It takes a structure (a: a vtable at a->0) and a structure (b: a field at b->8). It calls a method on a (bctrl, via
# the vtable's +0 entry). When the result is 1 it calls another method (bctrl, via the vtable's +20 entry). When that result is 1 it reads b->8 (a count) and performs a large amount of
# processing: it allocates memory (Allocate_OrDbgBreak_8221F388), pushes to an array (Array_PushBack_82C21E08), grows (ProcessGrow_82C21C90), and makes virtual calls
# (VirtualCallPair_82C01D60, sub_82BFF7D8, sub_82BFF848, sub_82C02B98, sub_82C24250). This is the "construct and process this data" routine (fifth form), part of the 0x82C24 construct
# cluster. 443 lines. 8 callees. 1 caller (sub_82C251D0).
[entrypoint.functions."0x82C24398"]
name = "ProcessAndConstructAndProcess5_82C24398"

# A processor that constructs and processes the data (sixth form). It takes a structure (a: a vtable at a->0) and a structure (b: a field at b->8). It calls a method on a (bctrl, via
# the vtable's +0 entry). When the result is 1 it calls another method (bctrl, via the vtable's +20 entry). When that result is 1 it reads b->8 (a count) and performs a large amount of
# processing: it allocates memory (Allocate_OrDbgBreak_8221F388), pushes to an array (Array_PushBack_82C21E08), atomically increments (AtomicIncrement3_821F7798), grows
# (ProcessGrow_82C21C90), and makes virtual calls (VirtualCallPair_82C01D60, sub_82BFF7D8, sub_82BFF848, sub_82C02B98, sub_82C24010). This is the "construct and process this data"
# routine (sixth form), part of the 0x82C24 construct cluster. 483 lines. 9 callees. 1 caller (sub_82C25360).
[entrypoint.functions."0x82C246B0"]
name = "ProcessAndConstructAndProcess6_82C246B0"

# A constructor that sorts (second form). It takes a structure (a: fields at a->0, a->4, a->8, and a->12) and a value (b). It initializes a: it stores a .data descriptor into a->0 and
# zeroes a->4, a->8, and a->12. It then sorts (BubbleSort3_82C16C78, with b and a + 4). It then stores a different .data descriptor into a->0. This is the "construct and sort this"
# routine (second form), part of the 0x82C24 construct cluster. 58 lines. 1 callee (BubbleSort3). 1 caller (sub_82C25360).
[entrypoint.functions."0x82C24AB0"]
name = "ConstructAndSort2_82C24AB0"

# A processor that dispatches and processes the data (fifth form). It takes a structure (a: a vtable at a->0) and a structure (b: a field at b->8). It calls a method on a (bctrl, via
# the vtable's +0 entry). When the result is 1 it calls another method (bctrl, via the vtable's +20 entry). When that result is 1 it reads b->8 (a count) and dispatches to various
# processing routines: it constructs and processes (ProcessAndConstructAndProcess5_82C24398), finds in a sorted array (SortedFind_82C1B998 and SortedFind2_82C3ADF0), constructs a sorted
# array (ConstructSortedArray_82C3D028), clears element arrays (ClearElementArray_82C2D228 and CompactClearArray_82C39298), and performs further processing (sub_82BFF7D8, sub_82C3BF18,
# sub_82C40BD0). This is the "dispatch and process this data" routine (fifth form), part of the 0x82C25 dispatch cluster. 230 lines. 9 callees. 1 caller (sub_82C254F0).
[entrypoint.functions."0x82C251D0"]
name = "ProcessAndDispatchAndProcess5_82C251D0"

# A processor that dispatches and processes the data (sixth form). It takes a structure (a: a vtable at a->0) and a structure (b: a field at b->8). It calls a method on a (bctrl, via
# the vtable's +0 entry). When the result is 1 it calls another method (bctrl, via the vtable's +20 entry). When that result is 1 it reads b->8 (a count) and dispatches to various
# processing routines: it constructs and processes (ProcessAndConstructAndProcess6_82C246B0), destructs and frees arrays (DestructAndFreeArray3_82C23E28 and DestructAndFreeArray4_82C23F18),
# constructs and sorts (ConstructAndSort2_82C24AB0), finds in a sorted array (SortedFind_82C1B998 and SortedFind2_82C3ADF0), and performs further processing (sub_82BFF7D8, sub_82C3BF18,
# sub_82C40BD0). This is the "dispatch and process this data" routine (sixth form), part of the 0x82C25 dispatch cluster. 230 lines. 9 callees. 1 caller (sub_82C25500).
[entrypoint.functions."0x82C25360"]
name = "ProcessAndDispatchAndProcess6_82C25360"

# A dispatcher that processes the data (third form). It takes a value (a) and a value (b). It tail-calls ProcessAndDispatchAndProcess5_82C251D0 with b and a + 4. This is the "dispatch
# and process this data" routine (third form, a simple wrapper), part of the 0x82C25 dispatch cluster. 13 lines. 1 callee (ProcessAndDispatchAndProcess5). 0 callers (unresolved — body
# is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x82C254F0"]
name = "DispatchAndProcess3_82C254F0"

# A dispatcher that processes the data (fourth form). It takes a value (a) and a value (b). It tail-calls ProcessAndDispatchAndProcess6_82C25360 with b and a + 4. This is the "dispatch
# and process this data" routine (fourth form, a simple wrapper), part of the 0x82C25 dispatch cluster. 13 lines. 1 callee (ProcessAndDispatchAndProcess6). 0 callers (unresolved — body
# is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x82C25500"]
name = "DispatchAndProcess4_82C25500"

# A processor that constructs and processes the data (seventh form). It takes a structure (a: a vtable at a->0) and a structure (b: a field at b->8). It calls a method on a (bctrl, via
# the vtable's +0 entry). When the result is 1 it calls another method (bctrl, via the vtable's +20 entry). When that result is 1 it reads b->8 (a count) and performs a large amount of
# processing: it allocates memory (Allocate_OrDbgBreak_8221F388), pushes to an array (Array_PushBack_82C21E08), grows (ProcessGrow_82C21C90), and makes virtual calls
# (VirtualCallPair_82C01D60, DispatchVcallSequence6_82C26AF0, sub_82BFF7D8, sub_82BFF848, sub_82C02B98). This is the "construct and process this data" routine (seventh form), part of the
# 0x82C26 construct cluster. 443 lines. 8 callees. 1 caller (sub_82C27BB0).
[entrypoint.functions."0x82C26CA0"]
name = "ProcessAndConstructAndProcess7_82C26CA0"

# A processor that constructs and processes the data (eighth form). It takes a structure (a: a vtable at a->0) and a structure (b: a field at b->8). It calls a method on a (bctrl, via
# the vtable's +0 entry). When the result is 1 it calls another method (bctrl, via the vtable's +20 entry). When that result is 1 it reads b->8 (a count) and performs a large amount of
# processing: it allocates memory (Allocate_OrDbgBreak_8221F388), pushes to an array (Array_PushBack_82C21E08), atomically increments (AtomicIncrement3_821F7798), grows
# (ProcessGrow_82C21C90), and makes virtual calls (VirtualCallPair_82C01D60, ProcessVcallSequence5_82C26940, sub_82BFF7D8, sub_82BFF848, sub_82C02B98). This is the "construct and
# process this data" routine (eighth form), part of the 0x82C26 construct cluster. 473 lines. 9 callees. 1 caller (sub_82C27D40).
[entrypoint.functions."0x82C26FB8"]
name = "ProcessAndConstructAndProcess8_82C26FB8"

# A processor that dispatches and processes the data (seventh form). It takes a structure (a: a vtable at a->0) and a structure (b: a field at b->8). It calls a method on a (bctrl, via
# the vtable's +0 entry). When the result is 1 it calls another method (bctrl, via the vtable's +20 entry). When that result is 1 it reads b->8 (a count) and dispatches to various
# processing routines: it constructs and processes (ProcessAndConstructAndProcess7_82C26CA0), finds in a sorted array (SortedFind_82C1B998 and SortedFind2_82C3ADF0), constructs a sorted
# array (ConstructSortedArray_82C3D028), clears element arrays (ClearElementArray_82C2D228 and CompactClearArray_82C39298), and performs further processing (sub_82BFF7D8, sub_82C3BF18,
# sub_82C40BD0). This is the "dispatch and process this data" routine (seventh form), part of the 0x82C27 dispatch cluster. 230 lines. 9 callees. 1 caller (sub_82C27ED0).
[entrypoint.functions."0x82C27BB0"]
name = "ProcessAndDispatchAndProcess7_82C27BB0"

# A processor that dispatches and processes the data (eighth form). It takes a structure (a: a vtable at a->0) and a structure (b: a field at b->8). It calls a method on a (bctrl, via
# the vtable's +0 entry). When the result is 1 it calls another method (bctrl, via the vtable's +20 entry). When that result is 1 it reads b->8 (a count) and dispatches to various
# processing routines: it constructs and processes (ProcessAndConstructAndProcess8_82C26FB8), finds in a sorted array (SortedFind_82C1B998 and SortedFind2_82C3ADF0), constructs and
# bubble-sorts (ConstructAndBubbleSort_82C31B48), destructs arrays (CompactAndDestructArray_82C2FDC0 and DestructElementArray2_82C2AAF8), and performs further processing
# (sub_82BFF7D8, sub_82C3BF18, sub_82C40BD0). This is the "dispatch and process this data" routine (eighth form), part of the 0x82C27 dispatch cluster. 230 lines. 9 callees. 1 caller
# (sub_82C27F80).
[entrypoint.functions."0x82C27D40"]
name = "ProcessAndDispatchAndProcess8_82C27D40"

# A processor that dispatches and validates the data (second form). It takes a value (a) and a structure (b: a vtable at b->0). It dispatches and processes (ProcessAndDispatchAndProcess7_82C27BB0,
# with b and a + 4). When the result is non-zero it calls a method on b (bctrl, via the vtable's +20 entry). When that result is zero it sorts and validates (ProcessAndSortAndValidate_82C27B20,
# with a + 4). It performs further processing (sub_82BFF7D8). This is the "dispatch and validate this data" routine (second form), part of the 0x82C27 dispatch cluster. 101 lines. 3 callees
# (ProcessAndDispatchAndProcess7, ProcessAndSortAndValidate, sub_82BFF7D8). 0 callers (unresolved — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x82C27ED0"]
name = "ProcessAndDispatchAndValidate2_82C27ED0"

# A dispatcher that processes the data (fifth form). It takes a value (a) and a value (b). It tail-calls ProcessAndDispatchAndProcess8_82C27D40 with b and a + 4. This is the "dispatch
# and process this data" routine (fifth form, a simple wrapper), part of the 0x82C27 dispatch cluster. 13 lines. 1 callee (ProcessAndDispatchAndProcess8). 0 callers (unresolved — body
# is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x82C27F80"]
name = "DispatchAndProcess5_82C27F80"

# A processor that constructs and processes the data (ninth form). It takes a structure (a: a vtable at a->0) and a structure (b: a field at b->8). It calls a method on a (bctrl, via
# the vtable's +0 entry). When the result is 1 it calls another method (bctrl, via the vtable's +20 entry). When that result is 1 it reads b->8 (a count) and performs a large amount of
# processing: it allocates memory (Allocate_OrDbgBreak_8221F388), pushes to an array (Array_PushBack_82C21E08), grows (ProcessGrow_82C21C90), and makes virtual calls
# (VirtualCallPair_82C01D60, sub_82BFF7D8, sub_82BFF848, sub_82C02B98, sub_82C282E8). This is the "construct and process this data" routine (ninth form), part of the 0x82C28 construct
# cluster. 443 lines. 8 callees. 1 caller (sub_82C290D0).
[entrypoint.functions."0x82C285E8"]
name = "ProcessAndConstructAndProcess9_82C285E8"

# A processor that dispatches and processes the data (ninth form). It takes a structure (a: a vtable at a->0) and a structure (b: a field at b->8). It calls a method on a (bctrl, via
# the vtable's +0 entry). When the result is 1 it calls another method (bctrl, via the vtable's +20 entry). When that result is 1 it reads b->8 (a count) and dispatches to various
# processing routines: it constructs and processes (ProcessAndConstructAndProcess9_82C285E8), sorts elements (ProcessAndSortElements_82C28E80 and ProcessAndSortElements2_82C28EF0),
# constructs a sorted array (ConstructSortedArray_82C3D028), clears element arrays (ClearElementArray_82C2D228 and CompactClearArray_82C39298), and performs further processing
# (sub_82BFF7D8, sub_82C3BF18, sub_82C40BD0). This is the "dispatch and process this data" routine (ninth form), part of the 0x82C29 dispatch cluster. 230 lines. 9 callees. 1 caller
# (sub_82C29260).
[entrypoint.functions."0x82C290D0"]
name = "ProcessAndDispatchAndProcess9_82C290D0"

# A dispatcher that processes the data (sixth form). It takes a value (a) and a value (b). It tail-calls ProcessAndDispatchAndProcess9_82C290D0 with b and a + 4. This is the "dispatch
# and process this data" routine (sixth form, a simple wrapper), part of the 0x82C29 dispatch cluster. 13 lines. 1 callee (ProcessAndDispatchAndProcess9). 0 callers (unresolved — body
# is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x82C29260"]
name = "DispatchAndProcess6_82C29260"

# A checker that checks if an array is sorted. It takes a structure (a: an array at a->0 and a count at a->8). It reads a->8 (a count). When the count is greater than 1 it iterates
# over adjacent pairs (from index 1 to count): for each pair it compares their +4 fields (primary key) and, when equal, their +12 fields (secondary key). It returns 1 when all
# adjacent pairs are in non-decreasing order (the array is sorted), and 0 otherwise. This is the "check if this array is sorted" routine, part of the 0x82C2A check cluster. 58 lines.
# 0 callees (leaf function). 1 caller (sub_82C2C958).
[entrypoint.functions."0x82C2AD48"]
name = "CheckIfSorted_82C2AD48"

# A processor that constructs and processes the data (tenth form). It takes a structure (a: a vtable at a->0) and a structure (b: a field at b->8). It calls a method on a (bctrl, via
# the vtable's +0 entry). When the result is 1 it calls another method (bctrl, via the vtable's +20 entry). When that result is 1 it reads b->8 (a count) and performs a large amount of
# processing: it allocates memory (Allocate_OrDbgBreak_8221F388), pushes to an array (Array_PushBack_82C21E08), grows (ProcessGrow_82C21C90), and makes virtual calls
# (VirtualCallPair_82C01D60, ProcessAndDispatchSubVirtual_82C2AEC0, sub_82BFF7D8, sub_82BFF848, sub_82C02B98). This is the "construct and process this data" routine (tenth form), part
# of the 0x82C2B construct cluster. 443 lines. 8 callees. 1 caller (sub_82C2C9E8).
[entrypoint.functions."0x82C2B118"]
name = "ProcessAndConstructAndProcess10_82C2B118"

# A processor that constructs and processes the data (eleventh form). It takes a structure (a: a vtable at a->0) and a structure (b: a field at b->8). It calls a method on a (bctrl, via
# the vtable's +0 entry). When the result is 1 it calls another method (bctrl, via the vtable's +20 entry). When that result is 1 it reads b->8 (a count) and performs a large amount of
# processing: it allocates memory (Allocate_OrDbgBreak_8221F388), pushes to an array (Array_PushBack_82C21E08), atomically increments (AtomicIncrement3_821F7798), grows
# (ProcessGrow_82C21C90), and makes virtual calls (VirtualCallPair_82C01D60, ProcessAndDispatchVcallGroup_82C2ABE0, sub_82BFF7D8, sub_82BFF848, sub_82C02B98). This is the "construct and
# process this data" routine (eleventh form), part of the 0x82C2B construct cluster. 473 lines. 9 callees. 1 caller (sub_82C2CB78).
[entrypoint.functions."0x82C2B430"]
name = "ProcessAndConstructAndProcess11_82C2B430"

# A processor that sorts and checks the data. It takes a structure (a: an array at a->0 and a count at a->8). It reads a->8 (a count). When the count is non-zero it sorts the array
# (sub_82C2C520, with a->0 and the array's end, plus a count and a flag). It then checks whether the array is sorted (CheckIfSorted_82C2AD48, with a). It performs a binary search
# comparison (BinarySearch_Cmp_82C39988). This is the "sort this array and check the result" routine, part of the 0x82C2C process cluster. 86 lines. 3 callees (sub_82C2C520,
# CheckIfSorted, BinarySearch_Cmp). 1 caller (sub_82C2CD08).
[entrypoint.functions."0x82C2C958"]
name = "ProcessAndSortAndCheck_82C2C958"

# A processor that dispatches and processes the data (tenth form). It takes a structure (a: a vtable at a->0) and a structure (b: a field at b->8). It calls a method on a (bctrl, via
# the vtable's +0 entry). When the result is 1 it calls another method (bctrl, via the vtable's +20 entry). When that result is 1 it reads b->8 (a count) and dispatches to various
# processing routines: it constructs and processes (ProcessAndConstructAndProcess10_82C2B118), finds in a sorted array (SortedFind_82C1B998 and SortedFind2_82C3ADF0), constructs a sorted
# array (ConstructSortedArray_82C3D028), clears element arrays (ClearElementArray_82C2D228 and CompactClearArray_82C39298), and performs further processing (sub_82BFF7D8, sub_82C3BF18,
# sub_82C40BD0). This is the "dispatch and process this data" routine (tenth form), part of the 0x82C2C dispatch cluster. 230 lines. 9 callees. 1 caller (sub_82C2CD08).
[entrypoint.functions."0x82C2C9E8"]
name = "ProcessAndDispatchAndProcess10_82C2C9E8"

# A processor that dispatches and processes the data (eleventh form). It takes a structure (a: a vtable at a->0) and a structure (b: a field at b->8). It calls a method on a (bctrl, via
# the vtable's +0 entry). When the result is 1 it calls another method (bctrl, via the vtable's +20 entry). When that result is 1 it reads b->8 (a count) and dispatches to various
# processing routines: it constructs and processes (ProcessAndConstructAndProcess11_82C2B430), finds in a sorted array (SortedFind_82C1B998 and SortedFind2_82C3ADF0), constructs and
# bubble-sorts (ConstructAndBubbleSort_82C31B48), destructs arrays (CompactAndDestructArray_82C2FDC0 and DestructElementArray2_82C2AAF8), and performs further processing
# (sub_82BFF7D8, sub_82C3BF18, sub_82C40BD0). This is the "dispatch and process this data" routine (eleventh form), part of the 0x82C2C dispatch cluster. 230 lines. 9 callees. 1 caller
# (sub_82C2CDB8).
[entrypoint.functions."0x82C2CB78"]
name = "ProcessAndDispatchAndProcess11_82C2CB78"

# A processor that dispatches and validates the data (third form). It takes a value (a) and a structure (b: a vtable at b->0). It dispatches and processes (ProcessAndDispatchAndProcess10_82C2C9E8,
# with b and a + 4). When the result is non-zero it calls a method on b (bctrl, via the vtable's +20 entry). When that result is zero it sorts and checks (ProcessAndSortAndCheck_82C2C958,
# with a + 4). It performs further processing (sub_82BFF7D8). This is the "dispatch and validate this data" routine (third form), part of the 0x82C2C dispatch cluster. 101 lines. 3 callees
# (ProcessAndDispatchAndProcess10, ProcessAndSortAndCheck, sub_82BFF7D8). 0 callers (unresolved — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x82C2CD08"]
name = "ProcessAndDispatchAndValidate3_82C2CD08"

# A dispatcher that processes the data (seventh form). It takes a value (a) and a value (b). It tail-calls ProcessAndDispatchAndProcess11_82C2CB78 with b and a + 4. This is the
# "dispatch and process this data" routine (seventh form, a simple wrapper), part of the 0x82C2C dispatch cluster. 13 lines. 1 callee (ProcessAndDispatchAndProcess11). 0 callers
# (unresolved — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x82C2CDB8"]
name = "DispatchAndProcess7_82C2CDB8"

# A processor that constructs and processes the data (twelfth form). It takes a structure (a: a vtable at a->0) and a structure (b: a field at b->8). It calls a method on a (bctrl, via
# the vtable's +0 entry). When the result is 1 it calls another method (bctrl, via the vtable's +20 entry). When that result is 1 it reads b->8 (a count) and performs a large amount of
# processing: it allocates memory (Allocate_OrDbgBreak_8221F388), pushes to an array (Array_PushBack_82C21E08), atomically increments (AtomicIncrement3_821F7798), grows
# (ProcessGrow_82C21C90), and makes virtual calls (VirtualCallPair_82C01D60, sub_82BFF7D8, sub_82BFF848, sub_82C02B98, sub_82C2D0C0). This is the "construct and process this data"
# routine (twelfth form), part of the 0x82C2D construct cluster. 473 lines. 9 callees. 1 caller (sub_82C2EB88).
[entrypoint.functions."0x82C2D9E8"]
name = "ProcessAndConstructAndProcess12_82C2D9E8"

# A processor that dispatches and processes the data (twelfth form). It takes a structure (a: a vtable at a->0) and a structure (b: a field at b->8). It calls a method on a (bctrl, via
# the vtable's +0 entry). When the result is 1 it calls another method (bctrl, via the vtable's +20 entry). When that result is 1 it reads b->8 (a count) and dispatches to various
# processing routines: it constructs and processes (ProcessAndConstructAndProcess12_82C2D9E8), finds in a sorted array (SortedFind_82C1B998 and SortedFind2_82C3ADF0), constructs and
# bubble-sorts (ConstructAndBubbleSort_82C31B48), destructs arrays (CompactAndDestructArray_82C2FDC0 and DestructElementArray2_82C2AAF8), and performs further processing
# (sub_82BFF7D8, sub_82C3BF18, sub_82C40BD0). This is the "dispatch and process this data" routine (twelfth form), part of the 0x82C2E dispatch cluster. 230 lines. 9 callees. 1 caller
# (sub_82C2EDC8).
[entrypoint.functions."0x82C2EB88"]
name = "ProcessAndDispatchAndProcess12_82C2EB88"

# A dispatcher that processes the data (eighth form). It takes a value (a) and a value (b). It tail-calls ProcessAndDispatchAndProcess12_82C2EB88 with b and a + 4. This is the
# "dispatch and process this data" routine (eighth form, a simple wrapper), part of the 0x82C2E dispatch cluster. 13 lines. 1 callee (ProcessAndDispatchAndProcess12). 0 callers
# (unresolved — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x82C2EDC8"]
name = "DispatchAndProcess8_82C2EDC8"

# A processor that constructs and processes the data (thirteenth form). It takes a structure (a: a vtable at a->0) and a structure (b: a field at b->8). It calls a method on a (bctrl, via
# the vtable's +0 entry). When the result is 1 it calls another method (bctrl, via the vtable's +20 entry). When that result is 1 it reads b->8 (a count) and performs a large amount of
# processing: it allocates memory (Allocate_OrDbgBreak_8221F388), pushes to an array (Array_PushBack_82C21E08), atomically increments (AtomicIncrement3_821F7798), grows
# (ProcessGrow_82C21C90), and makes virtual calls (VirtualCallPair_82C01D60, sub_82BFF7D8, sub_82BFF848, sub_82C02B98, sub_82C2FF28). This is the "construct and process this data"
# routine (thirteenth form), part of the 0x82C30 construct cluster. 473 lines. 9 callees. 1 caller (sub_82C30490).
[entrypoint.functions."0x82C30010"]
name = "ProcessAndConstructAndProcess13_82C30010"

# A constructor that constructs a 16-byte object (second form). It allocates 16 bytes (Allocate_OrDbgBreak_8221F388, with 16). When the allocation succeeds it stores a .data descriptor
# into the first 4 bytes (offset 0) and zeroes the next three 4-byte fields (offsets 4, 8, and 12). When the allocation fails it returns 0. This is the "construct a 16-byte object"
# routine (second form), part of the 0x82C30 construct cluster. 55 lines. 1 callee (Allocate_OrDbgBreak). 0 callers (unresolved — body is recovered but no callers found; likely a vtable
# method or indirect entry).
[entrypoint.functions."0x82C30358"]
name = "Construct16ByteObject2_82C30358"

# A processor that dispatches and processes the data (thirteenth form). It takes a structure (a: a vtable at a->0) and a structure (b: a field at b->8). It calls a method on a (bctrl, via
# the vtable's +0 entry). When the result is 1 it calls another method (bctrl, via the vtable's +20 entry). When that result is 1 it reads b->8 (a count) and dispatches to various
# processing routines: it constructs and processes (ProcessAndConstructAndProcess13_82C30010), finds in a sorted array (SortedFind_82C1B998 and SortedFind2_82C3ADF0), constructs and
# bubble-sorts (ConstructAndBubbleSort_82C31B48), destructs arrays (CompactAndDestructArray_82C2FDC0 and DestructElementArray2_82C2AAF8), and performs further processing
# (sub_82BFF7D8, sub_82C3BF18, sub_82C40BD0). This is the "dispatch and process this data" routine (thirteenth form), part of the 0x82C30 dispatch cluster. 230 lines. 9 callees. 1 caller
# (sub_82C30620).
[entrypoint.functions."0x82C30490"]
name = "ProcessAndDispatchAndProcess13_82C30490"

# A dispatcher that processes the data (ninth form). It takes a value (a) and a value (b). It tail-calls ProcessAndDispatchAndProcess13_82C30490 with b and a + 4. This is the
# "dispatch and process this data" routine (ninth form, a simple wrapper), part of the 0x82C30 dispatch cluster. 13 lines. 1 callee (ProcessAndDispatchAndProcess13). 0 callers
# (unresolved — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x82C30620"]
name = "DispatchAndProcess9_82C30620"

# A getter that returns a .data pointer. It returns the address of a .data structure (0xE8000000 + 19900, i.e. 0xE8004E00). This is a simple getter that returns a pointer to a global
# .data object, part of the 0x82C34 getter cluster. 10 lines. 0 callees (leaf function). 0 callers (unresolved — body is recovered but no callers found; likely a vtable method or indirect
# entry).
[entrypoint.functions."0x82C34F28"]
name = "GetDataPointer_82C34F28"

# A processor that constructs and processes the data (fourteenth form). It takes a structure (a: a vtable at a->0) and a structure (b: a field at b->8). It calls a method on a (bctrl, via
# the vtable's +0 entry). When the result is 1 it calls another method (bctrl, via the vtable's +20 entry). When that result is 1 it reads b->8 (a count) and performs a large amount of
# processing: it allocates memory (Allocate_OrDbgBreak_8221F388), pushes to an array (Array_PushBack_82C21E08), grows (ProcessGrow_82C21C90), and makes virtual calls
# (VirtualCallPair_82C01D60, sub_82BFF7D8, sub_82BFF848, sub_82C02B98, sub_82C3BE20, sub_82C3BF68). This is the "construct and process this data" routine (fourteenth form), part of the
# 0x82C3C construct cluster. 469 lines. 9 callees. 1 caller (sub_82C3F760).
[entrypoint.functions."0x82C3C700"]
name = "ProcessAndConstructAndProcess14_82C3C700"

# A processor that constructs and processes the data (fifteenth form). It takes a structure (a: a vtable at a->0) and a structure (b: a field at b->8). It calls a method on a (bctrl, via
# the vtable's +0 entry). When the result is 1 it calls another method (bctrl, via the vtable's +20 entry). When that result is 1 it reads b->8 (a count) and performs a large amount of
# processing: it allocates memory (Allocate_OrDbgBreak_8221F388), pushes to an array (Array_PushBack_82C21E08), grows (ProcessGrow_82C21C90), and makes virtual calls
# (VirtualCallPair_82C01D60, sub_82BFF7D8, sub_82BFF848, sub_82C02B98, sub_82C3C200). This is the "construct and process this data" routine (fifteenth form), part of the 0x82C3C
# construct cluster. 443 lines. 8 callees. 1 caller (sub_82C3F8F0).
[entrypoint.functions."0x82C3CA40"]
name = "ProcessAndConstructAndProcess15_82C3CA40"

# A processor that dispatches and processes the data (fourteenth form). It takes a structure (a: a vtable at a->0) and a structure (b: a field at b->8). It calls a method on a (bctrl,
# via the vtable's +0 entry). When the result is 1 it calls another method (bctrl, via the vtable's +20 entry). When that result is 1 it reads b->8 (a count) and dispatches to various
# processing routines: it constructs and processes (ProcessAndConstructAndProcess14_82C3C700), finds in a sorted array (SortedFind_82C1B998 and SortedFind2_82C3ADF0), constructs and
# bubble-sorts (ConstructAndBubbleSort_82C31B48), destructs arrays (CompactAndDestructArray_82C2FDC0 and DestructElementArray2_82C2AAF8), and performs further processing
# (sub_82BFF7D8, sub_82C3BF18, sub_82C40BD0). This is the "dispatch and process this data" routine (fourteenth form), part of the 0x82C3F dispatch cluster. 230 lines. 9 callees. 1
# caller (sub_82C3FA80).
[entrypoint.functions."0x82C3F760"]
name = "ProcessAndDispatchAndProcess14_82C3F760"

# A processor that dispatches and processes the data (fifteenth form). It takes a structure (a: a vtable at a->0) and a structure (b: a field at b->8). It calls a method on a (bctrl,
# via the vtable's +0 entry). When the result is 1 it calls another method (bctrl, via the vtable's +20 entry). When that result is 1 it reads b->8 (a count) and dispatches to various
# processing routines: it constructs and processes (ProcessAndConstructAndProcess15_82C3CA40), finds in a sorted array (SortedFind_82C1B998 and SortedFind2_82C3ADF0), constructs a sorted
# array (ConstructSortedArray_82C3D028), clears element arrays (ClearElementArray_82C2D228 and CompactClearArray_82C39298), and performs further processing (sub_82BFF7D8, sub_82C3BF18,
# sub_82C40BD0). This is the "dispatch and process this data" routine (fifteenth form), part of the 0x82C3F dispatch cluster. 230 lines. 9 callees. 1 caller (sub_82C3FA90).
[entrypoint.functions."0x82C3F8F0"]
name = "ProcessAndDispatchAndProcess15_82C3F8F0"

# A dispatcher that processes the data (tenth form). It takes a value (a) and a value (b). It tail-calls ProcessAndDispatchAndProcess14_82C3F760 with b and a + 4. This is the
# "dispatch and process this data" routine (tenth form, a simple wrapper), part of the 0x82C3F dispatch cluster. 13 lines. 1 callee (ProcessAndDispatchAndProcess14). 0 callers
# (unresolved — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x82C3FA80"]
name = "DispatchAndProcess10_82C3FA80"

# A dispatcher that processes the data (eleventh form). It takes a value (a) and a value (b). It tail-calls ProcessAndDispatchAndProcess15_82C3F8F0 with b and a + 4. This is the
# "dispatch and process this data" routine (eleventh form, a simple wrapper), part of the 0x82C3F dispatch cluster. 13 lines. 1 callee (ProcessAndDispatchAndProcess15). 0 callers
# (unresolved — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x82C3FA90"]
name = "DispatchAndProcess11_82C3FA90"

# A processor that constructs and validates the data. It takes a structure (a: fields at a->4, a->8, and a->12) and a value (b). It zeroes a->4, a->8, and a->12. When b is non-zero it
# checks the value: when b is less than or equal to a large constant (0x07207217) it validates and constructs (ValidateAndConstruct_82377F60, with b and 0); when b exceeds that constant it
# handles the error (sub_82684B38, with b). This is the "construct and validate this data" routine, part of the 0x82376 process cluster. 89 lines. 2 callees (ValidateAndConstruct,
# sub_82684B38). 1 caller (sub_823769E8).
[entrypoint.functions."0x82376200"]
name = "ProcessAndConstructAndValidate_82376200"

# A processor that loads and processes the data. It takes a value (a) and a structure (b: fields at b->4 and b->8). It reads b->4 (a pointer). When the pointer is non-zero it computes a
# value (b->8 minus b->4, divided by 36); when it is zero it uses 0. It then constructs and validates (ProcessAndConstructAndValidate_82376200, with a and the computed value). It loads
# from a source (LoadFrom_Source_822F2398). This is the "load and process this data" routine, part of the 0x82376 process cluster. 100 lines. 2 callees (ProcessAndConstructAndValidate,
# LoadFrom_Source). 2 callers (sub_82377340 and SearchTreeRange_82375AB8).
[entrypoint.functions."0x823769E8"]
name = "ProcessAndLoadAndProcess_823769E8"

# A processor that calls methods on a list of structures (sixth form). It takes a value (a), a value (b), a value (c), and a value (d). When b is not equal to c it iterates over an array of
# pointers (from d to c): for each non-zero pointer it stores the pointer and calls a method on the structure it points to (bctrl, via the structure's +4 entry). This is the "call methods on
# this list of structures" routine (sixth form), part of the 0x825F5 method cluster. 63 lines. 0 callees (leaf function). 3 callers (sub_822F7870, sub_825F4EA0, and sub_822F7B70).
[entrypoint.functions."0x825F51D0"]
name = "ProcessAndCallMethods6_825F51D0"

# A processor that processes and stores a byte. It takes a value (a, a destination), a value (b, a source), and a value (c). When b is zero or c is zero it returns 0. Otherwise it reads the
# first byte of b. When the byte is non-zero it (optionally, when a is non-zero) stores the byte (masked to 8 bits) into a's first 2 bytes and returns 1; when the byte is zero it (optionally,
# when a is non-zero) stores 0 into a's first 2 bytes and returns 0. This is the "process and store this byte" routine, part of the 0x82CB8 process cluster. 43 lines. 0 callees (leaf function).
# 12 callers (sub_82C7A5A0, sub_82C75198, sub_82CB1438, sub_82C75A50, sub_830043D0, sub_82C7AE40, sub_82C76100, sub_82CACCC0, sub_82CB2038, sub_82C743B8, sub_82CB3260, and sub_82C747D8).
[entrypoint.functions."0x82CB81C8"]
name = "ProcessAndStoreByte_82CB81C8"

# A processor that queries and processes the data. It takes a structure (a: fields at a->0 and a->4), a value (b), and a count (c). When c is non-zero it iterates: when a->0 is zero it
# queries an index (ProcessAndQueryIndex_82BCD8A8, with a), and when the result is not -1 it updates a->0 (increment) and a->4 (decrement). It then reads a byte from a->4 and performs
# processing (MemCpy_SdkRuntime_82CA2C60). This is the "query and process this data" routine, part of the 0x82179 process cluster. 112 lines. 2 callees (ProcessAndQueryIndex, MemCpy).
# 7 callers (sub_82BF58B8, sub_82BF5470, sub_82BF4E00, sub_82BF51D8, sub_82BF59A0, sub_82A36390, and sub_82A36910).
[entrypoint.functions."0x82179420"]
name = "ProcessAndQueryAndProcess_82179420"

# A processor that searches and processes the data. It takes a value (a), a structure (b: a field at b->4), and a value (c). It reads b->4 (a pointer), then reads the pointer's +4 field (another
# pointer). When the second pointer's +21 byte is zero it reads c's first field and iterates: it reads the current pointer's +12 field, computes the difference (c's field minus the +12 field),
# and checks the sign. When the sign is negative it reads the current pointer's +0 field (the left child) and continues; when it is positive it reads the +8 field (the right child) and continues.
# It then updates the node (ProcessAndUpdateNode_82344608) and processes the chain (ProcessChain2_8250AF60). This is the "search and process this data" routine, part of the 0x82344 process
# cluster. 183 lines. 2 callees (ProcessAndUpdateNode, ProcessChain2). 3 callers (sub_8281AFB0, sub_8235EF70, and ProcessAndSearchAndInsert_82343D18).
[entrypoint.functions."0x82344470"]
name = "ProcessAndSearchAndProcess_82344470"

# A processor that queries and processes the data (second form). It takes a structure (a: fields at a->0, a->4, and a->12). It reads a->4 and queries and processes it
# (ProcessAndQueryAndProcess_82179420, with a->4). When the result is non-zero it performs further processing (sub_82A2ED28, with a->0, two .data pointers, a->12, and a fourth .data pointer)
# and then processes (sub_821E51D0, with a->0 and 3). This is the "query and process this data" routine (second form), part of the 0x82A36 process cluster. 59 lines. 3 callees
# (ProcessAndQueryAndProcess, sub_82A2ED28, and sub_821E51D0). 3 callers (sub_82A36F40, sub_82A365D0, and sub_82A363F0).
[entrypoint.functions."0x82A36390"]
name = "ProcessAndQueryAndProcess2_82A36390"

# A processor that inserts a hash entry. It takes a structure (a: a field at a->8) and a value (b). When b is zero it zeroes the +4 field of a->8's target and advances a->8 by 8. When b is
# non-zero it finds the first zero byte in b (a NUL-terminated string) and inserts a hash entry (InsertHashEntry_8219CDA8, with a and the string's length minus 1). It dispatches the value
# (DispatchValue_821CF230) and removes the first element (ProcessAndRemoveFirstElement_82BC5630). This is the "insert this hash entry" routine, part of the 0x82BC6 process cluster. 132 lines.
# 3 callees (InsertHashEntry, DispatchValue, and ProcessAndRemoveFirstElement). 3 callers (sub_82BD5380, ProcessAndCheckAndProcess_82BD5440, and sub_82BD6D70).
[entrypoint.functions."0x82BC6F70"]
name = "ProcessAndInsertHash_82BC6F70"

# A processor that computes and converts the data. It takes a value (a), a value (b), and a value (c). It computes an element offset (ProcessAndComputeElementOffset_82229260, with b and c).
# When the result is a specific .data constant it performs a floating-point conversion (int to float to double to float, storing the result in a local buffer). It then dispatches the size
# (ProcessDispatchSize_82A2FBE0). This is the "compute and convert this data" routine, part of the 0x8223B process cluster. 77 lines. 2 callees (ProcessAndComputeElementOffset and
# ProcessDispatchSize). 3 callers (GuildChest_ProcessItems_822DE008, sub_82241898, and sub_82A2B578).
[entrypoint.functions."0x8223B810"]
name = "ProcessAndComputeAndConvert_8223B810"

# A processor that accesses and converts the data. It takes a structure (a: a field at a->8). It accesses an element (AccessElement8_82A1E620, with a). It then computes and converts
# (ProcessAndComputeAndConvert_8223B810, with a and the accessed element's first field). It then stores two fields (a->0 and a->4) from the computed result. It checks the element's -4 field
# (a type) and, when it is at least 4 and the element's -8 field's +5 byte (masked to 2 bits) is non-zero, it performs further processing. This is the "access and convert this data" routine,
# part of the 0x82241 process cluster. 94 lines. 2 callees (AccessElement8 and ProcessAndComputeAndConvert). 6 callers (sub_821A3A50, sub_82A23970, sub_82A23828, sub_82A298E8, sub_82A23EE8,
# and sub_8228EF20).
[entrypoint.functions."0x82241898"]
name = "ProcessAndAccessAndConvert_82241898"

# A processor that inserts and processes the data. It takes a structure (a: fields at a->4 and a->8), a value (b), a value (c), and a value (d). It reads a->4 (a pointer). When the pointer is
# non-zero it computes a value (a->8 minus a->4, divided by 12); when it is zero it uses 0. It then inserts into an array (Array_InsertAt_82370A98) and performs further processing
# (sub_82C49B70 and sub_82C4A858). This is the "insert and process this data" routine, part of the 0x82B42 process cluster. 165 lines. 3 callees (Array_InsertAt, sub_82C49B70, and
# sub_82C4A858). 1 caller (LockedSortedInsert_82B41738).
[entrypoint.functions."0x82B423F0"]
name = "ProcessAndInsertAndProcess_82B423F0"

# A processor that accesses and processes the data. It takes a structure (a), a value (b), a value (c), a value (d), and a value (e). It reads a saved pointer (a non-null element array). When the
# pointer is null it triggers a trap. It computes an offset: (c minus b, divided by 12, times 6) plus the array's element base. It then accesses the element at that offset and compares it against
# the array's end. It then processes the element (sub_82B43170). This is the "access and process this element" routine, part of the 0x82B42 process cluster. 133 lines. 1 callee (sub_82B43170).
# 1 caller (LockedSortedInsert_82B41738).
[entrypoint.functions."0x82B42CD8"]
name = "ProcessAndAccessAndProcess_82B42CD8"

# A merger that merges two sorted arrays. It takes two sorted arrays (a: from a to b, c: from c to d) and a destination (e). It iterates over both arrays (12-byte elements): for each pair it
# compares their first fields (the key). When the second array's key is smaller it copies that element (the 12 bytes) into the destination and advances the second array; otherwise it copies the
# first array's element and advances the first array. It continues until one of the arrays is exhausted. This is the "merge these two sorted arrays" routine, part of the 0x82B43 process cluster.
# 145 lines. 0 callees (leaf function). 1 caller (ProcessAndAccessAndProcess_82B42CD8).
[entrypoint.functions."0x82B43170"]
name = "MergeSortedArrays_82B43170"

# A setter that sets a field (offset 8). It takes a structure (a) and a value (b). It stores b into a->8. This is a simple setter that sets the +8 field of a structure, part of the 0x82BAC set
# cluster. 8 lines. 0 callees (leaf function). 0 callers (unresolved — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x82BACA60"]
name = "SetField8_82BACA60"

# A processor that stores and initializes the data. It takes a structure (a: a field at a->4), a value (b), and a value (c). It stores an element (ProcessAndStoreElement_82BB1E58, with a->4). It then
# initializes a string (ProcessAndInitializeString_82BA8798, with a local buffer and a->8). It then computes a length and processes (ProcessAndComputeLengthAndProcess_82BA89B0, with the local buffer
# and c and -1). It then reads a->4 (a pointer) and performs further processing (reading the pointer's +8, +12, and +16 fields and computing their differences). This is the "store and initialize
# this data" routine, part of the 0x82BB1 process cluster. 582 lines. 15 callees (including ProcessAndStoreElement, ProcessAndInitializeString, and ProcessAndComputeLengthAndProcess). 0 callers
# (unresolved — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x82BB1420"]
name = "ProcessAndStoreAndInitialize_82BB1420"

# A processor that reads and processes the data. It takes a structure (a: fields at a->8 and a->12). It reads a->8 (a value) and a->12 (a pointer). When a->12 is less than a->8 and not a specific
# .data constant, it reads a->12's +4 field (a type). When the type is 2 it skips further processing. Otherwise it processes (sub_82BC68F0, with a, 1, and a .data pointer's +8 field). This is the
# "read and process this data" routine, part of the 0x82BB7 process cluster. 667 lines. 13 callees (including ProcessAndStoreElement, ProcessAndReadOrResolveValue, and ProcessLoopDispatch). 0 callers
# (unresolved — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x82BB7100"]
name = "ProcessAndReadAndProcess_82BB7100"

# A processor that inserts and processes the data (second form). It takes a structure (a: fields at a->8 and a->12). It reads a->12 (a pointer) and a->8 (a value). When a->12 is less than a->8 and
# not a specific .data constant, it reads a->12's +4 field (a type). When the type is not -1 it performs processing (sub_82BC67F0, with a, 1, and a .data pointer). It then looks up an indexed
# pointer (ProcessAndLookupIndexedPointer_82BC5D40) and inserts a hash entry (ProcessAndInsertHash_82BC6F70). This is the "insert and process this data" routine (second form), part of the 0x82BD5
# process cluster. 111 lines. 3 callees (ProcessAndLookupIndexedPointer, sub_82BC67F0, and ProcessAndInsertHash). 0 callers (unresolved — body is recovered but no callers found; likely a vtable
# method or indirect entry).
[entrypoint.functions."0x82BD5380"]
name = "ProcessAndInsertAndProcess2_82BD5380"

# A processor that increments and stores the data. It takes a structure (a: fields at a->4, a->8, and a->12). It increments a counter (AtomicIncrement3_821F7798, with a). It then reads two float
# constants from .data (one at a .data offset and one at another .data offset) and stores them into the structure: a->4 and a->8 are set to the first float, and a->12 is set to the second float.
# This is the "increment and store this data" routine, part of the 0x82C14 process cluster. 53 lines. 1 callee (AtomicIncrement3). 1 caller (ProcessAndGrowAndProcess7_82C18F50).
[entrypoint.functions."0x82C14CA0"]
name = "ProcessAndIncrementAndStore_82C14CA0"

# A processor that performs a vcall sequence and processes the data. It takes a value (a) and a value (b). It performs a vcall sequence (ProcessVcallSequence3_82C14D70, with b, a .data pointer, and
# a). When the result's low byte is non-zero it performs another vcall sequence (VcallChain_AllSuccess_82C15068, with b, a .data pointer, and a+4). When the result's low byte is non-zero it performs
# further processing. This is the "perform a vcall sequence and process this data" routine, part of the 0x82C15 process cluster. 109 lines. 2 callees (ProcessVcallSequence3 and
# VcallChain_AllSuccess). 1 caller (sub_82C16BE0).
[entrypoint.functions."0x82C15478"]
name = "ProcessAndVcallAndProcess_82C15478"

# A processor that performs a vcall and dispatches the data. It takes a structure (a: a field at a->0) and a value (b). It reads a->0 (a vtable pointer). It then dispatches via the vtable's +24
# entry (bctrl). When the result's low byte is non-zero it performs a vcall sequence (ProcessAndVcallAndProcess_82C15478, with b and a). When the result's low byte is non-zero it dispatches via the
# vtable's +28 entry (bctrl). This is the "perform a vcall and dispatch this data" routine, part of the 0x82C16 process cluster. 86 lines. 1 callee (ProcessAndVcallAndProcess). 1 caller
# (ProcessAndGrowAndProcess7_82C18F50).
[entrypoint.functions."0x82C16BE0"]
name = "ProcessAndVcallAndDispatch_82C16BE0"

# A processor that processes and frees the data. It takes a value (a) and a value (b). It processes a value (sub_82C6CFF0, with a and b). When a is null it triggers a trap. It reads a->4 (a pointer
# and compares it against the processed value). When they differ it reads the processed value's +32 and +36 fields. When +36 is at least 16 it reads the processed value's +16 field; otherwise it uses
# the processed value's +12 address plus 4. It then clamps and grows (ProcessAndClampGrow_8217CBE8), performs a range update (RangeUpdate_8218EA38), and frees (Free_Thunk_824FE010). This is the
# "process and free this data" routine, part of the 0x82C6E process cluster. 156 lines. 5 callees (sub_82C6CFF0, ProcessAndClampGrow, RangeUpdate, sub_82C6E898, and Free_Thunk). 7 callers
# (sub_82C6F1F0, sub_82C6F2E0, sub_82C6F618, sub_82C6F3F0, sub_82C6F4B8, sub_82C6B390, and sub_82C6BEB0).
[entrypoint.functions."0x82C6EB30"]
name = "ProcessAndProcessAndFree_82C6EB30"

# A processor that dispatches by type and processes the data. It takes a structure (a: fields at a->8 and a->12). It reads a->8 (a value) and a->12 (a pointer). When a->12 is less than a->8 and
# not a specific .data constant, it reads a->12's +4 field (a type). When the type is 5 it skips further processing. Otherwise it processes (sub_82BC68F0, with a, 1, and a .data pointer's +20 field).
# It then performs further processing (ElementAccess, ProcessAndDispatchByType2, ProcessAndReadOrResolveValue, and ProcessAndStoreIndexedValue). This is the "dispatch by type and process this
# data" routine, part of the 0x82BDC process cluster. 188 lines. 6 callees (including ProcessAndDispatchByType2, ProcessAndReadOrResolveValue, and ProcessAndStoreIndexedValue). 0 callers
# (unresolved — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x82BDC238"]
name = "ProcessAndDispatchByTypeAndProcess_82BDC238"

# A processor that processes the data (second form). It takes a structure (a: fields at a->12 and a->32) and a value (b). It reads a->12 (a value). When a->12 is not equal to b it performs processing
# (sub_82BCDA68, with a and b). It then reads a->32 (a pointer) and performs further processing (sub_82BC8870, with a->32, a .data pointer, and b). It then processes (sub_82BCDAF8, with a, the
# result, and a->12). This is the "process this data" routine (second form), part of the 0x82BDD process cluster. 57 lines. 3 callees (sub_82BCDA68, sub_82BC8870, and sub_82BCDAF8). 12 callers
# (sub_82BDEB18, sub_82BDEBD0, sub_82BDECC0, sub_82BE0D80, sub_82BE0548, sub_82BE02F8, sub_82BE0BA8, sub_82BE07C0, sub_82BDEE48, sub_82BDF220, sub_82BE0E58, and sub_82BE09D0).
[entrypoint.functions."0x82BDDCC8"]
name = "ProcessAndProcessAndProcess2_82BDDCC8"

# A processor that processes and recurses the data. It takes a structure (a: fields at a->0 and a->50), a value (b), and a structure (c: fields at c->0, c->4, c->12, and c->16). When a is null it
# initializes c's fields (c->12 and c->16 to -1, c->0 to 8, and c->4 to 255). Otherwise it reads a->50 (a byte) and a->0 (a pointer). When the byte is at least 1 it performs processing
# (sub_82BDDFD0) and recurses (itself, sub_82BDE160). This is the "process and recurse this data" routine, part of the 0x82BDE process cluster. 191 lines. 2 callees (sub_82BDDFD0 and itself,
# making it recursive). 1 caller (sub_82BDE2A8).
[entrypoint.functions."0x82BDE160"]
name = "ProcessAndRecurseAndProcess_82BDE160"

# An initializer that initializes and appends an element. It takes a structure (a: fields at a->0, a->8, a->12, a->16, and a->24). It initializes the structure: a->0 is set to a .data pointer, a->8,
# a->12, and a->16 are set to 0, and a->24 is set to a float constant from .data. It then appends an element (AppendElement16_82442B68). This is the "initialize and append this element" routine,
# part of the 0x83236 process cluster. 107 lines. 1 callee (AppendElement16). 1 caller (sub_8323DB30).
[entrypoint.functions."0x83236A78"]
name = "InitializeAndAppendElement_83236A78"

# A constructor that constructs a 32-byte vector and initializes the data. It takes a structure (a: fields at a->0, a->224, a->228, a->232, a->236, a->240, a->244, and a->245). It constructs a 32-byte
# vector (Construct32ByteVector2_8323C7F0, with a). It then initializes the structure: a->0 is set to a .data pointer, a->224 is set to 0, a->228 is set to 1, a->232, a->236, and a->240 are set to float
# constants from .data, and a->244 and a->245 are set to 0. It then initializes and appends an element (InitializeAndAppendElement_83236A78). This is the "construct a 32-byte vector and initialize
# this data" routine, part of the 0x8323D process cluster. 80 lines. 2 callees (Construct32ByteVector2 and InitializeAndAppendElement). 2 callers (sub_8323E260 and sub_8323E208).
[entrypoint.functions."0x8323DB30"]
name = "Construct32ByteVectorAndInitialize_8323DB30"

# A processor that grows and appends the data. It takes a structure (a: a field at a->248) and a structure (b: fields at b->4, b->12, and b->20). It grows and appends (ProcessGrowAppend4_8323CB98,
# with a and b). It then reads a->248 (a vtable pointer). It reads the vtable's +8 entry and performs a vcall (bctrl). It then reads b->20 (a count). When the count is at least 4 it performs further
# processing (reading b->4 and b->12 and copying a struct region, ProcessAndCopyStructRegion_82A1B480). This is the "grow and append this data" routine, part of the 0x8323D process cluster. 605
# lines. 2 callees (ProcessGrowAppend4 and ProcessAndCopyStructRegion). 0 callers (unresolved — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x8323DBB0"]
name = "ProcessAndGrowAndAppend_8323DBB0"

# A processor that walks and releases the data. It takes a structure (a: fields at a->0 and a->4) and a value (b). It walks a chain (WalkChainFind_83239FA8, with a .data pointer, b, and a local buffer).
# When the result's low byte is non-zero it zeroes a->0 and a->4 and releases a reference count (Release_RefCounted_829FF648, with the local buffer). Otherwise it allocates 288 bytes
# (Allocate_OrDbgBreak_8221F388) and constructs a 32-byte vector (Construct32ByteVectorAndInitialize_8323DB30). It then sets a refcounted wrapper (SetRefcountedWrapper_8238A848) and processes the
# reference count (ProcessRefCount_83225538). This is the "walk and release this data" routine, part of the 0x8323E process cluster. 177 lines. 6 callees (including WalkChainFind, Release_RefCounted,
# Allocate_OrDbgBreak, and Construct32ByteVectorAndInitialize). 1 caller (StateDispatch4_83220888).
[entrypoint.functions."0x8323E260"]
name = "ProcessAndWalkAndRelease_8323E260"

# A processor that performs a vcall and frees the data. It takes a structure (a: fields at a->0 and a->8) and a value (b). It reads a->8 (a pointer). When the pointer is non-zero it reads the
# pointer's +0 field (a vtable pointer) and performs a vcall via the vtable's +8 entry (bctrl). It then sets a->0 to a .data pointer. When the low bit of b is set it frees (Free_SizeBucketed_Thunk_8221BE68,
# with a). This is the "perform a vcall and free this data" routine, part of the 0x82B4D process cluster. 69 lines. 1 callee (Free_SizeBucketed_Thunk). 0 callers (unresolved — body is recovered but
# no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x82B4DC00"]
name = "ProcessAndVcallAndFree_82B4DC00"

# A processor that processes an indexed region. It takes a structure (a: a field at a->36) and a value (b). It reads a->36 (a value). When b is greater than a->36 it computes an offset (b plus b times 8,
# then times 8). It then gets a singleton (GetSingleton_82BA8718) and processes an indexed region (ProcessIndexedRegion2_82BA8F18, with the result, the offset, a, a .data pointer, and 141). It then
# zeroes a buffer (MemSet_SdkRuntime_82CA3190). It also processes (ProcessIndexedRegion_82BA9068) and copies (MemCpy_SdkRuntime_82CA2C60). This is the "process this indexed region" routine, part of
# the 0x82BC4 process cluster. 100 lines. 5 callees (including GetSingleton, ProcessIndexedRegion2, and MemSet). 2 callers (sub_82BC3248 and ProcessFloat_8229DE10).
[entrypoint.functions."0x82BC4328"]
name = "ProcessAndIndexedRegion_82BC4328"

# A processor that dispatches and processes the data. It takes a structure (a: fields at a->16, a->20, a->40, and a->68) and a value (b). It reads a->16 (a pointer). It reads the pointer's +64 and +68
# fields. When +68 is at least +64 it performs a loop dispatch (ProcessLoopDispatch_8227B8B8, with a). It then reads a->20 (a pointer) and a->40 (a value). When a->20 equals a->40 it reads a->68; otherwise
# it reads a->20's +4 field (a pointer), the pointer's +0 field (another pointer), and that pointer's +12 field. It then performs further processing (sub_82BC9860 and sub_82BCCB88). This is the "dispatch
# and process this data" routine, part of the 0x82BC6 process cluster. 164 lines. 3 callees (ProcessLoopDispatch, sub_82BC9860, and sub_82BCCB88). 7 callers (sub_82BD70B0, sub_82BB5C30, sub_82BB6820,
# sub_82BD7DD8, sub_82BDA2C8, sub_82BBB168, and sub_82BBB5C8).
[entrypoint.functions."0x82BC6558"]
name = "ProcessAndDispatchAndProcess_82BC6558"

# A processor that performs a loop of vcalls. It takes a value (a, a pointer), a value (b, a stride), a value (c, a vtable entry function pointer), and a count (d). It iterates over the count (d):
# for each iteration it performs a vcall (bctrl via c) with a as the argument, and advances a by b. This is the "perform a loop of vcalls" routine, part of the 0x8221F process cluster. 48 lines. 0 callees
# (leaf function). 12 callers (sub_82B5AAF8, sub_82EEB1B0, sub_82EEBD90, sub_82EEAF60, sub_82EEA4A8, sub_82EEBC80, sub_82EEC088, sub_8328BEA8, sub_827D67E8, sub_82EEB220, sub_821C3CD0, and sub_82EED2F8).
[entrypoint.functions."0x8221FC28"]
name = "ProcessLoopVcall_8221FC28"

# A processor that processes an indexed region (second form). It takes a structure (a: fields at a->32, a->36, and a->40) and a value (b). It zeroes a->40. When the low byte of b is non-zero it
# reads a->32 and a->36, gets a singleton (GetSingleton_82BA8718), and processes an indexed region (ProcessIndexedRegion_82BA9068, with the singleton, a->32, and a->36 times 4). It then zeroes a->32
# and a->36. Otherwise it reads a->32 and a->36 and processes the region. This is the "process this indexed region" routine (second form), part of the 0x823F9 process cluster. 66 lines. 3 callees
# (GetSingleton, ProcessIndexedRegion, and MemSet). 11 callers (sub_82BC4880, sub_82BA9698, sub_82BC1090, sub_827D1EB0, sub_829DDA80, sub_822D0488, sub_82BC0F90, sub_823F96E8, sub_82BC3398,
# sub_822AC668, and sub_82BB80F0).
[entrypoint.functions."0x823F9A78"]
name = "ProcessAndIndexedRegion2_823F9A78"

# A serializer that serializes and processes an indexed region. It takes a structure (a: fields at a->12, a->44, a->48, a->52, and a->56). It zeroes a->44, a->48, and a->52. It sets a->56 to a float
# constant from .data. It then serializes a region of a (Serialize_82CA3EB8, with a+12, 32, and two .data pointers). It then processes and clears an indexed region (ProcessAndClearIndexedRegion_82BBF648).
# This is the "serialize and process this indexed region" routine, part of the 0x82BC4 process cluster. 172 lines. 2 callees (Serialize and ProcessAndClearIndexedRegion). 1 caller (sub_82BC43D0).
[entrypoint.functions."0x82BC44F0"]
name = "SerializeAndProcessIndexedRegion_82BC44F0"

# A processor that checks and clears the data. It takes a structure (a: a field at a->0). It reads a->0 (a value). When a->0 is non-zero and not -1 it checks success (ProcessAndCheckSuccess_82CBBF60,
# with a->0). It then zeroes a->0. This is the "check and clear this data" routine, part of the 0x82BF2 process cluster. 45 lines. 1 callee (ProcessAndCheckSuccess). 8 callers (sub_82BEBE58,
# sub_82BF3830, sub_82BF32C8, sub_82BEB858, ProcessAndCheckString2_82BF3690, sub_82BF0CE0, sub_82BEBFC0, and sub_82BF3428).
[entrypoint.functions."0x82BF2CB8"]
name = "ProcessAndCheckAndClear_82BF2CB8"

# A getter that gets the timebase. It takes a structure (a: a field at a->0). It reads the timebase (mftb). When the timebase is zero it reads the timebase again (a retry). It then stores the timebase
# into a->0 and returns 1. This is the "get the timebase" routine, part of the 0x8221E process cluster. 20 lines. 0 callees (leaf function). 12 callers (sub_82BBB2A8, sub_82D02A88, sub_82B9C530,
# sub_82A3E7C0, sub_82CFECA8, sub_822B6D70, sub_82A57418, sub_82AA2E80, sub_82B69488, sub_82AB2968, sub_82BE3E00, and sub_8238A2D8).
[entrypoint.functions."0x8221EB58"]
name = "GetTimebase_8221EB58"

# A processor that queries and dispatches the data. It takes a structure (a: a field at a->16), a value (b), a value (c), and a value (d). It computes c times d and queries and processes
# (ProcessAndQueryAndProcess2_82A36390, with a, b, and the product). It then reads a->16 (a value). When a->16 is non-zero and d minus 1 is at most 7 it performs a table lookup (a .data pointer
# indexed by d minus 1 times 4) and dispatches (bctr). This is the "query and dispatch this data" routine, part of the 0x82A36 process cluster. 213 lines. 3 callees (ProcessAndQueryAndProcess2,
# sub_82A2ED28, and sub_821E51D0). 5 callers (sub_82A365D0, sub_82A36D00, sub_82A36648, sub_82A36560, and sub_82A36910).
[entrypoint.functions."0x82A363F0"]
name = "ProcessAndQueryAndDispatch_82A363F0"

# A processor that destructs and frees the data. It takes a structure (a: fields at a->4 and a->12) and a value (b). When b is greater than a specific .data constant it errors (sub_82684B38). Otherwise
# it reads a->4 (a pointer). When the pointer is non-zero it computes a value (a->12 minus a->4, divided by 36). When the value is less than b it destructs an element range (DestructElementRange_82378088),
# frees (Free_SizeBucketed_Thunk_8221BE68), and validates and constructs (ValidateAndConstruct_82377F60). This is the "destruct and free this data" routine, part of the 0x82B41 process cluster. 165
# lines. 5 callees (including DestructElementRange, Free_SizeBucketed_Thunk, and ValidateAndConstruct). 3 callers (sub_82C67C70, sub_82B41100, and sub_82B4B0E0).
[entrypoint.functions."0x82B41E28"]
name = "ProcessAndDestructAndFree_82B41E28"

# A loader that loads elements from a source. It takes a value (a, a source), a value (b, a destination), a value (c, a count), a value (d, an offset), and a value (e, a base). It loops over the count
# (c): for each iteration it loads from a source (LoadFrom_Source_822F2398, with the base plus the offset and the base plus the current offset). It advances the base by 36 each time. This is the
# "load these elements from a source" routine, part of the 0x82B42 process cluster. 59 lines. 1 callee (LoadFrom_Source). 1 caller (ProcessAndDestructAndFree_82B41E28).
[entrypoint.functions."0x82B42DC8"]
name = "LoadElementsFromSource_82B42DC8"

# A sorter that sorts three elements. It takes three values (a, b, and c, each pointing to a structure with a field at +0 that points to an element with +4 and +12 fields). It sorts the three elements
# by their +4 field (the primary key), using the +12 field (the secondary key) as a tiebreaker. When a's element's +4 is greater than b's element's +4 (or equal and its +12 is greater) it swaps a and b.
# It then sorts b and c similarly. This is the "sort these three elements" routine, part of the 0x82C2A process cluster. 89 lines. 0 callees (leaf function). 1 caller (sub_82C2AF58).
[entrypoint.functions."0x82C2ADB0"]
name = "Sort3Elements_82C2ADB0"

# A getter that gets a data pointer (second form). It returns a .data pointer (a constant address). This is the "get this data pointer" routine (second form), part of the 0x82C43 process cluster. 10
# lines. 0 callees (leaf function). 0 callers (unresolved — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x82C43398"]
name = "GetDataPointer2_82C43398"

# A processor that stores and processes the data (second form). It takes a structure (a: fields at a->4, a->8, and a->12) and a value (b). It reads a->4 (a pointer). When the pointer is non-zero it
# computes a value (a->8 minus a->4, divided by 4). It then reads a->12 (another pointer) and computes another value (a->12 minus a->4, divided by 4). When the first value is less than the second value it
# reads a->8 (a pointer), reads b's first field, and stores it into a->8 (a pointer store). It then performs further processing (sub_82C6DF48). This is the "store and process this data" routine (second
# form), part of the 0x82C6E process cluster. 98 lines. 1 callee (sub_82C6DF48). 2 callers (sub_82C0A4F0 and sub_82C6E718).
[entrypoint.functions."0x82C6E430"]
name = "ProcessAndStoreAndProcess2_82C6E430"

# A processor that validates and stores the data. It takes a structure (a: fields at a->4 and a->8, plus 36 offset) and a value (b). It reads (a+36)->4 (a pointer) and (a+36)->8 (a value). It validates
# that the pointer is not greater than the value (triggering a trap when it is). It then stores and processes (ProcessAndStoreAndProcess2_82C6E430, with a+36 and b). This is the "validate and store this
# data" routine, part of the 0x82C6E process cluster. 110 lines. 1 callee (ProcessAndStoreAndProcess2). 2 callers (sub_82C6C1B0 and sub_82C6B390).
[entrypoint.functions."0x82C6E718"]
name = "ProcessAndValidateAndStore_82C6E718"

# A processor that accesses and shifts the data. It takes a structure (a: a field at a->8). It accesses an element (AccessElement8_82A1E620, with a). It then shifts data by 8 bytes: it reads a->8 (a
# pointer) and, for each 8-byte element from a+8 to a->8, it stores the element's first 8 bytes into the preceding 8 bytes. It then decrements a->8 by 8. This is the "access and shift this data"
# routine, part of the 0x821DF process cluster. 58 lines. 1 callee (AccessElement8). 12 callers (sub_82A28A50, sub_82947A58, sub_8244D168, sub_8252CF78, sub_828BE220, sub_82A2AC80, sub_8267CAB0,
# sub_824EF558, sub_82956010, sub_82A29F88, sub_821A1A90, and sub_82519218).
[entrypoint.functions."0x821DFF48"]
name = "ProcessAndAccessAndShift_821DFF48"

# A processor that grows and serializes the data. It takes a structure (a: fields at a->32, a->36, a->40, and a->44), a float (b), and a value (c). It stores b into a->44. It zeroes a->32, a->36, and
# a->40. It compares b to a float constant from .data. When b is less than the constant it overwrites a->44 with another float constant from .data. When c is zero it sets c to a .data pointer. It then
# grows a region (GrowRegion_823F9AE0) and serializes (Serialize_82CA3EB8). This is the "grow and serialize this data" routine, part of the 0x823F9 process cluster. 89 lines. 2 callees (GrowRegion and
# Serialize). 7 callers (sub_82BC3248, sub_82BB80F0, sub_829DDA80, sub_823F96E8, sub_82BACD98, sub_827D1EB0, and sub_82BC0E68).
[entrypoint.functions."0x823F99E8"]
name = "ProcessAndGrowAndSerialize_823F99E8"

# A processor that appends and processes the data (second form). It takes a value (a), a value (b), and a value (c). It validates that c is not zero (triggering a trap when it is) and that c is not equal
# to b (unless it is -2). It then performs processing (sub_826C3AA8, with a and a computed value). It then appends (ProcessAppend_823712D8). This is the "append and process this data" routine (second form),
# part of the 0x826C3 process cluster. 167 lines. 2 callees (ProcessAppend and sub_826C3AA8). 2 callers (sub_82C69318 and sub_826C00C0).
[entrypoint.functions."0x826C3908"]
name = "ProcessAndAppendAndProcess2_826C3908"

# A processor that checks and processes the data (third form). It takes a structure (a: fields at a->4, a->20, and a->24) and a value (b). It reads a->20 (a value) and a->24 (a value). When a->20 is less
# than or equal to b and a->24 is not equal to b it performs processing (ProcessValue_FlagNonZero_8217A5E0, with a and 1). When the result's low byte is non-zero it stores a->20 back into a->20 and reads
# a->24. When a->24 is at least 8 it reads a->4; otherwise it uses a+4. This is the "check and process this data" routine (third form), part of the 0x826C3 process cluster. 77 lines. 1 callee
# (ProcessValue_FlagNonZero). 1 caller (ProcessAndAppendAndProcess2_826C3908).
[entrypoint.functions."0x826C3AA8"]
name = "ProcessAndCheckAndProcess3_826C3AA8"

# A processor that looks up and dispatches the data. It takes a value (a), a value (b), and a value (c). It looks up in a hash table (HashTable_Find3_821CF1D0, with b and c). When the result is a specific
# .data constant it performs a state dispatch (StateDispatch3_82BCA3E8, with a, b, and a local buffer containing c and 4). This is the "look up and dispatch this data" routine, part of the 0x82BCA process
# cluster. 60 lines. 2 callees (HashTable_Find3 and StateDispatch3). 1 caller (sub_82BCDBA8).
[entrypoint.functions."0x82BCA5F8"]
name = "ProcessAndLookupAndDispatch_82BCA5F8"

# A processor that hashes and looks up the data. It takes a structure (a: fields at a->28 and a->32). It reads a->32 (a value). It computes a hash (ComputeHash_8227BA30, with a->32). It then reads a->28
# (a pointer) and looks up and dispatches (ProcessAndLookupAndDispatch_82BCA5F8, with a->32, the pointer's +4 field, and the hash). When the result's +4 field is zero it sets the result's +0 and +4 fields to 1.
# This is the "hash and look up this data" routine, part of the 0x82BCD process cluster. 68 lines. 2 callees (ComputeHash and ProcessAndLookupAndDispatch). 8 callers (sub_82BE09D0,
# ProcessAndDispatchByCounter_82BCEE60, sub_82BE0BA8, sub_82BDE878, sub_82BCE250, sub_82BDF0D8, sub_82BDF220, and sub_82BCE6E8).
[entrypoint.functions."0x82BCDBA8"]
name = "ProcessAndHashAndLookup_82BCDBA8"

# A processor that dispatches and checks the data. It takes a structure (a: fields at a->0, a->4, and a->16), a value (b), and a value (c). It reads a->16 (a value) and a->4 (a value). It dispatches a value
# (ProcessDispatchValue_8229A488, with a->16, a->4, and b). It then reads a->0 (a pointer) and the dispatched value's +4 field. When the +4 field is 3 it reads the pointer's +40 field and converts the
# dispatched value's first field (a float) to an int. This is the "dispatch and check this data" routine, part of the 0x82BE1 process cluster. 223 lines. 3 callees (ProcessDispatchValue, StateDispatch,
# and ProcessAndCheckAndProcess5). 7 callers (sub_82BDDDD8, sub_82BE12E8, sub_82BE09D0, sub_82BE20A0, sub_82BE24B0, sub_82BDF510, and sub_82BDE2A8).
[entrypoint.functions."0x82BE1C40"]
name = "ProcessAndDispatchAndCheck_82BE1C40"

# An incrementer that increments a field (offset 8). It takes a structure (a: a field at a->8). It increments a->8 by 1. This is the "increment this field" routine, part of the 0x82C4E process cluster.
# 12 lines. 0 callees (leaf function). 0 callers (unresolved — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x82C4E9A8"]
name = "IncrementField8_82C4E9A8"

# A processor that initializes and copies the data. It takes a structure (a: fields at a->0, a->4, a->8, a->12, and a->16). It reads a->4 (a pointer). When the pointer is non-zero and a->16 is zero it
# initializes the structure: a->0 is set to the pointer, a->4 is set to the pointer's first field, a->16 is set to a->0 plus 8, a->12 is set to a->0 plus 8, and a->8 is set to a->4 plus a->12. It then copies
# (MemCpy_SdkRuntime_82CA2C60). This is the "initialize and copy this data" routine, part of the 0x82C99 process cluster. 280 lines. 1 callee (MemCpy). 12 callers (sub_82C9AAD0,
# ProcessAndCheckAndReturn_82C9B2F8, sub_82C9C3A8, sub_82C9C9C0, ProcessAndCheckAndDispatch_82C9B1A8, sub_82C9ABF8, sub_82C9FB58, sub_82C9BE58, ProcessAndQueryAndProcess_82C9DC30, sub_82C9CC38,
# sub_82C9D748, and sub_82C9B268).
[entrypoint.functions."0x82C99808"]
name = "ProcessAndInitializeAndCopy_82C99808"

# A processor that initializes and processes the data. It reads a .data pointer's field (a flag). When the flag's low bit is clear it sets the flag's low bit and processes (sub_82C645A8, with a local
# buffer). It then processes (sub_823751A8, with a .data pointer and the local buffer's first field). This is the "initialize and process this data" routine, part of the 0x82375 process cluster. 97 lines.
# 3 callees (sub_823751A8, sub_82C645A8, and sub_82CA3700). 5 callers (sub_822F36B8, sub_822F3828, sub_82372758, sub_823735B0, and sub_823728E0).
[entrypoint.functions."0x82375100"]
name = "ProcessAndInitializeAndProcess_82375100"

# An offsetter that adds an offset (768). It takes a value (a) and returns a plus 768. This is the "add this offset" routine, part of the 0x82BAA process cluster. 8 lines. 0 callees (leaf function). 0 callers
# (unresolved — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x82BAA168"]
name = "AddOffset768_82BAA168"

# A processor that copies and grows the data. It takes a value (a) and a value (b). It copies data and sets a count (ProcessAndCopyDataAndSetCount_82BEC350, with a and 0 and 0). It then finds the first zero
# byte in b (a NUL-terminated string) and grows data by size (ProcessAndGrowDataBySize_82BED440, with a, b, and the string's length minus 1). This is the "copy and grow this data" routine, part of the
# 0x82BEE process cluster. 66 lines. 2 callees (ProcessAndCopyDataAndSetCount and ProcessAndGrowDataBySize). 3 callers (sub_82BF04F8, sub_82BEF170, and sub_82BEEC18).
[entrypoint.functions."0x82BEE748"]
name = "ProcessAndCopyAndGrow_82BEE748"

# A processor that performs a range update. It takes a structure (a: fields at a->0, a->8, a->24, a->28, a->32, and a->44), a value (b), a value (c), a value (d), and a value (e). It initializes the
# structure: a->0 is set to a .data pointer, a->8 is set to 0, a->24 is set to 0, and a->28 is set to 15. It then performs a range update (RangeUpdate_8218EA38, with a+4, b, 0, and -1). It then stores c into
# a->32 and performs further processing (sub_82C6EB30). This is the "perform this range update" routine, part of the 0x82C6F process cluster. 107 lines. 2 callees (RangeUpdate and ProcessAndProcessAndFree).
# 4 callers (sub_82C6F618, sub_82C6F4B8, sub_82C6F6E0, and sub_82C6F3F0).
[entrypoint.functions."0x82C6F2E0"]
name = "ProcessAndRangeUpdate_82C6F2E0"

# A processor that dispatches and processes the data. It takes a value (a), a value (b, a pointer), a value (c, an end pointer), and a value (d). It iterates over the range (b to c): for each element it reads
# the element's first byte, adds a to it, reads the result's +76 field (a byte), and subtracts 5. When the result is at most 24 it performs a table lookup (a .data pointer indexed by the result times 4)
# and dispatches (bctr). This is the "dispatch and process this data" routine, part of the 0x82C8A process cluster. 805 lines. 1 callee (sub_82C8A698). 1 caller (ProcessAndDispatchIndex_82C8AF48).
[entrypoint.functions."0x82C8A988"]
name = "ProcessAndDispatchAndProcess_82C8A988"

# A processor that performs a virtual operation and stores the data. It takes a value (a) and a structure (b: fields at b->16 and b->32). It computes a plus 1. When the result is at most a specific .data
# constant it performs a virtual operation (ProcessVirtual_82227940, with a, b->16, b->32 times 8, and a times 8). When the result is greater it performs further processing (sub_82A2C520, with a .data pointer).
# It then stores the result into b->16. This is the "perform a virtual operation and store this data" routine, part of the 0x82A2F process cluster. 89 lines. 2 callees (ProcessVirtual and sub_82A2C520).
# 2 callers (ProcessAndComputeAndGrow_82A2F880 and sub_822277C0).
[entrypoint.functions."0x82A2F6C0"]
name = "ProcessAndVirtualAndStore_82A2F6C0"

# A processor that performs a vcall and processes the data. It takes a value (a, an end pointer), a value (b, a pointer), and a value (c, a source). It iterates over the range (b to a, 8-byte steps):
# for each element it reads the source's corresponding field. When the field is non-zero it stores it into the element's first field and performs a vcall via the field's +0 and +4 entries (bctrl). When the
# field is zero it stores zero. It then copies the source's corresponding +4 field into the element's second field. This is the "perform a vcall and process this data" routine, part of the 0x82B4D process
# cluster. 70 lines. 0 callees (leaf function). 1 caller (sub_82B4C4A0).
[entrypoint.functions."0x82B4D290"]
name = "ProcessAndVcallAndProcess_82B4D290"

# A processor that computes and processes the data (second form). It takes a structure (a: fields at a->4, a->8, and a->12) and a value (b). It reads a->4 (a pointer). When the pointer is non-zero it computes
# two values: (a->8 minus a->4) divided by 12, and (a->12 minus a->4) divided by 12. When the first value is less than the second value and a->8 is non-zero it reads b's first field and performs further
# processing (sub_82BEEA18). This is the "compute and process this data" routine (second form), part of the 0x82BEE process cluster. 106 lines. 1 callee (sub_82BEEA18). 1 caller (sub_82BEF360).
[entrypoint.functions."0x82BEEB60"]
name = "ProcessAndComputeAndProcess2_82BEEB60"

# A processor that compares and processes the data. It takes a value (a), a value (b), a value (c), and a value (d). When any of b, c, or d is null it returns 87 (an error code). Otherwise it compares and
# returns a bit (CompareAndReturnBit_82BF2D60, with d). When the result is non-zero it performs further processing. It also searches for characters (SearchStringForChar_82CA70C0 and FindChar_82CA3980), queries file
# attributes (QueryFileAttributes_82CBD368), stores and dispatches (ProcessAndStoreAndDispatch_82BF2EC8), checks and clears (sub_82BF2CB8), and aborts (ProcessAndAbort_82170CC8). This is the "compare and
# process this data" routine, part of the 0x82BF3 process cluster. 324 lines. 10 callees (including CompareAndReturnBit, QueryFileAttributes, and ProcessAndAbort). 0 callers (unresolved — body is recovered but
# no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x82BF3830"]
name = "ProcessAndCompareAndProcess_82BF3830"

# A pointer getter that returns a data pointer (fourth form). It returns a .data pointer (a constant address, a plus 19952). This is the "get this data pointer" routine (fourth form), part of the 0x82C40
# process cluster. 10 lines. 0 callees (leaf function). 0 callers (unresolved — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x82C40EB0"]
name = "GetDataPointer4_82C40EB0"

# A processor that copies and frees the data. It takes a structure (a: fields at a->4, a->8, and a->12), a value (b), a structure (c: field at c->0), and a value (d). It reads a->4 (a pointer). When the
# pointer is non-zero it computes (a->12 minus a->4) shifted right by 2. When b is non-zero and a->4 is non-zero it computes (a->8 minus a->4). It then copies (CheckedCopy_82CA3808), allocates
# (AllocateChecked3_82333F78), and frees (Free_Thunk_824FE010). This is the "copy and free this data" routine, part of the 0x82C6D process cluster. 443 lines. 4 callees (including CheckedCopy,
# AllocateChecked3, and Free_Thunk). 1 caller (sub_82C6DF48).
[entrypoint.functions."0x82C6D7D8"]
name = "ProcessAndCopyAndFree_82C6D7D8"

# A processor that copies and processes the data (second form). It takes a value (a), a structure (b: fields at b->4 and b->8), and a value (c). It reads b->4 (a pointer). When the pointer is non-zero it
# computes (b->8 minus b->4) shifted right by 2. When the result is non-zero and b->4 is greater than b->8 it triggers a trap. When c is non-zero it copies and processes (ProcessAndCopyAndFree_82C6D7D8).
# This is the "copy and process this data" routine (second form), part of the 0x82C6D process cluster. 132 lines. 1 callee (ProcessAndCopyAndFree). 1 caller (ProcessAndStoreAndProcess2_82C6E430).
[entrypoint.functions."0x82C6DF48"]
name = "ProcessAndCopyAndProcess2_82C6DF48"

# A processor that initializes and processes the data. It takes a value (a), a value (b), a value (c), a value (d), and a structure (e: fields at e->0, e->8, e->12, and e->16). When d is non-zero it stores 0
# into d. When e is non-zero it stores 259 into e->0 (an error code) and checks e->16's low bit. It then stores the last error code (StoreLastErrorCode_82CC1C38). This is the "initialize and process this
# data" routine, part of the 0x82CBC process cluster. 219 lines. 1 callee (StoreLastErrorCode). 10 callers (sub_824D42B8, sub_82EE6AA0, sub_82CB6168, sub_832B86F8, sub_82B40060, sub_82BF0CE0,
# sub_82D17A38, sub_832B8058, sub_82BEBFC0, and sub_832B8230).
[entrypoint.functions."0x82CBCDF8"]
name = "ProcessAndInitializeAndProcess_82CBCDF8"

# A wrapper that calls sub_82CC1C70. It calls sub_82CC1C70 and returns. This is the "call sub_82CC1C70" routine, part of the 0x82CC0 wrapper cluster. 7 lines. 1 callee (sub_82CC1C70). 12 callers
# (sub_82CB6AA0, ProcessAndLookupWithDefaultInit_82CA5DC0, sub_83001DF8, sub_83002B80, sub_82D17A38, Func_82CAFC28, sub_82B3F010, sub_82CBC490, sub_82BF3010, sub_83001818, sub_82CAA7A8, and sub_83004D18).
[entrypoint.functions."0x82CC0758"]
name = "WrapperSub82CC1C70_82CC0758"

# A pointer getter that returns a pointer from the context. It reads the context's field at +336 (a flag). When the flag is non-zero it returns 0. Otherwise it reads the context's field at +256 (a pointer) and
# returns the pointer's field at +352. This is the "get this pointer from the context" routine, part of the 0x82CC1 process cluster. 21 lines. 0 callees (leaf function). 12 callers (sub_82CF9558,
# sub_82CBBFA8, sub_82CF9078, sub_82CFA6E8, sub_82CF9480, WrapperSub82CC1C70_82CC0758, sub_82CF9288, sub_82CF91E0, sub_82CF98F8, sub_82CF9130, sub_82CFA410, and sub_82CF96A0).
[entrypoint.functions."0x82CC1C70"]
name = "GetPointerFromContext_82CC1C70"

# A processor that appends and resolves the data. It takes a structure (a: field at a->0), a value (b), and a value (c). It reads a->0 (a pointer). When the pointer is non-zero it reads the pointer's +0
# and +4 fields and appends an element (ProcessAndAppendElement_82229208). It then dispatches and resolves (ProcessAndDispatchAndResolve_822279A0), resolves a typed field (ProcessAndResolveTypedField_8219AB28),
# hashes a string (ProcessAndHashString_82A1EC08), and copies a reference (ProcessAndCopyReferenceAndHash_824EF160). This is the "append and resolve this data" routine, part of the 0x82358 process cluster.
# 89 lines. 5 callees (including ProcessAndAppendElement, ProcessAndDispatchAndResolve, and ProcessAndHashString). 2 callers (sub_823577C0 and sub_8261C7E0).
[entrypoint.functions."0x82358980"]
name = "ProcessAndAppendAndResolve_82358980"

# A processor that constructs and processes the data. It takes a value (a), a structure (b: fields at b->68, b->72, b->96, and b->100), a value (c), and a value (d). It checks d's low bit. When the bit is set
# it reads b->100 (a pointer) and computes (b->104 minus b->100) divided by 28. When b->100 is zero it reads b->68 and b->72. It then constructs a 24-byte object (Construct24ByteObject_82375020), constructs a
# string (ConstructString_8217AB58), pushes an element (Array_PushElement_823758C0), allocates (Allocate_OrDbgBreak_8221F388), clamps and updates (ClampAndUpdate_822F6380), destructs (Destruct3_822F5208),
# and frees (Free_Thunk_824FE010). This is the "construct and process this data" routine, part of the 0x82C67 process cluster. 649 lines. 12 callees (including ConstructString, Array_PushElement, and
# Allocate_OrDbgBreak). 0 callers (unresolved — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x82C67C70"]
name = "ProcessAndConstructAndProcess_82C67C70"

# A destructor that destructs and frees the data. It takes a value (a). When a is non-zero it frees it (Free_Thunk_824FE010). It then decrements a .data pointer's counter (a pool size). This is the "destructor
# and free this data" routine, part of the 0x82C87 destruct cluster. 37 lines. 1 callee (Free_Thunk). 12 callers (sub_82C878E8, sub_82CA29E8, sub_832B0650, sub_82C81ED0, sub_82C88A58, sub_832B05F8,
# sub_832B06A8, sub_82C820B0, sub_82C81F50, sub_82C87698, sub_82C82038, and sub_82C88390).
[entrypoint.functions."0x82C87A68"]
name = "DestructAndFree_82C87A68"

# A processor that performs a virtual operation and processes the data. It takes a structure (a: field at a->16), a value (b), and a value (c). It performs a virtual operation (ProcessVirtual_82227940,
# with a, 0, 0, and 36). It then reads a->16 (a pointer) and the pointer's +20 and +28 fields, stores the +28 field into the result's +0, and stores the result into the pointer's +28. It then performs further
# processing (ProcessAndVirtualAndStore_82A2F6C0). This is the "perform a virtual operation and process this data" routine, part of the 0x82227 process cluster. 94 lines. 3 callees (including ProcessVirtual
# and ProcessAndVirtualAndStore). 12 callers (sub_82A298E8, sub_82A1FA08, sub_825DCA40, sub_82852140, sub_82A20198, sub_82A2B578, sub_821E1768, sub_82A32F40, sub_8284CE60, GuildChest_ProcessItems_822DE008,
# sub_82946478, and sub_825AE988).
[entrypoint.functions."0x822277C0"]
name = "ProcessAndVirtualAndProcess_822277C0"

# A processor that hashes and appends the data. It takes a structure (a: fields at a->4 and a->8) and a value (b). It hashes a string (ProcessAndHashString2_821E1408, with a, -10000, and b). It then reads a->8
# (a pointer) and checks it against a .data pointer. When the pointer matches a specific .data constant it returns 0. Otherwise it reads the pointer's +4 field; when the field is zero it stores the pointer
# into a->8 and appends a node (ProcessAndAppendNode_82BC5CD8). This is the "hash and append this data" routine, part of the 0x82BC6 process cluster. 103 lines. 3 callees (including ProcessAndHashString2 and
# ProcessAndAppendNode). 6 callers (sub_82BDB990, sub_82BD93F0, sub_82BBA1D8, sub_82BB46A8, sub_82BB5C90, and sub_82BBB3A8).
[entrypoint.functions."0x82BC6C30"]
name = "ProcessAndHashAndAppend_82BC6C30"

# A comparator that compares and returns a value. It takes a value (a), a structure (b: fields at b->0 and b->4), and a structure (c: fields at c->0 and c->4). It reads b->4 (a type tag) and c->4 (a type tag).
# When the tags match it compares b->0 and c->0: when the tag is 3 it compares them as floats (b->0 < c->0 returns 1, else 0), and when the tag is 4 it compares them as integers. This is the "compare and
# return this value" routine, part of the 0x822AB process cluster. 108 lines. 3 callees (sub_82A301A0, sub_82BC9708, and sub_82BCC1C0). 2 callers (ProcessAndDispatchElements_822DF280 and sub_82BDC730).
[entrypoint.functions."0x822AB768"]
name = "CompareAndReturnValue_822AB768"

# A processor that performs a vcall and processes the data. It takes a structure (a: field at a->0), a value (b), a value (c), and a value (d). When b is zero it stores 0 into a. When b is non-zero and d
# is 1 it performs a vcall via b's +0 and +24 entries (bctrl). It then constructs a linked node (ConstructLinkedNode_823445B0), sets and dispatches (SetAndDispatch_82366210), allocates (Allocate_OrDbgBreak_8221F388),
# and removes and destructs (RemoveAndDestructObject_82364420). This is the "perform a vcall and process this data" routine, part of the 0x82B48 process cluster. 286 lines. 5 callees (including
# ConstructLinkedNode and Allocate_OrDbgBreak). 8 callers (sub_82B48338, sub_82364C30, sub_82363628, sub_82C67878, sub_82363150, sub_824E93A8, sub_824EA150, and sub_82364A58).
[entrypoint.functions."0x82B483D0"]
name = "ProcessAndVcallAndProcess_82B483D0"

# A processor that frees and processes the data. It takes a value (a) and a value (b). It processes (sub_82B48618, with a and b). It then checks b's low bit; when the bit is set it frees the value
# (Free_SizeBucketed_Thunk_8221BE68, with a). This is the "free and process this data" routine, part of the 0x82B48 process cluster. 50 lines. 2 callees (sub_82B48618 and Free_SizeBucketed_Thunk). 0 callers
# (unresolved — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x82B485C8"]
name = "ProcessAndFreeAndProcess_82B485C8"

# A destructor that destructs and frees a structure. It takes a structure (a: fields at a->0, a->16, a->28, a->48, a->52, and a->112). It reads a->112 (a byte). When the byte is non-zero it performs further
# processing (sub_82B489D8, with a). It then frees a->16 and a->28 (Free_SizeBucketed_Thunk_8221BE68, with each). It then checks a->52 and sets a->48 to a .data pointer. This is the "destructor and free this
# structure" routine, part of the 0x82B48 destruct cluster. 107 lines. 4 callees (Free_SizeBucketed_Thunk, ProcessAndCallMethod, sub_82B489D8, and sub_82B52500). 1 caller (ProcessAndFreeAndProcess_82B485C8).
[entrypoint.functions."0x82B48618"]
name = "DestructAndFreeStructure_82B48618"

# A processor that indexes a region and processes the data. It takes a structure (a: fields at a->36 and a->40) and a value (b). It reads a->36 (a count). When b is greater than the count it gets a singleton
# (GetSingleton_82BA8718), processes an indexed region (ProcessIndexedRegion2_82BA8F18, with b times 8, a, a .data pointer, and 141), zeroes memory (MemSet_SdkRuntime_82CA3190), and copies (MemCpy_SdkRuntime_82CA2C60).
# This is the "index this region and process the data" routine, part of the 0x82BAF process cluster. 88 lines. 5 callees (including ProcessIndexedRegion2, MemSet, and MemCpy). 5 callers (sub_82BAF110,
# sub_82BABEE8, sub_82BAC4E8, sub_82BAC658, and sub_82BAC750).
[entrypoint.functions."0x82BAF4B0"]
name = "ProcessAndIndexRegionAndProcess_82BAF4B0"

# A processor that computes and allocates the data. It takes a value (a). It computes (the unsigned -1 divided by a); when a is non-zero and the result is less than 12 it performs further processing
# (sub_822F1F00, with a local buffer). It then computes (a plus a times 2) times 4 (a size) and allocates (Allocate_OrDbgBreak_8221F388). This is the "compute and allocate this data" routine, part of the
# 0x82C49 process cluster. 70 lines. 2 callees (sub_822F1F00 and Allocate_OrDbgBreak). 4 callers (sub_82C3D6B0, sub_82C66AD8, sub_82C4AB00, and sub_82C3D4A0).
[entrypoint.functions."0x82C49908"]
name = "ProcessAndComputeAndAllocate_82C49908"

# A destructor that destructs and frees a range of objects. It takes a pointer (a) and an end pointer (b). It iterates over the range (a to b, 28-byte steps): for each element it reads the element's +24
# field (a count). When the count is at least 16 it frees the element's +4 field (Free_Thunk_824FE010). It then stores 15 into the element's +24, 0 into the element's +20, and 0 into the element's +4.
# This is the "destructor and free this range of objects" routine, part of the 0x82C63 destruct cluster. 57 lines. 1 callee (Free_Thunk). 5 callers (sub_82C66F88, sub_82C640D8, sub_82C670E0,
# ProcessAndConstructAndProcess_82C67C70, and sub_82C82FE8).
[entrypoint.functions."0x82C63580"]
name = "DestructAndFreeRange_82C63580"

# A processor that performs a range update (second form). It takes a pointer (a), an end pointer (b), and a structure (c: fields at c->0, c->16, and c->20). It initializes a local structure (field at +100
# to 0, +116 to 0, and +120 to 15). When a is not equal to b it iterates over the range: for each element it stores 15 into the element's +20, 0 into the element's +16, and 0 into the element's +0. It then
# performs a range update (RangeUpdate_8218EA38) and frees (Free_Thunk_824FE010). This is the "perform this range update" routine (second form), part of the 0x82C63 process cluster. 160 lines. 2 callees
# (RangeUpdate and Free_Thunk). 2 callers (sub_82C66F88 and sub_82C640D8).
[entrypoint.functions."0x82C63DB0"]
name = "ProcessAndRangeUpdate2_82C63DB0"

# A copier that copies 12-byte elements. It takes a structure (a: fields at a->16, a->20, and a->28), a value (b), and a value (c). It reads a->20 (a source pointer) and a->28 (an end pointer). When the source
# is not equal to the end it iterates over the range: for each element it copies the source's +0, +4, and +8 fields into the element's +0, +4, and +8 (12-byte elements). This is the "copy these 12-byte
# elements" routine, part of the 0x82C66 process cluster. 50 lines. 0 callees (leaf function). 1 caller (sub_82C66AD8).
[entrypoint.functions."0x82C66918"]
name = "CopyElements12Bytes_82C66918"

# A processor that copies and frees the data (second form). It takes a structure (a: fields at a->4 and a->12) and a value (b). It reads a->4 (a pointer). When the pointer is non-zero it computes (a->12
# minus a->4) divided by 12 (an element count). When b is less than the element count it copies the 12-byte elements (CopyElements12Bytes_82C66918) and computes and allocates (ProcessAndComputeAndAllocate_82C49908).
# When b exceeds a specific .data constant it performs further processing (sub_82BF8E78). This is the "copy and free this data" routine (second form), part of the 0x82C66 process cluster. 154 lines. 4
# callees (Free_Thunk, sub_82BF8E78, ProcessAndComputeAndAllocate, and CopyElements12Bytes). 1 caller (sub_82C67878).
[entrypoint.functions."0x82C66AD8"]
name = "ProcessAndCopyAndFree2_82C66AD8"

# A processor that constructs and frees the data. It takes a structure (a: fields at a->4 and a->12) and a value (b). It reads a->4 (a pointer). When the pointer is non-zero it computes (a->12 minus a->4)
# divided by 28 (an element count). When b is less than the element count it constructs an element array (ConstructElementArray_829FED20), destructs and frees a range (DestructAndFreeRange_82C63580), and
# performs a range update (ProcessAndRangeUpdate2_82C63DB0). When b exceeds a specific .data constant it performs further processing (sub_82BF8E78). This is the "construct and free this data" routine,
# part of the 0x82C66 process cluster. 147 lines. 5 callees (ConstructElementArray, DestructAndFreeRange, ProcessAndRangeUpdate2, and sub_82BF8E78). 1 caller (sub_82C67878).
[entrypoint.functions."0x82C66F88"]
name = "ProcessAndConstructAndFree_82C66F88"

# A processor that constructs and processes the data (second form). It takes a structure (a: fields at a->0, a->4, a->12, a->28, and a->32), a value (b), a value (c), a value (d), and a value (e). It
# initializes the structure: a->0 is set to a .data pointer, a->4 is set to 0, a->12 is set to 0, a->28 is set to 0, and a->32 is set to 7. It then clamps and updates (ClampAndUpdate_822F6380). It then
# constructs a linked node (ConstructLinkedNode_823445B0), hashes (FNV1Hash_CaseInsensitive_82448030), pushes elements (Array_Push12_82C66E00 and Array_Push28_82C644D0), allocates and constructs
# (ProcessAndAllocateAndConstruct2_82C66830), processes an object (ProcessObject_82C669C0), and frees (Free_Thunk_824FE010). This is the "construct and process this data" routine (second form), part of
# the 0x82C67 process cluster. 566 lines. 16 callees (including ConstructLinkedNode, FNV1Hash, and Allocate). 2 callers (ProcessAndConstructAndProcess_82C67C70 and sub_82C680F8).
[entrypoint.functions."0x82C67878"]
name = "ProcessAndConstructAndProcess2_82C67878"

# A processor that constructs and processes the data (third form). It takes a structure (a: field at a->0), a value (b), a value (c), and a value (d). When b is zero it stores 0 into a. When b is non-zero it
# performs a vcall via b's +0 and +24 entries (bctrl). When the result is at least 12 it constructs a linked node (ConstructLinkedNode_823445B0), assigns a reference-counted object (AssignRefCounted_822F54C8),
# copies and dispatches (CopyAndDispatch_822F5478), allocates and constructs (ProcessAndAllocateAndConstruct2_82C66830), and sets and dispatches (SetAndDispatch_82366210). This is the "construct and process
# this data" routine (third form), part of the 0x82C68 process cluster. 831 lines. 13 callees (including ConstructLinkedNode, AssignRefCounted, and Allocate_OrDbgBreak). 2 callers (sub_822F36B8 and
# sub_82C686B0).
[entrypoint.functions."0x82C680F8"]
name = "ProcessAndConstructAndProcess3_82C680F8"

# A processor that hashes and dispatches the data. It takes a value (a) and a value (b). It accesses an element (AccessElement8_82A1E620, with a). It then finds the first zero byte in b (a NUL-terminated
# string) and computes a hash (ComputeStringHash_82227EA0, with a, b, and the string's length minus 1). It then dispatches a typed value (ProcessAndDispatchTypedValue_822286D0). This is the "hash and
# dispatch this data" routine, part of the 0x82228 process cluster. 83 lines. 3 callees (AccessElement8, ComputeStringHash, and ProcessAndDispatchTypedValue). 12 callers (sub_825BCED0, sub_827F1DA0,
# sub_82A28EB0, sub_824EEE50, sub_824EF558, sub_8260C260, sub_82433400, sub_82BFA208, sub_82A294B0, sub_82902EC0, sub_822A28A0, and sub_82844750).
[entrypoint.functions."0x82228488"]
name = "ProcessAndHashAndDispatch_82228488"

# A processor that recurses and processes the data (second form). It takes a value (a), a value (b), a value (c), and a value (d). It reads two .data pointers (r27 and r29). It computes (r29 minus r27)
# shifted right by 3. When the result is greater than 32 it iterates: for each element it processes (sub_82A59C98, with a local buffer, a, b, and d) and recurses (sub_82B830E8, with itself). This is the
# "recurse and process this data" routine (second form), part of the 0x82B83 process cluster. 278 lines. 5 callees (sub_82A59C98, itself, sub_82B833E0, sub_82B83598, and sub_82B83728). 1 caller (sub_82B80D70).
[entrypoint.functions."0x82B830E8"]
name = "ProcessAndRecurseAndProcess2_82B830E8"

# A processor that sorts and processes the data. It takes a value (a), a value (b), a structure (c: field at c->0), and an end pointer (d). It reads c (a pointer) and d (an end pointer). When c is not equal
# to d it iterates over the range: for each element it reads the element's +0 field (a key) and compares it to the next element's +0 field. When the first key is greater than or equal to the second it stores
# the element's +8 into a local. It then processes (sub_82B83808). This is the "sort and process this data" routine, part of the 0x82B83 process cluster. 134 lines. 1 callee (sub_82B83808). 1 caller
# (ProcessAndRecurseAndProcess2_82B830E8).
[entrypoint.functions."0x82B833E0"]
name = "ProcessAndSortAndProcess_82B833E0"

# A processor that reads and masks the data. It takes a value (a). It reads or resolves a value (ProcessAndReadOrResolveValue_82BC6F00, with a). When the result is greater than 255 it performs further
# processing (sub_82BC67F0, with a, 1, and a .data pointer). It then returns the result masked to its low 8 bits (a byte). This is the "read and mask this data" routine, part of the 0x82BB6 process
# cluster. 54 lines. 2 callees (ProcessAndReadOrResolveValue and sub_82BC67F0). 2 callers (sub_82BB62E0 and sub_82BB5EB0).
[entrypoint.functions."0x82BB6880"]
name = "ProcessAndReadAndMask_82BB6880"

# A processor that validates and constructs the data. It takes a value (a) and a value (b). It reads a .data pointer's field (a flag). When the flag is zero it stores 21 into a global field
# (SetGlobalField_Thunk_82CC0750) and returns -1. When the flag is non-zero it validates bounds (ValidateBounds4_82BEC300 and ProcessAndValidateBounds_82BEC640), constructs an object
# (ProcessAndConstructObject2_82BED1B0), and aborts (ProcessAndAbort_82170CC8). This is the "validate and construct this data" routine, part of the 0x82BED process cluster. 347 lines. 6 callees (including
# ValidateBounds, ProcessAndConstructObject2, and ProcessAndAbort). 2 callers (sub_823737D0 and sub_82374CA0).
[entrypoint.functions."0x82BED548"]
name = "ProcessAndValidateAndConstruct_82BED548"

# A processor that dispatches and processes the data (second form). It takes a value (a), a pointer (b), a value (c), and a value (d). When b equals c it returns -1. Otherwise it reads b's first byte,
# adds a to it, reads the result's +76 field (a byte), and subtracts 5. When the result is at most 24 it performs a table lookup (a .data pointer indexed by the result times 4) and dispatches (bctr).
# This is the "dispatch and process this data" routine (second form), part of the 0x82C8A process cluster. 501 lines. 0 callees (leaf function). 1 caller (ProcessAndDispatchIndex_82C8AF48).
[entrypoint.functions."0x82C8A210"]
name = "ProcessAndDispatchAndProcess2_82C8A210"

# A processor that appends and resolves the data (second form). It takes a structure (a: field at a->0), a value (b), and a value (c). It reads a->0 (a pointer). When the pointer is non-zero it reads the
# pointer's +0 and +4 fields and appends an element (ProcessAndAppendElement_82229208). It then dispatches and resolves (ProcessAndDispatchAndResolve_822279A0), resolves a typed field
# (ProcessAndResolveTypedField_8219AB28), hashes a string (ProcessAndHashString_82A1EC08), and copies a reference (ProcessAndCopyReferenceAndHash_824EF160). This is the "append and resolve this data"
# routine (second form), part of the 0x82358 process cluster. 89 lines. 5 callees (including ProcessAndAppendElement, ProcessAndDispatchAndResolve, and ProcessAndHashString). 9 callers (sub_824FA760,
# sub_82549CB8, sub_823944E0, sub_825ACEA0, sub_823A1980, sub_825207F0, sub_823ADF80, sub_82908E20, and sub_823577C0).
[entrypoint.functions."0x823588E8"]
name = "ProcessAndAppendAndResolve2_823588E8"

# A processor that initializes and processes the data (second form). It reads a .data pointer's field (a flag). When the flag's low bit is clear it sets the flag's low bit and processes (sub_82C645A8,
# with a local buffer). It then processes (sub_82369F88, with a .data pointer and the local buffer's first field). This is the "initialize and process this data" routine (second form), part of the
# 0x82369 process cluster. 97 lines. 3 callees (sub_82369F88, sub_82C645A8, and sub_82CA3700). 12 callers (sub_822F27C0, sub_822F3B18, sub_82374B38, sub_82198860, sub_823754C8, sub_823417D0,
# sub_822F4458, ProcessAndCompareAndConstruct_822F3D20, sub_82374808, sub_823726F8, sub_82185080, and sub_825F3050).
[entrypoint.functions."0x82369EE0"]
name = "ProcessAndInitializeAndProcess2_82369EE0"

# A processor that constructs and processes the data (third form). It takes a value (a) and a value (b). It initializes and processes (ProcessAndInitializeAndProcess_82375100, with a and b). It then
# constructs a string object (ConstructStringObject_822F21B8). It then constructs a reference-counted object (ConstructRefCounted_8222CF18), loads a string (LoadString_FastOrSlow_823725E8), pushes if
# changed (ProcessAndPushIfChanged_82375270), destructs (Destruct3_822F5208 and DestructRefCounted_82214F08), releases a reference (Release_RefCounted_821C67D8), and frees (Free_SizeBucketed_Thunk_8221BE68).
# This is the "construct and process this data" routine (third form), part of the 0x82372 process cluster. 863 lines. 11 callees (including ConstructStringObject, ConstructRefCounted, and Free).
# 1 caller (sub_82372E88).
[entrypoint.functions."0x823728E0"]
name = "ProcessAndConstructAndProcess3_823728E0"

# A processor that constructs and processes the data (fourth form). It takes a structure (a: field at a->0) and a value (b). It reads a->0 (a pointer). When the pointer is non-zero it reads the
# pointer's +0 field; otherwise it reads a .data pointer and adds 63. It then validates and constructs (ProcessAndValidateAndConstruct_82BED548, with the value, a .data pointer, and 85). When the
# result is positive and a .data pointer's field is non-zero it constructs a 16-byte object (Construct16Byte_82275368), constructs a reference-counted object (ConstructRefCounted_8222CF18), and
# destructs (DestructRefCounted_82214F08). This is the "construct and process this data" routine (fourth form), part of the 0x82374 process cluster. 106 lines. 5 callees (including
# ProcessAndValidateAndConstruct and ConstructRefCounted). 3 callers (sub_8238E7D8, ProcessAndConstructAndProcess3_823728E0, and sub_825AB7C0).
[entrypoint.functions."0x82374CA0"]
name = "ProcessAndConstructAndProcess4_82374CA0"

# A processor that finds and processes the data. It takes a value (a), a structure (b: fields at b->4 and b->8), a value (c), and a value (d). It reads b->8 (a pointer). When the pointer is zero it
# processes (sub_82377340, with b, c, d, and 1). When the pointer is non-zero it reads b->4 (a count) and finds the root of a chain (FindChainRoot3_82A962B0 and FindChainRoot4_828836E8). This is the
# "find and process this data" routine, part of the 0x82376 process cluster. 306 lines. 4 callees (FindChainRoot3, FindChainRoot4, sub_82376DB0, and sub_82377340). 1 caller (SearchTreeRange_82375AB8).
[entrypoint.functions."0x82376358"]
name = "ProcessAndFindAndProcess_82376358"

# A processor that splices and processes the data. It takes a value (a), a structure (b: field at b->8), a value (c), a value (d), and a value (e). It reads b->8 (a count). When the count is at
# least a specific .data constant it sets a string (SetString_822F2020, with a local buffer and a .data pointer). It then splices a node (List_SpliceNode2_8292CC88 and List_SpliceNode3_8234D600),
# allocates (Allocate_OrDbgBreak_8221F388), and performs further processing (sub_826C3EF0 and sub_826C3FA8). This is the "splice and process this data" routine, part of the 0x82377 process cluster.
# 349 lines. 9 callees (including List_SpliceNode, SetString, and Allocate_OrDbgBreak). 2 callers (ProcessAndFindAndProcess_82376358 and sub_82376DB0).
[entrypoint.functions."0x82377340"]
name = "ProcessAndSpliceAndProcess_82377340"

# A processor that performs a vcall and processes the data (third form). It takes a value (a, a count), a value (b, a pointer), and a structure (c: field at c->0). It iterates a times: for each
# element it reads c->0 (a pointer). When the pointer is non-zero it stores it into the element's first field and performs a vcall via the pointer's +0 and +4 entries (bctrl). When the pointer is zero it
# stores zero. It then increments the element by 4 and decrements the count. This is the "perform a vcall and process this data" routine (third form), part of the 0x825F4 process cluster. 69 lines.
# 0 callees (leaf function). 3 callers (sub_822F7B70, sub_825F4EA0, and sub_822F7870).
[entrypoint.functions."0x825F4E30"]
name = "ProcessAndVcallAndProcess3_825F4E30"

# A processor that dispatches and processes the data (third form). It takes a structure (a: field at a->8), a value (b), and a value (c). It accesses an element (AccessElement8_82A1E620, with a). It
# then reads the element's +0 and +4 fields and stores them into a->8's +0 and +4. It then searches for a character (FindChar_82CA3980, with b and 46), computes a hash (ComputeStringHash_82227EA0),
# dispatches a value type (DispatchValueType_822285D8), and processes and dispatches by type (ProcessDispatchByType_82228E28). This is the "dispatch and process this data" routine (third form), part
# of the 0x82A1F process cluster. 319 lines. 8 callees (including AccessElement8, FindChar, and ProcessDispatchByType). 3 callers (sub_82A1F740, sub_82A298E8, and sub_82A294B0).
[entrypoint.functions."0x82A1FA08"]
name = "ProcessAndDispatchAndProcess3_82A1FA08"

# A processor that computes and processes the data (second form). It takes a value (a), a value (b), a value (c), a value (d), and a value (e). It reads d (an end pointer) and e (a start pointer). It
# computes (e minus d) shifted right by 3, then shifted right by 1 (rounded up), and computes the midpoint (d plus the result times 8). It then processes (sub_82A5A068 and sub_82A5A588). This is the
# "compute and process this data" routine (second form), part of the 0x82A59 process cluster. 407 lines. 2 callees (sub_82A5A068 and sub_82A5A588). 2 callers (ProcessAndRecurseAndProcess2_82B830E8
# and sub_82A59940).
[entrypoint.functions."0x82A59C98"]
name = "ProcessAndComputeAndProcess2_82A59C98"

# A processor that computes and processes the data (third form). It takes a value (a), a value (b), a value (c), a value (d), a value (e), and a value (f). It reads e (a start pointer) and f (an end
# pointer). It computes (f minus e) shifted right by 3. When the result is greater than 40 it adds 1, shifts right by 3 (rounded up), and computes a size (the result times 8). It then swaps and processes
# (ProcessAndSwapAndProcess_82B834D0). This is the "compute and process this data" routine (third form), part of the 0x82A5A process cluster. 159 lines. 1 callee (ProcessAndSwapAndProcess). 1 caller
# (ProcessAndComputeAndProcess2_82A59C98).
[entrypoint.functions."0x82A5A068"]
name = "ProcessAndComputeAndProcess3_82A5A068"

# A comparator that compares strings and returns a value. It takes a structure (a: fields at a->0 and a->4) and a structure (b: fields at b->0 and b->4). It reads a->4 (a length) and b->4 (a length).
# When the lengths differ it returns 0. When the length is zero it returns 1. When the length is non-zero it compares the strings byte-by-byte (a->0 and b->0); when the second string is null it
# compares against a .data pointer. This is the "compare these strings and return this value" routine, part of the 0x82BA8 process cluster. 65 lines. 0 callees (leaf function). 4 callers (sub_82BA8AC8,
# sub_82BC48F8, sub_82BB3C70, and sub_82BC1018).
[entrypoint.functions."0x82BA8B40"]
name = "CompareStringsAndReturnValue_82BA8B40"

# A processor that performs a vcall and processes the data (fourth form). It takes a structure (a: fields at a->4 and a->32), a value (b), a value (c), and a value (d). It reads a->4 (a pointer) and the
# pointer's +0 field. It performs a vcall via the +0 field's +32 entry (bctrl). When b is non-zero it performs another vcall via the +0 field's +76 entry (bctrl). It then checks the pointer's low 8 bits
# (a flag); when the flag is non-zero it reads a->32. This is the "perform a vcall and process this data" routine (fourth form), part of the 0x82BB0 process cluster. 152 lines. 0 callees (leaf function).
# 0 callers (unresolved — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x82BB0458"]
name = "ProcessAndVcallAndProcess4_82BB0458"

# A sorter that sorts and recurses the data. It takes a pointer (a), an end pointer (b), a count (c), and a value (d). It computes (b minus a) shifted right by 2. When the result is greater than 32 it
# iterates: for each element it sorts (sub_82C2B850, with a local buffer, a, b, and d) and recurses (sub_82C2C520, with itself). This is the "sort and recurse this data" routine, part of the 0x82C2C
# process cluster. 176 lines. 5 callees (sub_82C2B850, sub_82C2BB20, sub_82C2BC08, sub_82C2C1E8, and itself). 1 caller (ProcessAndSortAndCheck_82C2C958).
[entrypoint.functions."0x82C2C520"]
name = "SortAndRecurse_82C2C520"

# A processor that performs a vcall and returns the data. It takes a structure (a: field at a->60) and a value (b). It stores b into a local. It then reads a->60 (a vtable pointer) and performs a vcall
# (bctrl). When the result is non-zero it returns the result's low 8 bits (sign-extended); otherwise it returns -1. This is the "perform a vcall and return this data" routine, part of the 0x82C97
# process cluster. 63 lines. 0 callees (leaf function). 2 callers (sub_82C97458 and sub_82C971B8).
[entrypoint.functions."0x82C970B0"]
name = "ProcessAndVcallAndReturn_82C970B0"

# A processor that searches and returns the data. It takes a structure (a: field at a->4) and a value (b). It reads a->4 (a pointer) and the pointer's +4 field. It then searches a tree: it reads the
# tree node's +33 field (a flag). When the flag is zero it reads b's +0 field (a key) and walks the tree (comparing the node's +12 field to the key, and following the node's +8 or +0 fields). This is the
# "search and return this data" routine, part of the 0x822F5 process cluster. 87 lines. 0 callees (leaf function). 12 callers (sub_828919F0, sub_8288E478, sub_826167A0, sub_82892198, sub_82891B48,
# sub_823754C8, sub_82891E10, sub_8288E310, sub_822F4578, sub_822F4458, sub_828C78B8, and sub_82616458).
[entrypoint.functions."0x822F5BC8"]
name = "ProcessAndSearchAndReturn_822F5BC8"

# A processor that computes and processes the data (fourth form). It takes a structure (a: fields at a->4, a->8, and a->12) and a value (b). It reads a->4 (a pointer). When the pointer is non-zero it
# computes (a->8 minus the pointer) shifted right by 2, and (a->12 minus the pointer) shifted right by 2. When the first result is less than the second it processes (sub_822F6A88). This is the "compute and
# process this data" routine (fourth form), part of the 0x822F6 process cluster. 121 lines. 1 callee (sub_822F6A88). 5 callers (sub_82B4B2B8, sub_822F36B8, sub_8236A058, sub_82374808, and sub_824E9258).
[entrypoint.functions."0x822F6188"]
name = "ProcessAndComputeAndProcess4_822F6188"

# A processor that computes and processes the data (fifth form). It takes a structure (a: field at a->4), a structure (b: fields at b->4 and b->8), and a value (c). It stores c into a local. It then reads
# b->4 (a pointer). When the pointer is non-zero it reads b->8 (an end pointer) and computes (b->8 minus the pointer) shifted right by 2. When the result is non-zero and the pointer is less than the end
# pointer it processes (sub_822F7870). This is the "compute and process this data" routine (fifth form), part of the 0x822F6 process cluster. 132 lines. 1 callee (sub_822F7870). 1 caller
# (ProcessAndComputeAndProcess4_822F6188).
[entrypoint.functions."0x822F6A88"]
name = "ProcessAndComputeAndProcess5_822F6A88"

# A processor that performs a vcall and processes the data (sixth form). It takes a pointer (a) and an end pointer (b). It iterates from a to b: for each element it reads the element's first field (a
# pointer). When the pointer is non-zero it performs a vcall via the pointer's +0 and +8 fields (bctrl). This is the "perform a vcall and process this data" routine (sixth form), part of the 0x822F6
# process cluster. 61 lines. 0 callees (leaf function). 11 callers (sub_822F5E78, sub_822F5E20, sub_822F4458, sub_822F6328, sub_822F8400, sub_824E92D8, sub_8236A170, sub_832A0800, sub_82B4B2B8,
# sub_822F7870, and sub_822F5780).
[entrypoint.functions."0x822F6B68"]
name = "ProcessAndVcallAndProcess6_822F6B68"

# A processor that performs a vcall and processes the data (seventh form). It takes a structure (a: fields at a->4 and a->12), a structure (b: field at b->0), and a value (c). It reads b->0 (a pointer).
# When the pointer is non-zero it performs a vcall via the pointer's +0 and +4 fields (bctrl). It then reads a->4 (a pointer); when the pointer is non-zero it reads a->12 (an end pointer) and computes (a->12
# minus the pointer) shifted right by 2. This is the "perform a vcall and process this data" routine (seventh form), part of the 0x822F7 process cluster. 445 lines. 7 callees (including sub_822F6B68 and
# sub_825F4E30). 1 caller (ProcessAndComputeAndProcess5_822F6A88).
[entrypoint.functions."0x822F7870"]
name = "ProcessAndVcallAndProcess7_822F7870"

# A processor that appends and resolves the data (third form). It takes a structure (a: field at a->0), a value (b), and a value (c). It reads a->0 (a pointer). When the pointer is non-zero it reads the
# pointer's +0 field (a value) and the +4 field (a count). It then appends an element (ProcessAndAppendElement_82229208, with the value, -10000, and the count), dispatches and resolves (ProcessAndDispatch
# AndResolve_822279A0, with the value and 4), and stores c into the resolved value's first field. This is the "append and resolve this data" routine (third form), part of the 0x8260D process cluster.
# 89 lines. 5 callees (including ProcessAndAppendElement, ProcessAndDispatchAndResolve, and ProcessAndResolveTypedField). 3 callers (sub_82903620, sub_8260C3B8, and sub_8290B948).
[entrypoint.functions."0x8260D230"]
name = "ProcessAndAppendAndResolve3_8260D230"

# A processor that computes and processes the data (seventh form). It takes a structure (a: field at a->4), a structure (b: fields at b->4 and b->8), and a value (c). It stores c into a local. It then
# reads b->4 (a pointer). When the pointer is non-zero it reads b->8 (an end pointer) and computes (b->8 minus the pointer) divided by 36. When the result is non-zero and the pointer is less than the end
# pointer it processes (sub_82B42518). This is the "compute and process this data" routine (seventh form), part of the 0x82B42 process cluster. 136 lines. 1 callee (sub_82B42518). 1 caller
# (LockedProcessContainer_82B412B8).
[entrypoint.functions."0x82B422A8"]
name = "ProcessAndComputeAndProcess7_82B422A8"

# A processor that constructs and processes the data (fifth form). It takes a count (a), a pointer (b), and a value (c). It iterates a times: for each element it constructs (Construct10_82B42918, with the
# element at b plus the offset and c). It then increments the offset by 36. This is the "construct and process this data" routine (fifth form), part of the 0x82B42 process cluster. 63 lines. 1 callee
# (Construct10). 1 caller (sub_82B42518).
[entrypoint.functions."0x82B42390"]
name = "ProcessAndConstructAndProcess5_82B42390"

# A processor that constructs and processes the data (sixth form). It takes a structure (a: fields at a->4, a->8, and a->12) and a value (b). It stores b into a local. It then reads a->4 (a pointer); when
# the pointer is non-zero it reads a->12 (an end pointer) and computes (a->12 minus the pointer) divided by 36. It then reads a->8 (a start pointer) and computes (the pointer minus a->8). This is the
# "construct and process this data" routine (sixth form), part of the 0x82B42 process cluster. 516 lines. 8 callees (including ClampAndUpdate, Construct10, and Destruct5). 1 caller
# (ProcessAndComputeAndProcess7_82B422A8).
[entrypoint.functions."0x82B42518"]
name = "ProcessAndConstructAndProcess6_82B42518"

# A processor that computes and processes the data (eighth form). It takes a value (a). When a is non-zero it computes (-1 divided by a) (a signed division). When the result is less than 36 it allocates
# (Allocate_OrDbgBreak_8221F388) and processes (sub_822F1F00). This is the "compute and process this data" routine (eighth form), part of the 0x82B42 process cluster. 70 lines. 2 callees
# (Allocate_OrDbgBreak and sub_822F1F00). 6 callers (sub_82B42518, sub_82A00EC0, sub_829CE3E0, sub_8247DCC8, sub_8246EA40, and sub_8246E9C0).
[entrypoint.functions."0x82B428A0"]
name = "ProcessAndComputeAndProcess8_82B428A0"

# A processor that validates and stores the data (second form). It takes a value (a) and a structure (b: fields at b->4, b->20, and b->24). It reads b->24 (a count). When the count is at least 16 it reads
# b->4 (a pointer); otherwise it uses the address of b->4. It then reads b->20 (an offset) and adds it to the pointer. This is the "validate and store this data" routine (second form), part of the
# 0x8236D process cluster. 54 lines. 1 callee (ValidateAndStorePointer_8236F008). 2 callers (sub_82C69318 and sub_82BF69A0).
[entrypoint.functions."0x8236DF68"]
name = "ProcessAndValidateAndStore2_8236DF68"

# A processor that validates and stores the data (third form). It takes a value (a) and a structure (b: fields at b->4 and b->24). It reads b->24 (a count). When the count is at least 16 it reads b->4
# (a pointer); otherwise it uses the address of b->4. This is the "validate and store this data" routine (third form), part of the 0x826C0 process cluster. 50 lines. 1 callee
# (ValidateAndStorePointer_8236F008). 2 callers (sub_82BF69A0 and sub_82C69318).
[entrypoint.functions."0x826C09B8"]
name = "ProcessAndValidateAndStore3_826C09B8"

# A processor that constructs and processes the data (seventh form). It takes a value (a) and a structure (b: field at b->0). It reads b->0 (a pointer). When the pointer is non-zero it reads the pointer's
# +4 field (a count). It then clears the fields (ProcessAndClearFields_82A1D980, with a local buffer and the count plus 1). This is the "construct and process this data" routine (seventh form), part of
# the 0x82B3A process cluster. 152 lines. 4 callees (including ConstructRefCounted and ProcessAndClearFields). 2 callers (sub_822F38B0 and sub_82B3AEA0).
[entrypoint.functions."0x82B3ADA0"]
name = "ProcessAndConstructAndProcess7_82B3ADA0"

# An initializer that initializes and processes the data. It takes a structure (a: fields at a->0, a->4, ..., a->80). It zeros out the first 88 bytes of the structure (a->0 through a->80). It then reads
# a .data pointer's +28560 field (a counter) and increments it by 64. This is the "initialize and process this data" routine, part of the 0x82BA8 process cluster. 88 lines. 0 callees (leaf function).
# 1 caller (sub_82BA8BB0).
[entrypoint.functions."0x82BA8CE8"]
name = "InitializeAndProcess_82BA8CE8"

# A processor that shifts and processes the data. It takes a structure (a: fields at a->16 and a->28) and a value (b). It calls sub_82BDDCC8 (with a and 285). It then reads a->16 (a pointer) and shifts
# the fields (ProcessAndShiftFields_82BCF7D8, with a and the pointer). It then reads a->28 (a value) and processes (sub_82BDE160, with the value, the pointer, b, and 1). This is the "shift and process
# this data" routine, part of the 0x82BDE process cluster. 70 lines. 4 callees (ProcessAndShiftFields, sub_82BDDCC8, sub_82BDE160, and sub_82BE1C40). 2 callers (sub_82BDF510 and sub_82BE12E8).
[entrypoint.functions."0x82BDE2A8"]
name = "ProcessAndShiftAndProcess_82BDE2A8"

# A processor that increments and processes the data (second form). It takes a structure (a: fields at a->4, a->32, and a->44). It reads a->32 (a pointer). It then reads the pointer's +52 field (a
# count, a halfword) and increments it. When the count is greater than 200 it reads a->44 (a pointer) and processes (sub_82A2ED70, with a local buffer, a pointer plus 16, and 80). This is the
# "increment and process this data" routine (second form), part of the 0x82BDE process cluster. 79 lines. 3 callees (sub_82A2ED70, sub_82BC8870, and sub_82BCCB88). 2 callers (sub_82BE1720 and
# sub_82BDFEB8).
[entrypoint.functions."0x82BDE440"]
name = "ProcessAndIncrementAndProcess2_82BDE440"

# A processor that dispatches and processes the data (fourth form). It takes a structure (a: fields at a->4, a->12, and a->28) and a value (b). It reads a->12 (a type tag). When the type tag is 40 it
# reads a->4 (a pointer) and shifts the fields (ProcessAndShiftFields_82BCF7D8, with a and the pointer). When the type tag is 285 it shifts and processes (ProcessAndShiftAndProcess_82BDE2A8, with a and
# b). When the type tag is neither it dispatches by value (ProcessAndDispatchByValue_82BE1F00). This is the "dispatch and process this data" routine (fourth form), part of the 0x82BDF process cluster.
# 748 lines. 15 callees (including ProcessAndDispatchByValue, ProcessAndShiftFields, and ProcessAndShiftAndProcess). 3 callers (sub_82BE1500, sub_82BDFA98, and sub_82BE02F8).
[entrypoint.functions."0x82BDF510"]
name = "ProcessAndDispatchAndProcess4_82BDF510"

# A processor that constructs and processes the data (ninth form). It takes a structure (a: field at a->0), a structure (b: field at b->0), a value (c), and a value (d). When b is non-zero it reads b->0
# (a pointer) and performs a vcall via the pointer's +16 entry (bctrl). It then reads the vcall's result (from a local buffer). This is the "construct and process this data" routine (ninth form), part of
# the 0x82C68 process cluster. 97 lines. 1 callee (ProcessAndConstructAndProcess3_82C680F8). 3 callers (sub_822F27C0, sub_82C07AB0, and sub_82C6CE00).
[entrypoint.functions."0x82C686B0"]
name = "ProcessAndConstructAndProcess9_82C686B0"

# A processor that validates and stores the data (fourth form). It takes a structure (a: fields at a->4, a->20, and a->24) and a value (b). It validates and stores (ProcessAndValidateAndStore2_8236DF68
# and ProcessAndValidateAndStore3_826C09B8, with local buffers and b). It then stores 7 into a->24, 0 into a->20, and 0 into a->4. This is the "validate and store this data" routine (fourth form), part
# of the 0x82C69 process cluster. 73 lines. 4 callees (ProcessElementAccess3, ProcessAndValidateAndStore2, ProcessAndValidateAndStore3, and sub_826C3908). 1 caller (sub_82C69570).
[entrypoint.functions."0x82C69318"]
name = "ProcessAndValidateAndStore4_82C69318"

# A processor that resolves and writes the data (second form). It takes a value (a) and a structure (b: field at b->4). It initializes a local structure (15 into a field, 0 into another, 2 into a flag). It
# then writes a range (WriteRange_AtPos_821A8F68, with a local buffer, a .data pointer, and 2) and resolves and writes (ProcessAndResolveAndWrite_82C694C0, with another local buffer, the local structure,
# and b->4). This is the "resolve and write this data" routine (second form), part of the 0x82C69 process cluster. 404 lines. 7 callees (including WriteRange_AtPos, ProcessAndResolveAndWrite, and
# ProcessAndClampGrow). 0 callers (unresolved — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x82C69570"]
name = "ProcessAndResolveAndWrite2_82C69570"

# A processor that constructs and processes the data (tenth form). It takes a structure (a: field at a->0) and a structure (b: fields at b->76, b->80, and b->116). It reads b->76 (a pointer), b->80 (an
# offset), and b->116 (a value). It then constructs and processes (ProcessAndConstructAndProcess9_82C686B0, with a local buffer, the pointer, the offset plus 80, and the value). When the result is
# non-zero it performs a vcall via the result's +0 and +4 fields (bctrl). This is the "construct and process this data" routine (tenth form), part of the 0x82C6C process cluster. 76 lines. 1 callee
# (ProcessAndConstructAndProcess9). 0 callers (unresolved — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x82C6CE00"]
name = "ProcessAndConstructAndProcess10_82C6CE00"

# A processor that constructs and processes the data (eleventh form). It takes a structure (a: fields at a->0 and a->76), a value (b), a value (c), a structure (d: field at d->0), a value (e), a value (f),
# a value (g), and a value (h). It calls sub_82C6F2E0 (with b, c, d, e, f, g, h). It then stores a .data pointer into a->0 and d into a->76. When d is non-zero it performs a vcall via d's +0 and +4
# fields (bctrl). This is the "construct and process this data" routine (eleventh form), part of the 0x82C6F process cluster. 113 lines. 3 callees (ClampAndUpdate, sub_82C6EB30, and sub_82C6F2E0). 1
# caller (ProcessAndResolveAndWrite2_82C69570).
[entrypoint.functions."0x82C6F3F0"]
name = "ProcessAndConstructAndProcess11_82C6F3F0"

# A processor that reads the timebase and returns the data. It takes a structure (a: fields at a->8, a->16, and a->24). It reads a->24 (a flag). When the flag is non-zero it reads the timebase
# (GetTimebase_8221EB58, with a local buffer). It then reads a->16 (a denominator) and a->8 (an offset), computes (the timebase times a .data constant) divided by the denominator, and subtracts the
# offset. This is the "read the timebase and return this data" routine, part of the 0x822BE process cluster. 67 lines. 1 callee (GetTimebase). 0 callers (unresolved — body is recovered but no callers
# found; likely a vtable method or indirect entry).
[entrypoint.functions."0x822BE4B8"]
name = "ProcessAndTimebaseAndReturn_822BE4B8"

# A processor that performs a vcall and returns the data (second form). It takes a structure (a: field at a->0). It reads a->0 (a pointer) and performs a vcall via the pointer's +20 entry (bctrl). It
# then converts the vcall's result (a double) to an integer (via fcfid and fctiwz, with a .data pointer). This is the "perform a vcall and return this data" routine (second form), part of the 0x822BE
# process cluster. 49 lines. 0 callees (leaf function). 0 callers (unresolved — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x822BE530"]
name = "ProcessAndVcallAndReturn2_822BE530"

# A comparator that compares and processes the data. It takes a structure (a: field at a->0). It reads a .data pointer's +23408 field (a pointer). When the pointer is zero it reads another .data
# pointer (3224) and compares the strings byte-by-byte (a->0 and the pointer). It then computes the leading zeros of the comparison result. This is the "compare and process this data" routine, part
# of the 0x82B3A process cluster. 127 lines. 5 callees (ConstructRefCounted, CopyAssign_RefCounted2, CopyAssign_RefCounted, DestructRefCounted, and StrCmp_Signed). 5 callers (sub_82B3C358,
# sub_82B3AF28, sub_82B3C9D0, sub_82B3AEA0, and sub_82B4A230).
[entrypoint.functions."0x82B3ACC0"]
name = "CompareAndProcess_82B3ACC0"

# A processor that constructs and processes the data (twelfth form). It takes a value (a). It constructs and processes (ProcessAndConstructAndProcess7_82B3ADA0, with a). It then constructs a lower
# string (ProcessAndConstructLowerString_82B40708, with a local buffer and a) and compares (CompareAndProcess_82B3ACC0, with another local buffer). It then destructs and copy-assigns
# (DestructRefCounted_82214F08 and CopyAssign_RefCounted2_82265160). This is the "construct and process this data" routine (twelfth form), part of the 0x82B3A process cluster. 81 lines. 6 callees
# (including ProcessAndConstructAndProcess7, ProcessAndConstructLowerString, and CompareAndProcess). 12 callers (sub_82B3C898, sub_8233AE50, sub_82B3B110, sub_825B4CC8, sub_82460F70, sub_823A5498,
# sub_824155C8, sub_825F3D58, sub_82B3C4F8, sub_825F45A8, sub_823ADB60, and sub_823FE828).
[entrypoint.functions."0x82B3AEA0"]
name = "ProcessAndConstructAndProcess12_82B3AEA0"

# A processor that finds a substring and processes the data. It takes a structure (a: field at a->0), a value (b), and a value (c). It stores 0 into a->0. It then increments a global counter
# (via lwarx/stwcx with a global lock, with a .data pointer's +28344 field). It then copy-assigns a reference (CopyAssign_RefCounted_821F0108, with a local buffer and c). This is the "find a
# substring and process this data" routine, part of the 0x82B3A process cluster. 240 lines. 8 callees (including CopyAssign_RefCounted, DestructRefCounted, and FindSubstring). 4 callers
# (sub_82B3AF28, sub_82B3C9D0, ProcessAndConstructAndProcess12_82B3AEA0, and sub_82B4A230).
[entrypoint.functions."0x82B3AF80"]
name = "ProcessAndFindSubstringAndProcess_82B3AF80"

# A getter that returns the data. It takes a structure (a: field at a->200). It reads a->200 (a value) and returns it. This is the "get this field" routine, part of the 0x82B56 process cluster.
# 8 lines. 0 callees (leaf function). 0 callers (unresolved — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x82B56838"]
name = "GetField200_82B56838"

# A processor that compares and processes the data. It takes a value (a) and a value (b). It compares the strings (CompareStringsAndReturnValue_82BA8B40, with a and b). When the comparison
# returns zero it reads b's +0 field (a pointer) and the +4 field (a length, a halfword); when the pointer is zero it uses a .data pointer (3224). This is the "compare and process this data"
# routine, part of the 0x82BA8 process cluster. 69 lines. 2 callees (CompareStringsAndReturnValue and StringInsert). 9 callers (sub_82BBCD00, sub_82BBD7B8, sub_82BAA048, sub_82BC0D30,
# sub_82BB0D78, sub_82BAA238, ProcessAndReadAndProcess_82BB7100, sub_82BB1398, and sub_82BAA310).
[entrypoint.functions."0x82BA8AC8"]
name = "ProcessAndCompareAndProcess_82BA8AC8"

# A getter that returns the data. It takes a structure (a: field at a->28). It reads a->28 (a value) and returns it. This is the "get this field" routine, part of the 0x82BAA process cluster.
# 8 lines. 0 callees (leaf function). 1 caller (sub_82C886F0).
[entrypoint.functions."0x82BAA130"]
name = "GetField28_82BAA130"

# An offsetter that adds an offset to the data. It takes a value (a). It adds 592 to a and returns it. This is the "add this offset" routine, part of the 0x82BAA process cluster. 8 lines. 0
# callees (leaf function). 0 callers (unresolved — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x82BAA160"]
name = "AddOffset592_82BAA160"

# A processor that performs a vcall and clears the data. It takes a structure (a: field at a->4). It reads a->4 (a pointer). When the pointer is non-zero it reads a .data pointer's +19052 field
# (another pointer) and performs a vcall (bctrl). It then stores 0 into a->4. This is the "perform a vcall and clear this data" routine, part of the 0x82BBC process cluster. 47 lines. 0 callees
# (leaf function). 0 callers (unresolved — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x82BBC828"]
name = "ProcessAndVcallAndClear_82BBC828"

# A processor that resolves and writes the data (third form). It takes a value (a), a structure (b: field at b->4), a value (c), and a value (d). It initializes a local structure (15 into a field,
# 0 into another, 2 into a flag). It then writes a range (WriteRange_AtPos_821A8F68, with a local buffer, a .data pointer, and 2) and resolves and writes (ProcessAndResolveAndWrite_82C694C0, with
# another local buffer, the local structure, and b->4). This is the "resolve and write this data" routine (third form), part of the 0x82C6B process cluster. 380 lines. 11 callees (including
# WriteRange_AtPos, ProcessAndResolveAndWrite, and ProcessAndClampGrow). 4 callers (sub_82C6B640, sub_82C6C1B0, sub_82C6B630, and sub_82C6BB68).
[entrypoint.functions."0x82C6B390"]
name = "ProcessAndResolveAndWrite3_82C6B390"

# A processor that validates and processes the data. It takes a structure (a: fields at a->12 and a->16) and a structure (b: field at b->20). It reads b->20 (a pointer). When the pointer is non-zero it
# reads a->12 (a pointer) and processes (sub_82C6D518, with a local buffer and a pointer plus 12). It then validates the result (checking that it matches a->16 and the pointer plus 12). This is the
# "validate and process this data" routine, part of the 0x82C6D process cluster. 94 lines. 1 callee (sub_82C6D518). 3 callers (sub_82C6AAB8, ProcessAndResolveAndWrite3_82C6B390, and sub_82C6D710).
[entrypoint.functions."0x82C6D670"]
name = "ProcessAndValidateAndProcess_82C6D670"

# A processor that updates a range and processes the data (third form). It takes a structure (a: fields at a->0, a->8, a->24, a->28, and a->32), a value (b), a value (c), and a value (d). It stores a
# .data pointer into a->0, 15 into a->28, 0 into a->8, and 0 into a->24. It then updates a range (RangeUpdate_8218EA38, with a pointer plus 4, 0, -1, and b). This is the "update a range and process
# this data" routine (third form), part of the 0x82C6F process cluster. 92 lines. 2 callees (RangeUpdate and sub_82C6EB30). 2 callers (ProcessAndResolveAndWrite3_82C6B390 and sub_82C6BEB0).
[entrypoint.functions."0x82C6F1F0"]
name = "ProcessAndRangeUpdate3_82C6F1F0"

# An allocator that allocates and increments the counter. It takes no arguments. It reads a .data pointer's +22692 field (a counter) and increments it. It then allocates (Allocate_OrDbgBreak_8221F388).
# This is the "allocate and increment this counter" routine, part of the 0x82C87 allocate cluster. 15 lines. 1 callee (Allocate_OrDbgBreak). 12 callers (sub_82C87698, sub_82C88678, sub_82C88628,
# HashTableLookup_82C822D8, sub_82C886F0, sub_82C81ED0, sub_82C87708, sub_82C88390, sub_82C87348, sub_82C81D90, sub_82C820B0, and ProcessAndGrowHashTable_82C82220).
[entrypoint.functions."0x82C87A50"]
name = "AllocateAndIncrementCounter_82C87A50"

# An offsetter that adds an offset to the data. It takes a value (a). It adds 8 to a and returns it. This is the "add this offset" routine, part of the 0x82D79 process cluster. 8 lines. 0 callees
# (leaf function). 0 callers (unresolved — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x82D79B50"]
name = "AddOffset8_82D79B50"

# A processor that initializes and stores the data. It takes a structure (a: fields at a->0, a->20, a->24, and a->28). It reads a .data pointer (a float). It stores the float into a->0, a->20,
# a->24, and a->28. This is the "initialize and store this data" routine, part of the 0x83211 process cluster. 107 lines. 0 callees (leaf function). 1 caller (ProcessDispatchValue2_832351B0).
[entrypoint.functions."0x83211860"]
name = "ProcessAndInitializeAndStore_83211860"

# A processor that finds and processes the data (second form). It takes a value (a), a value (b), a value (c), a value (d), and a float (e). It reads a .data pointer's +13728 field (a flag). When the
# flag is zero it sets the flag (bit 0) and processes (sub_823D2790, with the pointer plus 13716). It then finds the chain root (FindChainRoot_82A596F0), compares struct fields
# (CompareStructFields_82A12C70), and checks the range (ProcessAndCheckRange_825BF630). This is the "find and process this data" routine (second form), part of the 0x83233 process cluster.
# 1 callee (sub_823D2790). 1 caller (sub_82A13750).
[entrypoint.functions."0x83233268"]
name = "ProcessAndFindAndProcess2_83233268"

# A processor that performs a vcall and returns the data (third form). It takes a structure (a: field at a->16). It reads a->16 (a pointer) and the pointer's +0 field. It performs a vcall via the
# +0 field's +24 entry (bctrl). This is the "perform a vcall and return this data" routine (third form), part of the 0x82B4E process cluster. 15 lines. 0 callees (leaf function). 0 callers
# (unresolved — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x82B4EC78"]
name = "ProcessAndVcallAndReturn3_82B4EC78"

# A processor that computes and processes the data (ninth form). It takes a structure (a: fields at a->8, a->20, a->32, a->40, and a->84) and a value (b). It reads a->8 (a pointer) and a->32 (an
# offset). It computes (the pointer minus b) shifted right by 3, then shifted left by 3, and adds the offset. It stores the result into a->8. It then reads a->84 (a pointer); when the pointer is
# non-zero it iterates: for each element it reads the element's +8 field (a pointer) and computes (the pointer minus b) shifted right by 3, then shifted left by 3, and adds the offset. This is the
# "compute and process this data" routine (ninth form), part of the 0x82BCC process cluster. 128 lines. 0 callees (leaf function). 1 caller (sub_82BCCD58).
[entrypoint.functions."0x82BCCC70"]
name = "ProcessAndComputeAndProcess9_82BCCC70"

# A processor that dispatches and processes the data (fifth form). It takes a structure (a: fields at a->32 and a->44) and a value (b). It adds 6 to b and checks the result against a .data pointer
# (536805376). When the result is within range it reads a->32 (a value) and a->44 (a pointer) and dispatches (DispatchAndAdjustPos_8227BB58, with a, the value, the pointer times 8, and the result
# times 8). When the result is out of range it processes (sub_82BC9860, with a and a .data pointer). This is the "dispatch and process this data" routine (fifth form), part of the 0x82BCC process
# cluster. 80 lines. 3 callees (DispatchAndAdjustPos, sub_82BC9860, and ProcessAndComputeAndProcess9). 12 callers (sub_82BC8490, sub_82BCCF08, sub_82BDE750, ProcessAndDispatchElements_822DF280,
# sub_82BC8CB8, sub_82BCD3B0, sub_82BC9788, sub_82BC83E8, sub_82BC5510, ProcessDispatchLoop_8229A518, sub_82BCD1E8, and ProcessState_8219F010).
[entrypoint.functions."0x82BCCD58"]
name = "ProcessAndDispatchAndProcess5_82BCCD58"

# A processor that initializes and processes the data (third form). It takes a structure (a: field at a->16), a structure (b: fields at b->4, b->8, b->20, b->28, b->32, b->36, b->40, b->44, and b->48),
# a value (c), and a value (d). It stores 1 into b->4 and b->8, 287 into b->20, 0 into b->28, a into b->32, c into b->36, d into b->44, and 46 into b->48. This is the "initialize and process this
# data" routine (third form), part of the 0x82BCD process cluster. 135 lines. 2 callees (ProcessAndQueryIndex and sub_82BCCB88). 1 caller (sub_82BDEB18).
[entrypoint.functions."0x82BCDD18"]
name = "ProcessAndInitializeAndProcess3_82BCDD18"

# A processor that stores and processes the data (third form). It takes a structure (a: fields at a->28 and a->32) and a structure (b: fields at b->0, b->8, b->12, b->16, b->24, b->28, and b->32). It
# reads a->32 (a pointer) and processes (ProcessElement76_82BCC888, with the pointer). It then stores the result into b->0, a->28 into b->8, a into b->12, the pointer into b->16, and b into a->28.
# It then stores 0 into b->24 and -1 into b->28 and b->32. This is the "store and process this data" routine (third form), part of the 0x82BDE process cluster. 165 lines. 3 callees
# (ProcessAdjustPosition, ProcessElement76, and ProcessAndDispatchAndProcess5). 2 callers (sub_82BDEB18 and sub_82BDF220).
[entrypoint.functions."0x82BDE750"]
name = "ProcessAndStoreAndProcess3_82BDE750"

# A processor that computes and processes the data (tenth form). It takes a structure (a: fields at a->28 and a->32). It reads a->28 (a pointer). It then reads the pointer's +50 field (a count,
# a byte). When the count is non-zero it iterates: for each element it decrements the count (by 1, with a wraparound check) and reads the element's +0 field (a pointer). It then reads the
# pointer's +24 field and computes (the count plus 86) times 1, times 1, times 2, and adds the pointer's +24 field. This is the "compute and process this data" routine (tenth form), part of the
# 0x82BDE process cluster. 375 lines. 4 callees (DispatchAndAdjustPos, ProcessAndComputeAndStore, sub_82BC9860, and sub_82BCDBA8). 2 callers (sub_82BDF220 and sub_82BDEB18).
[entrypoint.functions."0x82BDE878"]
name = "ProcessAndComputeAndProcess10_82BDE878"

# A processor that computes and processes the data (eleventh form). It takes a value (a), a value (b), a value (c), and a value (d). It stores d into a local. It then reads c's bytes (a count)
# and computes the length (c minus the end). It then computes a hash (ComputeHash_8227BA30, with a, c, and the length) and initializes and processes (ProcessAndInitializeAndProcess3_82BCDD18,
# with a, a local buffer, b, and the hash). This is the "compute and process this data" routine (eleventh form), part of the 0x82BDE process cluster. 107 lines. 7 callees (including ComputeHash,
# ProcessAndShiftFields, and ProcessAndInitializeAndProcess3). 0 callers (unresolved — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x82BDEB18"]
name = "ProcessAndComputeAndProcess11_82BDEB18"

# A processor that dispatches and processes the data (sixth form). It takes a structure (a: fields at a->4 and a->12). It reads a->12 (a type tag) and subtracts 258. When the result is within
# range (0 to 19) it performs a vcall via a .data pointer (a table) and dispatches by type tag (16 callees). This is the "dispatch and process this data" routine (sixth form), part of the 0x82BE1
# process cluster. 316 lines. 16 callees (including ProcessAndShiftFields, sub_82BDF510, and sub_82BDF220). 1 caller (sub_82BE1720).
[entrypoint.functions."0x82BE1500"]
name = "ProcessAndDispatchAndProcess6_82BE1500"

# A processor that dispatches and processes the data (seventh form). It takes a structure (a: fields at a->12 and a->28). It increments and processes (ProcessAndIncrementAndProcess2_82BDE440,
# with a). It then reads a->12 (a type tag) and dispatches (SwitchDispatch_82BE0100, with the type tag). When the dispatch returns zero it dispatches and processes (ProcessAndDispatchAndProcess6
# _82BE1500, with a). When the type tag is 59 it shifts the fields (ProcessAndShiftFields_82BCF7D8, with a). This is the "dispatch and process this data" routine (seventh form), part of the 0x82BE1
# process cluster. 84 lines. 4 callees (ProcessAndIncrementAndProcess2, SwitchDispatch, ProcessAndDispatchAndProcess6, and ProcessAndShiftFields). 5 callers (sub_82BE01A8, sub_82BDF220,
# sub_82BE0EB8, sub_82BE0648, and sub_82BDEB18).
[entrypoint.functions."0x82BE1720"]
name = "ProcessAndDispatchAndProcess7_82BE1720"

# A processor that dispatches and processes the data (eighth form). It takes a value (a), a structure (b: field at b->0), and a value (c). It dispatches by value (ProcessAndDispatchByValue_82BE1F00,
# with a, b, and c). It then reads b->0 (a type tag) and subtracts 1. When the result is within range (0 to 11) it performs a vcall via a .data pointer (a table) and dispatches by type tag
# (4 callees). This is the "dispatch and process this data" routine (eighth form), part of the 0x82BE2 process cluster. 218 lines. 4 callees (ProcessAndComputeAndStore, ProcessAndDispatchByValue,
# sub_82BE17B0, and sub_82BE1C40). 3 callers (sub_82BE28D0, sub_82BE2218, and sub_82BE2BE0).
[entrypoint.functions."0x82BE20A0"]
name = "ProcessAndDispatchAndProcess8_82BE20A0"

# A processor that dispatches and processes the data (ninth form). It takes a value (a), a structure (b: fields at b->0, b->4, b->12, and b->16), and a value (c). It dispatches and processes
# (ProcessAndDispatchAndProcess8_82BE20A0, with a, b, and c). It then reads b->0 (a type tag). When the tag is 10 it processes (sub_82BE1B98, with a, a pointer (b+12), and b->4). This is the
# "dispatch and process this data" routine (ninth form), part of the 0x82BE2 process cluster. 191 lines. 6 callees (ProcessAndComputeAndStore, ProcessAndComputeValue, sub_82BE1858, sub_82BE18C0,
# sub_82BE1B98, and ProcessAndDispatchAndProcess8). 3 callers (sub_82BE25E8, sub_82BE2428, and sub_82BE2368).
[entrypoint.functions."0x82BE2218"]
name = "ProcessAndDispatchAndProcess9_82BE2218"

# A processor that dispatches and processes the data (tenth form). It takes a structure (a: fields at a->0, a->36, and a->50) and a structure (b: fields at b->0 and b->4). It dispatches by value
# (ProcessAndDispatchByValue_82BE1F00, with a and b). It then reads b->0 (a type tag). When the tag is 12 it reads b->4 (a value); when the 0x100 bit is not set and the value is at least a->50 (a
# count) it decrements a->36 (a counter). This is the "dispatch and process this data" routine (tenth form), part of the 0x82BE2 process cluster. 109 lines. 3 callees (ProcessAndDispatchByValue,
# sub_82BCDAF8, and ProcessAndDispatchAndProcess9). 10 callers (sub_82BDF398, sub_82BDFEB8, sub_82BE2428, sub_82BDE318, sub_82BE13E8, sub_82BDF510, sub_82BDF330, sub_82BE09D0, sub_82BDEE48, and
# sub_82BE3140).
[entrypoint.functions."0x82BE2368"]
name = "ProcessAndDispatchAndProcess10_82BE2368"

# A processor that dispatches and processes the data (eleventh form). It takes a structure (a: field at a->50) and a structure (b: fields at b->0, b->4, b->12, and b->16). It dispatches by value
# (ProcessAndDispatchByValue_82BE1F00, with a and b). It then reads b->0 (a type tag). When the tag is 12 it reads b->12 and b->16 (two values). When the values differ and b->4 (a value) is at
# least a->50 (a count) it dispatches and processes (ProcessAndDispatchAndProcess9_82BE2218, with a and b). This is the "dispatch and process this data" routine (eleventh form), part of the 0x82BE2
# process cluster. 78 lines. 3 callees (ProcessAndDispatchByValue, ProcessAndDispatchAndProcess9, and ProcessAndDispatchAndProcess10). 10 callers (sub_82BE13E8, sub_82BDEC40, sub_82BDFEB8,
# sub_82BDF510, sub_82BE25E8, sub_82BE24B0, sub_82BE12E8, sub_82BE3140, sub_82BDEBD0, and sub_82BE2758).
[entrypoint.functions."0x82BE2428"]
name = "ProcessAndDispatchAndProcess11_82BE2428"

# A processor that performs a vcall and processes the data (tenth form). It takes a structure (a: field at a->4) and a structure (b: field at b->4). When the two structures differ it reads
# b->4 (a pointer); when the pointer is non-zero it reads the pointer's +0 field and performs a vcall via the +0 field's +0 entry (bctrl). It then reads a->4 (a pointer); when the pointer is
# non-zero it reads the pointer's +0 field and performs a vcall via the +0 field's +4 entry (bctrl). This is the "perform a vcall and process this data" routine (tenth form), part of the 0x82C0B
# process cluster. 82 lines. 0 callees (leaf function). 12 callers (sub_82C43AB8, sub_82C0D8B8, sub_82C0DC18, sub_826E8878, sub_82C2C780, sub_82C31790, sub_82C309B0, sub_82C0DB70, sub_82C4ECB0,
# sub_82C216A8, sub_82C2F748, and sub_82C38548).
[entrypoint.functions."0x82C0B768"]
name = "ProcessAndVcallAndProcess10_82C0B768"

# A processor that sets a jump point and processes the data. It takes a structure (a: field at a->96), a value (b), and a value (c). It stores b and c into local buffers. It then reads a->96 (a
# pointer) and stores a local buffer into a->96 (a stack pointer). It then sets a jump point (ppc_setjmp_83000200, with a local buffer). This is the "set a jump point and process this data"
# routine, part of the 0x82277 process cluster. 88 lines. 0 callees (leaf function). 4 callers (sub_82A20528, sub_82415428, sub_82277B88, and sub_8219A8E8).
[entrypoint.functions."0x82277AF8"]
name = "ProcessAndSetjmpAndProcess_82277AF8"

# A processor that performs a vcall and clears the data (third form). It takes a structure (a: fields at a->0 and a->4). It reads a->4 (a pointer); when the pointer is non-zero it reads the
# pointer's +0 field and performs a vcall via the +0 field's +4 entry (bctrl). It then stores 0 into a->4 and a->0. This is the "perform a vcall and clear this data" routine (third form), part
# of the 0x822AD process cluster. 49 lines. 0 callees (leaf function). 12 callers (sub_82479C28, sub_828DAAF8, sub_82173280, sub_826EF1D0, sub_825832B0, sub_82C43AB8, sub_82819740, sub_826C9460,
# sub_826E5D10, sub_82405088, sub_827B6FA8, and sub_8240C950).
[entrypoint.functions."0x822ADE08"]
name = "ProcessAndVcallAndClear3_822ADE08"

# A processor that computes and processes the data (twelfth form). It takes a value (a), a structure (b: field at b->4), a value (c), and a value (d). It lazily gets the data (LazyGetData_822F2088,
# with d, c, and a). It then searches and computes (sub_822F5BC8, with a local buffer, b, and a local buffer). This is the "compute and process this data" routine (twelfth form), part of the 0x82375
# process cluster. 292 lines. 5 callees (CopyAndDispatch, LazyGetData, sub_822F5BC8, sub_82369EE0, and sub_8236A3B0). 5 callers (sub_823757D8, sub_82372758, sub_822F36B8, sub_822F3828, and
# sub_82375750).
[entrypoint.functions."0x823754C8"]
name = "ProcessAndComputeAndProcess12_823754C8"

# A processor that finds and processes the data (third form). It takes a structure (a: fields at a->0 and a->4) and a value (b). It walks the chain and finds (WalkChainFind_83239FA8, with a .data
# pointer, b, and a local buffer). When the result is non-zero it stores 0 into a->0 and a->4 and releases (Release_RefCounted_829FF648, with a local buffer and b). This is the "find and process
# this data" routine (third form), part of the 0x82A0D process cluster. 233 lines. 5 callees (Allocate_OrDbgBreak, Construct32ByteVector2, ProcessRefCount, Release_RefCounted, and WalkChainFind).
# 1 caller (StateDispatch4_83220888).
[entrypoint.functions."0x82A0D630"]
name = "ProcessAndFindAndProcess3_82A0D630"

# A processor that copies and processes the data (fourth form). It takes a value (a), a structure (b: fields at b->0, b->4, b->12, and b->20), a value (c), and a value (d). It reads b->0 (a
# pointer) and performs a vcall via the pointer's +8 field (bctrl). It then reads b->20 (a count). When the count is at least 4 it copies the bytes (from b->12) into a local buffer and processes
# the data (ProcessFloatDataDispatch_8323EB40, with a local buffer and the count). This is the "copy and process this data" routine (fourth form), part of the 0x82A0D process cluster. 251 lines.
# 3 callees (ProcessAndCopyStructRegion, ProcessFloatDataDispatch, and sub_8321D458). 0 callers (unresolved — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x82A0D878"]
name = "ProcessAndCopyAndProcess4_82A0D878"

# A processor that computes and processes the data (fourteenth form). It takes a structure (a: fields at a->28 and a->32), a structure (b: field at b->0), and a value (c). It reads a->28 (a
# pointer); when the pointer is non-zero it reads a->32 (a pointer) and computes (a->32 minus a->28) divided by 20. This is the "compute and process this data" routine (fourteenth form), part of
# the 0x82B4A process cluster. 176 lines. 1 callee (sub_82B4BD30). 1 caller (sub_82C6D3F0).
[entrypoint.functions."0x82B4AB70"]
name = "ProcessAndComputeAndProcess14_82B4AB70"

# A processor that computes and processes the data (fifteenth form). It takes a value (a), a structure (b: fields at b->4 and b->8), and a value (c). It reads b->4 (a pointer); when the pointer
# is non-zero it reads b->8 (a pointer) and computes (b->8 minus b->4) shifted right by 3. This is the "compute and process this data" routine (fifteenth form), part of the 0x82B4B process cluster.
# 132 lines. 1 callee (sub_82B4C4A0). 1 caller (ProcessAndComputeAndProcess14_82B4AB70).
[entrypoint.functions."0x82B4BD30"]
name = "ProcessAndComputeAndProcess15_82B4BD30"

# A processor that performs a vcall and stores the data (third form). It takes a value (a), a value (b), a structure (c: fields at c->0 and c->4), and a value (d). When a is non-zero it iterates:
# for each element it reads c->0 (a pointer) and stores it into b's +0 field. When the pointer is non-zero it reads the pointer's +0 field and performs a vcall via the +0 field's +4 entry
# (bctrl). It then reads c->4 (a pointer) and stores it into b's +4 field. This is the "perform a vcall and store this data" routine (third form), part of the 0x82B4B process cluster. 74 lines.
# 0 callees (leaf function). 1 caller (sub_82B4C4A0).
[entrypoint.functions."0x82B4BE10"]
name = "ProcessAndVcallAndStore3_82B4BE10"

# A processor that computes and processes the data (sixteenth form). It takes a structure (a: fields at a->4, a->8, and a->12), a value (b), and a value (c). It calls sub_82B4CD80 (with a local
# buffer, c, and b). It then reads a->4 (a pointer); when the pointer is non-zero it reads a->12 (a pointer) and computes (a->12 minus a->4) shifted right by 3. This is the "compute and process
# this data" routine (sixteenth form), part of the 0x82B4C process cluster. 442 lines. 8 callees (AllocateChecked4, Free_SizeBucketed_Thunk, VirtualCall4, sub_82684B38, sub_82B4BE10, sub_82B4C438,
# sub_82B4CD80, and sub_82B4D290). 1 caller (ProcessAndComputeAndProcess15_82B4BD30).
[entrypoint.functions."0x82B4C4A0"]
name = "ProcessAndComputeAndProcess16_82B4C4A0"

# A processor that performs a vcall and stores the data (fourth form). It takes a structure (a: fields at a->0 and a->4) and a structure (b: fields at b->0 and b->4). It reads b->0 (a pointer)
# and stores it into a->0. When the pointer is non-zero it reads the pointer's +0 field and performs a vcall via the +0 field's +4 entry (bctrl). It then reads b->4 (a pointer) and stores it
# into a->4. This is the "perform a vcall and store this data" routine (fourth form), part of the 0x82B4C process cluster. 57 lines. 0 callees (leaf function). 1 caller
# (ProcessAndComputeAndProcess16_82B4C4A0).
[entrypoint.functions."0x82B4CD80"]
name = "ProcessAndVcallAndStore4_82B4CD80"

# A processor that checks and processes the data. It takes a structure (a: fields at a->8 and a->12). It reads a->12 (a pointer) and a->8 (a value). When the pointer is at least the value and
# is not a .data pointer (1876) it reads the pointer's +4 field (a type tag). When the tag is 5 it processes (sub_82BC68F0, with a and a .data pointer). This is the "check and process this data"
# routine, part of the 0x82BD6 process cluster. 146 lines. 1 callee (sub_82BC68F0). 0 callers (unresolved — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x82BD61F0"]
name = "ProcessAndCheckAndProcess_82BD61F0"

# A processor that jumps to a function. It takes a value (a). It jumps to sub_82C13078 (with a). This is a thunk that forwards to sub_82C13078, part of the 0x82BFF process cluster. 4 lines.
# 0 callees (leaf function). 1 caller (sub_82C161D8).
[entrypoint.functions."0x82BFFB38"]
name = "ProcessAndJumpTo_82BFFB38"

# A processor that computes and processes the data (seventeenth form). It takes a value (a), a float (b), a value (c), and a value (d). It calls sub_82C13068 (with a). It then calls sub_82C13E48 (with a, the result of the first call, c, and d).
# This is the "compute and process this data" routine (seventeenth form), part of the 0x82BFF process cluster. 52 lines. 2 callees (sub_82C13068 and sub_82C13E48). 1 caller (sub_82C161D8).
[entrypoint.functions."0x82BFFF40"]
name = "ProcessAndComputeAndProcess17_82BFFF40"

# A getter for a global field (offset 19764). It reads a field from a .data structure (at offset 19764) and returns it. This is the "get this global field" routine, part of the 0x82C13 process cluster. 10 lines. 0 callees (leaf function).
# 12 callers (sub_82BFFD88, sub_82BFFC68, sub_82BFFBD8, sub_82BFFCE8, ProcessAndComputeAndProcess17_82BFFF40, sub_82BFFD30, sub_82BFFEE8, sub_82BFFE50, sub_82BFFBB0, sub_82BFFEA8, sub_82BFFB10, and sub_82BFFC20).
[entrypoint.functions."0x82C13068"]
name = "GetGlobalField19764_82C13068"

# A compute function that returns a boolean bit from a global field (offset 19764). It reads the field from a .data structure, counts the leading zeros (cntlzw), rotates the count left by 27 (equivalent to taking bit 5), and XORs the result with 1.
# This is the "compute a bit from this global field" routine, part of the 0x82C13 process cluster. 16 lines. 0 callees (leaf function). 1 caller (ProcessAndJumpTo_82BFFB38).
[entrypoint.functions."0x82C13078"]
name = "ComputeGlobalField19764Bit_82C13078"

# A function that decrements a counter and performs a vcall when it reaches zero. It takes a structure (a: fields at a->4 and a->8). It reads a->8 (a counter) and decrements it. When the counter reaches zero it reads a->4 (a pointer) and
# performs a vcall via the pointer's +0 field's +4 entry (bctrl). This is the "decrement and perform a vcall when zero" routine, part of the 0x82C13 process cluster. 25 lines. 0 callees (leaf function). 0 callers (unresolved — body is recovered
# but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x82C13510"]
name = "DecrementAndVcall_82C13510"

# A constructor that allocates and processes a 20-byte structure. It takes a value (a), a float (b), a value (c), and a value (d). It allocates a 20-byte block (Allocate_OrDbgBreak_8221F388, with 20). When the allocation succeeds it initializes
# the structure with .data pointers (at -21108 and -20952) and zeros. This is the "construct and process this 20-byte structure" routine, part of the 0x82C13 process cluster. 159 lines. 4 callees (Allocate_OrDbgBreak, MemCpy_SdkRuntime,
# sub_82C4D7F8, and sub_82C4DA20). 1 caller (ProcessAndComputeAndProcess17_82BFFF40).
[entrypoint.functions."0x82C13E48"]
name = "ConstructAndProcess20Byte_82C13E48"

# An initializer that stores float constants into a structure. It takes a structure (a: fields at a->0, a->4, a->8, a->12, a->16, and a->24). It stores 0 into a->0 and a->8, -10000 into a->4 and a->24, and various float constants (from .data
# pointers) into a->12, a->16, and a->20. This is the "initialize and store floats into this structure" routine, part of the 0x82C16 process cluster. 85 lines. 0 callees (leaf function). 1 caller (ProcessAndGrowAndProcess8_82C1A5D0).
[entrypoint.functions."0x82C16158"]
name = "InitializeAndStoreFloats_82C16158"

# A processor that performs a vcall and processes the data (eleventh form). It takes a value (a) and a structure (b: field at b->0). It reads b->0 (a pointer) and performs a vcall via the pointer's +20 field (bctrl). It then calls
# VcallTriple_AllSuccess_82C011E8 (with b, a .data pointer, and a). When the result is non-zero it calls VcallTriple_AllSuccess_82C011E8 again (with b, a .data pointer, and a+4). This is the "perform a vcall and process this data"
# routine (eleventh form), part of the 0x82C16 process cluster. 402 lines. 7 callees (DispatchVcallTriple, ProcessAndJumpTo, ProcessAndVcallAndClear3, ProcessAndVcallAndProcess10, VcallChain_AllSuccess, VcallTriple_AllSuccess, and
# ProcessAndComputeAndProcess17). 1 caller (sub_82C192D8).
[entrypoint.functions."0x82C161D8"]
name = "ProcessAndVcallAndProcess11_82C161D8"

# A processor that performs a vcall and processes the data (twelfth form). It takes a structure (a: field at a->0) and a value (b). It reads a->0 (a pointer) and performs a vcall via the pointer's +24 field (bctrl). When the result is
# non-zero it processes and performs a vcall (ProcessAndVcallAndProcess11_82C161D8, with b and a). When that result is also non-zero it performs a vcall via the pointer's +28 field (bctrl). This is the "perform a vcall and process this
# data" routine (twelfth form), part of the 0x82C19 process cluster. 86 lines. 1 callee (ProcessAndVcallAndProcess11). 1 caller (ProcessAndGrowAndProcess8_82C1A5D0).
[entrypoint.functions."0x82C192D8"]
name = "ProcessAndVcallAndProcess12_82C192D8"

# A function that swaps and compares elements. It takes a pointer (a) and a pointer (b). When the pointers differ it iterates: for each pair of elements (a and a+4) it reads the element's +0 field (a pointer) and compares the pointers' +4
# fields. When the fields are equal it compares the pointers' +12 fields. This is the "swap and compare these elements" routine, part of the 0x82C2B process cluster. 135 lines. 1 callee (Process2DRegion). 1 caller (SortAndRecurse_82C2C520).
[entrypoint.functions."0x82C2BB20"]
name = "SwapAndCompareElements_82C2BB20"

# A processor that calls and checks a method. It takes a value (a). It calls methods (ProcessAndCallMethods_8223E958, with a .data pointer and 1). When the result is non-zero it stores a .data pointer into the result's +0 field. It then
# stores a into the result's +4 field and checks the method (ProcessAndCheckMethod_8223E820, with a .data pointer). This is the "call and check this method" routine, part of the 0x82C4D process cluster. 60 lines. 2 callees
# (ProcessAndCallMethods and ProcessAndCheckMethod). 1 caller (ConstructAndProcess20Byte_82C13E48).
[entrypoint.functions."0x82C4D7F8"]
name = "ProcessAndCallAndCheckMethod_82C4D7F8"

# A function that clears two fields (offsets 0 and 56) of a structure. It takes a structure (a: fields at a->0 and a->56). It stores 0 into a->0 and a->56. This is the "clear fields 0 and 56 of this structure" routine, part of the 0x82C4D
# process cluster. 12 lines. 0 callees (leaf function). 1 caller (ConstructAndProcess20Byte_82C13E48).
[entrypoint.functions."0x82C4DA20"]
name = "ClearFields0And56_82C4DA20"

# A processor that performs a vcall and processes the data (thirteenth form). It takes a value (a) and a structure (b: fields at b->0 and b->40). When b->40 is zero it reads b->0 (a pointer) and performs a vcall via the pointer's +16
# field (bctrl). It then calls VirtualCall4_8260C130 (with b+40 and a local buffer). When the result is non-zero it reads the result's +0 field and performs a vcall via the +0 field's +8 entry (bctrl). This is the "perform a vcall and
# process this data" routine (thirteenth form), part of the 0x82C6C process cluster. 93 lines. 1 callee (VirtualCall4). 0 callers (unresolved — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x82C6CD60"]
name = "ProcessAndVcallAndProcess13_82C6CD60"

# A processor that computes and processes the data (eighteenth form). It takes a structure (a: fields at a->4, a->8, and a->12), a structure (b: field at b->0), a value (c), and a value (d). It reads a->4 (a pointer); when the pointer is
# non-zero it reads a->12 (a pointer) and computes (a->12 minus a->4) shifted right by 3. This is the "compute and process this data" routine (eighteenth form), part of the 0x82C6D process cluster. 503 lines. 3 callees
# (AllocateChecked4, Free_Thunk, and sub_82BF8E78). 1 caller (sub_82C6E028).
[entrypoint.functions."0x82C6DAC8"]
name = "ProcessAndComputeAndProcess18_82C6DAC8"

# A processor that computes and processes the data (nineteenth form). It takes a value (a), a structure (b: fields at b->4 and b->8), and a value (c). It reads b->4 (a pointer); when the pointer is non-zero it reads b->8 (a pointer) and
# computes (b->8 minus b->4) shifted right by 3. This is the "compute and process this data" routine (nineteenth form), part of the 0x82C6E process cluster. 132 lines. 1 callee (ProcessAndComputeAndProcess18). 1 caller (sub_82C6E4D8).
[entrypoint.functions."0x82C6E028"]
name = "ProcessAndComputeAndProcess19_82C6E028"

# A processor that computes and processes the data (twentieth form). It takes a structure (a: fields at a->4, a->8, and a->12) and a structure (b: field at b->0). It reads a->4 (a pointer); when the pointer is non-zero it reads a->8 (a
# pointer) and computes (a->8 minus a->4) shifted right by 3. This is the "compute and process this data" routine (twentieth form), part of the 0x82C6E process cluster. 107 lines. 1 callee (ProcessAndComputeAndProcess19). 1 caller
# (sub_82C6E7D8).
[entrypoint.functions."0x82C6E4D8"]
name = "ProcessAndComputeAndProcess20_82C6E4D8"

# A processor that computes and processes the data (twenty-first form). It takes a value (a), a value (b), and a value (c). It adds 76 to a and computes and processes (ProcessAndComputeAndProcess20_82C6E4D8, with a+76, b, and c). This is
# the "compute and process this data" routine (twenty-first form), part of the 0x82C6E process cluster. 32 lines. 1 callee (ProcessAndComputeAndProcess20). 1 caller (sub_82C6B640).
[entrypoint.functions."0x82C6E7D8"]
name = "ProcessAndComputeAndProcess21_82C6E7D8"

# A processor that copies and processes the data (fifth form). It takes a value (a) and a structure (b: fields at b->4, b->12, and b->20). It reads b->20 (a count). When the count is at least 1 it reads b->12 (a pointer), decrements the
# count, increments b->4 (a pointer) and b->12 (a pointer) by 1, and stores the byte at b->12 into a local buffer. This is the "copy and process this data" routine (fifth form), part of the 0x8321D process cluster. 78 lines. 1 callee
# (ProcessAndCopyStructRegion). 1 caller (ProcessAndCopyAndProcess4_82A0D878).
[entrypoint.functions."0x8321D458"]
name = "ProcessAndCopyAndProcess5_8321D458"

# A processor that performs a vcall and processes the data (fourteenth form). It takes a value (a). It stores and processes (ProcessAndStoreElement_82BB1E58, with a). It then reads the result's +0 field (a pointer) and performs a vcall via
# the pointer's +88 field (bctrl). It then performs a vcall via the pointer's +84 field (bctrl). It then performs a vcall via the pointer's +52 field (bctrl). This is the "perform a vcall and process this data" routine (fourteenth form),
# part of the 0x8219F process cluster. 131 lines. 1 callee (ProcessAndStoreElement). 0 callers (unresolved — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x8219F2A8"]
name = "ProcessAndVcallAndProcess14_8219F2A8"

# A processor that performs a vcall and processes the data (fifteenth form). It takes a value (a), a structure (b: field at b->0), a structure (c: fields at c->32 and c->40), and a value (d). It reads c->40 (a count) and c->32 (a
# pointer). It then iterates: for each element it reads b->0 (a pointer) and performs a vcall via the pointer's +76 field (bctrl). When the result is non-zero it reads b->0 again and continues. This is the "perform a vcall and process
# this data" routine (fifteenth form), part of the 0x821D2 process cluster. 99 lines. 1 callee (ProcessAndApplyAndAccessElement). 1 caller (sub_821D2C70).
[entrypoint.functions."0x821D2BB0"]
name = "ProcessAndVcallAndProcess15_821D2BB0"

# A processor that checks and processes the data (second form). It takes a structure (a: field at a->8). It gets an indexed pointer (GetIndexedPointer_82BC5448, with a). It then reads the result's +4 field (a type tag). When the tag is 6
# or 7 it processes (StateDispatch_82BCA7D8). When the tag is 8 it reads a->8 (a pointer), stores 5 into the result's +72 field, and stores the pointer's -8 field into the result's +68 field. This is the "check and process this data"
# routine (second form), part of the 0x822C0 process cluster. 132 lines. 2 callees (GetIndexedPointer and StateDispatch). 4 callers (sub_82BD93F0, sub_82BDB558, sub_82BCFAE8, and sub_82BD5750).
[entrypoint.functions."0x822C06C0"]
name = "ProcessAndCheckAndProcess2_822C06C0"

# A function that adds an offset (852) to a value. It takes a value (a) and returns a+852. This is the "add offset 852 to this value" routine, part of the 0x822D3 process cluster. 8 lines. 0 callees (leaf function). 0 callers (unresolved —
# body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x822D3D78"]
name = "AddOffset852_822D3D78"

# A processor that frees and processes the data. It takes a structure (a: fields at a->4, a->20, and a->24), a value (b), and a value (c). When the low byte of b is non-zero and a->24 is at least 8 it reads a->4 (a pointer). When c is
# non-zero it copies or zeros (Buffer_CopyOrZero_Error_82CA3730, with a+4, 16, the pointer, and c times 2). It then frees (Free_SizeBucketed_Thunk_8221BE68, with the pointer). This is the "free and process this data" routine, part of the
# 0x822F6 process cluster. 73 lines. 2 callees (Buffer_CopyOrZero_Error and Free_SizeBucketed_Thunk). 2 callers (sub_825F3050 and ProcessValue_FlagNonZero_8217A5E0).
[entrypoint.functions."0x822F65E8"]
name = "ProcessAndFreeAndProcess_822F65E8"

# A processor that applies and processes the data. It takes a structure (a: field at a->0) and a structure (b: field at b->4). It reads b->4 (a pointer); when the pointer is non-zero it reads the pointer's +12 field (a value) and +8 field
# (a value) and computes (the +8 field minus the +12 field) shifted right by 3. It then applies (ProcessApply_8227B7E0, with the pointer, a .data pointer, 0, and the computed value). This is the "apply and process this data" routine, part
# of the 0x82306 process cluster. 143 lines. 4 callees (GrowAndZeroBuffer, ProcessAndConstructAndDispatch, ProcessAndGuardedDispatch, and ProcessApply). 2 callers (sub_823F8908 and sub_82173718).
[entrypoint.functions."0x82306718"]
name = "ProcessAndApplyAndProcess_82306718"

# A processor that performs a vcall and processes the data (sixteenth form). It takes a value (a) and a structure (b: field at b->0). When b is non-zero and a .data flag is set it reads b->0 (a pointer) and performs a vcall via the
# pointer's +20 field (bctrl). When the result is non-zero it applies and processes (ProcessAndApplyAndProcess_82306718, with a and the result). This is the "perform a vcall and process this data" routine (sixteenth form), part of the
# 0x823F8 process cluster. 97 lines. 3 callees (ProcessAndApplyAndProcess, sub_823068E0, and sub_823077B8). 12 callers (sub_828DF1A8, sub_824F9B08, sub_8257D250, sub_8283FD80, sub_82411528, sub_828CBB40, sub_828ECC10, sub_8257EA18,
# sub_82582E50, sub_825914F0, sub_82595098, and sub_826C71C8).
[entrypoint.functions."0x823F8908"]
name = "ProcessAndVcallAndProcess16_823F8908"

# A processor that validates and processes the data (third form). It takes a value (a) and a structure (b: fields at b->4, b->20, and b->24). It reads b->24 (a size); when the size is at least 16 it reads b->4 (a pointer), otherwise it uses
# b+4 (a local buffer). It then validates and stores a pointer (ValidateAndStorePointer_8236F008, with a local buffer, b->20 plus the pointer, and b). This is the "validate and process this data" routine (third form), part of the 0x826C0
# process cluster. 99 lines. 3 callees (ProcessAndAppendAndProcess2, ReadStringElement, and ValidateAndStorePointer). 3 callers (sub_82C6DE30, sub_82C6A3A8, and sub_826C0628).
[entrypoint.functions."0x826C00C0"]
name = "ProcessAndValidateAndProcess3_826C00C0"

# A recursive processor that recurses and frees the data. It takes a value (a) and a structure (b: fields at b->0, b->8, and b->33). When b->33 (a flag) is zero it iterates: for each element it recurses (with a and b->8) and frees
# (Free_SizeBucketed_Thunk_8221BE68, with b). It then reads b->0 (a pointer) and continues the loop. This is the "recurse and free this data" routine, part of the 0x82B4F process cluster. 61 lines. 2 callees (Free_SizeBucketed_Thunk and
# itself). 2 callers (sub_822EBD28 and sub_8233E468).
[entrypoint.functions."0x82B4F2F8"]
name = "ProcessAndRecurseAndFree_82B4F2F8"

# A processor that frees and processes the data (second form). It takes a value (a) and a value (b). It calls sub_82B4F4C8 (with a and b). When the low bit of b is set it frees (Free_SizeBucketed_Thunk_8221BE68, with a). This is the
# "free and process this data" routine (second form), part of the 0x82B4F process cluster. 50 lines. 2 callees (sub_82B4F4C8 and Free_SizeBucketed_Thunk). 0 callers (unresolved — body is recovered but no callers found; likely a vtable
# method or indirect entry).
[entrypoint.functions."0x82B4F478"]
name = "ProcessAndFreeAndProcess2_82B4F478"

# A processor that checks and processes the data (third form). It takes a structure (a: fields at a->0, a->8, and a->12). It stores a .data pointer (-28180) into a->0. It then reads a->8 (a pointer) and checks success (ProcessAndCheckSuccess
# _82CBBF60, with the pointer). It then reads a->12 (a value); when the value is not -1 and differs from a->8 it checks success again (ProcessAndCheckSuccess_82CBBF60, with a->8). This is the "check and process this data" routine (third form),
# part of the 0x82B4F process cluster. 126 lines. 1 callee (ProcessAndCheckSuccess). 1 caller (ProcessAndFreeAndProcess2_82B4F478).
[entrypoint.functions."0x82B4F4C8"]
name = "ProcessAndCheckAndProcess3_82B4F4C8"

# A getter for a structure field (offset 20). It takes a structure (a: field at a->20). It reads a->20 and returns it. This is the "get field 20 of this structure" routine, part of the 0x82B56 process cluster. 8 lines. 0 callees
# (leaf function). 12 callers (sub_82E938B0, sub_82E81258, sub_82EB7F78, sub_82E92610, sub_82EB7A18, sub_82E821F8, sub_82EC52E8, sub_82E84DB8, sub_82EBC060, sub_82E6A1C8, sub_82EAD038, and sub_82E7C5F8).
[entrypoint.functions."0x82B568C0"]
name = "GetField20_82B568C0"

# A function that erases a byte from a buffer (a string structure). It takes a structure (a: fields at a->0 (a pointer) and a->4 (a halfword length)), an offset (b), and a count (c). It clamps the offset to the length. When c is -1 it
# computes (the length minus the offset). It then stores the new length into a->4 and memmoves (MemMove_SdkRuntime_82CAA2E0, with a->0, a->0+the offset, and the new length). It then stores a NUL byte at a->0+the new length. This is the
# "erase a byte from this buffer" routine, part of the 0x82BA8 process cluster. 68 lines. 1 callee (MemMove_SdkRuntime). 3 callers (sub_82BAA238, sub_82BAA3D0, and sub_82BBE220).
[entrypoint.functions."0x82BA8938"]
name = "EraseByteFromBuffer_82BA8938"

# A wrapper that initializes a string with an offset. It takes a value (a) and a value (b). It adds 12 to b and calls ProcessAndInitializeString (ProcessAndInitializeString_82BA8798, with a and b+12). This is the "initialize a string with an
# offset" routine, part of the 0x82BAA process cluster. 34 lines. 1 callee (ProcessAndInitializeString). 0 callers (unresolved — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x82BAA0D8"]
name = "InitializeStringWithOffset_82BAA0D8"

# A processor that computes and processes the data (twenty-second form). It takes a structure (a: fields at a->8 and a->12). It computes (a->8 minus a->12) shifted right by 3 and calls sub_82BB5C30 (with a and the result). It then reads a->12
# (a pointer); when the pointer is less than a->8 and not equal to a .data pointer (1876) it reads the pointer's +4 field. This is the "compute and process this data" routine (twenty-second form), part of the 0x82BB4 process cluster. 191
# lines. 4 callees (ProcessAndGetPointerAndProcess, sub_82BB5BD0, sub_82BB5C30, and sub_82BC6A18). 0 callers (unresolved — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x82BB49E0"]
name = "ProcessAndComputeAndProcess22_82BB49E0"

# A processor that dispatches and processes the data (twelfth form). It takes a value (a). It dispatches and processes (ProcessAndDispatchAndProcess_82BC6558, with a and 12). It then hashes a string (ProcessAndHashString2_821E1408, with a,
# -10000, and a .data pointer (-9996)). It then dispatches by type (ProcessAndDispatchByType_82BC5FF0, with a and -2). This is the "dispatch and process this data" routine (twelfth form), part of the 0x82BB5 process cluster. 58 lines.
# 3 callees (ProcessAndDispatchAndProcess, ProcessAndDispatchByType, and ProcessAndHashString2). 5 callers (sub_82BB5388, sub_82BB18A0, sub_82BB5130, sub_82BB5260, and ProcessAndComputeAndProcess22_82BB49E0).
[entrypoint.functions."0x82BB5C30"]
name = "ProcessAndDispatchAndProcess12_82BB5C30"

# A processor that hashes and stores the data (second form). It takes a structure (a: fields at a->8 and a->28) and a pointer (b). It reads a->8 (a pointer). It then scans for a NUL terminator (from b), computing the length (the NUL position minus
# b minus 1). It then computes a hash (ComputeHash_8227BA30, with a and the length). It then stores a and 4 into a->8's +0 and +4 fields. This is the "hash and store this data" routine (second form), part of the 0x82BC8 process cluster. 94 lines.
# 2 callees (ComputeHash and ProcessAndDispatchAndProcess5). 1 caller (sub_82BC8490).
[entrypoint.functions."0x82BC83E8"]
name = "ProcessAndHashAndStore2_82BC83E8"

# A processor that checks and processes the data (fourth form). It takes a structure (a: fields at a->8 and a->12). It reads a->8 (a pointer) and a->12 (a pointer). When a->12 is less than a->8 and not equal to a .data pointer (1876) it reads the
# pointer's +4 field; when that field is not 5 it processes (sub_82BC68F0, with a, 1, and a .data pointer's +20 field). This is the "check and process this data" routine (fourth form), part of the 0x82BD5 process cluster. 173 lines. 3 callees
# (ProcessAndDispatchIndexedValue, sub_82BC67F0, and sub_82BC68F0). 0 callers (unresolved — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x82BD5AD8"]
name = "ProcessAndCheckAndProcess4_82BD5AD8"

# A processor that checks and processes the data (fifth form). It takes a structure (a: fields at a->8 and a->12). It reads a->8 (a pointer) and a->12 (a pointer). When a->12 is less than a->8 and not equal to a .data pointer (1876) it reads the
# pointer's +4 field; when that field is -1 it processes (sub_82BC67F0, with a, 1, and a .data pointer's +8120 field). This is the "check and process this data" routine (fifth form), part of the 0x82BD6 process cluster. 129 lines. 3 callees
# (ProcessAndCheckAndProcess, sub_82BC67F0, and sub_82BC6A18). 0 callers (unresolved — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x82BD6748"]
name = "ProcessAndCheckAndProcess5_82BD6748"

# A processor that dispatches and processes the data (thirteenth form). It takes a value (a). It dispatches and processes (ProcessAndDispatchAndProcess_82BC6558, with a and 4). It then stores 0 into the result's +0 field. It then hashes a
# string (ProcessAndHashString2_821E1408, with a, -10000, and a .data pointer (-4576)). It then dispatches by type (ProcessAndDispatchByType_82BC5FF0, with a and -2). This is the "dispatch and process this data" routine (thirteenth form), part
# of the 0x82BD7 process cluster. 62 lines. 3 callees (ProcessAndDispatchAndProcess, ProcessAndDispatchByType, and ProcessAndHashString2). 6 callers (sub_82BD8698, sub_82BD82F0, sub_82BD81F8, sub_82BD93F0, sub_82BD8458, and sub_82BD80F0).
[entrypoint.functions."0x82BD7DD8"]
name = "ProcessAndDispatchAndProcess13_82BD7DD8"

# A processor that dispatches and processes the data (fourteenth form). It takes a structure (a: fields at a->4, a->12, and a->32), a value (b), and a value (c). When a->12 equals b it shifts fields (ProcessAndShiftFields_82BCF7D8). When a->4
# equals c it processes (sub_82BCDA68, with a and b) and then calls sub_82BC8870 (with a->32, a .data pointer (13404), and the result). This is the "dispatch and process this data" routine (fourteenth form), part of the 0x82BDD process
# cluster. 105 lines. 4 callees (ProcessAndShiftFields, sub_82BC8870, sub_82BCDA68, and sub_82BCDAF8). 9 callers (ProcessAndDispatchAndProcess6, sub_82BE0EB8, ProcessAndDispatchAndProcess4, sub_82BDF220, sub_82BE0648, sub_82BDEE48, sub_82BE0548,
# sub_82BE0D80, and sub_82BDF398).
[entrypoint.functions."0x82BDDD28"]
name = "ProcessAndDispatchAndProcess14_82BDDD28"

# A processor that dispatches and processes the data (fifteenth form). It takes a structure (a: fields at a->4, a->12, a->16, and a->28) and a value (b). It reads a->12 (a type tag) and dispatches: when the tag is 40, 123, or 286 it performs a
# specific operation; otherwise it calls sub_82BCDAF8 (with a and a .data pointer (13760)). It then shifts fields (ProcessAndShiftFields_82BCF7D8). This is the "dispatch and process this data" routine (fifteenth form), part of the 0x82BDF process
# cluster. 216 lines. 9 callees (ProcessAndComputeAndStore, ProcessAndDispatchAndProcess10, ProcessAndShiftFields, sub_82BCDAF8, ProcessAndDispatchAndProcess14, sub_82BDDDD8, sub_82BDEE48, sub_82BDF330, and sub_82BE1DD0). 1 caller
# (ProcessAndDispatchAndProcess4_82BDF510).
[entrypoint.functions."0x82BDF398"]
name = "ProcessAndDispatchAndProcess15_82BDF398"

# A processor that dispatches and processes the data (sixteenth form). It takes a value (a) and a structure (b: fields at b->0, b->12, and b->16). It reads b->12 (a value) and b->16 (a value). When the two values differ it dispatches and
# processes (ProcessAndDispatchAndProcess11_82BE2428). When the two values are equal it dispatches by value (ProcessAndDispatchByValue_82BE1F00). It then reads b->0 (a value); when the value minus 1 is at most 4 it performs a table-lookup dispatch.
# This is the "dispatch and process this data" routine (sixteenth form), part of the 0x82BE2 process cluster. 183 lines. 3 callees (ProcessAndDispatchAndCheck, ProcessAndDispatchAndProcess11, and ProcessAndDispatchByValue). 9 callers
# (sub_82BDECC0, sub_82BDEBD0, sub_82BE25E8, sub_82BE12E8, sub_82BE2EE0, ProcessAndDispatchAndProcess4, sub_82BDFEB8, sub_82BE3038, and sub_82BE2758).
[entrypoint.functions."0x82BE24B0"]
name = "ProcessAndDispatchAndProcess16_82BE24B0"

# A processor that dispatches and processes the data (seventeenth form). It takes a structure (a: fields at a->0, a->36, and a->50), a structure (b: fields at b->0 and b->4), and a value (c). It dispatches and processes
# (ProcessAndDispatchAndProcess11_82BE2428, with a, b, and c). When b->0 equals 12 and b->4 has the 0x100 bit clear and b->4 is at least a->50 (a byte) it decrements a->36. This is the "dispatch and process this data" routine (seventeenth
# form), part of the 0x82BE2 process cluster. 155 lines. 4 callees (ProcessAndComputeAndStore, ProcessAndDispatchAndProcess11, sub_82BCDAF8, and ProcessAndDispatchAndProcess16). 1 caller (ProcessAndDispatchAndProcess4_82BDF510).
[entrypoint.functions."0x82BE2758"]
name = "ProcessAndDispatchAndProcess17_82BE2758"

# A processor that performs a vcall and destructs the data (third form). It takes a structure (a: fields at a->0, a->8, and a->12). It stores a .data pointer (-8672) into a->0. It then reads a->12 (a pointer); when the pointer is non-zero it
# performs a vcall via the pointer's +8 field (bctrl). It then reads a->8 (a pointer); when the pointer is non-zero it performs a vcall via the pointer's +8 field. This is the "perform a vcall and destruct this data" routine (third form), part
# of the 0x82C62 process cluster. 76 lines. 1 callee (DestructRefCounted). 12 callers (sub_8290BE28, sub_826939A0, sub_82359BE8, sub_8281A568, sub_82AA9548, sub_82357DF8, sub_826928F8, sub_82B7CD18, sub_8233EC30, sub_824E9E70, sub_825F4120, and
# sub_823B07D0).
[entrypoint.functions."0x82C62A08"]
name = "ProcessAndVcallAndDestruct3_82C62A08"

# A check for whether a field is non-zero. It takes a structure (a: field at a->8). It reads a->8 (a value) and returns 1 when the value is non-zero, 0 when it is zero. This is the "is this field non-zero" routine, part of the 0x82C62 process
# cluster. 14 lines. 0 callees (leaf function). 12 callers (sub_826935F0, sub_822F3B18, sub_8233EC30, sub_82466098, sub_8290BE28, sub_82691A70, sub_825F45A8, sub_82A4BE40, sub_823FE828, sub_823B07D0, sub_825F3050, and sub_822F27C0).
[entrypoint.functions."0x82C62DE8"]
name = "IsNonZero_82C62DE8"

# A lazy initializer for a global. It reads a .data flag (offset 22452) and checks the low bit. When the bit is clear it sets the bit (storing the flag with the low bit set), stores the original low bit into a .data field (offset 22448), and calls
# sub_82CA3700 (with a .data pointer (1280) and other .data pointers). This is the "lazy-initialize this global" routine, part of the 0x82C63 process cluster. 69 lines. 1 callee (sub_82CA3700). 2 callers (sub_82C635D8 and sub_82C634C0).
[entrypoint.functions."0x82C63258"]
name = "LazyInitializeGlobal_82C63258"

# A processor that constructs and processes the data (twenty-third form). It takes a structure (a: field at a->4) and a value (b). It converts to lowercase (ProcessToLower_821EEA00, with a local buffer and b). It then copies a refcounted
# (CopyAssign_RefCounted2_82265160, with a+4 and the local buffer). It then constructs a refcounted (ConstructRefCounted_8222CF18, with a local buffer, a .data pointer (-29772), and -1). This is the "construct and process this data" routine
# (twenty-third form), part of the 0x82C63 process cluster. 428 lines. 11 callees (ConstructRefCounted, ConstructString, CopyAssign_RefCounted2, DestructRefCounted, Free_Thunk, ProcessAtomicIncrement, ProcessToLower, StringCompare, VirtualCall4,
# sub_82B3E0A8, and LazyInitializeGlobal). 3 callers (sub_822F3B18, sub_82A4BE40, and sub_82C63FB8).
[entrypoint.functions."0x82C635D8"]
name = "ProcessAndConstructAndProcess23_82C635D8"

# A processor that performs a vcall and processes the data (seventeenth form). It takes a structure (a: fields at a->0, a->36, and a->40) and a value (b). When a->36 (a flag) is zero and a->40 is zero it reads a->0 (a pointer) and performs a
# vcall via the pointer's +16 field (bctrl). It then performs a virtual call (VirtualCall4_8260C130, with a+40 and the local buffer). This is the "perform a vcall and process this data" routine (seventeenth form), part of the 0x82C6D process
# cluster. 163 lines. 4 callees (DestructAndClearField, Free_Thunk, VirtualCall4, and ProcessAndValidateAndProcess3). 0 callers (unresolved — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x82C6DE30"]
name = "ProcessAndVcallAndProcess17_82C6DE30"

# A getter for a structure field (offset 8). It takes a structure (a: field at a->8). It reads a->8 and returns it. This is the "get field 8 of this structure" routine, part of the 0x83041 process cluster. 8 lines. 0 callees (leaf function).
# 12 callers (sub_82C510C8, sub_82C50FE8, UIFont_Initialize, sub_82C88C58, sub_82C50E28, sub_82C88B38, sub_82C88BC8, sub_82C51810, sub_82C51870, sub_82C51960, sub_82C519C8, and sub_82C51040).
[entrypoint.functions."0x830418A8"]
name = "GetField8_830418A8"

# A processor that sets up a setjmp and processes the data (second form). It takes a structure (a: fields at a->20, a->32, a->40, a->52, a->57, and a->100), a value (b), and a value (c). It stores c into a->100. It then reads a->20 (a pointer) and
# a->40 (a pointer) and computes (a->20 minus a->40). It then sets up a setjmp (ProcessAndSetjmpAndProcess_82277AF8). When the result is non-zero it processes (sub_8222B4C0, with a and a->32 plus the result). This is the "set up a setjmp and process
# this data" routine (second form), part of the 0x8219A process cluster. 114 lines. 4 callees (ProcessAndSetjmpAndProcess, sub_8222B4C0, sub_82A2DF88, and sub_82A2E1C8). 12 callers (sub_8244FC00, sub_82459A28, sub_8244FAB0, sub_825B8300, sub_825B7A78,
# sub_82755128, sub_82A2ADF8, sub_82A22470, sub_82916888, sub_825AC638, sub_829F0B38, and sub_82A22368).
[entrypoint.functions."0x8219A8E8"]
name = "ProcessAndSetjmpAndProcess2_8219A8E8"

# A processor that checks and processes the data (sixth form). It takes a structure (a: fields at a->8 and a->16), a value (b), and a value (c). It reads a->16 (a pointer) and then reads the pointer's +68 field (a value) and +64 field (a value).
# When the +68 field is at least the +64 field it processes (sub_82179350, with a, b, and c). It then performs a virtual call (ProcessAndVirtualAndProcess_822277C0, with a, b, and c). This is the "check and process this data" routine (sixth form),
# part of the 0x821E1 process cluster. 62 lines. 2 callees (ProcessAndVirtualAndProcess and sub_82179350). 12 callers (sub_828BD0F8, sub_82846D48, sub_828BD288, sub_825D43A0, sub_82671D50, sub_825E05E0, sub_827593F8, sub_82934F38, sub_82A1F5B0,
# sub_827714A8, sub_82A22938, and sub_82425908).
[entrypoint.functions."0x821E1768"]
name = "ProcessAndCheckAndProcess6_821E1768"

# A processor that computes and processes the data (twenty-fourth form). It takes a structure (a: fields at a->20, a->32, and a->56) and a value (b). It reads a->56 (a flag) and checks the 0x2 bit. When the bit is set it reads a->32 (a value) and
# computes (b minus a->32). It then processes (sub_82A2E310, with a, 1, and -1). This is the "compute and process this data" routine (twenty-fourth form), part of the 0x82228 process cluster. 184 lines. 1 callee (sub_82A2E310). 3 callers
# (GuildChest_ProcessItems_822DE008, sub_82A2E4D0, and GuildChest_ProcessItem_82228830).
[entrypoint.functions."0x82228B28"]
name = "ProcessAndComputeAndProcess24_82228B28"

# A processor that computes and processes the data (twenty-fifth form). It takes a structure (a: fields at a->4, a->8, and a->12) and a structure (b: fields at b->4 and b->8). It reads b->4 (a pointer). When the pointer is non-zero it reads b->8
# (a pointer) and computes (b->8 minus b->4) shifted right by 2. It then stores 0 into a->4, a->8, and a->12. This is the "compute and process this data" routine (twenty-fifth form), part of the 0x822F7 process cluster. 122 lines. 1 callee
# (sub_825813E8). 2 callers (sub_822F72B8 and sub_822F5608).
[entrypoint.functions."0x822F77A0"]
name = "ProcessAndComputeAndProcess25_822F77A0"

# A function that finds a value in a list. It takes a structure (a: fields at a->52 and a->56) and a value (b). It reads a->56 (a pointer) and a+52 (a pointer). It then iterates the list (from a+52), reading each element's +4 field (a pointer). When it
# finds a matching element (where the element's +4 field equals a->56) it returns the element. When it reaches the end of the list (where the element is null) it traps (r0, 22). This is the "find a value in this list" routine, part of the 0x8236A
# process cluster. 104 lines. 1 callee (FindListTail_82C705D8). 2 callers (sub_8233A8D8 and ProcessAndComputeAndProcess12_823754C8).
[entrypoint.functions."0x8236A3B0"]
name = "FindInList_8236A3B0"

# A processor that iterates and processes the data. It takes a structure (a: fields at a->20 and a->40) and a value (b). It reads a->20 (a pointer). When b is greater than 0 it reads a->40 (a pointer) and iterates the list (from a->20 to a->40), reading
# each element's +4 field (a pointer). It decrements b for each element; when the element's +6 field (a byte) is zero it also decrements b by the element's +20 field. This is the "iterate and process this data" routine, part of the 0x82A2B process
# cluster. 82 lines. 0 callees (leaf function). 12 callers (sub_82BD5590, sub_82BF94E0, sub_82BCFD70, sub_82A294B0, sub_82A2A388, sub_82A1F360, sub_82BD0E28, sub_82A2A028, sub_82BF63F8, sub_82BD00D0, sub_82BF7A38, and sub_82BF8820).
[entrypoint.functions."0x82A2B3C0"]
name = "ProcessAndIterateAndProcess_82A2B3C0"

# A processor that constructs and processes the data (twenty-fourth form). It takes a value (a), a structure (b: field at b->0), a structure (c: fields at c->4, c->8, c->12, c->16, c->20, c->24, c->28, c->32, and c+36), a value (d), and a value (e). When d is zero it stores default constants into the c structure and constructs (sub_82A2ED70, with c+36 and the constants). When d is non-zero it reads b->0 (a byte) and processes with a
# set of constants. This is the "construct and process this data" routine (twenty-fourth form), part of the 0x82BC8 process cluster. 372 lines. 3 callees (sub_82A2ED70, sub_82BC88B8, and sub_82BC9560). 1 caller (sub_82BC8CB8).
[entrypoint.functions."0x82BC8A10"]
name = "ProcessAndConstructAndProcess24_82BC8A10"

# A processor that computes and processes the data (twenty-sixth form). It takes a structure (a: fields at a->8 and a->40), a structure (b: field at b->0), and a structure (c: field at c->96). It reads b->0 (a byte); when the byte is 62 it reads a->8 (a pointer), decrements it by 8, and reads the pointer's +0 field. When the byte is not 62 it reads c->96 (a value); when the value is non-zero it computes (the value times 3 shifted left by 3 and aligned to 8) plus a->40. It then constructs and processes (ProcessAndConstructAndProcess24_82BC8A10). This is the
# "compute and process this data" routine (twenty-sixth form), part of the 0x82BC8 process cluster. 162 lines. 4 callees (FindChar, ProcessAndDispatchAndProcess5, sub_82BC8928, and ProcessAndConstructAndProcess24). 6 callers (sub_82BD5590, sub_82BC6980, sub_82BC67F0, sub_82BDB558, sub_82BCFD70, and sub_82BD0E28).
[entrypoint.functions."0x82BC8CB8"]
name = "ProcessAndComputeAndProcess26_82BC8CB8"

# A wrapper that checks and clears with an offset. It takes a value (a). It adds 4 to a and calls ProcessAndCheckAndClear (ProcessAndCheckAndClear_82BF2CB8, with a+4). This is the "check and clear with an offset" routine, part of the 0x82BEB process cluster. 9 lines. 1 callee (ProcessAndCheckAndClear). 0 callers
# (unresolved — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x82BEB858"]
name = "CheckAndClearWithOffset_82BEB858"

# A processor that validates and processes the data (fourth form). It takes a structure (a: fields at a->4 and a->8) and a value (b). It reads a->4 (a value) and a->8 (a value); when a->4 is at most a->8 it traps (r0, 22). It then iterates a list (from a), reading each element's +4 and +8 fields; when +4 is at most +8 it traps (r0, 22). It then writes to a buffer (ProcessAndWriteToBuffer_82BF0458). This is the "validate and process this data" routine (fourth form), part of the 0x82BF0 process cluster. 120 lines. 2 callees (ProcessAndWriteToBuffer and ValidateBounds4). 1 caller (sub_82BF07C0).
[entrypoint.functions."0x82BF06F8"]
name = "ProcessAndValidateAndProcess4_82BF06F8"

# A processor that performs a vcall and stores the result (fifth form). It takes a structure (a: fields at a->0 and a->4) and a structure (b: field at b->0). It reads a->0 (a pointer) and then reads the pointer's +4 field (a function pointer). It then performs a vcall via the function pointer (bctrl). It then reads b->0 (a value) and stores it into a->4. This is the "perform a vcall and store the result" routine (fifth form), part of the 0x82BF2 process cluster. 48 lines. 0 callees (leaf function). 2 callers (sub_82BF3428 and sub_82BF32C8).
[entrypoint.functions."0x82BF2E78"]
name = "ProcessAndVcallAndStore5_82BF2E78"

# A processor that queries and processes the data. It takes a value (a), a value (b), a value (c), and a value (d). When b is negative, or c is zero with d non-zero, or c is non-zero with d non-positive it sets an error code (87) and reports the error (SetGlobalField_Thunk_82CC0750). This is the "query and process this data" routine, part of the 0x82BF3 process cluster. 356 lines. 10 callees (CountOrError_Thunk, ProcessAndAbort, ProcessAndCheckAndClear, ProcessAndConstructWithFlag3, ProcessAndQueryAndCopy, ProcessAndStoreAndDispatch, SetGlobalField_Thunk, sub_82BF2B68, ProcessAndVcallAndStore5, and
# sub_82CC0C10). 0 callers (unresolved — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x82BF3428"]
name = "ProcessAndQueryAndProcess_82BF3428"

# A sort routine that partitions the data. It takes a pointer (a), a value (b), a pointer (c), and a value (d). It computes the range size (c minus a) shifted right by 2. When the range is greater than 40 it computes pivot positions (using (the range plus 1) shifted right by 3 plus 1) and sorts 3 elements (Sort3Elements_82C2ADB0, with a, a plus the pivot position times 4, and a plus the pivot position times 8). This is the "sort and partition this data" routine, part of the 0x82C2A process cluster. 105 lines. 1 callee (Sort3Elements). 1 caller (sub_82C2B850).
[entrypoint.functions."0x82C2AF58"]
name = "SortAndPartition_82C2AF58"

# A sort routine that recurses (second form). It takes a pointer (a), a pointer (b), and a pointer (c). It computes the range size (c minus b) shifted right by 2, then the midpoint (the range size shifted right by 1 plus 1) times 4 plus b. It then partitions (SortAndPartition_82C2AF58, with b, the midpoint, c minus 4, and the midpoint). It then recursively sorts the partitions. This is the "sort and recurse" routine (second form), part of the 0x82C2B process cluster. 398 lines. 1 callee (SortAndPartition). 1 caller (SortAndRecurse_82C2C520).
[entrypoint.functions."0x82C2B850"]
name = "SortAndRecurse2_82C2B850"

# A constructor that constructs and processes the data. It takes a structure (a: fields at a->0, a->8, and a->12), a value (b), a value (c), and a value (d). It stores a .data pointer (-8672) into a->0. It then atomically increments (AtomicIncrement3_821F7798, with a+4 and the .data pointer). It then stores 0 into a->8 and a->12. It then constructs and processes (ProcessAndConstructAndProcess23_82C635D8, with a, b, c, and d). This is the "construct and process this data" routine, part of the 0x82C63 process cluster. 57 lines. 2 callees (AtomicIncrement3 and ProcessAndConstructAndProcess23). 12 callers (sub_8233EC30, sub_82466098, sub_826928F8, sub_82357DF8, sub_825F45A8, sub_824E9E70, sub_82691A70, sub_8281A568, sub_822F27C0, sub_82359BE8, sub_8233AE50, and sub_8290BE28).
[entrypoint.functions."0x82C63FB8"]
name = "ConstructAndProcess25Byte_82C63FB8"

# A processor that initializes a string (second form). It takes a value (a) and a value (b). It stores 0 into a local buffer and initializes an ANSI string (RtlInitAnsiString, with the local buffer and a). It then stores -3, 64, and the buffer pointer into a .data structure. It then processes (sub_82CC1E48, with the .data structure and other values). This is the "initialize a string" routine (second form), part of the 0x82CC0 process cluster. 83 lines. 3 callees (CopyFieldsAndBlock, StoreLastErrorCode, and sub_82CC1E48). 7 callers (ProcessAndQueryAndProcess, sub_82BF32C8, ProcessAndCompareAndProcess, ProcessAndCheckString2, sub_83002B80, sub_82CAA7A8, and sub_82B3EB30).
[entrypoint.functions."0x82CC0C10"]
name = "ProcessAndInitializeString2_82CC0C10"

# A processor that computes and processes the data (twenty-seventh form). It takes a structure (a: fields at a->0, a->4, and a->8), a value (b), a value (c), and a value (d). It reads a->4 (a pointer) and then reads the pointer's +0 field (a value). It then iterates a range (from 0 to the value), reading each byte (a byte) and computing. This is the "compute and process this data" routine (twenty-seventh form), part of the 0x82CC1 process cluster. 216 lines. 0 callees (leaf function). 1 caller (ProcessAndInitializeString2_82CC0C10).
[entrypoint.functions."0x82CC1E48"]
name = "ProcessAndComputeAndProcess27_82CC1E48"

# A destructor that destructs and frees the data. It takes a structure (a: fields at a->0, a->4, and a->8). When the structure is non-zero it reads a->0 (a pointer) and a->4 (a value) and processes (sub_821A3A50, with a->0, -10000, and a->4). It then stores 0 into a->0. It then destructs (DestructRefCounted_82214F08, with a+8) and frees (Free_SizeBucketed_Thunk_8221BE68, with a). This is the "destruct and free this data" routine, part of the 0x82198 process cluster. 55 lines. 3 callees (DestructRefCounted, Free_SizeBucketed_Thunk, and sub_821A3A50). 0 callers (unresolved — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x821981B0"]
name = "DestructAndFree_821981B0"

# A processor that appends and processes the data (third form). It takes a structure (a: field at a->8) and a value (b). When b is non-negative it appends an element (ProcessAndAppendElement_82229208, with a, -10000, and 0). It then accesses and converts (ProcessAndAccessAndConvert_82241898, with a, -10000, and b). This is the "append and process this data" routine (third form), part of the 0x821A3 process cluster. 90 lines. 2 callees (ProcessAndAccessAndConvert and ProcessAndAppendElement). 2 callers (sub_827ECD08 and DestructAndFree_821981B0).
[entrypoint.functions."0x821A3A50"]
name = "ProcessAndAppendAndProcess3_821A3A50"

# A processor that copies and processes the data (fifth form). It takes a structure (a: field at a->8) and a pointer (b). It accesses an element (AccessElement8_82A1E620, with b). It then reads a->8 (a pointer); when the pointer is greater than b it iterates the list (from the pointer to b), copying 8 bytes (the +0 and +4 fields) from each element. It then reads the pointer's +0 and +4 fields and stores them into b. This is the "copy and process this data" routine (fifth form), part of the 0x821E5 process cluster. 60 lines. 1 callee (AccessElement8). 12 callers (sub_82A21B50, sub_82A1A360, sub_82A283E0, sub_82A21C20, sub_824EF558, sub_82A22EC8, sub_82A1FE28, sub_82A22368, sub_82BF4B30, sub_82A22470, sub_82A21DE0, and sub_82A20FF8).
[entrypoint.functions."0x821E5168"]
name = "ProcessAndCopyAndProcess5_821E5168"

# A processor that increments and processes the data (third form). It takes a structure (a: field at a->52), a value (b), and a value (c). It reads a->52 (a halfword counter), increments it, and stores it back. When the counter is less than 200 it continues. When the counter equals 200 it processes (sub_82A2C520, with a and a .data pointer (11840)). When the counter is at least 225 it processes with a flag (5). This is the "increment and process this data" routine (third form), part of the 0x82228 process cluster. 112 lines. 5 callees (GuildChest_ProcessItem, GuildChest_ProcessItems, sub_82179350, sub_821E51D0, and sub_82A2C520). 12 callers (sub_82A20768, sub_82A1ED58, GuildChest_ProcessItems, sub_82A294B0, sub_82A21CB0, ProcessDispatchByType, sub_82A19CD8, sub_82A23390, sub_82A2FF50, sub_822A8F70, sub_82A2D660, and sub_82A1A360).
[entrypoint.functions."0x82228C70"]
name = "ProcessAndIncrementAndProcess3_82228C70"

# A processor that accesses and processes the data (second form). It takes a structure (a: fields at a->0, a->4, and a->84). It accesses an element (AccessElement8_82A1E620, with a). It then reads a->4 (a value); when the value is not 3 it processes (sub_8222D580, with a and a local buffer). This is the "access and process this data" routine (second form), part of the 0x8222A process cluster. 59 lines. 2 callees (AccessElement8 and sub_8222D580). 9 callers (sub_8228EF20, sub_82A2A028, sub_82A2B000, sub_82A26158, sub_82873360, sub_8249A910, sub_8222A668, sub_825E0348, and sub_825360C0).
[entrypoint.functions."0x8222A608"]
name = "ProcessAndAccessAndProcess2_8222A608"

# A processor that releases and processes the data. It takes a structure (a: fields at a->120 and a->124). It reads a->124 (a pointer) and a->120 (a pointer) and computes (a->124 minus a->120) shifted right by 3. It then iterates the list (from a->120), reading each element's +4 field (a pointer) and +0 field (a value); when the +4 field is non-zero it acquires a global lock and releases (Release_RefCounted_829FF648). This is the "release and process this data" routine, part of the 0x82270 process cluster. 104 lines. 2 callees (Release_RefCounted and sub_82270C00). 4 callers (sub_82283D80, sub_823FBF88, sub_8226EA00, and ProcessAndDispatch_828180A8).
[entrypoint.functions."0x82270B68"]
name = "ProcessAndReleaseAndProcess_82270B68"

# A processor that constructs and processes the data (twenty-fifth form). It takes a structure (a: field at a->8). It reads a->8 (a value); when the value is not -1 it reads a .data pointer (offset 26788) and constructs a 12-byte structure (Construct12Byte_825F7B10, with a .data pointer (144) and other values). It then stores -1 into a->8. This is the "construct and process this data" routine (twenty-fifth form), part of the 0x82270 process cluster. 73 lines. 1 callee (Construct12Byte). 2 callers (ProcessAndReleaseAndProcess_82270B68 and sub_827DFE18).
[entrypoint.functions."0x82270C00"]
name = "ProcessAndConstructAndProcess25_82270C00"

# A processor that accesses and processes the data (third form). It takes a structure (a: field at a->8). It reads a->8 (a pointer), decrements it by 8, and compares it against a .data pointer (-26784). When the pointer equals the .data pointer or the pointer's +4 field is non-zero it appends an element (ProcessAndAppendElement_82229208, with a, -10000, and 0). This is the "access and process this data" routine (third form), part of the 0x8228E process cluster. 123 lines. 4 callees (ProcessAndAccessAndConvert, ProcessAndAppendElement, ProcessAndAccessAndProcess2, and sub_822D7F88). 2 callers (sub_822B95B0 and sub_827ECC60).
[entrypoint.functions."0x8228EF20"]
name = "ProcessAndAccessAndProcess3_8228EF20"

# A constructor that constructs and processes the data. It takes a structure (a: field at a->0), a value (b), and a value (c). It allocates a 16-byte buffer (Allocate_OrDbgBreak_8221F388, with 16). When the allocation succeeds it processes (sub_822B95B0, with the buffer, b, and c). It then stores the result into a->0. This is the "construct and process this data" routine, part of the 0x822B9 process cluster. 94 lines. 2 callees (Allocate_OrDbgBreak and sub_822B95B0). 12 callers (sub_82845010, sub_82903620, sub_827EFE48, sub_82981B90, sub_8244F5D8, sub_8290AF70, sub_8253A510, sub_823A52C0, sub_82981570, sub_827EFAB8, sub_82924730, and sub_827EFC18).
[entrypoint.functions."0x822B9510"]
name = "ConstructAndProcess26_822B9510"

# A constructor that constructs and processes the data (second form). It takes a structure (a: fields at a->0, a->4, and a->12), a value (b), and a value (c). It stores b into a->0 and -2 into a->4. It then constructs a refcounted (ConstructRefCounted_8222CF18, with a+8, c, and -1). It then stores 1 into a->12. It then accesses and processes (ProcessAndAccessAndProcess3_8228EF20, with b) and stores the result into a->4. This is the "construct and process this data" routine (second form), part of the 0x822B9 process cluster. 63 lines. 2 callees (ConstructRefCounted and ProcessAndAccessAndProcess3). 2 callers (ConstructAndProcess26_822B9510 and sub_824EED10).
[entrypoint.functions."0x822B95B0"]
name = "ConstructAndProcess27_822B95B0"

# A constructor that constructs a 12-byte node. It takes a structure (a: fields at a->4 and a->8). It allocates a 12-byte buffer (Allocate_OrDbgBreak_8221F388, with 12). When the allocation succeeds it stores the buffer pointer into the buffer's +0 field and +4 field. It then stores the buffer pointer into a->4 and 0 into a->8. This is the "construct a 12-byte node" routine, part of the 0x8235E process cluster. 56 lines. 1 callee (Allocate_OrDbgBreak). 12 callers (sub_829DDCA0, sub_826147A0, sub_82348CC0, sub_82B45B28, sub_82B44D10, sub_824B9840, sub_828E5B50, sub_828914E8, sub_82359090, sub_82B78468, sub_82933020, and sub_8256ABB0).
[entrypoint.functions."0x8235E278"]
name = "Construct12ByteNode_8235E278"

# A processor that searches and processes the data. It takes a pointer (a), a structure (b: fields at b->36, b->40, and b->44), a pointer (c), and a pointer (d). It reads b->40 (a pointer) and b->44 (a pointer); when b->40 is greater than b->44 it traps (ppc_trap 22). It then iterates the list (from b+36), reading each element's +4 field (a pointer) and +8 field (a pointer); when the +4 field is greater than the +8 field it traps (ppc_trap 22). This is the "search and process this data" routine, part of the 0x8236A process cluster. 176 lines. 0 callees (leaf function — performs raw memory operations and traps). 5 callers (sub_82185080, sub_822F4458, sub_823726F8, ProcessAndCompareAndConstruct_822F3D20, and sub_822F3B18).
[entrypoint.functions."0x8236A530"]
name = "ProcessAndSearchAndProcess_8236A530"

# A processor that hashes and processes the data (second form). It takes a structure (a: field at a->0), a value (b), and a value (c). It reads a->0 (a pointer); when the pointer is non-zero it reads the pointer's +0 field (a pointer) and +4 field (a value). It then appends an element (ProcessAndAppendElement_82229208, with the pointer, -10000, and the value). It then dispatches and resolves (ProcessAndDispatchAndResolve_822279A0, with the pointer and 4). It then hashes and processes the result. This is the "hash and process this data" routine (second form), part of the 0x824EF process cluster. 89 lines. 5 callees (ProcessAndAppendElement, ProcessAndCopyReferenceAndHash, ProcessAndDispatchAndResolve, ProcessAndHashString, and ProcessAndResolveTypedField). 12 callers (sub_825BD148, sub_8293E9A0, sub_8290B260, sub_825ACEA0, sub_8290B948, sub_82908E20, sub_8253B3F0, sub_827EFE48, sub_82415778, sub_82425680, sub_82845010, and sub_8260C3B8).
[entrypoint.functions."0x824EF020"]
name = "ProcessAndHashAndProcess2_824EF020"

# A processor that counts and processes the data. It takes a value (a), a value (b), and a value (c). When a is non-zero it counts the elements (from c) by reading each 8-byte element's +0 field. It then processes the counted elements (ProcessAndDispatchAndProcess3_82A1FA08, with b, a .data pointer (8136), -10000, and other values). This is the "count and process this data" routine, part of the 0x82A1F process cluster. 203 lines. 8 callees (GrowAndZeroBuffer, ProcessAndAccessAndShift, ProcessAndDispatchAndProcess3, ProcessAndHashAndDispatch, ProcessAndHashString, ProcessAndResolveTypedField, ProcessAndCopyAndProcess5, and sub_82A1F3F8). 8 callers (sub_82A24590, sub_82A1F738, sub_8220AFD0, sub_822E38E0, sub_82A23340, sub_82A298E8, sub_82A27460, and sub_82A23198).
[entrypoint.functions."0x82A1F740"]
name = "ProcessAndCountAndProcess_82A1F740"

# A processor that vcalls and processes the data (eighteenth form). It takes a value (a), a structure (b: fields at b->0 and b->44), a value (c), and a value (d). It reads b->44 (a pointer) and indexes an element (ProcessAndIndexElement_8233D880, with b->44 + 8). It then reads b->0 (a pointer) and +16 (a method pointer) and performs a virtual call. This is the "vcall and process this data" routine (eighteenth form), part of the 0x82B3E process cluster. 53 lines. 1 callee (ProcessAndIndexElement). 0 callers (unresolved — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x82B3E9D0"]
name = "ProcessAndVcallAndProcess18_82B3E9D0"

# A processor that vcalls and processes the data (nineteenth form). It takes a structure (a: fields at a->380 and a->492) and a value (b). It processes (sub_82BABEE8, with a+380). It then reads a->492 (a pointer) and +80 (a method pointer) and performs a virtual call (with a+492, b, 128, and 1). This is the "vcall and process this data" routine (nineteenth form), part of the 0x82BA9 process cluster. 55 lines. 1 callee (sub_82BABEE8). 0 callers (unresolved — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x82BA9870"]
name = "ProcessAndVcallAndProcess19_82BA9870"

# A processor that serializes and processes the data. It takes a structure (a: fields at a->32 and a->40), a value (b), a value (c), a value (d), and a value (e). It reads a->40 (a value) and a->32 (a pointer) and computes (a->40 shifted left by 2 and aligned to 4 bytes, plus a->32). It then processes (ProcessAndIndexRegionAndProcess_82BAF4B0, with a+32 and other values) and serializes (Serialize_82CA3EB8). This is the "serialize and process this data" routine, part of the 0x82BAB process cluster. 416 lines. 6 callees (GetSingleton, ProcessAndClearIndexedRegion, ProcessAndIndexRegionAndProcess, ProcessIndexedRegion2, Serialize, and sub_82BAC750). 1 caller (ProcessAndVcallAndProcess19_82BA9870).
[entrypoint.functions."0x82BABEE8"]
name = "ProcessAndSerializeAndProcess_82BABEE8"

# A processor that computes and processes the data (twenty-eighth form). It takes a structure (a: fields at a->36, a->40, and a->44) and a value (b). It reads a->36 (a value) and a->40 (a value); when they are equal it reads a->44 (a float) and computes (a->44 times a->36, converted to a 32-bit integer). It then processes (ProcessAndIndexRegionAndProcess_82BAF4B0, with the result and b). This is the "compute and process this data" routine (twenty-eighth form), part of the 0x82BAC process cluster. 100 lines. 1 callee (ProcessAndIndexRegionAndProcess). 2 callers (ProcessAndSerializeAndProcess_82BABEE8 and sub_82BAC4E8).
[entrypoint.functions."0x82BAC750"]
name = "ProcessAndComputeAndProcess28_82BAC750"

# A processor that initializes and processes the data. It processes (sub_82BB9068, with 0) and returns 1. This is the "initialize and process this data" routine, part of the 0x82BB8 process cluster. 28 lines. 1 callee (sub_82BB9068). 0 callers (unresolved — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x82BB89B0"]
name = "ProcessAndInitializeAndProcess_82BB89B0"

# A processor that grows and processes the data (second form). It takes a structure (a: fields at a->8, a->12, and a->16), a value (b), a value (c), and a value (d). It reads a->16 (a pointer) and +92 (a pointer). It then accesses an element (ElementAccess_821E1498, with a->16 + 92) and computes (a->8 minus a->12) shifted right by 3. It then grows the buffer (GrowAndZeroBuffer_82BC55B8, with other values). This is the "grow and process this data" routine (second form), part of the 0x82BB8 process cluster. 158 lines. 4 callees (CompareIndexedPointers, ElementAccess, GrowAndZeroBuffer, and ProcessAndStoreElement2). 2 callers (sub_82BB9600 and sub_82BB9068).
[entrypoint.functions."0x82BB8F50"]
name = "ProcessAndGrowAndProcess2_82BB8F50"

# A processor that dispatches and processes the data (eighteenth form). It takes a structure (a: fields at a->8 and a->12) and a value (b). It reads a->12 (a pointer) and a->8 (a pointer); when a->12 is less than a->8 it uses a .data pointer (1876) instead. It then reads the pointer's +4 field (a value); when the value is not 4 it processes (sub_82BCBDC8, with a) and continues. This is the "dispatch and process this data" routine (eighteenth form), part of the 0x82BB9 process cluster. 796 lines. 24 callees (GetSingleton, GetTypedField, HashCstring, IncrementAndCheckThreshold, InsertHashEntry, MemMove_SdkRuntime, ProcessAndCheckAndProcess2, ProcessAndCheckAndProcess3, ProcessAndDispatchValue, ProcessAndHashString2, ProcessAndReadConvertedValue, ProcessAndStoreElement2, ProcessAndStoreElement, ProcessIndexedRegion2, ProcessIndexedRegion, ProcessLoopDispatch, ProcessAndGrowAndProcess2, and 8 sub_ functions). 2 callers (sub_82BB89D8 and ProcessAndInitializeAndProcess_82BB89B0).
[entrypoint.functions."0x82BB9068"]
name = "ProcessAndDispatchAndProcess18_82BB9068"

# A processor that inserts and processes the data (second form). It takes a structure (a: fields at a->8, a->12, and a->68) and a value (b). It reads a->12 (a pointer) and a->8 (a pointer) and computes (a->8 minus a->12) shifted right by 3. It then inserts a hash entry (InsertHashEntry_8219CDA8, with a .data pointer (3488), the result, and other values). It then dispatches the value (DispatchValue_821CF230, with a->68 and a->8 - 8). This is the "insert and process this data" routine (second form), part of the 0x82BB9 process cluster. 137 lines. 5 callees (CompareIndexedPointers, DispatchValue, GrowAndZeroBuffer, InsertHashEntry, and SearchElementArray). 2 callers (sub_82BB9600 and ProcessAndDispatchAndProcess18_82BB9068).
[entrypoint.functions."0x82BB9B60"]
name = "ProcessAndInsertAndProcess2_82BB9B60"

# A processor that inserts and processes the data (third form). It takes a structure (a: fields at a->8, a->12, and a->68). It reads a->8 (a pointer) and a->12 (a pointer) and computes (a->8 minus a->12) shifted right by 3. It then inserts a hash entry (InsertHashEntry_8219CDA8, with a .data pointer (3488), the result, and other values). It then dispatches the value (DispatchValue_821CF230, with a->68 and a->8 - 8). This is the "insert and process this data" routine (third form), part of the 0x82BB9 process cluster. 124 lines. 4 callees (DispatchValue, GetTypedField, GrowAndZeroBuffer, and InsertHashEntry). 2 callers (sub_82BB9600 and ProcessAndDispatchAndProcess18_82BB9068).
[entrypoint.functions."0x82BB9C50"]
name = "ProcessAndInsertAndProcess3_82BB9C50"

# A processor that computes and processes the data (twenty-ninth form). It takes a structure (a: fields at a->36, a->40, and a->44) and a value (b). It reads a->36 (a value) and a->40 (a value); when they are equal it reads a->44 (a float) and computes (a->44 times a->36, converted to a 32-bit integer). It then clears the indexed region (ProcessAndClearIndexedRegion_82BBF648, with the result and b). This is the "compute and process this data" routine (twenty-ninth form), part of the 0x82BBA process cluster. 94 lines. 1 callee (ProcessAndClearIndexedRegion). 3 callers (ProcessAndDispatchAndProcess18_82BB9068, sub_82BB9600, and sub_82BBE220).
[entrypoint.functions."0x82BBA130"]
name = "ProcessAndComputeAndProcess29_82BBA130"

# A processor that checks and processes the data (seventh form). It takes a structure (a: fields at a->8, a->12, and a->20). It reads a->8 (a pointer) and a->12 (a pointer); when a->12 is greater than or equal to a->8, or a->12 equals a .data pointer (1876), or a->12's +4 field equals 5 it processes (sub_82BC68F0, with a, 1, and other values). This is the "check and process this data" routine (seventh form), part of the 0x82BD6 process cluster. 136 lines. 1 callee (sub_82BC68F0). 0 callers (unresolved — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x82BD6000"]
name = "ProcessAndCheckAndProcess7_82BD6000"

# A processor that finds and processes the data. It takes a pointer (a). When a is zero it processes (sub_82BF2B68, with a .data pointer (31320), a .data pointer (31624), 753, and a .data pointer (31608)). It then finds characters (FindChar_82CA3980) and substrings (FindSubstring_821E6388) in the string. This is the "find and process this data" routine, part of the 0x82BEA process cluster. 174 lines. 5 callees (CountOrError_Thunk, FindChar, FindSubstring, sub_82BEB720, and sub_82BF2B68). 2 callers (sub_82BEEC70 and sub_82BEEE88).
[entrypoint.functions."0x82BEBA50"]
name = "ProcessAndFindAndProcess_82BEBA50"

# A constructor that constructs and stores the data. It takes a value (a), a value (b), a value (c), a structure (d: fields at d->0 and d->4), and a value (e). It processes and updates a global (ProcessAndUpdateGlobal_82BF21E0, with 24). When the result is non-zero it stores a into the buffer's +0 field, b into +4, c into +8, d->0 into +12, d->4 into +16, e into +20, and 0 into +21. This is the "construct and store this data" routine, part of the 0x82BEB process cluster. 59 lines. 1 callee (ProcessAndUpdateGlobal). 1 caller (sub_82BEE1B0).
[entrypoint.functions."0x82BEBDF8"]
name = "ConstructAndStore24_82BEBDF8"

# A constructor that constructs and initializes the data. It processes and updates a global (ProcessAndUpdateGlobal_82BF21E0, with 24). When the result is non-zero it stores 0 into the buffer's +0, +4, +8, and +12 fields, 1 into +20, and 0 into +21. This is the "construct and initialize this data" routine, part of the 0x82BEB process cluster. 59 lines. 1 callee (ProcessAndUpdateGlobal). 1 caller (sub_82BEC3C0).
[entrypoint.functions."0x82BEBEB8"]
name = "ConstructAndInitialize24_82BEBEB8"

# A processor that formats and processes the data (second form). It takes a value (a), a value (b), a value (c), and a value (d). It reads a .data flag (offset 18504); when the flag is zero it processes (SetGlobalField_Thunk_82CC0750, with 21). It then formats a string with type (ProcessAndFormatStringWithType_82CBCC00, with other values). This is the "format and process this data" routine (second form), part of the 0x82BEB process cluster. 483 lines. 10 callees (ProcessAndCheckAndClear, ProcessAndFormatStringWithType, ProcessAndInitializeAndProcess, SetGlobalField_Thunk, WrapperSub82CC1C70, and 5 sub_ functions). 1 caller (sub_82BEC9E0).
[entrypoint.functions."0x82BEBFC0"]
name = "ProcessAndFormatAndProcess2_82BEBFC0"

# A constructor that constructs and links the data. It takes a structure (a: fields at a->4 and a->8). It constructs and initializes a 24-byte buffer (ConstructAndInitialize24_82BEBEB8). It then stores the buffer pointer into a->4, 1 into the buffer's +21 field, and links the buffer to itself (storing the buffer pointer into the buffer's +0, +4, and +8 fields). It then stores 0 into a->8. This is the "construct and link this data" routine, part of the 0x82BEC process cluster. 52 lines. 1 callee (ConstructAndInitialize24). 1 caller (sub_82BEF170).
[entrypoint.functions."0x82BEC3C0"]
name = "ConstructAndLink24_82BEC3C0"

# A processor that formats and processes the data (third form). It takes a value (a) and a value (b). It formats and processes (ProcessAndFormatAndProcess2_82BEBFC0, with a, 0, a .data pointer (-17544), and b). This is the "format and process this data" routine (third form), part of the 0x82BEC process cluster. 15 lines. 1 callee (ProcessAndFormatAndProcess2). 1 caller (sub_82BF3010).
[entrypoint.functions."0x82BEC9E0"]
name = "ProcessAndFormatAndProcess3_82BEC9E0"

# A processor that formats and processes the data (fourth form). It takes a value (a), a value (b), and a value (c). It reads a .data flag (offset 18504); when the flag is zero it processes (SetGlobalField_Thunk_82CC0750, with 21). It then formats a string (sub_82BF2B68, with a .data pointer (31320), a .data pointer (31624), 650, and other values). This is the "format and process this data" routine (fourth form), part of the 0x82BEC process cluster. 334 lines. 4 callees (ProcessAndAbort, ProcessAndValidateBounds, SetGlobalField_Thunk, and sub_82BF2B68). 1 caller (sub_823737D0).
[entrypoint.functions."0x82BECC38"]
name = "ProcessAndFormatAndProcess4_82BECC38"

# A processor that stores and processes the data (second form). It takes a value (a), a structure (b: field at b->8), a value (c), a value (d), and a value (e). It reads b->8 (a value); when the value is -2 or greater it sets a string (SetString_822F2020, with a local buffer and a .data pointer (5552)). It then constructs and stores a 24-byte buffer (ConstructAndStore24_82BEBDF8, with other values). This is the "store and process this data" routine (second form), part of the 0x82BEE process cluster. 307 lines. 8 callees (SetString, sub_822F1DB0, sub_822F1EC0, sub_822F1F00, sub_826C3FA8, sub_82BEB8F0, sub_82BEB958, and ConstructAndStore24). 1 caller (sub_82BEE970).
[entrypoint.functions."0x82BEE1B0"]
name = "ProcessAndStoreAndProcess2_82BEE1B0"

# A processor that iterates and processes the data (second form). It takes a value (a), a structure (b: field at b->4), and a value (c). It reads b->4 (a pointer) and +4 (a pointer) and +21 (a byte); when the byte is zero it iterates the list (from the pointer), reading each element's +12 field (a value). It then stores and processes (ProcessAndStoreAndProcess2_82BEE1B0, with other values). This is the "iterate and process this data" routine (second form), part of the 0x82BEE process cluster. 101 lines. 1 callee (ProcessAndStoreAndProcess2). 1 caller (sub_82BEF360).
[entrypoint.functions."0x82BEE970"]
name = "ProcessAndIterateAndProcess2_82BEE970"

# A processor that writes and processes the data. It takes a value (a). When a is zero it processes (sub_82BF2B68, with a .data pointer (31320), a .data pointer (31624), 875, and a .data pointer (32344)). It then writes to the buffer (ProcessAndWriteToBuffer_82BF0458, with other values). This is the "write and process this data" routine, part of the 0x82BEF process cluster. 190 lines. 9 callees (ProcessAndConstructObject, ProcessAndCopyAndGrow, ProcessAndUpdateGlobal, ProcessAndValidateBounds2, ProcessAndWriteToBuffer, sub_82BEC3C0, sub_82BF0290, sub_82BF2B68, and sub_82BF2C28). 1 caller (sub_82BEF360).
[entrypoint.functions."0x82BEF170"]
name = "ProcessAndWriteAndProcess_82BEF170"

# A processor that writes and processes the data (second form). It takes a value (a), a value (b), a value (c), and a value (d). It reads a .data flag (offset 18504); when the flag is zero it processes (SetGlobalField_Thunk_82CC0750, with 21). It then writes to the buffer (sub_82BF2B68, with a .data pointer (31320), other values). This is the "write and process this data" routine (second form), part of the 0x82BF1 process cluster. 356 lines. 8 callees (CountOrError_Thunk, MemCpy_SdkRuntime, NoOp_Stub, ProcessAndAbort, ProcessAndConstructObject, SetGlobalField_Thunk, ValidateBounds16, and sub_82BF2B68). 1 caller (sub_82BEF360).
[entrypoint.functions."0x82BF16E0"]
name = "ProcessAndWriteAndProcess2_82BF16E0"

# A processor that formats and processes the data (fifth form). It takes a value (a), a value (b), and a value (c). When a is zero it processes (SetGlobalField_Thunk_82CC0750, with 87). It then formats and processes (ProcessAndFormatAndProcess3_82BEC9E0, with other values). This is the "format and process this data" routine (fifth form), part of the 0x82BF3 process cluster. 228 lines. 5 callees (ProcessAndStoreAndDispatch, SetGlobalField_Thunk, WrapperSub82CC1C70, ProcessAndFormatAndProcess3, and sub_82BF2B68). 0 callers (unresolved — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x82BF3010"]
name = "ProcessAndFormatAndProcess5_82BF3010"

# A processor that copies and processes the data (sixth form). It takes a structure (a: fields at a->8 and a->12) and a pointer (b). It reads a->8 (a pointer) and a->12 (a pointer); when they are equal it processes and initializes (ProcessAndInitializeAndCopy_82C99808, with a). It then stores a byte (b->0) into the buffer's +0 field and increments the counter (a->12). This is the "copy and process this data" routine (sixth form), part of the 0x82C9B process cluster. 84 lines. 1 callee (ProcessAndInitializeAndCopy). 3 callers (sub_82CA2588, ProcessAndQueryAndProcess_82C9DC30, and sub_82C9CC38).
[entrypoint.functions."0x82C9B268"]
name = "ProcessAndCopyAndProcess6_82C9B268"

# A processor that checks and processes the data (eighth form). It takes a structure (a: fields at a->20 and a->379) and a pointer (b). It reads a->20 (a value) and checks bit 0x40000; when the bit is set it processes (KeBugCheckEx, with a, 5170, and other values). This is the "check and process this data" routine (eighth form), part of the 0x82239 process cluster. 100 lines. 0 callees (leaf function — performs raw memory operations and kernel calls). 2 callers (sub_82CAACD0 and sub_822397A8).
[entrypoint.functions."0x822397E0"]
name = "ProcessAndCheckAndProcess8_822397E0"

# A processor that accesses and processes the data (fourth form). It takes a structure (a: fields at a->32 and a->40), a value (b), a value (c), and a value (d). It reads a->40 (a value) and a->32 (a pointer) and computes (a->40 shifted left by 2 and aligned to 4 bytes, plus a->32). It then accesses an element (ProcessAndApplyAndAccessElement_821E15A0, with the result and other values). This is the "access and process this data" routine (fourth form), part of the 0x822C5 process cluster. 129 lines. 1 callee (ProcessAndApplyAndAccessElement). 1 caller (sub_821D2C70).
[entrypoint.functions."0x822C52B0"]
name = "ProcessAndAccessAndProcess4_822C52B0"

# A processor that compares and processes the data. It takes a value (a). It stores 0 into a local buffer, constructs a refcounted (ConstructRefCounted_8222CF18, with the buffer, -1, and 3). It then compares strings (StrCmp_Signed_8226D7A8, with a .data pointer (3224) and a .data pointer (11932)). This is the "compare and process this data" routine, part of the 0x82304 process cluster. 554 lines. 5 callees (Allocate_OrDbgBreak, ConstructRefCounted, DestructRefCounted, ProcessAndConstructAndAtomicIncrement, and StrCmp_Signed). 0 callers (unresolved — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x82304E40"]
name = "ProcessAndCompareAndProcess_82304E40"

# A processor that vcalls and processes the data (twentieth form). It takes a structure (a: field at a->0). It reads a->0 (a pointer) and +8 (a method pointer) and performs a virtual call. It then reads a->0 (a pointer) and +0 (a method pointer) and performs a virtual call (with a and 1). This is the "vcall and process this data" routine (twentieth form), part of the 0x82305 process cluster. 51 lines. 0 callees (leaf function — performs raw memory operations and virtual calls). 0 callers (unresolved — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x82305218"]
name = "ProcessAndVcallAndProcess20_82305218"

# A constructor that constructs and processes the data. It takes a structure (a: fields at a->4 and a->8) and a value (b). It allocates a 12-byte buffer (Allocate_OrDbgBreak_8221F388, with 12). When the allocation succeeds it stores the buffer pointer into the buffer's +0 field and +4 field. It then stores the buffer pointer into a->4 and 0 into a->8. This is the "construct and process this data" routine, part of the 0x82308 process cluster. 132 lines. 2 callees (Allocate_OrDbgBreak and Construct12Byte). 2 callers (ChainSearch4_82306B80 and sub_82308418).
[entrypoint.functions."0x82308C28"]
name = "ConstructAndProcess28_82308C28"

# A processor that constructs and processes the data (twenty-sixth form). It takes a value (a) and a pointer (b). It reads b->0 (a pointer); when the pointer is zero it uses a .data pointer (63) instead. It then constructs with a flag (ProcessAndConstructWithFlag2_82BEDAB0, with the pointer, a local buffer, 0, and 260). This is the "construct and process this data" routine (twenty-sixth form), part of the 0x82374 process cluster. 92 lines. 4 callees (ConstructRefCounted, CopyAssign_RefCounted2, DestructRefCounted, and ProcessAndConstructWithFlag2). 3 callers (sub_823746F0, sub_82373E08, and sub_824F74B0).
[entrypoint.functions."0x82374C00"]
name = "ProcessAndConstructAndProcess26_82374C00"

# A constructor that constructs and processes the data. It takes a structure (a: fields at a->0 and a->4). It allocates a 12-byte buffer (Allocate_OrDbgBreak_8221F388, with 12). When the allocation succeeds it stores 1 into the buffer's +0 field, a .data pointer (-32320) into +4, and a->0 into +8. It then stores the buffer pointer into a->4. This is the "construct and process this data" routine, part of the 0x8254C process cluster. 62 lines. 1 callee (Allocate_OrDbgBreak). 12 callers (sub_82275F00, sub_82506290, sub_82993780, sub_823E25D0, sub_821E9290, sub_82194130, sub_82742760, sub_828214A8, sub_82531DC0, sub_82595D80, sub_823BD4A0, and sub_82549230).
[entrypoint.functions."0x8254C780"]
name = "ConstructAndProcess29_8254C780"

# A checker that checks a bit and returns the result. It takes a structure (a: field at a->4). It reads a->4 (a value), counts the leading zeros, and extracts bit 27 (the result of the count shifted left by 27 and masked with 0x1). It returns the inverted result (0 if the bit is set, 1 if the bit is clear). This is the "check a bit and return the result" routine, part of the 0x825F1 process cluster. 14 lines. 0 callees (leaf function — performs raw memory operations and bit manipulation). 0 callers (unresolved — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x825F13C8"]
name = "CheckBitAndReturn_825F13C8"

# A processor that computes and processes the data (thirtieth form). It takes a value (a), a structure (b: fields at b->4 and b->8), and a value (c). It reads b->8 (a pointer) and b->4 (a pointer) and computes (b->8 minus b->4) shifted right by 3. It then processes and finds (ProcessAndFindAndProcess2_83233268, with other values). This is the "compute and process this data" routine (thirtieth form), part of the 0x82A13 process cluster. 365 lines. 1 callee (ProcessAndFindAndProcess2). 1 caller (ProcessDispatchValue2_832351B0).
[entrypoint.functions."0x82A13750"]
name = "ProcessAndComputeAndProcess30_82A13750"

# A processor that dispatches and processes the data (nineteenth form). It takes a structure (a: fields at a->20, a->40, and a->72) and a pointer (b). It reads a->20 (a pointer) and a->40 (a pointer); when they are equal it reads a->72 (a value). It then dispatches virtually (ProcessAndDispatchVirtual2_8229B748, with a and 0). This is the "dispatch and process this data" routine (nineteenth form), part of the 0x82A1E process cluster. 103 lines. 2 callees (ProcessAndDispatchVirtual2 and ProcessAndIncrementAndProcess3). 0 callers (unresolved — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x82A1ED58"]
name = "ProcessAndDispatchAndProcess19_82A1ED58"

# A processor that dispatches and processes the data (twentieth form). It takes a structure (a: fields at a->0 and a->12). It queries and dispatches (ProcessAndQueryAndDispatch_82A363F0, with a local buffer, 1, and 4). When the result is negative it processes (sub_82A2ED28, with a->0, a->12, a .data pointer (14016), and a .data pointer (14072)). It then processes (sub_821E51D0, with a->0 and 3). This is the "dispatch and process this data" routine (twentieth form), part of the 0x82A36 process cluster. 65 lines. 3 callees (ProcessAndQueryAndDispatch, sub_821E51D0, and sub_82A2ED28). 3 callers (sub_82A36D00, sub_82A36648, and sub_82A36910).
[entrypoint.functions."0x82A36560"]
name = "ProcessAndDispatchAndProcess20_82A36560"

# A processor that vcalls and processes the data (twenty-first form). It takes a pointer (a) and a pointer (b). It iterates the list (from a to b), reading each 8-byte element's +0 field (a pointer); when the pointer is non-zero it reads the pointer's +0 field (a pointer) and +8 (a method pointer) and performs a virtual call. This is the "vcall and process this data" routine (twenty-first form), part of the 0x82B4C process cluster. 61 lines. 0 callees (leaf function — performs raw memory operations and virtual calls). 2 callers (ProcessAndComputeAndProcess16_82B4C4A0 and sub_82B4ACF0).
[entrypoint.functions."0x82B4C438"]
name = "ProcessAndVcallAndProcess21_82B4C438"

# A processor that vcalls and processes the data (twenty-second form). It takes a pointer (a), a pointer (b), a value (c), a value (d), and a value (e). It reads a->0 (a pointer); when the pointer is non-zero it reads b->0 (a pointer) and +16 (a method pointer) and performs a virtual call (with other values). This is the "vcall and process this data" routine (twenty-second form), part of the 0x82B4F process cluster. 116 lines. 0 callees (leaf function — performs raw memory operations and virtual calls). 0 callers (unresolved — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x82B4F5A8"]
name = "ProcessAndVcallAndProcess22_82B4F5A8"

# A processor that copies and processes the data (seventh form). It takes a pointer (a), a pointer (b), and a pointer (c). When b equals c it returns. It then iterates the list (from b to a), copying 24-byte elements (the +0 through +20 fields) from each element. This is the "copy and process this data" routine (seventh form), part of the 0x82B6C process cluster. 70 lines. 0 callees (leaf function — performs raw memory operations and copying). 1 caller (sub_82B6C650).
[entrypoint.functions."0x82B6CE30"]
name = "ProcessAndCopyAndProcess7_82B6CE30"

# A getter that reads a field at offset 832. It takes a pointer (a) and returns a+832 (a pointer to a field at offset 832). This is the "get field 832" routine, part of the 0x82BAA process cluster. 8 lines. 0 callees (leaf function — performs raw memory operations). 0 callers (unresolved — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x82BAA158"]
name = "GetField832_82BAA158"

# A processor that erases and processes the data. It takes a structure (a: fields at a->0 and a->4) and a value (b). It reads a->4 (a halfword length) and iterates the string (from a->0), searching for byte 46 (the period character). When the byte is found it erases it from the buffer (EraseByteFromBuffer_82BA8938, with a, b, and the index). This is the "erase and process this data" routine, part of the 0x82BAA process cluster. 85 lines. 2 callees (EraseByteFromBuffer and ProcessAndComputeLengthAndProcess). 2 callers (sub_823FC460 and sub_82BC3430).
[entrypoint.functions."0x82BAA3D0"]
name = "ProcessAndEraseAndProcess_82BAA3D0"

# A processor that serializes and processes the data (second form). It takes a structure (a: fields at a->32, a->36, a->40, and a->44) and a value (b). It stores a .data value (-16596) into a->44 and 0 into a->32, a->36, and a->40. It then serializes (Serialize_82CA3EB8, with a, 32, a .data pointer (-6332), and other values). This is the "serialize and process this data" routine (second form), part of the 0x82BB2 process cluster. 76 lines. 2 callees (ProcessAndClearIndexedRegion and Serialize). 1 caller (sub_82BB0D78).
[entrypoint.functions."0x82BB2CB8"]
name = "ProcessAndSerializeAndProcess2_82BB2CB8"

# A processor that vcalls and processes the data (twenty-third form). It takes a structure (a: fields at a->0 and a->4) and a value (b) and a value (c). It reads a->0 (a pointer) and +16 (a method pointer) and performs a virtual call. It then reads a .data pointer (19048) and performs another virtual call (with b and c). It then stores the result into a->4. This is the "vcall and process this data" routine (twenty-third form), part of the 0x82BBC process cluster. 49 lines. 0 callees (leaf function — performs raw memory operations and virtual calls). 0 callers (unresolved — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x82BBC7D8"]
name = "ProcessAndVcallAndProcess23_82BBC7D8"

# A processor that dispatches and processes the data (twenty-first form). It takes a structure (a: fields at a->8, a->20, and a->40) and a value (b). When b equals -10001 and a->20 equals a->40 it processes (sub_82BC9860, with a .data pointer (7912)). It then gets an indexed pointer (GetIndexedPointer_82BC5448, with a and b). This is the "dispatch and process this data" routine (twenty-first form), part of the 0x82BC5 process cluster. 203 lines. 3 callees (GetIndexedPointer, StateDispatch, and sub_82BC9860). 2 callers (sub_82BDB990 and sub_82BD93F0).
[entrypoint.functions."0x82BC56F8"]
name = "ProcessAndDispatchAndProcess21_82BC56F8"

# A processor that stores and processes the data (third form). It takes a structure (a: field at a->8). It processes and gets a pointer (ProcessAndGetPointerAndProcess_821885C0, with a and 1). It then stores a float value (the absolute value of f1) into the pointer's +0 field and 3 into +4. It then increments the pointer by 8 and stores it back into a->8. This is the "store and process this data" routine (third form), part of the 0x82BD1 process cluster. 53 lines. 1 callee (ProcessAndGetPointerAndProcess). 0 callers (unresolved — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x82BD1188"]
name = "ProcessAndStoreAndProcess3_82BD1188"

# A processor that hashes and processes the data (third form). It takes a structure (a: field at a->8) and a value (b). It reads a->8 (a pointer) and copies 8 bytes (the +0 and +4 fields) from the previous element. It then increments the pointer by 8 and stores it back into a->8. It then computes a string hash (ComputeStringHash_82BC5EB0, with the pointer, -2, and a .data pointer (6552)). This is the "hash and process this data" routine (third form), part of the 0x82BDB process cluster. 149 lines. 3 callees (ComputeStringHash, InsertHashEntry, and SearchStringForChar). 1 caller (sub_82BDB558).
[entrypoint.functions."0x82BDB450"]
name = "ProcessAndHashAndProcess3_82BDB450"

# A processor that dispatches and processes the data (twenty-second form). It takes a structure (a: fields at a->8, a->12, a->16, a->64, and a->68). It reads a->12 (a pointer) and a->8 (a pointer); when a->12 is less than a->8 it uses a .data pointer (1876) instead. It then reads the pointer's +4 field (a value); when the value is not 4 it processes (sub_82BCBDC8, with a) and continues. This is the "dispatch and process this data" routine (twenty-second form), part of the 0x82BDB process cluster. 380 lines. 13 callees (ComputeStringHash, GetIndexedPointer, IncrementAndCheckThreshold, ProcessAndAppendAndFindChar, ProcessAndCheckAndProcess2, ProcessAndHashString2, ProcessAndIterateAndProcess, ProcessLoopDispatch, and 5 sub_ functions). 0 callers (unresolved — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x82BDB558"]
name = "ProcessAndDispatchAndProcess22_82BDB558"

# A processor that hashes and processes the data (fourth form). It takes a structure (a: field at a->8), a value (b), and a pointer (c). When c is zero it stores 0 into the pointer's +4 field and increments the pointer by 8. When c is non-zero it computes a string hash (ComputeStringHash_82BC5EB0, with the pointer, other values). This is the "hash and process this data" routine (fourth form), part of the 0x82BDB process cluster. 86 lines. 2 callees (ComputeStringHash and InsertHashEntry). 1 caller (sub_82BDB990).
[entrypoint.functions."0x82BDB8F8"]
name = "ProcessAndHashAndProcess4_82BDB8F8"

# A processor that formats and processes the data (sixth form). It takes a value (a). When a is zero it processes (sub_82BF2B68, with a .data pointer (31320), a .data pointer (31624), 858, and a .data pointer (32324)). It then reads a .data pointer (24188) and validates the bounds (ValidateBounds4_82BEC300, with other values). This is the "format and process this data" routine (sixth form), part of the 0x82BEC process cluster. 181 lines. 3 callees (ValidateBounds16, ValidateBounds4, and sub_82BF2B68). 1 caller (sub_82BEEE88).
[entrypoint.functions."0x82BEC778"]
name = "ProcessAndFormatAndProcess6_82BEC778"

# A processor that moves and processes the data. It takes a value (a), a value (b), and a structure (c: field at c->8). It stores a and b into local variables. It then moves memory (from a to b, in 12-byte chunks) using raw memory operations. This is the "move and process this data" routine, part of the 0x82BEC process cluster. 79 lines. 0 callees (leaf function — performs raw memory operations). 1 caller (sub_82BECEE8).
[entrypoint.functions."0x82BEC9F8"]
name = "ProcessAndMoveAndProcess_82BEC9F8"

# A processor that moves and processes the data (second form). It takes a structure (a: fields at a->4 and a->8). It reads a->8 (a pointer) and a->4 (a pointer); when a->4 is greater than a->8 it traps (ppc_trap 22). It then moves the memory (ProcessAndMoveAndProcess_82BEC9F8, with the pointer and other values). This is the "move and process this data" routine (second form), part of the 0x82BEC process cluster. 58 lines. 1 callee (ProcessAndMoveAndProcess). 1 caller (sub_82BEF360).
[entrypoint.functions."0x82BECEE8"]
name = "ProcessAndMoveAndProcess2_82BECEE8"

# A processor that moves and processes the data (third form). It takes a value (a), a value (b), and a value (c). It stores a and b into local variables. It then moves memory (from c to a, in 12-byte chunks) using raw memory operations. This is the "move and process this data" routine (third form), part of the 0x82BEC process cluster. 50 lines. 0 callees (leaf function — performs raw memory operations). 1 caller (sub_82BEE0C8).
[entrypoint.functions."0x82BECF48"]
name = "ProcessAndMoveAndProcess3_82BECF48"

# A processor that grows and processes the data (third form). It takes a structure (a: fields at a->4, a->8, and a->12) and a value (b). When b is greater than -1 it processes (sub_82BEFF50, with b). It then computes (a->4 minus a->12) divided by 12. When b is less than the result it grows the buffer (ProcessAndUpdateGlobal_82BF21E0, with b times 12) and moves the memory (ProcessAndMoveAndProcess3_82BECF48, with other values). This is the "grow and process this data" routine (third form), part of the 0x82BEE process cluster. 137 lines. 4 callees (ProcessAndUpdateGlobal, ProcessAndMoveAndProcess3, sub_82BEFF50, and sub_82BF2158). 1 caller (sub_82BEF360).
[entrypoint.functions."0x82BEE0C8"]
name = "ProcessAndGrowAndProcess3_82BEE0C8"

# Constructs a 64-byte structure (second form). It takes a structure (a: fields at a->28, a->60, a->64, and a->68) and a value (b). It processes and copies and grows (ProcessAndCopyAndGrow_82BEE748, with a and b) twice (for a and a->28). It then zeroes the +60, +64, and +68 fields. This is the "construct and initialize a 64-byte structure" routine (second form), part of the 0x82BEE construct cluster. 53 lines. 1 callee (ProcessAndCopyAndGrow). 1 caller (sub_82BEEE88).
[entrypoint.functions."0x82BEEC18"]
name = "ConstructAndInitialize25_82BEEC18"

# A processor that formats and processes the data (seventh form). It takes a value (a) and a value (b). When a is zero it processes (sub_82BF2B68, with a .data pointer (31320), a .data pointer (31624), 168, and a .data pointer (32304)). It then constructs and initializes a structure (ConstructAndInitialize25_82BEEC18, with a and b). It then formats and processes the data (ProcessAndFormatAndProcess6_82BEC778, with other values). This is the "format and process this data" routine (seventh form), part of the 0x82BEE process cluster. 377 lines. 13 callees (FindChar, FindSubstring, ProcessAndAbort, ProcessAndUpdateGlobal, ProcessAndValidateBounds, ProcessAndWriteToBuffer, SetGlobalField_Thunk, ConstructAndInitialize25, ProcessAndFormatAndProcess6, and 4 sub_ functions). 1 caller (sub_82373E08).
[entrypoint.functions."0x82BEEE88"]
name = "ProcessAndFormatAndProcess7_82BEEE88"

# A processor that grows and processes the data (fourth form). It takes a structure (a: fields at a->4, a->8, and a->12) and a value (b). When b is greater than -1 it processes (sub_82BEFF50, with b). It then computes (a->4 minus a->12) divided by 4. When b is less than the result it grows the buffer (ProcessAndUpdateGlobal_82BF21E0, with b times 4) and moves the memory (sub_82BF13B0, with other values). This is the "grow and process this data" routine (fourth form), part of the 0x82BF0 process cluster. 137 lines. 4 callees (ProcessAndUpdateGlobal, sub_82BEFF50, sub_82BF13B0, and sub_82BF2158). 6 callers (sub_82BF07C0, ProcessAndCheckString, sub_82BEEC70, sub_82BF0610, ProcessAndFormatAndProcess7, and ProcessAndWriteAndProcess).
[entrypoint.functions."0x82BF0290"]
name = "ProcessAndGrowAndProcess4_82BF0290"

# Moves and processes 32-bit data. It takes a value (a), a value (b), and a value (c). It stores a and b into local variables. It then computes the size (c minus a) divided by 4. It then moves the memory (CheckedCopy_82CA3808, with c and the size times 4). This is the "move and process 32-bit data" routine, part of the 0x82BF1 move cluster. 55 lines. 1 callee (CheckedCopy). 1 caller (ProcessAndGrowAndProcess4).
[entrypoint.functions."0x82BF13B0"]
name = "MoveAndProcess32Bit_82BF13B0"

# A processor that initializes the buffer. It takes a value (a). When a is zero it processes (sub_82BF2B68, with a .data pointer (31320), a .data pointer (-29204), 154, and a .data pointer (-29176)). It then reads a .data pointer (18484) and a .data pointer (18500) and processes the buffer (ProcessAndLookupWithDefaultInit_82CA5DC0, with other values). This is the "initialize the buffer" routine, part of the 0x82BF2 process cluster. 81 lines. 2 callees (ProcessAndLookupWithDefaultInit and sub_82BF2B68). 12 callers (sub_82BEF110, ProcessAndCopyOrZeroBuffer, ProcessAndGrowAndProcess4, sub_82BEE7B8, sub_82BED050, sub_82BF2470, sub_82BEF8E8, sub_82BEE3D8, sub_82BEBF10, sub_832AFB40, sub_82BF2DA8, and sub_82BF1BC8).
[entrypoint.functions."0x82BF2158"]
name = "ProcessAndInitializeBuffer_82BF2158"

# A processor that writes to the buffer (second form). It takes a value (a), a value (b), a value (c), a value (d), a value (e), a value (f), a value (g), and a value (h). It stores b, c, d, e, f, g, and h into local variables. When a is zero it processes (sub_82BF2B68, with a .data pointer (31320), a .data pointer (-29040), 65, and a .data pointer (-29008)). It then writes to the buffer (sub_82CA70B0, with other values). This is the "write to the buffer" routine (second form), part of the 0x82BF2 process cluster. 80 lines. 2 callees (sub_82BF2B68 and sub_82CA70B0). 8 callers (sub_82BEBB78, ProcessAndConstructObject, ProcessAndFormatAndProcess7, ProcessAndCheckString, sub_82BF0CE0, ProcessAndFormatAndProcess2, ProcessAndWriteAndProcess, and sub_82BEF360).
[entrypoint.functions."0x82BF2C28"]
name = "ProcessAndWriteToBuffer2_82BF2C28"

# A processor that parses and processes the data. It takes a value (a), a pointer (b), a value (c), and a pointer (d). When b is less than c it processes the string (b, in 45-byte chunks). It then stores the pointer into d->0. This is the "parse and process this data" routine, part of the 0x82C89 process cluster. 292 lines. 0 callees (leaf function — performs raw memory operations). 2 callers (SwitchDispatch and ProcessAndDispatchIndex).
[entrypoint.functions."0x82C89638"]
name = "ProcessAndParseAndProcess_82C89638"

# A processor that dispatches and processes the data (twenty-third form). It takes a pointer (a), a value (b), a value (c), and a pointer (d). When a equals b it returns -4. It then reads a->0 (a value) and performs a virtual call (with other values). This is the "dispatch and process this data" routine (twenty-third form), part of the 0x82C8C process cluster. 1211 lines. 5 callees (SwitchDispatch, sub_82C89AB0, sub_82C8B988, sub_82C8BC70, and sub_82C8BF60). 0 callers (unresolved — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x82C8C178"]
name = "ProcessAndDispatchAndProcess23_82C8C178"

# A processor that checks and processes the data (ninth form). It takes a structure (a: fields at a->68, a->80, and a->416). When a->68 is zero it processes (sub_82C98FD8, with other values). It then checks and returns (ProcessAndCheckAndReturn_82C9B2F8, with a->416, other values). This is the "check and process this data" routine (ninth form), part of the 0x82C9C process cluster. 97 lines. 4 callees (ClearAndZeroAndProcess, ProcessAndCheckAndReturn, sub_82C98F58, and sub_82C98FD8). 3 callers (sub_82C9EAC0, sub_82C9D500, and sub_82C9FB58).
[entrypoint.functions."0x82C9C918"]
name = "ProcessAndCheckAndProcess9_82C9C918"

# A processor that vcalls and processes the data (twenty-fourth form). It takes a value (a). When a is zero it reads a .data pointer (3264) and performs a virtual call. When a is non-zero it reads a .data pointer (3260) and performs a virtual call. It then processes (sub_82CAC700, with 0). This is the "vcall and process this data" routine (twenty-fourth form), part of the 0x82CA3 process cluster. 65 lines. 1 callee (sub_82CAC700). 0 callers (unresolved — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x82CA3318"]
name = "ProcessAndVcallAndProcess24_82CA3318"

# A processor that allocates and processes the data. It takes a value (a). It processes (sub_82CA95C8, with a). It then reads two .data pointers (29892 and 29896) and allocates a buffer (sub_82CAACD0, with the second pointer). This is the "allocate and process this data" routine, part of the 0x82CA3 process cluster. 136 lines. 5 callees (TailCall, sub_82CA36DC, sub_82CA8570, sub_82CA95C8, and sub_82CAACD0). 2 callers (sub_82B44D10 and sub_82CA3700).
[entrypoint.functions."0x82CA35F8"]
name = "ProcessAndAllocateAndProcess_82CA35F8"

# A processor that frees and processes the data. It calls sub_82CA95D0 (with no arguments) and returns. This is the "free and process this data" routine, part of the 0x82CA3 process cluster. 24 lines. 1 callee (sub_82CA95D0). 3 callers (ProcessAndAllocateAndProcess, Func_82CA3614, and TailCall).
[entrypoint.functions."0x82CA36DC"]
name = "ProcessAndFreeAndProcess_82CA36DC"

# A processor that allocates and processes the data (second form). It calls sub_82CA35F8 (with no arguments) and returns a value (the result of ProcessAndAllocateAndProcess_82CA35F8, with a bit check). This is the "allocate and process this data" routine (second form), part of the 0x82CA3 process cluster. 30 lines. 1 callee (ProcessAndAllocateAndProcess). 12 callers (sub_832501F0, sub_832476F0, sub_83288CE8, sub_83250470, sub_8327D178, sub_832524C8, sub_83272D98, sub_8324AD00, sub_83287058, sub_832906C8, sub_82EB4B80, and sub_83261DC8).
[entrypoint.functions."0x82CA3700"]
name = "ProcessAndAllocateAndProcess2_82CA3700"

# A processor that writes and processes the data (third form). It takes a value (a), a value (b), a pointer (c), and a value (d). When b is zero it writes to the pointer (sub_82CAB630, with other values) and returns -1. When b is non-zero it processes (sub_82CAB770, with other values). This is the "write and process this data" routine (third form), part of the 0x82CA6 process cluster. 206 lines. 3 callees (sub_82CAB630, sub_82CAB770, and sub_82CAB7E0). 1 caller (sub_82CA7000).
[entrypoint.functions."0x82CA6EA0"]
name = "ProcessAndWriteAndProcess3_82CA6EA0"

# A processor that writes and processes the data (fourth form). It takes a value (a), a value (b), a value (c), a value (d), and a value (e). When c is zero it writes to the buffer (sub_82CAB630, with other values) and returns 22. When c is non-zero it writes and processes the data (ProcessAndWriteAndProcess3_82CA6EA0, with a, b, c, d, and e). This is the "write and process this data" routine (fourth form), part of the 0x82CA7 process cluster. 102 lines. 3 callees (ProcessAndWriteAndProcess3, sub_82CAB630, and sub_82CAB770). 2 callers (sub_82CA3D30 and sub_82CA70B0).
[entrypoint.functions."0x82CA7000"]
name = "ProcessAndWriteAndProcess4_82CA7000"

# A processor that writes and processes the data (fifth form). It takes a value (a), a value (b), a value (c), a value (d), and a value (e). It calls sub_82CA7000 (with c, d, e, 0, and b) and returns the result. This is the "write and process this data" routine (fifth form), part of the 0x82CA7 process cluster. 11 lines. 1 callee (ProcessAndWriteAndProcess4). 3 callers (sub_82EE49A8, ProcessAndWriteToBuffer2, and sub_82BF2B68).
[entrypoint.functions."0x82CA70B0"]
name = "ProcessAndWriteAndProcess5_82CA70B0"

# A processor that allocates and processes the data (third form). It takes a value (a). It processes (sub_82CAFF48, with 8). This is the "allocate and process this data" routine (third form), part of the 0x82CA9 process cluster. 9 lines. 1 callee (sub_82CAFF48). 1 caller (ProcessAndAllocateAndProcess).
[entrypoint.functions."0x82CA95C8"]
name = "ProcessAndAllocateAndProcess3_82CA95C8"

# A processor that frees and processes the data (second form). It takes a value (a). It processes (sub_82CAFDE8, with 8). This is the "free and process this data" routine (second form), part of the 0x82CA9 process cluster. 9 lines. 1 callee (sub_82CAFDE8). 1 caller (ProcessAndFreeAndProcess).
[entrypoint.functions."0x82CA95D0"]
name = "ProcessAndFreeAndProcess2_82CA95D0"

# A processor that allocates and processes the data (fourth form). It takes a value (a). When a is zero it processes (sub_82CAB630, with other values) and returns -1. When a is non-zero it gets the global pointer (GetGlobalPointer_82239798, with 0) and processes (sub_822397E0, with the pointer and a). This is the "allocate and process this data" routine (fourth form), part of the 0x82CAA process cluster. 67 lines. 4 callees (GetGlobalPointer, sub_822397E0, sub_82CAB630, and sub_82CAB770). 4 callers (ProcessAndAllocateAndProcess, Func_82CA3614, sub_82CA8660, and sub_82B4F470).
[entrypoint.functions."0x82CAACD0"]
name = "ProcessAndAllocateAndProcess4_82CAACD0"

# A processor that processes the TLS. It takes a value (a). It gets the TLS value (KeTlsGetValue, with a .data pointer (-3788)) and sets it (KeTlsSetValue, with a .data pointer (-3788) and the value). This is the "process the TLS" routine, part of the 0x82CAC process cluster. 55 lines. 0 callees (leaf function — performs raw memory operations and calls KeTlsGetValue/KeTlsSetValue). 5 callers (sub_82CA34B0, sub_82CA3430, sub_83002A68, sub_830029D8, and sub_82CAC520).
[entrypoint.functions."0x82CAC428"]
name = "ProcessAndTlsAndProcess_82CAC428"

# A processor that frees and processes the data (third form). It takes a value (a). It frees the memory (sub_82CAFDE8, with a, in 8-byte chunks) using RtlLeaveCriticalSection. This is the "free and process this data" routine (third form), part of the 0x82CAE process cluster. 15 lines. 0 callees (leaf function — performs raw memory operations). 12 callers (sub_82CA9738, sub_82CA8970, sub_82CA50A4, sub_82CA8930, sub_82CB10C0, sub_82CB10E0, sub_82CB9054, sub_82CB1410, sub_82CBA8E4, ProcessAndFreeAndProcess2, sub_82CA9758, and sub_82CB5930).
[entrypoint.functions."0x82CAFDE8"]
name = "ProcessAndFreeAndProcess3_82CAFDE8"

# A processor that allocates and processes the data (fifth form). It takes a value (a). It allocates a buffer (sub_82CAFE08, with a, in 8-byte chunks) and enters a critical section (RtlEnterCriticalSection). This is the "allocate and process this data" routine (fifth form), part of the 0x82CAE process cluster. 59 lines. 2 callees (sub_82CA9578 and sub_82CAFE08). 12 callers (sub_830007E8, sub_82CBAE38, sub_82CA88E0, sub_82CBA740, sub_82CB5870, sub_83000A60, sub_82CB0E20, sub_82CB1368, sub_82CA8870, sub_83003368, sub_82CB8CF8, and ProcessAndAllocateAndProcess3).
[entrypoint.functions."0x82CAFF48"]
name = "ProcessAndAllocateAndProcess5_82CAFF48"

# A processor that dispatches and processes the data (twenty-fourth form). It takes a value (a), a value (b), and a value (c). It processes (sub_82CA2B88, with other values) and performs a virtual call. This is the "dispatch and process this data" routine (twenty-fourth form), part of the 0x82CB1 process cluster. 1772 lines. 11 callees (CheckFlagAndIncrement, GetGlobalPtr3, LookupMasked, ProcessAndLookupWithDefaultInit, ProcessAndResolveGlobal, ProcessAndStoreByte, and 5 sub_ functions). 0 callers (unresolved — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x82CB1438"]
name = "ProcessAndDispatchAndProcess24_82CB1438"

# A processor that destructs and processes the data (twenty-fifth form). It takes a value (a). It enters a critical section (RtlEnterCriticalSection, with a .data pointer (-352)) and iterates through a list (calling the method at +8 on each element). It then exits the critical section. This is the "destruct and process this data" routine (twenty-fifth form), part of the 0x82CC1 process cluster. 66 lines. 0 callees (leaf function — performs raw memory operations and virtual calls). 5 callers (sub_82CCA400, FatalError, sub_82CC0728, xstart, and Thread).
[entrypoint.functions."0x82CC16C0"]
name = "ProcessAndDestructAndProcess25_82CC16C0"

# Constructs a 44-byte structure (second form). It takes a structure (a: fields at a->8, a->12, a->16, a->20, a->24, a->28, a->36, and a->44) and a value (b). It zeroes the +8, +12, +16, and +20 fields. It then stores a .data value (384) into a->36, 1 into a->24, and 5 into a->28. It then constructs a float struct container (ConstructFloatStructContainer_82A142D0, with a and b). This is the "construct and initialize a 44-byte structure" routine (second form), part of the 0x8323F construct cluster. 327 lines. 3 callees (Construct32Byte3, ConstructFloatStructContainer, and ProcessGrowAppend). 1 caller (ProcessGrowAppend8).
[entrypoint.functions."0x8323FA78"]
name = "ConstructAndInitialize26_8323FA78"

# A processor that grows and processes the data (fifth form). It takes a structure (a: fields at a->0 and a->8) and a value (b). When b is greater than a->8 it grows the buffer (sub_82A2C520, with a and b) and processes (ProcessVirtual_82227940, with a and b). This is the "grow and process this data" routine (fifth form), part of the 0x821B2 process cluster. 81 lines. 2 callees (ProcessVirtual and sub_82A2C520). 3 callers (sub_821CC868, sub_82A36910, and sub_82A365D0).
[entrypoint.functions."0x821B2788"]
name = "ProcessAndGrowAndProcess5_821B2788"

# A getter that reads a field. It takes a pointer (a). It reads a->0 (a pointer). When a->0 is zero it returns a .data pointer (63). When a->0 is non-zero it returns a->0. This is the "read this field" routine, part of the 0x822F8 get cluster. 21 lines. 0 callees (leaf function — performs raw memory operations). 12 callers (sub_8223C950, sub_82C01930, sub_8260B9E0, sub_8253FBE8, sub_82C47BC0, sub_82A1A360, sub_82C01668, sub_82281EE0, sub_825C6248, sub_8256F540, sub_82C48210, and sub_82C00DA0).
[entrypoint.functions."0x822F8628"]
name = "GetField833_822F8628"

# A processor that frees and processes the data (fourth form). It takes a structure (a: field at a->4), a value (b), a value (c), and a value (d). It reads a->4 (a pointer) and the pointer's +0 field. When b is zero or b equals a it traps (ppc_trap 22). It then frees and processes the data (ProcessAndRecurseAndFree_82B4F2F8, with other values). This is the "free and process this data" routine (fourth form), part of the 0x8233E process cluster. 155 lines. 3 callees (FindChainRoot3, ProcessAndRecurseAndFree, and sub_822FF3C8). 3 callers (sub_82B4F198, sub_8233DDD8, and sub_82304A00).
[entrypoint.functions."0x8233E468"]
name = "ProcessAndFreeAndProcess4_8233E468"

# A processor that dispatches and processes the data (twenty-fifth form). It takes a value (a). When a is zero it processes (sub_823FE8F8, with a .data pointer (26788)). It then reads the pointer's +32 field (a method pointer) and performs a virtual call. This is the "dispatch and process this data" routine (twenty-fifth form), part of the 0x823F8 process cluster. 81 lines. 1 callee (sub_823FE8F8). 12 callers (sub_823BDCF8, sub_826C61B8, sub_82841C10, sub_821762A0, sub_826C5ED0, sub_823F8A38, sub_823FACE0, sub_82378FA0, sub_8240C4E0, sub_8240C330, sub_826C69B0, and sub_82583140).
[entrypoint.functions."0x823F89B0"]
name = "ProcessAndDispatchAndProcess25_823F89B0"

# A processor that frees and processes the data (fifth form). It takes a structure (a: field at a->4), a value (b), a value (c), and a value (d). It reads a->4 (a pointer) and the pointer's +0 field. When b is zero or b equals a it traps (ppc_trap 22). It then frees and processes the data (sub_8246FFC8, with other values). This is the "free and process this data" routine (fifth form), part of the 0x8246E process cluster. 155 lines. 3 callees (DestructorTree2, sub_8246FFC8, and sub_8273F9D8). 11 callers (sub_824ED700, sub_83229940, sub_8257C780, sub_8236DFC0, sub_828A1890, sub_829CCCD8, sub_8281A568, sub_8246C798, sub_8322C450, sub_829FBC40, and sub_825F1D78).
[entrypoint.functions."0x8246E8B0"]
name = "ProcessAndFreeAndProcess5_8246E8B0"

# Destructs and frees a structure (second form). It takes a structure (a: field at a->4). It reads a->4 (a pointer) and the pointer's +4 field (another pointer). It then destructs the structure (DestroyList_Recursive_82443440, with a and the pointer's +8 field) and frees the memory (Free_SizeBucketed_Thunk_8221BE68, with the pointer). It then releases the reference count (Release_RefCounted_829FF648, with the pointer's +0 field). This is the "destruct and free a structure" routine (second form), part of the 0x8247E destruct cluster. 78 lines. 3 callees (DestroyList_Recursive, Free_SizeBucketed_Thunk, and Release_RefCounted). 12 callers (sub_83225760, sub_83239FA0, sub_832275C0, sub_82A10A18, sub_832AA2F0, sub_8322F768, sub_832273E8, sub_83225D70, sub_832254A8, sub_8322F940, sub_827C4EC0, and sub_824EC530).
[entrypoint.functions."0x8247E448"]
name = "DestructAndFree2_8247E448"

# A processor that queries and processes the data (fourth form). It takes a structure (a: fields at a->0 and a->8). It processes and queries and dispatches (ProcessAndQueryAndDispatch_82A363F0, with a, a local pointer, 1, and 4). It then processes and grows (ProcessAndGrowAndProcess5_821B2788, with a, the local pointer, and the result). This is the "query and process this data" routine (fourth form), part of the 0x82A36 process cluster. 73 lines. 4 callees (ComputeStringHash, ProcessAndQueryAndDispatch, ProcessAndQueryAndProcess2, and ProcessAndGrowAndProcess5). 2 callers (sub_82A36648 and sub_82A36D00).
[entrypoint.functions."0x82A365D0"]
name = "ProcessAndQueryAndProcess4_82A365D0"

# A processor that frees and processes the data (sixth form). It takes a structure (a: field at a->4), a value (b), a value (c), and a value (d). It reads a->4 (a pointer) and the pointer's +0 field. When b is zero or b equals a it traps (ppc_trap 22). It then frees and processes the data (sub_82B4CDE0, with other values). This is the "free and process this data" routine (sixth form), part of the 0x82B4C process cluster. 155 lines. 3 callees (DestroyTree_Recursive, FindChainRoot3, and sub_82B4CDE0). 2 callers (sub_822F6990 and sub_82B4B2B8).
[entrypoint.functions."0x82B4CC70"]
name = "ProcessAndFreeAndProcess6_82B4CC70"

# A processor that searches and processes the data (third form). It takes a structure (a: field at a->4), a value (b), and a value (c). When b is greater than zero it searches the structure (a->4, in 4-byte chunks) and processes (sub_82BAFBE8, with other values). This is the "search and process this data" routine (third form), part of the 0x82BAF process cluster. 86 lines. 1 callee (sub_82BAFBE8). 0 callers (unresolved — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x82BAFB58"]
name = "ProcessAndSearchAndProcess3_82BAFB58"

# A getter that reads a field. It takes a value (a). When a is zero it returns a .data pointer (19876). When a is non-zero it returns a .data pointer (19880). This is the "read this field" routine, part of the 0x82C30 get cluster. 21 lines. 0 callees (leaf function — performs raw memory operations). 0 callers (unresolved — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x82C30640"]
name = "GetField834_82C30640"

# A processor that searches and processes the data (fourth form). It takes a structure (a: fields at a->0 and a->4) and a value (b). It reads a->0 (a pointer) and the pointer's +4 field (a method pointer). It then performs a virtual call (with a and b). It then searches and processes the data (BinarySearch_4Stride_82C40EC0, with other values). This is the "search and process this data" routine (fourth form), part of the 0x82C39 process cluster. 83 lines. 1 callee (BinarySearch_4Stride). 2 callers (sub_82C39470 and sub_82C3AD48).
[entrypoint.functions."0x82C39098"]
name = "ProcessAndSearchAndProcess4_82C39098"

# A processor that vcalls and processes the data (twenty-fifth form). It takes a structure (a: field at a->0) and a value (b). It reads a->0 (a pointer) and the pointer's +20 field (a method pointer). It then performs a virtual call (with a and b). It then constructs a string (ProcessConstructString_82BFD780, with other values). This is the "vcall and process this data" routine (twenty-fifth form), part of the 0x82C39 process cluster. 161 lines. 6 callees (AtomicIncrement3, DestructRefCounted, ProcessConstructString, ProcessVcallSequence3, VcallChain_AllSuccess, and VcallTriple_AllSuccess). 1 caller (sub_82C39370).
[entrypoint.functions."0x82C39188"]
name = "ProcessAndVcallAndProcess25_82C39188"

# A processor that vcalls and processes the data (twenty-sixth form). It takes a structure (a: field at a->0) and a value (b). It reads a->0 (a pointer) and the pointer's +24 field (a method pointer). It then performs a virtual call (with a and b). It then vcalls and processes the data (ProcessAndVcallAndProcess25_82C39188, with a and b). This is the "vcall and process this data" routine (twenty-sixth form), part of the 0x82C39 process cluster. 86 lines. 1 callee (ProcessAndVcallAndProcess25). 1 caller (sub_82C39510).
[entrypoint.functions."0x82C39370"]
name = "ProcessAndVcallAndProcess26_82C39370"

# Constructs a 16-byte structure (third form). It takes a value (a). When a is zero it allocates a 16-byte buffer (Allocate_OrDbgBreak_8221F388, with 16). It then stores a .data pointer (-15672) into the buffer's +0 field and 0 into the +4, +8, and +12 fields. This is the "construct and initialize a 16-byte structure" routine (third form), part of the 0x82C39 construct cluster. 59 lines. 1 callee (Allocate_OrDbgBreak). 0 callers (unresolved — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x82C39408"]
name = "ConstructAndInitialize27_82C39408"

# A processor that searches and processes the data (fifth form). It takes a value (a). It calls sub_82C39098 (with no arguments) and returns a value (the result of ProcessAndSearchAndProcess4_82C39098, with a bit check). This is the "search and process this data" routine (fifth form), part of the 0x82C39 process cluster. 30 lines. 1 callee (ProcessAndSearchAndProcess4). 0 callers (unresolved — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x82C39470"]
name = "ProcessAndSearchAndProcess5_82C39470"

# A processor that searches and processes the data (sixth form). It takes a structure (a: field at a->0) and a value (b). It reads a->0 (a pointer) and the pointer's +4 field (a method pointer). It then performs a virtual call (with a and b). It then searches and processes the data (a, in 4-byte chunks). This is the "search and process this data" routine (sixth form), part of the 0x82C39 process cluster. 65 lines. 0 callees (leaf function — performs raw memory operations). 0 callers (unresolved — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x82C394A0"]
name = "ProcessAndSearchAndProcess6_82C394A0"

# A processor that grows and processes the data (sixth form). It takes a structure (a: field at a->0) and a structure (b: field at b->8). It reads a->0 (a pointer) and the pointer's +0 field (a method pointer). It then performs a virtual call (with a and b). It then grows and processes the data (ProcessGrow_82C21C90, with other values). This is the "grow and process this data" routine (sixth form), part of the 0x82C39 process cluster. 522 lines. 8 callees (Allocate_OrDbgBreak, Array_PushBack, ProcessGrow, VirtualCallPair, sub_82BFF7D8, sub_82BFF848, sub_82C02B98, and ProcessAndVcallAndProcess26). 1 caller (sub_82C3B5C8).
[entrypoint.functions."0x82C39510"]
name = "ProcessAndGrowAndProcess6_82C39510"

# A processor that sorts and processes the data. It takes a structure (a: field at a->0) and a structure (b: field at b->8). It reads a->0 (a pointer) and the pointer's +0 field (a method pointer). It then performs a virtual call (with a and b). It then sorts and processes the data (SortedFind2_82C3ADF0, with other values). This is the "sort and process this data" routine, part of the 0x82C3B process cluster. 230 lines. 9 callees (ClearElementArray, CompactClearArray, ConstructSortedArray, SortedFind2, SortedFind, sub_82BFF7D8, ProcessAndGrowAndProcess6, sub_82C3BF18, and sub_82C40BD0). 1 caller (sub_82C3B758).
[entrypoint.functions."0x82C3B5C8"]
name = "ProcessAndSortAndProcess_82C3B5C8"

# A processor that sorts and processes the data (second form). It takes a value (a) and a value (b). It calls sub_82C3B5C8 (with b and a + 4) and returns the result. This is the "sort and process this data" routine (second form), part of the 0x82C3B process cluster. 13 lines. 1 callee (ProcessAndSortAndProcess). 0 callers (unresolved — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x82C3B758"]
name = "ProcessAndSortAndProcess2_82C3B758"

# A processor that vcalls and processes the data (twenty-seventh form). It takes a structure (a: field at a->0). It stores a .data pointer (22488) into a->0. When the pointer is non-zero it performs a virtual call (with the pointer's +4 field). This is the "vcall and process this data" routine (twenty-seventh form), part of the 0x82C64 process cluster. 49 lines. 0 callees (leaf function — performs raw memory operations). 7 callers (sub_822F36B8, sub_825F2E10, sub_822F27C0, ProcessAndInitializeAndProcess, ProcessAndInitializeAndProcess2, sub_822F4578, and sub_823746F0).
[entrypoint.functions."0x82C645A8"]
name = "ProcessAndVcallAndProcess27_82C645A8"

# A processor that clamps and processes the data (third form). It takes a structure (a: field at a->4), a value (b), and a value (c). It processes and clamps (ProcessAndClampAndProcess_82C6CFF0, with a and b). It then clamps and processes the data (ProcessAndClampGrow_8217CBE8, with other values). This is the "clamp and process this data" routine (third form), part of the 0x82C6D process cluster. 104 lines. 2 callees (ProcessAndClampAndProcess and ProcessAndClampGrow). 2 callers (sub_82C6D5C8 and ProcessAndValidateAndProcess).
[entrypoint.functions."0x82C6D518"]
name = "ProcessAndClampAndProcess3_82C6D518"

# A processor that vcalls and processes the data (twenty-eighth form). It takes a structure (a: fields at a->40 and a->44). When a->44 is non-zero it performs a virtual call (with the pointer's +8 field). It then performs a no-op (NoOp_Stub_829CE870, with a->12). This is the "vcall and process this data" routine (twenty-eighth form), part of the 0x82C70 process cluster. 76 lines. 1 callee (NoOp_Stub). 1 caller (sub_82C70090).
[entrypoint.functions."0x82C70010"]
name = "ProcessAndVcallAndProcess28_82C70010"

# A processor that vcalls and processes the data (twenty-ninth form). It takes a value (a) and a value (b). It vcalls and processes the data (ProcessAndVcallAndProcess28_82C70010, with a). It then frees the memory (Free_Thunk_824FE010, with a) when b is non-zero. This is the "vcall and process this data" routine (twenty-ninth form), part of the 0x82C70 process cluster. 50 lines. 2 callees (Free_Thunk and ProcessAndVcallAndProcess28). 0 callers (unresolved — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x82C70090"]
name = "ProcessAndVcallAndProcess29_82C70090"

# A processor that iterates and processes the data (third form). It takes a pointer (a) and a structure (b: field at b->20). It iterates through a list (a, in 4-byte chunks) and performs a virtual call (with a->16 and b->20). This is the "iterate and process this data" routine (third form), part of the 0x82C98 process cluster. 57 lines. 0 callees (leaf function — performs raw memory operations and virtual calls). 1 caller (sub_82C9B770).
[entrypoint.functions."0x82C98658"]
name = "ProcessAndIterateAndProcess3_82C98658"

# A processor that iterates and processes the data (fourth form). It takes a structure (a: fields at a->0, a->8, and a->16). It iterates through a list (a, in 4-byte chunks) and performs a virtual call (with a->0, a->8, and a->16). This is the "iterate and process this data" routine (fourth form), part of the 0x82C99 process cluster. 67 lines. 0 callees (leaf function — performs raw memory operations and virtual calls). 1 caller (sub_82C9B0C0).
[entrypoint.functions."0x82C996B8"]
name = "ProcessAndIterateAndProcess4_82C996B8"

# A processor that processes the TLS (second form). It takes a value (a). It processes (WrapperSub82CC1C70_82CC0758, with no arguments) and reads a .data pointer (-3792). It then processes the TLS (ProcessAndTlsAndProcess_82CAC428, with the pointer). This is the "process the TLS" routine (second form), part of the 0x82CAC process cluster. 144 lines. 6 callees (GetGlobalField, ProcessAndLookupWithDefaultInit, SetGlobalField_Thunk, WrapperSub82CC1C70, sub_82CAAE18, and ProcessAndTlsAndProcess). 12 callers (sub_82240508, sub_82CAB7A8, Func_83000858, sub_82CA3388, Func_82CA33AC, sub_82CAC610, sub_82CB0E20, sub_83003670, sub_83000F30, sub_82CAB770, sub_82CAC8C8, and sub_830007E8).
[entrypoint.functions."0x82CAC520"]
name = "ProcessAndTlsAndProcess2_82CAC520"

# A processor that checks and processes the data (tenth form). It takes a structure (a: fields at a->0 and a->4) and a structure (b: fields at b->0 and b->4). When a->4 is 3 it processes (sub_82A2E7B0, with a and b). When a->4 is 4 it processes and stores a float value into b->0 and 3 into b->4. This is the "check and process this data" routine (tenth form), part of the 0x8222D process cluster. 78 lines. 1 callee (sub_82A2E7B0). 12 callers (sub_8239FA30, sub_8243E690, sub_825360C0, sub_825C98E0, sub_825B3AD0, sub_82763318, sub_829E9478, sub_82903F90, sub_827651B0, sub_8232EA88, sub_82873360, and sub_825D56D0).
[entrypoint.functions."0x8222D580"]
name = "ProcessAndCheckAndProcess10_8222D580"

# A processor that searches and processes the data (seventh form). It takes a structure (a: fields at a->16 and a->32). When a->32 is non-zero it searches the structure (a->16, in 8-byte chunks) and processes (sub_82A2FD80, with a->32). This is the "search and process this data" routine (seventh form), part of the 0x82242 process cluster. 79 lines. 1 callee (sub_82A2FD80). 2 callers (sub_822D7F88 and GuildChest_ProcessItems).
[entrypoint.functions."0x82242490"]
name = "ProcessAndSearchAndProcess7_82242490"

# A processor that grows and processes the data (seventh form). It takes a structure (a: field at a->16) and a value (b). It reads a->16 (a pointer) and the pointer's +21 field (a value). When the value is 2 it processes (ProcessVirtual_82227940, with other values). This is the "grow and process this data" routine (seventh form), part of the 0x822A0 process cluster. 162 lines. 2 callees (ProcessVirtual and sub_82A2C520). 3 callers (sub_82A2DAC0, sub_82A20198, and ProcessVirtualBound).
[entrypoint.functions."0x822A0F58"]
name = "ProcessAndGrowAndProcess7_822A0F58"

# A processor that accesses and processes the data (fifth form). It takes a value (a). It accesses an element (AccessElement8_82A1E620, with a) and performs a switch statement (with the element's +4 field). This is the "access and process this data" routine (fifth form), part of the 0x822D7 process cluster. 126 lines. 3 callees (AccessElement8, ProcessAndSearchAndProcess7, and sub_8229E9E8). 12 callers (sub_82A1FCB8, sub_827C06B8, sub_82A24440, sub_82A23390, sub_82A22080, sub_82A23828, sub_82A23970, sub_82A23BB8, sub_827BFCD8, sub_82BF4688, sub_824F2D08, and ProcessAndAccessAndProcess3).
[entrypoint.functions."0x822D7F88"]
name = "ProcessAndAccessAndProcess5_822D7F88"

# A processor that computes and processes the data (thirty-first form). It takes a value (a) and a structure (b: field at b->36). It initializes and processes (ProcessAndInitializeAndProcess_82375100, with a). It then computes and processes the data (ProcessAndComputeAndProcess12_823754C8, with a and b). This is the "compute and process this data" routine (thirty-first form), part of the 0x822F3 process cluster. 210 lines. 7 callees (ConstructString, Destruct3, ProcessAndComputeAndProcess12, ProcessAndComputeAndProcess4, ProcessAndConstructAndProcess3, ProcessAndInitializeAndProcess, and ProcessAndVcallAndProcess27). 0 callers (unresolved — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x822F36B8"]
name = "ProcessAndComputeAndProcess31_822F36B8"

# A processor that searches and processes the data (eighth form). It takes a value (a), a value (b), and a value (c). It loads from source (LazyGetData_822F2088, with b). It then searches and processes the data (ProcessAndSearchAndReturn_822F5BC8, with a and b). This is the "search and process this data" routine (eighth form), part of the 0x822F4 process cluster. 167 lines. 9 callees (Free_SizeBucketed_Thunk, LazyGetData, LoadFrom_Source, ProcessAndInitializeAndProcess2, ProcessAndSearchAndReturn, ProcessAndVcallAndProcess6, sub_822F5608, sub_822F5E78, and sub_8236A530). 3 callers (sub_82374240, sub_822F4260, and sub_822F3B18).
[entrypoint.functions."0x822F4458"]
name = "ProcessAndSearchAndProcess8_822F4458"

# A processor that searches and processes the data (ninth form). It takes a structure (a: field at a->4) and a structure (b: field at b->0). It reads a->4 (a pointer) and the pointer's +4 field (a value). It then searches and processes the data (ProcessAndComputeAndProcess25_822F77A0, with a and b). This is the "search and process this data" routine (ninth form), part of the 0x822F5 process cluster. 155 lines. 4 callees (ProcessAndComputeAndProcess25, sub_822F5E20, sub_822F6328, and sub_822F6708). 1 caller (ProcessAndSearchAndProcess8).
[entrypoint.functions."0x822F5608"]
name = "ProcessAndSearchAndProcess9_822F5608"

# A processor that frees and processes the data (seventh form). It takes a structure (a: fields at a->4, a->8, and a->12). When a->4 is non-zero it frees the memory (Free_SizeBucketed_Thunk_8221BE68, with a->4). It then zeros the structure (a->4, a->8, and a->12). This is the "free and process this data" routine (seventh form), part of the 0x822F5 process cluster. 52 lines. 2 callees (Free_SizeBucketed_Thunk and ProcessAndVcallAndProcess6). 1 caller (ProcessAndSearchAndProcess9).
[entrypoint.functions."0x822F5E20"]
name = "ProcessAndFreeAndProcess7_822F5E20"

# A processor that computes and processes the data (thirty-second form). It takes a structure (a: fields at a->4 and a->8) and a structure (b: fields at b->4 and b->8). It computes (sub_825813E8, with a and b). It then computes and processes the data (ProcessAndComputeAndProcess_82C9B9B0, with other values). This is the "compute and process this data" routine (thirty-second form), part of the 0x822F5 process cluster. 450 lines. 4 callees (Free_SizeBucketed_Thunk, ProcessAndVcallAndProcess6, VirtualCall4, and sub_825813E8). 1 caller (ProcessAndSearchAndProcess8).
[entrypoint.functions."0x822F5E78"]
name = "ProcessAndComputeAndProcess32_822F5E78"

# A processor that frees and processes the data (eighth form). It takes a structure (a: fields at a->4, a->8, and a->12). When a->8 is non-zero it frees the memory (Free_SizeBucketed_Thunk_8221BE68, with a->4). It then zeros the structure (a->4, a->8, and a->12). This is the "free and process this data" routine (eighth form), part of the 0x822F6 process cluster. 54 lines. 2 callees (Free_SizeBucketed_Thunk and ProcessAndVcallAndProcess6). 1 caller (ProcessAndSearchAndProcess9).
[entrypoint.functions."0x822F6328"]
name = "ProcessAndFreeAndProcess8_822F6328"

# A processor that finds the chain root and processes the data (second form). It takes a structure (a: fields at a->4 and a->8), a value (b), a value (c), and a value (d). It finds the chain root (FindChainRoot3_82A962B0, with a). It then finds the chain root and processes the data (FindChainRoot4_828836E8, with other values). This is the "find the chain root and process this data" routine (second form), part of the 0x822F6 process cluster. 306 lines. 4 callees (FindChainRoot3, FindChainRoot4, sub_822F7068, and sub_822F72B8). 1 caller (ProcessAndSearchAndProcess9).
[entrypoint.functions."0x822F6708"]
name = "ProcessAndFindChainRootAndProcess2_822F6708"

# A processor that splices and processes the data (third form). It takes a structure (a: field at a->8), a value (b), a value (c), a value (d), and a value (e). It sets a string (SetString_822F2020, with a). It then splices and processes the data (List_SpliceNode2_8292CC88, with other values). This is the "splice and process this data" routine (third form), part of the 0x822F7 process cluster. 349 lines. 9 callees (Allocate_OrDbgBreak, List_SpliceNode2, List_SpliceNode3, ProcessAndComputeAndProcess25, SetString, sub_82171810, sub_822F1F00, sub_826C3EF0, and sub_826C3FA8). 2 callers (ProcessAndFindChainRootAndProcess2 and sub_822F7068).
[entrypoint.functions."0x822F72B8"]
name = "ProcessAndSpliceAndProcess3_822F72B8"

# A processor that appends and processes the data (sixteenth form). It takes a structure (a: field at a->0), a value (b), and a value (c). It appends an element (ProcessAndAppendElement_82229208, with a). It then appends and processes the data (ProcessAndAppendAndProcess_82228B98, with other values). This is the "append and process this data" routine (sixteenth form), part of the 0x82350 process cluster. 89 lines. 5 callees (ProcessAndAppendElement, ProcessAndCopyReferenceAndHash, ProcessAndDispatchAndResolve, ProcessAndHashString, and ProcessAndResolveTypedField). 12 callers (sub_825ACEA0, sub_82683240, sub_823944E0, sub_828577E8, sub_826779B0, sub_82526808, sub_825F0488, sub_82908E20, sub_8297B2E0, sub_824DE170, sub_824C3FE0, and sub_824FA760).
[entrypoint.functions."0x82350E30"]
name = "ProcessAndAppendAndProcess16_82350E30"

# A processor that allocates and processes the data (sixth form). It takes a structure (a: fields at a->4, a->8, and a->12). It allocates 36 bytes (Allocate_OrDbgBreak_8221F388, with a->4). It then zeros the structure (a->4, a->8, and a->12). This is the "allocate and process this data" routine (sixth form), part of the 0x8236F process cluster. 83 lines. 1 callee (Allocate_OrDbgBreak). 1 caller (sub_8236D4E0).
[entrypoint.functions."0x8236FB20"]
name = "ProcessAndAllocateAndProcess6_8236FB20"

# A processor that allocates and processes the data (seventh form). It takes a structure (a: fields at a->4, a->8, and a->12). It allocates 20 bytes (Allocate_OrDbgBreak_8221F388, with a->4). It then zeros the structure (a->4, a->8, and a->12). This is the "allocate and process this data" routine (seventh form), part of the 0x823D2 process cluster. 87 lines. 1 callee (Allocate_OrDbgBreak). 12 callers (sub_8284F0B0, ProcessAndFindAndProcess2, sub_825C0658, sub_8328BCF8, sub_825F1D78, sub_8328BD30, ProcessFloat, sub_82724AE8, sub_8322C030, sub_824CC6A0, sub_8264C430, and InitializeSubsystems).
[entrypoint.functions."0x823D2790"]
name = "ProcessAndAllocateAndProcess7_823D2790"

# A processor that splices and processes the data (fourth form). It takes a value (a), a value (b), a value (c), and a structure (d: field at d->21). It sets a string (SetString_822F2020, with a). It then splices and processes the data (List_SpliceNode4_82C239D0, with other values). This is the "splice and process this data" routine (fourth form), part of the 0x8246F process cluster. 583 lines. 9 callees (Free_SizeBucketed_Thunk, List_SpliceNode4, ProcessAndUpdateLinkedList, SetString, sub_82171810, sub_822F1F00, sub_826C3EF0, sub_826C3FA8, and sub_8273F9D8). 7 callers (ValidateAndProcessNode, sub_83229DC0, sub_82C540B8, sub_8246E8B0, sub_828C9360, sub_82C089C8, and sub_82B4E240).
[entrypoint.functions."0x8246FFC8"]
name = "ProcessAndSpliceAndProcess4_8246FFC8"

# A processor that allocates and processes the data (eighth form). It takes a structure (a: fields at a->4, a->8, and a->12) and a value (b). When b is 0xFFF or less it allocates checked memory (AllocateChecked3_82333F78, with a and b). It then stores the allocated pointer into a->4, a->8, and a->12. This is the "allocate and process this data" routine (eighth form), part of the 0x82581 process cluster. 69 lines. 2 callees (AllocateChecked3 and sub_82684B38). 5 callers (ProcessAndComputeAndProcess32, sub_82A800A0, sub_823D2CA0, sub_82A7FA88, and ProcessAndComputeAndProcess25).
[entrypoint.functions."0x825813E8"]
name = "ProcessAndAllocateAndProcess8_825813E8"

# A processor that validates and processes the data (third form). It takes a structure (a: fields at a->0 and a->4). It validates the structure (a->0 and a->4). It then processes the data (a->0 and a->4). This is the "validate and process this data" routine (third form), part of the 0x8273F process cluster. 93 lines. 0 callees (leaf function — performs raw memory operations). 12 callers (sub_825F1D78, sub_824D8638, sub_82A586E0, sub_82B5BEC8, sub_82C0FFF8, sub_82418758, sub_8218CA10, sub_82BFD118, sub_82376898, sub_82A50BA0, sub_824EBE60, and ProcessAndSpliceAndProcess4).
[entrypoint.functions."0x8273F9D8"]
name = "ProcessAndValidateAndProcess3_8273F9D8"

# A processor that copies and processes the data (ninth form). It takes a structure (a: fields at a->12, a->16, a->20, and a->28), a value (b), and a value (c). It copies and processes the data (CheckedCopy_82CA3808, with a and b). It then copies the data (MemCpy_SdkRuntime_82CA2C60, with other values). This is the "copy and process this data" routine (ninth form), part of the 0x82A1D process cluster. 128 lines. 3 callees (CheckedCopy, MemCpy_SdkRuntime, and sub_82A1DA08). 0 callers (unresolved — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x82A1D838"]
name = "ProcessAndCopyAndProcess9_82A1D838"

# A processor that allocates and processes the data (ninth form). It takes a structure (a: fields at a->4, a->8, and a->12), a value (b), and a value (c). It allocates and initializes (Allocate_Init_821A9258, with a). It then copies the data (CheckedCopy_82CA3808, with a and b). This is the "allocate and process this data" routine (ninth form), part of the 0x82A1D process cluster. 324 lines. 4 callees (Allocate_Init, CheckedCopy, Free_SizeBucketed_Thunk, and sub_82A97648). 1 caller (ProcessAndCopyAndProcess9).
[entrypoint.functions."0x82A1DA08"]
name = "ProcessAndAllocateAndProcess9_82A1DA08"

# A processor that hashes and processes the data (fifth form). It takes a structure (a: field at a->8) and a value (b). It hashes and dispatches (ProcessAndHashAndDispatch_82228488, with a and b). It then checks and processes the data (ProcessAndCheckAndProcess6_821E1768, with other values). This is the "hash and process this data" routine (fifth form), part of the 0x82A1F process cluster. 103 lines. 3 callees (ProcessAndCheckAndProcess6, ProcessAndHashAndDispatch, and ProcessAndHashString). 10 callers (sub_82A298E8, sub_825BD148, sub_82903620, sub_827EFE48, sub_8290B260, sub_8260C3B8, sub_82845010, sub_827F96B8, sub_8290B948, and sub_8253B3F0).
[entrypoint.functions."0x82A1F5B0"]
name = "ProcessAndHashAndProcess5_82A1F5B0"

# A processor that allocates and processes the data (tenth form). It takes a value (a) and a value (b). It allocates 44 bytes (Allocate_OrDbgBreak_8221F388, with a). It then initializes the structure (a->0, a->4, a->12, a->16, a->20, a->28, a->32, a->36, and a->40). This is the "allocate and process this data" routine (tenth form), part of the 0x82B4A process cluster. 90 lines. 1 callee (Allocate_OrDbgBreak). 1 caller (sub_82C6D3F0).
[entrypoint.functions."0x82B4AAD0"]
name = "ProcessAndAllocateAndProcess10_82B4AAD0"

# A processor that constructs and processes the data (fourth form). It takes a value (a), a structure (b: fields at b->24 and b->28), and a value (c). It constructs a 24-byte object (Construct24ByteObject_82375020, with a). It then pushes an element (Array_PushElement_823758C0, with other values). This is the "construct and process this data" routine (fourth form), part of the 0x82B4B process cluster. 270 lines. 5 callees (Array_PushElement, Construct24ByteObject, Free_SizeBucketed_Thunk, ProcessAndDestructAndFree, and sub_82B4B2B8). 0 callers (unresolved — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x82B4B0E0"]
name = "ProcessAndConstructAndProcess4_82B4B0E0"

# A processor that computes and processes the data (thirty-third form). It takes a structure (a: fields at a->8, a->12, and a->16). It computes (sub_82B4CC70, with a). It then computes and processes the data (ProcessAndComputeAndProcess4_822F6188, with other values). This is the "compute and process this data" routine (thirty-third form), part of the 0x82B4B process cluster. 1005 lines. 13 callees (Allocate_SizeBucketed_Thunk, ConstructFromArray, Construct_FromValue, FindChainRoot3, Free_SizeBucketed_Thunk, ProcessAndComputeAndProcess4, ProcessAndVcallAndProcess6, ProcessCountDispatch, ProcessStructureNode, sub_82B4B9A8, sub_82B4BE88, sub_82B4CC70, and sub_82CBBB58). 1 caller (ProcessAndConstructAndProcess4).
[entrypoint.functions."0x82B4B2B8"]
name = "ProcessAndComputeAndProcess33_82B4B2B8"

# A processor that constructs and processes the data (fifth form). It takes a structure (a: fields at a->4 and a->12) and a value (b). It constructs from an array (ConstructFromArray_82B4BFD8, with a). It then constructs and processes the data (ProcessAndComputeAndProcess33_82B4B2B8, with other values). This is the "construct and process this data" routine (fifth form), part of the 0x82B4B process cluster. 207 lines. 4 callees (Allocate20ByteArray, ConstructFromArray, Free_SizeBucketed_Thunk, and sub_82684B38). 1 caller (ProcessAndComputeAndProcess33).
[entrypoint.functions."0x82B4B9A8"]
name = "ProcessAndConstructAndProcess5_82B4B9A8"

# A processor that allocates and processes the data (eleventh form). It reads a .data pointer (13664) and checks a flag (bit 0). When the flag is set it allocates and processes the data (sub_82B4DF88, with a .data pointer). It then allocates (sub_82CA3700, with a value). This is the "allocate and process this data" routine (eleventh form), part of the 0x82B4D process cluster. 72 lines. 2 callees (sub_82B4DF88 and sub_82CA3700). 2 callers (sub_82B4EAD0 and sub_82B4ED00).
[entrypoint.functions."0x82B4DF08"]
name = "ProcessAndAllocateAndProcess11_82B4DF08"

# A processor that finds and processes the data (third form). It takes a structure (a: fields at a->40 and a->44) and a value (b). It enters the critical section (CriticalSection_EnterOrTry_82200688, with a). It then finds a tree leaf (FindTreeLeaf_825CB448, with a and b). This is the "find and process this data" routine (third form), part of the 0x82B4E process cluster. 426 lines. 6 callees (CriticalSection_EnterOrTry, FindTreeLeaf, ProcessAndSpliceAndProcess4, ProcessAndValidateAndProcess3, sub_82B4E508, and sub_82B4F198). 1 caller (sub_82B4EAD0).
[entrypoint.functions."0x82B4E240"]
name = "ProcessAndFindAndProcess3_82B4E240"

# A processor that processes the timebase (second form). It takes a structure (a: fields at a->40 and a->44). It gets the timebase (GetTimebase_8221EB58, with a). It then processes the timebase (sub_82B4F198, with a). This is the "process the timebase" routine (second form), part of the 0x82B4E process cluster. 307 lines. 5 callees (FindChainRoot3, Free_SizeBucketed_Thunk, GetTimebase, ProcessAndComputeAndProcess2, and sub_82B4F198). 2 callers (ProcessAndFindAndProcess3 and sub_82B4ED00).
[entrypoint.functions."0x82B4E508"]
name = "ProcessAndTimebaseAndProcess2_82B4E508"

# A processor that finds and processes the data (fourth form). It takes a value (a) and a value (b). It finds and processes the data (sub_82B4EAD0, with a). It then frees the memory (Free_SizeBucketed_Thunk_8221BE68, with a) when b is non-zero. This is the "find and process this data" routine (fourth form), part of the 0x82B4E process cluster. 50 lines. 2 callees (Free_SizeBucketed_Thunk and sub_82B4EAD0). 0 callers (unresolved — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x82B4EA80"]
name = "ProcessAndFindAndProcess4_82B4EA80"

# A processor that finds and processes the data (fifth form). It takes a structure (a: fields at a->0, a->4, a->12, and a->16). It finds and processes the data (ProcessAndFindAndProcess3_82B4E240, with a). It then finds and processes the data (ProcessAndAllocateAndProcess11_82B4DF08, with other values). This is the "find and process this data" routine (fifth form), part of the 0x82B4E process cluster. 161 lines. 3 callees (Free_SizeBucketed_Thunk, ProcessAndAllocateAndProcess11, and ProcessAndFindAndProcess3). 1 caller (ProcessAndFindAndProcess4).
[entrypoint.functions."0x82B4EAD0"]
name = "ProcessAndFindAndProcess5_82B4EAD0"

# A processor that vcalls and processes the data (thirtieth form). It takes a structure (a: field at a->16). It reads a->16 (a pointer) and the pointer's +0 field (a value). It then reads the value's +16 field (a value). This is the "vcall and process this data" routine (thirtieth form), part of the 0x82B4E process cluster. 15 lines. 0 callees (leaf function — performs raw memory operations). 0 callers (unresolved — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x82B4EBF0"]
name = "ProcessAndVcallAndProcess30_82B4EBF0"

# A processor that vcalls and processes the data (thirty-first form). It takes a structure (a: fields at a->0 and a->4) and a structure (b: fields at b->12 and b->16). It reads b->16 (a pointer) and the pointer's +0 field (a value). It then reads the value's +16 field (a value) and stores it into a->0. This is the "vcall and process this data" routine (thirty-first form), part of the 0x82B4E process cluster. 66 lines. 0 callees (leaf function — performs raw memory operations). 0 callers (unresolved — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x82B4EC08"]
name = "ProcessAndVcallAndProcess31_82B4EC08"

# A processor that vcalls and processes the data (thirty-second form). It takes a structure (a: field at a->16). It reads a->16 (a pointer) and the pointer's +0 field (a value). It then reads the value's +32 field (a value). This is the "vcall and process this data" routine (thirty-second form), part of the 0x82B4E process cluster. 15 lines. 0 callees (leaf function — performs raw memory operations). 0 callers (unresolved — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x82B4EEF8"]
name = "ProcessAndVcallAndProcess32_82B4EEF8"

# A processor that finds and processes the data (sixth form). It takes a structure (a: field at a->4) and a structure (b: field at b->0). It finds the chain root (FindChainRoot3_82A962B0, with a). It then finds and processes the data (ProcessAndFreeAndProcess_8233E468, with other values). This is the "find and process this data" routine (sixth form), part of the 0x82B4F process cluster. 153 lines. 2 callees (FindChainRoot3 and ProcessAndFreeAndProcess). 2 callers (ProcessAndTimebaseAndProcess2 and ProcessAndFindAndProcess3).
[entrypoint.functions."0x82B4F198"]
name = "ProcessAndFindAndProcess6_82B4F198"

# A processor that serializes and processes the data (third form). It takes a structure (a: fields at a->32, a->36, a->40, and a->44) and a value (b). It serializes the data (Serialize_82CA3EB8, with a). It then clears the indexed region (ProcessAndClearIndexedRegion_82BBF648, with other values). This is the "serialize and process this data" routine (third form), part of the 0x82BAE process cluster. 75 lines. 2 callees (ProcessAndClearIndexedRegion and Serialize). 7 callers (sub_82BAD1D8, sub_82BC4040, sub_82BBDE60, sub_82BC3568, sub_82BAD9D8, sub_82BAE500, and sub_82BC37B8).
[entrypoint.functions."0x82BAE2E8"]
name = "ProcessAndSerializeAndProcess3_82BAE2E8"

# A processor that reads and processes the data (fourth form). It takes a structure (a: fields at a->8 and a->12). It reads and processes the data (ProcessAndReadAndMask_82BB6880, with a). It then reads the data (sub_82BB6820, with a). This is the "read and process this data" routine (fourth form), part of the 0x82BB5 process cluster. 168 lines. 4 callees (ProcessAndReadAndMask, sub_82BB67C0, sub_82BB6820, and sub_82BC6A18). 0 callers (unresolved — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x82BB5EB0"]
name = "ProcessAndReadAndProcess4_82BB5EB0"

# A processor that hashes and processes the data (sixth form). It takes a value (a). It dispatches and processes (ProcessAndDispatchAndProcess_82BC6558, with a). It then hashes the string (ProcessAndHashString2_821E1408, with a). This is the "hash and process this data" routine (sixth form), part of the 0x82BB6 process cluster. 58 lines. 3 callees (ProcessAndDispatchAndProcess, ProcessAndDispatchByType, and ProcessAndHashString2). 2 callers (ProcessAndReadAndProcess4 and sub_82BB18A0).
[entrypoint.functions."0x82BB6820"]
name = "ProcessAndHashAndProcess6_82BB6820"

# A processor that initializes and processes the data (third form). It takes a structure (a: fields at a->0, a->8, a->16, and a->24). It initializes the structure (a->0, a->8, a->16, and a->24). It then processes the data (sub_82BBB228, with a). This is the "initialize and process this data" routine (third form), part of the 0x82BBB process cluster. 57 lines. 2 callees (sub_82BBB228 and sub_82CC0620). 2 callers (sub_82BB0D78 and sub_82BB39F8).
[entrypoint.functions."0x82BBB1C8"]
name = "ProcessAndInitializeAndProcess3_82BBB1C8"

# A processor that processes the timebase (third form). It takes a structure (a: fields at a->8, a->16, and a->24). It gets the timebase (GetTimebase_8221EB58, with a). It then processes the timebase (a->8, a->16, and a->24). This is the "process the timebase" routine (third form), part of the 0x82BBB process cluster. 71 lines. 1 callee (GetTimebase). 1 caller (ProcessAndInitializeAndProcess3).
[entrypoint.functions."0x82BBB228"]
name = "ProcessAndTimebaseAndProcess3_82BBB228"

# A processor that vcalls and processes the data (thirty-third form). It takes a structure (a: fields at a->0 and a->4), a value (b), and a value (c). It reads a .data pointer (-7716) and the pointer's +16 field (a value). It then performs a virtual call (with the pointer's +16 field). This is the "vcall and process this data" routine (thirty-third form), part of the 0x82BBC process cluster. 64 lines. 0 callees (leaf function — performs raw memory operations and virtual calls). 2 callers (sub_82BB2A70 and sub_82BC3018).
[entrypoint.functions."0x82BBC6B8"]
name = "ProcessAndVcallAndProcess33_82BBC6B8"

# A processor that vcalls and processes the data (thirty-fourth form). It takes a structure (a: fields at a->0, a->8, and a->9). It vcalls and processes the data (ProcessAndVcallAndProcess33_82BBC6B8, with a). It then initializes the structure (a->0, a->8, and a->9). This is the "vcall and process this data" routine (thirty-fourth form), part of the 0x82BC3 process cluster. 50 lines. 1 callee (ProcessAndVcallAndProcess33). 2 callers (sub_82BC0E68 and sub_82BC4760).
[entrypoint.functions."0x82BC3018"]
name = "ProcessAndVcallAndProcess34_82BC3018"

# A processor that writes to a buffer (third form). It takes a structure (a: fields at a->4, a->8, and a->12), a value (b), and a value (c). It writes to a buffer (sub_82BF2158, with a). It then copies the data (CheckedCopy_82CA3808, with a and b). This is the "write to a buffer" routine (third form), part of the 0x82BEF process cluster. 437 lines. 4 callees (CheckedCopy, ProcessAndUpdateGlobal, sub_82BEFF50, and sub_82BF2158). 1 caller (sub_82BF0378).
[entrypoint.functions."0x82BEFFB0"]
name = "ProcessAndWriteToBuffer3_82BEFFB0"

# A processor that writes to a buffer (fourth form). It takes a structure (a: fields at a->4 and a->8), a value (b), and a value (c). It writes to a buffer (ProcessAndWriteToBuffer3_82BEFFB0, with a). It then writes to a buffer (ProcessAndWriteToBuffer_82BF0458, with other values). This is the "write to a buffer" routine (fourth form), part of the 0x82BF0 process cluster. 132 lines. 1 callee (ProcessAndWriteToBuffer3). 1 caller (ProcessAndWriteToBuffer).
[entrypoint.functions."0x82BF0378"]
name = "ProcessAndWriteToBuffer4_82BF0378"

# A processor that queries and processes the data (fifth form). It takes a structure (a: field at a->0), a value (b), a structure (c: field at c->0), a value (d), a value (e), and a value (f). It queries the data (ProcessAndQueryAllocate_82B4E708, with a). It then queries and processes the data (a, b, c, d, e, and f). This is the "query and process this data" routine (fifth form), part of the 0x82C64 process cluster. 107 lines. 1 callee (ProcessAndQueryAllocate). 1 caller (sub_82C647D0).
[entrypoint.functions."0x82C64650"]
name = "ProcessAndQueryAndProcess5_82C64650"

# A processor that queries and processes the data (sixth form). It takes a value (a), a value (b), a value (c), a value (d), a value (e), and a value (f). It queries and processes the data (ProcessAndQueryAndProcess5_82C64650, with a). It then frees the memory (Free_Thunk_824FE010, with a). This is the "query and process this data" routine (sixth form), part of the 0x82C64 process cluster. 129 lines. 2 callees (Free_Thunk and ProcessAndQueryAndProcess5). 12 callers (sub_82A50EF0, sub_82AB7B68, sub_821BC7E8, sub_8266F8F0, sub_8223F1E0, sub_82B80D70, sub_822001F8, sub_82B5D3A0, sub_82AB7F10, sub_82B68128, sub_82AB2A90, and sub_82A6EA50).
[entrypoint.functions."0x82C647D0"]
name = "ProcessAndQueryAndProcess6_82C647D0"

# A processor that allocates and processes the data (twelfth form). It takes a value (a). It allocates memory (Allocate_OrDbgBreak_8221F388, with a). It then processes the data (sub_822F1F00, with a). This is the "allocate and process this data" routine (twelfth form), part of the 0x82C66 process cluster. 70 lines. 2 callees (Allocate_OrDbgBreak and sub_822F1F00). 2 callers (sub_82C673D8 and sub_82C672D8).
[entrypoint.functions."0x82C66180"]
name = "ProcessAndAllocateAndProcess12_82C66180"

# A processor that sets and processes the data (fifth form). It takes a pointer (a), a pointer (b), and a value (c). It sets a value (SetValue2_8236EC48, with a). It then sets and processes the data (a, b, and c). This is the "set and process this data" routine (fifth form), part of the 0x82C67 process cluster. 57 lines. 1 callee (SetValue2). 2 callers (sub_82C673D8 and sub_82C672D8).
[entrypoint.functions."0x82C67088"]
name = "ProcessAndSetAndProcess5_82C67088"

# A processor that grows and processes the data (eighth form). It takes a structure (a: fields at a->4 and a->12) and a value (b). It grows the data (sub_82C66EC8, with a). It then grows and processes the data (sub_82C66180, with a). This is the "grow and process this data" routine (eighth form), part of the 0x82C67 process cluster. 150 lines. 5 callees (Free_Thunk, sub_82BF8E78, ProcessAndAllocateAndProcess12, sub_82C66EC8, and ProcessAndSetAndProcess5). 1 caller (ProcessAndConstructAndProcess2).
[entrypoint.functions."0x82C672D8"]
name = "ProcessAndGrowAndProcess8_82C672D8"

# A processor that writes and processes the data (sixth form). It takes a structure (a: field at a->4). It writes a range at a position (WriteRange_AtPos_821A8F68, with a). It then resolves and writes the data (ProcessAndResolveAndWrite_82C694C0, with a). This is the "write and process this data" routine (sixth form), part of the 0x82C69 process cluster. 104 lines. 5 callees (Allocate_OrDbgBreak, Free_Thunk, ProcessAndResolveAndWrite, WriteRange_AtPos, and sub_82C6F6E0). 0 callers (unresolved — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x82C69840"]
name = "ProcessAndWriteAndProcess6_82C69840"

# A processor that writes and processes the data (seventh form). It takes a value (a) and a structure (b: field at b->4). It writes a range at a position (WriteRange_AtPos_821A8F68, with b). It then resolves and writes the data (ProcessAndResolveAndWrite_82C694C0, with a and b). This is the "write and process this data" routine (seventh form), part of the 0x82C6B process cluster. 746 lines. 13 callees (Allocate_OrDbgBreak, Free_Thunk, GetOffset36, LockedTableLookup, LookupFromStructure, ProcessAndClampGrow, ProcessAndComputeAndProcess21, ProcessAndResolveAndWrite3, ProcessAndResolveAndWrite, ProcessGlobalStruct, WriteRange_AtPos, sub_82C6F618, and sub_82CA3B30). 0 callers (unresolved — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x82C6B640"]
name = "ProcessAndWriteAndProcess7_82C6B640"

# A processor that computes and processes the data (thirty-fourth form). It takes a structure (a: fields at a->76, a->80, and a->92) and a value (b). It computes and processes the data (ProcessAndComputeAndProcess14_82B4AB70, with a). It then computes and processes the data (ProcessAndAllocateAndProcess10_82B4AAD0, with other values). This is the "compute and process this data" routine (thirty-fourth form), part of the 0x82C6D process cluster. 171 lines. 2 callees (ProcessAndComputeAndProcess14 and ProcessAndAllocateAndProcess10). 0 callers (unresolved — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x82C6D3F0"]
name = "ProcessAndComputeAndProcess34_82C6D3F0"

# A processor that clamps and processes the data (fourth form). It takes a value (a) and a structure (b: field at b->20). It clamps and processes the data (ProcessAndClampAndProcess3_82C6D518, with a). It then clamps and processes the data (a and b). This is the "clamp and process this data" routine (fourth form), part of the 0x82C6D process cluster. 95 lines. 1 callee (ProcessAndClampAndProcess3). 2 callers (sub_82C6D710 and sub_82C6D760).
[entrypoint.functions."0x82C6D5C8"]
name = "ProcessAndClampAndProcess4_82C6D5C8"

# A processor that clamps and processes the data (fifth form). It takes a value (a) and a structure (b: fields at b->76 and b->104). It clamps and processes the data (ProcessAndClampAndProcess4_82C6D5C8, with b). It then clamps and processes the data (a and b). This is the "clamp and process this data" routine (fifth form), part of the 0x82C6D process cluster. 70 lines. 1 callee (ProcessAndClampAndProcess4). 0 callers (unresolved — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x82C6D760"]
name = "ProcessAndClampAndProcess5_82C6D760"

# A processor that writes and processes the data (eighth form). It takes a structure (a: fields at a->0, a->80, a->84, a->88, and a->92), a structure (b: field at b->20), a value (c), and a value (d). It writes and processes the data (ProcessAndRangeUpdate_82C6F2E0, with a). It then processes and frees the data (ProcessAndProcessAndFree_82C6EB30, with b). This is the "write and process this data" routine (eighth form), part of the 0x82C6F process cluster. 70 lines. 2 callees (ProcessAndProcessAndFree and ProcessAndRangeUpdate). 1 caller (ProcessAndWriteAndProcess7).
[entrypoint.functions."0x82C6F618"]
name = "ProcessAndWriteAndProcess8_82C6F618"

# A processor that writes and processes the data (ninth form). It takes a value (a), a value (b), and a value (c). It writes a range at a position (WriteRange_AtPos_821A8F68, with a). It then writes and processes the data (ProcessAndRangeUpdate_82C6F2E0, with b). This is the "write and process this data" routine (ninth form), part of the 0x82C6F process cluster. 139 lines. 4 callees (Free_Thunk, ProcessAndRangeUpdate, RangeUpdate, and WriteRange_AtPos). 1 caller (ProcessAndWriteAndProcess6).
[entrypoint.functions."0x82C6F6E0"]
name = "ProcessAndWriteAndProcess9_82C6F6E0"

# A processor that recurses and processes the data (second form). It takes a structure (a: fields at a->20 and a->32) and a structure (b: field at b->12). It recurses and processes the data (ProcessAndRecurseAndProcess_82C89110, with a). It then links and processes the data (ProcessAndLinkAndProcess_82C88F88, with a). This is the "recurse and process this data" routine (second form), part of the 0x82C89 process cluster. 64 lines. 4 callees (ProcessAndFreeAndProcess, ProcessAndLinkAndProcess, ProcessAndRecurseAndProcess, and sub_82C89028). 3 callers (sub_82C88A58, sub_82C88630, and sub_82C88AC0).
[entrypoint.functions."0x82C89208"]
name = "ProcessAndRecurseAndProcess2_82C89208"

# A processor that compares and processes the data. It takes a pointer (a) and a pointer (b). It compares the data (a and b). It then processes the data (a and b). This is the "compare and process this data" routine, part of the 0x82C97 process cluster. 64 lines. 0 callees (leaf function — performs raw memory operations). 3 callers (sub_82C984F0, sub_82C982E8, and sub_82C97EE0).
[entrypoint.functions."0x82C97030"]
name = "ProcessAndCompareAndProcess_82C97030"

# A processor that iterates and processes the data (fifth form). It takes a structure (a: fields at a->0, a->4, and a->20). It iterates through a list (a, in 4-byte chunks) and performs a virtual call (with a->0 and a->20). This is the "iterate and process this data" routine (fifth form), part of the 0x82C99 process cluster. 81 lines. 0 callees (leaf function — performs raw memory operations and virtual calls). 2 callers (sub_82C9B770 and sub_82C9B0C0).
[entrypoint.functions."0x82C99780"]
name = "ProcessAndIterateAndProcess5_82C99780"

# A processor that dispatches and processes the data (twenty-sixth form). It takes a value (a), a value (b), and a value (c). It dispatches the data (a, b, and c). It then processes the data (a, b, and c). This is the "dispatch and process this data" routine (twenty-sixth form), part of the 0x82CA2 process cluster. 175 lines. 0 callees (leaf function — performs raw memory operations). 0 callers (unresolved — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x82CA22E8"]
name = "ProcessAndDispatchAndProcess26_82CA22E8"

# A processor that processes the TLS (third form). It processes the TLS (ProcessAndTlsAndProcess2_82CAC520, with no arguments). It then processes the data (sub_82CA9578, with 16). This is the "process the TLS" routine (third form), part of the 0x82CAC process cluster. 41 lines. 2 callees (sub_82CA9578 and ProcessAndTlsAndProcess2). 12 callers (sub_82CA7E48, sub_82CA7F00, Func_82CA7C70, sub_82CA3388, sub_82CB4A18, sub_82CB52C0, sub_82CA7C30, sub_82CA79C8, sub_83002950, sub_82CB5150, sub_82CB4818, and sub_82CB5580).
[entrypoint.functions."0x82CAC610"]
name = "ProcessAndTlsAndProcess3_82CAC610"

# A processor that sets the timebase (fourth form). It takes a structure (a: field at a->0). It sets the timebase (KeQueryPerformanceFrequency, with a). This is the "set the timebase" routine (fourth form), part of the 0x82CC0 process cluster. 34 lines. 0 callees (leaf function — performs raw memory operations and calls KeQueryPerformanceFrequency). 9 callers (ProcessAndInitializeAndProcess3, sub_82BA0220, sub_8234A128, sub_82D02BF8, sub_82CD4448, sub_82CFF878, sub_830CD2B0, sub_82B3D380, and sub_82FFE500).
[entrypoint.functions."0x82CC0620"]
name = "ProcessAndTimebaseAndProcess4_82CC0620"

# A processor that returns a value. It returns a value (0xC0004001, with no arguments). This is the "return this value" routine, part of the 0x8302B process cluster. 10 lines. 0 callees (leaf function — performs raw memory operations). 0 callers (unresolved — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x8302B3E0"]
name = "ProcessAndReturnAndProcess_8302B3E0"

# A processor that allocates and processes the data (thirteenth form). It takes a structure (a: fields at a->0 and a->8). It allocates and processes the data (ProcessAndResolveGlobal_82CA3C68, with a). It then allocates and processes the data (MemSet_SdkRuntime, with a). This is the "allocate and process this data" routine (thirteenth form), part of the 0x8322F process cluster. 102 lines. 3 callees (MemSet_SdkRuntime, ProcessAndResolveGlobal, and VirtualAllocate). 1 caller (ProcessListTail).
[entrypoint.functions."0x8322FE78"]
name = "ProcessAndAllocateAndProcess13_8322FE78"

# A processor that allocates and processes the data (fourteenth form). It takes a structure (a: field at a->0), a value (b), a value (c), and a value (d). It allocates and processes the data (ProcessAndResolveGlobal_82CA3C68, with a). It then allocates and processes the data (MemSet_SdkRuntime, with a). This is the "allocate and process this data" routine (fourteenth form), part of the 0x83236 process cluster. 113 lines. 2 callees (MemSet_SdkRuntime and ProcessAndResolveGlobal). 2 callers (sub_829FCEE8 and sub_8240D938).
[entrypoint.functions."0x83236480"]
name = "ProcessAndAllocateAndProcess14_83236480"

# A processor that processes the data. It takes a structure (a: fields at a->324, a->368, a->372, and a->376). It processes the data (a, with a->324, a->368, a->372, and a->376). This is the "process this data" routine, part of the 0x82174 process cluster. 266 lines. 6 callees (GetSingleton, ProcessIndexedRegion, sub_82BA9938, sub_82BA9988, sub_82BA9B40, and sub_82BAC4E8). 1 caller (sub_822AC668).
[entrypoint.functions."0x82174D38"]
name = "ProcessAndProcessAndProcess_82174D38"

# A processor that accesses and processes the data (sixth form). It takes a pointer (a). It accesses the data (a->873). It then processes the data (a->873). This is the "access and process this data" routine (sixth form), part of the 0x8217F process cluster. 8 lines. 0 callees (leaf function — performs raw memory operations). 0 callers (unresolved — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x8217F248"]
name = "ProcessAndAccessAndProcess6_8217F248"

# A processor that processes the data (second form). It takes a structure (a: fields at a->8, a->12, and a->24). It processes the data (a, with a->8, a->12, and a->24). This is the "process this data" routine (second form), part of the 0x82184 process cluster. 61 lines. 1 callee (sub_82284218). 0 callers (unresolved — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x82184518"]
name = "ProcessAndProcessAndProcess2_82184518"

# A processor that processes the data (third form). It takes a structure (a: fields at a->0, a->4, a->92, and a->1724). It processes the data (a, with a->0, a->4, a->92, and a->1724). This is the "process this data" routine (third form), part of the 0x82185 process cluster. 142 lines. 1 callee (MemSet_SdkRuntime). 0 callers (unresolved — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x821854A8"]
name = "ProcessAndProcessAndProcess3_821854A8"

# A processor that processes the data (fourth form). It takes a structure (a: fields at a->4 and a->8). It processes the data (a, with a->4 and a->8). This is the "process this data" routine (fourth form), part of the 0x82188 process cluster. 232 lines. 3 callees (FloatToIntProcess, GetSingleton, and ProcessIndexedRegion). 0 callers (unresolved — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x82188720"]
name = "ProcessAndProcessAndProcess4_82188720"

# A processor that processes the data (fifth form). It takes a pointer (a) and a pointer (b). It processes the data (a and b). This is the "process this data" routine (fifth form), part of the 0x8219E process cluster. 36 lines. 1 callee (ProcessAndCountAndProcess). 0 callers (unresolved — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x8219EDC8"]
name = "ProcessAndProcessAndProcess5_8219EDC8"

# A processor that processes the data (sixth form). It takes a value (a), a structure (b: field at b->8), a value (c), a value (d), and a value (e). It processes the data (a, b, c, d, and e). This is the "process this data" routine (sixth form), part of the 0x821AC process cluster. 379 lines. 8 callees (Allocate_OrDbgBreak, List_SpliceNode4, ProcessAndUpdateLinkedList, SetString, sub_82171810, sub_822F1F00, sub_826C3EF0, and sub_826C3FA8). 3 callers (sub_821ACF30, sub_82420FB8, and sub_825F2B58).
[entrypoint.functions."0x821ACC90"]
name = "ProcessAndProcessAndProcess6_821ACC90"

# A processor that processes the data (seventh form). It takes a structure (a: fields at a->4, a->16, and a->20). It processes the data (a, with a->4, a->16, and a->20). This is the "process this data" routine (seventh form), part of the 0x821D2 process cluster. 345 lines. 8 callees (GrowAndZeroBuffer, ProcessAndApplyAndAccessElement, ProcessAndConstructAndDispatch, ProcessAndGuardedDispatch, ProcessAndStoreElement, ProcessAndVcallAndProcess15, ProcessApply, and sub_822C52B0). 0 callers (unresolved — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x821D2C70"]
name = "ProcessAndProcessAndProcess7_821D2C70"

# A processor that processes the data (eighth form). It takes a structure (a: field at a->4), a pointer (b), and a value (c). It processes the data (a, b, and c). This is the "process this data" routine (eighth form), part of the 0x82284 process cluster. 136 lines. 1 callee (Access2DElement). 1 caller (ProcessAndProcessAndProcess2).
[entrypoint.functions."0x82284218"]
name = "ProcessAndProcessAndProcess8_82284218"

# A processor that processes the data (ninth form). It takes a structure (a: field at a->4), a pointer (b), and a value (c). It processes the data (a, b, and c). This is the "process this data" routine (ninth form), part of the 0x82284 process cluster. 325 lines. 3 callees (FloatToIntProcess, LinearSearch, and sub_82BACBE8). 1 caller (sub_822AC668).
[entrypoint.functions."0x82284508"]
name = "ProcessAndProcessAndProcess9_82284508"

# A processor that allocates and processes the data (fifteenth form). It takes a value (a). It allocates and processes the data (Allocate_OrDbgBreak_8221F388, with a). This is the "allocate and process this data" routine (fifteenth form), part of the 0x822AA process cluster. 66 lines. 2 callees (Allocate_OrDbgBreak and sub_822F1F00). 3 callers (sub_82A7BDB8, sub_822AAD68, and sub_82A4D7F0).
[entrypoint.functions."0x822AACF8"]
name = "ProcessAndAllocateAndProcess15_822AACF8"

# A processor that processes the data (tenth form). It takes a structure (a: fields at a->0, a->32, a->40, a->852, and a->873). It processes the data (a, with a->0, a->32, a->40, a->852, and a->873). This is the "process this data" routine (tenth form), part of the 0x822AC process cluster. 270 lines. 4 callees (ProcessAndIndexedRegion2, ProcessAndStoreTypedValue, ProcessAndProcessAndProcess, and ProcessAndProcessAndProcess9). 0 callers (unresolved — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x822AC668"]
name = "ProcessAndProcessAndProcess10_822AC668"

# A processor that processes the data (eleventh form). It takes a pointer (a) and a pointer (b). It processes the data (a and b). This is the "process this data" routine (eleventh form), part of the 0x822D0 process cluster. 22 lines. 0 callees (leaf function — performs raw memory operations). 0 callers (unresolved — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x822D0F10"]
name = "ProcessAndProcessAndProcess11_822D0F10"

# A processor that processes the data (twelfth form). It takes a structure (a: fields at a->480, a->484, and a->488). It processes the data (a, with a->480, a->484, and a->488). This is the "process this data" routine (twelfth form), part of the 0x822D3 process cluster. 18 lines. 0 callees (leaf function — performs raw memory operations). 0 callers (unresolved — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x822D3EA0"]
name = "ProcessAndProcessAndProcess12_822D3EA0"

# A processor that allocates and processes the data (sixteenth form). It takes a structure (a: field at a->0) and a structure (b: field at b->0). It allocates and processes the data (Allocate_OrDbgBreak_8221F388, with a). It then constructs a reference-counted object (ConstructRefCounted_8222CF18, with a). This is the "allocate and process this data" routine (sixteenth form), part of the 0x822F2 process cluster. 101 lines. 5 callees (Allocate_OrDbgBreak, ConstructRefCounted, CopyAssign_RefCounted2, DestructRefCounted, and Free_SizeBucketed_Thunk). 12 callers (sub_82355C08, sub_8233AE50, sub_826928F8, sub_8233EC30, sub_826935F0, sub_825F45A8, sub_8281A568, sub_826939A0, sub_82691A70, sub_822F27C0, sub_82359BE8, and sub_82466098).
[entrypoint.functions."0x822F2400"]
name = "ProcessAndAllocateAndProcess16_822F2400"

# A processor that searches and processes the data (tenth form). It takes a value (a) and a pointer (b). It searches and processes the data (a and b). This is the "search and process this data" routine (tenth form), part of the 0x822F4 process cluster. 153 lines. 5 callees (ConstructString, Destruct3, StrCmpIgnoreCase, sub_822F4458, and sub_829F85B0). 3 callers (sub_822F3B18, sub_82374240, and sub_82354A70).
[entrypoint.functions."0x822F4260"]
name = "ProcessAndSearchAndProcess10_822F4260"

# A processor that processes the data (thirteenth form). It takes a value (a), a structure (b: fields at b->4 and b->8), a pointer (c), and a value (d). It processes the data (a, b, c, and d). This is the "process this data" routine (thirteenth form), part of the 0x82307 process cluster. 306 lines. 4 callees (sub_82308190, sub_82308418, sub_8273ACB8, and sub_8273FAF8). 1 caller (ChainSearch4).
[entrypoint.functions."0x823074B8"]
name = "ProcessAndProcessAndProcess13_823074B8"

# A processor that processes the data (fourteenth form). It takes a value (a), a structure (b: field at b->8), a value (c), a value (d), and a value (e). It processes the data (a, b, c, d, and e). This is the "process this data" routine (fourteenth form), part of the 0x82308 process cluster. 349 lines. 9 callees (Allocate_OrDbgBreak, SetString, sub_82171810, sub_822F1F00, sub_82308C28, sub_8234DF98, sub_824AF0E8, sub_826C3EF0, and sub_826C3FA8). 2 callers (ProcessAndProcessAndProcess13 and sub_82308190).
[entrypoint.functions."0x82308418"]
name = "ProcessAndProcessAndProcess14_82308418"

# A processor that processes the data (fifteenth form). It takes a structure (a: field at a->8), a pointer (b), and a pointer (c). It processes the data (a, b, and c). This is the "process this data" routine (fifteenth form), part of the 0x82370 process cluster. 83 lines. 0 callees (leaf function — performs raw memory operations). 2 callers (sub_8236FEC0 and sub_82371EE8).
[entrypoint.functions."0x82370E60"]
name = "ProcessAndProcessAndProcess15_82370E60"

# A processor that processes the data (sixteenth form). It takes a structure (a: field at a->0), a value (b), and a structure (c: fields at c->44 and c->48). It processes the data (a, b, and c). This is the "process this data" routine (sixteenth form), part of the 0x82375 process cluster. 57 lines. 1 callee (ProcessAndComputeAndProcess12). 0 callers (unresolved — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x823757D8"]
name = "ProcessAndProcessAndProcess16_823757D8"

# A processor that processes the data (seventeenth form). It takes a value (a) and a pointer (b). It processes the data (a and b). This is the "process this data" routine (seventeenth form), part of the 0x823F8 process cluster. 133 lines. 3 callees (HashCstring, sub_822F8628, and sub_823FE8F8). 4 callers (sub_8259D6C8, sub_8259D510, sub_8259D3C8, and sub_8259D5F8).
[entrypoint.functions."0x823F8828"]
name = "ProcessAndProcessAndProcess17_823F8828"

# A processor that processes the data (eighteenth form). It takes a structure (a: field at a->0) and a structure (b: field at b->0). It processes the data (a and b). This is the "process this data" routine (eighteenth form), part of the 0x823FC process cluster. 375 lines. 10 callees (Allocate_OrDbgBreak, ConstructRefCounted, DestructRefCounted, GetSingleton, ProcessAndInitializeString, ProcessIndexedRegion, Release_RefCounted, StoreValueGrow2, sub_8281A568, and sub_82BAA3D0). 0 callers (unresolved — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x823FC460"]
name = "ProcessAndProcessAndProcess18_823FC460"

# A processor that processes the data (nineteenth form). It takes a pointer (a) and a structure (b: fields at b->0, b->12, b->16, and b->20). It processes the data (a and b). This is the "process this data" routine (nineteenth form), part of the 0x823FC process cluster. 120 lines. 0 callees (leaf function — performs raw memory operations). 0 callers (unresolved — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x823FC6E0"]
name = "ProcessAndProcessAndProcess19_823FC6E0"

# A processor that processes the data (twentieth form). It takes a structure (a: field at a->0) and a pointer (b). It processes the data (a and b). This is the "process this data" routine (twentieth form), part of the 0x823FE process cluster. 123 lines. 6 callees (DestructRefCounted, IncrementGlobalCounter2, IsNonZero, ProcessAndConstructAndProcess12, ProcessAndVcallAndDestruct3, and sub_82C63FB8). 7 callers (sub_823FACE0, sub_82378FA0, sub_823790B8, sub_826CAF30, sub_823FE5A0, sub_8240AF70, and sub_823FE6F0).
[entrypoint.functions."0x823FE828"]
name = "ProcessAndProcessAndProcess20_823FE828"

# A processor that processes the data (twenty-first form). It takes a structure (a: field at a->0) and a structure (b: field at b->0). It processes the data (a and b). This is the "process this data" routine (twenty-first form), part of the 0x823FE process cluster. 175 lines. 7 callees (ConstructRefCounted, DestructRefCounted, ProcessAndConstructAndProcess12, ProcessAndVcallAndProcess16, sub_823FECA0, sub_8259D6C8, and sub_82B3C670). 6 callers (sub_823FE5A0, sub_823FE6F0, sub_826CAF30, sub_82378FA0, sub_823790B8, and sub_823FACE0).
[entrypoint.functions."0x823FE988"]
name = "ProcessAndProcessAndProcess21_823FE988"

# A processor that processes the data (twenty-second form). It takes a structure (a: field at a->0) and a structure (b: fields at b->0 and b->4). It processes the data (a and b). This is the "process this data" routine (twenty-second form), part of the 0x823FE process cluster. 84 lines. 0 callees (leaf function — performs raw memory operations). 2 callers (ProcessAndProcessAndProcess21 and sub_823FE6F0).
[entrypoint.functions."0x823FECA0"]
name = "ProcessAndProcessAndProcess22_823FECA0"

# A processor that processes the data (twenty-third form). It takes a structure (a: field at a->0), a pointer (b), and a pointer (c). It processes the data (a, b, and c). This is the "process this data" routine (twenty-third form), part of the 0x82416 process cluster. 83 lines. 2 callees (ProcessAndAppendElement and ProcessAndHashString). 4 callers (sub_827EFAB8, sub_828BD288, sub_82415778, and sub_8253A510).
[entrypoint.functions."0x82416140"]
name = "ProcessAndProcessAndProcess23_82416140"

# A processor that processes the data (twenty-fourth form). It takes a structure (a: field at a->0) and a structure (b: field at b->8). It processes the data (a and b). This is the "process this data" routine (twenty-fourth form), part of the 0x824EF process cluster. 70 lines. 3 callees (ProcessAndAppendStringNode, ProcessAndDispatchTypedValue, and sub_822B9510). 12 callers (sub_8278CFD8, sub_827992D8, sub_823D7960, sub_82602160, sub_823A24A0, sub_82920338, sub_8297F4E8, sub_827EDF10, sub_8243BF10, sub_829957B8, sub_82950618, and sub_82939B88).
[entrypoint.functions."0x824EF300"]
name = "ProcessAndProcessAndProcess24_824EF300"

# A processor that processes the data (twenty-fifth form). It takes a structure (a: field at a->8), a structure (b: field at b->8), and a structure (c: field at c->8). It processes the data (a, b, and c). This is the "process this data" routine (twenty-fifth form), part of the 0x824EF process cluster. 107 lines. 5 callees (ProcessAndAccessAndShift, ProcessAndAppendElement, ProcessAndHashAndDispatch, ProcessAndHashString, and sub_821E5168). 9 callers (sub_8253B3F0, sub_8260C3B8, sub_827EFE48, sub_827F96B8, sub_82845010, sub_825BD148, sub_8290B260, sub_8290B948, and sub_82903620).
[entrypoint.functions."0x824EF558"]
name = "ProcessAndProcessAndProcess25_824EF558"

# A processor that processes the data (twenty-sixth form). It takes a pointer (a), a pointer (b), and a pointer (c). It processes the data (a, b, and c). This is the "process this data" routine (twenty-sixth form), part of the 0x8259D process cluster. 145 lines. 4 callees (HashCstring, StringInsert, ProcessAndProcessAndProcess17, and sub_823FE8F8). 12 callers (ProcessAndProcessAndProcess21, sub_8259DA00, sub_826C72A8, sub_828D0220, sub_8283FD80, sub_828FCA50, sub_828E4360, sub_828E0A90, sub_828CAA88, sub_82337C68, sub_826CBA78, and sub_82409CF0).
[entrypoint.functions."0x8259D6C8"]
name = "ProcessAndProcessAndProcess26_8259D6C8"

# A processor that processes the data (twenty-seventh form). It takes a value (a), a structure (b: field at b->4), and a pointer (c). It processes the data (a, b, and c). This is the "process this data" routine (twenty-seventh form), part of the 0x825F2 process cluster. 183 lines. 2 callees (ProcessChain2 and ProcessAndProcessAndProcess6). 3 callers (ProcessAndFindTreeLeafLocked, sub_8223F1E0, and sub_82420FB8).
[entrypoint.functions."0x825F2B58"]
name = "ProcessAndProcessAndProcess27_825F2B58"

# A processor that processes the data (twenty-eighth form). It takes a structure (a: field at a->0), a value (b), and a value (c). It processes the data (a, b, and c). This is the "process this data" routine (twenty-eighth form), part of the 0x8260D process cluster. 89 lines. 5 callees (ProcessAndAppendElement, ProcessAndCopyReferenceAndHash, ProcessAndDispatchAndResolve, ProcessAndHashString, and ProcessAndResolveTypedField). 1 caller (sub_8260C3B8).
[entrypoint.functions."0x8260D020"]
name = "ProcessAndProcessAndProcess28_8260D020"

# A processor that processes the data (twenty-ninth form). It takes a value (a), a value (b), and a value (c). It processes the data (a, b, and c). This is the "process this data" routine (twenty-ninth form), part of the 0x8281A process cluster. 1270 lines. 31 callees (Allocate_OrDbgBreak, Allocate_SizeBucketed_Thunk, ChainSearch4, ChainSearch, Construct12Byte, Construct8, ConstructRefCounted, Destruct4, Free_SizeBucketed_Thunk, IsNonZero, Process12Byte, ProcessAndComputeVector, ProcessAndVcallAndDestruct3, ProcessAtomic2, ProcessLineParse, ProcessRecursive, ProcessString2, ProcessString3, Release_RefCounted, Release_RefCounted, ScanLineLength, StrCmp_Signed, ProcessAndAllocateAndProcess16, sub_8235E278, sub_823F89B0, sub_8246E8B0, sub_8254C780, sub_82A3B298, sub_82C62A88, sub_82C63FB8, and sub_82CBBB58). 1 caller (ProcessAndProcessAndProcess18).
[entrypoint.functions."0x8281A568"]
name = "ProcessAndProcessAndProcess29_8281A568"

# A processor that processes the data (thirtieth form). It takes a structure (a: fields at a->0, a->4, a->8, a->12, and a->16), a pointer (b), a pointer (c), and a pointer (d). It processes the data (a, b, c, and d). This is the "process this data" routine (thirtieth form), part of the 0x828C9 process cluster. 107 lines. 3 callees (DestructRefCounted, ProcessConstructString, and ProcessToLower). 2 callers (sub_827CD128 and ProcessHashLookup).
[entrypoint.functions."0x828C9450"]
name = "ProcessAndProcessAndProcess30_828C9450"

# A processor that processes the data (thirty-first form). It takes a structure (a: field at a->0), a value (b), and a value (c). It processes the data (a, b, and c). This is the "process this data" routine (thirty-first form), part of the 0x82903 process cluster. 89 lines. 5 callees (ProcessAndAppendElement, ProcessAndCopyReferenceAndHash, ProcessAndDispatchAndResolve, ProcessAndHashString, and ProcessAndResolveTypedField). 3 callers (sub_8260C3B8, sub_8290B948, and sub_82903620).
[entrypoint.functions."0x82903C38"]
name = "ProcessAndProcessAndProcess31_82903C38"

# A processor that processes the data (thirty-second form). It takes a structure (a: field at a->32) and a value (b). It processes the data (a and b). This is the "process this data" routine (thirty-second form), part of the 0x829DD process cluster. 138 lines. 2 callees (ParseLine and ScanLineLength). 0 callers (unresolved — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x829DD350"]
name = "ProcessAndProcessAndProcess32_829DD350"

# A processor that processes the data (thirty-third form). It takes a value (a) and a value (b). It processes the data (a and b). This is the "process this data" routine (thirty-third form), part of the 0x82A2F process cluster. 140 lines. 1 callee (ProcessAndComputeElementOffset). 1 caller (ProcessAndSearchAndProcess7).
[entrypoint.functions."0x82A2FD80"]
name = "ProcessAndProcessAndProcess33_82A2FD80"

# A processor that processes the data (thirty-fourth form). It takes a value (a) and a value (b). It processes the data (a and b). This is the "process this data" routine (thirty-fourth form), part of the 0x82A3B process cluster. 103 lines. 0 callees (leaf function — performs raw memory operations). 1 caller (ProcessAndProcessAndProcess29).
[entrypoint.functions."0x82A3B298"]
name = "ProcessAndProcessAndProcess34_82A3B298"

# A processor that processes the data (thirty-fifth form). It takes a structure (a: fields at a->4 and a->8) and a value (b). It processes the data (a and b). This is the "process this data" routine (thirty-fifth form), part of the 0x82A7B process cluster. 95 lines. 2 callees (CheckedCopy and sub_82A7BDB8). 3 callers (sub_82B6AC38, sub_82A798F0, and sub_82B31CA0).
[entrypoint.functions."0x82A7BD18"]
name = "ProcessAndProcessAndProcess35_82A7BD18"

# A processor that processes the data (thirty-sixth form). It takes a structure (a: fields at a->4, a->8, and a->12), a pointer (b), and a value (c). It processes the data (a, b, and c). This is the "process this data" routine (thirty-sixth form), part of the 0x82A7B process cluster. 368 lines. 4 callees (CheckedCopy, Free_SizeBucketed_Thunk, ProcessAndAllocateAndProcess15, and sub_82A97648). 1 caller (ProcessAndProcessAndProcess35).
[entrypoint.functions."0x82A7BDB8"]
name = "ProcessAndProcessAndProcess36_82A7BDB8"

# A processor that processes the data (thirty-seventh form). It takes a value (a). It processes the data (a). This is the "process this data" routine (thirty-seventh form), part of the 0x82B3C process cluster. 94 lines. 3 callees (DestructRefCounted, ProcessAndConstructAndProcess12, and QueryFileAttributes). 4 callers (sub_82692F08, sub_82692600, ProcessAndProcessAndProcess21, and sub_825F3D58).
[entrypoint.functions."0x82B3C670"]
name = "ProcessAndProcessAndProcess37_82B3C670"

# A processor that processes the data (thirty-eighth form). It takes a structure (a: fields at a->8 and a->24), a value (b), and a value (c). It processes the data (a, b, and c). This is the "process this data" routine (thirty-eighth form), part of the 0x82B49 process cluster. 115 lines. 1 callee (HashLookupWithIndex). 0 callers (unresolved — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x82B49530"]
name = "ProcessAndProcessAndProcess38_82B49530"

# A processor that processes the data (thirty-ninth form). It takes a value (a) and a pointer (b). It processes the data (a and b). This is the "process this data" routine (thirty-ninth form), part of the 0x82B6A process cluster. 1614 lines. 10 callees (Allocate_OrDbgBreak, Allocate_SizeBucketed_Thunk, GetGlobalField, MemCpy_SdkRuntime, ProcessAndCopyStructRegion, ProcessAndProcessAndProcess35, sub_82B6A9B0, sub_82B6C380, sub_82C647D0, and sub_82CBBB58). 1 caller (ProcessAndFindTreeLeafLocked).
[entrypoint.functions."0x82B6AC38"]
name = "ProcessAndProcessAndProcess39_82B6AC38"

# A processor that processes the data (fortieth form). It takes a structure (a: fields at a->4 and a->8), a value (b), and a pointer (c). It processes the data (a, b, and c). This is the "process this data" routine (fortieth form), part of the 0x82B6C process cluster. 124 lines. 1 callee (sub_82B6C650). 1 caller (ProcessAndProcessAndProcess39).
[entrypoint.functions."0x82B6C380"]
name = "ProcessAndProcessAndProcess40_82B6C380"

# A processor that processes the data (forty-first form). It takes a structure (a: field at a->4), a pointer (b), and a value (c). It processes the data (a, b, and c). This is the "process this data" routine (forty-first form), part of the 0x82B6C process cluster. 400 lines. 5 callees (Free_SizeBucketed_Thunk, sub_82A97648, sub_82B6CD40, sub_82B6CDB0, and sub_82B6CE30). 1 caller (ProcessAndProcessAndProcess40).
[entrypoint.functions."0x82B6C650"]
name = "ProcessAndProcessAndProcess41_82B6C650"

# A processor that processes the data (forty-second form). It takes a value (a), a pointer (b), and a pointer (c). It processes the data (a, b, and c). This is the "process this data" routine (forty-second form), part of the 0x82B6C process cluster. 74 lines. 0 callees (leaf function — performs raw memory operations). 1 caller (ProcessAndProcessAndProcess41).
[entrypoint.functions."0x82B6CD40"]
name = "ProcessAndProcessAndProcess42_82B6CD40"

# A processor that processes the data (forty-third form). It takes a value (a). It processes the data (a). This is the "process this data" routine (forty-third form), part of the 0x82B6C process cluster. 72 lines. 2 callees (Allocate_OrDbgBreak and sub_822F1F00). 1 caller (ProcessAndProcessAndProcess41).
[entrypoint.functions."0x82B6CDB0"]
name = "ProcessAndProcessAndProcess43_82B6CDB0"

# A processor that processes the data (forty-fourth form). It takes a structure (a: fields at a->0, a->4, a->8, a->10, a->12, a->16, a->18, and a->20), a value (b), and a value (c). It processes the data (a, b, and c). This is the "process this data" routine (forty-fourth form), part of the 0x82BA9 process cluster. 470 lines. 7 callees (GetSingleton, ProcessAndClearIndexedRegion, ProcessIndexedRegion2, Serialize, StringInsert, sub_82BAC658, and sub_82BACD98). 1 caller (sub_82BE75D8).
[entrypoint.functions."0x82BA92E0"]
name = "ProcessAndProcessAndProcess44_82BA92E0"

# A processor that processes the data (forty-fifth form). It takes a structure (a: field at a->12) and a value (b). It processes the data (a and b). This is the "process this data" routine (forty-fifth form), part of the 0x82BAA process cluster. 86 lines. 4 callees (GetSingleton, ProcessAndCompareAndProcess, ProcessIndexedRegion, and StringInsert). 0 callers (unresolved — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x82BAA048"]
name = "ProcessAndProcessAndProcess45_82BAA048"

# A processor that sets and processes the data (sixth form). It takes a value (a). It sets the data (a). This is the "set and process this data" routine (sixth form), part of the 0x82BAA process cluster. 8 lines. 0 callees (leaf function — performs raw memory operations). 0 callers (unresolved — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x82BAA110"]
name = "ProcessAndSetAndProcess6_82BAA110"

# A processor that sets and processes the data (seventh form). It takes a value (a). It sets the data (a). This is the "set and process this data" routine (seventh form), part of the 0x82BAA process cluster. 8 lines. 0 callees (leaf function — performs raw memory operations). 0 callers (unresolved — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x82BAA120"]
name = "ProcessAndSetAndProcess7_82BAA120"

# A processor that sets and processes the data (eighth form). It takes a value (a). It sets the data (a). This is the "set and process this data" routine (eighth form), part of the 0x82BAA process cluster. 8 lines. 0 callees (leaf function — performs raw memory operations). 0 callers (unresolved — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x82BAA128"]
name = "ProcessAndSetAndProcess8_82BAA128"

# A processor that processes the data (forty-sixth form). It takes a structure (a: field at a->0) and a structure (b: fields at b->0 and b->4). It processes the data (a and b). This is the "process this data" routine (forty-sixth form), part of the 0x82BAA process cluster. 130 lines. 3 callees (EraseByteFromBuffer, ProcessAndCompareAndProcess, and StringInsert). 1 caller (sub_82BB0D78).
[entrypoint.functions."0x82BAA238"]
name = "ProcessAndProcessAndProcess46_82BAA238"

# A processor that processes the data (forty-seventh form). It takes a structure (a: field at a->-12) and a structure (b: field at b->-16). It processes the data (a and b). This is the "process this data" routine (forty-seventh form), part of the 0x82BAA process cluster. 48 lines. 2 callees (GetSingleton and ProcessIndexedRegion). 6 callers (sub_82BC0F90, sub_82BC4880, sub_82BA9708, sub_82BB3BC8, sub_82BB2A70, and sub_82BC3B68).
[entrypoint.functions."0x82BAA458"]
name = "ProcessAndProcessAndProcess47_82BAA458"

# A processor that processes the data (forty-eighth form). It takes a structure (a: fields at a->32, a->36, a->40, and a->44) and a value (b). It processes the data (a and b). This is the "process this data" routine (forty-eighth form), part of the 0x82BAC process cluster. 89 lines. 2 callees (ProcessAndIndexRegionAndProcess and Serialize). 1 caller (ProcessAndProcessAndProcess44).
[entrypoint.functions."0x82BAC658"]
name = "ProcessAndProcessAndProcess48_82BAC658"

# A processor that processes the data (forty-ninth form). It takes a value (a), a value (b), and a value (c). It processes the data (a, b, and c). This is the "process this data" routine (forty-ninth form), part of the 0x82BAC process cluster. 57 lines. 1 callee (LinearSearch). 0 callers (unresolved — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x82BAC918"]
name = "ProcessAndProcessAndProcess49_82BAC918"

# A processor that processes the data (fiftieth form). It takes a structure (a: fields at a->8, a->12, and a->16) and a structure (b: field at b->32). It processes the data (a and b). This is the "process this data" routine (fiftieth form), part of the 0x82BAC process cluster. 96 lines. 0 callees (leaf function — performs raw memory operations). 0 callers (unresolved — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x82BACA78"]
name = "ProcessAndProcessAndProcess50_82BACA78"

# A processor that processes the data (fifty-first form). It takes a structure (a: fields at a->48 and a->96). It processes the data (a). This is the "process this data" routine (fifty-first form), part of the 0x82BAC process cluster. 122 lines. 1 callee (ProcessAndGrowAndSerialize). 1 caller (ProcessAndProcessAndProcess44).
[entrypoint.functions."0x82BACD98"]
name = "ProcessAndProcessAndProcess51_82BACD98"

# A processor that processes the data (fifty-second form). It takes a structure (a: fields at a->0, a->4, a->8, a->12, a->14, a->16, a->20, and a->24) and a value (b). It processes the data (a and b). This is the "process this data" routine (fifty-second form), part of the 0x82BB0 process cluster. 527 lines. 17 callees (DispatchValue, GetSingleton, GetTimebase, InsertHashEntry, ProcessAndAppendNode, ProcessAndCompareAndProcess, ProcessAndGuardedDispatch, ProcessAndHashString2, ProcessAndStoreIndexedValue, ProcessIndexedRegion, StringInsert, ProcessAndProcessAndProcess46, sub_82BB2528, sub_82BB2CB8, sub_82BBB1C8, sub_82BC62B8, and sub_82BC7FB0). 1 caller (sub_82BBDDE8).
[entrypoint.functions."0x82BB0D78"]
name = "ProcessAndProcessAndProcess52_82BB0D78"

# A processor that processes the data (fifty-third form). It takes a structure (a: field at a->4). It processes the data (a). This is the "process this data" routine (fifty-third form), part of the 0x82BB2 process cluster. 663 lines. 6 callees (ComputeStringHash, ProcessApply, sub_82BB46A8, sub_82BB5C90, sub_82BBA1D8, and sub_82BBB3A8). 1 caller (ProcessAndProcessAndProcess52).
[entrypoint.functions."0x82BB2528"]
name = "ProcessAndProcessAndProcess53_82BB2528"

# A processor that processes the data (fifty-fourth form). It takes a value (a) and a value (b). It processes the data (a and b). This is the "process this data" routine (fifty-fourth form), part of the 0x82BB3 process cluster. 473 lines. 9 callees (ComputeStringHash, GetSingleton, ProcessAndClearFields, ProcessApply, ProcessIndexedRegion, StringInsert, sub_82BBC920, sub_82BBF6D8, and sub_82BE75D8). 0 callers (unresolved — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x82BB3E88"]
name = "ProcessAndProcessAndProcess54_82BB3E88"

# A processor that processes the data (fifty-fifth form). It takes a structure (a: fields at a->8 and a->12). It processes the data (a). This is the "process this data" routine (fifty-fifth form), part of the 0x82BB4 process cluster. 456 lines. 7 callees (ComputeStringHash, GrowAndZeroBuffer, InsertHashEntry, ProcessAndCountAndProcess, ProcessAndHashAndAppend, ProcessApply, and ProcessDispatchLoop). 1 caller (ProcessAndProcessAndProcess53).
[entrypoint.functions."0x82BB46A8"]
name = "ProcessAndProcessAndProcess55_82BB46A8"

# A processor that processes the data (fifty-sixth form). It takes a structure (a: fields at a->8 and a->12). It processes the data (a). This is the "process this data" routine (fifty-sixth form), part of the 0x82BB5 process cluster. 306 lines. 7 callees (ComputeStringHash, GrowAndZeroBuffer, InsertHashEntry, ProcessAndCountAndProcess, ProcessAndHashAndAppend, ProcessApply, and ProcessDispatchLoop). 1 caller (ProcessAndProcessAndProcess53).
[entrypoint.functions."0x82BB5C90"]
name = "ProcessAndProcessAndProcess56_82BB5C90"

# A processor that processes the data (fifty-seventh form). It takes a structure (a: fields at a->8 and a->12). It processes the data (a). This is the "process this data" routine (fifty-seventh form), part of the 0x82BBA process cluster. 456 lines. 7 callees (ComputeStringHash, GrowAndZeroBuffer, InsertHashEntry, ProcessAndCountAndProcess, ProcessAndHashAndAppend, ProcessApply, and ProcessDispatchLoop). 1 caller (ProcessAndProcessAndProcess53).
[entrypoint.functions."0x82BBA1D8"]
name = "ProcessAndProcessAndProcess57_82BBA1D8"

# A processor that processes the data (fifty-eighth form). It takes a structure (a: fields at a->8 and a->12). It processes the data (a). This is the "process this data" routine (fifty-eighth form), part of the 0x82BBB process cluster. 306 lines. 7 callees (ComputeStringHash, GrowAndZeroBuffer, InsertHashEntry, ProcessAndCountAndProcess, ProcessAndHashAndAppend, ProcessApply, and ProcessDispatchLoop). 1 caller (ProcessAndProcessAndProcess53).
[entrypoint.functions."0x82BBB3A8"]
name = "ProcessAndProcessAndProcess58_82BBB3A8"

# A processor that processes the data (fifty-ninth form). It takes a structure (a: fields at a->8 and a->12). It processes the data (a). This is the "process this data" routine (fifty-ninth form), part of the 0x82BBB process cluster. 146 lines. 6 callees (MemCpy_SdkRuntime, ProcessAndDispatchAndProcess, ProcessAndDispatchByType, ProcessAndHashString2, sub_82BBC658, and sub_82BC6A18). 0 callers (unresolved — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x82BBB5C8"]
name = "ProcessAndProcessAndProcess59_82BBB5C8"

# A processor that processes the data (sixtieth form). It takes a structure (a: fields at a->0 and a->4). It processes the data (a). This is the "process this data" routine (sixtieth form), part of the 0x82BBC process cluster. 59 lines. 0 callees (leaf function — performs raw memory operations). 2 callers (sub_82BC3070 and sub_82BBC720).
[entrypoint.functions."0x82BBC770"]
name = "ProcessAndProcessAndProcess60_82BBC770"

# A processor that processes the data (sixty-first form). It takes a structure (a: fields at a->0 and a->4) and a structure (b: fields at b->0 and b->4). It processes the data (a and b). This is the "process this data" routine (sixty-first form), part of the 0x82BBC process cluster. 151 lines. 4 callees (MemMove_SdkRuntime, ProcessAndClearFields, ProcessAndInitializeString, and ToLower_Ascii). 1 caller (ProcessAndProcessAndProcess54).
[entrypoint.functions."0x82BBC920"]
name = "ProcessAndProcessAndProcess61_82BBC920"

# A processor that processes the data (sixty-second form). It takes a value (a) and a value (b). It processes the data (a and b). This is the "process this data" routine (sixty-second form), part of the 0x82BBD process cluster. 78 lines. 3 callees (GetSingleton, ProcessIndexedRegion2, and sub_82BC3248). 0 callers (unresolved — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x82BBDC90"]
name = "ProcessAndProcessAndProcess62_82BBDC90"

# A processor that processes the data (sixty-third form). It takes a value (a). It processes the data (a). This is the "process this data" routine (sixty-third form), part of the 0x82BBD process cluster. 74 lines. 3 callees (GetSingleton, ProcessIndexedRegion2, and ProcessAndProcessAndProcess52). 0 callers (unresolved — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x82BBDDE8"]
name = "ProcessAndProcessAndProcess63_82BBDDE8"

# A processor that processes the data (sixty-fourth form). It takes a structure (a: field at a->8) and a value (b). It processes the data (a and b). This is the "process this data" routine (sixty-fourth form), part of the 0x82BBF process cluster. 186 lines. 2 callees (ComputeStringHash and ProcessApply). 1 caller (ProcessAndProcessAndProcess54).
[entrypoint.functions."0x82BBF6D8"]
name = "ProcessAndProcessAndProcess64_82BBF6D8"

# A processor that processes the data (sixty-fifth form). It takes a structure (a: fields at a->0, a->4, a->8, and a->10). It processes the data (a). This is the "process this data" routine (sixty-fifth form), part of the 0x82BC0 process cluster. 56 lines. 2 callees (GetSingleton and ProcessIndexedRegion). 4 callers (sub_82BC0F90, sub_82BC0E08, sub_82BC0DB8, and sub_82BC4880).
[entrypoint.functions."0x82BC0CC0"]
name = "ProcessAndProcessAndProcess65_82BC0CC0"

# A processor that processes the data (sixty-sixth form). It takes a value (a). It processes the data (a). This is the "process this data" routine (sixty-sixth form), part of the 0x82BC1 process cluster. 72 lines. 4 callees (CompareStringsAndReturnValue, GetSingleton, ProcessIndexedRegion, and StringInsert). 0 callers (unresolved — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x82BC1018"]
name = "ProcessAndProcessAndProcess66_82BC1018"

# A processor that processes the data (sixty-seventh form). It takes a value (a) and a value (b). It processes the data (a and b). This is the "process this data" routine (sixty-seventh form), part of the 0x82BC1 process cluster. 11 lines. 1 callee (ProcessAndIndexedRegion2). 0 callers (unresolved — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x82BC1090"]
name = "ProcessAndProcessAndProcess67_82BC1090"

# A processor that processes the data (sixty-eighth form). It takes a structure (a: fields at a->0 and a->12), a structure (b: field at b->0), and a value (c). It processes the data (a, b, and c). This is the "process this data" routine (sixty-eighth form), part of the 0x82BC1 process cluster. 290 lines. 9 callees (sub_82BC12A0, sub_82BC1560, sub_82BC1968, sub_82BC1DF8, sub_82BC1FB8, sub_82BC2328, sub_82BC26D0, sub_82BC2860, and sub_82BC2DA0). 0 callers (unresolved — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x82BC10A0"]
name = "ProcessAndProcessAndProcess68_82BC10A0"

# A processor that processes the data (sixty-ninth form). It takes a structure (a: fields at a->12, a->140, and a->141) and a value (b). It processes the data (a and b). This is the "process this data" routine (sixty-ninth form), part of the 0x82BC1 process cluster. 386 lines. 0 callees (leaf function — performs raw memory operations). 1 caller (ProcessAndProcessAndProcess68).
[entrypoint.functions."0x82BC12A0"]
name = "ProcessAndProcessAndProcess69_82BC12A0"

# A processor that processes the data (seventieth form). It takes a structure (a: fields at a->12 and a->16) and a value (b). It processes the data (a and b). This is the "process this data" routine (seventieth form), part of the 0x82BC1 process cluster. 558 lines. 2 callees (GetSingleton and ProcessIndexedRegion2). 1 caller (ProcessAndProcessAndProcess68).
[entrypoint.functions."0x82BC1560"]
name = "ProcessAndProcessAndProcess70_82BC1560"

# A processor that processes the data (seventy-first form). It takes a structure (a: field at a->92) and a structure (b: field at b->0). It processes the data (a and b). This is the "process this data" routine (seventy-first form), part of the 0x82BC1 process cluster. 640 lines. 4 callees (ConstructAndAppend68Byte, ConstructNode32, FloatToIntProcess, and GrowRegion). 1 caller (ProcessAndProcessAndProcess68).
[entrypoint.functions."0x82BC1968"]
name = "ProcessAndProcessAndProcess71_82BC1968"

# A processor that processes the data (seventy-second form). It takes a structure (a: field at a->12) and a structure (b: field at b->0). It processes the data (a and b). This is the "process this data" routine (seventy-second form), part of the 0x82BC1 process cluster. 249 lines. 0 callees (leaf function — performs raw memory operations). 1 caller (ProcessAndProcessAndProcess68).
[entrypoint.functions."0x82BC1DF8"]
name = "ProcessAndProcessAndProcess72_82BC1DF8"

# A processor that processes the data (seventy-third form). It takes a structure (a: fields at a->12 and a->48) and a value (b). It processes the data (a and b). This is the "process this data" routine (seventy-third form), part of the 0x82BC1 process cluster. 478 lines. 0 callees (leaf function — performs raw memory operations). 1 caller (ProcessAndProcessAndProcess68).
[entrypoint.functions."0x82BC1FB8"]
name = "ProcessAndProcessAndProcess73_82BC1FB8"

# A processor that processes the data (seventy-fourth form). It takes a structure (a: field at a->12) and a structure (b: field at b->0). It processes the data (a and b). This is the "process this data" routine (seventy-fourth form), part of the 0x82BC2 process cluster. 500 lines. 0 callees (leaf function — performs raw memory operations). 1 caller (ProcessAndProcessAndProcess68).
[entrypoint.functions."0x82BC2328"]
name = "ProcessAndProcessAndProcess74_82BC2328"

# A processor that processes the data (seventy-fifth form). It takes a structure (a: field at a->12) and a structure (b: field at b->0). It processes the data (a and b). This is the "process this data" routine (seventy-fifth form), part of the 0x82BC2 process cluster. 225 lines. 1 callee (HashCstring). 1 caller (ProcessAndProcessAndProcess68).
[entrypoint.functions."0x82BC26D0"]
name = "ProcessAndProcessAndProcess75_82BC26D0"

# A processor that processes the data (seventy-sixth form). It takes a structure (a: field at a->12) and a structure (b: field at b->0). It processes the data (a and b). This is the "process this data" routine (seventy-sixth form), part of the 0x82BC2 process cluster. 719 lines. 1 callee (ProcessObject). 1 caller (ProcessAndProcessAndProcess68).
[entrypoint.functions."0x82BC2860"]
name = "ProcessAndProcessAndProcess76_82BC2860"

# A processor that processes the data (seventy-seventh form). It takes a structure (a: fields at a->12 and a->40) and a value (b). It processes the data (a and b). This is the "process this data" routine (seventy-seventh form), part of the 0x82BC2 process cluster. 356 lines. 4 callees (GetSingleton, ProcessIndexedRegion2, ProcessIndexedRegion, and StringInsert). 1 caller (ProcessAndProcessAndProcess68).
[entrypoint.functions."0x82BC2DA0"]
name = "ProcessAndProcessAndProcess77_82BC2DA0"

# A processor that processes the data (seventy-eighth form). It takes a structure (a: field at a->0) and a value (b). It processes the data (a and b). This is the "process this data" routine (seventy-eighth form), part of the 0x82BC3 process cluster. 56 lines. 2 callees (Free_SizeBucketed_Thunk and ProcessAndProcessAndProcess60). 0 callers (unresolved — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x82BC3070"]
name = "ProcessAndProcessAndProcess78_82BC3070"

# A processor that processes the data (seventy-ninth form). It takes a structure (a: fields at a->0, a->4, a->8, a->12, a->16, a->20, a->24, and a->44), a value (b), and a value (c). It processes the data (a, b, and c). This is the "process this data" routine (seventy-ninth form), part of the 0x82BC3 process cluster. 150 lines. 6 callees (MemCpy_SdkRuntime, ProcessAndGrowAndSerialize, ProcessAndIndexedRegion, ProcessFloat, Serialize, and sub_82BC43D0). 1 caller (ProcessAndProcessAndProcess62).
[entrypoint.functions."0x82BC3248"]
name = "ProcessAndProcessAndProcess79_82BC3248"

# A processor that processes the data (eightieth form). It takes a structure (a: field at a->4). It processes the data (a). This is the "process this data" routine (eightieth form), part of the 0x82BC3 process cluster. 181 lines. 7 callees (GetSingleton, ProcessIndexedRegion, StringInsert, sub_82BAA3D0, sub_82BC4760, sub_82BC4880, and sub_82BC4978). 0 callers (unresolved — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x82BC3430"]
name = "ProcessAndProcessAndProcess80_82BC3430"

# A processor that processes the data (eighty-first form). It takes a structure (a: fields at a->16, a->24, a->32, and a->36) and a value (b). It processes the data (a and b). This is the "process this data" routine (eighty-first form), part of the 0x82BC3 process cluster. 336 lines. 5 callees (GetSingleton, MemSet_SdkRuntime, ProcessIndexedRegion2, ProcessIndexedRegion, and sub_82BAE2E8). 0 callers (unresolved — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x82BC3568"]
name = "ProcessAndProcessAndProcess81_82BC3568"

# A processor that sets and processes the data (ninth form). It takes a value (a). It sets the data (a). This is the "set and process this data" routine (ninth form), part of the 0x82BC3 process cluster. 8 lines. 0 callees (leaf function — performs raw memory operations). 0 callers (unresolved — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x82BC37A8"]
name = "ProcessAndSetAndProcess9_82BC37A8"

# A processor that gets and processes the data (third form). It takes a value (a). It gets the data (a). This is the "get and process this data" routine (third form), part of the 0x82BC3 process cluster. 8 lines. 0 callees (leaf function — performs raw memory operations). 0 callers (unresolved — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x82BC37B0"]
name = "ProcessAndGetAndProcess3_82BC37B0"

# A processor that processes the data (eighty-second form). It takes a structure (a: fields at a->4, a->36, and a->52) and a value (b). It processes the data (a and b). This is the "process this data" routine (eighty-second form), part of the 0x82BC3 process cluster. 195 lines. 2 callees (ProcessAndClearIndexedRegion and ProcessAndProcessAndProcess47). 0 callers (unresolved — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x82BC3B68"]
name = "ProcessAndProcessAndProcess82_82BC3B68"

# A processor that processes the data (eighty-third form). It takes a structure (a: fields at a->60 and a->1632). It processes the data (a). This is the "process this data" routine (eighty-third form), part of the 0x82BC4 process cluster. 164 lines. 2 callees (SerializeAndProcessIndexedRegion and Serialize). 1 caller (ProcessAndProcessAndProcess79).
[entrypoint.functions."0x82BC43D0"]
name = "ProcessAndProcessAndProcess83_82BC43D0"

# A processor that processes the data (eighty-fourth form). It takes a structure (a: fields at a->32, a->36, a->40, and a->44) and a value (b). It processes the data (a and b). This is the "process this data" routine (eighty-fourth form), part of the 0x82BC4 process cluster. 89 lines. 2 callees (ProcessAndClearIndexedRegion and Serialize). 1 caller (sub_82BC4760).
[entrypoint.functions."0x82BC46D0"]
name = "ProcessAndProcessAndProcess84_82BC46D0"

# A processor that processes the data (eighty-fifth form). It takes a structure (a: fields at a->0, a->4, a->8, a->10, a->12, a->16, a->64, a->68, and a->72). It processes the data (a). This is the "process this data" routine (eighty-fifth form), part of the 0x82BC4 process cluster. 124 lines. 4 callees (GetSingleton, ProcessIndexedRegion2, sub_82BC3018, and ProcessAndProcessAndProcess84). 1 caller (ProcessAndProcessAndProcess80).
[entrypoint.functions."0x82BC4760"]
name = "ProcessAndProcessAndProcess85_82BC4760"

# A processor that processes the data (eighty-sixth form). It takes a structure (a: fields at a->0, a->12, and a->16). It processes the data (a). This is the "process this data" routine (eighty-sixth form), part of the 0x82BC4 process cluster. 72 lines. 3 callees (ProcessAndIndexedRegion2, ProcessAndProcessAndProcess47, and ProcessAndProcessAndProcess65). 2 callers (ProcessAndProcessAndProcess80 and sub_82BC4830).
[entrypoint.functions."0x82BC4880"]
name = "ProcessAndProcessAndProcess86_82BC4880"

# A processor that processes the data (eighty-seventh form). It takes a structure (a: field at a->12), a structure (b: field at b->0), a value (c), and a value (d). It processes the data (a, b, c, and d). This is the "process this data" routine (eighty-seventh form), part of the 0x82BC4 process cluster. 147 lines. 4 callees (sub_82BC4A78, sub_82BC4C90, sub_82BC4D98, and sub_82BC5330). 1 caller (ProcessAndProcessAndProcess80).
[entrypoint.functions."0x82BC4978"]
name = "ProcessAndProcessAndProcess87_82BC4978"

# A processor that processes the data (eighty-eighth form). It takes a structure (a: fields at a->12, a->64, and a->65). It processes the data (a). This is the "process this data" routine (eighty-eighth form), part of the 0x82BC4 process cluster. 298 lines. 0 callees (leaf function — performs raw memory operations). 1 caller (ProcessAndProcessAndProcess87).
[entrypoint.functions."0x82BC4A78"]
name = "ProcessAndProcessAndProcess88_82BC4A78"

# A processor that processes the data (eighty-ninth form). It takes a structure (a: fields at a->12, a->68, and a->72) and a value (b). It processes the data (a and b). This is the "process this data" routine (eighty-ninth form), part of the 0x82BC4 process cluster. 149 lines. 2 callees (GetSingleton and ProcessIndexedRegion2). 1 caller (ProcessAndProcessAndProcess87).
[entrypoint.functions."0x82BC4C90"]
name = "ProcessAndProcessAndProcess89_82BC4C90"

# A processor that processes the data (ninetieth form). It takes a structure (a: fields at a->12, a->16, and a->68), a value (b), and a value (c). It processes the data (a, b, and c). This is the "process this data" routine (ninetieth form), part of the 0x82BC4 process cluster. 768 lines. 3 callees (MemSet_SdkRuntime, ProcessAndClearIndexedRegion, and ProcessFloatToInt). 1 caller (ProcessAndProcessAndProcess87).
[entrypoint.functions."0x82BC4D98"]
name = "ProcessAndProcessAndProcess90_82BC4D98"

# A processor that processes the data (ninety-first form). It takes a structure (a: fields at a->12 and a->72) and a structure (b: fields at b->8, b->12, and b->20). It processes the data (a and b). This is the "process this data" routine (ninety-first form), part of the 0x82BC5 process cluster. 157 lines. 0 callees (leaf function — performs raw memory operations). 1 caller (ProcessAndProcessAndProcess87).
[entrypoint.functions."0x82BC5330"]
name = "ProcessAndProcessAndProcess91_82BC5330"

# A processor that processes and dispatches the data (twenty-seventh form). It takes a value (a), a value (b), a value (c), and a value (d). It processes and dispatches the data (a, b, c, and d). This is the "process and dispatch this data" routine (twenty-seventh form), part of the 0x82BC6 process cluster. 273 lines. 3 callees (ProcessAndDispatchState, ProcessLoopDispatch, and StateDispatch). 3 callers (ProcessAndProcessAndProcess52, sub_82BD5C78, and sub_82BD5C08).
[entrypoint.functions."0x82BC62B8"]
name = "ProcessAndDispatchAndProcess27_82BC62B8"

# A processor that processes the data (ninety-second form). It takes a value (a) and a structure (b: fields at b->12, b->16, and b->68). It processes the data (a and b). This is the "process this data" routine (ninety-second form), part of the 0x82BC7 process cluster. 149 lines. 1 callee (sub_82BCCB88). 2 callers (sub_82BC7C20 and sub_82BC7E98).
[entrypoint.functions."0x82BC7AA8"]
name = "ProcessAndProcessAndProcess92_82BC7AA8"

# A processor that processes the data (ninety-third form). It takes a structure (a: fields at a->16, a->68, and a->72) and a structure (b: fields at b->16, b->92, and b->96). It processes the data (a and b). This is the "process this data" routine (ninety-third form), part of the 0x82BC7 process cluster. 196 lines. 4 callees (ComputeHash, ProcessAdjustPosition, ProcessAndDispatchAndAdjust, and ProcessAndProcessAndProcess92). 0 callers (unresolved — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x82BC7C20"]
name = "ProcessAndProcessAndProcess93_82BC7C20"

# A processor that processes the data (ninety-fourth form). It takes a structure (a: fields at a->0, a->4, a->100, and a->120). It processes the data (a). This is the "process this data" routine (ninety-fourth form), part of the 0x82BC7 process cluster. 205 lines. 2 callees (ProcessAndSetjmp and sub_82BC7D80). 1 caller (ProcessAndProcessAndProcess52).
[entrypoint.functions."0x82BC7FB0"]
name = "ProcessAndProcessAndProcess94_82BC7FB0"

# A processor that processes the data (ninety-fifth form). It takes a structure (a: field at a->8). It processes the data (a). This is the "process this data" routine (ninety-fifth form), part of the 0x82BC8 process cluster. 133 lines. 4 callees (ComputeHash, IncrementAndCheckThreshold, ProcessApply, and ProcessLoopDispatch). 0 callers (unresolved — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x82BC8178"]
name = "ProcessAndProcessAndProcess95_82BC8178"

# A processor that processes the data (ninety-sixth form). It takes a value (a), a value (b), and a value (c). It processes the data (a, b, and c). This is the "process this data" routine (ninety-sixth form), part of the 0x82BC8 process cluster. 540 lines. 6 callees (ComputeHash, FindChar, ProcessAndDispatchAndProcess5, ProcessAndDispatchElements2, ProcessAndHashAndStore2, and sub_8223F888). 4 callers (sub_82BC6A18, sub_82BC9860, sub_82BC8870, and sub_82BC5C08).
[entrypoint.functions."0x82BC8490"]
name = "ProcessAndProcessAndProcess96_82BC8490"

# A processor that processes the data (ninety-seventh form). It takes a structure (a: fields at a->8, a->28, and a->32), a value (b), a structure (c: fields at c->0 and c->4), a structure (d: fields at d->0 and d->4), and a structure (e: fields at e->0 and e->4). It processes the data (a, b, c, d, and e). This is the "process this data" routine (ninety-seventh form), part of the 0x82BCB process cluster. 125 lines. 2 callees (IncrementAndCheckThreshold and ProcessAndDispatchAndProcess5). 4 callers (DispatchValueProcess, sub_82BCC1C0, sub_82BCC038, and DispatchTypeTag2).
[entrypoint.functions."0x82BCBF50"]
name = "ProcessAndProcessAndProcess97_82BCBF50"

# A processor that processes the data (ninety-eighth form). It takes a value (a), a value (b), and a value (c). It processes the data (a, b, and c). This is the "process this data" routine (ninety-eighth form), part of the 0x82BD1 process cluster. 36 lines. 1 callee (ProcessAndCountAndProcess). 0 callers (unresolved — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x82BD1150"]
name = "ProcessAndProcessAndProcess98_82BD1150"

# A processor that processes the data (ninety-ninth form). It takes a value (a). It processes the data (a). This is the "process this data" routine (ninety-ninth form), part of the 0x82BD1 process cluster. 31 lines. 2 callees (ProcessAndReadOrResolveValue and sub_82CA3B50). 0 callers (unresolved — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x82BD1DD8"]
name = "ProcessAndProcessAndProcess99_82BD1DD8"

# A processor that processes the data (hundredth form). It takes a structure (a: field at a->8). It processes the data (a). This is the "process this data" routine (hundredth form), part of the 0x82BD1 process cluster. 132 lines. 3 callees (ComputeStringHash, ProcessAndCountAndProcess, and ProcessAndHashString2). 0 callers (unresolved — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x82BD1E08"]
name = "ProcessAndProcessAndProcess100_82BD1E08"

# A processor that processes the data (one hundred and first form). It takes a value (a). It processes the data (a). This is the "process this data" routine (one hundred and first form), part of the 0x82BD4 process cluster. 144 lines. 6 callees (ComputeStringHash, InsertHashEntry, ProcessAndAppendNode, ProcessAndCountAndProcess, ProcessAndDispatchByType, and ProcessAndHashString2). 0 callers (unresolved — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x82BD4DA0"]
name = "ProcessAndProcessAndProcess101_82BD4DA0"

# A processor that processes the data (one hundred and second form). It takes a structure (a: fields at a->8 and a->12). It processes the data (a). This is the "process this data" routine (one hundred and second form), part of the 0x82BD5 process cluster. 140 lines. 2 callees (InsertHashEntry and sub_82BC67F0). 0 callers (unresolved — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x82BD5E10"]
name = "ProcessAndProcessAndProcess102_82BD5E10"

# A processor that processes the data (one hundred and third form). It takes a structure (a: fields at a->8, a->68, and a->72) and a structure (b: field at b->8). It processes the data (a and b). This is the "process this data" routine (one hundred and third form), part of the 0x82BD7 process cluster. 235 lines. 6 callees (ComputeStringHash, InsertHashEntry, ProcessAndAppendNode, ProcessAndCountAndProcess, ProcessAndDispatchByType, and ProcessApply). 1 caller (sub_82BD7A58).
[entrypoint.functions."0x82BD78B0"]
name = "ProcessAndProcessAndProcess103_82BD78B0"

# A processor that processes the data (one hundred and fourth form). It takes a value (a). It processes the data (a). This is the "process this data" routine (one hundred and fourth form), part of the 0x82BD7 process cluster. 47 lines. 2 callees (ProcessAndCountAndProcess and ProcessAndProcessAndProcess103). 0 callers (unresolved — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x82BD7A58"]
name = "ProcessAndProcessAndProcess104_82BD7A58"

# A processor that processes the data (one hundred and fifth form). It takes a structure (a: field at a->8). It processes the data (a). This is the "process this data" routine (one hundred and fifth form), part of the 0x82BD9 process cluster. 333 lines. 12 callees (ComputeStringHash, GetGlobalPtr3, GrowAndZeroBuffer, ProcessAndAppendNode, ProcessAndCheckAndProcess2, ProcessAndCountAndProcess, ProcessAndDispatchAndProcess13, ProcessAndHashAndAppend, ProcessAndHashString2, ProcessAndStoreIndexedValue, ProcessApply, and sub_82BC56F8). 0 callers (unresolved — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x82BD93F0"]
name = "ProcessAndProcessAndProcess105_82BD93F0"

# A processor that processes the data (one hundred and sixth form). It takes a value (a), a value (b), and a value (c). It processes the data (a, b, and c). This is the "process this data" routine (one hundred and sixth form), part of the 0x82BDA process cluster. 36 lines. 1 callee (ProcessAndCountAndProcess). 0 callers (unresolved — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x82BDA290"]
name = "ProcessAndProcessAndProcess106_82BDA290"

# A processor that processes the data (one hundred and seventh form). It takes a value (a). It processes the data (a). This is the "process this data" routine (one hundred and seventh form), part of the 0x82BDB process cluster. 375 lines. 13 callees (ComputeHash, ComputeStringHash, GrowAndZeroBuffer, ProcessAdjustPosition, ProcessAndAppendAndFindChar, ProcessAndAppendNode, ProcessAndCountAndProcess, ProcessAndHashAndAppend, ProcessAndStoreIndexedValue, ProcessApply, ProcessLoopDispatch, sub_82BC56F8, and sub_82BDB8F8). 0 callers (unresolved — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x82BDB990"]
name = "ProcessAndProcessAndProcess107_82BDB990"

# A processor that processes the data (one hundred and eighth form). It takes a value (a), a value (b), and a value (c). It processes the data (a, b, and c). This is the "process this data" routine (one hundred and eighth form), part of the 0x82BE7 process cluster. 123 lines. 4 callees (GetSingleton, ProcessAndClearIndexedRegion, ProcessIndexedRegion2, and ProcessAndProcessAndProcess44). 1 caller (ProcessAndProcessAndProcess54).
[entrypoint.functions."0x82BE75D8"]
name = "ProcessAndProcessAndProcess108_82BE75D8"

# A comparator that compares two strings (the first form). It takes a string (a), a string (b), a string (c), and a string (d). It compares the strings (a, b, c, and d) and returns a comparison result. This is the "compare these strings" routine (first form), part of the 0x82C1A compare cluster. 123 lines. 2 callees (CompareStringsSigned and Process2DRegion). 1 caller (sub_82C1B770).
[entrypoint.functions."0x82C1A200"]
name = "CompareAndCompareAndProcess_82C1A200"

# A sorter that sorts the data (third form). It takes a value (a), a value (b), a value (c), and a value (d). It sorts the data (a, b, c, and d). This is the "sort this data" routine (third form), part of the 0x82C1B sort cluster. 176 lines. 5 callees (CompareAndCompareAndProcess, sub_82C1AA30, sub_82C1AC78, sub_82C1AFC0, and itself — recursive). 2 callers (ProcessAndSortAndProcess and ProcessAndSortAndProcess2).
[entrypoint.functions."0x82C1B770"]
name = "ProcessAndSortAndProcess3_82C1B770"

# A processor that processes the data (one hundred and ninth form). It takes a structure (a: field at a->12). It processes the data (a). This is the "process this data" routine (one hundred and ninth form), part of the 0x82C62 process cluster. 47 lines. 0 callees (no function calls). 4 callers (sub_8290BE28, ProcessAndProcessAndProcess29, sub_826935F0, and sub_826939A0).
[entrypoint.functions."0x82C62A88"]
name = "ProcessAndProcessAndProcess109_82C62A88"

# A processor that processes the data (one hundred and tenth form). It takes a structure (a: fields at a->8 and a->12), a value (b), a value (c), and a structure (d: field at d->12). It processes the data (a, b, c, and d). This is the "process this data" routine (one hundred and tenth form), part of the 0x82C62 process cluster. 91 lines. 2 callees (Construct104ByteObject and VirtualCall4). 1 caller (sub_8290BE28).
[entrypoint.functions."0x82C62AD8"]
name = "ProcessAndProcessAndProcess110_82C62AD8"

# A processor that processes the data (one hundred and eleventh form). It takes a structure (a: field at a->12). It processes the data (a). This is the "process this data" routine (one hundred and eleventh form), part of the 0x82C62 process cluster. 49 lines. 0 callees (no function calls). 0 callers (unresolved — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x82C62D48"]
name = "ProcessAndProcessAndProcess111_82C62D48"

# A processor that processes the data (one hundred and twelfth form). It takes a structure (a: field at a->8). It processes the data (a). This is the "process this data" routine (one hundred and twelfth form), part of the 0x82C62 process cluster. 49 lines. 0 callees (no function calls). 1 caller (sub_826935F0).
[entrypoint.functions."0x82C62D98"]
name = "ProcessAndProcessAndProcess112_82C62D98"

# An allocator that allocates the data (seventeenth form). It takes a value (a), a value (b), a value (c), a value (d), and a value (e). It allocates the data (a, b, c, d, and e). This is the "allocate this data" routine (seventeenth form), part of the 0x82C6A allocate cluster. 78 lines. 2 callees (Allocate_OrDbgBreak and RangeUpdate). 1 caller (sub_82C6E108).
[entrypoint.functions."0x82C6A248"]
name = "ProcessAndAllocateAndProcess17_82C6A248"

# A processor that processes the data (one hundred and thirteenth form). It takes a value (a), a structure (b: field at b->8), a value (c), a value (d), and a value (e). It processes the data (a, b, c, d, and e). This is the "process this data" routine (one hundred and thirteenth form), part of the 0x82C6E process cluster. 440 lines. 8 callees (Free_Thunk, ListNodeInsert2, SetString, sub_822F1EC0, sub_822F1F00, sub_826C3FA8, ProcessAndAllocateAndProcess17, and sub_82C70560). 2 callers (sub_82C6E898 and sub_82C6E590).
[entrypoint.functions."0x82C6E108"]
name = "ProcessAndProcessAndProcess113_82C6E108"

# A processor that processes the data (one hundred and fourteenth form). It takes a value (a), a structure (b: fields at b->4 and b->8), a value (c), and a value (d). It processes the data (a, b, c, and d). This is the "process this data" routine (one hundred and fourteenth form), part of the 0x82C6E process cluster. 376 lines. 5 callees (FindListTail, sub_8236AC48, sub_82370FD8, ProcessAndProcessAndProcess113, and sub_82C6E590). 1 caller (ProcessAndProcessAndFree).
[entrypoint.functions."0x82C6E898"]
name = "ProcessAndProcessAndProcess114_82C6E898"

# A processor that processes the data (one hundred and fifteenth form). It takes a structure (a: fields at a->16, a->20, and a->24). It processes the data (a). This is the "process this data" routine (one hundred and fifteenth form), part of the 0x82C82 process cluster. 71 lines. 1 callee (DestructAndFree). 4 callers (sub_82C88A58, sub_82C82138, sub_82C88AC0, and sub_832B06A8).
[entrypoint.functions."0x82C82038"]
name = "ProcessAndProcessAndProcess115_82C82038"

# A processor that processes the data (one hundred and sixteenth form). It takes a structure (a: fields at a->0, a->4, and a->8) and a value (b). It processes the data (a and b). This is the "process this data" routine (one hundred and sixteenth form), part of the 0x82C82 process cluster. 82 lines. 3 callees (AllocateAndIncrementCounter, DestructAndFree, and MemCpy_SdkRuntime). 2 callers (ProcessAndGrowHashTable and ProcessGlobalStruct).
[entrypoint.functions."0x82C820B0"]
name = "ProcessAndProcessAndProcess116_82C820B0"

# A processor that processes the data (one hundred and seventeenth form). It takes a value (a), a value (b), a value (c), a structure (d: field at d->0), a value (e), a value (f), and a value (g). It processes the data (a, b, c, d, e, f, and g). This is the "process this data" routine (one hundred and seventeenth form), part of the 0x82C97 process cluster. 379 lines. 2 callees (ProcessAndVcallAndReturn and SwitchDispatch). 1 caller (sub_82C97458).
[entrypoint.functions."0x82C971B8"]
name = "ProcessAndProcessAndProcess117_82C971B8"

# A processor that processes the data (one hundred and eighteenth form). It takes a value (a). It processes the data (a). This is the "process this data" routine (one hundred and eighteenth form), part of the 0x82C97 process cluster. 81 lines. 1 callee (sub_82C97030). 4 callers (sub_82C98268, sub_82C98470, sub_82C984F0, and sub_82C982E8).
[entrypoint.functions."0x82C97EE0"]
name = "ProcessAndProcessAndProcess118_82C97EE0"

# A processor that processes the data (one hundred and nineteenth form). It takes a value (a). It processes the data (a). This is the "process this data" routine (one hundred and nineteenth form), part of the 0x82CA3 process cluster. 32 lines. 1 callee (sub_82CAC610). 4 callers (sub_82A1E130, sub_82A465C8, sub_82A28180, and ProcessAndProcessAndProcess99).
[entrypoint.functions."0x82CA3B50"]
name = "ProcessAndProcessAndProcess119_82CA3B50"

# A processor that processes the data (one hundred and twentieth form). It takes a structure (a: fields at a->36, a->44, a->52, and a->60). It processes the data (a). This is the "process this data" routine (one hundred and twentieth form), part of the 0x82CAC process cluster. 111 lines. 1 callee (ProcessAndLookupWithDefaultInit). 3 callers (sub_830029D8, sub_82CA3430, and sub_82CAC700).
[entrypoint.functions."0x82CAC650"]
name = "ProcessAndProcessAndProcess120_82CAC650"

# A processor that processes the data (one hundred and twenty-first form). It takes a value (a). It processes the data (a). This is the "process this data" routine (one hundred and twenty-first form), part of the 0x82CAC process cluster. 94 lines. 1 callee (ProcessAndProcessAndProcess120). 1 caller (sub_82CA3318).
[entrypoint.functions."0x82CAC700"]
name = "ProcessAndProcessAndProcess121_82CAC700"

# A processor that processes the data (one hundred and twenty-second form). It takes a value (a). It processes the data (a). This is the "process this data" routine (one hundred and twenty-second form), part of the 0x82CBC process cluster. 9 lines. 0 callees (no function calls). 7 callers (sub_826C6A80, sub_829F9D38, sub_82378CD0, sub_8233AE50, sub_82373E08, sub_82A153F0, and sub_822EB0C8).
[entrypoint.functions."0x82CBC5F0"]
name = "ProcessAndProcessAndProcess122_82CBC5F0"

# A processor that processes the data (one hundred and twenty-third form). It takes a value (a) and a value (b). It processes the data (a and b). This is the "process this data" routine (one hundred and twenty-third form), part of the 0x82CCA process cluster. 58 lines. 1 callee (sub_82CC16C0). 0 callers (unresolved — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x82CCA400"]
name = "ProcessAndProcessAndProcess123_82CCA400"

# A freer that frees the data (ninth form). It takes a structure (a: fields at a->4, a->20, and a->24). It frees the data (a). This is the "free this data" routine (ninth form), part of the 0x82171 free cluster. 49 lines. 1 callee (Free_SizeBucketed_Thunk). 12 callers (sub_82684BB0, sub_82BE3F30, sub_825507A0, sub_8292C840, sub_82773520, sub_82AA39D8, ProcessNodeString, sub_82395330, sub_828A4738, sub_82B4CDE0, sub_82B5FEB0, and sub_82AAD4D8).
[entrypoint.functions."0x82171810"]
name = "ProcessAndFreeAndProcess9_82171810"

# A processor that processes the data (one hundred and twenty-fourth form). It takes a structure (a: fields at a->4, a->20, and a->24), a value (b), and a value (c). It processes the data (a, b, and c). This is the "process this data" routine (one hundred and twenty-fourth form), part of the 0x821A1 process cluster. 207 lines. 4 callees (Buffer_CopyOrZero_Error, ProcessValue_FlagNonZero, sub_82374F20, and sub_82CD11D0). 4 callers (sub_829FB018, sub_82374E78, sub_82C7AE40, and sub_826C2250).
[entrypoint.functions."0x821A1648"]
name = "ProcessAndProcessAndProcess124_821A1648"

# A processor that processes the data (one hundred and twenty-fifth form). It takes a structure (a: fields at a->8 and a->16) and a structure (b: fields at b->0 and b->4). It processes the data (a and b). This is the "process this data" routine (one hundred and twenty-fifth form), part of the 0x82227 process cluster. 166 lines. 2 callees (AccessElement8 and sub_82A2C778). 12 callers (sub_82702C58, sub_82A22938, sub_82BF4C50, sub_82A29758, sub_82A27460, sub_8290B7B0, sub_82227CD0, sub_82903300, sub_8260C1A8, sub_8253A408, sub_82911500, and sub_82902EC0).
[entrypoint.functions."0x82227BA0"]
name = "ProcessAndProcessAndProcess125_82227BA0"

# A processor that processes the data (one hundred and twenty-sixth form). It takes a structure (a: fields at a->16 and a->88), a value (b), and a structure (c: fields at c->16 and c->20). It processes the data (a, b, and c). This is the "process this data" routine (one hundred and twenty-sixth form), part of the 0x8222B process cluster. 220 lines. 2 callees (ProcessVirtual and sub_82A2C778). 6 callers (ProcessAndSetjmpAndProcess2, DispatchTypeTag, sub_82415428, sub_82A202F8, GuildChest_ProcessItems, and sub_821E51D0).
[entrypoint.functions."0x8222B4C0"]
name = "ProcessAndProcessAndProcess126_8222B4C0"

# A comparator that compares two strings. It takes a string (a: fields at a->0 and a->4) and a string (b: fields at b->0 and b->4). It compares the strings (a and b) and returns a comparison result. This is the "compare these strings" routine, part of the 0x8226D compare cluster. 51 lines. 1 callee (StrCmp_Signed). 12 callers (sub_829F94B8, sub_823B1350, sub_82291460, sub_8250E9F8, sub_8250D0D0, sub_8247A828, sub_822719D8, sub_825A2E78, sub_82420C80, sub_829F9000, sub_82606F78, and sub_829DF130).
[entrypoint.functions."0x8226D750"]
name = "CompareStringsSigned_8226D750"

# An allocator that allocates the data (eighteenth form). It takes a structure (a: fields at a->4, a->8, and a->12) and a value (b). It allocates the data (a and b). This is the "allocate this data" routine (eighteenth form), part of the 0x82301 allocate cluster. 65 lines. 2 callees (ProcessAllocateCheck and sub_82A97648). 3 callers (sub_82350EC8, sub_82B29880, and sub_8272E610).
[entrypoint.functions."0x82301240"]
name = "ProcessAndAllocateAndProcess18_82301240"

# An allocator that allocates the data (nineteenth form). It takes a structure (a: fields at a->4, a->8, and a->12), a value (b), and a structure (c: field at c->0). It allocates the data (a, b, and c). This is the "allocate this data" routine (nineteenth form), part of the 0x82350 allocate cluster. 78 lines. 1 callee (ProcessAndAllocateAndProcess18). 9 callers (sub_824E5978, sub_82A1CA98, sub_824A5918, sub_82350D58, sub_824EEA98, sub_82A1CCD8, sub_82670858, sub_82A1B600, and sub_82A1C918).
[entrypoint.functions."0x82350EC8"]
name = "ProcessAndAllocateAndProcess19_82350EC8"

# A processor that processes the data (one hundred and twenty-seventh form). It takes a structure (a: field at a->0), a value (b), and a value (c). It processes the data (a, b, and c). This is the "process this data" routine (one hundred and twenty-seventh form), part of the 0x82358 process cluster. 89 lines. 5 callees (ProcessAndAppendElement, ProcessAndCopyReferenceAndHash, ProcessAndDispatchAndResolve, ProcessAndHashString, and ProcessAndResolveTypedField). 5 callers (sub_823ADF80, sub_82415778, sub_823577C0, sub_825AF6D0, and sub_82526808).
[entrypoint.functions."0x82358A18"]
name = "ProcessAndProcessAndProcess127_82358A18"

# A processor that processes the data (one hundred and twenty-eighth form). It takes a structure (a: field at a->0), a value (b), and a value (c). It processes the data (a, b, and c). This is the "process this data" routine (one hundred and twenty-eighth form), part of the 0x82358 process cluster. 89 lines. 5 callees (ProcessAndAppendElement, ProcessAndCopyReferenceAndHash, ProcessAndDispatchAndResolve, ProcessAndHashString, and ProcessAndResolveTypedField). 7 callers (sub_8251D958, sub_824DE170, sub_82415830, sub_823ADF80, sub_823577C0, sub_82908E20, and sub_824FA760).
[entrypoint.functions."0x82358B48"]
name = "ProcessAndProcessAndProcess128_82358B48"

# A processor that processes the data (one hundred and twenty-ninth form). It takes a structure (a: fields at a->8, a->12, a->16, a->24, a->28, a->32, a->36, and a->40). It processes the data (a). This is the "process this data" routine (one hundred and twenty-ninth form), part of the 0x8236D process cluster. 169 lines. 6 callees (Allocate_OrDbgBreak, ConstructAllocObject, ConstructLinkedNode, Free_SizeBucketed_Thunk, sub_8236FB20, and sub_82C6CF68). 4 callers (sub_822F33B8, sub_82372758, sub_82374240, and sub_822F4D10).
[entrypoint.functions."0x8236D4E0"]
name = "ProcessAndProcessAndProcess129_8236D4E0"

# A processor that processes the data (one hundred and thirtieth form). It takes a structure (a: fields at a->4, a->8, and a->12). It processes the data (a). This is the "process this data" routine (one hundred and thirtieth form), part of the 0x82371 process cluster. 58 lines. 1 callee (sub_82370E60). 2 callers (sub_82372108 and sub_82371BF0).
[entrypoint.functions."0x82371EE8"]
name = "ProcessAndProcessAndProcess130_82371EE8"

# A processor that processes the data (one hundred and thirty-first form). It takes a structure (a: fields at a->20, a->40, a->60, a->64, and a->68). It processes the data (a). This is the "process this data" routine (one hundred and thirty-first form), part of the 0x8240D process cluster. 201 lines. 3 callees (ProcessAndAccessAndStore, ProcessAndPopulateGlobal, and sub_83236480). 2 callers (sub_823052C0 and sub_824151E8).
[entrypoint.functions."0x8240D938"]
name = "ProcessAndProcessAndProcess131_8240D938"

# A processor that processes the data (one hundred and thirty-second form). It takes a value (a), a structure (b: field at b->0), and a value (c). It processes the data (a, b, and c). This is the "process this data" routine (one hundred and thirty-second form), part of the 0x824EE process cluster. 147 lines. 4 callees (ProcessAndAppendElement, ProcessAndHashAndDispatch, Release_RefCounted, and sub_822B9510). 12 callers (sub_823D7120, sub_825EED60, sub_82775898, sub_824C7198, sub_82774D00, sub_8294ECA0, sub_8243BF10, sub_827AA498, sub_82774338, sub_8279B590, sub_8287B7C8, and sub_8297C970).
[entrypoint.functions."0x824EEE50"]
name = "ProcessAndProcessAndProcess132_824EEE50"

# A processor that processes the data (one hundred and thirty-third form). It takes a structure (a: field at a->0), a value (b), and a value (c). It processes the data (a, b, and c). This is the "process this data" routine (one hundred and thirty-third form), part of the 0x8260C process cluster. 89 lines. 5 callees (ProcessAndAppendElement, ProcessAndCopyReferenceAndHash, ProcessAndDispatchAndResolve, ProcessAndHashString, and ProcessAndResolveTypedField). 2 callers (sub_8253B3F0 and sub_8260C3B8).
[entrypoint.functions."0x8260CF40"]
name = "ProcessAndProcessAndProcess133_8260CF40"

# A processor that processes the data (one hundred and thirty-fourth form). It takes a structure (a: field at a->0), a value (b), and a value (c). It processes the data (a, b, and c). This is the "process this data" routine (one hundred and thirty-fourth form), part of the 0x8260D process cluster. 89 lines. 5 callees (ProcessAndAppendElement, ProcessAndCopyReferenceAndHash, ProcessAndDispatchAndResolve, ProcessAndHashString, and ProcessAndResolveTypedField). 1 caller (sub_8260C3B8).
[entrypoint.functions."0x8260D0B8"]
name = "ProcessAndProcessAndProcess134_8260D0B8"

# A processor that processes the data (one hundred and thirty-fifth form). It takes a structure (a: field at a->0), a value (b), and a value (c). It processes the data (a, b, and c). This is the "process this data" routine (one hundred and thirty-fifth form), part of the 0x8260D process cluster. 89 lines. 5 callees (ProcessAndAppendElement, ProcessAndCopyReferenceAndHash, ProcessAndDispatchAndResolve, ProcessAndHashString, and ProcessAndResolveTypedField). 1 caller (sub_8260C3B8).
[entrypoint.functions."0x8260D150"]
name = "ProcessAndProcessAndProcess135_8260D150"

# An allocator that allocates the data (twentieth form). It takes a value (a), a value (b), a structure (c: field at c->4), and a structure (d: field at d->0). It allocates the data (a, b, c, and d). This is the "allocate this data" routine (twentieth form), part of the 0x826A0 allocate cluster. 131 lines. 6 callees (Allocate_OrDbgBreak, SetString, ProcessAndFreeAndProcess9, sub_822F1F00, sub_826C3EF0, and sub_826C3FA8). 12 callers (sub_8268D3A0, sub_828559D0, sub_825D3100, sub_824F4148, sub_825DF908, sub_8282A360, sub_823CA178, sub_82826880, sub_829FB280, sub_8269E208, sub_828837B0, and sub_8219AF38).
[entrypoint.functions."0x826A03A8"]
name = "ProcessAndAllocateAndProcess20_826A03A8"

# A processor that processes the data (one hundred and thirty-sixth form). It takes a structure (a: fields at a->0, a->8, a->12, a->16, a->20, a->24, a->32, a->36, a->40, a->44, a->48, a->52, and a->56). It processes the data (a). This is the "process this data" routine (one hundred and thirty-sixth form), part of the 0x826CA process cluster. 72 lines. 1 callee (CopyAssign_RefCounted). 2 callers (sub_82378CD0 and sub_826C6A80).
[entrypoint.functions."0x826CADF0"]
name = "ProcessAndProcessAndProcess136_826CADF0"

# A freer that frees the data (tenth form). It takes a structure (a: fields at a->4, a->8, and a->12). It frees the data (a). This is the "free this data" routine (tenth form), part of the 0x829EA free cluster. 54 lines. 2 callees (DestructRange and Free_SizeBucketed_Thunk). 12 callers (sub_8223C950, sub_8226D580, sub_82515540, sub_8270A080, sub_826EA288, sub_829E9C68, sub_82B723E8, sub_82B80D70, sub_829B8890, sub_829E9CD0, sub_82B828C8, and sub_828BFFF8).
[entrypoint.functions."0x829EA980"]
name = "ProcessAndFreeAndProcess10_829EA980"

# A processor that processes the data (one hundred and thirty-seventh form). It takes a structure (a: fields at a->4 and a->8) and a structure (b: field at b->0). It processes the data (a and b). This is the "process this data" routine (one hundred and thirty-seventh form), part of the 0x829FA process cluster. 155 lines. 4 callees (Allocate_OrDbgBreak, Free_SizeBucketed_Thunk, HashTable_Find, and MemSet_SdkRuntime). 1 caller (sub_829FB530).
[entrypoint.functions."0x829FAC30"]
name = "ProcessAndProcessAndProcess137_829FAC30"

# A processor that processes the data (one hundred and thirty-eighth form). It takes a structure (a: field at a->0), a value (b), and a value (c). It processes the data (a, b, and c). This is the "process this data" routine (one hundred and thirty-eighth form), part of the 0x829FB process cluster. 144 lines. 8 callees (ConstructRefCounted, DestructRefCounted, ProcessAndVcallAndDestruct3, SetString, ProcessAndFreeAndProcess9, ProcessAndProcessAndProcess124, sub_829FB280, and sub_82C63FB8). 6 callers (sub_825B4CC8, sub_8241DC68, sub_825F3050, sub_822F33B8, sub_8233AE50, and sub_82354BB8).
[entrypoint.functions."0x829FB018"]
name = "ProcessAndProcessAndProcess138_829FB018"

# An allocator that allocates the data (twenty-first form). It takes a structure (a: field at a->12), a value (b), and a value (c). It allocates the data (a, b, and c). This is the "allocate this data" routine (twenty-first form), part of the 0x829FB allocate cluster. 157 lines. 5 callees (Allocate_OrDbgBreak, ProcessAndAllocateAndProcess20, sub_829FB530, sub_829FC9A8, and sub_829FCBE0). 1 caller (ProcessAndProcessAndProcess138).
[entrypoint.functions."0x829FB280"]
name = "ProcessAndAllocateAndProcess21_829FB280"

# A processor that processes the data (one hundred and thirty-ninth form). It takes a value (a) and a structure (b: field at b->0). It processes the data (a and b). This is the "process this data" routine (one hundred and thirty-ninth form), part of the 0x829FB process cluster. 116 lines. 3 callees (Free_SizeBucketed_Thunk, ProcessAndProcessAndProcess137, and sub_829FCDF8). 2 callers (sub_829FB390 and ProcessAndAllocateAndProcess21).
[entrypoint.functions."0x829FB530"]
name = "ProcessAndProcessAndProcess139_829FB530"

# A processor that processes the data (one hundred and fortieth form). It takes a structure (a: field at a->0) and a structure (b: field at b->0). It processes the data (a and b). This is the "process this data" routine (one hundred and fortieth form), part of the 0x829FC process cluster. 189 lines. 5 callees (Allocate_OrDbgBreak, Free_SizeBucketed_Thunk, HashTable_Find2, HashTable_InsertGrow, and sub_829FAB50). 1 caller (sub_829FCBE0).
[entrypoint.functions."0x829FC858"]
name = "ProcessAndProcessAndProcess140_829FC858"

# A processor that processes the data (one hundred and forty-first form). It takes a structure (a: field at a->32) and a value (b). It processes the data (a and b). This is the "process this data" routine (one hundred and forty-first form), part of the 0x829FC process cluster. 297 lines. 10 callees (Allocate_OrDbgBreak, CheckAndExtractBit, ConstructRefCounted, DestructRefCounted, ProcessAndConstructAndAtomicIncrement, ProcessAndDestructAndZeroField, ProcessAndDestructAndZero, VcallSlot3_AdvancePosition, WriteRange_AtPos, and ProcessAndProcessAndProcess140). 1 caller (ProcessAndAllocateAndProcess21).
[entrypoint.functions."0x829FCBE0"]
name = "ProcessAndProcessAndProcess141_829FCBE0"

# A processor that processes the data (one hundred and forty-second form). It takes a structure (a: fields at a->0 and a->4) and a value (b). It processes the data (a and b). This is the "process this data" routine (one hundred and forty-second form), part of the 0x829FC process cluster. 58 lines. 2 callees (Allocate_OrDbgBreak and Free_SizeBucketed_Thunk). 1 caller (ProcessAndProcessAndProcess139).
[entrypoint.functions."0x829FCDF8"]
name = "ProcessAndProcessAndProcess142_829FCDF8"

# A processor that processes the data (one hundred and forty-third form). It takes a value (a), a value (b), and a value (c). It processes the data (a, b, and c). This is the "process this data" routine (one hundred and forty-third form), part of the 0x82A1B process cluster. 112 lines. 3 callees (ConstructRefCounted, sub_82A1C918, and sub_82A1D770). 9 callers (sub_82B80D70, sub_82A855A8, sub_82B7CD18, sub_828839A8, sub_82AAAC20, sub_8322F940, sub_82A576E8, sub_82A51178, and sub_82B5C8B0).
[entrypoint.functions."0x82A1B8C8"]
name = "ProcessAndProcessAndProcess143_82A1B8C8"

# A processor that processes the data (one hundred and forty-fourth form). It takes a value (a), a value (b), and a value (c). It processes the data (a, b, and c). This is the "process this data" routine (one hundred and forty-fourth form), part of the 0x82A1C process cluster. 169 lines. 4 callees (Free_SizeBucketed_Thunk, MemCpy_SdkRuntime, ProcessAndCopyStructRegion, and ProcessAndAllocateAndProcess19). 1 caller (ProcessAndProcessAndProcess143).
[entrypoint.functions."0x82A1C918"]
name = "ProcessAndProcessAndProcess144_82A1C918"

# A processor that processes the data (one hundred and forty-fifth form). It takes a structure (a: fields at a->0 and a->16) and a structure (b: fields at b->12, b->16, b->20, and b->24). It processes the data (a and b). This is the "process this data" routine (one hundred and forty-fifth form), part of the 0x82A1D process cluster. 68 lines. 2 callees (DestructRefCounted and Free_SizeBucketed_Thunk). 2 callers (sub_82A1D720 and ProcessAndProcessAndProcess143).
[entrypoint.functions."0x82A1D770"]
name = "ProcessAndProcessAndProcess145_82A1D770"

# A processor that processes the data (one hundred and forty-sixth form). It takes a structure (a: field at a->8), a value (b), and a value (c). It processes the data (a, b, and c). This is the "process this data" routine (one hundred and forty-sixth form), part of the 0x82A29 process cluster. 86 lines. 2 callees (ProcessAndAppendStringNode and ProcessAndHashString). 1 caller (sub_82A298E8).
[entrypoint.functions."0x82A29850"]
name = "ProcessAndProcessAndProcess146_82A29850"

# A processor that processes the data (one hundred and forty-seventh form). It takes a structure (a: fields at a->20 and a->24) and a structure (b: fields at b->4, b->12, and b->16). It processes the data (a and b). This is the "process this data" routine (one hundred and forty-seventh form), part of the 0x82A2B process cluster. 53 lines. 0 callees (no function calls). 6 callers (sub_82A2C220, sub_82BC9320, sub_82BC88B8, sub_82A2BFE0, sub_82A2B450, and sub_82BC9560).
[entrypoint.functions."0x82A2B328"]
name = "ProcessAndProcessAndProcess147_82A2B328"

# A processor that processes the data (one hundred and forty-eighth form). It takes a value (a), a structure (b: field at b->0), and a value (c). It processes the data (a, b, and c). This is the "process this data" routine (one hundred and forty-eighth form), part of the 0x82A2E process cluster. 293 lines. 3 callees (StringCopyN, sub_82CA5E70, and sub_82CA99C8). 12 callers (sub_82A30E20, ProcessAndIncrementAndProcess2, sub_82A2C520, sub_82BC8A10, sub_82BCDAF8, sub_82BDDC28, sub_82A312B0, sub_82A2B660, ProcessAndDispatchByCounter, sub_82A323C0, sub_82A32C30, and sub_82A30A58).
[entrypoint.functions."0x82A2ED70"]
name = "ProcessAndProcessAndProcess148_82A2ED70"

# A comparator that compares two strings. It takes a string (a: field at a->0) and a string (b: field at b->0). It compares the strings (a and b) and returns a comparison result. This is the "compare these strings" routine, part of the 0x82B3A compare cluster. 57 lines. 1 callee (CompareStringsSigned). 12 callers (sub_825A2AA0, sub_821A60C8, sub_8242D7B0, sub_82A576E8, sub_82B80D70, sub_82A51178, sub_82310D70, sub_825D0248, sub_82495A58, sub_8230DFF0, sub_822CD590, and sub_8283C618).
[entrypoint.functions."0x82B3A158"]
name = "CompareStringsSigned_82B3A158"

# A processor that processes the data (one hundred and forty-ninth form). It takes a value (a) and a structure (b: fields at b->4 and b->24). It processes the data (a and b). This is the "process this data" routine (one hundred and forty-ninth form), part of the 0x82B40 process cluster. 265 lines. 5 callees (CriticalSection_EnterOrTry, FindCommonPrefix16, LazyGetData, sub_82B3E0A8, and sub_82B41C90). 0 callers (unresolved — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x82B40F30"]
name = "ProcessAndProcessAndProcess149_82B40F30"

# A processor that processes the data (one hundred and fiftieth form). It takes a value (a) and a structure (b: fields at b->0, b->4, and b->8). It processes the data (a and b). This is the "process this data" routine (one hundred and fiftieth form), part of the 0x82B82 process cluster. 64 lines. 2 callees (DestructRange and Free_SizeBucketed_Thunk). 5 callers (sub_82B828C8, sub_82B80D70, sub_82B82558, sub_82A49D48, and sub_82B82778).
[entrypoint.functions."0x82B82860"]
name = "ProcessAndProcessAndProcess150_82B82860"

# A processor that processes the data (one hundred and fifty-first form). It takes a structure (a: fields at a->8, a->12, and a->16). It processes the data (a). This is the "process this data" routine (one hundred and fifty-first form), part of the 0x82BB1 process cluster. 369 lines. 11 callees (ElementAccess, GetIndexedPointer, GetSingleton, GetTypedField, ProcessAndComputeLengthAndProcess, ProcessAndGuardedDispatch, ProcessAndHashString2, ProcessIndexedRegion, ProcessLoopDispatch, StringInsert, and sub_82BCBDC8). 1 caller (sub_82BB8790).
[entrypoint.functions."0x82BB1ED0"]
name = "ProcessAndProcessAndProcess151_82BB1ED0"

# A processor that processes the data (one hundred and fifty-second form). It takes a structure (a: fields at a->8 and a->16). It processes the data (a). This is the "process this data" routine (one hundred and fifty-second form), part of the 0x82BB2 process cluster. 94 lines. 2 callees (ProcessLoopDispatch and sub_82BCBDC8). 0 callers (unresolved — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x82BB29D0"]
name = "ProcessAndProcessAndProcess152_82BB29D0"

# A processor that processes the data (one hundred and fifty-third form). It takes a value (a). It processes the data (a). This is the "process this data" routine (one hundred and fifty-third form), part of the 0x82BC6 process cluster. 86 lines. 4 callees (InsertHashEntry, ProcessAndIterateAndProcess, sub_82BC5C08, and sub_82BC8CB8). 3 callers (sub_82BD5248, sub_82BD7498, and sub_82BC6A18).
[entrypoint.functions."0x82BC6980"]
name = "ProcessAndProcessAndProcess153_82BC6980"

# A processor that processes the data (one hundred and fifty-fourth form). It takes a structure (a: field at a->16) and a value (b). It processes the data (a and b). This is the "process this data" routine (one hundred and fifty-fourth form), part of the 0x82BC6 process cluster. 128 lines. 5 callees (ProcessAndDispatchElements2, ProcessLoopDispatch, ProcessAndProcessAndProcess153, sub_82BC8490, and sub_82BC9788). 12 callers (sub_82BD83B0, sub_82BDCDD0, sub_82BB4D78, sub_82BC67F0, ProcessAndCheckAndProcess5, sub_82BD2FF8, sub_82BD2E98, sub_82BBA8A8, sub_82BD3FD0, sub_82BD3E60, sub_82BB62E0, and sub_82BB4B08).
[entrypoint.functions."0x82BC6A18"]
name = "ProcessAndProcessAndProcess154_82BC6A18"

# A processor that processes the data (one hundred and fifty-fifth form). It takes a value (a) and a structure (b: field at b->4). It processes the data (a and b). This is the "process this data" routine (one hundred and fifty-fifth form), part of the 0x82BC8 process cluster. 66 lines. 1 callee (ProcessAndProcessAndProcess147). 4 callers (sub_82A2B660, sub_82A2C520, sub_82BC9860, and sub_82BC8A10).
[entrypoint.functions."0x82BC88B8"]
name = "ProcessAndProcessAndProcess155_82BC88B8"

# A processor that processes the data (one hundred and fifty-sixth form). It takes a structure (a: fields at a->8, a->32, and a->96). It processes the data (a). This is the "process this data" routine (one hundred and fifty-sixth form), part of the 0x82BC9 process cluster. 123 lines. 3 callees (IncrementAndCheckThreshold, ProcessAndDispatchAndProcess5, and sub_82BCCB88). 5 callers (ProcessAndProcessAndProcess154, sub_82BD7498, sub_82BC9860, sub_82BD5248, and ProcessAndDispatchElements3).
[entrypoint.functions."0x82BC9788"]
name = "ProcessAndProcessAndProcess156_82BC9788"

# A processor that processes the data (one hundred and fifty-seventh form). It takes a value (a), a value (b), and a value (c). It processes the data (a, b, and c). This is the "process this data" routine (one hundred and fifty-seventh form), part of the 0x82BCC process cluster. 118 lines. 1 callee (ComputeHash). 3 callers (sub_82BCCB88, sub_822DDF38, and ProcessAndGuardedDispatch).
[entrypoint.functions."0x82BCCAB8"]
name = "ProcessAndProcessAndProcess157_82BCCAB8"

# A processor that processes the data (one hundred and fifty-eighth form). It takes a structure (a: fields at a->6, a->16, a->20, a->40, and a->92) and a value (b). It processes the data (a and b). This is the "process this data" routine (one hundred and fifty-eighth form), part of the 0x82BCC process cluster. 130 lines. 4 callees (ProcessAndIterateLinkedList, ProcessAndProcessAndProcess157, sub_82BCCDE0, and sub_82CA9798). 12 callers (ProcessAndInitializeAndProcess3, ProcessAndInitializeAndProcessElements, ProcessStridedElement2, ProcessElement76, DispatchElement, DispatchAndAdjustPos, sub_82BCE250, ProcessAndIncrementAndProcess2, sub_82BCDAF8, ProcessAndProcessAndProcess156, IncrementAndCheckThreshold, and Process2DStrided).
[entrypoint.functions."0x82BCCB88"]
name = "ProcessAndProcessAndProcess158_82BCCB88"

# A processor that processes the data (one hundred and fifty-ninth form). It takes a structure (a: fields at a->40 and a->48) and a value (b). It processes the data (a and b). This is the "process this data" routine (one hundred and fifty-ninth form), part of the 0x82BCC process cluster. 103 lines. 2 callees (DispatchAndAdjustPos and sub_82BC9860). 4 callers (ProcessAndProcessAndProcess158, ProcessAndGuardedDispatch, sub_82BCCE98, and ProcessStateAndRange).
[entrypoint.functions."0x82BCCDE0"]
name = "ProcessAndProcessAndProcess159_82BCCDE0"

# A processor that processes the data (one hundred and sixtieth form). It takes a structure (a: fields at a->20 and a->48). It processes the data (a). This is the "process this data" routine (one hundred and sixtieth form), part of the 0x82BCC process cluster. 68 lines. 3 callees (sub_82BC9860, ProcessAndProcessAndProcess158, and ProcessAndProcessAndProcess159). 1 caller (ProcessState).
[entrypoint.functions."0x82BCCE98"]
name = "ProcessAndProcessAndProcess160_82BCCE98"

# A processor that processes the data (one hundred and sixty-first form). It takes a structure (a: fields at a->0 and a->36). It processes the data (a). This is the "process this data" routine (one hundred and sixty-first form), part of the 0x82BCD process cluster. 139 lines. 2 callees (ProcessAndQueryIndex and sub_82BCDAF8). 3 callers (ProcessAndDispatchByCounter, sub_82BCE6E8, and sub_82BCE250).
[entrypoint.functions."0x82BCDC20"]
name = "ProcessAndProcessAndProcess161_82BCDC20"

# A processor that processes the data (one hundred and sixty-second form). It takes a structure (a: fields at a->8 and a->12). It processes the data (a). This is the "process this data" routine (one hundred and sixty-second form), part of the 0x82BD6 process cluster. 470 lines. 11 callees (GetIndexedPointer, GetTypedField, IncrementAndCheckThreshold, InsertHashEntry, ProcessAndInsertHash, ProcessAndLookupIndexedPointer, ProcessLoopDispatch, sub_82BC5A30, sub_82BC5C08, sub_82BC67F0, and sub_82BCBDC8). 0 callers (unresolved — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x82BD6D70"]
name = "ProcessAndProcessAndProcess162_82BD6D70"

# A processor that processes the data (one hundred and sixty-third form). It takes a structure (a: fields at a->12 and a->28) and a value (b). It processes the data (a and b). This is the "process this data" routine (one hundred and sixty-third form), part of the 0x82BDF process cluster. 65 lines. 3 callees (ProcessAndDispatchAndProcess10, ProcessAndShiftFields, and sub_82BDFEB8). 5 callers (sub_82BE0BA8, sub_82BE11D0, ProcessAndDispatchAndProcess15, sub_82BE02F8, and sub_82BE13E8).
[entrypoint.functions."0x82BDF330"]
name = "ProcessAndProcessAndProcess163_82BDF330"

# A processor that processes the data (one hundred and sixty-fourth form). It takes a structure (a: field at a->12) and a value (b). It processes the data (a and b). This is the "process this data" routine (one hundred and sixty-fourth form), part of the 0x82BDF process cluster. 562 lines. 7 callees (ProcessAndComputeAndStore, ProcessAndDispatchAndProcess4, ProcessAndShiftFields, sub_82BCDAF8, sub_82BDDDD8, sub_82BDEE48, and sub_82BDF220). 1 caller (sub_82BDFEB8).
[entrypoint.functions."0x82BDFA98"]
name = "ProcessAndProcessAndProcess164_82BDFA98"

# A processor that processes the data (one hundred and sixty-fifth form). It takes a structure (a: field at a->12), a value (b), and a value (c). It processes the data (a, b, and c). This is the "process this data" routine (one hundred and sixty-fifth form), part of the 0x82BDF process cluster. 339 lines. 13 callees (ProcessAndDispatchAndProcess10, ProcessAndDispatchAndProcess11, ProcessAndDispatchAndProcess16, ProcessAndIncrementAndProcess2, ProcessAndShiftFields, SwitchDispatch, ProcessAndProcessAndProcess164, itself, sub_82BE2A08, sub_82BE2AF8, sub_82BE2BE0, sub_82BE2EE0, and sub_82BE3140). 8 callers (sub_82BE0440, sub_82BDEDD0, sub_82BDECC0, sub_82BDEC40, ProcessAndProcessAndProcess163, sub_82BE0EB8, ProcessAndDispatchAndProcess4, and sub_82BE09D0).
[entrypoint.functions."0x82BDFEB8"]
name = "ProcessAndProcessAndProcess165_82BDFEB8"

# A processor that processes the data (one hundred and sixty-sixth form). It takes a structure (a: field at a->12), a structure (b: field at b->4), and a value (c). It processes the data (a, b, and c). This is the "process this data" routine (one hundred and sixty-sixth form), part of the 0x82BE0 process cluster. 191 lines. 10 callees (ProcessAndDispatchAndProcess4, ProcessAndProcessAndProcess2, ProcessAndShiftFields, sub_82BCDAF8, sub_82BDE318, ProcessAndProcessAndProcess163, sub_82BE0208, itself, sub_82BE1E98, and sub_82BE25E8). 1 caller (ProcessAndDispatchAndProcess6).
[entrypoint.functions."0x82BE02F8"]
name = "ProcessAndProcessAndProcess166_82BE02F8"

# A processor that processes the data (one hundred and sixty-seventh form). It takes a structure (a: fields at a->0 and a->12) and a structure (b: fields at b->0 and b->4). It processes the data (a and b). This is the "process this data" routine (one hundred and sixty-seventh form), part of the 0x82BE1 process cluster. 57 lines. 0 callees (no function calls). 4 callers (ProcessAndDispatchByValue, sub_82A377B8, sub_82A34EB0, and ProcessAndProcessAndProcess166).
[entrypoint.functions."0x82BE1E98"]
name = "ProcessAndProcessAndProcess167_82BE1E98"

# A processor that processes the data (one hundred and sixty-eighth form). It takes a value (a), a structure (b: field at b->0), and a structure (c: field at c->0). It processes the data (a, b, and c). This is the "process this data" routine (one hundred and sixty-eighth form), part of the 0x82BE2 process cluster. 208 lines. 4 callees (ProcessAndComputeAndStore, ProcessAndDispatchAndProcess11, ProcessAndDispatchAndProcess16, and ProcessAndDispatchAndProcess9). 3 callers (ProcessAndDispatchAndProcess6, sub_82BE10A0, and ProcessAndProcessAndProcess166).
[entrypoint.functions."0x82BE25E8"]
name = "ProcessAndProcessAndProcess168_82BE25E8"

# A processor that processes the data (one hundred and sixty-ninth form). It takes a structure (a: fields at a->4 and a->12), a value (b), and a structure (c: fields at c->0, c->4, and c->8). It processes the data (a, b, and c). This is the "process this data" routine (one hundred and sixty-ninth form), part of the 0x82BEE process cluster. 497 lines. 4 callees (ProcessAndUpdateGlobal, sub_82BECA88, sub_82BEFF50, and sub_82BF2158). 1 caller (sub_82BEEA18).
[entrypoint.functions."0x82BEE3D8"]
name = "ProcessAndProcessAndProcess169_82BEE3D8"

# A processor that processes the data (one hundred and seventy form). It takes a value (a) and a structure (b: fields at b->4 and b->8). It processes the data (a and b). This is the "process this data" routine (one hundred and seventy form), part of the 0x82BEE process cluster. 132 lines. 1 callee (ProcessAndProcessAndProcess169). 1 caller (ProcessAndComputeAndProcess2).
[entrypoint.functions."0x82BEEA18"]
name = "ProcessAndProcessAndProcess170_82BEEA18"

# A processor that processes the data (one hundred and seventy-first form). It takes a value (a). It processes the data (a). This is the "process this data" routine (one hundred and seventy-first form), part of the 0x82BEF process cluster. 133 lines. 2 callees (ValidateBounds16 and sub_82BF2B68). 1 caller (sub_82BF04F8).
[entrypoint.functions."0x82BEFC70"]
name = "ProcessAndProcessAndProcess171_82BEFC70"

# A processor that processes the data (one hundred and seventy-second form). It takes a value (a). It processes the data (a). This is the "process this data" routine (one hundred and seventy-second form), part of the 0x82BF2 process cluster. 75 lines. 2 callees (QueryFileAttributes and SetGlobalField_Thunk). 1 caller (sub_82BEEC70).
[entrypoint.functions."0x82BF2DB0"]
name = "ProcessAndProcessAndProcess172_82BF2DB0"

# A processor that processes the data (one hundred and seventy-third form). It takes a structure (a: fields at a->0, a->4, and a->12). It processes the data (a). This is the "process this data" routine (one hundred and seventy-third form), part of the 0x82BF2 process cluster. 57 lines. 2 callees (Buffer_CopyOrZero_Error and ProcessAndAbort). 1 caller (sub_82BEEC70).
[entrypoint.functions."0x82BF2FB0"]
name = "ProcessAndProcessAndProcess173_82BF2FB0"

# A processor that processes the data (one hundred and seventy-fourth form). It takes a structure (a: fields at a->4 and a->76) and a value (b). It processes the data (a and b). This is the "process this data" routine (one hundred and seventy-fourth form), part of the 0x82C14 process cluster. 24 lines. 1 callee (sub_82C1DF00). 0 callers (unresolved — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x82C14D18"]
name = "ProcessAndProcessAndProcess174_82C14D18"

# A processor that processes the data (one hundred and seventy-fifth form). It takes a structure (a: fields at a->36 and a->40) and a value (b). It processes the data (a and b). This is the "process this data" routine (one hundred and seventy-fifth form), part of the 0x82C1D process cluster. 62 lines. 1 callee (ProcessElements_Vcall4). 1 caller (ProcessAndProcessAndProcess174).
[entrypoint.functions."0x82C1DF00"]
name = "ProcessAndProcessAndProcess175_82C1DF00"

# A processor that processes the data (one hundred and seventy-sixth form). It takes a value (a). It processes the data (a). This is the "process this data" routine (one hundred and seventy-sixth form), part of the 0x82C32 process cluster. 95 lines. 1 callee (Allocate_OrDbgBreak). 0 callers (unresolved — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x82C328D8"]
name = "ProcessAndProcessAndProcess176_82C328D8"

# A processor that processes the data (one hundred and seventy-seventh form). It takes a value (a). It processes the data (a). This is the "process this data" routine (one hundred and seventy-seventh form), part of the 0x82C35 process cluster. 95 lines. 1 callee (Allocate_OrDbgBreak). 0 callers (unresolved — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x82C353A0"]
name = "ProcessAndProcessAndProcess177_82C353A0"

# A processor that processes the data (one hundred and seventy-eighth form). It takes nothing. It processes the data. This is the "process this data" routine (one hundred and seventy-eighth form), part of the 0x82C41 process cluster. 10 lines. 0 callees (no function calls). 0 callers (unresolved — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x82C41B80"]
name = "ProcessAndProcessAndProcess178_82C41B80"

# A processor that processes the data (one hundred and seventy-ninth form). It takes a value (a). It processes the data (a). This is the "process this data" routine (one hundred and seventy-ninth form), part of the 0x82C64 process cluster. 70 lines. 1 callee (Free_Thunk). 8 callers (sub_825ADB40, sub_825AD498, sub_825B2E20, sub_82C65310, sub_824150C0, sub_825B27D0, sub_8292ECD0, and sub_825B26A8).
[entrypoint.functions."0x82C648B0"]
name = "ProcessAndProcessAndProcess179_82C648B0"

# A processor that processes the data (one hundred and eightieth form). It takes a structure (a: field at a->0). It processes the data (a). This is the "process this data" routine (one hundred and eightieth form), part of the 0x82C65 process cluster. 80 lines. 4 callees (ConstructString, Free_Thunk, Trampoline2, and ProcessAndProcessAndProcess179). 5 callers (sub_82AAAA18, sub_825F3050, sub_82A48B68, sub_82516300, and sub_82AAA818).
[entrypoint.functions."0x82C65310"]
name = "ProcessAndProcessAndProcess180_82C65310"

# A processor that processes the data (one hundred and eighty-first form). It takes a structure (a: field at a->4). It processes the data (a). This is the "process this data" routine (one hundred and eighty-first form), part of the 0x82C6C process cluster. 83 lines. 1 callee (Allocate_OrDbgBreak). 5 callers (ProcessAndProcessAndProcess129, sub_83291AB8, sub_82C6EF38, sub_82B40A80, and sub_82C6BEB0).
[entrypoint.functions."0x82C6CF68"]
name = "ProcessAndProcessAndProcess181_82C6CF68"

# A processor that processes the data (one hundred and eighty-second form). It takes a structure (a: fields at a->36, a->40, a->44, and a->52). It processes the data (a). This is the "process this data" routine (one hundred and eighty-second form), part of the 0x82C6D process cluster. 98 lines. 0 callees (no function calls). 0 callers (unresolved — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x82C6D100"]
name = "ProcessAndProcessAndProcess182_82C6D100"

# A processor that processes the data (one hundred and eighty-third form). It takes a structure (a: fields at a->0 and a->4). It processes the data (a). This is the "process this data" routine (one hundred and eighty-third form), part of the 0x82C81 process cluster. 47 lines. 2 callees (sub_82C81D28 and sub_82C87A28). 4 callers (sub_82C86338, sub_82C83138, sub_82C874E0, and sub_82C85758).
[entrypoint.functions."0x82C81BA8"]
name = "ProcessAndProcessAndProcess183_82C81BA8"

# A processor that processes the data (one hundred and eighty-fourth form). It takes a structure (a: field at a->0). It processes the data (a). This is the "process this data" routine (one hundred and eighty-fourth form), part of the 0x82C81 process cluster. 33 lines. 2 callees (sub_82C82138 and sub_82C87AA0). 7 callers (sub_82C87538, sub_82C86828, sub_82C83078, sub_82C831B0, sub_82C87610, sub_82C84DF8, and sub_82C86388).
[entrypoint.functions."0x82C81BF8"]
name = "ProcessAndProcessAndProcess184_82C81BF8"

# A processor that processes the data (one hundred and eighty-fifth form). It takes a value (a). It processes the data (a). This is the "process this data" routine (one hundred and eighty-fifth form), part of the 0x82C81 process cluster. 61 lines. 0 callees (no function calls). 1 caller (ProcessAndProcessAndProcess183).
[entrypoint.functions."0x82C81D28"]
name = "ProcessAndProcessAndProcess185_82C81D28"

# A processor that processes the data (one hundred and eighty-sixth form). It takes a value (a), a value (b), and a value (c). It processes the data (a, b, and c). This is the "process this data" routine (one hundred and eighty-sixth form), part of the 0x82C82 process cluster. 137 lines. 2 callees (DestructAndFree and sub_82C82038). 2 callers (sub_82C81C30 and ProcessAndProcessAndProcess184).
[entrypoint.functions."0x82C82138"]
name = "ProcessAndProcessAndProcess186_82C82138"

# A processor that processes the data (one hundred and eighty-seventh form). It takes nothing. It processes the data. This is the "process this data" routine (one hundred and eighty-seventh form), part of the 0x82C87 process cluster. 14 lines. 0 callees (no function calls). 1 caller (ProcessAndProcessAndProcess183).
[entrypoint.functions."0x82C87A28"]
name = "ProcessAndProcessAndProcess187_82C87A28"

# A processor that processes the data (one hundred and eighty-eighth form). It takes a structure (a: fields at a->0, a->12, a->16, and a->20). It processes the data (a). This is the "process this data" routine (one hundred and eighty-eighth form), part of the 0x82C87 process cluster. 64 lines. 1 callee (Free_Thunk). 2 callers (sub_82C81C30 and ProcessAndProcessAndProcess184).
[entrypoint.functions."0x82C87AA0"]
name = "ProcessAndProcessAndProcess188_82C87AA0"

# A processor that processes the data (one hundred and eighty-ninth form). It takes nothing. It processes the data. This is the "process this data" routine (one hundred and eighty-ninth form), part of the 0x82C88 process cluster. 7 lines. 1 callee (AllocateAndIncrementCounter). 4 callers (ProcessAndAllocateAndProcess2, sub_82C85E98, sub_82C85F68, and sub_82C85F00).
[entrypoint.functions."0x82C88628"]
name = "ProcessAndProcessAndProcess189_82C88628"

# A processor that processes the data (one hundred and ninety form). It takes a structure (a: fields at a->0 and a->4). It processes the data (a). This is the "process this data" routine (one hundred and ninety form), part of the 0x82C88 process cluster. 69 lines. 1 callee (AllocateAndIncrementCounter). 4 callers (ProcessAndAllocateAndProcess2, sub_82C85E98, sub_82C85F68, and sub_82C85F00).
[entrypoint.functions."0x82C88678"]
name = "ProcessAndProcessAndProcess190_82C88678"

# A processor that processes the data (one hundred and ninety-first form). It takes a structure (a: fields at a->0, a->4, and a->16). It processes the data (a). This is the "process this data" routine (one hundred and ninety-first form), part of the 0x82C88 process cluster. 64 lines. 3 callees (DestructAndFree, sub_82C82038, and sub_82C89208). 7 callers (sub_82C87538, sub_82C86828, sub_82C87610, sub_82C85F68, sub_82C85F00, sub_82C86388, and sub_82C85E98).
[entrypoint.functions."0x82C88A58"]
name = "ProcessAndProcessAndProcess191_82C88A58"

# A comparator that compares the data. It takes a value (a), a value (b), and a value (c). It compares the data (a, b, and c) and returns a comparison result. This is the "compare this data" routine, part of the 0x82C8D compare cluster. 51 lines. 0 callees (no function calls). 0 callers (unresolved — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x82C8D5D8"]
name = "CompareAndProcess_82C8D5D8"

# A processor that processes the data (one hundred and ninety-second form). It takes a structure (a: fields at a->0, a->4, a->73, and a->76), a value (b), and a value (c). It processes the data (a, b, and c). This is the "process this data" routine (one hundred and ninety-second form), part of the 0x82C98 process cluster. 74 lines. 1 callee (sub_82C97EE0). 1 caller (sub_82CA2588).
[entrypoint.functions."0x82C98268"]
name = "ProcessAndProcessAndProcess192_82C98268"

# A processor that processes the data (one hundred and ninety-third form). It takes a value (a). It processes the data (a). This is the "process this data" routine (one hundred and ninety-third form), part of the 0x82CA2 process cluster. 7 lines. 1 callee (DestructAndFree). 10 callers (sub_82C85F68, sub_82C89028, ProcessAndRecurseAndProcess, sub_82C87610, sub_82C85E98, sub_82C85F00, sub_82C86388, sub_82C87538, sub_82C86828, and ProcessAndFreeAndProcess).
[entrypoint.functions."0x82CA29E8"]
name = "ProcessAndProcessAndProcess193_82CA29E8"

# A processor that processes the data (one hundred and ninety-fourth form). It takes nothing. It processes the data. This is the "process this data" routine (one hundred and ninety-fourth form), part of the 0x82CA3 process cluster. 11 lines. 1 callee (sub_82CA62E0). 12 callers (sub_82E96140, sub_82ED4E00, sub_82EC4C40, sub_82E99A58, sub_8306B058, sub_82E95850, sub_82ED6368, sub_82E945E8, sub_8306AC80, sub_82E911F8, sub_82F9B808, and sub_82EBB350).
[entrypoint.functions."0x82CA3B30"]
name = "ProcessAndProcessAndProcess194_82CA3B30"

# A processor that processes the data (one hundred and ninety-fifth form). It takes a structure (a: field at a->0), a value (b), a value (c), and a value (d). It processes the data (a, b, c, and d). This is the "process this data" routine (one hundred and ninety-fifth form), part of the 0x82CA5 process cluster. 434 lines. 3 callees (sub_82CAB630, sub_82CAB770, and sub_82CB03D8). 2 callers (sub_82CA6300 and sub_82CA62E0).
[entrypoint.functions."0x82CA5FF0"]
name = "ProcessAndProcessAndProcess195_82CA5FF0"

# A processor that processes the data (one hundred and ninety-sixth form). It takes a value (a), a value (b), and a value (c). It processes the data (a, b, and c). This is the "process this data" routine (one hundred and ninety-sixth form), part of the 0x82CA6 process cluster. 21 lines. 1 callee (ProcessAndProcessAndProcess195). 5 callers (sub_82EEEA48, sub_82F322D8, ProcessAndProcessAndProcess194, sub_82F6A8C0, and sub_82F11C60).
[entrypoint.functions."0x82CA62E0"]
name = "ProcessAndProcessAndProcess196_82CA62E0"

# A processor that processes the data (one hundred and ninety-seventh form). It takes nothing. It processes the data. This is the "process this data" routine (one hundred and ninety-seventh form), part of the 0x82CAC process cluster. 10 lines. 0 callees (no function calls). 2 callers (sub_82CA3430 and sub_830029D8).
[entrypoint.functions."0x82CAC418"]
name = "ProcessAndProcessAndProcess197_82CAC418"

# A processor that processes the data (one hundred and ninety-eighth form). It takes a value (a). It processes the data (a). This is the "process this data" routine (one hundred and ninety-eighth form), part of the 0x82CC1 process cluster. 62 lines. 0 callees (no function calls). 3 callers (sub_82CFAC40, sub_82CFACA8, and sub_82CBC3B8).
[entrypoint.functions."0x82CC1C90"]
name = "ProcessAndProcessAndProcess198_82CC1C90"

# A processor that processes the data (one hundred and ninety-ninth form). It takes a value (a), a value (b), a value (c), a value (d), a value (e), a value (f), and a value (g). It processes the data (a, b, c, d, e, f, and g). This is the "process this data" routine (one hundred and ninety-ninth form), part of the 0x82CFA process cluster. 62 lines. 1 callee (ProcessAndProcessAndProcess198). 5 callers (sub_823C2148, sub_82886658, sub_824E7A50, sub_82374D58, and sub_824E5978).
[entrypoint.functions."0x82CFAC40"]
name = "ProcessAndProcessAndProcess199_82CFAC40"

# A processor that processes the data (two hundred form). It takes a structure (a: field at a->12). It processes the data (a). This is the "process this data" routine (two hundred form), part of the 0x8322F process cluster. 61 lines. 2 callees (ProcessAndLookupWithDefaultInit and VirtualFree). 2 callers (sub_83236448 and sub_832363D8).
[entrypoint.functions."0x8322FF28"]
name = "ProcessAndProcessAndProcess200_8322FF28"

# A processor that processes the data (two hundred and first form). It takes a structure (a: fields at a->0, a->4, a->8, a->12, and a->20) and a value (b). It processes the data (a and b). This is the "process this data" routine (two hundred and first form), part of the 0x83230 process cluster. 48 lines. 0 callees (no function calls). 1 caller (ProcessAndAccessAndStore).
[entrypoint.functions."0x83230490"]
name = "ProcessAndProcessAndProcess201_83230490"

# A processor that processes the data (two hundred and second form). It takes a structure (a: fields at a->0, a->4, a->8, and a->20). It processes the data (a). This is the "process this data" routine (two hundred and second form), part of the 0x83230 process cluster. 184 lines. 3 callees (MemSet_SdkRuntime, PoolAllocateLock, and VirtualAllocate). 1 caller (HeapAllocate).
[entrypoint.functions."0x83230DA0"]
name = "ProcessAndProcessAndProcess202_83230DA0"

# A processor that processes the data (two hundred and third form). It takes a structure (a: fields at a->0, a->8, and a->12). It processes the data (a). This is the "process this data" routine (two hundred and third form), part of the 0x83236 process cluster. 74 lines. 2 callees (ProcessAndLookupWithDefaultInit and ProcessAndProcessAndProcess200). 2 callers (sub_823058E8 and sub_82415428).
[entrypoint.functions."0x832363D8"]
name = "ProcessAndProcessAndProcess203_832363D8"

# A processor that processes the data (two hundred and fourth form). It takes a structure (a: fields at a->0, a->4, a->8, and a->20). It processes the data (a). This is the "process this data" routine (two hundred and fourth form), part of the 0x832B8 process cluster. About 100 lines. 3 callees (MutexLock_WaitInfinite, MutexUnlock_ReleaseMutant, and PriorityArbitrate_PendingMutexRequests). 0 callers (unresolved — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x832B8DA8"]
name = "ProcessAndProcessAndProcess204_832B8DA8"

# A processor that processes the data (two hundred and fifth form). It takes nothing. It processes the data. This is the "process this data" routine (two hundred and fifth form), part of the 0x8220A process cluster. 36 lines. 1 callee (sub_82A1F740). 0 callers (unresolved — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x8220AFD0"]
name = "ProcessAndProcessAndProcess205_8220AFD0"

# A processor that processes the data (two hundred and sixth form). It takes a value (a). It processes the data (a). This is the "process this data" routine (two hundred and sixth form), part of the 0x82228 process cluster. 60 lines. 1 callee (AccessElement8). 12 callers (sub_82227750, sub_825DA1D8, sub_82956010, sub_828BE3F0, sub_827ACF10, sub_825B96B8, sub_82794058, sub_82833598, sub_825B3CF0, sub_8234E170, sub_825D6840, and sub_823B2598).
[entrypoint.functions."0x822281F8"]
name = "ProcessAndProcessAndProcess206_822281F8"

# A processor that processes the data (two hundred and seventh form). It takes a value (a), a value (b), a value (c), and a value (d). It processes the data (a, b, c, and d). This is the "process this data" routine (two hundred and seventh form), part of the 0x82228 process cluster. 53 lines. 1 callee (HashLookupByKey). 5 callers (ProcessAndDispatchTypedValue, sub_82A2D660, ProcessDispatchByType, sub_82A30038, and sub_82A2C9F8).
[entrypoint.functions."0x82228518"]
name = "ProcessAndProcessAndProcess207_82228518"

# A processor that processes the data (two hundred and eighth form). It takes a value (a), a value (b), and a value (c). It processes the data (a, b, and c). This is the "process this data" routine (two hundred and eighth form), part of the 0x82233 process cluster. 82 lines. 1 callee (ProcessVirtual). 4 callers (sub_82A2E630, GuildChest_ProcessItems, sub_82BF5038, and sub_82BF4E00).
[entrypoint.functions."0x822338F0"]
name = "ProcessAndProcessAndProcess208_822338F0"

# A processor that processes the data (two hundred and ninth form). It takes a value (a) and a value (b). It processes the data (a and b). This is the "process this data" routine (two hundred and ninth form), part of the 0x822D5 process cluster. 76 lines. 1 callee (Release_RefCounted). 12 callers (sub_828F49B0, sub_8239BDA8, sub_828A1890, sub_82186720, sub_824E83A0, sub_82343B10, sub_824FE6D8, sub_823A18C0, sub_82308690, sub_823A1CF0, sub_823419E0, and sub_82608810).
[entrypoint.functions."0x822D52C0"]
name = "ProcessAndProcessAndProcess209_822D52C0"

# A processor that processes the data (two hundred and tenth form). It takes a structure (a: field at a->8). It processes the data (a). This is the "process this data" routine (two hundred and tenth form), part of the 0x822E3 process cluster. 110 lines. 2 callees (ProcessAndHashString and sub_82A1F740). 0 callers (unresolved — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x822E38E0"]
name = "ProcessAndProcessAndProcess210_822E38E0"

# A processor that processes the data (two hundred and eleventh form). It takes a value (a). It processes the data (a). This is the "process this data" routine (two hundred and eleventh form), part of the 0x822F3 process cluster. 377 lines. 24 callees (Allocate_OrDbgBreak, ConstructRefCounted, CopyAssign_RefCounted, CriticalSection_EnterOrTry, DestructRefCounted, ProcessAndConstructAndAtomicIncrement, ProcessAndConstructAndProcess12, ProcessAndDestructAndZero, Release_RefCounted, SetStringBuffer, sub_822F3B18, sub_822F55A0, sub_8236D4E0, sub_823726F8, sub_82378CD0, sub_82378FA0, sub_829FAF40, sub_829FB018, sub_82A1CDD8, sub_82A1CEF0, sub_82B38798, sub_82CBBE20, and sub_8322F940). 0 callers (unresolved — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x822F33B8"]
name = "ProcessAndProcessAndProcess211_822F33B8"

# A processor that processes the data (two hundred and twelfth form). It takes a value (a) and a value (b). It processes the data (a and b). This is the "process this data" routine (two hundred and twelfth form), part of the 0x822F3 process cluster. 78 lines. 3 callees (ProcessAndComputeAndProcess12, ProcessAndInitializeAndProcess, and sub_822F6258). 0 callers (unresolved — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x822F3828"]
name = "ProcessAndProcessAndProcess212_822F3828"

# A processor that processes the data (two hundred and thirteenth form). It takes a value (a). It processes the data (a). This is the "process this data" routine (two hundred and thirteenth form), part of the 0x822F3 process cluster. 357 lines. 20 callees (Allocate_OrDbgBreak, ConstructRefCounted, ConstructStringElement, CopyAssign_RefCounted2, CopyAssign_RefCounted, Destruct3, DestructRefCounted, Free_SizeBucketed_Thunk, IncrementGlobalCounter2, ProcessAndConstructAndProcess7, ProcessAndConstructLowerString, ProcessAtomicIncrement, ProcessElementAccess3, ReleaseGlobalRef, StringCompare, sub_823577C0, sub_824151E8, sub_82415428, sub_824155C8, and sub_82B39978). 0 callers (unresolved — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x822F38B0"]
name = "ProcessAndProcessAndProcess213_822F38B0"

# A processor that processes the data (two hundred and fourteenth form). It takes a value (a). It processes the data (a). This is the "process this data" routine (two hundred and fourteenth form), part of the 0x822F3 process cluster. 305 lines. 21 callees (ConstructRefCounted, ConstructString, CopyAssign_RefCounted, DestructRefCounted, Free_SizeBucketed_Thunk, IsNonZero, ProcessAndCompareAndConstruct, ProcessAndConstructAndProcess23, ProcessAndInitializeAndProcess2, ProcessAndVcallAndDestruct3, sub_822F4260, sub_822F4458, sub_82368538, sub_8236A460, sub_8236A530, sub_82372E88, sub_82373E08, sub_82A48B68, sub_82A492B0, sub_82A493A8, and sub_82C62940). 1 caller (ProcessAndProcessAndProcess211).
[entrypoint.functions."0x822F3B18"]
name = "ProcessAndProcessAndProcess214_822F3B18"

# A processor that processes the data (two hundred and fifteenth form). It takes a structure (a: fields at a->108, a->168, a->180, and a->204). It processes the data (a). This is the "process this data" routine (two hundred and fifteenth form), part of the 0x822F4 process cluster. 100 lines. 7 callees (DestructNodePayload, Free_SizeBucketed_Thunk, sub_822F5D48, sub_822F6660, sub_822F6990, sub_822F69F8, and sub_8233DDD8). 3 callers (sub_82372758, sub_822F66C8, and sub_822F4D10).
[entrypoint.functions."0x822F4DF8"]
name = "ProcessAndProcessAndProcess215_822F4DF8"

# A processor that processes the data (two hundred and sixteenth form). It takes a structure (a: fields at a->0 and a->4). It processes the data (a). This is the "process this data" routine (two hundred and sixteenth form), part of the 0x822F5 process cluster. 62 lines. 1 callee (Allocate_OrDbgBreak). 2 callers (ProcessAndProcessAndProcess211 and sub_82374240).
[entrypoint.functions."0x822F55A0"]
name = "ProcessAndProcessAndProcess216_822F55A0"

# A processor that processes the data (two hundred and seventeenth form). It takes a structure (a: fields at a->20, a->24, and a->28). It processes the data (a). This is the "process this data" routine (two hundred and seventeenth form), part of the 0x822F5 process cluster. 130 lines. 1 callee (Free_SizeBucketed_Thunk). 1 caller (ProcessAndProcessAndProcess215).
[entrypoint.functions."0x822F5D48"]
name = "ProcessAndProcessAndProcess217_822F5D48"

# A processor that processes the data (two hundred and eighteenth form). It takes a structure (a: fields at a->4, a->8, and a->12) and a value (b). It processes the data (a and b). This is the "process this data" routine (two hundred and eighteenth form), part of the 0x822F6 process cluster. 121 lines. 1 callee (sub_822F6BD0). 1 caller (ProcessAndProcessAndProcess212).
[entrypoint.functions."0x822F6258"]
name = "ProcessAndProcessAndProcess218_822F6258"

# A processor that processes the data (two hundred and nineteenth form). It takes a structure (a: fields at a->0, a->4, and a->8). It processes the data (a). This is the "process this data" routine (two hundred and nineteenth form), part of the 0x822F6 process cluster. 61 lines. 2 callees (Free_SizeBucketed_Thunk and sub_822F6F58). 2 callers (sub_82B40B50 and ProcessAndProcessAndProcess215).
[entrypoint.functions."0x822F6660"]
name = "ProcessAndProcessAndProcess219_822F6660"

# A processor that processes the data (two hundred and twentieth form). It takes a structure (a: fields at a->0, a->4, and a->8). It processes the data (a). This is the "process this data" routine (two hundred and twentieth form), part of the 0x822F6 process cluster. 61 lines. 2 callees (Free_SizeBucketed_Thunk and sub_82B4CC70). 4 callers (ProcessAndProcessAndProcess215, sub_82A46EA8, sub_82A47D48, and sub_832ABE40).
[entrypoint.functions."0x822F6990"]
name = "ProcessAndProcessAndProcess220_822F6990"

# A processor that processes the data (two hundred and twenty-first form). It takes a structure (a: fields at a->0, a->4, and a->8). It processes the data (a). This is the "process this data" routine (two hundred and twenty-first form), part of the 0x822F6 process cluster. 87 lines. 1 callee (Free_SizeBucketed_Thunk). 1 caller (ProcessAndProcessAndProcess215).
[entrypoint.functions."0x822F69F8"]
name = "ProcessAndProcessAndProcess221_822F69F8"

# A processor that processes the data (two hundred and twenty-second form). It takes a value (a), a structure (b: fields at b->4 and b->8), and a value (c). It processes the data (a, b, and c). This is the "process this data" routine (two hundred and twenty-second form), part of the 0x822F6 process cluster. 132 lines. 1 callee (sub_822F7B70). 1 caller (ProcessAndProcessAndProcess218).
[entrypoint.functions."0x822F6BD0"]
name = "ProcessAndProcessAndProcess222_822F6BD0"

# A processor that processes the data (two hundred and twenty-third form). It takes a value (a), a structure (b: field at b->4), a value (c), and a value (d). It processes the data (a, b, c, and d). This is the "process this data" routine (two hundred and twenty-third form), part of the 0x822F6 process cluster. 155 lines. 3 callees (DestructNested, FindListTail, and sub_822F7F40). 1 caller (ProcessAndProcessAndProcess219).
[entrypoint.functions."0x822F6F58"]
name = "ProcessAndProcessAndProcess223_822F6F58"

# A processor that processes the data (two hundred and twenty-fourth form). It takes a value (a), a value (b), and a value (c). It processes the data (a, b, and c). This is the "process this data" routine (two hundred and twenty-fourth form), part of the 0x822F7 process cluster. 402 lines. 8 callees (AllocateChecked3, Free_SizeBucketed_Thunk, ProcessAndCallMethods6, ProcessAndVcallAndProcess3, VirtualCall4, sub_822F84D8, sub_822F8540, and sub_82684B38). 1 caller (ProcessAndProcessAndProcess222).
[entrypoint.functions."0x822F7B70"]
name = "ProcessAndProcessAndProcess224_822F7B70"

# A processor that processes the data (two hundred and twenty-fifth form). It takes a value (a) and a value (b). It processes the data (a and b). This is the "process this data" routine (two hundred and twenty-fifth form), part of the 0x822F8 process cluster. 61 lines. 0 callees (no function calls). 6 callers (ProcessAndProcessAndProcess224, ProcessAndCompareAndConstruct, sub_825F4EA0, sub_825F3050, sub_8233E9C0, and sub_826928F8).
[entrypoint.functions."0x822F84D8"]
name = "ProcessAndProcessAndProcess225_822F84D8"

# A processor that processes the data (two hundred and twenty-sixth form). It takes a structure (a: fields at a->8, a->12, a->16, a->20, a->24, a->28, a->32, a->36, a->40, a->44, a->48, a->52, a->56, a->60, and a->64). It processes the data (a). This is the "process this data" routine (two hundred and twenty-sixth form), part of the 0x82305 process cluster. 417 lines. 7 callees (Allocate_OrDbgBreak, Construct16Byte, sub_8235E278, sub_8240D938, sub_8241AAB0, sub_8241AC20, and sub_829F8250). 2 callers (sub_82378CD0 and sub_8233AE50).
[entrypoint.functions."0x823052C0"]
name = "ProcessAndProcessAndProcess226_823052C0"

# A processor that processes the data (two hundred and twenty-seventh form). It takes a value (a), a value (b), and a value (c). It processes the data (a, b, and c). This is the "process this data" routine (two hundred and twenty-seventh form), part of the 0x82307 process cluster. 77 lines. 3 callees (Allocate_OrDbgBreak, CopyAssign_RefCounted, and sub_825BBA48). 11 callers (sub_823A5870, sub_82A188F0, sub_82357490, sub_825B27D0, sub_82308B48, sub_825B2E20, sub_82A17518, sub_825B26A8, sub_82172A10, sub_82A18998, and ProcessAndVcallAndProcess16).
[entrypoint.functions."0x823077B8"]
name = "ProcessAndProcessAndProcess227_823077B8"

# A processor that processes the data (two hundred and twenty-eighth form). It takes nothing. It processes the data. This is the "process this data" routine (two hundred and twenty-eighth form), part of the 0x82338 process cluster. 135 lines. 0 callees (no function calls). 2 callers (sub_82368538 and sub_82368B20).
[entrypoint.functions."0x82338DA8"]
name = "ProcessAndProcessAndProcess228_82338DA8"

# A processor that processes the data (two hundred and twenty-ninth form). It takes a structure (a: fields at a->0, a->4, and a->8). It processes the data (a). This is the "process this data" routine (two hundred and twenty-ninth form), part of the 0x8233D process cluster. 61 lines. 2 callees (Free_SizeBucketed_Thunk and sub_8233E468). 4 callers (sub_8233DB90, sub_832AEFD0, sub_822F0250, and ProcessAndProcessAndProcess215).
[entrypoint.functions."0x8233DDD8"]
name = "ProcessAndProcessAndProcess229_8233DDD8"

# A processor that processes the data (two hundred and thirtieth form). It takes a structure (a: fields at a->4, a->8, and a->12) and a value (b). It processes the data (a and b). This is the "process this data" routine (two hundred and thirtieth form), part of the 0x82344 process cluster. 69 lines. 2 callees (ConstructArray4 and sub_82A97648). 5 callers (sub_82343C68, sub_82AB7210, sub_82A4D268, sub_82343788, and sub_82B80D70).
[entrypoint.functions."0x82344AC8"]
name = "ProcessAndProcessAndProcess230_82344AC8"

# A processor that processes the data (two hundred and thirty-first form). It takes a structure (a: field at a->4) and a structure (b: fields at b->0, b->4, and b->8). It processes the data (a and b). This is the "process this data" routine (two hundred and thirty-first form), part of the 0x8234D process cluster. 67 lines. 0 callees (no function calls). 12 callers (sub_8266D550, sub_822CC120, sub_824AE5C8, sub_82C36948, sub_82B64498, sub_825516E0, sub_8234D710, sub_82893788, sub_824DF9C0, sub_82C36510, sub_82B64B70, and sub_82308418).
[entrypoint.functions."0x8234DF98"]
name = "ProcessAndProcessAndProcess231_8234DF98"

# A processor that processes the data (two hundred and thirty-second form). It takes a value (a). It processes the data (a). This is the "process this data" routine (two hundred and thirty-second form), part of the 0x82357 process cluster. 84 lines. 2 callees (ProcessAndProcessAndProcess227 and sub_82358830). 0 callers (unresolved — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x82357490"]
name = "ProcessAndProcessAndProcess232_82357490"

# A processor that processes the data (two hundred and thirty-third form). It takes a value (a). It processes the data (a). This is the "process this data" routine (two hundred and thirty-third form), part of the 0x82357 process cluster. 874 lines. 15 callees (ProcessAndAppendAndResolve2, ProcessAndAppendAndResolve, ProcessAndAppendElement, ProcessAndCopyReferenceAndHash, ProcessAndDispatchAndResolve, ProcessAndHashString, ProcessAndResolveTypedField, Release_RefCounted, sub_82350E30, sub_82358A18, sub_82358AB0, sub_82358B48, sub_82415778, sub_824EF300, and sub_8260C3B8). 2 callers (ProcessAndProcessAndProcess213 and sub_82357DF8).
[entrypoint.functions."0x823577C0"]
name = "ProcessAndProcessAndProcess233_823577C0"

# Find a string in a linked list of string entries. Walks the list (each node: [0]=next, [8]=string pointer) from the start up to an end sentinel, comparing each node's string against a target string (pointer identity first, then content via CompareStringsSigned). On a match it writes the matched node to the output slot and stops; otherwise it advances to the next node. Returns whether a match was found. Used to look up a path in the VFS file index and in other string tables. 106 lines. 1 callee (CompareStringsSigned_8226D750). 5 callers (sub_823A5870, ProcessAndProcessAndProcess232, sub_825B26A8, sub_825B27D0, and sub_825B2E20).
[entrypoint.functions."0x82358830"]
name = "FindStringInList_82358830"

# A processor that processes the data (two hundred and thirty-fifth form). It takes a value (a), a value (b), and a value (c). It processes the data (a, b, and c). This is the "process this data" routine (two hundred and thirty-fifth form), part of the 0x82358 process cluster. 89 lines. 5 callees (ProcessAndAppendElement, ProcessAndCopyReferenceAndHash, ProcessAndDispatchAndResolve, ProcessAndHashString, and ProcessAndResolveTypedField). 9 callers (sub_82526808, sub_82908E20, sub_82683240, sub_823ADF80, sub_824DE170, sub_82425680, ProcessAndProcessAndProcess233, sub_825ACEA0, and sub_82879778).
[entrypoint.functions."0x82358AB0"]
name = "ProcessAndProcessAndProcess235_82358AB0"

# A processor that processes the data (two hundred and thirty-sixth form). It takes a value (a). It processes the data (a). This is the "process this data" routine (two hundred and thirty-sixth form), part of the 0x82358 process cluster. 63 lines. 3 callees (DestructRefCounted, ProcessAndProcessAndProcess206, and sub_823A5228). 0 callers (unresolved — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x82358CB0"]
name = "ProcessAndProcessAndProcess236_82358CB0"

# A processor that processes the data (two hundred and thirty-seventh form). It takes a value (a). It processes the data (a). This is the "process this data" routine (two hundred and thirty-seventh form), part of the 0x82368 process cluster. 440 lines. 7 callees (Free_SizeBucketed_Thunk, ProcessAndProcessAndProcess228, sub_82368830, sub_82368E30, sub_82368F10, sub_82369210, and sub_8236A058). 1 caller (ProcessAndProcessAndProcess214).
[entrypoint.functions."0x82368538"]
name = "ProcessAndProcessAndProcess237_82368538"

# A processor that processes the data (two hundred and thirty-eighth form). It takes a value (a) and a structure (b: field at b->0). It processes the data (a and b). This is the "process this data" routine (two hundred and thirty-eighth form), part of the 0x82368 process cluster. 412 lines. 11 callees (AssignRefCounted, ConstructStringElement, Destruct3, MemSet_SdkRuntime, ProcessAndCheckSuccess, ProcessElementAccess3, ReadStringElement, sub_826C0460, sub_82CA3B30, sub_82CBCFA8, and sub_82CBD1B0). 3 callers (sub_82368C30, sub_82368B20, and ProcessAndProcessAndProcess237).
[entrypoint.functions."0x82368830"]
name = "ProcessAndProcessAndProcess238_82368830"

# A processor that processes the data (two hundred and thirty-ninth form). It takes a structure (a: fields at a->36, a->40, and a->44) and a value (b). It processes the data (a and b). This is the "process this data" routine (two hundred and thirty-ninth form), part of the 0x8236A process cluster. 125 lines. 1 callee (sub_82C6C328). 2 callers (sub_825F3050 and ProcessAndProcessAndProcess214).
[entrypoint.functions."0x8236A460"]
name = "ProcessAndProcessAndProcess239_8236A460"

# A processor that processes the data (two hundred and fortieth form). It takes a structure (a: field at a->20) and a structure (b: fields at b->4, b->20, and b->24). It processes the data (a and b). This is the "process this data" routine (two hundred and fortieth form), part of the 0x8236A process cluster. 52 lines. 1 callee (ProcessAndClampGrow). 6 callers (sub_8236A660, sub_8236ACA0, sub_8236AFC8, sub_82C6AF80, sub_82C6E898, and sub_8236A170).
[entrypoint.functions."0x8236AC48"]
name = "ProcessAndProcessAndProcess240_8236AC48"

# A processor that processes the data (two hundred and forty-first form). It takes a value (a) and a value (b). It processes the data (a and b). This is the "process this data" routine (two hundred and forty-first form), part of the 0x8236D process cluster. 110 lines. 4 callees (Construct104ByteObject, ConstructLinkedNode, RemoveAndDestructObject, and sub_8236DC20). 2 callers (sub_82372758 and sub_822F4D10).
[entrypoint.functions."0x8236DB60"]
name = "ProcessAndProcessAndProcess241_8236DB60"

# A processor that processes the data (two hundred and forty-second form). It takes a structure (a: fields at a->108 and a->140) and a value (b). It processes the data (a and b). This is the "process this data" routine (two hundred and forty-second form), part of the 0x8236D process cluster. 173 lines. 7 callees (CriticalSection_EnterOrTry, sub_8236E9F0, sub_8236F800, sub_823715E8, sub_82371820, sub_82371958, and sub_82371AA0). 1 caller (ProcessAndProcessAndProcess241).
[entrypoint.functions."0x8236DC20"]
name = "ProcessAndProcessAndProcess242_8236DC20"

# A processor that processes the data (two hundred and forty-third form). It takes a structure (a: field at a->140) and a structure (b: fields at b->64 and b->68). It processes the data (a and b). This is the "process this data" routine (two hundred and forty-third form), part of the 0x8236D process cluster. 311 lines. 9 callees (ChainSearch5, ClampAndUpdate, CriticalSection_EnterOrTry, FindChainRoot3, FindListTail, FindTreeNode, GetOrCreateByNameHash, ProcessCompareDispatch, and sub_8236DFC0). 2 callers (sub_82372758 and sub_822F4D10).
[entrypoint.functions."0x8236DD48"]
name = "ProcessAndProcessAndProcess243_8236DD48"

# A processor that processes the data (two hundred and forty-fourth form). It takes a value (a) and a structure (b: fields at b->20, b->24, and b->28). It processes the data (a and b). This is the "process this data" routine (two hundred and forty-fourth form), part of the 0x8236D process cluster. 285 lines. 9 callees (Allocate_SizeBucketed_Thunk, BinarySearch_12Stride, Construct24Byte, CopyStructWithPayload, Free_SizeBucketed_Thunk, InsertElement4, Tree_Insert, sub_8246E8B0, and sub_82CBBB58). 1 caller (ProcessAndProcessAndProcess243).
[entrypoint.functions."0x8236DFC0"]
name = "ProcessAndProcessAndProcess244_8236DFC0"

# A processor that processes the data (two hundred and forty-fifth form). It takes a structure (a: fields at a->4 and a->8). It processes the data (a). This is the "process this data" routine (two hundred and forty-fifth form), part of the 0x8236F process cluster. 58 lines. 1 callee (sub_82370E60). 1 caller (sub_82371820).
[entrypoint.functions."0x8236FEC0"]
name = "ProcessAndProcessAndProcess245_8236FEC0"

# A processor that processes the data (two hundred and forty-sixth form). It takes a structure (a: field at a->4) and a value (b). It processes the data (a and b). This is the "process this data" routine (two hundred and forty-sixth form), part of the 0x82371 process cluster. 327 lines. 11 callees (Allocate_SizeBucketed_Thunk, Construct24Byte, ConstructAllocObject, Free_SizeBucketed_Thunk, SubObject_VcallSlot3_GatedStore32, Tree_Insert, VcallSlot3_Flag4, sub_82371BF0, sub_82372108, sub_8246C798, and sub_82CBBB58). 1 caller (ProcessAndProcessAndProcess242).
[entrypoint.functions."0x823715E8"]
name = "ProcessAndProcessAndProcess246_823715E8"

# A processor that processes the data (two hundred and forty-seventh form). It takes a structure (a: field at a->4) and a structure (b: field at b->4). It processes the data (a and b). This is the "process this data" routine (two hundred and forty-seventh form), part of the 0x82371 process cluster. 178 lines. 7 callees (EvaluateSource, FNV1Hash, Free_SizeBucketed_Thunk, GetOrCreateByNameHash, SubObject_VcallSlot3_GatedStore32, WriteRange_AtPos, and ProcessAndProcessAndProcess245). 1 caller (ProcessAndProcessAndProcess242).
[entrypoint.functions."0x82371820"]
name = "ProcessAndProcessAndProcess247_82371820"

# A processor that processes the data (two hundred and forty-eighth form). It takes a structure (a: field at a->4) and a structure (b: fields at b->4 and b->8). It processes the data (a and b). This is the "process this data" routine (two hundred and forty-eighth form), part of the 0x82371 process cluster. 187 lines. 6 callees (DestroyTree_Recursive, FindChainRoot3, Free_SizeBucketed_Thunk, ProcessChainLookup, ProcessDispatchAndVcall, and SubObject_VcallSlot3_GatedStore32). 1 caller (ProcessAndProcessAndProcess242).
[entrypoint.functions."0x82371958"]
name = "ProcessAndProcessAndProcess248_82371958"

# A processor that processes the data (two hundred and forty-ninth form). It takes a structure (a: field at a->4) and a structure (b: fields at b->4 and b->8). It processes the data (a and b). This is the "process this data" routine (two hundred and forty-ninth form), part of the 0x82371 process cluster. 191 lines. 6 callees (DestructNested, FindListTail, Free_SizeBucketed_Thunk, ProcessRemoveElement, SubObject_VcallSlot3_GatedStore32, and ValidateSizeOrRaise). 1 caller (ProcessAndProcessAndProcess242).
[entrypoint.functions."0x82371AA0"]
name = "ProcessAndProcessAndProcess249_82371AA0"

# A processor that processes the data (two hundred and fiftieth form). It takes a structure (a: fields at a->20, a->24, and a->28). It processes the data (a). This is the "process this data" routine (two hundred and fiftieth form), part of the 0x82371 process cluster. 142 lines. 4 callees (CheckedCopy, ConstructAllocObject, Free_SizeBucketed_Thunk, and sub_82371EE8). 1 caller (ProcessAndProcessAndProcess246).
[entrypoint.functions."0x82371BF0"]
name = "ProcessAndProcessAndProcess250_82371BF0"

# A processor that processes the data (two hundred and fifty-first form). It takes a structure (a: field at a->4) and a value (b). It processes the data (a and b). This is the "process this data" routine (two hundred and fifty-first form), part of the 0x82372 process cluster. 130 lines. 4 callees (BinarySearch_12Stride, SubObject_VcallSlot3_GatedStore32, VcallSlot3_Flag4, and sub_82371EE8). 1 caller (ProcessAndProcessAndProcess246).
[entrypoint.functions."0x82372108"]
name = "ProcessAndProcessAndProcess251_82372108"

# A processor that processes the data (two hundred and fifty-second form). It takes a value (a). It processes the data (a). This is the "process this data" routine (two hundred and fifty-second form), part of the 0x82372 process cluster. 59 lines. 2 callees (ProcessAndInitializeAndProcess2 and sub_8236A530). 2 callers (sub_82374240 and ProcessAndProcessAndProcess211).
[entrypoint.functions."0x823726F8"]
name = "ProcessAndProcessAndProcess252_823726F8"

# A processor that processes the data (two hundred and fifty-third form). It takes a value (a) and a value (b). It processes the data (a and b). This is the "process this data" routine (two hundred and fifty-third form), part of the 0x82372 process cluster. 229 lines. 9 callees (ConstructStringElement, Free_SizeBucketed_Thunk, ProcessAndComputeAndProcess12, ProcessAndInitializeAndProcess, ProcessElementAccess3, ProcessAndProcessAndProcess215, sub_8236D4E0, ProcessAndProcessAndProcess241, and ProcessAndProcessAndProcess243). 0 callers (unresolved — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x82372758"]
name = "ProcessAndProcessAndProcess253_82372758"

# A processor that processes the data (two hundred and fifty-fourth form). It takes nothing. It processes the data. This is the "process this data" routine (two hundred and fifty-fourth form), part of the 0x82372 process cluster. 545 lines. 3 callees (ConstructString, Free_SizeBucketed_Thunk, and ProcessAndConstructAndProcess3). 2 callers (ProcessAndProcessAndProcess214 and sub_823746F0).
[entrypoint.functions."0x82372E88"]
name = "ProcessAndProcessAndProcess254_82372E88"

# A processor that processes the data (two hundred and fifty-fifth form). It takes a value (a). It processes the data (a). This is the "process this data" routine (two hundred and fifty-fifth form), part of the 0x82373 process cluster. 841 lines. 17 callees (Allocate_OrDbgBreak, ConstructRefCounted, Free_SizeBucketed_Thunk, MemSet_SdkRuntime, ProcessAndAbort, ProcessAndConstructWithFlag2, ProcessAndConstructWithFlag3, ProcessAndParseValue, ProcessAndValidateAndConstruct, Release_RefCounted, ToUpper_SdkRuntime, sub_82373D60, sub_823746F0, sub_82BECB98, sub_82BECC38, sub_82BEF360, and sub_82BEFAA0). 3 callers (sub_82373D60, sub_82373E08, and sub_8229A9D8).
[entrypoint.functions."0x823737D0"]
name = "ProcessAndProcessAndProcess255_823737D0"

# A processor that processes the data (two hundred and fifty-sixth form). It takes nothing. It processes the data. This is the "process this data" routine (two hundred and fifty-sixth form), part of the 0x82373 process cluster. 131 lines. 10 callees (ProcessAndProcessAndProcess255, sub_82374808, sub_82374C00, sub_82374D58, sub_82BEEC70, sub_82BEEE88, sub_82BF2128, sub_82BF2140, sub_82BF28A8, and sub_82CBC5F0). 1 caller (ProcessAndProcessAndProcess214).
[entrypoint.functions."0x82373E08"]
name = "ProcessAndProcessAndProcess256_82373E08"

# A processor that processes the data (two hundred and fifty-seventh form). It takes nothing. It processes the data. This is the "process this data" routine (two hundred and fifty-seventh form), part of the 0x82374 process cluster. 461 lines. 19 callees (AssignRefCounted, ConstructStringElement, Destruct3, ProcessAndCheckSuccess, ProcessAndComputeAndProcess4, ProcessAndInitializeAndProcess2, ProcessElementAccess3, RangeUpdate, ReadStringElement, SetString, sub_82171810, sub_8236A660, sub_82374B38, sub_82374E78, sub_826C0460, sub_82BEEC70, sub_82CA3230, sub_82CBCFA8, and sub_82CBD1B0). 2 callers (sub_8229A9D8 and ProcessAndProcessAndProcess256).
[entrypoint.functions."0x82374808"]
name = "ProcessAndProcessAndProcess257_82374808"

# A processor that processes the data (two hundred and fifty-eighth form). It takes nothing. It processes the data. This is the "process this data" routine (two hundred and fifty-eighth form), part of the 0x82374 process cluster. 118 lines. 5 callees (Free_SizeBucketed_Thunk, ProcessAndInitializeAndProcess2, WriteRange_AtPos, sub_8236A170, and sub_82CA3230). 3 callers (ProcessAndProcessAndProcess257, sub_8229A9D8, and sub_822F4690).
[entrypoint.functions."0x82374B38"]
name = "ProcessAndProcessAndProcess258_82374B38"

# A processor that processes the data (two hundred and fifty-ninth form). It takes nothing. It processes the data. This is the "process this data" routine (two hundred and fifty-ninth form), part of the 0x82374 process cluster. 161 lines. 4 callees (sub_82BEF360, sub_82BF09C0, sub_82CBC150, and ProcessAndProcessAndProcess199). 1 caller (ProcessAndProcessAndProcess256).
[entrypoint.functions."0x82374D58"]
name = "ProcessAndProcessAndProcess259_82374D58"

# A processor that processes the data (two hundred and sixtieth form). It takes a value (a). It processes the data (a). This is the "process this data" routine (two hundred and sixtieth form), part of the 0x82375 process cluster. 118 lines. 2 callees (Allocate_OrDbgBreak and ConstructLinkedNode). 1 caller (ProcessAndInitializeAndProcess).
[entrypoint.functions."0x823751A8"]
name = "ProcessAndProcessAndProcess260_823751A8"

# A processor that processes the data (two hundred and sixty-first form). It takes a structure (a: field at a->0). It processes the data (a). This is the "process this data" routine (two hundred and sixty-first form), part of the 0x82378 process cluster. 236 lines. 7 callees (Allocate_OrDbgBreak, Construct16Byte, Construct16SelfRef, ProcessAndProcessAndProcess226, sub_826C6A80, sub_826CADF0, and sub_82CBC5F0). 2 callers (ProcessAndProcessAndProcess211 and sub_822F4C08).
[entrypoint.functions."0x82378CD0"]
name = "ProcessAndProcessAndProcess261_82378CD0"

# A processor that processes the data (two hundred and sixty-second form). It takes a structure (a: field at a->180) and a structure (b: field at b->80). It processes the data (a and b). This is the "process this data" routine (two hundred and sixty-second form), part of the 0x82378 process cluster. 163 lines. 4 callees (ProcessHashLookup, sub_823F89B0, sub_823FE828, and sub_823FE988). 1 caller (ProcessAndProcessAndProcess211).
[entrypoint.functions."0x82378FA0"]
name = "ProcessAndProcessAndProcess262_82378FA0"

# A processor that processes the data (two hundred and sixty-third form). It takes a value (a), a structure (b: field at b->16), and a value (c). It processes the data (a, b, and c). This is the "process this data" routine (two hundred and sixty-third form), part of the 0x823A5 process cluster. 92 lines. 4 callees (AccessElement8, ConstructRefCounted, sub_82179350, and sub_8229E9E8). 12 callers (sub_824D2428, sub_8233E578, sub_825B7BC0, sub_827F9E78, sub_82793FC0, sub_82421F98, sub_8257A928, sub_8290A808, sub_82358CB0, sub_82621FD8, sub_82982820, and sub_827F9DD8).
[entrypoint.functions."0x823A5228"]
name = "ProcessAndProcessAndProcess263_823A5228"

# A processor that processes the data (two hundred and sixty-fourth form). It takes a structure (a: fields at a->0 and a->4) and a structure (b: field at b->0). It processes the data (a and b). This is the "process this data" routine (two hundred and sixty-fourth form), part of the 0x823BE process cluster. 51 lines. 1 callee (Release_RefCounted). 12 callers (sub_82531DC0, sub_83223C50, sub_822A2BA0, sub_828126D0, sub_827C38D8, sub_821D5E90, sub_83225F18, sub_82275F00, sub_828DD0F8, sub_829D3CD8, sub_829D1FB0, and sub_821C38C8).
[entrypoint.functions."0x823BE9C0"]
name = "ProcessAndProcessAndProcess264_823BE9C0"

# A processor that processes the data (two hundred and sixty-fifth form). It takes nothing. It processes the data. This is the "process this data" routine (two hundred and sixty-fifth form), part of the 0x823FB process cluster. 793 lines. 0 callees. 1 caller (sub_8241AC20).
[entrypoint.functions."0x823FB510"]
name = "ProcessAndProcessAndProcess265_823FB510"

# A processor that processes the data (two hundred and sixty-sixth form). It takes nothing. It processes the data. This is the "process this data" routine (two hundred and sixty-sixth form), part of the 0x82415 process cluster. 307 lines. 4 callees (Allocate_OrDbgBreak, ProcessAndSetjmpAndProcess2, sub_8240D938, and sub_82A20528). 3 callers (sub_8233AE50, ProcessAndProcessAndProcess213, and sub_82357DF8).
[entrypoint.functions."0x824151E8"]
name = "ProcessAndProcessAndProcess266_824151E8"

# A processor that processes the data (two hundred and sixty-seventh form). It takes a structure (a: fields at a->8, a->20, a->32, a->40, a->52, a->54, and a->100). It processes the data (a). This is the "process this data" routine (two hundred and sixty-seventh form), part of the 0x82415 process cluster. 116 lines. 7 callees (Free_SizeBucketed_Thunk, ProcessAndSetjmpAndProcess, sub_8222B4C0, sub_82A202F8, sub_82A2C8E8, sub_83230FA0, and ProcessAndProcessAndProcess203). 3 callers (sub_8233BD40, sub_82357DF8, and ProcessAndProcessAndProcess213).
[entrypoint.functions."0x82415428"]
name = "ProcessAndProcessAndProcess267_82415428"

# A processor that processes the data (two hundred and sixty-eighth form). It takes a structure (a: fields at a->8 and a->12), a value (b), a value (c), and a value (d). It processes the data (a, b, c, and d). This is the "process this data" routine (two hundred and sixty-eighth form), part of the 0x82415 process cluster. 252 lines. 10 callees (ConstructRefCounted, DestructRefCounted, GrowAndZeroBuffer, IncrementGlobalCounter2, ProcessAndConstructAndProcess12, ProcessAndSetjmpAndProcess2, ProcessGrowBitArray, sub_82179350, sub_8229E9E8, and sub_82A1EE10). 2 callers (sub_824154F0 and ProcessAndProcessAndProcess213).
[entrypoint.functions."0x824155C8"]
name = "ProcessAndProcessAndProcess268_824155C8"

# A processor that processes the data (two hundred and sixty-ninth form). It takes a value (a). It processes the data (a). This is the "process this data" routine (two hundred and sixty-ninth form), part of the 0x82415 process cluster. 108 lines. 6 callees (Release_RefCounted, sub_82358A18, sub_82416140, sub_824EEF40, sub_824EF020, and sub_824EF300). 2 callers (ProcessAndProcessAndProcess233 and sub_825ACEA0).
[entrypoint.functions."0x82415778"]
name = "ProcessAndProcessAndProcess269_82415778"

# A processor that processes the data (two hundred and seventieth form). It takes a value (a). It processes the data (a). This is the "process this data" routine (two hundred and seventieth form), part of the 0x8241A process cluster. 214 lines. 2 callees (Allocate_OrDbgBreak and sub_82BB39F8). 1 caller (ProcessAndProcessAndProcess226).
[entrypoint.functions."0x8241AAB0"]
name = "ProcessAndProcessAndProcess270_8241AAB0"

# A processor that processes the data (two hundred and seventy-first form). It takes a value (a). It processes the data (a). This is the "process this data" routine (two hundred and seventy-first form), part of the 0x8241A process cluster. 1577 lines. 17 callees (Allocate_OrDbgBreak, Allocate_SizeBucketed_Thunk, ConstructRefCounted, Free_SizeBucketed_Thunk, Release_RefCounted, sub_822D52C0, sub_82306578, sub_82307830, sub_82308D08, sub_82335218, sub_823F9C10, sub_823FA870, ProcessAndProcessAndProcess265, sub_8241B648, sub_82741BD8, sub_8281AFB0, and sub_82CBBB58). 1 caller (ProcessAndProcessAndProcess226).
[entrypoint.functions."0x8241AC20"]
name = "ProcessAndProcessAndProcess271_8241AC20"

# A processor that processes the data (two hundred and seventy-second form). It takes a value (a). It processes the data (a). This is the "process this data" routine (two hundred and seventy-second form), part of the 0x8241B process cluster. 559 lines. 5 callees (ConstructRefCounted, DestructRefCounted, ProcessConstructString, ProcessGrowBitArray, and ProcessToLower). 1 caller (ProcessAndProcessAndProcess271).
[entrypoint.functions."0x8241B648"]
name = "ProcessAndProcessAndProcess272_8241B648"

# A processor that processes the data (two hundred and seventy-third form). It takes a structure (a: fields at a->4 and a->8). It processes the data (a). This is the "process this data" routine (two hundred and seventy-third form), part of the 0x8246C process cluster. 61 lines. 2 callees (Free_SizeBucketed_Thunk and sub_8246E8B0). 12 callers (sub_82B3D578, sub_8227CC80, sub_822A2840, sub_82509DB0, sub_82466738, ProcessAndProcessAndProcess246, sub_823FA870, sub_8322C840, sub_82350BA0, sub_8233DB90, sub_823F9C10, and sub_824A1F08).
[entrypoint.functions."0x8246C798"]
name = "ProcessAndProcessAndProcess273_8246C798"

# A processor that processes the data (two hundred and seventy-fourth form). It takes a value (a), a structure (b: field at b->0), and a value (c). It processes the data (a, b, and c). This is the "process this data" routine (two hundred and seventy-fourth form), part of the 0x824EE process cluster. 143 lines. 5 callees (ProcessAndAppendElement, ProcessAndCheckAndProcess6, ProcessAndHashString, Release_RefCounted, and sub_824EEE50). 12 callers (sub_82985AE0, sub_828778C0, sub_82683240, sub_82470C20, sub_82982060, sub_8254AC68, sub_826FB378, sub_82751BF8, sub_8295B958, sub_82967640, sub_825AF6D0, and sub_82425680).
[entrypoint.functions."0x824EEF40"]
name = "ProcessAndProcessAndProcess274_824EEF40"

# A processor that processes the data (two hundred and seventy-fifth form). It takes a structure (a: field at a->8) and a value (b). It processes the data (a and b). This is the "process this data" routine (two hundred and seventy-fifth form), part of the 0x825BB process cluster. 81 lines. 5 callees (SetString, sub_82171810, sub_822F1F00, sub_826C3EF0, and sub_826C3FA8). 11 callers (sub_823420C0, sub_82B78208, ProcessAndProcessAndProcess227, sub_82B44D10, sub_825BBEE8, sub_8266CC98, sub_825BB9D0, sub_82392118, sub_82B3DE68, sub_828E7008, and sub_82A18998).
[entrypoint.functions."0x825BBA48"]
name = "ProcessAndProcessAndProcess275_825BBA48"

# A processor that processes the data (two hundred and seventy-sixth form). It takes a structure (a: field at a->8). It processes the data (a). This is the "process this data" routine (two hundred and seventy-sixth form), part of the 0x8260C process cluster. 1143 lines. 17 callees (ProcessAndAppendAndResolve3, ProcessAndAppendElement, ProcessAndCopyReferenceAndHash, ProcessAndDispatchAndResolve, ProcessAndHashString, ProcessAndResolveTypedField, Release_RefCounted, sub_822B9510, sub_824EF020, sub_824EF300, sub_824EF558, sub_8260CF40, sub_8260D020, sub_8260D0B8, sub_8260D150, sub_82903C38, and sub_82A1F5B0). 2 callers (sub_825ACEA0 and ProcessAndProcessAndProcess233).
[entrypoint.functions."0x8260C3B8"]
name = "ProcessAndProcessAndProcess276_8260C3B8"

# A processor that processes the data (two hundred and seventy-seventh form). It takes a structure (a: fields at a->0, a->60, a->64, a->72, a->76, a->80, and a->88). It processes the data (a). This is the "process this data" routine (two hundred and seventy-seventh form), part of the 0x826C6 process cluster. 139 lines. 2 callees (sub_826CADF0 and sub_82CBC5F0). 1 caller (ProcessAndProcessAndProcess261).
[entrypoint.functions."0x826C6A80"]
name = "ProcessAndProcessAndProcess277_826C6A80"

# A processor that processes the data (two hundred and seventy-eighth form). It takes a value (a). It processes the data (a). This is the "process this data" routine (two hundred and seventy-eighth form), part of the 0x8281A process cluster. 1687 lines. 5 callees (ConstructLinkedNode, ConstructRefCounted, CopyAssign_RefCounted, DestructRefCounted, and ProcessAndSearchAndProcess). 1 caller (ProcessAndProcessAndProcess271).
[entrypoint.functions."0x8281AFB0"]
name = "ProcessAndProcessAndProcess278_8281AFB0"

# A processor that processes the data (two hundred and seventy-ninth form). It takes a value (a). It processes the data (a). This is the "process this data" routine (two hundred and seventy-ninth form), part of the 0x829F8 process cluster. 197 lines. 5 callees (Allocate_OrDbgBreak, Construct12Byte, CriticalSection_EnterOrTry, ProcessAndVcallAndProcess10, and sub_82BFCDD0). 4 callers (ProcessAndProcessAndProcess226, sub_829F7C40, sub_825AB3D0, and sub_821D9258).
[entrypoint.functions."0x829F8250"]
name = "ProcessAndProcessAndProcess279_829F8250"

# A processor that processes the data (two hundred and eightieth form). It takes a structure (a: fields at a->0 and a->4) and a value (b). It processes the data (a and b). This is the "process this data" routine (two hundred and eightieth form), part of the 0x829FA process cluster. 131 lines. 3 callees (Allocate_OrDbgBreak, Free_SizeBucketed_Thunk, and MemSet_SdkRuntime). 3 callers (sub_829FC858, HashTable_InsertGrow, and sub_829FC9A8).
[entrypoint.functions."0x829FAB50"]
name = "ProcessAndProcessAndProcess280_829FAB50"

# A processor that processes the data (two hundred and eighty-first form). It takes nothing. It processes the data. This is the "process this data" routine (two hundred and eighty-first form), part of the 0x829FA process cluster. 131 lines. 3 callees (Allocate_OrDbgBreak, MemSet_SdkRuntime, and sub_829FC9A8). 3 callers (ProcessAndProcessAndProcess211, sub_8233AE50, and sub_82354BB8).
[entrypoint.functions."0x829FAF40"]
name = "ProcessAndProcessAndProcess281_829FAF40"

# A processor that processes the data (two hundred and eighty-second form). It takes a structure (a: field at a->0). It processes the data (a). This is the "process this data" routine (two hundred and eighty-second form), part of the 0x829FC process cluster. 73 lines. 2 callees (Allocate_OrDbgBreak and ProcessAndProcessAndProcess280). 2 callers (ProcessAndProcessAndProcess281 and sub_829FB280).
[entrypoint.functions."0x829FC9A8"]
name = "ProcessAndProcessAndProcess282_829FC9A8"

# A processor that processes the data (two hundred and eighty-third form). It takes a structure (a: fields at a->4 and a->8). It processes the data (a). This is the "process this data" routine (two hundred and eighty-third form), part of the 0x82A0B process cluster. 67 lines. 1 callee (Free_SizeBucketed_Thunk). 11 callers (sub_83216518, sub_82A10950, sub_8323E5D8, sub_83217058, sub_8323C5C0, sub_8323BAC8, sub_8323F120, sub_8321F618, sub_8323A908, sub_8323B040, and sub_8323D6D0).
[entrypoint.functions."0x82A0B9D0"]
name = "ProcessAndProcessAndProcess283_82A0B9D0"

# A processor that processes the data (two hundred and eighty-fourth form). It takes a structure (a: fields at a->4 and a->8). It processes the data (a). This is the "process this data" routine (two hundred and eighty-fourth form), part of the 0x82A0B process cluster. 67 lines. 1 callee (Free_SizeBucketed_Thunk). 11 callers (sub_8323B040, sub_8323D6D0, sub_82A10950, sub_83216518, sub_8323F120, sub_8323BAC8, sub_8323A908, sub_83217058, sub_8323C5C0, sub_8323E5D8, and sub_8321F618).
[entrypoint.functions."0x82A0BA38"]
name = "ProcessAndProcessAndProcess284_82A0BA38"

# A processor that processes the data (two hundred and eighty-fifth form). It takes a structure (a: fields at a->0, a->96, and a->140) and a value (b). It processes the data (a and b). This is the "process this data" routine (two hundred and eighty-fifth form), part of the 0x82A10 process cluster. 69 lines. 3 callees (Free_SizeBucketed_Thunk, ProcessAndProcessAndProcess283, and ProcessAndProcessAndProcess284). 0 callers (unresolved — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x82A10950"]
name = "ProcessAndProcessAndProcess285_82A10950"

# A processor that processes the data (two hundred and eighty-sixth form). It takes a structure (a: fields at a->0, a->4, a->8, a->12, a->16, a->20, a->24, a->28, a->32, and a->36) and a value (b). It processes the data (a and b). This is the "process this data" routine (two hundred and eighty-sixth form), part of the 0x82A1C process cluster. 101 lines. 2 callees (Allocate_OrDbgBreak and UpdateRegion). 5 callers (sub_82B7CD18, sub_82A4BE40, ProcessAndProcessAndProcess211, sub_824EB4F8, and sub_82AA9548).
[entrypoint.functions."0x82A1CDD8"]
name = "ProcessAndProcessAndProcess286_82A1CDD8"

# A processor that processes the data (two hundred and eighty-seventh form). It takes a structure (a: fields at a->28, a->32, and a->36). It processes the data (a). This is the "process this data" routine (two hundred and eighty-seventh form), part of the 0x82A1C process cluster. 97 lines. 1 callee (Free_SizeBucketed_Thunk). 6 callers (ProcessAndProcessAndProcess211, sub_82A1CE88, sub_82AA9548, sub_82B7CD18, sub_824EB4F8, and sub_82A4BE40).
[entrypoint.functions."0x82A1CEF0"]
name = "ProcessAndProcessAndProcess287_82A1CEF0"

# A processor that processes the data (two hundred and eighty-eighth form). It takes a structure (a: fields at a->8, a->20, and a->40) and a value (b). It processes the data (a and b). This is the "process this data" routine (two hundred and eighty-eighth form), part of the 0x82A1E process cluster. 203 lines. 3 callees (AccessElement8, sub_82A2C520, and sub_82A2C778). 2 callers (sub_82A298E8 and sub_82BF5F40).
[entrypoint.functions."0x82A1E778"]
name = "ProcessAndProcessAndProcess288_82A1E778"

# A processor that processes the data (two hundred and eighty-ninth form). It takes a structure (a: fields at a->0 and a->4). It processes the data (a). This is the "process this data" routine (two hundred and eighty-ninth form), part of the 0x82A1E process cluster. 12 lines. 1 callee (sub_82228C70). 0 callers (unresolved — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x82A1ED48"]
name = "ProcessAndProcessAndProcess289_82A1ED48"

# A processor that processes the data (two hundred and ninetieth form). It takes a structure (a: field at a->8), a value (b), a value (c), and a value (d). It processes the data (a, b, c, and d). This is the "process this data" routine (two hundred and ninetieth form), part of the 0x82A1E process cluster. 100 lines. 1 callee (ProcessAndSetjmpAndProcess2). 9 callers (sub_82A2ADF8, ProcessAndProcessAndProcess268, sub_82A1FF90, sub_82A1A768, sub_82A1FED0, sub_82A21B50, sub_82A1A360, sub_82A1FFC8, and sub_82A21DE0).
[entrypoint.functions."0x82A1EE10"]
name = "ProcessAndProcessAndProcess290_82A1EE10"

# A processor that processes the data (two hundred and ninety-first form). It takes a value (a) and a structure (b: field at b->16). It processes the data (a and b). This is the "process this data" routine (two hundred and ninety-first form), part of the 0x82A20 process cluster. 149 lines. 1 callee (sub_821E51D0). 2 callers (sub_82A20198 and sub_82A20410).
[entrypoint.functions."0x82A20020"]
name = "ProcessAndProcessAndProcess291_82A20020"

# A processor that processes the data (two hundred and ninety-second form). It takes a value (a) and a structure (b: fields at b->32, b->40, b->44, and b->48). It processes the data (a and b). This is the "process this data" routine (two hundred and ninety-second form), part of the 0x82A20 process cluster. 61 lines. 1 callee (ProcessVirtual). 2 callers (sub_82A202F8 and DispatchTypeTag).
[entrypoint.functions."0x82A20130"]
name = "ProcessAndProcessAndProcess292_82A20130"

# A processor that processes the data (two hundred and ninety-third form). It takes a structure (a: fields at a->16, a->72, and a->76). It processes the data (a). This is the "process this data" routine (two hundred and ninety-third form), part of the 0x82A20 process cluster. 196 lines. 4 callees (ComputeStringHash, ProcessAndVirtualAndProcess, sub_822A0F58, and ProcessAndProcessAndProcess291). 0 callers (unresolved — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x82A20198"]
name = "ProcessAndProcessAndProcess293_82A20198"

# A processor that processes the data (two hundred and ninety-fourth form). It takes a structure (a: fields at a->16 and a->32). It processes the data (a). This is the "process this data" routine (two hundred and ninety-fourth form), part of the 0x82A20 process cluster. 158 lines. 3 callees (ProcessTreeRecursive, sub_8222B4C0, and ProcessAndProcessAndProcess292). 2 callers (ProcessAndProcessAndProcess267 and sub_82A20528).
[entrypoint.functions."0x82A202F8"]
name = "ProcessAndProcessAndProcess294_82A202F8"

# A processor that processes the data (two hundred and ninety-fifth form). It takes nothing. It processes the data. This is the "process this data" routine (two hundred and ninety-fifth form), part of the 0x82A20 process cluster. 269 lines. 7 callees (HeapAllocate, ProcessAndSetjmpAndProcess, sub_82195930, sub_824149C8, sub_828457F0, ProcessAndProcessAndProcess294, and sub_82CBBB58). 1 caller (ProcessAndProcessAndProcess266).
[entrypoint.functions."0x82A20528"]
name = "ProcessAndProcessAndProcess295_82A20528"

# A processor that processes the data (two hundred and ninety-sixth form). It takes a structure (a: field at a->16). It processes the data (a). This is the "process this data" routine (two hundred and ninety-sixth form), part of the 0x82A20 process cluster. 50 lines. 1 callee (sub_82A2D660). 0 callers (unresolved — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x82A20718"]
name = "ProcessAndProcessAndProcess296_82A20718"

# A processor that processes the data (two hundred and ninety-seventh form). It takes a structure (a: fields at a->8, a->72, and a->76). It processes the data (a). This is the "process this data" routine (two hundred and ninety-seventh form), part of the 0x82A23 process cluster. 235 lines. 6 callees (ProcessAndAppendStringNode, ProcessAndCheckAndProcess6, ProcessAndHashString, ProcessAndResolveTypedField, sub_82227BA0, and sub_82A1F740). 1 caller (sub_82A23340).
[entrypoint.functions."0x82A23198"]
name = "ProcessAndProcessAndProcess297_82A23198"

# A processor that processes the data (two hundred and ninety-eighth form). It takes a value (a). It processes the data (a). This is the "process this data" routine (two hundred and ninety-eighth form), part of the 0x82A23 process cluster. 47 lines. 2 callees (sub_82A1F740 and ProcessAndProcessAndProcess297). 0 callers (unresolved — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x82A23340"]
name = "ProcessAndProcessAndProcess298_82A23340"

# A processor that processes the data (two hundred and ninety-ninth form). It takes nothing. It processes the data. This is the "process this data" routine (two hundred and ninety-ninth form), part of the 0x82A24 process cluster. 26 lines. 1 callee (sub_82A1F740). 0 callers (unresolved — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x82A24590"]
name = "ProcessAndProcessAndProcess299_82A24590"

# A processor that processes the data (three hundredth form). It takes a structure (a: field at a->8). It processes the data (a). This is the "process this data" routine (three hundredth form), part of the 0x82A27 process cluster. 122 lines. 5 callees (ProcessAndAppendStringNode, ProcessAndCheckAndProcess6, ProcessAndHashString, sub_82227BA0, and sub_82A1F740). 0 callers (unresolved — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x82A27460"]
name = "ProcessAndProcessAndProcess300_82A27460"

# A processor that processes the data (three hundred and first form). It takes a value (a). It processes the data (a). This is the "process this data" routine (three hundred and first form), part of the 0x82A29 process cluster. 375 lines. 13 callees (ComputeStringHash, GrowAndZeroBuffer, ProcessAndAccessAndConvert, ProcessAndCheckAndProcess6, ProcessAndDispatchAndProcess3, ProcessAndHashString, ProcessAndResolveTypedField, ProcessAndVirtualAndProcess, sub_82179350, ProcessAndProcessAndProcess288, sub_82A1F5B0, sub_82A1F740, and sub_82A29850). 0 callers (unresolved — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x82A298E8"]
name = "ProcessAndProcessAndProcess301_82A298E8"

# A processor that processes the data (three hundred and second form). It takes a structure (a: fields at a->36, a->44, a->48, a->72, a->73, a->74, and a->75). It processes the data (a). This is the "process this data" routine (three hundred and second form), part of the 0x82A2B process cluster. 796 lines. 0 callees. 2 callers (sub_82A2BFE0 and sub_82A36D00).
[entrypoint.functions."0x82A2BA20"]
name = "ProcessAndProcessAndProcess302_82A2BA20"

# A processor that processes the data (three hundred and third form). It takes a structure (a: field at a->16) and a value (b). It processes the data (a and b). This is the "process this data" routine (three hundred and third form), part of the 0x82A2C process cluster. 156 lines. 1 callee (HashLookupByKey). 1 caller (ProcessAndProcessAndProcess267).
[entrypoint.functions."0x82A2C8E8"]
name = "ProcessAndProcessAndProcess303_82A2C8E8"

# A processor that processes the data (three hundred and fourth form). It takes a structure (a: field at a->16). It processes the data (a). This is the "process this data" routine (three hundred and fourth form), part of the 0x82A2D process cluster. 124 lines. 1 callee (sub_821E51D0). 4 callers (sub_82A36D00, sub_82BF4D58, sub_82BF51D8, and sub_82A32F40).
[entrypoint.functions."0x82A2DDC0"]
name = "ProcessAndProcessAndProcess304_82A2DDC0"

# A processor that processes the data (three hundred and fifth form). It takes a value (a) and a structure (b: fields at b->8, b->12, b->16, b->40, b->44, and b->52). It processes the data (a and b). This is the "process this data" routine (three hundred and fifth form), part of the 0x82A2D process cluster. 124 lines. 1 callee (ProcessVirtual). 1 caller (DispatchTypeTag).
[entrypoint.functions."0x82A2DEA8"]
name = "ProcessAndProcessAndProcess305_82A2DEA8"

# A processor that processes the data (three hundred and sixth form). It takes a structure (a: field at a->16) and a structure (b: field at b->0). It processes the data (a and b). This is the "process this data" routine (three hundred and sixth form), part of the 0x82A2E process cluster. 216 lines. 5 callees (ProcessAndQueryAndAdvance, sub_82179350, sub_821E51D0, ProcessAndProcessAndProcess208, and sub_82A2E140). 0 callers (unresolved — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x82A2E630"]
name = "ProcessAndProcessAndProcess306_82A2E630"

# A processor that processes the data (three hundred and seventh form). It takes a structure (a: field at a->0) and a value (b). It processes the data (a and b). This is the "process this data" routine (three hundred and seventh form), part of the 0x82A36 process cluster. 413 lines. 8 callees (ProcessAndQueryAndDispatch, ProcessVirtual, sub_821E51D0, sub_82A2C520, sub_82A2ED28, sub_82A36560, sub_82A365D0, and sub_82A36D00). 1 caller (sub_82A36D00).
[entrypoint.functions."0x82A36648"]
name = "ProcessAndProcessAndProcess307_82A36648"

# A processor that processes the data (three hundred and eighth form). It takes a structure (a: field at a->0) and a value (b). It processes the data (a and b). This is the "process this data" routine (three hundred and eighth form), part of the 0x82A36 process cluster. 570 lines. 9 callees (ComputeStringHash, ProcessAndQueryAndDispatch, ProcessAndQueryAndProcess, ProcessVirtual, sub_821B2788, sub_821E51D0, sub_82A2C520, sub_82A2ED28, and sub_82A36560). 1 caller (sub_82A36D00).
[entrypoint.functions."0x82A36910"]
name = "ProcessAndProcessAndProcess308_82A36910"

# A processor that processes the data (three hundred and ninth form). It takes a structure (a: fields at a->0 and a->12) and a value (b). It processes the data (a and b). This is the "process this data" routine (three hundred and ninth form), part of the 0x82A36 process cluster. 324 lines. 12 callees (ProcessAndQueryAndDispatch, ProcessVirtual, sub_821E51D0, ProcessAndProcessAndProcess302, sub_82A2C520, ProcessAndProcessAndProcess304, sub_82A2E140, sub_82A2ED28, sub_82A36560, sub_82A365D0, ProcessAndProcessAndProcess307, and ProcessAndProcessAndProcess308). 2 callers (sub_82A37030 and ProcessAndProcessAndProcess307).
[entrypoint.functions."0x82A36D00"]
name = "ProcessAndProcessAndProcess309_82A36D00"

# A processor that processes the data (three hundred and tenth form). It takes a value (a). It processes the data (a). This is the "process this data" routine (three hundred and tenth form), part of the 0x82A36 process cluster. 132 lines. 3 callees (ProcessAndQueryAndProcess2, sub_821E51D0, and sub_82A2ED28). 1 caller (sub_82A37030).
[entrypoint.functions."0x82A36F40"]
name = "ProcessAndProcessAndProcess310_82A36F40"

# A processor that processes the data (three hundred and eleventh form). It takes a value (a), a value (b), and a structure (c: field at c->0). It processes the data (a, b, and c). This is the "process this data" routine (three hundred and eleventh form), part of the 0x82A37 process cluster. 91 lines. 3 callees (ComputeStringHash, ProcessAndProcessAndProcess309, and ProcessAndProcessAndProcess310). 0 callers (unresolved — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x82A37030"]
name = "ProcessAndProcessAndProcess311_82A37030"

# A processor that processes the data (three hundred and twelfth form). It takes a value (a) and a value (b). It processes the data (a and b). This is the "process this data" routine (three hundred and twelfth form), part of the 0x82A48 process cluster. 211 lines. 9 callees (Allocate_OrDbgBreak, DestructRefCounted, ProcessConstructString, ProcessToLower, sub_82A4A6F8, sub_82B80D70, sub_82B820A8, sub_82B82248, and sub_82C65310). 1 caller (ProcessAndProcessAndProcess214).
[entrypoint.functions."0x82A48B68"]
name = "ProcessAndProcessAndProcess312_82A48B68"

# A processor that processes the data (three hundred and thirteenth form). It takes a value (a). It processes the data (a). This is the "process this data" routine (three hundred and thirteenth form), part of the 0x82A49 process cluster. 145 lines. 6 callees (GetGlobalField, sub_8221EB78, sub_822A9FA8, sub_822E0508, sub_82A391D0, and sub_82B69040). 12 callers (sub_824F7CA0, sub_82A391A8, sub_825AAD70, sub_825AAD00, sub_82A38FD8, sub_82A74AA0, sub_82A38FA8, sub_8235A7E0, ProcessAndProcessAndProcess214, sub_82A74F18, sub_8235ABA0, and sub_8236C9F8).
[entrypoint.functions."0x82A492B0"]
name = "ProcessAndProcessAndProcess313_82A492B0"

# A processor that processes the data (three hundred and fourteenth form). It takes nothing. It processes the data. This is the "process this data" routine (three hundred and fourteenth form), part of the 0x82A49 process cluster. 114 lines. 2 callees (GetGlobalField and sub_821A5840). 12 callers (sub_8235B708, sub_825AB3D0, sub_8260B9E0, sub_82A3CCE8, sub_8235CF68, sub_82359868, sub_82A3CE30, sub_8235C778, sub_823AD1F0, sub_82A391A8, sub_82A38FD8, and sub_8236BF18).
[entrypoint.functions."0x82A493A8"]
name = "ProcessAndProcessAndProcess314_82A493A8"

# A processor that processes the data (three hundred and fifteenth form). It takes nothing. It processes the data. This is the "process this data" routine (three hundred and fifteenth form), part of the 0x82A4A process cluster. 92 lines. 1 callee (Free_SizeBucketed_Thunk). 2 callers (sub_832AB9C0 and ProcessAndProcessAndProcess312).
[entrypoint.functions."0x82A4A6F8"]
name = "ProcessAndProcessAndProcess315_82A4A6F8"

# A processor that processes the data (three hundred and sixteenth form). It takes a structure (a: fields at a->4, a->8, and a->12). It processes the data (a). This is the "process this data" routine (three hundred and sixteenth form), part of the 0x82A57 process cluster. 60 lines. 2 callees (Dispose17 and Free_SizeBucketed_Thunk). 9 callers (sub_82A56A50, sub_82A49D48, sub_82A425F8, sub_82B825D0, sub_82172DC8, sub_82B80C18, sub_8218EB30, sub_822BFBD8, and sub_82A42AC8).
[entrypoint.functions."0x82A57DE8"]
name = "ProcessAndProcessAndProcess316_82A57DE8"

# A processor that processes the data (three hundred and seventeenth form). It takes a structure (a: fields at a->4 and a->12) and a value (b). It processes the data (a and b). This is the "process this data" routine (three hundred and seventeenth form), part of the 0x82B29 process cluster. 140 lines. 3 callees (ConstructArray4, Free_SizeBucketed_Thunk, and sub_82A97648). 2 callers (sub_82B82248 and sub_82B281F8).
[entrypoint.functions."0x82B290A8"]
name = "ProcessAndProcessAndProcess317_82B290A8"

# A processor that processes the data (three hundred and eighteenth form). It takes multiple values. It processes the data. This is the "process this data" routine (three hundred and eighteenth form), part of the 0x82B38 process cluster. 64 lines. 1 callee (sub_82CC0D58). 12 callers (sub_82C7DCC0, sub_824E3350, sub_82B44D10, sub_82C72950, sub_82B45528, sub_82B67DB8, sub_822F33B8, sub_82A8B778, sub_822EA928, sub_82A393A8, sub_82354AE0, and sub_8236C360).
[entrypoint.functions."0x82B38798"]
name = "ProcessAndProcessAndProcess318_82B38798"

# A processor that processes the data (three hundred and nineteenth form). It takes a value (a). It processes the data (a). This is the "process this data" routine (three hundred and nineteenth form), part of the 0x82B39 process cluster. 119 lines. 3 callees (Allocate_OrDbgBreak, MemSet_SdkRuntime, and ProcessAndAllocateAligned). 1 caller (sub_82B39978).
[entrypoint.functions."0x82B395D8"]
name = "ProcessAndProcessAndProcess319_82B395D8"

# A processor that processes the data (three hundred and twentieth form). It takes a value (a) and a structure (b: fields at b->0 and b->4). It processes the data (a and b). This is the "process this data" routine (three hundred and twentieth form), part of the 0x82B39 process cluster. 117 lines. 1 callee (ProcessAndProcessAndProcess319). 12 callers (sub_82436940, sub_824EFB50, sub_82809250, sub_82355C08, sub_8245E3F8, sub_8218FC98, sub_82339090, sub_8239AA00, sub_82842860, sub_827EA850, sub_8252AF38, and sub_82697790).
[entrypoint.functions."0x82B39978"]
name = "ProcessAndProcessAndProcess320_82B39978"

# A processor that processes the data (three hundred and twenty-first form). It takes a value (a). It processes the data (a). This is the "process this data" routine (three hundred and twenty-first form), part of the 0x82B80 process cluster. 68 lines. 2 callees (Allocate_OrDbgBreak and sub_822F1F00). 5 callers (sub_82B800A0, sub_82B63FC0, sub_82B7FAF8, sub_82B82D50, and sub_82B82638).
[entrypoint.functions."0x82B80118"]
name = "ProcessAndProcessAndProcess321_82B80118"

# A processor that processes the data (three hundred and twenty-second form). It takes a value (a) and a structure (b: field at b->0). It processes the data (a and b). This is the "process this data" routine (three hundred and twenty-second form), part of the 0x82B80 process cluster. 2806 lines — the largest function in this batch. 24 callees (Allocate_SizeBucketed_Thunk, AtomicIncrement4, CopyAssign_RefCounted2, CopyAssign_RefCounted, DestructRefCounted, Free_SizeBucketed_Thunk, ProcessAndCopyStructRegion, ProcessAndRecurseAndProcess2, ProcessRefCountedBuffer, ProcessVcallConstruct, RegisterWithCounter, Release_RefCounted, sub_822D52C0, sub_82344AC8, sub_829EA980, sub_82A1B8C8, sub_82B3A158, sub_82B82638, sub_82B82860, sub_82B828C8, sub_82B82D50, sub_82B82FE0, sub_82C647D0, and sub_82CBBB58). 1 caller (ProcessAndProcessAndProcess312).
[entrypoint.functions."0x82B80D70"]
name = "ProcessAndProcessAndProcess322_82B80D70"

# A processor that processes the data (three hundred and twenty-third form). It takes a value (a) and a structure (b: field at b->0). It processes the data (a and b). This is the "process this data" routine (three hundred and twenty-third form), part of the 0x82B82 process cluster. 234 lines. 4 callees (Allocate_OrDbgBreak, VirtualCall4, sub_82C64708, and sub_82CBBB60). 1 caller (ProcessAndProcessAndProcess312).
[entrypoint.functions."0x82B820A8"]
name = "ProcessAndProcessAndProcess323_82B820A8"

# A processor that processes the data (three hundred and twenty-fourth form). It takes a structure (a: fields at a->4, a->8, a->16, a->36, and a->40). It processes the data (a). This is the "process this data" routine (three hundred and twenty-fourth form), part of the 0x82B82 process cluster. 306 lines. 9 callees (Allocate_OrDbgBreak, Construct32Byte, ProcessCriticalSection, QueuePush, VirtualCall4, ProcessAndProcessAndProcess317, sub_82B5D5F0, sub_82B82558, and sub_82B825D0). 1 caller (ProcessAndProcessAndProcess312).
[entrypoint.functions."0x82B82248"]
name = "ProcessAndProcessAndProcess324_82B82248"

# A processor that processes the data (three hundred and twenty-fifth form). It takes a structure (a: fields at a->4, a->8, and a->12). It processes the data (a). This is the "process this data" routine (three hundred and twenty-fifth form), part of the 0x82B82 process cluster. 71 lines. 3 callees (Free_SizeBucketed_Thunk, sub_82B82778, and sub_82B82860). 2 callers (sub_82B80C18 and ProcessAndProcessAndProcess324).
[entrypoint.functions."0x82B82558"]
name = "ProcessAndProcessAndProcess325_82B82558"

# A processor that processes the data (three hundred and twenty-sixth form). It takes a structure (a: fields at a->4, a->8, a->12, and a->17). It processes the data (a). This is the "process this data" routine (three hundred and twenty-sixth form), part of the 0x82B82 process cluster. 61 lines. 2 callees (ProcessAndProcessAndProcess316 and sub_82B827D8). 1 caller (ProcessAndProcessAndProcess324).
[entrypoint.functions."0x82B825D0"]
name = "ProcessAndProcessAndProcess326_82B825D0"

# A processor that processes the data (three hundred and twenty-seventh form). It takes a structure (a: fields at a->4 and a->12) and a value (b). It processes the data (a and b). This is the "process this data" routine (three hundred and twenty-seventh form), part of the 0x82B82 process cluster. 195 lines. 4 callees (Dispose17, Free_SizeBucketed_Thunk, sub_82A97648, and ProcessAndProcessAndProcess321). 1 caller (ProcessAndProcessAndProcess322).
[entrypoint.functions."0x82B82638"]
name = "ProcessAndProcessAndProcess327_82B82638"

# A processor that processes the data (three hundred and twenty-eighth form). It takes a structure (a: fields at a->4 and a->8). It processes the data (a). This is the "process this data" routine (three hundred and twenty-eighth form), part of the 0x82B82 process cluster. 58 lines. 2 callees (sub_82B82860 and sub_82B82FE0). 1 caller (ProcessAndProcessAndProcess325).
[entrypoint.functions."0x82B82778"]
name = "ProcessAndProcessAndProcess328_82B82778"

# A processor that processes the data (three hundred and twenty-ninth form). It takes a structure (a: fields at a->4 and a->8). It processes the data (a). This is the "process this data" routine (three hundred and twenty-ninth form), part of the 0x82B82 process cluster. 85 lines. 2 callees (Dispose17 and sub_82B83318). 2 callers (ProcessAndProcessAndProcess326 and sub_82B80C18).
[entrypoint.functions."0x82B827D8"]
name = "ProcessAndProcessAndProcess329_82B827D8"

# A processor that processes the data (three hundred and thirtieth form). It takes a structure (a: fields at a->4, a->8, and a->12), a value (b), a value (c), and a value (d). It processes the data (a, b, c, and d). This is the "process this data" routine (three hundred and thirtieth form), part of the 0x82B82 process cluster. 476 lines. 8 callees (Copy20Bytes, Free_SizeBucketed_Thunk, sub_821F01B8, sub_829EA980, sub_82A97648, sub_82B82860, sub_82B82CD0, and sub_82B83078). 1 caller (ProcessAndProcessAndProcess322).
[entrypoint.functions."0x82B828C8"]
name = "ProcessAndProcessAndProcess330_82B828C8"

# A processor that processes the data (three hundred and thirty-first form). It takes a value (a). It processes the data (a). This is the "process this data" routine (three hundred and thirty-first form), part of the 0x82B82 process cluster. 72 lines. 2 callees (Allocate_OrDbgBreak and sub_822F1F00). 1 caller (ProcessAndProcessAndProcess330).
[entrypoint.functions."0x82B82CD0"]
name = "ProcessAndProcessAndProcess331_82B82CD0"

# A processor that processes the data (three hundred and thirty-second form). It takes multiple values. It processes the data. This is the "process this data" routine (three hundred and thirty-second form), part of the 0x82B82 process cluster. 90 lines. 1 callee (sub_821F01B8). 2 callers (ProcessAndProcessAndProcess322 and ProcessAndProcessAndProcess328).
[entrypoint.functions."0x82B82FE0"]
name = "ProcessAndProcessAndProcess332_82B82FE0"

# A processor that processes the data (three hundred and thirty-third form). It takes nothing. It processes the data. This is the "process this data" routine (three hundred and thirty-third form), part of the 0x82BA8 process cluster. 181 lines. 4 callees (InitializeAndProcess, MemSet_SdkRuntime, ProcessLoopVcall, and sub_82BAA948). 1 caller (GetSingleton).
[entrypoint.functions."0x82BA8BB0"]
name = "ProcessAndProcessAndProcess333_82BA8BB0"

# A processor that processes the data (three hundred and thirty-fourth form). It takes multiple values. It processes the data. This is the "process this data" routine (three hundred and thirty-fourth form), part of the 0x82BA9 process cluster. 86 lines. 1 callee (sub_82BAA1D0). 0 callers (unresolved — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x82BA9F00"]
name = "ProcessAndProcessAndProcess334_82BA9F00"

# A processor that processes the data (three hundred and thirty-fifth form). It takes a structure (a: fields at a->32, a->36, a->40, a->44, a->48, and a->52). It processes the data (a). This is the "process this data" routine (three hundred and thirty-fifth form), part of the 0x82BAA process cluster. 55 lines. 0 callees (self-contained). 3 callers (sub_82BA9E18, ProcessAndProcessAndProcess334, and sub_82BA9F98).
[entrypoint.functions."0x82BAA1D0"]
name = "ProcessAndProcessAndProcess335_82BAA1D0"

# A processor that processes the data (three hundred and thirty-sixth form). It takes nothing. It processes the data. This is the "process this data" routine (three hundred and thirty-sixth form), part of the 0x82BAA process cluster. 68 lines. 0 callees (self-contained). 1 caller (ProcessAndProcessAndProcess333).
[entrypoint.functions."0x82BAA948"]
name = "ProcessAndProcessAndProcess336_82BAA948"

# A processor that processes the data (three hundred and thirty-seventh form). It takes a structure (a: fields at a->0, a->40, a->520, a->524, and a->528), a value (b), and a value (c). It processes the data (a, b, and c). This is the "process this data" routine (three hundred and thirty-seventh form), part of the 0x82BB3 process cluster. 223 lines. 8 callees (GetSingleton, ProcessAndClearIndexedRegion, ProcessIndexedRegion2, Serialize, sub_82BBB1C8, sub_82BBDE60, sub_82BC0E68, and sub_82BE7460). 1 caller (ProcessAndProcessAndProcess270).
[entrypoint.functions."0x82BB39F8"]
name = "ProcessAndProcessAndProcess337_82BB39F8"

# A processor that processes the data (three hundred and thirty-eighth form). It takes a structure (a: fields at a->8 and a->12). It processes the data (a). This is the "process this data" routine (three hundred and thirty-eighth form), part of the 0x82BB8 process cluster. 311 lines. 9 callees (GetTypedField, HashCstring, ProcessAndStoreElement, ProcessLoopDispatch, sub_82BB1ED0, sub_82BB9F40, sub_82BC5C08, sub_82BC68F0, and sub_82BCBDC8). 0 callers (unresolved — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x82BB8790"]
name = "ProcessAndProcessAndProcess338_82BB8790"

# A processor that processes the data (three hundred and thirty-ninth form). It takes a structure (a: fields at a->0 and a->4). It processes the data (a). This is the "process this data" routine (three hundred and thirty-ninth form), part of the 0x82BBD process cluster. 130 lines. 5 callees (GetSingleton, MemSet_SdkRuntime, ProcessAndClearIndexedRegion, ProcessIndexedRegion2, and Serialize). 0 callers (unresolved — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x82BBDD10"]
name = "ProcessAndProcessAndProcess339_82BBDD10"

# A processor that processes the data (three hundred and fortieth form). It takes a structure (a: fields at a->0, a->4, a->8, a->40, a->44, a->48, and a->52), a value (b), and a value (c). It processes the data (a, b, and c). This is the "process this data" routine (three hundred and fortieth form), part of the 0x82BBD process cluster. 128 lines. 3 callees (ProcessAndClearIndexedRegion, Serialize, and sub_82BAE2E8). 1 caller (ProcessAndProcessAndProcess337).
[entrypoint.functions."0x82BBDE60"]
name = "ProcessAndProcessAndProcess340_82BBDE60"

# A processor that processes the data (three hundred and forty-first form). It takes a structure (a: fields at a->0, a->4, a->8, a->10, a->12, a->16, a->92, and a->140). It processes the data (a). This is the "process this data" routine (three hundred and forty-first form), part of the 0x82BC0 process cluster. 129 lines. 5 callees (GetSingleton, MemSet_SdkRuntime, ProcessAndGrowAndSerialize, ProcessIndexedRegion2, and sub_82BC3018). 1 caller (ProcessAndProcessAndProcess337).
[entrypoint.functions."0x82BC0E68"]
name = "ProcessAndProcessAndProcess341_82BC0E68"

# A processor that processes the data (three hundred and forty-second form). It takes a structure (a: field at a->16) and multiple values. It processes the data (a and the values). This is the "process this data" routine (three hundred and forty-second form), part of the 0x82BC5 process cluster. 74 lines. 2 callees (ProcessLoopDispatch and sub_82BC8490). 12 callers (sub_82BCFD70, sub_82BD8AC0, sub_82BD9640, ProcessAndDispatchAndProcess18, sub_82BD7AA8, sub_82BDA2C8, sub_82BB5B60, sub_82BDAC88, sub_82BB6768, sub_82BB9600, sub_82BC6980, and sub_82BDAE28).
[entrypoint.functions."0x82BC5C08"]
name = "ProcessAndProcessAndProcess342_82BC5C08"

# A processor that processes the data (three hundred and forty-third form). It takes a structure (a: fields at a->32, a->36, a->40, and a->44). It processes the data (a). This is the "process this data" routine (three hundred and forty-third form), part of the 0x82BE7 process cluster. 121 lines. 3 callees (MemSet_SdkRuntime, ProcessAndClearIndexedRegion, and Serialize). 1 caller (ProcessAndProcessAndProcess337).
[entrypoint.functions."0x82BE7460"]
name = "ProcessAndProcessAndProcess343_82BE7460"

# A processor that processes the data (three hundred and forty-fourth form). It takes a value (a). It processes the data (a). This is the "process this data" routine (three hundred and forty-fourth form), part of the 0x82BEC process cluster. 150 lines. 2 callees (ValidateBounds4 and sub_82BF2B68). 1 caller (sub_82BECE80).
[entrypoint.functions."0x82BEC540"]
name = "ProcessAndProcessAndProcess344_82BEC540"

# A processor that processes the data (three hundred and forty-fifth form). It takes a value (a). It processes the data (a). This is the "process this data" routine (three hundred and forty-fifth form), part of the 0x82BEC process cluster. 97 lines. 2 callees (ProcessAndValidateBounds and SetGlobalField_Thunk). 1 caller (ProcessAndProcessAndProcess255).
[entrypoint.functions."0x82BECB98"]
name = "ProcessAndProcessAndProcess345_82BECB98"

# A processor that processes the data (three hundred and forty-sixth form). It takes a value (a). It processes the data (a). This is the "process this data" routine (three hundred and forty-sixth form), part of the 0x82BEC process cluster. 63 lines. 1 callee (ProcessAndProcessAndProcess344). 1 caller (sub_82BEEC70).
[entrypoint.functions."0x82BECE80"]
name = "ProcessAndProcessAndProcess346_82BECE80"

# A processor that processes the data (three hundred and forty-seventh form). It takes a value (a). It processes the data (a). This is the "process this data" routine (three hundred and forty-seventh form), part of the 0x82BEE process cluster. 313 lines. 10 callees (ProcessAndAbort, ProcessAndWriteToBuffer, SetGlobalField_Thunk, sub_82BEBA50, ProcessAndProcessAndProcess346, sub_82BF0290, sub_82BF2B68, sub_82BF2DB0, sub_82BF2E70, and sub_82BF2FB0). 2 callers (ProcessAndProcessAndProcess256 and ProcessAndProcessAndProcess257).
[entrypoint.functions."0x82BEEC70"]
name = "ProcessAndProcessAndProcess347_82BEEC70"

# A processor that processes the data (three hundred and forty-eighth form). It takes a structure (a: fields at a->4, a->8, and a->12) and a value (b). It processes the data (a and b). This is the "process this data" routine (three hundred and forty-eighth form), part of the 0x82BEF process cluster. 112 lines. 3 callees (ValidateBounds4, sub_82BEF110, and sub_82BEFC10). 2 callers (sub_82BEF8E8 and sub_82BEF360).
[entrypoint.functions."0x82BEF2A8"]
name = "ProcessAndProcessAndProcess348_82BEF2A8"

# A processor that processes the data (three hundred and forty-ninth form). It takes a value (a). It processes the data (a). This is the "process this data" routine (three hundred and forty-ninth form), part of the 0x82BEF process cluster. 797 lines. 14 callees (ProcessAndComputeAndProcess2, ProcessAndValidateList, SetGlobalField_Thunk, ValidateBounds4, sub_82BECEE8, sub_82BEE0C8, sub_82BEE970, sub_82BEF170, ProcessAndProcessAndProcess348, sub_82BF0610, sub_82BF07C0, sub_82BF16E0, sub_82BF2B68, and sub_82BF2C28). 2 callers (ProcessAndProcessAndProcess259 and ProcessAndProcessAndProcess255).
[entrypoint.functions."0x82BEF360"]
name = "ProcessAndProcessAndProcess349_82BEF360"

# A processor that processes the data (three hundred and fiftieth form). It takes a value (a). It processes the data (a). This is the "process this data" routine (three hundred and fiftieth form), part of the 0x82BEF process cluster. 137 lines. 3 callees (Buffer_CopyOrZero_Error, SetGlobalField_Thunk, and sub_82BF2B68). 1 caller (ProcessAndProcessAndProcess255).
[entrypoint.functions."0x82BEFAA0"]
name = "ProcessAndProcessAndProcess350_82BEFAA0"

# A processor that processes the data (three hundred and fifty-first form). It takes a value (a), a structure (b: field at b->8), a value (c), and a value (d). It processes the data (a, b, c, and d). This is the "process this data" routine (three hundred and fifty-first form), part of the 0x82BEF process cluster. 80 lines. 1 callee (CheckedCopy). 1 caller (sub_82BEFD50).
[entrypoint.functions."0x82BEFB90"]
name = "ProcessAndProcessAndProcess351_82BEFB90"

# A processor that processes the data (three hundred and fifty-second form). It takes a structure (a: fields at a->4 and a->8). It processes the data (a). This is the "process this data" routine (three hundred and fifty-second form), part of the 0x82BEF process cluster. 58 lines. 1 callee (ProcessAndProcessAndProcess351). 1 caller (sub_82BF07C0).
[entrypoint.functions."0x82BEFD50"]
name = "ProcessAndProcessAndProcess352_82BEFD50"

# A processor that processes the data (three hundred and fifty-third form). It takes a structure (a: fields at a->4, a->8, and a->12) and a value (b). It processes the data (a and b). This is the "process this data" routine (three hundred and fifty-third form), part of the 0x82BEF process cluster. 121 lines. 4 callees (Free_Thunk, ProcessAndCopyDataAndSetCount, ValidateBounds4, and sub_82BEFC10). 2 callers (sub_82BF0610 and sub_82BEFE78).
[entrypoint.functions."0x82BEFDB0"]
name = "ProcessAndProcessAndProcess353_82BEFDB0"

# A processor that processes the data (three hundred and fifty-fourth form). It takes a value (a). It processes the data (a). This is the "process this data" routine (three hundred and fifty-fourth form), part of the 0x82BF0 process cluster. 162 lines. 7 callees (Allocate_Thunk, ProcessAndConstructObject, ProcessAndCopyAndGrow, ProcessAndWriteToBuffer, ValidateBounds16, sub_82BEFC70, and sub_82BF2B68). 1 caller (sub_82BF0610).
[entrypoint.functions."0x82BF04F8"]
name = "ProcessAndProcessAndProcess354_82BF04F8"

# A processor that processes the data (three hundred and fifty-fifth form). It takes a value (a). It processes the data (a). This is the "process this data" routine (three hundred and fifty-fifth form), part of the 0x82BF0 process cluster. 135 lines. 6 callees (ProcessAndProcessAndProcess353, sub_82BF0290, ProcessAndProcessAndProcess354, sub_82BF2B68, sub_82CC0888, and sub_82CC0918). 1 caller (ProcessAndProcessAndProcess349).
[entrypoint.functions."0x82BF0610"]
name = "ProcessAndProcessAndProcess355_82BF0610"

# A processor that processes the data (three hundred and fifty-sixth form). It takes a value (a). It processes the data (a). This is the "process this data" routine (three hundred and fifty-sixth form), part of the 0x82BF0 process cluster. 302 lines. 8 callees (ProcessAndConstructObject, ProcessAndValidateBounds2, ProcessAndWriteToBuffer, ValidateBounds4, ProcessAndProcessAndProcess352, sub_82BF0290, sub_82BF06F8, and sub_82BF2B68). 3 callers (sub_82BF09C0, ProcessAndProcessAndProcess349, and sub_82BF0AD0).
[entrypoint.functions."0x82BF07C0"]
name = "ProcessAndProcessAndProcess356_82BF07C0"

# A processor that processes the data (three hundred and fifty-seventh form). It takes multiple values. It processes the data. This is the "process this data" routine (three hundred and fifty-seventh form), part of the 0x82BF0 process cluster. 508 lines. 10 callees (ConstructObject_Thunk, ProcessAndCheckAndClear, ProcessAndFormatStringWithType, ProcessAndInitializeAndProcess, SetGlobalField_Thunk, StrLenUTF16_Thunk, Thunk_82BF2358, WrapperSub82CC1C70, sub_82BF2B68, and sub_82BF2C28). 1 caller (sub_82BF2128).
[entrypoint.functions."0x82BF0CE0"]
name = "ProcessAndProcessAndProcess357_82BF0CE0"

# A processor that processes the data (three hundred and fifty-eighth form). It takes nothing. It processes the data. This is the "process this data" routine (three hundred and fifty-eighth form), part of the 0x82BF2 process cluster. 11-line thunk. 1 callee (ProcessAndProcessAndProcess357). 1 caller (ProcessAndProcessAndProcess256).
[entrypoint.functions."0x82BF2128"]
name = "ProcessAndProcessAndProcess358_82BF2128"

# A processor that processes the data (three hundred and fifty-ninth form). It takes a value (a) and a value (b). It processes the data (a and b). This is the "process this data" routine (three hundred and fifty-ninth form), part of the 0x82BF2 process cluster. 11 lines. 0 callees (self-contained). 1 caller (ProcessAndProcessAndProcess256).
[entrypoint.functions."0x82BF2140"]
name = "ProcessAndProcessAndProcess359_82BF2140"

# A processor that processes the data (three hundred and sixtieth form). It takes a structure (a: fields at a->4, a->8, and a->12), a value (b), a value (c), and a structure (d: field at d->0). It processes the data (a, b, c, and d). This is the "process this data" routine (three hundred and sixtieth form), part of the 0x82BF2 process cluster. 469 lines. 3 callees (ProcessAndUpdateGlobal, sub_82BEFF50, and sub_82BF2158). 1 caller (sub_82BF2790).
[entrypoint.functions."0x82BF2470"]
name = "ProcessAndProcessAndProcess360_82BF2470"

# A processor that processes the data (three hundred and sixty-first form). It takes a structure (a: fields at a->4 and a->8), a value (b), and a value (c). It processes the data (a, b, and c). This is the "process this data" routine (three hundred and sixty-first form), part of the 0x82BF2 process cluster. 163 lines. 2 callees (sub_82BF2390 and ProcessAndProcessAndProcess360). 1 caller (sub_82BF28A8).
[entrypoint.functions."0x82BF2790"]
name = "ProcessAndProcessAndProcess361_82BF2790"

# A processor that processes the data (three hundred and sixty-second form). It takes a value (a). It processes the data (a). This is the "process this data" routine (three hundred and sixty-second form), part of the 0x82BF2 process cluster. 209 lines. 2 callees (SetGlobalField_Thunk and ProcessAndProcessAndProcess361). 1 caller (ProcessAndProcessAndProcess256).
[entrypoint.functions."0x82BF28A8"]
name = "ProcessAndProcessAndProcess362_82BF28A8"

# A processor that processes the data (three hundred and sixty-third form). It takes nothing. It processes the data. This is the "process this data" routine (three hundred and sixty-third form), part of the 0x82BF2 process cluster. 7-line thunk. 1 callee (ProcessAndUpdateGlobal). 1 caller (ProcessAndProcessAndProcess347).
[entrypoint.functions."0x82BF2E70"]
name = "ProcessAndProcessAndProcess363_82BF2E70"

# A processor that processes the data (three hundred and sixty-fourth form). It takes a value (a). It processes the data (a). This is the "process this data" routine (three hundred and sixty-fourth form), part of the 0x82BFC process cluster. 25-line wrapper. 1 callee (sub_82C009F0). 3 callers (sub_829F96A8, ProcessAndProcessAndProcess279, and sub_824F9CB8).
[entrypoint.functions."0x82BFCDD0"]
name = "ProcessAndProcessAndProcess364_82BFCDD0"

# A processor that processes the data (three hundred and sixty-fifth form). It takes a value (a). It processes the data (a). This is the "process this data" routine (three hundred and sixty-fifth form), part of the 0x82BFE process cluster. 60 lines. 2 callees (ProcessAndCallMethods and ProcessAndCheckMethod). 1 caller (sub_82C00850).
[entrypoint.functions."0x82BFEBC0"]
name = "ProcessAndProcessAndProcess365_82BFEBC0"

# A processor that processes the data (three hundred and sixty-sixth form). It takes a structure (a: fields at a->4 and a->8). It processes the data (a). This is the "process this data" routine (three hundred and sixty-sixth form), part of the 0x82C00 process cluster. 31 lines. 0 callees (uses an indirect call). 0 callers (unresolved — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x82C006F0"]
name = "ProcessAndProcessAndProcess366_82C006F0"

# A processor that processes the data (three hundred and sixty-seventh form). It takes a structure (a: fields at a->0 and a->4), a value (b), and a structure (c: field at c->0). It processes the data (a, b, and c). This is the "process this data" routine (three hundred and sixty-seventh form), part of the 0x82C00 process cluster. 69 lines. 0 callees (uses indirect calls). 9 callers (sub_82C10D00, sub_82C10370, sub_82C0F360, sub_82C13A70, sub_82C0F8E0, sub_82C009F0, sub_82C00A90, sub_821D5238, and sub_82C00978).
[entrypoint.functions."0x82C00760"]
name = "ProcessAndProcessAndProcess367_82C00760"

# A processor that processes the data (three hundred and sixty-eighth form). It takes a structure (a: fields at a->0, a->4, a->8, and a->12). It processes the data (a). This is the "process this data" routine (three hundred and sixty-eighth form), part of the 0x82C00 process cluster. 79 lines. 2 callees (Allocate_OrDbgBreak and ProcessAndProcessAndProcess365). 1 caller (sub_82C009F0).
[entrypoint.functions."0x82C00850"]
name = "ProcessAndProcessAndProcess368_82C00850"

# A processor that processes the data (three hundred and sixty-ninth form). It takes a structure (a: fields at a->0 and a->4). It processes the data (a). This is the "process this data" routine (three hundred and sixty-ninth form), part of the 0x82C00 process cluster. 65 lines. 3 callees (Allocate_OrDbgBreak, ProcessAndProcessAndProcess367, and ProcessAndProcessAndProcess368). 1 caller (ProcessAndProcessAndProcess364).
[entrypoint.functions."0x82C009F0"]
name = "ProcessAndProcessAndProcess369_82C009F0"

# A processor that processes the data (three hundred and seventieth form). It takes a value (a) and a value (b). It processes the data (a and b). This is the "process this data" routine (three hundred and seventieth form), part of the 0x82C15 process cluster. 75 lines. 2 callees (ProcessVcallSequence3 and VcallTriple_AllSuccess). 1 caller (sub_82C16A10).
[entrypoint.functions."0x82C152D8"]
name = "ProcessAndProcessAndProcess370_82C152D8"

# A processor that processes the data (three hundred and seventy-first form). It takes a structure (a: fields at a->0, a->24, and a->28) and a value (b). It processes the data (a and b). This is the "process this data" routine (three hundred and seventy-first form), part of the 0x82C16 process cluster. 86 lines. 1 callee (ProcessAndProcessAndProcess370). 1 caller (ProcessAndGrowAndProcess5).
[entrypoint.functions."0x82C16A10"]
name = "ProcessAndProcessAndProcess371_82C16A10"

# A processor that processes the data (three hundred and seventy-second form). It takes multiple values. It processes the data. This is the "process this data" routine (three hundred and seventy-second form), part of the 0x82C1B process cluster. 176 lines. 5 callees (sub_82C19370, sub_82C196E8, sub_82C19E70, sub_82C1AEF0, and itself — recursive). 1 caller (ProcessAndWalkAndCompare2).
[entrypoint.functions."0x82C1B4E0"]
name = "ProcessAndProcessAndProcess372_82C1B4E0"

# A processor that processes the data (three hundred and seventy-third form). It takes a structure (a: fields at a->0 and a->20) and a structure (b: field at b->8). It processes the data (a and b). This is the "process this data" routine (three hundred and seventy-third form), part of the 0x82C1E process cluster. 511 lines. 8 callees (Allocate_OrDbgBreak, Array_PushBack, ProcessGrow, VcallChain_6_Dispatch2_7, VirtualCallPair, sub_82BFF7D8, sub_82BFF848, and sub_82C02B98). 1 caller (sub_82C1F0B8).
[entrypoint.functions."0x82C1EBE8"]
name = "ProcessAndProcessAndProcess373_82C1EBE8"

# A processor that processes the data (three hundred and seventy-fourth form). It takes nothing. It processes the data. This is the "process this data" routine (three hundred and seventy-fourth form), part of the 0x82C1E process cluster. 55 lines. 1 callee (Allocate_OrDbgBreak). 0 callers (unresolved — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x82C1EF80"]
name = "ProcessAndProcessAndProcess374_82C1EF80"

# A processor that processes the data (three hundred and seventy-fifth form). It takes a structure (a: fields at a->0 and a->20) and a structure (b: field at b->8). It processes the data (a and b). This is the "process this data" routine (three hundred and seventy-fifth form), part of the 0x82C1F process cluster. 230 lines. 9 callees (ProcessAndCompactAndFree, ProcessAndConstructAndSort, ProcessAndFreeElements, SortedFind2, SortedFind, sub_82BFF7D8, ProcessAndProcessAndProcess373, sub_82C3BF18, and sub_82C40BD0). 1 caller (sub_82C1F248).
[entrypoint.functions."0x82C1F0B8"]
name = "ProcessAndProcessAndProcess375_82C1F0B8"

# A processor that processes the data (three hundred and seventy-sixth form). It takes a value (a) and a value (b). It processes the data (a and b). This is the "process this data" routine (three hundred and seventy-sixth form), part of the 0x82C1F process cluster. 13-line thunk. 1 callee (ProcessAndProcessAndProcess375). 0 callers (unresolved — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x82C1F248"]
name = "ProcessAndProcessAndProcess376_82C1F248"

# A processor that processes the data (three hundred and seventy-seventh form). It takes a structure (a: fields at a->0 and a->4) and a value (b). It processes the data (a and b). This is the "process this data" routine (three hundred and seventy-seventh form), part of the 0x82C32 process cluster. 83 lines. 1 callee (BinarySearch_4Stride). 2 callers (sub_82C32980 and sub_82C34918).
[entrypoint.functions."0x82C321E0"]
name = "ProcessAndProcessAndProcess377_82C321E0"

# A processor that processes the data (three hundred and seventy-eighth form). It takes a structure (a: fields at a->0, a->4, a->8, a->12, a->16, and a->20). It processes the data (a). This is the "process this data" routine (three hundred and seventy-eighth form), part of the 0x82C32 process cluster. 46 lines. 1 callee (AtomicIncrement3). 1 caller (sub_82C32AB8).
[entrypoint.functions."0x82C32328"]
name = "ProcessAndProcessAndProcess378_82C32328"

# A processor that processes the data (three hundred and seventy-ninth form). It takes a value (a) and a value (b). It processes the data (a and b). This is the "process this data" routine (three hundred and seventy-ninth form), part of the 0x82C32 process cluster. 141 lines. 2 callees (ProcessVcallSequence3 and VcallTriple_AllSuccess). 1 caller (sub_82C326D8).
[entrypoint.functions."0x82C32388"]
name = "ProcessAndProcessAndProcess379_82C32388"

# A processor that processes the data (three hundred and eightyth form). It takes a structure (a: fields at a->0, a->4, and a->12). It processes the data (a). This is the "process this data" routine (three hundred and eightyth form), part of the 0x82C32 process cluster. 92 lines. 2 callees (DestructRefCounted and Free_Thunk). 2 callers (sub_82C349B0 and sub_82C32598).
[entrypoint.functions."0x82C32488"]
name = "ProcessAndProcessAndProcess380_82C32488"

# A processor that processes the data (three hundred and eighty-first form). It takes a structure (a: fields at a->0, a->4, and a->12). It processes the data (a). This is the "process this data" routine (three hundred and eighty-first form), part of the 0x82C32 process cluster. 95 lines. 3 callees (ArrayCompact, DestructRefCounted, and Free_Thunk). 3 callers (sub_82C349B0, sub_82C32688, and sub_82C331A8).
[entrypoint.functions."0x82C325E8"]
name = "ProcessAndProcessAndProcess381_82C325E8"

# A processor that processes the data (three hundred and eighty-second form). It takes a structure (a: fields at a->0, a->24, and a->28) and a value (b). It processes the data (a and b). This is the "process this data" routine (three hundred and eighty-second form), part of the 0x82C32 process cluster. 86 lines. 1 callee (ProcessAndProcessAndProcess379). 1 caller (sub_82C32AB8).
[entrypoint.functions."0x82C326D8"]
name = "ProcessAndProcessAndProcess382_82C326D8"

# A processor that processes the data (three hundred and eighty-third form). It takes a structure (a: fields at a->0 and a->4). It processes the data (a). This is the "process this data" routine (three hundred and eighty-third form), part of the 0x82C32 process cluster. 66 lines. 1 callee (ProcessAndProcessAndProcess377). 0 callers (unresolved — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x82C32980"]
name = "ProcessAndProcessAndProcess383_82C32980"

# A processor that processes the data (three hundred and eighty-fourth form). It takes a structure (a: fields at a->0 and a->4) and a value (b). It processes the data (a and b). This is the "process this data" routine (three hundred and eighty-fourth form), part of the 0x82C32 process cluster. 65 lines. 0 callees (uses an indirect call). 0 callers (unresolved — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x82C329F0"]
name = "ProcessAndProcessAndProcess384_82C329F0"

# A processor that processes the data (three hundred and eighty-fifth form). It takes a structure (a: fields at a->0 and a->20) and a structure (b: field at b->8). It processes the data (a and b). This is the "process this data" routine (three hundred and eighty-fifth form), part of the 0x82C32 process cluster. 493 lines. 10 callees (Allocate_OrDbgBreak, Array_PushBack, AtomicIncrement3, ProcessGrow, VirtualCallPair, sub_82BFF7D8, sub_82BFF848, sub_82C02B98, ProcessAndProcessAndProcess378, and ProcessAndProcessAndProcess382). 1 caller (sub_82C349B0).
[entrypoint.functions."0x82C32AB8"]
name = "ProcessAndProcessAndProcess385_82C32AB8"

# A processor that processes the data (three hundred and eighty-sixth form). It takes a structure (a: fields at a->0 and a->20) and a structure (b: field at b->8). It processes the data (a and b). This is the "process this data" routine (three hundred and eighty-sixth form), part of the 0x82C32 process cluster. 443 lines. 8 callees (Allocate_OrDbgBreak, Array_PushBack, ProcessAndCallMethods4, ProcessGrow, VirtualCallPair, sub_82BFF7D8, sub_82BFF848, and sub_82C02B98). 1 caller (sub_82C34B40).
[entrypoint.functions."0x82C32E28"]
name = "ProcessAndProcessAndProcess386_82C32E28"

# A processor that processes the data (three hundred and eighty-seventh form). It takes a structure (a: fields at a->0, a->4, a->8, and a->12). It processes the data (a). This is the "process this data" routine (three hundred and eighty-seventh form), part of the 0x82C33 process cluster. 58 lines. 1 callee (BubbleSort3). 1 caller (sub_82C349B0).
[entrypoint.functions."0x82C33140"]
name = "ProcessAndProcessAndProcess387_82C33140"

# A processor that processes the data (three hundred and eighty-eighth form). It takes a structure (a: fields at a->0 and a->20) and a structure (b: field at b->8). It processes the data (a and b). This is the "process this data" routine (three hundred and eighty-eighth form), part of the 0x82C34 process cluster. 230 lines. 9 callees (SortedFind2, SortedFind, sub_82BFF7D8, ProcessAndProcessAndProcess380, ProcessAndProcessAndProcess381, ProcessAndProcessAndProcess385, ProcessAndProcessAndProcess387, sub_82C3BF18, and sub_82C40BD0). 1 caller (sub_82C34CD0).
[entrypoint.functions."0x82C349B0"]
name = "ProcessAndProcessAndProcess388_82C349B0"

# A processor that processes the data (three hundred and eighty-ninth form). It takes a structure (a: fields at a->0 and a->20) and a structure (b: field at b->8). It processes the data (a and b). This is the "process this data" routine (three hundred and eighty-ninth form), part of the 0x82C34 process cluster. 230 lines. 9 callees (ClearElementArray, CompactClearArray, ConstructSortedArray, SortedFind2, SortedFind, sub_82BFF7D8, ProcessAndProcessAndProcess386, sub_82C3BF18, and sub_82C40BD0). 1 caller (sub_82C34CE0).
[entrypoint.functions."0x82C34B40"]
name = "ProcessAndProcessAndProcess389_82C34B40"

# A processor that processes the data (three hundred and ninetieth form). It takes a value (a) and a value (b). It processes the data (a and b). This is the "process this data" routine (three hundred and ninetieth form), part of the 0x82C34 process cluster. 13-line thunk. 1 callee (ProcessAndProcessAndProcess388). 0 callers (unresolved — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x82C34CD0"]
name = "ProcessAndProcessAndProcess390_82C34CD0"

# A processor that processes the data (three hundred and ninety-first form). It takes a value (a) and a value (b). It processes the data (a and b). This is the "process this data" routine (three hundred and ninety-first form), part of the 0x82C34 process cluster. 13-line thunk. 1 callee (ProcessAndProcessAndProcess389). 0 callers (unresolved — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x82C34CE0"]
name = "ProcessAndProcessAndProcess391_82C34CE0"

# Binary-search lookup over a 4-stride element array (three hundred and ninety-second form). It takes a structure (a: fields at a->0 and a->4) and a value (b), walks the array with a binary search, and returns a matching element pointer (or null if none). This is the "process this data" routine (three hundred and ninety-second form), part of the 0x82C34 process cluster. 83 lines. 1 callee (BinarySearch_4Stride). 1 caller (sub_82C35448).
[entrypoint.functions."0x82C34DE0"]
name = "ProcessAndProcessAndProcess392_82C34DE0"

# Vcall-sequence gate that sets a result flag (three hundred and ninety-third form). It takes a value (a) and a value (b), issues a virtual call triple, then — if the low byte of the result is nonzero — issues a second virtual call sequence and sets an output flag (r29) accordingly. This is the "process this data" routine (three hundred and ninety-third form), part of the 0x82C34 process cluster. 76 lines. 2 callees (ProcessVcallSequence3 and VcallTriple_AllSuccess). 1 caller (sub_82C35190).
[entrypoint.functions."0x82C34F38"]
name = "ProcessAndProcessAndProcess393_82C34F38"

# Destruct-and-free loop over a refcounted element array (three hundred and ninety-fourth form). It takes a structure (a: fields at a->0, a->4, and a->12) where a->4 is the element base and a->12 is the count; for each nonzero element it runs the element destructor and frees the element, then zeroes out the array header. This is the "process this data" routine (three hundred and ninety-fourth form), part of the 0x82C34 process cluster. 92 lines. 2 callees (DestructRefCounted and Free_Thunk). 2 callers (sub_82C35050 and sub_82C38B68).
[entrypoint.functions."0x82C34FB8"]
name = "ProcessAndProcessAndProcess394_82C34FB8"

# Compact-then-destruct-free loop over a refcounted element array (three hundred and ninety-fifth form). It takes a structure (a: fields at a->0, a->4, and a->12) where a->4 is the element base and a->12 is the count; it first compacts the array, then for each nonzero element runs the element destructor and frees the element. This is the "process this data" routine (three hundred and ninety-fifth form), part of the 0x82C35 process cluster. 95 lines. 3 callees (ArrayCompact, DestructRefCounted, and Free_Thunk). 3 callers (sub_82C35140, sub_82C38B68, and sub_82C35F08).
[entrypoint.functions."0x82C350A0"]
name = "ProcessAndProcessAndProcess395_82C350A0"

# Virtual-call wrapper driving a vcall-sequence gate (three hundred and ninety-sixth form). It takes a structure (a: fields at a->0, a->24, and a->28) and a value (b); it issues a virtual call, and if the low byte of the result is nonzero it runs the vcall-sequence gate routine, then issues further virtual calls. This is the "process this data" routine (three hundred and ninety-sixth form), part of the 0x82C35 process cluster. 86 lines. 1 callee (ProcessAndProcessAndProcess393). 1 caller (sub_82C35580).
[entrypoint.functions."0x82C35190"]
name = "ProcessAndProcessAndProcess396_82C35190"

# Binary-search-lookup wrapper that computes a boolean parity bit (three hundred and ninety-seventh form). It takes a structure (a: fields at a->0 and a->4); it runs a binary-search lookup over a 4-stride element array, and if an element is found it issues a virtual call and derives a boolean from the leading-zero/parity of the result. This is the "process this data" routine (three hundred and ninety-seventh form), part of the 0x82C35 process cluster. 66 lines. 1 callee (ProcessAndProcessAndProcess392). 0 callers (unresolved — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x82C35448"]
name = "ProcessAndProcessAndProcess397_82C35448"

# Virtual-lookup wrapper returning an indexed sub-object field (three hundred and ninety-eighth form). It takes a structure (a: fields at a->0 and a->4) and a value (b); it issues a virtual call, and if the result is nonzero it scales b by 4, indexes into the element's sub-array, and returns the first field of the located sub-object. This is the "process this data" routine (three hundred and ninety-eighth form), part of the 0x82C35 process cluster. 65 lines. 0 callees (uses an indirect call). 0 callers (unresolved — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x82C354B8"]
name = "ProcessAndProcessAndProcess398_82C354B8"

# Large virtual-call-driven element collector (three hundred and ninety-ninth form). It takes a structure (a: fields at a->0 and a->20) and a structure (b: field at b->8); it issues virtual calls, and — driven by the results — allocates, grows, and appends elements to an array, calling the vcall-wrapper routine to inspect each candidate. This is the "process this data" routine (three hundred and ninety-ninth form), part of the 0x82C35 process cluster. 493 lines. 9 callees (Allocate_OrDbgBreak, Array_PushBack, AtomicIncrement3, ProcessGrow, VirtualCallPair, sub_82BFF7D8, sub_82BFF848, sub_82C02B98, and ProcessAndProcessAndProcess396). 1 caller (sub_82C38B68).
[entrypoint.functions."0x82C35580"]
name = "ProcessAndProcessAndProcess399_82C35580"

# Large virtual-call-driven float element collector (four hundredth form). It takes a structure (a: fields at a->0 and a->20) and a structure (b: field at b->8); it issues virtual calls and, driven by their results, allocates, grows, and appends float-valued elements to an array (using floating-point registers f30/f31). This is the "process this data" routine (four hundredth form), part of the 0x82C35 process cluster. 556 lines. 8 callees (Allocate_OrDbgBreak, Array_PushBack, ProcessAndDispatchVcallSequence, ProcessGrow, VirtualCallPair, sub_82BFF7D8, sub_82BFF848, and sub_82C02B98). 1 caller (sub_82C38CF8).
[entrypoint.functions."0x82C358F0"]
name = "ProcessAndProcessAndProcess400_82C358F0"

# Sort wrapper over a small inline struct (four hundred and first form). It takes a structure (a: fields at a->0, a->4, a->8, and a->12); it zeroes the struct fields, runs a bubble sort on the associated data, then stamps field 0 with a known constant. This is the "process this data" routine (four hundred and first form), part of the 0x82C35 process cluster. 58 lines. 1 callee (BubbleSort3). 1 caller (sub_82C38B68).
[entrypoint.functions."0x82C35EA0"]
name = "ProcessAndProcessAndProcess401_82C35EA0"

# Virtual-call dispatcher over element collect/destroy/sort routines (four hundred and second form). It takes a structure (a: fields at a->0 and a->20) and a structure (b: field at b->8); it issues virtual calls and, depending on their results, drives the element collection, destruct-free, and sort helpers, then searches the resulting sorted array. This is the "process this data" routine (four hundred and second form), part of the 0x82C38 process cluster. 230 lines. 9 callees (SortedFind2, SortedFind, sub_82BFF7D8, ProcessAndProcessAndProcess394, ProcessAndProcessAndProcess395, ProcessAndProcessAndProcess399, ProcessAndProcessAndProcess401, sub_82C3BF18, and sub_82C40BD0). 1 caller (sub_82C38E88).
[entrypoint.functions."0x82C38B68"]
name = "ProcessAndProcessAndProcess402_82C38B68"

# Virtual-call dispatcher over float element collect/clear/sort routines (four hundred and third form). It takes a structure (a: fields at a->0 and a->20) and a structure (b: field at b->8); it issues virtual calls and, depending on their results, drives the float element collection, array clearing, and sorted-array construction helpers, then searches the resulting array. This is the "process this data" routine (four hundred and third form), part of the 0x82C38 process cluster. 230 lines. 9 callees (ClearElementArray, CompactClearArray, ConstructSortedArray, SortedFind2, SortedFind, sub_82BFF7D8, ProcessAndProcessAndProcess400, sub_82C3BF18, and sub_82C40BD0). 1 caller (sub_82C38E98).
[entrypoint.functions."0x82C38CF8"]
name = "ProcessAndProcessAndProcess403_82C38CF8"

# Argument-reordering thunk into the element dispatcher (four hundred and fourth form). It takes a value (a) and a value (b), reorders the register arguments (passing a as the third arg and a+4 as the fourth), and forwards to the virtual-call dispatcher. This is the "process this data" routine (four hundred and fourth form), part of the 0x82C38 process cluster. 13-line thunk. 1 callee (ProcessAndProcessAndProcess402). 0 callers (unresolved — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x82C38E88"]
name = "ProcessAndProcessAndProcess404_82C38E88"

# Argument-reordering thunk into the float element dispatcher (four hundred and fifth form). It takes a value (a) and a value (b), reorders the register arguments (passing a as the third arg and a+4 as the fourth), and forwards to the float virtual-call dispatcher. This is the "process this data" routine (four hundred and fifth form), part of the 0x82C38 process cluster. 13-line thunk. 1 callee (ProcessAndProcessAndProcess403). 0 callers (unresolved — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x82C38E98"]
name = "ProcessAndProcessAndProcess405_82C38E98"

# Virtual-lookup wrapper returning an indexed sub-object field (four hundred and sixth form). It takes a structure (a: fields at a->0 and a->4) and a value (b); it issues a virtual call, then scales b by 4, indexes into the element's sub-array, and returns the first field of the located sub-object. This is the "process this data" routine (four hundred and sixth form), part of the 0x82C41 process cluster. 50 lines. 0 callees (uses an indirect call). 0 callers (unresolved — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x82C413C0"]
name = "ProcessAndProcessAndProcess406_82C413C0"

# Vcall-triple gate that sets a result flag (four hundred and seventh form). It takes a value (a) and a value (b), issues a virtual call triple, then — if the low byte of the result is nonzero — issues a second virtual call triple and sets an output flag accordingly. This is the "process this data" routine (four hundred and seventh form), part of the 0x82C41 process cluster. 75 lines. 1 callee (VcallTriple_AllSuccess). 1 caller (sub_82C41498).
[entrypoint.functions."0x82C41418"]
name = "ProcessAndProcessAndProcess407_82C41418"

# Virtual-call wrapper driving a vcall-triple gate (four hundred and eighth form). It takes a structure (a: fields at a->0, a->24, and a->28) and a value (b); it issues a virtual call, and if the low byte of the result is nonzero it runs the vcall-triple gate routine, then issues further virtual calls. This is the "process this data" routine (four hundred and eighth form), part of the 0x82C41 process cluster. 86 lines. 1 callee (ProcessAndProcessAndProcess407). 1 caller (sub_82C41580).
[entrypoint.functions."0x82C41498"]
name = "ProcessAndProcessAndProcess408_82C41498"

# Large virtual-call-driven element collector (four hundred and ninth form). It takes a structure (a: fields at a->0 and a->20) and a structure (b: field at b->8); it issues virtual calls and, driven by their results, allocates, grows, and appends elements to an array, calling the vcall-wrapper routine to inspect each candidate. This is the "process this data" routine (four hundred and ninth form), part of the 0x82C41 process cluster. 443 lines. 8 callees (Allocate_OrDbgBreak, Array_PushBack, ProcessGrow, VirtualCallPair, sub_82BFF7D8, sub_82BFF848, sub_82C02B98, and ProcessAndProcessAndProcess408). 1 caller (sub_82C419D0).
[entrypoint.functions."0x82C41580"]
name = "ProcessAndProcessAndProcess409_82C41580"

# Allocate-and-initialize a 16-byte header struct (four hundred and tenth form). It takes nothing; it allocates a 16-byte block and, on success, sets field 0 to a known constant and zeroes the remaining three fields, returning the pointer (or null on allocation failure). This is the "process this data" routine (four hundred and tenth form), part of the 0x82C41 process cluster. 55 lines. 1 callee (Allocate_OrDbgBreak). 0 callers (unresolved — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x82C41898"]
name = "ProcessAndProcessAndProcess410_82C41898"

# Virtual-call dispatcher over element collect/clear/sort routines (four hundred and eleventh form). It takes a structure (a: fields at a->0 and a->20) and a structure (b: field at b->8); it issues virtual calls and, depending on their results, drives the element collection, array clearing, and sorted-array construction helpers, then searches the resulting array. This is the "process this data" routine (four hundred and eleventh form), part of the 0x82C41 process cluster. 230 lines. 9 callees (ClearElementArray, CompactClearArray, ConstructSortedArray, SortedFind2, SortedFind, sub_82BFF7D8, sub_82C3BF18, sub_82C40BD0, and ProcessAndProcessAndProcess409). 1 caller (sub_82C41B60).
[entrypoint.functions."0x82C419D0"]
name = "ProcessAndProcessAndProcess411_82C419D0"

# Argument-reordering thunk into the element dispatcher (four hundred and twelfth form). It takes a value (a) and a value (b), reorders the register arguments (passing a as the third arg and a+4 as the fourth), and forwards to the virtual-call dispatcher. This is the "process this data" routine (four hundred and twelfth form), part of the 0x82C41 process cluster. 13-line thunk. 1 callee (ProcessAndProcessAndProcess411). 0 callers (unresolved — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x82C41B60"]
name = "ProcessAndProcessAndProcess412_82C41B60"

# Virtual-lookup wrapper returning an indexed sub-object field (four hundred and thirteenth form). It takes a structure (a: fields at a->0 and a->4) and a value (b); it issues a virtual call, then scales b by 4, indexes into the element's sub-array, and returns the first field of the located sub-object. This is the "process this data" routine (four hundred and thirteenth form), part of the 0x82C43 process cluster. 50 lines. 0 callees (uses an indirect call). 0 callers (unresolved — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x82C43340"]
name = "ProcessAndProcessAndProcess413_82C43340"

# Vcall-triple/sequence gate that sets result flags (four hundred and fourteenth form). It takes a value (a) and a value (b), issues a virtual call triple, and — when the low byte of the result is nonzero — issues further virtual call sequences and sets output flags accordingly. This is the "process this data" routine (four hundred and fourteenth form), part of the 0x82C43 process cluster. 126 lines. 2 callees (VcallChain_AllSuccess and VcallTriple_AllSuccess). 1 caller (sub_82C43488).
[entrypoint.functions."0x82C433A8"]
name = "ProcessAndProcessAndProcess414_82C433A8"

# Virtual-call wrapper driving a vcall-triple gate (four hundred and fifteenth form). It takes a structure (a: fields at a->0, a->24, and a->28) and a value (b); it issues a virtual call, and if the low byte of the result is nonzero it runs the vcall-triple gate routine, then issues further virtual calls. This is the "process this data" routine (four hundred and fifteenth form), part of the 0x82C43 process cluster. 86 lines. 1 callee (ProcessAndProcessAndProcess414). 1 caller (sub_82C43520).
[entrypoint.functions."0x82C43488"]
name = "ProcessAndProcessAndProcess415_82C43488"

# Large virtual-call-driven element collector (four hundred and sixteenth form). It takes a structure (a: fields at a->0 and a->20) and a structure (b: field at b->8); it issues virtual calls and, driven by their results, allocates, grows, and appends elements to an array, calling the vcall-wrapper routine to inspect each candidate. This is the "process this data" routine (four hundred and sixteenth form), part of the 0x82C43 process cluster. 443 lines. 8 callees (Allocate_OrDbgBreak, Array_PushBack, ProcessGrow, VirtualCallPair, sub_82BFF7D8, sub_82BFF848, sub_82C02B98, and ProcessAndProcessAndProcess415). 1 caller (sub_82C43DD8).
[entrypoint.functions."0x82C43520"]
name = "ProcessAndProcessAndProcess416_82C43520"

# Allocate-and-initialize a 16-byte header struct (four hundred and seventeenth form). It takes nothing; it allocates a 16-byte block and, on success, sets field 0 to a known constant and zeroes the remaining three fields, returning the pointer (or null on allocation failure). This is the "process this data" routine (four hundred and seventeenth form), part of the 0x82C43 process cluster. 55 lines. 1 callee (Allocate_OrDbgBreak). 0 callers (unresolved — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x82C43838"]
name = "ProcessAndProcessAndProcess417_82C43838"

# Virtual-call dispatcher over element collect/clear/sort routines (four hundred and eighteenth form). It takes a structure (a: fields at a->0 and a->20) and a structure (b: field at b->8); it issues virtual calls and, depending on their results, drives the element collection, array clearing, and sorted-array construction helpers, then searches the resulting array. This is the "process this data" routine (four hundred and eighteenth form), part of the 0x82C43 process cluster. 230 lines. 9 callees (ClearElementArray, CompactClearArray, ConstructSortedArray, SortedFind2, SortedFind, sub_82BFF7D8, sub_82C3BF18, sub_82C40BD0, and ProcessAndProcessAndProcess416). 1 caller (sub_82C43F68).
[entrypoint.functions."0x82C43DD8"]
name = "ProcessAndProcessAndProcess418_82C43DD8"

# Argument-reordering thunk into the element dispatcher (four hundred and nineteenth form). It takes a value (a) and a value (b), reorders the register arguments (passing a as the third arg and a+4 as the fourth), and forwards to the virtual-call dispatcher. This is the "process this data" routine (four hundred and nineteenth form), part of the 0x82C43 process cluster. 13-line thunk. 1 callee (ProcessAndProcessAndProcess418). 0 callers (unresolved — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x82C43F68"]
name = "ProcessAndProcessAndProcess419_82C43F68"

# Initialize a 16-byte counter header and atomically bump its counter (four hundred and twentieth form). It takes a structure (a: fields at a->0, a->4, a->8, and a->12); it sets field 0 to a known constant, points field 4 at a shared counter, zeroes fields 8-12, and atomically increments the counter. This is the "process this data" routine (four hundred and twentieth form), part of the 0x82C62 process cluster. 46 lines. 1 callee (AtomicIncrement3). 2 callers (ProcessAndProcessAndProcess214 and sub_82A4BE40).
[entrypoint.functions."0x82C62940"]
name = "ProcessAndProcessAndProcess420_82C62940"

# Conditional field accessor that returns a value only if a validity flag is set (four hundred and twenty-first form). It takes a structure (a: fields at a->8 and a->12); it returns a->12 when a->8 is nonzero, otherwise it returns 0. This is the "process this data" routine (four hundred and twenty-first form), part of the 0x82C62 process cluster. 19 lines. 0 callees. 0 callers (unresolved — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x82C62FE8"]
name = "ProcessAndProcessAndProcess421_82C62FE8"

# Stack-buffer-driven processor that frees a resource on success (four hundred and twenty-second form). It takes a value (a) and a value (b), builds a local scratch block on the stack, issues a virtual call to fill it in, and — depending on the outcome — frees a resource. This is the "process this data" routine (four hundred and twenty-second form), part of the 0x82C64 process cluster. 117 lines. 1 callee (Free_Thunk). 7 callers (sub_82AA9728, sub_8292ECD0, ProcessAndProcessAndProcess323, sub_82A78E30, sub_82C651D8, sub_82A855A8, and sub_82B28398).
[entrypoint.functions."0x82C64708"]
name = "ProcessAndProcessAndProcess422_82C64708"

# Fixed-argument thunk into the resolve-and-write routine (four hundred and twenty-third form). It takes nothing, sets up two constant arguments, and forwards to the resolve-and-write routine. This is the "process this data" routine (four hundred and twenty-third form), part of the 0x82C6B process cluster. 11-line thunk. 1 callee (ProcessAndResolveAndWrite3). 0 callers (unresolved — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x82C6B630"]
name = "ProcessAndProcessAndProcess423_82C6B630"

# Virtual-call wrapper driving resolve-and-write and validate-and-store routines (four hundred and twenty-fourth form). It takes a value (a), a value (b), and a structure (c: fields at c->0 and c->8); it issues a virtual call, and when the low byte of the result is nonzero it runs the resolve-and-write routine and the validate-and-store routine in sequence. This is the "process this data" routine (four hundred and twenty-fourth form), part of the 0x82C6C process cluster. 116 lines. 2 callees (ProcessAndResolveAndWrite3 and ProcessAndValidateAndStore). 1 caller (sub_82C6C328).
[entrypoint.functions."0x82C6C1B0"]
name = "ProcessAndProcessAndProcess424_82C6C1B0"

# Virtual-call-driven resolver that invokes several helper routines (four hundred and twenty-fifth form). It takes a value (a) and a structure (b: fields at b->0 and b->16); it issues a virtual call, checks the result, and — when valid — calls the resolve-and-write routine and three additional helper routines. This is the "process this data" routine (four hundred and twenty-fifth form), part of the 0x82C6C process cluster. 142 lines. 4 callees (ProcessAndProcessAndProcess424, sub_82C85F00, sub_82C86338, and sub_82C86388). 1 caller (ProcessAndProcessAndProcess239).
[entrypoint.functions."0x82C6C328"]
name = "ProcessAndProcessAndProcess425_82C6C328"

# Allocate-and-zero a sized block and store it in a 16-byte handle (four hundred and twenty-sixth form). It takes a structure (a: fields at a->0, a->4, a->8, and a->12) and a size (b); it fills field 0 with a known constant, field 4 with the size, then allocates (b*4) bytes (incrementing a counter) into field 8, zeroes the block, and sets field 12 to 0. This is the "process this data" routine (four hundred and twenty-sixth form), part of the 0x82C81 process cluster. 61 lines. 2 callees (AllocateAndIncrementCounter and MemSet_SdkRuntime). 1 caller (ProcessGlobalStruct).
[entrypoint.functions."0x82C81D90"]
name = "ProcessAndProcessAndProcess426_82C81D90"

# Destruct-and-free loop over an element array (four hundred and twenty-seventh form). It takes a structure (a: fields at a->0, a->4, and a->8); it sets field 0 to a known constant and, for each entry in the a->8 array (counted by a->4), reads the element pointer and, when it is a small object (size under 512), destructs and frees it. This is the "process this data" routine (four hundred and twenty-seventh form), part of the 0x82C81 process cluster. 91 lines. 1 callee (DestructAndFree). 1 caller (sub_82C81FE8).
[entrypoint.functions."0x82C81F50"]
name = "ProcessAndProcessAndProcess427_82C81F50"

# Destroy-and-maybe-free wrapper around the element destruct loop (four hundred and twenty-eighth form). It takes a value (a) and a flags value (b); it runs the destruct-and-free element loop, and — when the low bit of b is set — frees the handle itself. This is the "process this data" routine (four hundred and twenty-eighth form), part of the 0x82C81 process cluster. 50 lines. 2 callees (ProcessAndProcessAndProcess427 and Free_Thunk). 0 callers (unresolved — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x82C81FE8"]
name = "ProcessAndProcessAndProcess428_82C81FE8"

# Argument-forwarding thunk into the global-struct processor (four hundred and twenty-ninth form). It takes a value (a) and forwards it to the global-struct processor. This is the "process this data" routine (four hundred and twenty-ninth form), part of the 0x82C82 process cluster. 26-line thunk. 1 callee (ProcessGlobalStruct). 9 callers (ProcessAndMatchAndInsert, sub_82C88CF0, sub_82C86120, sub_82C88258, sub_82C884C0, sub_82C88390, sub_82C847B0, sub_82C843A0, and ProcessAndLookupAndProcess2).
[entrypoint.functions."0x82C825A0"]
name = "ProcessAndProcessAndProcess429_82C825A0"

# Destroy-and-invalidate a handle, then process its stored entry (four hundred and thirtieth form). It takes a structure (a: field at a->8) and a value (b); when a->8 is nonzero it destroys and invalidates the entry, then processes the stored value through several helper routines and updates a->8. This is the "process this data" routine (four hundred and thirtieth form), part of the 0x82C85 process cluster. 67 lines. 5 callees (ProcessAndProcessAndProcess189, ProcessAndProcessAndProcess190, ProcessAndProcessAndProcess191, ProcessAndProcessAndProcess193, and sub_82C88BC8). 4 callers (sub_82361E78, sub_8235F430, ProcessAndProcessAndProcess425, and sub_82361968).
[entrypoint.functions."0x82C85F00"]
name = "ProcessAndProcessAndProcess430_82C85F00"

# Process a value then initialize a 24-byte result structure (four hundred and thirty-first form). It takes a structure (a: fields at a->0, a->8, a->12, a->16, and a->20); it runs the process routine on it, then sets field 0 to a known constant and zeroes the remaining fields. This is the "process this data" routine (four hundred and thirty-first form), part of the 0x82C86 process cluster. 48 lines. 1 callee (ProcessAndProcessAndProcess183). 11 callers (sub_824EB648, sub_824EA150, ProcessAndProcessAndProcess425, sub_824E4EA8, sub_82931B88, sub_82361E78, sub_82BF8580, sub_824EB910, sub_8235F430, sub_82BF8468, and sub_82361968).
[entrypoint.functions."0x82C86338"]
name = "ProcessAndProcessAndProcess431_82C86338"

# Destroy-and-invalidate a stored entry, then re-initialize the handle (four hundred and thirty-second form). It takes a structure (a: fields at a->0 and a->8); when a->8 is nonzero it destroys and invalidates that entry, then re-initializes the handle (setting field 0 to a known constant). This is the "process this data" routine (four hundred and thirty-second form), part of the 0x82C86 process cluster. 59 lines. 3 callees (ProcessAndProcessAndProcess184, ProcessAndProcessAndProcess191, and ProcessAndProcessAndProcess193). 11 callers (sub_824EB910, sub_824EB648, ProcessAndProcessAndProcess425, sub_82BF8468, sub_82361968, sub_82361E78, sub_8235F430, sub_82BF8580, sub_824E4EA8, sub_82931B88, and sub_824EA150).
[entrypoint.functions."0x82C86388"]
name = "ProcessAndProcessAndProcess432_82C86388"

# Iterate a linked entry list performing a locked-table lookup and string match per entry (four hundred and thirty-third form). It takes a structure (a: fields at a->12 and a->20), a key (b), and a value (c); when a->12 is nonzero it walks the entry chain, running a locked-table lookup and a string-match test on each entry until a match is found or the list is exhausted. This is the "process this data" routine (four hundred and thirty-third form), part of the 0x82C86 process cluster. 155 lines. 2 callees (LockedTableLookup and MatchStringWrapper). 3 callers (sub_82C86890, TailCall_82C867E8, and sub_82C867C0).
[entrypoint.functions."0x82C863E8"]
name = "ProcessAndProcessAndProcess433_82C863E8"

# Process a value then initialize a 24-byte result structure with a set flag (four hundred and thirty-fourth form). It takes a structure (a: fields at a->0, a->8, a->12, a->16, and a->20); it runs the process routine on it, then sets field 0 to a known constant, zeroes fields 8-16, and sets field 20 to 1. This is the "process this data" routine (four hundred and thirty-fourth form), part of the 0x82C87 process cluster. 50 lines. 1 callee (ProcessAndProcessAndProcess183). 11 callers (sub_82BF65D8, sub_824E9A98, sub_824E9BE8, sub_82C88B38, sub_82BF7740, sub_82C88C58, sub_82C88BC8, sub_82BF7090, sub_82BF7A38, sub_82BF5FA0, and sub_82BF66B0).
[entrypoint.functions."0x82C874E0"]
name = "ProcessAndProcessAndProcess434_82C874E0"

# Destroy-and-invalidate a stored entry, then re-initialize the handle (four hundred and thirty-fifth form). It takes a structure (a: fields at a->0 and a->8); when a->8 is nonzero it destroys and invalidates that entry, then re-initializes the handle (setting field 0 to a known constant). This is the "process this data" routine (four hundred and thirty-fifth form), part of the 0x82C87 process cluster. 59 lines. 3 callees (ProcessAndProcessAndProcess184, ProcessAndProcessAndProcess191, and ProcessAndProcessAndProcess193). 12 callers (sub_82BF86C8, sub_82BF7740, sub_82C88B38, sub_82C88C58, sub_82BF65D8, sub_824E9BE8, sub_82BF8E28, sub_824E9A98, sub_82BF7090, sub_82BF66B0, sub_82BF7A38, and sub_82C88BC8).
[entrypoint.functions."0x82C87538"]
name = "ProcessAndProcessAndProcess435_82C87538"

# Initialize a 28-byte handle by processing two sub-structures and zeroing the tail (four hundred and thirty-sixth form). It takes a structure (a: fields at a->0, a->8, a->12, a->16, a->20, and a->24); it sets field 0 to a known constant, runs the global-struct processor on the a->8 and a->12 sub-structures (passing a size), and zeroes fields 16-24. This is the "process this data" routine (four hundred and thirty-sixth form), part of the 0x82C87 process cluster. 65 lines. 1 callee (ProcessGlobalStruct). 3 callers (sub_82C88BC8, sub_82C88B38, and sub_82C88C58).
[entrypoint.functions."0x82C87BC0"]
name = "ProcessAndProcessAndProcess436_82C87BC0"

# Build a string value in a structure by driving several string helpers (four hundred and thirty-seventh form). It takes a structure (a: field at a->4); it runs a helper with fixed constants and stores the result in a->4, then passes the value through several string-processing helpers. This is the "process this data" routine (four hundred and thirty-seventh form), part of the 0x82C88 process cluster. 80 lines. 5 callees (sub_82CA2968, sub_82C986B0, sub_82C986D0, sub_82C986E0, and sub_82C986E8). 3 callers (sub_82C88258, sub_82C884C0, and sub_82C88390).
[entrypoint.functions."0x82C88030"]
name = "ProcessAndProcessAndProcess437_82C88030"

# Validate-and-lookup routine that drives string helpers and a locked-table lookup (four hundred and thirty-eighth form). It takes a structure (a: fields at a->4 and a->8), a value (b), and a value (c); it runs a validation check on a->4 and, when it passes, runs a locked-table lookup on a->8 and passes the value through several string-processing helpers. This is the "process this data" routine (four hundred and thirty-eighth form), part of the 0x82C88 process cluster. 236 lines. 7 callees (LockedTableLookup, sub_8233D248, sub_82C87DC0, sub_82C87F30, sub_82C988B0, sub_82C98940, and sub_82C9B8F8). 3 callers (sub_82C884C0, sub_82C88390, and sub_82C88258).
[entrypoint.functions."0x82C880B8"]
name = "ProcessAndProcessAndProcess438_82C880B8"

# Allocate, build, and release a result for a reference-counted handle (four hundred and thirty-ninth form). It takes a structure (a: fields at a->8 and a->20), a value (b), and a value (c); when b is zero it runs a helper and bumps a->20, otherwise it allocates a block, builds the result by processing a->8 through the string helpers and a validate-lookup routine, and finally destructs/frees the block. This is the "process this data" routine (four hundred and thirty-ninth form), part of the 0x82C88 process cluster. 178 lines. 7 callees (AllocateAndIncrementCounter, DestructAndFree, ProcessAndProcessAndProcess429, sub_82C87DC0, ProcessAndProcessAndProcess437, ProcessAndProcessAndProcess438, and sub_82C9B770). 1 caller (sub_82C88BC8).
[entrypoint.functions."0x82C88390"]
name = "ProcessAndProcessAndProcess439_82C88390"

# Recurse into a sub-structure and store the result in the first field (four hundred and fortieth form). It takes a structure (a: fields at a->0 and a->4) and a value (b); it runs the recursive-process routine on the (a->0, a->4) pair and stores the result back into a->0. This is the "process this data" routine (four hundred and fortieth form), part of the 0x82C88 process cluster. 42 lines. 1 callee (ProcessAndRecurseAndProcess2). 1 caller (ProcessAndAllocateAndProcess2).
[entrypoint.functions."0x82C88630"]
name = "ProcessAndProcessAndProcess440_82C88630"

# Recurse into a sub-structure, then destroy-and-free its stored entry (four hundred and forty-first form). It takes a structure (a: field at a->0) and a structure (b: fields at b->0, b->4, b->16, b->20, and b->24); it runs the recursive-process routine on (b->0, b->4), copies a's field 0 into b->0 and zeroes a->0, then — when b->4 is nonzero — processes it, destructs and frees b->16, and zeroes b->16 through b->24. This is the "process this data" routine (four hundred and forty-first form), part of the 0x82C88 process cluster. 72 lines. 3 callees (ProcessAndRecurseAndProcess2, ProcessAndProcessAndProcess115, and DestructAndFree). 3 callers (sub_82C88B38, sub_82C88BC8, and sub_82C88C58).
[entrypoint.functions."0x82C88AC0"]
name = "ProcessAndProcessAndProcess441_82C88AC0"

# Stack-buffer orchestrator that drives the handle init, build, and release routines (four hundred and forty-second form). It takes a value (a), a value (b), and a value (c); it builds two local scratch blocks on the stack, runs the 28-byte-handle initializer and the 24-byte-result initializer, processes the blocks through the build-and-release routine, reads back an output field, and — when it is nonzero — runs a field-getter helper. This is the "process this data" routine (four hundred and forty-second form), part of the 0x82C88 process cluster. 90 lines. 6 callees (sub_82C87BC0, sub_82C874E0, ProcessAndProcessAndProcess439, GetField8, sub_82C87538, and ProcessAndProcessAndProcess441). 1 caller (ProcessAndProcessAndProcess430).
[entrypoint.functions."0x82C88BC8"]
name = "ProcessAndProcessAndProcess442_82C88BC8"

# Traversal over a linked list that calls a stub and an invalidate routine per entry (four hundred and forty-third form). It takes a structure (a: fields at a->0, a->4, and a->8); it walks the entry chain starting at a->0 (following the a->4 next pointers), calling a no-op stub and the invalidate routine on each entry, then zeroes fields 0-8. This is the "process this data" routine (four hundred and forty-third form), part of the 0x82C89 process cluster. 55 lines. 2 callees (NoOp_Stub and ProcessAndProcessAndProcess193). 1 caller (ProcessAndRecurseAndProcess2).
[entrypoint.functions."0x82C89028"]
name = "ProcessAndProcessAndProcess443_82C89028"

# Detect an "XML"/"xml" three-character tag and record a type code (four hundred and forty-fourth form). It takes a string pointer (a), a string end pointer (b), and an output structure (c: field at c->0); it stores the default type code (11) in c->0, then — if the string is exactly 3 characters and (case-insensitively) matches "XML" — stores the alternate type code (12) and returns 0, otherwise it returns 1. This is the "process this data" routine (four hundred and forty-fourth form), part of the 0x82C89 process cluster. 87 lines. 0 callees. 1 caller (sub_82C89AB0).
[entrypoint.functions."0x82C89A18"]
name = "ProcessAndProcessAndProcess444_82C89A18"

# Large parser/dispatcher that scans a buffer and invokes the format detector (four hundred and forty-fifth form). It takes a value (a), a buffer start (b), a buffer end (c), and an output structure (d); it walks the buffer character-by-character using a lookup table, dispatching on the characters found, and invokes the "XML"/"xml" format detector at the appropriate point. This is the "process this data" routine (four hundred and forty-fifth form), part of the 0x82C89 process cluster. 626 lines. 1 callee (ProcessAndProcessAndProcess444). 2 callers (ProcessAndDispatchAndProcess23 and ProcessAndDispatchIndex).
[entrypoint.functions."0x82C89AB0"]
name = "ProcessAndProcessAndProcess445_82C89AB0"

# Large bit-manipulating processor that drives a switch dispatch and vcall routines (four hundred and forty-sixth form). It takes a value (a), a value (b), a structure (c: field at c->68), and three additional values (d, e, f); it reads c->68, performs bit operations and arithmetic on it, builds a local scratch block, then drives a switch dispatch and a vcall-return routine. This is the "process this data" routine (four hundred and forty-sixth form), part of the 0x82C97 process cluster. 457 lines. 3 callees (ProcessAndProcessAndProcess117, ProcessAndVcallAndReturn, and SwitchDispatch). 2 callers (sub_82C983B0 and sub_82C985B8).
[entrypoint.functions."0x82C97458"]
name = "ProcessAndProcessAndProcess446_82C97458"

# Large self-contained processor over a multi-argument record (four hundred and forty-seventh form). It takes a value (a), a record (b: fields at b->73 and b->76), a start index (c), an end index (d), and two additional values (e and f); it compares the indices and, when they differ, processes the record using the fields at b->73 and b->76 (with inline logic and no further function calls). This is the "process this data" routine (four hundred and forty-seventh form), part of the 0x82C97 process cluster. 380 lines. 0 callees. 4 callers (sub_82C98420, sub_82C98218, sub_82C98240, and sub_82C98448).
[entrypoint.functions."0x82C97F70"]
name = "ProcessAndProcessAndProcess447_82C97F70"

# Argument-reordering thunk into the multi-argument processor (four hundred and forty-eighth form). It takes a value (a), a value (b), a value (c), and a value (d), reorders and extends the register arguments (passing a constant and 0 in addition), and forwards to the large self-contained processor. This is the "process this data" routine (four hundred and forty-eighth form), part of the 0x82C98 process cluster. 21-line thunk. 1 callee (ProcessAndProcessAndProcess447). 0 callers (unresolved — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x82C98218"]
name = "ProcessAndProcessAndProcess448_82C98218"

# Virtual-call wrapper that compares a result and drives a compare-and-process routine (four hundred and forty-ninth form). It takes a structure (a: field at a->60), a value (b), and a value (c); it issues a virtual call on a->60, compares the result against c, and — when they differ — runs the compare-and-process routine, otherwise it returns 0. This is the "process this data" routine (four hundred and forty-ninth form), part of the 0x82C98 process cluster. 112 lines. 2 callees (ProcessAndCompareAndProcess and ProcessAndProcessAndProcess118). 0 callers (unresolved — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x82C982E8"]
name = "ProcessAndProcessAndProcess449_82C982E8"

# Multi-argument thunk that reorders arguments into the large bit-manipulating processor (four hundred and fiftieth form). It takes many values (a through h), reorders and extends the register arguments (passing a constant and loading scratch fields), and forwards to the large bit-manipulating processor. This is the "process this data" routine (four hundred and fiftieth form), part of the 0x82C98 process cluster. 62 lines. 1 callee (ProcessAndProcessAndProcess446). 0 callers (unresolved — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x82C983B0"]
name = "ProcessAndProcessAndProcess450_82C983B0"

# Conditional setter that updates a value and, if a duplicate field still matches, updates that too (four hundred and fifty-first form). It takes a structure (a: fields at a->0 and a->4) and a value (b); it stores b into a->0, and — if a->4 still equals the original a->0 — it also stores b into a->4. This is the "process this data" routine (four hundred and fifty-first form), part of the 0x82C98 process cluster. 18 lines. 0 callees. 1 caller (ProcessAndProcessAndProcess437).
[entrypoint.functions."0x82C986B0"]
name = "ProcessAndProcessAndProcess451_82C986B0"

# Set two fields (52 and 56) in a structure (four hundred and fifty-second form). It takes a structure (a: fields at a->52 and a->56) and two values (b and c); it stores b into a->52 and c into a->56. This is the "process this data" routine (four hundred and fifty-second form), part of the 0x82C98 process cluster. 9 lines. 0 callees. 1 caller (ProcessAndProcessAndProcess437).
[entrypoint.functions."0x82C986D0"]
name = "ProcessAndProcessAndProcess452_82C986D0"

# Set a single field (60) in a structure (four hundred and fifty-third form). It takes a structure (a: field at a->60) and a value (b); it stores b into a->60. This is the "process this data" routine (four hundred and fifty-third form), part of the 0x82C98 process cluster. 7 lines. 0 callees. 1 caller (ProcessAndProcessAndProcess437).
[entrypoint.functions."0x82C986E0"]
name = "ProcessAndProcessAndProcess453_82C986E0"

# Set two fields (72 and 76) in a structure (four hundred and fifty-fourth form). It takes a structure (a: fields at a->72 and a->76) and two values (b and c); it stores b into a->72 and c into a->76. This is the "process this data" routine (four hundred and fifty-fourth form), part of the 0x82C98 process cluster. 9 lines. 0 callees. 1 caller (ProcessAndProcessAndProcess437).
[entrypoint.functions."0x82C986E8"]
name = "ProcessAndProcessAndProcess454_82C986E8"

# Type-driven memory mover over a large record (four hundred and fifty-fifth form). It takes a record (a: fields at a->8, a->24, a->28, a->32, and a->480) and an index (b); it dispatches on the type code at a->480 and performs the appropriate memory copy or move on the record's fields. This is the "process this data" routine (four hundred and fifty-fifth form), part of the 0x82C98 process cluster. 246 lines. 2 callees (MemCpy_SdkRuntime and MemMove_SdkRuntime). 1 caller (sub_82C9B8F8).
[entrypoint.functions."0x82C986F8"]
name = "ProcessAndProcessAndProcess455_82C986F8"

# Conditional virtual-call and copy over a nested record (four hundred and fifty-sixth form). It takes a record (a: fields at a->16 and a->364); when a->364 is nonzero it examines the nested record's fields (at +4, +8, +16, +24, +36, and +40), and — under a size condition — issues a virtual call and performs a memory copy. This is the "process this data" routine (four hundred and fifty-sixth form), part of the 0x82C98 process cluster. 128 lines. 1 callee (MemCpy_SdkRuntime). 3 callers (sub_82C98A50, sub_82C9BA50, and sub_82C9FAD8).
[entrypoint.functions."0x82C98970"]
name = "ProcessAndProcessAndProcess456_82C98970"

# Type-driven mutator over a large record that adjusts its internal fields in place (four hundred and fifty-seventh form). It takes a record (a: fields at a->24, a->28, a->36, a->40, a->280, a->296, and a->480), an offset (b), and a value (c); it dispatches on the type code at a->480 and, for the default case, recomputes and stores the record's offset fields in place. This is the "process this data" routine (four hundred and fifty-seventh form), part of the 0x82C9A process cluster. 170 lines. 0 callees. 1 caller (sub_82C9B8F8).
[entrypoint.functions."0x82C9A360"]
name = "ProcessAndProcessAndProcess457_82C9A360"

# Virtual-call wrapper over a record's flag-selected data (four hundred and fifty-eighth form). It takes a record (a: fields at a->144, a->148, a->232, and a->236); it selects a virtual target based on the flag at a->236, issues a virtual call using the a->144 and a->148 fields, and — when the result is nonzero — runs a helper routine. This is the "process this data" routine (four hundred and fifty-eighth form), part of the 0x82C9A process cluster. 80 lines. 1 callee (sub_82C98CA8). 1 caller (sub_82CA1B80).
[entrypoint.functions."0x82C9AA48"]
name = "ProcessAndProcessAndProcess458_82C9AA48"

# Virtual-call wrapper that conditionally initializes a 32-byte block (four hundred and fifty-ninth form). It takes a structure (a: field at a->0); it issues a virtual call (passing a constant of 188), and — when the result is nonzero — stores a into the result's field 100 and zeroes the result's fields 80 through 96. This is the "process this data" routine (four hundred and fifty-ninth form), part of the 0x82C9A process cluster. 155 lines. 0 callees (uses an indirect call). 1 caller (sub_82CA26F0).
[entrypoint.functions."0x82C9AF98"]
name = "ProcessAndProcessAndProcess459_82C9AF98"

# Iterate-and-process routine over a pointer array with zero-entry detection (four hundred and sixtieth form). It takes a structure (a: fields at a->20, a->28, and a->132), a value (b), and a value (c); it walks the pointer array starting at a->20 (counted by a->28, scaled by 4), detects zero entries, and drives the iterate-and-process helpers. This is the "process this data" routine (four hundred and sixtieth form), part of the 0x82C9B process cluster. 141 lines. 2 callees (ProcessAndIterateAndProcess4 and ProcessAndIterateAndProcess5). 1 caller (sub_82C9B770).
[entrypoint.functions."0x82C9B0C0"]
name = "ProcessAndProcessAndProcess460_82C9B0C0"

# Virtual-call-driven iterator over a record's linked data (four hundred and sixty-first form). It takes a record (a: fields at a->20, a->36, a->36, a->364, a->368, and a->44); when a is nonzero it reads the pointers at a->364 and a->368, issues a virtual call on the a->20 field, and — based on the result — drives the iterate-and-process helpers and the pointer-array iterator. This is the "process this data" routine (four hundred and sixty-first form), part of the 0x82C9B process cluster. 231 lines. 3 callees (ProcessAndIterateAndProcess3, ProcessAndIterateAndProcess5, and ProcessAndProcessAndProcess460). 5 callers (sub_82C88258, sub_82C88390, sub_82C884C0, sub_82CA26F0, and sub_82CA2968).
[entrypoint.functions."0x82C9B770"]
name = "ProcessAndProcessAndProcess461_82C9B770"

# Type-driven mutator over a large record that moves and updates its fields (four hundred and sixty-second form). It takes a record (a: fields at a->24, a->28, a->40, a->280, a->480, and a->484), a value (b), a value (c), and a value (d); it dispatches on the type code at a->480 and, for the default case, sets the type, performs a memory move, and drives the in-place field mutator routine. This is the "process this data" routine (four hundred and sixty-second form), part of the 0x82C9B process cluster. 198 lines. 3 callees (MemCpy_SdkRuntime, ProcessAndProcessAndProcess455, and ProcessAndProcessAndProcess457). 1 caller (ProcessAndProcessAndProcess438).
[entrypoint.functions."0x82C9B8F8"]
name = "ProcessAndProcessAndProcess462_82C9B8F8"

# Large flag-driven processor that clears, checks, and returns over a record's data (four hundred and sixty-third form). It takes a record (a: fields at a->144 and a->236), a value (b), a value (c), and a value (d); it selects a target based on the flag at a->236, builds a local scratch block, and drives the clear-and-zero, check-and-return, and helper routines. This is the "process this data" routine (four hundred and sixty-third form), part of the 0x82C9B process cluster. 373 lines. 4 callees (ClearAndZeroAndProcess, ProcessAndCheckAndReturn, sub_82C98CA8, and sub_82C98FD8). 1 caller (sub_82C9FB58).
[entrypoint.functions."0x82C9BBC0"]
name = "ProcessAndProcessAndProcess463_82C9BBC0"

# Large virtual-call-driven processor over a record's string-like field (four hundred and sixty-fourth form). It takes a record (a: fields at a->144, a->280, a->288, and a->292), a value (b), a value (c), and a value (d); it stores b into a->288, issues a virtual call on the a->144 field, and — based on the result length — drives the check-and-process helpers. This is the "process this data" routine (four hundred and sixty-fourth form), part of the 0x82C9D process cluster. 332 lines. 3 callees (ProcessAndCheckAndProcess9, sub_82C98FD8, and sub_82C9C810). 1 caller (sub_82C9EAC0).
[entrypoint.functions."0x82C9D500"]
name = "ProcessAndProcessAndProcess464_82C9D500"

# Mega-processor over a record that queries, checks, and clears its data through many helpers (four hundred and sixty-fifth form). It takes a record (a: fields at a->144, a->288, a->292, a->300, and a->356) and six additional values (b through g); it examines the record's fields and drives the query, check-and-process, check-and-return, clear-and-zero, and helper routines in a complex multi-branch flow. This is the "process this data" routine (four hundred and sixty-fifth form), part of the 0x82C9E process cluster. 1836 lines. 12 callees (ClearAndZeroAndProcess, ProcessAndCheckAndProcess9, ProcessAndCheckAndReturn, ProcessAndQueryAndProcess, ProcessAndQueryHashTable, sub_82C97790, sub_82C98DF8, sub_82C98FD8, sub_82C9A490, sub_82C9ABF8, sub_82C9C810, and ProcessAndProcessAndProcess464). 5 callers (sub_82C98DF8, sub_82CA17C8, sub_82C9FAD8, sub_82C9BA50, and sub_82C98A50).
[entrypoint.functions."0x82C9EAC0"]
name = "ProcessAndProcessAndProcess465_82C9EAC0"

# Argument-reordering wrapper that drives the mega-processor and a nested-record processor (four hundred and sixty-sixth form). It takes a record (a: fields at a->144 and a->484), a value (b), a value (c), and a value (d); it reorders the arguments (computing a flag from a->484), drives the mega-processor, and — when its result is zero — runs the nested-record processor. This is the "process this data" routine (four hundred and sixty-sixth form), part of the 0x82C9F process cluster. 73 lines. 2 callees (ProcessAndProcessAndProcess465 and ProcessAndProcessAndProcess456). 1 caller (sub_82C9FB58).
[entrypoint.functions."0x82C9FAD8"]
name = "ProcessAndProcessAndProcess466_82C9FAD8"

# Mega-processor over a record that queries, checks, initializes, and clears its data through many helpers (four hundred and sixty-seventh form). It takes a record (a: fields at a->144, a->288, a->292, a->300, and a->356) and seven additional values (b through h); it examines the record's fields and drives the query, check-and-process, check-and-dispatch, check-and-return, initialize-and-copy, clear-and-zero, and helper routines in a complex multi-branch flow. This is the "process this data" routine (four hundred and sixty-seventh form), part of the 0x82C9F process cluster. 3414 lines. 20 callees (ClearAndZeroAndProcess, ProcessAndCheckAndDispatch, ProcessAndCheckAndProcess9, ProcessAndCheckAndReturn, ProcessAndInitializeAndCopy, ProcessAndQueryAndProcess2, ProcessAndQueryHashTable, sub_82C98DF8, sub_82C98FD8, sub_82C990A8, sub_82C99200, sub_82C99A08, sub_82C99B50, sub_82C9A840, sub_82C9B388, ProcessAndProcessAndProcess463, sub_82C9C3A8, sub_82C9C810, sub_82C9D748, and ProcessAndProcessAndProcess466). 3 callers (sub_82CA17C8, sub_82CA1758, and sub_82C98DF8).
[entrypoint.functions."0x82C9FB58"]
name = "ProcessAndProcessAndProcess467_82C9FB58"

# Virtual-call wrapper that reorders arguments into the mega-processor (four hundred and sixty-eighth form). It takes a record (a: fields at a->144 and a->484), a value (b), a value (c), and a value (d); it issues a virtual call on the a->144 field, reorders the arguments (computing a flag from a->484), and forwards to the mega-processor. This is the "process this data" routine (four hundred and sixty-eighth form), part of the 0x82CA1 process cluster. 65 lines. 1 callee (ProcessAndProcessAndProcess467). 2 callers (sub_82C9BB40 and sub_82CA1B80).
[entrypoint.functions."0x82CA1758"]
name = "ProcessAndProcessAndProcess468_82CA1758"

# Conditional processor that runs the flag-selected data processor, and — when it returns zero — runs the mega-processor wrapper (four hundred and sixty-ninth form). It takes a record (a: field at a->280), a value (b), a value (c), and a value (d); it runs the flag-selected data processor on a, and if the result is zero it drives the mega-processor wrapper. This is the "process this data" routine (four hundred and sixty-ninth form), part of the 0x82CA1 process cluster. 52 lines. 2 callees (ProcessAndProcessAndProcess458 and ProcessAndProcessAndProcess468). 0 callers (unresolved — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x82CA1B80"]
name = "ProcessAndProcessAndProcess469_82CA1B80"

# Value-dispatch processor that branches over a range of cases using a jump table (four hundred and seventieth form). It takes a value (a), a value (b), and a value (c); it subtracts 11 from b, and if the result is in the range 0-18 it branches to the matching case via a jump table, otherwise it falls through to the default path. This is the "process this data" routine (four hundred and seventieth form), part of the 0x82CA2 process cluster. 201 lines. 0 callees. 0 callers (unresolved — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x82CA2420"]
name = "ProcessAndProcessAndProcess470_82CA2420"

# Initialize a large record with constants and conditionally copy-and-process data (four hundred and seventy-first form). It takes a record (a: fields at a->232, a->256, a->268, a->272, a->276, a->280, and a->416) and a value (b); it sets several fields to known constants, and — when b is nonzero — runs the copy-and-process routine on a->416, then stores the result in a->232 and drives a helper. This is the "process this data" routine (four hundred and seventy-first form), part of the 0x82CA2 process cluster. 191 lines. 2 callees (ProcessAndCopyAndProcess6 and ProcessAndProcessAndProcess192). 1 caller (sub_82CA26F0).
[entrypoint.functions."0x82CA2588"]
name = "ProcessAndProcessAndProcess471_82CA2588"

# Large virtual-call-driven processor that allocates a 496-byte block and drives helper routines (four hundred and seventy-second form). It takes a value (a), a structure (b: fields at b->0, b->4, and b->8), a value (c), and a value (d); when b is nonzero it issues a virtual call on b->0, copies b's fields into the result, and — based on the outcome — drives the resolve-global and helper routines. This is the "process this data" routine (four hundred and seventy-second form), part of the 0x82CA2 process cluster. 350 lines. 4 callees (ProcessAndResolveGlobal, ProcessAndProcessAndProcess459, ProcessAndProcessAndProcess461, and ProcessAndProcessAndProcess471). 1 caller (sub_82CA2968).
[entrypoint.functions."0x82CA26F0"]
name = "ProcessAndProcessAndProcess472_82CA26F0"

# Wrapper that drives the large virtual-call processor and two helpers based on the result (four hundred and seventy-third form). It takes a value (a); it runs the large virtual-call-driven processor, and — when the result is nonzero and its field 236 is set — drives a helper routine, then runs the linked-data iterator. This is the "process this data" routine (four hundred and seventy-third form), part of the 0x82CA2 process cluster. 75 lines. 3 callees (ProcessAndProcessAndProcess472, sub_82C9CC38, and ProcessAndProcessAndProcess461). 1 caller (ProcessAndProcessAndProcess437).
[entrypoint.functions."0x82CA2968"]
name = "ProcessAndProcessAndProcess473_82CA2968"

# TLS-driven processor that issues a virtual call and checks success (four hundred and seventy-fourth form). It takes many values; it runs a TLS-related routine to obtain a structure, issues a virtual call on it, runs another TLS-related routine, and — based on the result — drives the check-success helper. This is the "process this data" routine (four hundred and seventy-fourth form), part of the 0x82CA3 process cluster. 72 lines. 5 callees (ProcessAndTlsAndProcess3, ProcessAndTlsAndProcess2, ProcessAndCheckSuccess, sub_82CA97A8, and sub_82CC0728). 1 caller (sub_82CA3430).
[entrypoint.functions."0x82CA3388"]
name = "ProcessAndProcessAndProcess474_82CA3388"

# TLS-driven processor that obtains and processes a structure through several helpers (four hundred and seventy-fifth form). It takes a structure (a: fields at a->84 and a->88); it runs TLS-related routines, copies fields between the local and result structures, and drives the virtual-call/check-success helpers. This is the "process this data" routine (four hundred and seventy-fifth form), part of the 0x82CA3 process cluster. 83 lines. 8 callees (ProcessAndTlsAndProcess, ProcessAndProcessAndProcess197, sub_82CAC3D0, sub_82CAC480, WrapperSub82CC1C70, sub_82CC0728, ProcessAndProcessAndProcess474, and ProcessAndProcessAndProcess120). 0 callers (unresolved — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x82CA3430"]
name = "ProcessAndProcessAndProcess475_82CA3430"

# Read a TLS value and issue a virtual call on it (four hundred and seventy-sixth form). It takes a value (a); it reads a TLS value (via KeTlsGetValue) and issues a virtual call on the result. This is the "process this data" routine (four hundred and seventy-sixth form), part of the 0x82CAC process cluster. 33 lines. 1 callee (__imp__KeTlsGetValue). 2 callers (ProcessAndProcessAndProcess475 and sub_830029D8).
[entrypoint.functions."0x82CAC3D0"]
name = "ProcessAndProcessAndProcess476_82CAC3D0"

# Indirect-call thunk that invokes a global function pointer (four hundred and seventy-seventh form). It takes nothing; it loads a global function pointer and invokes it. This is the "process this data" routine (four hundred and seventy-seventh form), part of the 0x82CAC process cluster. 9-line thunk. 0 callees (uses an indirect call). 2 callers (sub_830029D8 and ProcessAndProcessAndProcess475).
[entrypoint.functions."0x82CAC480"]
name = "ProcessAndProcessAndProcess477_82CAC480"

# Set a thread's affinity mask via the Windows kernel API (four hundred and seventy-eighth form). It takes a value (a); when it is under 6 it references the current thread object by handle, sets its affinity mask (scaled by the value) via KeSetAffinityThread, and dereferences the object. This is the "process this data" routine (four hundred and seventy-eighth form), part of the 0x82CBB process cluster. 106 lines. 4 callees (__imp__ObReferenceObjectByHandle, __imp__KeSetAffinityThread, __imp__ObDereferenceObject, and sub_82CC1C18). 12 callers (sub_822D05C8, sub_82EF5510, sub_82C72950, sub_8236C360, sub_82B45528, sub_82B38590, sub_822EA928, sub_82A8B778, sub_832B8E50, sub_82BE3960, sub_82B44D10, and sub_82B9F598).
[entrypoint.functions."0x82CBBE20"]
name = "ProcessAndProcessAndProcess478_82CBBE20"

# Thin thunk that forwards to the XamUserGetSigninState API (four hundred and seventy-ninth form). It takes nothing and forwards directly to the Xam user sign-in state query. This is the "process this data" routine (four hundred and seventy-ninth form), part of the 0x82CBC process cluster. 5-line thunk. 1 callee (__imp__XamUserGetSigninState). 12 callers (sub_8247B510, sub_8230B708, sub_82339B30, sub_8247AB80, sub_8233C1D8, sub_824786B8, sub_82184968, sub_822CFDE0, sub_82479F18, sub_824788F8, ProcessAndProcessAndProcess259, and sub_8247C000).
[entrypoint.functions."0x82CBC150"]
name = "ProcessAndProcessAndProcess479_82CBC150"

# Argument-reordering thunk that forwards to the XamEnumerate API (four hundred and eightieth form). It takes four values (a through d), shifts the register arguments (inserting 0 as the first), and forwards to the Xam enumerate query. This is the "process this data" routine (four hundred and eightieth form), part of the 0x82CBC process cluster. 13-line thunk. 1 callee (__imp__XamEnumerate). 10 callers (ProcessAndProcessAndProcess238, sub_824E62B8, ProcessAndProcessAndProcess257, sub_824E69E8, sub_82368D38, sub_824E68C8, sub_822FA2A0, sub_824A5918, sub_824E6728, and sub_823C2270).
[entrypoint.functions."0x82CBCFA8"]
name = "ProcessAndProcessAndProcess480_82CBCFA8"

# Thin thunk that forwards to the XamContentCreateEnumerator API (four hundred and eighty-first form). It takes nothing and forwards directly to the Xam content enumerator creation. This is the "process this data" routine (four hundred and eighty-first form), part of the 0x82CBD process cluster. 5-line thunk. 1 callee (__imp__XamContentCreateEnumerator). 7 callers (ProcessAndProcessAndProcess257, sub_824E68C8, sub_824E6728, sub_824E69E8, sub_824E62B8, ProcessAndProcessAndProcess238, and sub_823C2270).
[entrypoint.functions."0x82CBD1B0"]
name = "ProcessAndProcessAndProcess481_82CBD1B0"

# Destruct a value and terminate the current thread (four hundred and eighty-second form). It takes a value (a); it destructs the value and then terminates the current thread via ExTerminateThread. This is the "process this data" routine (four hundred and eighty-second form), part of the 0x82CC0 process cluster. 27 lines. 2 callees (ProcessAndDestructAndProcess25 and __imp__ExTerminateThread). 9 callers (sub_830029D8, Func_83002974, ProcessAndProcessAndProcess474, Func_82CA33AC, sub_8262DDD0, sub_82D012D8, ProcessAndProcessAndProcess475, sub_83002950, and sub_83002940).
[entrypoint.functions."0x82CC0728"]
name = "ProcessAndProcessAndProcess482_82CC0728"

# Query the console region via the XConfig setting, falling back to the game-region API (four hundred and eighty-third form). It takes nothing; it reads an XConfig setting (category 3, value 9) into a local and, when the result is out of the valid 0-12 range, falls back to the game-region query (XGetGameRegion). This is the "process this data" routine (four hundred and eighty-third form), part of the 0x82CC0 process cluster. 87 lines. 2 callees (__imp__ExGetXConfigSetting and __imp__XGetGameRegion). 1 caller (ProcessAndProcessAndProcess355).
[entrypoint.functions."0x82CC0888"]
name = "ProcessAndProcessAndProcess483_82CC0888"

# Large processor over the console-language XConfig setting (four hundred and eighty-fourth form). It takes nothing; it reads the console-language XConfig setting (category 3, value 14), validates the returned language code against a range of known values, performs a table lookup to resolve the language, and drives further processing. This is the "process this data" routine (four hundred and eighty-fourth form), part of the 0x82CC0 process cluster. 520 lines. 2 callees (__imp__ExGetXConfigSetting and __imp__XGetGameRegion). 1 caller (ProcessAndProcessAndProcess355).
[entrypoint.functions."0x82CC0918"]
name = "ProcessAndProcessAndProcess484_82CC0918"

# Build a value array on the stack with a clamped count, raising an exception on failure (four hundred and eighty-fifth form). It takes a value (a), a flag (b: low bit extracted), a count (c: clamped to 0-15), and a value (d); it sets up a local structure, copies an aligned block of values, and — when the result is invalid — raises an exception. This is the "process this data" routine (four hundred and eighty-fifth form), part of the 0x82CC0 process cluster. 76 lines. 2 callees (MemCpy_Aligned and __imp__RtlRaiseException). 4 callers (sub_82CA69D0, Func_82B387D0, ProcessAndProcessAndProcess318, and sub_82CAD810).
[entrypoint.functions."0x82CC0D58"]
name = "ProcessAndProcessAndProcess485_82CC0D58"

# Walk a chain to resolve a refcounted resource, constructing a 32-byte vector on failure (four hundred and eighty-sixth form). It takes a structure (a: fields at a->0 and a->4) and a value (b); it walks the chain to find the resource, and — when found — stores it in a, otherwise constructs a 32-byte vector and releases the refcounted entry. This is the "process this data" routine (four hundred and eighty-sixth form), part of the 0x83220 process cluster. 137 lines. 6 callees (WalkChainFind, Release_RefCounted, Allocate_OrDbgBreak, Construct32ByteVector, ProcessRefCount, and SetRefcountedWrapper). 3 callers (StateDispatch4, sub_832255B8, and sub_8322F808).
[entrypoint.functions."0x83220478"]
name = "ProcessAndProcessAndProcess486_83220478"

# Large processor that destructs and copies a structure, grows/appends data, and drives a state dispatch (four hundred and eighty-seventh form). It takes a structure (a: fields at a->20, a->12, and a->4); it destructs and frees the old state, copies a struct region, grows and appends to the buffer, releases the refcounted entry, and drives the state dispatch. This is the "process this data" routine (four hundred and eighty-seventh form), part of the 0x83225 process cluster. 227 lines. 5 callees (DestructAndFree2, ProcessAndCopyStructRegion, ProcessGrowAppend2, Release_RefCounted, and StateDispatch4). 1 caller (sub_8322F940).
[entrypoint.functions."0x83225760"]
name = "ProcessAndProcessAndProcess487_83225760"

# Large processor that destructs and copies a structure, grows/appends data, and does a lookup-and-construct (four hundred and eighty-eighth form). It takes a structure (a: fields at a->20, a->12, and a->4); it destructs and frees the old state, copies a struct region, grows and appends to the buffer, performs a lookup-and-construct, and releases the refcounted entry. This is the "process this data" routine (four hundred and eighty-eighth form), part of the 0x83225 process cluster. 223 lines. 5 callees (DestructAndFree2, ProcessAndCopyStructRegion, ProcessGrowAppend7, ProcessLookupConstruct, and Release_RefCounted). 1 caller (sub_8322F940).
[entrypoint.functions."0x83225BC0"]
name = "ProcessAndProcessAndProcess488_83225BC0"

# Large processor that destructs and copies a structure, constructs with a chain, and copies struct data (four hundred and eighty-ninth form). It takes a structure (a: fields at a->20, a->12, and a->4); it destructs and frees the old state, copies a struct region, constructs a new entry with a chain, copies struct data, and releases the refcounted entry. This is the "process this data" routine (four hundred and eighty-ninth form), part of the 0x83225 process cluster. 223 lines. 5 callees (DestructAndFree2, ProcessAndCopyStructRegion, ConstructWithChain, CopyStructData5, and Release_RefCounted). 1 caller (sub_8322F940).
[entrypoint.functions."0x83225FD0"]
name = "ProcessAndProcessAndProcess489_83225FD0"

# Large processor that destructs and copies a structure, resolves a reference, and releases a refcounted entry (four hundred and ninetieth form). It takes a structure (a: fields at a->20, a->12, and a->4); it destructs and frees the old state, copies a struct region, resolves a reference, and releases the refcounted entry. This is the "process this data" routine (four hundred and ninetieth form), part of the 0x83227 process cluster. 222 lines. 4 callees (DestructAndFree2, ProcessAndCopyStructRegion, Release_RefCounted, and ResolveRef). 1 caller (sub_8322F940).
[entrypoint.functions."0x832275C0"]
name = "ProcessAndProcessAndProcess490_832275C0"

# Large processor that performs a virtual dispatch on a structure and copies a struct region (four hundred and ninety-first form). It takes a structure (a: fields at a->0, a->20, a->12, and a->4); it issues a virtual call on the structure, then copies a struct region and processes the data. This is the "process this data" routine (four hundred and ninety-first form), part of the 0x8322D process cluster. 328 lines. 1 callee (ProcessAndCopyStructRegion). 1 caller (sub_8322F940).
[entrypoint.functions."0x8322D0B0"]
name = "ProcessAndProcessAndProcess491_8322D0B0"

# Dispatcher that drives one of several sibling processors based on the structure type (four hundred and ninety-second form). It takes a value (a); it sets up a local structure, compares strings (signed), and drives the appropriate processor based on the type. This is the "process this data" routine (four hundred and ninety-second form), part of the 0x8322F process cluster. 196 lines. 10 callees (ProcessAndProcessAndProcess143, CompareStringsSigned, DestructAndFree2, DestructRefCounted, ProcessAndCopyStructRegion, ProcessAndProcessAndProcess487, ProcessAndProcessAndProcess488, ProcessAndProcessAndProcess489, ProcessAndProcessAndProcess490, and ProcessAndProcessAndProcess491). 1 caller (ProcessAndProcessAndProcess211).
[entrypoint.functions."0x8322F940"]
name = "ProcessAndProcessAndProcess492_8322F940"

# Size-bucketed allocator that acquires a spin lock and allocates virtual memory (four hundred and ninety-third form). It takes a value (a); it acquires a spin lock, allocates a virtual memory region (with a computed size and alignment), and processes the allocation. This is the "process this data" routine (four hundred and ninety-third form), part of the 0x83230 process cluster. 97 lines. 3 callees (SpinLock_Acquire, VirtualAllocate, and ProcessAllocate). 1 caller (Allocate_SizeBucketed).
[entrypoint.functions."0x83230900"]
name = "ProcessAndProcessAndProcess493_83230900"

# Size-bucketed memory freer that iterates a list of allocations and releases virtual memory (four hundred and ninety-fourth form). It takes a structure (a: fields at a->24 and a->8); it iterates over the list of allocations, acquires a spin lock for each, decrements a counter, and frees the virtual memory region. This is the "process this data" routine (four hundred and ninety-fourth form), part of the 0x83230 process cluster. 109 lines. 3 callees (ProcessDecrement, SpinLock_Acquire, and VirtualFree). 2 callers (ProcessAndProcessAndProcess267 and sub_823058E8).
[entrypoint.functions."0x83230FA0"]
name = "ProcessAndProcessAndProcess494_83230FA0"

# The functions Under here happen when the trailer is playing when idle on main menu
# Check completion or progress by computing a percentage threshold and comparing values (a comparison routine). It takes a structure (a: fields at a->-228, a->-184, a->-188, and a->248) and a value (b); if the flag (a->-228) is zero it computes a progress percentage as 100*(a->-184+1)/(a->-188+1), and — if under 50% — returns the percentage minus one; otherwise it compares a->248 to b and returns the difference or zero. This is a progress/completion checker. 50 lines. 0 callees. 0 callers (unresolved — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x832B3C30"]
name = "CheckCompletionOrProgress_832B3C30"

# Issue a virtual call on a structure, then store a value in field 248 (a setter routine). It takes a structure (a: fields at a->-596, a->-244, and a->248) and a value (b); it issues a virtual call on the structure (via a->-244) and then stores b in a->248. This is a "set a field after a virtual call" routine. 46 lines. 0 callees (uses an indirect call). 0 callers (unresolved — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x832B3C88"]
name = "SetFieldAfterVirtualCall_832B3C88"

# Check a mutex-protected flag by locking the mutex and extracting a bit value (a flag-check routine). It takes a structure (a: fields at a->260 and a->264); if the pointer (a->260) is valid and the flag (a->264) is nonzero, it locks the mutex (MutexLock_WaitInfinite) and returns a specific bit from the flag; otherwise it returns 1. This is a "check a mutex-protected flag" routine. 53 lines. 1 callee (MutexLock_WaitInfinite). 0 callers (unresolved — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x832B3CF8"]
name = "CheckMutexFlag_832B3CF8"

# Unlock a mutex-protected flag by releasing the mutex (a mutex-unlock routine). It takes a structure (a: fields at a->260 and a->264); if the pointer (a->260) is valid and the flag (a->264) is nonzero, it releases the mutex (MutexUnlock_ReleaseMutant). This is the "unlock a mutex-protected flag" routine, counterpart to CheckMutexFlag. 19 lines. 1 callee (MutexUnlock_ReleaseMutant). 0 callers (unresolved — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x832B3D50"]
name = "UnlockMutexFlag_832B3D50"

# Large processor that issues a virtual call on a structure and drives initialization (four hundred and ninety-fifth form). It takes a structure (a: fields at a->44, a->32, a->80, and a->248); it checks the structure's state, and — when ready — issues a virtual call on a->248, then drives the initialization routine. This is the "process this data" routine (four hundred and ninety-fifth form), part of the 0x832B8 process cluster. 368 lines. 2 callees (ProcessAndInitializeAndProcess and sub_832B8168). 1 caller (sub_832B8960).
[entrypoint.functions."0x832B86F8"]
name = "ProcessAndProcessAndProcess495_832B86F8"

# Iterate a linked list to find a matching node and check a byte value (four hundred and ninety-sixth form). It takes a structure (a: field at a->136); it iterates over a global linked list, and for each node it searches for a match and checks a byte value. This is the "process this data" routine (four hundred and ninety-sixth form), part of the 0x82354 process cluster. 70 lines. 2 callees (ProcessAndSearchAndProcess10 and ByteValueCheck). 2 callers (sub_82355460 and sub_82354AE0).
[entrypoint.functions."0x82354A70"]
name = "ProcessAndProcessAndProcess496_82354A70"

# Process data within a critical section (four hundred and ninety-seventh form). It takes a structure (a: fields at a->164 and a->192); it enters a critical section, processes the data (setting thread affinity and searching), and leaves the critical section. This is the "process this data" routine (four hundred and ninety-seventh form), part of the 0x82354 process cluster. 71 lines. 5 callees (ProcessAndProcessAndProcess318, ProcessAndProcessAndProcess478, ProcessAndProcessAndProcess496, CriticalSection_EnterOrTry, and __imp__RtlLeaveCriticalSection). 0 callers (unresolved — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x82354AE0"]
name = "ProcessAndProcessAndProcess497_82354AE0"

# Originally Missed Functions
# Process two values with a buffer copy and global field update (four hundred and ninety-eighth form). It takes two values (a and b); it processes the values, performs a buffer copy/zero operation, and sets a global field. This is the "process this data" routine (four hundred and ninety-eighth form), part of the 0x82BF0 process cluster. 158 lines. 4 callees (ProcessAndProcessAndProcess356, sub_82BF2B68, Buffer_CopyOrZero_Error, and SetGlobalField_Thunk). 1 caller (ProcessAndProcessAndProcess259).
[entrypoint.functions."0x82BF09C0"]
name = "ProcessAndProcessAndProcess498_82BF09C0"

# Multiply-and-add in place on a 12-byte structure (a simple arithmetic routine). It takes a structure (a: fields at a->0, a->4, and a->8); it computes (a->0 * a->8) + a->4 and stores the result in a->8. This is a "multiply and add in place" routine. 20 lines. 0 callees. 1 caller (sub_82C60180).
[entrypoint.functions."0x82C627E0"]
name = "MultiplyAndAddInPlace_82C627E0"

# Compute a floating-point time difference and convert to an integer (a time-to-integer routine). It takes a floating-point value (a: f1); it compares a to a global threshold, and — if below — computes the difference from the threshold, otherwise computes the sum with another global, then converts the result to an integer. This is a "compute time difference as integer" routine. 39 lines. 0 callees. 1 caller (sub_82C60180).
[entrypoint.functions."0x82C60140"]
name = "ComputeTimeDifferenceAsInteger_82C60140"

# Interpolate between two floating-point arrays and store the result (a float interpolation routine). It takes a structure (a: fields at a->8, a->36, a->40, a->16, a->20, and a->12); if the count (a->8) is positive, it iterates over the range [a->36, a->40) and computes a weighted sum of two float arrays (a->16 and a->20), storing the interpolated result in a->12. This is a "interpolate float array" routine. 292 lines. 0 callees. 1 caller (sub_82C60180).
[entrypoint.functions."0x82C62548"]
name = "InterpolateFloatArray_82C62548"

# Process an entry array in a critical section with mutex protection (four hundred and ninety-ninth form). It takes a structure (a: fields at a->692, a->236, a->28, and a->704); it checks the structure's state, and — when ready — locks the mutex, processes the entry array in a critical section, and unlocks the mutex. This is the "process this data" routine (four hundred and ninety-ninth form), part of the 0x832B6 process cluster. 263 lines. 3 callees (CriticalSection_ProcessEntryArray, MutexLock_WaitInfinite, and MutexUnlock_ReleaseMutant). 1 caller (sub_82A3BC58).
[entrypoint.functions."0x832B6110"]
name = "ProcessAndProcessAndProcess499_832B6110"

# Issue a virtual call on a structure, then clear its fields (five hundredth form). It takes a structure (a: fields at a->80, a->96, and a->88); it issues a virtual call on a->80, then clears a->96 and a->88 to zero. This is the "process this data" routine (five hundredth form), part of the 0x822A2 process cluster. 66 lines. 1 callee (ProcessAndVcallAndClear3). 0 callers (unresolved — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x822A2A40"]
name = "ProcessAndProcessAndProcess500_822A2A40"

# Large processor that checks a structure's state and drives multiple sub-operations (five hundred and first form). It takes a structure (a: fields at a->129, a->160, a->84, and a->148) and two values (b and c); it checks the structure's state and drives the appropriate sub-operations based on the flags. This is the "process this data" routine (five hundred and first form), part of the 0x8221E process cluster. 310 lines. 3 callees (sub_821D7730, sub_8221D228, and sub_82A6EBA0). 12 callers (many from the 0x82A* cluster).
[entrypoint.functions."0x8221EC20"]
name = "ProcessAndProcessAndProcess501_8221EC20"

# Remove a node from a doubly-linked list and clean up its references (a list-removal routine). It takes a structure (a: fields at a->8, a->40, a->24, a->16, and a->12); it checks the node's state, and — if valid — unlinks the node from its prev/next pointers and clears the references. This is a "remove from linked list" routine. 80 lines. 0 callees. 8 callers (sub_8221EC20, sub_8221D118, sub_82181270, sub_82215978, sub_821815B0, sub_821AC058, sub_821D7730, and sub_82A6EBA0).
[entrypoint.functions."0x8221D228"]
name = "RemoveFromLinkedList_8221D228"

# Grow a buffer and append data, adjusting the size and capacity fields (a buffer-grow routine). It takes a structure (a: fields at a->12, a->4, a->8, and a->20); it computes the new size, and — if the buffer needs to grow — adjusts the size and capacity fields. This is a "grow buffer and append" routine. 122 lines. 0 callees. 12 callers (many from the 0x82A* and 0x822* clusters).
[entrypoint.functions."0x821FFC90"]
name = "GrowBufferAndAppend_821FFC90"

# Check a structure's state and drive the appropriate sub-operations (five hundred and second form). It takes a structure (a: fields at a->72, a->76, a->88, a->148, and a->152); it checks the structure's state, and — when ready — drives the appropriate sub-operations based on the flags. This is the "process this data" routine (five hundred and second form), part of the 0x82A62 process cluster. 148 lines. 2 callees (ProcessAndProcessAndProcess501 and sub_82A62CE0). 0 callers (unresolved — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x82A628A0"]
name = "ProcessAndProcessAndProcess502_82A628A0"

# Get a flag from a substructure, returning the byte value only when the guard byte is zero (a flag-getter routine). It takes a structure (a: field at a->16, which is a pointer to a sub-structure with bytes at +16 and +17); if the guard byte (a->16+17) is zero, it returns the flag byte (a->16+16), otherwise it returns 0. This is a "get flag from substructure" routine. 18 lines. 0 callees. 0 callers (unresolved — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x821DB8D0"]
name = "GetFlagFromSubstructure_821DB8D0"

# Atomically decrement a reference count, performing cleanup when it reaches zero (a refcount-decrement routine). It takes a structure (a: fields at a->4 and a->0); it uses atomic operations (lwarx/stwcx) to decrement the counter at a->4, and — when the counter reaches zero — performs additional cleanup based on the type field (a->0). This is a "decrement refcount atomically" routine. 88 lines. 2 callees (sub_821F6490 and sub_821FC1F0, recursive). 12 callers (many from the 0x832A* and 0x82* clusters).
[entrypoint.functions."0x821FC1F0"]
name = "DecrementRefcountAtomic_821FC1F0"

# Atomically increment a reference count, performing initialization when it reaches one (a refcount-increment routine). It takes a structure (a: fields at a->4 and a->0); it uses atomic operations (lwarx/stwcx) to increment the counter at a->4, and — when the counter reaches 1 — performs additional initialization based on the type field (a->0). This is a "increment refcount atomically" routine. 80 lines. 1 callee (sub_821FC048, recursive). 12 callers (many from the 0x82B* and 0x82* clusters).
[entrypoint.functions."0x821FC048"]
name = "IncrementRefcountAtomic_821FC048"

# Process a state transition by checking the state field and issuing a virtual call (a state-transition routine). It takes a structure (a: fields at a->0, a->4, a->20, a->21, and a->16); it checks the state field (a->21), and — when the state is valid — issues a virtual call based on the type. This is a "process state transition" routine. 92 lines. 0 callees (uses an indirect call). 12 callers (many from the 0x82B* and 0x82* clusters).
[entrypoint.functions."0x8221E680"]
name = "ProcessStateTransition_8221E680"

# Yield execution and check a value against a threshold, returning a bit (a yield-and-check routine). It takes a value (a); it yields execution (NtYieldExecution), then compares a to a threshold (16420), and returns a bit value based on the comparison. This is a "yield and check threshold" routine. 36 lines. 1 callee (__imp__NtYieldExecution). 10 callers (many from the 0x82B* and 0x82* clusters).
[entrypoint.functions."0x82CBD098"]
name = "YieldAndCheckThreshold_82CBD098"

# Check a structure's state and drive the appropriate sub-operations (five hundred and third form). It takes a structure (a: fields at a->32, a->8, and a->52) and two values (b and c); it checks the structure's state, and — when ready — drives the appropriate sub-operations based on the flags. This is the "process this data" routine (five hundred and third form), part of the 0x8222A process cluster. 171 lines. 2 callees (sub_821A5A30 and sub_82CBC6B0). 0 callers (unresolved — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x8222AC38"]
name = "ProcessAndProcessAndProcess503_8222AC38"

# Thunk that calls KeQuerySystemTime to get the current system time (a system-time thunk). It takes no arguments and simply calls __imp__KeQuerySystemTime. This is a "query system time" thunk. 7 lines. 1 callee (__imp__KeQuerySystemTime). 5 callers (sub_83000E70, sub_821882B0, sub_83003018, sub_82CA4248, and sub_830038E0).
[entrypoint.functions."0x821882A8"]
name = "QuerySystemTime_Thunk_821882A8"

# Get the current time in milliseconds by querying the system time and converting (a time-conversion routine). It takes no arguments; it queries the system time (KeQuerySystemTime), computes the time difference from a reference, and divides by 10000 to convert to milliseconds. This is a "get current time in milliseconds" routine. 46 lines. 1 callee (QuerySystemTime_Thunk). 11 callers (many from the 0x82C* and 0x82* clusters).
[entrypoint.functions."0x821882B0"]
name = "GetCurrentTimeMillis_821882B0"

# Large processor that performs complex floating-point operations (five hundred and fourth form). It takes two floating-point values (a and b: f1 and f2); it performs complex floating-point operations, comparing the values to global thresholds and driving the appropriate sub-operations based on the results. This is the "process this data" routine (five hundred and fourth form), part of the 0x821FE process cluster. 733 lines. 7 callees (sub_821F3C80, sub_821FE2F0, sub_821FE308, sub_821FE868, sub_821FE890, sub_82CA3D78, and sub_82CA40C0). 12 callers (many from the 0x82A* and 0x82* clusters).
[entrypoint.functions."0x821FE378"]
name = "ProcessAndProcessAndProcess504_821FE378"

# Check a specific bit in a flag byte, returning 0 or 1 (a flag-bit-check routine). It takes a structure (a: field at a->44); it checks a specific bit in the flag byte (a->44) and returns 0 or 1. This is a "check flag bit" routine. 12 lines. 0 callees. 0 callers (unresolved — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x82185418"]
name = "CheckFlagBit_82185418"

# Poll the OS notification queue (XNotifyGetNext) and process pending system events. Checks a flag first, then retrieves and dispatches notifications. Called from the main game loop. 109 lines. 6 callees. 3 callers.
[entrypoint.functions."0x8229A9D8"]
name = "PollAndProcessOsNotifications_8229A9D8"

# Iterate a linked list within a critical section, driving sub-operations for each node (five hundred and sixth form). It takes a structure (a: fields at a->12, a->16, and a->4); it enters a critical section, iterates the linked list, and drives the appropriate sub-operations for each node. This is the "process this data" routine (five hundred and sixth form), part of the 0x821DB process cluster. 183 lines. 5 callees (sub_822C5A18, sub_822A2948, sub_822606B0, CriticalSection_EnterOrTry, and __imp__RtlLeaveCriticalSection). 1 caller (ProcessAndProcessAndProcess505).
[entrypoint.functions."0x821DB798"]
name = "ProcessAndProcessAndProcess506_821DB798"

# Simple thunk that forwards to sub_82BFD850 (a forwarding thunk). It takes no arguments and simply calls sub_82BFD850. This is a "forward to another function" thunk. 7 lines. 1 callee (sub_82BFD850). 1 caller (ProcessAndProcessAndProcess506).
[entrypoint.functions."0x822C5A18"]
name = "ThunkTo_82BFD850_822C5A18"

# Return the byte at offset 8 of a structure (a byte-getter routine). It takes a structure (a: field at a->8) and returns the byte value at a->8. This is a "get byte at offset" routine. 7 lines. 0 callees. 0 callers (unresolved — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x8217FD88"]
name = "GetByteAtOffset8_8217FD88"

# Process notifications by checking a notification type and handling it (five hundred and seventh form). It takes a structure (a: field at a->4); it checks the notification type, and — when valid — processes the notification using XNotifyGetNext. This is the "process this data" routine (five hundred and seventh form), part of the 0x82185 process cluster. 92 lines. 1 callee (__imp__XNotifyGetNext). 0 callers (unresolved — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x82185228"]
name = "ProcessAndProcessAndProcess507_82185228"

# Iterate an array of pointers and issue a virtual call on each one (five hundred and eighth form). It takes a structure (a: array of pointers at a->0, a->4, a->8, ...); it iterates over the array (up to 6 entries), and for each valid pointer, issues a virtual call. This is the "process this data" routine (five hundred and eighth form), part of the 0x822A2 process cluster. 148 lines. 0 callees (uses indirect calls). 4 callers (sub_829F8D30, ProcessAndProcessAndProcess506, sub_829F8028, and sub_829F7F88). Strong override in fable2_text_probe.h.
[entrypoint.functions."0x822A2948"]
name = "ProcessAndProcessAndProcess508_822A2948"

# Return the byte at offset 96 of a structure (a byte-getter routine). It takes a structure (a: field at a->96) and returns the byte value at a->96. This is a "get byte at offset" routine. 7 lines. 0 callees. 0 callers (unresolved — body is recovered but no callers found; likely a vtable method or indirect entry).
[entrypoint.functions."0x822A9360"]
name = "GetByteAtOffset96_822A9360"

# Return the byte at offset 4 of a structure (a byte-getter routine). It takes a structure (a: field at a->4) and returns the byte value at a->4. This is a "get byte at offset" routine. 8 lines. 0 callees. 7 callers (many from the 0x82EE* cluster).
[entrypoint.functions."0x829F9C40"]
name = "GetByteAtOffset4_829F9C40"

# Simple thunk that forwards to UIText_FrameRender (a UI text rendering thunk). It takes no arguments and simply calls UIText_FrameRender. This is a "forward to UI text render" thunk. 7 lines. 1 callee (UIText_FrameRender). 1 caller (ThunkTo_82BFD850_822C5A18). Strong override in fable2_ui_render_probe.h.
[entrypoint.functions."0x82BFD850"]
name = "ThunkTo_UIText_FrameRender_82BFD850"

# Large processor that performs complex vector operations on a structure (five hundred and ninth form). It takes a value (a) and a structure (b: fields at b->0, b->4, and b->8); it performs complex vector operations on the structure, zeroing out various fields and processing the data. This is the "process this data" routine (five hundred and ninth form), part of the 0x832C1 process cluster. 1501 lines. 0 callees. 1 caller (sub_832BB018).
[entrypoint.functions."0x832C13A8"]
name = "ProcessAndProcessAndProcess509_832C13A8"

# Process two structures by checking a count and driving sub-operations (five hundred and tenth form). It takes a value (a), a structure (b: fields at b->3136 and b->48), and a structure (c: fields at c->48, c->32, c->44, c->28, c->36, and c->40); it checks the count, and — when valid — processes the structures and drives the appropriate sub-operations. This is the "process this data" routine (five hundred and tenth form), part of the 0x821B7 process cluster. 207 lines. 1 callee (sub_82B9B340). 12 callers (many from the 0x82* and 0x82A* clusters).
[entrypoint.functions."0x821B7020"]
name = "ProcessAndProcessAndProcess510_821B7020"

# Dispatch by type code, setting values in two output structures based on the code (a type-dispatch routine). It takes a type code (a), and two output structures (b: field at b->0, and c: field at c->0); it dispatches on the type code and sets the output values accordingly. This is a "dispatch by type code" routine. 87 lines. 0 callees. 10 callers (many from the 0x821F* and 0x8300* clusters).
[entrypoint.functions."0x821FAFF8"]
name = "DispatchByTypeCode_821FAFF8"

# Process a structure by checking its fields and performing floating-point computation (five hundred and eleventh form). It takes a structure (a: fields at a->44, a->52, a->60, a->0, a->56, a->48, and a->4); it checks the structure's fields, and — when valid — performs a floating-point computation based on the values. This is the "process this data" routine (five hundred and eleventh form), part of the 0x82C61 process cluster. 103 lines. 0 callees. 1 caller (sub_82C62210).
[entrypoint.functions."0x82C61898"]
name = "ProcessAndProcessAndProcess511_82C61898"

# Process a float array by iterating over its elements and performing float operations (five hundred and twelfth form). It takes a value (a, a count), a structure (b: fields at b->24, b->12, b->28, and b->20), a pointer to a float (c), and a pointer to a float (d); it iterates over the float array (up to a entries), performing float operations on each element. This is the "process this data" routine (five hundred and twelfth form), part of the 0x82C61 process cluster. 270 lines. 0 callees. 1 caller (sub_82C62210).
[entrypoint.functions."0x82C61950"]
name = "ProcessAndProcessAndProcess512_82C61950"

# Process a structure and drive several sub-operations on a float array (five hundred and thirteenth form). It takes a structure (a: fields at a->44, a->40, and a->48), a pointer to a float (b), a pointer to a float (c), and a value (d, a count); it checks the structure's fields, then drives several sub-operations (including ProcessAndProcessAndProcess511, sub_82C605D8, sub_82C61888, ProcessAndProcessAndProcess512, sub_82C61B28, sub_82C61D78, and sub_82C61FC8) on the float array. This is the "process this data" routine (five hundred and thirteenth form), part of the 0x82C62 process cluster. 267 lines. 6 callees. 1 caller (sub_82C5F810).
[entrypoint.functions."0x82C62210"]
name = "ProcessAndProcessAndProcess513_82C62210"

# Dispatch by type code, setting a value based on the structure's type (a type-dispatch routine, second form). It takes a structure (a: fields at a->0, a->48, and a->32); it reads the type code from the structure and sets a value based on the type. This is a "dispatch by type code" routine (second form), part of the 0x821FA type-dispatch cluster. 81 lines. 0 callees. 10 callers (many from the 0x821F*, 0x82D*, and 0x8300* clusters).
[entrypoint.functions."0x821FABA0"]
name = "DispatchByTypeCode2_821FABA0"

# Process multiple structures by dispatching on a type code (five hundred and fourteenth form). It takes several structures (a: at a->0, b: at b->0, c: at c->0, d: at d->0, e: at e->0), a value (f, a count), and a structure (g); it dispatches on a type code (via DispatchByTypeCode), then processes the structures based on the result. This is the "process this data" routine (five hundred and fourteenth form), part of the 0x821FB process cluster. 163 lines. 1 callee (DispatchByTypeCode). 7 callers (many from the 0x821F* and 0x8300* clusters).
[entrypoint.functions."0x821FB090"]
name = "ProcessAndProcessAndProcess514_821FB090"

# Process a value and a float by performing float computation and writing to a fixed address (five hundred and fifteenth form). It takes a value (a, a size/count) and a float (f1); it performs float computation based on the size, and writes the result to a fixed address in memory. This is the "process this data" routine (five hundred and fifteenth form), part of the 0x821E8 process cluster. 116 lines. 0 callees. 12 callers (many from the 0x821*, 0x822*, and 0x82A* clusters).
[entrypoint.functions."0x821E8AD8"]
name = "ProcessAndProcessAndProcess515_821E8AD8"

# Process vectors by performing vector operations on multiple structures (five hundred and sixteenth form). It takes several structures (a, b, c, d, e, f); it performs vector operations on the structures, and drives several sub-operations (sub_8321E6A0, sub_8321F598, and sub_83223220). This is the "process this data" routine (five hundred and sixteenth form), part of the 0x83229 process cluster. 238 lines. 3 callees. 8 callers (many from the 0x8226*, 0x829F*, and 0x82A0* clusters).
[entrypoint.functions."0x83229378"]
name = "ProcessAndProcessAndProcess516_83229378"

# Check a float against a threshold, returning a bit (a float-threshold check routine). It takes a structure (a: fields at a->368 and a->444), a value (b), and a float (f1); it checks the structure's fields, and — when valid — compares a float against a threshold and returns a bit value based on the comparison. This is a "check float threshold" routine. 79 lines. 0 callees. 7 callers (many from the 0x82A0* and 0x821C* clusters).
[entrypoint.functions."0x83226218"]
name = "CheckFloatThreshold_83226218"

# Process a structure by performing float operations on its elements (five hundred and seventeenth form). It takes a structure (a: field at a->100), a value (b, an offset), and a value (c); it performs float operations on the structure's elements based on the offset. This is the "process this data" routine (five hundred and seventeenth form), part of the 0x8321E process cluster. 144 lines. 0 callees. 12 callers (many from the 0x8226*, 0x8227*, 0x82A0*, 0x8322*, and 0x8323* clusters).
[entrypoint.functions."0x8321E6A0"]
name = "ProcessAndProcessAndProcess517_8321E6A0"

# Process vectors by performing vector operations on a data region (five hundred and eighteenth form). It takes a value (a, an offset), a pointer (b), and a value (c); it performs vector operations on the data region at the specified offset. This is the "process this data" routine (five hundred and eighteenth form), part of the 0x8321C process cluster. 226 lines. 0 callees. 1 caller (sub_82269BB8).
[entrypoint.functions."0x8321C518"]
name = "ProcessAndProcessAndProcess518_8321C518"

# Process a structure by finding a tree leaf and driving sub-operations (five hundred and nineteenth form). It takes a structure (a), a structure (b: fields at b->472 and b->464), and a structure (c: field at c->80); it finds a tree leaf (via FindTreeLeaf), then drives sub-operations (sub_8321E160) based on the result. This is the "process this data" routine (five hundred and nineteenth form), part of the 0x8321C process cluster. 97 lines. 2 callees. 1 caller (sub_82269FA0).
[entrypoint.functions."0x8321CB98"]
name = "ProcessAndProcessAndProcess519_8321CB98"

# Issue a virtual call after checking two values (a virtual-call wrapper). It takes a structure (a), a value (b), and a value (c); it checks the values, and — when valid — issues a virtual call via the structure's vtable. This is a "virtual call with check" routine. 27 lines. 0 callees. 0 callers (likely called via a vtable or function pointer).
[entrypoint.functions."0x82227130"]
name = "VirtualCallWithCheck_82227130"

# Process a structure by issuing virtual calls on it (five hundred and twentieth form). It takes a structure (a) and a structure (b: fields at b->124, b->251, b->0, and b->228); it issues virtual calls on the structures, processing their state. This is the "process this data" routine (five hundred and twentieth form), part of the 0x82227 process cluster. 172 lines. 0 callees (uses indirect calls). 0 callers (likely called via a vtable or function pointer).
[entrypoint.functions."0x82227160"]
name = "ProcessAndProcessAndProcess520_82227160"

# Issue a virtual call after checking two values (a virtual-call wrapper, second form). It takes a structure (a) and a value (b); it checks the values, and — when valid — issues a virtual call via the structure's vtable. This is a "virtual call with check" routine (second form). 25 lines. 0 callees. 0 callers (likely called via a vtable or function pointer).
[entrypoint.functions."0x82227290"]
name = "VirtualCallWithCheck2_82227290"

# Process a structure by issuing virtual calls and driving sub-operations (five hundred and twenty-first form). It takes a structure (a: fields at a->0 and a->136); it issues virtual calls on the structure, and drives sub-operations (sub_821C96B0 and sub_8226EA00) based on the result. This is the "process this data" routine (five hundred and twenty-first form), part of the 0x82227 process cluster. 277 lines. 2 callees. 0 callers (likely called via a vtable or function pointer).
[entrypoint.functions."0x822272C0"]
name = "ProcessAndProcessAndProcess521_822272C0"

# Process a bitmask by performing bit manipulation on its set bits (five hundred and twenty-second form). It takes a structure (a: fields at a->48 and a->52), a value (b, a bitmask), a value (c), and a value (d); it performs bit manipulation on the bitmask, processing each set bit. This is the "process this data" routine (five hundred and twenty-second form), part of the 0x8221C process cluster. 118 lines. 1 callee (sub_8228EAF0). 10 callers (many from the 0x822* and 0x82BA* clusters).
[entrypoint.functions."0x8221C908"]
name = "ProcessAndProcessAndProcess522_8221C908"

# Process timing and state by performing delay operations and driving sub-operations (five hundred and twenty-third form). It takes a structure (a: fields at a->0 and a->8); it performs delay operations (db16cyc), checks state flags, and drives sub-operations (GetGlobalField and sub_82BA0E80). This is the "process this data" routine (five hundred and twenty-third form), part of the 0x82B9B process cluster. 119 lines. 2 callees. 6 callers (including MainRenderLoop and sub_82242628).
[entrypoint.functions."0x82B9BF90"]
name = "ProcessAndProcessAndProcess523_82B9BF90"

# Process a structure by driving refcount operations and sub-operations (five hundred and twenty-fourth form). It takes a structure (a: field at a->4); it checks the structure's state, and drives several sub-operations (DecrementRefcountAtomic, Free_SizeBucketed_Thunk, IncrementRefcountAtomic, sub_821F6140, sub_82B83FA8, and sub_82B9B9F0) based on the result. This is the "process this data" routine (five hundred and twenty-fourth form), part of the 0x821FB process cluster. 142 lines. 6 callees. 12 callers (many from the 0x821F*, 0x82A*, 0x82B*, and 0x832A* clusters).
[entrypoint.functions."0x821FBED8"]
name = "ProcessAndProcessAndProcess524_821FBED8"

# Process a value and a vector by performing float operations and writing to a fixed address (five hundred and twenty-fifth form). It takes a value (a, a size/count) and a vector (v1); it performs float operations based on the size, and writes the result to a fixed address in memory. This is the "process this data" routine (five hundred and twenty-fifth form), part of the 0x821EA process cluster. 118 lines. 0 callees. 12 callers (many from the 0x821* and 0x82A* clusters).
[entrypoint.functions."0x821EAF90"]
name = "ProcessAndProcessAndProcess525_821EAF90"

# Process a structure by driving a state transition (five hundred and twenty-sixth form). It takes a structure (a: fields at a->8196 and a->8192); it processes the structure's state, and drives a state transition (via ProcessStateTransition). This is the "process this data" routine (five hundred and twenty-sixth form), part of the 0x8221F process cluster. 124 lines. 1 callee (ProcessStateTransition). 12 callers (many from the 0x821*, 0x822*, and 0x82A* clusters).
[entrypoint.functions."0x8221F478"]
name = "ProcessAndProcessAndProcess526_8221F478"

# Process a structure by driving sub-operations based on its state (five hundred and twenty-seventh form). It takes a structure (a: field at a->10896), a value (b), and a value (c); it checks the structure's state, and drives sub-operations (sub_82B9BEC8 and sub_82B9BF90) based on the result. This is the "process this data" routine (five hundred and twenty-seventh form), part of the 0x82206 process cluster. 107 lines. 2 callees. 1 caller (sub_822065F0).
[entrypoint.functions."0x82206538"]
name = "ProcessAndProcessAndProcess527_82206538"

# Process a structure by checking its state and driving sub-operations (five hundred and twenty-eighth form). It takes a structure (a: fields at a->14916, a->52, a->14908, and a->14920), a value (b, a flag), a structure (c), and a value (d); it checks the structure's state, and drives sub-operations (sub_82206538) based on the result. This is the "process this data" routine (five hundred and twenty-eighth form), part of the 0x82206 process cluster. 311 lines. 1 callee. 2 callers (sub_82206820 and sub_821D17B8).
[entrypoint.functions."0x822065F0"]
name = "ProcessAndProcessAndProcess528_822065F0"

# Process a structure under a critical section by driving sub-operations (five hundred and twenty-ninth form). It takes a structure (a: fields at a->10941 and a->13516), a value (b), and a value (c); it checks the structure's state, enters a critical section, and drives sub-operations (sub_822065F0) based on the result. This is the "process this data" routine (five hundred and twenty-ninth form), part of the 0x82206 process cluster. 234 lines. 4 callees. 12 callers (many from the 0x821*, 0x822*, 0x82A*, and 0x82B* clusters).
[entrypoint.functions."0x82206820"]
name = "ProcessAndProcessAndProcess529_82206820"

# Process a structure by checking its state and driving sub-operations (five hundred and thirtieth form). It takes a structure (a: fields at a->176, a->208, a->184, and a->180); it checks the structure's state, and drives sub-operations (ComputeVectorHash and sub_83222700) based on the result. This is the "process this data" routine (five hundred and thirtieth form), part of the 0x821D9 process cluster. 137 lines. 2 callees. 2 callers (sub_83222D78 and sub_83222CA8).
[entrypoint.functions."0x821D9D28"]
name = "ProcessAndProcessAndProcess530_821D9D28"

# Process vectors by performing vector operations on a data region (five hundred and thirty-first form). It takes a structure (a), a structure (b: field at b->120), a value (c), and a value (d); it performs vector operations on the data region, processing 128-byte blocks. This is the "process this data" routine (five hundred and thirty-first form), part of the 0x8221A process cluster. 138 lines. 0 callees. 12 callers (many from the 0x821*, 0x822*, 0x82A*, and 0x8304* clusters).
[entrypoint.functions."0x8221A9B0"]
name = "ProcessAndProcessAndProcess531_8221A9B0"

# Check a float-to-integer conversion, returning a status code (a float conversion check routine). It takes a float (f1); it attempts to convert the float to an integer, checking for precision loss and overflow, and returns a status code (0, 1, or 2) based on the result. This is a "check float to int conversion" routine. 65 lines. 0 callees. 2 callers (ProcessAndProcessAndProcess504 and sub_82CA40C0).
[entrypoint.functions."0x821FE308"]
name = "CheckFloatToIntConversion_821FE308"

# Process a bitmask by performing bit manipulation on its set bits (five hundred and thirty-second form). It takes a structure (a: fields at a->48 and a->52), a value (b, a bitmask), a value (c), and a value (d); it performs bit manipulation on the bitmask, processing each set bit. This is the "process this data" routine (five hundred and thirty-second form), part of the 0x8221D process cluster. 211 lines. 1 callee (sub_8228EAF0). 6 callers (many from the 0x822* cluster).
[entrypoint.functions."0x8221DF98"]
name = "ProcessAndProcessAndProcess532_8221DF98"

# Dispatch by type code, setting values in two output structures based on the code (a type-dispatch routine, third form). It takes a structure (a: fields at a->40, a->48, and a->36), and two output structures (b: field at b->0, and c: field at c->0); it dispatches on the type code and sets the output values accordingly. This is a "dispatch by type code" routine (third form), part of the 0x821FA type-dispatch cluster. 84 lines. 0 callees. 6 callers (many from the 0x821F*, 0x8300*, and 0x82B* clusters).
[entrypoint.functions."0x821FAA08"]
name = "DispatchByTypeCode3_821FAA08"

# Process a structure by dispatching on a type code and driving sub-operations (five hundred and thirty-third form). It takes a structure (a: fields at a->40, a->28, and a->32) and a value (b); it dispatches on a type code (via DispatchByTypeCode2), then drives sub-operations (sub_821FAC30) based on the result. This is the "process this data" routine (five hundred and thirty-third form), part of the 0x821FA process cluster. 137 lines. 2 callees. 12 callers (many from the 0x822*, 0x82A*, and 0x82B* clusters).
[entrypoint.functions."0x821FAAA0"]
name = "ProcessAndProcessAndProcess533_821FAAA0"

# Process a structure by dispatching on type codes and driving sub-operations (five hundred and thirty-fourth form). It takes a structure (a: field at a->32) and six values (b, c, d, e, f, g); it dispatches on type codes (via DispatchByTypeCode2, DispatchByTypeCode, and sub_821FAA08), then drives sub-operations (ProcessAndProcessAndProcess514) based on the results. This is the "process this data" routine (five hundred and thirty-fourth form), part of the 0x821FA process cluster. 531 lines. 4 callees. 2 callers (sub_821FAAA0 and sub_82B91E58).
[entrypoint.functions."0x821FAC30"]
name = "ProcessAndProcessAndProcess534_821FAC30"

# Process a structure by reading multiple 16-bit fields and combining them (five hundred and thirty-fifth form). It takes a structure (a: fields at a->16, a->32, a->48, a->64, a->80, a->96, and a->112); it reads multiple 16-bit fields from the structure, sign-extends them, and combines them using bitwise OR. This is the "process this data" routine (five hundred and thirty-fifth form), part of the 0x832C0 process cluster. 439 lines. 0 callees. 1 caller (sub_832BB018).
[entrypoint.functions."0x832C0998"]
name = "ProcessAndProcessAndProcess535_832C0998"

# Process two structures by performing bit shifting and OR operations (five hundred and thirty-sixth form). It takes a structure (a: fields at a->0, a->4, and a->40), a structure (b: fields at b->8, b->0, and b->4), and a value (c); it performs bit shifting and OR operations on the structures, combining their bit fields. This is the "process this data" routine (five hundred and thirty-sixth form), part of the 0x832B9 process cluster. 419 lines. 1 callee (MemSet_SdkRuntime). 1 caller (sub_832BB018).
[entrypoint.functions."0x832B9DB8"]
name = "ProcessAndProcessAndProcess536_832B9DB8"

# Process two structures by performing bit shifting and OR operations (five hundred and thirty-seventh form). It takes a structure (a: fields at a->0, a->4, and a->40), a structure (b: fields at b->8, b->0, and b->4), and a value (c); it performs bit shifting and OR operations on the structures, combining their bit fields. This is the "process this data" routine (five hundred and thirty-seventh form), part of the 0x832BA process cluster. 364 lines. 1 callee (MemSet_SdkRuntime). 1 caller (sub_832BB018).
[entrypoint.functions."0x832BAA28"]
name = "ProcessAndProcessAndProcess537_832BAA28"

# Process two structures by performing bit shifting and AND/OR operations (five hundred and thirty-eighth form). It takes a structure (a: fields at a->0, a->4, and a->40) and a structure (b: fields at b->8, b->0, and b->4); it performs bit shifting and AND/OR operations on the structures, combining their bit fields. This is the "process this data" routine (five hundred and thirty-eighth form), part of the 0x832BA process cluster. 477 lines. 0 callees. 1 caller (sub_832BB018).
[entrypoint.functions."0x832BACB8"]
name = "ProcessAndProcessAndProcess538_832BACB8"

# Process a structure by reading multiple 16-bit fields and combining them (five hundred and thirty-ninth form). It takes a value (a), a value (b, an offset), and a structure (c: fields at c->16, c->32, c->48, c->64, c->80, c->96, and c->112); it reads multiple 16-bit fields from the structure, sign-extends them, and combines them using bitwise OR. This is the "process this data" routine (five hundred and thirty-ninth form), part of the 0x832C0 process cluster. 485 lines. 0 callees. 1 caller (sub_832BB018).
[entrypoint.functions."0x832C0CB8"]
name = "ProcessAndProcessAndProcess539_832C0CB8"

# Process a structure by performing atomic operations on its fields (five hundred and fortieth form). It takes a structure (a: fields at a->4 and a->0) and a structure (b: field at b->0); it reads the structure's fields, performs atomic increment operations on a counter, and stores the result. This is the "process this data" routine (five hundred and fortieth form), part of the 0x821CC process cluster. 81 lines. 1 callee (sub_82B6A928). 6 callers (many from the 0x821*, 0x82A*, and 0x82B* clusters).
[entrypoint.functions."0x821CC7D8"]
name = "ProcessAndProcessAndProcess540_821CC7D8"

# Process a structure by driving sub-operations and freeing memory (five hundred and forty-first form). It takes a structure (a: fields at a->68 and a->72); it checks the structure's state, drives sub-operations (sub_82B6A9B0), and frees memory (via Free_SizeBucketed_Thunk). This is the "process this data" routine (five hundred and forty-first form), part of the 0x82B6A process cluster. 76 lines. 2 callees. 1 caller (sub_821CC7D8).
[entrypoint.functions."0x82B6A928"]
name = "ProcessAndProcessAndProcess541_82B6A928"

# Process a structure by reading multiple 16-bit fields and combining them (five hundred and forty-second form). It takes a structure (a: fields at a->16, a->32, a->48, a->64, a->80, a->96, and a->112) and a value (b, an offset); it reads multiple 16-bit fields from the structure, sign-extends them, and combines them using bitwise OR. This is the "process this data" routine (five hundred and forty-second form), part of the 0x832C1 process cluster. 485 lines. 0 callees. 1 caller (sub_832BB018).
[entrypoint.functions."0x832C1030"]
name = "ProcessAndProcessAndProcess542_832C1030"

# Check a timeout condition by comparing the current time against stored timestamps (a timeout check routine). It takes a structure (a: fields at a->24, a->28, and a->32); it checks the structure's state, and — when valid — compares the current time (via GetCurrentTimeMillis) against stored timestamps to determine if a timeout has occurred. This is a "check timeout condition" routine. 100 lines. 1 callee (GetCurrentTimeMillis). 1 caller (sub_82C08310).
[entrypoint.functions."0x82C08010"]
name = "CheckTimeoutCondition_82C08010"

# Return the constant value 5 (a trivial constant-return function). It takes no arguments and simply returns 5. This is a "return constant 5" routine. 4 lines. 0 callees. 0 callers.
[entrypoint.functions."0x8269B5C8"]
name = "ReturnConstant5_8269B5C8"

# Process a structure under a critical section by updating its state (five hundred and forty-third form). It takes a structure (a: fields at a->24, a->8, a->68, a->60, a->12, a->64, a->28, and a->16); it enters a critical section, updates the structure's state, and leaves the critical section. This is the "process this data" routine (five hundred and forty-third form), part of the 0x82294 process cluster. 96 lines. 2 callees. 3 callers (sub_822A8E78, sub_822E0508, and sub_8222A6D8).
[entrypoint.functions."0x822943E8"]
name = "ProcessAndProcessAndProcess543_822943E8"

# Process a structure by checking its state and driving sub-operations (five hundred and forty-fourth form). It takes a structure (a: fields at a->12, a->16, and a->28); it checks the structure's state, and — when valid — drives sub-operations (sub_822943E8) based on the result. This is the "process this data" routine (five hundred and forty-fourth form), part of the 0x822A8 process cluster. 80 lines. 1 callee. 1 caller (sub_822E0508).
[entrypoint.functions."0x822A8E78"]
name = "ProcessAndProcessAndProcess544_822A8E78"

# Process vectors by performing vector addition operations on multiple data regions (five hundred and forty-fifth form). It takes a pointer (a), a value (b), a pointer (c), a pointer (d), and a value (e); it performs vector addition operations on the data regions, computing sums. This is the "process this data" routine (five hundred and forty-fifth form), part of the 0x82CD8 process cluster. 144 lines. 0 callees. 1 caller (sub_82CD8F58).
[entrypoint.functions."0x82CD8E38"]
name = "ProcessAndProcessAndProcess545_82CD8E38"

# Process a structure by dispatching on its type (five hundred and forty-sixth form). It takes a structure (a: field at a->0) and a value (b); it dispatches on the structure's type (0-4), processing each case differently. This is the "process this data" routine (five hundred and forty-sixth form), part of the 0x82C5E process cluster. 608 lines. 1 callee (sub_82C5E698). 1 caller (sub_82C5F810).
[entrypoint.functions."0x82C5ECE0"]
name = "ProcessAndProcessAndProcess546_82C5ECE0"

# Process a float array by iterating over its elements and performing float operations (five hundred and forty-seventh form). It takes a structure (a: fields at a->8 and a->0), a pointer (b), and a value (c, a count); it iterates over the float array (up to c entries), performing float operations on each element. This is the "process this data" routine (five hundred and forty-seventh form), part of the 0x82C61 process cluster. 365 lines. 0 callees. 1 caller (sub_82C5F810).
[entrypoint.functions."0x82C613D0"]
name = "ProcessAndProcessAndProcess547_82C613D0"

# Process a structure by driving several sub-operations (five hundred and forty-eighth form). It takes a structure (a: field at a->10940), a structure (b: fields at b->20 and b->16), and a value (c); it checks the structure's state, and drives several sub-operations (MemCpy_SdkRuntime, sub_821D11C8, sub_821E8D20, and sub_82213C10) based on the result. This is the "process this data" routine (five hundred and forty-eighth form), part of the 0x8222B process cluster. 191 lines. 5 callees. 1 caller (sub_8221B140).
[entrypoint.functions."0x8222BFF8"]
name = "ProcessAndProcessAndProcess548_8222BFF8"

# Process a value by driving several sub-operations (five hundred and forty-ninth form). It takes a value (a); it checks the value against a global, and drives several sub-operations (sub_82208CE8, sub_82221680, sub_822228F0, and sub_82B7E050) based on the result. This is the "process this data" routine (five hundred and forty-ninth form), part of the 0x82208 process cluster. 94 lines. 4 callees. 12 callers (many from the 0x821*, 0x822*, 0x82A*, and 0x82B* clusters).
[entrypoint.functions."0x82208C48"]
name = "ProcessAndProcessAndProcess549_82208C48"

# Process a structure by driving several sub-operations and copying data (five hundred and fiftieth form). It takes a structure (a: fields at a->240 and a->236) and a pointer (b); it drives several sub-operations (sub_821940C8, sub_821C0210, and sub_821D9D28), and copies data. This is the "process this data" routine (five hundred and fiftieth form), part of the 0x83222 process cluster. 130 lines. 3 callees. 12 callers (many from the 0x8322* and 0x8323* clusters).
[entrypoint.functions."0x83222D78"]
name = "ProcessAndProcessAndProcess550_83222D78"

# Process a structure by iterating over an array and driving sub-operations (five hundred and fifty-first form). It takes a structure (a: field at a->0); it checks the structure's state, and — when valid — iterates over an array of 2 entries (8200 bytes apart), driving sub-operations (sub_8221F478) on each entry. This is the "process this data" routine (five hundred and fifty-first form), part of the 0x8221F process cluster. 60 lines. 1 callee. 12 callers (many from the 0x821*, 0x822*, 0x82A*, and 0x82B* clusters).
[entrypoint.functions."0x8221F410"]
name = "ProcessAndProcessAndProcess551_8221F410"

# Modify a float's precision by masking its mantissa bits (a float precision modifier). It takes a float (f1) and a value (b, an offset); it masks the float's mantissa bits to reduce its precision, and returns the modified float. This is a "modify float precision" routine. 26 lines. 0 callees. 4 callers (ProcessAndProcessAndProcess504, sub_82CA7568, sub_82CA77D0, and sub_8227A818).
[entrypoint.functions."0x821FE868"]
name = "ModifyFloatPrecision_821FE868"

# Check a float's properties (NaN, infinity, precision, sign) and return status codes (a float properties check routine). It takes a float (f1); it checks the float's properties (NaN, infinity, precision, and sign), and returns status codes based on the results. This is a "check float properties" routine. 149 lines. 0 callees. 4 callers (sub_82CA8E88, sub_82CADC18, ProcessAndProcessAndProcess504, and sub_8227A818).
[entrypoint.functions."0x821FE890"]
name = "CheckFloatProperties_821FE890"

# Process a value by driving several sub-operations (five hundred and fifty-second form). It takes a value (a); it checks the value against a global, and drives several sub-operations (sub_82221420, sub_822228F0, sub_822324E0, and sub_82B7E050) based on the result. This is the "process this data" routine (five hundred and fifty-second form), part of the 0x8222C process cluster. 94 lines. 4 callees. 12 callers (many from the 0x821*, 0x822*, 0x82A*, and 0x82B* clusters).
[entrypoint.functions."0x8222C268"]
name = "ProcessAndProcessAndProcess552_8222C268"

# Extract the exponent from a float (a float exponent extraction routine). It takes a float (f1); it extracts the exponent field from the float's bit pattern and returns it as an integer. This is an "extract float exponent" routine. 17 lines. 0 callees. 2 callers (ProcessAndProcessAndProcess504 and sub_82CA5C18).
[entrypoint.functions."0x821FE2F0"]
name = "GetFloatExponent_821FE2F0"

# Process a structure by driving a sub-operation (five hundred and fifty-third form). It takes a value (a), a structure (b: field at b->144), and a value (c); it checks the structure's state, and drives a sub-operation (sub_82A0EBF8) based on the result. This is the "process this data" routine (five hundred and fifty-third form), part of the 0x8321F process cluster. 75 lines. 1 callee. 4 callers (ProcessAndProcessAndProcess516, sub_83229DC0, sub_832391D8, and sub_832389D8).
[entrypoint.functions."0x8321F598"]
name = "ProcessAndProcessAndProcess553_8321F598"

# Acquire a critical section by checking the structure's state and entering it (a critical section acquire routine). It takes a structure (a: fields at a->4 and a->0); it checks the structure's state, and — when not already acquired — enters the critical section. This is an "acquire critical section" routine. 43 lines. 1 callee. 1 caller (sub_82C08310).
[entrypoint.functions."0x82B3DF70"]
name = "AcquireCriticalSection_82B3DF70"

# Process a structure by performing bit manipulation operations on its flags (five hundred and fifty-fourth form). It takes a structure (a: fields at a->12440, a->12028, and a->10372) and a value (b, a flag); it checks the structure's flags, and performs bit manipulation operations based on the result. This is the "process this data" routine (five hundred and fifty-fourth form), part of the 0x8221E process cluster. 78 lines. 0 callees. 4 callers (sub_8221E5D0, sub_825AB370, sub_82B75D48, and sub_82B34580).
[entrypoint.functions."0x8221E540"]
name = "ProcessAndProcessAndProcess554_8221E540"

# Process state transitions by iterating over a global array and driving sub-operations (five hundred and fifty-fifth form). It takes no arguments; it iterates over a global array of pointers, driving sub-operations (ProcessStateTransition_8221E680 and sub_8221E540) on each entry. This is the "process this data" routine (five hundred and fifty-fifth form), part of the 0x8221E process cluster. 104 lines. 2 callees. 12 callers (many from the 0x821*, 0x822*, and 0x82A* clusters).
[entrypoint.functions."0x8221E5D0"]
name = "ProcessAndProcessAndProcess555_8221E5D0"

# Process a vector by performing vector operations on its elements (five hundred and fifty-sixth form). It takes a pointer (a, a destination) and a pointer (b, a source); it copies data (MemCpy_SdkRuntime) and performs vector operations on the elements. This is the "process this data" routine (five hundred and fifty-sixth form), part of the 0x82270 process cluster. 104 lines. 1 callee. 12 callers (many from the 0x821*, 0x822*, and 0x82A* clusters).
[entrypoint.functions."0x82270C80"]
name = "ProcessAndProcessAndProcess556_82270C80"

# Process a structure by driving several sub-operations (five hundred and fifty-seventh form). It takes a structure (a: fields at a->12696, a->11820, and a->12692) and a value (b, a flag); it checks the structure's state, and drives several sub-operations (sub_821DFDE0, sub_821E8D20, sub_821F75E8, sub_8221AF48, and sub_8222BFF8) based on the result. This is the "process this data" routine (five hundred and fifty-seventh form), part of the 0x8221B process cluster. 1272 lines. 6 callees. 6 callers (all from the 0x8221*/0x8220* cluster).
[entrypoint.functions."0x8221B140"]
name = "ProcessAndProcessAndProcess557_8221B140"

# Process a structure by performing bit manipulation operations on its bitmask (five hundred and fifty-eighth form). It takes a structure (a: fields at a->1152, a->48, and a->52) and a value (b, a mask); it performs bit manipulation operations on the bitmask, and drives sub-operations based on the result. This is the "process this data" routine (five hundred and fifty-eighth form), part of the 0x8221D process cluster. 200 lines. 2 callees. 6 callers (all from the 0x8221*/0x8220* cluster).
[entrypoint.functions."0x8221D2E0"]
name = "ProcessAndProcessAndProcess558_8221D2E0"

# Swap reference counts between two structures by decrementing one and incrementing the other (a reference count swap routine). It takes two structures (a and b: fields at ->4 and ->8); it decrements the reference count of one (DecrementRefcountAtomic) and increments the other (IncrementRefcountAtomic), then copies the reference count. This is a "swap refcount" routine. 73 lines. 2 callees. 11 callers (many from the 0x821*, 0x82A*, and 0x82B* clusters).
[entrypoint.functions."0x821FBFC8"]
name = "SwapRefcount_821FBFC8"

# Process a structure by updating its state and driving sub-operations (five hundred and fifty-ninth form). It takes a structure (a: fields at a->12, a->20, a->24, a->28, a->4, and a->16); it updates the structure's state, and drives sub-operations (sub_82A5AD18) based on the result. This is the "process this data" routine (five hundred and fifty-ninth form), part of the 0x8222A process cluster. 138 lines. 1 callee. 1 caller (sub_822E0508).
[entrypoint.functions."0x8222A810"]
name = "ProcessAndProcessAndProcess559_8222A810"

# Process a structure by driving sub-operations (five hundred and sixtieth form). It takes a structure (a: fields at a->32, a->40, and a->44) and a value (b); it drives sub-operations (ProcessAndProcessAndProcess503) on the structure. This is the "process this data" routine (five hundred and sixtieth form), part of the 0x8222A process cluster. 62 lines. 1 callee. 12 callers (many from the 0x821*, 0x822*, and 0x82A* clusters).
[entrypoint.functions."0x8222ABD0"]
name = "ProcessAndProcessAndProcess560_8222ABD0"

# Process a structure by driving sub-operations recursively (five hundred and sixty-first form). It takes a structure (a: fields at a->76, a->28, and a->32) and a pointer (b); it drives sub-operations (FindChainValue and ProcessVcall3) on the structure, and recurses on itself. This is the "process this data" routine (five hundred and sixty-first form), part of the 0x82C55 process cluster. 90 lines. 3 callees. 2 callers (sub_82C55C40 and sub_82C55C88).
[entrypoint.functions."0x82C55AD8"]
name = "ProcessAndProcessAndProcess561_82C55AD8"

# Process a structure by computing wrapped values and driving sub-operations (five hundred and sixty-second form). It takes a structure (a: fields at a->12, a->28, and a->24) and a structure (b: field at b->20); it computes wrapped values (ComputeWrapped), and drives sub-operations based on the result. This is the "process this data" routine (five hundred and sixty-second form), part of the 0x8222C process cluster. 133 lines. 2 callees. 12 callers (many from the 0x821*, 0x822*, and 0x82A* clusters).
[entrypoint.functions."0x8222C308"]
name = "ProcessAndProcessAndProcess562_8222C308"

# Process a structure by updating its state and decrementing a global counter (five hundred and sixty-third form). It takes a structure (a: fields at a->4 and a->0); it updates the structure's state, and decrements a global counter. This is the "process this data" routine (five hundred and sixty-third form), part of the 0x82A6E process cluster. 24 lines. 0 callees. 0 callers.
[entrypoint.functions."0x82A6EF40"]
name = "ProcessAndProcessAndProcess563_82A6EF40"

# Process a structure by iterating over a linked list and driving sub-operations (five hundred and sixty-fourth form). It takes a structure (a: fields at a->16 and a->20) and a pointer (b); it iterates over a linked list, and drives sub-operations (Allocate_OrDbgBreak) based on the result. This is the "process this data" routine (five hundred and sixty-fourth form), part of the 0x82BE4 process cluster. 193 lines. 1 callee. 12 callers (many from the 0x821*, 0x822*, and 0x82A* clusters).
[entrypoint.functions."0x82BE4BF8"]
name = "ProcessAndProcessAndProcess564_82BE4BF8"

# Return the constant value 6 (a trivial constant-return function). It takes no arguments and simply returns 6. This is a "return constant 6" routine. 4 lines. 0 callees. 0 callers.
[entrypoint.functions."0x8269B5C0"]
name = "ReturnConstant6_8269B5C0"

# Process global state by checking flags and copying data (five hundred and sixty-fifth form). It takes no arguments; it checks global flags, and copies data based on the result. This is the "process this data" routine (five hundred and sixty-fifth form), part of the 0x822A8 process cluster. 79 lines. 0 callees. 12 callers (many from the 0x821*, 0x822*, 0x823*, 0x824*, 0x826*, 0x828*, and 0x829* clusters).
[entrypoint.functions."0x822A8780"]
name = "ProcessAndProcessAndProcess565_822A8780"

# Process a structure by updating its state based on pointer comparisons (five hundred and sixty-sixth form). It takes a structure (a: fields at a->12, a->4, a->8, and a->20); it updates the structure's state based on pointer comparisons. This is the "process this data" routine (five hundred and sixty-sixth form), part of the 0x821FF process cluster. 106 lines. 0 callees. 5 callers (sub_821FF718, sub_821D4C68, sub_82B232A0, sub_82A79528, and sub_821D2EE0).
[entrypoint.functions."0x821FF658"]
name = "ProcessAndProcessAndProcess566_821FF658"

# Process a structure by performing complex computations and driving sub-operations (five hundred and sixty-seventh form). It takes a pointer (a), a pointer (b), and a structure (c: field at c->28); it performs complex computations, and drives sub-operations (sub_82286850) based on the result. This is the "process this data" routine (five hundred and sixty-seventh form), part of the 0x82286 process cluster. 468 lines. 1 callee. 7 callers (sub_82BA5448, sub_821FC270, sub_82BA0058, sub_82B9C3D0, sub_821FC410, sub_821FC320, and sub_82B9FD98).
[entrypoint.functions."0x82286508"]
name = "ProcessAndProcessAndProcess567_82286508"

# Process a structure by checking its state and driving sub-operations (five hundred and sixty-eighth form). It takes a structure (a: fields at a->48, a->56, a->10940, and a->12440), and four values (b, c, d, and e); it checks the structure's state, and drives sub-operations (sub_821E8D20) based on the result. This is the "process this data" routine (five hundred and sixty-eighth form), part of the 0x821F9 process cluster. 460 lines. 1 callee. 6 callers (sub_82B92790, sub_821F9AD8, sub_822069C0, sub_822A6318, sub_82B988C8, and sub_8222B948).
[entrypoint.functions."0x821F97E8"]
name = "ProcessAndProcessAndProcess568_821F97E8"

# Process a structure by computing coordinates and driving sub-operations (five hundred and sixty-ninth form). It takes a structure (a: fields at a->4, a->12, and a->8) and a pointer (b); it computes coordinates, and drives sub-operations (sub_82B90080) based on the result. This is the "process this data" routine (five hundred and sixty-ninth form), part of the 0x82B8F process cluster. 96 lines. 1 callee. 1 caller (sub_82B8BC08).
[entrypoint.functions."0x82B8F7B8"]
name = "ProcessAndProcessAndProcess569_82B8F7B8"

# Process a structure by computing coordinates and driving sub-operations (five hundred and seventieth form). It takes a structure (a: fields at a->4, a->12, and a->8) and a pointer (b); it computes coordinates, and drives sub-operations (sub_82B90450) based on the result. This is the "process this data" routine (five hundred and seventieth form), part of the 0x82B8F process cluster. 221 lines. 1 callee. 1 caller (sub_82B8BC08).
[entrypoint.functions."0x82B8F860"]
name = "ProcessAndProcessAndProcess570_82B8F860"

# Process a structure by computing coordinates and copying data (five hundred and seventy-first form). It takes a pointer (a) and a structure (b: fields at b->4 and b->8); it computes coordinates, and copies data based on the result. This is the "process this data" routine (five hundred and seventy-first form), part of the 0x82B8F process cluster. 260 lines. 0 callees. 1 caller (sub_82B8BC08).
[entrypoint.functions."0x82B8F9F8"]
name = "ProcessAndProcessAndProcess571_82B8F9F8"

# Process a structure by checking its state and driving sub-operations (five hundred and seventy-second form). It takes a value (a) and a value (b); it checks the structure's state, and drives sub-operations (sub_8221A9B0) based on the result. This is the "process this data" routine (five hundred and seventy-second form), part of the 0x821C8 process cluster. 117 lines. 1 callee. 9 callers (many from the 0x821* and 0x82A* clusters).
[entrypoint.functions."0x821C8168"]
name = "ProcessAndProcessAndProcess572_821C8168"

# Process a structure by driving sub-operations and checking atomic state (five hundred and seventy-third form). It takes a structure (a: fields at a->20, a->8, and a->0); it drives sub-operations (ProcessAndDispatchByte, DecrementAndValidate, and Free_SizeBucketed_Thunk), and checks atomic state based on the result. This is the "process this data" routine (five hundred and seventy-third form), part of the 0x82B45 process cluster. 269 lines. 3 callees. 5 callers (sub_82C492A0, sub_82B45528, UITextPrompt_Render, sub_82B44D10, and sub_82C49388).
[entrypoint.functions."0x82B458C0"]
name = "ProcessAndProcessAndProcess573_82B458C0"

# Check if a structure has a valid pointer and return it (a valid pointer check routine). It takes a structure (a: field at a->4); it checks the structure's pointer, and returns it if valid. This is a "check valid pointer" routine. 50 lines. 1 callee. 12 callers (many from the 0x824*, 0x826*, 0x827*, 0x828*, and 0x829* clusters).
[entrypoint.functions."0x825575C8"]
name = "CheckValidPointer_825575C8"

# Atomically add a delta to a global value (an atomic add routine). It takes a value (a, an index) and a value (b, a delta); it atomically adds the delta to the global value using atomic operations. This is an "add atomic" routine. 43 lines. 0 callees. 6 callers (sub_82CDD088, sub_82CDC2F0, sub_82CDA840, sub_82CDB3C0, sub_82CE2618, and sub_82CE3CF0).
[entrypoint.functions."0x82CDD5D8"]
name = "AddAtomic_82CDD5D8"

# Process a structure by checking its state and driving sub-operations (five hundred and seventy-fourth form). It takes a structure (a: fields at a->12536 and a->12528), a pointer (b), a structure (c: fields at c->48, c->32, and c->40), and a value (d, an index); it checks the structure's state, and drives sub-operations (sub_821D3318) based on the result. This is the "process this data" routine (five hundred and seventy-fourth form), part of the 0x821F7 process cluster. 188 lines. 1 callee. 1 caller (sub_8221B140).
[entrypoint.functions."0x821F75E8"]
name = "ProcessAndProcessAndProcess574_821F75E8"

# Process a structure by driving several sub-operations (five hundred and seventy-fifth form). It takes a structure (a), and four values (b, c, d, and e); it drives several sub-operations (sub_8221E5D0, sub_82217EE8, and MemCpy_SdkRuntime) based on the result. This is the "process this data" routine (five hundred and seventy-fifth form), part of the 0x8220A process cluster. 94 lines. 3 callees. 12 callers (many from the 0x821*, 0x822*, and 0x82A* clusters).
[entrypoint.functions."0x8220A528"]
name = "ProcessAndProcessAndProcess575_8220A528"

# Process a structure by driving many sub-operations (five hundred and seventy-sixth form). It takes a structure (a: fields at a->11832 and a->12528), and seven values (b, c, d, e, f, g, and h); it checks the structure's state, and drives many sub-operations based on the result. This is the "process this data" routine (five hundred and seventy-sixth form), part of the 0x82217 process cluster. 779 lines. 9 callees. 4 callers (sub_8220A528, sub_8222E168, sub_821D71E8, and sub_82A9B3F8).
[entrypoint.functions."0x82217EE8"]
name = "ProcessAndProcessAndProcess576_82217EE8"

# Process a structure by checking its state and driving sub-operations (five hundred and seventy-seventh form). It takes a structure (a: fields at a->24 and a->16) and a value (b); it checks the structure's state, and drives sub-operations (sub_822943E8, GetTimebase, sub_82B69098, and sub_82CBC6B0) based on the result. This is the "process this data" routine (five hundred and seventy-seventh form), part of the 0x8222A process cluster. 120 lines. 4 callees. 2 callers (sub_82A5AD18 and sub_8222A7A8).
[entrypoint.functions."0x8222A6D8"]
name = "ProcessAndProcessAndProcess577_8222A6D8"

# Process a structure by driving sub-operations and updating its state (five hundred and seventy-eighth form). It takes a structure (a: fields at a->28, a->16, a->12, and a->20) and a value (b); it drives sub-operations (sub_8222A6D8), and updates the structure's state based on the result. This is the "process this data" routine (five hundred and seventy-eighth form), part of the 0x8222A process cluster. 62 lines. 1 callee. 1 caller (sub_822E0508).
[entrypoint.functions."0x8222A7A8"]
name = "ProcessAndProcessAndProcess578_8222A7A8"

# Make a virtual call by checking the structure's state (third form). It takes a structure (a: fields at a->4 and a->8); it checks the structure's state, and — when valid — makes a virtual call. This is a "virtual call with check" routine (third form). 23 lines. 0 callees. 0 callers.
[entrypoint.functions."0x82C55310"]
name = "VirtualCallWithCheck3_82C55310"

# Copy three fields from a structure to a destination (a three-field copy routine). It takes a structure (a: fields at a->20, a->24, and a->28) and a pointer (b, a destination); it copies the three fields to the destination. This is a "copy three fields" routine. 22 lines. 0 callees. 2 callers (sub_82CE3CF0 and sub_82CD8F58).
[entrypoint.functions."0x82CD7D40"]
name = "CopyThreeFields_82CD7D40"

# Process a value by updating a global structure under a spinlock (five hundred and seventy-ninth form). It takes a value (a); it raises the IRQL, acquires a spinlock, updates a global structure, releases the spinlock, and lowers the IRQL. This is the "process this data" routine (five hundred and seventy-ninth form), part of the 0x82CDA process cluster. 112 lines. 4 callees. 1 caller (sub_82CDAAE0).
[entrypoint.functions."0x82CDA698"]
name = "ProcessAndProcessAndProcess579_82CDA698"

# Process values by updating a global structure under a spinlock (five hundred and eightieth form). It takes a value (a) and a value (b); it raises the IRQL, acquires a spinlock, updates a global structure, drives sub-operations (sub_82CDD5D8), releases the spinlock, and lowers the IRQL. This is the "process this data" routine (five hundred and eightieth form), part of the 0x82CDA process cluster. 248 lines. 5 callees. 1 caller (sub_82CDABC8).
[entrypoint.functions."0x82CDA840"]
name = "ProcessAndProcessAndProcess580_82CDA840"

# Process a structure by checking its state and setting flags (five hundred and eighty-first form). It takes a structure (a: fields at a->304, a->328, a->324, and a->320) and a value (b, an offset); it checks the structure's state, and sets flags based on the result. This is the "process this data" routine (five hundred and eighty-first form), part of the 0x82CDA process cluster. 146 lines. 0 callees. 1 caller (sub_82CDABC8).
[entrypoint.functions."0x82CDA0F8"]
name = "ProcessAndProcessAndProcess581_82CDA0F8"

# Divide two values and return the result (a float division routine). It takes a structure (a: fields at a->196 and a->192); it divides the two values and returns the result. This is a "divide floats" routine. 56 lines. 0 callees. 12 callers (many from the 0x821*, 0x82A*, 0x8322*, and 0x8323* clusters).
[entrypoint.functions."0x832222D0"]
name = "DivideFloats_832222D0"

# Process structures by performing complex bit manipulation operations (five hundred and eighty-second form). It takes a structure (a: fields at a->0, a->4, and a->40), a structure (b: fields at b->8, b->0, and b->4), and a value (c); it performs complex bit manipulation operations on the structures. This is the "process this data" routine (five hundred and eighty-second form), part of the 0x832BA process cluster. Large. 1 callee (MemSet_SdkRuntime). 0 callers.
[entrypoint.functions."0x832BA368"]
name = "ProcessAndProcessAndProcess582_832BA368"

# Process structures by performing complex bit manipulation operations (five hundred and eighty-third form). It takes a structure (a: fields at a->0, a->4, and a->40), a structure (b: fields at b->8, b->0, and b->4), and a value (c); it performs complex bit manipulation operations on the structures. This is the "process this data" routine (five hundred and eighty-third form), part of the 0x832BA process cluster. Large. 1 callee (MemSet_SdkRuntime). 1 caller (sub_832BB018).
[entrypoint.functions."0x832BA608"]
name = "ProcessAndProcessAndProcess583_832BA608"

# Process structures by performing complex bit manipulation operations (five hundred and eighty-fourth form). It takes a structure (a: fields at a->0, a->4, and a->40) and a structure (b: fields at b->8, b->0, and b->4); it performs complex bit manipulation operations on the structures. This is the "process this data" routine (five hundred and eighty-fourth form), part of the 0x832BA process cluster. Large. 0 callees. 1 caller (sub_832BB018).
[entrypoint.functions."0x832BA838"]
name = "ProcessAndProcessAndProcess584_832BA838"

# Make a virtual call (fourth form). It takes a structure (a: fields at a->4 and a->0); it makes a virtual call based on the structure's vtable. This is a "virtual call" routine (fourth form). 15 lines. 0 callees. 0 callers.
[entrypoint.functions."0x82A5BCB8"]
name = "VirtualCallWithCheck4_82A5BCB8"

# Process a structure by driving sub-operations (five hundred and eighty-fifth form). It takes a structure (a: field at a->8) and a value (b); it checks the structure's state, and drives sub-operations (sub_82BE4BF8 and sub_8222C308) based on the result. This is the "process this data" routine (five hundred and eighty-fifth form), part of the 0x821D1 process cluster. 103 lines. 2 callees. 0 callers.
[entrypoint.functions."0x821D1388"]
name = "ProcessAndProcessAndProcess585_821D1388"

# Process values by driving many sub-operations (five hundred and eighty-sixth form). It takes a value (a) and a value (b); it checks the values' state, and drives many sub-operations based on the result. This is the "process this data" routine (five hundred and eighty-sixth form), part of the 0x821EA process cluster. 1417 lines. 13 callees. 0 callers.
[entrypoint.functions."0x821EA5C0"]
name = "ProcessAndProcessAndProcess586_821EA5C0"

# Process vectors by performing vector operations on multiple data regions (five hundred and eighty-seventh form). It takes a pointer (a) and a pointer (b); it performs vector operations on the data regions. This is the "process this data" routine (five hundred and eighty-seventh form), part of the 0x821C0 process cluster. 150 lines. 0 callees. 3 callers (sub_82B71288, sub_83222D78, and sub_83222CA8).
[entrypoint.functions."0x821C0210"]
name = "ProcessAndProcessAndProcess587_821C0210"

# Normalize a vector by dividing its components by its magnitude (a vector normalization routine). It takes a pointer (a) and a float (b, a threshold); it normalizes the vector by dividing its components by its magnitude, and returns the normalized vector. This is a "normalize vector" routine. 68 lines. 0 callees. 12 callers (many from the 0x822*, 0x824*, 0x825*, 0x826*, 0x827*, and 0x829* clusters).
[entrypoint.functions."0x82260BC8"]
name = "NormalizeVector_82260BC8"

# Copy a structure by copying its fields to a destination (a structure copy routine). It takes a pointer (a, a destination) and a pointer (b, a source); it copies the structure's fields to the destination using MemCpy_SdkRuntime. This is a "copy structure" routine. 141 lines. 1 callee. 12 callers (many from the 0x821*, 0x823*, 0x824*, 0x826*, 0x828*, 0x829*, and 0x82A* clusters).
[entrypoint.functions."0x82400600"]
name = "CopyStructure_82400600"

# Process a structure by making virtual calls (five hundred and eighty-eighth form). It takes a structure (a: field at a->208); it makes virtual calls based on the structure's vtable. This is the "process this data" routine (five hundred and eighty-eighth form), part of the 0x83222 process cluster. 65 lines. 0 callees. 1 caller (sub_8322D678).
[entrypoint.functions."0x83222368"]
name = "ProcessAndProcessAndProcess588_83222368"

# Process a structure by checking its state and driving sub-operations (five hundred and eighty-ninth form). It takes a value (a) and a value (b); it checks the structure's state, and drives sub-operations (sub_8221A9B0) based on the result. This is the "process this data" routine (five hundred and eighty-ninth form), part of the 0x821CA process cluster. 124 lines. 1 callee. 3 callers (sub_82A53B30, sub_821AB768, and sub_821CA160).
[entrypoint.functions."0x821CA088"]
name = "ProcessAndProcessAndProcess589_821CA088"

# Process a structure by checking its state and updating its flags (five hundred and ninetieth form). It takes a structure (a: fields at a->16, a->4, a->0, and a->8); it checks the structure's state, and updates its flags based on the result. This is the "process this data" routine (five hundred and ninetieth form), part of the 0x8223B process cluster. 74 lines. 0 callees. 7 callers (many from the 0x822*, 0x82A*, and 0x82B* clusters).
[entrypoint.functions."0x8223B118"]
name = "ProcessAndProcessAndProcess590_8223B118"

# Process a structure by performing vector operations and bit manipulation (five hundred and ninety-first form). It takes a structure (a: fields at a->316, a->312, a->308, a->304, and a->256), a structure (b: field at b->16), and a value (c); it performs vector operations and bit manipulation on the structures. This is the "process this data" routine (five hundred and ninety-first form), part of the 0x821DC process cluster. 247 lines. 0 callees. 1 caller (sub_82269BB8).
[entrypoint.functions."0x821DCAF0"]
name = "ProcessAndProcessAndProcess591_821DCAF0"

# Process a structure by driving sub-operations (five hundred and ninety-second form). It takes a value (a), a structure (b: field at b->472), and three values (c, d, and e); it checks the structure's state, and drives sub-operations (ProcessAndProcessAndProcess516 and ProcessAndProcessAndProcess519) based on the result. This is the "process this data" routine (five hundred and ninety-second form), part of the 0x82269 process cluster. 288 lines. 2 callees. 1 caller (sub_82269BB8).
[entrypoint.functions."0x82269FA0"]
name = "ProcessAndProcessAndProcess592_82269FA0"

# Process a structure by checking its state and driving sub-operations (five hundred and ninety-third form). It takes a value (a), a structure (b: fields at b->44, b->52, and b->48), and two values (c and d); it checks the structure's state, and drives sub-operations (sub_8321B498) based on the result. This is the "process this data" routine (five hundred and ninety-third form), part of the 0x8226A process cluster. 253 lines. 1 callee. 1 caller (sub_82269BB8).
[entrypoint.functions."0x8226A338"]
name = "ProcessAndProcessAndProcess593_8226A338"

# Process a structure by performing complex computations and driving sub-operations (five hundred and ninety-fourth form). It takes a value (a), a structure (b: fields at b->208 and b->308), and two values (c and d); it performs complex computations, and drives sub-operations (ComputeWrapped) based on the result. This is the "process this data" routine (five hundred and ninety-fourth form), part of the 0x8321B process cluster. 861 lines. 1 callee. 1 caller (sub_82269BB8).
[entrypoint.functions."0x8321BA10"]
name = "ProcessAndProcessAndProcess594_8321BA10"

# Process a structure by checking its state and driving sub-operations recursively (five hundred and ninety-fifth form). It takes a value (a), a structure (b: fields at b->72 and b->24), and three values (c, d, and e); it checks the structure's state, and drives sub-operations (sub_821CB260) based on the result, recursing on itself. This is the "process this data" routine (five hundred and ninety-fifth form), part of the 0x82297 process cluster. 180 lines. 2 callees. 1 caller (sub_822971B8).
[entrypoint.functions."0x82297258"]
name = "ProcessAndProcessAndProcess595_82297258"

# Copy a structure by copying its fields to a destination (second form). It takes a pointer (a, a destination) and a pointer (b, a source); it copies the structure's fields to the destination. This is a "copy structure" routine (second form). 251 lines. 0 callees. 5 callers (sub_82A60DE8, sub_82900B28, sub_82A61080, sub_8281AE60, and sub_82410D98).
[entrypoint.functions."0x82815108"]
name = "CopyStructure2_82815108"

# Process a structure by driving sub-operations (five hundred and ninety-sixth form). It takes a value (a) and a structure (b: fields at b->440 and b->436); it drives sub-operations (ProcessVector2, sub_825575C8, and sub_83222D78) based on the result. This is the "process this data" routine (five hundred and ninety-sixth form), part of the 0x83226 process cluster. 73 lines. 4 callees. 4 callers (sub_83226570, sub_832265D8, sub_83229DC0, and sub_83226730).
[entrypoint.functions."0x832264F8"]
name = "ProcessAndProcessAndProcess596_832264F8"

# Process a float array by iterating over its elements and performing float operations (five hundred and ninety-seventh form). It takes a pointer (a), a value (b), a value (c), and a pointer (d); it iterates over the float array, performing float operations on each element. This is the "process this data" routine (five hundred and ninety-seventh form), part of the 0x82C5F process cluster. 187 lines. 0 callees. 1 caller (sub_82C5F810).
[entrypoint.functions."0x82C5F1B8"]
name = "ProcessAndProcessAndProcess597_82C5F1B8"

# Process a structure by performing float operations on its fields (five hundred and ninety-eighth form). It takes a structure (a: fields at a->64, a->352, and a->356) and a value (b); it performs float operations on the structure's fields. This is the "process this data" routine (five hundred and ninety-eighth form), part of the 0x82C5F process cluster. 191 lines. 0 callees. 1 caller (sub_82C5F810).
[entrypoint.functions."0x82C5F330"]
name = "ProcessAndProcessAndProcess598_82C5F330"

# Process arrays by iterating over elements and performing complex operations (five hundred and ninety-ninth form). It takes a value (a), a pointer (b), a value (c), a value (d), and a value (e); it iterates over the arrays, performing complex operations on each element. This is the "process this data" routine (five hundred and ninety-ninth form), part of the 0x82C60 process cluster. 158 lines. 0 callees. 1 caller (sub_82C5F810).
[entrypoint.functions."0x82C606E8"]
name = "ProcessAndProcessAndProcess599_82C606E8"

# Process a structure by converting its float fields to integers (six hundredth form). It takes a structure (a: fields at a->12648, a->12656, a->12660, and a->12652) and a pointer (b); it converts the structure's float fields to integers, and drives sub-operations (sub_821F97E8) based on the result. This is the "process this data" routine (six hundredth form), part of the 0x821F9 process cluster. 142 lines. 1 callee. 5 callers (sub_82B90D28, sub_821F9BD0, sub_821B9AD0, sub_821C64E8, and sub_822A5F80).
[entrypoint.functions."0x821F9AD8"]
name = "ProcessAndProcessAndProcess600_821F9AD8"

# Process a structure by performing vector operations (six hundred and first form). It takes a structure (a: fields at a->0 and a->4), a structure (b: fields at b->0, b->4, b->8, and b->12), and a pointer (c); it performs vector operations on the structures. This is the "process this data" routine (six hundred and first form), part of the 0x8321C process cluster. 469 lines. 0 callees. 1 caller (sub_821CF480).
[entrypoint.functions."0x8321C688"]
name = "ProcessAndProcessAndProcess601_8321C688"

# Process a structure by checking its state and driving sub-operations (six hundred and second form). It takes a structure (a: fields at a->12440, a->12456, and a->10940) and four floats (b, c, d, and e); it checks the structure's state, and drives sub-operations (ProcessAndProcessAndProcess600) based on the result. This is the "process this data" routine (six hundred and second form), part of the 0x821F9 process cluster. 350 lines. 1 callee. 3 callers (sub_821F9E28, sub_822A5F80, and sub_821C64E8).
[entrypoint.functions."0x821F9BD0"]
name = "ProcessAndProcessAndProcess602_821F9BD0"

# Process a structure by converting its fields and driving sub-operations (six hundred and third form). It takes a value (a) and a structure (b: fields at b->0, b->4, b->8, b->12, b->16, and b->20); it converts the structure's fields, and drives sub-operations (ProcessAndProcessAndProcess602) based on the result. This is the "process this data" routine (six hundred and third form), part of the 0x821F9 process cluster. 76 lines. 1 callee. 7 callers (many from the 0x821*, 0x822*, and 0x82F* clusters).
[entrypoint.functions."0x821F9E28"]
name = "ProcessAndProcessAndProcess603_821F9E28"

# Process a structure by allocating memory and driving sub-operations (six hundred and fourth form). It takes a structure (a: field at a->0) and a value (b); it allocates memory (Allocate_OrDbgBreak) and drives sub-operations (sub_82A519D0, sub_82A51FE8, and sub_82A6D650) based on the result. This is the "process this data" routine (six hundred and fourth form), part of the 0x821AB process cluster. 301 lines. 4 callees. 12 callers (many from the 0x821*, 0x822*, 0x82A*, and 0x82B* clusters).
[entrypoint.functions."0x821ABC00"]
name = "ProcessAndProcessAndProcess604_821ABC00"

# Find an entry in a tree by traversing its nodes (a tree lookup routine). It takes a value (a), a value (b), and a pointer (c); it traverses the tree, comparing values at each node to find a match. This is a "find in tree" routine. 91 lines. 0 callees. 6 callers (many from the 0x82A5* cluster, and sub_821ABC00).
[entrypoint.functions."0x82A51FE8"]
name = "FindInTree_82A51FE8"

# Process a structure by checking its type code and driving sub-operations (six hundred and fifth form). It takes eight values (a through h); it checks the structure's type code, and drives sub-operations (DispatchByTypeCode and ProcessAndProcessAndProcess514) based on the result. This is the "process this data" routine (six hundred and fifth form), part of the 0x821FB process cluster. 491 lines. 2 callees. 1 caller (sub_821FB520).
[entrypoint.functions."0x821FB1B0"]
name = "ProcessAndProcessAndProcess605_821FB1B0"

# Process a structure by dispatching on its type code (six hundred and sixth form). It takes a type code (a) and seven values (b through h); it dispatches on the type code, and drives sub-operations (ProcessAndProcessAndProcess605 and sub_82B91C78) based on the result. This is the "process this data" routine (six hundred and sixth form), part of the 0x821FB process cluster. 443 lines. 2 callees. 5 callers (many from the 0x821* and 0x82B* clusters).
[entrypoint.functions."0x821FB520"]
name = "ProcessAndProcessAndProcess606_821FB520"

# Process a structure by dispatching on its type code and copying data (six hundred and seventh form). It takes eight values (a through h); it dispatches on the type code (DispatchByTypeCode), copies data (MemCpy_SdkRuntime), and drives sub-operations (ProcessAndProcessAndProcess606) based on the result. This is the "process this data" routine (six hundred and seventh form), part of the 0x821FB process cluster. 291 lines. 3 callees. 3 callers (sub_830054B8, sub_821FBA48, and sub_83005450).
[entrypoint.functions."0x821FB838"]
name = "ProcessAndProcessAndProcess607_821FB838"

# Process a structure by preparing parameters and driving sub-operations (six hundred and eighth form). It takes eight values (a through h); it prepares the parameters, and drives sub-operations (ProcessAndProcessAndProcess607) based on the result. This is the "process this data" routine (six hundred and eighth form), part of the 0x821FB process cluster. 64 lines. 1 callee. 12 callers (many from the 0x821*, 0x822*, 0x82A*, 0x82B*, and 0x82D* clusters).
[entrypoint.functions."0x821FBA48"]
name = "ProcessAndProcessAndProcess608_821FBA48"

# Process a structure by validating its bounds and computing offsets (six hundred and ninth form). It takes a structure (a: fields at a->16, a->12, a->8, and a->4); it validates the structure's bounds, and computes offsets. This is the "process this data" routine (six hundred and ninth form), part of the 0x82A6C process cluster. 64 lines. 0 callees. 4 callers (sub_82A6C398, sub_8226E6E8, sub_821E8A68, and sub_8225E300).
[entrypoint.functions."0x82A6C1C0"]
name = "ProcessAndProcessAndProcess609_82A6C1C0"

# Process a structure by allocating memory and constructing nodes (six hundred and tenth form). It takes a structure (a: field at a->4); it allocates memory (Allocate_OrDbgBreak), constructs nodes (ConstructAndInsertNode), and drives sub-operations (ProcessAndProcessAndProcess573) based on the result. This is the "process this data" routine (six hundred and tenth form), part of the 0x82C49 process cluster. 259 lines. 6 callees. 0 callers.
[entrypoint.functions."0x82C49388"]
name = "ProcessAndProcessAndProcess610_82C49388"

# Set a bit field in a structure by modifying specific bits (a bit field setter). It takes a structure (a: field at a->24), a structure (b: fields at b->48 and b->32), and a value (c); it modifies specific bits in the structures. This is a "set bit field" routine. 32 lines. 0 callees. 0 callers.
[entrypoint.functions."0x82237430"]
name = "SetBitField_82237430"

# Get a float field from a structure (a field getter). It takes a structure (a: field at a->396); it loads the float from the structure's field and returns it. This is a "get float field" routine. 11 lines. 0 callees. 0 callers.
[entrypoint.functions."0x821CC7D0"]
name = "GetFloatField396_821CC7D0"

# Set a flag in a linked list by traversing its nodes (a linked list flag setter). It takes a structure (a: fields at a->108 and a->100); it traverses the linked list, setting a flag bit in each node. This is a "set flag in list" routine. 51 lines. 0 callees. 4 callers (many from the 0x822* and 0x82A* clusters).
[entrypoint.functions."0x82AAF160"]
name = "SetFlagInList_82AAF160"

# Process a structure by performing float operations (six hundred and eleventh form). It takes four values (a, b, c, and d); it performs float operations on the values. This is the "process this data" routine (six hundred and eleventh form), part of the 0x82AAD process cluster. 243 lines. 0 callees. 2 callers (sub_82AADFF8 and sub_822B80A0).
[entrypoint.functions."0x82AADF30"]
name = "ProcessAndProcessAndProcess611_82AADF30"

# Destroy a size-bucketed object by freeing its memory (an object destructor). It takes a structure (a: field at a->0) and a flag (b); it sets a flag, and frees the object's memory (Free_SizeBucketed_Thunk) based on the flag. This is a "destroy size-bucketed object" routine. 45 lines. 1 callee. 0 callers.
[entrypoint.functions."0x822AB3B0"]
name = "DestroySizeBucketedObject_822AB3B0"

# Set a bit field in a structure by modifying specific bits (second form). It takes a structure (a: field at a->24), a structure (b: fields at b->48 and b->32), and a value (c); it modifies specific bits in the structures. This is a "set bit field" routine (second form). 34 lines. 0 callees. 0 callers.
[entrypoint.functions."0x822398A8"]
name = "SetBitField2_822398A8"

# Process a structure by checking its bounds and driving sub-operations (six hundred and twelfth form). It takes a structure (a: fields at a->48 and a->56) and two values (b and c); it checks the structure's bounds, and drives sub-operations (sub_821E8D20) based on the result. This is the "process this data" routine (six hundred and twelfth form), part of the 0x82215 process cluster. 587 lines. 1 callee. 11 callers (many from the 0x821*, 0x822*, and 0x82B* clusters).
[entrypoint.functions."0x822155E0"]
name = "ProcessAndProcessAndProcess612_822155E0"

# Set a bit field in a structure by modifying specific bits (third form). It takes a structure (a: field at a->24), a structure (b: fields at b->48 and b->32), and a value (c); it modifies specific bits in the structures. This is a "set bit field" routine (third form). 32 lines. 0 callees. 0 callers.
[entrypoint.functions."0x82202E48"]
name = "SetBitField3_82202E48"

# Set a bit field in a structure by modifying specific bits (fourth form). It takes a structure (a: field at a->24), a structure (b: fields at b->48 and b->32), and a value (c); it modifies specific bits in the structures. This is a "set bit field" routine (fourth form). 32 lines. 0 callees. 0 callers.
[entrypoint.functions."0x8222C5C0"]
name = "SetBitField4_8222C5C0"

# Process a structure by performing float operations (six hundred and thirteenth form). It takes a structure (a: fields at a->8 and a->4) and a structure (b: fields at b->40, b->44, b->52, and b->48); it performs float operations on the structures. This is the "process this data" routine (six hundred and thirteenth form), part of the 0x82C55 process cluster. 75 lines. 0 callees. 0 callers.
[entrypoint.functions."0x82C55470"]
name = "ProcessAndProcessAndProcess613_82C55470"

# Process a structure by performing float operations (six hundred and fourteenth form). It takes a structure (a: fields at a->8 and a->4) and a structure (b: fields at b->56, b->60, b->68, and b->64); it performs float operations on the structures. This is the "process this data" routine (six hundred and fourteenth form), part of the 0x82C55 process cluster. 75 lines. 0 callees. 0 callers.
[entrypoint.functions."0x82C554E8"]
name = "ProcessAndProcessAndProcess614_82C554E8"

# Process a value by preparing parameters and driving sub-operations (six hundred and fifteenth form). It takes a value (a) and a value (b); it prepares the parameters, and drives sub-operations (ProcessAndProcessAndProcess561) based on the result. This is the "process this data" routine (six hundred and fifteenth form), part of the 0x82C55 process cluster. 48 lines. 1 callee. 1 caller (sub_82C52A80).
[entrypoint.functions."0x82C55C40"]
name = "ProcessAndProcessAndProcess615_82C55C40"

# Process a value by preparing parameters and driving sub-operations (six hundred and sixteenth form). It takes a value (a) and a value (b); it prepares the parameters, and drives sub-operations (ProcessAndProcessAndProcess561) based on the result. This is the "process this data" routine (six hundred and sixteenth form), part of the 0x82C55 process cluster. 48 lines. 1 callee. 1 caller (sub_82C52A80).
[entrypoint.functions."0x82C55C88"]
name = "ProcessAndProcessAndProcess616_82C55C88"

# Process a structure by recursively traversing its nodes (six hundred and seventeenth form). It takes a structure (a: field at a->20) and three values (b, c, and d); it recursively traverses the structure's nodes, processing each one. This is the "process this data" routine (six hundred and seventeenth form), part of the 0x82B8B process cluster. 96 lines. 1 callee. 1 caller (sub_82B8BC08).
[entrypoint.functions."0x82B8BB60"]
name = "ProcessAndProcessAndProcess617_82B8BB60"

# Process a structure by checking its state and driving sub-operations (six hundred and eighteenth form). It takes a structure (a: fields at a->28, a->24, a->20, a->36, and a->0); it checks the structure's state, and drives sub-operations (SetFlagInList) based on the result. This is the "process this data" routine (six hundred and eighteenth form), part of the 0x822C5 process cluster. 100 lines. 1 callee. 8 callers (many from the 0x822* and 0x82A* clusters).
[entrypoint.functions."0x822C5040"]
name = "ProcessAndProcessAndProcess618_822C5040"

# Process a value by checking its state and performing bit manipulation (six hundred and nineteenth form). It takes a value (a); it checks the value's state, and performs bit manipulation. This is the "process this data" routine (six hundred and nineteenth form), part of the 0x82210 process cluster. 118 lines. 0 callees. 12 callers (many from the 0x821*, 0x822*, and 0x82A* clusters).
[entrypoint.functions."0x82210418"]
name = "ProcessAndProcessAndProcess619_82210418"

# Process a structure by checking its flags and driving sub-operations (six hundred and twentieth form). It takes a structure (a: fields at a->0, a->28, a->24, a->40, and a->36) and a structure (b: fields at b->8, b->12, b->20, b->0, and b->24); it checks the structure's flags, and drives sub-operations (ProcessAndProcessAndProcess533) based on the result. This is the "process this data" routine (six hundred and twentieth form), part of the 0x821F9 process cluster. 87 lines. 1 callee. 12 callers (many from the 0x821*, 0x82A*, 0x82B*, 0x82F*, and 0x830* clusters).
[entrypoint.functions."0x821F9F40"]
name = "ProcessAndProcessAndProcess620_821F9F40"

# Process a structure by iterating over its elements and driving sub-operations (six hundred and twenty-first form). It takes a structure (a: fields at a->32, a->28, and a->24); it iterates over the structure's elements, performing operations on each one. This is the "process this data" routine (six hundred and twenty-first form), part of the 0x82239 process cluster. 132 lines. 0 callees. 8 callers (many from the 0x821*, 0x822*, 0x82A*, and 0x82B* clusters).
[entrypoint.functions."0x82239C20"]
name = "ProcessAndProcessAndProcess621_82239C20"

# Process a structure by checking its state and driving sub-operations (six hundred and twenty-second form). It takes a structure (a: field at a->16); it checks the structure's state, and drives sub-operations (sub_82C5CB40 and sub_82C61128) based on the result. This is the "process this data" routine (six hundred and twenty-second form), part of the 0x82C5C process cluster. 52 lines. 2 callees. 2 callers (sub_82C59E10 and sub_82C59ED0).
[entrypoint.functions."0x82C5CC08"]
name = "ProcessAndProcessAndProcess622_82C5CC08"

# Process a structure by performing division operations and driving sub-operations (six hundred and twenty-third form). It takes a structure (a: fields at a->12, a->16, a->24, and a->13) and a value (b); it performs division operations, and drives sub-operations (MemSet_SdkRuntime) based on the result. This is the "process this data" routine (six hundred and twenty-third form), part of the 0x82CD7 process cluster. 160 lines. 1 callee. 4 callers (many from the 0x82C* cluster).
[entrypoint.functions."0x82CD7EB8"]
name = "ProcessAndProcessAndProcess623_82CD7EB8"

# Process a structure by iterating over its elements and performing vector operations (six hundred and twenty-fourth form). It takes a structure (a: field at a->256) and two vectors (b and c); it iterates over the structure's elements, performing vector operations on each one. This is the "process this data" routine (six hundred and twenty-fourth form), part of the 0x82236 process cluster. 72 lines. 0 callees. 12 callers (many from the 0x821*, 0x822*, 0x82A*, and 0x82B* clusters).
[entrypoint.functions."0x82236338"]
name = "ProcessAndProcessAndProcess624_82236338"

# Process a structure by performing float operations (six hundred and twenty-fifth form). It takes eight values (a through h); it performs float operations on the values. This is the "process this data" routine (six hundred and twenty-fifth form), part of the 0x8219D process cluster. 170 lines. 0 callees. 4 callers (many from the 0x821*, 0x823*, and 0x82C* clusters).
[entrypoint.functions."0x8219D738"]
name = "ProcessAndProcessAndProcess625_8219D738"

# Process a value by performing vector operations (six hundred and twenty-sixth form). It takes a value (a), a value (b), and a value (c); it performs vector operations on the values. This is the "process this data" routine (six hundred and twenty-sixth form), part of the 0x822BA process cluster. 274 lines. 0 callees. 12 callers (many from the 0x82C* and 0x82D* clusters).
[entrypoint.functions."0x822BA2F8"]
name = "ProcessAndProcessAndProcess626_822BA2F8"

# Process a structure by checking its state and performing bound checks (six hundred and twenty-seventh form). It takes a value (a), a structure (b: field at b->0), and a value (c); it checks the structure's state, and performs bound checks. This is the "process this data" routine (six hundred and twenty-seventh form), part of the 0x82AB1 process cluster. 151 lines. 0 callees. 1 caller (sub_82AB20E0).
[entrypoint.functions."0x82AB1FD0"]
name = "ProcessAndProcessAndProcess627_82AB1FD0"

# Process a structure by checking its state and driving sub-operations (six hundred and twenty-eighth form). It takes a structure (a: fields at a->0, a->44, a->48, and a->61); it checks the structure's state, and drives sub-operations (Stub_ReturnZero) based on the result. This is the "process this data" routine (six hundred and twenty-eighth form), part of the 0x82CE5 process cluster. 121 lines. 1 callee. 2 callers (sub_82CDD088 and sub_82CE4728).
[entrypoint.functions."0x82CE59D0"]
name = "ProcessAndProcessAndProcess628_82CE59D0"

# Process a structure by iterating over its elements and driving sub-operations (six hundred and twenty-ninth form). It takes a structure (a: fields at a->60 and a->36) and a value (b); it iterates over the structure's elements, and drives sub-operations (sub_82CE5F78) based on the result. This is the "process this data" routine (six hundred and twenty-ninth form), part of the 0x82CE6 process cluster. 64 lines. 1 callee. 2 callers (sub_82CE5610 and sub_82CE44E0).
[entrypoint.functions."0x82CE67B8"]
name = "ProcessAndProcessAndProcess629_82CE67B8"

# Process a structure by checking its state and returning a result (six hundred and thirtieth form). It takes a structure (a: fields at a->4, a->8, a->28, a->24, a->80, a->32, and a->16); it checks the structure's state, and returns a result based on the check. This is the "process this data" routine (six hundred and thirtieth form), part of the 0x82CE7 process cluster. 256 lines. 0 callees. 2 callers (sub_82CE3CF0 and sub_82CE2618).
[entrypoint.functions."0x82CE7730"]
name = "ProcessAndProcessAndProcess630_82CE7730"

# Process a structure by performing complex operations and driving many sub-operations (six hundred and thirty-first form). It takes a structure (a: field at a->80); it performs complex operations, and drives many sub-operations (CompareString, CopyStructure, Destruct4, DestructRefCounted, and more) based on the result. This is the "process this data" routine (six hundred and thirty-first form), part of the 0x821C9 process cluster. 1235 lines. 17 callees. 1 caller (ProcessAndProcessAndProcess521).
[entrypoint.functions."0x821C96B0"]
name = "ProcessAndProcessAndProcess631_821C96B0"

# Process a structure by performing complex float operations and driving sub-operations (six hundred and thirty-second form). It takes a value (a) and a structure (b: fields at b->40, b->24, b->8, b->56, b->60, and b->52); it performs complex float operations, and drives sub-operations (ProcessAndProcessAndProcess625 and more) based on the result. This is the "process this data" routine (six hundred and thirty-second form), part of the 0x8219D process cluster. 1085 lines. 3 callees. 0 callers.
[entrypoint.functions."0x8219D048"]
name = "ProcessAndProcessAndProcess632_8219D048"

# Process a structure by performing float operations (six hundred and thirty-third form). It takes a structure (a: fields at a->80 and a->548) and a float (b); it performs float operations on the structure's fields. This is the "process this data" routine (six hundred and thirty-third form), part of the 0x82815 process cluster. 58 lines. 0 callees. 0 callers.
[entrypoint.functions."0x82815C58"]
name = "ProcessAndProcessAndProcess633_82815C58"

# Check a float's sign by examining its bit pattern (a float sign checker). It takes a value (a) and a float (b); it examines the float's bit pattern, and returns a result based on the sign. This is a "check float sign" routine. 21 lines. 0 callees. 12 callers (many from the 0x822*, 0x830*, and 0x831* clusters).
[entrypoint.functions."0x82200D48"]
name = "CheckFloatSign_82200D48"

# Process a structure by performing division operations (six hundred and thirty-fourth form). It takes a value (a), a value (b), and a value (c); it performs division operations on the values. This is the "process this data" routine (six hundred and thirty-fourth form), part of the 0x82A69 process cluster. 191 lines. 0 callees. 7 callers (many from the 0x821*, 0x825*, 0x827*, and 0x82A* clusters).
[entrypoint.functions."0x82A695D8"]
name = "ProcessAndProcessAndProcess634_82A695D8"

# Process a structure by performing float operations and driving sub-operations (six hundred and thirty-fifth form). It takes eight values (a through h); it performs float operations, and drives sub-operations (NormalizeVector) based on the result. This is the "process this data" routine (six hundred and thirty-fifth form), part of the 0x8228D process cluster. 146 lines. 1 callee. 1 caller (sub_822D8180).
[entrypoint.functions."0x8228DC00"]
name = "ProcessAndProcessAndProcess635_8228DC00"

# Process a structure by dispatching on its type code (six hundred and thirty-sixth form). It takes a value (a), a structure (b: fields at b->8, b->24, b->40, b->56, b->60, b->48, and b->44), and a value (c); it dispatches on the type code, and drives sub-operations (ProcessAndProcessAndProcess635) based on the result. This is the "process this data" routine (six hundred and thirty-sixth form), part of the 0x822D8 process cluster. 195 lines. 1 callee. 3 callers (sub_8228DB78, sub_821A2738, and sub_82270EE0).
[entrypoint.functions."0x822D8180"]
name = "ProcessAndProcessAndProcess636_822D8180"

# Process a value by checking its state and performing bit manipulation (six hundred and thirty-seventh form). It takes a value (a); it checks the value's state, and performs bit manipulation. This is the "process this data" routine (six hundred and thirty-seventh form), part of the 0x821C5 process cluster. 120 lines. 0 callees. 10 callers (many from the 0x821*, 0x822*, and 0x82A* clusters).
[entrypoint.functions."0x821C56C0"]
name = "ProcessAndProcessAndProcess637_821C56C0"

# Process a value by checking its state and driving sub-operations (six hundred and thirty-eighth form). It takes four values (a, b, c, and d); it checks the value's state, and drives sub-operations (ProcessAndProcessAndProcess531) based on the result. This is the "process this data" routine (six hundred and thirty-eighth form), part of the 0x82286 process cluster. 128 lines. 1 callee. 1 caller (sub_821CA160).
[entrypoint.functions."0x82286F08"]
name = "ProcessAndProcessAndProcess638_82286F08"

# Process a structure by performing float operations (six hundred and thirty-ninth form). It takes a structure (a: fields at a->72, a->8, a->104, a->40, a->76, a->12, a->108, a->44, and a->4) and a structure (b: fields at b->4 and b->8); it performs float operations on the structures. This is the "process this data" routine (six hundred and thirty-ninth form), part of the 0x832BF process cluster. Large. 0 callees. 3 callers (sub_832BFA70, sub_832BDC90, and sub_832BF9D0).
[entrypoint.functions."0x832BFB18"]
name = "ProcessAndProcessAndProcess639_832BFB18"

# Process a value by driving sub-operations (six hundred and fortieth form). It takes a value (a), a structure (b: field at b->0), and a value (c); it drives sub-operations (ProcessAndProcessAndProcess560, ProcessAndProcessAndProcess503, and more) based on the result. This is the "process this data" routine (six hundred and fortieth form), part of the 0x82261 process cluster. 105 lines. 5 callees. 12 callers (many from the 0x821*, 0x822*, 0x825*, 0x826*, 0x828*, and 0x829* clusters).
[entrypoint.functions."0x82261A80"]
name = "ProcessAndProcessAndProcess640_82261A80"

# Process a value by checking its state and driving sub-operations (six hundred and forty-first form). It takes a value (a) and a value (b); it checks the value's state, and drives sub-operations (GetTimebase) based on the result. This is the "process this data" routine (six hundred and forty-first form), part of the 0x821F7 process cluster. 122 lines. 1 callee. 2 callers (ProcessAndProcessAndProcess640 and sub_822E0508).
[entrypoint.functions."0x821F7BD8"]
name = "ProcessAndProcessAndProcess641_821F7BD8"

# Process a structure by performing bit manipulation and driving sub-operations (six hundred and forty-second form). It takes a value (a), a value (b), a structure (c: fields at c->24 and c->28), a value (d), and a value (e); it performs bit manipulation, and drives sub-operations (sub_82B9B340) based on the result. This is the "process this data" routine (six hundred and forty-second form), part of the 0x821B6 process cluster. 162 lines. 1 callee. 12 callers (many from the 0x821*, 0x822*, 0x82A*, and 0x82B* clusters).
[entrypoint.functions."0x821B6DA0"]
name = "ProcessAndProcessAndProcess642_821B6DA0"

# Process a structure by checking its state and driving sub-operations (six hundred and forty-third form). It takes a structure (a: fields at a->16, a->8, and a->0); it checks the structure's state, and drives sub-operations (ProcessAndProcessAndProcess524 and sub_82A3ED80) based on the result. This is the "process this data" routine (six hundred and forty-third form), part of the 0x821FB process cluster. 69 lines. 2 callees. 12 callers (many from the 0x822*, 0x82A*, 0x82B*, and 0x832* clusters).
[entrypoint.functions."0x821FBE60"]
name = "ProcessAndProcessAndProcess643_821FBE60"

# Destruct a structure and decrement its refcount (a destruct and decrement function). It takes a structure (a: fields at a->12, a->4, a->0, and a->8); it destructs the structure, and decrements its refcount (DecrementRefcountAtomic). This is a "destruct and decrement" routine. 68 lines. 1 callee. 2 callers (ProcessAndProcessAndProcess643 and sub_82A4A8B8).
[entrypoint.functions."0x82A3ED80"]
name = "DestructAndDecrementRefcount_82A3ED80"

# Delay for zero milliseconds by calling the delay routine (a zero delay function). It takes no arguments; it calls the delay routine (DelayMilliseconds) with a zero argument. This is a "delay for zero" routine. 9 lines. 1 callee. 12 callers (many from the 0x822*, 0x823*, 0x824*, 0x82B*, 0x82C*, 0x82E*, 0x830* clusters, and ProcessAndProcessAndProcess503).
[entrypoint.functions."0x82CBC6B0"]
name = "DelayZero_82CBC6B0"

# Process a structure by checking its state and driving sub-operations (six hundred and forty-fourth form). It takes a structure (a: fields at a->12692, a->10908, a->10912, a->13528, and a->13524) and a value (b); it checks the structure's state, and drives sub-operations (MemCpy and sub_82B9B340) based on the result. This is the "process this data" routine (six hundred and forty-fourth form), part of the 0x82208 process cluster. 246 lines. 2 callees. 5 callers (many from the 0x822*, 0x82B*, and 0x82F* clusters, and ProcessAndProcessAndProcess549).
[entrypoint.functions."0x82208CE8"]
name = "ProcessAndProcessAndProcess644_82208CE8"

# Process a value by checking its state and driving sub-operations (six hundred and forty-fifth form). It takes no arguments; it checks the value's state, and drives sub-operations (sub_82B7E050) based on the result. This is the "process this data" routine (six hundred and forty-fifth form), part of the 0x82221 process cluster. 139 lines. 1 callee. 3 callers (sub_822209A0, sub_82221508, and sub_82221680).
[entrypoint.functions."0x82221590"]
name = "ProcessAndProcessAndProcess645_82221590"

# Process a value by driving many sub-operations (six hundred and forty-sixth form). It takes three values (a, b, and c); it drives many sub-operations (CopyThreeFields, MemCpy_Vectorized, ProcessAndProcessAndProcess545, and spinlock operations) based on the result. This is the "process this data" routine (six hundred and forty-sixth form), part of the 0x82CD8 process cluster. 532 lines. 8 callees. 0 callers.
[entrypoint.functions."0x82CD8F58"]
name = "ProcessAndProcessAndProcess646_82CD8F58"

# Process a structure by driving many sub-operations (six hundred and forty-seventh form). It takes a structure (a: fields at a->76 and a->84), a value (b), and a value (c); it drives many sub-operations (RemoveFromLinkedList, Release_RefCounted, ProcessAndProcessAndProcess533, and more) based on the result. This is the "process this data" routine (six hundred and forty-seventh form), part of the 0x821D7 process cluster. 794 lines. 8 callees. 2 callers (sub_82215978 and ProcessAndProcessAndProcess501).
[entrypoint.functions."0x821D7730"]
name = "ProcessAndProcessAndProcess647_821D7730"

# Process a value by performing bit manipulation (six hundred and forty-eighth form). It takes four values (a, b, c, and d); it performs bit manipulation on the values. This is the "process this data" routine (six hundred and forty-eighth form), part of the 0x821F5 process cluster. 68 lines. 0 callees. 5 callers (many from the 0x821*, 0x822*, and 0x82B* clusters).
[entrypoint.functions."0x821F5170"]
name = "ProcessAndProcessAndProcess648_821F5170"

# Process a structure by checking its state and driving sub-operations (six hundred and forty-ninth form). It takes a structure (a: fields at a->69 and a->72) and a structure (b: field at b->0); it checks the structure's state, and drives sub-operations (ProcessAndProcessAndProcess629) based on the result. This is the "process this data" routine (six hundred and forty-ninth form), part of the 0x82CE5 process cluster. 86 lines. 1 callee. 1 caller (sub_82CDC2F0).
[entrypoint.functions."0x82CE5610"]
name = "ProcessAndProcessAndProcess649_82CE5610"

# Process a structure by checking its state (six hundred and fiftieth form). It takes a value (a), a structure (b: fields at b->5 and b->6), a structure (c: field at c->0), and a structure (d: field at d->268); it checks the structure's state. This is the "process this data" routine (six hundred and fiftieth form), part of the 0x82CE5 process cluster. 160 lines. 0 callees. 2 callers (ProcessAndProcessAndProcess629 and sub_82CDC2F0).
[entrypoint.functions."0x82CE5F78"]
name = "ProcessAndProcessAndProcess650_82CE5F78"

# Process a structure by performing vector operations (six hundred and fifty-first form). It takes a structure (a: fields at a->0 and a->80); it performs vector operations on the structure's fields. This is the "process this data" routine (six hundred and fifty-first form), part of the 0x82233 process cluster. 107 lines. 0 callees. 0 callers.
[entrypoint.functions."0x822330E8"]
name = "ProcessAndProcessAndProcess651_822330E8"

# Make a virtual call by looking up the function pointer in a vtable (a virtual call function). It takes a value (a) and a structure (b: fields at b->0 and b->8); it looks up the function pointer in the vtable, and makes the call. This is a "virtual call" routine. 19 lines. 0 callees. 0 callers.
[entrypoint.functions."0x82AB0090"]
name = "VirtualCallWithCheck5_82AB0090"

# Flush the cache by performing cache barrier operations (a cache flush function). It takes a value (a) and a value (b); it performs cache barrier operations. This is a "flush cache" routine. 90 lines. 0 callees. 12 callers (many from the 0x82A* and 0x82B* clusters).
[entrypoint.functions."0x821D11C8"]
name = "FlushCache_821D11C8"

# Process a value by checking its state and driving many sub-operations (six hundred and fifty-second form). It takes a value (a) and a value (b); it checks the value's state, and drives many sub-operations (Allocate_OrDbgBreak, SwitchDispatch, and more) based on the result. This is the "process this data" routine (six hundred and fifty-second form), part of the 0x8222C process cluster. 364 lines. 6 callees. 12 callers (many from the 0x821*, 0x822*, 0x82A*, and 0x82B* clusters).
[entrypoint.functions."0x8222CA28"]
name = "ProcessAndProcessAndProcess652_8222CA28"

# Process a structure by checking its state and driving sub-operations (six hundred and fifty-third form). It takes a structure (a: fields at a->0, a->4, a->8, and a->12); it checks the structure's state, and drives sub-operations (sub_821F9E28) based on the result. This is the "process this data" routine (six hundred and fifty-third form), part of the 0x821F9 process cluster. 88 lines. 1 callee. 10 callers (many from the 0x821*, 0x822*, and 0x82B* clusters).
[entrypoint.functions."0x821F9EA8"]
name = "ProcessAndProcessAndProcess653_821F9EA8"

# Process a structure by performing bit manipulation (six hundred and fifty-fourth form). It takes a structure (a: fields at a->0, a->4, and a->8) and a value (b); it performs bit manipulation on the structure's fields. This is the "process this data" routine (six hundred and fifty-fourth form), part of the 0x832B9 process cluster. Large. 1 callee. 1 caller (sub_832BB018).
[entrypoint.functions."0x832B95B0"]
name = "ProcessAndProcessAndProcess654_832B95B0"

# Process a value by checking its state and driving many sub-operations (six hundred and fifty-fifth form). It takes a value (a) and a value (b); it checks the value's state, and drives many sub-operations (Free_SizeBucketed_Thunk and more) based on the result. This is the "process this data" routine (six hundred and fifty-fifth form), part of the 0x82202 process cluster. 324 lines. 4 callees. 12 callers (many from the 0x82B* clusters).
[entrypoint.functions."0x822023F0"]
name = "ProcessAndProcessAndProcess655_822023F0"

# Process a value by checking its state and driving sub-operations (six hundred and fifty-sixth form). It takes no arguments; it checks the value's state, and drives sub-operations (sub_82B7E050) based on the result. This is the "process this data" routine (six hundred and fifty-sixth form), part of the 0x82221 process cluster. 139 lines. 1 callee. 3 callers (sub_82221420, sub_82221508, and sub_82232468).
[entrypoint.functions."0x82221770"]
name = "ProcessAndProcessAndProcess656_82221770"

# Process a structure by checking its state and driving sub-operations (six hundred and fifty-seventh form). It takes a structure (a: fields at a->16, a->12696, a->10908, a->10912, and a->13528) and a value (b); it checks the structure's state, and drives sub-operations (MemCpy and sub_82B9B340) based on the result. This is the "process this data" routine (six hundred and fifty-seventh form), part of the 0x82232 process cluster. 257 lines. 2 callees. 10 callers (many from the 0x822*, 0x82B*, and 0x82F* clusters, and ProcessAndProcessAndProcess552).
[entrypoint.functions."0x822324E0"]
name = "ProcessAndProcessAndProcess657_822324E0"

# Process a float by checking its state (six hundred and fifty-eighth form). It takes a value (a) and a float (b); it checks the float's state. This is the "process this data" routine (six hundred and fifty-eighth form), part of the 0x821F3 process cluster. 221 lines. 0 callees. 12 callers (many from the 0x82F* and 0x830* clusters).
[entrypoint.functions."0x821F3C80"]
name = "ProcessAndProcessAndProcess658_821F3C80"

# Process a structure by checking its state and driving many sub-operations (six hundred and fifty-ninth form). It takes a structure (a: field at a->0); it checks the structure's state, and drives many sub-operations (RtlEnterCriticalSection, RtlLeaveCriticalSection, ProcessAndProcessAndProcess655, and more) based on the result. This is the "process this data" routine (six hundred and fifty-ninth form), part of the 0x821F6 process cluster. 342 lines. 7 callees. 1 caller (DecrementRefcountAtomic).
[entrypoint.functions."0x821F6490"]
name = "ProcessAndProcessAndProcess659_821F6490"

# Process a value by driving many sub-operations (six hundred and sixtieth form). It takes five values (a, b, c, d, and e); it drives many sub-operations (RtlEnterCriticalSection, RtlLeaveCriticalSection, ProcessAndProcessAndProcess655, ProcessAndProcessAndProcess652, and more) based on the result. This is the "process this data" routine (six hundred and sixtieth form), part of the 0x821F4 process cluster. 205 lines. 7 callees. 5 callers (many from the 0x821*, 0x822*, and 0x82B* clusters).
[entrypoint.functions."0x821F4D90"]
name = "ProcessAndProcessAndProcess660_821F4D90"

# Process a value by performing bit manipulation and driving sub-operations (six hundred and sixty-first form). It takes eight values (a through h); it performs bit manipulation on the values, and drives sub-operations (ProcessAndProcessAndProcess648) based on the result. This is the "process this data" routine (six hundred and sixty-first form), part of the 0x821F4 process cluster. 344 lines. 1 callee. 2 callers (ProcessAndProcessAndProcess660 and sub_82BA5448).
[entrypoint.functions."0x821F4EF8"]
name = "ProcessAndProcessAndProcess661_821F4EF8"

# Process a value by performing bit manipulation (six hundred and sixty-second form). It takes three values (a, b, and c); it performs bit manipulation on the values. This is the "process this data" routine (six hundred and sixty-second form), part of the 0x82238 process cluster. 106 lines. 0 callees. 2 callers (sub_82FE88C0 and sub_82FEB738).
[entrypoint.functions."0x82238168"]
name = "ProcessAndProcessAndProcess662_82238168"

# Process a value by performing bit manipulation (six hundred and sixty-third form). It takes three values (a, b, and c); it performs bit manipulation on the values. This is the "process this data" routine (six hundred and sixty-third form), part of the 0x82238 process cluster. 106 lines. 0 callees. 2 callers (sub_82FE88C0 and sub_82FEB738).
[entrypoint.functions."0x82238440"]
name = "ProcessAndProcessAndProcess663_82238440"

# Process a value by checking its state and driving sub-operations (six hundred and sixty-fourth form). It takes a value (a); it checks the value's state, and drives sub-operations (sub_8273FAF8) based on the result. This is the "process this data" routine (six hundred and sixty-fourth form), part of the 0x82B67 process cluster. 195 lines. 1 callee. 1 caller (sub_82B67950).
[entrypoint.functions."0x82B67E98"]
name = "ProcessAndProcessAndProcess664_82B67E98"

# Process a structure by checking its state and driving sub-operations (six hundred and sixty-fifth form). It takes a structure (a: fields at a->4, a->0, and a->8); it checks the structure's state, and drives sub-operations (Free_SizeBucketed_Thunk and sub_82B5A890) based on the result. This is the "process this data" routine (six hundred and sixty-fifth form), part of the 0x82B5B process cluster. 70 lines. 2 callees. 3 callers (sub_82B68378, sub_832AF210, and sub_82B67950).
[entrypoint.functions."0x82B5BCC0"]
name = "ProcessAndProcessAndProcess665_82B5BCC0"

# Process a value by checking its state and driving sub-operations (six hundred and sixty-sixth form). It takes a value (a); it checks the value's state, and drives sub-operations (sub_8273FAF8) based on the result. This is the "process this data" routine (six hundred and sixty-sixth form), part of the 0x82B67 process cluster. 188 lines. 1 callee. 1 caller (sub_82B67950).
[entrypoint.functions."0x82B67FE8"]
name = "ProcessAndProcessAndProcess666_82B67FE8"

# Process a structure by performing bit manipulation (six hundred and sixty-seventh form). It takes a structure (a: fields at a->8, a->36, a->13, a->28, a->4, a->24, a->0, and a->20); it performs bit manipulation on the structure's fields. This is the "process this data" routine (six hundred and sixty-seventh form), part of the 0x82CEF process cluster. 187 lines. 0 callees. 0 callers.
[entrypoint.functions."0x82CEFDE0"]
name = "ProcessAndProcessAndProcess667_82CEFDE0"

# Process a value by driving many sub-operations (six hundred and sixty-eighth form). It takes no arguments; it drives many sub-operations (RtlEnterCriticalSection, RtlLeaveCriticalSection, ProcessAndProcessAndProcess665, ProcessAndProcessAndProcess664, ProcessAndProcessAndProcess666, and more) based on the result. This is the "process this data" routine (six hundred and sixty-eighth form), part of the 0x82B67 process cluster. 617 lines. 10 callees. 1 caller (sub_82B67DB8).
[entrypoint.functions."0x82B67950"]
name = "ProcessAndProcessAndProcess668_82B67950"

# Process a structure by checking its state and driving many sub-operations (six hundred and sixty-ninth form). It takes a structure (a: fields at a->176 and a->216) and a value (b); it checks the structure's state, and drives many sub-operations (ProcessAndProcessAndProcess562, ProcessAndProcessAndProcess564, and more) based on the result. This is the "process this data" routine (six hundred and sixty-ninth form), part of the 0x82274 process cluster. 566 lines. 4 callees. 0 callers.
[entrypoint.functions."0x82274910"]
name = "ProcessAndProcessAndProcess669_82274910"

# Process a structure by checking its state and driving sub-operations (six hundred and seventy-first form). It takes a structure (a: fields at a->32 and a->48), a value (b), and a value (c); it checks the structure's state, and drives sub-operations (sub_8225FE18) based on the result. This is the "process this data" routine (six hundred and seventy-first form), part of the 0x8225F process cluster. 61 lines. 1 callee. 4 callers (many from the 0x821*, 0x822*, and 0x82B* clusters).
[entrypoint.functions."0x8225FDB0"]
name = "ProcessAndProcessAndProcess670_8225FDB0"

# Get a bit field by extracting bits from a structure (a bit field getter). It takes a structure (a: field at a->44); it extracts bits from the field, and returns the result. This is a "get bit field" routine. 12 lines. 0 callees. 5 callers (many from the 0x82D* clusters, and ProcessAndProcessAndProcess670).
[entrypoint.functions."0x8225FE18"]
name = "GetBitField_8225FE18"

# Process a structure by checking its state and driving sub-operations (six hundred and seventy-second form). It takes a structure (a: fields at a->4, a->0, and a->8), a value (b), a value (c), a value (d), and a value (e); it checks the structure's state, and drives sub-operations (DecrementRefcountAtomic, ProcessAndProcessAndProcess660, and more) based on the result. This is the "process this data" routine (six hundred and seventy-second form), part of the 0x821FC process cluster. 101 lines. 2 callees. 9 callers (many from the 0x821*, 0x82A*, and 0x82B* clusters).
[entrypoint.functions."0x821FC7A8"]
name = "ProcessAndProcessAndProcess671_821FC7A8"

# Process a structure by checking its state and driving sub-operations (six hundred and seventy-third form). It takes a structure (a: fields at a->60, a->56, and a->52); it checks the structure's state, and drives sub-operations (Allocate_OrDbgBreak, Free_SizeBucketed_Thunk, and more) based on the result. This is the "process this data" routine (six hundred and seventy-third form), part of the 0x821EE process cluster. 104 lines. 2 callees. 2 callers (sub_8225FB90 and sub_821FBC60).
[entrypoint.functions."0x821EE710"]
name = "ProcessAndProcessAndProcess672_821EE710"

# Process a structure by checking its state and driving many sub-operations (six hundred and seventy-fourth form). It takes a structure (a: fields at a->32, a->24, and a->28), a value (b), and a value (c); it checks the structure's state, and drives many sub-operations (Allocate_OrDbgBreak, DestructRefCounted, and more) based on the result. This is the "process this data" routine (six hundred and seventy-fourth form), part of the 0x8225E process cluster. 199 lines. 5 callees. 2 callers (sub_821FBC60 and sub_8225FB90).
[entrypoint.functions."0x8225E300"]
name = "ProcessAndProcessAndProcess673_8225E300"

# Process a structure by checking its state and driving many sub-operations (six hundred and seventy-fifth form). It takes a structure (a: field at a->4), a value (b), a structure (c: field at c->0), a value (d), a value (e), and a value (f); it checks the structure's state, and drives many sub-operations (ProcessAndProcessAndProcess524, ProcessAndProcessAndProcess671, and more) based on the result. This is the "process this data" routine (six hundred and seventy-fifth form), part of the 0x821FB process cluster. 93 lines. 4 callees. 1 caller (sub_821FBC60).
[entrypoint.functions."0x821FBB20"]
name = "ProcessAndProcessAndProcess674_821FBB20"

# Process a value by driving many sub-operations (six hundred and seventy-sixth form). It takes seven values (a through g); it drives many sub-operations (IncrementRefcountAtomic, ProcessAndProcessAndProcess524, ProcessAndProcessAndProcess672, ProcessAndProcessAndProcess674, ProcessAndProcessAndProcess673, ProcessAndProcessAndProcess670, and more) based on the result. This is the "process this data" routine (six hundred and seventy-sixth form), part of the 0x821FB process cluster. 262 lines. 8 callees. 12 callers (many from the 0x821*, 0x822*, and 0x82A* clusters).
[entrypoint.functions."0x821FBC60"]
name = "ProcessAndProcessAndProcess675_821FBC60"

# Process a value by checking its state and driving many sub-operations (six hundred and seventy-seventh form). It takes a value (a); it checks the value's state, and drives many sub-operations (IncrementRefcountAtomic, DecrementRefcountAtomic, ProcessAndProcessAndProcess510, and more) based on the result. This is the "process this data" routine (six hundred and seventy-seventh form), part of the 0x821FC process cluster. 180 lines. 4 callees. 5 callers (many from the 0x821*, 0x822*, and 0x82A* clusters).
[entrypoint.functions."0x821FC0C0"]
name = "ProcessAndProcessAndProcess676_821FC0C0"

# Process a structure by checking its state and driving sub-operations (six hundred and seventy-eighth form). It takes a structure (a: fields at a->16 and a->12) and a value (b); it checks the structure's state, and drives sub-operations (sub_82A6C1C0) based on the result. This is the "process this data" routine (six hundred and seventy-eighth form), part of the 0x821E8 process cluster. 69 lines. 1 callee. 4 callers (many from the 0x821* and 0x822* clusters).
[entrypoint.functions."0x821E8A68"]
name = "ProcessAndProcessAndProcess677_821E8A68"

# Process a structure by checking its state and driving many sub-operations (six hundred and seventy-ninth form). It takes a structure (a: field at a->32) and a structure (b: fields at b->4, b->20, b->16, and b->8); it checks the structure's state, and drives many sub-operations (DecrementRefcountAtomic, ProcessAndProcessAndProcess524, ProcessAndProcessAndProcess677, ProcessAndProcessAndProcess676, and more) based on the result. This is the "process this data" routine (six hundred and seventy-ninth form), part of the 0x82237 process cluster. 84 lines. 5 callees. 12 callers (many from the 0x821*, 0x822*, and 0x82A* clusters).
[entrypoint.functions."0x82237060"]
name = "ProcessAndProcessAndProcess678_82237060"

# Process a value by performing atomic operations (six hundred and eighty-first form). It takes no arguments; it performs atomic operations on the value. This is the "process this data" routine (six hundred and eighty-first form), part of the 0x82B9E process cluster. 83 lines. 0 callees. 3 callers (sub_82B9F280, sub_82B9F0A8, and sub_82B9F730).
[entrypoint.functions."0x82B9E598"]
name = "ProcessAndProcessAndProcess679_82B9E598"

# Process a structure by performing float operations (six hundred and eightieth form). It takes a structure (a: fields at a->12, a->16, a->24, and a->20), a value (b), and a value (c); it performs float operations on the structure's fields. This is the "process this data" routine (six hundred and eightieth form), part of the 0x82C49 process cluster. 189 lines. 0 callees. 2 callers (sub_82C0EEC8 and sub_82C0EBF8).
[entrypoint.functions."0x82C497C0"]
name = "ProcessAndProcessAndProcess680_82C497C0"

# Process a structure by checking its state and driving sub-operations (six hundred and eighty-second form). It takes a structure (a: field at a->12) and a value (b); it checks the structure's state, and drives sub-operations (Free_Thunk, StoreDescriptor_Thunk, and more) based on the result. This is the "process this data" routine (six hundred and eighty-second form), part of the 0x82C0D process cluster. 200 lines. 2 callees. 2 callers (sub_82C0FFF8 and sub_82C0FB78).
[entrypoint.functions."0x82C0DD48"]
name = "ProcessAndProcessAndProcess681_82C0DD48"

# Process a structure by checking its state and driving many sub-operations (six hundred and eighty-third form). It takes a structure (a: fields at a->8 and a->20) and a structure (b: fields at b->96 and b->648); it checks the structure's state, and drives many sub-operations (StoreDescriptor_Thunk, ProcessAndProcessAndProcess680, and more) based on the result. This is the "process this data" routine (six hundred and eighty-third form), part of the 0x82C0E process cluster. 407 lines. 3 callees. 2 callers (sub_82C0FFF8 and sub_82C0FB78).
[entrypoint.functions."0x82C0EBF8"]
name = "ProcessAndProcessAndProcess682_82C0EBF8"

# Process a float by checking its state and driving sub-operations (six hundred and eighty-fourth form). It takes a value (a) and a float (b); it checks the float's state, and drives sub-operations (ProcessAndProcessAndProcess658) based on the result. This is the "process this data" routine (six hundred and eighty-fourth form), part of the 0x821B1 process cluster. 31 lines. 1 callee. 9 callers (many from the 0x821*, 0x822*, 0x82A*, 0x82B*, 0x82C*, and 0x82D* clusters).
[entrypoint.functions."0x821B1580"]
name = "ProcessAndProcessAndProcess683_821B1580"

# Process a structure by checking its state and driving sub-operations (six hundred and eighty-fifth form). It takes a structure (a: field at a->104); it checks the structure's state, and drives sub-operations (sub_8229F268) based on the result. This is the "process this data" routine (six hundred and eighty-fifth form), part of the 0x8229F process cluster. 89 lines. 1 callee. 4 callers (many from the 0x822* and 0x82A* clusters).
[entrypoint.functions."0x8229F2D8"]
name = "ProcessAndProcessAndProcess684_8229F2D8"

# Process a structure by checking its state and driving many sub-operations (six hundred and eighty-sixth form). It takes a structure (a: field at a->20), a structure (b: fields at b->8 and b->16), and a structure (c: fields at c->4 and c->0); it checks the structure's state, and drives many sub-operations (ProcessAndProcessAndProcess684, sub_82AAF160, and more) based on the result. This is the "process this data" routine (six hundred and eighty-sixth form), part of the 0x82AAE process cluster. 354 lines. 4 callees. 4 callers (many from the 0x82A* clusters).
[entrypoint.functions."0x82AAE190"]
name = "ProcessAndProcessAndProcess685_82AAE190"

# Process a float by checking its state and driving sub-operations (six hundred and eighty-seventh form). It takes a value (a) and a float (b); it checks the float's state, and drives sub-operations (ProcessAndProcessAndProcess683) based on the result. This is the "process this data" routine (six hundred and eighty-seventh form), part of the 0x82AAD process cluster. 84 lines. 1 callee. 3 callers (many from the 0x82A* clusters).
[entrypoint.functions."0x82AADEA8"]
name = "ProcessAndProcessAndProcess686_82AADEA8"

# Process a structure by performing vector operations (six hundred and eighty-eighth form). It takes a value (a) and a structure (b: fields at b->208, b->16, and b->24); it performs vector operations on the structure's fields. This is the "process this data" routine (six hundred and eighty-eighth form), part of the 0x83222 process cluster. 97 lines. 0 callees. 1 caller (ProcessAndProcessAndProcess530).
[entrypoint.functions."0x83222700"]
name = "ProcessAndProcessAndProcess687_83222700"

# Process a structure by checking its state and driving sub-operations (six hundred and eighty-ninth form). It takes a structure (a: fields at a->368, a->232, and a->360); it checks the structure's state, and drives sub-operations (sub_8322B420) based on the result. This is the "process this data" routine (six hundred and eighty-ninth form), part of the 0x821C7 process cluster. 147 lines. 1 callee. 1 caller (sub_83229B58).
[entrypoint.functions."0x821C71D0"]
name = "ProcessAndProcessAndProcess688_821C71D0"

# Process a structure by checking its state and driving many sub-operations (six hundred and ninetieth form). It takes a structure (a: field at a->340), a value (b), and a value (c); it checks the structure's state, and drives many sub-operations (DivideFloats, CheckFloatThreshold, ProcessAndProcessAndProcess601, and more) based on the result. This is the "process this data" routine (six hundred and ninetieth form), part of the 0x821CF process cluster. 219 lines. 5 callees. 1 caller (sub_82269BB8).
[entrypoint.functions."0x821CF480"]
name = "ProcessAndProcessAndProcess689_821CF480"

# Process a structure by checking its state and driving sub-operations (six hundred and ninety-first form). It takes a structure (a: fields at a->28, a->8, and a->12), a value (b), and a value (c); it checks the structure's state, and drives sub-operations (sub_8222AB20) based on the result. This is the "process this data" routine (six hundred and ninety-first form), part of the 0x821E7 process cluster. 176 lines. 1 callee. 0 callers.
[entrypoint.functions."0x821E79D0"]
name = "ProcessAndProcessAndProcess690_821E79D0"

# Process a structure by checking its state and driving many sub-operations (six hundred and ninety-second form). It takes a structure (a: fields at a->196, a->260, a->252, and a->264); it checks the structure's state, and drives many sub-operations (ProcessAndProcessAndProcess517 and more) based on the result. This is the "process this data" routine (six hundred and ninety-second form), part of the 0x82269 process cluster. 445 lines. 5 callees. 1 caller (sub_83229B58).
[entrypoint.functions."0x822698B8"]
name = "ProcessAndProcessAndProcess691_822698B8"

# Process a structure by checking its state and driving many sub-operations (six hundred and ninety-third form). It takes a structure (a: fields at a->368, a->340, and a->360) and a value (b); it checks the structure's state, and drives many sub-operations (DivideFloats, ProcessAndProcessAndProcess591, ProcessAndProcessAndProcess592, ProcessAndProcessAndProcess593, ProcessAndProcessAndProcess594, ProcessAndProcessAndProcess689, and more) based on the result. This is the "process this data" routine (six hundred and ninety-third form), part of the 0x82269 process cluster. 581 lines. 10 callees. 1 caller (sub_83229B58).
[entrypoint.functions."0x82269BB8"]
name = "ProcessAndProcessAndProcess692_82269BB8"

# Process a value by performing vector operations (six hundred and ninety-fourth form). It takes a value (a); it performs vector operations on the value. This is the "process this data" routine (six hundred and ninety-fourth form), part of the 0x8227C process cluster. 221 lines. 0 callees. 5 callers (many from the 0x821*, 0x822*, 0x82A*, 0x82B*, 0x82C*, and 0x832* clusters).
[entrypoint.functions."0x8227C9F8"]
name = "ProcessAndProcessAndProcess693_8227C9F8"

# Process a structure by performing float operations (six hundred and ninety-fifth form). It takes a structure (a: fields at a->252 and a->196); it performs float operations on the structure's fields. This is the "process this data" routine (six hundred and ninety-fifth form), part of the 0x83222 process cluster. 44 lines. 0 callees. 0 callers.
[entrypoint.functions."0x83222978"]
name = "ProcessAndProcessAndProcess694_83222978"

# Process a structure by checking its state and driving many sub-operations (six hundred and ninety-sixth form). It takes a structure (a: fields at a->240 and a->236), a value (b), and a value (c); it checks the structure's state, and drives many sub-operations (ProcessAndProcessAndProcess530, ProcessAndProcessAndProcess587, and more) based on the result. This is the "process this data" routine (six hundred and ninety-sixth form), part of the 0x83222 process cluster. 126 lines. 3 callees. 1 caller (sub_83226470).
[entrypoint.functions."0x83222CA8"]
name = "ProcessAndProcessAndProcess695_83222CA8"

# Process a structure by checking its state and driving many sub-operations (six hundred and ninety-seventh form). It takes a value (a) and a structure (b: fields at b->436 and b->440); it checks the structure's state, and drives many sub-operations (CheckValidPointer, ProcessAndProcessAndProcess550, ProcessAndProcessAndProcess695, and more) based on the result. This is the "process this data" routine (six hundred and ninety-seventh form), part of the 0x83226 process cluster. 82 lines. 4 callees. 1 caller (sub_83229DC0).
[entrypoint.functions."0x83226470"]
name = "ProcessAndProcessAndProcess696_83226470"

# Process a structure by checking its state and driving sub-operations (six hundred and ninety-eighth form). It takes a structure (a: fields at a->252, a->196, and a->444); it checks the structure's state, and drives sub-operations (ProcessAndProcessAndProcess596) based on the result. This is the "process this data" routine (six hundred and ninety-eighth form), part of the 0x83226 process cluster. 117 lines. 1 callee. 2 callers (many from the 0x832* clusters).
[entrypoint.functions."0x832265D8"]
name = "ProcessAndProcessAndProcess697_832265D8"

# Dispatch to one of several handlers based on a type code (third form). It takes a structure (a: field at a->368) and a value (b); it reads the type code from a->368->32 and dispatches to one of several handlers (ProcessAndProcessAndProcess688, ProcessAndProcessAndProcess691, ProcessAndProcessAndProcess692, and more) based on its value. This is the type-code dispatcher (third form), part of the 0x83229 dispatch cluster. 149 lines. 10 callees. 0 callers.
[entrypoint.functions."0x83229B58"]
name = "DispatchByTypeCode3_83229B58"

# Process a large data structure (six hundred and ninety-eighth form). It checks the structure's state and drives many sub-operations (DivideFloats, ProcessAndProcessAndProcess517, ProcessAndProcessAndProcess553, ProcessAndProcessAndProcess596, ProcessAndProcessAndProcess693, ProcessAndProcessAndProcess696, ProcessAndProcessAndProcess697, and more) based on the result. Part of the 0x83229 dispatch cluster. 655 lines. 10 callees. 0 callers.
[entrypoint.functions."0x83229DC0"]
name = "ProcessAndProcessAndProcess698_83229DC0"

# Iterate over a list of elements, calling a virtual method on each one. It takes a list (a->0, spanning a->28 to a->40); it walks the elements, invoking the per-element virtual method (via the element's vtable) and releasing reference counts (Release_RefCounted) as it goes. This is the "process each list element" routine, part of the 0x8322D cluster. 85 lines. 1 callee. 1 caller (sub_8322E3C0).
[entrypoint.functions."0x8322D018"]
name = "ProcessListElements_8322D018"

# Process a large data structure (six hundred and ninety-ninth form). It checks the structure's state and drives sub-operations (ProcessAndProcessAndProcess588, Release_RefCounted, and more) based on the result. Part of the 0x8322D cluster. 336 lines. 4 callees. 1 caller (sub_8322E208).
[entrypoint.functions."0x8322D678"]
name = "ProcessAndProcessAndProcess699_8322D678"

# Process a structure by setting a flag and dispatching on its state (seven hundredth form). It sets a flag in a list (SetFlagInList) and dispatches on the structure's state (SwitchDispatch). Part of the 0x822B8 process cluster. 130 lines. 2 callees. 3 callers (sub_82AAE5C0, sub_82AAEC58, sub_82AAED38).
[entrypoint.functions."0x822B82F8"]
name = "ProcessAndProcessAndProcess700_822B82F8"

# Process a structure by driving a sub-operation (seven hundred and first form). It checks the structure's state and drives a sub-operation (ProcessAndProcessAndProcess611) based on the result. Part of the 0x82AAD process cluster. 190 lines. 1 callee. 3 callers (sub_82AAE5C0, sub_82AAEC58, sub_82AAED38).
[entrypoint.functions."0x82AADFF8"]
name = "ProcessAndProcessAndProcess701_82AADFF8"

# Process a value (seven hundred and second form). It takes a value and drives a sub-operation (sub_821940C8). Part of the 0x83222 process cluster. 59 lines. 1 callee. 1 caller (sub_83223150).
[entrypoint.functions."0x83222B20"]
name = "ProcessAndProcessAndProcess702_83222B20"

# Process a value by wrapping a sub-operation (seven hundred and third form). It takes a value and drives sub-operations (ProcessAndProcessAndProcess702, sub_821940C8). Part of the 0x83223 process cluster. 124 lines. 2 callees. 1 caller (sub_8322DAC0).
[entrypoint.functions."0x83223150"]
name = "ProcessAndProcessAndProcess703_83223150"

# Copy an object from a source to a destination. It copies the object's header fields (source->48, source->32, source->40, source->20) to the destination, then copies the object body (MemCpy_Vectorized) when the size warrants it. This is the "copy object" routine, part of the 0x821C6/0x821C7 object-serialization cluster. 131 lines. 2 callees. 2 callers (sub_821C70B8 and sub_82293290).
[entrypoint.functions."0x821C6FD8"]
name = "CopyObject_821C6FD8"

# Clone an object by allocating a destination and copying into it. It allocates fresh memory (Allocate_OrDbgBreak), copies the source object into it (CopyObject), and finalizes the clone (sub_82A3F478). This is the "clone object" routine (allocate + copy), part of the 0x821C6/0x821C7 object-serialization cluster. 166 lines. 3 callees. 1 caller (sub_8227CB98).
[entrypoint.functions."0x821C70B8"]
name = "CloneObject_821C70B8"

# Process a structure (seven hundred and fourth form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x821CD process cluster. 138 lines. 0 callees. 2 callers (sub_82282ED8 and sub_82A3F7E8).
[entrypoint.functions."0x821CDC28"]
name = "ProcessAndProcessAndProcess704_821CDC28"

# Process a large data structure (seven hundred and fifth form). It takes a structure, checks its state, and drives many sub-operations (Allocate_OrDbgBreak, Allocate_SizeBucketed, Free_SizeBucketed, ProcessAndProcessAndProcess640, and more) based on the result. Part of the 0x821CE process cluster. 1609 lines. 12 callees. 0 callers.
[entrypoint.functions."0x821CE6C8"]
name = "ProcessAndProcessAndProcess705_821CE6C8"

# Process a structure by cloning and updating it (seven hundred and sixth form). It takes a structure, checks its state, and drives sub-operations (CloneObject, sub_822BFD90, sub_82A3F378) based on the result. Part of the 0x8227C process cluster. 135 lines. 3 callees. 0 callers.
[entrypoint.functions."0x8227CB98"]
name = "ProcessAndProcessAndProcess706_8227CB98"

# Process a structure by driving sub-operations (seven hundred and seventh form). It takes a structure and drives sub-operations (ProcessAndProcessAndProcess503, ProcessAndProcessAndProcess704) based on its state. Part of the 0x82282 process cluster. 92 lines. 2 callees. 0 callers.
[entrypoint.functions."0x82282ED8"]
name = "ProcessAndProcessAndProcess707_82282ED8"

# Process a value (seven hundred and eighth form). It takes a value and checks its state, with no external sub-operations. Part of the 0x8241B process cluster. 73 lines. 0 callees. 0 callers.
[entrypoint.functions."0x8241BDF8"]
name = "ProcessAndProcessAndProcess708_8241BDF8"

# Process a value (seven hundred and ninth form). It takes a value and checks its state, with no external sub-operations. Part of the 0x83217 process cluster. 71 lines. 0 callees. 3 callers (sub_83221FC8, sub_832389D8, sub_832391D8).
[entrypoint.functions."0x83217A38"]
name = "ProcessAndProcessAndProcess709_83217A38"

# Process a large data structure (seven hundred and tenth form). It takes a structure, checks its state, and drives many sub-operations (Free_SizeBucketed, ProcessAndProcessAndProcess709, and more) based on the result, freeing resources as it goes. Part of the 0x83221 process cluster. 358 lines. 8 callees. 9 callers.
[entrypoint.functions."0x83221FC8"]
name = "ProcessAndProcessAndProcess710_83221FC8"

# Process a large data structure by dispatching on its state (seven hundred and eleventh form). It takes a structure, checks its state, and drives sub-operations (ProcessAndProcessAndProcess611, ProcessAndProcessAndProcess684, ProcessAndProcessAndProcess712, ProcessAndProcessAndProcess713) based on the result. This is the dispatcher for the 0x822B8/0x82AAE process cluster. 455 lines. 4 callees. 4 callers.
[entrypoint.functions."0x822B80A0"]
name = "ProcessAndProcessAndProcess711_822B80A0"

# Process a structure by driving a sub-operation (seven hundred and twelfth form). It takes a structure and drives a sub-operation (ProcessAndProcessAndProcess686) based on its state. Part of the 0x82AAE process cluster, dispatched by ProcessAndProcessAndProcess711. 69 lines. 1 callee. 1 caller (sub_822B80A0).
[entrypoint.functions."0x82AAE120"]
name = "ProcessAndProcessAndProcess712_82AAE120"

# Process a structure by driving several sub-operations (seven hundred and thirteenth form). It takes a structure and drives sub-operations (ProcessAndProcessAndProcess685, ProcessAndProcessAndProcess700, ProcessAndProcessAndProcess701) based on its state. Part of the 0x82AAE process cluster, dispatched by ProcessAndProcessAndProcess711. 64 lines. 3 callees. 1 caller (sub_822B80A0).
[entrypoint.functions."0x82AAE5C0"]
name = "ProcessAndProcessAndProcess713_82AAE5C0"

# Process a large data structure (seven hundred and fourteenth form). It takes a structure, checks its state, and drives sub-operations (CheckFloatSign, ProcessAndProcessAndProcess715, and more) based on the result. Part of the 0x821D8 process cluster. 543 lines. 4 callees. 0 callers.
[entrypoint.functions."0x821D8B60"]
name = "ProcessAndProcessAndProcess714_821D8B60"

# Allocate and copy an object (seven hundred and fifteenth form). It takes a structure, allocates fresh memory (Allocate_OrDbgBreak), and copies the source into it (CopyObject). Part of the 0x82293 process cluster. 202 lines. 2 callees. 1 caller (ProcessAndProcessAndProcess714).
[entrypoint.functions."0x82293290"]
name = "ProcessAndProcessAndProcess715_82293290"

# Process a structure by copying its data (seven hundred and sixteenth form). It takes a structure and drives sub-operations (MemCpy_SdkRuntime, ProcessAndProcessAndProcess503) based on its state. Part of the 0x821D7 process cluster. 102 lines. 2 callees. 0 callers.
[entrypoint.functions."0x821D7C78"]
name = "ProcessAndProcessAndProcess716_821D7C78"

# Process a frequently-used structure (seven hundred and seventeenth form). It takes a structure and drives sub-operations (ProcessAndProcessAndProcess313, ProcessAndProcessAndProcess314, ProcessAndProcessAndProcess503, ProcessAndProcessAndProcess560) based on its state. This is a hot utility in the 0x8222A process cluster. 105 lines. 5 callees. 27 callers.
[entrypoint.functions."0x8222AB20"]
name = "ProcessAndProcessAndProcess717_8222AB20"

# Process a structure (seven hundred and eighteenth form). It takes a structure, checks its state, and drives a sub-operation (sub_82A6CF88) based on the result. Part of the 0x821D9 process cluster. 145 lines. 1 callee. 0 callers.
[entrypoint.functions."0x821D9990"]
name = "ProcessAndProcessAndProcess718_821D9990"

# Process a large data structure by dispatching on its state (seven hundred and nineteenth form). It takes a structure, checks its state, and drives sub-operations (ProcessAndProcessAndProcess618, SetFlagInList, SwitchDispatch, ProcessAndProcessAndProcess711) based on the result. Part of the 0x8221A process cluster. 325 lines. 4 callees. 2 callers (sub_821F4590 and sub_82222400).
[entrypoint.functions."0x8221AD90"]
name = "ProcessAndProcessAndProcess719_8221AD90"

# Process a structure using the timebase (seven hundred and twentieth form). It takes a structure, reads the timebase (GetTimebase), and drives a sub-operation (ProcessAndProcessAndProcess719) based on the result. Part of the 0x82222 process cluster. 175 lines. 2 callees. 2 callers (sub_8222AB20 and sub_822E0508).
[entrypoint.functions."0x82222400"]
name = "ProcessAndProcessAndProcess720_82222400"

# Free a size-bucketed object (seven hundred and twenty-first form). It takes a structure and frees its size-bucketed memory (Free_SizeBucketed). Part of the 0x82546 process cluster. 45 lines. 1 callee. 0 callers.
[entrypoint.functions."0x825469A0"]
name = "ProcessAndProcessAndProcess721_825469A0"

# Process a value (seven hundred and twenty-second form). It takes a value and checks its state, with no external sub-operations. Part of the 0x8322A process cluster. 33 lines. 0 callees. 0 callers.
[entrypoint.functions."0x8322A478"]
name = "ProcessAndProcessAndProcess722_8322A478"

# Process a structure by driving sub-operations (seven hundred and twenty-third form). It takes a structure, checks its state, and drives sub-operations (ProcessAndProcessAndProcess550, sub_821940C8, sub_83235820) based on the result. Part of the 0x83223 process cluster. 234 lines. 3 callees. 2 callers (ProcessAndProcessAndProcess516 and sub_8323A408).
[entrypoint.functions."0x83223220"]
name = "ProcessAndProcessAndProcess723_83223220"

# Process a structure by driving sub-operations (seven hundred and twenty-fourth form). It takes a structure, checks its state, and drives sub-operations (sub_82A00700, ProcessAndProcessAndProcess726) based on the result. Part of the 0x8322A process cluster. 171 lines. 2 callees. 1 caller (sub_8322A908).
[entrypoint.functions."0x8322A6A8"]
name = "ProcessAndProcessAndProcess724_8322A6A8"

# Process a structure by driving sub-operations (seven hundred and twenty-fifth form). It takes a structure, checks its state, and drives sub-operations (ProcessAndProcessAndProcess724, sub_8322B330, ProcessAndProcessAndProcess728) based on the result. Part of the 0x8322A process cluster. 141 lines. 3 callees. 0 callers.
[entrypoint.functions."0x8322A908"]
name = "ProcessAndProcessAndProcess725_8322A908"

# Process a large data structure (seven hundred and twenty-sixth form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x8322F process cluster. 552 lines. 0 callees. 2 callers (sub_83227D40 and sub_8322A6A8).
[entrypoint.functions."0x8322F0B0"]
name = "ProcessAndProcessAndProcess726_8322F0B0"

# Process a structure (seven hundred and twenty-seventh form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x83236 process cluster. 189 lines. 0 callees. 0 callers.
[entrypoint.functions."0x83236678"]
name = "ProcessAndProcessAndProcess727_83236678"

# Process a structure by driving sub-operations (seven hundred and twenty-eighth form). It takes a structure, checks its state, and drives sub-operations (ProcessAndProcessAndProcess517, ProcessAndProcessAndProcess550, ProcessAndProcessAndProcess723) based on the result. This is the shared tail of the 0x8322A/0x83223 process cluster. 188 lines. 3 callees. 5 callers.
[entrypoint.functions."0x8323A408"]
name = "ProcessAndProcessAndProcess728_8323A408"

# Process a value (seven hundred and twenty-ninth form). It takes a value and checks its state, with no external sub-operations. Part of the 0x83222 process cluster. 38 lines. 0 callees. 0 callers.
[entrypoint.functions."0x83222330"]
name = "ProcessAndProcessAndProcess729_83222330"

# Manage a refcounted buffer by allocating, reassigning, and releasing it (seven hundred and thirtieth form). It takes a buffer structure, allocates fresh memory (Allocate_OrDbgBreak) when needed, adjusts the reference count (AtomicIncrement5, Release_RefCounted), and reassigns the buffer (ReassignBuffer) based on the result. This is a frequently-used buffer manager. 135 lines. 5 callees. 11 callers.
[entrypoint.functions."0x82197488"]
name = "ProcessAndProcessAndProcess730_82197488"

# Process a structure (seven hundred and thirty-first form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82B8B process cluster. 63 lines. 0 callees. 4 callers.
[entrypoint.functions."0x82B8BA00"]
name = "ProcessAndProcessAndProcess731_82B8BA00"

# Process a large data structure (seven hundred and thirty-second form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82A60 process cluster. 348 lines. 0 callees. 1 caller (ProcessAndProcessAndProcess586).
[entrypoint.functions."0x82A60BB8"]
name = "ProcessAndProcessAndProcess732_82A60BB8"

# Process a structure by driving a sub-operation (seven hundred and thirty-third form). It takes a structure, checks its state, and drives a sub-operation (ProcessAndProcessAndProcess603) based on the result. Part of the 0x82286 process cluster. 81 lines. 1 callee. 2 callers (ProcessAndProcessAndProcess567 and sub_82264550).
[entrypoint.functions."0x82286850"]
name = "ProcessAndProcessAndProcess733_82286850"

# Process a structure (seven hundred and thirty-fourth form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82C5D process cluster. 90 lines. 0 callees. 1 caller (sub_82C5DD68).
[entrypoint.functions."0x82C5DC48"]
name = "ProcessAndProcessAndProcess734_82C5DC48"

# Process a structure by copying it (seven hundred and thirty-fifth form). It takes a structure, copies the structure (CopyStructure), and drives a sub-operation (ProcessAndProcessAndProcess503) based on the result. Part of the 0x82A3B process cluster. 110 lines. 2 callees. 0 callers.
[entrypoint.functions."0x82A3B670"]
name = "ProcessAndProcessAndProcess735_82A3B670"

# Process a value (seven hundred and thirty-sixth form). It takes a value and checks its state, with no external sub-operations. Part of the 0x8269B process cluster. 8 lines. 0 callees. 0 callers.
[entrypoint.functions."0x8269B628"]
name = "ProcessAndProcessAndProcess736_8269B628"

# Process a large data structure by copying it (seven hundred and thirty-seventh form). It takes a structure, copies the structure (CopyStructure2), and drives a sub-operation (ProcessAndProcessAndProcess604) based on the result. Part of the 0x82A60 process cluster. 440 lines. 2 callees. 0 callers.
[entrypoint.functions."0x82A60DE8"]
name = "ProcessAndProcessAndProcess737_82A60DE8"

# Grow and clear a bit array buffer (seven hundred and thirty-eighth form). It takes a buffer structure, grows the bit array (GrowBitArray), and clears the newly-allocated space (MemSet). This is the "grow buffer" helper for the 0x82197 refcounted bit-array buffer cluster, called by ProcessAndProcessAndProcess730. 77 lines. 2 callees. 1 caller (sub_82197488).
[entrypoint.functions."0x82197400"]
name = "ProcessAndProcessAndProcess738_82197400"

# Process a structure by copying its data (seven hundred and thirty-ninth form). It takes a structure and copies its data (MemCpy). Part of the 0x82B9D process cluster. 114 lines. 1 callee. 1 caller (sub_82B9D618).
[entrypoint.functions."0x82B9D490"]
name = "ProcessAndProcessAndProcess739_82B9D490"

# Process a value (seven hundred and fortieth form). It takes a value and checks its state, with no external sub-operations. Part of the 0x8222C process cluster. 34 lines. 0 callees. 0 callers.
[entrypoint.functions."0x8222C870"]
name = "ProcessAndProcessAndProcess740_8222C870"

# Process a value (seven hundred and forty-first form). It takes a value and checks its state, with no external sub-operations. This is a frequently-called helper in the 0x82CD7 process cluster. 29 lines. 0 callees. 8 callers.
[entrypoint.functions."0x82CD7D68"]
name = "ProcessAndProcessAndProcess741_82CD7D68"

# Process a structure by driving sub-operations (seven hundred and forty-second form). It takes a structure, checks its state, and drives sub-operations (sub_82C03C50, sub_82C03E40) based on the result. Part of the 0x82C4B process cluster. 109 lines. 2 callees. 1 caller (sub_82C55160).
[entrypoint.functions."0x82C4BF98"]
name = "ProcessAndProcessAndProcess742_82C4BF98"

# Process a large data structure under a critical section (seven hundred and forty-third form). It takes a structure, enters or tries the critical section (CriticalSection_EnterOrTry), and drives sub-operations (ProcessAndLeaveCritical) based on the result. Part of the 0x82C52 process cluster, dispatched by ProcessAndProcessAndProcess746. 576 lines. 2 callees. 1 caller (sub_82C55160).
[entrypoint.functions."0x82C52F60"]
name = "ProcessAndProcessAndProcess743_82C52F60"

# Process a structure by driving sub-operations (seven hundred and forty-fourth form). It takes a structure, checks its state, and drives sub-operations (ProcessAndProcessAndProcess751, sub_82C53A00) based on the result. Part of the 0x82C53 process cluster, dispatched by ProcessAndProcessAndProcess746. 78 lines. 2 callees. 1 caller (sub_82C55160).
[entrypoint.functions."0x82C53D10"]
name = "ProcessAndProcessAndProcess744_82C53D10"

# Process a structure by driving sub-operations (seven hundred and forty-fifth form). It takes a structure, checks its state, and drives sub-operations (sub_82C52A80, sub_82C543F8, sub_82C544D0, sub_82C54E08) based on the result. Part of the 0x82C55 process cluster, dispatched by ProcessAndProcessAndProcess746. 94 lines. 4 callees. 1 caller (sub_82C55160).
[entrypoint.functions."0x82C550C0"]
name = "ProcessAndProcessAndProcess745_82C550C0"

# Process pending timed operations (seven hundred and forty-sixth form). It takes a structure, reads the current time (GetCurrentTimeMillis), and dispatches to the timed sub-operations (ProcessAndProcessAndProcess743, ProcessAndProcessAndProcess744, ProcessAndProcessAndProcess745) based on the result. This is the dispatcher for the 0x82C52/0x82C53/0x82C55 timed-process cluster. 91 lines. 6 callees. 0 callers.
[entrypoint.functions."0x82C55160"]
name = "ProcessAndProcessAndProcess746_82C55160"

# Process a value (seven hundred and forty-seventh form). It takes a value and checks its state, with no external sub-operations. Part of the 0x82C55 process cluster. 13 lines. 0 callees. 0 callers.
[entrypoint.functions."0x82C553D0"]
name = "ProcessAndProcessAndProcess747_82C553D0"

# Process a structure by normalizing a vector (seven hundred and forty-eighth form). It takes a structure, normalizes the vector (NormalizeVector), and drives sub-operations based on the result. Part of the 0x8322B process cluster. 98 lines. 1 callee. 2 callers (ProcessAndProcessAndProcess699 and sub_8322B1E8).
[entrypoint.functions."0x8322B158"]
name = "ProcessAndProcessAndProcess748_8322B158"

# Process a structure by driving a sub-operation (seven hundred and forty-ninth form). It takes a structure and drives a sub-operation (ProcessAndProcessAndProcess624) based on its state. Part of the 0x82A67 process cluster. 18 lines. 1 callee. 0 callers.
[entrypoint.functions."0x82A67378"]
name = "ProcessAndProcessAndProcess749_82A67378"

# Process a value (seven hundred and fiftieth form). It takes a value and checks its state, with no external sub-operations. Part of the 0x82A67 process cluster. 13 lines. 0 callees. 0 callers.
[entrypoint.functions."0x82A67390"]
name = "ProcessAndProcessAndProcess750_82A67390"

# Free a large data structure and its memory (seven hundred and fifty-first form). It takes a structure, frees its memory (Free_Thunk), clears the freed space (MemSet), and drives sub-operations (sub_82CD2C38, ProcessAndProcessAndProcess752) based on the result. This is the memory-freeing tail of the 0x82C53 process cluster. 377 lines. 4 callees. 3 callers.
[entrypoint.functions."0x82C53750"]
name = "ProcessAndProcessAndProcess751_82C53750"

# Process a structure (seven hundred and fifty-second form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82CD2 process cluster. 54 lines. 0 callees. 1 caller (sub_82C53750).
[entrypoint.functions."0x82CD2F00"]
name = "ProcessAndProcessAndProcess752_82CD2F00"

# Process a value (seven hundred and fifty-third form). It takes a value and checks its state, with no external sub-operations. Part of the 0x82CE0 process cluster. 12 lines. 0 callees. 0 callers.
[entrypoint.functions."0x82CE0BD8"]
name = "ProcessAndProcessAndProcess753_82CE0BD8"

# Process a value (seven hundred and fifty-fourth form). It takes a value and checks its state, with no external sub-operations. Part of the 0x82CDC process cluster. 52 lines. 0 callees. 0 callers.
[entrypoint.functions."0x82CDC3E0"]
name = "ProcessAndProcessAndProcess754_82CDC3E0"

# Process a structure (seven hundred and fifty-fifth form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82CE3 process cluster. 122 lines. 0 callees. 1 caller (TailCall_82CE3770).
[entrypoint.functions."0x82CE3460"]
name = "ProcessAndProcessAndProcess755_82CE3460"

# Process a value (seven hundred and fifty-sixth form). It takes a value and checks its state, with no external sub-operations. This is a frequently-called helper in the 0x82CD2 process cluster. 73 lines. 0 callees. 5 callers.
[entrypoint.functions."0x82CD2F58"]
name = "ProcessAndProcessAndProcess756_82CD2F58"

# Process a structure by driving a sub-operation (seven hundred and fifty-seventh form). It takes a structure, checks its state, and drives a sub-operation (sub_82CD2B88) based on the result. Part of the 0x82C52 process cluster. 135 lines. 1 callee. 1 caller (sub_82C543F8).
[entrypoint.functions."0x82C522A8"]
name = "ProcessAndProcessAndProcess757_82C522A8"

# Render a large UI text element (seven hundred and fifty-eighth form). It takes a structure, looks up chain values (FindChainValue), makes virtual calls (ProcessVcall3), and drives many sub-operations (ProcessAndProcessAndProcess504, ProcessAndProcessAndProcess615, ProcessAndProcessAndProcess616, and more) based on the result. This is the large UI text rendering routine, part of the 0x82C52 UI-text cluster. 686 lines. 9 callees. 1 caller (sub_82C550C0).
[entrypoint.functions."0x82C52A80"]
name = "ProcessAndProcessAndProcess758_82C52A80"

# Process a UI text element (seven hundred and fifty-ninth form). It takes a structure, checks its state, and drives sub-operations (ProcessAndProcessAndProcess683, ProcessAndProcessAndProcess757, sub_82C54320, sub_82CD2D38, ProcessAndProcessAndProcess756) based on the result. Part of the 0x82C54 UI-text cluster. 142 lines. 6 callees. 2 callers (sub_82C544D0 and sub_82C550C0).
[entrypoint.functions."0x82C543F8"]
name = "ProcessAndProcessAndProcess759_82C543F8"

# Render UI text glyph quads (seven hundred and sixtieth form). It takes a structure, checks its state, and drives sub-operations (Free_Thunk, UIFont_EmitGlyphQuad, sub_82C51E20, sub_82C53358, sub_82C53FE0, ProcessAndProcessAndProcess759) based on the result. This is a UI text glyph-emission routine, part of the 0x82C54 UI-text cluster. 270 lines. 7 callees. 1 caller (sub_82C550C0).
[entrypoint.functions."0x82C544D0"]
name = "ProcessAndProcessAndProcess760_82C544D0"

# Render UI text glyph quads (seven hundred and sixty-first form). It takes a structure, checks its state, and drives sub-operations (UIFont_EmitGlyphQuad, sub_82C54250, sub_82C54DB0, sub_82CD2CF0, sub_82CD2D38, sub_82CD2E50, ProcessAndProcessAndProcess756) based on the result. This is a UI text glyph-emission routine, part of the 0x82C54 UI-text cluster. 160 lines. 7 callees. 1 caller (sub_82C550C0).
[entrypoint.functions."0x82C54E08"]
name = "ProcessAndProcessAndProcess761_82C54E08"

# Process a value (seven hundred and sixty-second form). It takes a value and checks its state, with no external sub-operations. Part of the 0x82C03 process cluster. 29 lines. 0 callees. 1 caller (ProcessAndProcessAndProcess742).
[entrypoint.functions."0x82C03C50"]
name = "ProcessAndProcessAndProcess762_82C03C50"

# Process a value (seven hundred and sixty-third form). It takes a value and checks its state, with no external sub-operations. This is a frequently-called helper in the 0x82FFF process cluster. 27 lines. 0 callees. 24 callers.
[entrypoint.functions."0x82FFFB40"]
name = "ProcessAndProcessAndProcess763_82FFFB40"

# Process a structure (seven hundred and sixty-fourth form). It takes a structure, checks its state, and drives sub-operations (sub_821D17B8, sub_821E8D20) based on the result. This is a frequently-called helper, including from the main render loop. 73 lines. 2 callees. 15 callers.
[entrypoint.functions."0x82213C10"]
name = "ProcessAndProcessAndProcess764_82213C10"

# Process a frequently-used structure (seven hundred and sixty-fifth form). It takes a structure, checks its state, and drives sub-operations (ProcessAndProcessAndProcess503, ProcessAndProcessAndProcess560, sub_82A83420) based on the result. This is a very hot utility in the 0x821DD process cluster. 183 lines. 3 callees. 60 callers.
[entrypoint.functions."0x821DDE30"]
name = "ProcessAndProcessAndProcess765_821DDE30"

# Process a frequently-used structure (seven hundred and sixty-sixth form). It takes a structure, checks its state, and drives sub-operations (sub_8235E148, sub_82A49AA8, sub_82A83420) based on the result. Part of the 0x821E1 process cluster. 230 lines. 3 callees. 15 callers.
[entrypoint.functions."0x821E1B00"]
name = "ProcessAndProcessAndProcess766_821E1B00"

# Process a structure by driving sub-operations (seven hundred and sixty-seventh form). It takes a structure, checks its state, and drives sub-operations (ProcessAndProcessAndProcess711, ProcessAndProcessAndProcess769) based on the result. Part of the 0x82A4F process cluster. 74 lines. 2 callees. 3 callers.
[entrypoint.functions."0x82A4F270"]
name = "ProcessAndProcessAndProcess767_82A4F270"

# Process a value (seven hundred and sixty-eighth form). It takes a value and checks its state, with no external sub-operations. Part of the 0x82A6D process cluster. 8 lines. 0 callees. 0 callers.
[entrypoint.functions."0x82A6D018"]
name = "ProcessAndProcessAndProcess768_82A6D018"

# Insert elements into a structure (seven hundred and sixty-ninth form). It takes a structure, checks its state, and drives sub-operations (InsertElement4, sub_82AB20E0) based on the result. This is the element-insertion routine of the 0x82A4F/0x82A6D/0x82A7F element cluster. 333 lines. 2 callees. 4 callers.
[entrypoint.functions."0x82A7F338"]
name = "ProcessAndProcessAndProcess769_82A7F338"

# Process a structure by wrapping a sub-operation (seven hundred and seventieth form). It takes a structure and drives a sub-operation (ProcessAndProcessAndProcess767). Part of the 0x82A6D process cluster. 13 lines. 1 callee. 0 callers.
[entrypoint.functions."0x82A6D008"]
name = "ProcessAndProcessAndProcess770_82A6D008"

# Process a structure by driving sub-operations (seven hundred and seventy-first form). It takes a structure, checks its state, and drives sub-operations (ProcessAndProcessAndProcess523, sub_82B9BEC8) based on the result. Part of the 0x82286 process cluster. 119 lines. 2 callees. 2 callers.
[entrypoint.functions."0x82286180"]
name = "ProcessAndProcessAndProcess771_82286180"

# Process a structure by driving sub-operations (seven hundred and seventy-second form). It takes a structure, checks its state, and drives sub-operations (ProcessAndProcessAndProcess620, ProcessAndProcessAndProcess653) based on the result. Part of the 0x821FA process cluster. 97 lines. 2 callees. 4 callers.
[entrypoint.functions."0x821FA028"]
name = "ProcessAndProcessAndProcess772_821FA028"

# Process a structure by driving sub-operations (seven hundred and seventy-third form). It takes a structure, checks its state, and drives sub-operations (ProcessAndProcessAndProcess645, sub_82B7E050) based on the result. Part of the 0x82221 process cluster. 138 lines. 2 callees. 4 callers.
[entrypoint.functions."0x82221680"]
name = "ProcessAndProcessAndProcess773_82221680"

# Process a structure (seven hundred and seventy-fourth form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x821B1 process cluster. 103 lines. 0 callees. 3 callers.
[entrypoint.functions."0x821B1D20"]
name = "ProcessAndProcessAndProcess774_821B1D20"

# Grow a buffer and append to it (seven hundred and seventy-fifth form). It takes a buffer and grows it, appending data (GrowBufferAndAppend). Part of the 0x821F4 process cluster. 78 lines. 1 callee. 2 callers (sub_821F4350 and sub_82A7F950).
[entrypoint.functions."0x821F42C8"]
name = "ProcessAndProcessAndProcess775_821F42C8"

# Append to a buffer (seven hundred and seventy-sixth form). It takes a buffer and drives a sub-operation (ProcessAndProcessAndProcess775) that grows the buffer and appends to it. This is the frequently-used buffer-append path in the 0x821F4 process cluster. 142 lines. 1 callee. 21 callers.
[entrypoint.functions."0x821F4350"]
name = "ProcessAndProcessAndProcess776_821F4350"

# Process a large data structure (seven hundred and seventy-seventh form). It takes a structure, copies its data (MemCpy), and checks its state. Part of the 0x821D3 process cluster. 841 lines. 1 callee. 3 callers.
[entrypoint.functions."0x821D3318"]
name = "ProcessAndProcessAndProcess777_821D3318"

# Process a structure by flushing the cache and copying data (seven hundred and seventy-eighth form). It takes a structure, flushes the cache (FlushCache), copies data (MemCpy), and drives sub-operations (ProcessAndProcessAndProcess777, ProcessAndProcessAndProcess764) based on the result. Part of the 0x821DF process cluster. 205 lines. 5 callees. 1 caller (ProcessAndProcessAndProcess557).
[entrypoint.functions."0x821DFDE0"]
name = "ProcessAndProcessAndProcess778_821DFDE0"

# Process a structure by driving sub-operations (seven hundred and seventy-ninth form). It takes a structure, checks its state, and drives sub-operations (ProcessAndProcessAndProcess739, sub_82B9D560) based on the result. Part of the 0x82B9D process cluster. 192 lines. 2 callees. 2 callers (sub_821DFDE0 and sub_82B9D980).
[entrypoint.functions."0x82B9D618"]
name = "ProcessAndProcessAndProcess779_82B9D618"

# Process a large data structure (seven hundred and eightieth form). It takes a structure, checks its state, and drives many sub-operations (CompareString, CopyStructure, Destruct4, DestructRefCounted, ProcessAndProcessAndProcess565, ProcessAndProcessAndProcess765, and more) based on the result. Part of the 0x82815 process cluster. 845 lines. 14 callees. 1 caller (ProcessAndProcessAndProcess631).
[entrypoint.functions."0x828156B8"]
name = "ProcessAndProcessAndProcess780_828156B8"

# Allocate and construct an object (seven hundred and eighty-first form). It takes a value, allocates fresh memory (Allocate_OrDbgBreak), and constructs the object (ProcessAndProcessAndProcess782). Part of the 0x82A3B process cluster. 92 lines. 2 callees. 0 callers.
[entrypoint.functions."0x82A3B5D0"]
name = "ProcessAndProcessAndProcess781_82A3B5D0"

# Construct an object (seven hundred and eighty-second form). It takes a structure and constructs it (Construct9). This is the constructor for the 0x82A60/0x82A3B object cluster, called by ProcessAndProcessAndProcess781. 136 lines. 1 callee. 1 caller (sub_82A3B5D0).
[entrypoint.functions."0x82A608F8"]
name = "ProcessAndProcessAndProcess782_82A608F8"

# Process a frequently-used value (seven hundred and eighty-third form). It takes a value and checks its state, with no external sub-operations. Part of the 0x82A48 process cluster. 68 lines. 0 callees. 9 callers.
[entrypoint.functions."0x82A48DB0"]
name = "ProcessAndProcessAndProcess783_82A48DB0"

# Push an element onto a queue (seven hundred and eighty-fourth form). It takes a structure, checks its state, and drives sub-operations (ProcessAndProcessAndProcess618, QueuePush, sub_82A83338, ProcessAndProcessAndProcess785) based on the result. This is a frequently-used queue/element manager in the 0x82A6C process cluster. 91 lines. 4 callees. 24 callers.
[entrypoint.functions."0x82A6CEF0"]
name = "ProcessAndProcessAndProcess784_82A6CEF0"

# Process an element (seven hundred and eighty-fifth form). It takes a structure and drives a sub-operation (ProcessAndProcessAndProcess627) based on its state. Part of the 0x82AB2 process cluster. 74 lines. 1 callee. 5 callers.
[entrypoint.functions."0x82AB20E0"]
name = "ProcessAndProcessAndProcess785_82AB20E0"

# Free an object (seven hundred and eighty-sixth form). It takes a structure, frees its size-bucketed memory (Free_SizeBucketed), and drives a sub-operation (ProcessAndProcessAndProcess787). Part of the 0x82A60 process cluster. 50 lines. 2 callees. 0 callers.
[entrypoint.functions."0x82A609E0"]
name = "ProcessAndProcessAndProcess786_82A609E0"

# Process a structure by driving a sub-operation (seven hundred and eighty-seventh form). It takes a structure, checks its state, and drives a sub-operation (ProcessAndProcessAndProcess784) based on the result. Part of the 0x82A60 process cluster. 146 lines. 1 callee. 1 caller (sub_82A609E0).
[entrypoint.functions."0x82A60A30"]
name = "ProcessAndProcessAndProcess787_82A60A30"

# Process a large data structure (seven hundred and eightyeighth form). It takes a structure, checks its state, and drives a sub-operation (sub_821E8D20) based on the result. Part of the 0x8221A process cluster. 288 lines. 1 callee. 1 caller (ProcessAndProcessAndProcess557).
[entrypoint.functions."0x8221AF48"]
name = "ProcessAndProcessAndProcess788_8221AF48"

# Process a structure by driving sub-operations (seven hundred and eighty-ninth form). It takes a structure, checks its state, and drives sub-operations (ProcessAndProcessAndProcess656, sub_82B7E050) based on the result. Part of the 0x82221 process cluster. 134 lines. 2 callees. 4 callers.
[entrypoint.functions."0x82221420"]
name = "ProcessAndProcessAndProcess789_82221420"

# Process a large data structure (seven hundred and ninetieth form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x8226A process cluster. 537 lines. 0 callees. 2 callers (sub_821E5978 and sub_822683C0).
[entrypoint.functions."0x8226A9F0"]
name = "ProcessAndProcessAndProcess790_8226A9F0"

# Process a frequently-used structure (seven hundred and ninety-first form). It takes a structure, checks its state, and drives a sub-operation (sub_821E8D20) based on the result. Part of the 0x82265 process cluster. 213 lines. 1 callee. 25 callers.
[entrypoint.functions."0x822655B0"]
name = "ProcessAndProcessAndProcess791_822655B0"

# Process a refcounted structure (seven hundred and ninety-second form). It takes a structure, adjusts its reference count (DecrementRefcountAtomic, SwapRefcount), and drives sub-operations (ProcessAndProcessAndProcess567, ProcessAndProcessAndProcess772) based on the result. This is part of the 0x821FA/0x821FC refcount cluster. 106 lines. 4 callees. 16 callers.
[entrypoint.functions."0x821FC270"]
name = "ProcessAndProcessAndProcess792_821FC270"

# Process a structure (seven hundred and ninety-third form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x821E4 process cluster. 60 lines. 0 callees. 0 callers.
[entrypoint.functions."0x821E4E50"]
name = "ProcessAndProcessAndProcess793_821E4E50"

# Process a structure (seven hundred and ninety-fourth form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x821EC process cluster. 54 lines. 0 callees. 1 caller (sub_82FEB738).
[entrypoint.functions."0x821EC608"]
name = "ProcessAndProcessAndProcess794_821EC608"

# Process a structure (seven hundred and ninety-fifth form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x8226B process cluster. 42 lines. 0 callees. 0 callers.
[entrypoint.functions."0x8226B808"]
name = "ProcessAndProcessAndProcess795_8226B808"

# Process a value (seven hundred and ninety-sixth form). It takes a value and checks its state, with no external sub-operations. Part of the 0x82CE3 process cluster. 12 lines. 0 callees. 1 caller (TailCall_82CE3768).
[entrypoint.functions."0x82CE3238"]
name = "ProcessAndProcessAndProcess796_82CE3238"

# Process a large data structure (seven hundred and ninety-seventh form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x832BF process cluster. 778 lines. 0 callees. 2 callers (sub_832BF9D0 and sub_832BFA70).
[entrypoint.functions."0x832BFF28"]
name = "ProcessAndProcessAndProcess797_832BFF28"

# Process a value (seven hundred and ninety-eighth form). It takes a value and checks its state, with no external sub-operations. Part of the 0x82CDC process cluster. 18 lines. 0 callees. 0 callers.
[entrypoint.functions."0x82CDC2D0"]
name = "ProcessAndProcessAndProcess798_82CDC2D0"

# Process a structure by driving sub-operations (seven hundred and ninety-ninth form). It takes a structure, adjusts a counter (AddAtomic), and drives sub-operations (ProcessAndProcessAndProcess649, ProcessAndProcessAndProcess650, sub_82CE62C0) based on the result. Part of the 0x82CDC process cluster. 139 lines. 4 callees. 0 callers.
[entrypoint.functions."0x82CDC2F0"]
name = "ProcessAndProcessAndProcess799_82CDC2F0"

# Process a very large data structure (seven hundred and one hundredth form). It takes a structure, adjusts a counter (AddAtomic), copies fields (CopyThreeFields), clears memory (MemSet), and drives sub-operations (ProcessAndProcessAndProcess623, ProcessAndProcessAndProcess630) based on the result. Part of the 0x82CE3 process cluster. 1034 lines. 5 callees. 0 callers.
[entrypoint.functions."0x82CE3CF0"]
name = "ProcessAndProcessAndProcess800_82CE3CF0"

# Process a structure (seven hundred and one hundred and first form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x8321C process cluster. 226 lines. 0 callees. 1 caller (ProcessAndProcessAndProcess689).
[entrypoint.functions."0x8321C988"]
name = "ProcessAndProcessAndProcess801_8321C988"

# Process a frequently-used structure (seven hundred and one hundred and second form). It takes a structure and checks its state, with no external sub-operations. This is an extremely hot helper in the 0x821F5 process cluster. 121 lines. 0 callees. 66 callers.
[entrypoint.functions."0x821F58B0"]
name = "ProcessAndProcessAndProcess802_821F58B0"

# Process a structure by driving sub-operations (seven hundred and one hundred and third form). It takes a structure, checks its state, and drives sub-operations (ProcessAndProcessAndProcess644, ProcessAndProcessAndProcess645, sub_822228F0) based on the result. Part of the 0x82220 process cluster. 72 lines. 3 callees. 15 callers.
[entrypoint.functions."0x822209A0"]
name = "ProcessAndProcessAndProcess803_822209A0"

# Process a structure by driving sub-operations (seven hundred and one hundred and fourth form). It takes a structure, checks its state, and drives sub-operations (ProcessAndProcessAndProcess656, ProcessAndProcessAndProcess657, sub_822228F0) based on the result. Part of the 0x82232 process cluster. 72 lines. 3 callees. 9 callers.
[entrypoint.functions."0x82232468"]
name = "ProcessAndProcessAndProcess804_82232468"

# Process a structure by dispatching its state transition (seven hundred and one hundred and fifth form). It takes a structure, checks its state, and drives sub-operations (ProcessStateTransition, ProcessAndProcessAndProcess803, ProcessAndProcessAndProcess804) based on the result. Part of the 0x82232 process cluster. 247 lines. 3 callees. 23 callers.
[entrypoint.functions."0x82232298"]
name = "ProcessAndProcessAndProcess805_82232298"

# Process a very large data structure (seven hundred and one hundred and sixth form). It takes a structure, copies data (MemCpy), and drives many sub-operations (ProcessAndProcessAndProcess522, ProcessAndProcessAndProcess529, ProcessAndProcessAndProcess568, ProcessAndProcessAndProcess612, and more) based on the result. This is a very large, frequently-used processor in the 0x82206 process cluster. 2126 lines. 10 callees. 23 callers.
[entrypoint.functions."0x822069C0"]
name = "ProcessAndProcessAndProcess806_822069C0"

# Process a structure (seven hundred and one hundred and seventh form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x8321B process cluster. 185 lines. 0 callees. 1 caller (ProcessAndProcessAndProcess593).
[entrypoint.functions."0x8321B498"]
name = "ProcessAndProcessAndProcess807_8321B498"

# Process a frequently-used structure (seven hundred and one hundred and eighth form). It takes a structure and checks its state, with no external sub-operations. This is a very hot helper in the 0x8223B process cluster. 261 lines. 0 callees. 60 callers.
[entrypoint.functions."0x8223B988"]
name = "ProcessAndProcessAndProcess808_8223B988"

# Process a value (seven hundred and one hundred and ninth form). It takes a value and checks its state, with no external sub-operations. This is a frequently-called helper in the 0x82CAE process cluster. 29 lines. 0 callees. 12 callers.
[entrypoint.functions."0x82CAE2D0"]
name = "ProcessAndProcessAndProcess809_82CAE2D0"

# Process a structure (seven hundred and one hundred and tenth form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x8321B process cluster. 216 lines. 0 callees. 1 caller (ProcessAndProcessAndProcess689).
[entrypoint.functions."0x8321B8C0"]
name = "ProcessAndProcessAndProcess810_8321B8C0"

# Process a large data structure by driving a sub-operation (seven hundred and one hundred and eleventh form). It takes a structure, checks its state, and drives a sub-operation (ProcessAndProcessAndProcess733) based on the result. Part of the 0x82264 process cluster. 408 lines. 1 callee. 8 callers.
[entrypoint.functions."0x82264550"]
name = "ProcessAndProcessAndProcess811_82264550"

# Process a structure by adjusting a counter (seven hundred and one hundred and twelfth form). It takes a structure, adjusts a counter (AddAtomic), and drives sub-operations (ProcessAndProcessAndProcess623, ProcessAndProcessAndProcess628) based on the result. Part of the 0x82CDD process cluster. 68 lines. 3 callees. 0 callers.
[entrypoint.functions."0x82CDD088"]
name = "ProcessAndProcessAndProcess812_82CDD088"

# Process a structure (seven hundred and one hundred and thirteenth form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82CDA process cluster. 224 lines. 0 callees. 1 caller (sub_82CDABC8).
[entrypoint.functions."0x82CDA2F8"]
name = "ProcessAndProcessAndProcess813_82CDA2F8"

# Process a refcounted structure (seven hundred and one hundred and fourteenth form). It takes a structure, adjusts a counter (ProcessAndIncrementCounter), replaces a value (ProcessAndReplaceValue), releases a reference (ReleaseGlobalRef), and drives sub-operations based on the result. Part of the 0x82270 process cluster. 185 lines. 7 callees. 7 callers.
[entrypoint.functions."0x82270A38"]
name = "ProcessAndProcessAndProcess814_82270A38"

# Process an enormous data structure (seven hundred and one hundred and fifteenth form). It takes a structure, checks its state, and drives many sub-operations (ConstructRefCounted, DestructRefCounted, ProcessAndReleaseAndProcess, Release_RefCounted, ProcessAndProcessAndProcess814, and more) based on the result. This is the largest processor in the recompiled binary so far, part of the 0x8226E process cluster. 5208 lines. 9 callees. 1 caller (ProcessAndProcessAndProcess521).
[entrypoint.functions."0x8226EA00"]
name = "ProcessAndProcessAndProcess815_8226EA00"

# Process a large data structure by driving a sub-operation (seven hundred and one hundred and sixteenth form). It takes a structure, checks its state, and drives a sub-operation (ProcessAndProcessAndProcess771) based on the result. Part of the 0x82286 process cluster. 271 lines. 1 callee. 3 callers.
[entrypoint.functions."0x82286248"]
name = "ProcessAndProcessAndProcess816_82286248"

# Process a structure (seven hundred and one hundred and seventeenth form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x8228E process cluster. 115 lines. 0 callees. 2 callers (sub_821DFFA8 and sub_8228DFB8).
[entrypoint.functions."0x8228E198"]
name = "ProcessAndProcessAndProcess817_8228E198"

# Process a frequently-used structure (seven hundred and one hundred and eighteenth form). It takes a structure and checks its state, with no external sub-operations. This is a frequently-called helper in the 0x821FD process cluster. 123 lines. 0 callees. 28 callers.
[entrypoint.functions."0x821FDE30"]
name = "ProcessAndProcessAndProcess818_821FDE30"

# Process a structure by driving sub-operations (seven hundred and one hundred and nineteenth form). It takes a structure, checks its state, and drives sub-operations (ProcessAndProcessAndProcess566, sub_821D2F68, sub_822971B8) based on the result. Part of the 0x821D4 process cluster. 153 lines. 3 callees. 7 callers.
[entrypoint.functions."0x821D4C68"]
name = "ProcessAndProcessAndProcess819_821D4C68"

# Process a large data structure (seven hundred and one hundred and twentieth form). It takes a structure, checks its state, and drives sub-operations (ProcessAndProcessAndProcess529, sub_821E8D20) based on the result. Part of the 0x821EF process cluster. 478 lines. 2 callees. 1 caller (sub_821EF6F8).
[entrypoint.functions."0x821EF988"]
name = "ProcessAndProcessAndProcess820_821EF988"

# Process a structure (seven hundred and one hundred and twenty-first form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x821B1 process cluster. 103 lines. 0 callees. 2 callers (sub_821B1B70 and sub_821B1CA0).
[entrypoint.functions."0x821B1C00"]
name = "ProcessAndProcessAndProcess821_821B1C00"

# Process a value by wrapping a sub-operation (seven hundred and one hundred and twenty-second form). It takes a value and drives a sub-operation (ProcessAndProcessAndProcess824). Part of the 0x821FF process cluster. 7 lines. 1 callee. 0 callers.
[entrypoint.functions."0x821FF1E0"]
name = "ProcessAndProcessAndProcess822_821FF1E0"

# Process a structure (seven hundred and one hundred and twenty-third form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82219 process cluster. 64 lines. 0 callees. 3 callers.
[entrypoint.functions."0x82219B88"]
name = "ProcessAndProcessAndProcess823_82219B88"

# Process a structure by driving sub-operations (seven hundred and one hundred and twenty-fourth form). It takes a structure, checks its state, and drives sub-operations (sub_821CD298, ProcessAndProcessAndProcess823) based on the result. Part of the 0x82B18 process cluster. 64 lines. 2 callees. 1 caller (sub_821FF1E0).
[entrypoint.functions."0x82B189A8"]
name = "ProcessAndProcessAndProcess824_82B189A8"

# Process a refcounted structure (seven hundred and one hundred and twenty-fifth form). It takes a structure, adjusts its reference count (DecrementRefcountAtomic), and drives a sub-operation (ProcessAndProcessAndProcess811) based on the result. Part of the 0x82242 process cluster. 122 lines. 2 callees. 16 callers.
[entrypoint.functions."0x822421D8"]
name = "ProcessAndProcessAndProcess825_822421D8"

# Process a structure by wrapping a sub-operation (seven hundred and one hundred and twenty-sixth form). It takes a structure and drives a sub-operation (ProcessAndProcessAndProcess818). Part of the 0x82B17 process cluster. 90 lines. 1 callee. 1 caller (sub_82B17E18).
[entrypoint.functions."0x82B17180"]
name = "ProcessAndProcessAndProcess826_82B17180"

# Process a large data structure (seven hundred and one hundred and twenty-seventh form). It takes a structure, checks its state, and drives many sub-operations (ProcessAndProcessAndProcess551, sub_821B2810, sub_821CA7E8, sub_82A537F8, and more) based on the result. Part of the 0x821CA object-processing cluster. 231 lines. 7 callees. 1 caller (sub_821D1440).
[entrypoint.functions."0x821CA640"]
name = "ProcessAndProcessAndProcess827_821CA640"

# Process a structure by wrapping a sub-operation (seven hundred and one hundred and twenty-eighth form). It takes a structure and drives a sub-operation (ProcessAndProcessAndProcess827). Part of the 0x821D1 process cluster. 13 lines. 1 callee. 0 callers.
[entrypoint.functions."0x821D1440"]
name = "ProcessAndProcessAndProcess828_821D1440"

# Process a structure by driving a sub-operation (seven hundred and one hundred and twenty-ninth form). It takes a structure, checks its state, and drives a sub-operation (sub_8226DA10) based on the result. Part of the 0x82222 process cluster. 195 lines. 1 callee. 3 callers.
[entrypoint.functions."0x82222C48"]
name = "ProcessAndProcessAndProcess829_82222C48"

# Process a structure by driving a sub-operation (seven hundred and one hundred and thirtieth form). It takes a structure, checks its state, and drives a sub-operation (sub_82A54B98) based on the result. Part of the 0x822B8 process cluster. 86 lines. 1 callee. 2 callers (ProcessAndProcessAndProcess669 and sub_82A6FEC8).
[entrypoint.functions."0x822B8008"]
name = "ProcessAndProcessAndProcess830_822B8008"

# Process a structure by driving sub-operations (seven hundred and one hundred and thirty-first form). It takes a structure, checks its state, and drives sub-operations (ProcessAndProcessAndProcess829, sub_82B18D28) based on the result. Part of the 0x82B18 process cluster. 82 lines. 2 callees. 5 callers.
[entrypoint.functions."0x82B18B88"]
name = "ProcessAndProcessAndProcess831_82B18B88"

# Process a structure by wrapping a sub-operation (seven hundred and one hundred and thirty-second form). It takes a structure and drives a sub-operation (ProcessAndProcessAndProcess833). Part of the 0x8226D process cluster. 78 lines. 1 callee. 3 callers.
[entrypoint.functions."0x8226DF58"]
name = "ProcessAndProcessAndProcess832_8226DF58"

# Process a structure (seven hundred and one hundred and thirty-third form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x8226D process cluster. 57 lines. 0 callees. 1 caller (sub_8226DF58).
[entrypoint.functions."0x8226DFE0"]
name = "ProcessAndProcessAndProcess833_8226DFE0"

# Process a large data structure (seven hundred and one hundred and thirty-fourth form). It takes a structure, checks its state, and drives a sub-operation (sub_821F3E10) based on the result. Part of the 0x82190 process cluster. 351 lines. 1 callee. 2 callers (sub_82190A60 and sub_821A3AE8).
[entrypoint.functions."0x82190830"]
name = "ProcessAndProcessAndProcess834_82190830"

# Process a structure by wrapping a sub-operation (seven hundred and one hundred and thirty-fifth form). It takes a structure and drives a sub-operation (ProcessAndProcessAndProcess834). Part of the 0x82190 process cluster. 99 lines. 1 callee. 3 callers.
[entrypoint.functions."0x82190A60"]
name = "ProcessAndProcessAndProcess835_82190A60"

# Process a large data structure (seven hundred and one hundred and thirty-sixth form). It takes a structure, clears memory (MemSet), and drives sub-operations (ProcessAndProcessAndProcess501, ProcessAndProcessAndProcess604, sub_82A6E5A0) based on the result. Part of the 0x821AB process cluster. 400 lines. 4 callees. 7 callers.
[entrypoint.functions."0x821ABDC8"]
name = "ProcessAndProcessAndProcess836_821ABDC8"

# Process a structure by driving sub-operations (seven hundred and one hundred and thirty-seventh form). It takes a structure, checks its state, and drives sub-operations (ProcessAndProcessAndProcess510, ProcessAndProcessAndProcess525, sub_82B7DF88) based on the result. Part of the 0x821B1 process cluster. 126 lines. 3 callees. 6 callers.
[entrypoint.functions."0x821B1E30"]
name = "ProcessAndProcessAndProcess837_821B1E30"

# Process a structure by driving a sub-operation (seven hundred and one hundred and thirty-eighth form). It takes a structure, checks its state, and drives a sub-operation (ProcessAndProcessAndProcess637) based on the result. Part of the 0x821B3 process cluster. 167 lines. 1 callee. 3 callers.
[entrypoint.functions."0x821B3260"]
name = "ProcessAndProcessAndProcess838_821B3260"

# Process a large data structure (seven hundred and one hundred and thirty-ninth form). It takes a structure, checks its state, and drives many sub-operations (ProcessAndProcessAndProcess531, ProcessAndProcessAndProcess556, ProcessAndProcessAndProcess572, ProcessAndProcessAndProcess589, ProcessAndProcessAndProcess619, ProcessAndProcessAndProcess637, ProcessAndProcessAndProcess638, and more) based on the result. Part of the 0x821CA object-processing cluster. 720 lines. 9 callees. 2 callers (sub_821CA7E8 and sub_82A537F8).
[entrypoint.functions."0x821CA160"]
name = "ProcessAndProcessAndProcess839_821CA160"

# Dispatch a large object-processing operation (seven hundred and one hundred and fortieth form). It takes a structure, checks its state, and drives many sub-operations (ProcessAndProcessAndProcess510, ProcessAndProcessAndProcess515, ProcessAndProcessAndProcess549, ProcessAndProcessAndProcess552, ProcessAndProcessAndProcess555, ProcessAndProcessAndProcess556, ProcessAndProcessAndProcess619, ProcessAndProcessAndProcess642, SwitchDispatch, and more) based on the result. This is the major dispatcher of the 0x821CA object-processing cluster. 1339 lines. 20 callees. 1 caller (sub_821CA640).
[entrypoint.functions."0x821CA7E8"]
name = "ProcessAndProcessAndProcess840_821CA7E8"

# Process a large data structure (seven hundred and one hundred and forty-first form). It takes a structure, checks its state, and drives many sub-operations (ProcessAndProcessAndProcess522, ProcessAndProcessAndProcess532, ProcessAndProcessAndProcess557, ProcessAndProcessAndProcess558, ProcessAndProcessAndProcess612, and more) based on the result. Part of the 0x8221C process cluster. 593 lines. 8 callees. 11 callers.
[entrypoint.functions."0x8221C518"]
name = "ProcessAndProcessAndProcess841_8221C518"

# Process a structure by driving a sub-operation (seven hundred and one hundred and forty-second form). It takes a structure, checks its state, and drives a sub-operation (ProcessAndProcessAndProcess642) based on the result. This is a frequently-called helper in the 0x82220 process cluster. 81 lines. 1 callee. 18 callers.
[entrypoint.functions."0x82220D38"]
name = "ProcessAndProcessAndProcess842_82220D38"

# Process a large data structure (seven hundred and one hundred and forty-third form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x822BE process cluster. 1108 lines. 0 callees. 1 caller (sub_822A9E60).
[entrypoint.functions."0x822BEAA0"]
name = "ProcessAndProcessAndProcess843_822BEAA0"

# Process a frequently-used structure (seven hundred and one hundred and forty-fourth form). It takes a structure and checks its state, with no external sub-operations. This is a frequently-called helper in the 0x8290F process cluster. 103 lines. 0 callees. 13 callers.
[entrypoint.functions."0x8290F680"]
name = "ProcessAndProcessAndProcess844_8290F680"

# Process a structure by copying its data (seven hundred and one hundred and forty-fifth form). It takes a structure and copies its data (MemCpy). Part of the 0x82B9D process cluster. 108 lines. 1 callee. 1 caller (ProcessAndProcessAndProcess779).
[entrypoint.functions."0x82B9D560"]
name = "ProcessAndProcessAndProcess845_82B9D560"

# Process a large data structure (seven hundred and one hundred and forty-sixth form). It takes a structure, checks its state, and drives a sub-operation (ProcessAndProcessAndProcess832) based on the result. Part of the 0x8226D process cluster. 260 lines. 1 callee. 2 callers (sub_8226DA10 and sub_82AAC580).
[entrypoint.functions."0x8226DD88"]
name = "ProcessAndProcessAndProcess846_8226DD88"

# Process a structure (seven hundred and one hundred and forty-seventh form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82A71 process cluster. 121 lines. 0 callees. 3 callers.
[entrypoint.functions."0x82A71530"]
name = "ProcessAndProcessAndProcess847_82A71530"

# Process a structure by wrapping a sub-operation (seven hundred and one hundred and forty-eighth form). It takes a structure and drives a sub-operation (ProcessAndProcessAndProcess847). Part of the 0x82A71 process cluster. 13 lines. 1 callee. 0 callers.
[entrypoint.functions."0x82A71AC0"]
name = "ProcessAndProcessAndProcess848_82A71AC0"

# Process a large data structure by allocating and driving sub-operations (seven hundred and one hundred and forty-ninth form). It takes a structure, allocates fresh memory (Allocate_OrDbgBreak, ProcessAndAllocateAndProcess2), and drives many sub-operations based on the result. Part of the 0x8226D process cluster. 556 lines. 10 callees. 2 callers (sub_82198F98 and sub_82222C48).
[entrypoint.functions."0x8226DA10"]
name = "ProcessAndProcessAndProcess849_8226DA10"

# Process a structure by driving a sub-operation (seven hundred and one hundred and fiftieth form). It takes a structure, checks its state, and drives a sub-operation (ProcessAndProcessAndProcess590) based on the result. Part of the 0x82AAC process cluster. 204 lines. 1 callee. 1 caller (sub_82B18D28).
[entrypoint.functions."0x82AAC108"]
name = "ProcessAndProcessAndProcess850_82AAC108"

# Process a structure by driving sub-operations (seven hundred and one hundred and fifty-first form). It takes a structure, checks its state, and drives sub-operations (ProcessAndProcessAndProcess590, sub_829FD4B8, ProcessAndProcessAndProcess850, sub_82B16D30) based on the result. Part of the 0x82B18 process cluster. 180 lines. 4 callees. 3 callers.
[entrypoint.functions."0x82B18D28"]
name = "ProcessAndProcessAndProcess851_82B18D28"

# Process a frequently-used structure (seven hundred and one hundred and fifty-second form). It takes a structure and checks its state, with no external sub-operations. This is one of the most frequently-called helpers in the recompiled binary. 62 lines. 0 callees. 98 callers.
[entrypoint.functions."0x823DB930"]
name = "ProcessAndProcessAndProcess852_823DB930"

# Process a large data structure (seven hundred and one hundred and fifty-third form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x832BF process cluster. 537 lines. 0 callees. 3 callers.
[entrypoint.functions."0x832BF218"]
name = "ProcessAndProcessAndProcess853_832BF218"

# Process a frequently-used structure (seven hundred and one hundred and fifty-fourth form). It takes a structure and checks its state, with no external sub-operations. This is a frequently-called helper in the 0x821A2 process cluster. 101 lines. 0 callees. 13 callers.
[entrypoint.functions."0x821A2538"]
name = "ProcessAndProcessAndProcess854_821A2538"

# Process a structure by driving a sub-operation (seven hundred and one hundred and fifty-fifth form). It takes a structure, checks its state, and drives a sub-operation (ProcessAndProcessAndProcess566) based on the result. Part of the 0x821D2 process cluster. 78 lines. 1 callee. 1 caller (sub_821D2F68).
[entrypoint.functions."0x821D2EE0"]
name = "ProcessAndProcessAndProcess855_821D2EE0"

# Process a structure by wrapping a sub-operation (seven hundred and one hundred and fifty-sixth form). It takes a structure and drives a sub-operation (ProcessAndProcessAndProcess855). Part of the 0x821D2 process cluster. 88 lines. 1 callee. 4 callers.
[entrypoint.functions."0x821D2F68"]
name = "ProcessAndProcessAndProcess856_821D2F68"

# Process a frequently-used structure (seven hundred and one hundred and fifty-seventh form). It takes a structure and checks its state, with no external sub-operations. This is a frequently-called helper in the 0x82293 process cluster. 115 lines. 0 callees. 28 callers.
[entrypoint.functions."0x82293CD0"]
name = "ProcessAndProcessAndProcess857_82293CD0"

# Process a large data structure by allocating and freeing memory (seven hundred and one hundred and fifty-eighth form). It takes a structure, allocates and frees size-bucketed memory (AllocateChecked4, Free_SizeBucketed), and drives sub-operations based on the result. Part of the 0x82A68 process cluster. 400 lines. 3 callees. 9 callers.
[entrypoint.functions."0x82A68BB8"]
name = "ProcessAndProcessAndProcess858_82A68BB8"

# Process a structure (seven hundred and one hundred and fifty-ninth form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82CD5 process cluster. 237 lines. 0 callees. 1 caller (sub_82CD6010).
[entrypoint.functions."0x82CD5720"]
name = "ProcessAndProcessAndProcess859_82CD5720"

# Process a structure by driving sub-operations (seven hundred and one hundred and sixtieth form). It takes a structure, checks its state, and drives sub-operations (sub_821E8D20, sub_82B9B340) based on the result. Part of the 0x8227C process cluster. 156 lines. 2 callees. 6 callers.
[entrypoint.functions."0x8227CFC8"]
name = "ProcessAndProcessAndProcess860_8227CFC8"

# Process a structure by driving sub-operations (seven hundred and one hundred and sixty-first form). It takes a structure, checks its state, and drives sub-operations (sub_82A00988, sub_82A00A18) based on the result. Part of the 0x82A00 process cluster. 105 lines. 2 callees. 3 callers (including CopyObject).
[entrypoint.functions."0x82A00808"]
name = "ProcessAndProcessAndProcess861_82A00808"

# Process a large data structure (seven hundred and one hundred and sixty-second form). It takes a structure, checks its state, and drives sub-operations (ProcessAndProcessAndProcess596, sub_821940C8, sub_8322A7C8) based on the result. Part of the 0x83226 process cluster. 346 lines. 3 callees. 0 callers.
[entrypoint.functions."0x83226730"]
name = "ProcessAndProcessAndProcess862_83226730"

# Process a structure by wrapping a sub-operation (seven hundred and one hundred and sixty-third form). It takes a structure and drives a sub-operation (ProcessAndProcessAndProcess819). Part of the 0x8227B process cluster. 21 lines. 1 callee. 0 callers.
[entrypoint.functions."0x8227B030"]
name = "ProcessAndProcessAndProcess863_8227B030"

# Process a refcounted structure (seven hundred and one hundred and sixty-fourth form). It takes a structure, adjusts its reference count (DecrementRefcountAtomic), and drives sub-operations (ProcessAndProcessAndProcess567, ProcessAndProcessAndProcess772) based on the result. Part of the 0x821FC refcount cluster. 85 lines. 3 callees. 8 callers.
[entrypoint.functions."0x821FC410"]
name = "ProcessAndProcessAndProcess864_821FC410"

# Process a structure by driving sub-operations (seven hundred and one hundred and sixty-fifth form). It takes a structure, checks its state, and drives sub-operations (ProcessAndProcessAndProcess510, ProcessAndProcessAndProcess790) based on the result. Part of the 0x82268 process cluster. 94 lines. 2 callees. 3 callers.
[entrypoint.functions."0x822683C0"]
name = "ProcessAndProcessAndProcess865_822683C0"

# Process a structure by checking a float sign (seven hundred and one hundred and sixty-sixth form). It takes a structure, checks a float sign (CheckFloatSign), and drives sub-operations based on the result. Part of the 0x8322A process cluster. 82 lines. 1 callee. 3 callers.
[entrypoint.functions."0x8322A7C8"]
name = "ProcessAndProcessAndProcess866_8322A7C8"

# Process a frequently-used structure (seven hundred and one hundred and sixty-seventh form). It takes a structure and checks its state, with no external sub-operations. This is a frequently-called helper in the 0x82226 process cluster. 127 lines. 0 callees. 18 callers.
[entrypoint.functions."0x82226520"]
name = "ProcessAndProcessAndProcess867_82226520"

# Process a structure by wrapping a sub-operation (seven hundred and one hundred and sixty-eighth form). It takes a structure and drives a sub-operation (ProcessAndProcessAndProcess623). Part of the 0x82CD7 process cluster. 16 lines. 1 callee. 6 callers.
[entrypoint.functions."0x82CD7FE0"]
name = "ProcessAndProcessAndProcess868_82CD7FE0"

# Process a structure by looking up chain values (seven hundred and one hundred and sixty-ninth form). It takes a structure, looks up chain values (FindChainValue), makes virtual calls (ProcessVcall3), and drives sub-operations based on the result. Part of the 0x82C0C process cluster. 62 lines. 2 callees. 2 callers (sub_82C0D488 and sub_82C0FFF8).
[entrypoint.functions."0x82C0CDC8"]
name = "ProcessAndProcessAndProcess869_82C0CDC8"

# Process a structure (seven hundred and one hundred and seventieth form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82182 process cluster. 202 lines. 0 callees. 0 callers.
[entrypoint.functions."0x821825B8"]
name = "ProcessAndProcessAndProcess870_821825B8"

# Process a structure by driving sub-operations (seven hundred and one hundred and seventy-first form). It takes a structure, checks its state, and drives sub-operations (ProcessAndProcessAndProcess562, ProcessAndProcessAndProcess564) based on the result. Part of the 0x82192 process cluster. 103 lines. 2 callees. 0 callers.
[entrypoint.functions."0x821920B0"]
name = "ProcessAndProcessAndProcess871_821920B0"

# Process a very large data structure (seven hundred and one hundred and seventy-second form). It takes a structure, checks its state, and drives many sub-operations (ProcessAndDecrementDispose, ProcessAndProcessAndProcess510, ProcessAndProcessAndProcess515, ProcessAndProcessAndProcess525, ProcessAndProcessAndProcess531, ProcessAndProcessAndProcess533, ProcessAndProcessAndProcess540, ProcessAndProcessAndProcess549, Release_RefCounted, and more) based on the result. Part of the 0x821C4 process cluster. 983 lines. 10 callees. 0 callers.
[entrypoint.functions."0x821C43E8"]
name = "ProcessAndProcessAndProcess872_821C43E8"

# Process a large data structure (seven hundred and one hundred and seventy-third form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x821C6 process cluster. 625 lines. 0 callees. 0 callers.
[entrypoint.functions."0x821C6958"]
name = "ProcessAndProcessAndProcess873_821C6958"

# Process a very large data structure (seven hundred and one hundred and seventy-fourth form). It takes a structure, checks its state, and drives many sub-operations (ProcessAndDecrementDispose, ProcessAndProcessAndProcess525, ProcessAndProcessAndProcess533, ProcessAndProcessAndProcess540, ProcessAndProcessAndProcess551, ProcessAndProcessAndProcess575, Release_RefCounted, and more) based on the result. Part of the 0x821DD process cluster. 1740 lines. 9 callees. 0 callers.
[entrypoint.functions."0x821DD2C0"]
name = "ProcessAndProcessAndProcess874_821DD2C0"

# Process a large data structure by copying and allocating (seven hundred and one hundred and seventy-fifth form). It takes a structure, copies its data (MemCpy), allocates fresh memory (ProcessAndAllocateAndProcess2), and drives sub-operations (ProcessAndProcessAndProcess555, sub_82B86DF8, sub_82B86F38) based on the result. Part of the 0x821EB process cluster. 342 lines. 6 callees. 3 callers.
[entrypoint.functions."0x821EBDA0"]
name = "ProcessAndProcessAndProcess875_821EBDA0"

# Process a large data structure (seven hundred and one hundred and seventy-sixth form). It takes a structure, checks its state, and drives many sub-operations (ProcessAndProcessAndProcess522, ProcessAndProcessAndProcess529, ProcessAndProcessAndProcess532, ProcessAndProcessAndProcess557, ProcessAndProcessAndProcess558, ProcessAndProcessAndProcess612, sub_821D1270, sub_821E8D20) based on the result. Part of the 0x82206 process cluster. 700 lines. 9 callees. 5 callers.
[entrypoint.functions."0x822060A0"]
name = "ProcessAndProcessAndProcess876_822060A0"

# Process a structure (seven hundred and one hundred and seventy-seventh form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82A9E process cluster. 113 lines. 0 callees. 1 caller (sub_82A9E4F8).
[entrypoint.functions."0x82A9E940"]
name = "ProcessAndProcessAndProcess877_82A9E940"

# Process a frequently-used structure (seven hundred and one hundred and seventy-eighth form). It takes a structure and checks its state, with no external sub-operations. This is a frequently-called helper in the 0x82CA9 process cluster. 29 lines. 0 callees. 21 callers.
[entrypoint.functions."0x82CA9E84"]
name = "ProcessAndProcessAndProcess878_82CA9E84"

# Process a structure by driving sub-operations (seven hundred and one hundred and seventy-ninth form). It takes a structure, checks its state, and drives sub-operations (ProcessAndProcessAndProcess639, ProcessAndProcessAndProcess797, sub_832C0440, sub_832C05C8) based on the result. Part of the 0x832BF process cluster. 97 lines. 4 callees. 3 callers.
[entrypoint.functions."0x832BF9D0"]
name = "ProcessAndProcessAndProcess879_832BF9D0"

# Dispatch a frequently-used structure (seven hundred and one hundred and eightieth form). It takes a structure, dispatches by type code (DispatchByTypeCode2), and drives a sub-operation (ProcessAndProcessAndProcess670) based on the result. This is a frequently-called dispatcher in the 0x8225F process cluster. 124 lines. 2 callees. 15 callers.
[entrypoint.functions."0x8225FCD8"]
name = "ProcessAndProcessAndProcess880_8225FCD8"

# Process a structure by wrapping a sub-operation (seven hundred and one hundred and eighty-first form). It takes a structure and drives a sub-operation (ProcessAndProcessAndProcess624). Part of the 0x82218 process cluster. 35 lines. 1 callee. 0 callers.
[entrypoint.functions."0x82218BC8"]
name = "ProcessAndProcessAndProcess881_82218BC8"

# Process a structure by driving sub-operations (seven hundred and one hundred and eighty-second form). It takes a structure, checks its state, and drives sub-operations (sub_82CE6C20, sub_82CEBF30) based on the result. Part of the 0x82CF4 process cluster. 123 lines. 2 callees. 0 callers.
[entrypoint.functions."0x82CF48B0"]
name = "ProcessAndProcessAndProcess882_82CF48B0"

# Process a structure by driving sub-operations (seven hundred and one hundred and eighty-third form). It takes a structure, checks its state, and drives sub-operations (ProcessAndProcessAndProcess549, ProcessAndProcessAndProcess552) based on the result. Part of the 0x821EC process cluster. 57 lines. 2 callees. 2 callers (sub_821DFFA8 and sub_821ECE40).
[entrypoint.functions."0x821ECDE0"]
name = "ProcessAndProcessAndProcess883_821ECDE0"

# Process a large data structure (seven hundred and one hundred and eighty-fourth form). It takes a structure, checks its state, and drives many sub-operations (ProcessAndProcessAndProcess510, ProcessAndProcessAndProcess525, ProcessAndProcessAndProcess533, ProcessAndProcessAndProcess619, ProcessAndProcessAndProcess875, ProcessAndProcessAndProcess883, ProcessAndProcessAndProcess865, sub_822B9D00) based on the result. Part of the 0x821EC process cluster. 481 lines. 8 callees. 7 callers.
[entrypoint.functions."0x821ECE40"]
name = "ProcessAndProcessAndProcess884_821ECE40"

# Acquire or release a refcount by driving a processor (seven hundred and one hundred and eighty-fifth form). It takes a structure, adjusts its reference count (IncrementRefcountAtomic), and drives sub-operations (ProcessAndProcessAndProcess792, ProcessAndProcessAndProcess805, ProcessAndProcessAndProcess806, ProcessAndProcessAndProcess825, sub_821FC410) based on the result. This is the core refcount acquire/release dispatcher in the 0x82242 cluster. 121 lines. 6 callees. 10 callers.
[entrypoint.functions."0x82242110"]
name = "ProcessAndProcessAndProcess885_82242110"

# Process a structure (seven hundred and one hundred and eighty-sixth form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82CFF process cluster. 221 lines. 0 callees. 1 caller (sub_82D005F8).
[entrypoint.functions."0x82CFFF70"]
name = "ProcessAndProcessAndProcess886_82CFFF70"

# Process a structure (seven hundred and one hundred and eighty-seventh form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82C61 process cluster. 78 lines. 0 callees. 2 callers (sub_82C5D350 and sub_82C5E370).
[entrypoint.functions."0x82C61340"]
name = "ProcessAndProcessAndProcess887_82C61340"

# Process a structure (seven hundred and one hundred and eighty-eighth form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82C60 process cluster. 101 lines. 0 callees. 2 callers (sub_82C5B2E0 and sub_82C5B490).
[entrypoint.functions."0x82C60458"]
name = "ProcessAndProcessAndProcess888_82C60458"

# Process a large data structure (seven hundred and one hundred and eighty-ninth form). It takes a structure, checks its state, and drives a sub-operation (ProcessAndProcessAndProcess763) based on the result. Part of the 0x82CE6 process cluster. 413 lines. 1 callee. 1 caller (sub_82CF48B0).
[entrypoint.functions."0x82CE6C20"]
name = "ProcessAndProcessAndProcess889_82CE6C20"

# Process a large data structure (seven hundred and one hundred and eightieth form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x8321B process cluster. 431 lines. 0 callees. 3 callers (including ProcessAndProcessAndProcess691).
[entrypoint.functions."0x8321B5D8"]
name = "ProcessAndProcessAndProcess890_8321B5D8"

# Process a structure (seven hundred and one hundred and eighty-first form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x83228 process cluster. 112 lines. 0 callees. 1 caller (ProcessAndProcessAndProcess691).
[entrypoint.functions."0x83228E80"]
name = "ProcessAndProcessAndProcess891_83228E80"

# Process a structure by driving sub-operations (seven hundred and one hundred and eighty-second form). It takes a structure, checks its state, and drives sub-operations (ProcessAndProcessAndProcess861, sub_82A008C8) based on the result. Part of the 0x83229 process cluster. 130 lines. 2 callees. 1 caller (ProcessAndProcessAndProcess691).
[entrypoint.functions."0x832294F0"]
name = "ProcessAndProcessAndProcess892_832294F0"

# Process a structure (seven hundred and one hundred and eighty-third form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82CE6 process cluster. 68 lines. 0 callees. 1 caller (sub_82CDDB80).
[entrypoint.functions."0x82CE6820"]
name = "ProcessAndProcessAndProcess893_82CE6820"

# Process a structure by wrapping a sub-operation (seven hundred and one hundred and eighty-fourth form). It takes a structure and drives a sub-operation (sub_82B9C530). Part of the 0x82B9B process cluster. 91 lines. 1 callee. 0 callers.
[entrypoint.functions."0x82B9B8D8"]
name = "ProcessAndProcessAndProcess894_82B9B8D8"

# Acquire a refcount by swapping it (seven hundred and one hundred and eighty-fifth form). It takes a structure, increments and swaps its reference count (IncrementRefcountAtomic, SwapRefcount), and drives a sub-operation (ProcessAndProcessAndProcess524) based on the result. This is a refcount acquirer in the 0x821FB cluster. 61 lines. 3 callees. 14 callers.
[entrypoint.functions."0x821FBAB8"]
name = "ProcessAndProcessAndProcess895_821FBAB8"

# Process a structure (seven hundred and one hundred and eighty-sixth form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x821B9 process cluster. 39 lines. 0 callees. 2 callers (sub_821B9AD0 and sub_82286D00).
[entrypoint.functions."0x821B9DA0"]
name = "ProcessAndProcessAndProcess896_821B9DA0"

# Flush a cache and process a structure (seven hundred and one hundred and eighty-seventh form). It takes a structure, flushes the cache (FlushCache), and drives a sub-operation (ProcessAndProcessAndProcess529) based on the result. This is a frequently-called helper in the 0x821D1 process cluster. 153 lines. 2 callees. 13 callers.
[entrypoint.functions."0x821D1270"]
name = "ProcessAndProcessAndProcess897_821D1270"

# Process a structure by checking float properties (seven hundred and one hundred and eighty-eighth form). It takes a structure, checks and modifies float properties (CheckFloatProperties, ModifyFloatPrecision), and drives sub-operations (ProcessAndProcessAndProcess809) based on the result. Part of the 0x8227A process cluster. 285 lines. 7 callees. 5 callers.
[entrypoint.functions."0x8227A818"]
name = "ProcessAndProcessAndProcess898_8227A818"

# Process a structure by driving sub-operations (seven hundred and one hundred and ninety-ninth form). It takes a structure, checks its state, and drives sub-operations (ProcessAndProcessAndProcess774, ProcessAndProcessAndProcess821) based on the result. Part of the 0x821B1 process cluster. 92 lines. 2 callees. 6 callers.
[entrypoint.functions."0x821B1B70"]
name = "ProcessAndProcessAndProcess899_821B1B70"

# Process a structure (seven hundred and one hundred and ninetieth form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82236 process cluster. 88 lines. 0 callees. 1 caller (sub_82236EE8).
[entrypoint.functions."0x82236FD0"]
name = "ProcessAndProcessAndProcess900_82236FD0"

# Process a structure by freeing size-bucketed memory (seven hundred and one hundred and ninety-first form). It takes a structure, frees size-bucketed memory (Free_SizeBucketed), and drives sub-operations (sub_82196C60, sub_821B7918, sub_822CCDB0, sub_82A9D640) based on the result. Part of the 0x821CD process cluster. 223 lines. 5 callees. 1 caller (ProcessAndProcessAndProcess824).
[entrypoint.functions."0x821CD298"]
name = "ProcessAndProcessAndProcess901_821CD298"

# Process a structure by wrapping a sub-operation (seven hundred and one hundred and ninety-second form). It takes a structure and drives a sub-operation (ProcessAndProcessAndProcess771). Part of the 0x82B9B process cluster. 142 lines. 1 callee. 2 callers (sub_82B9BA58 and sub_82B9E848).
[entrypoint.functions."0x82B9B7E8"]
name = "ProcessAndProcessAndProcess902_82B9B7E8"

# Process a structure by wrapping a sub-operation (seven hundred and one hundred and ninety-third form). It takes a structure and drives a sub-operation (ProcessAndProcessAndProcess902). Part of the 0x82B9E process cluster. 132 lines. 1 callee. 1 caller (sub_82B9ED28).
[entrypoint.functions."0x82B9E848"]
name = "ProcessAndProcessAndProcess903_82B9E848"

# Process a very frequently-used structure (seven hundred and one hundred and ninety-fourth form). It takes a structure and checks its state, with no external sub-operations. This is one of the most frequently-called helpers in the recompiled binary. 122 lines. 0 callees. 84 callers.
[entrypoint.functions."0x8221ACC0"]
name = "ProcessAndProcessAndProcess904_8221ACC0"

# Process a frequently-used structure (seven hundred and one hundred and ninety-fifth form). It takes a structure, checks its state, and drives sub-operations (sub_821E8D20, ProcessAndProcessAndProcess906) based on the result. This is a frequently-called helper in the 0x8219C process cluster. 150 lines. 2 callees. 18 callers.
[entrypoint.functions."0x8219CE10"]
name = "ProcessAndProcessAndProcess905_8219CE10"

# Process a structure by wrapping a sub-operation (seven hundred and one hundred and ninety-sixth form). It takes a structure and drives a sub-operation (ProcessAndProcessAndProcess568). Part of the 0x82B92 process cluster. 149 lines. 1 callee. 1 caller (sub_8219CE10).
[entrypoint.functions."0x82B92790"]
name = "ProcessAndProcessAndProcess906_82B92790"

# Process a refcounted structure (seven hundred and one hundred and ninety-seventh form). It takes a structure, swaps its reference count (SwapRefcount), and drives sub-operations (ProcessAndProcessAndProcess772, ProcessAndProcessAndProcess811) based on the result. This is a refcount processor in the 0x8226B cluster. 59 lines. 3 callees. 16 callers.
[entrypoint.functions."0x8226B038"]
name = "ProcessAndProcessAndProcess907_8226B038"

# Process a structure by wrapping a sub-operation (seven hundred and one hundred and ninety-eighth form). It takes a structure and drives a sub-operation (ProcessAndProcessAndProcess579). Part of the 0x82CDA DPC atomic cluster. 138 lines. 1 callee. 2 callers (sub_82CDB248 and sub_82CDB3C0).
[entrypoint.functions."0x82CDAAE0"]
name = "ProcessAndProcessAndProcess908_82CDAAE0"

# Process a structure by driving sub-operations (seven hundred and one hundred and ninety-ninth form). It takes a structure, checks its state, and drives sub-operations (ProcessAndProcessAndProcess580, ProcessAndProcessAndProcess581, ProcessAndProcessAndProcess813) based on the result. Part of the 0x82CDA DPC atomic cluster. 183 lines. 3 callees. 3 callers (including sub_82CDB3C0).
[entrypoint.functions."0x82CDABC8"]
name = "ProcessAndProcessAndProcess909_82CDABC8"

# Process a structure by wrapping a sub-operation (seven hundred and one hundred and one hundredth form). It takes a structure and drives a sub-operation (sub_82CDB3C0). Part of the 0x82CE4 process cluster. 11 lines. 1 callee. 0 callers.
[entrypoint.functions."0x82CE4540"]
name = "ProcessAndProcessAndProcess910_82CE4540"

# Process a structure by driving sub-operations (seven hundred and one hundred and one hundred and first form). It takes a structure, checks its state, and drives sub-operations (ProcessAndProcessAndProcess741, sub_82D02DD8, sub_82D03340) based on the result. Part of the 0x82CFF process cluster. 205 lines. 3 callees. 0 callers.
[entrypoint.functions."0x82CFFB48"]
name = "ProcessAndProcessAndProcess911_82CFFB48"

# Process a structure by copying its data (seven hundred and one hundred and one hundred and second form). It takes a structure and copies its data (MemCpy). Part of the 0x82CD8 process cluster. 119 lines. 1 callee. 0 callers.
[entrypoint.functions."0x82CD8168"]
name = "ProcessAndProcessAndProcess912_82CD8168"

# Process a structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and third form). It takes a structure and drives a sub-operation (ProcessAndProcessAndProcess741). Part of the 0x82CD8 process cluster. 59 lines. 1 callee. 0 callers.
[entrypoint.functions."0x82CD82E8"]
name = "ProcessAndProcessAndProcess913_82CD82E8"

# Process a DPC atomic structure (seven hundred and one hundred and one hundred and fourth form). It takes a structure, adjusts an atomic counter (AddAtomic), clears memory (MemSet), returns a stub (Stub_ReturnZero), and drives sub-operations (ProcessAndProcessAndProcess908, ProcessAndProcessAndProcess909, ProcessAndProcessAndProcess915) based on the result. Part of the 0x82CDB DPC atomic cluster. 163 lines. 6 callees. 1 caller (ProcessAndProcessAndProcess910).
[entrypoint.functions."0x82CDB3C0"]
name = "ProcessAndProcessAndProcess914_82CDB3C0"

# Process a structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and fifth form). It takes a structure and drives a sub-operation (sub_83004A90). Part of the 0x82CDD process cluster. 108 lines. 1 callee. 1 caller (ProcessAndProcessAndProcess914).
[entrypoint.functions."0x82CDD610"]
name = "ProcessAndProcessAndProcess915_82CDD610"

# Process a structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and sixth form). It takes a structure and drives a sub-operation (ProcessAndProcessAndProcess629). Part of the 0x82CE4 process cluster. 56 lines. 1 callee. 0 callers.
[entrypoint.functions."0x82CE44E0"]
name = "ProcessAndProcessAndProcess916_82CE44E0"

# Process a structure by driving sub-operations (seven hundred and one hundred and one hundred and seventh form). It takes a structure, checks its state, and drives sub-operations (ProcessAndProcessAndProcess628, ProcessAndProcessAndProcess868) based on the result. Part of the 0x82CE4 process cluster. 52 lines. 2 callees. 0 callers.
[entrypoint.functions."0x82CE4728"]
name = "ProcessAndProcessAndProcess917_82CE4728"

# Process a structure by copying its data (seven hundred and one hundred and one hundred and eighth form). It takes a structure, copies its data (MemCpy), and drives a sub-operation (ProcessAndProcessAndProcess878) based on the result. Part of the 0x83004 process cluster. 162 lines. 2 callees. 2 callers (sub_82BA7298 and ProcessAndProcessAndProcess915).
[entrypoint.functions."0x83004A90"]
name = "ProcessAndProcessAndProcess918_83004A90"

# Process a structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and nineteenth form). It takes a structure and drives a sub-operation (sub_8222B948). Part of the 0x821E2 process cluster. 171 lines. 1 callee. 7 callers.
[entrypoint.functions."0x821E2BA0"]
name = "ProcessAndProcessAndProcess919_821E2BA0"

# Process a large data structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and twentieth form). It takes a structure and drives a sub-operation (ProcessAndProcessAndProcess820). Part of the 0x821EF process cluster. 366 lines. 1 callee. 1 caller (sub_821F0CC8).
[entrypoint.functions."0x821EF6F8"]
name = "ProcessAndProcessAndProcess920_821EF6F8"

# Process a large data structure (seven hundred and one hundred and one hundred and twenty-first form). It takes a structure, checks its state, and drives many sub-operations (ProcessAndProcessAndProcess612, sub_821E8D20, ProcessAndProcessAndProcess920, sub_8227A9F0) based on the result. Part of the 0x821F0 process cluster. 795 lines. 4 callees. 1 caller (sub_8222B948).
[entrypoint.functions."0x821F0CC8"]
name = "ProcessAndProcessAndProcess921_821F0CC8"

# Process a very large data structure (seven hundred and one hundred and one hundred and twenty-second form). It takes a structure, copies its data (MemCpy), and drives many sub-operations (ProcessAndProcessAndProcess522, ProcessAndProcessAndProcess568, ProcessAndProcessAndProcess612, sub_821E8D20, ProcessAndProcessAndProcess921) based on the result. Part of the 0x8222B process cluster. 998 lines. 6 callees. 1 caller (ProcessAndProcessAndProcess919).
[entrypoint.functions."0x8222B948"]
name = "ProcessAndProcessAndProcess922_8222B948"

# Insert a node into a tree by driving sub-operations (seven hundred and one hundred and one hundred and twenty-third form). It takes a structure, allocates fresh memory (Allocate_OrDbgBreak), finds tree leaves (FindTreeLeaf), inserts into the tree (Tree_Insert), and drives many sub-operations (ProcessAndProcessAndProcess516, ProcessAndProcessAndProcess517, ProcessAndProcessAndProcess808, sub_8321D848, sub_8321E160, sub_83221D10, sub_83222238, sub_83228F28) based on the result. This is a tree insertion routine in the 0x829FF cluster. 581 lines. 11 callees. 0 callers.
[entrypoint.functions."0x829FFC48"]
name = "ProcessAndProcessAndProcess923_829FFC48"

# Compute special-function values and normalize a vector (seven hundred and one hundred and one hundred and twenty-fourth form). It takes a structure, computes special-function values (ComputeSpecialFunction, ComputeSpecialFunction2), normalizes a vector (NormalizeVector), and drives a sub-operation (ProcessAndProcessAndProcess802) based on the result. Part of the 0x8321D process cluster. 318 lines. 4 callees. 1 caller (ProcessAndProcessAndProcess923).
[entrypoint.functions."0x8321D848"]
name = "ProcessAndProcessAndProcess924_8321D848"

# Process a structure (seven hundred and one hundred and one hundred and twenty-fifth form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82C47 process cluster. 108 lines. 0 callees. 5 callers (sub_82C47700, sub_82C47808, sub_82C47978, sub_82C47A50, sub_82C47BC0).
[entrypoint.functions."0x82C47620"]
name = "ProcessAndProcessAndProcess925_82C47620"

# Process a structure by driving sub-operations (seven hundred and one hundred and one hundred and twenty-sixth form). It takes a structure, checks its state, and drives sub-operations (ProcessAndProcessAndProcess816, sub_822AAFB8) based on the result. Part of the 0x82286 process cluster. 122 lines. 2 callees. 3 callers.
[entrypoint.functions."0x82286430"]
name = "ProcessAndProcessAndProcess926_82286430"

# Process a structure by driving sub-operations (seven hundred and one hundred and one hundred and twenty-seventh form). It takes a structure, checks its state, and drives many sub-operations (ProcessAndProcessAndProcess791, sub_8219CE10, sub_821CCF58, sub_82221860, sub_82223C70, sub_8228EEA8, sub_822AA000, sub_82B9B5D0) based on the result. Part of the 0x821DC process cluster. 132 lines. 8 callees. 1 caller (sub_82236EE8).
[entrypoint.functions."0x821DCE30"]
name = "ProcessAndProcessAndProcess927_821DCE30"

# Process a structure by driving sub-operations (seven hundred and one hundred and one hundred and twenty-eighth form). It takes a structure, checks its state, and drives sub-operations (ProcessAndProcessAndProcess791, sub_821DCE30, ProcessAndProcessAndProcess900, sub_82A6CAD8, sub_82A6CC18, sub_82B9B5D0) based on the result. Part of the 0x82236 process cluster. 139 lines. 6 callees. 1 caller (sub_8223CF88).
[entrypoint.functions."0x82236EE8"]
name = "ProcessAndProcessAndProcess928_82236EE8"

# Process a structure by driving sub-operations (seven hundred and one hundred and one hundred and twenty-ninth form). It takes a structure, checks its state, and drives many sub-operations (ProcessAndProcessAndProcess621, ProcessAndProcessAndProcess805, sub_8217ECD8, sub_82236EE8, sub_822732F0, sub_8228EEA8, sub_822AA000) based on the result. Part of the 0x8223C process cluster. 260 lines. 7 callees. 9 callers.
[entrypoint.functions."0x8223CF88"]
name = "ProcessAndProcessAndProcess929_8223CF88"

# Process a structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and thirtieth form). It takes a structure and drives a sub-operation (ProcessAndProcessAndProcess636). Part of the 0x8228D process cluster. 78 lines. 1 callee. 8 callers.
[entrypoint.functions."0x8228DB78"]
name = "ProcessAndProcessAndProcess930_8228DB78"

# Process a structure (seven hundred and one hundred and one hundred and thirty-first form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x832B9 process cluster. 89 lines. 0 callees. 1 caller (ProcessAndProcessAndProcess654).
[entrypoint.functions."0x832B9518"]
name = "ProcessAndProcessAndProcess931_832B9518"

# Process a large data structure (seven hundred and one hundred and one hundred and thirty-second form). It takes a structure, checks its state, and drives sub-operations (ProcessAndProcessAndProcess898, sub_82B9B298) based on the result. Part of the 0x8227A process cluster. 685 lines. 2 callees. 2 callers (sub_821F0CC8 and sub_822A0760).
[entrypoint.functions."0x8227A9F0"]
name = "ProcessAndProcessAndProcess932_8227A9F0"

# Process a structure (seven hundred and one hundred and one hundred and thirty-third form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x821A8 process cluster. 7 lines. 0 callees. 3 callers.
[entrypoint.functions."0x821A8B20"]
name = "ProcessAndProcessAndProcess933_821A8B20"

# Process a structure by driving a sub-operation (seven hundred and one hundred and one hundred and thirty-fourth form). It takes a structure, checks its state, and drives a sub-operation (ProcessAndProcessAndProcess510) based on the result. Part of the 0x821B6 process cluster. 103 lines. 1 callee. 6 callers.
[entrypoint.functions."0x821B6F70"]
name = "ProcessAndProcessAndProcess934_821B6F70"

# Process a structure (seven hundred and one hundred and one hundred and thirty-fifth form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x8228E process cluster. 63 lines. 0 callees. 3 callers (sub_821DCE30, sub_8223CF88, sub_82282020).
[entrypoint.functions."0x8228EEA8"]
name = "ProcessAndProcessAndProcess935_8228EEA8"

# Process a frequently-used structure (seven hundred and one hundred and one hundred and thirty-sixth form). It takes a structure and checks its state, with no external sub-operations. This is a frequently-called helper in the 0x82214 process cluster. 138 lines. 0 callees. 14 callers.
[entrypoint.functions."0x82214F60"]
name = "ProcessAndProcessAndProcess936_82214F60"

# Process a structure (seven hundred and one hundred and one hundred and thirty-seventh form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x8220B process cluster. 18 lines. 0 callees. 1 caller (sub_82FEB738).
[entrypoint.functions."0x8220B5A8"]
name = "ProcessAndProcessAndProcess937_8220B5A8"

# Process a structure by allocating fresh memory (seven hundred and one hundred and one hundred and thirty-eighth form). It takes a structure, allocates fresh memory (ProcessAndAllocateAndProcess2), and drives a sub-operation (sub_822EFF58) based on the result. This is a frequently-called allocator in the 0x8217E process cluster. 72 lines. 2 callees. 43 callers.
[entrypoint.functions."0x8217E3F8"]
name = "ProcessAndProcessAndProcess938_8217E3F8"

# Process a structure by driving sub-operations (seven hundred and one hundred and one hundred and thirty-ninth form). It takes a structure, checks its state, and drives sub-operations (ProcessAndProcessAndProcess897, sub_821D17B8, ProcessAndProcessAndProcess926, sub_822981F0, sub_82B9B408) based on the result. Part of the 0x821E8 process cluster. 222 lines. 5 callees. 1 caller (sub_821E8D20).
[entrypoint.functions."0x821E8B98"]
name = "ProcessAndProcessAndProcess939_821E8B98"

# Process a frequently-used structure by driving sub-operations (seven hundred and one hundred and one hundred and fortieth form). It takes a structure, checks its state, and drives sub-operations (ProcessAndProcessAndProcess529, sub_821E8B98, sub_82242628, sub_82283530, ProcessAndProcessAndProcess926) based on the result. This is one of the hottest core process helpers in the recompiled binary, called from the main render loop. 150 lines. 5 callees. 78 callers.
[entrypoint.functions."0x821E8D20"]
name = "ProcessAndProcessAndProcess940_821E8D20"

# Process a very large data structure (seven hundred and one hundred and one hundred and forty-first form). It takes a structure and checks its state, with no external sub-operations. This is a leaf handler in the 0x82CD4/0x82CD5 cluster, dispatched solely by sub_82CD6010. 969 lines. 0 callees. 1 caller (sub_82CD6010).
[entrypoint.functions."0x82CD4A10"]
name = "ProcessAndProcessAndProcess941_82CD4A10"

# Process a structure (seven hundred and one hundred and one hundred and forty-second form). It takes a structure and checks its state, with no external sub-operations. This is a leaf handler in the 0x82CD5 cluster, dispatched solely by sub_82CD6010. 174 lines. 0 callees. 1 caller (sub_82CD6010).
[entrypoint.functions."0x82CD5060"]
name = "ProcessAndProcessAndProcess942_82CD5060"

# Process a structure (seven hundred and one hundred and one hundred and forty-third form). It takes a structure and checks its state, with no external sub-operations. This is a leaf handler in the 0x82CD5 cluster, dispatched solely by sub_82CD6010. 128 lines. 0 callees. 1 caller (sub_82CD6010).
[entrypoint.functions."0x82CD5170"]
name = "ProcessAndProcessAndProcess943_82CD5170"

# Process a large data structure (seven hundred and one hundred and one hundred and forty-fourth form). It takes a structure and checks its state, with no external sub-operations. This is a leaf handler in the 0x82CD5 cluster, dispatched solely by sub_82CD6010. 428 lines. 0 callees. 1 caller (sub_82CD6010).
[entrypoint.functions."0x82CD5238"]
name = "ProcessAndProcessAndProcess944_82CD5238"

# Process a structure (seven hundred and one hundred and one hundred and forty-fifth form). It takes a structure and checks its state, with no external sub-operations. This is a leaf handler in the 0x82CD5 cluster, dispatched solely by sub_82CD6010. 128 lines. 0 callees. 1 caller (sub_82CD6010).
[entrypoint.functions."0x82CD54E0"]
name = "ProcessAndProcessAndProcess945_82CD54E0"

# Process a structure (seven hundred and one hundred and one hundred and forty-sixth form). It takes a structure and checks its state, with no external sub-operations. This is a leaf handler in the 0x82CD5 cluster, dispatched solely by sub_82CD6010. 237 lines. 0 callees. 1 caller (sub_82CD6010).
[entrypoint.functions."0x82CD55A8"]
name = "ProcessAndProcessAndProcess946_82CD55A8"

# Process a structure (seven hundred and one hundred and one hundred and forty-seventh form). It takes a structure and checks its state, with no external sub-operations. This is a leaf handler in the 0x82CD5 cluster, dispatched solely by sub_82CD6010. 239 lines. 0 callees. 1 caller (sub_82CD6010).
[entrypoint.functions."0x82CD5898"]
name = "ProcessAndProcessAndProcess947_82CD5898"

# Process a structure (seven hundred and one hundred and one hundred and forty-eighth form). It takes a structure and checks its state, with no external sub-operations. This is a leaf handler in the 0x82CD5 cluster, dispatched solely by sub_82CD6010. 108 lines. 0 callees. 1 caller (sub_82CD6010).
[entrypoint.functions."0x82CD5A10"]
name = "ProcessAndProcessAndProcess948_82CD5A10"

# Process a large data structure (seven hundred and one hundred and one hundred and forty-ninth form). It takes a structure and checks its state, with no external sub-operations. This is the largest leaf handler in the 0x82CD5 cluster, dispatched solely by sub_82CD6010. 748 lines. 0 callees. 1 caller (sub_82CD6010).
[entrypoint.functions."0x82CD5AB8"]
name = "ProcessAndProcessAndProcess949_82CD5AB8"

# Process a structure (seven hundred and one hundred and one hundred and fiftieth form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x8226B process cluster. 18 lines. 0 callees. 1 caller (sub_82FEB738).
[entrypoint.functions."0x8226B018"]
name = "ProcessAndProcessAndProcess950_8226B018"

# Process a refcounted string or record (seven hundred and one hundred and one hundred and fifty-first form). It takes a structure, constructs and destructs refcounted values (ConstructRefCounted, DestructRefCounted), performs refcounted copy assignments (CopyAssign_RefCounted, CopyAssign_RefCounted2), compares strings (StringCompare), and drives sub-operations (ProcessAndProcessAndProcess852, ProcessGrowBitArray) based on the result. Part of the 0x82B7C process cluster. 143 lines. 7 callees. 4 callers.
[entrypoint.functions."0x82B7C760"]
name = "ProcessAndProcessAndProcess951_82B7C760"

# Process a large data structure (seven hundred and one hundred and one hundred and fifty-second form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x821F5 process cluster. 365 lines. 0 callees. 6 callers.
[entrypoint.functions."0x821F5F20"]
name = "ProcessAndProcessAndProcess952_821F5F20"

# Process a structure by driving sub-operations (seven hundred and one hundred and one hundred and fifty-third form). It takes a structure, checks its state, and drives sub-operations (sub_8218D890, sub_82A9D720, sub_82A9EA10) based on the result. Part of the 0x82196 process cluster. 255 lines. 3 callees. 1 caller (ProcessAndProcessAndProcess901).
[entrypoint.functions."0x82196C60"]
name = "ProcessAndProcessAndProcess953_82196C60"

# Process a structure (seven hundred and one hundred and one hundred and fifty-fourth form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82229 process cluster. 268 lines. 0 callees. 2 callers (sub_82197150 and sub_82AAE628).
[entrypoint.functions."0x82229960"]
name = "ProcessAndProcessAndProcess954_82229960"

# Acquire a refcount by swapping it (seven hundred and one hundred and one hundred and fifty-fifth form). It takes a structure and swaps its reference count (SwapRefcount). This is a hot refcount acquirer in the 0x821FB cluster. 97 lines. 1 callee. 20 callers.
[entrypoint.functions."0x821FBBB8"]
name = "ProcessAndProcessAndProcess955_821FBBB8"

# Release a refcount by driving a processor (seven hundred and one hundred and one hundred and fifty-sixth form). It takes a structure, decrements its reference count (DecrementRefcountAtomic), swaps its reference count (SwapRefcount), and drives sub-operations (ProcessAndProcessAndProcess567, ProcessAndProcessAndProcess653, ProcessAndProcessAndProcess811) based on the result. This is a hot refcount release in the 0x821FC cluster. 138 lines. 5 callees. 20 callers.
[entrypoint.functions."0x821FC320"]
name = "ProcessAndProcessAndProcess956_821FC320"

# Process a structure by driving a sub-operation (seven hundred and one hundred and one hundred and fifty-seventh form). It takes a structure, checks its state, and drives a sub-operation (ProcessAndProcessAndProcess510) based on the result. Part of the 0x821B7 process cluster. 115 lines. 1 callee. 4 callers.
[entrypoint.functions."0x821B71A0"]
name = "ProcessAndProcessAndProcess957_821B71A0"

# Grow a buffer and append to it (seven hundred and one hundred and one hundred and fifty-eighth form). It takes a structure, grows a buffer (GrowBufferAndAppend), and drives a sub-operation (ProcessAndProcessAndProcess776) based on the result. Part of the 0x821D9 process cluster. 99 lines. 2 callees. 0 callers.
[entrypoint.functions."0x821D98E8"]
name = "ProcessAndProcessAndProcess958_821D98E8"

# Process a structure by driving a sub-operation (seven hundred and one hundred and one hundred and fifty-ninth form). It takes a structure, checks its state, and drives a sub-operation (ProcessAndProcessAndProcess852) based on the result. Part of the 0x821B6 process cluster. 116 lines. 1 callee. 6 callers.
[entrypoint.functions."0x821B6A00"]
name = "ProcessAndProcessAndProcess959_821B6A00"

# Process a structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and sixtieth form). It takes a structure and drives a sub-operation (ProcessAndProcessAndProcess624). Part of the 0x821B9 process cluster. 20 lines. 1 callee. 0 callers.
[entrypoint.functions."0x821B9EB8"]
name = "ProcessAndProcessAndProcess960_821B9EB8"

# Process a structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and sixty-first form). It takes a structure and drives a sub-operation (ProcessAndProcessAndProcess869). Part of the 0x82C0D process cluster. 124 lines. 1 callee. 1 caller (sub_82C0FFF8).
[entrypoint.functions."0x82C0D488"]
name = "ProcessAndProcessAndProcess961_82C0D488"

# Process a structure (seven hundred and one hundred and one hundred and sixty-second form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x829FD process cluster. 61 lines. 0 callees. 2 callers (sub_82261558 and sub_82261668).
[entrypoint.functions."0x829FDF28"]
name = "ProcessAndProcessAndProcess962_829FDF28"

# Process a large data structure (seven hundred and one hundred and one hundred and sixty-third form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x832BF process cluster. 629 lines. 0 callees. 3 callers (sub_832BECC0, sub_832BED90, sub_832BEFC0).
[entrypoint.functions."0x832BF5A0"]
name = "ProcessAndProcessAndProcess963_832BF5A0"

# Process a frequently-used structure (seven hundred and one hundred and one hundred and sixty-fourth form). It takes a structure and checks its state, with no external sub-operations. This is a frequently-called helper in the 0x82C51 process cluster. 10 lines. 0 callees. 23 callers.
[entrypoint.functions."0x82C51BF8"]
name = "ProcessAndProcessAndProcess964_82C51BF8"

# Process a structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and sixty-fifth form). It takes a structure and drives a sub-operation (ProcessAndProcessAndProcess940). Part of the 0x822AF process cluster. 73 lines. 1 callee. 4 callers.
[entrypoint.functions."0x822AF3A0"]
name = "ProcessAndProcessAndProcess965_822AF3A0"

# Process an event by wrapping a sub-operation (seven hundred and one hundred and one hundred and sixty-sixth form). It takes a structure, processes an event (ProcessEvent), and drives sub-operations based on the result. This is part of the UI text render path, called from UIText_FrameRenderIter. Part of the 0x82C03 process cluster. 102 lines. 1 callee. 5 callers.
[entrypoint.functions."0x82C03D10"]
name = "ProcessAndProcessAndProcess966_82C03D10"

# Free memory by wrapping a sub-operation (seven hundred and one hundred and one hundred and sixty-seventh form). It takes a structure and frees memory (Free). Part of the 0x82BFD process cluster. 45 lines. 1 callee. 0 callers.
[entrypoint.functions."0x82BFDB80"]
name = "ProcessAndProcessAndProcess967_82BFDB80"

# Process a structure (seven hundred and one hundred and one hundred and sixty-eighth form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x821F0 process cluster. 70 lines. 0 callees. 1 caller (sub_82FE8740).
[entrypoint.functions."0x821F0358"]
name = "ProcessAndProcessAndProcess968_821F0358"

# Process a structure (seven hundred and one hundred and one hundred and sixty-ninth form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x821A8 process cluster. 141 lines. 0 callees. 2 callers (sub_821A8E20 and sub_821DBDD0).
[entrypoint.functions."0x821A8D38"]
name = "ProcessAndProcessAndProcess969_821A8D38"

# Process a structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and seventieth form). It takes a structure and drives a sub-operation (ProcessAndProcessAndProcess966). This is part of the UI text render path (ProcessAndProcessAndProcess742 → this → ProcessAndProcessAndProcess966 → ProcessEvent). Part of the 0x82C03 process cluster. 83 lines. 1 callee. 1 caller (ProcessAndProcessAndProcess742).
[entrypoint.functions."0x82C03E40"]
name = "ProcessAndProcessAndProcess970_82C03E40"

# Process a structure by driving sub-operations (seven hundred and one hundred and one hundred and seventy-first form). It takes a structure, checks its state, and drives sub-operations (ProcessAndProcessAndProcess624, ProcessAndProcessAndProcess867, sub_82201DE8, ProcessAndProcessAndProcess972) based on the result. Part of the 0x821CB process cluster. 214 lines. 4 callees. 1 caller (ProcessAndProcessAndProcess595).
[entrypoint.functions."0x821CB260"]
name = "ProcessAndProcessAndProcess971_821CB260"

# Process a structure by driving a sub-operation (seven hundred and one hundred and one hundred and seventy-second form). It takes a structure, checks its state, and drives a sub-operation (ProcessAndProcessAndProcess867) based on the result. Part of the 0x82231 process cluster. 142 lines. 1 callee. 2 callers (sub_821A1428 and sub_821CB260).
[entrypoint.functions."0x82231EA8"]
name = "ProcessAndProcessAndProcess972_82231EA8"

# Process a structure (seven hundred and one hundred and one hundred and seventy-third form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x8228F process cluster. 109 lines. 0 callees. 4 callers (sub_821BDE80, sub_8228F0C0, sub_82A4EF80, sub_82A4F3E0).
[entrypoint.functions."0x8228F000"]
name = "ProcessAndProcessAndProcess973_8228F000"

# Process a structure (seven hundred and one hundred and one hundred and seventy-fourth form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82C12 process cluster. 30 lines. 0 callees. 0 callers.
[entrypoint.functions."0x82C12EA0"]
name = "ProcessAndProcessAndProcess974_82C12EA0"

# Process a frequently-used structure (seven hundred and one hundred and one hundred and seventy-fifth form). It takes a structure and checks its state, with no external sub-operations. This is a frequently-called helper in the 0x82345 process cluster. 59 lines. 0 callees. 19 callers.
[entrypoint.functions."0x823459D8"]
name = "ProcessAndProcessAndProcess975_823459D8"

# Process a structure (seven hundred and one hundred and one hundred and seventy-sixth form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x821DB process cluster. 70 lines. 0 callees. 1 caller (sub_82FE8740).
[entrypoint.functions."0x821DBCB8"]
name = "ProcessAndProcessAndProcess976_821DBCB8"

# Process a large data structure (seven hundred and one hundred and one hundred and seventy-seventh form). It takes a structure, checks its state, and drives sub-operations (ProcessAndProcessAndProcess774, ProcessAndProcessAndProcess978) based on the result. Part of the 0x821B1 process cluster. 391 lines. 2 callees. 1 caller (ProcessAndProcessAndProcess980).
[entrypoint.functions."0x821B1910"]
name = "ProcessAndProcessAndProcess977_821B1910"

# Process a structure by driving sub-operations (seven hundred and one hundred and one hundred and seventy-eighth form). It takes a structure, checks its state, and drives sub-operations (ProcessAndProcessAndProcess774, ProcessAndProcessAndProcess821) based on the result. Part of the 0x821B1 process cluster. 77 lines. 2 callees. 1 caller (sub_821B1910).
[entrypoint.functions."0x821B1CA0"]
name = "ProcessAndProcessAndProcess978_821B1CA0"

# Process a structure (seven hundred and one hundred and one hundred and seventy-ninth form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x821C6 process cluster. 53 lines. 0 callees. 5 callers.
[entrypoint.functions."0x821C6778"]
name = "ProcessAndProcessAndProcess979_821C6778"

# Dispatch a large object-processing operation (seven hundred and one hundred and one hundred and eightieth form). It takes a structure, checks its state, and drives many sub-operations (ProcessAndProcessAndProcess899, ProcessAndProcessAndProcess935, ProcessAndProcessAndProcess977, ProcessAndProcessAndProcess979, sub_822228F0, ProcessAndProcessAndProcess981, ProcessAndProcessAndProcess982) based on the result. This is the dispatcher of the 0x82282 process cluster. 589 lines. 7 callees. 6 callers.
[entrypoint.functions."0x82282020"]
name = "ProcessAndProcessAndProcess980_82282020"

# Process a structure by driving sub-operations (seven hundred and one hundred and one hundred and eighty-first form). It takes a structure, checks its state, and drives sub-operations (ProcessAndProcessAndProcess510, ProcessAndProcessAndProcess936) based on the result. Part of the 0x82282 process cluster. 123 lines. 2 callees. 1 caller (ProcessAndProcessAndProcess980).
[entrypoint.functions."0x822823D8"]
name = "ProcessAndProcessAndProcess981_822823D8"

# Process a structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and eighty-second form). It takes a structure and drives a sub-operation (ProcessAndProcessAndProcess533). Part of the 0x82A72 process cluster. 185 lines. 1 callee. 1 caller (ProcessAndProcessAndProcess980).
[entrypoint.functions."0x82A72B60"]
name = "ProcessAndProcessAndProcess982_82A72B60"

# Process a structure by driving sub-operations (seven hundred and one hundred and one hundred and eighty-third form). It takes a structure, checks its state, and drives sub-operations (ProcessAndProcessAndProcess556, ProcessAndProcessAndProcess984) based on the result. Part of the 0x82270 process cluster. 262 lines. 2 callees. 4 callers.
[entrypoint.functions."0x82270D28"]
name = "ProcessAndProcessAndProcess983_82270D28"

# Process a large data structure (seven hundred and one hundred and one hundred and eighty-fourth form). It takes a structure, checks its state, and drives sub-operations (ProcessAndProcessAndProcess636, ProcessAndProcessAndProcess854) based on the result. Part of the 0x82270 process cluster. 631 lines. 2 callees. 1 caller (sub_82270D28).
[entrypoint.functions."0x82270EE0"]
name = "ProcessAndProcessAndProcess984_82270EE0"

# Process a large data structure (seven hundred and one hundred and one hundred and eighty-fifth form). It takes a structure, checks its state, and drives sub-operations (ProcessAndProcessAndProcess626, ProcessAndProcessAndProcess886, sub_82D02240, sub_82D03B48) based on the result. Part of the 0x82D00 process cluster. 437 lines. 4 callees. 1 caller (sub_82D00C30).
[entrypoint.functions."0x82D005F8"]
name = "ProcessAndProcessAndProcess985_82D005F8"

# Process a large data structure (seven hundred and one hundred and one hundred and eighty-sixth form). It takes a structure, checks its state, and drives sub-operations (ProcessAndProcessAndProcess626, sub_82CD2C38, sub_82CD2CF0, ProcessAndProcessAndProcess985, sub_82D01E18, sub_82D037E0) based on the result. Part of the 0x82D00 process cluster. 215 lines. 6 callees. 1 caller (sub_82D012D8).
[entrypoint.functions."0x82D00C30"]
name = "ProcessAndProcessAndProcess986_82D00C30"

# Process a frequently-used structure (seven hundred and one hundred and one hundred and eighty-seventh form). It takes a structure and checks its state, with no external sub-operations. This is a frequently-called helper in the 0x82293 process cluster. 215 lines. 0 callees. 12 callers.
[entrypoint.functions."0x82293EC0"]
name = "ProcessAndProcessAndProcess987_82293EC0"

# Process a structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and eighty-eighth form). It takes a structure and drives a sub-operation (ProcessAndProcessAndProcess634). Part of the 0x8218D process cluster. 157 lines. 1 callee. 3 callers.
[entrypoint.functions."0x8218D890"]
name = "ProcessAndProcessAndProcess988_8218D890"

# Process a structure (seven hundred and one hundred and one hundred and eighty-ninth form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82C61 process cluster. 19 lines. 0 callees. 2 callers (sub_82C5D2F0 and sub_82C5D350).
[entrypoint.functions."0x82C611A0"]
name = "ProcessAndProcessAndProcess989_82C611A0"

# Process a structure (seven hundred and one hundred and one hundred and eightieth form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82C61 process cluster. 19 lines. 0 callees. 2 callers (sub_82C5D2F0 and sub_82C5D350).
[entrypoint.functions."0x82C611C0"]
name = "ProcessAndProcessAndProcess990_82C611C0"

# Process a very frequently-used structure (seven hundred and one hundred and one hundred and ninety-first form). It takes a structure and checks its state, with no external sub-operations. This is one of the most frequently-called helpers in the recompiled binary. 6 lines. 0 callees. 16 callers.
[entrypoint.functions."0x82F279E0"]
name = "ProcessAndProcessAndProcess991_82F279E0"

# Process a structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and ninety-second form). It takes a structure and drives a sub-operation (ProcessAndProcessAndProcess741). Part of the 0x82C59 process cluster. 117 lines. 1 callee. 0 callers.
[entrypoint.functions."0x82C59450"]
name = "ProcessAndProcessAndProcess992_82C59450"

# Process a structure by driving sub-operations (seven hundred and one hundred and one hundred and ninety-third form). It takes a structure, checks its state, and drives sub-operations (ProcessAndProcessAndProcess622, sub_82C5D428) based on the result. Part of the 0x82C59 process cluster. 113 lines. 2 callees. 0 callers.
[entrypoint.functions."0x82C59E10"]
name = "ProcessAndProcessAndProcess993_82C59E10"

# Process a large data structure (seven hundred and one hundred and one hundred and ninety-fourth form). It takes a structure, checks its state, and drives sub-operations (ProcessAndProcessAndProcess888, sub_82C5F810, sub_82C60180, sub_82C605D8) based on the result. Part of the 0x82C5B process cluster. 208 lines. 4 callees. 0 callers.
[entrypoint.functions."0x82C5B490"]
name = "ProcessAndProcessAndProcess994_82C5B490"

# Process a structure by driving sub-operations (seven hundred and one hundred and one hundred and ninety-fifth form). It takes a structure, checks its state, and drives sub-operations (ProcessAndProcessAndProcess887, sub_82C5D170, sub_82C5D1A0, sub_82C60820, sub_82C60A38, ProcessAndProcessAndProcess989, ProcessAndProcessAndProcess990) based on the result. Part of the 0x82C5D process cluster. 119 lines. 7 callees. 2 callers (sub_82C5D428 and sub_82C5D608).
[entrypoint.functions."0x82C5D350"]
name = "ProcessAndProcessAndProcess995_82C5D350"

# Process a large data structure by copying aligned data (seven hundred and one hundred and one hundred and ninety-sixth form). It takes a structure, copies aligned data (MemCpy_Aligned), and drives a sub-operation (ProcessAndProcessAndProcess995) based on the result. Part of the 0x82C5D process cluster. 277 lines. 2 callees. 1 caller (sub_82C59E10).
[entrypoint.functions."0x82C5D428"]
name = "ProcessAndProcessAndProcess996_82C5D428"

# Process a structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and ninety-seventh form). It takes a structure and drives a sub-operation (ProcessAndProcessAndProcess734). Part of the 0x82C5D process cluster. 67 lines. 1 callee. 1 caller (sub_82C5DE68).
[entrypoint.functions."0x82C5DD68"]
name = "ProcessAndProcessAndProcess997_82C5DD68"

# Process a structure by copying aligned data (seven hundred and one hundred and one hundred and ninety-eighth form). It takes a structure, copies aligned data (MemCpy_Aligned), and drives sub-operations (ProcessAndProcessAndProcess997, sub_82C5DDD8) based on the result. Part of the 0x82C5D process cluster. 172 lines. 3 callees. 2 callers (sub_82C5B2E0 and sub_82C5F810).
[entrypoint.functions."0x82C5DE68"]
name = "ProcessAndProcessAndProcess998_82C5DE68"

# Process a large data structure by copying aligned data (seven hundred and one hundred and one hundred and ninety-ninth form). It takes a structure, copies aligned data (MemCpy_Aligned), and drives a sub-operation (ProcessAndProcessAndProcess887) based on the result. Part of the 0x82C5E process cluster. 322 lines. 2 callees. 0 callers.
[entrypoint.functions."0x82C5E370"]
name = "ProcessAndProcessAndProcess999_82C5E370"

# Process a structure (seven hundred and one hundred and one hundred and one hundredth form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82C5F process cluster. 64 lines. 0 callees. 1 caller (sub_82C5F810).
[entrypoint.functions."0x82C5F2D8"]
name = "ProcessAndProcessAndProcess1000_82C5F2D8"

# Process a very large data structure by copying aligned data (seven hundred and one hundred and one hundred and one hundred and first form). It takes a structure, copies aligned data (MemCpy_Aligned), and drives many sub-operations (ProcessAndProcessAndProcess513, ProcessAndProcessAndProcess546, ProcessAndProcessAndProcess547, ProcessAndProcessAndProcess597, ProcessAndProcessAndProcess598, ProcessAndProcessAndProcess599, ProcessAndProcessAndProcess998, and more) based on the result. Part of the 0x82C5F process cluster. 596 lines. 15 callees. 2 callers (sub_82C5B2E0 and sub_82C5B490).
[entrypoint.functions."0x82C5F810"]
name = "ProcessAndProcessAndProcess1001_82C5F810"

# Interpolate float arrays over a time difference (seven hundred and one hundred and one hundred and one hundred and second form). It takes a structure, computes a time difference (ComputeTimeDifferenceAsInteger), interpolates a float array (InterpolateFloatArray), multiplies and adds in place (MultiplyAndAddInPlace), and drives a sub-operation (sub_82C605D8) based on the result. This is a time-interpolation (tween) routine in the 0x82C60 process cluster. 300 lines. 4 callees. 1 caller (sub_82C5B490).
[entrypoint.functions."0x82C60180"]
name = "ProcessAndProcessAndProcess1002_82C60180"

# Process a structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and one hundred and third form). It takes a structure and drives a sub-operation (ProcessAndProcessAndProcess595). Part of the 0x82297 process cluster. 98 lines. 1 callee. 2 callers (ProcessAndProcessAndProcess819 and sub_82197D00).
[entrypoint.functions."0x822971B8"]
name = "ProcessAndProcessAndProcess1003_822971B8"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and fourth form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82379 process cluster. 86 lines. 0 callees. 3 callers (sub_823790B8, sub_823FC178, sub_82A3D510).
[entrypoint.functions."0x82379920"]
name = "ProcessAndProcessAndProcess1004_82379920"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and fifth form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82C0D process cluster. 90 lines. 0 callees. 0 callers.
[entrypoint.functions."0x82C0D068"]
name = "ProcessAndProcessAndProcess1005_82C0D068"

# Process a structure by driving sub-operations (seven hundred and one hundred and one hundred and one hundred and sixth form). It takes a structure, checks its state, and drives sub-operations (ProcessAndProcessAndProcess639, ProcessAndProcessAndProcess797, sub_832C0440, sub_832C05C8) based on the result. Part of the 0x832BF process cluster. 101 lines. 4 callees. 2 callers (sub_832BED90 and sub_832BEFC0).
[entrypoint.functions."0x832BFA70"]
name = "ProcessAndProcessAndProcess1006_832BFA70"

# Process a structure by looking up a tree leaf (seven hundred and one hundred and one hundred and one hundred and seventh form). It takes a structure, looks up a tree leaf (FindTreeLeaf), and drives a sub-operation (ProcessAndProcessAndProcess951) based on the result. Part of the 0x82B7D process cluster. 96 lines. 2 callees. 3 callers.
[entrypoint.functions."0x82B7DE00"]
name = "ProcessAndProcessAndProcess1007_82B7DE00"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and eighth form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82201 process cluster. 72 lines. 0 callees. 7 callers.
[entrypoint.functions."0x82201DE8"]
name = "ProcessAndProcessAndProcess1008_82201DE8"

# Process a very frequently-used structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and one hundred and ninth form). It takes a structure and drives a sub-operation (ProcessAndProcessAndProcess1010). This is one of the most frequently-called helpers in the recompiled binary. 82 lines. 1 callee. 94 callers.
[entrypoint.functions."0x821F0890"]
name = "ProcessAndProcessAndProcess1009_821F0890"

# Process a frequently-used structure (seven hundred and one hundred and one hundred and one hundred and tenth form). It takes a structure and checks its state, with no external sub-operations. This is a frequently-called helper in the 0x82200 process cluster. 218 lines. 0 callees. 25 callers.
[entrypoint.functions."0x82200800"]
name = "ProcessAndProcessAndProcess1010_82200800"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and eleventh form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82C50 process cluster, called from UITextPrompt_RenderElement. 16 lines. 0 callees. 7 callers.
[entrypoint.functions."0x82C50A10"]
name = "ProcessAndProcessAndProcess1011_82C50A10"

# Process a frequently-used structure (seven hundred and one hundred and one hundred and one hundred and twelfth form). It takes a structure and checks its state, with no external sub-operations. This is a frequently-called helper in the 0x82CA6 process cluster, called from ParseLineValue. 19 lines. 0 callees. 20 callers.
[entrypoint.functions."0x82CA6AB0"]
name = "ProcessAndProcessAndProcess1012_82CA6AB0"

# Read the merged pad state (seven hundred and one hundred and one hundred and one hundred and thirteenth form). It takes a structure and checks its state, with no external sub-operations. This is the guest's XamInputGetState wrapper that reads the final merged pad state (all drivers OR-merged), called from the state probe hook. Part of the 0x822B2 process cluster. 11 lines. 0 callees. 10 callers.
[entrypoint.functions."0x822B2D60"]
name = "ProcessAndProcessAndProcess1013_822B2D60"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and fourteenth form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82204 process cluster. 127 lines. 0 callees. 6 callers (including ProcessAndProcessAndProcess874).
[entrypoint.functions."0x82204638"]
name = "ProcessAndProcessAndProcess1014_82204638"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and fifteenth form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x821E0 process cluster. 18 lines. 0 callees. 0 callers.
[entrypoint.functions."0x821E07B8"]
name = "ProcessAndProcessAndProcess1015_821E07B8"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and sixteenth form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x8226A process cluster. 18 lines. 0 callees. 0 callers.
[entrypoint.functions."0x8226AE38"]
name = "ProcessAndProcessAndProcess1016_8226AE38"

# Process a structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and one hundred and seventeenth form). It takes a structure and drives a sub-operation (ProcessAndProcessAndProcess763). Part of the 0x82B89 process cluster. 181 lines. 1 callee. 2 callers (sub_82B69488 and sub_82B69FA0).
[entrypoint.functions."0x82B89960"]
name = "ProcessAndProcessAndProcess1017_82B89960"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and eighteenth form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82286 process cluster. 33 lines. 0 callees. 1 caller (sub_82FEB738).
[entrypoint.functions."0x822868E0"]
name = "ProcessAndProcessAndProcess1018_822868E0"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and nineteenth form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x8229F process cluster. 49 lines. 0 callees. 1 caller (sub_8227F0B0).
[entrypoint.functions."0x8229F550"]
name = "ProcessAndProcessAndProcess1019_8229F550"

# Process a structure by performing a checked copy (seven hundred and one hundred and one hundred and one hundred and twentieth form). It takes a structure and performs a checked copy (CheckedCopy). This is a frequently-called helper in the 0x821B1 process cluster. 69 lines. 1 callee. 23 callers.
[entrypoint.functions."0x821B1DC0"]
name = "ProcessAndProcessAndProcess1020_821B1DC0"

# Process a structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and one hundred and twenty-first form). It takes a structure and drives a sub-operation (ProcessAndProcessAndProcess973). Part of the 0x8228F process cluster. 77 lines. 1 callee. 5 callers.
[entrypoint.functions."0x8228F0C0"]
name = "ProcessAndProcessAndProcess1021_8228F0C0"

# Process a structure by driving sub-operations (seven hundred and one hundred and one hundred and one hundred and twenty-second form). It takes a structure, checks its state, and drives sub-operations (ProcessAndProcessAndProcess983, ProcessLoopVcall) based on the result. Part of the 0x8226D process cluster. 106 lines. 2 callees. 7 callers.
[entrypoint.functions."0x8226D4D0"]
name = "ProcessAndProcessAndProcess1022_8226D4D0"

# Process a structure by driving sub-operations (seven hundred and one hundred and one hundred and one hundred and twenty-third form). It takes a structure, checks its state, and drives sub-operations (sub_821B75A8, sub_821B7AB0, sub_82A9EB58) based on the result. Part of the 0x821B7 process cluster. 245 lines. 3 callees. 1 caller (ProcessAndProcessAndProcess901).
[entrypoint.functions."0x821B7918"]
name = "ProcessAndProcessAndProcess1023_821B7918"

# Process a large data structure (seven hundred and one hundred and one hundred and one hundred and twenty-fourth form). It takes a structure, checks its state, and drives a sub-operation (ProcessAndProcessAndProcess590) based on the result. Part of the 0x82B1A process cluster. 296 lines. 1 callee. 2 callers (sub_8223EA38 and sub_82A84280).
[entrypoint.functions."0x82B1A3F0"]
name = "ProcessAndProcessAndProcess1024_82B1A3F0"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and twenty-fifth form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x821FF process cluster. 120 lines. 0 callees. 0 callers.
[entrypoint.functions."0x821FFA48"]
name = "ProcessAndProcessAndProcess1025_821FFA48"

# Acquire a refcount by driving a processor (seven hundred and one hundred and one hundred and one hundred and twenty-sixth form). It takes a structure, increments its reference count (IncrementRefcountAtomic), and drives a sub-operation (ProcessAndProcessAndProcess792) based on the result. This is a refcount acquirer in the 0x82278 process cluster. 55 lines. 2 callees. 6 callers.
[entrypoint.functions."0x82278C38"]
name = "ProcessAndProcessAndProcess1026_82278C38"

# Process a large data structure by normalizing a vector (seven hundred and one hundred and one hundred and one hundred and twenty-seventh form). It takes a structure, normalizes a vector (NormalizeVector), and drives sub-operations based on the result. Part of the 0x821B4 process cluster. 353 lines. 1 callee. 4 callers.
[entrypoint.functions."0x821B4B38"]
name = "ProcessAndProcessAndProcess1027_821B4B38"

# Process a very large data structure (seven hundred and one hundred and one hundred and one hundred and twenty-eighth form). It takes a structure, checks its state, and drives sub-operations (ProcessAndProcessAndProcess604, ProcessAndProcessAndProcess1009) based on the result. Part of the 0x821F0 process cluster. 738 lines. 2 callees. 1 caller (sub_8228DF78).
[entrypoint.functions."0x821F0918"]
name = "ProcessAndProcessAndProcess1028_821F0918"

# Process a large data structure (seven hundred and one hundred and one hundred and one hundred and twenty-ninth form). It takes a structure, checks its state, and drives sub-operations (ProcessAndProcessAndProcess904, ProcessAndProcessAndProcess930, ProcessAndProcessAndProcess1027, sub_82293DA8) based on the result. Part of the 0x8228D process cluster. 271 lines. 4 callees. 1 caller (sub_8228DF78).
[entrypoint.functions."0x8228DDC8"]
name = "ProcessAndProcessAndProcess1029_8228DDC8"

# Dispatch a process operation (seven hundred and one hundred and one hundred and one hundred and thirtieth form). It takes a structure, checks its state, and drives sub-operations (ProcessAndProcessAndProcess1028, ProcessAndProcessAndProcess1029, sub_8228DFB8) based on the result. This is the dispatcher of the 0x8228D process cluster. 42 lines. 3 callees. 0 callers.
[entrypoint.functions."0x8228DF78"]
name = "ProcessAndProcessAndProcess1030_8228DF78"

# Process a large data structure (seven hundred and one hundred and one hundred and one hundred and thirty-first form). It takes a structure, checks its state, and drives sub-operations (ProcessAndProcessAndProcess817, ProcessAndProcessAndProcess1033) based on the result. Part of the 0x8228D process cluster. 299 lines. 2 callees. 1 caller (ProcessAndProcessAndProcess1030).
[entrypoint.functions."0x8228DFB8"]
name = "ProcessAndProcessAndProcess1031_8228DFB8"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and thirty-second form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x8228E process cluster. 97 lines. 0 callees. 2 callers (ProcessAndProcessAndProcess1033 and sub_822A9960).
[entrypoint.functions."0x8228E250"]
name = "ProcessAndProcessAndProcess1032_8228E250"

# Process a structure by copying its data (seven hundred and one hundred and one hundred and one hundred and thirty-third form). It takes a structure, copies its data (MemCpy), and drives sub-operations (ProcessAndProcessAndProcess1032, ProcessAndProcessAndProcess1034) based on the result. Part of the 0x8228E process cluster. 97 lines. 3 callees. 9 callers (including ProcessAndProcessAndProcess840).
[entrypoint.functions."0x8228E2F8"]
name = "ProcessAndProcessAndProcess1033_8228E2F8"

# Process a large data structure (seven hundred and one hundred and one hundred and one hundred and thirty-fourth form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x8228E process cluster. 280 lines. 0 callees. 2 callers (ProcessAndProcessAndProcess1033 and sub_822A9960).
[entrypoint.functions."0x8228E3A0"]
name = "ProcessAndProcessAndProcess1034_8228E3A0"

# Process a structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and one hundred and thirty-fifth form). It takes a structure and drives a sub-operation (ProcessAndProcessAndProcess857). Part of the 0x82293 process cluster. 177 lines. 1 callee. 2 callers (ProcessAndProcessAndProcess1029 and sub_82A95B20).
[entrypoint.functions."0x82293DA8"]
name = "ProcessAndProcessAndProcess1035_82293DA8"

# Process a frequently-used structure (seven hundred and one hundred and one hundred and one hundred and thirty-sixth form). It takes a structure and checks its state, with no external sub-operations. This is a frequently-called helper in the 0x8220B process cluster. 128 lines. 0 callees. 13 callers.
[entrypoint.functions."0x8220B5C8"]
name = "ProcessAndProcessAndProcess1036_8220B5C8"

# Process a structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and one hundred and thirty-seventh form). It takes a structure and drives a sub-operation (ProcessAndProcessAndProcess1036). Part of the 0x82A60 process cluster. 72 lines. 1 callee. 1 caller (sub_8228B290).
[entrypoint.functions."0x82A602D8"]
name = "ProcessAndProcessAndProcess1037_82A602D8"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and thirty-eighth form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82225 process cluster. 34 lines. 0 callees. 0 callers.
[entrypoint.functions."0x82225F28"]
name = "ProcessAndProcessAndProcess1038_82225F28"

# Process a very frequently-used structure (seven hundred and one hundred and one hundred and one hundred and thirty-ninth form). It takes a structure and checks its state, with no external sub-operations. This is one of the most frequently-called helpers in the recompiled binary. 93 lines. 0 callees. 70 callers.
[entrypoint.functions."0x8273FAF8"]
name = "ProcessAndProcessAndProcess1039_8273FAF8"

# Process a very large data structure (seven hundred and one hundred and one hundred and one hundred and fortieth form). It takes a structure, checks its state, and drives sub-operations (ProcessAndProcessAndProcess867, ProcessAndProcessAndProcess954, SwitchDispatch, sub_82AAE408) based on the result. Part of the 0x82AAE process cluster. 1228 lines. 4 callees. 1 caller (sub_821D4E50).
[entrypoint.functions."0x82AAE628"]
name = "ProcessAndProcessAndProcess1040_82AAE628"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and forty-first form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82215 process cluster. 22 lines. 0 callees. 1 caller (sub_82FEB738).
[entrypoint.functions."0x822155B8"]
name = "ProcessAndProcessAndProcess1041_822155B8"

# Process a very frequently-used structure (seven hundred and one hundred and one hundred and one hundred and forty-second form). It takes a structure and checks its state, with no external sub-operations. This is a frequently-called helper in the 0x82BFC process cluster, called from UITextPrompt_RenderElement. 10 lines. 0 callees. 23 callers.
[entrypoint.functions."0x82BFCB58"]
name = "ProcessAndProcessAndProcess1042_82BFCB58"

# Process a large data structure by freeing size-bucketed memory (seven hundred and one hundred and one hundred and one hundred and forty-third form). It takes a structure, frees size-bucketed memory (Free_SizeBucketed), and drives sub-operations (sub_82A77928, sub_82A97648) based on the result. Part of the 0x82A00 process cluster. 593 lines. 3 callees. 4 callers (including ProcessAndProcessAndProcess861).
[entrypoint.functions."0x82A00A18"]
name = "ProcessAndProcessAndProcess1043_82A00A18"

# Process a large data structure (seven hundred and one hundred and one hundred and one hundred and forty-fourth form). It takes a structure, checks its state, and drives a sub-operation (ProcessAndProcessAndProcess529) based on the result. Part of the 0x82283 process cluster. 331 lines. 1 callee. 2 callers (including ProcessAndProcessAndProcess940).
[entrypoint.functions."0x82283530"]
name = "ProcessAndProcessAndProcess1044_82283530"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and forty-fifth form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82298 process cluster. 145 lines. 0 callees. 2 callers (including ProcessAndProcessAndProcess939).
[entrypoint.functions."0x822981F0"]
name = "ProcessAndProcessAndProcess1045_822981F0"

# Process a structure by driving sub-operations (seven hundred and one hundred and one hundred and one hundred and forty-sixth form). It takes a structure, checks its state, and drives sub-operations (ProcessAndProcessAndProcess568, ProcessAndProcessAndProcess940, ProcessAndProcessAndProcess965) based on the result. Part of the 0x822A6 process cluster, called from the main render loop. 154 lines. 3 callees. 6 callers.
[entrypoint.functions."0x822A6318"]
name = "ProcessAndProcessAndProcess1046_822A6318"

# Process a structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and one hundred and forty-seventh form). It takes a structure and drives a sub-operation (ProcessAndProcessAndProcess932). Part of the 0x822A0 process cluster. 124 lines. 1 callee. 1 caller (ProcessAndProcessAndProcess806).
[entrypoint.functions."0x822A0760"]
name = "ProcessAndProcessAndProcess1047_822A0760"

# Process a very frequently-used structure (seven hundred and one hundred and one hundred and one hundred and forty-eighth form). It takes a structure and checks its state, with no external sub-operations. This is one of the most frequently-called helpers in the recompiled binary. 352 lines. 0 callees. 92 callers.
[entrypoint.functions."0x8220CA28"]
name = "ProcessAndProcessAndProcess1048_8220CA28"

# Process a structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and one hundred and forty-ninth form). It takes a structure and drives a sub-operation (sub_82366FB0). This is a frequently-called helper in the 0x82365 process cluster. 126 lines. 1 callee. 17 callers.
[entrypoint.functions."0x82365FF0"]
name = "ProcessAndProcessAndProcess1049_82365FF0"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and fiftieth form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82A45 process cluster. 8 lines. 0 callees. 0 callers.
[entrypoint.functions."0x82A459F8"]
name = "ProcessAndProcessAndProcess1050_82A459F8"

# Process a structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and one hundred and fifty-first form). It takes a structure and drives a sub-operation (ProcessAndProcessAndProcess533). This is a frequently-called helper in the 0x82B87 process cluster. 75 lines. 1 callee. 16 callers.
[entrypoint.functions."0x82B876D8"]
name = "ProcessAndProcessAndProcess1051_82B876D8"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and fifty-second form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82272 process cluster. 22 lines. 0 callees. 0 callers.
[entrypoint.functions."0x82272E00"]
name = "ProcessAndProcessAndProcess1052_82272E00"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and fifty-third form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x821C9 process cluster. 33 lines. 0 callees. 2 callers (sub_82FE8E60 and sub_82FE9018).
[entrypoint.functions."0x821C9220"]
name = "ProcessAndProcessAndProcess1053_821C9220"

# Dispatch the entire 0x82CD4/0x82CD5 leaf-processor cluster (seven hundred and one hundred and one hundred and one hundred and fifty-fourth form). It takes a structure, checks its state, and drives all the leaf handlers (ProcessAndProcessAndProcess859, ProcessAndProcessAndProcess941, ProcessAndProcessAndProcess942, ProcessAndProcessAndProcess943, ProcessAndProcessAndProcess944, ProcessAndProcessAndProcess945, ProcessAndProcessAndProcess946, ProcessAndProcessAndProcess947, ProcessAndProcessAndProcess948, ProcessAndProcessAndProcess949) based on the result. This is the master dispatcher of the 0x82CD6 cluster and the second-largest function in the recompiled binary. 3363 lines. 10 callees. 1 caller (sub_82CD7788).
[entrypoint.functions."0x82CD6010"]
name = "ProcessAndProcessAndProcess1054_82CD6010"

# Process a structure by driving sub-operations (seven hundred and one hundred and one hundred and one hundred and fifty-fifth form). It takes a structure, checks its state, and drives sub-operations (ProcessAndProcessAndProcess1054, sub_82CDDB68, sub_82CDDB80, sub_82CDF788) based on the result. Part of the 0x82CD7 process cluster. 186 lines. 4 callees. 1 caller (sub_82CD7960).
[entrypoint.functions."0x82CD7788"]
name = "ProcessAndProcessAndProcess1055_82CD7788"

# Process a large data structure by querying the timebase (seven hundred and one hundred and one hundred and one hundred and fifty-sixth form). It takes a structure, queries the timebase (GetTimebase), and drives sub-operations (ProcessAndProcessAndProcess741, ProcessAndProcessAndProcess868, ProcessAndProcessAndProcess1055, ProcessAndProcessAndProcess1057) based on the result. This is the top-level entry to the 0x82CD6/0x82CD7 cluster. 199 lines. 5 callees. 0 callers.
[entrypoint.functions."0x82CD7960"]
name = "ProcessAndProcessAndProcess1056_82CD7960"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and fifty-seventh form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82CDD process cluster. 8 lines. 0 callees. 1 caller (sub_82CD7960).
[entrypoint.functions."0x82CDDB60"]
name = "ProcessAndProcessAndProcess1057_82CDDB60"

# Process a structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and one hundred and fifty-eighth form). It takes a structure and drives a sub-operation (ProcessAndProcessAndProcess940). Part of the 0x82BA8 process cluster. 129 lines. 1 callee. 1 caller (sub_82B9C7F8).
[entrypoint.functions."0x82BA8170"]
name = "ProcessAndProcessAndProcess1058_82BA8170"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and fifty-ninth form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x821B1 process cluster. 18 lines. 0 callees. 0 callers.
[entrypoint.functions."0x821B1548"]
name = "ProcessAndProcessAndProcess1059_821B1548"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and sixtieth form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x821B1 process cluster. 14 lines. 0 callees. 0 callers.
[entrypoint.functions."0x821B1568"]
name = "ProcessAndProcessAndProcess1060_821B1568"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and sixty-first form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x8229A process cluster. 18 lines. 0 callees. 2 callers (sub_82FE8E60 and sub_82FE9018).
[entrypoint.functions."0x8229A778"]
name = "ProcessAndProcessAndProcess1061_8229A778"

# Process a large data structure (seven hundred and one hundred and one hundred and one hundred and sixty-second form). It takes a structure, checks its state, and drives a sub-operation (ProcessAndProcessAndProcess877) based on the result. Part of the 0x82A9D process cluster. 480 lines. 1 callee. 1 caller (ProcessAndProcessAndProcess953).
[entrypoint.functions."0x82A9D720"]
name = "ProcessAndProcessAndProcess1062_82A9D720"

# Process a structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and one hundred and sixty-third form). It takes a structure and drives a sub-operation (ProcessAndProcessAndProcess877). Part of the 0x82A9E process cluster. 159 lines. 1 callee. 1 caller (sub_82A9D720).
[entrypoint.functions."0x82A9E4F8"]
name = "ProcessAndProcessAndProcess1063_82A9E4F8"

# Process a frequently-used structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and one hundred and sixty-fourth form). It takes a structure and drives a sub-operation (sub_82B9B340). This is a frequently-called helper in the 0x8219C process cluster. 86 lines. 1 callee. 18 callers.
[entrypoint.functions."0x8219CCD8"]
name = "ProcessAndProcessAndProcess1064_8219CCD8"

# Process a structure by driving sub-operations (seven hundred and one hundred and one hundred and one hundred and sixty-fifth form). It takes a structure, checks its state, and drives sub-operations (ProcessAndProcessAndProcess510, sub_82B60D10) based on the result. Part of the 0x821B5 process cluster. 120 lines. 2 callees. 1 caller (sub_821B6EC0).
[entrypoint.functions."0x821B5358"]
name = "ProcessAndProcessAndProcess1065_821B5358"

# Process a structure by driving sub-operations (seven hundred and one hundred and one hundred and one hundred and sixty-sixth form). It takes a structure, checks its state, and drives sub-operations (ProcessAndProcessAndProcess642, ProcessAndProcessAndProcess934, ProcessAndProcessAndProcess1064, ProcessAndProcessAndProcess1065) based on the result. Part of the 0x821B6 process cluster. 100 lines. 4 callees. 3 callers.
[entrypoint.functions."0x821B6EC0"]
name = "ProcessAndProcessAndProcess1066_821B6EC0"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and sixty-seventh form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x821DC process cluster. 72 lines. 0 callees. 1 caller (sub_82FE8860).
[entrypoint.functions."0x821DC600"]
name = "ProcessAndProcessAndProcess1067_821DC600"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and sixty-eighth form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82BFF process cluster. 8 lines. 0 callees. 6 callers (sub_82C0F3F8, sub_82C0F998, sub_82C2F748, sub_82C41BF0, sub_82C48210, sub_82C6ABE8).
[entrypoint.functions."0x82BFFA40"]
name = "ProcessAndProcessAndProcess1068_82BFFA40"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and sixty-ninth form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82193 process cluster. 109 lines. 0 callees. 2 callers (sub_82193280 and sub_82A4EF80).
[entrypoint.functions."0x82193398"]
name = "ProcessAndProcessAndProcess1069_82193398"

# Process a structure by freeing size-bucketed memory (seven hundred and one hundred and one hundred and one hundred and seventieth form). It takes a structure, frees size-bucketed memory (Free_SizeBucketed), and drives a sub-operation (sub_8272EFC0) based on the result. Part of the 0x82A77 process cluster. 62 lines. 2 callees. 4 callers.
[entrypoint.functions."0x82A773E0"]
name = "ProcessAndProcessAndProcess1070_82A773E0"

# Process a structure by freeing size-bucketed memory (seven hundred and one hundred and one hundred and one hundred and seventy-first form). It takes a structure, frees size-bucketed memory (Free_SizeBucketed), and drives sub-operations (ProcessAndProcessAndProcess1069, sub_82344230, sub_82A95E48) based on the result. Part of the 0x82193 process cluster. 159 lines. 4 callees. 5 callers.
[entrypoint.functions."0x82193280"]
name = "ProcessAndProcessAndProcess1071_82193280"

# Process a structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and one hundred and seventy-second form). It takes a structure and drives a sub-operation (ProcessAndProcessAndProcess919). This is a frequently-called helper in the 0x821AD process cluster. 101 lines. 1 callee. 10 callers.
[entrypoint.functions."0x821ADF68"]
name = "ProcessAndProcessAndProcess1072_821ADF68"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and seventy-third form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x822AA process cluster. 51 lines. 0 callees. 3 callers (ProcessAndProcessAndProcess927, ProcessAndProcessAndProcess929, sub_821C3CD0).
[entrypoint.functions."0x822AA000"]
name = "ProcessAndProcessAndProcess1073_822AA000"

# Process a structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and one hundred and seventy-fourth form). It takes a structure and drives a sub-operation (ProcessAndProcessAndProcess1024). Part of the 0x8223E process cluster. 60 lines. 1 callee. 3 callers.
[entrypoint.functions."0x8223EA38"]
name = "ProcessAndProcessAndProcess1074_8223EA38"

# Free size-bucketed memory (seven hundred and one hundred and one hundred and one hundred and seventy-fifth form). It takes a structure and frees size-bucketed memory (Free_SizeBucketed). This is a frequently-called allocator in the 0x82A6C process cluster. 98 lines. 1 callee. 12 callers.
[entrypoint.functions."0x82A6C450"]
name = "ProcessAndProcessAndProcess1075_82A6C450"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and seventy-sixth form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82205 process cluster. 127 lines. 0 callees. 8 callers.
[entrypoint.functions."0x82205A50"]
name = "ProcessAndProcessAndProcess1076_82205A50"

# Process a structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and one hundred and seventy-seventh form). It takes a structure and drives a sub-operation (ProcessAndProcessAndProcess678). Part of the 0x82A6C process cluster. 89 lines. 1 callee. 2 callers (sub_82276700 and sub_8229B650).
[entrypoint.functions."0x82A6C128"]
name = "ProcessAndProcessAndProcess1077_82A6C128"

# Process a structure by allocating fresh memory (seven hundred and one hundred and one hundred and one hundred and seventy-eighth form). It takes a structure, allocates fresh memory (Allocate_OrDbgBreak), and drives a sub-operation (sub_82A96358) based on the result. Part of the 0x82A6C process cluster. 110 lines. 2 callees. 3 callers.
[entrypoint.functions."0x82A6C4F0"]
name = "ProcessAndProcessAndProcess1078_82A6C4F0"

# Process a large data structure by copying its data (seven hundred and one hundred and one hundred and one hundred and seventy-ninth form). It takes a structure, copies its data (MemCpy), and drives sub-operations (ProcessAndProcessAndProcess816, ProcessAndProcessAndProcess903, sub_82B9E610, sub_82B9E928, sub_82B9E9F0, sub_82B9EB28, sub_82B9EC20) based on the result. Part of the 0x82B9E process cluster. 502 lines. 8 callees. 1 caller (sub_82B9F280).
[entrypoint.functions."0x82B9ED28"]
name = "ProcessAndProcessAndProcess1079_82B9ED28"

# Process a large data structure (seven hundred and one hundred and one hundred and one hundred and eightieth form). It takes a structure, checks its state, and drives sub-operations (ProcessAndProcessAndProcess523, ProcessAndProcessAndProcess679, sub_82B9BEC8) based on the result. Part of the 0x82B9F process cluster. 273 lines. 3 callees. 1 caller (sub_82B9F280).
[entrypoint.functions."0x82B9F0A8"]
name = "ProcessAndProcessAndProcess1080_82B9F0A8"

# Dispatch a process operation (seven hundred and one hundred and one hundred and one hundred and eighty-first form). It takes a structure, checks its state, and drives sub-operations (ProcessAndProcessAndProcess679, ProcessAndProcessAndProcess1079, ProcessAndProcessAndProcess1080) based on the result. This is the dispatcher of the 0x82B9F process cluster. 471 lines. 3 callees. 1 caller (sub_82B9F598).
[entrypoint.functions."0x82B9F280"]
name = "ProcessAndProcessAndProcess1081_82B9F280"

# Process a structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and one hundred and eighty-second form). It takes a structure and drives a sub-operation (ProcessAndProcessAndProcess679). Part of the 0x82B9F process cluster. 97 lines. 1 callee. 0 callers.
[entrypoint.functions."0x82B9F730"]
name = "ProcessAndProcessAndProcess1082_82B9F730"

# Process a structure by querying the timebase (seven hundred and one hundred and one hundred and one hundred and eighty-third form). It takes a structure, queries the timebase (GetTimebase), and drives sub-operations based on the result. Part of the 0x82B9C process cluster. 155 lines. 1 callee. 1 caller (ProcessAndProcessAndProcess894).
[entrypoint.functions."0x82B9C530"]
name = "ProcessAndProcessAndProcess1083_82B9C530"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and eighty-fourth form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82C08 process cluster. 114 lines. 0 callees. 0 callers.
[entrypoint.functions."0x82C08E40"]
name = "ProcessAndProcessAndProcess1084_82C08E40"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and eighty-fifth form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82C4D process cluster. 65 lines. 0 callees. 0 callers.
[entrypoint.functions."0x82C4DEA8"]
name = "ProcessAndProcessAndProcess1085_82C4DEA8"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and eighty-sixth form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82C0B process cluster. 26 lines. 0 callees. 0 callers.
[entrypoint.functions."0x82C0B3D8"]
name = "ProcessAndProcessAndProcess1086_82C0B3D8"

# Process a frequently-used structure by driving sub-operations (seven hundred and one hundred and one hundred and one hundred and eighty-seventh form). It takes a structure, checks its state, and drives sub-operations (ProcessAndProcessAndProcess523, ProcessAndProcessAndProcess940, sub_82B9BEC8) based on the result. This is a frequently-called helper in the 0x82242 process cluster, called from the main render loop and ProcessAndProcessAndProcess940. 139 lines. 3 callees. 11 callers.
[entrypoint.functions."0x82242628"]
name = "ProcessAndProcessAndProcess1087_82242628"

# Process a large data structure by freeing size-bucketed memory (seven hundred and one hundred and one hundred and one hundred and eighty-eighth form). It takes a structure, frees size-bucketed memory (Free_SizeBucketed), and drives sub-operations (sub_82B7FDB0, ProcessAndProcessAndProcess1089) based on the result. Part of the 0x82B8B process cluster. 339 lines. 3 callees. 3 callers.
[entrypoint.functions."0x82B8B690"]
name = "ProcessAndProcessAndProcess1088_82B8B690"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and eighty-ninth form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82B8B process cluster. 66 lines. 0 callees. 3 callers (sub_82B7D0F0, sub_82B8AA48, ProcessAndProcessAndProcess1088).
[entrypoint.functions."0x82B8B8E8"]
name = "ProcessAndProcessAndProcess1089_82B8B8E8"

# Process a structure by performing a refcounted copy assignment (seven hundred and one hundred and one hundred and one hundred and ninetieth form). It takes a structure, performs a refcounted copy assignment (CopyAssign_RefCounted2), and drives a sub-operation (ProcessAndProcessAndProcess1088) based on the result. Part of the 0x82B8A process cluster. 203 lines. 2 callees. 1 caller (sub_82B7EC80).
[entrypoint.functions."0x82B8A360"]
name = "ProcessAndProcessAndProcess1090_82B8A360"

# Process a large data structure by copying its data (seven hundred and one hundred and one hundred and one hundred and ninety-first form). It takes a structure, copies its data (MemCpy), and drives sub-operations (ProcessAndProcessAndProcess1100, sub_829FA5E8) based on the result. Part of the 0x82188 time cluster. 282 lines. 3 callees. 1 caller (sub_82189058).
[entrypoint.functions."0x82188D40"]
name = "ProcessAndProcessAndProcess1091_82188D40"

# Process a large data structure (seven hundred and one hundred and one hundred and one hundred and ninety-second form). It takes a structure, checks its state, and drives a sub-operation (sub_822A8EF8) based on the result. Part of the 0x82189 process cluster. 334 lines. 1 callee. 1 caller (sub_82189058).
[entrypoint.functions."0x821895B0"]
name = "ProcessAndProcessAndProcess1092_821895B0"

# Process a structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and one hundred and ninety-third form). It takes a structure and drives a sub-operation (ProcessAndProcessAndProcess962). Part of the 0x82261 process cluster. 97 lines. 1 callee. 2 callers (sub_8322D988 and ProcessAndProcessAndProcess1095).
[entrypoint.functions."0x82261668"]
name = "ProcessAndProcessAndProcess1093_82261668"

# Process a structure by driving sub-operations (seven hundred and one hundred and one hundred and one hundred and ninety-fourth form). It takes a structure, checks its state, and drives sub-operations (ProcessAndProcessAndProcess962, sub_829FDC88, sub_8322B0D0) based on the result. Part of the 0x82261 process cluster. 153 lines. 3 callees. 2 callers (sub_8322D988 and ProcessAndProcessAndProcess1095).
[entrypoint.functions."0x82261558"]
name = "ProcessAndProcessAndProcess1094_82261558"

# Process a structure by driving sub-operations (seven hundred and one hundred and one hundred and one hundred and ninety-fifth form). It takes a structure, checks its state, and drives sub-operations (ProcessAndProcessAndProcess1094, ProcessAndProcessAndProcess1093) based on the result. This is a frequently-called helper in the 0x8322D process cluster. 79 lines. 2 callees. 6 callers (including ProcessAndProcessAndProcess710).
[entrypoint.functions."0x8322DA38"]
name = "ProcessAndProcessAndProcess1095_8322DA38"

# Process a structure by driving sub-operations (seven hundred and one hundred and one hundred and one hundred and ninety-sixth form). It takes a structure, checks its state, and drives sub-operations (ProcessAndProcessAndProcess853, sub_832BECC0, ProcessAndProcessAndProcess1097) based on the result. Part of the 0x832BE process cluster. 128 lines. 3 callees. 2 callers (sub_832BDC90 and sub_832BECC0).
[entrypoint.functions."0x832BEBE8"]
name = "ProcessAndProcessAndProcess1096_832BEBE8"

# Process a large data structure (seven hundred and one hundred and one hundred and one hundred and ninety-seventh form). It takes a structure, checks its state, and drives sub-operations (ProcessAndProcessAndProcess1006, ProcessAndProcessAndProcess853, ProcessAndProcessAndProcess879, ProcessAndProcessAndProcess963) based on the result. Part of the 0x832BE process cluster. 316 lines. 4 callees. 2 callers (sub_832BDC90 and ProcessAndProcessAndProcess1096).
[entrypoint.functions."0x832BED90"]
name = "ProcessAndProcessAndProcess1097_832BED90"

# Manage a circular-list or event-queue node (seven hundred and one hundred and one hundred and one hundred and ninety-eighth form). It takes a structure, allocates and frees size-bucketed memory (Allocate_SizeBucketed, Free_SizeBucketed), constructs and inserts nodes (ConstructAndInsertNode, InsertElement4), destroys circular lists (DestroyCircularList), sets an event (SetEvent), and drives many sub-operations based on the result. This is a circular-list/event-queue node manager in the 0x82B44 cluster. 511 lines. 11 callees. 1 caller (sub_82B44D10).
[entrypoint.functions."0x82B44400"]
name = "ProcessAndProcessAndProcess1098_82B44400"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and ninety-ninth form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82B45 process cluster. 93 lines. 0 callees. 4 callers (sub_82B44D10, sub_82B45EE0, sub_82B46450, sub_82B46650).
[entrypoint.functions."0x82B45DF0"]
name = "ProcessAndProcessAndProcess1099_82B45DF0"

# Query the timebase (seven hundred and one hundred and one hundred and one hundred and one hundredth form). It takes a structure and queries the timebase (GetTimebase). This is a hot time-lookup wrapper in the 0x8221E time cluster. 43 lines. 1 callee. 31 callers.
[entrypoint.functions."0x8221EB78"]
name = "ProcessAndProcessAndProcess1100_8221EB78"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and first form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82B68 process cluster. 88 lines. 0 callees. 2 callers (sub_82B683E8 and sub_82B686E8).
[entrypoint.functions."0x82B68BB0"]
name = "ProcessAndProcessAndProcess1101_82B68BB0"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and second form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x822CD process cluster. 8 lines. 0 callees. 0 callers.
[entrypoint.functions."0x822CD888"]
name = "ProcessAndProcessAndProcess1102_822CD888"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and third form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x822CD process cluster. 81 lines. 0 callees. 0 callers.
[entrypoint.functions."0x822CD890"]
name = "ProcessAndProcessAndProcess1103_822CD890"

# Process a structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and one hundred and one hundred and fourth form). It takes a structure and drives a sub-operation (ProcessAndProcessAndProcess1101). This is a frequently-called helper in the 0x82B68 process cluster. 124 lines. 1 callee. 6 callers (sub_82B66C20, sub_82B66F68, sub_82B67360, sub_82B67418, sub_82B67628, sub_82B67700).
[entrypoint.functions."0x82B686E8"]
name = "ProcessAndProcessAndProcess1104_82B686E8"

# Hash a C-string and process it (seven hundred and one hundred and one hundred and one hundred and one hundred and fifth form). It takes a structure, hashes a C-string (HashCstring), and drives a sub-operation (ProcessAndProcessAndProcess17) based on the result. This is a string-hashing helper in the 0x8259D process cluster. 117 lines. 2 callees. 9 callers.
[entrypoint.functions."0x8259D3C8"]
name = "ProcessAndProcessAndProcess1105_8259D3C8"

# Process a structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and one hundred and one hundred and sixth form). It takes a structure and drives a sub-operation (ProcessAndProcessAndProcess975). Part of the 0x821B4 process cluster. 137 lines. 1 callee. 1 caller (sub_821CCB20).
[entrypoint.functions."0x821B4238"]
name = "ProcessAndProcessAndProcess1106_821B4238"

# Process a structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and one hundred and one hundred and seventh form). It takes a structure and drives a sub-operation (ProcessAndProcessAndProcess1017). Part of the 0x82B69 process cluster. 71 lines. 1 callee. 2 callers (sub_8236C520 and sub_82B69488).
[entrypoint.functions."0x82B69FA0"]
name = "ProcessAndProcessAndProcess1107_82B69FA0"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and eighth form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82C0C process cluster. 12 lines. 0 callees. 0 callers.
[entrypoint.functions."0x82C0CB88"]
name = "ProcessAndProcessAndProcess1108_82C0CB88"

# Process a structure by accessing thread-local storage (seven hundred and one hundred and one hundred and one hundred and one hundred and ninth form). It takes a structure and accesses thread-local storage (ProcessAndTlsAndProcess3). This is a frequently-called TLS helper in the 0x82CA3 process cluster. 40 lines. 1 callee. 19 callers.
[entrypoint.functions."0x82CA3B80"]
name = "ProcessAndProcessAndProcess1109_82CA3B80"

# Process a structure by computing a special-function value (seven hundred and one hundred and one hundred and one hundred and one hundred and tenth form). It takes a structure, computes a special-function value (ComputeSpecialFunction), and drives a sub-operation (ProcessAndProcessAndProcess504) based on the result. Part of the 0x82CDD process cluster. 219 lines. 2 callees. 2 callers (sub_82CDDB80 and sub_82CDE918).
[entrypoint.functions."0x82CDD758"]
name = "ProcessAndProcessAndProcess1110_82CDD758"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and eleventh form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x8237A process cluster. 127 lines. 0 callees. 2 callers (sub_823790B8 and sub_823FC178).
[entrypoint.functions."0x8237A818"]
name = "ProcessAndProcessAndProcess1111_8237A818"

# Process a structure by freeing size-bucketed memory (seven hundred and one hundred and one hundred and one hundred and one hundred and twelfth form). It takes a structure, frees size-bucketed memory (Free_SizeBucketed), and drives a sub-operation (ProcessAndProcessAndProcess1004) based on the result. Part of the 0x82A3D process cluster. 50 lines. 2 callees. 0 callers.
[entrypoint.functions."0x82A3D510"]
name = "ProcessAndProcessAndProcess1112_82A3D510"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and thirteenth form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82A3D process cluster. 8 lines. 0 callees. 0 callers.
[entrypoint.functions."0x82A3D560"]
name = "ProcessAndProcessAndProcess1113_82A3D560"

# Process a structure by allocating fresh memory (seven hundred and one hundred and one hundred and one hundred and one hundred and fourteenth form). It takes a structure and allocates fresh memory (Allocate_OrDbgBreak). Part of the 0x82A3D process cluster. 107 lines. 1 callee. 0 callers.
[entrypoint.functions."0x82A3D568"]
name = "ProcessAndProcessAndProcess1114_82A3D568"

# Process a structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and one hundred and one hundred and fifteenth form). It takes a structure and drives a sub-operation (ProcessAndProcessAndProcess1116). Part of the 0x82A3D process cluster. 13 lines. 1 callee. 0 callers.
[entrypoint.functions."0x82A3D630"]
name = "ProcessAndProcessAndProcess1115_82A3D630"

# Process a structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and one hundred and one hundred and sixteenth form). It takes a structure and drives a sub-operation (ProcessAndProcessAndProcess503). Part of the 0x82A3D process cluster. 158 lines. 1 callee. 1 caller (sub_82A3D630).
[entrypoint.functions."0x82A3D640"]
name = "ProcessAndProcessAndProcess1116_82A3D640"

# Process a large data structure (seven hundred and one hundred and one hundred and one hundred and one hundred and seventeenth form). It takes a structure, checks its state, and drives sub-operations (ProcessAndProcessAndProcess604, sub_82A50320) based on the result. Part of the 0x82A62 process cluster. 580 lines. 2 callees. 0 callers.
[entrypoint.functions."0x82A62990"]
name = "ProcessAndProcessAndProcess1117_82A62990"

# Process a structure by allocating and setting a string (seven hundred and one hundred and one hundred and one hundred and one hundred and eighteenth form). It takes a structure, allocates fresh memory (Allocate_OrDbgBreak), frees memory (ProcessAndFreeAndProcess9), sets a string (SetString), and drives sub-operations (sub_822F1F00, sub_826C3EF0, sub_826C3FA8) based on the result. Part of the 0x8237A process cluster. 164 lines. 6 callees. 2 callers (sub_823790B8 and sub_823FC178).
[entrypoint.functions."0x8237A710"]
name = "ProcessAndProcessAndProcess1118_8237A710"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and nineteenth form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82A3D process cluster. 72 lines. 0 callees. 4 callers (sub_823790B8, sub_823FC178, sub_82531DC0, sub_8254A3C0).
[entrypoint.functions."0x82A3D4A0"]
name = "ProcessAndProcessAndProcess1119_82A3D4A0"

# Free size-bucketed memory (seven hundred and one hundred and one hundred and one hundred and one hundred and twentieth form). It takes a structure and frees size-bucketed memory (Free_SizeBucketed). This is the single most-called function in the entire recompiled binary, invoked from across the codebase. 62 lines. 1 callee. 2320 callers.
[entrypoint.functions."0x821940C8"]
name = "ProcessAndProcessAndProcess1120_821940C8"

# Process a structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and one hundred and one hundred and twenty-first form). It takes a structure and drives a sub-operation (sub_82A734D0). This is a frequently-called helper in the 0x829FD process cluster. 83 lines. 1 callee. 36 callers.
[entrypoint.functions."0x829FD4B8"]
name = "ProcessAndProcessAndProcess1121_829FD4B8"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and twenty-second form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x8279D process cluster. 8 lines. 0 callees. 0 callers.
[entrypoint.functions."0x8279DEC0"]
name = "ProcessAndProcessAndProcess1122_8279DEC0"

# Process a very large data structure (seven hundred and one hundred and one hundred and one hundred and one hundred and twenty-third form). It takes a structure, allocates and frees size-bucketed memory (Allocate_OrDbgBreak, Free_SizeBucketed), constructs a 12-byte value (Construct12Byte), and drives sub-operations (sub_821CD618, sub_829176C8) based on the result. Part of the 0x821CD process cluster. 791 lines. 5 callees. 1 caller (sub_821CCB20).
[entrypoint.functions."0x821CD6B8"]
name = "ProcessAndProcessAndProcess1123_821CD6B8"

# Process a very frequently-used structure (seven hundred and one hundred and one hundred and one hundred and one hundred and twenty-fourth form). It takes a structure and checks its state, with no external sub-operations. This is the second-most-called helper in the recompiled binary. 121 lines. 0 callees. 145 callers.
[entrypoint.functions."0x822641F0"]
name = "ProcessAndProcessAndProcess1124_822641F0"

# Process a structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and one hundred and one hundred and twenty-fifth form). It takes a structure and drives a sub-operation (ProcessAndProcessAndProcess1126). Part of the 0x82A6E process cluster. 9 lines. 1 callee. 0 callers.
[entrypoint.functions."0x82A6E9B0"]
name = "ProcessAndProcessAndProcess1125_82A6E9B0"

# Process a large data structure by removing from a linked list (seven hundred and one hundred and one hundred and one hundred and one hundred and twenty-sixth form). It takes a structure, removes from a linked list (RemoveFromLinkedList), and drives many sub-operations (sub_82A46D90, sub_82A50C78, sub_82A6DDE0, sub_82A6E200, sub_82B5A938, sub_82B5A9D8, sub_82B67360, sub_82B67700) based on the result. Part of the 0x82A6E process cluster. 358 lines. 9 callees. 3 callers (including ProcessAndProcessAndProcess501).
[entrypoint.functions."0x82A6EBA0"]
name = "ProcessAndProcessAndProcess1126_82A6EBA0"

# Process a structure by entering a critical section (seven hundred and one hundred and one hundred and one hundred and one hundred and twenty-seventh form). It takes a structure, enters or tries a critical section (CriticalSection_EnterOrTry), and drives a sub-operation (ProcessAndProcessAndProcess1104) based on the result. Part of the 0x82B67 process cluster. 111 lines. 2 callees. 2 callers (sub_821F79A8 and ProcessAndProcessAndProcess1126).
[entrypoint.functions."0x82B67360"]
name = "ProcessAndProcessAndProcess1127_82B67360"

# Process a hash-table (seven hundred and one hundred and one hundred and one hundred and one hundred and twenty-eighth form). It takes a structure, validates it (ProcessAndValidateAndProcess3), processes a chain (ProcessChain2), processes a hash-table (ProcessHashTable), and drives a sub-operation (sub_823660D0) based on the result. This is a hash-table processor in the 0x82366 cluster. 298 lines. 4 callees. 3 callers (including ProcessAndProcessAndProcess1049).
[entrypoint.functions."0x82366FB0"]
name = "ProcessAndProcessAndProcess1128_82366FB0"

# Process a structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and one hundred and one hundred and twenty-ninth form). It takes a structure and drives a sub-operation (sub_82C4D4F0). Part of the 0x82C13 process cluster. 39 lines. 1 callee. 0 callers.
[entrypoint.functions."0x82C13148"]
name = "ProcessAndProcessAndProcess1129_82C13148"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and thirtieth form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82818 process cluster. 248 lines. 0 callees. 2 callers (ProcessAndProcessAndProcess814 and sub_82818398).
[entrypoint.functions."0x82818FA0"]
name = "ProcessAndProcessAndProcess1130_82818FA0"

# Process a structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and one hundred and one hundred and thirty-first form). It takes a structure and drives a sub-operation (ProcessAndProcessAndProcess1130). Part of the 0x82818 process cluster. 332 lines. 1 callee. 0 callers.
[entrypoint.functions."0x82818398"]
name = "ProcessAndProcessAndProcess1131_82818398"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and thirty-second form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82818 process cluster. 88 lines. 0 callees. 0 callers.
[entrypoint.functions."0x828185B0"]
name = "ProcessAndProcessAndProcess1132_828185B0"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and thirty-third form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82A00 process cluster. 79 lines. 0 callees. 5 callers (including ProcessAndProcessAndProcess861).
[entrypoint.functions."0x82A00988"]
name = "ProcessAndProcessAndProcess1133_82A00988"

# Process a structure by freeing memory and storing a descriptor (seven hundred and one hundred and one hundred and one hundred and one hundred and thirty-fourth form). It takes a structure, frees memory (Free_Thunk), stores a descriptor (StoreDescriptor_Thunk), and drives a sub-operation (sub_82C55338) based on the result. Part of the 0x82C0D process cluster. 239 lines. 3 callees. 1 caller (ProcessAndProcessAndProcess682).
[entrypoint.functions."0x82C0D9D0"]
name = "ProcessAndProcessAndProcess1134_82C0D9D0"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and thirty-fifth form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82C50 process cluster. 29 lines. 0 callees. 4 callers (sub_82C440E8, sub_82C442C0, sub_82C44468, sub_82C44538).
[entrypoint.functions."0x82C50A28"]
name = "ProcessAndProcessAndProcess1135_82C50A28"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and thirty-sixth form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82A6C process cluster. 65 lines. 0 callees. 4 callers (sub_8217FAB8, sub_8272E910, sub_82A49E50, sub_82A71150).
[entrypoint.functions."0x82A6CD68"]
name = "ProcessAndProcessAndProcess1136_82A6CD68"

# Process a structure by rendering a UI text-prompt element (seven hundred and one hundred and one hundred and one hundred and one hundred and thirty-seventh form). It takes a structure, renders a UI text-prompt element (ProcessAndProcessAndProcess1011), and drives sub-operations (ProcessAndProcessAndProcess1135, sub_82C50A90, sub_82CD3380) based on the result. Part of the 0x82C44 UI cluster. 115 lines. 4 callees. 1 caller (sub_82C457C0).
[entrypoint.functions."0x82C440E8"]
name = "ProcessAndProcessAndProcess1137_82C440E8"

# Process a large structure by rendering a UI text-prompt element (seven hundred and one hundred and one hundred and one hundred and one hundred and thirty-eighth form). It takes a structure, renders a UI text-prompt element (ProcessAndProcessAndProcess1011), and drives sub-operations (ProcessAndProcessAndProcess1135, sub_82CD3968, sub_82CD3B20) based on the result. Part of the 0x82C44 UI cluster. 242 lines. 4 callees. 1 caller (sub_82C457C0).
[entrypoint.functions."0x82C442C0"]
name = "ProcessAndProcessAndProcess1138_82C442C0"

# Dispatch a large UI text-prompt rendering operation (seven hundred and one hundred and one hundred and one hundred and one hundred and thirty-ninth form). It takes a structure, renders a UI text-prompt element (ProcessAndProcessAndProcess1011), and drives many sub-operations (ProcessAndProcessAndProcess1137, ProcessAndProcessAndProcess1138, ProcessAndProcessAndProcess1143, ProcessAndProcessAndProcess1144, ProcessAndProcessAndProcess1146, ProcessAndProcessAndProcess1147, ProcessAndProcessAndProcess1148, ProcessAndProcessAndProcess1149, sub_82C081A0, sub_82C08B00, sub_82C440E8, sub_82C442C0, sub_82C44468, sub_82C45350, sub_82C454B0, sub_82C45578, sub_82CBB610, sub_82CD42B8) based on the result. This is a large UI text-prompt dispatcher in the 0x82C45 cluster. 407 lines. 17 callees. 2 callers (sub_82C45A78 and sub_82C45C98).
[entrypoint.functions."0x82C457C0"]
name = "ProcessAndProcessAndProcess1139_82C457C0"

# Process a structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and one hundred and one hundred and fortieth form). It takes a structure and drives a sub-operation (ProcessAndProcessAndProcess1139). Part of the 0x82C45 process cluster. 15 lines. 1 callee. 0 callers.
[entrypoint.functions."0x82C45C98"]
name = "ProcessAndProcessAndProcess1140_82C45C98"

# Process a structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and one hundred and one hundred and forty-first form). It takes a structure and drives a sub-operation (ProcessAndProcessAndProcess626). Part of the 0x82C49 process cluster. 163 lines. 1 callee. 1 caller (ProcessAndProcessAndProcess610).
[entrypoint.functions."0x82C49088"]
name = "ProcessAndProcessAndProcess1141_82C49088"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and forty-second form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82C4D process cluster. 135 lines. 0 callees. 0 callers.
[entrypoint.functions."0x82C4DD20"]
name = "ProcessAndProcessAndProcess1142_82C4DD20"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and forty-third form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82CD3 UI rendering cluster. 57 lines. 0 callees. 2 callers (ProcessAndProcessAndProcess1139 and sub_82CE1588).
[entrypoint.functions."0x82CD3810"]
name = "ProcessAndProcessAndProcess1143_82CD3810"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and forty-fourth form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82CD3 UI rendering cluster. 53 lines. 0 callees. 3 callers (ProcessAndProcessAndProcess1139, sub_82CE1588, sub_82CE1E70).
[entrypoint.functions."0x82CD3878"]
name = "ProcessAndProcessAndProcess1144_82CD3878"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and forty-fifth form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82CD3 UI rendering cluster. 91 lines. 0 callees. 3 callers (ProcessAndProcessAndProcess1138, sub_82C44468, sub_82CE2618).
[entrypoint.functions."0x82CD3B20"]
name = "ProcessAndProcessAndProcess1145_82CD3B20"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and forty-sixth form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82CD3 UI rendering cluster. 37 lines. 0 callees. 3 callers (ProcessAndProcessAndProcess1139, sub_82CE09E0, sub_82CE1588).
[entrypoint.functions."0x82CD3D00"]
name = "ProcessAndProcessAndProcess1146_82CD3D00"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and forty-seventh form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82CD3 UI rendering cluster. 37 lines. 0 callees. 2 callers (ProcessAndProcessAndProcess1139 and sub_82CE09E0).
[entrypoint.functions."0x82CD3D40"]
name = "ProcessAndProcessAndProcess1147_82CD3D40"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and forty-eighth form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82CD4 UI rendering cluster. 80 lines. 0 callees. 5 callers (sub_82C44F50, ProcessAndProcessAndProcess1139, sub_82CE11E0, sub_82CE12E0, sub_82CE2618).
[entrypoint.functions."0x82CD4008"]
name = "ProcessAndProcessAndProcess1148_82CD4008"

# Process a structure by driving sub-operations (seven hundred and one hundred and one hundred and one hundred and one hundred and forty-ninth form). It takes a structure and drives sub-operations (sub_82CD3C50, ProcessAndProcessAndProcess1150) based on the result. Part of the 0x82CD4 UI rendering cluster. 117 lines. 2 callees. 5 callers (sub_82C44F50, ProcessAndProcessAndProcess1139, sub_82CE11E0, sub_82CE12E0, sub_82CE2618).
[entrypoint.functions."0x82CD40A0"]
name = "ProcessAndProcessAndProcess1149_82CD40A0"

# Process a large structure (seven hundred and one hundred and one hundred and one hundred and one hundred and fiftieth form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82CD4 UI rendering cluster. 188 lines. 0 callees. 2 callers (ProcessAndProcessAndProcess1149 and sub_82CE12E0).
[entrypoint.functions."0x82CD4168"]
name = "ProcessAndProcessAndProcess1150_82CD4168"

# Process a large structure (seven hundred and one hundred and one hundred and one hundred and one hundred and fifty-first form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82CD4 UI rendering cluster. 161 lines. 0 callees. 4 callers (sub_82C44F50, ProcessAndProcessAndProcess1139, sub_82CE12E0, sub_82CE2618).
[entrypoint.functions."0x82CD42B8"]
name = "ProcessAndProcessAndProcess1151_82CD42B8"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and fifty-second form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82C12 process cluster. 38 lines. 0 callees. 0 callers.
[entrypoint.functions."0x82C12ED8"]
name = "ProcessAndProcessAndProcess1152_82C12ED8"

# Process a large structure (seven hundred and one hundred and one hundred and one hundred and one hundred and fifty-third form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82CD3 UI rendering cluster. 234 lines. 0 callees. 3 callers (ProcessAndProcessAndProcess1138, sub_82C44468, sub_82CE2618).
[entrypoint.functions."0x82CD3968"]
name = "ProcessAndProcessAndProcess1153_82CD3968"

# Process a structure by incrementing a reference count (seven hundred and one hundred and one hundred and one hundred and one hundred and fifty-fourth form). It takes a structure and increments an atomic reference count (IncrementRefcountAtomic). Part of the 0x821A1 process cluster. 55 lines. 1 callee. 4 callers (sub_8227B3C0, ProcessAndProcessAndProcess1156, sub_82AA6728, sub_82B35238).
[entrypoint.functions."0x821A1520"]
name = "ProcessAndProcessAndProcess1154_821A1520"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and fifty-fifth form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82191 process cluster. 126 lines. 0 callees. 5 callers (sub_821C38C8, sub_8226E6E8, sub_822B4D70, sub_82A50E40, sub_82B2F860).
[entrypoint.functions."0x82191FD8"]
name = "ProcessAndProcessAndProcess1155_82191FD8"

# Process a structure by allocating and performing a refcounted copy assignment (seven hundred and one hundred and one hundred and one hundred and one hundred and fifty-sixth form). It takes a structure, allocates fresh memory (Allocate_OrDbgBreak), performs a refcounted copy assignment (CopyAssign_RefCounted), and drives a sub-operation (ProcessAndProcessAndProcess1154) based on the result. Part of the 0x8228B process cluster. 84 lines. 3 callees. 7 callers.
[entrypoint.functions."0x8228B620"]
name = "ProcessAndProcessAndProcess1156_8228B620"

# Process a structure by decrementing a reference count (seven hundred and one hundred and one hundred and one hundred and one hundred and fifty-seventh form). It takes a structure, decrements an atomic reference count (DecrementRefcountAtomic), and drives sub-operations (ProcessAndProcessAndProcess648, ProcessAndProcessAndProcess660) based on the result. Part of the 0x82294 process cluster. 130 lines. 3 callees. 12 callers (including ProcessAndProcessAndProcess674).
[entrypoint.functions."0x82294F38"]
name = "ProcessAndProcessAndProcess1157_82294F38"

# Process a structure by allocating fresh memory (seven hundred and one hundred and one hundred and one hundred and one hundred and fifty-eighth form). It takes a structure, allocates fresh memory (Allocate_OrDbgBreak), and drives sub-operations (sub_82A6D4C8, sub_82A6E200) based on the result. Part of the 0x822B6 process cluster. 168 lines. 3 callees. 8 callers.
[entrypoint.functions."0x822B6C70"]
name = "ProcessAndProcessAndProcess1158_822B6C70"

# Process a structure by freeing size-bucketed memory (seven hundred and one hundred and one hundred and one hundred and one hundred and fifty-ninth form). It takes a structure, frees size-bucketed memory (Free_SizeBucketed), and drives sub-operations (sub_821F6140, sub_82B38478, sub_82B9B9F0) based on the result. Part of the 0x82AAB process cluster. 134 lines. 4 callees. 2 callers (sub_82AABF18 and ProcessAndProcessAndProcess1160).
[entrypoint.functions."0x82AAB3B0"]
name = "ProcessAndProcessAndProcess1159_82AAB3B0"

# Process a structure by driving sub-operations (seven hundred and one hundred and one hundred and one hundred and one hundred and sixtieth form). It takes a structure and drives sub-operations (ProcessAndProcessAndProcess1159, sub_82AACC80) based on the result. Part of the 0x82AAC process cluster. 113 lines. 2 callees. 2 callers (sub_82240CF0 and sub_82A4F3E0).
[entrypoint.functions."0x82AAC048"]
name = "ProcessAndProcessAndProcess1160_82AAC048"

# Process a structure by driving sub-operations (seven hundred and one hundred and one hundred and one hundred and one hundred and sixty-first form). It takes a structure and drives sub-operations (ProcessAndProcessAndProcess1020, sub_821DE178, sub_822C50F0) based on the result. Part of the 0x821A4 process cluster. 71 lines. 3 callees. 0 callers.
[entrypoint.functions."0x821A4F60"]
name = "ProcessAndProcessAndProcess1161_821A4F60"

# Process a structure by driving sub-operations (seven hundred and one hundred and one hundred and one hundred and one hundred and sixty-second form). It takes a structure and drives sub-operations (sub_821C9100, sub_8227B050) based on the result. Part of the 0x821C8 process cluster. 44 lines. 2 callees. 0 callers.
[entrypoint.functions."0x821C8EB0"]
name = "ProcessAndProcessAndProcess1162_821C8EB0"

# Process a structure by driving sub-operations (seven hundred and one hundred and one hundred and one hundred and one hundred and sixty-third form). It takes a structure and drives sub-operations (sub_82B27418, sub_82B344D0) based on the result. Part of the 0x821C9 process cluster. 170 lines. 2 callees. 1 caller (sub_821C8EB0).
[entrypoint.functions."0x821C9100"]
name = "ProcessAndProcessAndProcess1163_821C9100"

# Process a large structure (seven hundred and one hundred and one hundred and one hundred and one hundred and sixty-fourth form). It takes a structure and drives a sub-operation (sub_82B28EB8) based on the result. Part of the 0x821DE process cluster. 237 lines. 1 callee. 1 caller (sub_821A4F60).
[entrypoint.functions."0x821DE178"]
name = "ProcessAndProcessAndProcess1164_821DE178"

# Process a large data structure (seven hundred and one hundred and one hundred and one hundred and one hundred and sixty-fifth form). It takes a structure, checks its state, and drives a sub-operation (ProcessAndProcessAndProcess575) based on the result. Part of the 0x821F2 process cluster. 524 lines. 1 callee. 3 callers (sub_821FA0D0, ProcessAndProcessAndProcess1171, sub_82A66180).
[entrypoint.functions."0x821F27E0"]
name = "ProcessAndProcessAndProcess1165_821F27E0"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and sixty-sixth form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82233 process cluster. 73 lines. 0 callees. 0 callers.
[entrypoint.functions."0x82233190"]
name = "ProcessAndProcessAndProcess1166_82233190"

# Process a refcounted object by constructing and destructing it (seven hundred and one hundred and one hundred and one hundred and one hundred and sixty-seventh form). It takes a structure, allocates fresh memory (Allocate_OrDbgBreak), constructs and destructs a refcounted object (ConstructRefCounted, DestructRefCounted), and drives sub-operations (ProcessAndProcessAndProcess551, ProcessAndProcessAndProcess885, ProcessAndProcessAndProcess955, ProcessAndProcessAndProcess956, ProcessAndProcessAndProcess1156) based on the result. Part of the 0x82277 process cluster. 196 lines. 8 callees. 2 callers (sub_82277240 and sub_8228A7A8).
[entrypoint.functions."0x822778B0"]
name = "ProcessAndProcessAndProcess1167_822778B0"

# Process a large data structure (seven hundred and one hundred and one hundred and one hundred and one hundred and sixty-eighth form). It takes a structure, checks its state, and drives sub-operations (ProcessAndProcessAndProcess510, ProcessAndProcessAndProcess549, ProcessAndProcessAndProcess551, ProcessAndProcessAndProcess552, ProcessAndProcessAndProcess864, ProcessAndProcessAndProcess907, ProcessAndProcessAndProcess1171, ProcessAndProcessAndProcess1172) based on the result. Part of the 0x82278 process cluster. 578 lines. 8 callees. 2 callers (sub_821CB3A0 and sub_8226E6E8).
[entrypoint.functions."0x82278DF8"]
name = "ProcessAndProcessAndProcess1168_82278DF8"

# Process a large data structure by inserting an element (seven hundred and one hundred and one hundred and one hundred and one hundred and sixty-ninth form). It takes a structure, frees size-bucketed memory (Free_SizeBucketed), inserts an element (InsertElement4), and drives sub-operations (ProcessAndProcessAndProcess1020, sub_822B9880, sub_823B0B10) based on the result. Part of the 0x8227B process cluster. 501 lines. 5 callees. 1 caller (sub_821C8EB0).
[entrypoint.functions."0x8227B050"]
name = "ProcessAndProcessAndProcess1169_8227B050"

# Process a large structure (seven hundred and one hundred and one hundred and one hundred and one hundred and seventieth form). It takes a structure and drives sub-operations (ProcessAndProcessAndProcess791, ProcessAndProcessAndProcess860, ProcessAndProcessAndProcess940) based on the result. Part of the 0x8227C process cluster. 282 lines. 3 callees. 2 callers (sub_822872D0 and sub_822AA060).
[entrypoint.functions."0x8227CDD0"]
name = "ProcessAndProcessAndProcess1170_8227CDD0"

# Process a structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and one hundred and one hundred and seventy-first form). It takes a structure and drives a sub-operation (ProcessAndProcessAndProcess1165). Part of the 0x8228A process cluster. 67 lines. 1 callee. 5 callers (sub_8226E130, ProcessAndProcessAndProcess1168, sub_82A3E7C0, sub_82B15E20, sub_82B264B8).
[entrypoint.functions."0x8228AD10"]
name = "ProcessAndProcessAndProcess1171_8228AD10"

# Process a structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and one hundred and one hundred and seventy-second form). It takes a structure and drives a sub-operation (ProcessAndProcessAndProcess919). Part of the 0x8228A process cluster. 106 lines. 1 callee. 4 callers (ProcessAndProcessAndProcess1168, sub_8228D838, sub_82A5D668, sub_82B264B8).
[entrypoint.functions."0x8228ADE8"]
name = "ProcessAndProcessAndProcess1172_8228ADE8"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and seventy-third form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x822B3 process cluster. 188 lines. 0 callees. 1 caller (ProcessAndProcessAndProcess806).
[entrypoint.functions."0x822B3C28"]
name = "ProcessAndProcessAndProcess1173_822B3C28"

# Process a structure by freeing size-bucketed memory (seven hundred and one hundred and one hundred and one hundred and one hundred and seventy-fourth form). It takes a structure, frees size-bucketed memory (Free_SizeBucketed), and drives a sub-operation (sub_82B17B58) based on the result. Part of the 0x82B17 process cluster. 50 lines. 2 callees. 0 callers.
[entrypoint.functions."0x82B17B08"]
name = "ProcessAndProcessAndProcess1174_82B17B08"

# Destruct a refcounted object and free it (seven hundred and one hundred and one hundred and one hundred and one hundred and seventy-fifth form). It takes a structure, decrements an atomic reference count (DecrementRefcountAtomic), destructs a refcounted object (DestructRefCounted), and frees size-bucketed memory (Free_SizeBucketed). This is a refcounted-object destructor in the 0x82B17 cluster. 68 lines. 3 callees. 8 callers.
[entrypoint.functions."0x82B17B58"]
name = "ProcessAndProcessAndProcess1175_82B17B58"

# Process a large structure (seven hundred and one hundred and one hundred and one hundred and one hundred and seventy-sixth form). It takes a structure and drives a sub-operation (sub_822A9E60) based on the result. Part of the 0x821BA process cluster. 181 lines. 1 callee. 2 callers (sub_821C36E0 and sub_822C4990).
[entrypoint.functions."0x821BA2D0"]
name = "ProcessAndProcessAndProcess1176_821BA2D0"

# Process a structure by growing a buffer and appending (seven hundred and one hundred and one hundred and one hundred and one hundred and seventy-seventh form). It takes a structure, grows a buffer and appends (GrowBufferAndAppend), and drives sub-operations (ProcessAndProcessAndProcess776, sub_82A734D0) based on the result. Part of the 0x821BD buffer-processing cluster. 242 lines. 3 callees. 1 caller (ProcessAndProcessAndProcess1180).
[entrypoint.functions."0x821BD2C8"]
name = "ProcessAndProcessAndProcess1177_821BD2C8"

# Process a large data structure (seven hundred and one hundred and one hundred and one hundred and one hundred and seventy-eighth form). It takes a structure and drives a sub-operation (ProcessAndProcessAndProcess1180) based on the result. Part of the 0x821D1 buffer-processing cluster. 481 lines. 1 callee. 2 callers (sub_821BA1D0 and sub_821C38C8).
[entrypoint.functions."0x821D1960"]
name = "ProcessAndProcessAndProcess1178_821D1960"

# Process a structure by dispatching (seven hundred and one hundred and one hundred and one hundred and one hundred and seventy-ninth form). It takes a structure and drives sub-operations (ProcessAndProcessAndProcess1040, sub_82AAF7C8) based on the result. Part of the 0x821D4 process cluster. 93 lines. 2 callees. 3 callers (sub_821BA1D0, sub_821C38C8, sub_82A4F520).
[entrypoint.functions."0x821D4E50"]
name = "ProcessAndProcessAndProcess1179_821D4E50"

# Process a structure by driving buffer-growth sub-operations (seven hundred and one hundred and one hundred and one hundred and one hundred and eightieth form). It takes a structure, performs a checked copy (CheckedCopy), and drives buffer-growth sub-operations (ProcessAndProcessAndProcess776, ProcessAndProcessAndProcess1177, ProcessAndProcessAndProcess1181, ProcessAndProcessAndProcess1183) based on the result. This is a buffer-growth dispatcher in the 0x821D5 cluster. 258 lines. 5 callees. 1 caller (ProcessAndProcessAndProcess1178).
[entrypoint.functions."0x821D52C0"]
name = "ProcessAndProcessAndProcess1180_821D52C0"

# Process a structure by growing a buffer and appending (seven hundred and one hundred and one hundred and one hundred and one hundred and eighty-first form). It takes a structure, grows a buffer and appends (GrowBufferAndAppend), and drives sub-operations (ProcessAndProcessAndProcess1121, ProcessAndProcessAndProcess776, sub_82A734D0) based on the result. Part of the 0x821DB buffer-processing cluster. 255 lines. 4 callees. 1 caller (ProcessAndProcessAndProcess1180).
[entrypoint.functions."0x821DB8F0"]
name = "ProcessAndProcessAndProcess1181_821DB8F0"

# Process a large data structure (seven hundred and one hundred and one hundred and one hundred and one hundred and eighty-second form). It takes a structure, checks its state, and drives sub-operations (ProcessAndProcessAndProcess566, ProcessAndProcessAndProcess856, sub_82A04BC0, sub_82AAB860, sub_82AACE40) based on the result. Part of the 0x821FF process cluster. 474 lines. 5 callees. 1 caller (sub_82240CF0).
[entrypoint.functions."0x821FF718"]
name = "ProcessAndProcessAndProcess1182_821FF718"

# Process a structure by growing a buffer and appending (seven hundred and one hundred and one hundred and one hundred and one hundred and eighty-third form). It takes a structure, grows a buffer and appends (GrowBufferAndAppend), and drives sub-operations (ProcessAndProcessAndProcess1121, ProcessAndProcessAndProcess776, sub_82A734D0) based on the result. Part of the 0x821FF buffer-processing cluster. 255 lines. 4 callees. 1 caller (ProcessAndProcessAndProcess1180).
[entrypoint.functions."0x821FFAD8"]
name = "ProcessAndProcessAndProcess1183_821FFAD8"

# Process a large data structure (seven hundred and one hundred and one hundred and one hundred and one hundred and eighty-fourth form). It takes a structure, checks its state, and drives sub-operations (ProcessAndAllocateAndProcess2, ProcessAndProcessAndProcess1074, ProcessAndProcessAndProcess1160, ProcessAndProcessAndProcess590, ProcessAndProcessAndProcess1182, sub_82AAB590, sub_82AAB770, sub_82AAC418, sub_82AAC500) based on the result. Part of the 0x82240 process cluster. 514 lines. 9 callees. 2 callers (sub_821BA1D0 and sub_821C38C8).
[entrypoint.functions."0x82240CF0"]
name = "ProcessAndProcessAndProcess1184_82240CF0"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and eighty-fifth form). It takes a structure, computes a wrapped value (ComputeWrapped), and drives a sub-operation (ProcessAndProcessAndProcess653) based on the result. Part of the 0x82275 process cluster. 120 lines. 2 callees. 2 callers (sub_822824B8 and ProcessAndProcessAndProcess1186).
[entrypoint.functions."0x822757D0"]
name = "ProcessAndProcessAndProcess1185_822757D0"

# Process a large data structure (seven hundred and one hundred and one hundred and one hundred and one hundred and eighty-sixth form). It takes a structure, checks its state, and drives sub-operations (ProcessAndProcessAndProcess904, sub_821B4D60, ProcessAndProcessAndProcess1185, sub_8228AE98, sub_8228B290, sub_8228DCE8) based on the result. Part of the 0x8228A process cluster. 577 lines. 6 callees. 3 callers (sub_821BA1D0, sub_821C38C8, sub_82A4F520).
[entrypoint.functions."0x8228AFD0"]
name = "ProcessAndProcessAndProcess1186_8228AFD0"

# Process a structure by freeing size-bucketed memory (seven hundred and one hundred and one hundred and one hundred and one hundred and eighty-seventh form). It takes a structure, frees size-bucketed memory (Free_SizeBucketed), and drives sub-operations (ProcessAndProcessAndProcess1075, ProcessAndProcessAndProcess1077, sub_82A6C5B0) based on the result. Part of the 0x8229B process cluster. 105 lines. 4 callees. 2 callers (sub_821BDE80 and sub_8229B700).
[entrypoint.functions."0x8229B650"]
name = "ProcessAndProcessAndProcess1187_8229B650"

# Process a structure by allocating fresh memory (seven hundred and one hundred and one hundred and one hundred and one hundred and eighty-eighth form). It takes a structure, allocates fresh memory (Allocate_OrDbgBreak), and drives a sub-operation (sub_82A96358) based on the result. Part of the 0x822A0 process cluster. 139 lines. 2 callees. 2 callers (sub_821BDE80 and sub_8229B700).
[entrypoint.functions."0x822A0CF8"]
name = "ProcessAndProcessAndProcess1188_822A0CF8"

# Process a large structure (seven hundred and one hundred and one hundred and one hundred and one hundred and eighty-ninth form). It takes a structure, checks its state, and drives sub-operations (ProcessAndProcessAndProcess843, sub_821799D0, sub_822C2628, sub_822C6288, sub_82A534C8) based on the result. Part of the 0x822A9 process cluster. 187 lines. 5 callees. 1 caller (ProcessAndProcessAndProcess1176).
[entrypoint.functions."0x822A9E60"]
name = "ProcessAndProcessAndProcess1189_822A9E60"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and ninetieth form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82A04 process cluster. 66 lines. 0 callees. 4 callers (ProcessAndProcessAndProcess1182, sub_82A043F0, sub_82AAB938, sub_82BE4720).
[entrypoint.functions."0x82A04BC0"]
name = "ProcessAndProcessAndProcess1190_82A04BC0"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and ninety-first form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82A6C process cluster. 65 lines. 0 callees. 2 callers (ProcessAndProcessAndProcess1187 and sub_82A7DA30).
[entrypoint.functions."0x82A6C5B0"]
name = "ProcessAndProcessAndProcess1191_82A6C5B0"

# Process a structure by driving sub-operations (seven hundred and one hundred and one hundred and one hundred and one hundred and ninety-second form). It takes a structure and drives sub-operations (ProcessAndProcessAndProcess549, ProcessAndProcessAndProcess884, ProcessAndProcessAndProcess1197) based on the result. Part of the 0x82197 process cluster. 72 lines. 3 callees. 0 callers.
[entrypoint.functions."0x82197AE8"]
name = "ProcessAndProcessAndProcess1192_82197AE8"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and ninety-third form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x8221F process cluster. 22 lines. 0 callees. 0 callers.
[entrypoint.functions."0x8221FC00"]
name = "ProcessAndProcessAndProcess1193_8221FC00"

# Process a structure by driving sub-operations (seven hundred and one hundred and one hundred and one hundred and one hundred and ninety-fourth form). It takes a structure and drives sub-operations (ProcessAndProcessAndProcess515, ProcessAndProcessAndProcess525, ProcessAndProcessAndProcess549, ProcessAndProcessAndProcess884) based on the result. Part of the 0x82283 process cluster. 95 lines. 4 callees. 0 callers.
[entrypoint.functions."0x822834A8"]
name = "ProcessAndProcessAndProcess1194_822834A8"

# Process a structure by driving sub-operations (seven hundred and one hundred and one hundred and one hundred and one hundred and ninety-fifth form). It takes a structure and drives sub-operations (ProcessAndProcessAndProcess1078, ProcessAndProcessAndProcess643, ProcessAndProcessAndProcess675, ProcessAndProcessAndProcess895) based on the result. Part of the 0x82287 process cluster. 74 lines. 4 callees. 2 callers (sub_82276700 and sub_8229F5A8).
[entrypoint.functions."0x822874A8"]
name = "ProcessAndProcessAndProcess1195_822874A8"

# Process a structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and one hundred and one hundred and ninety-sixth form). It takes a structure and drives a sub-operation (ProcessAndProcessAndProcess826). Part of the 0x82B17 refcounted-object cluster. 156 lines. 1 callee. 1 caller (sub_82B17F08).
[entrypoint.functions."0x82B17E18"]
name = "ProcessAndProcessAndProcess1196_82B17E18"

# Process a structure by driving sub-operations (seven hundred and one hundred and one hundred and one hundred and one hundred and ninety-seventh form). It takes a structure and drives sub-operations (ProcessAndProcessAndProcess1196, ProcessAndProcessAndProcess1198) based on the result. Part of the 0x82B17 refcounted-object cluster. 169 lines. 2 callees. 1 caller (ProcessAndProcessAndProcess1192).
[entrypoint.functions."0x82B17F08"]
name = "ProcessAndProcessAndProcess1197_82B17F08"

# Process a structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and one hundred and one hundred and ninety-eighth form). It takes a structure and drives a sub-operation (ProcessAndProcessAndProcess936). Part of the 0x82B7D process cluster. 115 lines. 1 callee. 7 callers (including ProcessAndProcessAndProcess837).
[entrypoint.functions."0x82B7DF88"]
name = "ProcessAndProcessAndProcess1198_82B7DF88"

# Process a structure by flushing the cache (seven hundred and one hundred and one hundred and one hundred and one hundred and ninety-ninth form). It takes a structure and flushes the cache (FlushCache). Part of the 0x82B9E process cluster. 146 lines. 1 callee. 1 caller (ProcessAndProcessAndProcess1079).
[entrypoint.functions."0x82B9EC20"]
name = "ProcessAndProcessAndProcess1199_82B9EC20"

# Process a structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and one hundred and one hundred and two hundredth form). It takes a structure and drives a sub-operation (sub_821EFC78) based on the result. Part of the 0x82AAB process cluster. 124 lines. 1 callee. 1 caller (ProcessAndProcessAndProcess1182).
[entrypoint.functions."0x82AAB860"]
name = "ProcessAndProcessAndProcess1200_82AAB860"

# Process a structure by driving sub-operations (seven hundred and one hundred and one hundred and one hundred and one hundred and two hundred and first form). It takes a structure and drives sub-operations (ProcessAndProcessAndProcess625, ProcessAndProcessAndProcess857) based on the result. Part of the 0x821B4 process cluster. 150 lines. 2 callees. 1 caller (ProcessAndProcessAndProcess1186).
[entrypoint.functions."0x821B4D60"]
name = "ProcessAndProcessAndProcess1201_821B4D60"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and two hundred and second form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x8222E process cluster. 136 lines. 0 callees. 4 callers (sub_8222E300, ProcessAndProcessAndProcess1203, sub_82B1AD00, sub_82B71388).
[entrypoint.functions."0x8222E208"]
name = "ProcessAndProcessAndProcess1202_8222E208"

# Process a large structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and one hundred and one hundred and two hundred and third form). It takes a structure and drives a sub-operation (ProcessAndProcessAndProcess1202) based on the result. Part of the 0x82233 process cluster. 237 lines. 1 callee. 2 callers (sub_822333B8 and ProcessAndProcessAndProcess1205).
[entrypoint.functions."0x82233428"]
name = "ProcessAndProcessAndProcess1203_82233428"

# Process a structure by computing a wrapped value (seven hundred and one hundred and one hundred and one hundred and one hundred and two hundred and fourth form). It takes a structure and computes a wrapped value (ComputeWrapped). Part of the 0x8228A process cluster. 195 lines. 1 callee. 1 caller (ProcessAndProcessAndProcess1186).
[entrypoint.functions."0x8228AE98"]
name = "ProcessAndProcessAndProcess1204_8228AE98"

# Process a large data structure (seven hundred and one hundred and one hundred and one hundred and one hundred and two hundred and fifth form). It takes a structure, checks its state, and drives sub-operations (ProcessAndProcessAndProcess1037, ProcessAndProcessAndProcess1048, ProcessAndProcessAndProcess987, ProcessAndProcessAndProcess1203, ProcessAndProcessAndProcess1207) based on the result. Part of the 0x8228B process cluster. 462 lines. 5 callees. 1 caller (ProcessAndProcessAndProcess1186).
[entrypoint.functions."0x8228B290"]
name = "ProcessAndProcessAndProcess1205_8228B290"

# Process a structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and one hundred and one hundred and two hundred and sixth form). It takes a structure and drives a sub-operation (ProcessAndProcessAndProcess930) based on the result. Part of the 0x8228D process cluster. 140 lines. 1 callee. 1 caller (ProcessAndProcessAndProcess1186).
[entrypoint.functions."0x8228DCE8"]
name = "ProcessAndProcessAndProcess1206_8228DCE8"

# Process a large structure (seven hundred and one hundred and one hundred and one hundred and one hundred and two hundred and seventh form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x822B7 process cluster. 275 lines. 0 callees. 1 caller (ProcessAndProcessAndProcess1205).
[entrypoint.functions."0x822B7AB8"]
name = "ProcessAndProcessAndProcess1207_822B7AB8"

# Process a large structure by entering a critical section (seven hundred and one hundred and one hundred and one hundred and one hundred and two hundred and eighth form). It takes a structure, enters or tries a critical section (CriticalSection_EnterOrTry), leaves a critical section (ProcessAndLeaveCritical), and drives a sub-operation (sub_82C4C068) based on the result. Part of the 0x82C13 process cluster. 304 lines. 3 callees. 2 callers (sub_82BFFB10 and sub_82C144A0).
[entrypoint.functions."0x82C13C40"]
name = "ProcessAndProcessAndProcess1208_82C13C40"

# Process a structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and one hundred and one hundred and two hundred and ninth form). It takes a structure and drives a sub-operation (ProcessAndProcessAndProcess964) based on the result. Part of the 0x82C4C process cluster. 33 lines. 1 callee. 1 caller (ProcessAndProcessAndProcess1208).
[entrypoint.functions."0x82C4C068"]
name = "ProcessAndProcessAndProcess1209_82C4C068"

# Process a large structure (seven hundred and one hundred and one hundred and one hundred and one hundred and two hundred and tenth form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82CD8 process cluster. 276 lines. 0 callees. 0 callers.
[entrypoint.functions."0x82CD8C58"]
name = "ProcessAndProcessAndProcess1210_82CD8C58"

# Process a structure by allocating fresh memory (seven hundred and one hundred and one hundred and one hundred and one hundred and two hundred and eleventh form). It takes a structure, allocates fresh memory (Allocate_OrDbgBreak), and drives a sub-operation (sub_822F1F00) based on the result. This is a frequently-called helper in the 0x82A77 process cluster. 70 lines. 2 callees. 11 callers (including ProcessAndProcessAndProcess1043).
[entrypoint.functions."0x82A77928"]
name = "ProcessAndProcessAndProcess1211_82A77928"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and two hundred and twelfth form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82A7C process cluster. 53 lines. 0 callees. 0 callers.
[entrypoint.functions."0x82A7C950"]
name = "ProcessAndProcessAndProcess1212_82A7C950"

# Process a structure by performing a checked copy (seven hundred and one hundred and one hundred and one hundred and one hundred and two hundred and thirteenth form). It takes a structure, performs a checked copy (CheckedCopy), and drives a sub-operation (sub_82A86778) based on the result. This is a frequently-called helper in the 0x822AC process cluster. 95 lines. 2 callees. 15 callers.
[entrypoint.functions."0x822ACE60"]
name = "ProcessAndProcessAndProcess1213_822ACE60"

# Process a structure by driving sub-operations (seven hundred and one hundred and one hundred and one hundred and one hundred and two hundred and fourteenth form). It takes a structure and drives sub-operations (ProcessAndProcessAndProcess783, ProcessAndProcessAndProcess1213) based on the result. Part of the 0x821C3 process cluster. 73 lines. 2 callees. 2 callers (sub_821C38C8 and sub_822E0508).
[entrypoint.functions."0x821C3670"]
name = "ProcessAndProcessAndProcess1214_821C3670"

# Process a large structure by entering a critical section (seven hundred and one hundred and one hundred and one hundred and one hundred and two hundred and fifteenth form). It takes a structure, enters or tries a critical section (CriticalSection_EnterOrTry), destructs an array (DestructArray), frees and shrinks (ProcessAndFreeAndShrink), and drives sub-operations (ProcessAndProcessAndProcess1216, sub_82B63C88, sub_82B647B0, sub_82B65418) based on the result. Part of the 0x8218F process cluster. 287 lines. 7 callees. 4 callers.
[entrypoint.functions."0x8218FAA0"]
name = "ProcessAndProcessAndProcess1215_8218FAA0"

# Destruct a refcounted object by releasing it (seven hundred and one hundred and one hundred and one hundred and one hundred and two hundred and sixteenth form). It takes a structure, frees size-bucketed memory (Free_SizeBucketed), frees and shrinks (ProcessAndFreeAndShrink), releases a refcounted object (Release_RefCounted), and drives sub-operations (sub_829FF3E8, sub_82B800A0) based on the result. Part of the 0x8217D process cluster. 352 lines. 5 callees. 8 callers.
[entrypoint.functions."0x8217DAD8"]
name = "ProcessAndProcessAndProcess1216_8217DAD8"

# Process a very frequently-used large structure (seven hundred and one hundred and one hundred and one hundred and one hundred and two hundred and seventeenth form). It takes a structure and checks its state, with no external sub-operations. This is a frequently-called helper in the 0x82B6B process cluster, invoked by the largest functions in the binary. 351 lines. 0 callees. 21 callers (including ProcessAndProcessAndProcess815).
[entrypoint.functions."0x82B6B7D0"]
name = "ProcessAndProcessAndProcess1217_82B6B7D0"

# Process a structure by entering a critical section (seven hundred and one hundred and one hundred and one hundred and one hundred and two hundred and eighteenth form). It takes a structure and enters or tries a critical section (CriticalSection_EnterOrTry). Part of the 0x821A5 process cluster. 87 lines. 1 callee. 6 callers (including ProcessAndProcessAndProcess503).
[entrypoint.functions."0x821A5A30"]
name = "ProcessAndProcessAndProcess1218_821A5A30"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and two hundred and nineteenth form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x821E5 process cluster. 33 lines. 0 callees. 2 callers (sub_82FE8E60 and sub_82FE9098).
[entrypoint.functions."0x821E52B8"]
name = "ProcessAndProcessAndProcess1219_821E52B8"

# Process a structure by driving sub-operations (seven hundred and one hundred and one hundred and one hundred and one hundred and two hundred and twentieth form). It takes a structure and drives sub-operations (ProcessAndProcessAndProcess503, sub_822B9620) based on the result. Part of the 0x822B7 process cluster. 81 lines. 2 callees. 0 callers.
[entrypoint.functions."0x822B7C80"]
name = "ProcessAndProcessAndProcess1220_822B7C80"

# Process a large structure (seven hundred and one hundred and one hundred and one hundred and one hundred and two hundred and twenty-first form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x822B9 process cluster. 179 lines. 0 callees. 2 callers (ProcessAndProcessAndProcess1220 and sub_823594D8).
[entrypoint.functions."0x822B9620"]
name = "ProcessAndProcessAndProcess1221_822B9620"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and two hundred and twenty-second form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82C4F process cluster. 24 lines. 0 callees. 0 callers.
[entrypoint.functions."0x82C4FF28"]
name = "ProcessAndProcessAndProcess1222_82C4FF28"

# Process a large structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and one hundred and one hundred and two hundred and twenty-third form). It takes a structure and drives a sub-operation (ProcessAndProcessAndProcess969) based on the result. Part of the 0x821A8 process cluster. 199 lines. 1 callee. 0 callers.
[entrypoint.functions."0x821A8E20"]
name = "ProcessAndProcessAndProcess1223_821A8E20"

# Process a structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and one hundred and one hundred and two hundred and twenty-fourth form). It takes a structure and drives a sub-operation (ProcessAndProcessAndProcess808). This is a frequently-called helper in the 0x821B0 process cluster. 147 lines. 1 callee. 9 callers.
[entrypoint.functions."0x821B0280"]
name = "ProcessAndProcessAndProcess1224_821B0280"

# Process a very frequently-used structure (seven hundred and one hundred and one hundred and one hundred and one hundred and two hundred and twenty-fifth form). It takes a structure and checks its state, with no external sub-operations. This is a frequently-called helper in the 0x8227E process cluster. 132 lines. 0 callees. 18 callers.
[entrypoint.functions."0x8227EE80"]
name = "ProcessAndProcessAndProcess1225_8227EE80"

# Process a structure by driving sub-operations (seven hundred and one hundred and one hundred and one hundred and one hundred and two hundred and twenty-sixth form). It takes a structure and drives sub-operations (ProcessAndProcessAndProcess640, sub_8229B5D0) based on the result. Part of the 0x82817 process cluster. 77 lines. 2 callees. 2 callers (sub_82817538 and sub_82817748).
[entrypoint.functions."0x82817BC8"]
name = "ProcessAndProcessAndProcess1226_82817BC8"

# Process a large structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and one hundred and one hundred and two hundred and twenty-seventh form). It takes a structure and drives a sub-operation (sub_82AB1D38) based on the result. Part of the 0x82AB1 process cluster. 180 lines. 1 callee. 1 caller (sub_821AE470).
[entrypoint.functions."0x82AB1B00"]
name = "ProcessAndProcessAndProcess1227_82AB1B00"

# Hash a string and set it (seven hundred and one hundred and one hundred and one hundred and one hundred and two hundred and twenty-eighth form). It takes a structure, computes an FNV-1 hash (FNV1Hash), frees size-bucketed memory (Free_SizeBucketed), and sets a string (SetString). This is a very frequently-called string-hash-and-set helper in the 0x821C1 process cluster. 76 lines. 3 callees. 46 callers.
[entrypoint.functions."0x821C1FC0"]
name = "ProcessAndProcessAndProcess1228_821C1FC0"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and two hundred and twenty-ninth form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82191 process cluster. 42 lines. 0 callees. 0 callers.
[entrypoint.functions."0x82191F60"]
name = "ProcessAndProcessAndProcess1229_82191F60"

# Process a large structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and one hundred and one hundred and two hundred and thirtieth form). It takes a structure and drives a sub-operation (sub_82AB1BF8) based on the result. Part of the 0x82AB1 process cluster. 180 lines. 1 callee. 1 caller (sub_82192640).
[entrypoint.functions."0x82AB1A08"]
name = "ProcessAndProcessAndProcess1230_82AB1A08"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and two hundred and thirty-first form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82C0D process cluster. 90 lines. 0 callees. 0 callers.
[entrypoint.functions."0x82C0D2F8"]
name = "ProcessAndProcessAndProcess1231_82C0D2F8"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and two hundred and thirty-second form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82172 process cluster. 15 lines. 0 callees. 0 callers.
[entrypoint.functions."0x82172390"]
name = "ProcessAndProcessAndProcess1232_82172390"

# Process a large structure by performing a linear search (seven hundred and one hundred and one hundred and one hundred and one hundred and two hundred and thirty-third form). It takes a structure, performs a linear search (LinearSearch), does a signed string compare (StrCmp_Signed), and drives a sub-operation (sub_822B33B0) based on the result. Part of the 0x821BD process cluster. 279 lines. 3 callees. 0 callers.
[entrypoint.functions."0x821BD468"]
name = "ProcessAndProcessAndProcess1233_821BD468"

# Process a large structure (seven hundred and one hundred and one hundred and one hundred and one hundred and two hundred and thirty-fourth form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82226 process cluster. 319 lines. 0 callees. 1 caller (sub_822CD988).
[entrypoint.functions."0x82226F00"]
name = "ProcessAndProcessAndProcess1234_82226F00"

# Process a structure by driving sub-operations (seven hundred and one hundred and one hundred and one hundred and one hundred and two hundred and thirty-fifth form). It takes a structure and drives sub-operations (ProcessAndProcessAndProcess1234, sub_823FC878) based on the result. Part of the 0x822CD process cluster. 87 lines. 2 callees. 0 callers.
[entrypoint.functions."0x822CD988"]
name = "ProcessAndProcessAndProcess1235_822CD988"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and two hundred and thirty-sixth form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x822D6 process cluster. 15 lines. 0 callees. 0 callers.
[entrypoint.functions."0x822D6648"]
name = "ProcessAndProcessAndProcess1236_822D6648"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and two hundred and thirty-seventh form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x822D6 process cluster. 17 lines. 0 callees. 0 callers.
[entrypoint.functions."0x822D6660"]
name = "ProcessAndProcessAndProcess1237_822D6660"

# Process a very frequently-used large data structure (seven hundred and one hundred and one hundred and one hundred and one hundred and two hundred and thirty-eighth form). It takes a structure, checks its state, and drives sub-operations (AllocateChecked, Free_SizeBucketed, sub_82A97648) based on the result. This is the third-most-called function in the recompiled binary. 441 lines. 3 callees. 302 callers.
[entrypoint.functions."0x823955A8"]
name = "ProcessAndProcessAndProcess1238_823955A8"

# Process a structure by decrementing and replacing a reference count (seven hundred and one hundred and one hundred and one hundred and one hundred and two hundred and thirty-ninth form). It takes a structure, decrements a reference count (ProcessAndDecrementRefCount), and replaces a refcounted object (ProcessAndReplaceRefCounted) based on the result. Part of the 0x8219C process cluster. 172 lines. 2 callees. 1 caller (sub_82A7C780).
[entrypoint.functions."0x8219C2D0"]
name = "ProcessAndProcessAndProcess1239_8219C2D0"

# Process a structure by incrementing a counter (seven hundred and one hundred and one hundred and one hundred and one hundred and two hundred and fortieth form). It takes a structure and increments a counter (ProcessAndIncrementCounter). Part of the 0x82287 counter cluster. 144 lines. 1 callee. 2 callers (sub_82261B30 and ProcessAndProcessAndProcess1244).
[entrypoint.functions."0x82287628"]
name = "ProcessAndProcessAndProcess1240_82287628"

# Process a structure by incrementing a counter (seven hundred and one hundred and one hundred and one hundred and one hundred and two hundred and forty-first form). It takes a structure and increments a counter (ProcessAndIncrementCounter). Part of the 0x82287 counter cluster. 154 lines. 1 callee. 2 callers (sub_82287248 and ProcessAndProcessAndProcess1245).
[entrypoint.functions."0x822877D0"]
name = "ProcessAndProcessAndProcess1241_822877D0"

# Process a structure by releasing a global reference (seven hundred and one hundred and one hundred and one hundred and one hundred and two hundred and forty-second form). It takes a structure, frees size-bucketed memory (Free_SizeBucketed), decrements and disposes (ProcessAndDecrementDispose), and releases a global reference (ReleaseGlobalRef). Part of the 0x826CA process cluster. 69 lines. 3 callees. 0 callers.
[entrypoint.functions."0x826CAD78"]
name = "ProcessAndProcessAndProcess1242_826CAD78"

# Destruct a refcounted object by dispatching (seven hundred and one hundred and one hundred and one hundred and one hundred and two hundred and forty-third form). It takes a structure, decrements and disposes (ProcessAndDecrementDispose), releases a global reference (ReleaseGlobalRef), releases a refcounted object (Release_RefCounted), dispatches by type code (SwitchDispatch), and drives sub-operations (ProcessAndProcessAndProcess565, ProcessAndProcessAndProcess1226, ProcessAndProcessAndProcess1244) based on the result. This is a refcounted-object destroy/dispatch routine in the 0x82817 cluster. 316 lines. 7 callees. 1 caller (ProcessAndProcessAndProcess815).
[entrypoint.functions."0x82817538"]
name = "ProcessAndProcessAndProcess1243_82817538"

# Process a large data structure (seven hundred and one hundred and one hundred and one hundred and one hundred and two hundred and forty-fourth form). It takes a structure and drives a sub-operation (ProcessAndProcessAndProcess1240) based on the result. Part of the 0x82A45 process cluster. 472 lines. 1 callee. 2 callers (sub_826CB338 and ProcessAndProcessAndProcess1243).
[entrypoint.functions."0x82A456F0"]
name = "ProcessAndProcessAndProcess1244_82A456F0"

# Process a structure by copying data and incrementing a counter (seven hundred and one hundred and one hundred and one hundred and one hundred and two hundred and forty-fifth form). It takes a structure, copies its data (MemCpy), and drives sub-operations (ProcessAndProcessAndProcess503, ProcessAndProcessAndProcess1241) based on the result. Part of the 0x82A45 process cluster. 115 lines. 3 callees. 0 callers.
[entrypoint.functions."0x82A45A48"]
name = "ProcessAndProcessAndProcess1245_82A45A48"

# Process a structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and one hundred and one hundred and two hundred and forty-sixth form). It takes a structure and drives a sub-operation (ProcessAndProcessAndProcess1239) based on the result. Part of the 0x82A7C process cluster. 78 lines. 1 callee. 0 callers.
[entrypoint.functions."0x82A7C780"]
name = "ProcessAndProcessAndProcess1246_82A7C780"

# Destruct a refcounted object (seven hundred and one hundred and one hundred and one hundred and one hundred and two hundred and forty-seventh form). It takes a structure, drives a sub-operation (ProcessAndProcessAndProcess1217), and releases a refcounted object (Release_RefCounted) based on the result. Part of the 0x82817 refcounted-object cluster. 271 lines. 2 callees. 1 caller (ProcessAndProcessAndProcess815).
[entrypoint.functions."0x82817370"]
name = "ProcessAndProcessAndProcess1247_82817370"

# Process a structure by performing a checked copy (seven hundred and one hundred and one hundred and one hundred and one hundred and two hundred and forty-eighth form). It takes a structure, performs a checked copy (CheckedCopy), and drives a sub-operation (ProcessAndAllocateAndProcess8) based on the result. This is a frequently-called helper in the 0x82A7F process cluster. 114 lines. 2 callees. 11 callers.
[entrypoint.functions."0x82A7FA88"]
name = "ProcessAndProcessAndProcess1248_82A7FA88"

# Process a structure by driving sub-operations (seven hundred and one hundred and one hundred and one hundred and one hundred and two hundred and forty-ninth form). It takes a structure and drives sub-operations (ProcessAndProcessAndProcess1105, sub_823FE8F8) based on the result. Part of the 0x8259D string-hashing cluster. 83 lines. 2 callees. 1 caller (sub_826CBB58).
[entrypoint.functions."0x8259D340"]
name = "ProcessAndProcessAndProcess1249_8259D340"

# Process a large data structure (seven hundred and one hundred and one hundred and one hundred and one hundred and two hundred and fiftieth form). It takes a structure, gets a global field (GetGlobalField), and drives sub-operations (ProcessAndProcessAndProcess1087, ProcessAndProcessAndProcess940) based on the result. This is a frequently-called helper in the 0x821F6 process cluster. 445 lines. 3 callees. 18 callers.
[entrypoint.functions."0x821F61D0"]
name = "ProcessAndProcessAndProcess1250_821F61D0"

# Process a structure by flushing the cache (seven hundred and one hundred and one hundred and one hundred and one hundred and two hundred and fifty-first form). It takes a structure and flushes the cache (FlushCache). This is a very frequently-called helper in the 0x822C2 process cluster. 174 lines. 1 callee. 52 callers (including ProcessAndProcessAndProcess1189).
[entrypoint.functions."0x822C2628"]
name = "ProcessAndProcessAndProcess1251_822C2628"

# Process a large data structure (seven hundred and one hundred and one hundred and one hundred and one hundred and two hundred and fifty-second form). It takes a structure and drives a sub-operation (ProcessAndProcessAndProcess510) based on the result. Part of the 0x82A3C process cluster. 463 lines. 1 callee. 1 caller (sub_82A3BC58).
[entrypoint.functions."0x82A3CEF0"]
name = "ProcessAndProcessAndProcess1252_82A3CEF0"

# Process an enormous data structure by dispatching (seven hundred and one hundred and one hundred and one hundred and one hundred and two hundred and fifty-third form). It takes a structure, checks its state, and drives many sub-operations (ProcessAndProcessAndProcess509, ProcessAndProcessAndProcess535, ProcessAndProcessAndProcess536, ProcessAndProcessAndProcess537, ProcessAndProcessAndProcess538, ProcessAndProcessAndProcess539, ProcessAndProcessAndProcess542, ProcessAndProcessAndProcess583, ProcessAndProcessAndProcess584, ProcessAndProcessAndProcess654, sub_832C23A8) based on the result. This is the third-largest function in the recompiled binary, a massive dispatcher in the 0x832BB/0x832C cluster. 4766 lines. 11 callees. 1 caller (sub_832BD3E8).
[entrypoint.functions."0x832BB018"]
name = "ProcessAndProcessAndProcess1253_832BB018"

# Process a structure by reading a field (seven hundred and one hundred and one hundred and one hundred and one hundred and two hundred and fifty-fourth form). It takes a structure and reads a field (GetField8). Part of the 0x82C50 UI cluster. 56 lines. 1 callee. 3 callers (sub_82C45258, sub_82C454B0, sub_82C45578).
[entrypoint.functions."0x82C50FE8"]
name = "ProcessAndProcessAndProcess1254_82C50FE8"

# Process a large data structure (seven hundred and one hundred and one hundred and one hundred and one hundred and two hundred and fifty-fifth form). It takes a structure, checks its state, and drives sub-operations (ProcessAndProcessAndProcess551, ProcessAndProcessAndProcess555, ProcessAndProcessAndProcess919, ProcessAndProcessAndProcess940) based on the result. Part of the 0x821E2 process cluster. 532 lines. 4 callees. 10 callers.
[entrypoint.functions."0x821E27C8"]
name = "ProcessAndProcessAndProcess1255_821E27C8"

# Process a structure by driving sub-operations (seven hundred and one hundred and one hundred and one hundred and one hundred and two hundred and fifty-sixth form). It takes a structure and drives sub-operations (ProcessAndProcessAndProcess764, sub_82B9B970) based on the result. Part of the render-loop process path in the 0x82BA8 cluster. 136 lines. 2 callees. 2 callers (including MainRenderLoop).
[entrypoint.functions."0x82BA8630"]
name = "ProcessAndProcessAndProcess1256_82BA8630"

# Process a large structure by driving sub-operations (seven hundred and one hundred and one hundred and one hundred and one hundred and two hundred and fifty-seventh form). It takes a structure and drives sub-operations (ProcessAndProcessAndProcess1087, ProcessAndProcessAndProcess528, sub_82B9B770) based on the result. Part of the render-loop process path in the 0x821D1 cluster. 244 lines. 3 callees. 3 callers (including MainRenderLoop).
[entrypoint.functions."0x821D17B8"]
name = "ProcessAndProcessAndProcess1257_821D17B8"

# Process a large data structure (seven hundred and one hundred and one hundred and one hundred and one hundred and two hundred and fifty-eighth form). It takes a structure, checks its state, and drives sub-operations (ProcessAndProcessAndProcess1058, ProcessAndProcessAndProcess791, ProcessAndProcessAndProcess940, ProcessAndProcessAndProcess952) based on the result. Part of the render-loop process path in the 0x82B9C cluster. 293 lines. 4 callees. 6 callers (including MainRenderLoop).
[entrypoint.functions."0x82B9C7F8"]
name = "ProcessAndProcessAndProcess1258_82B9C7F8"

# Process a structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and one hundred and one hundred and two hundred and fifty-ninth form). It takes a structure and drives a sub-operation (ProcessAndProcessAndProcess940) based on the result. Part of the render-loop process path in the 0x82B9C cluster. 104 lines. 1 callee. 4 callers (including MainRenderLoop).
[entrypoint.functions."0x82B9C9D8"]
name = "ProcessAndProcessAndProcess1259_82B9C9D8"

# Process a structure by driving sub-operations (seven hundred and one hundred and one hundred and one hundred and one hundred and two hundred and sixtieth form). It takes a structure and drives sub-operations (sub_82BA21F8, sub_82BA2568, sub_82BA29B8) based on the result. Part of the render-loop process path in the 0x82BA3 cluster. 138 lines. 3 callees. 1 caller (MainRenderLoop).
[entrypoint.functions."0x82BA32C8"]
name = "ProcessAndProcessAndProcess1260_82BA32C8"

# Process a structure by clearing memory and driving sub-operations (seven hundred and one hundred and one hundred and one hundred and one hundred and two hundred and sixty-first form). It takes a structure, clears its memory (MemSet), and drives sub-operations (ProcessAndProcessAndProcess940, ProcessAndProcessAndProcess952, sub_82BA6820) based on the result. Part of the render-loop process path in the 0x82BA6 cluster. 174 lines. 4 callees. 1 caller (MainRenderLoop).
[entrypoint.functions."0x82BA6F08"]
name = "ProcessAndProcessAndProcess1261_82BA6F08"

# Process a structure by clearing memory (seven hundred and one hundred and one hundred and one hundred and one hundred and two hundred and sixty-second form). It takes a structure, clears its memory (MemSet), and drives sub-operations (sub_82BA7298, ProcessAndProcessAndProcess1264) based on the result. Part of the render-loop process path in the 0x82BA7 cluster. 200 lines. 3 callees. 1 caller (ProcessAndProcessAndProcess1263).
[entrypoint.functions."0x82BA7BE0"]
name = "ProcessAndProcessAndProcess1262_82BA7BE0"

# Process a large data structure by clearing memory (seven hundred and one hundred and one hundred and one hundred and one hundred and two hundred and sixty-third form). It takes a structure, clears its memory (MemSet), and drives sub-operations (sub_82B99578, sub_82B99C10, sub_82B9B970, ProcessAndProcessAndProcess1262, sub_82BA8248) based on the result. Part of the render-loop process path in the 0x82BA8 cluster. 410 lines. 6 callees. 1 caller (MainRenderLoop).
[entrypoint.functions."0x82BA8350"]
name = "ProcessAndProcessAndProcess1263_82BA8350"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and two hundred and sixty-fourth form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x83004 process cluster. 30 lines. 0 callees. 1 caller (ProcessAndProcessAndProcess1262).
[entrypoint.functions."0x83004BB0"]
name = "ProcessAndProcessAndProcess1264_83004BB0"

# Orchestrate a render-loop frame (seven hundred and one hundred and one hundred and one hundred and one hundred and two hundred and sixty-fifth form). It takes a structure, enters a critical section (CriticalSection_EnterOrTry), queries the timebase (GetTimebase), increments an atomic reference count (IncrementRefcountAtomic), calls the main render loop (MainRenderLoop), checks a yield threshold (YieldAndCheckThreshold), and drives many sub-operations (ProcessAndProcessAndProcess1017, ProcessAndProcessAndProcess1107, ProcessAndProcessAndProcess792, ProcessAndProcessAndProcess805, ProcessAndProcessAndProcess806, ProcessAndProcessAndProcess905, ProcessAndProcessAndProcess907, ProcessAndProcessAndProcess940, ProcessAndProcessAndProcess952, ProcessAndProcessAndProcess1258) based on the result. This is the main render-loop orchestrator in the 0x82B69 cluster. 426 lines. 15 callees. 3 callers (sub_8236C520, sub_8236D0A8, sub_82A393A8).
[entrypoint.functions."0x82B69488"]
name = "ProcessAndProcessAndProcess1265_82B69488"

# Process a large structure (seven hundred and one hundred and one hundred and one hundred and one hundred and two hundred and sixty-sixth form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82B9C process cluster. 238 lines. 0 callees. 0 callers.
[entrypoint.functions."0x82B9C648"]
name = "ProcessAndProcessAndProcess1266_82B9C648"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and two hundred and sixty-seventh form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82B89 process cluster. 89 lines. 0 callees. 0 callers.
[entrypoint.functions."0x82B89A78"]
name = "ProcessAndProcessAndProcess1267_82B89A78"

# Process a structure by driving sub-operations (seven hundred and one hundred and one hundred and one hundred and one hundred and two hundred and sixty-eighth form). It takes a structure and drives sub-operations (ProcessAndProcessAndProcess940, sub_8228EAF0) based on the result. Part of the 0x822B3 process cluster. 236 lines. 2 callees. 6 callers (including ProcessAndProcessAndProcess841).
[entrypoint.functions."0x822B3E40"]
name = "ProcessAndProcessAndProcess1268_822B3E40"

# Process a structure by driving sub-operations (seven hundred and one hundred and one hundred and one hundred and one hundred and two hundred and sixty-ninth form). It takes a structure, makes a virtual call (VirtualCall4), reads a field (ProcessAndProcessAndProcess1254), and drives sub-operations (sub_82C08B00, sub_82C08C70, sub_82C452D0, sub_82C50AE8, sub_82C50F28, sub_82CBB610) based on the result. Part of the 0x82C45 UI cluster. 247 lines. 8 callees. 1 caller (ProcessAndProcessAndProcess1139).
[entrypoint.functions."0x82C45578"]
name = "ProcessAndProcessAndProcess1269_82C45578"

# Grow an allocated buffer (seven hundred and one hundred and one hundred and one hundred and one hundred and two hundred and seventieth form). It takes a structure and grows an allocated buffer (ProcessGrowAllocate). This is the second-most-called function in the recompiled binary. 117 lines. 1 callee. 306 callers.
[entrypoint.functions."0x82B13410"]
name = "ProcessAndProcessAndProcess1270_82B13410"

# Process a structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and one hundred and one hundred and two hundred and seventy-first form). It takes a structure and drives a sub-operation (ProcessAndProcessAndProcess1273) based on the result. Part of the 0x821C6 process cluster. 11 lines. 1 callee. 6 callers (including ProcessAndProcessAndProcess1272).
[entrypoint.functions."0x821C68C0"]
name = "ProcessAndProcessAndProcess1271_821C68C0"

# Process a large data structure by reassigning a buffer (seven hundred and one hundred and one hundred and one hundred and one hundred and two hundred and seventy-second form). It takes a structure, allocates fresh memory (Allocate_OrDbgBreak), drives a sub-operation (ProcessAndProcessAndProcess878), reassigns a buffer (ReassignBuffer), releases a refcounted object (Release_RefCounted), and drives a sub-operation (ProcessAndProcessAndProcess1271) based on the result. This is a very frequently-called refcounted-buffer reassign routine in the 0x821E3 process cluster. 168 lines. 5 callees. 76 callers (including ProcessAndProcessAndProcess673).
[entrypoint.functions."0x821E3A10"]
name = "ProcessAndProcessAndProcess1272_821E3A10"

# Process a structure by building a buffer (seven hundred and one hundred and one hundred and one hundred and one hundred and two hundred and seventy-third form). It takes a structure, builds a buffer (BuildBuffer), and drives sub-operations (sub_82CAB630, sub_82CAB770, sub_82CAB7E0) based on the result. Part of the 0x82286 process cluster. 110 lines. 4 callees. 1 caller (ProcessAndProcessAndProcess1271).
[entrypoint.functions."0x82286918"]
name = "ProcessAndProcessAndProcess1273_82286918"

# Process a structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and one hundred and one hundred and two hundred and seventy-fourth form). It takes a structure and drives a sub-operation (ProcessAndProcessAndProcess1272) based on the result. This is a very frequently-called helper in the 0x82331 process cluster. 74 lines. 1 callee. 96 callers.
[entrypoint.functions."0x82331308"]
name = "ProcessAndProcessAndProcess1274_82331308"

# Process a large structure (seven hundred and one hundred and one hundred and one hundred and one hundred and two hundred and seventy-fifth form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x8236F process cluster. 168 lines. 0 callees. 3 callers (sub_8236D600, sub_8236D798, sub_8236EF80).
[entrypoint.functions."0x8236F270"]
name = "ProcessAndProcessAndProcess1275_8236F270"

# Process a large data structure by constructing a refcounted object (seven hundred and one hundred and one hundred and one hundred and one hundred and two hundred and seventy-sixth form). It takes a structure, constructs a refcounted object (ConstructRefCounted), performs a refcounted copy assignment (CopyAssign_RefCounted2), destructs a refcounted object (DestructRefCounted), increments a global counter (IncrementGlobalCounter2), copies a structure region (ProcessAndCopyStructRegion), and drives sub-operations (ProcessAndProcessAndProcess951, ProcessAndProcessAndProcess1274) based on the result. This is a refcounted-object processor in the 0x82B8A cluster. 429 lines. 7 callees. 1 caller (sub_82B7C858).
[entrypoint.functions."0x82B8A758"]
name = "ProcessAndProcessAndProcess1276_82B8A758"

# Process a structure by entering a critical section (seven hundred and one hundred and one hundred and one hundred and one hundred and two hundred and seventy-seventh form). It takes a structure, enters or tries a critical section (CriticalSection_EnterOrTry), leaves a critical section (ProcessAndLeaveCritical), drives a sub-operation (ProcessAndProcessAndProcess1049), and drives a sub-operation (sub_82C08778) based on the result. Part of the 0x82C08 process cluster. 132 lines. 4 callees. 1 caller (ProcessAndProcessAndProcess1278).
[entrypoint.functions."0x82C08B90"]
name = "ProcessAndProcessAndProcess1277_82C08B90"

# Process a structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and one hundred and one hundred and two hundred and seventy-eighth form). It takes a structure and drives a sub-operation (ProcessAndProcessAndProcess1277) based on the result. Part of the 0x82C08 process cluster. 23 lines. 1 callee. 2 callers (sub_82C454B0 and ProcessAndProcessAndProcess1269).
[entrypoint.functions."0x82C08C70"]
name = "ProcessAndProcessAndProcess1278_82C08C70"

# Process a structure by computing and copying (seven hundred and one hundred and one hundred and one hundred and one hundred and two hundred and seventy-ninth form). It takes a structure, computes and copies (ProcessAndComputeAndCopy), and drives a sub-operation (sub_822F6928) based on the result. Part of the 0x82A83 process cluster. 99 lines. 2 callees. 5 callers (sub_822363A8, sub_82A83338, sub_82A83420, sub_82AB2EF8, sub_82B27418).
[entrypoint.functions."0x82A83138"]
name = "ProcessAndProcessAndProcess1279_82A83138"

# Process a structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and one hundred and one hundred and two hundred and eightieth form). It takes a structure and drives a sub-operation (sub_829FA2E0) based on the result. Part of the 0x82172 process cluster. 13 lines. 1 callee. 0 callers.
[entrypoint.functions."0x82172ED0"]
name = "ProcessAndProcessAndProcess1280_82172ED0"

# Process a large data structure by clearing memory (seven hundred and one hundred and one hundred and one hundred and one hundred and two hundred and eighty-first form). It takes a structure, clears its memory (MemSet), checks for success (ProcessAndCheckSuccess), and drives sub-operations (sub_82172EE8, sub_822EF9B8, sub_8230F690, sub_82334B98, sub_82334DA8, sub_82334E80, sub_82334F88, sub_82CBC490) based on the result. Part of the 0x82177 process cluster. 233 lines. 10 callees. 1 caller (sub_82184D38).
[entrypoint.functions."0x82177590"]
name = "ProcessAndProcessAndProcess1281_82177590"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and two hundred and eighty-second form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82179 process cluster. 112 lines. 0 callees. 4 callers (sub_822B36F8, sub_82BE3B78, sub_82BE3D50, sub_82BE3E80).
[entrypoint.functions."0x82179FB8"]
name = "ProcessAndProcessAndProcess1282_82179FB8"

# Process a large structure by freeing size-bucketed memory (seven hundred and one hundred and one hundred and one hundred and one hundred and two hundred and eighty-third form). It takes a structure, frees size-bucketed memory (Free_SizeBucketed), gets the current time (GetCurrentTimeMs), reads a field (GetField4), and drives sub-operations (NoOp_Stub, sub_822D5640, sub_82348CA8, sub_824D4230, sub_824E3C28, sub_82C816E0, sub_82C816E8) based on the result. This is a frequently-called helper in the 0x82182 process cluster. 180 lines. 10 callees. 12 callers.
[entrypoint.functions."0x821827F0"]
name = "ProcessAndProcessAndProcess1283_821827F0"

# Process a large data structure (seven hundred and one hundred and one hundred and one hundred and one hundred and two hundred and eighty-fourth form). It takes a structure, drives a sub-operation (ProcessAndProcessAndProcess479), and drives sub-operations (sub_82CBC558, sub_82CBC5F8, sub_82CBC630, sub_82CBCFC0, sub_82CBD008) based on the result. Part of the 0x82184 process cluster. 425 lines. 6 callees. 1 caller (ProcessAndProcessAndProcess1286).
[entrypoint.functions."0x82184968"]
name = "ProcessAndProcessAndProcess1284_82184968"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and two hundred and eighty-fifth form). It takes a structure, gets the current time (GetCurrentTimeMs), and drives sub-operations (ProcessAndProcessAndProcess1124, sub_822EAC40) based on the result. Part of the 0x82184 process cluster. 120 lines. 3 callees. 1 caller (ProcessAndProcessAndProcess1286).
[entrypoint.functions."0x82184C68"]
name = "ProcessAndProcessAndProcess1285_82184C68"

# Process a large data structure by dispatching (seven hundred and one hundred and one hundred and one hundred and one hundred and two hundred and eighty-sixth form). It takes a structure, gets the current time (GetCurrentTimeMs), gets a flag (GetFlag), aborts on failure (ProcessAndAbort), and drives sub-operations (ProcessAndProcessAndProcess1281, ProcessAndProcessAndProcess1284, ProcessAndProcessAndProcess1285, sub_822CAD30, sub_822D3FA0, sub_822EACA8, sub_822EAE30, sub_822EAFC0, sub_822EB478, sub_822EB9C8, sub_822EC348, sub_823040E0, sub_82334AA8, sub_82CBC148, sub_82CBC160, sub_82CBC440, sub_82CF9AF8, sub_82CF9B58) based on the result. Part of the 0x82184 process cluster. 479 lines. 21 callees. 4 callers.
[entrypoint.functions."0x82184D38"]
name = "ProcessAndProcessAndProcess1286_82184D38"

# Process a large structure by copying data (seven hundred and one hundred and one hundred and one hundred and one hundred and two hundred and eighty-seventh form). It takes a structure, copies its data (MemCpy), and drives sub-operations (sub_829FA5E8, sub_82A666A8, sub_82BE3F30) based on the result. Part of the 0x82185 process cluster. 317 lines. 4 callees. 1 caller (ProcessAndProcessAndProcess1294).
[entrypoint.functions."0x82185C90"]
name = "ProcessAndProcessAndProcess1287_82185C90"

# Process a very large data structure by copying data (seven hundred and one hundred and one hundred and one hundred and one hundred and two hundred and eighty-eighth form). It takes a structure, copies its data (MemCpy), and drives sub-operations (ProcessAndProcessAndProcess1091, ProcessAndProcessAndProcess1092, sub_82188F20, sub_829FA5E8) based on the result. Part of the 0x82189 process cluster. 780 lines. 5 callees. 1 caller (ProcessAndProcessAndProcess1294).
[entrypoint.functions."0x82189058"]
name = "ProcessAndProcessAndProcess1288_82189058"

# Process a large data structure by freeing size-bucketed memory (seven hundred and one hundred and one hundred and one hundred and one hundred and two hundred and eighty-ninth form). It takes a structure, frees size-bucketed memory (Free_SizeBucketed), and drives a sub-operation (sub_82A1E2B8) based on the result. Part of the 0x8218C process cluster. 417 lines. 2 callees. 2 callers (sub_821B9320 and ProcessAndProcessAndProcess1293).
[entrypoint.functions."0x8218C500"]
name = "ProcessAndProcessAndProcess1289_8218C500"

# Process an enormous data structure by dispatching (seven hundred and one hundred and one hundred and one hundred and one hundred and two hundred and ninetieth form). It takes a structure, allocates fresh memory (Allocate_OrDbgBreak), gets the current time (GetCurrentTimeMs), and drives sub-operations (ProcessAndProcessAndProcess1124, ProcessAndProcessAndProcess938, ProcessAndProcessAndProcess1283, ProcessAndProcessAndProcess1286, sub_82172EE8, sub_822C3F48, sub_822C5390, sub_822CFDE0, sub_822D1568, sub_822D3FA0, sub_823023E0, sub_8230B488, sub_8230B8B8, sub_8230B998, sub_8230BB78, sub_82317970, sub_823201F8, sub_823210D8, sub_82321938, sub_82455F20, sub_824E3D68) based on the result. This is a very large dispatcher in the 0x82196 process cluster. 728 lines. 24 callees. 7 callers.
[entrypoint.functions."0x821961A0"]
name = "ProcessAndProcessAndProcess1290_821961A0"

# Process a large data structure (seven hundred and one hundred and one hundred and one hundred and one hundred and two hundred and ninety-first form). It takes a structure, allocates fresh memory (Allocate_OrDbgBreak), and drives sub-operations (ProcessAndProcessAndProcess933, sub_82422A20, sub_829FA358, sub_829FA4B0) based on the result. Part of the 0x821A8 process cluster. 303 lines. 5 callees. 0 callers.
[entrypoint.functions."0x821A8B28"]
name = "ProcessAndProcessAndProcess1291_821A8B28"

# Process a very large data structure (seven hundred and one hundred and one hundred and one hundred and one hundred and two hundred and ninety-second form). It takes a structure, destroys a circular list (DestroyCircularList), and drives sub-operations (ProcessAndProcessAndProcess1106, ProcessAndProcessAndProcess1123, ProcessAndProcessAndProcess1124, sub_822D1410, sub_82365780, sub_825BB9D0, sub_825BBBA8, sub_825BBC28) based on the result. Part of the 0x821CC process cluster. 601 lines. 9 callees. 4 callers.
[entrypoint.functions."0x821CCB20"]
name = "ProcessAndProcessAndProcess1292_821CCB20"

# Process a large data structure (seven hundred and one hundred and one hundred and one hundred and one hundred and two hundred and ninety-third form). It takes a structure, queries the timebase (GetTimebase), drives a sub-operation (ProcessAndProcessAndProcess1013), and drives sub-operations (ProcessAndProcessAndProcess1282, ProcessAndProcessAndProcess1289) based on the result. Part of the input/state processing cluster in the 0x822B3 region. 314 lines. 4 callees. 1 caller (sub_822BA4A8).
[entrypoint.functions."0x822B36F8"]
name = "ProcessAndProcessAndProcess1293_822B36F8"

# Process a structure by driving sub-operations (seven hundred and one hundred and one hundred and one hundred and one hundred and two hundred and ninety-fourth form). It takes a structure and drives sub-operations (ProcessAndProcessAndProcess1013, ProcessAndProcessAndProcess933, ProcessAndProcessAndProcess1287, ProcessAndProcessAndProcess1288) based on the result. Part of the input/state processing cluster in the 0x822B3 region. 108 lines. 4 callees. 0 callers.
[entrypoint.functions."0x822B3848"]
name = "ProcessAndProcessAndProcess1294_822B3848"

# Process a large structure by querying the timebase (seven hundred and one hundred and one hundred and one hundred and one hundred and two hundred and ninety-fifth form). It takes a structure, queries the timebase (GetTimebase), copies its data (MemCpy), and drives a sub-operation (sub_829FA5E8) based on the result. Part of the 0x822B3 process cluster. 247 lines. 3 callees. 1 caller (ProcessAndProcessAndProcess1296).
[entrypoint.functions."0x822B3900"]
name = "ProcessAndProcessAndProcess1295_822B3900"

# Process a structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and one hundred and one hundred and two hundred and ninety-sixth form). It takes a structure and drives a sub-operation (ProcessAndProcessAndProcess1295) based on the result. Part of the 0x822B3 process cluster. 41 lines. 1 callee. 0 callers.
[entrypoint.functions."0x822B3AA8"]
name = "ProcessAndProcessAndProcess1296_822B3AA8"

# Process a structure by driving sub-operations (seven hundred and one hundred and one hundred and one hundred and one hundred and two hundred and ninety-seventh form). It takes a structure and drives sub-operations (ProcessAndProcessAndProcess1013, ProcessAndProcessAndProcess1293) based on the result. Part of the input/state processing cluster in the 0x822BA region. 44 lines. 2 callees. 0 callers.
[entrypoint.functions."0x822BA4A8"]
name = "ProcessAndProcessAndProcess1297_822BA4A8"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and two hundred and ninety-eighth form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x822C1 process cluster. 60 lines. 0 callees. 0 callers.
[entrypoint.functions."0x822C17B8"]
name = "ProcessAndProcessAndProcess1298_822C17B8"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and two hundred and ninety-ninth form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x822C1 process cluster. 19 lines. 0 callees. 0 callers.
[entrypoint.functions."0x822C1BC8"]
name = "ProcessAndProcessAndProcess1299_822C1BC8"

# Process a large structure by freeing size-bucketed memory (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundredth form). It takes a structure, frees size-bucketed memory (Free_SizeBucketed), gets the current time (GetCurrentTimeMs), reads a field (GetField4), and drives sub-operations (NoOp_Stub, sub_82172EE8, sub_82323230, sub_82348CA8, sub_824D4138, sub_824D4230, sub_824E3C28, sub_82C816E0, sub_82C816E8) based on the result. This is a frequently-called helper in the 0x822C3 process cluster. 221 lines. 12 callees. 12 callers (including ProcessAndProcessAndProcess1290).
[entrypoint.functions."0x822C3F48"]
name = "ProcessAndProcessAndProcess1300_822C3F48"

# Process an enormous data structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and first form). It takes a structure, destroys a circular list (DestroyCircularList), frees size-bucketed memory (Free_SizeBucketed), gets the current time (GetCurrentTimeMs), gets a flag (GetFlag), aborts on failure (NoOp_Stub), releases a refcounted object (Release_RefCounted), and drives many sub-operations (sub_82309E98, sub_8230B348, sub_8230B398, sub_8230DFF0, sub_82310D70, sub_82326080, sub_823915D8, sub_82392F70, sub_824E3438, sub_8256B248, sub_828580F8, sub_82858B28, sub_82C81690) based on the result. This is a very large circular-list/refcounted-object destroy routine in the 0x822C5 process cluster. 739 lines. 19 callees. 1 caller (ProcessAndProcessAndProcess1290).
[entrypoint.functions."0x822C5390"]
name = "ProcessAndProcessAndProcess1301_822C5390"

# Process a large data structure by constructing a refcounted object (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and second form). It takes a structure, constructs a refcounted object (ConstructRefCounted), and drives many sub-operations (sub_821EC8A0, sub_822D3FA0, sub_822EAF40, sub_82301D40, sub_82303DD8, sub_82305D28, sub_823210D8, sub_823214B8, sub_82337C68, sub_8235D728, sub_82CBCF80, sub_82CFA080) based on the result. Part of the 0x822CA process cluster. 586 lines. 13 callees. 1 caller (ProcessAndProcessAndProcess1286).
[entrypoint.functions."0x822CAD30"]
name = "ProcessAndProcessAndProcess1302_822CAD30"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and third form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x822CE process cluster. 19 lines. 0 callees. 0 callers.
[entrypoint.functions."0x822CEEF8"]
name = "ProcessAndProcessAndProcess1303_822CEEF8"

# Process a large data structure by clearing memory (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and fourth form). It takes a structure, clears its memory (MemSet), checks for success (ProcessAndCheckSuccess), drives a sub-operation (ProcessAndProcessAndProcess479), and drives sub-operations (sub_8230F690, sub_82339C20, sub_82339D50, sub_82339E68, sub_82339F50, sub_8233A0D0, sub_82CBC0A8, sub_82CBC490) based on the result. Part of the 0x822CF process cluster. 468 lines. 11 callees. 1 caller (ProcessAndProcessAndProcess1290).
[entrypoint.functions."0x822CFDE0"]
name = "ProcessAndProcessAndProcess1304_822CFDE0"

# Process a structure by destroying a circular list (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and fifth form). It takes a structure, destroys a circular list (DestroyCircularList), gets a flag (GetFlag), and drives sub-operations (sub_8245BF58, sub_825BB9D0, sub_825BBBA8) based on the result. Part of the 0x822D1 process cluster. 196 lines. 5 callees. 1 caller (ProcessAndProcessAndProcess1292).
[entrypoint.functions."0x822D1410"]
name = "ProcessAndProcessAndProcess1305_822D1410"

# Process a structure by getting the current time (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and sixth form). It takes a structure and gets the current time (GetCurrentTimeMs). Part of the 0x822D1 process cluster. 153 lines. 1 callee. 2 callers (ProcessAndProcessAndProcess1290 and sub_822F9608).
[entrypoint.functions."0x822D1568"]
name = "ProcessAndProcessAndProcess1306_822D1568"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and seventh form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x822D1 process cluster. 8 lines. 0 callees. 0 callers.
[entrypoint.functions."0x822D1678"]
name = "ProcessAndProcessAndProcess1307_822D1678"

# Process a structure by clearing memory and allocating (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and eighth form). It takes a structure, clears its memory (MemSet), and allocates fresh memory (ProcessAndAllocateAndProcess2). This is a frequently-called helper in the 0x822D3 process cluster. 142 lines. 2 callees. 20 callers (including ProcessAndProcessAndProcess1286 and ProcessAndProcessAndProcess1290).
[entrypoint.functions."0x822D3FA0"]
name = "ProcessAndProcessAndProcess1308_822D3FA0"

# Process a large structure by getting the current time (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and ninth form). It takes a structure, gets the current time (GetCurrentTimeMs), and drives sub-operations (sub_82172EE8, ProcessAndProcessAndProcess1308, sub_822EC470, sub_822EC580, sub_82CBC490, sub_82CF9858) based on the result. Part of the 0x822EC process cluster. 170 lines. 7 callees. 3 callers (including ProcessAndProcessAndProcess1286).
[entrypoint.functions."0x822EC348"]
name = "ProcessAndProcessAndProcess1309_822EC348"

# Process a large data structure by freeing size-bucketed memory (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and tenth form). It takes a structure, frees size-bucketed memory (Free_SizeBucketed), drives a sub-operation (ProcessAndProcessAndProcess479), and drives sub-operations (sub_8238E348, sub_8238E400, sub_8238E4E8, sub_8238E7D8, sub_82CFA1B0) based on the result. Part of the 0x8230B process cluster. 353 lines. 7 callees. 1 caller (ProcessAndProcessAndProcess1290).
[entrypoint.functions."0x8230B488"]
name = "ProcessAndProcessAndProcess1310_8230B488"

# Process a very large data structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and eleventh form). It takes a structure, checks a valid pointer (CheckValidPointer), constructs a refcounted object (ConstructRefCounted), counts non-zero bytes (CountNonZeroBytes), destructs a refcounted object (DestructRefCounted), finds a chain root (FindChainRoot3), gets a flag (GetFlag), decrements and disposes (ProcessAndDecrementDispose), finds a tree leaf (ProcessAndFindTreeLeafLocked), and drives many sub-operations (ProcessAndProcessAndProcess1124, ProcessAndProcessAndProcess320, ReleaseGlobalRef, sub_82172EE8, sub_821B3360, sub_822F9408, sub_822FE7F0, sub_8230B348, sub_8230C328, sub_8230CE40, sub_82323398, sub_8238A2D8, sub_82455F20) based on the result. This is a very large refcounted-object tree processor in the 0x8230B cluster. 1118 lines. 21 callees. 2 callers (ProcessAndProcessAndProcess1290 and sub_823214B8).
[entrypoint.functions."0x8230BB78"]
name = "ProcessAndProcessAndProcess1311_8230BB78"

# Process an enormous data structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and twelfth form). It takes a structure, checks a valid pointer (CheckValidPointer), constructs a refcounted object (ConstructRefCounted), destructs a refcounted object (DestructRefCounted), gets a flag (GetFlag), increments a counter (ProcessAndIncrementCounter), grows a bit array (ProcessGrowBitArray), releases a global reference (ReleaseGlobalRef), and drives many sub-operations (ProcessAndProcessAndProcess1120, sub_82172EE8, sub_821BD998, sub_822D0688, sub_822FE7F0, sub_8230BAF0, sub_8230CE40, sub_82323168, sub_82323398, sub_82323550, sub_82323D58, sub_82331378, sub_82392FF8, sub_823B6D98) based on the result. This is the fifth-largest function in the recompiled binary, a refcounted-object tree processor in the 0x8230C cluster. 1588 lines. 21 callees. 1 caller (ProcessAndProcessAndProcess1311).
[entrypoint.functions."0x8230C328"]
name = "ProcessAndProcessAndProcess1312_8230C328"

# Process a structure by releasing a refcounted object (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and thirteenth form). It takes a structure, releases a refcounted object (Release_RefCounted), and drives a sub-operation (sub_828580F8) based on the result. Part of the 0x824E3 process cluster. 160 lines. 2 callees. 1 caller (ProcessAndProcessAndProcess1301).
[entrypoint.functions."0x824E3438"]
name = "ProcessAndProcessAndProcess1313_824E3438"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and fourteenth form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x829FA process cluster. 68 lines. 0 callees. 2 callers (ProcessAndProcessAndProcess1280 and sub_822A9B20).
[entrypoint.functions."0x829FA2E0"]
name = "ProcessAndProcessAndProcess1314_829FA2E0"

# Process a structure by entering a critical section (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and fifteenth form). It takes a structure, enters or tries a critical section (CriticalSection_EnterOrTry), and drives a sub-operation (ProcessAndProcessAndProcess1275) based on the result. This is a frequently-called helper in the 0x8236D process cluster. 100 lines. 2 callees. 10 callers.
[entrypoint.functions."0x8236D600"]
name = "ProcessAndProcessAndProcess1315_8236D600"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and sixteenth form). It takes a structure, checks a character-type predicate bit (Ctype_Pred_Bit3), and drives a sub-operation (ProcessAndProcessAndProcess925) based on the result. Part of the 0x82C47 process cluster. 93 lines. 2 callees. 3 callers (sub_82C47AF0, sub_82C47BC0, sub_82C48180).
[entrypoint.functions."0x82C47A50"]
name = "ProcessAndProcessAndProcess1316_82C47A50"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and seventeenth form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82356 process cluster. 10 lines. 0 callees. 0 callers.
[entrypoint.functions."0x82356338"]
name = "ProcessAndProcessAndProcess1317_82356338"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and eighteenth form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82C02 process cluster. 8 lines. 0 callees. 0 callers.
[entrypoint.functions."0x82C02410"]
name = "ProcessAndProcessAndProcess1318_82C02410"

# Process a large structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and nineteenth form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82396 process cluster. 214 lines. 0 callees. 1 caller (sub_82B14718).
[entrypoint.functions."0x82396A68"]
name = "ProcessAndProcessAndProcess1319_82396A68"

# Process a structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and twentieth form). It takes a structure and drives a sub-operation (ProcessAndProcessAndProcess940). This is a frequently-called helper in the 0x821F6 process cluster. 89 lines. 1 callee. 13 callers (including ProcessAndProcessAndProcess1159).
[entrypoint.functions."0x821F6140"]
name = "ProcessAndProcessAndProcess1320_821F6140"

# Process a structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and twenty-first form). It takes a structure and drives a sub-operation (ProcessAndProcessAndProcess573). Part of the UI text-rendering cluster in the 0x82C49 region. 44 lines. 1 callee. 1 caller (UIText_RenderCurrentObject).
[entrypoint.functions."0x82C492A0"]
name = "ProcessAndProcessAndProcess1321_82C492A0"

# Process a structure by making a vtable call (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and twenty-second form). It takes a structure, makes a vtable call (VtableSlotX3173), and drives a sub-operation (ProcessAndProcessAndProcess1320) based on the result. Part of the 0x821EE process cluster. 79 lines. 2 callees. 5 callers (sub_821EE438, sub_821F77C8, sub_82276700, sub_822964F8, sub_82B5A248).
[entrypoint.functions."0x821EE680"]
name = "ProcessAndProcessAndProcess1322_821EE680"

# Process a large data structure by dispatching by type code (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and twenty-third form). It takes a structure, checks its state, and drives sub-operations (DispatchByTypeCode, DispatchByTypeCode2, DispatchByTypeCode3, ProcessAndProcessAndProcess514, sub_82B91D00) based on the result. This is a large type-dispatch routine in the 0x821FA cluster. 629 lines. 5 callees. 7 callers (including ProcessAndProcessAndProcess806).
[entrypoint.functions."0x821FA590"]
name = "ProcessAndProcessAndProcess1323_821FA590"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and twenty-fourth form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82289 process cluster. 94 lines. 0 callees. 0 callers.
[entrypoint.functions."0x82289648"]
name = "ProcessAndProcessAndProcess1324_82289648"

# Process a structure by dispatching by type code (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and twenty-fifth form). It takes a structure, dispatches by type code (DispatchByTypeCode), and drives sub-operations (ProcessAndProcessAndProcess1250, ProcessAndProcessAndProcess1323) based on the result. Part of the 0x82277 process cluster. 114 lines. 3 callees. 4 callers (sub_821AB648, sub_82A3CCE8, sub_82B91F58, sub_82B92008).
[entrypoint.functions."0x82277FF8"]
name = "ProcessAndProcessAndProcess1325_82277FF8"

# Process a structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and twenty-sixth form). It takes a structure and drives a sub-operation (ProcessAndProcessAndProcess1325). Part of the 0x821AB process cluster. 86 lines. 1 callee. 6 callers (sub_8228FF90, sub_82B7B870, sub_82B87648, sub_82B921C0, sub_82B92298, sub_82B924D8).
[entrypoint.functions."0x821AB648"]
name = "ProcessAndProcessAndProcess1326_821AB648"

# Process a very frequently-used structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and twenty-seventh form). It takes a structure and checks its state, with no external sub-operations. This is a frequently-called helper in the 0x8272E process cluster. 80 lines. 0 callees. 9 callers (including ProcessAndProcessAndProcess1070).
[entrypoint.functions."0x8272EFC0"]
name = "ProcessAndProcessAndProcess1327_8272EFC0"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and twenty-eighth form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82A1B process cluster. 84 lines. 0 callees. 1 caller (sub_82A1B1A0).
[entrypoint.functions."0x82A1B098"]
name = "ProcessAndProcessAndProcess1328_82A1B098"

# Process a structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and twenty-ninth form). It takes a structure and drives a sub-operation (ProcessAndProcessAndProcess1330) based on the result. Part of the 0x82284 process cluster. 32 lines. 1 callee. 0 callers.
[entrypoint.functions."0x82284B78"]
name = "ProcessAndProcessAndProcess1329_82284B78"

# Process a large data structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and thirtieth form). It takes a structure, checks its state, and drives sub-operations (sub_82283ED0, sub_82293968, sub_823F7CE8, sub_823F94C8, sub_828C9860, sub_82A39D88, sub_82A39E30, sub_8322BA18, sub_8322BB68, sub_8322BD20) based on the result. Part of the 0x828C9 process cluster. 433 lines. 10 callees. 2 callers (sub_82201C70 and ProcessAndProcessAndProcess1329).
[entrypoint.functions."0x828C95E8"]
name = "ProcessAndProcessAndProcess1330_828C95E8"

# Process a structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and thirty-first form). It takes a structure and drives a sub-operation (ProcessAndProcessAndProcess1336). Part of the 0x82181 process cluster. 120 lines. 1 callee. 5 callers (sub_82A6C880, sub_82A6C8E8, sub_82A6C9E0, sub_82AA5040, sub_82B34A18).
[entrypoint.functions."0x82181828"]
name = "ProcessAndProcessAndProcess1331_82181828"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and thirty-second form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x8228A process cluster. 8 lines. 0 callees. 6 callers (sub_8232DBB8, sub_82397040, sub_823971C8, sub_82397348, sub_82C70A00, sub_82C71050).
[entrypoint.functions."0x8228A610"]
name = "ProcessAndProcessAndProcess1332_8228A610"

# Process a structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and thirty-third form). It takes a structure and drives a sub-operation (ProcessAndProcessAndProcess1042). Part of the 0x82C08 process cluster. 78 lines. 1 callee. 2 callers (sub_82C08F08 and sub_82C09500).
[entrypoint.functions."0x82C08DC0"]
name = "ProcessAndProcessAndProcess1333_82C08DC0"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and thirty-fourth form). It takes a structure and checks its state, with no external sub-operations. Part of the UI text-rendering cluster in the 0x82C09 region. 102 lines. 0 callees. 1 caller (UIText_RenderElement).
[entrypoint.functions."0x82C09018"]
name = "ProcessAndProcessAndProcess1334_82C09018"

# Process a large structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and thirty-fifth form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82C12 process cluster. 152 lines. 0 callees. 0 callers.
[entrypoint.functions."0x82C12D90"]
name = "ProcessAndProcessAndProcess1335_82C12D90"

# Process a large data structure by allocating checked memory (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and thirty-sixth form). It takes a structure, allocates checked memory (AllocateChecked3), frees size-bucketed memory (Free_SizeBucketed), and drives sub-operations (ProcessAndProcessAndProcess1136, sub_8272EF00, ProcessAndProcessAndProcess1327, ProcessAndProcessAndProcess1337, sub_82A6CDC8, sub_82A97648) based on the result. Part of the 0x8217F process cluster. 362 lines. 8 callees. 1 caller (ProcessAndProcessAndProcess1331).
[entrypoint.functions."0x8217FAB8"]
name = "ProcessAndProcessAndProcess1336_8217FAB8"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and thirty-seventh form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82A6C process cluster. 70 lines. 0 callees. 2 callers (ProcessAndProcessAndProcess1336 and sub_8272E910).
[entrypoint.functions."0x82A6CD08"]
name = "ProcessAndProcessAndProcess1337_82A6CD08"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and thirty-eighth form). It takes a structure and checks its state, with no external sub-operations. Part of the tree-search cluster in the 0x82C46 region. 112 lines. 0 callees. 1 caller (Tree_Search).
[entrypoint.functions."0x82C46A30"]
name = "ProcessAndProcessAndProcess1338_82C46A30"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and thirty-ninth form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82C4D process cluster. 12 lines. 0 callees. 8 callers.
[entrypoint.functions."0x82C4DA10"]
name = "ProcessAndProcessAndProcess1339_82C4DA10"

# Process a structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and fortieth form). It takes a structure and drives a sub-operation (ProcessAndProcessAndProcess1331). Part of the 0x82A6C process cluster. 60 lines. 1 callee. 5 callers (including ProcessAndProcessAndProcess1342).
[entrypoint.functions."0x82A6C880"]
name = "ProcessAndProcessAndProcess1340_82A6C880"

# Process a structure by finding a tree leaf (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and forty-first form). It takes a structure, finds a tree leaf (FindTreeLeaf), and drives a sub-operation (ProcessAndProcessAndProcess951) based on the result. Part of the 0x82B7D process cluster. 96 lines. 2 callees. 3 callers (sub_82A3BC58, sub_82B61BE0, sub_82B624C8).
[entrypoint.functions."0x82B7DD58"]
name = "ProcessAndProcessAndProcess1341_82B7DD58"

# Process a large data structure by constructing a refcounted object (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and forty-second form). It takes a structure, allocates fresh memory (Allocate_OrDbgBreak), constructs a refcounted object (ConstructRefCounted), performs a refcounted copy assignment (CopyAssign_RefCounted), destructs a refcounted object (DestructRefCounted), allocates fresh memory (ProcessAndAllocateAndProcess2), and drives a sub-operation (ProcessAndProcessAndProcess1340) based on the result. This is a refcounted-object processor in the 0x822A2 cluster. 220 lines. 6 callees. 10 callers.
[entrypoint.functions."0x822A2478"]
name = "ProcessAndProcessAndProcess1342_822A2478"

# Process a structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and forty-third form). It takes a structure and drives a sub-operation (ProcessAndProcessAndProcess620). Part of the 0x82199 process cluster. 75 lines. 1 callee. 7 callers (sub_821A2E00, sub_82A69728, sub_82A6BC50, sub_82A6BDF8, sub_82B7B760, sub_82B7B9B8, sub_82B839B0).
[entrypoint.functions."0x821993C0"]
name = "ProcessAndProcessAndProcess1343_821993C0"

# Process a data structure by performing a refcounted copy assignment (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and forty-fourth form). It takes a structure, performs a refcounted copy assignment (CopyAssign_RefCounted2), calls methods (ProcessAndCallMethods), checks a method (ProcessAndCheckMethod), and drives a sub-operation (sub_82C04078) based on the result. This is a refcounted method-dispatch routine in the 0x82C04 cluster. 172 lines. 4 callees. 2 callers (sub_82C04248 and sub_82C04280).
[entrypoint.functions."0x82C04138"]
name = "ProcessAndProcessAndProcess1344_82C04138"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and forty-fifth form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x8221D process cluster. 18 lines. 0 callees. 0 callers.
[entrypoint.functions."0x8221D2C0"]
name = "ProcessAndProcessAndProcess1345_8221D2C0"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and forty-sixth form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82188 time cluster. 18 lines. 0 callees. 0 callers.
[entrypoint.functions."0x82188300"]
name = "ProcessAndProcessAndProcess1346_82188300"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and forty-seventh form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82188 time cluster. 26 lines. 0 callees. 0 callers.
[entrypoint.functions."0x82188320"]
name = "ProcessAndProcessAndProcess1347_82188320"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and forty-eighth form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82188 time cluster. 41 lines. 0 callees. 0 callers.
[entrypoint.functions."0x82188348"]
name = "ProcessAndProcessAndProcess1348_82188348"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and forty-ninth form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82188 time cluster. 22 lines. 0 callees. 1 caller (sub_82FE8E60).
[entrypoint.functions."0x82188388"]
name = "ProcessAndProcessAndProcess1349_82188388"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and fiftieth form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82188 time cluster. 22 lines. 0 callees. 1 caller (sub_82FE8E60).
[entrypoint.functions."0x821883B0"]
name = "ProcessAndProcessAndProcess1350_821883B0"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and fifty-first form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82188 time cluster. 22 lines. 0 callees. 0 callers.
[entrypoint.functions."0x821883D8"]
name = "ProcessAndProcessAndProcess1351_821883D8"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and fifty-second form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82188 time cluster. 22 lines. 0 callees. 0 callers.
[entrypoint.functions."0x82188400"]
name = "ProcessAndProcessAndProcess1352_82188400"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and fifty-third form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82188 time cluster. 22 lines. 0 callees. 0 callers.
[entrypoint.functions."0x82188428"]
name = "ProcessAndProcessAndProcess1353_82188428"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and fifty-fourth form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82188 time cluster. 18 lines. 0 callees. 0 callers.
[entrypoint.functions."0x82188450"]
name = "ProcessAndProcessAndProcess1354_82188450"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and fifty-fifth form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82188 time cluster. 22 lines. 0 callees. 0 callers.
[entrypoint.functions."0x82188470"]
name = "ProcessAndProcessAndProcess1355_82188470"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and fifty-sixth form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82188 time cluster. 22 lines. 0 callees. 0 callers.
[entrypoint.functions."0x82188498"]
name = "ProcessAndProcessAndProcess1356_82188498"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and fifty-seventh form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82188 time cluster. 18 lines. 0 callees. 0 callers.
[entrypoint.functions."0x821884C0"]
name = "ProcessAndProcessAndProcess1357_821884C0"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and fifty-eighth form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82188 time cluster. 22 lines. 0 callees. 0 callers.
[entrypoint.functions."0x821884E0"]
name = "ProcessAndProcessAndProcess1358_821884E0"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and fifty-ninth form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x8219C process cluster. 41 lines. 0 callees. 0 callers.
[entrypoint.functions."0x8219CD68"]
name = "ProcessAndProcessAndProcess1359_8219CD68"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and sixtieth form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x821A8 process cluster. 83 lines. 0 callees. 0 callers.
[entrypoint.functions."0x821A89E8"]
name = "ProcessAndProcessAndProcess1360_821A89E8"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and sixty-first form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x821A8 process cluster. 101 lines. 0 callees. 0 callers.
[entrypoint.functions."0x821A8A78"]
name = "ProcessAndProcessAndProcess1361_821A8A78"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and sixty-second form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x821BF process cluster. 22 lines. 0 callees. 1 caller (sub_82FEB738).
[entrypoint.functions."0x821BFFF0"]
name = "ProcessAndProcessAndProcess1362_821BFFF0"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and sixty-third form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82208 process cluster. 18 lines. 0 callees. 0 callers.
[entrypoint.functions."0x82208158"]
name = "ProcessAndProcessAndProcess1363_82208158"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and sixty-fourth form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x8220A process cluster. 18 lines. 0 callees. 0 callers.
[entrypoint.functions."0x8220A5C8"]
name = "ProcessAndProcessAndProcess1364_8220A5C8"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and sixty-fifth form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82239 process cluster. 14 lines. 0 callees. 2 callers (sub_82FE8E60 and sub_82FE9018).
[entrypoint.functions."0x822398E8"]
name = "ProcessAndProcessAndProcess1365_822398E8"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and sixty-sixth form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x8229A process cluster. 22 lines. 0 callees. 0 callers.
[entrypoint.functions."0x8229A750"]
name = "ProcessAndProcessAndProcess1366_8229A750"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and sixty-seventh form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x822A5 process cluster. 18 lines. 0 callees. 2 callers (sub_82FE8E60 and sub_82FE9018).
[entrypoint.functions."0x822A5F40"]
name = "ProcessAndProcessAndProcess1367_822A5F40"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and sixty-eighth form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x822A5 process cluster. 18 lines. 0 callees. 0 callers.
[entrypoint.functions."0x822A5F60"]
name = "ProcessAndProcessAndProcess1368_822A5F60"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and sixty-ninth form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x822C0 process cluster. 20 lines. 0 callees. 0 callers.
[entrypoint.functions."0x822C07B0"]
name = "ProcessAndProcessAndProcess1369_822C07B0"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and seventieth form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x822C0 process cluster. 22 lines. 0 callees. 0 callers.
[entrypoint.functions."0x822C07D0"]
name = "ProcessAndProcessAndProcess1370_822C07D0"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and seventy-first form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x822C0 process cluster. 22 lines. 0 callees. 0 callers.
[entrypoint.functions."0x822C07F8"]
name = "ProcessAndProcessAndProcess1371_822C07F8"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and seventy-second form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x822C0 process cluster. 22 lines. 0 callees. 0 callers.
[entrypoint.functions."0x822C0820"]
name = "ProcessAndProcessAndProcess1372_822C0820"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and seventy-third form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x822C0 process cluster. 22 lines. 0 callees. 0 callers.
[entrypoint.functions."0x822C0848"]
name = "ProcessAndProcessAndProcess1373_822C0848"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and seventy-fourth form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x822C0 process cluster. 22 lines. 0 callees. 0 callers.
[entrypoint.functions."0x822C0870"]
name = "ProcessAndProcessAndProcess1374_822C0870"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and seventy-fifth form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x822C0 process cluster. 22 lines. 0 callees. 0 callers.
[entrypoint.functions."0x822C0898"]
name = "ProcessAndProcessAndProcess1375_822C0898"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and seventy-sixth form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x822C0 process cluster. 20 lines. 0 callees. 0 callers.
[entrypoint.functions."0x822C08C0"]
name = "ProcessAndProcessAndProcess1376_822C08C0"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and seventy-seventh form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x822C0 process cluster. 14 lines. 0 callees. 2 callers (sub_82FE8E60 and sub_82FE9018).
[entrypoint.functions."0x822C08E0"]
name = "ProcessAndProcessAndProcess1377_822C08E0"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and seventy-eighth form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x822C0 process cluster. 8 lines. 0 callees. 0 callers.
[entrypoint.functions."0x822C08F8"]
name = "ProcessAndProcessAndProcess1378_822C08F8"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and seventy-ninth form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x822C0 process cluster. 45 lines. 0 callees. 0 callers.
[entrypoint.functions."0x822C0900"]
name = "ProcessAndProcessAndProcess1379_822C0900"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and eightieth form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x822C0 process cluster. 14 lines. 0 callees. 0 callers.
[entrypoint.functions."0x822C0948"]
name = "ProcessAndProcessAndProcess1380_822C0948"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and eighty-first form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x822C0 process cluster. 70 lines. 0 callees. 1 caller (sub_82FE8740).
[entrypoint.functions."0x822C0960"]
name = "ProcessAndProcessAndProcess1381_822C0960"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and eighty-second form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82237 process cluster. 107 lines. 0 callees. 2 callers (ProcessAndProcessAndProcess1385 and sub_82B608B8).
[entrypoint.functions."0x82237378"]
name = "ProcessAndProcessAndProcess1382_82237378"

# Process a structure by entering a critical section (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and eighty-third form). It takes a structure, enters or tries a critical section (CriticalSection_EnterOrTry), gets a global field (GetGlobalField), yields and checks a threshold (YieldAndCheckThreshold), and drives sub-operations (sub_82B68F60, sub_82B68FC8) based on the result. Part of the 0x82B69 process cluster. 142 lines. 5 callees. 4 callers (including ProcessAndProcessAndProcess577).
[entrypoint.functions."0x82B69098"]
name = "ProcessAndProcessAndProcess1383_82B69098"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and eighty-fourth form). It takes a structure, checks its state, and drives sub-operations (sub_82C08F08, sub_82C09680) based on the result. Part of the UI text-rendering cluster in the 0x82C09 region. 68 lines. 2 callees. 1 caller (UIText_RenderElement).
[entrypoint.functions."0x82C09870"]
name = "ProcessAndProcessAndProcess1384_82C09870"

# Process a very large data structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and eighty-fifth form). It takes a structure, enters a critical section (CriticalSection_EnterOrTry), gets a global field (GetGlobalField), and drives many sub-operations (ProcessAndProcessAndProcess1021, ProcessAndProcessAndProcess1066, ProcessAndProcessAndProcess1155, ProcessAndProcessAndProcess1178, ProcessAndProcessAndProcess1179, ProcessAndProcessAndProcess1184, ProcessAndProcessAndProcess1186, ProcessAndProcessAndProcess1214, ProcessAndProcessAndProcess1255, ProcessAndProcessAndProcess264, ProcessAndProcessAndProcess526, ProcessAndProcessAndProcess555, ProcessAndProcessAndProcess653, ProcessAndProcessAndProcess805, sub_821815B0, sub_821A5CB8, sub_821C3CD0, sub_821E2D40, sub_821F4448, ProcessAndProcessAndProcess1382, sub_82263530, sub_822B4D70, sub_822B4E08, sub_822C09E0, ProcessAndProcessAndProcess1388, sub_822E0508, sub_82B38478, sub_82B6C060, sub_82B7CD18, sub_82B9BA58, sub_82BE4D50) based on the result. This is a large dispatcher in the 0x821C3 cluster. 592 lines. 33 callees. 1 caller (ProcessAndProcessAndProcess1389).
[entrypoint.functions."0x821C38C8"]
name = "ProcessAndProcessAndProcess1385_821C38C8"

# Process a structure by flushing the cache (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and eighty-sixth form). It takes a structure and flushes the cache (FlushCache). Part of the 0x822AA process cluster. 97 lines. 1 callee. 1 caller (ProcessAndProcessAndProcess926).
[entrypoint.functions."0x822AAFB8"]
name = "ProcessAndProcessAndProcess1386_822AAFB8"

# Process a structure by performing a refcounted copy assignment (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and eighty-seventh form). It takes a structure, performs a refcounted copy assignment (CopyAssign_RefCounted), and destructs a refcounted object (DestructRefCounted). This is a frequently-called helper in the 0x822AB process cluster. 85 lines. 2 callees. 12 callers.
[entrypoint.functions."0x822AB200"]
name = "ProcessAndProcessAndProcess1387_822AB200"

# Process an enormous data structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and eighty-eighth form). It takes a structure and checks its state, with no external sub-operations. This is the sixth-largest function in the recompiled binary, in the 0x822C2 cluster. 2460 lines. 0 callees. 1 caller (ProcessAndProcessAndProcess1385).
[entrypoint.functions."0x822C2C58"]
name = "ProcessAndProcessAndProcess1388_822C2C58"

# Process a very large data structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and eighty-ninth form). It takes a structure, checks a byte value (ByteValueCheck), enters a critical section (CriticalSection_EnterOrTry), gets the timebase (GetTimebase), and drives many sub-operations (ProcessAndProcessAndProcess1107, ProcessAndProcessAndProcess1215, ProcessAndProcessAndProcess1265, ProcessAndProcessAndProcess313, ProcessAndProcessAndProcess314, sub_8217FD08, ProcessAndProcessAndProcess1385, sub_822C6C60, sub_8235E0D8, sub_824F7CA0, sub_825AB3D0, sub_82A50E40, sub_82B610B0, ProcessAndProcessAndProcess1383) based on the result. This is a large dispatcher in the 0x8236C cluster. 544 lines. 17 callees. 2 callers (sub_8236C360 and sub_825AAC00).
[entrypoint.functions."0x8236C520"]
name = "ProcessAndProcessAndProcess1389_8236C520"

# Process an enormous data structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and ninetieth form). It takes a structure, drives a sub-operation (ProcessAndProcessAndProcess554), and drives a state transition (ProcessStateTransition) based on the result. This is the third-largest function in the recompiled binary, in the 0x82B75 cluster. 4088 lines. 2 callees. 2 callers (sub_82B608B8 and sub_82B610B0).
[entrypoint.functions."0x82B75D48"]
name = "ProcessAndProcessAndProcess1390_82B75D48"

# Lazy-initialize subsystem A on first use. Checks a one-bit flag; if unset, sets it and calls the initialization routine (sub_825AB160) followed by ProcessAndAllocateAndProcess2. Returns the initialized subsystem pointer. 72 lines. 2 callees. 4 callers.
[entrypoint.functions."0x8217FD08"]
name = "LazyInitSubsystemA_8217FD08"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and ninety-second form). It takes a structure, checks its state, and drives sub-operations (sub_82A70878, sub_82A96EF8) based on the result. Part of the 0x821A5 process cluster. 118 lines. 2 callees. 1 caller (ProcessAndProcessAndProcess1385).
[entrypoint.functions."0x821A5CB8"]
name = "ProcessAndProcessAndProcess1392_821A5CB8"

# Lazy-initialize subsystem B on first use. Checks a one-bit flag; if unset, sets it and calls the initialization routine (sub_824F73A8) followed by ProcessAndAllocateAndProcess2. Returns the initialized subsystem pointer. 72 lines. 2 callees. 12 callers.
[entrypoint.functions."0x822C6C60"]
name = "LazyInitSubsystemB_822C6C60"

# Process a very large data structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and ninety-fourth form). It takes a structure, allocates fresh memory (Allocate_OrDbgBreak), enters a critical section (CriticalSection_EnterOrTry), frees size-bucketed memory (Free_SizeBucketed), gets the current time (GetCurrentTimeMs), decrements and disposes (ProcessAndDecrementDispose), releases a global reference (ReleaseGlobalRef), and drives many sub-operations (ProcessAndProcessAndProcess1215, ProcessAndProcessAndProcess1255, ProcessAndProcessAndProcess313, ProcessAndProcessAndProcess314, ProcessAndProcessAndProcess766, sub_823034F0, sub_824F82F8, sub_824F83B0, sub_824F8508, sub_824F9090, sub_824F9118, sub_824F91A8, sub_82A39078, sub_82A3AD50, sub_82A3B890, sub_82A3BAD0, sub_82A3BB78, sub_82A3BC58, sub_82A48E30, sub_82B38D00, ProcessAndProcessAndProcess1399, sub_82B614D0, sub_82B622F0) based on the result. This is a large state-transition processor in the 0x824F7 cluster. 943 lines. 29 callees. 1 caller (ProcessAndProcessAndProcess1389).
[entrypoint.functions."0x824F7CA0"]
name = "ProcessAndProcessAndProcess1394_824F7CA0"

# Process a large data structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and ninety-fifth form). It takes a structure, enters a critical section (CriticalSection_EnterOrTry), gets the current time (GetCurrentTimeMs), and drives sub-operations (ProcessAndProcessAndProcess279, ProcessAndProcessAndProcess313, ProcessAndProcessAndProcess314, sub_825AAD70, sub_825AB370, ProcessAndProcessAndProcess1396, sub_825ABC40, sub_829F83A0, sub_82B83FA8, sub_82CC0560, sub_82CC05E0) based on the result. Part of the 0x825AB process cluster. 482 lines. 14 callees. 1 caller (ProcessAndProcessAndProcess1389).
[entrypoint.functions."0x825AB3D0"]
name = "ProcessAndProcessAndProcess1395_825AB3D0"

# Process a structure by entering a critical section (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and ninety-sixth form). It takes a structure and enters or tries a critical section (CriticalSection_EnterOrTry). Part of the 0x825AB process cluster. 52 lines. 1 callee. 1 caller (ProcessAndProcessAndProcess1395).
[entrypoint.functions."0x825AB768"]
name = "ProcessAndProcessAndProcess1396_825AB768"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and ninety-seventh form). It takes a structure and drives a sub-operation (DelayZero). Part of the 0x82A14 process cluster. 52 lines. 1 callee. 0 callers.
[entrypoint.functions."0x82A14C48"]
name = "ProcessAndProcessAndProcess1397_82A14C48"

# Process a structure by destructing a refcounted object (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and ninety-eighth form). It takes a structure, destructs a refcounted object (DestructRefCounted), and frees size-bucketed memory (Free_SizeBucketed). Part of the 0x82AA5 process cluster. 64 lines. 2 callees. 0 callers.
[entrypoint.functions."0x82AA5DF8"]
name = "ProcessAndProcessAndProcess1398_82AA5DF8"

# Process a large data structure by entering a critical section (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and ninety-ninth form). It takes a structure, enters or tries a critical section (CriticalSection_EnterOrTry), drives a sub-operation (ProcessAndProcessAndProcess551), and drives a state transition (ProcessAndProcessAndProcess1390) based on the result. Part of the 0x82B61 process cluster. 351 lines. 3 callees. 3 callers (sub_822C4F30, ProcessAndProcessAndProcess1389, ProcessAndProcessAndProcess1394).
[entrypoint.functions."0x82B610B0"]
name = "ProcessAndProcessAndProcess1399_82B610B0"

# Process a structure by freeing size-bucketed memory (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and one hundredth form). It takes a structure, frees size-bucketed memory (Free_SizeBucketed), releases a refcounted object (Release_RefCounted), and drives a sub-operation (sub_822CCA58) based on the result. Part of the 0x82B6C process cluster. 80 lines. 3 callees. 3 callers (including ProcessAndProcessAndProcess1385).
[entrypoint.functions."0x82B6C060"]
name = "ProcessAndProcessAndProcess1400_82B6C060"

# Process a structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and one hundred and first form). It takes a structure and drives a sub-operation (sub_82A1DFB8). Part of the 0x82180 process cluster. 76 lines. 1 callee. 4 callers (including ProcessAndProcessAndProcess1402).
[entrypoint.functions."0x82180AB0"]
name = "ProcessAndProcessAndProcess1401_82180AB0"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and one hundred and second form). It takes a structure, checks its state, and drives sub-operations (ProcessAndProcessAndProcess1066, ProcessAndProcessAndProcess1155, ProcessAndProcessAndProcess1401, sub_822C5898) based on the result. Part of the 0x822B4 process cluster. 87 lines. 4 callees. 2 callers (including ProcessAndProcessAndProcess1385).
[entrypoint.functions."0x822B4D70"]
name = "ProcessAndProcessAndProcess1402_822B4D70"

# Process a structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and forty-third form). It takes a structure and drives a sub-operation (ProcessAndProcessAndProcess1404). Part of the 0x822B4 process cluster. 97 lines. 1 callee. 6 callers (including ProcessAndProcessAndProcess1385).
[entrypoint.functions."0x822B4E08"]
name = "ProcessAndProcessAndProcess1403_822B4E08"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and forty-fourth form). It takes a structure, checks its state, and drives sub-operations (ProcessAndProcessAndProcess934, sub_82B5A248, sub_82B5BDA0) based on the result. Part of the 0x822B4 process cluster. 124 lines. 3 callees. 1 caller (ProcessAndProcessAndProcess1403).
[entrypoint.functions."0x822B4EB8"]
name = "ProcessAndProcessAndProcess1404_822B4EB8"

# Process a large structure by freeing size-bucketed memory (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and forty-fifth form). It takes a structure, frees size-bucketed memory (Free_SizeBucketed), makes a vtable call (VtableSlotX3173), and drives sub-operations (ProcessAndProcessAndProcess1320, sub_82B84288) based on the result. Part of the 0x822C5 process cluster. 222 lines. 4 callees. 2 callers (including ProcessAndProcessAndProcess1402).
[entrypoint.functions."0x822C5898"]
name = "ProcessAndProcessAndProcess1405_822C5898"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and forty-sixth form). It takes a structure and checks its state, with no external sub-operations. Part of the UI text-rendering cluster in the 0x82CD2 region. 66 lines. 0 callees. 4 callers (sub_82C560A0, sub_82C566A0, sub_82C568A0, sub_82D017B0).
[entrypoint.functions."0x82CD2AC0"]
name = "ProcessAndProcessAndProcess1406_82CD2AC0"

# Process a structure by dispatching by type code (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and forty-seventh form). It takes a structure, checks its state, and drives sub-operations (ProcessAndProcessAndProcess1326, ProcessAndProcessAndProcess533) based on the result. This is a frequently-called type-dispatch helper in the 0x8228F cluster. 81 lines. 2 callees. 15 callers.
[entrypoint.functions."0x8228FF90"]
name = "ProcessAndProcessAndProcess1407_8228FF90"

# Process an enormous data structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and forty-eighth form). It takes a structure, allocates fresh memory (Allocate_OrDbgBreak), destructs a refcounted object (DestructRefCounted), frees size-bucketed memory (Free_SizeBucketed), gets the timebase (GetTimebase), grows a buffer (GrowBufferAndAppend), checks for success (ProcessAndCheckSuccess), sets an event (SetEvent), yields and checks a threshold (YieldAndCheckThreshold), and drives many sub-operations (ProcessAndProcessAndProcess1214, ProcessAndProcessAndProcess543, ProcessAndProcessAndProcess544, ProcessAndProcessAndProcess559, ProcessAndProcessAndProcess578, ProcessAndProcessAndProcess641, ProcessAndProcessAndProcess720, ProcessAndProcessAndProcess766, ProcessAndProcessAndProcess776, ProcessAndProcessAndProcess783, sub_827D4B70, sub_82A48FC8, sub_82A49158, sub_82A491F0, sub_82A498E8, sub_82A4A3F0, sub_82A4EB10, sub_82A4EF80, sub_82A4F1C0, sub_82A4F2F0, sub_82A4F3E0, sub_82A50A38, sub_82A50AD8, sub_82A50E40, sub_82A5AC08, sub_82A5AD18, sub_82A71B80) based on the result. This is the fourth-largest function in the recompiled binary, in the 0x822E0 cluster. 3501 lines. 35 callees. 2 callers (including ProcessAndProcessAndProcess1385).
[entrypoint.functions."0x822E0508"]
name = "ProcessAndProcessAndProcess1408_822E0508"

# Process a large structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and forty-ninth form). It takes a structure, checks its state, and drives sub-operations (ProcessAndProcessAndProcess988, sub_82A9DA90, sub_82A9DB28, sub_82A9DC38) based on the result. Part of the 0x822CC process cluster. 144 lines. 4 callees. 1 caller (ProcessAndProcessAndProcess901).
[entrypoint.functions."0x822CCDB0"]
name = "ProcessAndProcessAndProcess1409_822CCDB0"

# Process a structure by entering a critical section (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and fiftieth form). It takes a structure, enters or tries a critical section (CriticalSection_EnterOrTry), and drives a sub-operation (sub_8235ABA0) based on the result. Part of the 0x8235E process cluster. 66 lines. 2 callees. 1 caller (ProcessAndProcessAndProcess1389).
[entrypoint.functions."0x8235E0D8"]
name = "ProcessAndProcessAndProcess1410_8235E0D8"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and fifty-first form). It takes a structure, constructs a 12-byte structure (Construct12Byte), and drives sub-operations (ProcessAndProcessAndProcess524, ProcessAndProcessAndProcess676, ProcessAndProcessAndProcess677) based on the result. Part of the 0x821D9 process cluster. 68 lines. 4 callees. 5 callers (sub_821BDE80, sub_822C50F0, sub_82A3E7C0, sub_82A48AA0, sub_82B34580).
[entrypoint.functions."0x821D9840"]
name = "ProcessAndProcessAndProcess1411_821D9840"

# Process a large structure by validating (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and fifty-second form). It takes a structure, validates (ProcessAndValidateAndProcess3), and drives sub-operations (sub_82A56CA0, sub_82A58240) based on the result. Part of the 0x822C0 process cluster. 172 lines. 3 callees. 3 callers (including ProcessAndProcessAndProcess1385).
[entrypoint.functions."0x822C09E0"]
name = "ProcessAndProcessAndProcess1412_822C09E0"

# Process a large structure by getting the timebase (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and fifty-third form). It takes a structure, frees size-bucketed memory (Free_SizeBucketed), gets the timebase (GetTimebase), removes from a linked list (RemoveFromLinkedList), and drives sub-operations (ProcessAndProcessAndProcess1100, sub_82518078, sub_82A56428, sub_82A56600, sub_82A56678, sub_82A56868, sub_82A56950, sub_82A58BE8, sub_82A59438) based on the result. Part of the 0x82181 process cluster. 476 lines. 12 callees. 1 caller (ProcessAndProcessAndProcess1414).
[entrypoint.functions."0x82181270"]
name = "ProcessAndProcessAndProcess1413_82181270"

# Process a large structure by growing a buffer (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and fifty-fourth form). It takes a structure, grows a buffer (GrowBufferAndAppend), removes from a linked list (RemoveFromLinkedList), and drives sub-operations (ProcessAndProcessAndProcess776, ProcessAndProcessAndProcess1413, sub_82A4F520, sub_82A50EF0, sub_82AAA818) based on the result. Part of the 0x82181 process cluster. 361 lines. 7 callees. 1 caller (ProcessAndProcessAndProcess1385).
[entrypoint.functions."0x821815B0"]
name = "ProcessAndProcessAndProcess1414_821815B0"

# Process a structure by incrementing a refcount atomically (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and forty-fifth form). It takes a structure, increments a refcount atomically (IncrementRefcountAtomic), and drives sub-operations (ProcessAndProcessAndProcess524, Release_RefCounted) based on the result. Part of the 0x821A4 process cluster. 104 lines. 3 callees. 2 callers (sub_821BDE80 and sub_82A48AA0).
[entrypoint.functions."0x821A4FD0"]
name = "ProcessAndProcessAndProcess1415_821A4FD0"

# Process a large data structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and forty-sixth form). It takes a structure, checks its state, and drives sub-operations (ProcessAndProcessAndProcess1073, ProcessAndProcessAndProcess1255, ProcessAndProcessAndProcess510, ProcessAndProcessAndProcess934, ProcessAndProcessAndProcess980, ProcessAndProcessAndProcess983, ProcessLoopVcall, sub_8219B9B0, sub_821BDE80, sub_821C36E0, sub_8226E6E8, sub_8228A518, sub_82A7F2A8) based on the result. Part of the 0x821C3 cluster. 289 lines. 13 callees. 1 caller (ProcessAndProcessAndProcess1385).
[entrypoint.functions."0x821C3CD0"]
name = "ProcessAndProcessAndProcess1416_821C3CD0"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and forty-seventh form). It takes a structure and checks its state, with no external sub-operations. This is a frequently-called helper in the 0x8228B process cluster. 111 lines. 0 callees. 9 callers.
[entrypoint.functions."0x8228B8C8"]
name = "ProcessAndProcessAndProcess1417_8228B8C8"

# Process a structure by destructing a refcounted object (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and forty-eighth form). It takes a structure, destructs a refcounted object (DestructRefCounted), and drives sub-operations (ProcessAndProcessAndProcess1070, ProcessAndProcessAndProcess524, Release_RefCounted) based on the result. This is a frequently-called refcounted-destructor helper in the 0x8229D cluster. 86 lines. 4 callees. 17 callers.
[entrypoint.functions."0x8229D7D0"]
name = "ProcessAndProcessAndProcess1418_8229D7D0"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and forty-ninth form). It takes a structure and checks its state, with no external sub-operations. This is a frequently-called helper in the 0x823B0 process cluster. 24 lines. 0 callees. 11 callers.
[entrypoint.functions."0x823B0B10"]
name = "ProcessAndProcessAndProcess1419_823B0B10"

# Process a structure by freeing size-bucketed memory (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and fiftieth form). It takes a structure and frees size-bucketed memory (Free_SizeBucketed). Part of the 0x8217B free-and-process family. 68 lines. 1 callee. 2 callers (ProcessAndProcessAndProcess1426 and sub_832AE110).
[entrypoint.functions."0x8217BCC8"]
name = "ProcessAndProcessAndProcess1420_8217BCC8"

# Process a structure by freeing size-bucketed memory (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and fifty-first form). It takes a structure and frees size-bucketed memory (Free_SizeBucketed). Part of the 0x8217B free-and-process family. 68 lines. 1 callee. 2 callers (sub_8217C0C8 and sub_832AE230).
[entrypoint.functions."0x8217BD40"]
name = "ProcessAndProcessAndProcess1421_8217BD40"

# Process a structure by freeing size-bucketed memory (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and fifty-second form). It takes a structure and frees size-bucketed memory (Free_SizeBucketed). Part of the 0x8217B free-and-process family. 68 lines. 1 callee. 2 callers (sub_8217C1F8 and sub_832AE1A0).
[entrypoint.functions."0x8217BDB8"]
name = "ProcessAndProcessAndProcess1422_8217BDB8"

# Process a structure by freeing size-bucketed memory (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and fifty-third form). It takes a structure and frees size-bucketed memory (Free_SizeBucketed). Part of the 0x8217B free-and-process family. 68 lines. 1 callee. 2 callers (sub_8217C328 and sub_832AE278).
[entrypoint.functions."0x8217BE30"]
name = "ProcessAndProcessAndProcess1423_8217BE30"

# Process a structure by freeing size-bucketed memory (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and fifty-fourth form). It takes a structure and frees size-bucketed memory (Free_SizeBucketed). Part of the 0x8217B free-and-process family. 68 lines. 1 callee. 2 callers (sub_8217C458 and sub_832AE1E8).
[entrypoint.functions."0x8217BEA8"]
name = "ProcessAndProcessAndProcess1424_8217BEA8"

# Process a structure by freeing size-bucketed memory (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and fifty-fifth form). It takes a structure and frees size-bucketed memory (Free_SizeBucketed). Part of the 0x8217B free-and-process family. 68 lines. 1 callee. 2 callers (sub_8217C588 and sub_832AE158).
[entrypoint.functions."0x8217BF20"]
name = "ProcessAndProcessAndProcess1425_8217BF20"

# Process a large structure by freeing size-bucketed memory (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and fifty-sixth form). It takes a structure, frees size-bucketed memory (Free_SizeBucketed), and drives a sub-operation (ProcessAndProcessAndProcess1420) based on the result. This is the largest function in the 0x8217B free-and-process family. 175 lines. 2 callees. 2 callers (sub_8217C6B8 and sub_82A9AA00).
[entrypoint.functions."0x8217BF98"]
name = "ProcessAndProcessAndProcess1426_8217BF98"

# Process a large structure by freeing size-bucketed memory (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and forty-seventh form). It takes a structure, frees size-bucketed memory (Free_SizeBucketed), and drives a sub-operation (ProcessAndProcessAndProcess1421) based on the result. Part of the 0x8217B free-and-process family. 175 lines. 2 callees. 2 callers (including ProcessAndProcessAndProcess1432).
[entrypoint.functions."0x8217C0C8"]
name = "ProcessAndProcessAndProcess1427_8217C0C8"

# Process a large structure by freeing size-bucketed memory (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and forty-eighth form). It takes a structure, frees size-bucketed memory (Free_SizeBucketed), and drives a sub-operation (ProcessAndProcessAndProcess1422) based on the result. Part of the 0x8217B free-and-process family. 175 lines. 2 callees. 2 callers (including ProcessAndProcessAndProcess1432).
[entrypoint.functions."0x8217C1F8"]
name = "ProcessAndProcessAndProcess1428_8217C1F8"

# Process a large structure by freeing size-bucketed memory (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and forty-ninth form). It takes a structure, frees size-bucketed memory (Free_SizeBucketed), and drives a sub-operation (ProcessAndProcessAndProcess1423) based on the result. Part of the 0x8217B free-and-process family. 175 lines. 2 callees. 2 callers (including ProcessAndProcessAndProcess1432).
[entrypoint.functions."0x8217C328"]
name = "ProcessAndProcessAndProcess1429_8217C328"

# Process a large structure by freeing size-bucketed memory (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and fiftieth form). It takes a structure, frees size-bucketed memory (Free_SizeBucketed), and drives a sub-operation (ProcessAndProcessAndProcess1424) based on the result. Part of the 0x8217B free-and-process family. 175 lines. 2 callees. 1 caller (ProcessAndProcessAndProcess1432).
[entrypoint.functions."0x8217C458"]
name = "ProcessAndProcessAndProcess1430_8217C458"

# Process a large structure by freeing size-bucketed memory (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and fifty-first form). It takes a structure, frees size-bucketed memory (Free_SizeBucketed), and drives a sub-operation (ProcessAndProcessAndProcess1425) based on the result. Part of the 0x8217B free-and-process family. 175 lines. 2 callees. 2 callers (including ProcessAndProcessAndProcess1432).
[entrypoint.functions."0x8217C588"]
name = "ProcessAndProcessAndProcess1431_8217C588"

# Process a large data structure by driving the free-and-process family (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and fifty-second form). It takes a structure, checks its state, and drives sub-operations (ProcessAndProcessAndProcess1426, ProcessAndProcessAndProcess1427, ProcessAndProcessAndProcess1428, ProcessAndProcessAndProcess1429, ProcessAndProcessAndProcess1430, ProcessAndProcessAndProcess1431) based on the result. This is a dispatcher for the 0x8217B free-and-process family. 181 lines. 6 callees. 1 caller (ProcessAndProcessAndProcess1438).
[entrypoint.functions."0x8217C6B8"]
name = "ProcessAndProcessAndProcess1432_8217C6B8"

# Process a structure by destructing and freeing (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and fifty-third form). It takes a structure, destructs (Destruct4), and frees size-bucketed memory (Free_SizeBucketed). Part of the 0x8217C process cluster. 63 lines. 2 callees. 3 callers (sub_82197D00, sub_82A66888, sub_832ACA58).
[entrypoint.functions."0x8217C870"]
name = "ProcessAndProcessAndProcess1433_8217C870"

# Process a large data structure by allocating size-bucketed memory (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and fifty-fourth form). It takes a structure, allocates size-bucketed memory (Allocate_SizeBucketed), destructs (Destruct4), and drives sub-operations (sub_82A665B8, sub_82A666A8, sub_82A66888, sub_82B87160, sub_82CBBB58) based on the result. Part of the 0x8217C process cluster. 457 lines. 7 callees. 1 caller (sub_82197D00).
[entrypoint.functions."0x8217C8D8"]
name = "ProcessAndProcessAndProcess1434_8217C8D8"

# Process a structure by allocating fresh memory (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and fifty-fifth form). It takes a structure, allocates fresh memory (Allocate_OrDbgBreak), and drives a sub-operation (sub_82A676C0) based on the result. Part of the 0x8217C process cluster. 123 lines. 2 callees. 2 callers (sub_8217DD18 and sub_82A677A0).
[entrypoint.functions."0x8217CF78"]
name = "ProcessAndProcessAndProcess1435_8217CF78"

# Process a large structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and fifty-sixth form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x8217E process cluster. 199 lines. 0 callees. 1 caller (ProcessAndProcessAndProcess929).
[entrypoint.functions."0x8217ECD8"]
name = "ProcessAndProcessAndProcess1436_8217ECD8"

# Process a large data structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and fifty-seventh form). It takes a structure, checks its state, and drives sub-operations (ProcessAndProcessAndProcess551, ProcessAndProcessAndProcess643, sub_82365780, sub_82A5D208, sub_82A5D338, sub_82A5D838, sub_82A5E018) based on the result. Part of the 0x82180 process cluster. 373 lines. 7 callees. 1 caller (sub_821C36E0).
[entrypoint.functions."0x82180BA0"]
name = "ProcessAndProcessAndProcess1437_82180BA0"

# Process a large data structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and fifty-eighth form). It takes a structure, checks its state, and drives sub-operations (ProcessAndProcessAndProcess1432, sub_82A9ABB0, sub_82A9B3F8, sub_82A9BA38, sub_82A9C2F8, sub_82A9CCB0) based on the result. Part of the 0x82182 process cluster. 212 lines. 6 callees. 1 caller (sub_82182560).
[entrypoint.functions."0x821823F8"]
name = "ProcessAndProcessAndProcess1438_821823F8"

# Process a structure by wrapping sub-operations (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and fifty-ninth form). It takes a structure, checks its state, and drives sub-operations (ProcessAndProcessAndProcess819, ProcessAndProcessAndProcess1438) based on the result. Part of the 0x82182 process cluster. 53 lines. 2 callees. 0 callers.
[entrypoint.functions."0x82182560"]
name = "ProcessAndProcessAndProcess1439_82182560"

# Process a large structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and sixtieth form). It takes a structure, checks its state, and drives sub-operations (sub_8218DA00, sub_82A5FD88, sub_82A96970) based on the result. Part of the 0x8218D process cluster. 249 lines. 3 callees. 2 callers (ProcessAndProcessAndProcess1444 and sub_82267818).
[entrypoint.functions."0x8218DB30"]
name = "ProcessAndProcessAndProcess1440_8218DB30"

# Process a structure by wrapping sub-operations (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and sixty-first form). It takes a structure, checks its state, and drives sub-operations (ProcessAndProcessAndProcess965, sub_821B9AD0) based on the result. Part of the 0x82192 process cluster. 60 lines. 2 callees. 2 callers (sub_822C9378 and sub_82B69D00).
[entrypoint.functions."0x82192418"]
name = "ProcessAndProcessAndProcess1441_82192418"

# Process a structure by wrapping sub-operations (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and sixty-second form). It takes a structure, checks its state, and drives sub-operations (ProcessAndProcessAndProcess1046, ProcessAndProcessAndProcess897, ProcessAndProcessAndProcess940, sub_82286D00) based on the result. Part of the 0x82193 process cluster. 118 lines. 4 callees. 1 caller (sub_821B9AD0).
[entrypoint.functions."0x82193120"]
name = "ProcessAndProcessAndProcess1442_82193120"

# Process a large structure by incrementing a refcount atomically (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and sixty-third form). It takes a structure, increments a refcount atomically (IncrementRefcountAtomic), and drives sub-operations (ProcessAndProcessAndProcess524, sub_8225FB90, sub_82A96218, sub_82A96618) based on the result. Part of the 0x82197 process cluster. 257 lines. 5 callees. 1 caller (ProcessAndProcessAndProcess1444).
[entrypoint.functions."0x82197808"]
name = "ProcessAndProcessAndProcess1443_82197808"

# Process a large structure by freeing size-bucketed memory (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and sixty-fourth form). It takes a structure, frees size-bucketed memory (Free_SizeBucketed), and drives sub-operations (ProcessAndProcessAndProcess1071, ProcessAndProcessAndProcess1121, ProcessAndProcessAndProcess1440, ProcessAndProcessAndProcess1443, sub_8228A9A8, sub_822C41C0) based on the result. Part of the 0x82197 process cluster. 165 lines. 7 callees. 1 caller (ProcessAndProcessAndProcess1450).
[entrypoint.functions."0x821979D0"]
name = "ProcessAndProcessAndProcess1444_821979D0"

# Process a large data structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and sixty-fifth form). It takes a structure, checks its state, and drives sub-operations (ProcessAndProcessAndProcess1003, ProcessAndProcessAndProcess1010, ProcessAndProcessAndProcess551, ProcessAndProcessAndProcess653, ProcessAndProcessAndProcess955, ProcessAndProcessAndProcess956, ProcessAndProcessAndProcess1433, ProcessAndProcessAndProcess1434, sub_822C75A8, sub_82A647B0, sub_82A64958, sub_82A64A38, sub_82A64B78, sub_82A65070) based on the result. Part of the 0x82197 process cluster. 288 lines. 14 callees. 1 caller (sub_821C36E0).
[entrypoint.functions."0x82197D00"]
name = "ProcessAndProcessAndProcess1445_82197D00"

# Process a very large data structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and sixty-sixth form). It takes a structure, checks its state, and drives a sub-operation (ProcessAndProcessAndProcess510) based on the result. Part of the 0x8219B process cluster. 903 lines. 1 callee. 1 caller (ProcessAndProcessAndProcess1416).
[entrypoint.functions."0x8219B9B0"]
name = "ProcessAndProcessAndProcess1446_8219B9B0"

# Process a large data structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and sixty-seventh form). It takes a structure, checks its state, and drives sub-operations (ProcessAndProcessAndProcess603, ProcessAndProcessAndProcess940, sub_822A5F80) based on the result. Part of the 0x821A1 process cluster. 426 lines. 3 callees. 3 callers (sub_822872D0, sub_8228D838, sub_82B69D00).
[entrypoint.functions."0x821A17A8"]
name = "ProcessAndProcessAndProcess1447_821A17A8"

# Process a large structure by wrapping sub-operations (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and sixty-eighth form). It takes a structure, checks its state, and drives sub-operations (ProcessAndProcessAndProcess1051, ProcessAndProcessAndProcess1343, sub_8227F0B0) based on the result. Part of the 0x821A2 process cluster. 189 lines. 3 callees. 2 callers (sub_822A08C8 and sub_822C40C0).
[entrypoint.functions."0x821A2E00"]
name = "ProcessAndProcessAndProcess1448_821A2E00"

# Process an enormous data structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and sixty-ninth form). It takes a structure, checks its state, and drives many sub-operations (ProcessAndProcessAndProcess1064, ProcessAndProcessAndProcess501, ProcessAndProcessAndProcess510, ProcessAndProcessAndProcess525, ProcessAndProcessAndProcess549, ProcessAndProcessAndProcess551, ProcessAndProcessAndProcess552, ProcessAndProcessAndProcess555, ProcessAndProcessAndProcess556, ProcessAndProcessAndProcess572, ProcessAndProcessAndProcess619, ProcessAndProcessAndProcess834, ProcessVector2, sub_82189A40, sub_82198F98, sub_821E1C98, sub_8221E0F0, sub_82226D48) based on the result. This is the fifth-largest function in the recompiled binary, in the 0x821A3 cluster. 2794 lines. 18 callees. 0 callers.
[entrypoint.functions."0x821A3AE8"]
name = "ProcessAndProcessAndProcess1449_821A3AE8"

# Process a large structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and seventieth form). It takes a structure, checks its state, and drives sub-operations (sub_821973B0, ProcessAndProcessAndProcess1444, sub_8225FB90, sub_822A4C78, sub_82A46D90) based on the result. Part of the 0x821AD process cluster. 186 lines. 5 callees. 1 caller (sub_821C36E0).
[entrypoint.functions."0x821AD348"]
name = "ProcessAndProcessAndProcess1450_821AD348"

# Process a large structure by growing a buffer (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and seventy-first form). It takes a structure, grows a buffer (GrowBufferAndAppend), and drives sub-operations (ProcessAndProcessAndProcess776, ProcessAndProcessAndProcess905, sub_82A79528, sub_82A7A498) based on the result. Part of the 0x821AE process cluster. 148 lines. 5 callees. 1 caller (ProcessAndProcessAndProcess1459).
[entrypoint.functions."0x821AE598"]
name = "ProcessAndProcessAndProcess1451_821AE598"

# Process a structure by wrapping sub-operations (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and seventy-second form). It takes a structure, checks its state, and drives sub-operations (ProcessAndProcessAndProcess980, sub_82B27820) based on the result. Part of the 0x821B0 process cluster. 80 lines. 2 callees. 0 callers.
[entrypoint.functions."0x821B0370"]
name = "ProcessAndProcessAndProcess1452_821B0370"

# Process a structure by wrapping sub-operations (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and seventy-third form). It takes a structure, checks its state, and drives sub-operations (ProcessAndProcessAndProcess551, ProcessAndProcessAndProcess678, ProcessAndProcessAndProcess929, sub_82A65568, sub_82A66180) based on the result. Part of the 0x821B6 process cluster. 133 lines. 5 callees. 0 callers.
[entrypoint.functions."0x821B6CB0"]
name = "ProcessAndProcessAndProcess1453_821B6CB0"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and seventy-fourth form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x821B7 process cluster. 126 lines. 0 callees. 3 callers (sub_821C4AB0, sub_821CCF58, sub_82282938).
[entrypoint.functions."0x821B7268"]
name = "ProcessAndProcessAndProcess1454_821B7268"

# Process a structure by wrapping sub-operations (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and seventy-fifth form). It takes a structure, checks its state, and drives sub-operations (ProcessAndProcessAndProcess934, sub_8229AB10) based on the result. Part of the 0x821B9 process cluster. 84 lines. 2 callees. 1 caller (ProcessAndProcessAndProcess1459).
[entrypoint.functions."0x821B99A0"]
name = "ProcessAndProcessAndProcess1455_821B99A0"

# Process a large data structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and seventy-sixth form). It takes a structure, checks its state, and drives sub-operations (ProcessAndProcessAndProcess600, ProcessAndProcessAndProcess791, ProcessAndProcessAndProcess896, ProcessAndProcessAndProcess897, ProcessAndProcessAndProcess940, ProcessAndProcessAndProcess952, ProcessAndProcessAndProcess1442) based on the result. Part of the 0x821B9 process cluster. 404 lines. 7 callees. 1 caller (ProcessAndProcessAndProcess1441).
[entrypoint.functions."0x821B9AD0"]
name = "ProcessAndProcessAndProcess1456_821B9AD0"

# Process a very large data structure by constructing a 12-byte structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and seventy-seventh form). It takes a structure, constructs a 12-byte structure (Construct12Byte), increments a refcount atomically (IncrementRefcountAtomic), and drives sub-operations (ProcessAndProcessAndProcess524, ProcessAndProcessAndProcess676, ProcessAndProcessAndProcess677, sub_82A965C0) based on the result. Part of the 0x821BD process cluster. 502 lines. 6 callees. 1 caller (ProcessAndProcessAndProcess1458).
[entrypoint.functions."0x821BDB18"]
name = "ProcessAndProcessAndProcess1457_821BDB18"

# Process a large data structure by incrementing a refcount atomically (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and seventy-eighth form). It takes a structure, increments a refcount atomically (IncrementRefcountAtomic), and drives sub-operations (ProcessAndProcessAndProcess1187, ProcessAndProcessAndProcess1188, ProcessAndProcessAndProcess1411, ProcessAndProcessAndProcess1415, ProcessAndProcessAndProcess524, ProcessAndProcessAndProcess678, ProcessAndProcessAndProcess934, ProcessAndProcessAndProcess957, ProcessAndProcessAndProcess973, ProcessAndProcessAndProcess1457) based on the result. Part of the 0x821BD process cluster. 292 lines. 11 callees. 1 caller (ProcessAndProcessAndProcess1416).
[entrypoint.functions."0x821BDE80"]
name = "ProcessAndProcessAndProcess1458_821BDE80"

# Process a large data structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and seventy-ninth form). It takes a structure, checks its state, and drives sub-operations (ProcessAndProcessAndProcess1022, ProcessAndProcessAndProcess1176, ProcessAndProcessAndProcess1437, ProcessAndProcessAndProcess533, ProcessAndProcessAndProcess643, ProcessAndProcessAndProcess675, ProcessAndProcessAndProcess895, ProcessAndProcessAndProcess1445, ProcessAndProcessAndProcess1450, ProcessAndProcessAndProcess1451, ProcessAndProcessAndProcess1455, sub_82268C30, sub_8227B3C0, sub_8227D988, sub_82282938, sub_8228A660, sub_822961A0) based on the result. This is a dispatcher in the 0x821C3 cluster. 285 lines. 17 callees. 1 caller (ProcessAndProcessAndProcess1416).
[entrypoint.functions."0x821C36E0"]
name = "ProcessAndProcessAndProcess1459_821C36E0"

# Process a large data structure by copying memory (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and eightieth form). It takes a structure, copies memory (MemCpy), and drives sub-operations (ProcessAndProcessAndProcess1046, ProcessAndProcessAndProcess600, ProcessAndProcessAndProcess602, ProcessAndProcessAndProcess603, ProcessAndProcessAndProcess791, ProcessAndProcessAndProcess897, ProcessAndProcessAndProcess919, ProcessAndProcessAndProcess940, sub_822A5CA8) based on the result. Part of the 0x821C6 process cluster. 378 lines. 10 callees. 1 caller (sub_822AA5A0).
[entrypoint.functions."0x821C64E8"]
name = "ProcessAndProcessAndProcess1460_821C64E8"

# Process a large structure by wrapping sub-operations (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and eighty-first form). It takes a structure, checks its state, and drives sub-operations (ProcessAndProcessAndProcess1051, ProcessAndProcessAndProcess533, ProcessAndProcessAndProcess643, ProcessAndProcessAndProcess675, ProcessAndProcessAndProcess678, ProcessAndProcessAndProcess895, sub_8228A7A8) based on the result. Part of the 0x821C8 process cluster. 190 lines. 7 callees. 1 caller (ProcessAndProcessAndProcess1462).
[entrypoint.functions."0x821C8020"]
name = "ProcessAndProcessAndProcess1461_821C8020"

# Process a large data structure by constructing a refcounted object (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and eighty-second form). It takes a structure, constructs a refcounted object (ConstructRefCounted), grows a buffer (GrowBufferAndAppend), and drives many sub-operations (ProcessAndProcessAndProcess1021, ProcessAndProcessAndProcess1072, ProcessAndProcessAndProcess1168, ProcessAndProcessAndProcess1342, ProcessAndProcessAndProcess1387, ProcessAndProcessAndProcess1418, ProcessAndProcessAndProcess776, ProcessAndProcessAndProcess825, ProcessAndProcessAndProcess899, ProcessAndProcessAndProcess905, ProcessAndProcessAndProcess907, ProcessAndProcessAndProcess929, ProcessAndProcessAndProcess1461, sub_821D4028, sub_821E13B8, sub_822326B0, sub_82264828, sub_82267818, sub_82277240, sub_8227A300, sub_822C4288) based on the result. Part of the 0x821CB cluster. 338 lines. 23 callees. 1 caller (sub_8226E6E8).
[entrypoint.functions."0x821CB3A0"]
name = "ProcessAndProcessAndProcess1462_821CB3A0"

# Process a large data structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and eighty-third form). It takes a structure, checks its state, and drives sub-operations (ProcessAndProcessAndProcess791, ProcessAndProcessAndProcess905, ProcessAndProcessAndProcess940, sub_8217D050, ProcessAndProcessAndProcess1454, sub_822824B8, sub_822961A0, sub_822B7D08, sub_82A46D90, sub_82B16EF0, sub_82B9B5D0) based on the result. Part of the 0x821CC process cluster. 479 lines. 11 callees. 1 caller (ProcessAndProcessAndProcess927).
[entrypoint.functions."0x821CCF58"]
name = "ProcessAndProcessAndProcess1463_821CCF58"

# Process a large data structure by freeing size-bucketed memory (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and eighty-fourth form). It takes a structure, frees size-bucketed memory (Free_SizeBucketed), and drives sub-operations (sub_82A73AA8, sub_82AB2158) based on the result. Part of the 0x821D0 process cluster. 362 lines. 3 callees. 5 callers (sub_821BA578, sub_821D0860, sub_821DB590, ProcessAndProcessAndProcess1469, sub_822675D0).
[entrypoint.functions."0x821D08C0"]
name = "ProcessAndProcessAndProcess1464_821D08C0"

# Process a large structure by wrapping sub-operations (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and eighty-fifth form). It takes a structure, checks its state, and drives sub-operations (ProcessAndProcessAndProcess551, ProcessAndProcessAndProcess678, ProcessAndProcessAndProcess957, ProcessAndProcessAndProcess1469, sub_82276700) based on the result. Part of the 0x821D9 process cluster. 188 lines. 5 callees. 1 caller (sub_8226E6E8).
[entrypoint.functions."0x821D96E8"]
name = "ProcessAndProcessAndProcess1465_821D96E8"

# Process a structure by incrementing a refcount atomically (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and eighty-sixth form). It takes a structure, increments a refcount atomically (IncrementRefcountAtomic), and drives a sub-operation (ProcessAndProcessAndProcess792) based on the result. Part of the 0x821E1 process cluster. 50 lines. 2 callees. 5 callers (including ProcessAndProcessAndProcess1462).
[entrypoint.functions."0x821E13B8"]
name = "ProcessAndProcessAndProcess1466_821E13B8"

# Process a very large data structure by destroying a circular list (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and eighty-seventh form). It takes a structure, destroys a circular list (DestroyCircularList), sets an event (SetEvent), and drives sub-operations (sub_8218D9A8, sub_821AC058, sub_821D1C98, sub_821E1AA8, sub_821E1C98, sub_821E3070, sub_822955C0, sub_82A88E50) based on the result. Part of the 0x821E2 process cluster. 523 lines. 10 callees. 1 caller (ProcessAndProcessAndProcess1385).
[entrypoint.functions."0x821E2D40"]
name = "ProcessAndProcessAndProcess1467_821E2D40"

# Process a structure by growing a buffer (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and eighty-eighth form). It takes a structure, grows a buffer (GrowBufferAndAppend), and drives a sub-operation (ProcessAndProcessAndProcess776) based on the result. Part of the 0x821E9 process cluster. 99 lines. 2 callees. 0 callers.
[entrypoint.functions."0x821E9D18"]
name = "ProcessAndProcessAndProcess1468_821E9D18"

# Process a very large data structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and eighty-ninth form). It takes a structure, checks its state, and drives sub-operations (ProcessAndProcessAndProcess1021, ProcessAndProcessAndProcess818, sub_8217F038, ProcessAndProcessAndProcess1464) based on the result. Part of the 0x821EC process cluster. 915 lines. 4 callees. 1 caller (ProcessAndProcessAndProcess1465).
[entrypoint.functions."0x821EC008"]
name = "ProcessAndProcessAndProcess1469_821EC008"

# Process a large structure by growing a buffer (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and ninetieth form). It takes a structure, grows a buffer (GrowBufferAndAppend), and drives sub-operations (ProcessAndProcessAndProcess776, sub_821F4590) based on the result. Part of the 0x821F4 process cluster. 260 lines. 3 callees. 2 callers (including ProcessAndProcessAndProcess1385).
[entrypoint.functions."0x821F4448"]
name = "ProcessAndProcessAndProcess1470_821F4448"

# Process a structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and ninety-first form). It takes a structure and drives a sub-operation (ProcessAndProcessAndProcess940). Part of the 0x82221 process cluster. 165 lines. 1 callee. 5 callers (including ProcessAndProcessAndProcess927).
[entrypoint.functions."0x82221860"]
name = "ProcessAndProcessAndProcess1471_82221860"

# Process a structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and ninety-second form). It takes a structure and drives a sub-operation (ProcessAndProcessAndProcess940). Part of the 0x82223 process cluster. 165 lines. 1 callee. 4 callers (including ProcessAndProcessAndProcess927).
[entrypoint.functions."0x82223C70"]
name = "ProcessAndProcessAndProcess1472_82223C70"

# Process a structure by driving a state transition (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and ninety-third form). It takes a structure, checks its state, and drives sub-operations (ProcessAndProcessAndProcess805, ProcessAndProcessAndProcess806) based on the result. Part of the 0x82232 process cluster. 92 lines. 2 callees. 6 callers (including ProcessAndProcessAndProcess1462).
[entrypoint.functions."0x822326B0"]
name = "ProcessAndProcessAndProcess1473_822326B0"

# Process a large data structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and ninety-fourth form). It takes a structure, checks its state, and drives sub-operations (ProcessAndProcessAndProcess551, ProcessAndProcessAndProcess555, ProcessAndProcessAndProcess876, sub_821BC370, sub_821C4AB0, sub_822150E0, sub_82242720, sub_8226E048, sub_8228E688) based on the result. Part of the 0x82242 process cluster. 325 lines. 9 callees. 0 callers.
[entrypoint.functions."0x82242C18"]
name = "ProcessAndProcessAndProcess1474_82242C18"

# Process a large data structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and ninety-fifth form). It takes a structure, checks its state, and drives sub-operations (ProcessAndProcessAndProcess1026, ProcessAndProcessAndProcess1066, ProcessAndProcessAndProcess1155, ProcessAndProcessAndProcess1168, ProcessAndProcessAndProcess1402, ProcessAndProcessAndProcess1403, ProcessAndProcessAndProcess1462, ProcessAndProcessAndProcess609, ProcessAndProcessAndProcess677, ProcessAndProcessAndProcess678, ProcessAndProcessAndProcess899, ProcessAndProcessAndProcess957, ProcessAndProcessAndProcess1465, sub_821EFC78, sub_8226E130, sub_8229B7F0, sub_82A497A8) based on the result. Part of the 0x8226E process cluster. 331 lines. 17 callees. 1 caller (ProcessAndProcessAndProcess1416).
[entrypoint.functions."0x8226E6E8"]
name = "ProcessAndProcessAndProcess1475_8226E6E8"

# Process a very large data structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and ninety-sixth form). It takes a structure, checks its state, and drives sub-operations (ProcessAndProcessAndProcess531, ProcessAndProcessAndProcess549, ProcessAndProcessAndProcess551, ProcessAndProcessAndProcess552, sub_82273D90) based on the result. Part of the 0x82274 process cluster. 754 lines. 5 callees. 2 callers (including ProcessAndProcessAndProcess1479).
[entrypoint.functions."0x822742A0"]
name = "ProcessAndProcessAndProcess1476_822742A0"

# Process a large structure by decrementing a refcount atomically (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and ninety-seventh form). It takes a structure, decrements a refcount atomically (DecrementRefcountAtomic), and drives sub-operations (ProcessAndProcessAndProcess551, ProcessAndProcessAndProcess899, ProcessAndProcessAndProcess905, ProcessAndProcessAndProcess956, sub_82A83778, sub_82A83CB0, sub_82A84280) based on the result. Part of the 0x82275 process cluster. 279 lines. 8 callees. 1 caller (ProcessAndProcessAndProcess1481).
[entrypoint.functions."0x82275490"]
name = "ProcessAndProcessAndProcess1477_82275490"

# Process a very large data structure by constructing a refcounted object (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and ninety-eighth form). It takes a structure, constructs a refcounted object (ConstructRefCounted), destructs a refcounted object (DestructRefCounted), copies memory (MemCpy), and drives many sub-operations (ProcessAndProcessAndProcess1026, ProcessAndProcessAndProcess1051, ProcessAndProcessAndProcess1075, ProcessAndProcessAndProcess1077, ProcessAndProcessAndProcess1195, ProcessAndProcessAndProcess1322, ProcessAndProcessAndProcess504, ProcessAndProcessAndProcess533, ProcessAndProcessAndProcess551, ProcessAndProcessAndProcess643, ProcessAndProcessAndProcess825, ProcessAndProcessAndProcess885, ProcessAndProcessAndProcess905, ProcessAndProcessAndProcess959, sub_8217DD18, sub_82191C50, sub_821AD488, sub_821B6AC8, sub_821E5978, sub_8229F5A8, sub_822A22F0, sub_822AA2F0, sub_82A687B8) based on the result. Part of the 0x82276 process cluster. 809 lines. 26 callees. 1 caller (ProcessAndProcessAndProcess1465).
[entrypoint.functions."0x82276700"]
name = "ProcessAndProcessAndProcess1478_82276700"

# Process a very large data structure by incrementing a refcount atomically (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and ninety-ninth form). It takes a structure, increments a refcount atomically (IncrementRefcountAtomic), and drives many sub-operations (ProcessAndProcessAndProcess1021, ProcessAndProcessAndProcess1072, ProcessAndProcessAndProcess1167, ProcessAndProcessAndProcess533, ProcessAndProcessAndProcess551, ProcessAndProcessAndProcess643, ProcessAndProcessAndProcess653, ProcessAndProcessAndProcess675, ProcessAndProcessAndProcess678, ProcessAndProcessAndProcess792, ProcessAndProcessAndProcess805, ProcessAndProcessAndProcess806, ProcessAndProcessAndProcess907, ProcessAndProcessAndProcess955, ProcessAndProcessAndProcess956, sub_821D8310, ProcessAndProcessAndProcess1476, sub_822792D0) based on the result. Part of the 0x82277 process cluster. 877 lines. 19 callees. 1 caller (ProcessAndProcessAndProcess1462).
[entrypoint.functions."0x82277240"]
name = "ProcessAndProcessAndProcess1479_82277240"

# Process a large structure by wrapping sub-operations (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and one hundredth form). It takes a structure, checks its state, and drives sub-operations (ProcessAndProcessAndProcess533, ProcessAndProcessAndProcess643, ProcessAndProcessAndProcess675, ProcessAndProcessAndProcess895) based on the result. Part of the 0x8227A process cluster. 284 lines. 4 callees. 1 caller (ProcessAndProcessAndProcess1462).
[entrypoint.functions."0x8227A300"]
name = "ProcessAndProcessAndProcess1480_8227A300"

# Process a structure by wrapping sub-operations (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and one hundred and first form). It takes a structure, checks its state, and drives sub-operations (ProcessAndProcessAndProcess1022, ProcessAndProcessAndProcess1158, ProcessAndProcessAndProcess501, ProcessAndProcessAndProcess510, ProcessAndProcessAndProcess1477) based on the result. Part of the 0x8227D process cluster. 139 lines. 5 callees. 1 caller (ProcessAndProcessAndProcess1459).
[entrypoint.functions."0x8227D988"]
name = "ProcessAndProcessAndProcess1481_8227D988"

# Process a very large data structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and one hundred and second form). It takes a structure, checks its state, and drives sub-operations (ProcessAndProcessAndProcess1019, ProcessAndProcessAndProcess1046, ProcessAndProcessAndProcess603, ProcessAndProcessAndProcess791, ProcessAndProcessAndProcess806, ProcessAndProcessAndProcess897, ProcessAndProcessAndProcess940, ProcessAndProcessAndProcess952, ProcessAndProcessAndProcess1486) based on the result. Part of the 0x8227F process cluster. 697 lines. 9 callees. 3 callers (including ProcessAndProcessAndProcess1448).
[entrypoint.functions."0x8227F0B0"]
name = "ProcessAndProcessAndProcess1482_8227F0B0"

# Process a large structure by incrementing a refcount atomically (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and one hundred and third form). It takes a structure, increments a refcount atomically (IncrementRefcountAtomic), and drives sub-operations (ProcessAndProcessAndProcess1071, ProcessAndProcessAndProcess1158, ProcessAndProcessAndProcess1454, ProcessAndProcessAndProcess501, ProcessAndProcessAndProcess524, sub_8228D5C0) based on the result. Part of the 0x82282 process cluster. 213 lines. 7 callees. 1 caller (ProcessAndProcessAndProcess1459).
[entrypoint.functions."0x82282938"]
name = "ProcessAndProcessAndProcess1483_82282938"

# Process a structure by growing a buffer (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and one hundred and fourth form). It takes a structure, grows a buffer (GrowBufferAndAppend), and drives sub-operations (ProcessAndProcessAndProcess776, sub_821DD178, sub_821EFC78) based on the result. Part of the 0x82282 process cluster. 142 lines. 4 callees. 0 callers.
[entrypoint.functions."0x82282B58"]
name = "ProcessAndProcessAndProcess1484_82282B58"

# Process a structure by growing a buffer (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and one hundred and fifth form). It takes a structure, grows a buffer (GrowBufferAndAppend), and drives sub-operations (ProcessAndProcessAndProcess776, sub_821EFC78, sub_8226D8B8) based on the result. Part of the 0x82282 process cluster. 142 lines. 4 callees. 0 callers.
[entrypoint.functions."0x82282C48"]
name = "ProcessAndProcessAndProcess1485_82282C48"

# Process a large data structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and one hundred and sixth form). It takes a structure, checks its state, and drives sub-operations (ProcessAndProcessAndProcess1044, ProcessAndProcessAndProcess1045, ProcessAndProcessAndProcess529, ProcessAndProcessAndProcess896, ProcessAndProcessAndProcess926, ProcessAndProcessAndProcess952) based on the result. Part of the 0x82286 process cluster. 298 lines. 6 callees. 2 callers (including ProcessAndProcessAndProcess1442).
[entrypoint.functions."0x82286D00"]
name = "ProcessAndProcessAndProcess1486_82286D00"

# Process a large structure by incrementing a refcount atomically (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and ninety-seventh form). It takes a structure, increments a refcount atomically (IncrementRefcountAtomic), and drives sub-operations (ProcessAndProcessAndProcess1170, ProcessAndProcessAndProcess1255, ProcessAndProcessAndProcess1447, ProcessAndProcessAndProcess791, ProcessAndProcessAndProcess792, ProcessAndProcessAndProcess907, ProcessAndProcessAndProcess940, sub_82B9B5D0) based on the result. Part of the 0x82287 process cluster. 282 lines. 9 callees. 0 callers.
[entrypoint.functions."0x822872D0"]
name = "ProcessAndProcessAndProcess1487_822872D0"

# Process a structure by wrapping sub-operations (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and ninety-eighth form). It takes a structure, checks its state, and drives sub-operations (ProcessAndProcessAndProcess533, sub_82A488A0, sub_82A48AA0, sub_82A67A80, sub_82A67D38, sub_82A96DB0, sub_82AA65C8, sub_82AA8AD0) based on the result. Part of the 0x8228A process cluster. 148 lines. 8 callees. 1 caller (ProcessAndProcessAndProcess1416).
[entrypoint.functions."0x8228A518"]
name = "ProcessAndProcessAndProcess1488_8228A518"

# Process a large structure by decrementing a refcount atomically (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and ninety-ninth form). It takes a structure, decrements a refcount atomically (DecrementRefcountAtomic), and drives sub-operations (ProcessAndProcessAndProcess551, ProcessAndProcessAndProcess805, ProcessAndProcessAndProcess806, ProcessAndProcessAndProcess929, ProcessAndProcessAndProcess955, ProcessAndProcessAndProcess956, ProcessAndProcessAndProcess980, ProcessAndProcessAndProcess1492) based on the result. Part of the 0x8228A process cluster. 186 lines. 9 callees. 1 caller (ProcessAndProcessAndProcess1459).
[entrypoint.functions."0x8228A660"]
name = "ProcessAndProcessAndProcess1489_8228A660"

# Process a large structure by wrapping sub-operations (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and one hundredth form). It takes a structure, checks its state, and drives sub-operations (ProcessAndProcessAndProcess1167, ProcessAndProcessAndProcess551, ProcessAndProcessAndProcess653, ProcessAndProcessAndProcess805, ProcessAndProcessAndProcess806, ProcessAndProcessAndProcess929, ProcessAndProcessAndProcess955, ProcessAndProcessAndProcess956, ProcessAndProcessAndProcess1497) based on the result. Part of the 0x8228A process cluster. 194 lines. 9 callees. 1 caller (ProcessAndProcessAndProcess1461).
[entrypoint.functions."0x8228A7A8"]
name = "ProcessAndProcessAndProcess1490_8228A7A8"

# Process a large structure by allocating fresh memory (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and one hundred and first form). It takes a structure, allocates fresh memory (Allocate_OrDbgBreak), increments a refcount atomically (IncrementRefcountAtomic), and drives sub-operations (ProcessAndProcessAndProcess1071, ProcessAndProcessAndProcess524, sub_8225FB90, sub_82A96358) based on the result. Part of the 0x8228A process cluster. 256 lines. 6 callees. 1 caller (ProcessAndProcessAndProcess1444).
[entrypoint.functions."0x8228A9A8"]
name = "ProcessAndProcessAndProcess1491_8228A9A8"

# Process a very large structure by incrementing a refcount atomically (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and one hundred and second form). It takes a structure, increments a refcount atomically (IncrementRefcountAtomic), and drives sub-operations (ProcessAndProcessAndProcess1157, ProcessAndProcessAndProcess1255, ProcessAndProcessAndProcess533, ProcessAndProcessAndProcess792, ProcessAndProcessAndProcess907) based on the result. Part of the 0x8228F process cluster. 509 lines. 6 callees. 1 caller (ProcessAndProcessAndProcess1489).
[entrypoint.functions."0x8228F6D8"]
name = "ProcessAndProcessAndProcess1492_8228F6D8"

# Process a structure by processing a vector (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and one hundred and third form). It takes a structure, checks its state, and drives sub-operations (ProcessAndProcessAndProcess1158, ProcessAndProcessAndProcess501, ProcessAndProcessAndProcess510, ProcessVector2, sub_82281FB8) based on the result. Part of the 0x82296 process cluster. 173 lines. 5 callees. 2 callers (including ProcessAndProcessAndProcess1459).
[entrypoint.functions."0x822961A0"]
name = "ProcessAndProcessAndProcess1493_822961A0"

# Process a structure by wrapping sub-operations (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and one hundred and fourth form). It takes a structure, checks its state, and drives sub-operations (ProcessAndProcessAndProcess980, sub_82B275C8) based on the result. Part of the 0x8229A process cluster. 80 lines. 2 callees. 0 callers.
[entrypoint.functions."0x8229AA90"]
name = "ProcessAndProcessAndProcess1494_8229AA90"

# Process a structure by allocating fresh memory (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and one hundred and fifth form). It takes a structure, allocates fresh memory (Allocate_OrDbgBreak), frees size-bucketed memory (Free_SizeBucketed), and drives sub-operations (ProcessAndProcessAndProcess819, sub_82192640, sub_82AB08D0, sub_82AB0A68) based on the result. Part of the 0x8229A process cluster. 138 lines. 6 callees. 1 caller (ProcessAndProcessAndProcess1455).
[entrypoint.functions."0x8229AB10"]
name = "ProcessAndProcessAndProcess1495_8229AB10"

# Process a structure by allocating fresh memory (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and one hundred and sixth form). It takes a structure, allocates fresh memory (Allocate_OrDbgBreak), frees size-bucketed memory (Free_SizeBucketed), and drives sub-operations (sub_821AE470, sub_82AB08D0, sub_82AB0A68) based on the result. Part of the 0x8229B process cluster. 127 lines. 5 callees. 1 caller (ProcessAndProcessAndProcess1475).
[entrypoint.functions."0x8229B7F0"]
name = "ProcessAndProcessAndProcess1496_8229B7F0"

# Process a very large structure by incrementing a refcount atomically (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and one hundred and seventh form). It takes a structure, increments a refcount atomically (IncrementRefcountAtomic), and drives sub-operations (ProcessAndProcessAndProcess1072, ProcessAndProcessAndProcess792, ProcessAndProcessAndProcess907) based on the result. Part of the 0x822A0 process cluster. 641 lines. 4 callees. 1 caller (ProcessAndProcessAndProcess1490).
[entrypoint.functions."0x822A0298"]
name = "ProcessAndProcessAndProcess1497_822A0298"

# Process a structure by growing a buffer (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and one hundred and eighth form). It takes a structure, grows a buffer (GrowBufferAndAppend), and drives a sub-operation (ProcessAndProcessAndProcess776) based on the result. Part of the 0x822A0 process cluster. 99 lines. 2 callees. 0 callers.
[entrypoint.functions."0x822A0B88"]
name = "ProcessAndProcessAndProcess1498_822A0B88"

# Process a large structure by wrapping sub-operations (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and ninety-ninth form). It takes a structure, checks its state, and drives sub-operations (ProcessAndProcessAndProcess897, ProcessAndProcessAndProcess940) based on the result. Part of the 0x822A5 process cluster. 271 lines. 2 callees. 2 callers (including ProcessAndProcessAndProcess1460).
[entrypoint.functions."0x822A5CA8"]
name = "ProcessAndProcessAndProcess1499_822A5CA8"

# Process a very large data structure by copying memory (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and one hundredth form). It takes a structure, copies memory (MemCpy), and drives sub-operations (ProcessAndProcessAndProcess1046, ProcessAndProcessAndProcess600, ProcessAndProcessAndProcess602, ProcessAndProcessAndProcess603, ProcessAndProcessAndProcess764, ProcessAndProcessAndProcess897, ProcessAndProcessAndProcess919, ProcessAndProcessAndProcess940, ProcessAndProcessAndProcess1499, sub_822CA9B0) based on the result. Part of the 0x822A5 process cluster. 530 lines. 11 callees. 1 caller (ProcessAndProcessAndProcess1447).
[entrypoint.functions."0x822A5F80"]
name = "ProcessAndProcessAndProcess1500_822A5F80"

# Process a structure by wrapping sub-operations (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and one hundred and first form). It takes a structure, checks its state, and drives sub-operations (ProcessAndProcessAndProcess1170, ProcessAndProcessAndProcess1504) based on the result. Part of the 0x822AA process cluster. 61 lines. 2 callees. 0 callers.
[entrypoint.functions."0x822AA060"]
name = "ProcessAndProcessAndProcess1501_822AA060"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and one hundred and second form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x822AA process cluster. 32 lines. 0 callees. 0 callers.
[entrypoint.functions."0x822AA0C8"]
name = "ProcessAndProcessAndProcess1502_822AA0C8"

# Process a large structure by getting the timebase (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and one hundred and third form). It takes a structure, gets the timebase (GetTimebase), and drives a sub-operation (ProcessAndProcessAndProcess1505) based on the result. Part of the 0x822AA process cluster. 357 lines. 2 callees. 1 caller (ProcessAndProcessAndProcess1478).
[entrypoint.functions."0x822AA2F0"]
name = "ProcessAndProcessAndProcess1503_822AA2F0"

# Process a structure by wrapping sub-operations (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and one hundred and fourth form). It takes a structure, checks its state, and drives sub-operations (ProcessAndProcessAndProcess1460, ProcessAndProcessAndProcess555, ProcessAndProcessAndProcess965) based on the result. Part of the 0x822AA process cluster. 77 lines. 3 callees. 2 callers (ProcessAndProcessAndProcess1501 and sub_82B69D00).
[entrypoint.functions."0x822AA5A0"]
name = "ProcessAndProcessAndProcess1504_822AA5A0"

# Process a very large data structure by clearing memory (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and one hundred and fifth form). It takes a structure, clears its memory (MemSet), and drives sub-operations (ProcessAndProcessAndProcess1251, ProcessAndProcessAndProcess1407) based on the result. Part of the 0x822AA process cluster. 980 lines. 3 callees. 1 caller (ProcessAndProcessAndProcess1503).
[entrypoint.functions."0x822AA628"]
name = "ProcessAndProcessAndProcess1505_822AA628"

# Process a large data structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and one hundred and sixth form). It takes a structure, checks its state, and drives sub-operations (sub_82A880F0, sub_82A88438, sub_82A885C0) based on the result. Part of the 0x822B9 process cluster. 313 lines. 3 callees. 1 caller (ProcessAndProcessAndProcess1169).
[entrypoint.functions."0x822B9880"]
name = "ProcessAndProcessAndProcess1506_822B9880"

# Process a structure by wrapping sub-operations (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and one hundred and seventh form). It takes a structure, checks its state, and drives sub-operations (ProcessAndProcessAndProcess1448, ProcessAndProcessAndProcess805, ProcessAndProcessAndProcess806) based on the result. Part of the 0x822C4 process cluster. 106 lines. 3 callees. 0 callers.
[entrypoint.functions."0x822C40C0"]
name = "ProcessAndProcessAndProcess1507_822C40C0"

# Process a structure by allocating checked memory (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and one hundred and eighth form). It takes a structure, allocates checked memory (AllocateChecked3), and frees size-bucketed memory (Free_SizeBucketed). Part of the 0x822C4 process cluster. 119 lines. 2 callees. 1 caller (ProcessAndProcessAndProcess1444).
[entrypoint.functions."0x822C41C0"]
name = "ProcessAndProcessAndProcess1508_822C41C0"

# Process a very large data structure by incrementing a refcount atomically (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and one hundred and ninth form). It takes a structure, increments a refcount atomically (IncrementRefcountAtomic), and drives sub-operations (ProcessAndProcessAndProcess1072, ProcessAndProcessAndProcess551, ProcessAndProcessAndProcess653, ProcessAndProcessAndProcess792, ProcessAndProcessAndProcess805, ProcessAndProcessAndProcess806, ProcessAndProcessAndProcess823, ProcessAndProcessAndProcess905, ProcessAndProcessAndProcess907, ProcessAndProcessAndProcess955, ProcessAndProcessAndProcess956) based on the result. Part of the 0x822C4 process cluster. 946 lines. 12 callees. 1 caller (ProcessAndProcessAndProcess1462).
[entrypoint.functions."0x822C4288"]
name = "ProcessAndProcessAndProcess1509_822C4288"

# Process a large structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and one hundred and tenth form). It takes a structure and drives a sub-operation (ProcessAndProcessAndProcess1411) based on the result. Part of the 0x822C5 process cluster. 181 lines. 1 callee. 1 caller (ProcessAndProcessAndProcess1161).
[entrypoint.functions."0x822C50F0"]
name = "ProcessAndProcessAndProcess1510_822C50F0"

# Process a large structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and eleventh form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x822C7 process cluster. 256 lines. 0 callees. 1 caller (ProcessAndProcessAndProcess1445).
[entrypoint.functions."0x822C75A8"]
name = "ProcessAndProcessAndProcess1511_822C75A8"

# Process a structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and twelfth form). It takes a structure and drives a sub-operation (ProcessAndProcessAndProcess1441). Part of the 0x822C9 process cluster. 7 lines. 1 callee. 0 callers.
[entrypoint.functions."0x822C9378"]
name = "ProcessAndProcessAndProcess1512_822C9378"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and thirteenth form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x822CA process cluster. 169 lines. 0 callees. 1 caller (ProcessAndProcessAndProcess1500).
[entrypoint.functions."0x822CA9B0"]
name = "ProcessAndProcessAndProcess1513_822CA9B0"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and fourteenth form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82A1B process cluster. 79 lines. 0 callees. 2 callers (including ProcessAndProcessAndProcess1516).
[entrypoint.functions."0x82A1B3E0"]
name = "ProcessAndProcessAndProcess1514_82A1B3E0"

# Process a structure by driving a state transition (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and fifteenth form). It takes a structure, checks its state, and drives sub-operations (ProcessAndProcessAndProcess805, ProcessAndProcessAndProcess806, ProcessAndProcessAndProcess940) based on the result. Part of the 0x82A6C process cluster. 190 lines. 3 callees. 1 caller (ProcessAndProcessAndProcess928).
[entrypoint.functions."0x82A6CAD8"]
name = "ProcessAndProcessAndProcess1515_82A6CAD8"

# Process a large data structure by normalizing a vector (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and sixteenth form). It takes a structure, normalizes a vector (NormalizeVector), processes a vector (ProcessVector2), and drives sub-operations (ProcessAndProcessAndProcess904, ProcessAndProcessAndProcess1514) based on the result. This is a vector processor in the 0x82A83 cluster. 402 lines. 4 callees. 1 caller (ProcessAndProcessAndProcess1477).
[entrypoint.functions."0x82A83778"]
name = "ProcessAndProcessAndProcess1516_82A83778"

# Process a large data structure by incrementing a refcount atomically (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and seventeenth form). It takes a structure, increments a refcount atomically (IncrementRefcountAtomic), swaps a refcount (SwapRefcount), and drives sub-operations (ProcessAndProcessAndProcess1024, ProcessAndProcessAndProcess1157, ProcessAndProcessAndProcess524, ProcessAndProcessAndProcess643, ProcessAndProcessAndProcess675, ProcessAndProcessAndProcess772, ProcessAndProcessAndProcess811, ProcessAndProcessAndProcess899, ProcessAndProcessAndProcess905, ProcessAndProcessAndProcess955, ProcessAndProcessAndProcess1518, ProcessAndProcessAndProcess1521) based on the result. Part of the 0x82A84 cluster. 404 lines. 14 callees. 1 caller (ProcessAndProcessAndProcess1477).
[entrypoint.functions."0x82A84280"]
name = "ProcessAndProcessAndProcess1517_82A84280"

# Process a large structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and eighteenth form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82A84 cluster. 322 lines. 0 callees. 3 callers (including ProcessAndProcessAndProcess1517).
[entrypoint.functions."0x82A84608"]
name = "ProcessAndProcessAndProcess1518_82A84608"

# Process a large data structure by allocating fresh memory (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and nineteenth form). It takes a structure, allocates fresh memory (Allocate_OrDbgBreak), performs a checked copy (CheckedCopy), frees size-bucketed memory (Free_SizeBucketed), sets a string (SetString), and drives sub-operations (sub_822F1F00, sub_826C3EF0, sub_826C3FA8) based on the result. This is a string processor in the 0x82A96 cluster. 358 lines. 7 callees. 4 callers.
[entrypoint.functions."0x82A96358"]
name = "ProcessAndProcessAndProcess1519_82A96358"

# Process a structure by constructing a refcounted object (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and twentieth form). It takes a structure, constructs a refcounted object (ConstructRefCounted), destructs a refcounted object (DestructRefCounted), and drives sub-operations (ProcessAndProcessAndProcess1472, ProcessAndProcessAndProcess791, sub_82B15E20, sub_82B9B5D0) based on the result. Part of the 0x82B16 process cluster. 208 lines. 6 callees. 0 callers.
[entrypoint.functions."0x82B16250"]
name = "ProcessAndProcessAndProcess1520_82B16250"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and twenty-first form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82B18 process cluster. 186 lines. 0 callees. 1 caller (ProcessAndProcessAndProcess1517).
[entrypoint.functions."0x82B18C18"]
name = "ProcessAndProcessAndProcess1521_82B18C18"

# Process a large structure by getting the timebase (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and twenty-second form). It takes a structure, gets the timebase (GetTimebase), grows a buffer (GrowBufferAndAppend), and drives sub-operations (ProcessAndProcessAndProcess1155, ProcessAndProcessAndProcess1405, ProcessAndProcessAndProcess776, sub_82A60B10) based on the result. Part of the 0x82B2F process cluster. 196 lines. 6 callees. 0 callers.
[entrypoint.functions."0x82B2F860"]
name = "ProcessAndProcessAndProcess1522_82B2F860"

# Process a very large data structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and twenty-third form). It takes a structure, checks its state, and drives sub-operations (ProcessAndProcessAndProcess551, ProcessAndProcessAndProcess621, ProcessAndProcessAndProcess823, ProcessAndProcessAndProcess905) based on the result. Part of the 0x82B2F process cluster. 859 lines. 4 callees. 0 callers.
[entrypoint.functions."0x82B2F9B0"]
name = "ProcessAndProcessAndProcess1523_82B2F9B0"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and twenty-fourth form). It takes a structure and checks its state, with no external sub-operations. Part of the UI text-rendering cluster in the 0x82CD9 region. 120 lines. 0 callees. 0 callers.
[entrypoint.functions."0x82CD9AF0"]
name = "ProcessAndProcessAndProcess1524_82CD9AF0"

# Process a very large data structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and twenty-fifth form). It takes a structure, checks its state, and drives sub-operations (ProcessAndProcessAndProcess1435, ProcessAndProcessAndProcess504) based on the result. Part of the 0x8217D process cluster. 874 lines. 2 callees. 1 caller (ProcessAndProcessAndProcess1478).
[entrypoint.functions."0x8217DD18"]
name = "ProcessAndProcessAndProcess1525_8217DD18"

# Process a large data structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and twenty-sixth form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82191 process cluster. 499 lines. 0 callees. 1 caller (ProcessAndProcessAndProcess1478).
[entrypoint.functions."0x82191C50"]
name = "ProcessAndProcessAndProcess1526_82191C50"

# Process a structure by wrapping sub-operations (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and twenty-seventh form). It takes a structure, checks its state, and drives sub-operations (ProcessAndProcessAndProcess562, ProcessAndProcessAndProcess564) based on the result. Part of the 0x821A4 process cluster. 102 lines. 2 callees. 0 callers.
[entrypoint.functions."0x821A4EA8"]
name = "ProcessAndProcessAndProcess1527_821A4EA8"

# Process a structure by constructing a refcounted object (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and twenty-eighth form). It takes a structure, constructs a refcounted object (ConstructRefCounted), destructs a refcounted object (DestructRefCounted), and drives sub-operations (ProcessAndProcessAndProcess885, ProcessAndProcessAndProcess959) based on the result. Part of the 0x821AD process cluster. 85 lines. 4 callees. 1 caller (ProcessAndProcessAndProcess1478).
[entrypoint.functions."0x821AD488"]
name = "ProcessAndProcessAndProcess1528_821AD488"

# Process a large structure by constructing a refcounted object (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and twenty-ninth form). It takes a structure, constructs a refcounted object (ConstructRefCounted), destructs a refcounted object (DestructRefCounted), and drives sub-operations (ProcessAndProcessAndProcess852, ProcessAndProcessAndProcess885) based on the result. Part of the 0x821B6 process cluster. 249 lines. 4 callees. 1 caller (sub_8229F5A8).
[entrypoint.functions."0x821B6870"]
name = "ProcessAndProcessAndProcess1529_821B6870"

# Process a structure by constructing a refcounted object (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and thirtieth form). It takes a structure, constructs a refcounted object (ConstructRefCounted), destructs a refcounted object (DestructRefCounted), and drives sub-operations (ProcessAndProcessAndProcess1026, ProcessAndProcessAndProcess825, ProcessAndProcessAndProcess885, ProcessAndProcessAndProcess959) based on the result. Part of the 0x821B6 process cluster. 180 lines. 6 callees. 1 caller (ProcessAndProcessAndProcess1478).
[entrypoint.functions."0x821B6AC8"]
name = "ProcessAndProcessAndProcess1530_821B6AC8"

# Process a structure by wrapping sub-operations (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and thirty-first form). It takes a structure, checks its state, and drives sub-operations (ProcessAndProcessAndProcess1022, ProcessAndProcessAndProcess1178, ProcessAndProcessAndProcess1179, ProcessAndProcessAndProcess1184, ProcessAndProcessAndProcess1186, ProcessAndProcessAndProcess1417, sub_82275238) based on the result. Part of the 0x821BA process cluster. 148 lines. 7 callees. 0 callers.
[entrypoint.functions."0x821BA1D0"]
name = "ProcessAndProcessAndProcess1531_821BA1D0"

# Process a structure by wrapping sub-operations (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and thirty-second form). It takes a structure, checks its state, and drives sub-operations (ProcessAndProcessAndProcess1076, ProcessAndProcessAndProcess515, ProcessAndProcessAndProcess549, ProcessAndProcessAndProcess865, ProcessAndProcessAndProcess884) based on the result. Part of the 0x821BD process cluster. 151 lines. 5 callees. 0 callers.
[entrypoint.functions."0x821BD770"]
name = "ProcessAndProcessAndProcess1532_821BD770"

# Process a very large data structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and thirty-third form). It takes a structure, checks its state, and drives sub-operations (ProcessAndProcessAndProcess1026, ProcessAndProcessAndProcess1076, ProcessAndProcessAndProcess510, ProcessAndProcessAndProcess515, ProcessAndProcessAndProcess525, ProcessAndProcessAndProcess533, ProcessAndProcessAndProcess549, ProcessAndProcessAndProcess552, ProcessAndProcessAndProcess619, ProcessAndProcessAndProcess620, ProcessAndProcessAndProcess790, ProcessAndProcessAndProcess825, ProcessAndProcessAndProcess875, ProcessAndProcessAndProcess905) based on the result. Part of the 0x821E5 process cluster. 1336 lines. 14 callees. 1 caller (ProcessAndProcessAndProcess1478).
[entrypoint.functions."0x821E5978"]
name = "ProcessAndProcessAndProcess1533_821E5978"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and thirty-fourth form). It takes a structure and checks its state, with no external sub-operations. This is a frequently-called helper in the 0x821EF process cluster. 68 lines. 0 callees. 5 callers.
[entrypoint.functions."0x821EFC78"]
name = "ProcessAndProcessAndProcess1534_821EFC78"

# Process a very large data structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and thirty-fifth form). It takes a structure, checks its state, and drives sub-operations (ProcessAndProcessAndProcess1171, ProcessAndProcessAndProcess510, ProcessAndProcessAndProcess533, ProcessAndProcessAndProcess549, ProcessAndProcessAndProcess551, ProcessAndProcessAndProcess552, ProcessAndProcessAndProcess619, ProcessAndProcessAndProcess803, ProcessAndProcessAndProcess804) based on the result. Part of the 0x8226E process cluster. 805 lines. 9 callees. 1 caller (ProcessAndProcessAndProcess1475).
[entrypoint.functions."0x8226E130"]
name = "ProcessAndProcessAndProcess1535_8226E130"

# Process a structure by freeing size-bucketed memory (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and thirty-sixth form). It takes a structure and frees size-bucketed memory (Free_SizeBucketed). This is a frequently-called helper in the 0x82275 process cluster. 76 lines. 1 callee. 11 callers.
[entrypoint.functions."0x82275238"]
name = "ProcessAndProcessAndProcess1536_82275238"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and thirty-seventh form). It takes a structure and checks its state, with no external sub-operations. This is a frequently-called helper in the 0x82295 process cluster. 35 lines. 0 callees. 63 callers.
[entrypoint.functions."0x822955C0"]
name = "ProcessAndProcessAndProcess1537_822955C0"

# Process a structure by wrapping sub-operations (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and thirty-eighth form). It takes a structure, checks its state, and drives sub-operations (ProcessAndProcessAndProcess1187, ProcessAndProcessAndProcess1188, ProcessAndProcessAndProcess929) based on the result. Part of the 0x8229B process cluster. 48 lines. 3 callees. 0 callers.
[entrypoint.functions."0x8229B700"]
name = "ProcessAndProcessAndProcess1538_8229B700"

# Process a large structure by wrapping sub-operations (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and thirty-ninth form). It takes a structure, checks its state, and drives sub-operations (ProcessAndProcessAndProcess1195, ProcessAndProcessAndProcess533, ProcessAndProcessAndProcess1529, sub_82A68688, sub_82A68720) based on the result. Part of the 0x8229F process cluster. 188 lines. 5 callees. 1 caller (ProcessAndProcessAndProcess1478).
[entrypoint.functions."0x8229F5A8"]
name = "ProcessAndProcessAndProcess1539_8229F5A8"

# Process a large structure by constructing a refcounted object (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and fortieth form). It takes a structure, constructs a refcounted object (ConstructRefCounted), destructs a refcounted object (DestructRefCounted), and drives sub-operations (ProcessAndProcessAndProcess1078, ProcessAndProcessAndProcess533, ProcessAndProcessAndProcess643, ProcessAndProcessAndProcess675, ProcessAndProcessAndProcess885, ProcessAndProcessAndProcess895, ProcessAndProcessAndProcess959) based on the result. Part of the 0x822A2 process cluster. 237 lines. 9 callees. 1 caller (ProcessAndProcessAndProcess1478).
[entrypoint.functions."0x822A22F0"]
name = "ProcessAndProcessAndProcess1540_822A22F0"

# Process a structure by wrapping sub-operations (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and forty-first form). It takes a structure, checks its state, and drives sub-operations (ProcessAndProcessAndProcess551, ProcessAndProcessAndProcess929) based on the result. Part of the 0x822A8 process cluster. 177 lines. 2 callees. 0 callers.
[entrypoint.functions."0x822A8D28"]
name = "ProcessAndProcessAndProcess1541_822A8D28"

# Process a structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and forty-second form). It takes a structure and drives a sub-operation (ProcessAndProcessAndProcess1176). Part of the 0x822C4 process cluster. 7 lines. 1 callee. 0 callers.
[entrypoint.functions."0x822C4990"]
name = "ProcessAndProcessAndProcess1542_822C4990"

# Process a structure by wrapping sub-operations (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and forty-third form). It takes a structure, checks its state, and drives sub-operations (ProcessAndProcessAndProcess819, ProcessAndProcessAndProcess1544) based on the result. Part of the 0x822C4 process cluster. 53 lines. 2 callees. 0 callers.
[entrypoint.functions."0x822C4998"]
name = "ProcessAndProcessAndProcess1543_822C4998"

# Process a large structure by comparing strings case-insensitively (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and forty-fourth form). It takes a structure, compares strings case-insensitively (StrCmpIgnoreCase), and drives sub-operations (ProcessAndProcessAndProcess1215, ProcessAndProcessAndProcess1399) based on the result. Part of the 0x822C4 process cluster. 159 lines. 3 callees. 1 caller (ProcessAndProcessAndProcess1543).
[entrypoint.functions."0x822C4F30"]
name = "ProcessAndProcessAndProcess1544_822C4F30"

# Process a structure by entering a critical section (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and forty-fifth form). It takes a structure, enters or tries a critical section (CriticalSection_EnterOrTry), waits on a mutex (MutexLock_WaitInfinite), and drives a sub-operation (ProcessAndProcessAndProcess621) based on the result. Part of the 0x82A49 process cluster. 130 lines. 3 callees. 1 caller (ProcessAndProcessAndProcess1475).
[entrypoint.functions."0x82A497A8"]
name = "ProcessAndProcessAndProcess1545_82A497A8"

# Process a structure by constructing a refcounted object (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and forty-sixth form). It takes a structure, constructs a refcounted object (ConstructRefCounted), destructs a refcounted object (DestructRefCounted), and drives sub-operations (ProcessAndProcessAndProcess885, ProcessAndProcessAndProcess959) based on the result. Part of the 0x82A68 process cluster. 98 lines. 4 callees. 2 callers (including ProcessAndProcessAndProcess1539).
[entrypoint.functions."0x82A68688"]
name = "ProcessAndProcessAndProcess1546_82A68688"

# Process a structure by constructing a refcounted object (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and forty-seventh form). It takes a structure, constructs a refcounted object (ConstructRefCounted), destructs a refcounted object (DestructRefCounted), and drives sub-operations (ProcessAndProcessAndProcess885, ProcessAndProcessAndProcess959) based on the result. Part of the 0x82A68 process cluster. 98 lines. 4 callees. 2 callers (including ProcessAndProcessAndProcess1539).
[entrypoint.functions."0x82A68720"]
name = "ProcessAndProcessAndProcess1547_82A68720"

# Process a structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and forty-eighth form). It takes a structure and drives a sub-operation (ProcessAndProcessAndProcess510). Part of the 0x82B60 process cluster. 55 lines. 1 callee. 1 caller (ProcessAndProcessAndProcess1065).
[entrypoint.functions."0x82B60D10"]
name = "ProcessAndProcessAndProcess1548_82B60D10"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and forty-ninth form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82BFC process cluster. 58 lines. 0 callees. 2 callers (sub_829F7B08 and sub_82C0FFF8).
[entrypoint.functions."0x82BFCB68"]
name = "ProcessAndProcessAndProcess1549_82BFCB68"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and fiftieth form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82BFC process cluster. 19 lines. 0 callees. 2 callers (sub_829F7B08 and sub_82C0FFF8).
[entrypoint.functions."0x82BFCBD8"]
name = "ProcessAndProcessAndProcess1550_82BFCBD8"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and fifty-first form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82A45 process cluster. 8 lines. 0 callees. 0 callers.
[entrypoint.functions."0x82A45DA0"]
name = "ProcessAndProcessAndProcess1551_82A45DA0"

# Process a structure by wrapping sub-operations (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and fifty-second form). It takes a structure, checks its state, and drives sub-operations (ProcessAndProcessAndProcess1042, UIText_FrameRenderIter, sub_82C03ED0) based on the result. Part of the UI text-rendering cluster in the 0x82BFD region. 59 lines. 3 callees. 0 callers.
[entrypoint.functions."0x82BFD7F0"]
name = "ProcessAndProcessAndProcess1552_82BFD7F0"

# Process a structure by getting a global field (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and fifty-third form). It takes a structure, gets a global field (GetGlobalField19764), and drives a sub-operation (ProcessAndProcessAndProcess1208) based on the result. Part of the UI text-rendering cluster in the 0x82BFF region. 27 lines. 2 callees. 1 caller (UIText_RenderCurrentObject).
[entrypoint.functions."0x82BFFB10"]
name = "ProcessAndProcessAndProcess1553_82BFFB10"

# Process a structure by getting a global field (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and fifty-fourth form). It takes a structure, gets a global field (GetGlobalField19764), and drives a sub-operation (sub_82C144A0) based on the result. Part of the UI text-rendering cluster in the 0x82BFF region. 27 lines. 2 callees. 1 caller (UIText_RenderCurrentObject).
[entrypoint.functions."0x82BFFB88"]
name = "ProcessAndProcessAndProcess1554_82BFFB88"

# Process a structure by getting a global field (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and fifty-fifth form). It takes a structure, gets a global field (GetGlobalField19764), and drives a sub-operation (sub_82C13038) based on the result. Part of the UI text-rendering cluster in the 0x82BFF region. 80 lines. 2 callees. 1 caller (sub_82C0FFF8).
[entrypoint.functions."0x82BFFC68"]
name = "ProcessAndProcessAndProcess1555_82BFFC68"

# Process a structure by getting a global field (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and fifty-sixth form). It takes a structure, gets a global field (GetGlobalField19764), and drives a sub-operation (sub_82C13060) based on the result. Part of the UI text-rendering cluster in the 0x82BFF region. 35 lines. 2 callees. 1 caller (sub_82C0B6A8).
[entrypoint.functions."0x82BFFD88"]
name = "ProcessAndProcessAndProcess1556_82BFFD88"

# Process a structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and fifty-seventh form). It takes a structure and drives a sub-operation (sub_82C0FFF8). Part of the UI text-rendering cluster in the 0x82C00 region. 9 lines. 1 callee. 0 callers.
[entrypoint.functions."0x82C000B0"]
name = "ProcessAndProcessAndProcess1557_82C000B0"

# Process a structure by wrapping sub-operations (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and fifty-eighth form). It takes a structure, checks its state, and drives sub-operations (sub_82C043D0, sub_82C485E8, sub_82C48B28) based on the result. Part of the UI text-rendering cluster in the 0x82C04 region. 124 lines. 3 callees. 2 callers (sub_82C0B6A8 and sub_82C346F0).
[entrypoint.functions."0x82C04A88"]
name = "ProcessAndProcessAndProcess1558_82C04A88"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and fifty-ninth form). It takes a structure and checks its state, with no external sub-operations. Part of the UI text-rendering cluster in the 0x82C09 region. 81 lines. 0 callees. 1 caller (UIText_RenderCurrentObject).
[entrypoint.functions."0x82C098E0"]
name = "ProcessAndProcessAndProcess1559_82C098E0"

# Process a large structure by finding a chain root (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and sixtieth form). It takes a structure, finds a chain root (FindChainRoot), and checks its state. Part of the UI text-rendering cluster in the 0x82C09 region. 288 lines. 1 callee. 1 caller (UIText_RenderCurrentObject).
[entrypoint.functions."0x82C09968"]
name = "ProcessAndProcessAndProcess1560_82C09968"

# Process a large structure by finding a chain root (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and sixty-first form). It takes a structure, finds a chain root (FindChainRoot), and drives a sub-operation (sub_82BFC278) based on the result. Part of the UI text-rendering cluster in the 0x82C09 region. 166 lines. 2 callees. 1 caller (UIText_RenderCurrentObject).
[entrypoint.functions."0x82C09DD8"]
name = "ProcessAndProcessAndProcess1561_82C09DD8"

# Process a structure by wrapping sub-operations (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and sixty-second form). It takes a structure, checks its state, and drives sub-operations (ProcessAndProcessAndProcess1042, ProcessAndProcessAndProcess1556, ProcessAndProcessAndProcess1558) based on the result. Part of the UI text-rendering cluster in the 0x82C0B region. 122 lines. 3 callees. 1 caller (ProcessAndProcessAndProcess1563).
[entrypoint.functions."0x82C0B6A8"]
name = "ProcessAndProcessAndProcess1562_82C0B6A8"

# Process a large data structure by wrapping sub-operations (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and sixty-third form). It takes a structure, checks its state, and drives sub-operations (ProcessAndProcessAndProcess681, ProcessAndProcessAndProcess682, ProcessAndProcessAndProcess869, ProcessAndProcessAndProcess961, ProcessAndValidateAndProcess3, ProcessEvent_Thunk, ProcessAndProcessAndProcess1549, ProcessAndProcessAndProcess1550, ProcessAndProcessAndProcess1555, ProcessAndProcessAndProcess1562, sub_82C0B9C8) based on the result. This is a UI-text dispatcher in the 0x82C0F region. 418 lines. 11 callees. 3 callers.
[entrypoint.functions."0x82C0FFF8"]
name = "ProcessAndProcessAndProcess1563_82C0FFF8"

# Process a structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and sixty-fourth form). It takes a structure and drives a sub-operation (ProcessAndProcessAndProcess1569). Part of the UI text-rendering cluster in the 0x82C13 region. 7 lines. 1 callee. 1 caller (ProcessAndProcessAndProcess1555).
[entrypoint.functions."0x82C13038"]
name = "ProcessAndProcessAndProcess1564_82C13038"

# Process a structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and sixty-fifth form). It takes a structure and drives a sub-operation (sub_82C4D8C8). Part of the UI text-rendering cluster in the 0x82C13 region. 9 lines. 1 callee. 1 caller (ProcessAndProcessAndProcess1556).
[entrypoint.functions."0x82C13060"]
name = "ProcessAndProcessAndProcess1565_82C13060"

# Process a structure by wrapping sub-operations (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and sixty-sixth form). It takes a structure, checks its state, and drives sub-operations (ProcessAndProcessAndProcess1208, ProcessAndProcessAndProcess1570, sub_82C4D998) based on the result. Part of the UI text-rendering cluster in the 0x82C14 region. 46 lines. 3 callees. 1 caller (ProcessAndProcessAndProcess1554).
[entrypoint.functions."0x82C144A0"]
name = "ProcessAndProcessAndProcess1566_82C144A0"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and sixty-seventh form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82C4B process cluster. 16 lines. 0 callees. 0 callers.
[entrypoint.functions."0x82C4BF80"]
name = "ProcessAndProcessAndProcess1567_82C4BF80"

# Process a structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and sixty-eighth form). It takes a structure and drives a sub-operation (ProcessAndProcessAndProcess964). Part of the 0x82C4C process cluster. 41 lines. 1 callee. 0 callers.
[entrypoint.functions."0x82C4C630"]
name = "ProcessAndProcessAndProcess1568_82C4C630"

# Process a large structure by calling methods (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and sixty-ninth form). It takes a structure, calls methods (ProcessAndCallMethods), and checks a method (ProcessAndCheckMethod) based on the result. Part of the 0x82C4C process cluster. 202 lines. 2 callees. 1 caller (ProcessAndProcessAndProcess1564).
[entrypoint.functions."0x82C4CA38"]
name = "ProcessAndProcessAndProcess1569_82C4CA38"

# Process a structure by calling methods (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and seventieth form). It takes a structure, calls methods (ProcessAndCallMethods), and checks a method (ProcessAndCheckMethod) based on the result. Part of the 0x82C4D process cluster. 69 lines. 2 callees. 1 caller (ProcessAndProcessAndProcess1566).
[entrypoint.functions."0x82C4D6A0"]
name = "ProcessAndProcessAndProcess1570_82C4D6A0"

# Process a structure by calling methods (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and seventy-first form). It takes a structure, calls methods (ProcessAndCallMethods), and checks a method (ProcessAndCheckMethod) based on the result. Part of the 0x82C4D process cluster. 60 lines. 2 callees. 1 caller (ProcessAndProcessAndProcess1565).
[entrypoint.functions."0x82C4D8C8"]
name = "ProcessAndProcessAndProcess1571_82C4D8C8"

# Process a structure by calling methods (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and seventy-second form). It takes a structure, calls methods (ProcessAndCallMethods), and checks a method (ProcessAndCheckMethod) based on the result. Part of the 0x82C4D process cluster. 82 lines. 2 callees. 1 caller (ProcessAndProcessAndProcess1566).
[entrypoint.functions."0x82C4D998"]
name = "ProcessAndProcessAndProcess1572_82C4D998"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and seventy-third form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82C55 process cluster. 8 lines. 0 callees. 0 callers.
[entrypoint.functions."0x82C55308"]
name = "ProcessAndProcessAndProcess1573_82C55308"

# Process a very large data structure by copying memory (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and seventy-fourth form). It takes a structure, copies memory (MemCpy_SdkRuntime), and drives a sub-operation (sub_82CDD6D0) based on the result. Part of the 0x82CD4 process cluster. 552 lines. 2 callees. 0 callers.
[entrypoint.functions."0x82CD4658"]
name = "ProcessAndProcessAndProcess1574_82CD4658"

# Process a structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and seventy-fifth form). It takes a structure and drives a sub-operation (ProcessAndProcessAndProcess964). Part of the 0x82C4C process cluster. 41 lines. 1 callee. 0 callers.
[entrypoint.functions."0x82C4C780"]
name = "ProcessAndProcessAndProcess1575_82C4C780"

# Process a structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and seventy-sixth form). It takes a structure and drives a sub-operation (ProcessAndProcessAndProcess964). Part of the 0x82C4C process cluster. 134 lines. 1 callee. 0 callers.
[entrypoint.functions."0x82C4C7C0"]
name = "ProcessAndProcessAndProcess1576_82C4C7C0"

# Process a large structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and seventy-seventh form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82C51 process cluster. 126 lines. 0 callees. 0 callers.
[entrypoint.functions."0x82C51C08"]
name = "ProcessAndProcessAndProcess1577_82C51C08"

# Process a structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and seventy-eighth form). It takes a structure and drives a sub-operation (ProcessAndProcessAndProcess1580). Part of the 0x82C51 process cluster. 9 lines. 1 callee. 0 callers.
[entrypoint.functions."0x82C51DA0"]
name = "ProcessAndProcessAndProcess1578_82C51DA0"

# Process a large structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and seventy-ninth form). It takes a structure and drives a sub-operation (ProcessAndProcessAndProcess1581). Part of the 0x82C56 process cluster. 164 lines. 1 callee. 2 callers (sub_82C566A0 and ProcessAndProcessAndProcess1580).
[entrypoint.functions."0x82C56590"]
name = "ProcessAndProcessAndProcess1579_82C56590"

# Process a large structure by finding a tree leaf (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and eightieth form). It takes a structure, finds a tree leaf (FindTreeLeaf), and drives sub-operations (ProcessAndProcessAndProcess1406, ProcessAndProcessAndProcess1579) based on the result. Part of the 0x82C56 process cluster. 227 lines. 3 callees. 1 caller (ProcessAndProcessAndProcess1578).
[entrypoint.functions."0x82C568A0"]
name = "ProcessAndProcessAndProcess1580_82C568A0"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and eighty-first form). It takes a structure and checks its state, with no external sub-operations. This is a frequently-called helper in the 0x82CD2 process cluster. 54 lines. 0 callees. 5 callers.
[entrypoint.functions."0x82CD2B88"]
name = "ProcessAndProcessAndProcess1581_82CD2B88"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and eighty-second form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82CE5 process cluster. 88 lines. 0 callees. 0 callers.
[entrypoint.functions."0x82CE5410"]
name = "ProcessAndProcessAndProcess1582_82CE5410"

# Process a large data structure by computing a special function (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and eighty-third form). It takes a structure, computes a special function (ComputeSpecialFunction), and drives a sub-operation (ProcessAndProcessAndProcess1584) based on the result. Part of the 0x82A67 process cluster. 314 lines. 2 callees. 0 callers.
[entrypoint.functions."0x82A674C8"]
name = "ProcessAndProcessAndProcess1583_82A674C8"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and eighty-fourth form). It takes a structure and checks its state, with no external sub-operations. This is a frequently-called helper in the 0x82B16 process cluster. 68 lines. 0 callees. 3 callers.
[entrypoint.functions."0x82B16EF0"]
name = "ProcessAndProcessAndProcess1584_82B16EF0"

# Process a very large data structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and eighty-fifth form). It takes a structure, checks its state, and drives sub-operations (ProcessAndProcessAndProcess1048, ProcessAndProcessAndProcess857, ProcessAndProcessAndProcess987) based on the result. This is a frequently-called processor in the 0x82293 process cluster. 546 lines. 3 callees. 10 callers.
[entrypoint.functions."0x82293968"]
name = "ProcessAndProcessAndProcess1585_82293968"

# Process a structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and eighty-sixth form). It takes a structure and drives a sub-operation (ProcessAndProcessAndProcess1344). Part of the UI text-rendering cluster in the 0x82C04 region. 36 lines. 1 callee. 2 callers (sub_82C2F3C8 and ProcessAndProcessAndProcess1591).
[entrypoint.functions."0x82C04248"]
name = "ProcessAndProcessAndProcess1586_82C04248"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and eighty-seventh form). It takes a structure and checks its state, with no external sub-operations. Part of the UI text-rendering cluster in the 0x82C09 region. 60 lines. 0 callees. 0 callers.
[entrypoint.functions."0x82C090E0"]
name = "ProcessAndProcessAndProcess1587_82C090E0"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and eighty-eighth form). It takes a structure and checks its state, with no external sub-operations. Part of the UI text-rendering cluster in the 0x82C0C region. 14 lines. 0 callees. 0 callers.
[entrypoint.functions."0x82C0C990"]
name = "ProcessAndProcessAndProcess1588_82C0C990"

# Process a structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and eighty-ninth form). It takes a structure and drives a sub-operation (sub_82C4D0F0). Part of the UI text-rendering cluster in the 0x82C13 region. 57 lines. 1 callee. 0 callers.
[entrypoint.functions."0x82C133D0"]
name = "ProcessAndProcessAndProcess1589_82C133D0"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and ninetieth form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82C4E process cluster. 118 lines. 0 callees. 2 callers (ProcessAndProcessAndProcess1591 and sub_82C4F240).
[entrypoint.functions."0x82C4E7D0"]
name = "ProcessAndProcessAndProcess1590_82C4E7D0"

# Process a large data structure by wrapping sub-operations (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and ninety-first form). It takes a structure, checks its state, and drives sub-operations (ProcessAndProcessAndProcess1586, sub_82C4E550, ProcessAndProcessAndProcess1590, sub_82C4EB08, sub_82C4ECB0, ProcessAndProcessAndProcess1592) based on the result. Part of the 0x82C4F process cluster. 258 lines. 6 callees. 0 callers.
[entrypoint.functions."0x82C4F080"]
name = "ProcessAndProcessAndProcess1591_82C4F080"

# Process a structure by wrapping sub-operations (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and ninety-second form). It takes a structure, checks its state, and drives sub-operations (sub_82C50860, sub_82C508C0, sub_82C50938) based on the result. Part of the 0x82C50 process cluster. 70 lines. 3 callees. 4 callers.
[entrypoint.functions."0x82C509A8"]
name = "ProcessAndProcessAndProcess1592_82C509A8"

# Process a very large data structure by inserting into a tree (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and ninety-third form). It takes a structure, allocates fresh memory (Allocate_OrDbgBreak), finds a tree leaf (FindTreeLeaf), inserts into a tree (Tree_Insert), and drives sub-operations (ProcessAndProcessAndProcess516, ProcessAndProcessAndProcess517, ProcessAndProcessAndProcess808, ProcessAndProcessAndProcess1594, sub_8321E160, sub_83221D10, sub_83222238, sub_83228F28) based on the result. This is a tree-insertion processor in the 0x829FF cluster. 581 lines. 11 callees. 0 callers.
[entrypoint.functions."0x829FF798"]
name = "ProcessAndProcessAndProcess1593_829FF798"

# Process a very large data structure by computing a special function (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and ninety-fourth form). It takes a structure, computes a special function (ComputeSpecialFunction), normalizes a vector (NormalizeVector), and drives sub-operations (ComputeSpecialFunction2, ProcessAndProcessAndProcess802) based on the result. Part of the 0x8321D process cluster. 397 lines. 4 callees. 1 caller (ProcessAndProcessAndProcess1593).
[entrypoint.functions."0x8321DDB8"]
name = "ProcessAndProcessAndProcess1594_8321DDB8"

# Process a large data structure by zeroing memory (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and ninety-fifth form). It takes a structure, zeroes memory (MemSet_SdkRuntime), and checks its state. Part of the 0x832B7 process cluster. 441 lines. 1 callee. 1 caller (sub_832B7620).
[entrypoint.functions."0x832B7318"]
name = "ProcessAndProcessAndProcess1595_832B7318"

# Process a large structure by wrapping sub-operations (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and ninety-sixth form). It takes a structure, checks its state, and drives sub-operations (sub_832BD9B0, sub_832BDBA0, ProcessAndProcessAndProcess1597, ProcessAndProcessAndProcess1602) based on the result. Part of the 0x832BD process cluster. 309 lines. 4 callees. 1 caller (sub_832B7620).
[entrypoint.functions."0x832BD7B0"]
name = "ProcessAndProcessAndProcess1596_832BD7B0"

# Process a very large data structure by wrapping sub-operations (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and ninety-seventh form). It takes a structure, checks its state, and drives sub-operations (ProcessAndProcessAndProcess1096, ProcessAndProcessAndProcess1097, ProcessAndProcessAndProcess639, ProcessAndProcessAndProcess879, ProcessAndProcessAndProcess1598, ProcessAndProcessAndProcess1599, ProcessAndProcessAndProcess1600, sub_832C0440) based on the result. This is a dispatcher in the 0x832BD process cluster. 557 lines. 8 callees. 1 caller (ProcessAndProcessAndProcess1596).
[entrypoint.functions."0x832BDC90"]
name = "ProcessAndProcessAndProcess1597_832BDC90"

# Process a very large data structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and ninety-eighth form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x832BD process cluster. 812 lines. 0 callees. 1 caller (ProcessAndProcessAndProcess1597).
[entrypoint.functions."0x832BDFD8"]
name = "ProcessAndProcessAndProcess1598_832BDFD8"

# Process a very large data structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and ninety-ninth form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x832BE process cluster. 1052 lines. 0 callees. 1 caller (ProcessAndProcessAndProcess1597).
[entrypoint.functions."0x832BE4F8"]
name = "ProcessAndProcessAndProcess1599_832BE4F8"

# Process a structure by wrapping sub-operations (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and one hundredth form). It takes a structure, checks its state, and drives sub-operations (ProcessAndProcessAndProcess1096, ProcessAndProcessAndProcess963, ProcessAndProcessAndProcess1601) based on the result. Part of the 0x832BE process cluster. 124 lines. 3 callees. 2 callers (ProcessAndProcessAndProcess1096 and ProcessAndProcessAndProcess1597).
[entrypoint.functions."0x832BECC0"]
name = "ProcessAndProcessAndProcess1600_832BECC0"

# Process a large structure by wrapping sub-operations (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and one hundred and first form). It takes a structure, checks its state, and drives sub-operations (ProcessAndProcessAndProcess1006, ProcessAndProcessAndProcess853, ProcessAndProcessAndProcess879, ProcessAndProcessAndProcess963) based on the result. Part of the 0x832BE process cluster. 339 lines. 4 callees. 1 caller (ProcessAndProcessAndProcess1600).
[entrypoint.functions."0x832BEFC0"]
name = "ProcessAndProcessAndProcess1601_832BEFC0"

# Process a large structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and one hundred and second form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x832C0 process cluster. 309 lines. 0 callees. 1 caller (ProcessAndProcessAndProcess1596).
[entrypoint.functions."0x832C0798"]
name = "ProcessAndProcessAndProcess1602_832C0798"

# Process a structure by wrapping sub-operations (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and one hundred and third form). It takes a structure, checks its state, and drives sub-operations (sub_82CE69B8, sub_82CEBC18) based on the result. Part of the 0x82CF4 process cluster. 123 lines. 2 callees. 0 callers.
[entrypoint.functions."0x82CF47D8"]
name = "ProcessAndProcessAndProcess1603_82CF47D8"

# Process a structure by wrapping sub-operations (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and one hundred and fourth form). It takes a structure, checks its state, and drives sub-operations (ProcessAndProcessAndProcess1218, ProcessAndProcessAndProcess503, ProcessAndProcessAndProcess560) based on the result. This is a frequently-called helper in the 0x821A5 process cluster. 76 lines. 3 callees. 19 callers.
[entrypoint.functions."0x821A5840"]
name = "ProcessAndProcessAndProcess1604_821A5840"

# Process a very large data structure by wrapping sub-operations (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and one hundred and fifth form). It takes a structure, checks its state, and drives sub-operations (ProcessAndProcessAndProcess1224, ProcessAndProcessAndProcess1225, ProcessAndProcessAndProcess857, ProcessAndProcessAndProcess1606) based on the result. Part of the 0x821A5 process cluster. 404 lines. 4 callees. 2 callers (sub_821DBDD0 and sub_8238DE58).
[entrypoint.functions."0x821A5D80"]
name = "ProcessAndProcessAndProcess1605_821A5D80"

# Process a structure by normalizing a vector (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and one hundred and sixth form). It takes a structure and normalizes a vector (NormalizeVector). This is a frequently-called helper in the 0x822B5 process cluster. 73 lines. 1 callee. 3 callers.
[entrypoint.functions."0x822B5130"]
name = "ProcessAndProcessAndProcess1606_822B5130"

# Process a structure by releasing and processing (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and one hundred and seventh form). It takes a structure, releases and processes (ProcessAndReleaseAndProcess), and resets fields (ProcessAndResetFields). This is a frequently-called helper in the 0x823FB process cluster. 109 lines. 2 callees. 8 callers.
[entrypoint.functions."0x823FBF88"]
name = "ProcessAndProcessAndProcess1607_823FBF88"

# Process a structure by copying memory (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and one hundred and eighth form). It takes a structure and copies memory (MemCpy_SdkRuntime). Part of the 0x82171 process cluster. 51 lines. 1 callee. 0 callers.
[entrypoint.functions."0x82171A50"]
name = "ProcessAndProcessAndProcess1608_82171A50"

# Process a structure by destroying a circular list (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and one hundred and ninth form). It takes a structure, destroys a circular list (DestroyCircularList), and drives sub-operations (ProcessAndProcessAndProcess503, ProcessAndProcessAndProcess560) based on the result. This is a frequently-called helper in the 0x82190 process cluster. 113 lines. 3 callees. 4 callers.
[entrypoint.functions."0x82190118"]
name = "ProcessAndProcessAndProcess1609_82190118"

# Process a very large data structure by computing a special function (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and one hundred and tenth form). It takes a structure, computes a special function (ComputeSpecialFunction2), enters or tries a critical section (CriticalSection_EnterOrTry), and drives sub-operations (ProcessAndProcessAndProcess1215, ProcessAndProcessAndProcess1224, ProcessAndProcessAndProcess640, ProcessAndProcessAndProcess969, ProcessAndProcessAndProcess1605, sub_821DCF10, sub_821DF990, sub_821F8760, sub_822A88C0) based on the result. Part of the 0x821DB process cluster. 552 lines. 11 callees. 2 callers (sub_82278C90 and sub_82355460).
[entrypoint.functions."0x821DBDD0"]
name = "ProcessAndProcessAndProcess1610_821DBDD0"

# Process a structure by wrapping sub-operations (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and one hundred and eleventh form). It takes a structure, checks its state, and drives sub-operations (ProcessAndProcessAndProcess1612, ProcessAndProcessAndProcess1613) based on the result. Part of the 0x822BE process cluster. 128 lines. 2 callees. 0 callers.
[entrypoint.functions."0x822BE580"]
name = "ProcessAndProcessAndProcess1611_822BE580"

# Process a large structure by wrapping an indexed region (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and one hundred and twelfth form). It takes a structure, wraps an indexed region (ProcessAndIndexedRegion2), and drives sub-operations (sub_82BBECF0, sub_82BBEDE0) based on the result. Part of the 0x822D0 process cluster. 185 lines. 3 callees. 2 callers (ProcessAndProcessAndProcess1611 and sub_82BBE220).
[entrypoint.functions."0x822D0488"]
name = "ProcessAndProcessAndProcess1612_822D0488"

# Process a large structure by copying and zeroing memory (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and one hundred and thirteenth form). It takes a structure, copies memory (MemCpy_SdkRuntime), and zeroes memory (MemSet_SdkRuntime). Part of the 0x822D0 process cluster. 318 lines. 2 callees. 1 caller (ProcessAndProcessAndProcess1611).
[entrypoint.functions."0x822D06E8"]
name = "ProcessAndProcessAndProcess1613_822D06E8"

# Process a very large data structure by wrapping sub-operations (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and one hundred and fourteenth form). It takes a structure, constructs a 12-byte object (Construct12Byte), constructs a refcounted object (ConstructRefCounted), destructs a refcounted object (DestructRefCounted), dispatches and processes (ProcessAndDispatchAndProcess25), and drives sub-operations (ProcessAndProcessAndProcess1095, ProcessAndProcessAndProcess1100, ProcessAndProcessAndProcess1105, ProcessAndProcessAndProcess1609, sub_8219CEF8, sub_8219D000, sub_822BE890, sub_823F9140, ProcessAndProcessAndProcess1607, sub_8259D7C0, sub_8322B050, sub_8322E3C0) based on the result. This is a dispatcher in the 0x82379 process cluster. 466 lines. 16 callees. 0 callers.
[entrypoint.functions."0x82379C10"]
name = "ProcessAndProcessAndProcess1614_82379C10"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and one hundred and fifteenth form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x826C5 process cluster. 145 lines. 0 callees. 0 callers.
[entrypoint.functions."0x826C5A88"]
name = "ProcessAndProcessAndProcess1615_826C5A88"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and one hundred and sixteenth form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x822CD process cluster. 66 lines. 0 callees. 0 callers.
[entrypoint.functions."0x822CD918"]
name = "ProcessAndProcessAndProcess1616_822CD918"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and one hundred and seventeenth form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x822D1 process cluster. 15 lines. 0 callees. 0 callers.
[entrypoint.functions."0x822D13F8"]
name = "ProcessAndProcessAndProcess1617_822D13F8"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and one hundred and eighteenth form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82178 process cluster. 15 lines. 0 callees. 0 callers.
[entrypoint.functions."0x821781C8"]
name = "ProcessAndProcessAndProcess1618_821781C8"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and one hundred and nineteenth form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82185 process cluster. 72 lines. 0 callees. 0 callers.
[entrypoint.functions."0x82185428"]
name = "ProcessAndProcessAndProcess1619_82185428"

# Process a structure by wrapping sub-operations (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and one hundred and twentieth form). It takes a structure, checks its state, and drives sub-operations (ProcessAndProcessAndProcess1095, ProcessAndProcessAndProcess1630, sub_8322B050) based on the result. Part of the 0x8219C process cluster. 154 lines. 3 callees. 2 callers (sub_82295F28 and ProcessAndProcessAndProcess1614).
[entrypoint.functions."0x8219CEF8"]
name = "ProcessAndProcessAndProcess1620_8219CEF8"

# Process a structure by allocating memory (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and one hundred and twenty-first form). It takes a structure, allocates fresh memory (Allocate_OrDbgBreak), and drives a sub-operation (ProcessAndProcessAndProcess1120) based on the result. This is a very frequently-called helper in the 0x82214 process cluster. 96 lines. 2 callees. 555 callers.
[entrypoint.functions."0x822149C8"]
name = "ProcessAndProcessAndProcess1621_822149C8"

# Process a large structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and one hundred and twenty-second form). It takes a structure and drives a sub-operation (sub_82BE4350). This is a frequently-called helper in the 0x822BE process cluster. 178 lines. 1 callee. 7 callers.
[entrypoint.functions."0x822BE890"]
name = "ProcessAndProcessAndProcess1622_822BE890"

# Process a very large data structure by constructing a refcounted object (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and one hundred and twenty-third form). It takes a structure, constructs a refcounted object (ConstructRefCounted), copy-assigns a refcounted object (CopyAssign_RefCounted), copies a structure (CopyStructure), destructs a refcounted object (DestructRefCounted), constructs a string (ProcessConstructString), lowercases (ProcessToLower), releases a global reference (ReleaseGlobalRef), and drives sub-operations (ProcessAndProcessAndProcess1217, ProcessAndProcessAndProcess1244, ProcessAndProcessAndProcess565, ProcessAndProcessAndProcess765, sub_82301D40, sub_823F8C30, sub_826CAD28, ProcessAndProcessAndProcess1624) based on the result. Part of the 0x826CB process cluster. 727 lines. 15 callees. 2 callers (sub_826C63D8 and sub_826C6F28).
[entrypoint.functions."0x826CB338"]
name = "ProcessAndProcessAndProcess1623_826CB338"

# Process a structure by constructing a 12-byte object (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and one hundred and twenty-fourth form). It takes a structure and constructs a 12-byte object (Construct12Byte). Part of the 0x826CB process cluster. 95 lines. 1 callee. 2 callers (sub_826C9460 and ProcessAndProcessAndProcess1623).
[entrypoint.functions."0x826CBC78"]
name = "ProcessAndProcessAndProcess1624_826CBC78"

# Process a structure by destroying tree nodes (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and one hundred and twenty-fifth form). It takes a structure and destroys tree nodes (DestroyTreeNodes). Part of the 0x829FD process cluster. 54 lines. 1 callee. 2 callers (sub_829FEC30 and ProcessAndProcessAndProcess1627).
[entrypoint.functions."0x829FDAE8"]
name = "ProcessAndProcessAndProcess1625_829FDAE8"

# Process a structure by wrapping sub-operations (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and one hundred and twenty-sixth form). It takes a structure, checks its state, and drives sub-operations (ProcessAndProcessAndProcess1120, sub_8232D310, sub_82A86B88) based on the result. Part of the 0x8322D process cluster. 115 lines. 3 callees. 2 callers (sub_8322D670 and ProcessAndProcessAndProcess1629).
[entrypoint.functions."0x8322D5A8"]
name = "ProcessAndProcessAndProcess1626_8322D5A8"

# Process a very large data structure by allocating and freeing memory (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and one hundred and twenty-seventh form). It takes a structure, allocates fresh memory (Allocate_OrDbgBreak), frees size-bucketed memory (Free_SizeBucketed), releases a refcounted object (Release_RefCounted), and drives sub-operations (ProcessAndAllocateAndProcess2, ProcessAndProcessAndProcess703, sub_8217F978, sub_822B7258, sub_829FD450, sub_829FD878, sub_829FD940, ProcessAndProcessAndProcess1625, sub_829FF3E8, sub_8322CE18) based on the result. Part of the 0x8322D process cluster. 707 lines. 13 callees. 2 callers (sub_8322DF98 and ProcessAndProcessAndProcess1629).
[entrypoint.functions."0x8322DAC0"]
name = "ProcessAndProcessAndProcess1627_8322DAC0"

# Process a large structure by releasing a refcounted object (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and one hundred and twenty-eighth form). It takes a structure, releases a refcounted object (Release_RefCounted), and drives sub-operations (ProcessAndProcessAndProcess1095, ProcessAndProcessAndProcess699, sub_83222610) based on the result. Part of the 0x8322E process cluster. 267 lines. 4 callees. 1 caller (ProcessAndProcessAndProcess1629).
[entrypoint.functions."0x8322E208"]
name = "ProcessAndProcessAndProcess1628_8322E208"

# Process a large structure by wrapping sub-operations (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and one hundred and twenty-ninth form). It takes a structure, processes list elements (ProcessListElements), releases a refcounted object (Release_RefCounted), and drives sub-operations (sub_8322CF48, ProcessAndProcessAndProcess1626, ProcessAndProcessAndProcess1627, ProcessAndProcessAndProcess1628) based on the result. This is a dispatcher in the 0x8322E process cluster. 218 lines. 6 callees. 7 callers.
[entrypoint.functions."0x8322E3C0"]
name = "ProcessAndProcessAndProcess1629_8322E3C0"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and one hundred and thirtieth form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x8219D process cluster. 45 lines. 0 callees. 2 callers (ProcessAndProcessAndProcess1620 and ProcessAndProcessAndProcess1614).
[entrypoint.functions."0x8219D000"]
name = "ProcessAndProcessAndProcess1630_8219D000"

# Process a structure by computing a special function (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and one hundred and thirty-first form). It takes a structure and computes a special function (ComputeSpecialFunction). Part of the 0x8322A process cluster. 70 lines. 2 callees. 1 caller (ProcessAndProcessAndProcess1632).
[entrypoint.functions."0x8322AF80"]
name = "ProcessAndProcessAndProcess1631_8322AF80"

# Process a structure by wrapping sub-operations (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and one hundred and thirty-second form). It takes a structure, checks its state, and drives sub-operations (ProcessAndProcessAndProcess1225, ProcessAndProcessAndProcess857, ProcessAndProcessAndProcess1631) based on the result. Part of the 0x8322B process cluster. 90 lines. 3 callees. 3 callers.
[entrypoint.functions."0x8322B050"]
name = "ProcessAndProcessAndProcess1632_8322B050"

# Process a structure by copy-assigning a refcounted object (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and one hundred and thirty-third form). It takes a structure, copy-assigns a refcounted object (CopyAssign_RefCounted), destructs a refcounted object (DestructRefCounted), increments a global counter (IncrementGlobalCounter2), and drives sub-operations (ProcessAndProcessAndProcess1228, ProcessAndProcessAndProcess1315, ProcessAndProcessAndProcess1634) based on the result. This is a frequently-called helper in the 0x82301 process cluster. 114 lines. 6 callees. 12 callers.
[entrypoint.functions."0x82301FF8"]
name = "ProcessAndProcessAndProcess1633_82301FF8"

# Process a large structure by constructing a refcounted object (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and one hundred and thirty-fourth form). It takes a structure, constructs a refcounted object (ConstructRefCounted), copy-assigns a refcounted object (CopyAssign_RefCounted), and drives a sub-operation (sub_8222C210) based on the result. Part of the 0x82303 process cluster. 289 lines. 3 callees. 1 caller (ProcessAndProcessAndProcess1633).
[entrypoint.functions."0x82303898"]
name = "ProcessAndProcessAndProcess1634_82303898"

# Process a large structure by freeing size-bucketed memory (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and one hundred and thirty-fifth form). It takes a structure, frees size-bucketed memory (Free_SizeBucketed), and drives sub-operations (ProcessAndProcessAndProcess1255, ProcessAndProcessAndProcess1265, sub_8236CBB8, sub_82A3B848, sub_82A3BC58, sub_82B68F60) based on the result. Part of the 0x8236D process cluster. 166 lines. 7 callees. 2 callers (sub_823784A0 and sub_826C63D8).
[entrypoint.functions."0x8236D0A8"]
name = "ProcessAndProcessAndProcess1635_8236D0A8"

# Process a large data structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and one hundred and thirty-sixth form). It takes a structure and drives a sub-operation (ProcessAndProcessAndProcess763). Part of the 0x82CE6 process cluster. 359 lines. 1 callee. 1 caller (ProcessAndProcessAndProcess1603).
[entrypoint.functions."0x82CE69B8"]
name = "ProcessAndProcessAndProcess1636_82CE69B8"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and one hundred and thirty-seventh form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82B4E process cluster. 15 lines. 0 callees. 0 callers.
[entrypoint.functions."0x82B4EEE0"]
name = "ProcessAndProcessAndProcess1637_82B4EEE0"

# Process a large structure by wrapping an element (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and one hundred and thirty-eighth form). It takes a structure, accesses an element (ElementAccess), dispatches by type (ProcessAndDispatchByType2), reads or resolves a value (ProcessAndReadOrResolveValue), stores an element (ProcessAndStoreElement2), stores an indexed value (ProcessAndStoreIndexedValue), and drives a sub-operation (sub_82BC68F0) based on the result. Part of the 0x82BDC process cluster. 206 lines. 6 callees. 0 callers.
[entrypoint.functions."0x82BDC380"]
name = "ProcessAndProcessAndProcess1638_82BDC380"

# Process a large structure by hashing a cstring (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and one hundred and thirty-ninth form). It takes a structure, gets a typed field (GetTypedField), hashes a cstring (HashCstring), stores an element (ProcessAndStoreElement), and drives sub-operations (ProcessLoopDispatch, sub_82BC68F0, sub_82BCBDC8) based on the result. Part of the 0x82BB7 process cluster. 212 lines. 6 callees. 0 callers.
[entrypoint.functions."0x82BB7590"]
name = "ProcessAndProcessAndProcess1639_82BB7590"

# Process a large structure by wrapping sub-operations (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and one hundred and fortieth form). It takes a structure, checks its state, and drives sub-operations (ProcessAndProcessAndProcess1042, ProcessAndProcessAndProcess1333, ProcessAndVcallAndProcess10, sub_82C49228) based on the result. Part of the UI text-rendering cluster in the 0x82C08 region. 158 lines. 4 callees. 1 caller (ProcessAndProcessAndProcess1384).
[entrypoint.functions."0x82C08F08"]
name = "ProcessAndProcessAndProcess1640_82C08F08"

# Process a structure by wrapping sub-operations (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and one hundred and forty-first form). It takes a structure, checks its state, and drives sub-operations (ProcessAndProcessAndProcess925, sub_82BFF768, sub_82BFF7D8, sub_82BFF810) based on the result. Part of the 0x82C47 process cluster. 157 lines. 4 callees. 3 callers.
[entrypoint.functions."0x82C47700"]
name = "ProcessAndProcessAndProcess1641_82C47700"

# Process a structure by freeing size-bucketed memory (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and one hundred and forty-second form). It takes a structure and frees size-bucketed memory (Free_SizeBucketed). This is a very frequently-called helper in the 0x82A66 process cluster. 106 lines. 1 callee. 52 callers.
[entrypoint.functions."0x82A666A8"]
name = "ProcessAndProcessAndProcess1642_82A666A8"

# Process a structure by performing a checked copy (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and one hundred and forty-third form). It takes a structure and performs a checked copy (CheckedCopy). This is a frequently-called helper in the 0x822F6 process cluster. 64 lines. 1 callee. 5 callers.
[entrypoint.functions."0x822F6928"]
name = "ProcessAndProcessAndProcess1643_822F6928"

# Process a very large data structure by freeing size-bucketed memory (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and one hundred and forty-fourth form). It takes a structure, frees size-bucketed memory (Free_SizeBucketed), moves memory (MemMove_SdkRuntime), sets an event (SetEvent_SdkRuntime), and drives sub-operations (ProcessAndProcessAndProcess1100, SwitchDispatch, sub_82B57010) based on the result. Part of the 0x82B55 process cluster. 678 lines. 6 callees. 0 callers.
[entrypoint.functions."0x82B55F78"]
name = "ProcessAndProcessAndProcess1644_82B55F78"

# Process a structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and one hundred and forty-fifth form). It takes a structure and drives a sub-operation (sub_82CC1C18). Part of the 0x82CC1 process cluster. 87 lines. 1 callee. 2 callers (sub_82B56918 and sub_82B57010).
[entrypoint.functions."0x82CC1578"]
name = "ProcessAndProcessAndProcess1645_82CC1578"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and one hundred and forty-sixth form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82CC9 process cluster. 63 lines. 0 callees. 0 callers.
[entrypoint.functions."0x82CC9F00"]
name = "ProcessAndProcessAndProcess1646_82CC9F00"

# Process a large structure by allocating size-bucketed memory (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and one hundred and forty-seventh form). It takes a structure, allocates size-bucketed memory (Allocate_SizeBucketed), and drives sub-operations (ProcessAndProcessAndProcess275, sub_82CBBB58) based on the result. Part of the 0x82B3D process cluster. 157 lines. 3 callees. 3 callers (ProcessAndProcessAndProcess1098, sub_82B45278, sub_82B46378).
[entrypoint.functions."0x82B3DE68"]
name = "ProcessAndProcessAndProcess1647_82B3DE68"

# Process a structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and one hundred and forty-eighth form). It takes a structure and drives a sub-operation (ProcessAndProcessAndProcess1649). Part of the UI text-rendering cluster in the 0x82C52 region. 9 lines. 1 callee. 0 callers.
[entrypoint.functions."0x82C52640"]
name = "ProcessAndProcessAndProcess1648_82C52640"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and one hundred and forty-ninth form). It takes a structure and checks its state, with no external sub-operations. Part of the UI text-rendering cluster in the 0x82C55 region. 8 lines. 0 callees. 1 caller (ProcessAndProcessAndProcess1648).
[entrypoint.functions."0x82C55278"]
name = "ProcessAndProcessAndProcess1649_82C55278"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and one hundred and fiftieth form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x8269B process cluster. 8 lines. 0 callees. 0 callers.
[entrypoint.functions."0x8269B5F0"]
name = "ProcessAndProcessAndProcess1650_8269B5F0"

# Process a structure by wrapping sub-operations (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and one hundred and fifty-first form). It takes a structure, checks its state, and drives sub-operations (ProcessAndProcessAndProcess1254, ProcessAndProcessAndProcess1278, sub_82C08B00, sub_82C452D0, sub_82C50E28, sub_82CBB610) based on the result. Part of the 0x82C45 process cluster. 114 lines. 6 callees. 1 caller (ProcessAndProcessAndProcess1139).
[entrypoint.functions."0x82C454B0"]
name = "ProcessAndProcessAndProcess1651_82C454B0"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and one hundred and fifty-second form). It takes a structure and checks its state, with no external sub-operations. This is a frequently-called helper in the 0x821E5 process cluster. 87 lines. 0 callees. 6 callers.
[entrypoint.functions."0x821E5530"]
name = "ProcessAndProcessAndProcess1652_821E5530"

# Process a large structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and one hundred and fifty-third form). It takes a structure and drives a sub-operation (sub_82B8FCA8). This is a frequently-called helper in the 0x82B8B process cluster. 138 lines. 1 callee. 5 callers.
[entrypoint.functions."0x82B8BA70"]
name = "ProcessAndProcessAndProcess1653_82B8BA70"

# Process a large data structure by wrapping sub-operations (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and one hundred and fifty-fourth form). It takes a structure, checks its state, and drives sub-operations (ProcessAndProcessAndProcess569, ProcessAndProcessAndProcess570, ProcessAndProcessAndProcess571, ProcessAndProcessAndProcess617) based on the result. Part of the 0x82B8B process cluster. 371 lines. 4 callees. 4 callers.
[entrypoint.functions."0x82B8BC08"]
name = "ProcessAndProcessAndProcess1654_82B8BC08"

# Process a very large data structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and one hundred and fifty-fifth form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82CEB process cluster. 513 lines. 0 callees. 1 caller (ProcessAndProcessAndProcess882).
[entrypoint.functions."0x82CEBF30"]
name = "ProcessAndProcessAndProcess1655_82CEBF30"

# Process a structure by linearly searching (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and one hundred and fifty-sixth form). It takes a structure and performs a linear search (LinearSearch). This is a frequently-called helper in the 0x82283 process cluster. 139 lines. 1 callee. 3 callers.
[entrypoint.functions."0x82283ED0"]
name = "ProcessAndProcessAndProcess1656_82283ED0"

# Process a structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and one hundred and fifty-seventh form). It takes a structure and drives a sub-operation (ProcessAndProcessAndProcess652). This is a very frequently-called helper in the 0x821F4 process cluster. 7 lines. 1 callee. 185 callers.
[entrypoint.functions."0x821F4D88"]
name = "ProcessAndProcessAndProcess1657_821F4D88"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and one hundred and fifty-eighth form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82CD9 process cluster. 45 lines. 0 callees. 2 callers (sub_82CDAD08 and sub_82CDAD90).
[entrypoint.functions."0x82CD9910"]
name = "ProcessAndProcessAndProcess1658_82CD9910"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and one hundred and fifty-ninth form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82CE4 process cluster. 64 lines. 0 callees. 0 callers.
[entrypoint.functions."0x82CE4BE8"]
name = "ProcessAndProcessAndProcess1659_82CE4BE8"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and one hundred and sixtieth form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82CE4 process cluster. 12 lines. 0 callees. 0 callers.
[entrypoint.functions."0x82CE4CE0"]
name = "ProcessAndProcessAndProcess1660_82CE4CE0"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and one hundred and sixty-first form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82B55 process cluster. 70 lines. 0 callees. 1 caller (sub_82B55A98).
[entrypoint.functions."0x82B55DF8"]
name = "ProcessAndProcessAndProcess1661_82B55DF8"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and one hundred and sixty-second form). It takes a structure and checks its state, with no external sub-operations. This is a frequently-called helper in the 0x82581 process cluster. 55 lines. 0 callees. 12 callers.
[entrypoint.functions."0x82581388"]
name = "ProcessAndProcessAndProcess1662_82581388"

# Process a very large data structure by constructing a refcounted object (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and one hundred and sixty-third form). It takes a structure, constructs a refcounted object (ConstructRefCounted), copy-assigns a refcounted object (CopyAssign_RefCounted), destructs a refcounted object (DestructRefCounted), releases a global reference (ReleaseGlobalRef), and drives sub-operations (ProcessAndProcessAndProcess1623, sub_82301D40, sub_82368C30, sub_826C9EE0, ProcessAndProcessAndProcess1664, ProcessAndProcessAndProcess1665, sub_82CBD008) based on the result. Part of the 0x826C6 process cluster. 410 lines. 11 callees. 0 callers.
[entrypoint.functions."0x826C6F28"]
name = "ProcessAndProcessAndProcess1663_826C6F28"

# Process a large structure by constructing a refcounted object (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and one hundred and sixty-fourth form). It takes a structure, constructs a refcounted object (ConstructRefCounted), destructs a refcounted object (DestructRefCounted), and drives sub-operations (ProcessAndProcessAndProcess1249, ProcessAndVcallAndProcess16) based on the result. Part of the 0x826CB process cluster. 171 lines. 4 callees. 1 caller (ProcessAndProcessAndProcess1663).
[entrypoint.functions."0x826CBB58"]
name = "ProcessAndProcessAndProcess1664_826CBB58"

# Process a structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and one hundred and sixty-fifth form). It takes a structure and drives a sub-operation (ProcessAndProcessAndProcess1662). This is a frequently-called helper in the 0x828CE process cluster. 114 lines. 1 callee. 14 callers.
[entrypoint.functions."0x828CE180"]
name = "ProcessAndProcessAndProcess1665_828CE180"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and one hundred and sixty-sixth form). It takes a structure and checks its state, with no external sub-operations. This is a frequently-called helper in the 0x822D6 process cluster. 127 lines. 0 callees. 4 callers.
[entrypoint.functions."0x822D6998"]
name = "ProcessAndProcessAndProcess1666_822D6998"

# Process a structure by copy-assigning a refcounted object (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and one hundred and sixty-seventh form). It takes a structure, copy-assigns a refcounted object (CopyAssign_RefCounted), destructs a refcounted object (DestructRefCounted), and drives sub-operations (ProcessAndProcessAndProcess1228, ProcessAndProcessAndProcess1315, ProcessAndProcessAndProcess1633) based on the result. This is a frequently-called helper in the 0x82301 process cluster. 95 lines. 5 callees. 11 callers.
[entrypoint.functions."0x82301F58"]
name = "ProcessAndProcessAndProcess1667_82301F58"

# Process a structure by replacing a refcounted object (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and one hundred and sixty-eighth form). It takes a structure, replaces a refcounted object (ProcessAndReplaceRefCounted), and drives a sub-operation (ProcessAndProcessAndProcess1666) based on the result. Part of the 0x82817 process cluster. 52 lines. 2 callees. 1 caller (ProcessAndProcessAndProcess1669).
[entrypoint.functions."0x82817318"]
name = "ProcessAndProcessAndProcess1668_82817318"

# Process a large structure by copy-assigning a refcounted object (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and one hundred and sixty-ninth form). It takes a structure, copy-assigns a refcounted object (CopyAssign_RefCounted), frees size-bucketed memory (Free_SizeBucketed), increments a counter (ProcessAndIncrementCounter), releases a global reference (ReleaseGlobalRef), and drives sub-operations (sub_82301D40, ProcessAndProcessAndProcess1667, sub_82816D20, ProcessAndProcessAndProcess1668) based on the result. Part of the 0x82817 process cluster. 126 lines. 8 callees. 2 callers (ProcessAndDispatch and ProcessAndParseLines).
[entrypoint.functions."0x82817C50"]
name = "ProcessAndProcessAndProcess1669_82817C50"

# Process a structure by copying a struct region (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and one hundred and seventieth form). It takes a structure, copies a struct region (ProcessAndCopyStructRegion), and drives a sub-operation (ProcessAndProcessAndProcess730) based on the result. Part of the 0x82A1B process cluster. 123 lines. 2 callees. 0 callers.
[entrypoint.functions."0x82A1B988"]
name = "ProcessAndProcessAndProcess1670_82A1B988"

# Process a structure by processing an entry array (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and one hundred and seventy-first form). It takes a structure, processes an entry array (CriticalSection_ProcessEntryArray), and gets the elapsed milliseconds since start (GetElapsedMsSinceStart). This is a frequently-called helper in the 0x832B3 process cluster. 173 lines. 2 callees. 3 callers.
[entrypoint.functions."0x832B39F8"]
name = "ProcessAndProcessAndProcess1671_832B39F8"

# Process a very large data structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and one hundred and seventy-second form). It takes a structure, computes a special function (ComputeSpecialFunction), constructs a refcounted object (ConstructRefCounted), destructs a refcounted object (DestructRefCounted), and drives sub-operations (ProcessAndProcessAndProcess1007, ProcessAndProcessAndProcess1252, ProcessAndProcessAndProcess1341, ProcessAndProcessAndProcess499, ProcessAndProcessAndProcess510, ProcessAndProcessAndProcess525, ProcessAndProcessAndProcess549, ProcessAndProcessAndProcess551, ProcessAndProcessAndProcess552, ProcessAndProcessAndProcess575, ProcessAndProcessAndProcess619, ProcessAndProcessAndProcess1677, ProcessAndProcessAndProcess1675, ProcessAndProcessAndProcess1678, ProcessAndProcessAndProcess1676) based on the result. This is one of the largest functions in the binary in the 0x82A3B process cluster. 2026 lines. 19 callees. 3 callers.
[entrypoint.functions."0x82A3BC58"]
name = "ProcessAndProcessAndProcess1672_82A3BC58"

# Process a structure by zeroing memory (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and one hundred and seventy-third form). It takes a structure, zeroes memory (MemSet_SdkRuntime), and drives a sub-operation (sub_832B64D0) based on the result. Part of the 0x832B5 process cluster. 172 lines. 2 callees. 1 caller (ProcessAndProcessAndProcess1675).
[entrypoint.functions."0x832B50C0"]
name = "ProcessAndProcessAndProcess1673_832B50C0"

# Process a large structure by processing an entry array (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and one hundred and seventy-fourth form). It takes a structure, processes an entry array (CriticalSection_ProcessEntryArray), and gets the elapsed milliseconds since start (GetElapsedMsSinceStart). Part of the 0x832B5 process cluster. 265 lines. 2 callees. 1 caller (ProcessAndProcessAndProcess1675).
[entrypoint.functions."0x832B51F0"]
name = "ProcessAndProcessAndProcess1674_832B51F0"

# Process a large data structure by wrapping sub-operations (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and one hundred and seventy-fifth form). It takes a structure, delays (DelayZero), gets the current time (GetCurrentTimeMs), waits on a mutex (MutexLock_WaitInfinite), releases a mutex (MutexUnlock_ReleaseMutant), and drives sub-operations (GetElapsedMsSinceStart, ProcessAndProcessAndProcess1673, ProcessAndProcessAndProcess1674, sub_832B53E0, ProcessAndProcessAndProcess1676) based on the result. This is a dispatcher in the 0x832B5 process cluster. 385 lines. 9 callees. 2 callers (ProcessAndProcessAndProcess1672 and sub_832B5C18).
[entrypoint.functions."0x832B5530"]
name = "ProcessAndProcessAndProcess1675_832B5530"

# Process a very large data structure by wrapping sub-operations (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and one hundred and seventy-sixth form). It takes a structure, checks its state, and drives sub-operations (ProcessAndProcessAndProcess1253, ProcessAndProcessAndProcess878) based on the result. Part of the 0x832BD process cluster. 510 lines. 2 callees. 2 callers (ProcessAndProcessAndProcess1675 and sub_832B57E8).
[entrypoint.functions."0x832BD3E8"]
name = "ProcessAndProcessAndProcess1676_832BD3E8"

# Process a structure by getting the elapsed time (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and one hundred and seventy-seventh form). It takes a structure and gets the elapsed milliseconds since start (GetElapsedMsSinceStart). This is a frequently-called helper in the 0x832B4 process cluster. 168 lines. 1 callee. 2 callers (ProcessAndProcessAndProcess1672 and ProcessAndProcessAndProcess1678).
[entrypoint.functions."0x832B4F98"]
name = "ProcessAndProcessAndProcess1677_832B4F98"

# Process a large data structure by processing an entry array (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and one hundred and seventy-eighth form). It takes a structure, processes an entry array (CriticalSection_ProcessEntryArray), gets the elapsed milliseconds since start (GetElapsedMsSinceStart), releases a mutex (MutexUnlock_ReleaseMutant), and drives sub-operations (ProcessAndProcessAndProcess1671, ProcessAndProcessAndProcess1677, sub_832B64D0, sub_832B8B80) based on the result. Part of the 0x832B5 process cluster. 291 lines. 7 callees. 2 callers (ProcessAndProcessAndProcess1672 and sub_832B5C18).
[entrypoint.functions."0x832B5A18"]
name = "ProcessAndProcessAndProcess1678_832B5A18"

# Process a structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and one hundred and seventy-ninth form). It takes a structure and drives a sub-operation (ProcessAndProcessAndProcess1680). Part of the 0x82CD9 process cluster. 58 lines. 1 callee. 1 caller (sub_82CE09E0).
[entrypoint.functions."0x82CD9F68"]
name = "ProcessAndProcessAndProcess1679_82CD9F68"

# Process a large structure by zeroing memory (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and one hundred and eightieth form). It takes a structure and zeroes memory (MemSet_SdkRuntime). Part of the 0x82CE0 process cluster. 259 lines. 1 callee. 1 caller (ProcessAndProcessAndProcess1679).
[entrypoint.functions."0x82CE04C8"]
name = "ProcessAndProcessAndProcess1680_82CE04C8"

# Process a structure by allocating and copy-assigning (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and one hundred and eighty-first form). It takes a structure, allocates fresh memory (Allocate_OrDbgBreak), and copy-assigns a refcounted object (CopyAssign_RefCounted). Part of the 0x82497 process cluster. 108 lines. 2 callees. 1 caller (sub_82496EF0).
[entrypoint.functions."0x82497878"]
name = "ProcessAndProcessAndProcess1681_82497878"

# Process a structure by linearly searching (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and one hundred and eighty-second form). It takes a structure and performs a linear search (LinearSearch). This is a frequently-called helper in the 0x82284 process cluster. 53 lines. 1 callee. 10 callers.
[entrypoint.functions."0x822844B0"]
name = "ProcessAndProcessAndProcess1682_822844B0"

# Process a large structure by linearly searching (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and one hundred and eighty-third form). It takes a structure and drives a sub-operation (ProcessAndProcessAndProcess1682). Part of the 0x823F7 process cluster. 215 lines. 1 callee. 3 callers.
[entrypoint.functions."0x823F7B78"]
name = "ProcessAndProcessAndProcess1683_823F7B78"

# Process a large structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and one hundred and eighty-fourth form). It takes a structure and drives a sub-operation (ProcessAndProcessAndProcess1683). Part of the 0x823F7 process cluster. 184 lines. 1 callee. 2 callers (ProcessAndProcessAndProcess1330 and sub_8218C750).
[entrypoint.functions."0x823F7CE8"]
name = "ProcessAndProcessAndProcess1684_823F7CE8"

# Process a large structure by wrapping sub-operations (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and one hundred and eighty-fifth form). It takes a structure, checks its state, and drives sub-operations (ProcessAndProcessAndProcess625, ProcessAndProcessAndProcess1686) based on the result. Part of the 0x823F9 process cluster. 225 lines. 2 callees. 1 caller (ProcessAndProcessAndProcess1330).
[entrypoint.functions."0x823F94C8"]
name = "ProcessAndProcessAndProcess1685_823F94C8"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and one hundred and eighty-sixth form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x823F9 process cluster. 97 lines. 0 callees. 1 caller (ProcessAndProcessAndProcess1685).
[entrypoint.functions."0x823F9B70"]
name = "ProcessAndProcessAndProcess1686_823F9B70"

# Process a structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and one hundred and eighty-seventh form). It takes a structure and drives a sub-operation (ProcessAndProcessAndProcess1585). This is a frequently-called helper in the 0x82A39 process cluster. 106 lines. 1 callee. 7 callers.
[entrypoint.functions."0x82A39D88"]
name = "ProcessAndProcessAndProcess1687_82A39D88"

# Process a very large data structure by wrapping sub-operations (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and one hundred and eighty-eighth form). It takes a structure, checks its state, and drives sub-operations (ProcessAndProcessAndProcess1048, ProcessAndProcessAndProcess857) based on the result. This is a frequently-called processor in the 0x82A39 process cluster. 442 lines. 2 callees. 5 callers.
[entrypoint.functions."0x82A39E30"]
name = "ProcessAndProcessAndProcess1688_82A39E30"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and one hundred and eighty-ninth form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82B4E process cluster. 15 lines. 0 callees. 0 callers.
[entrypoint.functions."0x82B4EC90"]
name = "ProcessAndProcessAndProcess1689_82B4EC90"

# Process a very large data structure by wrapping sub-operations (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and one hundred and ninetieth form). It takes a structure, checks its state, and drives sub-operations (ProcessAndProcessAndProcess1595, ProcessAndProcessAndProcess1596) based on the result. Part of the 0x832B7 process cluster. 991 lines. 2 callees. 1 caller (sub_832B7F28).
[entrypoint.functions."0x832B7620"]
name = "ProcessAndProcessAndProcess1690_832B7620"

# Process a structure by copying memory (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and one hundred and ninety-first form). It takes a structure, copies memory (MemCpy_SdkRuntime), and drives a sub-operation (ProcessAndProcessAndProcess1690) based on the result. Part of the 0x832B7 process cluster. 173 lines. 2 callees. 1 caller (ProcessAndProcessAndProcess1699).
[entrypoint.functions."0x832B7F28"]
name = "ProcessAndProcessAndProcess1691_832B7F28"

# Process a structure by constructing a 16-byte object (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and one hundred and ninety-second form). It takes a structure, constructs a 16-byte object (Construct16Byte), checks a ctype predicate (Ctype_Pred_Bit2), and drives sub-operations (ProcessAndProcessAndProcess1012, ProcessAndProcessAndProcess730, ProcessAndProcessAndProcess925, sub_82BFF768) based on the result. Part of the 0x82C47 process cluster. 153 lines. 6 callees. 1 caller (sub_82C47BC0).
[entrypoint.functions."0x82C47808"]
name = "ProcessAndProcessAndProcess1692_82C47808"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and one hundred and ninety-third form). It takes a structure and checks its state, with no external sub-operations. Part of the UI text-rendering cluster in the 0x82C12 region. 38 lines. 0 callees. 0 callers.
[entrypoint.functions."0x82C12F78"]
name = "ProcessAndProcessAndProcess1693_82C12F78"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and one hundred and ninety-fourth form). It takes a structure and checks its state, with no external sub-operations. This is a frequently-called helper in the 0x82CA9 process cluster. 44 lines. 0 callees. 8 callers.
[entrypoint.functions."0x82CA9A18"]
name = "ProcessAndProcessAndProcess1694_82CA9A18"

# Process a structure by setting a global field (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and one hundred and ninety-fifth form). It takes a structure, sets a global field (SetGlobalField_Thunk), and drives a sub-operation (ProcessAndProcessAndProcess1696) based on the result. Part of the 0x82CBD process cluster. 126 lines. 2 callees. 3 callers.
[entrypoint.functions."0x82CBD290"]
name = "ProcessAndProcessAndProcess1695_82CBD290"

# Process a large structure by setting a global field (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and one hundred and ninety-sixth form). It takes a structure and sets a global field (SetGlobalField_Thunk). Part of the 0x82CC8 process cluster. 278 lines. 1 callee. 1 caller (ProcessAndProcessAndProcess1695).
[entrypoint.functions."0x82CC87C8"]
name = "ProcessAndProcessAndProcess1696_82CC87C8"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and one hundred and ninety-seventh form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82B4D process cluster. 40 lines. 0 callees. 0 callers.
[entrypoint.functions."0x82B4DD80"]
name = "ProcessAndProcessAndProcess1697_82B4DD80"

# Process a structure by freeing memory (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and one hundred and ninety-eighth form). It takes a structure and frees memory (Free_Thunk). This is a frequently-called helper in the 0x82C08 process cluster. 106 lines. 1 callee. 7 callers.
[entrypoint.functions."0x82C081A0"]
name = "ProcessAndProcessAndProcess1698_82C081A0"

# Process a large structure by copying memory (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and one hundred and ninety-ninth form). It takes a structure, copies memory (MemCpy_SdkRuntime), and drives a sub-operation (ProcessAndProcessAndProcess1691) based on the result. Part of the 0x832B5 process cluster. 190 lines. 2 callees. 2 callers (ProcessAndProcessAndProcess1675 and sub_832B57E8).
[entrypoint.functions."0x832B53E0"]
name = "ProcessAndProcessAndProcess1699_832B53E0"

# Process a structure by freeing size-bucketed memory (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundredth form). It takes a structure, frees size-bucketed memory (Free_SizeBucketed), and drives a sub-operation (sub_82AAE408) based on the result. Part of the 0x82AAF process cluster. 71 lines. 2 callees. 2 callers (sub_82AAF558 and sub_82AAF7C8).
[entrypoint.functions."0x82AAF1C0"]
name = "ProcessAndProcessAndProcess1700_82AAF1C0"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and first form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82C2F process cluster. 31 lines. 0 callees. 0 callers.
[entrypoint.functions."0x82C2F060"]
name = "ProcessAndProcessAndProcess1701_82C2F060"

# Process a structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and second form). It takes a structure and drives a sub-operation (ProcessAndProcessAndProcess1250). This is a frequently-called helper in the 0x822C6 process cluster. 54 lines. 1 callee. 21 callers.
[entrypoint.functions."0x822C6288"]
name = "ProcessAndProcessAndProcess1702_822C6288"

# Process a structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and third form). It takes a structure and drives a sub-operation (ProcessAndProcessAndProcess1120). This is a frequently-called helper in the 0x83222 process cluster. 59 lines. 1 callee. 7 callers.
[entrypoint.functions."0x83222B80"]
name = "ProcessAndProcessAndProcess1703_83222B80"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and fourth form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82C4F process cluster. 65 lines. 0 callees. 0 callers.
[entrypoint.functions."0x82C4F9A8"]
name = "ProcessAndProcessAndProcess1704_82C4F9A8"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and fifth form). It takes a structure and checks its state, with no external sub-operations. This is a frequently-called helper in the 0x82CDD process cluster. 88 lines. 0 callees. 3 callers.
[entrypoint.functions."0x82CDD6D0"]
name = "ProcessAndProcessAndProcess1705_82CDD6D0"

# Process a very large data structure by copying memory (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and sixth form). It takes a structure, copies memory (MemCpy_SdkRuntime), and drives sub-operations (ProcessAndProcessAndProcess1110, ProcessAndProcessAndProcess504, ProcessAndProcessAndProcess893) based on the result. This is one of the largest functions in the binary in the 0x82CDD process cluster. 2052 lines. 4 callees. 2 callers (ProcessAndProcessAndProcess1055 and ProcessAndProcessAndProcess1708).
[entrypoint.functions."0x82CDDB80"]
name = "ProcessAndProcessAndProcess1706_82CDDB80"

# Process a large data structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and seventh form). It takes a structure and drives a sub-operation (ProcessAndProcessAndProcess683). Part of the 0x82CDF process cluster. 386 lines. 1 callee. 1 caller (ProcessAndProcessAndProcess1708).
[entrypoint.functions."0x82CDF4F8"]
name = "ProcessAndProcessAndProcess1707_82CDF4F8"

# Process a large data structure by copying memory (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and eighth form). It takes a structure, copies memory (MemCpy_SdkRuntime), and drives sub-operations (ProcessAndProcessAndProcess1705, ProcessAndProcessAndProcess1706, ProcessAndProcessAndProcess1707) based on the result. Part of the 0x82CDF process cluster. 227 lines. 4 callees. 1 caller (ProcessAndProcessAndProcess1055).
[entrypoint.functions."0x82CDF788"]
name = "ProcessAndProcessAndProcess1708_82CDF788"

# Process a large structure by pushing onto a queue (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and ninth form). It takes a structure, enters or tries a critical section (CriticalSection_EnterOrTry), copies memory (MemCpy_SdkRuntime), pushes onto a queue (QueuePush), and drives a sub-operation (ProcessAndProcessAndProcess1279) based on the result. This is a frequently-called helper in the 0x82A83 process cluster. 201 lines. 4 callees. 4 callers.
[entrypoint.functions."0x82A83420"]
name = "ProcessAndProcessAndProcess1709_82A83420"

# Process a structure by setting an event (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and tenth form). It takes a structure and sets an event (SetEvent_SdkRuntime). Part of the 0x82B45 process cluster. 47 lines. 1 callee. 0 callers.
[entrypoint.functions."0x82B45420"]
name = "ProcessAndProcessAndProcess1710_82B45420"

# Process a structure by constructing and inserting a node (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and eleventh form). It takes a structure, constructs and inserts a node (ConstructAndInsertNode), and sets an event (SetEvent_SdkRuntime). Part of the 0x82B45 process cluster. 63 lines. 2 callees. 1 caller (ProcessAndProcessAndProcess1098).
[entrypoint.functions."0x82B454C0"]
name = "ProcessAndProcessAndProcess1711_82B454C0"

# Process a structure by wrapping sub-operations (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and twelfth form). It takes a structure, checks its state, and drives sub-operations (ProcessAndProcessAndProcess1120, ProcessAndProcessAndProcess1621) based on the result. This is a very frequently-called helper in the 0x82978 process cluster. 60 lines. 2 callees. 384 callers.
[entrypoint.functions."0x82978470"]
name = "ProcessAndProcessAndProcess1712_82978470"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and thirteenth form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82B56 process cluster. 47 lines. 0 callees. 0 callers.
[entrypoint.functions."0x82B568C8"]
name = "ProcessAndProcessAndProcess1713_82B568C8"

# Process a very large data structure by getting the timebase (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and fourteenth form). It takes a structure, gets the timebase (GetTimebase), sets an event (SetEvent_SdkRuntime), and drives sub-operations (ProcessAndProcessAndProcess1645, SwitchDispatch, WrapperSub82CC1C70) based on the result. Part of the 0x82B56 process cluster. 595 lines. 5 callees. 0 callers.
[entrypoint.functions."0x82B56918"]
name = "ProcessAndProcessAndProcess1714_82B56918"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and fifteenth form). It takes a structure and checks its state, with no external sub-operations. This is a frequently-called helper in the 0x82C79 process cluster. 8 lines. 0 callees. 4 callers.
[entrypoint.functions."0x82C79700"]
name = "ProcessAndProcessAndProcess1715_82C79700"

# Process a structure by wrapping sub-operations (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and sixteenth form). It takes a structure, checks its state, and drives sub-operations (ProcessAndProcessAndProcess751, ProcessAndProcessAndProcess756, sub_82C54250, ProcessAndProcessAndProcess1717, sub_82CD2CF0) based on the result. Part of the UI text-rendering cluster in the 0x82C54 region. 126 lines. 5 callees. 3 callers.
[entrypoint.functions."0x82C54320"]
name = "ProcessAndProcessAndProcess1716_82C54320"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and seventeenth form). It takes a structure and checks its state, with no external sub-operations. This is a frequently-called helper in the 0x82CD2 process cluster. 54 lines. 0 callees. 2 callers.
[entrypoint.functions."0x82CD2BE0"]
name = "ProcessAndProcessAndProcess1717_82CD2BE0"

# Process a large structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and eighteenth form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82CDC process cluster. 136 lines. 0 callees. 0 callers.
[entrypoint.functions."0x82CDC1E8"]
name = "ProcessAndProcessAndProcess1718_82CDC1E8"

# Process a large structure by wrapping sub-operations (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and nineteenth form). It takes a structure, checks its state, and drives sub-operations (ProcessAndProcessAndProcess1128, sub_8236F8D8) based on the result. Part of the 0x8236E process cluster. 177 lines. 2 callees. 2 callers (sub_8236D798 and sub_8236E830).
[entrypoint.functions."0x8236EB10"]
name = "ProcessAndProcessAndProcess1719_8236EB10"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and twentieth form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x83226 process cluster. 58 lines. 0 callees. 0 callers.
[entrypoint.functions."0x832261B8"]
name = "ProcessAndProcessAndProcess1720_832261B8"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and twenty-first form). It takes a structure and checks its state, with no external sub-operations. Part of the UI text-rendering cluster in the 0x82C55 region. 8 lines. 0 callees. 0 callers.
[entrypoint.functions."0x82C559B8"]
name = "ProcessAndProcessAndProcess1721_82C559B8"

# Process a large structure by entering a critical section (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and twenty-second form). It takes a structure, enters or tries a critical section (CriticalSection_EnterOrTry), copies memory (MemCpy_SdkRuntime), and drives a sub-operation (ProcessAndProcessAndProcess1279) based on the result. Part of the 0x82A83 process cluster. 141 lines. 3 callees. 2 callers.
[entrypoint.functions."0x82A83338"]
name = "ProcessAndProcessAndProcess1722_82A83338"

# Process a very large data structure by allocating fresh memory (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and twenty-third form). It takes a structure, allocates fresh memory (Allocate_OrDbgBreak), frees size-bucketed memory (Free_SizeBucketed), dispatches and processes (ProcessAndDispatchAndProcess25), processes a hash lookup (ProcessHashLookup), and drives sub-operations (ProcessAndProcessAndProcess1100, ProcessAndProcessAndProcess1623, ProcessAndProcessAndProcess1635, sub_822F9408, sub_82368C30, sub_8236CBB8, sub_8236CD68, sub_82379A68, sub_82379B28, sub_82379F28, sub_82379FE0, sub_824E6A88, sub_824E6C78, sub_824E6D28, sub_82583608, sub_826C67F8, sub_829F88B0, sub_829F8948, sub_829F8AC0, sub_829F8B58, sub_82A3B848, sub_82B68F60, sub_82CBD008) based on the result. This is a dispatcher in the 0x826C6 process cluster. 610 lines. 27 callees. 0 callers.
[entrypoint.functions."0x826C63D8"]
name = "ProcessAndProcessAndProcess1723_826C63D8"

# Process a structure by comparing strings case-insensitively (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and twenty-fourth form). It takes a structure and compares strings case-insensitively (StrCmpIgnoreCase). Part of the 0x82C0B process cluster. 88 lines. 1 callee. 1 caller (ProcessAndProcessAndProcess1725).
[entrypoint.functions."0x82C0BD40"]
name = "ProcessAndProcessAndProcess1724_82C0BD40"

# Process a structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and twenty-fifth form). It takes a structure and drives a sub-operation (ProcessAndProcessAndProcess1724). Part of the 0x82C0C process cluster. 11 lines. 1 callee. 0 callers.
[entrypoint.functions."0x82C0CB78"]
name = "ProcessAndProcessAndProcess1725_82C0CB78"

# Process a large data structure by constructing a refcounted object (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and twenty-sixth form). It takes a structure, constructs a refcounted object (ConstructRefCounted), copy-assigns a refcounted object (CopyAssign_RefCounted), destructs a refcounted object (DestructRefCounted), replaces a refcounted object (ProcessAndReplaceRefCounted), releases a global reference (ReleaseGlobalRef), and drives sub-operations (ProcessAndProcessAndProcess1228, ProcessAndProcessAndProcess1633, ProcessAndProcessAndProcess320, sub_821E6408, sub_8236D798, sub_82B3A7A0, sub_82B3AA88) based on the result. This is a very frequently-called helper in the 0x82301 process cluster. 229 lines. 12 callees. 245 callers.
[entrypoint.functions."0x82301D40"]
name = "ProcessAndProcessAndProcess1726_82301D40"

# Process a very large data structure by entering a critical section (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and twenty-seventh form). It takes a structure, enters or tries a critical section (CriticalSection_EnterOrTry), destructs (Destruct3), increments a counter (ProcessAndIncrementCounter), and drives sub-operations (ProcessAndProcessAndProcess1275, sub_822F51B8, sub_8236E830, ProcessAndProcessAndProcess1719, sub_8236EF80) based on the result. This is a frequently-called helper in the 0x8236D process cluster. 290 lines. 8 callees. 38 callers.
[entrypoint.functions."0x8236D798"]
name = "ProcessAndProcessAndProcess1727_8236D798"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and twenty-eighth form). It takes a structure and checks its state, with no external sub-operations. This is a very frequently-called helper in the 0x82D4F process cluster. 42 lines. 0 callees. 323 callers.
[entrypoint.functions."0x82D4F620"]
name = "ProcessAndProcessAndProcess1728_82D4F620"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and twenty-ninth form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x826F9 process cluster. 8 lines. 0 callees. 0 callers.
[entrypoint.functions."0x826F97D0"]
name = "ProcessAndProcessAndProcess1729_826F97D0"

# Process a structure by freeing size-bucketed memory (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and thirtieth form). It takes a structure and frees size-bucketed memory (Free_SizeBucketed). This is a frequently-called helper in the 0x82A10 process cluster. 57 lines. 1 callee. 5 callers.
[entrypoint.functions."0x82A10870"]
name = "ProcessAndProcessAndProcess1730_82A10870"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and thirty-first form). It takes a structure and checks its state, with no external sub-operations. Part of the UI text-rendering cluster in the 0x82C13 region. 25 lines. 0 callees. 0 callers.
[entrypoint.functions."0x82C13430"]
name = "ProcessAndProcessAndProcess1731_82C13430"

# Process a structure by setting a string (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and thirty-second form). It takes a structure, frees memory (Free_Thunk), sets a string (SetString), and drives sub-operations (sub_822F1EC0, sub_822F1F00, sub_826C3FA8) based on the result. Part of the 0x82C49 process cluster. 93 lines. 5 callees. 2 callers.
[entrypoint.functions."0x82C492E8"]
name = "ProcessAndProcessAndProcess1732_82C492E8"

# Process a large structure by validating and processing (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and thirty-third form). It takes a structure, validates and processes (ProcessAndValidateAndProcess3), and validates and processes a node (ValidateAndProcessNode). This is a frequently-called helper in the 0x8236F process cluster. 153 lines. 2 callees. 5 callers.
[entrypoint.functions."0x8236F8D8"]
name = "ProcessAndProcessAndProcess1733_8236F8D8"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and thirty-fourth form). It takes a structure and checks its state, with no external sub-operations. This is a frequently-called helper in the 0x82CD2 process cluster. 58 lines. 0 callees. 4 callers.
[entrypoint.functions."0x82CD2C38"]
name = "ProcessAndProcessAndProcess1734_82CD2C38"

# Process a very large data structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and thirty-fifth form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82CDF process cluster. 498 lines. 0 callees. 2 callers (ProcessAndProcessAndProcess1736 and sub_82CE0260).
[entrypoint.functions."0x82CDFB90"]
name = "ProcessAndProcessAndProcess1735_82CDFB90"

# Process a very large data structure by copying memory (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and thirty-sixth form). It takes a structure, copies memory (MemCpy_SdkRuntime), and drives a sub-operation (ProcessAndProcessAndProcess1735) based on the result. Part of the 0x82CDF process cluster. 389 lines. 2 callees. 2 callers (sub_82CE0890 and ProcessAndProcessAndProcess1737).
[entrypoint.functions."0x82CDFFA8"]
name = "ProcessAndProcessAndProcess1736_82CDFFA8"

# Process a structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and thirty-seventh form). It takes a structure and drives a sub-operation (ProcessAndProcessAndProcess1736). Part of the 0x82CE3 process cluster. 9 lines. 1 callee. 1 caller (TailCall_82CE3760).
[entrypoint.functions."0x82CE3220"]
name = "ProcessAndProcessAndProcess1737_82CE3220"

# Process a structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and thirty-eighth form). It takes a structure and drives a sub-operation (ProcessAndProcessAndProcess954). Part of the 0x82197 process cluster. 82 lines. 1 callee. 1 caller (sub_82197000).
[entrypoint.functions."0x82197150"]
name = "ProcessAndProcessAndProcess1738_82197150"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and thirty-ninth form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82C44 process cluster. 25 lines. 0 callees. 0 callers.
[entrypoint.functions."0x82C44270"]
name = "ProcessAndProcessAndProcess1739_82C44270"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and fortieth form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82C4D process cluster. 70 lines. 0 callees. 0 callers.
[entrypoint.functions."0x82C4DE30"]
name = "ProcessAndProcessAndProcess1740_82C4DE30"

# Process a large structure by freeing memory (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and forty-first form). It takes a structure, frees memory (Free_Thunk), and drives sub-operations (ProcessAndProcessAndProcess1333, sub_82C51E20) based on the result. Part of the UI text-rendering cluster in the 0x82C09 region. 209 lines. 3 callees. 1 caller (UIText_RenderCurrentObject).
[entrypoint.functions."0x82C09500"]
name = "ProcessAndProcessAndProcess1741_82C09500"

# Process a structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and forty-second form). It takes a structure and drives a sub-operation (ProcessAndProcessAndProcess1745). Part of the UI text-rendering cluster in the 0x82C12 region. 57 lines. 1 callee. 0 callers.
[entrypoint.functions."0x82C12D28"]
name = "ProcessAndProcessAndProcess1742_82C12D28"

# Process a structure by constructing and inserting a node (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and forty-third form). It takes a structure and constructs and inserts a node (ConstructAndInsertNode). Part of the 0x82C49 process cluster. 65 lines. 1 callee. 1 caller (ProcessAndProcessAndProcess1744).
[entrypoint.functions."0x82C491B8"]
name = "ProcessAndProcessAndProcess1743_82C491B8"

# Process a structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and forty-fourth form). It takes a structure and drives a sub-operation (ProcessAndProcessAndProcess1743). Part of the 0x82C49 process cluster. 70 lines. 1 callee. 1 caller (ProcessAndProcessAndProcess1640).
[entrypoint.functions."0x82C49228"]
name = "ProcessAndProcessAndProcess1744_82C49228"

# Process a structure by calling methods (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and forty-fifth form). It takes a structure, calls methods (ProcessAndCallMethods), and checks a method (ProcessAndCheckMethod) based on the result. Part of the 0x82C4C process cluster. 60 lines. 2 callees. 1 caller (ProcessAndProcessAndProcess1742).
[entrypoint.functions."0x82C4CFE0"]
name = "ProcessAndProcessAndProcess1745_82C4CFE0"

# Process a large structure by allocating fresh memory (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and forty-sixth form). It takes a structure, allocates fresh memory (Allocate_OrDbgBreak), and drives a sub-operation (ProcessAndProcessAndProcess1716) based on the result. Part of the UI text-rendering cluster in the 0x82C54 region. 146 lines. 2 callees. 0 callers.
[entrypoint.functions."0x82C54880"]
name = "ProcessAndProcessAndProcess1746_82C54880"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and forty-seventh form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82C0C process cluster. 24 lines. 0 callees. 0 callers.
[entrypoint.functions."0x82C0CB98"]
name = "ProcessAndProcessAndProcess1747_82C0CB98"

# Process a structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and forty-eighth form). It takes a structure and drives a sub-operation (ProcessAndProcessAndProcess1749). This is a very frequently-called helper in the 0x82B13 process cluster. 69 lines. 1 callee. 299 callers.
[entrypoint.functions."0x82B13250"]
name = "ProcessAndProcessAndProcess1748_82B13250"

# Process a very large data structure by wrapping sub-operations (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and forty-ninth form). It takes a structure, checks its state, and drives sub-operations (sub_826188E0, sub_82B14328, sub_82B14718) based on the result. Part of the 0x82B13 process cluster. 287 lines. 3 callees. 2 callers (sub_826171A8 and ProcessAndProcessAndProcess1748).
[entrypoint.functions."0x82B139B8"]
name = "ProcessAndProcessAndProcess1749_82B139B8"

# Process a structure by comparing signed strings (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and fiftieth form). It takes a structure and compares signed strings (StrCmp_Signed). This is a frequently-called helper in the 0x82859 process cluster. 100 lines. 1 callee. 4 callers.
[entrypoint.functions."0x82859208"]
name = "ProcessAndProcessAndProcess1750_82859208"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and fifty-first form). It takes a structure and checks its state, with no external sub-operations. Part of the UI text-rendering cluster in the 0x82C51 region. 16 lines. 0 callees. 0 callers.
[entrypoint.functions."0x82C51F90"]
name = "ProcessAndProcessAndProcess1751_82C51F90"

# Process a very large data structure by allocating checked memory (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and fifty-second form). It takes a structure, allocates checked memory (AllocateChecked3), performs a checked copy (CheckedCopy), frees size-bucketed memory (Free_SizeBucketed), and drives a sub-operation (sub_82A97648) based on the result. This is a frequently-called processor in the 0x82A73 process cluster. 337 lines. 4 callees. 17 callers.
[entrypoint.functions."0x82A734D0"]
name = "ProcessAndProcessAndProcess1752_82A734D0"

# Process a structure by wrapping sub-operations (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and fifty-third form). It takes a structure, checks its state, and drives sub-operations (ProcessAndProcessAndProcess1652, ProcessAndProcessAndProcess1760) based on the result. This is a frequently-called helper in the 0x821E5 process cluster. 108 lines. 2 callees. 63 callers.
[entrypoint.functions."0x821E55C8"]
name = "ProcessAndProcessAndProcess1753_821E55C8"

# Process a structure by wrapping sub-operations (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and fifty-fourth form). It takes a structure, checks its state, and drives sub-operations (ProcessAndProcessAndProcess1753, sub_8229CA50) based on the result. This is a very frequently-called helper in the 0x829FB process cluster. 64 lines. 2 callees. 203 callers.
[entrypoint.functions."0x829FB978"]
name = "ProcessAndProcessAndProcess1754_829FB978"

# Process a very large data structure by wrapping sub-operations (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and fifty-fifth form). It takes a structure, checks its state, and drives sub-operations (Stub_ReturnZero, sub_82CDA478) based on the result. Part of the 0x82CE6 process cluster. 547 lines. 2 callees. 5 callers.
[entrypoint.functions."0x82CE62C0"]
name = "ProcessAndProcessAndProcess1755_82CE62C0"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and fifty-sixth form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x83222 process cluster. 101 lines. 0 callees. 0 callers.
[entrypoint.functions."0x832227A8"]
name = "ProcessAndProcessAndProcess1756_832227A8"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and fifty-seventh form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82173 process cluster. 40 lines. 0 callees. 0 callers.
[entrypoint.functions."0x82173E78"]
name = "ProcessAndProcessAndProcess1757_82173E78"

# Process a structure by freeing size-bucketed memory (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and fifty-eighth form). It takes a structure, frees size-bucketed memory (Free_SizeBucketed), and drives a sub-operation (ProcessAndProcessAndProcess1759) based on the result. Part of the 0x82B55 process cluster. 50 lines. 2 callees. 0 callers.
[entrypoint.functions."0x82B55E68"]
name = "ProcessAndProcessAndProcess1758_82B55E68"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and fifty-ninth form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82B55 process cluster. 67 lines. 0 callees. 1 caller (ProcessAndProcessAndProcess1758).
[entrypoint.functions."0x82B55EB8"]
name = "ProcessAndProcessAndProcess1759_82B55EB8"

# Process a structure by computing an FNV-1 hash (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and sixtieth form). It takes a structure, computes an FNV-1 hash (FNV1Hash), and drives sub-operations (ProcessAndProcessAndProcess1652, sub_821E3E10) based on the result. This is a frequently-called helper in the 0x821E5 process cluster. 90 lines. 3 callees. 7 callers.
[entrypoint.functions."0x821E5498"]
name = "ProcessAndProcessAndProcess1760_821E5498"

# Process a large structure by processing a vector (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and sixty-first form). It takes a structure, drives a sub-operation (ProcessAndProcessAndProcess1120), and processes a vector (ProcessVector2). This is a frequently-called helper in the 0x83223 process cluster. 214 lines. 2 callees. 4 callers.
[entrypoint.functions."0x832239B0"]
name = "ProcessAndProcessAndProcess1761_832239B0"

# Process a structure by releasing a refcounted object (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and sixty-second form). It takes a structure, releases a refcounted object (Release_RefCounted), and validates and searches (ValidateAndSearch). This is a frequently-called helper in the 0x83225 process cluster. 75 lines. 2 callees. 16 callers.
[entrypoint.functions."0x832254B8"]
name = "ProcessAndProcessAndProcess1762_832254B8"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and sixty-third form). It takes a structure and checks its state, with no external sub-operations. This is a frequently-called helper in the 0x82C01 process cluster. 81 lines. 0 callees. 3 callers.
[entrypoint.functions."0x82C01150"]
name = "ProcessAndProcessAndProcess1763_82C01150"

# Process a structure by freeing size-bucketed memory (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and sixty-fourth form). It takes a structure, frees size-bucketed memory (Free_SizeBucketed), and releases a refcounted object (Release_RefCounted). This is a frequently-called helper in the 0x8217A process cluster. 96 lines. 2 callees. 3 callers.
[entrypoint.functions."0x8217AE48"]
name = "ProcessAndProcessAndProcess1764_8217AE48"

# Process a structure by building a buffer (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and sixty-fifth form). It takes a structure, builds a buffer (BuildBuffer), and drives sub-operations (sub_82CAB630, sub_82CAB770, sub_82CAB7E0) based on the result. This is a frequently-called helper in the 0x8223F process cluster. 132 lines. 4 callees. 49 callers.
[entrypoint.functions."0x8223F888"]
name = "ProcessAndProcessAndProcess1765_8223F888"

# Process a large data structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and sixty-sixth form). It takes a structure and drives a sub-operation (sub_82BA9E18). Part of the 0x82BA9 process cluster. 246 lines. 1 callee. 1 caller (ProcessAndProcessAndProcess_82174D38).
[entrypoint.functions."0x82BA9988"]
name = "ProcessAndProcessAndProcess1766_82BA9988"

# Process a large data structure by wrapping sub-operations (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and sixty-seventh form). It takes a structure, checks its state, and drives sub-operations (sub_82BB0568, sub_82BB0628) based on the result. Part of the 0x82BB0 process cluster. 224 lines. 2 callees. 0 callers.
[entrypoint.functions."0x82BB02D0"]
name = "ProcessAndProcessAndProcess1767_82BB02D0"

# Process a structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and sixty-eighth form). It takes a structure and drives a sub-operation (sub_82B5A778). This is a frequently-called helper in the 0x82B5A process cluster. 106 lines. 1 callee. 11 callers.
[entrypoint.functions."0x82B5A938"]
name = "ProcessAndProcessAndProcess1768_82B5A938"

# Process a very large data structure by copying memory (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and sixty-ninth form). It takes a structure, copies memory (MemCpy_SdkRuntime), aborts (ProcessAndAbort), and drives a sub-operation (sub_82CAB678) based on the result. This is one of the largest functions in the binary in the 0x82170 process cluster. 1765 lines. 3 callees. 1 caller (ProcessAndProcessAndProcess1770).
[entrypoint.functions."0x82170050"]
name = "ProcessAndProcessAndProcess1769_82170050"

# Process a structure by wrapping sub-operations (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and seventieth form). It takes a structure, aborts (ProcessAndAbort), and drives sub-operations (ProcessAndProcessAndProcess1769, ProcessAndProcessAndProcess1771, sub_82CAB678) based on the result. Part of the 0x82170 process cluster. 92 lines. 4 callees. 3 callers.
[entrypoint.functions."0x82170DA8"]
name = "ProcessAndProcessAndProcess1770_82170DA8"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and seventy-first form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82170 process cluster. 126 lines. 0 callees. 1 caller (ProcessAndProcessAndProcess1770).
[entrypoint.functions."0x82170E48"]
name = "ProcessAndProcessAndProcess1771_82170E48"

# Process a large data structure by moving memory (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and seventy-second form). It takes a structure, moves memory (MemMove_SdkRuntime), and drives sub-operations (sub_82CAB630, sub_82CAB770) based on the result. Part of the 0x82171 process cluster. 227 lines. 3 callees. 3 callers.
[entrypoint.functions."0x82171010"]
name = "ProcessAndProcessAndProcess1772_82171010"

# Process a large data structure by wrapping sub-operations (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and seventy-third form). It takes a structure, checks its state, and drives sub-operations (ProcessAndProcessAndProcess1770, ProcessAndProcessAndProcess1772, sub_82CAB630, sub_82CAB770, sub_82CABAE8, sub_82CAC1A8) based on the result. Part of the 0x82171 process cluster. 191 lines. 6 callees. 2 callers (sub_82278A90 and sub_82CAC370).
[entrypoint.functions."0x821712B0"]
name = "ProcessAndProcessAndProcess1773_821712B0"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and seventy-fourth form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x8218D process cluster. 104 lines. 0 callees. 1 caller (sub_82CABAE0).
[entrypoint.functions."0x8218D7D8"]
name = "ProcessAndProcessAndProcess1774_8218D7D8"

# Process a structure by wrapping sub-operations (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and seventy-fifth form). It takes a structure, checks its state, and drives sub-operations (sub_82170F28, sub_82171198, ProcessAndProcessAndProcess1773, sub_82CABD60) based on the result. Part of the 0x82278 process cluster. 49 lines. 4 callees. 0 callers.
[entrypoint.functions."0x82278A90"]
name = "ProcessAndProcessAndProcess1775_82278A90"

# Process a structure by computing a hash (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and seventy-sixth form). It takes a structure, computes a hash (ComputeHash), and builds a buffer (BuildBuffer) via a sub-operation (ProcessAndProcessAndProcess1765). This is a frequently-called helper in the 0x82BCB process cluster. 97 lines. 2 callees. 64 callers.
[entrypoint.functions."0x82BCBDC8"]
name = "ProcessAndProcessAndProcess1776_82BCBDC8"

# Process a large data structure by moving memory (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and seventy-seventh form). It takes a structure, moves memory (MemMove_SdkRuntime), zeroes memory (MemSet_SdkRuntime), and drives sub-operations (sub_82CAB630, sub_82CAB770) based on the result. Part of the 0x82CAC process cluster. 256 lines. 4 callees. 2 callers (sub_82170F28 and ProcessAndProcessAndProcess1773).
[entrypoint.functions."0x82CAC1A8"]
name = "ProcessAndProcessAndProcess1777_82CAC1A8"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and seventy-eighth form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82CE3 process cluster. 47 lines. 0 callees. 1 caller (sub_82CD8348).
[entrypoint.functions."0x82CE31B8"]
name = "ProcessAndProcessAndProcess1778_82CE31B8"

# Process a structure by freeing size-bucketed memory (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and seventy-ninth form). It takes a structure, frees size-bucketed memory (Free_SizeBucketed), and drives a sub-operation (ProcessAndProcessAndProcess1730) based on the result. Part of the 0x82BE4 process cluster. 50 lines. 2 callees. 0 callers.
[entrypoint.functions."0x82BE4658"]
name = "ProcessAndProcessAndProcess1779_82BE4658"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and eightieth form). It takes a structure and checks its state, with no external sub-operations. This is a frequently-called helper in the 0x82B41 process cluster. 18 lines. 0 callees. 2 callers.
[entrypoint.functions."0x82B41E08"]
name = "ProcessAndProcessAndProcess1780_82B41E08"

# Process a structure by finding and setting a flag bit (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and eighty-first form). It takes a structure, finds and sets a flag bit (FindAndSetFlagBit), and drives a sub-operation (SwitchDispatch) based on the result. Part of the 0x82BCC process cluster. 108 lines. 2 callees. 1 caller (DispatchTypeTag2).
[entrypoint.functions."0x82BCC108"]
name = "ProcessAndProcessAndProcess1781_82BCC108"

# Process a large structure by allocating fresh memory (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and eighty-second form). It takes a structure, allocates fresh memory (Allocate_OrDbgBreak), and drives sub-operations (sub_82A6D4C8, sub_82A6E200) based on the result. This is a frequently-called helper in the 0x82A6E process cluster. 174 lines. 3 callees. 7 callers.
[entrypoint.functions."0x82A6E5A0"]
name = "ProcessAndProcessAndProcess1782_82A6E5A0"

# Process a large structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and eighty-third form). It takes a structure and drives a sub-operation (ProcessAndProcessAndProcess1784). Part of the 0x82B55 process cluster. 165 lines. 1 callee. 2 callers (sub_82B4AD78 and sub_82B4AF20).
[entrypoint.functions."0x82B55318"]
name = "ProcessAndProcessAndProcess1783_82B55318"

# Process a large structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and eighty-fourth form). It takes a structure and drives a sub-operation (sub_82B55A98). Part of the 0x82B55 process cluster. 132 lines. 1 callee. 1 caller (ProcessAndProcessAndProcess1783).
[entrypoint.functions."0x82B558D8"]
name = "ProcessAndProcessAndProcess1784_82B558D8"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and eighty-fifth form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82B55 process cluster. 74 lines. 0 callees. 1 caller (sub_82B55A98).
[entrypoint.functions."0x82B559B8"]
name = "ProcessAndProcessAndProcess1785_82B559B8"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and eighty-sixth form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82B55 process cluster. 61 lines. 0 callees. 2 callers (sub_82B55298 and sub_82B55A98).
[entrypoint.functions."0x82B55A30"]
name = "ProcessAndProcessAndProcess1786_82B55A30"

# Process a very large data structure by constructing an array (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and eighty-seventh form). It takes a structure, constructs an array (ConstructArray16), frees size-bucketed memory (Free_SizeBucketed), performs a virtual call (VirtualCall4), and drives sub-operations (ProcessAndProcessAndProcess1661, sub_82684B38, ProcessAndProcessAndProcess1785, ProcessAndProcessAndProcess1786, ProcessAndProcessAndProcess1788) based on the result. Part of the 0x82B55 process cluster. 442 lines. 8 callees. 1 caller (ProcessAndProcessAndProcess1784).
[entrypoint.functions."0x82B55A98"]
name = "ProcessAndProcessAndProcess1787_82B55A98"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and eighty-eighth form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82B55 process cluster. 57 lines. 0 callees. 1 caller (ProcessAndProcessAndProcess1787).
[entrypoint.functions."0x82B55D98"]
name = "ProcessAndProcessAndProcess1788_82B55D98"

# Process a structure by freeing size-bucketed memory (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and eighty-ninth form). It takes a structure and frees size-bucketed memory (Free_SizeBucketed). This is a frequently-called helper in the 0x8229A process cluster. 70 lines. 1 callee. 78 callers.
[entrypoint.functions."0x8229A410"]
name = "ProcessAndProcessAndProcess1789_8229A410"

# Process a large structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and ninetieth form). It takes a structure and drives a sub-operation (ProcessAndProcessAndProcess1319). Part of the 0x82B14 process cluster. 157 lines. 1 callee. 3 callers (ProcessAndProcessAndProcess1749, sub_82395D00, sub_825EAFB8).
[entrypoint.functions."0x82B14718"]
name = "ProcessAndProcessAndProcess1790_82B14718"

# Process a very large data structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and ninety-first form). It takes a structure and drives a sub-operation (ProcessAndProcessAndProcess1793). Part of the 0x82CDC process cluster. 372 lines. 1 callee. 2 callers (sub_82CDCE38 and sub_82CDCFE8).
[entrypoint.functions."0x82CDC850"]
name = "ProcessAndProcessAndProcess1791_82CDC850"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and ninety-second form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82C42 process cluster. 12 lines. 0 callees. 0 callers.
[entrypoint.functions."0x82C42DB8"]
name = "ProcessAndProcessAndProcess1792_82C42DB8"

# Process a large structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and ninety-third form). It takes a structure and checks its state, with no external sub-operations. This is a frequently-called helper in the 0x82CDA process cluster. 189 lines. 0 callees. 2 callers (ProcessAndProcessAndProcess1755 and ProcessAndProcessAndProcess1791).
[entrypoint.functions."0x82CDA478"]
name = "ProcessAndProcessAndProcess1793_82CDA478"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and ninety-fourth form). It takes a structure and checks its state, with no external sub-operations. This is a frequently-called helper in the 0x82CBB process cluster. 7 lines. 0 callees. 3 callers.
[entrypoint.functions."0x82CBB608"]
name = "ProcessAndProcessAndProcess1794_82CBB608"

# Process a large structure by processing a hash table (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and ninety-fifth form). It takes a structure, processes a chain (ProcessChain2), and processes a hash table (ProcessHashTable). This is a frequently-called helper in the 0x82366 process cluster. 183 lines. 2 callees. 11 callers.
[entrypoint.functions."0x823660D0"]
name = "ProcessAndProcessAndProcess1795_823660D0"

# Process a large structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and ninety-sixth form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82CE3 process cluster. 177 lines. 0 callees. 1 caller (sub_82CE3BD0).
[entrypoint.functions."0x82CE3988"]
name = "ProcessAndProcessAndProcess1796_82CE3988"

# Process a large structure by decrementing a refcount atomically (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and ninety-seventh form). It takes a structure, decrements a refcount atomically (DecrementRefcountAtomic), and copies memory (MemCpy_SdkRuntime). This is a frequently-called helper in the 0x82B86 process cluster. 174 lines. 2 callees. 18 callers.
[entrypoint.functions."0x82B86DF8"]
name = "ProcessAndProcessAndProcess1797_82B86DF8"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and ninety-eighth form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82A44 process cluster. 8 lines. 0 callees. 0 callers.
[entrypoint.functions."0x82A44D60"]
name = "ProcessAndProcessAndProcess1798_82A44D60"

# Process a structure by wrapping sub-operations (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and ninety-ninth form). It takes a structure, checks its state, and drives sub-operations (sub_822397A8, ProcessAndProcessAndProcess1794) based on the result. Part of the 0x82202 process cluster. 16 lines. 2 callees. 2 callers (ProcessAndProcessAndProcess652 and ProcessAndProcessAndProcess655).
[entrypoint.functions."0x822023E0"]
name = "ProcessAndProcessAndProcess1799_822023E0"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and one hundred form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82A45 process cluster. 8 lines. 0 callees. 1 caller (sub_831B4B98).
[entrypoint.functions."0x82A452F8"]
name = "ProcessAndProcessAndProcess1800_82A452F8"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and one hundred and first form). It takes a structure and checks its state, with no external sub-operations. Part of the UI text-rendering cluster in the 0x82C50 region. 63 lines. 0 callees. 0 callers.
[entrypoint.functions."0x82C50C18"]
name = "ProcessAndProcessAndProcess1801_82C50C18"

# Process a structure by destructing a refcounted object (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and one hundred and second form). It takes a structure and destructs a refcounted object (DestructRefCounted). This is a very frequently-called helper in the 0x82289 process cluster. 9 lines. 1 callee. 88 callers.
[entrypoint.functions."0x82289530"]
name = "ProcessAndProcessAndProcess1802_82289530"

# Process a structure by allocating fresh memory (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and one hundred and third form). It takes a structure, allocates fresh memory (Allocate_OrDbgBreak), and drives a sub-operation (ProcessAndProcessAndProcess1695) based on the result. This is a frequently-called helper in the 0x82816 process cluster. 133 lines. 2 callees. 9 callers.
[entrypoint.functions."0x82816D20"]
name = "ProcessAndProcessAndProcess1803_82816D20"

# Process a structure by comparing strings (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and one hundred and fourth form). It takes a structure, compares strings (CompareStrings), and drives a sub-operation (ProcessAndProcessAndProcess1750) based on the result. This is a frequently-called helper in the 0x82858 process cluster. 86 lines. 2 callees. 18 callers.
[entrypoint.functions."0x82858A30"]
name = "ProcessAndProcessAndProcess1804_82858A30"

# Process a structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and one hundred and fifth form). It takes a structure and drives a sub-operation (ProcessAndProcessAndProcess642). This is a frequently-called helper in the 0x82226 process cluster. 81 lines. 1 callee. 7 callers.
[entrypoint.functions."0x82226D48"]
name = "ProcessAndProcessAndProcess1805_82226D48"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and one hundred and sixth form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x8228F process cluster. 31 lines. 0 callees. 0 callers.
[entrypoint.functions."0x8228F2F0"]
name = "ProcessAndProcessAndProcess1806_8228F2F0"

# Process a very large data structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and one hundred and seventh form). It takes a structure and drives a sub-operation (ProcessAndProcessAndProcess1009). Part of the 0x82816 process cluster. 324 lines. 1 callee. 1 caller (ProcessAndProcessAndProcess632).
[entrypoint.functions."0x828167A0"]
name = "ProcessAndProcessAndProcess1807_828167A0"

# Process a structure by wrapping sub-operations (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and one hundred and eighth form). It takes a structure, checks its state, and drives sub-operations (ProcessAndProcessAndProcess1755, ProcessAndProcessAndProcess1791) based on the result. Part of the 0x82CDC process cluster. 101 lines. 2 callees. 0 callers.
[entrypoint.functions."0x82CDCE38"]
name = "ProcessAndProcessAndProcess1808_82CDCE38"

# Process a large structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and one hundred and ninth form). It takes a structure and checks its state, with no external sub-operations. This is a frequently-called helper in the 0x82AAF process cluster. 150 lines. 0 callees. 3 callers.
[entrypoint.functions."0x82AAF350"]
name = "ProcessAndProcessAndProcess1809_82AAF350"

# Process a large structure by getting a singleton (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and one hundred and tenth form). It takes a structure, gets a singleton (GetSingleton), initializes a string (ProcessAndInitializeString), processes an indexed region (ProcessIndexedRegion2), and drives a sub-operation (ProcessAndProcessAndProcess335) based on the result. Part of the 0x82BA9 process cluster. 138 lines. 4 callees. 1 caller (ProcessAndProcessAndProcess1766).
[entrypoint.functions."0x82BA9E18"]
name = "ProcessAndProcessAndProcess1810_82BA9E18"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and one hundred and eleventh form). It takes a structure and checks its state, with no external sub-operations. This is a frequently-called helper in the 0x82C0 process cluster. 12 lines. 0 callees. 12 callers.
[entrypoint.functions."0x82C00018"]
name = "ProcessAndProcessAndProcess1811_82C00018"

# Process a very large data structure by allocating fresh memory (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and one hundred and twelfth form). It takes a structure, allocates fresh memory (Allocate_OrDbgBreak), sets a string (SetString), frees size-bucketed memory (Free_SizeBucketed), and drives sub-operations (ProcessAndFreeAndProcess9, ProcessAndProcessAndProcess231, sub_822F1F00, sub_824AF0E8, sub_826C3EF0, sub_826C3FA8) based on the result. Part of the 0x822CC process cluster. 352 lines. 8 callees. 6 callers.
[entrypoint.functions."0x822CC120"]
name = "ProcessAndProcessAndProcess1812_822CC120"

# Process a very large data structure by allocating fresh memory (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and one hundred and thirteenth form). It takes a structure, sets a string (SetString), frees size-bucketed memory (Free_SizeBucketed), and drives sub-operations (ProcessAndFreeAndProcess9, ProcessAndProcessAndProcess1039, ProcessAndProcessAndProcess231, sub_822F1F00, sub_824AF0E8, sub_826C3EF0, sub_826C3FA8) based on the result. Part of the 0x824AE process cluster. 583 lines. 9 callees. 7 callers.
[entrypoint.functions."0x824AE5C8"]
name = "ProcessAndProcessAndProcess1813_824AE5C8"

# Process a structure by calling methods (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and one hundred and fourteenth form). It takes a structure, calls methods (ProcessAndCallMethods), and checks a method (ProcessAndCheckMethod) based on the result. This is a frequently-called helper in the 0x82C4D process cluster. 60 lines. 2 callees. 5 callers.
[entrypoint.functions."0x82C4D930"]
name = "ProcessAndProcessAndProcess1814_82C4D930"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and one hundred and fifteenth form). It takes a structure and checks its state, with no external sub-operations. This is a frequently-called helper in the 0x82BB3 process cluster. 100 lines. 0 callees. 5 callers.
[entrypoint.functions."0x82BB3368"]
name = "ProcessAndProcessAndProcess1815_82BB3368"

# Process a structure by freeing memory (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and one hundred and sixteenth form). It takes a structure and frees memory (Free_Thunk). Part of the 0x82C4C process cluster. 36 lines. 1 callee. 0 callers.
[entrypoint.functions."0x82C4CA00"]
name = "ProcessAndProcessAndProcess1816_82C4CA00"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and one hundred and seventeenth form). It takes a structure and checks its state, with no external sub-operations. This is a frequently-called helper in the 0x82C55 process cluster. 81 lines. 0 callees. 6 callers.
[entrypoint.functions."0x82C55338"]
name = "ProcessAndProcessAndProcess1817_82C55338"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and one hundred and eighteenth form). It takes a structure and checks its state, with no external sub-operations. This is a frequently-called helper in the 0x8241C process cluster. 61 lines. 0 callees. 2 callers (sub_8241BF80 and sub_8241C140).
[entrypoint.functions."0x8241C538"]
name = "ProcessAndProcessAndProcess1818_8241C538"

# Process a very large data structure by constructing a refcounted object (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and one hundred and nineteenth form). It takes a structure, constructs a refcounted object (ConstructRefCounted), copies a refcounted object (CopyAssign_RefCounted), destructs a refcounted object (DestructRefCounted), replaces a refcounted object (ProcessAndReplaceRefCounted), releases a global reference (ReleaseGlobalRef), hashes a C string (HashCstring), increments a counter (ProcessAndIncrementCounter), and drives sub-operations (ProcessAndDispatchAndProcess25, ProcessAndVcallAndClear3, ProcessAndVcallAndProcess16, ProcessAndProcessAndProcess26, and many others) based on the result. This is one of the largest functions in the binary in the 0x826C7 process cluster. 2204 lines. 29 callees. 0 callers.
[entrypoint.functions."0x826C72A8"]
name = "ProcessAndProcessAndProcess1819_826C72A8"

# Process a very large data structure by allocating fresh memory (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and one hundred and twentieth form). It takes a structure, allocates fresh memory (Allocate_OrDbgBreak), sets a string (SetString), and drives sub-operations (ProcessAndFreeAndProcess9, sub_822F1F00, sub_826C3EF0, sub_826C3FA8, sub_829FDF90, sub_829FE010) based on the result. Part of the 0x8217F process cluster. 343 lines. 8 callees. 1 caller (ProcessAndProcessAndProcess1821).
[entrypoint.functions."0x8217F708"]
name = "ProcessAndProcessAndProcess1820_8217F708"

# Process a large structure by finding a chain root (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and one hundred and twenty-first form). It takes a structure, finds a chain root (FindChainRoot5), and drives a sub-operation (ProcessAndProcessAndProcess1820) based on the result. Part of the 0x8217F process cluster. 185 lines. 2 callees. 1 caller (ProcessAndProcessAndProcess1627).
[entrypoint.functions."0x8217F978"]
name = "ProcessAndProcessAndProcess1821_8217F978"

# Process a very large data structure by finding a chain root (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and one hundred and twenty-second form). It takes a structure, finds a chain root (FindChainRoot), sets a string (SetString), frees size-bucketed memory (Free_SizeBucketed), and drives sub-operations (ProcessAndFreeAndProcess9, sub_822F1F00, sub_826C3EF0, sub_826C3FA8, sub_829FDF90, sub_829FE010) based on the result. Part of the 0x822B7 process cluster. 591 lines. 9 callees. 2 callers (ProcessAndProcessAndProcess1627 and sub_829FEC30).
[entrypoint.functions."0x822B7258"]
name = "ProcessAndProcessAndProcess1822_822B7258"

# Process a structure by releasing a refcounted object (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and one hundred and twenty-third form). It takes a structure and releases a refcounted object (Release_RefCounted). Part of the 0x829FD process cluster. 61 lines. 1 callee. 1 caller (ProcessAndProcessAndProcess1627).
[entrypoint.functions."0x829FD878"]
name = "ProcessAndProcessAndProcess1823_829FD878"

# Process a structure by freeing size-bucketed memory (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and one hundred and twenty-fourth form). It takes a structure, frees size-bucketed memory (Free_SizeBucketed), drives a sub-operation (ProcessAndProcessAndProcess1120), releases a refcounted object (Release_RefCounted), and processes (ProcessAndProcessAndProcess1789) based on the result. This is a frequently-called helper in the 0x83222 process cluster. 79 lines. 4 callees. 10 callers.
[entrypoint.functions."0x83222AA0"]
name = "ProcessAndProcessAndProcess1824_83222AA0"

# Process a structure by constructing a refcounted object (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and one hundred and twenty-fifth form). It takes a structure, constructs a 16-byte object (Construct16Byte), constructs a refcounted object (ConstructRefCounted), destructs a refcounted object (DestructRefCounted), and compares strings ignoring case (StrCmpIgnoreCase). Part of the 0x82C01 process cluster. 89 lines. 4 callees. 0 callers.
[entrypoint.functions."0x82C01330"]
name = "ProcessAndProcessAndProcess1825_82C01330"

# Process a structure by constructing a 16-byte object (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and one hundred and twenty-sixth form). It takes a structure, constructs a 16-byte object (Construct16Byte), and drives sub-operations (ProcessAndProcessAndProcess1641, ProcessAndProcessAndProcess730, ProcessAndProcessAndProcess925) based on the result. Part of the 0x82C47 process cluster. 125 lines. 4 callees. 1 caller (sub_82C47BC0).
[entrypoint.functions."0x82C47978"]
name = "ProcessAndProcessAndProcess1826_82C47978"

# Process a large structure by wrapping sub-operations (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and one hundred and twenty-seventh form). It takes a structure, checks its state, checks a valid pointer (CheckValidPointer), and drives sub-operations (ProcessAndProcessAndProcess1120, ProcessAndProcessAndProcess1621, ProcessAndProcessAndProcess1712) based on the result. Part of the 0x83222 process cluster. 118 lines. 4 callees. 2 callers (sub_8322C2E8 and sub_8322CA88).
[entrypoint.functions."0x83222BE0"]
name = "ProcessAndProcessAndProcess1827_83222BE0"

# Process a very large data structure by releasing a refcounted object (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and one hundred and twenty-eighth form). It takes a structure, releases a refcounted object (Release_RefCounted), and drives sub-operations (sub_82A08B68, sub_82A08C80, sub_82A08D98, sub_82A08EB0, sub_83228530, sub_8322AB48, sub_83235D80, sub_83238E68, sub_83239630, sub_83239D98) based on the result. Part of the 0x83224 process cluster. 994 lines. 11 callees. 3 callers (sub_83224818, sub_83224FD0, sub_8322CA88).
[entrypoint.functions."0x83224138"]
name = "ProcessAndProcessAndProcess1828_83224138"

# Process a structure by zeroing memory (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and one hundred and twenty-ninth form). It takes a structure, zeroes memory (MemSet_SdkRuntime), and drives sub-operations (sub_82238740, ProcessAndProcessAndProcess1833) based on the result. Part of the 0x82238 process cluster. 93 lines. 3 callees. 1 caller (ProcessAndProcessAndProcess652).
[entrypoint.functions."0x822386A8"]
name = "ProcessAndProcessAndProcess1829_822386A8"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and one hundred and thirtieth form). It takes a structure and checks its state, with no external sub-operations. Part of the UI text-rendering cluster in the 0x82C50 region. 19 lines. 0 callees. 0 callers.
[entrypoint.functions."0x82C50C88"]
name = "ProcessAndProcessAndProcess1830_82C50C88"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and one hundred and thirty-first form). It takes a structure and checks its state, with no external sub-operations. Part of the UI text-rendering cluster in the 0x82C51 region. 22 lines. 0 callees. 2 callers (sub_82C50CA8 and sub_82C51678).
[entrypoint.functions."0x82C51290"]
name = "ProcessAndProcessAndProcess1831_82C51290"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and one hundred and thirty-second form). It takes a structure and checks its state, with no external sub-operations. Part of the UI text-rendering cluster in the 0x82C51 region. 31 lines. 0 callees. 2 callers (sub_82C44688 and sub_82C46010).
[entrypoint.functions."0x82C512B8"]
name = "ProcessAndProcessAndProcess1832_82C512B8"

# Process a structure by setting a global field (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and one hundred and thirty-third form). It takes a structure and sets a global field (SetGlobalField_Thunk). This is a frequently-called helper in the 0x82CBB process cluster. 80 lines. 1 callee. 2 callers (ProcessAndProcessAndProcess1829 and sub_82B38380).
[entrypoint.functions."0x82CBB580"]
name = "ProcessAndProcessAndProcess1833_82CBB580"

# Process a structure by wrapping sub-operations (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and one hundred and thirty-fourth form). It takes a structure, checks its state, and drives sub-operations (ProcessAndProcessAndProcess313, ProcessAndProcessAndProcess314, ProcessAndProcessAndProcess765, ProcessAndProcessAndProcess766) based on the result. This is a frequently-called helper in the 0x8229B process cluster. 78 lines. 4 callees. 5 callers.
[entrypoint.functions."0x8229B5D0"]
name = "ProcessAndProcessAndProcess1834_8229B5D0"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and one hundred and thirty-fifth form). It takes a structure and checks its state, with no external sub-operations. This is a frequently-called helper in the 0x82CDF process cluster. 92 lines. 0 callees. 3 callers.
[entrypoint.functions."0x82CDFA78"]
name = "ProcessAndProcessAndProcess1835_82CDFA78"

# Process a structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and one hundred and thirty-sixth form). It takes a structure and drives a sub-operation (ProcessAndProcessAndProcess1835). Part of the 0x82CE3 process cluster. 9 lines. 1 callee. 1 caller (sub_82CE3748).
[entrypoint.functions."0x82CE3230"]
name = "ProcessAndProcessAndProcess1836_82CE3230"

# Process a structure by decrementing and disposing (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and one hundred and thirty-seventh form). It takes a structure, decrements and disposes (ProcessAndDecrementDispose), and drives a sub-operation (ProcessAndProcessAndProcess1217) based on the result. This is a frequently-called helper in the 0x827DE process cluster. 75 lines. 2 callees. 4 callers.
[entrypoint.functions."0x827DEDE0"]
name = "ProcessAndProcessAndProcess1837_827DEDE0"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and one hundred and thirty-eighth form). It takes a structure and checks its state, with no external sub-operations. This is a frequently-called helper in the 0x829FD process cluster. 71 lines. 0 callees. 2 callers (ProcessAndProcessAndProcess1820 and ProcessAndProcessAndProcess1822).
[entrypoint.functions."0x829FDF90"]
name = "ProcessAndProcessAndProcess1838_829FDF90"

# Process a very large data structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and one hundred and thirty-ninth form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82BA9 process cluster. 401 lines. 0 callees. 1 caller (ProcessAndProcessAndProcess_82174D38).
[entrypoint.functions."0x82BA9B40"]
name = "ProcessAndProcessAndProcess1839_82BA9B40"

# Process a structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and one hundred and fortieth form). It takes a structure and drives a sub-operation (ProcessAndProcessAndProcess335). Part of the 0x82BA9 process cluster. 80 lines. 1 callee. 0 callers.
[entrypoint.functions."0x82BA9F98"]
name = "ProcessAndProcessAndProcess1840_82BA9F98"

# Process a structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and one hundred and forty-first form). It takes a structure and drives a sub-operation (ProcessAndProcessAndProcess1842). Part of the 0x82BB2 process cluster. 30 lines. 1 callee. 0 callers.
[entrypoint.functions."0x82BB2FA0"]
name = "ProcessAndProcessAndProcess1841_82BB2FA0"

# Process a very large data structure by getting a typed field (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and one hundred and forty-second form). It takes a structure, gets a typed field (GetTypedField), hashes a C string (HashCstring), processes a loop dispatch (ProcessLoopDispatch), reads a converted value (ProcessAndReadConvertedValue), stores an element (ProcessAndStoreElement), and drives sub-operations (ProcessAndCheckAndProcess3, ProcessAndProcessAndProcess154, ProcessAndProcessAndProcess1776, ProcessAndProcessAndProcess1815, sub_82188568, sub_82BC5870, sub_82BC68F0) based on the result. This is a frequently-called processor in the 0x82BB3 process cluster. 371 lines. 12 callees. 7 callers.
[entrypoint.functions."0x82BB30D8"]
name = "ProcessAndProcessAndProcess1842_82BB30D8"

# Process a structure by freeing memory (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and one hundred and forty-third form). It takes a structure and frees memory (Free_Thunk). Part of the UI text-rendering cluster in the 0x82C51 region. 56 lines. 1 callee. 2 callers (sub_82C50D00 and sub_82C516C8).
[entrypoint.functions."0x82C51430"]
name = "ProcessAndProcessAndProcess1843_82C51430"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and one hundred and forty-fourth form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82C0B process cluster. 28 lines. 0 callees. 0 callers.
[entrypoint.functions."0x82C0B590"]
name = "ProcessAndProcessAndProcess1844_82C0B590"

# Process a structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and one hundred and forty-fifth form). It takes a structure and drives a sub-operation (ProcessAndProcessAndProcess1846). Part of the 0x82C23 process cluster. 11 lines. 1 callee. 0 callers.
[entrypoint.functions."0x82C239C0"]
name = "ProcessAndProcessAndProcess1845_82C239C0"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and one hundred and forty-sixth form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82C4F process cluster. 29 lines. 0 callees. 1 caller (ProcessAndProcessAndProcess1845).
[entrypoint.functions."0x82C4FBC8"]
name = "ProcessAndProcessAndProcess1846_82C4FBC8"

# Process a very large data structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and one hundred and forty-seventh form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82CD8 process cluster. 352 lines. 0 callees. 0 callers.
[entrypoint.functions."0x82CD88C0"]
name = "ProcessAndProcessAndProcess1847_82CD88C0"

# Process a very large data structure by constructing a refcounted object (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and one hundred and forty-eighth form). It takes a structure, constructs a refcounted object (ConstructRefCounted), copies a refcounted object (CopyAssign_RefCounted), destructs a refcounted object (DestructRefCounted), hashes a C string (HashCstring), moves memory (MemMove_SdkRuntime), searches for a substring offset (SearchAndFindSubstringOffset), shrinks a buffer (ShrinkBufferToFit), and drives sub-operations (AtomicIncrement5, GetSubObjField4, ProcessAndDispatchAndProcess25, ProcessAtomicIncrement, Release_RefCounted) based on the result. This is a frequently-called processor in the 0x823F8 process cluster. 305 lines. 12 callees. 43 callers.
[entrypoint.functions."0x823F8A38"]
name = "ProcessAndProcessAndProcess1848_823F8A38"

# Process a large structure by freeing size-bucketed memory (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and one hundred and forty-ninth form). It takes a structure, frees size-bucketed memory (Free_SizeBucketed), and drives a sub-operation (sub_821756E0) based on the result. Part of the 0x822D5 process cluster. 172 lines. 2 callees. 2 callers (ProcessAndProcessAndProcess705 and sub_822D6A80).
[entrypoint.functions."0x822D5350"]
name = "ProcessAndProcessAndProcess1849_822D5350"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and one hundred and fiftieth form). It takes a structure and checks its state, with no external sub-operations. This is a frequently-called helper in the 0x83217 process cluster. 67 lines. 0 callees. 10 callers.
[entrypoint.functions."0x832179C0"]
name = "ProcessAndProcessAndProcess1850_832179C0"

# Process a very large data structure by wrapping sub-operations (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and one hundred and fifty-first form). It takes a structure, checks its state, and drives sub-operations (sub_82C57AC8, sub_82C58318, sub_82C58398, sub_82C58680) based on the result. Part of the 0x82C58 process cluster. 563 lines. 4 callees. 1 caller (sub_82C59000).
[entrypoint.functions."0x82C58890"]
name = "ProcessAndProcessAndProcess1851_82C58890"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and one hundred and fifty-second form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x822D3 process cluster. 15 lines. 0 callees. 0 callers.
[entrypoint.functions."0x822D3F88"]
name = "ProcessAndProcessAndProcess1852_822D3F88"

# Process a structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and one hundred and fifty-third form). It takes a structure and drives a sub-operation (ProcessAndProcessAndProcess1248). Part of the 0x82A7F process cluster. 65 lines. 1 callee. 1 caller (sub_82A7FBC0).
[entrypoint.functions."0x82A7FE80"]
name = "ProcessAndProcessAndProcess1853_82A7FE80"

# Process a large structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and one hundred and fifty-fourth form). It takes a structure and checks its state, with no external sub-operations. This is a frequently-called helper in the 0x82C20 process cluster. 181 lines. 0 callees. 2 callers (sub_82C216A8 and sub_82C22FB0).
[entrypoint.functions."0x82C20CD8"]
name = "ProcessAndProcessAndProcess1854_82C20CD8"

# Process a large structure by setting an event (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and one hundred and fifty-fifth form). It takes a structure, sets an event (SetEvent_SdkRuntime), drives a sub-operation (SwitchDispatch), and processes (ProcessAndProcessAndProcess1645) based on the result. Part of the 0x82B57 process cluster. 181 lines. 4 callees. 1 caller (ProcessAndProcessAndProcess1644).
[entrypoint.functions."0x82B57010"]
name = "ProcessAndProcessAndProcess1855_82B57010"

# Process a structure by hashing a C string (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and one hundred and fifty-sixth form). It takes a structure, hashes a C string (HashCstring), and drives sub-operations (ProcessAndProcessAndProcess17, sub_823FE8F8) based on the result. This is a frequently-called helper in the 0x8259D process cluster. 128 lines. 3 callees. 12 callers.
[entrypoint.functions."0x8259D5F8"]
name = "ProcessAndProcessAndProcess1856_8259D5F8"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and one hundred and fifty-seventh form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82B4F process cluster. 47 lines. 0 callees. 0 callers.
[entrypoint.functions."0x82B4FA30"]
name = "ProcessAndProcessAndProcess1857_82B4FA30"

# Process a structure by constructing a 16-byte object (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and one hundred and fifty-eighth form). It takes a structure and constructs a 16-byte object (Construct16Byte). This is a frequently-called helper in the 0x82275 process cluster. 109 lines. 1 callee. 3 callers.
[entrypoint.functions."0x822752B0"]
name = "ProcessAndProcessAndProcess1858_822752B0"

# Process a large structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and one hundred and fifty-ninth form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82BE6 process cluster. 225 lines. 0 callees. 1 caller (sub_82BE5338).
[entrypoint.functions."0x82BE66B8"]
name = "ProcessAndProcessAndProcess1859_82BE66B8"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and one hundred and sixtieth form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82C1D process cluster. 41 lines. 0 callees. 0 callers.
[entrypoint.functions."0x82C1DDF8"]
name = "ProcessAndProcessAndProcess1860_82C1DDF8"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and one hundred and sixty-first form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82CDB process cluster. 22 lines. 0 callees. 1 caller (ProcessAndProcessAndProcess1863).
[entrypoint.functions."0x82CDBAA0"]
name = "ProcessAndProcessAndProcess1861_82CDBAA0"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and one hundred and sixty-second form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82CDB process cluster. 78 lines. 0 callees. 0 callers.
[entrypoint.functions."0x82CDBB80"]
name = "ProcessAndProcessAndProcess1862_82CDBB80"

# Process a structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and one hundred and sixty-third form). It takes a structure and drives a sub-operation (ProcessAndProcessAndProcess1861). This is a frequently-called helper in the 0x82CE5 process cluster. 88 lines. 1 callee. 3 callers.
[entrypoint.functions."0x82CE5C70"]
name = "ProcessAndProcessAndProcess1863_82CE5C70"

# Process a structure by incrementing atomically (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and one hundred and sixty-fourth form). It takes a structure, increments atomically (AtomicIncrement3), and constructs a 16-byte object (Construct16Byte). This is a frequently-called helper in the 0x821D5 process cluster. 39 lines. 2 callees. 17 callers.
[entrypoint.functions."0x821D5BA0"]
name = "ProcessAndProcessAndProcess1864_821D5BA0"

# Process a structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and one hundred and sixty-fifth form). It takes a structure and drives a sub-operation (sub_82C14AC0). Part of the 0x82C14 process cluster. 19 lines. 1 callee. 0 callers.
[entrypoint.functions."0x82C14C80"]
name = "ProcessAndProcessAndProcess1865_82C14C80"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and one hundred and sixty-sixth form). It takes a structure and checks its state, with no external sub-operations. This is a frequently-called helper in the 0x82CDB process cluster. 22 lines. 0 callees. 3 callers.
[entrypoint.functions."0x82CDBA80"]
name = "ProcessAndProcessAndProcess1866_82CDBA80"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and one hundred and sixty-seventh form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82CDB process cluster. 76 lines. 0 callees. 0 callers.
[entrypoint.functions."0x82CDBAF8"]
name = "ProcessAndProcessAndProcess1867_82CDBAF8"

# Process a structure by wrapping sub-operations (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and one hundred and sixty-eighth form). It takes a structure, checks its state, and drives sub-operations (ProcessAndProcessAndProcess1326, ProcessAndProcessAndProcess620) based on the result. This is a frequently-called helper in the 0x82B7B process cluster. 81 lines. 2 callees. 10 callers.
[entrypoint.functions."0x82B7B870"]
name = "ProcessAndProcessAndProcess1868_82B7B870"

# Process a large structure by allocating fresh memory (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and one hundred and sixty-ninth form). It takes a structure, allocates fresh memory (Allocate_OrDbgBreak), and reallocates a buffer (ProcessAndReallocateBuffer). This is a frequently-called helper in the 0x82B39 process cluster. 112 lines. 2 callees. 4 callers.
[entrypoint.functions."0x82B396A8"]
name = "ProcessAndProcessAndProcess1869_82B396A8"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and one hundred and seventieth form). It takes a structure and checks its state, with no external sub-operations. This is a frequently-called helper in the 0x82C50 process cluster. 8 lines. 0 callees. 2 callers (UITextPrompt_RenderElement and sub_82C441B0).
[entrypoint.functions."0x82C50A88"]
name = "ProcessAndProcessAndProcess1870_82C50A88"

# Process a structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and one hundred and seventy-first form). It takes a structure and drives a sub-operation (ProcessAndProcessAndProcess1694). This is a frequently-called helper in the 0x827DF process cluster. 56 lines. 1 callee. 4 callers.
[entrypoint.functions."0x827DFDB8"]
name = "ProcessAndProcessAndProcess1871_827DFDB8"

# Process a very large data structure by computing a wrapped value (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and one hundred and seventy-second form). It takes a structure and computes a wrapped value (ComputeWrapped). This is a frequently-called processor in the 0x82B8B process cluster. 419 lines. 1 callee. 5 callers.
[entrypoint.functions."0x82B8BEA0"]
name = "ProcessAndProcessAndProcess1872_82B8BEA0"

# Process a large structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and one hundred and seventy-third form). It takes a structure and drives a sub-operation (ProcessAndProcessAndProcess1874). This is a frequently-called helper in the 0x82BE5 process cluster. 136 lines. 1 callee. 4 callers.
[entrypoint.functions."0x82BE5700"]
name = "ProcessAndProcessAndProcess1873_82BE5700"

# Process a very large data structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and one hundred and seventy-fourth form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82BE6 process cluster. 395 lines. 0 callees. 1 caller (ProcessAndProcessAndProcess1873).
[entrypoint.functions."0x82BE6CF8"]
name = "ProcessAndProcessAndProcess1874_82BE6CF8"

# Process a structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and one hundred and seventy-fifth form). It takes a structure and drives a sub-operation (ProcessAndProcessAndProcess230). This is a frequently-called helper in the 0x82343 process cluster. 104 lines. 1 callee. 7 callers.
[entrypoint.functions."0x82343C68"]
name = "ProcessAndProcessAndProcess1875_82343C68"

# Process a very large data structure by constructing a refcounted object (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and one hundred and seventy-sixth form). It takes a structure, constructs a refcounted object (ConstructRefCounted), destructs a refcounted object (DestructRefCounted), finds a chain value (FindChainValue), compares strings ignoring case (StrCmpIgnoreCase), and drives sub-operations (sub_82BFF7A0, sub_82BFF7D8, ProcessAndProcessAndProcess1880) based on the result. Part of the 0x82C02 process cluster. 344 lines. 7 callees. 0 callers.
[entrypoint.functions."0x82C02C30"]
name = "ProcessAndProcessAndProcess1876_82C02C30"

# Process a structure by incrementing a refcount atomically (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and one hundred and seventy-seventh form). It takes a structure, increments a refcount atomically (IncrementRefcountAtomic), and drives a sub-operation (ProcessAndProcessAndProcess524) based on the result. This is a frequently-called helper in the 0x82A46 process cluster. 55 lines. 2 callees. 10 callers.
[entrypoint.functions."0x82A46D90"]
name = "ProcessAndProcessAndProcess1877_82A46D90"

# Process a large structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and one hundred and seventy-eighth form). It takes a structure, checks its state, and drives sub-operations (ProcessAndProcessAndProcess1812, sub_8273ACB8) based on the result. Part of the 0x82B68 process cluster. 261 lines. 2 callees. 3 callers.
[entrypoint.functions."0x82B68518"]
name = "ProcessAndProcessAndProcess1878_82B68518"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and one hundred and seventy-ninth form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82B6B process cluster. 83 lines. 0 callees. 0 callers.
[entrypoint.functions."0x82B6BE18"]
name = "ProcessAndProcessAndProcess1879_82B6BE18"

# Process a structure by processing elements (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and one hundred and eightieth form). It takes a structure and processes elements (ProcessElements_Vcall4). Part of the 0x82C02 process cluster. 49 lines. 1 callee. 1 caller (ProcessAndProcessAndProcess1876).
[entrypoint.functions."0x82C027B0"]
name = "ProcessAndProcessAndProcess1880_82C027B0"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and one hundred and eighty-first form). It takes a structure and checks its state, with no external sub-operations. This is a frequently-called helper in the 0x82C0B process cluster. 51 lines. 0 callees. 3 callers.
[entrypoint.functions."0x82C0B7F8"]
name = "ProcessAndProcessAndProcess1881_82C0B7F8"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and one hundred and eighty-second form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82CD4 process cluster. 53 lines. 0 callees. 0 callers.
[entrypoint.functions."0x82CD43F0"]
name = "ProcessAndProcessAndProcess1882_82CD43F0"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and one hundred and eighty-third form). It takes a structure and checks its state, with no external sub-operations. This is a frequently-called helper in the 0x82B16 process cluster. 70 lines. 0 callees. 2 callers (ProcessAndProcessAndProcess851 and sub_82B18A20).
[entrypoint.functions."0x82B16D30"]
name = "ProcessAndProcessAndProcess1883_82B16D30"

# Process a structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and one hundred and eighty-fourth form). It takes a structure and drives a sub-operation (ProcessAndProcessAndProcess1885). This is a frequently-called helper in the 0x82CD2 process cluster. 54 lines. 1 callee. 4 callers.
[entrypoint.functions."0x82CD2C98"]
name = "ProcessAndProcessAndProcess1884_82CD2C98"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and one hundred and eighty-fifth form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82CDC process cluster. 60 lines. 0 callees. 1 caller (ProcessAndProcessAndProcess1884).
[entrypoint.functions."0x82CDC180"]
name = "ProcessAndProcessAndProcess1885_82CDC180"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and one hundred and eighty-sixth form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82C6C process cluster. 69 lines. 0 callees. 1 caller (ProcessAndValidateAndConstruct_82C66570).
[entrypoint.functions."0x82C6C6C0"]
name = "ProcessAndProcessAndProcess1886_82C6C6C0"

# Process a structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and one hundred and eighty-seventh form). It takes a structure and drives a sub-operation (ProcessAndProcessAndProcess1657). This is a frequently-called helper in the 0x82CD9 process cluster. 11 lines. 1 callee. 5 callers.
[entrypoint.functions."0x82CD9FC8"]
name = "ProcessAndProcessAndProcess1887_82CD9FC8"

# Process a structure by wrapping sub-operations (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and one hundred and eighty-eighth form). It takes a structure, checks its state, and drives sub-operations (sub_82B84060, ProcessAndProcessAndProcess1889, sub_82BE63B8, sub_82CBBB58) based on the result. This is a frequently-called helper in the 0x82B83 process cluster. 125 lines. 4 callees. 8 callers.
[entrypoint.functions."0x82B83ED0"]
name = "ProcessAndProcessAndProcess1888_82B83ED0"

# Process a large structure by entering a critical section (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and one hundred and eighty-ninth form). It takes a structure, enters a critical section (CriticalSection_EnterOrTry), and drives a sub-operation (ProcessAndProcessAndProcess1890) based on the result. Part of the 0x82BE5 process cluster. 142 lines. 2 callees. 1 caller (ProcessAndProcessAndProcess1888).
[entrypoint.functions."0x82BE5240"]
name = "ProcessAndProcessAndProcess1889_82BE5240"

# Process a large structure by wrapping sub-operations (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and one hundred and ninetieth form). It takes a structure, checks its state, and drives sub-operations (ProcessAndProcessAndProcess1859, ProcessAndProcessAndProcess1891, ProcessAndProcessAndProcess1892, sub_82BE6508) based on the result. Part of the 0x82BE5 process cluster. 163 lines. 4 callees. 2 callers (ProcessAndProcessAndProcess1889 and sub_82BE6300).
[entrypoint.functions."0x82BE5338"]
name = "ProcessAndProcessAndProcess1890_82BE5338"

# Process a large structure by wrapping sub-operations (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and one hundred and ninety-first form). It takes a structure, checks its state, and drives sub-operations (ProcessAndProcessAndProcess1873, ProcessAndProcessAndProcess1892) based on the result. Part of the 0x82BE5 process cluster. 261 lines. 2 callees. 2 callers (ProcessAndProcessAndProcess1890 and sub_82BE5FF8).
[entrypoint.functions."0x82BE5458"]
name = "ProcessAndProcessAndProcess1891_82BE5458"

# Process a structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and one hundred and ninety-second form). It takes a structure and drives a sub-operation (ProcessAndProcessAndProcess1893). This is a frequently-called helper in the 0x82BE5 process cluster. 92 lines. 1 callee. 4 callers.
[entrypoint.functions."0x82BE57F8"]
name = "ProcessAndProcessAndProcess1892_82BE57F8"

# Process a large structure by wrapping sub-operations (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and one hundred and ninety-third form). It takes a structure, checks its state, and drives sub-operations (sub_82BE6508, ProcessAndProcessAndProcess1894) based on the result. Part of the 0x82BE6 process cluster. 171 lines. 2 callees. 1 caller (ProcessAndProcessAndProcess1892).
[entrypoint.functions."0x82BE6860"]
name = "ProcessAndProcessAndProcess1893_82BE6860"

# Process a very large data structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and one hundred and ninety-fourth form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82BE7 process cluster. 315 lines. 0 callees. 1 caller (ProcessAndProcessAndProcess1893).
[entrypoint.functions."0x82BE7198"]
name = "ProcessAndProcessAndProcess1894_82BE7198"

# Process a structure by freeing memory (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and one hundred and ninety-fifth form). It takes a structure and frees memory (Free_Thunk). Part of the UI text-rendering cluster in the 0x82C51 region. 43 lines. 1 callee. 2 callers (sub_82C44688 and sub_82C46010).
[entrypoint.functions."0x82C51508"]
name = "ProcessAndProcessAndProcess1895_82C51508"

# Process a structure by wrapping sub-operations (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and one hundred and ninety-sixth form). It takes a structure, checks its state, and drives sub-operations (ProcessAndProcessAndProcess1780, sub_82B41DF8) based on the result. Part of the 0x82C6C process cluster. 89 lines. 2 callees. 1 caller (ProcessAndProcessAndProcess1897).
[entrypoint.functions."0x82C6C528"]
name = "ProcessAndProcessAndProcess1896_82C6C528"

# Process a structure by freeing memory (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and one hundred and ninety-seventh form). It takes a structure, frees memory (Free_Thunk), and drives a sub-operation (ProcessAndProcessAndProcess1896) based on the result. Part of the 0x82C6C process cluster. 50 lines. 2 callees. 0 callers.
[entrypoint.functions."0x82C6C738"]
name = "ProcessAndProcessAndProcess1897_82C6C738"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and one hundred and ninety-eighth form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82CE3 process cluster. 16 lines. 0 callees. 1 caller (sub_82CE1540).
[entrypoint.functions."0x82CE31A0"]
name = "ProcessAndProcessAndProcess1898_82CE31A0"

# Process a large structure by wrapping sub-operations (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and one hundred and ninety-ninth form). It takes a structure, checks its state, and drives sub-operations (ProcessAndProcessAndProcess1783, ProcessAndProcessAndProcess1900, ProcessAndProcessAndProcess1903) based on the result. Part of the 0x82B4A process cluster. 258 lines. 3 callees. 0 callers.
[entrypoint.functions."0x82B4AF20"]
name = "ProcessAndProcessAndProcess1899_82B4AF20"

# Process a structure by allocating fresh memory (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and two hundred form). It takes a structure and allocates fresh memory (Allocate_OrDbgBreak). This is a frequently-called helper in the 0x82B55 process cluster. 78 lines. 1 callee. 2 callers (sub_82B4AD78 and ProcessAndProcessAndProcess1899).
[entrypoint.functions."0x82B551C0"]
name = "ProcessAndProcessAndProcess1900_82B551C0"

# Process a structure by freeing size-bucketed memory (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and two hundred and first form). It takes a structure, frees size-bucketed memory (Free_SizeBucketed), and drives a sub-operation (ProcessAndProcessAndProcess1902) based on the result. Part of the 0x82B55 process cluster. 50 lines. 2 callees. 0 callers.
[entrypoint.functions."0x82B55248"]
name = "ProcessAndProcessAndProcess1901_82B55248"

# Process a structure by freeing size-bucketed memory (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and two hundred and second form). It takes a structure, frees size-bucketed memory (Free_SizeBucketed), and drives a sub-operation (ProcessAndProcessAndProcess1786) based on the result. Part of the 0x82B55 process cluster. 72 lines. 2 callees. 1 caller (ProcessAndProcessAndProcess1901).
[entrypoint.functions."0x82B55298"]
name = "ProcessAndProcessAndProcess1902_82B55298"

# Process a structure by copying and dispatching (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and two hundred and third form). It takes a structure, copies and dispatches (CopyAndDispatch), and sets and dispatches (SetAndDispatch). This is a frequently-called helper in the 0x82B55 process cluster. 104 lines. 2 callees. 2 callers (sub_82B4AD78 and ProcessAndProcessAndProcess1899).
[entrypoint.functions."0x82B55438"]
name = "ProcessAndProcessAndProcess1903_82B55438"

# Process a very large data structure by wrapping sub-operations (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and two hundred and fourth form). It takes a structure, checks its state, and drives sub-operations (sub_82B4FE30, sub_82B4FFE0) based on the result. Part of the 0x82B55 process cluster. 434 lines. 2 callees. 0 callers.
[entrypoint.functions."0x82B554E8"]
name = "ProcessAndProcessAndProcess1904_82B554E8"

# Process a large structure by incrementing a refcount atomically (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and two hundred and fifth form). It takes a structure, increments a refcount atomically (IncrementRefcountAtomic), and drives sub-operations (ProcessAndProcessAndProcess1323, ProcessAndProcessAndProcess652) based on the result. This is a frequently-called helper in the 0x82B92 process cluster. 117 lines. 3 callees. 19 callers.
[entrypoint.functions."0x82B920F0"]
name = "ProcessAndProcessAndProcess1905_82B920F0"

# Process a large structure by zeroing memory (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and two hundred and sixth form). It takes a structure, zeroes memory (MemSet_SdkRuntime), and drives sub-operations (ProcessAndComputeAndProcess28, ProcessAndIndexRegionAndProcess) based on the result. Part of the 0x82BAC process cluster. 207 lines. 3 callees. 1 caller (ProcessAndProcessAndProcess_82174D38).
[entrypoint.functions."0x82BAC4E8"]
name = "ProcessAndProcessAndProcess1906_82BAC4E8"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and one hundred and ninety-seventh form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82C0 process cluster. 12 lines. 0 callees. 0 callers.
[entrypoint.functions."0x82C00CE8"]
name = "ProcessAndProcessAndProcess1907_82C00CE8"

# Process a structure by incrementing atomically (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and one hundred and ninety-eighth form). It takes a structure and increments atomically (AtomicIncrement3). Part of the 0x82C01 process cluster. 56 lines. 1 callee. 1 caller (ProcessAndProcessAndProcess1910).
[entrypoint.functions."0x82C012D0"]
name = "ProcessAndProcessAndProcess1908_82C012D0"

# Process a structure by destructing a refcounted object (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and one hundred and ninety-ninth form). It takes a structure, destructs a refcounted object (DestructRefCounted), frees memory (Free_Thunk), and drives sub-operations (ProcessAndProcessAndProcess1916, sub_82C02800) based on the result. This is a frequently-called helper in the 0x82C03 process cluster. 73 lines. 4 callees. 3 callers.
[entrypoint.functions."0x82C03240"]
name = "ProcessAndProcessAndProcess1909_82C03240"

# Process a large structure by allocating fresh memory (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and two hundred and first form). It takes a structure, allocates fresh memory (Allocate_OrDbgBreak), constructs a 16-byte object (Construct16Byte), and drives sub-operations (ProcessAndProcessAndProcess1908, ProcessAndProcessAndProcess1915, sub_82C032B0) based on the result. Part of the 0x82C03 process cluster. 143 lines. 5 callees. 0 callers.
[entrypoint.functions."0x82C03808"]
name = "ProcessAndProcessAndProcess1910_82C03808"

# Process a large structure by finding a chain value (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and two hundred and second form). It takes a structure, finds a chain value (FindChainValue), frees memory (Free_Thunk), and drives sub-operations (sub_82BFF7A0, sub_82BFF7D8, ProcessAndProcessAndProcess1909) based on the result. Part of the 0x82C03 process cluster. 198 lines. 5 callees. 0 callers.
[entrypoint.functions."0x82C038F0"]
name = "ProcessAndProcessAndProcess1911_82C038F0"

# Process a very large data structure by incrementing atomically (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and two hundred and third form). It takes a structure, increments atomically (AtomicIncrement3), destructs a refcounted object (DestructRefCounted), gets a field (GetField833), processes a string (ProcessString3), compares strings ignoring case (StrCmpIgnoreCase), and drives sub-operations (ProcessAndProcessAndProcess1316, ProcessAndProcessAndProcess1641, ProcessAndProcessAndProcess1692, ProcessAndProcessAndProcess1826, ProcessAndProcessAndProcess730, ProcessAndProcessAndProcess925, sub_82BFF768, sub_82BFF7A0, sub_82BFF7D8, sub_82C47908) based on the result. Part of the 0x82C47 process cluster. 852 lines. 15 callees. 1 caller (sub_82C48180).
[entrypoint.functions."0x82C47BC0"]
name = "ProcessAndProcessAndProcess1912_82C47BC0"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and two hundred and fourth form). It takes a structure and checks its state, with no external sub-operations. This is a frequently-called helper in the 0x82CE5 process cluster. 60 lines. 0 callees. 2 callers (sub_82CE54A8 and sub_82CE5548).
[entrypoint.functions."0x82CE53B0"]
name = "ProcessAndProcessAndProcess1913_82CE53B0"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and two hundred and fifth form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82C01 process cluster. 59 lines. 0 callees. 1 caller (ProcessAndProcessAndProcess1915).
[entrypoint.functions."0x82C010E0"]
name = "ProcessAndProcessAndProcess1914_82C010E0"

# Process a large structure by allocating fresh memory (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and two hundred and sixth form). It takes a structure, allocates fresh memory (Allocate_OrDbgBreak), gets or creates a tree node (Tree_GetOrCreate), resets a tree object (ResetTreeObj), processes elements (ProcessElements_Vcall4), and drives a sub-operation (ProcessAndProcessAndProcess1914) based on the result. Part of the 0x82C02 process cluster. 95 lines. 5 callees. 1 caller (ProcessAndProcessAndProcess1910).
[entrypoint.functions."0x82C02610"]
name = "ProcessAndProcessAndProcess1915_82C02610"

# Process a structure by freeing memory (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and two hundred and seventh form). It takes a structure, frees memory (Free_Thunk), processes elements (ProcessElements_Vcall4), processes a tree (Tree_Process_Wrapper), and drives sub-operations (NoOp_Stub, ProcessAndProcessAndProcess1763) based on the result. Part of the 0x82C02 process cluster. 85 lines. 5 callees. 1 caller (ProcessAndProcessAndProcess1909).
[entrypoint.functions."0x82C026A8"]
name = "ProcessAndProcessAndProcess1916_82C026A8"

# Process a structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and two hundred and eighth form). It takes a structure and drives a sub-operation (sub_82CBB610). This is a frequently-called helper in the 0x82C08 process cluster. 85 lines. 1 callee. 2 callers (ProcessAndProcessAndProcess1277 and sub_82C0A4F0).
[entrypoint.functions."0x82C08778"]
name = "ProcessAndProcessAndProcess1917_82C08778"

# Process a structure by wrapping sub-operations (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and two hundred and ninth form). It takes a structure, checks its state, and drives sub-operations (ProcessAndProcessAndProcess626, sub_82CFE4C8) based on the result. This is a frequently-called helper in the 0x82CFC process cluster. 90 lines. 2 callees. 3 callers.
[entrypoint.functions."0x82CFC8E8"]
name = "ProcessAndProcessAndProcess1918_82CFC8E8"

# Process a structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and two hundred and twelfth form). It takes a structure and drives a sub-operation (ProcessAndProcessAndProcess1920). This is a frequently-called helper in the 0x82CFE process cluster. 27 lines. 1 callee. 5 callers.
[entrypoint.functions."0x82CFE4C8"]
name = "ProcessAndProcessAndProcess1919_82CFE4C8"

# Process a structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and two hundred and thirteenth form). It takes a structure and drives a sub-operation (ProcessAndProcessAndProcess626). Part of the 0x82CFE process cluster. 93 lines. 1 callee. 1 caller (ProcessAndProcessAndProcess1919).
[entrypoint.functions."0x82CFE978"]
name = "ProcessAndProcessAndProcess1920_82CFE978"

# Process a large structure by allocating fresh memory (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and two hundred and fourteenth form). It takes a structure, allocates fresh memory (Allocate_OrDbgBreak), and drives sub-operations (sub_82A6D4C8, sub_82A6E310) based on the result. This is a frequently-called helper in the 0x82215 process cluster. 166 lines. 3 callees. 3 callers.
[entrypoint.functions."0x82215378"]
name = "ProcessAndProcessAndProcess1921_82215378"

# Process a very large data structure by constructing a 9-byte object (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and two hundred and fifteenth form). It takes a structure, constructs a 9-byte object (Construct9), copies a refcounted object (CopyAssign_RefCounted), copies a structure (CopyStructure), destructs a refcounted object (DestructRefCounted), constructs a string (ProcessConstructString), processes a lower-case string (ProcessToLower), processes a vector (ProcessVector2), releases a refcounted object (Release_RefCounted), sets a string buffer (SetStringBuffer), and drives sub-operations (ProcessAndProcessAndProcess1226, ProcessAndProcessAndProcess565) based on the result. Part of the 0x82817 process cluster. 699 lines. 11 callees. 1 caller (ProcessAndProcessAndProcess815).
[entrypoint.functions."0x82817748"]
name = "ProcessAndProcessAndProcess1922_82817748"

# Process a structure by getting a global field (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and two hundred and sixteenth form). It takes a structure, gets a global field (GetGlobalField19764), and drives a sub-operation (ProcessAndProcessAndProcess1925) based on the result. Part of the 0x82BFF process cluster. 43 lines. 2 callees. 1 caller (ProcessAndProcessAndProcess1924).
[entrypoint.functions."0x82BFFBD8"]
name = "ProcessAndProcessAndProcess1923_82BFFBD8"

# Process a structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and two hundred and seventeenth form). It takes a structure and drives a sub-operation (ProcessAndProcessAndProcess1923). Part of the 0x82C08 process cluster. 9 lines. 1 callee. 0 callers.
[entrypoint.functions."0x82C08DB0"]
name = "ProcessAndProcessAndProcess1924_82C08DB0"

# Process a large structure by allocating fresh memory (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and two hundred and eighteenth form). It takes a structure, allocates fresh memory (Allocate_OrDbgBreak), gets or creates a tree node (Tree_GetOrCreate), resets a tree object (ResetTreeObj), processes elements (ProcessElements_Vcall4), and drives sub-operations (sub_82C4D2D0, ProcessAndProcessAndProcess1927) based on the result. Part of the 0x82C14 process cluster. 129 lines. 6 callees. 1 caller (ProcessAndProcessAndProcess1923).
[entrypoint.functions."0x82C14688"]
name = "ProcessAndProcessAndProcess1925_82C14688"

# Process a structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and two hundred and nineteenth form). It takes a structure and drives a sub-operation (ProcessAndProcessAndProcess964). Part of the 0x82C4C process cluster. 47 lines. 1 callee. 0 callers.
[entrypoint.functions."0x82C4C478"]
name = "ProcessAndProcessAndProcess1926_82C4C478"

# Process a structure by calling methods (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and two hundred and twentieth form). It takes a structure, calls methods (ProcessAndCallMethods), and checks a method (ProcessAndCheckMethod) based on the result. Part of the 0x82C4D process cluster. 56 lines. 2 callees. 1 caller (ProcessAndProcessAndProcess1925).
[entrypoint.functions."0x82C4D328"]
name = "ProcessAndProcessAndProcess1927_82C4D328"

# Process a structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and two hundred and twenty-first form). It takes a structure and drives a sub-operation (ProcessAndProcessAndProcess1929). Part of the 0x82C4D process cluster. 56 lines. 1 callee. 0 callers.
[entrypoint.functions."0x82C4DFC8"]
name = "ProcessAndProcessAndProcess1928_82C4DFC8"

# Process a structure by allocating fresh memory (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and two hundred and twenty-second form). It takes a structure, allocates fresh memory (Allocate_OrDbgBreak), gets or creates a tree node (Tree_GetOrCreate), resets a tree object (ResetTreeObj), performs a vcall (ProcessAndVcallAndProcess10), and drives sub-operations based on the result. This is a frequently-called helper in the 0x82C50 process cluster. 65 lines. 4 callees. 6 callers.
[entrypoint.functions."0x82C50270"]
name = "ProcessAndProcessAndProcess1929_82C50270"

# Process a structure by allocating fresh memory (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and two hundred and twenty-third form). It takes a structure, allocates fresh memory (Allocate_OrDbgBreak), gets or creates a tree node (Tree_GetOrCreate), and drives a sub-operation (sub_82C551F8) based on the result. Part of the 0x82C52 process cluster. 69 lines. 3 callees. 0 callers.
[entrypoint.functions."0x82C52EF0"]
name = "ProcessAndProcessAndProcess1930_82C52EF0"

# Process a structure by resetting a tree object (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and two hundred and thirty-first form). It takes a structure and resets a tree object (ResetTreeObj). Part of the 0x82C55 process cluster. 45 lines. 1 callee. 1 caller (ProcessAndProcessAndProcess1930).
[entrypoint.functions."0x82C551F8"]
name = "ProcessAndProcessAndProcess1931_82C551F8"

# Process a structure by releasing a refcounted object (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and two hundred and thirty-second form). It takes a structure and releases a refcounted object (Release_RefCounted). This is a frequently-called helper in the 0x829FF process cluster. 82 lines. 1 callee. 24 callers.
[entrypoint.functions."0x829FF3E8"]
name = "ProcessAndProcessAndProcess1932_829FF3E8"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and two hundred and thirty-third form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82B4E process cluster. 40 lines. 0 callees. 0 callers.
[entrypoint.functions."0x82B4ECA8"]
name = "ProcessAndProcessAndProcess1933_82B4ECA8"

# Process a structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and two hundred and thirty-fourth form). It takes a structure and drives a sub-operation (sub_829FA700). This is a frequently-called helper in the 0x829FA process cluster. 104 lines. 1 callee. 6 callers.
[entrypoint.functions."0x829FA5E8"]
name = "ProcessAndProcessAndProcess1934_829FA5E8"

# Process a structure by constructing a 16-byte object (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and two hundred and thirty-fifth form). It takes a structure and constructs a 16-byte object (Construct16Byte). Part of the 0x82C00 process cluster. 11 lines. 1 callee. 0 callers.
[entrypoint.functions."0x82C00CF8"]
name = "ProcessAndProcessAndProcess1935_82C00CF8"

# Process a structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and two hundred and thirty-sixth form). It takes a structure and drives a sub-operation (sub_82BFF768). Part of the 0x82C47 process cluster. 63 lines. 1 callee. 1 caller (ProcessAndProcessAndProcess1912).
[entrypoint.functions."0x82C47908"]
name = "ProcessAndProcessAndProcess1936_82C47908"

# Process a very large data structure by copying memory (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and two hundred and thirty-seventh form). It takes a structure, copies memory (MemCpy_SdkRuntime), computes a wrapped value (ComputeWrapped), and drives sub-operations (sub_82B8C1C8, sub_82B8C900, sub_82B8D010, sub_82B8DFA0) based on the result. Part of the 0x82B8E process cluster. 1273 lines. 6 callees. 1 caller (ProcessAndProcessAndProcess1938).
[entrypoint.functions."0x82B8E320"]
name = "ProcessAndProcessAndProcess1937_82B8E320"

# Process a structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and two hundred and thirty-eighth form). It takes a structure and drives a sub-operation (ProcessAndProcessAndProcess1937). Part of the 0x82B8F process cluster. 74 lines. 1 callee. 0 callers.
[entrypoint.functions."0x82B8F568"]
name = "ProcessAndProcessAndProcess1938_82B8F568"

# Process a structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and two hundred and thirty-ninth form). It takes a structure and drives a sub-operation (sub_82B380F0). This is a frequently-called helper in the 0x82BE4 process cluster. 88 lines. 1 callee. 3 callers.
[entrypoint.functions."0x82BE4EC0"]
name = "ProcessAndProcessAndProcess1939_82BE4EC0"

# Process a structure by freeing size-bucketed memory (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and two hundred and fortieth form). It takes a structure and frees size-bucketed memory (Free_SizeBucketed). This is a frequently-called helper in the 0x82BE4 process cluster. 88 lines. 1 callee. 4 callers.
[entrypoint.functions."0x82BE4F60"]
name = "ProcessAndProcessAndProcess1940_82BE4F60"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and two hundred and forty-first form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82C08 process cluster. 28 lines. 0 callees. 1 caller (ProcessAndProcessAndProcess1942).
[entrypoint.functions."0x82C08618"]
name = "ProcessAndProcessAndProcess1941_82C08618"

# Process a structure by wrapping sub-operations (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and two hundred and forty-second form). It takes a structure, checks its state, and drives sub-operations (ProcessAndProcessAndProcess1941, sub_82CBB610) based on the result. This is a frequently-called helper in the 0x82C08 process cluster. 84 lines. 2 callees. 2 callers (sub_82C089C8 and sub_82C09B50).
[entrypoint.functions."0x82C08810"]
name = "ProcessAndProcessAndProcess1942_82C08810"

# Process a very large data structure by copying memory (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and two hundred and forty-third form). It takes a structure, copies memory (MemCpy_SdkRuntime), drives a sub-operation (ProcessAndProcessAndProcess1657), and processes (sub_821F5F18, sub_830055C8) based on the result. Part of the 0x83005 process cluster. 517 lines. 4 callees. 2 callers (sub_830060B0 and ProcessAndProcessAndProcess1944).
[entrypoint.functions."0x83005800"]
name = "ProcessAndProcessAndProcess1943_83005800"

# Process a very large data structure by wrapping sub-operations (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and two hundred and forty-fourth form). It takes a structure, checks its state, and drives sub-operations (ProcessAndProcessAndProcess1943, ProcessAndProcessAndProcess1945, ProcessAndProcessAndProcess1946) based on the result. This is a frequently-called processor in the 0x83006 process cluster. 342 lines. 3 callees. 6 callers.
[entrypoint.functions."0x830060B8"]
name = "ProcessAndProcessAndProcess1944_830060B8"

# Process a large structure by wrapping sub-operations (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and two hundred and forty-fifth form). It takes a structure, checks its state, and drives sub-operations (ProcessAndProcessAndProcess514, sub_82B91D00) based on the result. This is a frequently-called helper in the 0x8300A process cluster. 238 lines. 2 callees. 5 callers.
[entrypoint.functions."0x8300A198"]
name = "ProcessAndProcessAndProcess1945_8300A198"

# Process a structure by dispatching by type code (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and two hundred and forty-sixth form). It takes a structure and dispatches by type code (DispatchByTypeCode). This is a frequently-called helper in the 0x8300A process cluster. 58 lines. 1 callee. 9 callers.
[entrypoint.functions."0x8300A370"]
name = "ProcessAndProcessAndProcess1946_8300A370"

# Process a structure by freeing memory (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and two hundred and forty-seventh form). It takes a structure, frees memory (Free_Thunk), processes elements (ProcessElements_Vcall4), processes a tree (Tree_Process_Wrapper), and drives sub-operations (NoOp_Stub, ProcessAndProcessAndProcess1949, sub_82C4D3D8) based on the result. This is a frequently-called helper in the 0x82C14 process cluster. 103 lines. 6 callees. 3 callers.
[entrypoint.functions."0x82C14760"]
name = "ProcessAndProcessAndProcess1947_82C14760"

# Process a structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and two hundred and forty-eighth form). It takes a structure and drives a sub-operation (ProcessAndProcessAndProcess964). Part of the 0x82C4C process cluster. 47 lines. 1 callee. 0 callers.
[entrypoint.functions."0x82C4C4C8"]
name = "ProcessAndProcessAndProcess1948_82C4C4C8"

# Process a structure by calling methods (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and two hundred and forty-ninth form). It takes a structure, calls methods (ProcessAndCallMethods), and checks a method (ProcessAndCheckMethod) based on the result. Part of the 0x82C4D process cluster. 56 lines. 2 callees. 1 caller (ProcessAndProcessAndProcess1947).
[entrypoint.functions."0x82C4D380"]
name = "ProcessAndProcessAndProcess1949_82C4D380"

# Process a structure by freeing memory (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and two hundred and fiftieth form). It takes a structure, frees memory (Free_Thunk), processes elements (ProcessElements_Vcall4), processes a tree (Tree_Process_Wrapper), and drives a sub-operation (ProcessAndProcessAndProcess1951) based on the result. Part of the 0x82C53 process cluster. 72 lines. 4 callees. 0 callers.
[entrypoint.functions."0x82C53C08"]
name = "ProcessAndProcessAndProcess1950_82C53C08"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and two hundred and fifty-first form). It takes a structure and drives a no-op stub (NoOp_Stub). Part of the 0x82C55 process cluster. 37 lines. 1 callee. 1 caller (ProcessAndProcessAndProcess1950).
[entrypoint.functions."0x82C55240"]
name = "ProcessAndProcessAndProcess1951_82C55240"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and two hundred and fifty-second form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82CDC process cluster. 60 lines. 0 callees. 1 caller (ProcessAndProcessAndProcess1953).
[entrypoint.functions."0x82CDC118"]
name = "ProcessAndProcessAndProcess1952_82CDC118"

# Process a large structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and two hundred and fifty-third form). It takes a structure and drives a sub-operation (ProcessAndProcessAndProcess1952). This is a frequently-called helper in the 0x82CDC process cluster. 115 lines. 1 callee. 2 callers (sub_82CDC4F8 and sub_82CDC770).
[entrypoint.functions."0x82CDC430"]
name = "ProcessAndProcessAndProcess1953_82CDC430"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and two hundred and fifty-fourth form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82C13 process cluster. 25 lines. 0 callees. 0 callers.
[entrypoint.functions."0x82C13480"]
name = "ProcessAndProcessAndProcess1954_82C13480"

# Process a structure by copying a refcounted object (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and two hundred and fifty-fifth form). It takes a structure and copies a refcounted object (CopyAssign_RefCounted2). This is a frequently-called helper in the 0x821D4 process cluster. 120 lines. 1 callee. 5 callers.
[entrypoint.functions."0x821D4F80"]
name = "ProcessAndProcessAndProcess1955_821D4F80"

# Process a very large data structure by inserting a hash entry (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and two hundred and fifty-sixth form). It takes a structure and inserts a hash entry (InsertHashEntry). Part of the 0x82BB9 process cluster. 291 lines. 1 callee. 1 caller (sub_82BB8A50).
[entrypoint.functions."0x82BB9D30"]
name = "ProcessAndProcessAndProcess1956_82BB9D30"

# Process a large structure by freeing memory (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and two hundred and fifty-seventh form). It takes a structure, frees memory (Free_Thunk), and drives a sub-operation (ProcessAndProcessAndProcess1854) based on the result. Part of the 0x82C22 process cluster. 186 lines. 2 callees. 0 callers.
[entrypoint.functions."0x82C22FB0"]
name = "ProcessAndProcessAndProcess1957_82C22FB0"

# Process a structure by releasing a refcounted object (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and two hundred and fifty-eighth form). It takes a structure, releases a refcounted object (Release_RefCounted), and drives a sub-operation (ProcessAndProcessAndProcess1869) based on the result. This is a frequently-called helper in the 0x82B38 process cluster. 68 lines. 2 callees. 2 callers (sub_827DECA8 and sub_82818988).
[entrypoint.functions."0x82B38AA8"]
name = "ProcessAndProcessAndProcess1958_82B38AA8"

# Process a large structure by performing a vcall (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and two hundred and fifty-ninth form). It takes a structure, performs a vcall (ProcessAndVcallAndClear3), and drives a sub-operation (sub_82C0CF68) based on the result. Part of the 0x82C0E process cluster. 106 lines. 2 callees. 0 callers.
[entrypoint.functions."0x82C0E108"]
name = "ProcessAndProcessAndProcess1959_82C0E108"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and two hundred and sixtieth form). It takes a structure and checks its state, with no external sub-operations. This is a frequently-called helper in the 0x82B30 process cluster. 51 lines. 0 callees. 13 callers.
[entrypoint.functions."0x82B30AF8"]
name = "ProcessAndProcessAndProcess1960_82B30AF8"

# Process a structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and two hundred and sixty-first form). It takes a structure and drives a sub-operation (sub_82692048). Part of the 0x82355 process cluster. 23 lines. 1 callee. 0 callers.
[entrypoint.functions."0x82355BE0"]
name = "ProcessAndProcessAndProcess1961_82355BE0"

# Process a very large data structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and two hundred and sixty-second form). It takes a structure and drives a sub-operation (sub_829FA020). Part of the 0x826C5 process cluster. 428 lines. 1 callee. 0 callers.
[entrypoint.functions."0x826C5B88"]
name = "ProcessAndProcessAndProcess1962_826C5B88"

# Process a structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and two hundred and sixty-third form). It takes a structure and drives a sub-operation (ProcessAndProcessAndProcess1657). Part of the 0x82CFD process cluster. 15 lines. 1 callee. 0 callers.
[entrypoint.functions."0x82CFDDF0"]
name = "ProcessAndProcessAndProcess1963_82CFDDF0"

# Process a structure by growing a bit array (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and two hundred and sixty-fourth form). It takes a structure and grows a bit array (ProcessGrowBitArray). This is a frequently-called helper in the 0x82BFF process cluster. 34 lines. 1 callee. 17 callers.
[entrypoint.functions."0x82BFF7A0"]
name = "ProcessAndProcessAndProcess1964_82BFF7A0"

# Process a structure by getting a global field (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and two hundred and sixty-fifth form). It takes a structure, gets a global field (GetGlobalField19764), and drives a sub-operation (sub_82C14BC0) based on the result. This is a frequently-called helper in the 0x82BFF process cluster. 39 lines. 2 callees. 7 callers.
[entrypoint.functions."0x82BFFEA8"]
name = "ProcessAndProcessAndProcess1965_82BFFEA8"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and two hundred and sixty-sixth form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82C13 process cluster. 65 lines. 0 callees. 1 caller (sub_82C14BC0).
[entrypoint.functions."0x82C135B0"]
name = "ProcessAndProcessAndProcess1966_82C135B0"

# Process a large structure by constructing a 20-byte node (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and two hundred and sixty-seventh form). It takes a structure, increments atomically (AtomicIncrement3), and constructs a 20-byte node (Construct20ByteNode). Part of the 0x82C14 process cluster. 145 lines. 2 callees. 1 caller (ProcessAndProcessAndProcess1968).
[entrypoint.functions."0x82C14970"]
name = "ProcessAndProcessAndProcess1967_82C14970"

# Process a large structure by allocating fresh memory (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and two hundred and sixty-eighth form). It takes a structure, allocates fresh memory (Allocate_OrDbgBreak), and drives sub-operations (ProcessAndProcessAndProcess1966, ProcessAndProcessAndProcess1967, ProcessAndProcessAndProcess1970, ProcessAndProcessAndProcess1971) based on the result. Part of the 0x82C14 process cluster. 121 lines. 5 callees. 1 caller (ProcessAndProcessAndProcess1965).
[entrypoint.functions."0x82C14BC0"]
name = "ProcessAndProcessAndProcess1968_82C14BC0"

# Process a structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and two hundred and sixty-ninth form). It takes a structure and drives a sub-operation (ProcessAndProcessAndProcess964). Part of the 0x82C4C process cluster. 41 lines. 1 callee. 0 callers.
[entrypoint.functions."0x82C4C3E8"]
name = "ProcessAndProcessAndProcess1969_82C4C3E8"

# Process a structure by calling methods (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and two hundred and seventieth form). It takes a structure, calls methods (ProcessAndCallMethods), and checks a method (ProcessAndCheckMethod) based on the result. Part of the 0x82C4D process cluster. 60 lines. 2 callees. 1 caller (ProcessAndProcessAndProcess1968).
[entrypoint.functions."0x82C4D268"]
name = "ProcessAndProcessAndProcess1970_82C4D268"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and two hundred and seventy-first form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82C4D process cluster. 27 lines. 0 callees. 1 caller (ProcessAndProcessAndProcess1968).
[entrypoint.functions."0x82C4DA80"]
name = "ProcessAndProcessAndProcess1971_82C4DA80"

# Process a large structure by allocating fresh memory (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and two hundred and seventy-second form). It takes a structure, allocates fresh memory (Allocate_OrDbgBreak), and drives a sub-operation (ProcessAndProcessAndProcess1973) based on the result. Part of the 0x82C52 process cluster. 104 lines. 2 callees. 0 callers.
[entrypoint.functions."0x82C52518"]
name = "ProcessAndProcessAndProcess1972_82C52518"

# Process a large structure by constructing a 20-byte node (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and two hundred and seventy-third form). It takes a structure and constructs a 20-byte node (Construct20ByteNode). Part of the 0x82C55 process cluster. 116 lines. 1 callee. 1 caller (ProcessAndProcessAndProcess1972).
[entrypoint.functions."0x82C55F88"]
name = "ProcessAndProcessAndProcess1973_82C55F88"

# Process a large structure by wrapping sub-operations (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and two hundred and seventy-fourth form). It takes a structure, checks its state, and drives sub-operations (ProcessAndProcessAndProcess1230, sub_8220BCF8) based on the result. Part of the 0x82192 process cluster. 223 lines. 2 callees. 1 caller (ProcessAndProcessAndProcess1495).
[entrypoint.functions."0x82192640"]
name = "ProcessAndProcessAndProcess1974_82192640"

# Process a large structure by wrapping sub-operations (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and two hundred and seventy-fifth form). It takes a structure, checks its state, and drives sub-operations (ProcessAndProcessAndProcess907, sub_82AB02E0) based on the result. This is a frequently-called helper in the 0x821AE process cluster. 131 lines. 2 callees. 2 callers (ProcessAndProcessAndProcess1976 and sub_82B26968).
[entrypoint.functions."0x821AE380"]
name = "ProcessAndProcessAndProcess1975_821AE380"

# Process a large structure by wrapping sub-operations (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and two hundred and seventy-sixth form). It takes a structure, checks its state, and drives sub-operations (ProcessAndProcessAndProcess1227, ProcessAndProcessAndProcess526, ProcessAndProcessAndProcess1975, sub_82AB00B0) based on the result. Part of the 0x821AE process cluster. 143 lines. 4 callees. 1 caller (ProcessAndProcessAndProcess1496).
[entrypoint.functions."0x821AE470"]
name = "ProcessAndProcessAndProcess1976_821AE470"

# Process a structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and two hundred and seventy-seventh form). It takes a structure and drives a sub-operation (sub_82CA3FC8). This is a frequently-called helper in the 0x82260 process cluster. 9 lines. 1 callee. 54 callers.
[entrypoint.functions."0x82260900"]
name = "ProcessAndProcessAndProcess1977_82260900"

# Process a very large data structure by computing a special function (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and two hundred and seventy-eighth form). It takes a structure, computes a special function (ComputeSpecialFunction), computes a special function 2 (ComputeSpecialFunction2), and drives a sub-operation (ProcessAndProcessAndProcess1977) based on the result. This is a frequently-called processor in the 0x829FA process cluster. 513 lines. 3 callees. 6 callers.
[entrypoint.functions."0x829FA020"]
name = "ProcessAndProcessAndProcess1978_829FA020"

# Process a large structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and two hundred and seventy-ninth form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82AB0 process cluster. 144 lines. 0 callees. 1 caller (ProcessAndProcessAndProcess1976).
[entrypoint.functions."0x82AB00B0"]
name = "ProcessAndProcessAndProcess1979_82AB00B0"

# Process a very large data structure by wrapping sub-operations (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and two hundred and eightieth form). It takes a structure, checks its state, and drives sub-operations (ProcessAndProcessAndProcess1805, ProcessAndProcessAndProcess531, ProcessAndProcessAndProcess552, ProcessAndProcessAndProcess555, ProcessAndProcessAndProcess642, ProcessAndProcessAndProcess803, ProcessAndProcessAndProcess905) based on the result. Part of the 0x82AB0 process cluster. 595 lines. 7 callees. 1 caller (ProcessAndProcessAndProcess1975).
[entrypoint.functions."0x82AB02E0"]
name = "ProcessAndProcessAndProcess1980_82AB02E0"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and two hundred and eighty-first form). It takes a structure and checks its state, with no external sub-operations. This is a frequently-called helper in the 0x82BFC process cluster. 16 lines. 0 callees. 13 callers.
[entrypoint.functions."0x82BFC7F8"]
name = "ProcessAndProcessAndProcess1981_82BFC7F8"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and two hundred and eighty-second form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82C0B process cluster. 21 lines. 0 callees. 0 callers.
[entrypoint.functions."0x82C0B538"]
name = "ProcessAndProcessAndProcess1982_82C0B538"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and two hundred and eighty-third form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82C0B process cluster. 59 lines. 0 callees. 1 caller (ProcessAndProcessAndProcess1990).
[entrypoint.functions."0x82C0B850"]
name = "ProcessAndProcessAndProcess1983_82C0B850"

# Process a structure by performing a binary search (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and two hundred and eighty-fourth form). It takes a structure and performs a binary search (BinarySearch). Part of the 0x82C0C process cluster. 57 lines. 1 callee. 1 caller (sub_82C0F998).
[entrypoint.functions."0x82C0CC38"]
name = "ProcessAndProcessAndProcess1984_82C0CC38"

# Process a structure by incrementing atomically (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and two hundred and eighty-fifth form). It takes a structure and increments atomically (AtomicIncrement3). Part of the 0x82C0D process cluster. 88 lines. 1 callee. 1 caller (ProcessAndProcessAndProcess1990).
[entrypoint.functions."0x82C0D738"]
name = "ProcessAndProcessAndProcess1985_82C0D738"

# Process a large structure by getting a global field (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and two hundred and eighty-sixth form). It takes a structure, gets a global field (GetGlobalField19764), and performs a vcall (ProcessAndVcallAndProcess10) based on the result. Part of the 0x82C0D process cluster. 167 lines. 2 callees. 1 caller (ProcessAndProcessAndProcess1990).
[entrypoint.functions."0x82C0D8B8"]
name = "ProcessAndProcessAndProcess1986_82C0D8B8"

# Process a structure by performing a binary search (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and two hundred and eighty-seventh form). It takes a structure and performs a binary search (BinarySearch). Part of the 0x82C0D process cluster. 73 lines. 1 callee. 1 caller (ProcessAndProcessAndProcess1990).
[entrypoint.functions."0x82C0DEA8"]
name = "ProcessAndProcessAndProcess1987_82C0DEA8"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and two hundred and eighty-eighth form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82C0E process cluster. 81 lines. 0 callees. 1 caller (sub_82C0F998).
[entrypoint.functions."0x82C0E010"]
name = "ProcessAndProcessAndProcess1988_82C0E010"

# Process a structure by storing a descriptor (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and two hundred and eighty-ninth form). It takes a structure and stores a descriptor (StoreDescriptor_Thunk). This is a frequently-called helper in the 0x82C0E process cluster. 66 lines. 1 callee. 2 callers (sub_82C0F1B0 and sub_82C0F998).
[entrypoint.functions."0x82C0E098"]
name = "ProcessAndProcessAndProcess1989_82C0E098"

# Process a very large data structure by allocating fresh memory (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and two hundred and ninetieth form). It takes a structure, allocates fresh memory (Allocate_OrDbgBreak), performs a binary search (BinarySearch), copies a refcounted object (CopyAssign_RefCounted2), stores a descriptor (StoreDescriptor_Thunk), and drives sub-operations (ProcessAndProcessAndProcess680, ProcessAndProcessAndProcess1983, ProcessAndProcessAndProcess1985, ProcessAndProcessAndProcess1986, ProcessAndProcessAndProcess1987, sub_82C175E8) based on the result. This is a frequently-called processor in the 0x82C0E process cluster. 419 lines. 10 callees. 2 callers (sub_82C0F200 and sub_82C0F998).
[entrypoint.functions."0x82C0EEC8"]
name = "ProcessAndProcessAndProcess1990_82C0EEC8"

# Process a large structure by constructing and incrementing a counter (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and two hundred and ninety-first form). It takes a structure, constructs and increments a counter (ConstructAndIncrementCounter), destructs and sets a descriptor (DestructAndSetDescriptor), grows a bit array (ProcessGrowBitArray), stores a descriptor (StoreDescriptor_Thunk), and drives sub-operations (ProcessAndProcessAndProcess1068, ProcessAndProcessAndProcess1984, ProcessAndProcessAndProcess1988, ProcessAndProcessAndProcess1990) based on the result. This is a frequently-called processor in the 0x82C0F process cluster. 180 lines. 8 callees. 3 callers.
[entrypoint.functions."0x82C0F998"]
name = "ProcessAndProcessAndProcess1991_82C0F998"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and two hundred and ninety-second form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82C17 process cluster. 81 lines. 0 callees. 1 caller (ProcessAndProcessAndProcess1990).
[entrypoint.functions."0x82C175E8"]
name = "ProcessAndProcessAndProcess1992_82C175E8"

# Process a large structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and two hundred and ninety-third form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82CA3 process cluster. 141 lines. 0 callees. 1 caller (ProcessAndProcessAndProcess1977).
[entrypoint.functions."0x82CA3FC8"]
name = "ProcessAndProcessAndProcess1993_82CA3FC8"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and two hundred and ninety-fourth form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x83005 process cluster. 85 lines. 0 callees. 1 caller (sub_83005FA0).
[entrypoint.functions."0x83005528"]
name = "ProcessAndProcessAndProcess1994_83005528"

# Process a very large data structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and two hundred and ninety-fifth form). It takes a structure and drives a sub-operation (ProcessAndProcessAndProcess1738). Part of the 0x82197 process cluster. 279 lines. 1 callee. 1 caller (ProcessAndProcessAndProcess685).
[entrypoint.functions."0x82197000"]
name = "ProcessAndProcessAndProcess1995_82197000"

# Process a structure by freeing memory (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and two hundred and ninety-sixth form). It takes a structure, frees memory (Free_Thunk), sets a string (SetString), and drives sub-operations (sub_822F1EC0, sub_822F1F00, sub_826C3FA8) based on the result. This is a frequently-called helper in the 0x82C70 process cluster. 93 lines. 5 callees. 4 callers.
[entrypoint.functions."0x82C70F48"]
name = "ProcessAndProcessAndProcess1996_82C70F48"

# Process a very large data structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and two hundred and ninety-seventh form). It takes a structure and drives a sub-operation (Stub_ReturnZero). This is a frequently-called processor in the 0x82CE6 process cluster. 315 lines. 1 callee. 2 callers (sub_82CDCFE8 and sub_82CE4780).
[entrypoint.functions."0x82CE6098"]
name = "ProcessAndProcessAndProcess1997_82CE6098"

# Process a large structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and two hundred and ninety-eighth form). It takes a structure and checks its state, with no external sub-operations. This is a frequently-called helper in the 0x82B15 process cluster. 205 lines. 0 callees. 9 callers.
[entrypoint.functions."0x82B15760"]
name = "ProcessAndProcessAndProcess1998_82B15760"

# Process a large structure by wrapping sub-operations (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and two hundred and ninety-ninth form). It takes a structure, checks its state, and drives sub-operations (ProcessAndProcessAndProcess618, sub_822A0EB8) based on the result. This is a frequently-called helper in the 0x82AAE process cluster. 124 lines. 2 callees. 2 callers (ProcessAndProcessAndProcess1040 and ProcessAndProcessAndProcess1700).
[entrypoint.functions."0x82AAE408"]
name = "ProcessAndProcessAndProcess1999_82AAE408"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and three hundred form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82BFF process cluster. 12 lines. 0 callees. 1 caller (sub_82C14140).
[entrypoint.functions."0x82BFFAE8"]
name = "ProcessAndProcessAndProcess2000_82BFFAE8"

# Process a structure by destructing a refcounted object (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and three hundred and first form). It takes a structure, destructs a refcounted object (DestructRefCounted), and drives a sub-operation (ProcessAndProcessAndProcess1811) based on the result. Part of the 0x82C0D process cluster. 131 lines. 2 callees. 1 caller (ProcessAndProcessAndProcess2002).
[entrypoint.functions."0x82C0D7D8"]
name = "ProcessAndProcessAndProcess2001_82C0D7D8"

# Process a structure by freeing memory (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and three hundred and second form). It takes a structure, frees memory (Free_Thunk), and drives a sub-operation (ProcessAndProcessAndProcess2001) based on the result. Part of the 0x82C0E process cluster. 50 lines. 2 callees. 0 callers.
[entrypoint.functions."0x82C0EB58"]
name = "ProcessAndProcessAndProcess2002_82C0EB58"

# Process a structure by destructing a 20-byte node (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and three hundred and third form). It takes a structure, destructs a 20-byte node (Destruct20ByteNode), destructs a refcounted object (DestructRefCounted), and drives a sub-operation (ProcessAndProcessAndProcess2000) based on the result. Part of the 0x82C14 process cluster. 75 lines. 3 callees. 1 caller (ProcessAndProcessAndProcess2005).
[entrypoint.functions."0x82C14140"]
name = "ProcessAndProcessAndProcess2003_82C14140"

# Process a very large data structure by finding a chain value (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and three hundred and fourth form). It takes a structure, finds a chain value (FindChainValue), frees memory (Free_Thunk), processes elements (ProcessElements_Vcall4), processes a tree (Tree_Process_Wrapper), and drives sub-operations (NoOp_Stub, ProcessAndProcessAndProcess1763, ProcessAndProcessAndProcess1814, ProcessAndProcessAndProcess1947, ProcessAndProcessAndProcess2007, sub_82C4CE08) based on the result. Part of the 0x82C14 process cluster. 213 lines. 10 callees. 0 callers.
[entrypoint.functions."0x82C14808"]
name = "ProcessAndProcessAndProcess2004_82C14808"

# Process a structure by freeing memory (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and three hundred and fifth form). It takes a structure, frees memory (Free_Thunk), and drives a sub-operation (ProcessAndProcessAndProcess2003) based on the result. Part of the 0x82C14 process cluster. 50 lines. 2 callees. 0 callers.
[entrypoint.functions."0x82C14A70"]
name = "ProcessAndProcessAndProcess2005_82C14A70"

# Process a structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and three hundred and sixth form). It takes a structure and drives a sub-operation (ProcessAndProcessAndProcess964). Part of the 0x82C4C process cluster. 41 lines. 1 callee. 0 callers.
[entrypoint.functions."0x82C4C188"]
name = "ProcessAndProcessAndProcess2006_82C4C188"

# Process a structure by calling methods (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and three hundred and seventh form). It takes a structure, calls methods (ProcessAndCallMethods), and checks a method (ProcessAndCheckMethod) based on the result. Part of the 0x82C4C process cluster. 60 lines. 2 callees. 1 caller (ProcessAndProcessAndProcess2004).
[entrypoint.functions."0x82C4CD38"]
name = "ProcessAndProcessAndProcess2007_82C4CD38"

# Process a structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and three hundred and eighth form). It takes a structure and drives a sub-operation (ProcessAndProcessAndProcess1817). Part of the 0x82C52 process cluster. 70 lines. 1 callee. 0 callers.
[entrypoint.functions."0x82C525C8"]
name = "ProcessAndProcessAndProcess2008_82C525C8"

# Process a structure by destructing a 20-byte node (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and three hundred and ninth form). It takes a structure and destructs a 20-byte node (Destruct20ByteNode). Part of the 0x82C55 process cluster. 65 lines. 1 callee. 1 caller (ProcessAndProcessAndProcess2010).
[entrypoint.functions."0x82C55EA0"]
name = "ProcessAndProcessAndProcess2009_82C55EA0"

# Process a structure by freeing memory (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and three hundred and tenth form). It takes a structure, frees memory (Free_Thunk), and drives a sub-operation (ProcessAndProcessAndProcess2009) based on the result. Part of the 0x82C56 process cluster. 50 lines. 2 callees. 0 callers.
[entrypoint.functions."0x82C56050"]
name = "ProcessAndProcessAndProcess2010_82C56050"

# Process a large structure by wrapping sub-operations (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and three hundred and eleventh form). It takes a structure, checks its state, and drives sub-operations (ProcessAndProcessAndProcess1818, sub_8241C298) based on the result. This is a frequently-called helper in the 0x8241C process cluster. 132 lines. 2 callees. 5 callers.
[entrypoint.functions."0x8241C140"]
name = "ProcessAndProcessAndProcess2011_8241C140"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and three hundred and twelfth form). It takes a structure and checks its state, with no external sub-operations. This is a frequently-called helper in the 0x82AA7 process cluster. 61 lines. 0 callees. 2 callers (sub_82AA79F0 and sub_82AA7B90).
[entrypoint.functions."0x82AA7ED8"]
name = "ProcessAndProcessAndProcess2012_82AA7ED8"

# Process a large structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and three hundred and thirteenth form). It takes a structure and drives a sub-operation (ProcessAndProcessAndProcess1809). Part of the 0x82AAF process cluster. 163 lines. 1 callee. 1 caller (sub_82AAF5A0).
[entrypoint.functions."0x82AAF230"]
name = "ProcessAndProcessAndProcess2013_82AAF230"

# Process a large structure by allocating fresh memory (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and three hundred and fourteenth form). It takes a structure, allocates fresh memory (Allocate_OrDbgBreak), and drives sub-operations (ProcessAndProcessAndProcess1809, ProcessAndProcessAndProcess685) based on the result. Part of the 0x82AAF process cluster. 138 lines. 3 callees. 1 caller (sub_82AAF5A0).
[entrypoint.functions."0x82AAF478"]
name = "ProcessAndProcessAndProcess2014_82AAF478"

# Process a structure by entering a critical section (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and three hundred and fifteenth form). It takes a structure, enters a critical section (CriticalSection_EnterOrTry), and leaves the critical section (ProcessAndLeaveCritical). Part of the 0x82C6F process cluster. 78 lines. 2 callees. 0 callers.
[entrypoint.functions."0x82C6F9E8"]
name = "ProcessAndProcessAndProcess2015_82C6F9E8"

# Process a structure by entering a critical section (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and three hundred and sixteenth form). It takes a structure, enters a critical section (CriticalSection_EnterOrTry), and leaves the critical section (ProcessAndLeaveCritical). Part of the 0x82C6F process cluster. 74 lines. 2 callees. 0 callers.
[entrypoint.functions."0x82C6FA70"]
name = "ProcessAndProcessAndProcess2016_82C6FA70"

# Process a structure by entering a critical section (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and three hundred and seventeenth form). It takes a structure, enters a critical section (CriticalSection_EnterOrTry), and leaves the critical section (ProcessAndLeaveCritical). Part of the 0x82C6F process cluster. 78 lines. 2 callees. 0 callers.
[entrypoint.functions."0x82C6FB78"]
name = "ProcessAndProcessAndProcess2017_82C6FB78"

# Process a very large data structure by allocating fresh memory (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and three hundred and eighteenth form). It takes a structure, allocates fresh memory (Allocate_OrDbgBreak), enters a critical section (CriticalSection_EnterOrTry), destructs and clears a field (DestructAndClearField), processes and allocates (ProcessAndAllocateAndProcess2), dispatches by type (ProcessAndDispatchByType), initializes with descriptors (ProcessAndInitializeWithDescriptors), leaves the critical section (ProcessAndLeaveCritical), processes a type dispatch (ProcessTypeDispatch), and drives a sub-operation (sub_82B49C60) based on the result. Part of the 0x82C6F process cluster. 377 lines. 9 callees. 0 callers.
[entrypoint.functions."0x82C6FCB8"]
name = "ProcessAndProcessAndProcess2018_82C6FCB8"

# Process a structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and three hundred and nineteenth form). It takes a structure and drives a sub-operation (sub_82A86778). This is a frequently-called helper in the 0x8235E process cluster. 83 lines. 1 callee. 16 callers.
[entrypoint.functions."0x8235E148"]
name = "ProcessAndProcessAndProcess2019_8235E148"

# Process a structure by allocating fresh memory (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and three hundred and twentieth form). It takes a structure and allocates fresh memory (Allocate_OrDbgBreak). Part of the 0x8236B process cluster. 65 lines. 1 callee. 0 callers.
[entrypoint.functions."0x8236B588"]
name = "ProcessAndProcessAndProcess2020_8236B588"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and three hundred and twenty-first form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x8241B process cluster. 8 lines. 0 callees. 0 callers.
[entrypoint.functions."0x8241BE70"]
name = "ProcessAndProcessAndProcess2021_8241BE70"

# Process a very large data structure by wrapping sub-operations (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and three hundred and twenty-second form). It takes a structure, checks its state, and drives sub-operations (ProcessAndProcessAndProcess1818, ProcessAndProcessAndProcess1834, ProcessAndProcessAndProcess2011) based on the result. Part of the 0x8241B process cluster. 253 lines. 3 callees. 0 callers.
[entrypoint.functions."0x8241BF80"]
name = "ProcessAndProcessAndProcess2022_8241BF80"

# Process a large structure by wrapping sub-operations (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and three hundred and twenty-third form). It takes a structure, checks its state, and drives sub-operations (sub_82A00E30, ProcessAndProcessAndProcess2024) based on the result. This is a frequently-called helper in the 0x82A00 process cluster. 105 lines. 2 callees. 2 callers (ProcessAndProcessAndProcess892 and sub_83227F60).
[entrypoint.functions."0x82A008C8"]
name = "ProcessAndProcessAndProcess2023_82A008C8"

# Process a very large data structure by freeing size-bucketed memory (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and three hundred and twenty-fourth form). It takes a structure, frees size-bucketed memory (Free_SizeBucketed), computes and processes (ProcessAndComputeAndProcess8), and drives a sub-operation (sub_82A97648) based on the result. This is a frequently-called processor in the 0x82A00 process cluster. 593 lines. 3 callees. 2 callers (ProcessAndProcessAndProcess2023 and sub_8321E160).
[entrypoint.functions."0x82A00EC0"]
name = "ProcessAndProcessAndProcess2024_82A00EC0"

# Process a structure by allocating fresh memory (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and three hundred and twenty-fifth form). It takes a structure, allocates fresh memory (Allocate_OrDbgBreak), and drives a sub-operation (sub_82A6FBD0) based on the result. Part of the 0x82A3F process cluster. 48 lines. 2 callees. 0 callers.
[entrypoint.functions."0x82A3F328"]
name = "ProcessAndProcessAndProcess2025_82A3F328"

# Process a structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and three hundred and twenty-sixth form). It takes a structure and drives a sub-operation (sub_82A3F510). This is a frequently-called helper in the 0x82A3F process cluster. 87 lines. 1 callee. 3 callers (CloneObject, sub_822BFD90, sub_82A3F378).
[entrypoint.functions."0x82A3F478"]
name = "ProcessAndProcessAndProcess2026_82A3F478"

# Process a structure by wrapping sub-operations (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and three hundred and twenty-seventh form). It takes a structure, checks its state, and drives sub-operations (ProcessAndProcessAndProcess1998, ProcessVector2) based on the result. This is a frequently-called helper in the 0x82A6F process cluster. 128 lines. 2 callees. 2 callers (ProcessAndProcessAndProcess2025 and sub_82A3F778).
[entrypoint.functions."0x82A6FBD0"]
name = "ProcessAndProcessAndProcess2027_82A6FBD0"

# Process a structure by allocating fresh memory (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and three hundred and twenty-eighth form). It takes a structure, allocates fresh memory (Allocate_OrDbgBreak), and drives a sub-operation (sub_822F1F00) based on the result. This is a frequently-called helper in the 0x82B7F process cluster. 72 lines. 2 callees. 3 callers.
[entrypoint.functions."0x82B7FFA8"]
name = "ProcessAndProcessAndProcess2028_82B7FFA8"

# Process a structure by allocating fresh memory (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and three hundred and twenty-ninth form). It takes a structure and allocates fresh memory (Allocate_OrDbgBreak). This is a frequently-called helper in the 0x82BE4 process cluster. 66 lines. 1 callee. 3 callers.
[entrypoint.functions."0x82BE43B0"]
name = "ProcessAndProcessAndProcess2029_82BE43B0"

# Process a large structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and three hundred and thirtieth form). It takes a structure and drives a sub-operation (ProcessAndProcessAndProcess2029). Part of the 0x8321C process cluster. 99 lines. 1 callee. 1 caller (ProcessAndProcessAndProcess2032).
[entrypoint.functions."0x8321CAF0"]
name = "ProcessAndProcessAndProcess2030_8321CAF0"

# Process a large structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and three hundred and thirty-first form). It takes a structure and drives a sub-operation (ProcessAndProcessAndProcess1850). This is a frequently-called helper in the 0x83221 process cluster. 108 lines. 1 callee. 7 callers.
[entrypoint.functions."0x83221D10"]
name = "ProcessAndProcessAndProcess2031_83221D10"

# Process a very large data structure by allocating fresh memory (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and three hundred and thirty-second form). It takes a structure, allocates fresh memory (Allocate_OrDbgBreak), and drives sub-operations (sub_8321B3F8, ProcessAndProcessAndProcess2030, sub_8321D258) based on the result. Part of the 0x83221 process cluster. 231 lines. 4 callees. 2 callers (sub_83221F50 and ProcessAndProcessAndProcess2033).
[entrypoint.functions."0x83221DC0"]
name = "ProcessAndProcessAndProcess2032_83221DC0"

# Process a structure by wrapping sub-operations (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and three hundred and thirty-third form). It takes a structure, checks its state, releases a refcounted object (Release_RefCounted), and drives sub-operations (ProcessAndProcessAndProcess1960, ProcessAndProcessAndProcess2032) based on the result. This is a frequently-called helper in the 0x83222 process cluster. 88 lines. 3 callees. 7 callers.
[entrypoint.functions."0x83222238"]
name = "ProcessAndProcessAndProcess2033_83222238"

# Process a structure by computing a vector hash (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and three hundred and thirty-fourth form). It takes a structure and computes a vector hash (ComputeVectorHash). This is a frequently-called helper in the 0x83222 process cluster. 74 lines. 1 callee. 3 callers.
[entrypoint.functions."0x83222500"]
name = "ProcessAndProcessAndProcess2034_83222500"

# Process a structure by allocating fresh memory (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and three hundred and thirty-fifth form). It takes a structure, allocates fresh memory (Allocate_OrDbgBreak), constructs and processes (ConstructAndProcess29), drives a sub-operation (ProcessAndProcessAndProcess1761), and sets a value (SetValue4) based on the result. This is a frequently-called helper in the 0x83223 process cluster. 77 lines. 4 callees. 3 callers.
[entrypoint.functions."0x83223B10"]
name = "ProcessAndProcessAndProcess2035_83223B10"

# Process a structure by releasing a refcounted object (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and three hundred and thirty-sixth form). It takes a structure, releases a refcounted object (Release_RefCounted), and validates and searches (ValidateAndSearch). This is a frequently-called helper in the 0x83225 process cluster. 75 lines. 2 callees. 3 callers.
[entrypoint.functions."0x83225920"]
name = "ProcessAndProcessAndProcess2036_83225920"

# Process a large structure by wrapping sub-operations (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and three hundred and thirty-seventh form). It takes a structure, checks its state, and drives sub-operations (ProcessAndProcessAndProcess1120, ProcessAndProcessAndProcess1621) based on the result. This is a frequently-called helper in the 0x83226 process cluster. 101 lines. 2 callees. 3 callers.
[entrypoint.functions."0x832263D0"]
name = "ProcessAndProcessAndProcess2037_832263D0"

# Process a structure by wrapping sub-operations (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and three hundred and thirty-eighth form). It takes a structure, checks its state, and drives sub-operations (ProcessAndProcessAndProcess1120, ProcessAndProcessAndProcess1703, ProcessAndProcessAndProcess2037) based on the result. Part of the 0x83226 process cluster. 90 lines. 3 callees. 0 callers.
[entrypoint.functions."0x83226698"]
name = "ProcessAndProcessAndProcess2038_83226698"

# Process a very large data structure by wrapping sub-operations (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and three hundred and thirty-ninth form). It takes a structure, checks its state, releases a refcounted object (Release_RefCounted), and drives sub-operations (ProcessAndProcessAndProcess1120, ProcessAndProcessAndProcess1761, ProcessAndProcessAndProcess2031) based on the result. Part of the 0x83226 process cluster. 256 lines. 4 callees. 1 caller (ProcessAndProcessAndProcess2041).
[entrypoint.functions."0x83226DC0"]
name = "ProcessAndProcessAndProcess2039_83226DC0"

# Process a structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and three hundred and fortieth form). It takes a structure and drives a sub-operation (ProcessAndProcessAndProcess2033). Part of the 0x83227 process cluster. 9 lines. 1 callee. 0 callers.
[entrypoint.functions."0x83227068"]
name = "ProcessAndProcessAndProcess2040_83227068"

# Process a large structure by constructing a 24-byte object (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and three hundred and forty-first form). It takes a structure, constructs a 24-byte object (Construct24Byte), releases a refcounted object (Release_RefCounted), and drives a sub-operation (ProcessAndProcessAndProcess2039) based on the result. This is a frequently-called helper in the 0x83229 process cluster. 105 lines. 3 callees. 4 callers.
[entrypoint.functions."0x83229AB8"]
name = "ProcessAndProcessAndProcess2041_83229AB8"

# Process a structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and three hundred and forty-second form). It takes a structure and drives a sub-operation (ProcessAndProcessAndProcess1120). Part of the 0x8322B process cluster. 91 lines. 1 callee. 1 caller (sub_8322B710).
[entrypoint.functions."0x8322B678"]
name = "ProcessAndProcessAndProcess2042_8322B678"

# Process a very large data structure by wrapping sub-operations (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and three hundred and forty-third form). It takes a structure, frees size-bucketed memory (Free_SizeBucketed), releases a refcounted object (Release_RefCounted), sets a value (SetValue4), and drives sub-operations (ProcessAndProcessAndProcess1120, ProcessAndProcessAndProcess1703, ProcessAndProcessAndProcess1762, ProcessAndProcessAndProcess1827, ProcessAndProcessAndProcess2019, ProcessAndProcessAndProcess2035) based on the result. Part of the 0x8322C process cluster. 212 lines. 9 callees. 1 caller (sub_8322CA88).
[entrypoint.functions."0x8322C2E8"]
name = "ProcessAndProcessAndProcess2043_8322C2E8"

# Process a structure by freeing size-bucketed memory (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and three hundred and forty-fourth form). It takes a structure, frees size-bucketed memory (Free_SizeBucketed), updates a range (RangeUpdate), and drives a sub-operation (ProcessAndProcessAndProcess124) based on the result. This is a frequently-called helper in the 0x82374 process cluster. 99 lines. 3 callees. 3 callers.
[entrypoint.functions."0x82374E78"]
name = "ProcessAndProcessAndProcess2044_82374E78"

# Process a very large data structure by freeing size-bucketed memory (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and three hundred and forty-fifth form). It takes a structure, frees size-bucketed memory (Free_SizeBucketed), and drives sub-operations (sub_82A97648, ProcessAndProcessAndProcess2028) based on the result. Part of the 0x82B90 process cluster. 477 lines. 3 callees. 1 caller (ProcessAndProcessAndProcess569).
[entrypoint.functions."0x82B90080"]
name = "ProcessAndProcessAndProcess2045_82B90080"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and three hundred and forty-sixth form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82C44 process cluster. 8 lines. 0 callees. 0 callers.
[entrypoint.functions."0x82C44298"]
name = "ProcessAndProcessAndProcess2046_82C44298"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and three hundred and forty-seventh form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x821E0 process cluster. 31 lines. 0 callees. 0 callers.
[entrypoint.functions."0x821E0CE8"]
name = "ProcessAndProcessAndProcess2047_821E0CE8"

# Process a structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and three hundred and forty-eighth form). It takes a structure and drives a sub-operation (ProcessAndProcessAndProcess655). This is a very frequently-called helper in the 0x821F5 process cluster. 7 lines. 1 callee. 219 callers.
[entrypoint.functions."0x821F5F18"]
name = "ProcessAndProcessAndProcess2048_821F5F18"

# Process a large structure by wrapping sub-operations (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and three hundred and forty-ninth form). It takes a structure, checks its state, and drives sub-operations (ProcessAndProcessAndProcess2050, sub_82370FD8) based on the result. Part of the 0x822F0 process cluster. 197 lines. 2 callees. 1 caller (sub_822EC9C8).
[entrypoint.functions."0x822F0878"]
name = "ProcessAndProcessAndProcess2049_822F0878"

# Process a very large data structure by allocating fresh memory (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and three hundred and fiftieth form). It takes a structure, allocates fresh memory (Allocate_OrDbgBreak), frees and processes (ProcessAndFreeAndProcess9), updates a range (RangeUpdate), sets a string (SetString), and drives sub-operations (sub_822F16C8, sub_822F1748, sub_822F1F00, sub_826C3EF0, sub_826C3FA8) based on the result. Part of the 0x822F0 process cluster. 364 lines. 9 callees. 1 caller (ProcessAndProcessAndProcess2049).
[entrypoint.functions."0x822F0F00"]
name = "ProcessAndProcessAndProcess2050_822F0F00"

# Process a structure by destructing a refcounted object (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and three hundred and fifty-first form). It takes a structure, destructs a refcounted object (DestructRefCounted), and drives a sub-operation (NoOp_Stub) based on the result. Part of the 0x82C0B process cluster. 59 lines. 2 callees. 1 caller (ProcessAndProcessAndProcess2052).
[entrypoint.functions."0x82C0B208"]
name = "ProcessAndProcessAndProcess2051_82C0B208"

# Process a large structure by atomic-incrementing a counter (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and three hundred and fifty-second form). It takes a structure, atomic-increments a counter (AtomicIncrement3), copy-assigns a refcounted object (CopyAssign_RefCounted2), processes elements (ProcessElements_Vcall4), resets a tree object (ResetTreeObj), and drives a sub-operation (ProcessAndProcessAndProcess2051) based on the result. Part of the 0x82C0E process cluster. 87 lines. 5 callees. 1 caller (ProcessAndProcessAndProcess2053).
[entrypoint.functions."0x82C0E548"]
name = "ProcessAndProcessAndProcess2052_82C0E548"

# Process a structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and three hundred and fifty-third form). It takes a structure and drives a sub-operation (ProcessAndProcessAndProcess2052). Part of the 0x82C0F process cluster. 56 lines. 1 callee. 0 callers.
[entrypoint.functions."0x82C0F238"]
name = "ProcessAndProcessAndProcess2053_82C0F238"

# Process a structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and three hundred and fifty-fourth form). It takes a structure and drives a sub-operation (ProcessAndProcessAndProcess1870). Part of the 0x82C44 process cluster. 44 lines. 1 callee. 0 callers.
[entrypoint.functions."0x82C441B0"]
name = "ProcessAndProcessAndProcess2054_82C441B0"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and three hundred and fifty-fifth form). It takes a structure and checks its state, with no external sub-operations. This is a frequently-called helper in the 0x82C50 process cluster. 16 lines. 0 callees. 2 callers (ProcessAndProcessAndProcess1137 and sub_82C441F0).
[entrypoint.functions."0x82C50A90"]
name = "ProcessAndProcessAndProcess2055_82C50A90"

# Process a structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and three hundred and fifty-sixth form). It takes a structure and drives a sub-operation (ProcessAndProcessAndProcess2057). Part of the 0x82C55 process cluster. 42 lines. 1 callee. 1 caller (sub_82C53D90).
[entrypoint.functions."0x82C55A10"]
name = "ProcessAndProcessAndProcess2056_82C55A10"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and three hundred and fifty-seventh form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82C56 process cluster. 16 lines. 0 callees. 1 caller (ProcessAndProcessAndProcess2056).
[entrypoint.functions."0x82C56B98"]
name = "ProcessAndProcessAndProcess2057_82C56B98"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and three hundred and fifty-eighth form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82C3F process cluster. 36 lines. 0 callees. 0 callers.
[entrypoint.functions."0x82C3FD50"]
name = "ProcessAndProcessAndProcess2058_82C3FD50"

# Process a structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and three hundred and fifty-ninth form). It takes a structure and drives a sub-operation (ProcessAndVcallAndClear3). Part of the 0x82C41 process cluster. 84 lines. 1 callee. 0 callers.
[entrypoint.functions."0x82C410B0"]
name = "ProcessAndProcessAndProcess2059_82C410B0"

# Process a large structure by copy-assigning a refcounted object (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and three hundred and sixtieth form). It takes a structure and copy-assigns a refcounted object (CopyAssign_RefCounted). This is a frequently-called helper in the 0x82C0B process cluster. 120 lines. 1 callee. 6 callers.
[entrypoint.functions."0x82C0B288"]
name = "ProcessAndProcessAndProcess2060_82C0B288"

# Process a structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and three hundred and sixty-first form). It takes a structure and drives a sub-operation (ProcessAndProcessAndProcess1658). This is a frequently-called helper in the 0x82CDA process cluster. 78 lines. 1 callee. 2 callers (sub_82CDAD90 and sub_82CE6898).
[entrypoint.functions."0x82CDAD08"]
name = "ProcessAndProcessAndProcess2061_82CDAD08"

# Process a large structure by releasing a refcounted object (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and three hundred and sixty-second form). It takes a structure, releases a refcounted object (Release_RefCounted), and drives a sub-operation (sub_8323A6E8) based on the result. Part of the 0x8322A process cluster. 131 lines. 2 callees. 1 caller (sub_8322AB48).
[entrypoint.functions."0x8322AA78"]
name = "ProcessAndProcessAndProcess2062_8322AA78"

# Process a large structure by allocating fresh memory (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and three hundred and sixty-third form). It takes a structure, allocates fresh memory (Allocate_OrDbgBreak), releases a refcounted object (Release_RefCounted), sets a value (SetValue4), and drives a sub-operation (ProcessAndProcessAndProcess2062) based on the result. Part of the 0x8322A process cluster. 141 lines. 4 callees. 1 caller (ProcessAndProcessAndProcess1828).
[entrypoint.functions."0x8322AB48"]
name = "ProcessAndProcessAndProcess2063_8322AB48"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and three hundred and sixty-fourth form). It takes a structure and checks its state, with no external sub-operations. This is a frequently-called helper in the 0x8322B process cluster. 91 lines. 0 callees. 2 callers (ProcessAndProcessAndProcess725 and sub_83228260).
[entrypoint.functions."0x8322B330"]
name = "ProcessAndProcessAndProcess2064_8322B330"

# Process a large structure by releasing a refcounted object (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and three hundred and sixty-fifth form). It takes a structure, releases a refcounted object (Release_RefCounted), and drives a sub-operation (ProcessAndProcessAndProcess1761) based on the result. This is a frequently-called helper in the 0x8323A process cluster. 103 lines. 2 callees. 5 callers.
[entrypoint.functions."0x8323A6E8"]
name = "ProcessAndProcessAndProcess2065_8323A6E8"

# Process a very large data structure by wrapping sub-operations (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and three hundred and sixty-sixth form). It takes a structure, checks its state, and drives sub-operations (ProcessAndProcessAndProcess1653, ProcessAndProcessAndProcess1654, ProcessAndProcessAndProcess731) based on the result. Part of the 0x82B8C process cluster. 1009 lines. 3 callees. 1 caller (ProcessAndProcessAndProcess1937).
[entrypoint.functions."0x82B8C1C8"]
name = "ProcessAndProcessAndProcess2066_82B8C1C8"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and three hundred and sixty-seventh form). It takes a structure and checks its state, with no external sub-operations. This is a frequently-called helper in the 0x82C08 process cluster. 12 lines. 0 callees. 6 callers.
[entrypoint.functions."0x82C08C90"]
name = "ProcessAndProcessAndProcess2067_82C08C90"

# Process a large structure by finding a chain root (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and three hundred and sixty-eighth form). It takes a structure, finds a chain root (FindChainRoot5), and drives a sub-operation (ProcessAndProcessAndProcess2070) based on the result. Part of the 0x82C09 process cluster. 258 lines. 2 callees. 1 caller (ProcessAndProcessAndProcess2069).
[entrypoint.functions."0x82C09FE8"]
name = "ProcessAndProcessAndProcess2068_82C09FE8"

# Process a large structure by wrapping sub-operations (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and three hundred and sixty-ninth form). It takes a structure, checks its state, and drives sub-operations (ProcessAndProcessAndProcess2068, sub_82C0A1A0) based on the result. This is a frequently-called helper in the 0x82C0A process cluster. 189 lines. 2 callees. 4 callers (UIText_RenderElement, sub_82C0A590, sub_82C0A988, sub_82C0AA60).
[entrypoint.functions."0x82C0A230"]
name = "ProcessAndProcessAndProcess2069_82C0A230"

# Process a very large data structure by inserting a linked-list node (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and three hundred and seventieth form). It takes a structure, allocates fresh memory (Allocate_OrDbgBreak), frees memory (Free_Thunk), inserts a linked-list node (ListNodeInsert), splices a linked-list node (SpliceListNode), sets a string (SetString), and drives sub-operations (sub_822F1EC0, sub_822F1F00, sub_826C3FA8) based on the result. This is a frequently-called processor in the 0x82C7E process cluster. 451 lines. 8 callees. 2 callers (ProcessAndProcessAndProcess2068 and sub_82C7E470).
[entrypoint.functions."0x82C7E138"]
name = "ProcessAndProcessAndProcess2070_82C7E138"

# Process a structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and three hundred and seventy-first form). It takes a structure and drives a sub-operation (ProcessAndProcessAndProcess2048). This is a frequently-called helper in the 0x82CD9 process cluster. 11 lines. 1 callee. 16 callers.
[entrypoint.functions."0x82CD9FD8"]
name = "ProcessAndProcessAndProcess2071_82CD9FD8"

# Process a large structure by comparing strings (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and three hundred and seventy-second form). It takes a structure, compares strings (CompareStrings), copy-assigns a refcounted object (CopyAssign_RefCounted), destructs a refcounted object (DestructRefCounted), and drives sub-operations (ProcessAndProcessAndProcess1750, sub_82858CA0) based on the result. This is a frequently-called helper in the 0x82858 process cluster. 117 lines. 5 callees. 17 callers.
[entrypoint.functions."0x82858910"]
name = "ProcessAndProcessAndProcess2072_82858910"

# Process a large structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and three hundred and seventy-third form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82B8A process cluster. 167 lines. 0 callees. 0 callers.
[entrypoint.functions."0x82B8A628"]
name = "ProcessAndProcessAndProcess2073_82B8A628"

# Process a very large data structure by finding a chain root (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and three hundred and seventy-fourth form). It takes a structure, finds a chain root (FindChainRoot), frees memory (Free_Thunk), inserts a linked-list node (ListNodeInsert), splices a linked-list node (SpliceListNode), sets a string (SetString), and drives sub-operations (sub_822F1EC0, sub_822F1F00, sub_826C3FA8) based on the result. This is a frequently-called processor in the 0x82BFC process cluster. 594 lines. 8 callees. 3 callers (ProcessAndProcessAndProcess1561, sub_82BFBC40, sub_82BFC6A8).
[entrypoint.functions."0x82BFC278"]
name = "ProcessAndProcessAndProcess2074_82BFC278"

# Process a structure by freeing memory (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and three hundred and seventy-fifth form). It takes a structure and frees memory (Free_Thunk). This is a frequently-called helper in the 0x82C04 process cluster. 66 lines. 1 callee. 6 callers.
[entrypoint.functions."0x82C045B8"]
name = "ProcessAndProcessAndProcess2075_82C045B8"

# Process a very large data structure by freeing memory (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and three hundred and seventy-sixth form). It takes a structure, frees memory (Free_Thunk), and drives sub-operations (ProcessAndProcessAndProcess1884, sub_82C52A28) based on the result. Part of the 0x82C53 process cluster. 385 lines. 3 callees. 0 callers.
[entrypoint.functions."0x82C53498"]
name = "ProcessAndProcessAndProcess2076_82C53498"

# Process a structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and three hundred and seventy-seventh form). It takes a structure and drives a sub-operation (ProcessAndProcessAndProcess2078). Part of the 0x82C55 process cluster. 7 lines. 1 callee. 1 caller (sub_82C54750).
[entrypoint.functions."0x82C55280"]
name = "ProcessAndProcessAndProcess2077_82C55280"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and three hundred and seventy-eighth form). It takes a structure and checks its state, with no external sub-operations. This is a frequently-called helper in the 0x82C56 process cluster. 12 lines. 0 callees. 2 callers (ProcessAndProcessAndProcess2077 and sub_82C559C0).
[entrypoint.functions."0x82C56BB0"]
name = "ProcessAndProcessAndProcess2078_82C56BB0"

# Process a structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and three hundred and seventy-ninth form). It takes a structure and drives a sub-operation (ProcessAndProcessAndProcess1887). Part of the 0x82CE4 process cluster. 90 lines. 1 callee. 1 caller (ProcessAndProcessAndProcess2080).
[entrypoint.functions."0x82CE4F40"]
name = "ProcessAndProcessAndProcess2079_82CE4F40"

# Process a large structure by wrapping sub-operations (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and three hundred and eightieth form). It takes a structure, checks its state, and drives sub-operations (sub_82CE4E90, ProcessAndProcessAndProcess2079) based on the result. This is a frequently-called helper in the 0x82CE4 process cluster. 105 lines. 2 callees. 4 callers.
[entrypoint.functions."0x82CE4FE0"]
name = "ProcessAndProcessAndProcess2080_82CE4FE0"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and three hundred and eighty-first form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82C6C process cluster. 68 lines. 0 callees. 0 callers.
[entrypoint.functions."0x82C6C608"]
name = "ProcessAndProcessAndProcess2081_82C6C608"

# Process a very large data structure by allocating fresh memory (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and three hundred and eighty-second form). It takes a structure, allocates fresh memory (Allocate_OrDbgBreak), frees memory (Free_Thunk), splices a linked-list node (List_SpliceNode4), updates a linked list (ProcessAndUpdateLinkedList), sets a string (SetString), and drives sub-operations (sub_822F1EC0, sub_822F1F00, sub_826C3FA8) based on the result. This is a frequently-called processor in the 0x82C82 process cluster. 455 lines. 8 callees. 5 callers.
[entrypoint.functions."0x82C82A80"]
name = "ProcessAndProcessAndProcess2082_82C82A80"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and three hundred and eighty-third form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82CD8 process cluster. 10 lines. 0 callees. 0 callers.
[entrypoint.functions."0x82CD8678"]
name = "ProcessAndProcessAndProcess2083_82CD8678"

# Process a large structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and three hundred and eighty-fourth form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82CD8 process cluster. 113 lines. 0 callees. 0 callers.
[entrypoint.functions."0x82CD86A0"]
name = "ProcessAndProcessAndProcess2084_82CD86A0"

# Process a large structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and three hundred and eighty-fifth form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82CD9 process cluster. 122 lines. 0 callees. 0 callers.
[entrypoint.functions."0x82CD9320"]
name = "ProcessAndProcessAndProcess2085_82CD9320"

# Process a large structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and three hundred and eighty-sixth form). It takes a structure and drives a sub-operation (sub_82CD2660). Part of the 0x82CE5 process cluster. 114 lines. 1 callee. 1 caller (sub_82CE5548).
[entrypoint.functions."0x82CE52F0"]
name = "ProcessAndProcessAndProcess2086_82CE52F0"

# Process a large structure by wrapping sub-operations (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and three hundred and eighty-seventh form). It takes a structure, checks its state, and drives sub-operations (ProcessAndProcessAndProcess1866, sub_82CD7FF8) based on the result. This is a frequently-called helper in the 0x82CE5 process cluster. 129 lines. 2 callees. 2 callers (sub_82CE48C8 and ProcessAndProcessAndProcess2092).
[entrypoint.functions."0x82CE5AB8"]
name = "ProcessAndProcessAndProcess2087_82CE5AB8"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and three hundred and eighty-eighth form). It takes a structure and checks its state, with no external sub-operations. This is a frequently-called helper in the 0x82CE5 process cluster. 87 lines. 0 callees. 2 callers (sub_82CE4A50 and ProcessAndProcessAndProcess2091).
[entrypoint.functions."0x82CE5E08"]
name = "ProcessAndProcessAndProcess2088_82CE5E08"

# Process a large structure by wrapping sub-operations (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and three hundred and eighty-ninth form). It takes a structure, checks its state, and drives sub-operations (sub_82CD7D00, sub_82CD8050, sub_82CE5F08) based on the result. This is a frequently-called helper in the 0x82CE6 process cluster. 176 lines. 3 callees. 2 callers (sub_82CE4600 and ProcessAndProcessAndProcess2093).
[entrypoint.functions."0x82CE6680"]
name = "ProcessAndProcessAndProcess2089_82CE6680"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and three hundred and ninetieth form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82CD9 process cluster. 41 lines. 0 callees. 0 callers.
[entrypoint.functions."0x82CD92D8"]
name = "ProcessAndProcessAndProcess2090_82CD92D8"

# Process a structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and three hundred and ninety-first form). It takes a structure and drives a sub-operation (ProcessAndProcessAndProcess2088). This is a frequently-called helper in the 0x82CE5 process cluster. 38 lines. 1 callee. 2 callers (sub_82CDCAE0 and sub_82CDD388).
[entrypoint.functions."0x82CE5090"]
name = "ProcessAndProcessAndProcess2091_82CE5090"

# Process a large structure by wrapping sub-operations (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and three hundred and ninety-second form). It takes a structure, checks its state, and drives sub-operations (ProcessAndProcessAndProcess1866, ProcessAndProcessAndProcess2087) based on the result. This is a frequently-called helper in the 0x82CE5 process cluster. 100 lines. 2 callees. 2 callers (sub_82CDC4F8 and sub_82CDD4C8).
[entrypoint.functions."0x82CE5120"]
name = "ProcessAndProcessAndProcess2092_82CE5120"

# Process a large structure by wrapping sub-operations (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and three hundred and ninety-third form). It takes a structure, checks its state, and drives sub-operations (ProcessAndProcessAndProcess1863, ProcessAndProcessAndProcess2089) based on the result. This is a frequently-called helper in the 0x82CE5 process cluster. 157 lines. 2 callees. 2 callers (sub_82CDC770 and sub_82CDD320).
[entrypoint.functions."0x82CE51D8"]
name = "ProcessAndProcessAndProcess2093_82CE51D8"

# Process a large structure by wrapping sub-operations (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and three hundred and ninety-fourth form). It takes a structure, checks its state, and drives sub-operations (ProcessAndProcessAndProcess1913, ProcessAndProcessAndProcess2086) based on the result. Part of the 0x82CE5 process cluster. 118 lines. 2 callees. 1 caller (sub_82CDD318).
[entrypoint.functions."0x82CE5548"]
name = "ProcessAndProcessAndProcess2094_82CE5548"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and three hundred and ninety-fifth form). It takes a structure and checks its state, with no external sub-operations. This is a frequently-called helper in the 0x82FFC process cluster. 12 lines. 0 callees. 8 callers.
[entrypoint.functions."0x82FFCFA8"]
name = "ProcessAndProcessAndProcess2095_82FFCFA8"

# Process a structure by accessing an element (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and three hundred and ninety-sixth form). It takes a structure and accesses an element (AccessElement8). This is a frequently-called helper in the 0x82309 process cluster. 59 lines. 1 callee. 77 callers.
[entrypoint.functions."0x82309520"]
name = "ProcessAndProcessAndProcess2096_82309520"

# Process a structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and three hundred and ninety-seventh form). It takes a structure and drives a sub-operation (ProcessAndProcessAndProcess1248). Part of the 0x82A7F process cluster. 75 lines. 1 callee. 1 caller (ProcessAndProcessAndProcess2098).
[entrypoint.functions."0x82A7FB48"]
name = "ProcessAndProcessAndProcess2097_82A7FB48"

# Process a very large data structure by freeing size-bucketed memory (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and three hundred and ninety-eighth form). It takes a structure, frees size-bucketed memory (Free_SizeBucketed), computes a size (ProcessSizeCompute), and drives sub-operations (ProcessAndProcessAndProcess1248, ProcessAndProcessAndProcess1853, ProcessAndProcessAndProcess2097, sub_82A800A0, sub_82A97648) based on the result. Part of the 0x82A7F process cluster. 392 lines. 7 callees. 1 caller (sub_82A7F580).
[entrypoint.functions."0x82A7FBC0"]
name = "ProcessAndProcessAndProcess2098_82A7FBC0"

# Process a large structure by wrapping sub-operations (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and three hundred and ninety-ninth form). It takes a structure, checks its state, gets a field (GetField833), processes and grows a bit array (ProcessAndProcessAndProcess1964), and drives sub-operations (sub_82BFF7D8, ProcessAndProcessAndProcess2101) based on the result. Part of the 0x82C01 process cluster. 196 lines. 4 callees. 0 callers.
[entrypoint.functions."0x82C01C10"]
name = "ProcessAndProcessAndProcess2099_82C01C10"

# Process a large structure by wrapping sub-operations (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and four hundred form). It takes a structure, checks its state, and drives sub-operations (sub_82CAB630, sub_82CAB770) based on the result. This is a frequently-called helper in the 0x82CA7 process cluster. 111 lines. 2 callees. 2 callers (ProcessAndProcessAndProcess2101 and sub_82CA7468).
[entrypoint.functions."0x82CA7348"]
name = "ProcessAndProcessAndProcess2100_82CA7348"

# Process a structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and four hundred and first form). It takes a structure and drives a sub-operation (ProcessAndProcessAndProcess2100). This is a frequently-called helper in the 0x82CA7 process cluster. 54 lines. 1 callee. 11 callers.
[entrypoint.functions."0x82CA7408"]
name = "ProcessAndProcessAndProcess2101_82CA7408"

# Process a very large data structure by processing a lookup with default initialization (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and four hundred and second form). It takes a structure, processes a lookup with default initialization (ProcessAndLookupWithDefaultInit), looks up a masked value (LookupMasked), stores a byte (ProcessAndStoreByte), checks character-type predicates (Ctype_Pred_Bit2, Ctype_Pred_Bit3), and drives sub-operations (sub_82CA2B88, sub_82CA6B30, sub_82CAB630, sub_82CAB770, sub_82CAF6C8, ProcessAndProcessAndProcess2103, sub_82CBA120) based on the result. Part of the 0x82CB2 process cluster. 2689 lines. 13 callees. 0 callers.
[entrypoint.functions."0x82CB2038"]
name = "ProcessAndProcessAndProcess2102_82CB2038"

# Process a large structure by wrapping sub-operations (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and four hundred and third form). It takes a structure, checks its state, and drives sub-operations (sub_82CAB630, sub_82CAB770, sub_82CAF6C8, sub_82CB67A8, sub_82CB6D00) based on the result. This is a frequently-called helper in the 0x82CB5 process cluster. 265 lines. 5 callees. 8 callers.
[entrypoint.functions."0x82CB5958"]
name = "ProcessAndProcessAndProcess2103_82CB5958"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and four hundred and fourth form). It takes a structure and checks its state, with no external sub-operations. This is a frequently-called helper in the 0x82A45 process cluster. 8 lines. 0 callees. 2 callers (sub_831B4DD8 and sub_831B5018).
[entrypoint.functions."0x82A45B10"]
name = "ProcessAndProcessAndProcess2104_82A45B10"

# Process a structure by allocating fresh memory (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and four hundred and fifth form). It takes a structure, allocates fresh memory (Allocate_OrDbgBreak), and drives a sub-operation (ProcessAndProcessAndProcess2106) based on the result. Part of the 0x82A45 process cluster. 44 lines. 2 callees. 0 callers.
[entrypoint.functions."0x82A45A00"]
name = "ProcessAndProcessAndProcess2105_82A45A00"

# Process a very large data structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and four hundred and sixth form). It takes a structure and drives a sub-operation (ProcessAndProcessAndProcess2107). Part of the 0x82A7C process cluster. 1114 lines. 1 callee. 1 caller (ProcessAndProcessAndProcess2105).
[entrypoint.functions."0x82A7C028"]
name = "ProcessAndProcessAndProcess2106_82A7C028"

# Process a large structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and four hundred and seventh form). It takes a structure and checks its state, with no external sub-operations. This is a frequently-called helper in the 0x82A7C process cluster. 192 lines. 0 callees. 2 callers (sub_82A45B18 and ProcessAndProcessAndProcess2106).
[entrypoint.functions."0x82A7C808"]
name = "ProcessAndProcessAndProcess2107_82A7C808"

# Process a large structure by wrapping sub-operations (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and four hundred and eighth form). It takes a structure, checks its state, and drives sub-operations (sub_82A301A0, sub_82BC9708, sub_82BCC1C0) based on the result. Part of the 0x82BCC process cluster. 134 lines. 3 callees. 1 caller (ProcessAndDispatchElements).
[entrypoint.functions."0x82BCC2C8"]
name = "ProcessAndProcessAndProcess2108_82BCC2C8"

# Process a large structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and four hundred and ninth form). It takes a structure and drives a sub-operation (ProcessAndProcessAndProcess608). This is a frequently-called helper in the 0x82286 process cluster. 207 lines. 1 callee. 4 callers.
[entrypoint.functions."0x82286008"]
name = "ProcessAndProcessAndProcess2109_82286008"

# Process a structure by wrapping sub-operations (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and four hundred and tenth form). It takes a structure, checks its state, and drives sub-operations (ProcessAndProcessAndProcess1881, ProcessAndProcessAndProcess1991, ProcessAndVcallAndClear3, sub_82C10A10) based on the result. Part of the 0x82C10 process cluster. 96 lines. 4 callees. 0 callers.
[entrypoint.functions."0x82C10AE0"]
name = "ProcessAndProcessAndProcess2110_82C10AE0"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and four hundred and eleventh form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82C4F process cluster. 19 lines. 0 callees. 0 callers.
[entrypoint.functions."0x82C4FFD0"]
name = "ProcessAndProcessAndProcess2111_82C4FFD0"

# Process a very large data structure by allocating fresh memory (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and four hundred and twelfth form). It takes a structure, allocates fresh memory (Allocate_OrDbgBreak), splices a linked-list node (List_SpliceNode2), splices another linked-list node (List_SpliceNode3), frees and processes (ProcessAndFreeAndProcess9), sets a string (SetString), and drives sub-operations (sub_822F1F00, sub_826C3EF0, sub_826C3FA8) based on the result. This is a frequently-called processor in the 0x82300 process cluster. 352 lines. 8 callees. 5 callers.
[entrypoint.functions."0x823003C8"]
name = "ProcessAndProcessAndProcess2112_823003C8"

# Process a very large data structure by entering a critical section (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and four hundred and thirteenth form). It takes a structure, enters a critical section (CriticalSection_EnterOrTry), gets the timebase (GetTimebase), processes (ProcessAndProcessAndProcess1795), validates (ProcessAndValidateAndProcess3), and drives a sub-operation (ProcessAndProcessAndProcess2115) based on the result. Part of the 0x82B4E process cluster. 262 lines. 5 callees. 1 caller (ProcessAndProcessAndProcess2114).
[entrypoint.functions."0x82B4E098"]
name = "ProcessAndProcessAndProcess2113_82B4E098"

# Process a very large data structure by allocating fresh memory (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and four hundred and fourteenth form). It takes a structure, allocates fresh memory (Allocate_OrDbgBreak), enters a critical section (CriticalSection_EnterOrTry), frees size-bucketed memory (Free_SizeBucketed), processes and allocates (ProcessAndAllocateAndProcess11), processes a timebase (ProcessAndTimebaseAndProcess2), and drives a sub-operation (ProcessAndProcessAndProcess2113) based on the result. Part of the 0x82B4E process cluster. 275 lines. 6 callees. 0 callers.
[entrypoint.functions."0x82B4ED00"]
name = "ProcessAndProcessAndProcess2114_82B4ED00"

# Process a large structure by finding a chain root (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and four hundred and fifteenth form). It takes a structure, finds a chain root (FindChainRoot4), and drives a sub-operation (ProcessAndProcessAndProcess2112) based on the result. Part of the 0x82B4F process cluster. 183 lines. 2 callees. 1 caller (ProcessAndProcessAndProcess2113).
[entrypoint.functions."0x82B4F058"]
name = "ProcessAndProcessAndProcess2115_82B4F058"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and four hundred and sixteenth form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82C12 process cluster. 24 lines. 0 callees. 0 callers.
[entrypoint.functions."0x82C12D08"]
name = "ProcessAndProcessAndProcess2116_82C12D08"

# Process a large structure by setting a global field (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and four hundred and seventeenth form). It takes a structure, sets a global field (SetGlobalField_Thunk), and computes a UTF-16 string length (StrLenUTF16). Part of the 0x82CC8 process cluster. 177 lines. 2 callees. 1 caller (ProcessAndClearAndConvert).
[entrypoint.functions."0x82CC8B50"]
name = "ProcessAndProcessAndProcess2117_82CC8B50"

# Process a structure by copy-assigning a refcounted object (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and four hundred and eighteenth form). It takes a structure, atomic-increments a counter (AtomicIncrement5), copy-assigns a refcounted object (CopyAssign_RefCounted), gets a sub-object field (GetSubObjField4), and converts to uppercase (ToUpper_SdkRuntime). This is a frequently-called helper in the 0x821EE process cluster. 80 lines. 4 callees. 7 callers.
[entrypoint.functions."0x821EEA90"]
name = "ProcessAndProcessAndProcess2118_821EEA90"

# Process a structure by destructing a refcounted object (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and four hundred and nineteenth form). It takes a structure, destructs a refcounted object (DestructRefCounted), computes an FNV-1 hash (FNV1Hash), gets a field (GetField833), and drives a sub-operation (ProcessAndProcessAndProcess2118) based on the result. This is a frequently-called helper in the 0x8228A process cluster. 55 lines. 4 callees. 12 callers.
[entrypoint.functions."0x8228A4C0"]
name = "ProcessAndProcessAndProcess2119_8228A4C0"

# Process a structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and four hundred and twentieth form). It takes a structure and drives a sub-operation (ProcessAndProcessAndProcess1754). This is a frequently-called helper in the 0x829FB process cluster. 57 lines. 1 callee. 16 callers.
[entrypoint.functions."0x829FB600"]
name = "ProcessAndProcessAndProcess2120_829FB600"

# Process a large structure by copy-assigning a refcounted object (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and four hundred and twenty-first form). It takes a structure, copy-assigns a refcounted object (CopyAssign_RefCounted), and drives a sub-operation (sub_823076D8) based on the result. This is a frequently-called helper in the 0x82175 process cluster. 109 lines. 2 callees. 22 callers.
[entrypoint.functions."0x82175020"]
name = "ProcessAndProcessAndProcess2121_82175020"

# Process a structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and four hundred and twenty-second form). It takes a structure and drives a sub-operation (ProcessAndProcessAndProcess1754). Part of the 0x829FB process cluster. 91 lines. 1 callee. 1 caller (sub_829FBC40).
[entrypoint.functions."0x829FBA70"]
name = "ProcessAndProcessAndProcess2122_829FBA70"

# Process a structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and four hundred and twenty-third form). It takes a structure and drives a sub-operation (ProcessAndProcessAndProcess1754). Part of the 0x829FB process cluster. 88 lines. 1 callee. 1 caller (sub_829FBC40).
[entrypoint.functions."0x829FBB10"]
name = "ProcessAndProcessAndProcess2123_829FBB10"

# Process a structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and four hundred and twenty-fourth form). It takes a structure and drives a sub-operation (ProcessAndProcessAndProcess1754). This is a frequently-called helper in the 0x829FB process cluster. 85 lines. 1 callee. 7 callers.
[entrypoint.functions."0x829FBBA8"]
name = "ProcessAndProcessAndProcess2124_829FBBA8"

# Process a large structure by wrapping sub-operations (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and four hundred and twenty-fifth form). It takes a structure, checks its state, processes a chain (ProcessChain2), and processes a hash table (ProcessHashTable) based on the result. Part of the 0x829FC process cluster. 209 lines. 2 callees. 1 caller (sub_829FBC40).
[entrypoint.functions."0x829FC180"]
name = "ProcessAndProcessAndProcess2125_829FC180"

# Process a large structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and four hundred and twenty-sixth form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x829FC process cluster. 111 lines. 0 callees. 1 caller (sub_829FBC40).
[entrypoint.functions."0x829FC2F0"]
name = "ProcessAndProcessAndProcess2126_829FC2F0"

# Process a large structure by wrapping sub-operations (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and four hundred and twenty-seventh form). It takes a structure, checks its state, and drives sub-operations (ProcessAndProcessAndProcess1883, ProcessAndProcessAndProcess590) based on the result. This is a frequently-called helper in the 0x82B18 process cluster. 147 lines. 2 callees. 15 callers.
[entrypoint.functions."0x82B18A20"]
name = "ProcessAndProcessAndProcess2127_82B18A20"

# Process a very large data structure by wrapping sub-operations (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and four hundred and twenty-eighth form). It takes a structure, checks its state, and drives sub-operations (ProcessAndProcessAndProcess1653, ProcessAndProcessAndProcess1654, ProcessAndProcessAndProcess1872, ProcessAndProcessAndProcess731) based on the result. Part of the 0x82B8D process cluster. 2191 lines. 4 callees. 1 caller (ProcessAndProcessAndProcess1937).
[entrypoint.functions."0x82B8D010"]
name = "ProcessAndProcessAndProcess2128_82B8D010"

# Process a structure by wrapping sub-operations (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and four hundred and twenty-ninth form). It takes a structure, checks its state, and drives sub-operations (ProcessAndProcessAndProcess313, ProcessAndProcessAndProcess314, ProcessAndProcessAndProcess503, ProcessAndProcessAndProcess560, ProcessAndProcessAndProcess783) based on the result. This is a frequently-called helper in the 0x82175 process cluster. 95 lines. 5 callees. 18 callers.
[entrypoint.functions."0x821756E0"]
name = "ProcessAndProcessAndProcess2129_821756E0"

# Process a structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and four hundred and thirtieth form). It takes a structure and drives a sub-operation (sub_8229F200). This is a frequently-called helper in the 0x822A0 process cluster. 96 lines. 1 callee. 2 callers (ProcessAndProcessAndProcess1999 and sub_82B15910).
[entrypoint.functions."0x822A0EB8"]
name = "ProcessAndProcessAndProcess2130_822A0EB8"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and four hundred and thirty-first form). It takes a structure and checks its state, with no external sub-operations. This is a frequently-called helper in the 0x82CBB process cluster. 11 lines. 0 callees. 3 callers (sub_82239888, sub_82A47D48, sub_82B38478).
[entrypoint.functions."0x82CBB620"]
name = "ProcessAndProcessAndProcess2131_82CBB620"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and four hundred and thirty-second form). It takes a structure and checks its state, with no external sub-operations. This is a frequently-called helper in the 0x8273A process cluster. 112 lines. 0 callees. 33 callers.
[entrypoint.functions."0x8273ACB8"]
name = "ProcessAndProcessAndProcess2132_8273ACB8"

# Process a structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and four hundred and thirty-third form). It takes a structure and drives a sub-operation (ProcessAndProcessAndProcess1657). This is a frequently-called helper in the 0x82C5D process cluster. 11 lines. 1 callee. 11 callers.
[entrypoint.functions."0x82C5DB88"]
name = "ProcessAndProcessAndProcess2133_82C5DB88"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and four hundred and thirty-fourth form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x83226 process cluster. 8 lines. 0 callees. 0 callers.
[entrypoint.functions."0x832261A0"]
name = "ProcessAndProcessAndProcess2134_832261A0"

# Process a structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and four hundred and thirty-fifth form). It takes a structure and drives a sub-operation (ProcessAndProcessAndProcess2138). This is a frequently-called helper in the 0x821CD process cluster. 82 lines. 1 callee. 2 callers (ProcessAndProcessAndProcess2137 and sub_82B5B388).
[entrypoint.functions."0x821CDD00"]
name = "ProcessAndProcessAndProcess2135_821CDD00"

# Process a large structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and four hundred and thirty-sixth form). It takes a structure and drives a sub-operation (ProcessAndProcessAndProcess2137). This is a frequently-called helper in the 0x821D7 process cluster. 128 lines. 1 callee. 2 callers (ProcessAndProcessAndProcess647 and sub_82A6D8C0).
[entrypoint.functions."0x821D7650"]
name = "ProcessAndProcessAndProcess2136_821D7650"

# Process a very large data structure by wrapping sub-operations (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and four hundred and thirty-seventh form). It takes a structure, checks its state, and drives sub-operations (ProcessAndProcessAndProcess2109, ProcessAndProcessAndProcess2135, ProcessAndProcessAndProcess2139, ProcessAndProcessAndProcess2140, ProcessAndProcessAndProcess2144) based on the result. This is a frequently-called processor in the 0x821E0 process cluster. 331 lines. 5 callees. 3 callers (ProcessAndProcessAndProcess2136, sub_82AB2650, sub_82B5B5D0).
[entrypoint.functions."0x821E08E0"]
name = "ProcessAndProcessAndProcess2137_821E08E0"

# Process a very large data structure by wrapping sub-operations (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and four hundred and thirty-eighth form). It takes a structure, checks its state, and drives sub-operations (ProcessAndProcessAndProcess1322, sub_82B5BDA0) based on the result. Part of the 0x821EE process cluster. 326 lines. 2 callees. 1 caller (ProcessAndProcessAndProcess2135).
[entrypoint.functions."0x821EE438"]
name = "ProcessAndProcessAndProcess2138_821EE438"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and four hundred and thirty-ninth form). It takes a structure and checks its state, with no external sub-operations. This is a frequently-called helper in the 0x8228C process cluster. 70 lines. 0 callees. 4 callers.
[entrypoint.functions."0x8228C028"]
name = "ProcessAndProcessAndProcess2139_8228C028"

# Process a large structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and four hundred and fortieth form). It takes a structure and checks its state, with no external sub-operations. This is a frequently-called helper in the 0x8228C process cluster. 233 lines. 0 callees. 2 callers (ProcessAndProcessAndProcess2137 and sub_82B5B388).
[entrypoint.functions."0x8228C098"]
name = "ProcessAndProcessAndProcess2140_8228C098"

# Process a large structure by freeing size-bucketed memory (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and four hundred and forty-first form). It takes a structure and frees size-bucketed memory (Free_SizeBucketed). This is a frequently-called helper in the 0x82A50 process cluster. 134 lines. 1 callee. 4 callers.
[entrypoint.functions."0x82A50C78"]
name = "ProcessAndProcessAndProcess2141_82A50C78"

# Process a structure by allocating fresh memory (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and four hundred and forty-second form). It takes a structure, allocates fresh memory (Allocate_OrDbgBreak), frees and processes (ProcessAndFreeAndProcess9), sets a string (SetString), and drives sub-operations (sub_822F1F00, sub_826C3EF0, sub_826C3FA8) based on the result. This is a frequently-called helper in the 0x82A52 process cluster. 129 lines. 6 callees. 2 callers (ProcessAndProcessAndProcess647 and sub_82AB7B68).
[entrypoint.functions."0x82A52470"]
name = "ProcessAndProcessAndProcess2142_82A52470"

# Process a very large data structure by wrapping sub-operations (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and four hundred and forty-third form). It takes a structure, checks its state, atomic-increments a refcount (IncrementRefcountAtomic), and drives sub-operations (ProcessAndProcessAndProcess1768, ProcessAndProcessAndProcess1782, ProcessAndProcessAndProcess1877, ProcessAndProcessAndProcess1921, ProcessAndProcessAndProcess524, ProcessAndProcessAndProcess2146) based on the result. This is a frequently-called processor in the 0x82A6D process cluster. 239 lines. 7 callees. 5 callers.
[entrypoint.functions."0x82A6DDE0"]
name = "ProcessAndProcessAndProcess2143_82A6DDE0"

# Process a very large data structure by allocating fresh memory (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and four hundred and forty-fourth form). It takes a structure, allocates fresh memory (Allocate_OrDbgBreak), sets a refcounted wrapper (SetRefcountedWrapper), and drives sub-operations (ProcessAndProcessAndProcess524, ProcessAndProcessAndProcess608, ProcessAndProcessAndProcess880) based on the result. This is a frequently-called processor in the 0x82B5A process cluster. 287 lines. 5 callees. 2 callers (ProcessAndProcessAndProcess2137 and sub_82AB3758).
[entrypoint.functions."0x82B5A310"]
name = "ProcessAndProcessAndProcess2144_82B5A310"

# Process a large structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and four hundred and forty-fifth form). It takes a structure and drives a sub-operation (ProcessAndProcessAndProcess1768). This is a frequently-called helper in the 0x82B5A process cluster. 143 lines. 1 callee. 3 callers (ProcessAndProcessAndProcess1126, sub_82A6DF58, sub_82B5B5D0).
[entrypoint.functions."0x82B5A9D8"]
name = "ProcessAndProcessAndProcess2145_82B5A9D8"

# Process a structure by wrapping sub-operations (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and four hundred and forty-sixth form). It takes a structure, checks its state, and drives sub-operations (sub_82B5A248, sub_82B5C440) based on the result. This is a frequently-called helper in the 0x82B5B process cluster. 83 lines. 2 callees. 3 callers (sub_82A6D9F0, ProcessAndProcessAndProcess2143, sub_82AB2E48).
[entrypoint.functions."0x82B5BC30"]
name = "ProcessAndProcessAndProcess2146_82B5BC30"

# Process a large structure by wrapping sub-operations (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and four hundred and forty-seventh form). It takes a structure, checks its state, and drives sub-operations (sub_82B868C0, sub_82B86A08) based on the result. Part of the 0x82B66 process cluster. 147 lines. 2 callees. 1 caller (ProcessAndProcessAndProcess668).
[entrypoint.functions."0x82B66948"]
name = "ProcessAndProcessAndProcess2147_82B66948"

# Process a structure by copying memory (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and four hundred and forty-eighth form). It takes a structure and copies memory (MemCpy_SdkRuntime). Part of the 0x82B66 process cluster. 80 lines. 1 callee. 1 caller (ProcessAndProcessAndProcess2149).
[entrypoint.functions."0x82B66A50"]
name = "ProcessAndProcessAndProcess2148_82B66A50"

# Process a very large data structure by entering a critical section (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and four hundred and forty-ninth form). It takes a structure, enters a critical section (CriticalSection_EnterOrTry), allocates fresh memory (Allocate_OrDbgBreak), copies memory (MemCpy_SdkRuntime via ProcessAndProcessAndProcess2148), processes (ProcessAndProcessAndProcess1104, ProcessAndProcessAndProcess533), and drives a sub-operation (ProcessAndProcessAndProcess2153) based on the result. This is a frequently-called processor in the 0x82B66 process cluster. 473 lines. 6 callees. 2 callers (ProcessAndProcessAndProcess647 and sub_821F79A8).
[entrypoint.functions."0x82B66C20"]
name = "ProcessAndProcessAndProcess2149_82B66C20"

# Process a structure by freeing size-bucketed memory (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and four hundred and fiftieth form). It takes a structure and frees size-bucketed memory (Free_SizeBucketed). This is a frequently-called helper in the 0x82B67 process cluster. 50 lines. 1 callee. 2 callers (sub_82B66F68 and ProcessAndProcessAndProcess2151).
[entrypoint.functions."0x82B67310"]
name = "ProcessAndProcessAndProcess2150_82B67310"

# Process a very large data structure by entering a critical section (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and four hundred and fifty-first form). It takes a structure, enters a critical section (CriticalSection_EnterOrTry), checks a byte value (ByteValueCheck), processes (ProcessAndProcessAndProcess1104, ProcessAndProcessAndProcess1813), and drives a sub-operation (ProcessAndProcessAndProcess2150) based on the result. This is a frequently-called processor in the 0x82B67 process cluster. 338 lines. 5 callees. 3 callers.
[entrypoint.functions."0x82B67700"]
name = "ProcessAndProcessAndProcess2151_82B67700"

# Process a large structure by querying (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and four hundred and fifty-second form). It takes a structure, queries (ProcessAndQueryAndProcess6), and makes a virtual call (VirtualCall4). Part of the 0x82B68 process cluster. 213 lines. 2 callees. 1 caller (ProcessAndProcessAndProcess668).
[entrypoint.functions."0x82B68128"]
name = "ProcessAndProcessAndProcess2152_82B68128"

# Process a large structure by wrapping sub-operations (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and four hundred and fifty-third form). It takes a structure, checks its state, and drives sub-operations (ProcessAndProcessAndProcess1101, ProcessAndProcessAndProcess2154) based on the result. Part of the 0x82B68 process cluster. 171 lines. 2 callees. 1 caller (ProcessAndProcessAndProcess2149).
[entrypoint.functions."0x82B683E8"]
name = "ProcessAndProcessAndProcess2153_82B683E8"

# Process a very large data structure by wrapping sub-operations (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and four hundred and fifty-fourth form). It takes a structure, checks its state, and drives sub-operations (ProcessAndProcessAndProcess1039, ProcessAndProcessAndProcess1812, ProcessAndProcessAndProcess1878, ProcessAndProcessAndProcess2132) based on the result. Part of the 0x82B68 process cluster. 565 lines. 4 callees. 1 caller (ProcessAndProcessAndProcess2153).
[entrypoint.functions."0x82B687C0"]
name = "ProcessAndProcessAndProcess2154_82B687C0"

# Process a structure by freeing size-bucketed memory (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and four hundred and fifty-fifth form). It takes a structure, frees size-bucketed memory (Free_SizeBucketed), and drives a sub-operation (sub_822D6A80) based on the result. Part of the 0x82171 process cluster. 50 lines. 2 callees. 0 callers.
[entrypoint.functions."0x82171698"]
name = "ProcessAndProcessAndProcess2155_82171698"

# Process a structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and four hundred and fifty-sixth form). It takes a structure and drives a sub-operation (ProcessAndProcessAndProcess2129). Part of the 0x82175 process cluster. 100 lines. 1 callee. 0 callers.
[entrypoint.functions."0x82175780"]
name = "ProcessAndProcessAndProcess2156_82175780"

# Process a structure by freeing size-bucketed memory (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and four hundred and fifty-seventh form). It takes a structure, frees size-bucketed memory (Free_SizeBucketed), and drives a sub-operation (sub_8321C478) based on the result. Part of the 0x8217A process cluster. 50 lines. 2 callees. 0 callers.
[entrypoint.functions."0x8217A160"]
name = "ProcessAndProcessAndProcess2157_8217A160"

# Process a structure by freeing size-bucketed memory (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and four hundred and fifty-eighth form). It takes a structure, frees size-bucketed memory (Free_SizeBucketed), and drives a sub-operation (ProcessAndProcessAndProcess1789) based on the result. Part of the 0x822C0 process cluster. 62 lines. 2 callees. 0 callers.
[entrypoint.functions."0x822C0208"]
name = "ProcessAndProcessAndProcess2158_822C0208"

# Process a structure by freeing size-bucketed memory (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and four hundred and fifty-ninth form). It takes a structure, frees size-bucketed memory (Free_SizeBucketed), and drives a sub-operation (ProcessAndProcessAndProcess1849) based on the result. Part of the 0x822D6 process cluster. 66 lines. 2 callees. 1 caller (ProcessAndProcessAndProcess2155).
[entrypoint.functions."0x822D6A80"]
name = "ProcessAndProcessAndProcess2159_822D6A80"

# Process a structure by freeing size-bucketed memory (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and four hundred and sixtieth form). It takes a structure, frees size-bucketed memory (Free_SizeBucketed), and drives a sub-operation (ProcessAndProcessAndProcess2161) based on the result. Part of the 0x82A6F process cluster. 50 lines. 2 callees. 0 callers.
[entrypoint.functions."0x82A6FB80"]
name = "ProcessAndProcessAndProcess2160_82A6FB80"

# Process a structure by wrapping sub-operations (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and four hundred and sixty-first form). It takes a structure, checks its state, and drives sub-operations (ProcessAndProcessAndProcess2127, ProcessAndProcessAndProcess2163) based on the result. This is a frequently-called helper in the 0x82A6F process cluster. 76 lines. 2 callees. 2 callers (ProcessAndProcessAndProcess2160 and sub_82A6FE68).
[entrypoint.functions."0x82A6FCA0"]
name = "ProcessAndProcessAndProcess2161_82A6FCA0"

# Process a structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and four hundred and sixty-second form). It takes a structure and drives a sub-operation (ProcessAndProcessAndProcess1700). This is a frequently-called helper in the 0x82AAF process cluster. 47 lines. 1 callee. 3 callers (sub_82A4EB10, sub_82A4EE10, sub_82A4EF80).
[entrypoint.functions."0x82AAF558"]
name = "ProcessAndProcessAndProcess2162_82AAF558"

# Process a structure by wrapping sub-operations (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and four hundred and sixty-third form). It takes a structure, checks its state, and drives sub-operations (ProcessAndProcessAndProcess2130, ProcessAndProcessAndProcess784) based on the result. This is a frequently-called helper in the 0x82B15 process cluster. 66 lines. 2 callees. 10 callers.
[entrypoint.functions."0x82B15910"]
name = "ProcessAndProcessAndProcess2163_82B15910"

# Process a large structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and four hundred and sixty-fourth form). It takes a structure and drives a sub-operation (ProcessAndProcessAndProcess2167). This is a frequently-called helper in the 0x82B86 process cluster. 135 lines. 1 callee. 3 callers (sub_82B86778, ProcessAndProcessAndProcess2165, sub_82B86A08).
[entrypoint.functions."0x82B86690"]
name = "ProcessAndProcessAndProcess2164_82B86690"

# Process a large structure by wrapping sub-operations (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and four hundred and sixty-fifth form). It takes a structure, checks its state, and drives sub-operations (sub_82B859F0, sub_82B85D40, sub_82B85E88, sub_82B86358, ProcessAndProcessAndProcess2164) based on the result. Part of the 0x82B86 process cluster. 180 lines. 5 callees. 1 caller (ProcessAndProcessAndProcess2147).
[entrypoint.functions."0x82B868C0"]
name = "ProcessAndProcessAndProcess2165_82B868C0"

# Process a very large data structure by decrementing a refcount (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and four hundred and sixty-sixth form). It takes a structure, decrements a refcount (DecrementRefcountAtomic), sets memory (MemSet_SdkRuntime), and drives sub-operations (ProcessAndProcessAndProcess1051, ProcessAndProcessAndProcess1251, ProcessAndProcessAndProcess1407, ProcessAndProcessAndProcess1868, ProcessAndProcessAndProcess1905, ProcessAndProcessAndProcess1939, ProcessAndProcessAndProcess1940, ProcessAndProcessAndProcess1944, ProcessAndProcessAndProcess533, ProcessAndProcessAndProcess620, SwitchDispatch) based on the result. Part of the 0x82B8F process cluster. 457 lines. 13 callees. 0 callers.
[entrypoint.functions."0x82B8F220"]
name = "ProcessAndProcessAndProcess2166_82B8F220"

# Process a structure by allocating fresh memory (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and four hundred and sixty-seventh form). It takes a structure, allocates fresh memory (Allocate_OrDbgBreak), drives a sub-operation (ProcessAndProcessAndProcess1872), and drives another sub-operation (sub_82B8EBD0) based on the result. Part of the 0x82B8F process cluster. 57 lines. 3 callees. 1 caller (ProcessAndProcessAndProcess2164).
[entrypoint.functions."0x82B8F760"]
name = "ProcessAndProcessAndProcess2167_82B8F760"

# Process a structure by freeing size-bucketed memory (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and four hundred and sixty-eighth form). It takes a structure and frees size-bucketed memory (Free_SizeBucketed). This is a frequently-called helper in the 0x82BE4 process cluster. 63 lines. 1 callee. 4 callers (ProcessAndProcessAndProcess1622, sub_8321B140, ProcessAndProcessAndProcess2169, sub_8321CF78).
[entrypoint.functions."0x82BE4350"]
name = "ProcessAndProcessAndProcess2168_82BE4350"

# Process a large structure by freeing size-bucketed memory (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and four hundred and sixty-ninth form). It takes a structure, frees size-bucketed memory (Free_SizeBucketed), drives a sub-operation (ProcessAndProcessAndProcess1730), and drives another sub-operation (ProcessAndProcessAndProcess2168) based on the result. Part of the 0x8321C process cluster. 95 lines. 3 callees. 1 caller (ProcessAndProcessAndProcess2157).
[entrypoint.functions."0x8321C478"]
name = "ProcessAndProcessAndProcess2169_8321C478"

# Process a large structure by freeing size-bucketed memory (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and four hundred and seventieth form). It takes a structure, frees size-bucketed memory (Free_SizeBucketed), drives a sub-operation (ProcessAndProcessAndProcess1850), and releases a refcounted object (Release_RefCounted) based on the result. This is a frequently-called helper in the 0x83221 process cluster. 107 lines. 3 callees. 5 callers.
[entrypoint.functions."0x83221C10"]
name = "ProcessAndProcessAndProcess2170_83221C10"

# Process a very large data structure by freeing size-bucketed memory (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and four hundred and seventy-first form). It takes a structure, frees size-bucketed memory (Free_SizeBucketed), frees and processes (ProcessAndFreeAndProcess5), processes (ProcessAndProcessAndProcess1824), validates (ProcessAndValidateAndProcess3), and releases a refcounted object (Release_RefCounted) based on the result, and drives a sub-operation (ProcessAndProcessAndProcess2170). This is a frequently-called processor in the 0x83229 process cluster. 215 lines. 6 callees. 5 callers.
[entrypoint.functions."0x83229940"]
name = "ProcessAndProcessAndProcess2171_83229940"

# Process a very large data structure by finding a chain root (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and four hundred and seventy-second form). It takes a structure, finds a chain root (FindChainRoot3), frees size-bucketed memory (Free_SizeBucketed), splices a linked-list node (List_SpliceNode2), splices another linked-list node (List_SpliceNode3), frees and processes (ProcessAndFreeAndProcess9), sets a string (SetString), and drives sub-operations (sub_822F1F00, sub_826C3EF0, sub_826C3FA8) based on the result. This is a frequently-called processor in the 0x822FF process cluster. 583 lines. 9 callees. 3 callers.
[entrypoint.functions."0x822FF3C8"]
name = "ProcessAndProcessAndProcess2172_822FF3C8"

# Process a large structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and four hundred and seventy-third form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82B16 process cluster. 197 lines. 0 callees. 1 caller (sub_82B1A348).
[entrypoint.functions."0x82B16DB0"]
name = "ProcessAndProcessAndProcess2173_82B16DB0"

# Process a very large data structure by copying memory (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and four hundred and seventy-fourth form). It takes a structure, copies memory (MemCpy_SdkRuntime), sets memory (MemSet_SdkRuntime), and copies a struct region (ProcessAndCopyStructRegion). This is a frequently-called processor in the 0x82B85 process cluster. 971 lines. 3 callees. 4 callers.
[entrypoint.functions."0x82B852D0"]
name = "ProcessAndProcessAndProcess2174_82B852D0"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and four hundred and seventy-fifth form). It takes a structure and checks its state, with no external sub-operations. This is a frequently-called helper in the 0x822F1 process cluster. 71 lines. 0 callees. 2 callers (ProcessAndProcessAndProcess2050 and sub_822F17C8).
[entrypoint.functions."0x822F16C8"]
name = "ProcessAndProcessAndProcess2175_822F16C8"

# Process a structure by freeing size-bucketed memory (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and four hundred and seventy-sixth form). It takes a structure and frees size-bucketed memory (Free_SizeBucketed). Part of the 0x822ED process cluster. 49 lines. 1 callee. 1 caller (sub_822EC9C8).
[entrypoint.functions."0x822EDFC8"]
name = "ProcessAndProcessAndProcess2176_822EDFC8"

# Process a structure by setting a string (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and four hundred and seventy-seventh form). It takes a structure and sets a string (SetString). Part of the 0x822F1 process cluster. 40 lines. 1 callee. 1 caller (sub_822EC9C8).
[entrypoint.functions."0x822F1C98"]
name = "ProcessAndProcessAndProcess2177_822F1C98"

# Process a structure by freeing size-bucketed memory (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and four hundred and seventy-eighth form). It takes a structure, frees size-bucketed memory (Free_SizeBucketed), and drives a sub-operation (ProcessAndProcessAndProcess1824) based on the result. Part of the 0x82A08 process cluster. 50 lines. 2 callees. 0 callers.
[entrypoint.functions."0x82A08B18"]
name = "ProcessAndProcessAndProcess2178_82A08B18"

# Process a structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and four hundred and seventy-ninth form). It takes a structure and drives a sub-operation (ProcessAndProcessAndProcess1270). Part of the 0x82B13 process cluster. 67 lines. 1 callee. 1 caller (ProcessAndProcessAndProcess2180).
[entrypoint.functions."0x82B133A8"]
name = "ProcessAndProcessAndProcess2179_82B133A8"

# Process a very large data structure by freeing size-bucketed memory (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and four hundred and eightieth form). It takes a structure, frees size-bucketed memory (Free_SizeBucketed), computes a size (ProcessSizeCompute), and drives sub-operations (ProcessAndProcessAndProcess1270, ProcessAndProcessAndProcess2179, sub_82B13BB0, sub_82A97648) based on the result. This is a very frequently-called processor in the 0x82B13 process cluster. 480 lines. 6 callees. 299 callers.
[entrypoint.functions."0x82B134D8"]
name = "ProcessAndProcessAndProcess2180_82B134D8"

# Process a structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and four hundred and eighty-first form). It takes a structure and drives a sub-operation (ProcessAndProcessAndProcess1120). This is a frequently-called helper in the 0x83222 process cluster. 67 lines. 1 callee. 4 callers.
[entrypoint.functions."0x83222610"]
name = "ProcessAndProcessAndProcess2181_83222610"

# Process a structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and four hundred and eighty-second form). It takes a structure and drives a sub-operation (ProcessAndProcessAndProcess1694). This is a frequently-called helper in the 0x827DF process cluster. 94 lines. 1 callee. 2 callers (sub_827DF7F0 and sub_827DF8D0).
[entrypoint.functions."0x827DFD18"]
name = "ProcessAndProcessAndProcess2182_827DFD18"

# Process a large structure by allocating fresh memory (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and four hundred and eighty-third form). It takes a structure, allocates fresh memory (Allocate_OrDbgBreak), constructs and processes (ConstructAndProcess29), releases a refcounted object (Release_RefCounted), sets a value (SetValue4), and drives a sub-operation (ProcessAndProcessAndProcess2041) based on the result. Part of the 0x82A08 process cluster. 185 lines. 5 callees. 1 caller (ProcessAndProcessAndProcess1828).
[entrypoint.functions."0x82A08C80"]
name = "ProcessAndProcessAndProcess2183_82A08C80"

# Process a structure by setting memory (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and four hundred and eighty-fourth form). It takes a structure and sets memory (MemSet_SdkRuntime). This is a frequently-called helper in the 0x83005 process cluster. 88 lines. 1 callee. 3 callers (sub_82AB0728, sub_82B850B8, sub_82FE9608).
[entrypoint.functions."0x830052A8"]
name = "ProcessAndProcessAndProcess2184_830052A8"

# Process a structure by wrapping sub-operations (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and four hundred and eighty-fifth form). It takes a structure, checks its state, and drives sub-operations (ProcessAndProcessAndProcess2131, sub_82239750) based on the result. This is a frequently-called helper in the 0x82239 process cluster. 20 lines. 2 callees. 2 callers (ProcessAndProcessAndProcess655 and sub_82BA0220).
[entrypoint.functions."0x82239888"]
name = "ProcessAndProcessAndProcess2185_82239888"

# Process a large structure by wrapping sub-operations (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and four hundred and eighty-sixth form). It takes a structure, checks its state, disposes (Dispose17), and drives a sub-operation (ProcessAndProcessAndProcess2187) based on the result. This is a frequently-called helper in the 0x82A51 process cluster. 135 lines. 2 callees. 6 callers.
[entrypoint.functions."0x82A519D0"]
name = "ProcessAndProcessAndProcess2186_82A519D0"

# Process a very large data structure by validating (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and four hundred and eighty-seventh form). It takes a structure, validates (ProcessAndValidateAndProcess3), processes a chain (ProcessChain2), and drives sub-operations (sub_82A52630, ProcessAndProcessAndProcess2188) based on the result. Part of the 0x82A52 process cluster. 311 lines. 4 callees. 1 caller (ProcessAndProcessAndProcess2186).
[entrypoint.functions."0x82A52148"]
name = "ProcessAndProcessAndProcess2187_82A52148"

# Process a very large data structure by allocating fresh memory (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and four hundred and eighty-eighth form). It takes a structure, allocates fresh memory (Allocate_OrDbgBreak), frees and processes (ProcessAndFreeAndProcess9), sets a string (SetString), and drives sub-operations (sub_822F1F00, sub_826C3EF0, sub_826C3FA8, sub_82A52370, sub_82A523F0) based on the result. This is a frequently-called processor in the 0x82A52 process cluster. 382 lines. 8 callees. 2 callers (ProcessAndProcessAndProcess2187 and sub_82A52630).
[entrypoint.functions."0x82A52860"]
name = "ProcessAndProcessAndProcess2188_82A52860"

# Process a very large data structure by constructing a node (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and four hundred and eighty-ninth form). It takes a structure, constructs a node (ConstructNode20), atomic-increments a refcount (IncrementRefcountAtomic), and drives sub-operations (ProcessAndProcessAndProcess1768, ProcessAndProcessAndProcess1782, ProcessAndProcessAndProcess1921, ProcessAndProcessAndProcess524, ProcessAndProcessAndProcess2190) based on the result. Part of the 0x82A6D process cluster. 369 lines. 7 callees. 1 caller (ProcessAndProcessAndProcess604).
[entrypoint.functions."0x82A6D650"]
name = "ProcessAndProcessAndProcess2189_82A6D650"

# Process a large structure by copying memory (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and four hundred and ninetieth form). It takes a structure, copies memory (MemCpy_SdkRuntime), sets memory (MemSet_SdkRuntime), queries (ProcessAndQueryAndProcess6), and drives sub-operations (sub_82A6E200, ProcessAndProcessAndProcess2174) based on the result. Part of the 0x82A6E process cluster. 187 lines. 5 callees. 1 caller (ProcessAndProcessAndProcess2189).
[entrypoint.functions."0x82A6EA50"]
name = "ProcessAndProcessAndProcess2190_82A6EA50"

# Process a large structure by allocating fresh memory (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and four hundred and ninety-first form). It takes a structure, allocates fresh memory (Allocate_OrDbgBreak), reallocates a buffer (ProcessAndReallocateBuffer), and releases a refcounted object (Release_RefCounted). This is a frequently-called helper in the 0x82B39 process cluster. 199 lines. 3 callees. 4 callers.
[entrypoint.functions."0x82B394B8"]
name = "ProcessAndProcessAndProcess2191_82B394B8"

# Process a structure by copying memory (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and four hundred and ninety-second form). It takes a structure, copies memory vectorized (MemCpy_Vectorized), sets memory (MemSet_SdkRuntime), and drives a sub-operation (ProcessAndProcessAndProcess2193) based on the result. This is a frequently-called helper in the 0x82B39 process cluster. 110 lines. 3 callees. 2 callers (ProcessAndReplaceValue2 and sub_82B38F30).
[entrypoint.functions."0x82B39A88"]
name = "ProcessAndProcessAndProcess2192_82B39A88"

# Process a large structure by allocating fresh memory (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and four hundred and ninety-third form). It takes a structure, allocates fresh memory (Allocate_OrDbgBreak), frees size-bucketed memory (Free_SizeBucketed), copies memory vectorized (MemCpy_Vectorized), and sets memory (MemSet_SdkRuntime). This is a frequently-called helper in the 0x82B39 process cluster. 227 lines. 4 callees. 2 callers (sub_82B39010 and ProcessAndProcessAndProcess2192).
[entrypoint.functions."0x82B39C48"]
name = "ProcessAndProcessAndProcess2193_82B39C48"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and four hundred and ninety-fourth form). It takes a structure and checks its state, with no external sub-operations. This is a frequently-called helper in the 0x82BFC process cluster. 14 lines. 0 callees. 10 callers.
[entrypoint.functions."0x82BFC810"]
name = "ProcessAndProcessAndProcess2194_82BFC810"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and four hundred and ninety-fifth form). It takes a structure and checks its state, with no external sub-operations. This is a frequently-called helper in the 0x82BFC process cluster. 21 lines. 0 callees. 3 callers (sub_82449448, sub_82C10498, sub_82C217B8).
[entrypoint.functions."0x82BFC8D0"]
name = "ProcessAndProcessAndProcess2195_82BFC8D0"

# Process a large structure by constructing a refcounted object (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and four hundred and ninety-sixth form). It takes a structure, constructs a refcounted object (ConstructRefCounted), and destructs a refcounted object (DestructRefCounted). This is a frequently-called helper in the 0x82C4F process cluster. 142 lines. 2 callees. 3 callers (sub_82C1E878, sub_82C1F910, sub_82C217B8).
[entrypoint.functions."0x82C4F8A8"]
name = "ProcessAndProcessAndProcess2196_82C4F8A8"

# Process a large structure by allocating fresh memory (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and four hundred and ninety-seventh form). It takes a structure, allocates fresh memory (Allocate_OrDbgBreak), constructs a refcounted object (ConstructRefCounted), and destructs a refcounted object (DestructRefCounted). This is a frequently-called helper in the 0x82C4F process cluster. 188 lines. 3 callees. 3 callers (sub_82C1E878, sub_82C1F910, sub_82C217B8).
[entrypoint.functions."0x82C4FA78"]
name = "ProcessAndProcessAndProcess2197_82C4FA78"

# Process a structure by freeing size-bucketed memory (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and four hundred and ninety-eighth form). It takes a structure, frees size-bucketed memory (Free_SizeBucketed), and drives a sub-operation (ProcessAndProcessAndProcess2200) based on the result. Part of the 0x82A00 process cluster. 50 lines. 2 callees. 0 callers.
[entrypoint.functions."0x82A00768"]
name = "ProcessAndProcessAndProcess2198_82A00768"

# Process a structure by allocating fresh memory (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and four hundred and ninety-ninth form). It takes a structure, allocates fresh memory (Allocate_OrDbgBreak), sets memory (MemSet_SdkRuntime via ProcessAndProcessAndProcess2184), and drives sub-operations (ProcessAndProcessAndProcess1888, ProcessAndProcessAndProcess880) based on the result. This is a frequently-called helper in the 0x82B85 process cluster. 111 lines. 4 callees. 20 callers.
[entrypoint.functions."0x82B850B8"]
name = "ProcessAndProcessAndProcess2199_82B850B8"

# Process a structure by freeing size-bucketed memory (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and five hundred form). It takes a structure, frees size-bucketed memory (Free_SizeBucketed), and drives a sub-operation (ProcessAndProcessAndProcess1824) based on the result. Part of the 0x8322A process cluster. 68 lines. 2 callees. 1 caller (ProcessAndProcessAndProcess2198).
[entrypoint.functions."0x8322A890"]
name = "ProcessAndProcessAndProcess2200_8322A890"

# Process a structure by decrementing and disposing (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and five hundred and first form). It takes a structure, frees size-bucketed memory (Free_SizeBucketed), decrements and disposes (ProcessAndDecrementDispose), releases a global ref (ReleaseGlobalRef), and drives a sub-operation (ProcessAndProcessAndProcess784) based on the result. Part of the 0x82A7C process cluster. 68 lines. 4 callees. 0 callers.
[entrypoint.functions."0x82A7C710"]
name = "ProcessAndProcessAndProcess2201_82A7C710"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and five hundred and second form). It takes a structure and checks its state, with no external sub-operations. This is a frequently-called helper in the 0x822A8 process cluster. 81 lines. 0 callees. 25 callers.
[entrypoint.functions."0x822A8EF8"]
name = "ProcessAndProcessAndProcess2202_822A8EF8"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and five hundred and third form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82C0C process cluster. 51 lines. 0 callees. 1 caller (ProcessAndProcessAndProcess1959).
[entrypoint.functions."0x82C0CF68"]
name = "ProcessAndProcessAndProcess2203_82C0CF68"

# Process a structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and five hundred and fourth form). It takes a structure and drives a sub-operation (sub_82C4D558). Part of the 0x82C13 process cluster. 24 lines. 1 callee. 0 callers.
[entrypoint.functions."0x82C132C8"]
name = "ProcessAndProcessAndProcess2204_82C132C8"

# Process a large structure by allocating fresh memory (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and five hundred and fifth form). It takes a structure, allocates fresh memory (Allocate_OrDbgBreak), and drives a sub-operation (sub_82C4DAA8) based on the result. Part of the 0x82C13 process cluster. 104 lines. 2 callees. 1 caller (ProcessAndProcessAndProcess2206).
[entrypoint.functions."0x82C137F8"]
name = "ProcessAndProcessAndProcess2205_82C137F8"

# Process a large structure by allocating fresh memory (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and five hundred and sixth form). It takes a structure, allocates fresh memory (Allocate_OrDbgBreak), and drives sub-operations (ProcessAndProcessAndProcess2205, sub_82C4D040) based on the result. Part of the 0x82C13 process cluster. 193 lines. 3 callees. 0 callers.
[entrypoint.functions."0x82C13928"]
name = "ProcessAndProcessAndProcess2206_82C13928"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and five hundred and seventh form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82C29 process cluster. 8 lines. 0 callees. 0 callers.
[entrypoint.functions."0x82C29300"]
name = "ProcessAndProcessAndProcess2207_82C29300"

# Process a structure by allocating fresh memory (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and five hundred and eighth form). It takes a structure and allocates fresh memory (Allocate_OrDbgBreak). This is a frequently-called helper in the 0x82C42 process cluster. 83 lines. 1 callee. 45 callers.
[entrypoint.functions."0x82C42F48"]
name = "ProcessAndProcessAndProcess2208_82C42F48"

# Process a structure by calling methods (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and five hundred and ninth form). It takes a structure, calls methods (ProcessAndCallMethods), and checks a method (ProcessAndCheckMethod). Part of the 0x82C4D process cluster. 56 lines. 2 callees. 1 caller (ProcessAndProcessAndProcess2206).
[entrypoint.functions."0x82C4D040"]
name = "ProcessAndProcessAndProcess2209_82C4D040"

# Process a structure by calling methods (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and five hundred and tenth form). It takes a structure, calls methods (ProcessAndCallMethods), and checks a method (ProcessAndCheckMethod). Part of the 0x82C4D process cluster. 67 lines. 2 callees. 1 caller (ProcessAndProcessAndProcess2204).
[entrypoint.functions."0x82C4D558"]
name = "ProcessAndProcessAndProcess2210_82C4D558"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and five hundred and eleventh form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82C4D process cluster. 22 lines. 0 callees. 1 caller (ProcessAndProcessAndProcess2205).
[entrypoint.functions."0x82C4DAA8"]
name = "ProcessAndProcessAndProcess2211_82C4DAA8"

# Process a large structure by allocating fresh memory (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and five hundred and twelfth form). It takes a structure, allocates fresh memory (Allocate_OrDbgBreak), and drives a sub-operation (ProcessAndProcessAndProcess2213) based on the result. Part of the 0x82C52 process cluster. 104 lines. 2 callees. 0 callers.
[entrypoint.functions."0x82C52388"]
name = "ProcessAndProcessAndProcess2212_82C52388"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and five hundred and thirteenth form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82C55 process cluster. 39 lines. 0 callees. 1 caller (ProcessAndProcessAndProcess2212).
[entrypoint.functions."0x82C55560"]
name = "ProcessAndProcessAndProcess2213_82C55560"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and five hundred and fourteenth form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82C55 process cluster. 8 lines. 0 callees. 0 callers.
[entrypoint.functions."0x82C559B0"]
name = "ProcessAndProcessAndProcess2214_82C559B0"

# Process a structure by emitting a glyph quad (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and five hundred and fifteenth form). It takes a structure and emits a glyph quad (UIFont_EmitGlyphQuad). Part of the 0x82C55 process cluster. 41 lines. 1 callee. 0 callers.
[entrypoint.functions."0x82C55CD0"]
name = "ProcessAndProcessAndProcess2215_82C55CD0"

# Process a structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and five hundred and sixteenth form). It takes a structure and drives a sub-operation (sub_82CDCEE8). This is a frequently-called helper in the 0x82CD3 process cluster. 59 lines. 1 callee. 4 callers.
[entrypoint.functions."0x82CD32A8"]
name = "ProcessAndProcessAndProcess2216_82CD32A8"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and five hundred and seventeenth form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82CD7 process cluster. 22 lines. 0 callees. 1 caller (ProcessAndProcessAndProcess2089).
[entrypoint.functions."0x82CD7D00"]
name = "ProcessAndProcessAndProcess2217_82CD7D00"

# Process a structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and five hundred and eighteenth form). It takes a structure and drives a sub-operation (sub_82CDFA10). This is a frequently-called helper in the 0x82CD9 process cluster. 29 lines. 1 callee. 2 callers (sub_82CE24B0 and sub_82CE3BE0).
[entrypoint.functions."0x82CD93F8"]
name = "ProcessAndProcessAndProcess2218_82CD93F8"

# Process a structure by wrapping sub-operations (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and five hundred and nineteenth form). It takes a structure, checks its state, and drives sub-operations (sub_82CE1560, ProcessAndProcessAndProcess2229) based on the result. Part of the 0x82CD9 process cluster. 28 lines. 2 callees. 0 callers.
[entrypoint.functions."0x82CD9618"]
name = "ProcessAndProcessAndProcess2219_82CD9618"

# Process a structure by wrapping sub-operations (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and five hundred and twentieth form). It takes a structure, checks its state, and drives sub-operations (sub_82CE30E0, sub_82CE4400) based on the result. Part of the 0x82CD9 process cluster. 28 lines. 2 callees. 0 callers.
[entrypoint.functions."0x82CD9640"]
name = "ProcessAndProcessAndProcess2220_82CD9640"

# Process a structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and five hundred and twenty-first form). It takes a structure and drives a sub-operation (ProcessAndProcessAndProcess2226). This is a frequently-called helper in the 0x82CD9 process cluster. 60 lines. 1 callee. 2 callers (sub_82CE24B0 and ProcessAndProcessAndProcess2230).
[entrypoint.functions."0x82CD9668"]
name = "ProcessAndProcessAndProcess2221_82CD9668"

# Process a large structure by copying memory (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and five hundred and twenty-second form). It takes a structure, copies memory (MemCpy_SdkRuntime), and drives sub-operations (ProcessAndProcessAndProcess1866, ProcessAndProcessAndProcess1953, ProcessAndProcessAndProcess2092) based on the result. Part of the 0x82CDC process cluster. 132 lines. 4 callees. 1 caller (ProcessAndProcessAndProcess2225).
[entrypoint.functions."0x82CDC4F8"]
name = "ProcessAndProcessAndProcess2222_82CDC4F8"

# Process a large structure by copying memory (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and five hundred and twenty-third form). It takes a structure, copies memory (MemCpy_SdkRuntime), and drives sub-operations (ProcessAndProcessAndProcess1863, ProcessAndProcessAndProcess1953, ProcessAndProcessAndProcess2093) based on the result. Part of the 0x82CDC process cluster. 129 lines. 4 callees. 1 caller (ProcessAndProcessAndProcess2225).
[entrypoint.functions."0x82CDC770"]
name = "ProcessAndProcessAndProcess2223_82CDC770"

# Process a large structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and five hundred and twenty-fourth form). It takes a structure and drives a sub-operation (ProcessAndProcessAndProcess2091). Part of the 0x82CDC process cluster. 147 lines. 1 callee. 1 caller (ProcessAndProcessAndProcess2225).
[entrypoint.functions."0x82CDCAE0"]
name = "ProcessAndProcessAndProcess2224_82CDCAE0"

# Process a large structure by wrapping sub-operations (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and five hundred and twenty-fifth form). It takes a structure, checks its state, and drives sub-operations (ProcessAndProcessAndProcess2080, ProcessAndProcessAndProcess2222, ProcessAndProcessAndProcess2223, ProcessAndProcessAndProcess2224) based on the result. Part of the 0x82CDC process cluster. 151 lines. 4 callees. 1 caller (ProcessAndProcessAndProcess2216).
[entrypoint.functions."0x82CDCEE8"]
name = "ProcessAndProcessAndProcess2225_82CDCEE8"

# Process a structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and five hundred and twenty-sixth form). It takes a structure and drives a sub-operation (sub_82CE6990). Part of the 0x82CDF process cluster. 40 lines. 1 callee. 1 caller (ProcessAndProcessAndProcess2221).
[entrypoint.functions."0x82CDF9B8"]
name = "ProcessAndProcessAndProcess2226_82CDF9B8"

# Process a structure by copying memory (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and five hundred and twenty-seventh form). It takes a structure, copies memory (MemCpy_SdkRuntime), and drives a sub-operation (sub_82CE68D0) based on the result. Part of the 0x82CDF process cluster. 68 lines. 2 callees. 1 caller (ProcessAndProcessAndProcess2218).
[entrypoint.functions."0x82CDFA10"]
name = "ProcessAndProcessAndProcess2227_82CDFA10"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and five hundred and twenty-eighth form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82CE3 process cluster. 18 lines. 0 callees. 1 caller (sub_82CE3758).
[entrypoint.functions."0x82CE3200"]
name = "ProcessAndProcessAndProcess2228_82CE3200"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and five hundred and twenty-ninth form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82CE3 process cluster. 16 lines. 0 callees. 1 caller (ProcessAndProcessAndProcess2219).
[entrypoint.functions."0x82CE3BB0"]
name = "ProcessAndProcessAndProcess2229_82CE3BB0"

# Process a large structure by setting memory (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and five hundred and thirtieth form). It takes a structure, sets memory (MemSet_SdkRuntime), and drives sub-operations (ProcessAndProcessAndProcess2218, ProcessAndProcessAndProcess2221) based on the result. Part of the 0x82CE3 process cluster. 151 lines. 3 callees. 1 caller (sub_82CE4400).
[entrypoint.functions."0x82CE3BE0"]
name = "ProcessAndProcessAndProcess2230_82CE3BE0"

# Process a structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and five hundred and thirty-first form). It takes a structure and drives a sub-operation (ProcessAndProcessAndProcess2230). Part of the 0x82CE4 process cluster. 69 lines. 1 callee. 1 caller (ProcessAndProcessAndProcess2220).
[entrypoint.functions."0x82CE4400"]
name = "ProcessAndProcessAndProcess2231_82CE4400"

# Process a large structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and five hundred and thirty-second form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82CE6 process cluster. 106 lines. 0 callees. 1 caller (ProcessAndProcessAndProcess2227).
[entrypoint.functions."0x82CE68D0"]
name = "ProcessAndProcessAndProcess2232_82CE68D0"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and five hundred and thirty-third form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82CE6 process cluster. 22 lines. 0 callees. 1 caller (ProcessAndProcessAndProcess2226).
[entrypoint.functions."0x82CE6990"]
name = "ProcessAndProcessAndProcess2233_82CE6990"

# Process a structure by resetting state (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and five hundred and thirty-fourth form). It takes a structure, resets state (ProcessAndResetState), and drives a sub-operation (sub_82B516A0) based on the result. This is a frequently-called helper in the 0x82B49 process cluster. 55 lines. 2 callees. 2 callers (ProcessAndProcessAndProcess2018 and sub_82B7E050).
[entrypoint.functions."0x82B49C60"]
name = "ProcessAndProcessAndProcess2234_82B49C60"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and five hundred and thirty-fifth form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82C08 process cluster. 10 lines. 0 callees. 0 callers.
[entrypoint.functions."0x82C08CD8"]
name = "ProcessAndProcessAndProcess2235_82C08CD8"

# Process a structure by processing a float-to-int conversion (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and five hundred and thirty-sixth form). It takes a structure and processes a float-to-int conversion (FloatToIntProcess). Part of the 0x82BAC process cluster. 96 lines. 1 callee. 1 caller (ProcessAndProcessAndProcess9).
[entrypoint.functions."0x82BACBE8"]
name = "ProcessAndProcessAndProcess2236_82BACBE8"

# Process a very large data structure by inserting a hash entry (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and five hundred and thirty-seventh form). It takes a structure, gets an indexed pointer (GetIndexedPointer), grows and zeros a buffer (GrowAndZeroBuffer), inserts a hash entry (InsertHashEntry), dispatches an indexed value (ProcessAndDispatchIndexedValue), performs a guarded dispatch (ProcessAndGuardedDispatch), stores an element (ProcessAndStoreElement2), and drives sub-operations (ProcessAndProcessAndProcess1956, ProcessApply) based on the result. Part of the 0x82BB8 process cluster. 237 lines. 8 callees. 0 callers.
[entrypoint.functions."0x82BB8A50"]
name = "ProcessAndProcessAndProcess2237_82BB8A50"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and five hundred and thirty-eighth form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82BFF process cluster. 12 lines. 0 callees. 1 caller (ProcessAndProcessAndProcess2240).
[entrypoint.functions."0x82BFFAD8"]
name = "ProcessAndProcessAndProcess2238_82BFFAD8"

# Process a structure by wrapping sub-operations (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and five hundred and thirty-ninth form). It takes a structure, checks its state, and drives sub-operations (ProcessAndProcessAndProcess1763, ProcessAndProcessAndProcess1814, ProcessAndProcessAndProcess2242) based on the result. Part of the 0x82C13 process cluster. 91 lines. 3 callees. 0 callers.
[entrypoint.functions."0x82C13228"]
name = "ProcessAndProcessAndProcess2239_82C13228"

# Process a structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and five hundred and fortieth form). It takes a structure and drives a sub-operation (ProcessAndProcessAndProcess2238). Part of the 0x82C13 process cluster. 72 lines. 1 callee. 1 caller (ProcessAndProcessAndProcess2241).
[entrypoint.functions."0x82C138A8"]
name = "ProcessAndProcessAndProcess2240_82C138A8"

# Process a structure by freeing memory (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and five hundred and forty-first form). It takes a structure, frees memory (Free_Thunk), and drives a sub-operation (ProcessAndProcessAndProcess2240) based on the result. Part of the 0x82C14 process cluster. 50 lines. 2 callees. 0 callers.
[entrypoint.functions."0x82C141C0"]
name = "ProcessAndProcessAndProcess2241_82C141C0"

# Process a structure by calling methods (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and five hundred and forty-second form). It takes a structure, calls methods (ProcessAndCallMethods), and checks a method (ProcessAndCheckMethod). Part of the 0x82C4D process cluster. 56 lines. 2 callees. 1 caller (ProcessAndProcessAndProcess2239).
[entrypoint.functions."0x82C4D430"]
name = "ProcessAndProcessAndProcess2242_82C4D430"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and five hundred and forty-third form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82CDC process cluster. 12 lines. 0 callees. 0 callers.
[entrypoint.functions."0x82CDC740"]
name = "ProcessAndProcessAndProcess2243_82CDC740"

# Process a large structure by incrementing a counter (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and five hundred and forty-fourth form). It takes a structure, increments a counter (ProcessAndIncrementCounter), releases a global ref (ReleaseGlobalRef), and drives sub-operations (ProcessAndProcessAndProcess1871, ProcessAndProcessAndProcess2182, sub_827DFA80) based on the result. This is a frequently-called helper in the 0x827DF process cluster. 128 lines. 5 callees. 2 callers (sub_827DF2C0 and sub_827DF7F0).
[entrypoint.functions."0x827DF8D0"]
name = "ProcessAndProcessAndProcess2244_827DF8D0"

# Process a structure by freeing size-bucketed memory (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and five hundred and forty-fifth form). It takes a structure, frees size-bucketed memory (Free_SizeBucketed), constructs and processes (ProcessAndConstructAndProcess25), and releases a global ref (ReleaseGlobalRef). Part of the 0x827DF process cluster. 45 lines. 3 callees. 0 callers.
[entrypoint.functions."0x827DFE18"]
name = "ProcessAndProcessAndProcess2245_827DFE18"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and five hundred and forty-sixth form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82C4C process cluster. 19 lines. 0 callees. 0 callers.
[entrypoint.functions."0x82C4C568"]
name = "ProcessAndProcessAndProcess2246_82C4C568"

# Process a structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and five hundred and forty-seventh form). It takes a structure and drives a sub-operation (ProcessAndProcessAndProcess1817). Part of the 0x82C52 process cluster. 70 lines. 1 callee. 0 callers.
[entrypoint.functions."0x82C52438"]
name = "ProcessAndProcessAndProcess2247_82C52438"

# Process a structure by freeing memory (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and five hundred and forty-eighth form). It takes a structure and frees memory (Free_Thunk). Part of the 0x82C55 process cluster. 45 lines. 1 callee. 0 callers.
[entrypoint.functions."0x82C553E0"]
name = "ProcessAndProcessAndProcess2248_82C553E0"

# Process a very large data structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and five hundred and forty-ninth form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82CE5 process cluster. 219 lines. 0 callees. 0 callers.
[entrypoint.functions."0x82CE5848"]
name = "ProcessAndProcessAndProcess2249_82CE5848"

# Process a structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and five hundred and fiftieth form). It takes a structure and drives a sub-operation (ProcessAndProcessAndProcess275). This is a frequently-called helper in the 0x825BB process cluster. 85 lines. 1 callee. 4 callers.
[entrypoint.functions."0x825BBEE8"]
name = "ProcessAndProcessAndProcess2250_825BBEE8"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and five hundred and fifty-first form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82C26 process cluster. 10 lines. 0 callees. 0 callers.
[entrypoint.functions."0x82C263C0"]
name = "ProcessAndProcessAndProcess2251_82C263C0"

# Process a large structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and five hundred and fifty-second form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82CD2 process cluster. 153 lines. 0 callees. 1 caller (ProcessAndProcessAndProcess2086).
[entrypoint.functions."0x82CD2660"]
name = "ProcessAndProcessAndProcess2252_82CD2660"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and five hundred and fifty-third form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82CDC process cluster. 18 lines. 0 callees. 0 callers.
[entrypoint.functions."0x82CDC750"]
name = "ProcessAndProcessAndProcess2253_82CDC750"

# Process a structure by wrapping sub-operations (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and five hundred and fifty-fourth form). It takes a structure, checks its state, and drives sub-operations (sub_82170010, sub_82CA46E8) based on the result. This is a frequently-called helper in the 0x82172 process cluster. 68 lines. 2 callees. 87 callers.
[entrypoint.functions."0x82172EE8"]
name = "ProcessAndProcessAndProcess2254_82172EE8"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and five hundred and fifty-fifth form). It takes a structure and checks its state, with no external sub-operations. This is a frequently-called helper in the 0x824AF process cluster. 67 lines. 0 callees. 19 callers.
[entrypoint.functions."0x824AF0E8"]
name = "ProcessAndProcessAndProcess2255_824AF0E8"

# Process a large structure by decrementing and disposing (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and five hundred and fifty-sixth form). It takes a structure, decrements and disposes (ProcessAndDecrementDispose), increments a counter (ProcessAndIncrementCounter), releases a global ref (ReleaseGlobalRef), and drives a sub-operation (ProcessAndProcessAndProcess1837) based on the result. This is a frequently-called helper in the 0x827DE process cluster. 114 lines. 4 callees. 3 callers (ProcessAndProcessAndProcess814, sub_82818728, sub_82818988).
[entrypoint.functions."0x827DEBF8"]
name = "ProcessAndProcessAndProcess2256_827DEBF8"

# Process a structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and five hundred and fifty-seventh form). It takes a structure and drives a sub-operation (ProcessAndProcessAndProcess1764). This is a frequently-called helper in the 0x82858 process cluster. 50 lines. 1 callee. 4 callers (ProcessAndProcessAndProcess2258, sub_82AFDD58, sub_82B03E50, sub_82B128E8).
[entrypoint.functions."0x828589D8"]
name = "ProcessAndProcessAndProcess2257_828589D8"

# Process a large structure by validating (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and five hundred and fifty-eighth form). It takes a structure, validates (ProcessAndValidateAndProcess3), and drives sub-operations (sub_825D93B0, ProcessAndProcessAndProcess2257) based on the result. This is a frequently-called helper in the 0x82859 process cluster. 137 lines. 3 callees. 24 callers.
[entrypoint.functions."0x82859120"]
name = "ProcessAndProcessAndProcess2258_82859120"

# Process a structure by allocating fresh memory (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and five hundred and fifty-ninth form). It takes a structure, allocates fresh memory (Allocate_OrDbgBreak), and drives a sub-operation (sub_822F1F00) based on the result. This is a frequently-called helper in the 0x82B6D process cluster. 68 lines. 2 callees. 4 callers.
[entrypoint.functions."0x82B6DDF8"]
name = "ProcessAndProcessAndProcess2259_82B6DDF8"

# Process a very large data structure by wrapping sub-operations (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and five hundred and sixtieth form). It takes a structure, checks its state, and drives sub-operations (ProcessAndProcessAndProcess1700, ProcessAndProcessAndProcess686, sub_82AAE4D8, sub_82AAEC58, sub_82AAED38) based on the result. Part of the 0x82AAF process cluster. 260 lines. 5 callees. 1 caller (ProcessAndProcessAndProcess1179).
[entrypoint.functions."0x82AAF7C8"]
name = "ProcessAndProcessAndProcess2260_82AAF7C8"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and five hundred and sixty-first form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82C1F process cluster. 10 lines. 0 callees. 0 callers.
[entrypoint.functions."0x82C1F268"]
name = "ProcessAndProcessAndProcess2261_82C1F268"

# Process a structure by allocating fresh memory (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and five hundred and sixty-second form). It takes a structure, allocates fresh memory (Allocate_OrDbgBreak), and drives a sub-operation (ProcessAndProcessAndProcess1996) based on the result. This is a frequently-called helper in the 0x82C45 process cluster. 80 lines. 2 callees. 2 callers (ProcessAndProcessAndProcess1269 and ProcessAndProcessAndProcess1651).
[entrypoint.functions."0x82C452D0"]
name = "ProcessAndProcessAndProcess2262_82C452D0"

# Process a structure by destructing a refcounted object (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and five hundred and sixty-third form). It takes a structure, destructs a refcounted object (DestructRefCounted), and frees size-bucketed memory (Free_SizeBucketed). This is a frequently-called helper in the 0x82357 process cluster. 56 lines. 2 callees. 2 callers (sub_82357550 and sub_82358590).
[entrypoint.functions."0x82357750"]
name = "ProcessAndProcessAndProcess2263_82357750"

# Process a large structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and five hundred and sixty-fourth form). It takes a structure and drives a sub-operation (sub_824618B0). This is a frequently-called helper in the 0x82461 process cluster. 103 lines. 1 callee. 14 callers.
[entrypoint.functions."0x82461638"]
name = "ProcessAndProcessAndProcess2264_82461638"

# Process a large structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and five hundred and sixty-fifth form). It takes a structure and drives a sub-operation (ProcessAndProcessAndProcess1681). This is a frequently-called helper in the 0x82496 process cluster. 110 lines. 1 callee. 26 callers.
[entrypoint.functions."0x82496EF0"]
name = "ProcessAndProcessAndProcess2265_82496EF0"

# Process a structure by freeing size-bucketed memory (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and five hundred and sixty-sixth form). It takes a structure, frees size-bucketed memory (Free_SizeBucketed), and drives a sub-operation (ProcessAndProcessAndProcess2258) based on the result. This is a frequently-called helper in the 0x82858 process cluster. 61 lines. 2 callees. 20 callers.
[entrypoint.functions."0x82858F30"]
name = "ProcessAndProcessAndProcess2266_82858F30"

# Process a large structure by copy-assigning a refcounted object (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and five hundred and sixty-seventh form). It takes a structure, copy-assigns a refcounted object (CopyAssign_RefCounted), destructs a refcounted object (DestructRefCounted), and drives sub-operations (ProcessAndProcessAndProcess2012, ProcessAndProcessAndProcess2269) based on the result. Part of the 0x82AA7 process cluster. 181 lines. 4 callees. 1 caller (sub_82AA7208).
[entrypoint.functions."0x82AA79F0"]
name = "ProcessAndProcessAndProcess2267_82AA79F0"

# Process a large structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and five hundred and sixty-eighth form). It takes a structure and drives a sub-operation (ProcessAndProcessAndProcess2012). This is a frequently-called helper in the 0x82AA7 process cluster. 97 lines. 1 callee. 2 callers (sub_82AA7208 and sub_82AA7908).
[entrypoint.functions."0x82AA7B90"]
name = "ProcessAndProcessAndProcess2268_82AA7B90"

# Process a very large data structure by validating (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and five hundred and sixty-ninth form). It takes a structure, validates (ProcessAndValidateAndProcess3), processes a chain (ProcessChain2), and drives sub-operations (sub_82AA7F40, ProcessAndProcessAndProcess2270) based on the result. Part of the 0x82AA7 process cluster. 377 lines. 4 callees. 1 caller (ProcessAndProcessAndProcess2267).
[entrypoint.functions."0x82AA7C38"]
name = "ProcessAndProcessAndProcess2269_82AA7C38"

# Process a very large data structure by allocating fresh memory (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and five hundred and seventieth form). It takes a structure, allocates fresh memory (Allocate_OrDbgBreak), copy-assigns a refcounted object (CopyAssign_RefCounted), frees and processes (ProcessAndFreeAndProcess9), sets a string (SetString), and drives sub-operations (sub_822F1F00, sub_826C3EF0, sub_826C3FA8, sub_82AA8888, sub_82AA8908) based on the result. This is a frequently-called processor in the 0x82AA8 process cluster. 352 lines. 9 callees. 2 callers (ProcessAndProcessAndProcess2269 and sub_82AA7F40).
[entrypoint.functions."0x82AA81B0"]
name = "ProcessAndProcessAndProcess2270_82AA81B0"

# Process a structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and five hundred and seventy-first form). It takes a structure and drives a sub-operation (ProcessAndProcessAndProcess1042). Part of the 0x82C08 process cluster. 49 lines. 1 callee. 1 caller (UITextPrompt_RenderElement).
[entrypoint.functions."0x82C085C8"]
name = "ProcessAndProcessAndProcess2271_82C085C8"

# Process a structure by wrapping a sub-operation (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and five hundred and seventy-second form). It takes a structure and drives a sub-operation (ProcessAndProcessAndProcess2273). This is a frequently-called helper in the 0x82CD2 process cluster. 48 lines. 1 callee. 7 callers.
[entrypoint.functions."0x82CD2CF0"]
name = "ProcessAndProcessAndProcess2272_82CD2CF0"

# Process a structure by wrapping sub-operations (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and five hundred and seventy-third form). It takes a structure, checks its state, and drives sub-operations (ProcessAndProcessAndProcess1791, ProcessAndProcessAndProcess1997, sub_82CDCC08) based on the result. This is a frequently-called helper in the 0x82CDC process cluster. 95 lines. 3 callees. 2 callers (ProcessAndProcessAndProcess2272 and sub_82CDA9F8).
[entrypoint.functions."0x82CDCFE8"]
name = "ProcessAndProcessAndProcess2273_82CDCFE8"

# Process a large structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and five hundred and seventy-fourth form). It takes a structure and checks its state, with no external sub-operations. Part of the 0x82CE3 process cluster. 144 lines. 0 callees. 1 caller (sub_82CE3BD8).
[entrypoint.functions."0x82CE3890"]
name = "ProcessAndProcessAndProcess2274_82CE3890"

# Process a large structure by setting memory (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and five hundred and seventy-fifth form). It takes a structure and sets memory (MemSet_SdkRuntime). Part of the 0x82B92 process cluster. 135 lines. 1 callee. 1 caller (ProcessAndProcessAndProcess2276).
[entrypoint.functions."0x82B92EC0"]
name = "ProcessAndProcessAndProcess2275_82B92EC0"

# Process a structure by wrapping sub-operations (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and five hundred and seventy-sixth form). It takes a structure, checks its state, and drives sub-operations (ProcessAndProcessAndProcess652, ProcessAndProcessAndProcess2275) based on the result. This is a frequently-called helper in the 0x82B92 process cluster. 80 lines. 2 callees. 6 callers.
[entrypoint.functions."0x82B92FB0"]
name = "ProcessAndProcessAndProcess2276_82B92FB0"

# Process a very large data structure by allocating fresh memory (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and five hundred and seventy-seventh form). It takes a structure, allocates fresh memory (Allocate_OrDbgBreak), and drives sub-operations (ProcessAndProcessAndProcess1011, ProcessAndProcessAndProcess1698, ProcessAndProcessAndProcess1996, sub_82CD36B8, sub_82CD3EE8) based on the result. Part of the 0x82C45 process cluster. 201 lines. 6 callees. 1 caller (ProcessAndProcessAndProcess1139).
[entrypoint.functions."0x82C45350"]
name = "ProcessAndProcessAndProcess2277_82C45350"

# Process a structure (seven hundred and one hundred and one hundred and one hundred and one hundred and three hundred and two hundred and five hundred and seventy-eighth form). It takes a structure and checks its state, with no external sub-operations. This is a frequently-called helper in the 0x82CD2 process cluster. 71 lines. 0 callees. 2 callers (ProcessAndProcessAndProcess759 and sub_832B66A0).
[entrypoint.functions."0x82CD2FD0"]
name = "ProcessAndProcessAndProcess2278_82CD2FD0"

# Process a structure by checking its state and driving sub-operations (two thousand two hundred and eightieth form). It takes a structure (a: field at a->8), a value (b), a value (c), and a value (d); it checks the structure's state, and drives sub-operations (MmGetPhysicalAddress and sub_82CD3F20) based on the result. This is the "process this data" routine (two thousand two hundred and eightieth form), part of the 0x82CD3 process cluster. 123 lines. 2 callees. 2 callers (ProcessAndProcessAndProcess2277 and sub_82CE1588).
[entrypoint.functions."0x82CD36B8"]
name = "ProcessAndProcessAndProcess2279_82CD36B8"

# Process a value by checking its state and driving many sub-operations (two thousand two hundred and eightieth form). It takes a value (a), a float (b), and a value (c); it checks the value's state, and drives many sub-operations (ProcessAndProcessAndProcess504, KeAcquireSpinLockAtRaisedIrql, KeRaiseIrqlToDpcLevel, KeReleaseSpinLockFromRaisedIrql, and KfLowerIrql) based on the result. This is the "process this data" routine (two thousand two hundred and eightieth form), part of the 0x82CE3 process cluster. 165 lines. 5 callees. 1 caller (sub_82CE3788).
[entrypoint.functions."0x82CE3528"]
name = "ProcessAndProcessAndProcess2280_82CE3528"

# Process a structure by initializing it from a source structure (two thousand two hundred and eighty-second form). It takes a structure (a: fields at a->1, a->12, a->20, a->24, a->8, and a->9) and a value (b); it initializes the structure's fields from the source. This is the "process this data" routine (two thousand two hundred and eighty-second form), part of the 0x82CDD process cluster. 72 lines. 0 callees. 2 callers (sub_82CDD4C8 and sub_82CDD320).
[entrypoint.functions."0x82CDD0F8"]
name = "ProcessAndProcessAndProcess2281_82CDD0F8"

# Process a value by checking its state and driving many sub-operations (two thousand two hundred and eighty-third form). It takes a value (a), a value (b), and a value (c); it checks the value's state, and drives many sub-operations (ProcessAndProcessAndProcess1871, ProcessAndValidateAndProcess3, ProcessChain2, and more) based on the result. This is the "process this data" routine (two thousand two hundred and eighty-third form), part of the 0x827DF process cluster. 376 lines. 5 callees. 1 caller (ProcessAndProcessAndProcess2244).
[entrypoint.functions."0x827DFA80"]
name = "ProcessAndProcessAndProcess2282_827DFA80"

# Process a value by checking its state and driving many sub-operations (two thousand two hundred and eighty-fourth form). It takes a value (a), a value (b), a value (c), and a value (d); it checks the value's state, and drives many sub-operations (SetString, Allocate_OrDbgBreak, ProcessAndFreeAndProcess9, ProcessAndIncrementCounter, and more) based on the result. This is the "process this data" routine (two thousand two hundred and eighty-fourth form), part of the 0x827E0 process cluster. 350 lines. 9 callees. 2 callers (sub_827DFED8 and ProcessAndProcessAndProcess2282).
[entrypoint.functions."0x827E0168"]
name = "ProcessAndProcessAndProcess2283_827E0168"

# Process a value by checking its state and driving sub-operations (two thousand two hundred and eighty-fifth form). It takes six values (a-f); it checks the value's state, and drives sub-operations (GetGlobalField19764 and sub_82C144E8) based on the result. This is the "process this data" routine (two thousand two hundred and eighty-fifth form), part of the 0x82BFF process cluster. 53 lines. 2 callees. 1 caller (sub_82C0AA60).
[entrypoint.functions."0x82BFFE50"]
name = "ProcessAndProcessAndProcess2284_82BFFE50"

# Process a value by checking its state and driving many sub-operations (two thousand two hundred and eighty-sixth form). It takes a value (a) and a value (b); it checks the value's state, and drives many sub-operations (ProcessAndProcessAndProcess1042, ConstructRefCounted, DestructRefCounted, Free_Thunk, ProcessAndAllocateAndDispatch, and more) based on the result. This is the "process this data" routine (two thousand two hundred and eighty-sixth form), part of the 0x82C04 process cluster. 184 lines. 6 callees. 1 caller (sub_82C04A38).
[entrypoint.functions."0x82C04900"]
name = "ProcessAndProcessAndProcess2285_82C04900"

# Process a value by checking its state and driving sub-operations (two thousand two hundred and eighty-seventh form). It takes a value (a); it checks the value's state, and drives sub-operations (ProcessAndProcessAndProcess1042 and ProcessAndProcessAndProcess2285) based on the result. This is the "process this data" routine (two thousand two hundred and eighty-seventh form), part of the 0x82C04 process cluster. 50 lines. 2 callees. 2 callers (sub_82C38F78 and sub_82C4F240).
[entrypoint.functions."0x82C04A38"]
name = "ProcessAndProcessAndProcess2286_82C04A38"

# Process a structure by checking its state and driving many sub-operations (two thousand two hundred and eighty-eighth form). It takes a structure (a: fields at a->56, a->0, and a->28), a value (b), and a value (c); it checks the structure's state, and drives many sub-operations (CriticalSection_EnterOrTry, FindTreeLeaf, ProcessAndLeaveCritical, ProcessAndProcessAndProcess1942, and ProcessAndSpliceAndProcess4) based on the result. This is the "process this data" routine (two thousand two hundred and eighty-eighth form), part of the 0x82C08 process cluster. 175 lines. 5 callees. 1 caller (sub_82C08B00).
[entrypoint.functions."0x82C089C8"]
name = "ProcessAndProcessAndProcess2287_82C089C8"

# Process a value by checking its state and driving sub-operations (two thousand two hundred and eighty-ninth form). It takes a value (a); it checks the value's state, and drives sub-operations (ProcessAndProcessAndProcess2287) based on the result. This is the "process this data" routine (two thousand two hundred and eighty-ninth form), part of the 0x82C08 process cluster. 23 lines. 1 callee. 5 callers (many from the 0x82C4* clusters).
[entrypoint.functions."0x82C08B00"]
name = "ProcessAndProcessAndProcess2288_82C08B00"

# Initialize a node structure with default values. It takes a value (a: the node to initialize); it sets the node's fields to 0, 0, 0, -1, 1, and 0. This is the node initializer, part of the 0x82C08 node cluster. 24 lines. 0 callees. 3 callers (sub_82C3AE60, sub_82C2F748, and sub_82C4F750).
[entrypoint.functions."0x82C08CA0"]
name = "InitializeNode_82C08CA0"

# Make a virtual call through a vtable. It takes a structure (a: fields at a->16, a->0, and a->8); it reads the vtable from a->16->0->8 and makes an indirect call through it. This is the virtual-call thunk, part of the 0x82C09 virtual-call cluster. 15 lines. 0 callees (indirect call). 0 callers.
[entrypoint.functions."0x82C090C8"]
name = "VirtualCallThunk_82C090C8"

# Process a structure by checking its state and driving many sub-operations (two thousand two hundred and ninetieth form). It takes a structure (a: field at a->157), a value (b), a value (c), and a value (d); it checks the structure's state, and drives many sub-operations (Allocate_OrDbgBreak, ProcessAndVcallAndProcess10, and more) based on the result. This is the "process this data" routine (two thousand two hundred and ninetieth form), part of the 0x82C09 process cluster. 95 lines. 3 callees. 0 callers.
[entrypoint.functions."0x82C097D0"]
name = "ProcessAndProcessAndProcess2289_82C097D0"

# Process a structure by checking its state and driving many sub-operations (two thousand two hundred and ninety-first form). It takes a structure (a: fields at a->4 and a->12) and a value (b); it checks the structure's state, and drives many sub-operations (AllocateChecked3, CheckedCopy, Free_Thunk, and more) based on the result. This is the "process this data" routine (two thousand two hundred and ninety-first form), part of the 0x82C09 process cluster. 140 lines. 4 callees. 1 caller (sub_82C0AA60).
[entrypoint.functions."0x82C09F00"]
name = "ProcessAndProcessAndProcess2290_82C09F00"

# Initialize a large object structure with default values. It takes a value (a: the object to initialize); it sets many of the object's fields to zero and specific constants. This is the object initializer, part of the 0x82C0A object cluster. 94 lines. 0 callees. 1 caller (sub_82C0AC38).
[entrypoint.functions."0x82C0A368"]
name = "InitializeObject_82C0A368"

# Process a structure by checking its state and driving sub-operations (two thousand two hundred and ninety-second form). It takes a structure (a: fields at a->72 and a->68), a value (b), and a value (c); it checks the structure's state, and drives sub-operations (ProcessAndProcessAndProcess1917 and ProcessAndStoreAndProcess2) based on the result. This is the "process this data" routine (two thousand two hundred and ninety-second form), part of the 0x82C0A process cluster. 101 lines. 2 callees. 1 caller (sub_82C0A828).
[entrypoint.functions."0x82C0A4F0"]
name = "ProcessAndProcessAndProcess2291_82C0A4F0"

# Process a structure by checking its state and driving sub-operations (two thousand two hundred and ninety-third form). It takes a structure (a: fields at a->68, a->8, a->72, and a->4), a value (b), a value (c), a value (d), and a value (e); it checks the structure's state, and drives sub-operations (ProcessAndProcessAndProcess2291) based on the result. This is the "process this data" routine (two thousand two hundred and ninety-third form), part of the 0x82C0A process cluster. 122 lines. 1 callee. 2 callers (sub_82C0A988 and sub_82C0AA60).
[entrypoint.functions."0x82C0A828"]
name = "ProcessAndProcessAndProcess2292_82C0A828"

# Process a structure by checking its state and driving many sub-operations (two thousand two hundred and ninety-fourth form). It takes a structure (a: field at a->156) and a structure (b: field at b->8); it checks the structure's state, and drives many sub-operations (ProcessAndProcessAndProcess1042, ProcessAndProcessAndProcess2069, ProcessAndVcallAndProcess10, ProcessAndProcessAndProcess2284, ProcessAndProcessAndProcess2290, ProcessAndProcessAndProcess2292, and more) based on the result. This is the "process this data" routine (two thousand two hundred and ninety-fourth form), part of the 0x82C0A process cluster. 264 lines. 6 callees. 1 caller (sub_82C0AC38).
[entrypoint.functions."0x82C0AA60"]
name = "ProcessAndProcessAndProcess2293_82C0AA60"

# Process a structure by checking its state and driving many sub-operations (two thousand two hundred and ninety-fifth form). It takes a structure (a: fields at a->0 and a->4) and a value (b); it checks the structure's state, and drives many sub-operations (Allocate_OrDbgBreak, InitializeObject, ProcessAndProcessAndProcess2293, and more) based on the result. This is the "process this data" routine (two thousand two hundred and ninety-fifth form), part of the 0x82C0A process cluster. 96 lines. 3 callees. 1 caller (sub_82C0ADF8).
[entrypoint.functions."0x82C0AC38"]
name = "ProcessAndProcessAndProcess2294_82C0AC38"

# Process a value by checking its state and driving sub-operations (two thousand two hundred and ninety-sixth form). It takes a value (a) and a value (b); it checks the value's state, and drives sub-operations (ProcessAndProcessAndProcess2294) based on the result. This is the "process this data" routine (two thousand two hundred and ninety-sixth form), part of the 0x82C0A process cluster. 38 lines. 1 callee. 3 callers (sub_82C2F748, sub_82C4ECB0, and sub_82C3AE60).
[entrypoint.functions."0x82C0ADF8"]
name = "ProcessAndProcessAndProcess2295_82C0ADF8"

# Process a value by checking its state and driving many sub-operations (two thousand two hundred and ninety-seventh form). It takes a value (a); it checks the value's state, and drives many sub-operations (ProcessToLower, FindChainValue, DestructRefCounted, ProcessVcall3, and StrCmp_Signed) based on the result. This is the "process this data" routine (two thousand two hundred and ninety-seventh form), part of the 0x82C0A process cluster. 168 lines. 5 callees. 1 caller (ProcessAndProcessAndProcess2285).
[entrypoint.functions."0x82C0AF58"]
name = "ProcessAndProcessAndProcess2296_82C0AF58"

# Process a value by checking its state and driving sub-operations (two thousand two hundred and ninety-eighth form). It takes a value (a) and a value (b); it checks the value's state, and drives sub-operations (CopyAssign_RefCounted) based on the result. This is the "process this data" routine (two thousand two hundred and ninety-eighth form), part of the 0x82C0B process cluster. 34 lines. 1 callee. 2 callers (sub_82C4F240 and sub_82C2F748).
[entrypoint.functions."0x82C0B360"]
name = "ProcessAndProcessAndProcess2297_82C0B360"

# Process a value by checking its state and driving sub-operations (two thousand two hundred and ninety-ninth form). It takes a value (a) and a value (b); it checks the value's state, and drives sub-operations (CopyAssign_RefCounted) based on the result. This is the "process this data" routine (two thousand two hundred and ninety-ninth form), part of the 0x82C0B process cluster. 34 lines. 1 callee. 2 callers (sub_82C2F748 and sub_82C4F240).
[entrypoint.functions."0x82C0B3A0"]
name = "ProcessAndProcessAndProcess2298_82C0B3A0"

# Set a field to a value if the field is currently empty. It takes a structure (a: fields at a->12 and a->164) and a value (b); it checks if a->12 is non-zero and a->164 is zero, and if so, sets a->164 to b. This is the "set this field if empty" routine, part of the 0x82C0C field cluster. 20 lines. 0 callees. 0 callers.
[entrypoint.functions."0x82C0C9A8"]
name = "SetFieldIfEmpty_82C0C9A8"

# Process a value by copying fields from a source structure (two thousand three hundredth form). It takes a value (a) and a structure (b: fields at b->52 and b->56); it copies fields from the source structure, and makes an indirect call if b->56 is non-zero. This is the "process this data" routine (two thousand three hundredth form), part of the 0x82C0D process cluster. 51 lines. 0 callees (indirect call). 2 callers (sub_82C4F240 and sub_82C2F748).
[entrypoint.functions."0x82C0D2A0"]
name = "ProcessAndProcessAndProcess2299_82C0D2A0"

# Process a value by copying fields from a source structure (two thousand three hundred and first form). It takes a value (a) and a structure (b: field at b->12); it copies fields from the source structure, and makes an indirect call if b->12 is non-zero. This is the "process this data" routine (two thousand three hundred and first form), part of the 0x82C0E process cluster. 64 lines. 0 callees (indirect call). 0 callers.
[entrypoint.functions."0x82C0E210"]
name = "ProcessAndProcessAndProcess2300_82C0E210"

# Process a structure by storing fields and driving sub-operations (two thousand three hundred and second form). It takes a structure (a: fields at a->36, a->40, a->44, and a->72), a float (b), and a value (c); it stores fields on the structure, and drives sub-operations (sub_82C4D638) based on the result. This is the "process this data" routine (two thousand three hundred and second form), part of the 0x82C13 process cluster. 24 lines. 1 callee. 0 callers.
[entrypoint.functions."0x82C133B0"]
name = "ProcessAndProcessAndProcess2301_82C133B0"

# Process a structure by modifying its fields (two thousand three hundred and third form). It takes a structure (a: fields at a->8, a->0, and a->4) and a structure (b: fields at b->8 and b->16); it modifies the structure's fields. This is the "process this data" routine (two thousand three hundred and third form), part of the 0x82C13 process cluster. 65 lines. 0 callees. 1 caller (sub_82C144E8).
[entrypoint.functions."0x82C13538"]
name = "ProcessAndProcessAndProcess2302_82C13538"

# Process a structure by checking its state and driving many sub-operations (two thousand three hundred and fourth form). It takes a structure (a: fields at a->0, a->4, a->8, a->12, a->16, a->20, and a->60); it checks the structure's state, and drives many sub-operations (AtomicIncrement3 and Construct20ByteNode) based on the result. This is the "process this data" routine (two thousand three hundred and fourth form), part of the 0x82C14 process cluster. 236 lines. 2 callees. 1 caller (sub_82C144E8).
[entrypoint.functions."0x82C14210"]
name = "ProcessAndProcessAndProcess2303_82C14210"

# Process a value by checking its state and driving many sub-operations (two thousand three hundred and fifth form). It takes seven values (a-g); it checks the value's state, and drives many sub-operations (Allocate_OrDbgBreak, ProcessAndProcessAndProcess2302, ProcessAndProcessAndProcess2303, and more) based on the result. This is the "process this data" routine (two thousand three hundred and fifth form), part of the 0x82C14 process cluster. 240 lines. 7 callees. 1 caller (ProcessAndProcessAndProcess2284).
[entrypoint.functions."0x82C144E8"]
name = "ProcessAndProcessAndProcess2304_82C144E8"

# Return a global pointer. It takes no arguments; it returns a pointer to a global object. This is the global-pointer getter, part of the 0x82C44 global cluster. 10 lines. 0 callees. 0 callers.
[entrypoint.functions."0x82C440B8"]
name = "GetGlobalPointer_82C440B8"

# Process a value by checking its state and driving sub-operations (two thousand three hundred and sixth form). It takes a value (a) and a value (b); it checks the value's state, and drives sub-operations (ProcessAndProcessAndProcess2055) based on the result. This is the "process this data" routine (two thousand three hundred and sixth form), part of the 0x82C44 process cluster. 44 lines. 1 callee. 0 callers.
[entrypoint.functions."0x82C441F0"]
name = "ProcessAndProcessAndProcess2305_82C441F0"

# Process a value by checking its state and driving sub-operations (two thousand three hundred and seventh form). It takes a value (a) and a value (b); it checks the value's state, and drives sub-operations (sub_82C50AA8) based on the result. This is the "process this data" routine (two thousand three hundred and seventh form), part of the 0x82C44 process cluster. 44 lines. 1 callee. 0 callers.
[entrypoint.functions."0x82C44230"]
name = "ProcessAndProcessAndProcess2306_82C44230"

# Process a structure by checking its state and driving many sub-operations (two thousand three hundred and eighth form). It takes a structure (a: field at a->4) and a value (b); it checks the structure's state, and drives many sub-operations (Construct104ByteObject, AssignRefCounted, OneTimeInitAndVcall, ProcessAndProcessAndProcess1832, ProcessAndProcessAndProcess1895, ProcessAndQueryAllocate, and more) based on the result. This is the "process this data" routine (two thousand three hundred and eighth form), part of the 0x82C44 process cluster. 621 lines. 7 callees. 1 caller (UITextPrompt_RenderElement).
[entrypoint.functions."0x82C44688"]
name = "ProcessAndProcessAndProcess2307_82C44688"

# Process a structure by checking its state and driving many sub-operations (two thousand three hundred and ninth form). It takes a structure (a: fields at a->0, a->4, a->8, a->120, a->124, and a->128), a value (b), and a value (c); it checks the structure's state, and drives many sub-operations (Allocate_OrDbgBreak, sub_82C50DB0, and more) based on the result. This is the "process this data" routine (two thousand three hundred and ninth form), part of the 0x82C44 process cluster. 138 lines. 2 callees. 1 caller (sub_82C45128).
[entrypoint.functions."0x82C44E68"]
name = "ProcessAndProcessAndProcess2308_82C44E68"

# Process a value by checking its state and driving many sub-operations (two thousand three hundred and tenth form). It takes a value (a) and a value (b); it checks the value's state, and drives many sub-operations (Allocate_OrDbgBreak, ConstructAndInsertNode, OneTimeInitAndVcall, UITextPrompt_RenderElement, ProcessAndProcessAndProcess2308, and more) based on the result. This is the "process this data" routine (two thousand three hundred and tenth form), part of the 0x82C45 process cluster. 178 lines. 6 callees. 0 callers.
[entrypoint.functions."0x82C45128"]
name = "ProcessAndProcessAndProcess2309_82C45128"

# Return a global pointer (second form). It takes no arguments; it returns a pointer to a global object. This is the global-pointer getter (second form), part of the 0x82C45 global cluster. 10 lines. 0 callees. 0 callers.
[entrypoint.functions."0x82C45D10"]
name = "GetGlobalPointer2_82C45D10"

# Process a structure by checking its state and driving sub-operations (two thousand three hundred and eleventh form). It takes a structure (a: fields at a->0, a->4, a->8, a->12, a->16, a->20, and a->24); it checks the structure's state, and drives sub-operations (ProcessAndProcessAndProcess964) based on the result. This is the "process this data" routine (two thousand three hundred and eleventh form), part of the 0x82C4C process cluster. 51 lines. 1 callee. 0 callers.
[entrypoint.functions."0x82C4C360"]
name = "ProcessAndProcessAndProcess2310_82C4C360"

# Make a virtual call through a vtable (second form). It takes a structure (a: fields at a->4, a->8, and a->12); it reads the vtable from a->4->52->0 and makes an indirect call through it. This is the virtual-call thunk (second form), part of the 0x82C4C virtual-call cluster. 24 lines. 0 callees (indirect call). 0 callers.
[entrypoint.functions."0x82C4C610"]
name = "VirtualCallThunk2_82C4C610"

# Process a value by checking its state and driving many sub-operations (two thousand three hundred and twelfth form). It takes six values (a-f); it checks the value's state, and drives many sub-operations (ProcessAndCallMethods, ProcessAndCheckMethod, and more) based on the result. This is the "process this data" routine (two thousand three hundred and twelfth form), part of the 0x82C4D process cluster. 72 lines. 2 callees. 1 caller (ProcessAndProcessAndProcess2304).
[entrypoint.functions."0x82C4D198"]
name = "ProcessAndProcessAndProcess2311_82C4D198"

# Process a value by checking its state and driving many sub-operations (two thousand three hundred and thirteenth form). It takes a value (a), a float (b), and a value (c); it checks the value's state, and drives many sub-operations (ProcessAndCallMethods, ProcessAndCheckMethod, and more) based on the result. This is the "process this data" routine (two thousand three hundred and thirteenth form), part of the 0x82C4D process cluster. 67 lines. 2 callees. 1 caller (ProcessAndProcessAndProcess2301).
[entrypoint.functions."0x82C4D638"]
name = "ProcessAndProcessAndProcess2312_82C4D638"

# Initialize a structure with a float constant and zero values. It takes a value (a: the structure to initialize); it sets the structure's fields to a float constant and zeros. This is the structure initializer, part of the 0x82C4D structure cluster. 49 lines. 0 callees. 1 caller (ProcessAndProcessAndProcess2304).
[entrypoint.functions."0x82C4DA30"]
name = "InitializeStruct_82C4DA30"

# Store a vector at a fixed offset. It takes a value (a) and a vector (b); it stores the vector at a + 112. This is the vector-store routine, part of the 0x82C4D vector cluster. 12 lines. 0 callees. 0 callers.
[entrypoint.functions."0x82C4DB80"]
name = "StoreVectorAtOffset_82C4DB80"

# Process a structure by checking its state and driving sub-operations (two thousand three hundred and fourteenth form). It takes a structure (a: field at a->16); it checks the structure's state, and drives sub-operations (indirect calls) based on the result. This is the "process this data" routine (two thousand three hundred and fourteenth form), part of the 0x82C4D process cluster. 76 lines. 0 callees (indirect calls). 0 callers.
[entrypoint.functions."0x82C4DC98"]
name = "ProcessAndProcessAndProcess2313_82C4DC98"

# Process a structure by checking its state and driving sub-operations (two thousand three hundred and fifteenth form). It takes a structure (a: fields at a->52, a->68, a->64, a->16, and a->32); it checks the structure's state, and drives sub-operations (indirect calls) based on the result. This is the "process this data" routine (two thousand three hundred and fifteenth form), part of the 0x82C4E process cluster. 114 lines. 0 callees (indirect calls). 1 caller (sub_82C4ECB0).
[entrypoint.functions."0x82C4E480"]
name = "ProcessAndProcessAndProcess2314_82C4E480"

# Process a value by copying fields between structures (two thousand three hundred and sixteenth form). It takes a value (a), a structure (b: fields at b->4, b->56, b->60, b->28, b->40, and b->74), and a structure (c: fields at c->0, c->32, and c->20); it copies fields between the structures. This is the "process this data" routine (two thousand three hundred and sixteenth form), part of the 0x82C4E process cluster. 158 lines. 0 callees. 2 callers (sub_82C1F910 and sub_82C1E878).
[entrypoint.functions."0x82C4E6D0"]
name = "ProcessAndProcessAndProcess2315_82C4E6D0"

# Process a structure by checking its state and driving many sub-operations (two thousand three hundred and seventeenth form). It takes a structure (a: field at a->0), a structure (b: fields at b->60 and b->4), and a value (c); it checks the structure's state, and drives many sub-operations (ConstructRefCounted, DestructRefCounted, Allocate_OrDbgBreak, and more) based on the result. This is the "process this data" routine (two thousand three hundred and seventeenth form), part of the 0x82C4E process cluster. 160 lines. 3 callees. 2 callers (sub_82C1F910 and sub_82C1E878).
[entrypoint.functions."0x82C4E890"]
name = "ProcessAndProcessAndProcess2316_82C4E890"

# Process a value by checking its state and driving sub-operations (two thousand three hundred and eighteenth form). It takes a value (a); it checks the value's state, and drives sub-operations (ProcessAndProcessAndProcess2067) based on the result. This is the "process this data" routine (two thousand three hundred and eighteenth form), part of the 0x82C4E process cluster. 56 lines. 1 callee. 1 caller (sub_82C4F240).
[entrypoint.functions."0x82C4E9B8"]
name = "ProcessAndProcessAndProcess2317_82C4E9B8"

# Process a structure by checking its state and driving sub-operations (two thousand three hundred and nineteenth form). It takes a structure (a: fields at a->48, a->32, a->52, a->56, and a->72); it checks the structure's state, and drives sub-operations (ProcessAndVcallAndClear3) based on the result. This is the "process this data" routine (two thousand three hundred and nineteenth form), part of the 0x82C4E process cluster. 236 lines. 1 callee. 2 callers (sub_82C4ECB0 and ProcessAndProcessAndProcess1591).
[entrypoint.functions."0x82C4EB08"]
name = "ProcessAndProcessAndProcess2318_82C4EB08"

# Process a structure by checking its state and driving many sub-operations (two thousand three hundred and twentieth form). It takes a structure (a: fields at a->32, a->248, and a->128) and a value (b); it checks the structure's state, and drives many sub-operations (ProcessAndVcallAndClear3, ProcessAndVcallAndProcess10, ProcessAndProcessAndProcess2295, ProcessAndProcessAndProcess2314, ProcessAndProcessAndProcess2318, and more) based on the result. This is the "process this data" routine (two thousand three hundred and twentieth form), part of the 0x82C4E process cluster. 427 lines. 7 callees. 2 callers (ProcessAndProcessAndProcess1591 and sub_82C4F240).
[entrypoint.functions."0x82C4ECB0"]
name = "ProcessAndProcessAndProcess2319_82C4ECB0"

# Process a structure by checking its state and driving many sub-operations (two thousand three hundred and twenty-first form). It takes a structure (a: fields at a->156 and a->128), a value (b), a value (c), and a value (d); it checks the structure's state, and drives many sub-operations (ProcessAndProcessAndProcess1955, Allocate_OrDbgBreak, AtomicIncrement3, CopyAssign_RefCounted2, DestructRefCounted, ProcessAndVcallAndClear3, ProcessAndProcessAndProcess2319, and many more) based on the result. This is the "process this data" routine (two thousand three hundred and twenty-first form), part of the 0x82C4F process cluster. 755 lines. 15 callees. 2 callers (sub_82C1F910 and sub_82C1E878).
[entrypoint.functions."0x82C4F240"]
name = "ProcessAndProcessAndProcess2320_82C4F240"

# Process a structure by checking its state and driving many sub-operations (two thousand three hundred and twenty-second form). It takes a structure (a: fields at a->0 and a->4); it checks the structure's state, and drives many sub-operations (Construct20ByteNode, AtomicIncrement3, ProcessAndProcessAndProcess1864, InitializeNode, and more) based on the result. This is the "process this data" routine (two thousand three hundred and twenty-second form), part of the 0x82C4F process cluster. 200 lines. 4 callees. 2 callers (sub_82C1F910 and sub_82C1E878).
[entrypoint.functions."0x82C4F750"]
name = "ProcessAndProcessAndProcess2321_82C4F750"

# Process a value by checking its state and driving many sub-operations (two thousand three hundred and twenty-third form). It takes a value (a) and a value (b); it checks the value's state, and drives many sub-operations (FindChainValue, ProcessVcall3, and more) based on the result. This is the "process this data" routine (two thousand three hundred and twenty-third form), part of the 0x82C50 process cluster. 66 lines. 2 callees. 2 callers (sub_82C3AE60 and ProcessAndProcessAndProcess2319).
[entrypoint.functions."0x82C50198"]
name = "ProcessAndProcessAndProcess2322_82C50198"

# Return a field from an object. It takes a structure (a: fields at a->92 and a->24); it returns a field from the object. This is the object-field getter, part of the 0x82C50 object cluster. 16 lines. 0 callees. 1 caller (UITextPrompt_RenderElement).
[entrypoint.functions."0x82C50A58"]
name = "GetFieldFromObject_82C50A58"

# Return a field from an object (second form). It takes a structure (a: fields at a->92 and a->36); it returns a field from the object. This is the object-field getter (second form), part of the 0x82C50 object cluster. 16 lines. 0 callees. 1 caller (UITextPrompt_RenderElement).
[entrypoint.functions."0x82C50A70"]
name = "GetFieldFromObject2_82C50A70"

# Process a structure by computing a value from its fields (two thousand three hundred and twenty-fourth form). It takes a structure (a: fields at a->92, a->100, a->4, a->8, and a->28); it computes a value from the structure's fields. This is the "process this data" routine (two thousand three hundred and twenty-fourth form), part of the 0x82C50 process cluster. 36 lines. 0 callees. 2 callers (ProcessAndProcessAndProcess2306 and sub_82C44570).
[entrypoint.functions."0x82C50AA8"]
name = "ProcessAndProcessAndProcess2323_82C50AA8"

# Process a structure by checking its state and modifying fields (two thousand three hundred and twenty-fifth form). It takes a structure (a: fields at a->92 and a->100), a value (b), a value (c), and a value (d); it checks the structure's state, and modifies fields based on the result. This is the "process this data" routine (two thousand three hundred and twenty-fifth form), part of the 0x82C50 process cluster. 154 lines. 0 callees. 2 callers (UITextPrompt_RenderElement and sub_82C45258).
[entrypoint.functions."0x82C50AF8"]
name = "ProcessAndProcessAndProcess2324_82C50AF8"

# Process a structure by checking its state and driving sub-operations (two thousand three hundred and twenty-sixth form). It takes a structure (a: fields at a->0, a->4, a->8, and a->12); it checks the structure's state, and drives sub-operations (ProcessAndProcessAndProcess1831) based on the result. This is the "process this data" routine (two thousand three hundred and twenty-sixth form), part of the 0x82C50 process cluster. 55 lines. 1 callee. 1 caller (sub_82C50DB0).
[entrypoint.functions."0x82C50CA8"]
name = "ProcessAndProcessAndProcess2325_82C50CA8"

# Process a structure by checking its state and driving sub-operations (two thousand three hundred and twenty-seventh form). It takes a structure (a: fields at a->0, a->92, a->96, a->100, a->104, and a->108); it checks the structure's state, and drives sub-operations (ProcessAndProcessAndProcess2325) based on the result. This is the "process this data" routine (two thousand three hundred and twenty-seventh form), part of the 0x82C50 process cluster. 50 lines. 1 callee. 1 caller (sub_82C44E68).
[entrypoint.functions."0x82C50DB0"]
name = "ProcessAndProcessAndProcess2326_82C50DB0"

# Process a structure by checking its state and driving many sub-operations (two thousand three hundred and twenty-eighth form). It takes a structure (a: fields at a->4 and a->52); it checks the structure's state, and drives many sub-operations (GetField8, GetField_12, and more) based on the result. This is the "process this data" routine (two thousand three hundred and twenty-eighth form), part of the 0x82C51 process cluster. 80 lines. 2 callees. 1 caller (sub_82C511C0).
[entrypoint.functions."0x82C51040"]
name = "ProcessAndProcessAndProcess2327_82C51040"

# Process a structure by checking its state and driving many sub-operations (two thousand three hundred and twenty-ninth form). It takes a structure (a: fields at a->4 and a->72); it checks the structure's state, and drives many sub-operations (GetField8, GetField_12, and more) based on the result. This is the "process this data" routine (two thousand three hundred and twenty-ninth form), part of the 0x82C51 process cluster. 80 lines. 2 callees. 1 caller (sub_82C511C0).
[entrypoint.functions."0x82C510C8"]
name = "ProcessAndProcessAndProcess2328_82C510C8"

# Process a structure by checking its state and driving many sub-operations (two thousand three hundred and thirtieth form). It takes a structure (a: fields at a->4 and a->32); it checks the structure's state, and drives many sub-operations (ProcessAndProcessAndProcess2327, ProcessAndProcessAndProcess2328, GetField_12, and more) based on the result. This is the "process this data" routine (two thousand three hundred and thirtieth form), part of the 0x82C51 process cluster. 118 lines. 3 callees. 0 callers.
[entrypoint.functions."0x82C511C0"]
name = "ProcessAndProcessAndProcess2329_82C511C0"

# Process a value by checking its state and modifying a structure's fields (two thousand three hundred and thirty-first form). It takes a value (a) and a structure (b: fields at b->8, b->4, and b->0); it checks the value's state, and modifies the structure's fields based on the result. This is the "process this data" routine (two thousand three hundred and thirty-first form), part of the 0x82C51 process cluster. 57 lines. 0 callees. 3 callers (sub_82C097D0, sub_82C52A28, and sub_82C53A00).
[entrypoint.functions."0x82C51DB8"]
name = "ProcessAndProcessAndProcess2330_82C51DB8"

# Process a structure by checking its state and modifying many fields (two thousand three hundred and thirty-second form). It takes a structure (a: field at a->4), a structure (b: field at b->636), a value (c), a value (d), and a value (e); it checks the structure's state, and modifies many fields based on the result. This is the "process this data" routine (two thousand three hundred and thirty-second form), part of the 0x82C51 process cluster. 410 lines. 0 callees. 1 caller (sub_82C526C8).
[entrypoint.functions."0x82C51FF0"]
name = "ProcessAndProcessAndProcess2331_82C51FF0"

# Process a structure by checking its state and driving many sub-operations (two thousand three hundred and thirty-third form). It takes a structure (a: field at a->20), a structure (b: fields at b->328 and b->636), and a value (c); it checks the structure's state, and drives many sub-operations (MemSet_SdkRuntime, ProcessAndProcessAndProcess2216, SwitchDispatch, ProcessAndProcessAndProcess2331, and more) based on the result. This is the "process this data" routine (two thousand three hundred and thirty-third form), part of the 0x82C52 process cluster. 308 lines. 6 callees. 1 caller (sub_82C53FE0).
[entrypoint.functions."0x82C526C8"]
name = "ProcessAndProcessAndProcess2332_82C526C8"

# Process a structure by checking its state and driving sub-operations (two thousand three hundred and thirty-fourth form). It takes a structure (a: field at a->344) and a value (b); it checks the structure's state, and drives sub-operations (UIFont_EmitGlyphQuad) based on the result. This is the "process this data" routine (two thousand three hundred and thirty-fourth form), part of the 0x82C52 process cluster. 41 lines. 1 callee. 0 callers.
[entrypoint.functions."0x82C528E8"]
name = "ProcessAndProcessAndProcess2333_82C528E8"

# Process a structure by checking its state and driving many sub-operations (two thousand three hundred and thirty-fifth form). It takes a structure (a: fields at a->0, a->4, a->8, a->12, a->16, a->20, and a->24); it checks the structure's state, and drives many sub-operations (Construct20ByteNode, Allocate_OrDbgBreak, ProcessAndProcessAndProcess2056, ProcessAndProcessAndProcess626, and more) based on the result. This is the "process this data" routine (two thousand three hundred and thirty-fifth form), part of the 0x82C53 process cluster. 343 lines. 4 callees. 1 caller (sub_82C54F18).
[entrypoint.functions."0x82C53D90"]
name = "ProcessAndProcessAndProcess2334_82C53D90"

# Process a structure by checking its state and driving many sub-operations (two thousand three hundred and thirty-sixth form). It takes a structure (a: fields at a->690, a->340, and a->36) and a value (b); it checks the structure's state, and drives many sub-operations (Allocate_OrDbgBreak, ProcessAndProcessAndProcess751, ProcessAndProcessAndProcess2332, and more) based on the result. This is the "process this data" routine (two thousand three hundred and thirty-sixth form), part of the 0x82C53 process cluster. 131 lines. 4 callees. 2 callers (ProcessAndProcessAndProcess760 and sub_82C54F18).
[entrypoint.functions."0x82C53FE0"]
name = "ProcessAndProcessAndProcess2335_82C53FE0"

# Process a structure by checking its state and driving many sub-operations (two thousand three hundred and thirty-seventh form). It takes a value (a), a structure (b: field at b->52), a value (c), a value (d), a value (e), a value (f), and a value (g); it checks the structure's state, and drives many sub-operations (Allocate_OrDbgBreak, ProcessAndProcessAndProcess2334, ProcessAndProcessAndProcess2335, and more) based on the result. This is the "process this data" routine (two thousand three hundred and thirty-seventh form), part of the 0x82C54 process cluster. 246 lines. 3 callees. 0 callers.
[entrypoint.functions."0x82C54F18"]
name = "ProcessAndProcessAndProcess2336_82C54F18"

# Process an array of integers by zeroing it (two thousand three hundred and thirty-eighth form). It takes a value (a) and a value (b); it zeros the array of integers. This is the "process this data" routine (two thousand three hundred and thirty-eighth form), part of the 0x82C5D process cluster. 28 lines. 0 callees. 1 caller (sub_82C5DB58).
[entrypoint.functions."0x82C5DB00"]
name = "ProcessAndProcessAndProcess2337_82C5DB00"

# Zero a region of bytes. It takes a value (a) and a value (b); it zeros the region of bytes. This is the byte memset routine, part of the 0x82C5D memory cluster. 24 lines. 0 callees. 1 caller (sub_82C5DB58).
[entrypoint.functions."0x82C5DB30"]
name = "MemSetBytes_82C5DB30"

# Process a value by zeroing it (two thousand three hundred and thirty-ninth form). It takes a value (a) and a value (b); it zeros the value. This is the "process this data" routine (two thousand three hundred and thirty-ninth form), part of the 0x82C5D process cluster. 33 lines. 2 callees. 10 callers (sub_82C62800, sub_82C60530, sub_82C60378, sub_82C5FBC8, sub_82C5DBB0, and more).
[entrypoint.functions."0x82C5DB58"]
name = "ProcessAndProcessAndProcess2338_82C5DB58"

# Process a value by zeroing it (two thousand three hundred and fortieth form). It takes a value (a), a value (b), and a value (c); it zeros the value. This is the "process this data" routine (two thousand three hundred and fortieth form), part of the 0x82CBE process cluster. 162 lines. 0 callees. 1 caller (sub_82CD3380).
[entrypoint.functions."0x82CBE140"]
name = "ProcessAndProcessAndProcess2339_82CBE140"

# Process a value by checking its state and driving many sub-operations (two thousand three hundred and forty-first form). It takes a value (a), a value (b), a value (c), a value (d), and a value (e); it checks the value's state, and drives many sub-operations (ProcessAndProcessAndProcess1887, MmGetPhysicalAddress, ProcessAndProcessAndProcess2339, and more) based on the result. This is the "process this data" routine (two thousand three hundred and forty-first form), part of the 0x82CD3 process cluster. 286 lines. 6 callees. 2 callers (ProcessAndProcessAndProcess1137 and sub_82CE1000).
[entrypoint.functions."0x82CD3380"]
name = "ProcessAndProcessAndProcess2340_82CD3380"

# Process a value by computing it (two thousand three hundred and forty-second form). It takes a value (a) and a value (b); it computes the value. This is the "process this data" routine (two thousand three hundred and forty-second form), part of the 0x82CD3 process cluster. 50 lines. 0 callees. 1 caller (ProcessAndProcessAndProcess2340).
[entrypoint.functions."0x82CD35D8"]
name = "ProcessAndProcessAndProcess2341_82CD35D8"

# Process a structure by checking its state and modifying it (two thousand three hundred and forty-third form). It takes a structure (a: field at a->8), a value (b), a value (c), and a value (d); it checks the structure's state, and modifies it based on the result. This is the "process this data" routine (two thousand three hundred and forty-third form), part of the 0x82CD3 process cluster. 30 lines. 0 callees. 2 callers (ProcessAndProcessAndProcess2277 and sub_82CE1588).
[entrypoint.functions."0x82CD3EE8"]
name = "ProcessAndProcessAndProcess2342_82CD3EE8"

# Process a value by computing it (two thousand three hundred and forty-fourth form). It takes a value (a); it computes the value. This is the "process this data" routine (two thousand three hundred and forty-fourth form), part of the 0x82CD3 process cluster. 18 lines. 0 callees. 1 caller (sub_82CD36B8).
[entrypoint.functions."0x82CD3F20"]
name = "ProcessAndProcessAndProcess2343_82CD3F20"

# Process a structure by checking its state and driving many sub-operations (two thousand three hundred and forty-fifth form). It takes a structure (a: fields at a->0 and a->8); it checks the structure's state, and drives many sub-operations (MmGetPhysicalAddress, XMACreateContext, and more) based on the result. This is the "process this data" routine (two thousand three hundred and forty-fifth form), part of the 0x82CD3 process cluster. 115 lines. 2 callees. 2 callers (sub_82CE2100 and ProcessAndProcessAndProcess2340).
[entrypoint.functions."0x82CD3F40"]
name = "ProcessAndProcessAndProcess2344_82CD3F40"

# Process a value by checking its state and driving many sub-operations (two thousand three hundred and forty-sixth form). It takes a value (a); it checks the value's state, and drives many sub-operations (KeAcquireSpinLockAtRaisedIrql, KeRaiseIrqlToDpcLevel, KeReleaseSpinLockFromRaisedIrql, KfLowerIrql, and more) based on the result. This is the "process this data" routine (two thousand three hundred and forty-sixth form), part of the 0x82CE5 process cluster. 188 lines. 4 callees. 0 callers.
[entrypoint.functions."0x82CE56A0"]
name = "ProcessAndProcessAndProcess2345_82CE56A0"

# Process a structure by checking its state and modifying it (two thousand three hundred and forty-seventh form). It takes a value (a) and a structure (b: fields at b->0 and b->4); it checks the structure's state, and modifies it based on the result. This is the "process this data" routine (two thousand three hundred and forty-seventh form), part of the 0x822F1 process cluster. 71 lines. 0 callees. 2 callers (ProcessAndProcessAndProcess2050 and sub_822F17C8).
[entrypoint.functions."0x822F1748"]
name = "ProcessAndProcessAndProcess2346_822F1748"

# Process a structure by checking its state and modifying it (two thousand three hundred and forty-eighth form). It takes a structure (a: fields at a->16, a->24, and a->8), a value (b), and a value (c); it checks the structure's state, and modifies it based on the result. This is the "process this data" routine (two thousand three hundred and forty-eighth form), part of the 0x82B4D process cluster. 36 lines. 0 callees. 0 callers.
[entrypoint.functions."0x82B4DC78"]
name = "ProcessAndProcessAndProcess2347_82B4DC78"

# Process a structure by checking its state and modifying it (two thousand three hundred and forty-ninth form). It takes a structure (a: fields at a->56, a->16, a->32, a->68, and a->52); it checks the structure's state, and modifies it based on the result. This is the "process this data" routine (two thousand three hundred and forty-ninth form), part of the 0x82C4E process cluster. 166 lines. 0 callees. 2 callers (ProcessAndProcessAndProcess2319 and ProcessAndProcessAndProcess1591).
[entrypoint.functions."0x82C4E550"]
name = "ProcessAndProcessAndProcess2348_82C4E550"

# Process a value by checking its state and driving sub-operations (two thousand three hundred and fiftieth form). It takes a value (a); it checks the value's state, and drives sub-operations (RtlEnterCriticalSection, RtlLeaveCriticalSection, and more) based on the result. This is the "process this data" routine (two thousand three hundred and fiftieth form), part of the 0x82CD2 process cluster. 60 lines. 2 callees. 10 callers (sub_82D01D30, sub_82D01858, sub_832B6FF8, sub_82D02058, sub_82C54750, and more).
[entrypoint.functions."0x82CD2A58"]
name = "ProcessAndProcessAndProcess2349_82CD2A58"

# Process a value by checking its state (two thousand three hundred and fifty-first form). It takes a value (a); it checks the value's state. This is the "process this data" routine (two thousand three hundred and fifty-first form), part of the 0x82CD7 process cluster. 22 lines. 0 callees. 2 callers (sub_82CDAD90 and sub_82CE5BA0).
[entrypoint.functions."0x82CD7D20"]
name = "ProcessAndProcessAndProcess2350_82CD7D20"

# Process a value by checking its state (two thousand three hundred and fifty-second form). It takes a value (a); it checks the value's state. This is the "process this data" routine (two thousand three hundred and fifty-second form), part of the 0x82CD8 process cluster. 9 lines. 1 callee. 0 callers.
[entrypoint.functions."0x82CD8648"]
name = "ProcessAndProcessAndProcess2351_82CD8648"

# Process a value by modifying it (two thousand three hundred and fifty-third form). It takes a value (a); it modifies the value. This is the "process this data" routine (two thousand three hundred and fifty-third form), part of the 0x82CD8 process cluster. 26 lines. 0 callees. 1 caller (ProcessAndProcessAndProcess2351).
[entrypoint.functions."0x82CD8F28"]
name = "ProcessAndProcessAndProcess2352_82CD8F28"

# Process a value by checking its state and driving sub-operations (two thousand three hundred and fifty-fourth form). It takes a value (a); it checks the value's state, and drives sub-operations based on the result. This is the "process this data" routine (two thousand three hundred and fifty-fourth form), part of the 0x82CD9 process cluster. 50 lines. 1 callee. 2 callers (sub_82CE0818 and sub_82CE33A8).
[entrypoint.functions."0x82CD9958"]
name = "ProcessAndProcessAndProcess2353_82CD9958"

# Process a structure by checking its state and driving sub-operations (two thousand three hundred and fifty-fifth form). It takes a structure (a: fields at a->8 and a->0); it checks the structure's state, and drives sub-operations based on the result. This is the "process this data" routine (two thousand three hundred and fifty-fifth form), part of the 0x82CDA process cluster. 78 lines. 0 callees. 0 callers.
[entrypoint.functions."0x82CDA270"]
name = "ProcessAndProcessAndProcess2354_82CDA270"

# Process a value by checking its state and driving many sub-operations (two thousand three hundred and fifty-sixth form). It takes a value (a); it checks the value's state, and drives many sub-operations (KeAcquireSpinLockAtRaisedIrql, KeRaiseIrqlToDpcLevel, KeReleaseSpinLockFromRaisedIrql, KfLowerIrql, and more) based on the result. This is the "process this data" routine (two thousand three hundred and fifty-sixth form), part of the 0x82CDC process cluster. 195 lines. 5 callees. 1 caller (sub_82CDCBD8).
[entrypoint.functions."0x82CDC5D8"]
name = "ProcessAndProcessAndProcess2355_82CDC5D8"

# Process a value by checking its state and driving sub-operations (two thousand three hundred and fifty-seventh form). It takes a value (a); it checks the value's state, and drives sub-operations (ProcessAndProcessAndProcess2355) based on the result. This is the "process this data" routine (two thousand three hundred and fifty-seventh form), part of the 0x82CDC process cluster. 32 lines. 1 callee. 0 callers.
[entrypoint.functions."0x82CDCBD8"]
name = "ProcessAndProcessAndProcess2356_82CDCBD8"

# Process a value by checking its state and driving sub-operations (two thousand three hundred and fifty-eighth form). It takes a value (a); it checks the value's state, and drives sub-operations based on the result. This is the "process this data" routine (two thousand three hundred and fifty-eighth form), part of the 0x82CDF process cluster. 17 lines. 1 callee. 2 callers (ProcessAndProcessAndProcess2353 and sub_82CD9EE0).
[entrypoint.functions."0x82CDF9F8"]
name = "ProcessAndProcessAndProcess2357_82CDF9F8"

# Process a value by checking its state and driving sub-operations (two thousand three hundred and fifty-ninth form). It takes a value (a); it checks the value's state, and drives sub-operations based on the result. This is the "process this data" routine (two thousand three hundred and fifty-ninth form), part of the 0x82CE3 process cluster. 9 lines. 1 callee. 0 callers.
[entrypoint.functions."0x82CE3198"]
name = "ProcessAndProcessAndProcess2358_82CE3198"

# Process a value by checking its state and driving sub-operations (two thousand three hundred and sixtieth form). It takes a value (a); it checks the value's state, and drives sub-operations (ProcessAndProcessAndProcess2353) based on the result. This is the "process this data" routine (two thousand three hundred and sixtieth form), part of the 0x82CE3 process cluster. 52 lines. 1 callee. 2 callers (ProcessAndProcessAndProcess2358 and sub_82CE3190).
[entrypoint.functions."0x82CE33A8"]
name = "ProcessAndProcessAndProcess2359_82CE33A8"

# Process a structure by checking its state and driving sub-operations (two thousand three hundred and sixty-first form). It takes a structure (a: fields at a->24 and a->4); it checks the structure's state, and drives sub-operations (ProcessAndProcessAndProcess2071) based on the result. This is the "process this data" routine (two thousand three hundred and sixty-first form), part of the 0x82CE4 process cluster. 71 lines. 1 callee. 3 callers (sub_82CE4DC8, sub_82CE4D68, and sub_82CE4E40).
[entrypoint.functions."0x82CE4C58"]
name = "ProcessAndProcessAndProcess2360_82CE4C58"

# Process a value by checking its state and driving sub-operations (two thousand three hundred and sixty-second form). It takes a value (a); it checks the value's state, and drives sub-operations based on the result. This is the "process this data" routine (two thousand three hundred and sixty-second form), part of the 0x82CE4 process cluster. 9 lines. 1 callee. 0 callers.
[entrypoint.functions."0x82CE4CF8"]
name = "ProcessAndProcessAndProcess2361_82CE4CF8"

# Process a structure by checking its state and driving sub-operations (two thousand three hundred and sixty-third form). It takes a structure (a: fields at a->0 and a->8); it checks the structure's state, and drives sub-operations (ProcessAndProcessAndProcess2071) based on the result. This is the "process this data" routine (two thousand three hundred and sixty-third form), part of the 0x82CE4 process cluster. 60 lines. 1 callee. 0 callers.
[entrypoint.functions."0x82CE4D00"]
name = "ProcessAndProcessAndProcess2362_82CE4D00"

# Process a value by checking its state and driving sub-operations (two thousand three hundred and sixty-fourth form). It takes a value (a); it checks the value's state, and drives sub-operations (ProcessAndProcessAndProcess2360) based on the result. This is the "process this data" routine (two thousand three hundred and sixty-fourth form), part of the 0x82CE4 process cluster. 48 lines. 1 callee. 1 caller (ProcessAndProcessAndProcess2361).
[entrypoint.functions."0x82CE4E40"]
name = "ProcessAndProcessAndProcess2363_82CE4E40"

# Process a structure by checking its state and driving many sub-operations (two thousand three hundred and sixty-fifth form). It takes a structure (a: fields at a->0, a->68, and a->72); it checks the structure's state, and drives many sub-operations (ProcessAndProcessAndProcess1755, ProcessAndProcessAndProcess1913, and more) based on the result. This is the "process this data" routine (two thousand three hundred and sixty-fifth form), part of the 0x82CE5 process cluster. 94 lines. 3 callees. 3 callers (sub_82CE57E8, ProcessAndProcessAndProcess2355, and sub_82CDD1F0).
[entrypoint.functions."0x82CE54A8"]
name = "ProcessAndProcessAndProcess2364_82CE54A8"

# Process a structure by checking its state and driving sub-operations (two thousand three hundred and sixty-sixth form). It takes a structure (a: fields at a->60 and a->36); it checks the structure's state, and drives sub-operations (ProcessAndProcessAndProcess2350) based on the result. This is the "process this data" routine (two thousand three hundred and sixty-sixth form), part of the 0x82CE5 process cluster. 119 lines. 1 callee. 3 callers (sub_82CE4470, sub_82CE5EA8, and ProcessAndProcessAndProcess2364).
[entrypoint.functions."0x82CE5BA0"]
name = "ProcessAndProcessAndProcess2365_82CE5BA0"

# Process a value by checking its state and driving sub-operations (two thousand three hundred and sixty-seventh form). It takes a value (a); it checks the value's state, and drives sub-operations (ProcessAndProcessAndProcess2061) based on the result. This is the "process this data" routine (two thousand three hundred and sixty-seventh form), part of the 0x82CE6 process cluster. 37 lines. 1 callee. 2 callers (ProcessAndProcessAndProcess2357 and sub_82CDFB20).
[entrypoint.functions."0x82CE6898"]
name = "ProcessAndProcessAndProcess2366_82CE6898"

# Process a value by checking its state and driving many sub-operations (two thousand three hundred and sixty-eighth form). It takes a value (a); it checks the value's state, and drives many sub-operations (AtomicIncrement3, DestructRefCounted, ProcessAndProcessAndProcess1344, and more) based on the result. This is the "process this data" routine (two thousand three hundred and sixty-eighth form), part of the 0x82C04 process cluster. 88 lines. 3 callees. 2 callers (sub_82C2F518 and sub_82C4EA18).
[entrypoint.functions."0x82C04280"]
name = "ProcessAndProcessAndProcess2367_82C04280"

# Process a value by setting a flag (two thousand three hundred and sixty-ninth form). It takes a value (a); it sets a flag. This is the "process this data" routine (two thousand three hundred and sixty-ninth form), part of the 0x82C08 process cluster. 10 lines. 0 callees. 0 callers.
[entrypoint.functions."0x82C08CC8"]
name = "ProcessAndProcessAndProcess2368_82C08CC8"

# Process a structure by checking its state and driving sub-operations (two thousand three hundred and seventieth form). It takes a structure (a: field at a->8), a value (b), and a value (c); it checks the structure's state, and drives sub-operations based on the result. This is the "process this data" routine (two thousand three hundred and seventieth form), part of the 0x82C1F process cluster. 102 lines. 0 callees. 1 caller (sub_82C1F7F8).
[entrypoint.functions."0x82C1F278"]
name = "ProcessAndProcessAndProcess2369_82C1F278"

# Process a structure by checking its state and driving sub-operations (two thousand three hundred and seventy-first form). It takes a value (a), a structure (b: fields at b->0 and b->4), and a value (c); it checks the structure's state, and drives sub-operations (ProcessAndProcessAndProcess2369) based on the result. This is the "process this data" routine (two thousand three hundred and seventy-first form), part of the 0x82C1F process cluster. 83 lines. 1 callee. 2 callers (sub_82C1F888 and sub_82C1F910).
[entrypoint.functions."0x82C1F7F8"]
name = "ProcessAndProcessAndProcess2370_82C1F7F8"

# Process a structure by checking its state and driving many sub-operations (two thousand three hundred and seventy-second form). It takes a value (a), a value (b), and a structure (c: fields at c->0 and c->16); it checks the structure's state, and drives many sub-operations (Allocate_OrDbgBreak, ProcessAndProcessAndProcess1109, ProcessAndProcessAndProcess2196, ProcessAndProcessAndProcess2197, ProcessAndProcessAndProcess2370, ProcessAndProcessAndProcess2315, ProcessAndProcessAndProcess2316, ProcessAndProcessAndProcess2320, ProcessAndProcessAndProcess2321, and more) based on the result. This is the "process this data" routine (two thousand three hundred and seventy-second form), part of the 0x82C1F process cluster. 220 lines. 11 callees. 0 callers.
[entrypoint.functions."0x82C1F910"]
name = "ProcessAndProcessAndProcess2371_82C1F910"

# Process a structure by checking its state and modifying it (two thousand three hundred and seventy-third form). It takes a value (a) and a structure (b: fields at b->4 and b->40); it checks the structure's state, and modifies it based on the result. This is the "process this data" routine (two thousand three hundred and seventy-third form), part of the 0x82C4D process cluster. 64 lines. 0 callees. 2 callers (ProcessAndProcessAndProcess2371 and sub_82C1E878).
[entrypoint.functions."0x82C4DBA8"]
name = "ProcessAndProcessAndProcess2372_82C4DBA8"

# Process a structure by checking its state and driving many sub-operations (two thousand three hundred and seventy-fourth form). It takes a structure (a: fields at a->0, a->152, a->40, a->148, a->144, a->140, and a->156); it checks the structure's state, and drives many sub-operations (DestructRefCounted, ProcessAndProcessAndProcess1802, ProcessAndProcessAndProcess1811, ProcessAndProcessAndProcess2367, and more) based on the result. This is the "process this data" routine (two thousand three hundred and seventy-fourth form), part of the 0x82C4E process cluster. 141 lines. 5 callees. 1 caller (sub_82C4F030).
[entrypoint.functions."0x82C4EA18"]
name = "ProcessAndProcessAndProcess2373_82C4EA18"

# Process a value by checking its state and driving sub-operations (two thousand three hundred and seventy-fifth form). It takes a value (a) and a value (b); it checks the value's state, and drives sub-operations (Free_Thunk, ProcessAndProcessAndProcess2373, and more) based on the result. This is the "process this data" routine (two thousand three hundred and seventy-fifth form), part of the 0x82C4F process cluster. 50 lines. 2 callees. 0 callers.
[entrypoint.functions."0x82C4F030"]
name = "ProcessAndProcessAndProcess2374_82C4F030"

# Process a structure by checking its state (two thousand three hundred and seventy-sixth form). It takes a structure (a: field at a->4); it checks the structure's state. This is the "process this data" routine (two thousand three hundred and seventy-sixth form), part of the 0x82C50 process cluster. 19 lines. 0 callees. 0 callers.
[entrypoint.functions."0x82C50110"]
name = "ProcessAndProcessAndProcess2375_82C50110"

# Process a value by checking its state and driving many sub-operations (two thousand three hundred and seventy-seventh form). It takes a value (a); it checks the value's state, and drives many sub-operations (FindChainValue, Tree_Process_Wrapper, Destruct20ByteNode, Free_Thunk, NoOp_Stub, and more) based on the result. This is the "process this data" routine (two thousand three hundred and seventy-seventh form), part of the 0x82C50 process cluster. 106 lines. 5 callees. 7 callers (sub_82C347E0, sub_82C3AA88, sub_82C389B0, sub_82C3F240, sub_82C26158, and more).
[entrypoint.functions."0x82C502D8"]
name = "ProcessAndProcessAndProcess2376_82C502D8"

# Process a value by checking its state and driving sub-operations (two thousand three hundred and seventy-eighth form). It takes a value (a), a value (b), a value (c), a value (d), a value (e), a value (f), a value (g), and a value (h); it checks the value's state, and drives sub-operations (DispatchByTypeCode) based on the result. This is the "process this data" routine (two thousand three hundred and seventy-eighth form), part of the 0x82B91 process cluster. 196 lines. 1 callee. 2 callers (ProcessAndProcessAndProcess1323 and ProcessAndProcessAndProcess1945).
[entrypoint.functions."0x82B91D00"]
name = "ProcessAndProcessAndProcess2377_82B91D00"

# Process a value by checking its state and driving many sub-operations (two thousand three hundred and seventy-ninth form). It takes a value (a), a value (b), a value (c), and a value (d); it checks the value's state, and drives many sub-operations (FindChainRoot, ProcessAndProcessAndProcess2074, and more) based on the result. This is the "process this data" routine (two thousand three hundred and seventy-ninth form), part of the 0x82BFC process cluster. 137 lines. 3 callees. 4 callers (sub_832AFB98, sub_82C7E5B0, sub_82C7E648, and sub_82C0A1A0).
[entrypoint.functions."0x82BFC6A8"]
name = "ProcessAndProcessAndProcess2378_82BFC6A8"

# Process a value by modifying it (two thousand three hundred and eightieth form). It takes a value (a); it modifies the value. This is the "process this data" routine (two thousand three hundred and eightieth form), part of the 0x82BFF process cluster. 12 lines. 0 callees. 1 caller (sub_82C14410).
[entrypoint.functions."0x82BFFAF8"]
name = "ProcessAndProcessAndProcess2379_82BFFAF8"

# Process a value by checking its state (two thousand three hundred and eighty-first form). It takes a value (a) and a value (b); it checks the value's state. This is the "process this data" routine (two thousand three hundred and eighty-first form), part of the 0x82C09 process cluster. 84 lines. 0 callees. 1 caller (sub_82C0A1A0).
[entrypoint.functions."0x82C092E0"]
name = "ProcessAndProcessAndProcess2380_82C092E0"

# Process a value by checking its state (two thousand three hundred and eighty-second form). It takes a value (a) and a value (b); it checks the value's state. This is the "process this data" routine (two thousand three hundred and eighty-second form), part of the 0x82C09 process cluster. 84 lines. 0 callees. 1 caller (sub_82C0A1A0).
[entrypoint.functions."0x82C09368"]
name = "ProcessAndProcessAndProcess2381_82C09368"

# Process a structure by checking its state and driving many sub-operations (two thousand three hundred and eighty-third form). It takes a structure (a: fields at a->64, a->72, and a->68), a value (b), and a value (c); it checks the structure's state, and drives many sub-operations (CheckedCopy, ProcessAndProcessAndProcess1942, and more) based on the result. This is the "process this data" routine (two thousand three hundred and eighty-third form), part of the 0x82C09 process cluster. 179 lines. 2 callees. 2 callers (UIText_RenderElement and sub_82C09C80).
[entrypoint.functions."0x82C09B50"]
name = "ProcessAndProcessAndProcess2382_82C09B50"

# Process a structure by checking its state and driving many sub-operations (two thousand three hundred and eighty-fourth form). It takes a structure (a: fields at a->0, a->44, a->136, a->64, and a->60); it checks the structure's state, and drives many sub-operations (Free_Thunk, ProcessAndProcessAndProcess2382, and more) based on the result. This is the "process this data" routine (two thousand three hundred and eighty-fourth form), part of the 0x82C09 process cluster. 153 lines. 2 callees. 1 caller (sub_82C09D88).
[entrypoint.functions."0x82C09C80"]
name = "ProcessAndProcessAndProcess2383_82C09C80"

# Process a value by checking its state and driving sub-operations (two thousand three hundred and eighty-fifth form). It takes a value (a) and a value (b); it checks the value's state, and drives sub-operations (Free_Thunk, ProcessAndProcessAndProcess2383, and more) based on the result. This is the "process this data" routine (two thousand three hundred and eighty-fifth form), part of the 0x82C09 process cluster. 50 lines. 2 callees. 0 callers.
[entrypoint.functions."0x82C09D88"]
name = "ProcessAndProcessAndProcess2384_82C09D88"

# Process a value by checking its state and driving many sub-operations (two thousand three hundred and eighty-sixth form). It takes a value (a) and a value (b); it checks the value's state, and drives many sub-operations (ProcessAndProcessAndProcess2378, ProcessAndProcessAndProcess2380, ProcessAndProcessAndProcess2381, and more) based on the result. This is the "process this data" routine (two thousand three hundred and eighty-sixth form), part of the 0x82C0A process cluster. 87 lines. 4 callees. 1 caller (ProcessAndProcessAndProcess2069).
[entrypoint.functions."0x82C0A1A0"]
name = "ProcessAndProcessAndProcess2385_82C0A1A0"

# Process a structure by checking its state and driving many sub-operations (two thousand three hundred and eighty-seventh form). It takes a structure (a: fields at a->16 and a->32); it checks the structure's state, and drives many sub-operations (Free_Thunk, ProcessAndProcessAndProcess2069, and more) based on the result. This is the "process this data" routine (two thousand three hundred and eighty-seventh form), part of the 0x82C0A process cluster. 156 lines. 2 callees. 1 caller (UIText_RenderElement).
[entrypoint.functions."0x82C0A590"]
name = "ProcessAndProcessAndProcess2386_82C0A590"

# Process a value by checking its state and driving many sub-operations (two thousand three hundred and eighty-eighth form). It takes a value (a), a value (b), a value (c), and a value (d); it checks the value's state, and drives many sub-operations (ProcessAndValidateAndProcess3, ProcessChain2, ProcessAndProcessAndProcess2082, and more) based on the result. This is the "process this data" routine (two thousand three hundred and eighty-eighth form), part of the 0x82C11 process cluster. 298 lines. 4 callees. 1 caller (sub_82C11FF8).
[entrypoint.functions."0x82C11BC0"]
name = "ProcessAndProcessAndProcess2387_82C11BC0"

# Process a structure by checking its state and driving sub-operations (two thousand three hundred and eighty-ninth form). It takes a structure (a: field at a->4) and a value (b); it checks the structure's state, and drives sub-operations (ProcessAndProcessAndProcess2387) based on the result. This is the "process this data" routine (two thousand three hundred and eighty-ninth form), part of the 0x82C11 process cluster. 126 lines. 1 callee. 2 callers (sub_82C12850 and sub_82C56B00).
[entrypoint.functions."0x82C11FF8"]
name = "ProcessAndProcessAndProcess2388_82C11FF8"

# Process a structure by checking its state and driving sub-operations (two thousand three hundred and ninetieth form). It takes a structure (a: field at a->72); it checks the structure's state, and drives sub-operations based on the result. This is the "process this data" routine (two thousand three hundred and ninetieth form), part of the 0x82C12 process cluster. 9 lines. 1 callee. 0 callers.
[entrypoint.functions."0x82C12D88"]
name = "ProcessAndProcessAndProcess2389_82C12D88"

# Process a structure by checking its state (two thousand three hundred and ninety-first form). It takes a value (a) and a structure (b: fields at b->16 and b->8); it checks the structure's state. This is the "process this data" routine (two thousand three hundred and ninety-first form), part of the 0x82C13 process cluster. 81 lines. 0 callees. 1 caller (sub_82C14AC0).
[entrypoint.functions."0x82C13090"]
name = "ProcessAndProcessAndProcess2390_82C13090"

# Process a structure by checking its state and driving many sub-operations (two thousand three hundred and ninety-second form). It takes a structure (a: fields at a->0, a->60, and a->56); it checks the structure's state, and drives many sub-operations (Destruct20ByteNode, DestructRefCounted, and more) based on the result. This is the "process this data" routine (two thousand three hundred and ninety-second form), part of the 0x82C14 process cluster. 84 lines. 3 callees. 1 caller (sub_82C14B70).
[entrypoint.functions."0x82C14410"]
name = "ProcessAndProcessAndProcess2391_82C14410"

# Process a structure by checking its state and driving many sub-operations (two thousand three hundred and ninety-third form). It takes a structure (a: field at a->64); it checks the structure's state, and drives many sub-operations (FindChainValue, ProcessAndProcessAndProcess1814, ProcessAndProcessAndProcess1947, ProcessAndProcessAndProcess2390, and more) based on the result. This is the "process this data" routine (two thousand three hundred and ninety-third form), part of the 0x82C14 process cluster. 108 lines. 5 callees. 1 caller (ProcessAndProcessAndProcess1865).
[entrypoint.functions."0x82C14AC0"]
name = "ProcessAndProcessAndProcess2392_82C14AC0"

# Process a value by checking its state and driving sub-operations (two thousand three hundred and ninety-fourth form). It takes a value (a) and a value (b); it checks the value's state, and drives sub-operations (Free_Thunk, ProcessAndProcessAndProcess2391, and more) based on the result. This is the "process this data" routine (two thousand three hundred and ninety-fourth form), part of the 0x82C14 process cluster. 50 lines. 2 callees. 0 callers.
[entrypoint.functions."0x82C14B70"]
name = "ProcessAndProcessAndProcess2393_82C14B70"

# Process a structure by checking its state and driving sub-operations (two thousand three hundred and ninety-fifth form). It takes a value (a), a structure (b: fields at b->0 and b->4), and a value (c); it checks the structure's state, and drives sub-operations (BinarySearch_4Stride) based on the result. This is the "process this data" routine (two thousand three hundred and ninety-fifth form), part of the 0x82C26 process cluster. 83 lines. 1 callee. 1 caller (sub_82C27538).
[entrypoint.functions."0x82C26648"]
name = "ProcessAndProcessAndProcess2394_82C26648"

# Process a value by checking its state and driving sub-operations (two thousand three hundred and ninety-sixth form). It takes a value (a) and a value (b); it checks the value's state, and drives sub-operations (Free_Thunk) based on the result. This is the "process this data" routine (two thousand three hundred and ninety-sixth form), part of the 0x82C26 process cluster. 50 lines. 2 callees. 0 callers.
[entrypoint.functions."0x82C26730"]
name = "ProcessAndProcessAndProcess2395_82C26730"

# Process a value by checking its state and driving many sub-operations (two thousand three hundred and ninety-seventh form). It takes a value (a), a value (b), and a structure (c: field at c->16); it checks the value's state, and drives many sub-operations (Allocate_OrDbgBreak, ConstructAndIncrementCounter, DestructAndSetDescriptor, ProcessVcallBinary, and many others) based on the result. This is the "process this data" routine (two thousand three hundred and ninety-seventh form), part of the 0x82C27 process cluster. 448 lines. 15 callees. 0 callers.
[entrypoint.functions."0x82C27538"]
name = "ProcessAndProcessAndProcess2396_82C27538"

# Process a value by checking its state (two thousand three hundred and ninety-eighth form). It takes a value (a); it checks the value's state. This is the "process this data" routine (two thousand three hundred and ninety-eighth form), part of the 0x82C44 process cluster. 19 lines. 0 callees. 0 callers.
[entrypoint.functions."0x82C442A0"]
name = "ProcessAndProcessAndProcess2397_82C442A0"

# Process a structure by checking its state (two thousand three hundred and ninety-ninth form). It takes a structure (a: field at a->0); it checks the structure's state. This is the "process this data" routine (two thousand three hundred and ninety-ninth form), part of the 0x82C44 process cluster. 47 lines. 0 callees. 1 caller (sub_82C45128).
[entrypoint.functions."0x82C44B00"]
name = "ProcessAndProcessAndProcess2398_82C44B00"

# Process a structure by checking its state and driving many sub-operations (two thousand four hundredth form). It takes a structure (a: fields at a->0, a->188, and a->160); it checks the structure's state, and drives many sub-operations (Free_Thunk, ProcessAndProcessAndProcess1698, ProcessAndProcessAndProcess2075, and more) based on the result. This is the "process this data" routine (two thousand four hundredth form), part of the 0x82C45 process cluster. 303 lines. 8 callees. 1 caller (sub_82C45CB0).
[entrypoint.functions."0x82C45A80"]
name = "ProcessAndProcessAndProcess2399_82C45A80"

# Process a value by checking its state and driving sub-operations (two thousand four hundred and first form). It takes a value (a) and a value (b); it checks the value's state, and drives sub-operations (Free_Thunk, ProcessAndProcessAndProcess2399, and more) based on the result. This is the "process this data" routine (two thousand four hundred and first form), part of the 0x82C45 process cluster. 50 lines. 2 callees. 0 callers.
[entrypoint.functions."0x82C45CB0"]
name = "ProcessAndProcessAndProcess2400_82C45CB0"

# Process a structure by checking its state and driving sub-operations (two thousand four hundred and second form). It takes a structure (a: field at a->4); it checks the structure's state, and drives sub-operations based on the result. This is the "process this data" routine (two thousand four hundred and second form), part of the 0x82C4C process cluster. 41 lines. 1 callee. 0 callers.
[entrypoint.functions."0x82C4C1C8"]
name = "ProcessAndProcessAndProcess2401_82C4C1C8"

# Process a value by checking its state and driving many sub-operations (two thousand four hundred and third form). It takes a value (a); it checks the value's state, and drives many sub-operations (ProcessAndCallMethods, ProcessAndCheckMethod, and more) based on the result. This is the "process this data" routine (two thousand four hundred and third form), part of the 0x82C4C process cluster. 60 lines. 2 callees. 1 caller (sub_82C14AC0).
[entrypoint.functions."0x82C4CDA0"]
name = "ProcessAndProcessAndProcess2402_82C4CDA0"

# Process a value by checking its state and driving many sub-operations (two thousand four hundred and fourth form). It takes a value (a); it checks the value's state, and drives many sub-operations (ProcessAndCallMethods, ProcessAndCheckMethod, and more) based on the result. This is the "process this data" routine (two thousand four hundred and fourth form), part of the 0x82C4C process cluster. 60 lines. 2 callees. 1 caller (sub_82C12D88).
[entrypoint.functions."0x82C4CF78"]
name = "ProcessAndProcessAndProcess2403_82C4CF78"

# Process a structure by checking its state and driving many sub-operations (two thousand four hundred and fifth form). It takes a structure (a: fields at a->0, a->16, and a->4) and a value (b); it checks the structure's state, and drives many sub-operations (Free_Thunk, ProcessAndProcessAndProcess2067, and more) based on the result. This is the "process this data" routine (two thousand four hundred and fifth form), part of the 0x82C4E process cluster. 84 lines. 2 callees. 0 callers.
[entrypoint.functions."0x82C4EFA0"]
name = "ProcessAndProcessAndProcess2404_82C4EFA0"

# Process a structure by checking its state and driving sub-operations (two thousand four hundred and sixth form). It takes a structure (a: fields at a->0, a->8, and a->4); it checks the structure's state, and drives sub-operations (ProcessAndProcessAndProcess1811) based on the result. This is the "process this data" routine (two thousand four hundred and sixth form), part of the 0x82C50 process cluster. 66 lines. 1 callee. 1 caller (sub_82C26730).
[entrypoint.functions."0x82C50200"]
name = "ProcessAndProcessAndProcess2405_82C50200"

# Process a value by checking its state and driving sub-operations (two thousand four hundred and seventh form). It takes a value (a); it checks the value's state, and drives sub-operations (ProcessAndProcessAndProcess1109, called twice) based on the result. This is the "process this data" routine (two thousand four hundred and seventh form), part of the 0x82C50 process cluster. 47 lines. 1 callee. 2 callers (sub_82C283D8 and sub_82C27538).
[entrypoint.functions."0x82C50810"]
name = "ProcessAndProcessAndProcess2406_82C50810"

# Process a structure by checking its state and driving sub-operations (two thousand four hundred and eighth form). It takes a structure (a: fields at a->0, a->92, and a->4); it checks the structure's state, and drives sub-operations (ProcessAndProcessAndProcess1843) based on the result. This is the "process this data" routine (two thousand four hundred and eighth form), part of the 0x82C50 process cluster. 61 lines. 1 callee. 3 callers (sub_82C50D60, sub_82C50E08, and sub_82C51150).
[entrypoint.functions."0x82C50D00"]
name = "ProcessAndProcessAndProcess2407_82C50D00"

# Process a structure by checking its state and driving sub-operations (two thousand four hundred and ninth form). It takes a structure (a: fields at a->92, a->0, a->96, and a->100); it checks the structure's state, and drives sub-operations based on the result. This is the "process this data" routine (two thousand four hundred and ninth form), part of the 0x82C50 process cluster. 21 lines. 1 callee. 1 caller (sub_82C45A80).
[entrypoint.functions."0x82C50E08"]
name = "ProcessAndProcessAndProcess2408_82C50E08"

# Process a structure by checking its state and driving many sub-operations (two thousand four hundred and tenth form). It takes a structure (a: fields at a->4, a->12, and a->108), a value (b), a value (c), and a structure (d: field at d->0); it checks the structure's state, and drives many sub-operations (MemSet_SdkRuntime, GetField8, and more) based on the result. This is the "process this data" routine (two thousand four hundred and tenth form), part of the 0x82C50 process cluster. 149 lines. 3 callees. 1 caller (ProcessAndProcessAndProcess1651).
[entrypoint.functions."0x82C50E28"]
name = "ProcessAndProcessAndProcess2409_82C50E28"

# Process a structure by checking its state (two thousand four hundred and eleventh form). It takes a structure (a: fields at a->8 and a->4), a structure (b: field at b->0), a value (c), a value (d), and a value (e); it checks the structure's state. This is the "process this data" routine (two thousand four hundred and eleventh form), part of the 0x82C51 process cluster. 81 lines. 0 callees. 3 callers (sub_82C50E28, sub_82C51870, and sub_82C519C8).
[entrypoint.functions."0x82C51300"]
name = "ProcessAndProcessAndProcess2410_82C51300"

# Process a structure by checking its state and driving many sub-operations (two thousand four hundred and twelfth form). It takes a structure (a: field at a->0); it checks the structure's state, and drives many sub-operations (Free_Thunk, ProcessAndProcessAndProcess1884, and more) based on the result. This is the "process this data" routine (two thousand four hundred and twelfth form), part of the 0x82C53 process cluster. 66 lines. 3 callees. 2 callers (ProcessAndProcessAndProcess760 and sub_82C53C80).
[entrypoint.functions."0x82C53358"]
name = "ProcessAndProcessAndProcess2411_82C53358"

# Process a structure by checking its state and driving many sub-operations (two thousand four hundred and thirteenth form). It takes a value (a) and a structure (b: field at b->52); it checks the structure's state, and drives many sub-operations (Free_Thunk, ProcessAndProcessAndProcess1817, ProcessAndProcessAndProcess2411, and more) based on the result. This is the "process this data" routine (two thousand four hundred and thirteenth form), part of the 0x82C53 process cluster. 89 lines. 3 callees. 0 callers.
[entrypoint.functions."0x82C53C80"]
name = "ProcessAndProcessAndProcess2412_82C53C80"

# Process a structure by checking its state and driving many sub-operations (two thousand four hundred and fourteenth form). It takes a structure (a: fields at a->684, a->0, a->685, a->680, and a->500); it checks the structure's state, and drives many sub-operations (Destruct20ByteNode, Free_Thunk, ProcessAndProcessAndProcess1884, ProcessAndProcessAndProcess2077, and more) based on the result. This is the "process this data" routine (two thousand four hundred and fourteenth form), part of the 0x82C54 process cluster. 178 lines. 6 callees. 1 caller (sub_82C54AB0).
[entrypoint.functions."0x82C54750"]
name = "ProcessAndProcessAndProcess2413_82C54750"

# Process a value by checking its state and driving sub-operations (two thousand four hundred and fifteenth form). It takes a value (a) and a value (b); it checks the value's state, and drives sub-operations (Free_Thunk, ProcessAndProcessAndProcess2413, and more) based on the result. This is the "process this data" routine (two thousand four hundred and fifteenth form), part of the 0x82C54 process cluster. 50 lines. 2 callees. 0 callers.
[entrypoint.functions."0x82C54AB0"]
name = "ProcessAndProcessAndProcess2414_82C54AB0"

# Process a value by checking its state and driving sub-operations (two thousand four hundred and sixteenth form). It takes a value (a), a value (b), and a structure (c: field at c->0); it checks the value's state, and drives sub-operations (FindChainRoot) based on the result. This is the "process this data" routine (two thousand four hundred and sixteenth form), part of the 0x82C7D process cluster. 64 lines. 1 callee. 2 callers (sub_82C0A1A0 and sub_82C7E648).
[entrypoint.functions."0x82C7DC58"]
name = "ProcessAndProcessAndProcess2415_82C7DC58"

# Process a structure by checking its state and driving sub-operations (two thousand four hundred and seventeenth form). It takes a structure (a: field at a->4); it checks the structure's state, and drives sub-operations (ProcessAndProcessAndProcess2071) based on the result. This is the "process this data" routine (two thousand four hundred and seventeenth form), part of the 0x82CD3 process cluster. 54 lines. 2 callees. 4 callers (sub_82CD3380, sub_82C45A80, sub_82CE0818, and sub_82C450D8).
[entrypoint.functions."0x82CD3580"]
name = "ProcessAndProcessAndProcess2416_82CD3580"

# Process a structure by checking its state and driving sub-operations (two thousand four hundred and eighteenth form). It takes a structure (a: fields at a->0 and a->8); it checks the structure's state, and drives sub-operations (XMAReleaseContext) based on the result. This is the "process this data" routine (two thousand four hundred and eighteenth form), part of the 0x82CD3 process cluster. 73 lines. 1 callee. 2 callers (sub_82CD3580 and sub_82CE22B8).
[entrypoint.functions."0x82CD3630"]
name = "ProcessAndProcessAndProcess2417_82CD3630"

# Process a value by checking its state and driving many sub-operations (two thousand four hundred and nineteenth form). It takes a value (a), a value (b), a value (c), and a structure (d: field at d->0); it checks the value's state, and drives many sub-operations (CheckedCopy, AllocateChecked3, Free_SizeBucketed_Thunk, and more) based on the result. This is the "process this data" routine (two thousand four hundred and nineteenth form), part of the 0x82A49 process cluster. 395 lines. 4 callees. 1 caller (ProcessAndProcessAndProcess766).
[entrypoint.functions."0x82A49AA8"]
name = "ProcessAndProcessAndProcess2418_82A49AA8"

# Process a structure by checking its state (two thousand four hundred and twentieth form). It takes a value (a) and a structure (b: fields at b->8, b->4, and b->0); it checks the structure's state. This is the "process this data" routine (two thousand four hundred and twentieth form), part of the 0x82C51 process cluster. 79 lines. 0 callees. 5 callers (sub_82C53A00, sub_82C53FE0, ProcessAndProcessAndProcess760, and more).
[entrypoint.functions."0x82C51E20"]
name = "ProcessAndProcessAndProcess2419_82C51E20"

# Process a structure by checking its state (two thousand four hundred and twenty-first form). It takes a structure (a: fields at a->0 and a->4); it checks the structure's state. This is the "process this data" routine (two thousand four hundred and twenty-first form), part of the 0x82370 process cluster. 112 lines. 0 callees. 9 callers (sub_8236ACA0, sub_8236E5E0, ProcessAndProcessAndProcess114, and more).
[entrypoint.functions."0x82370FD8"]
name = "ProcessAndProcessAndProcess2420_82370FD8"

# Process a structure by checking its state (two thousand four hundred and twenty-second form). It takes a value (a) and a structure (b: fields at b->8 and b->4); it checks the structure's state. This is the "process this data" routine (two thousand four hundred and twenty-second form), part of the 0x82AA8 process cluster. 71 lines. 0 callees. 2 callers (ProcessAndProcessAndProcess2270 and sub_82AA8430).
[entrypoint.functions."0x82AA8888"]
name = "ProcessAndProcessAndProcess2421_82AA8888"

# Process a value by checking its state (two thousand four hundred and twenty-third form). It takes a value (a); it checks the value's state. This is the "process this data" routine (two thousand four hundred and twenty-third form), part of the 0x82FFE process cluster. 10 lines. 0 callees. 12 callers (sub_82FFD038, sub_82FFF800, sub_82FFCDD0, and more).
[entrypoint.functions."0x82FFE110"]
name = "ProcessAndProcessAndProcess2422_82FFE110"

# Process a value by checking its state and driving sub-operations (two thousand four hundred and twenty-fourth form). It takes a value (a), a value (b), and a value (c); it checks the value's state, and drives sub-operations based on the result. This is the "process this data" routine (two thousand four hundred and twenty-fourth form), part of the 0x82FFE process cluster. 34 lines. 1 callee. 2 callers (sub_82FFF5F0 and sub_82FFD170).
[entrypoint.functions."0x82FFE428"]
name = "ProcessAndProcessAndProcess2423_82FFE428"

# Process a value by checking its state and driving sub-operations (two thousand four hundred and twenty-fifth form). It takes a value (a) and a structure (b: field at b->0); it checks the value's state, and drives sub-operations (Identity_ReturnArg2) based on the result. This is the "process this data" routine (two thousand four hundred and twenty-fifth form), part of the 0x82FFE process cluster. 64 lines. 1 callee. 1 caller (sub_82FFE428).
[entrypoint.functions."0x82FFE460"]
name = "ProcessAndProcessAndProcess2424_82FFE460"

# Process a value by checking its state and driving many sub-operations (two thousand four hundred and twenty-sixth form). It takes a value (a), a value (b), a value (c), and a value (d); it checks the value's state, and drives many sub-operations (CompareString, CopyAssign_RefCounted, CopyAssign_RefCounted2, DestructRefCounted, ProcessAndProcessAndProcess1875, ProcessAndProcessAndProcess2263, and more) based on the result. This is the "process this data" routine (two thousand four hundred and twenty-sixth form), part of the 0x82357 process cluster. 315 lines. 8 callees. 0 callers.
[entrypoint.functions."0x82357550"]
name = "ProcessAndProcessAndProcess2425_82357550"

# Process a structure by checking its state and driving many sub-operations (two thousand four hundred and twenty-seventh form). It takes a structure (a: field at a->0); it checks the structure's state, and drives many sub-operations (DestructRefCounted, ProcessAndProcessAndProcess206, ProcessAndProcessAndProcess2096, ProcessAndProcessAndProcess263, and more) based on the result. This is the "process this data" routine (two thousand four hundred and twenty-seventh form), part of the 0x82358 process cluster. 115 lines. 4 callees. 0 callers.
[entrypoint.functions."0x82358EA8"]
name = "ProcessAndProcessAndProcess2426_82358EA8"

# Process a structure by checking its state and driving sub-operations (two thousand four hundred and twenty-eighth form). It takes a structure (a: fields at a->8, a->4, and a->12); it checks the structure's state, and drives sub-operations (ProcessEvent_Thunk) based on the result. This is the "process this data" routine (two thousand four hundred and twenty-eighth form), part of the 0x82BFD process cluster. 48 lines. 2 callees. 0 callers.
[entrypoint.functions."0x82BFD858"]
name = "ProcessAndProcessAndProcess2427_82BFD858"

# Process a structure by checking its state and driving sub-operations (two thousand four hundred and twenty-ninth form). It takes a structure (a: field at a->12); it checks the structure's state, and drives sub-operations based on the result. This is the "process this data" routine (two thousand four hundred and twenty-ninth form), part of the 0x82C00 process cluster. 9 lines. 1 callee. 1 caller (sub_82BFD858).
[entrypoint.functions."0x82C00048"]
name = "ProcessAndProcessAndProcess2428_82C00048"

# Process a value by checking its state and driving many sub-operations (two thousand four hundred and thirtieth form). It takes a value (a), a value (b), and a value (c); it checks the value's state, and drives many sub-operations (Allocate_OrDbgBreak, FindTreeLeaf, GetField833, and more) based on the result. This is the "process this data" routine (two thousand four hundred and thirtieth form), part of the 0x82C12 process cluster. 371 lines. 8 callees. 1 caller (sub_82C00048).
[entrypoint.functions."0x82C12850"]
name = "ProcessAndProcessAndProcess2429_82C12850"

# Process a structure by checking its state and driving many sub-operations (two thousand four hundred and thirty-first form). It takes a value (a), a structure (b: field at b->4), and a structure (c: field at c->0); it checks the structure's state, and drives many sub-operations (ProcessChain2, ProcessAndProcessAndProcess2082, and more) based on the result. This is the "process this data" routine (two thousand four hundred and thirty-first form), part of the 0x82C82 process cluster. 183 lines. 2 callees. 4 callers (sub_82C12850, sub_82C11BC0, sub_82C83378, and sub_82C83238).
[entrypoint.functions."0x82C82EA8"]
name = "ProcessAndProcessAndProcess2430_82C82EA8"

# Process a structure by checking its state (two thousand four hundred and thirty-second form). It takes a value (a) and a structure (b: fields at b->0 and b->4); it checks the structure's state. This is the "process this data" routine (two thousand four hundred and thirty-second form), part of the 0x829FE process cluster. 71 lines. 0 callees. 2 callers (ProcessAndProcessAndProcess1820 and ProcessAndProcessAndProcess1822).
[entrypoint.functions."0x829FE010"]
name = "ProcessAndProcessAndProcess2431_829FE010"

# Process a structure by checking its state and driving sub-operations (two thousand four hundred and thirty-third form). It takes a structure (a: fields at a->4, a->8, and a->12) and a value (b); it checks the structure's state, and drives sub-operations based on the result. This is the "process this data" routine (two thousand four hundred and thirty-third form), part of the 0x82B3F process cluster. 107 lines. 2 callees. 2 callers (sub_82C72640 and sub_82B3EB30).
[entrypoint.functions."0x82B3F418"]
name = "ProcessAndProcessAndProcess2432_82B3F418"

# Process a value by checking its state and driving sub-operations (two thousand four hundred and thirty-fourth form). It takes a value (a), a value (b), a value (c), and a value (d); it checks the value's state, and drives sub-operations based on the result. This is the "process this data" routine (two thousand four hundred and thirty-fourth form), part of the 0x82B3F process cluster. 58 lines. 1 callee. 2 callers (sub_82B3F418 and sub_82B3F680).
[entrypoint.functions."0x82B3F618"]
name = "ProcessAndProcessAndProcess2433_82B3F618"

# Process a structure by checking its state and driving sub-operations (two thousand four hundred and thirty-fifth form). It takes a structure (a: fields at a->24, a->20, and a->4), a value (b), and a value (c); it checks the structure's state, and drives sub-operations (ClampAndUpdate) based on the result. This is the "process this data" routine (two thousand four hundred and thirty-fifth form), part of the 0x82B3F process cluster. 67 lines. 1 callee. 1 caller (sub_82B3F618).
[entrypoint.functions."0x82B3FAB0"]
name = "ProcessAndProcessAndProcess2434_82B3FAB0"

# Process a structure by checking its state and driving many sub-operations (two thousand four hundred and thirty-sixth form). It takes a structure (a: fields at a->4 and a->12), a value (b), a value (c), and a structure (d: fields at d->0, d->4, d->8, and d->12); it checks the structure's state, and drives many sub-operations (Free_SizeBucketed_Thunk, ProcessAndProcessAndProcess2259, and more) based on the result. This is the "process this data" routine (two thousand four hundred and thirty-sixth form), part of the 0x82B8F process cluster. 548 lines. 3 callees. 2 callers (sub_82B8AA48 and ProcessAndProcessAndProcess1653).
[entrypoint.functions."0x82B8FCA8"]
name = "ProcessAndProcessAndProcess2435_82B8FCA8"

# Process a structure by checking its state (two thousand four hundred and thirty-seventh form). It takes a structure (a: fields at a->8, a->13, a->28, a->24, a->0, a->4, a->20, and a->52); it checks the structure's state. This is the "process this data" routine (two thousand four hundred and thirty-seventh form), part of the 0x82CEB process cluster. 442 lines. 0 callees. 1 caller (ProcessAndProcessAndProcess1603).
[entrypoint.functions."0x82CEBC18"]
name = "ProcessAndProcessAndProcess2436_82CEBC18"

# Process a structure by checking its state and driving many sub-operations (two thousand four hundred and thirty-eighth form). It takes a structure (a: fields at a->40, a->44, a->48, and a->52) and a value (b); it checks the structure's state, and drives many sub-operations (ComputeSpecialFunction, ComputeSpecialFunction2, and more) based on the result. This is the "process this data" routine (two thousand four hundred and thirty-eighth form), part of the 0x82C57 process cluster. 287 lines. 3 callees. 2 callers (sub_82C57CD8 and ProcessAndProcessAndProcess1851).
[entrypoint.functions."0x82C57AC8"]
name = "ProcessAndProcessAndProcess2437_82C57AC8"

# Process a value by checking its state and driving many sub-operations (two thousand four hundred and thirty-ninth form). It takes a value (a) and a value (b); it checks the value's state, and drives many sub-operations (ModifyFloatPrecision, ProcessAndProcessAndProcess809, and more) based on the result. This is the "process this data" routine (two thousand four hundred and thirty-ninth form), part of the 0x82CA7 process cluster. 366 lines. 7 callees. 4 callers (sub_83098158, sub_82BD1238, sub_82A275F0, and sub_82C57AC8).
[entrypoint.functions."0x82CA7568"]
name = "ProcessAndProcessAndProcess2438_82CA7568"

# Process a structure by checking its state and driving many sub-operations (two thousand four hundred and fortieth form). It takes a structure (a: field at a->0) and a value (b); it checks the structure's state, and drives many sub-operations (StrLenUTF16, ProcessAndClearAndConvert, Allocate_OrDbgBreak, and more) based on the result. This is the "process this data" routine (two thousand four hundred and fortieth form), part of the 0x82407 process cluster. 116 lines. 3 callees. 12 callers (sub_828FCA50, sub_82594EA0, sub_828E4DD8, and more).
[entrypoint.functions."0x82407A98"]
name = "ProcessAndProcessAndProcess2439_82407A98"

# Process a value by checking its state and driving many sub-operations (two thousand four hundred and forty-first form). It takes a value (a), a value (b), a value (c), a value (d), a value (e), a value (f), a value (g), and a value (h); it checks the value's state, and drives many sub-operations (ConstructRefCounted, DestructRefCounted, Free_SizeBucketed_Thunk, ProcessAndVcallAndProcess16, and more) based on the result. This is the "process this data" routine (two thousand four hundred and forty-first form), part of the 0x826CB process cluster. 361 lines. 7 callees. 2 callers (sub_826CA6E0 and sub_826CA0B0).
[entrypoint.functions."0x826CB808"]
name = "ProcessAndProcessAndProcess2440_826CB808"

# Process a value by checking its state and driving sub-operations (two thousand four hundred and forty-second form). It takes a value (a) and a value (b); it checks the value's state, and drives sub-operations (GetGlobalPointer, ProcessAndDispatchException, and more) based on the result. This is the "process this data" routine (two thousand four hundred and forty-second form), part of the 0x82238 process cluster. 43 lines. 2 callees. 1 caller (ProcessAndProcessAndProcess1829).
[entrypoint.functions."0x82238740"]
name = "ProcessAndProcessAndProcess2441_82238740"

# Process a value by checking its state and driving sub-operations (two thousand four hundred and forty-third form). It takes a value (a); it checks the value's state, and drives sub-operations (GetGlobalPointer, ProcessAndCheckAndProcess8, and more) based on the result. This is the "process this data" routine (two thousand four hundred and forty-third form), part of the 0x82239 process cluster. 37 lines. 2 callees. 1 caller (ProcessAndProcessAndProcess1799).
[entrypoint.functions."0x822397A8"]
name = "ProcessAndProcessAndProcess2442_822397A8"

# Process a structure by checking its state and driving sub-operations (two thousand four hundred and forty-fourth form). It takes a structure (a: fields at a->12, a->8, and a->4), a structure (b: field at b->0), and a value (c); it checks the structure's state, and drives sub-operations (Buffer_CopyOrZero_Error) based on the result. This is the "process this data" routine (two thousand four hundred and forty-fourth form), part of the 0x8236D process cluster. 74 lines. 1 callee. 0 callers.
[entrypoint.functions."0x8236D258"]
name = "ProcessAndProcessAndProcess2443_8236D258"

# Process a structure by checking its state and driving many sub-operations (two thousand four hundred and forty-fifth form). It takes a value (a), a structure (b: field at b->0), and a structure (c: field at c->0); it checks the structure's state, and drives many sub-operations (ConstructRefCounted, HashCstring, ProcessAndProcessAndProcess17, and more) based on the result. This is the "process this data" routine (two thousand four hundred and forty-fifth form), part of the 0x8259D process cluster. 136 lines. 3 callees. 2 callers (sub_8259D488 and sub_8259D7C0).
[entrypoint.functions."0x8259D510"]
name = "ProcessAndProcessAndProcess2444_8259D510"

# Process a value by checking its state and driving many sub-operations (two thousand four hundred and forty-sixth form). It takes a value (a) and a value (b); it checks the value's state, and drives many sub-operations (ConstructRefCounted, DestructRefCounted, CopyAssign_RefCounted2, ProcessAndVcallAndProcess16, ProcessAndProcessAndProcess1105, ProcessAndProcessAndProcess2444, and more) based on the result. This is the "process this data" routine (two thousand four hundred and forty-sixth form), part of the 0x8259D process cluster. 204 lines. 6 callees. 2 callers (ProcessAndProcessAndProcess1614 and sub_82338648).
[entrypoint.functions."0x8259D7C0"]
name = "ProcessAndProcessAndProcess2445_8259D7C0"

# Process a structure by checking its state and driving many sub-operations (two thousand four hundred and forty-seventh form). It takes a value (a) and a structure (b: fields at b->4 and b->8); it checks the structure's state, and drives many sub-operations (ConstructRefCounted, DestructRefCounted, CopyAssign_RefCounted, ProcessAndComputeFloat, ProcessAndReplaceRefCounted, ProcessAndVcallAndClear3, ReleaseGlobalRef, StrCmp_Signed, and many others) based on the result. This is the "process this data" routine (two thousand four hundred and forty-seventh form), part of the 0x826C8 process cluster. 2077 lines. 34 callees. 0 callers.
[entrypoint.functions."0x826C81A0"]
name = "ProcessAndProcessAndProcess2446_826C81A0"

# Process a structure by checking its state and driving many sub-operations (two thousand four hundred and forty-eighth form). It takes a structure (a: fields at a->108, a->180, and a->80); it checks the structure's state, and drives many sub-operations (ReleaseGlobalRef, ProcessAndDecrementRefCount, ProcessAndDecrementDispose, ProcessAndIncrementAndProcess, ProcessAndIncrementCounter, ProcessAndReplaceValue, ProcessAndResolveHandler, ProcessAndProcessAndProcess2256, and more) based on the result. This is the "process this data" routine (two thousand four hundred and forty-eighth form), part of the 0x82818 process cluster. 371 lines. 10 callees. 1 caller (ProcessAndProcessAndProcess814).
[entrypoint.functions."0x82818728"]
name = "ProcessAndProcessAndProcess2447_82818728"

# Process a structure by checking its state and driving many sub-operations (two thousand four hundred and forty-ninth form). It takes a structure (a: fields at a->108, a->180, a->160, a->168, a->164, and a->112); it checks the structure's state, and drives many sub-operations (ConstructRefCounted, ReleaseGlobalRef, ProcessAndFindSubstring, ProcessAndIncrementAndProcess, ProcessAndIncrementCounter, ProcessAndReplaceValue, GetSubObjField4, ProcessAndProcessAndProcess1726, ProcessAndProcessAndProcess1837, ProcessAndProcessAndProcess1958, ProcessAndProcessAndProcess2256, and more) based on the result. This is the "process this data" routine (two thousand four hundred and forty-ninth form), part of the 0x82818 process cluster. 951 lines. 16 callees. 1 caller (ProcessAndProcessAndProcess814).
[entrypoint.functions."0x82818988"]
name = "ProcessAndProcessAndProcess2448_82818988"

# Process a value by checking its state and driving many sub-operations (two thousand four hundred and fiftieth form). It takes a value (a) and a value (b); it checks the value's state, and drives many sub-operations (ProcessAndCopyStructRegion, ProcessGrowCopy, UpdateRegion, ProcessAndProcessAndProcess2234, and more) based on the result. This is the "process this data" routine (two thousand four hundred and fiftieth form), part of the 0x82B7E process cluster. 473 lines. 6 callees. 12 callers (ProcessAndProcessAndProcess656, ProcessAndProcessAndProcess549, sub_82B8B5E8, and more).
[entrypoint.functions."0x82B7E050"]
name = "ProcessAndProcessAndProcess2449_82B7E050"

# Process a structure by checking its state and driving many sub-operations (two thousand four hundred and fifty-first form). It takes a value (a) and a structure (b: fields at b->20, b->12, and b->4); it checks the structure's state, and drives many sub-operations (Allocate_OrDbgBreak, Allocate_SizeBucketed_Thunk, ProcessAndCopyStructRegion, ProcessAndProcessAndProcess1089, ProcessAndProcessAndProcess2435, and more) based on the result. This is the "process this data" routine (two thousand four hundred and fifty-first form), part of the 0x82B8A process cluster. 1225 lines. 9 callees. 1 caller (ProcessAndProcessAndProcess2449).
[entrypoint.functions."0x82B8AA48"]
name = "ProcessAndProcessAndProcess2450_82B8AA48"

# Process a structure by checking its state (two thousand four hundred and fifty-second form). It takes a structure (a: fields at a->0, a->20, a->4, and a->16), a value (b), and a value (c); it checks the structure's state. This is the "process this data" routine (two thousand four hundred and fifty-second form), part of the 0x82B98 process cluster. 351 lines. 0 callees. 2 callers (sub_82B8AA48 and sub_82B983D8).
[entrypoint.functions."0x82B98158"]
name = "ProcessAndProcessAndProcess2451_82B98158"

# Process a structure by checking its state and driving sub-operations (two thousand four hundred and fifty-third form). It takes a structure (a: fields at a->480, a->0, and a->484); it checks the structure's state, and drives sub-operations (Release_RefCounted, ProcessAndProcessAndProcess2171, and more) based on the result. This is the "process this data" routine (two thousand four hundred and fifty-third form), part of the 0x829FF process cluster. 126 lines. 2 callees. 1 caller (sub_82A00580).
[entrypoint.functions."0x829FFB80"]
name = "ProcessAndProcessAndProcess2452_829FFB80"

# Process a value by checking its state and driving sub-operations (two thousand four hundred and fifty-fourth form). It takes a value (a) and a value (b); it checks the value's state, and drives sub-operations (Free_SizeBucketed_Thunk, ProcessAndProcessAndProcess2452, and more) based on the result. This is the "process this data" routine (two thousand four hundred and fifty-fourth form), part of the 0x82A00 process cluster. 50 lines. 2 callees. 0 callers.
[entrypoint.functions."0x82A00580"]
name = "ProcessAndProcessAndProcess2453_82A00580"

# Process a structure by checking its state and driving many sub-operations (two thousand four hundred and fifty-fifth form). It takes a value (a), a structure (b: fields at b->4 and b->0), and a value (c); it checks the structure's state, and drives many sub-operations (Allocate_OrDbgBreak, ConstructAndProcess29, Release_RefCounted, SetValue4, ProcessAndProcessAndProcess2041, and more) based on the result. This is the "process this data" routine (two thousand four hundred and fifty-fifth form), part of the 0x82A08 process cluster. 185 lines. 5 callees. 1 caller (ProcessAndProcessAndProcess1828).
[entrypoint.functions."0x82A08D98"]
name = "ProcessAndProcessAndProcess2454_82A08D98"

# Process a structure by checking its state (two thousand four hundred and fifty-sixth form). It takes a structure (a: fields at a->4, a->0, and a->8); it checks the structure's state. This is the "process this data" routine (two thousand four hundred and fifty-sixth form), part of the 0x82A4A process cluster. 18 lines. 0 callees. 0 callers.
[entrypoint.functions."0x82A4A910"]
name = "ProcessAndProcessAndProcess2455_82A4A910"

# Process a structure by checking its state and driving many sub-operations (two thousand four hundred and fifty-seventh form). It takes a structure (a: fields at a->4, a->8, and a->12), a value (b), a value (c), and a structure (d: field at d->0); it checks the structure's state, and drives many sub-operations (AllocateChecked3, CheckedCopy, Free_SizeBucketed_Thunk, and more) based on the result. This is the "process this data" routine (two thousand four hundred and fifty-seventh form), part of the 0x82A86 process cluster. 368 lines. 4 callees. 4 callers (ProcessAndProcessAndProcess1213, sub_82A855A8, ProcessAndProcessAndProcess2019, and sub_821FFD68).
[entrypoint.functions."0x82A86778"]
name = "ProcessAndProcessAndProcess2456_82A86778"

# Process a structure by checking its state and driving sub-operations (two thousand four hundred and fifty-eighth form). It takes a value (a), a structure (b: fields at b->72 and b->24), a value (c), a value (d), and a value (e); it checks the structure's state, and drives sub-operations (itself recursively, sub_82AAB4B0, and more) based on the result. This is the "process this data" routine (two thousand four hundred and fifty-eighth form), part of the 0x82AAD process cluster. 197 lines. 2 callees (itself and sub_82AAB4B0). 1 caller (sub_82AAB6C8).
[entrypoint.functions."0x82AADD90"]
name = "ProcessAndProcessAndProcess2457_82AADD90"

# Process a structure by checking its state and driving many sub-operations (two thousand four hundred and fifty-ninth form). It takes a structure (a: field at a->4); it checks the structure's state, and drives many sub-operations (ConstructRefCounted, DestructRefCounted, ProcessAndProcessAndProcess1804, and more) based on the result. This is the "process this data" routine (two thousand four hundred and fifty-ninth form), part of the 0x82B06 process cluster. 414 lines. 3 callees. 1 caller (sub_82B07280).
[entrypoint.functions."0x82B06FA0"]
name = "ProcessAndProcessAndProcess2458_82B06FA0"

# Process a value by checking its state and driving many sub-operations (two thousand four hundred and sixtieth form). It takes a value (a); it checks the value's state, and drives many sub-operations (Free_SizeBucketed_Thunk, ProcessAndProcessAndProcess2208, ProcessAndProcessAndProcess2258, ProcessAndProcessAndProcess2265, ProcessAndProcessAndProcess2266, ProcessAndProcessAndProcess2458, and more) based on the result. This is the "process this data" routine (two thousand four hundred and sixtieth form), part of the 0x82B07 process cluster. 350 lines. 8 callees. 1 caller (sub_82A96A88).
[entrypoint.functions."0x82B07280"]
name = "ProcessAndProcessAndProcess2459_82B07280"

# Process a structure by checking its state and driving sub-operations (two thousand four hundred and sixty-first form). It takes a structure (a: fields at a->0, a->8, a->4, and a->12); it checks the structure's state, and drives sub-operations (Allocate_OrDbgBreak) based on the result. This is the "process this data" routine (two thousand four hundred and sixty-first form), part of the 0x821D4 process cluster. 86 lines. 1 callee. 1 caller (sub_821D5238).
[entrypoint.functions."0x821D4EE8"]
name = "ProcessAndProcessAndProcess2460_821D4EE8"

# Process a structure by checking its state and driving sub-operations (two thousand four hundred and sixty-second form). It takes a value (a) and a structure (b: field at b->16); it checks the structure's state, and drives sub-operations (sub_821D5238) based on the result. This is the "process this data" routine (two thousand four hundred and sixty-second form), part of the 0x821D5 process cluster. 40 lines. 1 callee. 0 callers.
[entrypoint.functions."0x821D51F8"]
name = "ProcessAndProcessAndProcess2461_821D51F8"

# Process a structure by checking its state and driving many sub-operations (two thousand four hundred and sixty-third form). It takes a structure (a: fields at a->0 and a->4), a value (b), and a value (c); it checks the structure's state, and drives many sub-operations (Allocate_OrDbgBreak, ProcessAndProcessAndProcess367, ProcessAndProcessAndProcess2460, and more) based on the result. This is the "process this data" routine (two thousand four hundred and sixty-third form), part of the 0x821D5 process cluster. 82 lines. 4 callees. 1 caller (ProcessAndProcessAndProcess2461).
[entrypoint.functions."0x821D5238"]
name = "ProcessAndProcessAndProcess2462_821D5238"

# Process a value by checking its state and driving many sub-operations (two thousand four hundred and sixty-fourth form). It takes a value (a), a value (b), and a value (c); it checks the value's state, and drives many sub-operations (ProcessAndCallMethods, ProcessAndCheckMethod, ProcessAndProcessAndProcess1864, ProcessAndProcessAndProcess1955, and more) based on the result. This is the "process this data" routine (two thousand four hundred and sixty-fourth form), part of the 0x821D5 process cluster. 70 lines. 4 callees. 1 caller (ProcessAndProcessAndProcess2462).
[entrypoint.functions."0x821D5BE0"]
name = "ProcessAndProcessAndProcess2463_821D5BE0"

# Process a structure by checking its state and driving many sub-operations (two thousand four hundred and sixty-fifth form). It takes a structure (a: field at a->0) and a structure (b: field at b->0); it checks the structure's state, and drives many sub-operations (ConstructRefCounted, DestructRefCounted, CopyAssign_RefCounted2, ProcessAndProcessAndProcess2119, and more) based on the result. This is the "process this data" routine (two thousand four hundred and sixty-fifth form), part of the 0x82265 process cluster. 87 lines. 4 callees. 6 callers (sub_829F8158, sub_82267168, sub_829F99D8, and more).
[entrypoint.functions."0x822651E0"]
name = "ProcessAndProcessAndProcess2464_822651E0"

# Process a structure by checking its state and driving many sub-operations (two thousand four hundred and sixty-sixth form). It takes a structure (a: field at a->0) and a structure (b: field at b->0); it checks the structure's state, and drives many sub-operations (Allocate_OrDbgBreak, Release_RefCounted, ProcessAndReallocateBuffer, ProcessAndProcessAndProcess2191, ProcessAndProcessAndProcess2192, and more) based on the result. This is the "process this data" routine (two thousand four hundred and sixty-sixth form), part of the 0x82B38 process cluster. 133 lines. 5 callees. 12 callers (sub_82901C60, sub_828FD288, sub_82408B88, and more).
[entrypoint.functions."0x82B38F30"]
name = "ProcessAndProcessAndProcess2465_82B38F30"

# Process a structure by checking its state and driving sub-operations (two thousand four hundred and sixty-seventh form). It takes a structure (a: fields at a->0, a->4, a->8, a->12, a->16, a->20, a->24, a->28, a->32, and a->36) and a structure (b: fields at b->0, b->4, b->8, b->12, b->16, b->20, b->24, b->28, b->32, and b->36); it checks the structure's state, and drives sub-operations (CopyAssign_RefCounted, sub_82B800A0, and more) based on the result. This is the "process this data" routine (two thousand four hundred and sixty-seventh form), part of the 0x82B7F process cluster. 149 lines. 2 callees. 1 caller (sub_82B7F3D8).
[entrypoint.functions."0x82B7FE28"]
name = "ProcessAndProcessAndProcess2466_82B7FE28"

# Process a value by checking its state and driving sub-operations (two thousand four hundred and sixty-eighth form). It takes a value (a); it checks the value's state, and drives sub-operations (ProcessAndProcessAndProcess2276) based on the result. This is the "process this data" routine (two thousand four hundred and sixty-eighth form), part of the 0x82B86 process cluster. 68 lines. 1 callee. 12 callers (sub_82A8CCB8, sub_82A700A8, sub_82A53018, and more).
[entrypoint.functions."0x82B86F38"]
name = "ProcessAndProcessAndProcess2467_82B86F38"

# Process a structure by checking its state (two thousand four hundred and sixty-ninth form). It takes a structure (a: fields at a->0 and a->64) and a value (b); it checks the structure's state. This is the "process this data" routine (two thousand four hundred and sixty-ninth form), part of the 0x82BFC process cluster. 14 lines. 0 callees. 1 caller (sub_82C10498).
[entrypoint.functions."0x82BFC8A0"]
name = "ProcessAndProcessAndProcess2468_82BFC8A0"

# Process a structure by checking its state (two thousand four hundred and seventieth form). It takes a structure (a: fields at a->0 and a->65) and a value (b); it checks the structure's state. This is the "process this data" routine (two thousand four hundred and seventieth form), part of the 0x82BFC process cluster. 14 lines. 0 callees. 1 caller (sub_82C10498).
[entrypoint.functions."0x82BFC8B8"]
name = "ProcessAndProcessAndProcess2469_82BFC8B8"

# Process a structure by checking its state (two thousand four hundred and seventy-first form). It takes a structure (a: fields at a->0, a->20, a->24, and a->28); it checks the structure's state. This is the "process this data" routine (two thousand four hundred and seventy-first form), part of the 0x82BFC process cluster. 48 lines. 0 callees. 5 callers (sub_82C3E570, sub_82C29C10, sub_82C10F18, sub_82C2E2F0, and sub_82C25CE0).
[entrypoint.functions."0x82BFC910"]
name = "ProcessAndProcessAndProcess2470_82BFC910"

# Process a value by checking its state (two thousand four hundred and seventy-second form). It takes a value (a); it checks the value's state. This is the "process this data" routine (two thousand four hundred and seventy-second form), part of the 0x82BFD process cluster. 8 lines. 0 callees. 0 callers.
[entrypoint.functions."0x82BFDB60"]
name = "ProcessAndProcessAndProcess2471_82BFDB60"

# Process a structure by checking its state and driving sub-operations (two thousand four hundred and seventy-third form). It takes a structure (a: fields at a->4, a->0, a->8, and a->12); it checks the structure's state, and drives sub-operations (ProcessAndVcallAndProcess10) based on the result. This is the "process this data" routine (two thousand four hundred and seventy-third form), part of the 0x82BFF process cluster. 113 lines. 1 callee. 0 callers.
[entrypoint.functions."0x82BFF0F0"]
name = "ProcessAndProcessAndProcess2472_82BFF0F0"

# Process a structure by checking its state and driving sub-operations (two thousand four hundred and seventy-fourth form). It takes a structure (a: fields at a->12 and a->0) and a value (b); it checks the structure's state, and drives sub-operations (Free_Thunk, ProcessAndProcessAndProcess1802, and more) based on the result. This is the "process this data" routine (two thousand four hundred and seventy-fourth form), part of the 0x82BFF process cluster. 58 lines. 2 callees. 0 callers.
[entrypoint.functions."0x82BFF1B8"]
name = "ProcessAndProcessAndProcess2473_82BFF1B8"

# Process a structure by checking its state (two thousand four hundred and seventy-fifth form). It takes a structure (a: fields at a->8, a->0, and a->4) and a structure (b: fields at b->104 and b->96); it checks the structure's state. This is the "process this data" routine (two thousand four hundred and seventy-fifth form), part of the 0x82C0B process cluster. 59 lines. 0 callees. 1 caller (sub_82C10D00).
[entrypoint.functions."0x82C0B958"]
name = "ProcessAndProcessAndProcess2474_82C0B958"

# Process a structure by checking its state and driving sub-operations (two thousand four hundred and seventy-sixth form). It takes a structure (a: fields at a->4, a->0, a->72, a->76, a->80, a->84, a->88, a->92, a->96, a->100, a->104, and a->108); it checks the structure's state, and drives sub-operations (ProcessAndProcessAndProcess1864) based on the result. This is the "process this data" routine (two thousand four hundred and seventy-sixth form), part of the 0x82C0C process cluster. 88 lines. 1 callee. 1 caller (sub_82C10D00).
[entrypoint.functions."0x82C0CEC8"]
name = "ProcessAndProcessAndProcess2475_82C0CEC8"

# Process a structure by checking its state and driving sub-operations (two thousand four hundred and seventy-seventh form). It takes a structure (a: field at a->4) and a value (b); it checks the structure's state, and drives sub-operations (BinarySearch) based on the result. This is the "process this data" routine (two thousand four hundred and seventy-seventh form), part of the 0x82C0D process cluster. 65 lines. 1 callee. 1 caller (sub_82C10D00).
[entrypoint.functions."0x82C0DF28"]
name = "ProcessAndProcessAndProcess2476_82C0DF28"

# Process a structure by checking its state (two thousand four hundred and seventy-eighth form). It takes a structure (a: fields at a->8, a->0, and a->4) and a structure (b: fields at b->84 and b->92); it checks the structure's state. This is the "process this data" routine (two thousand four hundred and seventy-eighth form), part of the 0x82C0E process cluster. 65 lines. 0 callees. 1 caller (sub_82C10F18).
[entrypoint.functions."0x82C0E388"]
name = "ProcessAndProcessAndProcess2477_82C0E388"

# Process a structure by checking its state and driving many sub-operations (two thousand four hundred and seventy-ninth form). It takes a structure (a: field at a->4) and a structure (b: fields at b->8 and b->0); it checks the structure's state, and drives many sub-operations (BinarySearch, ProcessAndValidateAndProcess3, ProcessAndProcessAndProcess1981, ProcessAndProcessAndProcess2194, ProcessAndProcessAndProcess2195, ProcessAndProcessAndProcess2468, ProcessAndProcessAndProcess2469, and more) based on the result. This is the "process this data" routine (two thousand four hundred and seventy-ninth form), part of the 0x82C10 process cluster. 214 lines. 7 callees. 1 caller (sub_82C10908).
[entrypoint.functions."0x82C10498"]
name = "ProcessAndProcessAndProcess2478_82C10498"

# Process a value by checking its state and driving many sub-operations (two thousand four hundred and eightieth form). It takes a value (a), a value (b), a value (c), a structure (d: fields at d->0 and d->12), and a value (e); it checks the value's state, and drives many sub-operations (FindTreeLeaf, ProcessAndProcessAndProcess1991, ProcessAndProcessAndProcess2478, and more) based on the result. This is the "process this data" routine (two thousand four hundred and eightieth form), part of the 0x82C10 process cluster. 152 lines. 3 callees. 2 callers (sub_82C10B90 and sub_82C10D00).
[entrypoint.functions."0x82C10908"]
name = "ProcessAndProcessAndProcess2479_82C10908"

# Process a value by checking its state and driving many sub-operations (two thousand four hundred and eighty-first form). It takes a value (a), a value (b), a value (c), and a structure (d: field at d->0); it checks the value's state, and drives many sub-operations (Allocate_OrDbgBreak, ProcessAndVcallAndProcess10, ProcessAndProcessAndProcess1955, ProcessAndProcessAndProcess367, ProcessAndProcessAndProcess2474, ProcessAndProcessAndProcess2475, ProcessAndProcessAndProcess2476, ProcessAndProcessAndProcess2479, and more) based on the result. This is the "process this data" routine (two thousand four hundred and eighty-first form), part of the 0x82C10 process cluster. 315 lines. 8 callees. 1 caller (sub_82C10F18).
[entrypoint.functions."0x82C10D00"]
name = "ProcessAndProcessAndProcess2480_82C10D00"

# Process a structure by checking its state and driving many sub-operations (two thousand four hundred and eighty-second form). It takes a structure (a: fields at a->0 and a->4), a structure (b: fields at b->52 and b->80), and a value (c); it checks the structure's state, and drives many sub-operations (ProcessAndVcallAndClear3, ProcessAndProcessAndProcess1802, ProcessAndProcessAndProcess1881, ProcessAndProcessAndProcess2060, ProcessAndProcessAndProcess2470, ProcessAndProcessAndProcess2477, ProcessAndProcessAndProcess2480, and more) based on the result. This is the "process this data" routine (two thousand four hundred and eighty-second form), part of the 0x82C10 process cluster. 144 lines. 7 callees. 0 callers.
[entrypoint.functions."0x82C10F18"]
name = "ProcessAndProcessAndProcess2481_82C10F18"

# Process a structure by checking its state and driving sub-operations (two thousand four hundred and eighty-third form). It takes a structure (a: fields at a->480, a->0, and a->484); it checks the structure's state, and drives sub-operations (Release_RefCounted, ProcessAndProcessAndProcess2171, and more) based on the result. This is the "process this data" routine (two thousand four hundred and eighty-third form), part of the 0x8217A process cluster. 126 lines. 2 callees. 1 caller (sub_82A00530).
[entrypoint.functions."0x8217AD80"]
name = "ProcessAndProcessAndProcess2482_8217AD80"

# Process a value by checking its state and driving many sub-operations (two thousand four hundred and eighty-fourth form). It takes a value (a), a value (b), and a structure (c: field at c->0); it checks the value's state, and drives many sub-operations (Allocate_OrDbgBreak, CopyAssign_RefCounted, SetString, ProcessAndFreeAndProcess9, and more) based on the result. This is the "process this data" routine (two thousand four hundred and eighty-fourth form), part of the 0x821D5 process cluster. 152 lines. 7 callees. 1 caller (sub_821E0B08).
[entrypoint.functions."0x821D5050"]
name = "ProcessAndProcessAndProcess2483_821D5050"

# Process a structure by checking its state and driving many sub-operations (two thousand four hundred and eighty-fifth form). It takes a value (a), a structure (b: fields at b->40 and b->32), a value (c), and a value (d); it checks the structure's state, and drives many sub-operations (CriticalSection_EnterOrTry, DestructRefCounted, CopyAssign_RefCounted, ProcessAndVcallAndClear3, ProcessAndVcallAndProcess10, ProcessAndProcessAndProcess2483, and more) based on the result. This is the "process this data" routine (two thousand four hundred and eighty-fifth form), part of the 0x821E0 process cluster. 278 lines. 7 callees. 4 callers (sub_82306098, sub_829F8158, sub_825AAF30, and sub_821D9258).
[entrypoint.functions."0x821E0B08"]
name = "ProcessAndProcessAndProcess2484_821E0B08"

# Process a structure by checking its state and driving many sub-operations (two thousand four hundred and eighty-sixth form). It takes a value (a), a structure (b: fields at b->156 and b->160), and a value (c); it checks the structure's state, and drives many sub-operations (ProcessAndProcessAndProcess1802, ProcessAndProcessAndProcess1864, ProcessAndProcessAndProcess2194, ProcessAndProcessAndProcess2464, ProcessAndProcessAndProcess2484, and more) based on the result. This is the "process this data" routine (two thousand four hundred and eighty-sixth form), part of the 0x82306 process cluster. 64 lines. 5 callees. 12 callers (sub_82833A90, sub_82583140, sub_82583938, and more).
[entrypoint.functions."0x82306098"]
name = "ProcessAndProcessAndProcess2485_82306098"

# Process a value by checking its state and driving sub-operations (two thousand four hundred and eighty-seventh form). It takes a value (a) and a value (b); it checks the value's state, and drives sub-operations (Free_SizeBucketed_Thunk, ProcessAndProcessAndProcess2482, and more) based on the result. This is the "process this data" routine (two thousand four hundred and eighty-seventh form), part of the 0x82A00 process cluster. 50 lines. 2 callees. 0 callers.
[entrypoint.functions."0x82A00530"]
name = "ProcessAndProcessAndProcess2486_82A00530"

# Process a value by checking its state and driving many sub-operations (two thousand four hundred and eighty-eighth form). It takes a value (a) and a value (b); it checks the value's state, and drives many sub-operations (ProcessAndProcessAndProcess1794, ProcessAndProcessAndProcess1833, and more) based on the result. This is the "process this data" routine (two thousand four hundred and eighty-eighth form), part of the 0x82B38 process cluster. 163 lines. 3 callees. 8 callers (sub_82A3CCE8, sub_82AB0728, sub_82A46EA8, and more).
[entrypoint.functions."0x82B38380"]
name = "ProcessAndProcessAndProcess2487_82B38380"

# Process a structure by checking its state and driving sub-operations (two thousand four hundred and eighty-ninth form). It takes a structure (a: field at a->0) and a structure (b: field at b->0); it checks the structure's state, and drives sub-operations (FindUTF16Substring) based on the result. This is the "process this data" routine (two thousand four hundred and eighty-ninth form), part of the 0x82B38 process cluster. 56 lines. 1 callee. 6 callers (sub_82338EA0, ProcessAndProcessAndProcess2448, sub_829068D8, and more).
[entrypoint.functions."0x82B38B18"]
name = "ProcessAndProcessAndProcess2488_82B38B18"

# Process a structure by checking its state and driving many sub-operations (two thousand four hundred and ninetieth form). It takes a value (a) and a structure (b: fields at b->20, b->12, and b->4); it checks the structure's state, and drives many sub-operations (CopyAssign_RefCounted2, ProcessAndCopyStructRegion, Release_RefCounted, and more) based on the result. This is the "process this data" routine (two thousand four hundred and ninetieth form), part of the 0x82B7B process cluster. 1102 lines. 4 callees. 1 caller (sub_82B7C488).
[entrypoint.functions."0x82B7BCB0"]
name = "ProcessAndProcessAndProcess2489_82B7BCB0"

# Process a structure by checking its state and driving many sub-operations (two thousand four hundred and ninety-first form). It takes a structure (a: fields at a->4 and a->8); it checks the structure's state, and drives many sub-operations (DestructRefCounted, and more) based on the result. This is the "process this data" routine (two thousand four hundred and ninety-first form), part of the 0x82B7E process cluster. 130 lines. 4 callees. 1 caller (ProcessAndProcessAndProcess2489).
[entrypoint.functions."0x82B7E8D8"]
name = "ProcessAndProcessAndProcess2490_82B7E8D8"

# Process a structure by checking its state and driving many sub-operations (two thousand four hundred and ninety-second form). It takes a structure (a: fields at a->36, a->20, and a->0) and a value (b); it checks the structure's state, and drives many sub-operations (GetField833, and more) based on the result. This is the "process this data" routine (two thousand four hundred and ninety-second form), part of the 0x82C01 process cluster. 146 lines. 3 callees. 0 callers.
[entrypoint.functions."0x82C01930"]
name = "ProcessAndProcessAndProcess2491_82C01930"

# Process a value by checking its state and driving many sub-operations (two thousand four hundred and ninety-third form). It takes a value (a); it checks the value's state, and drives many sub-operations based on the result. This is the "process this data" routine (two thousand four hundred and ninety-third form), part of the 0x82CA5 process cluster. 144 lines. 4 callees. 1 caller (sub_82CA5860).
[entrypoint.functions."0x82CA5768"]
name = "ProcessAndProcessAndProcess2492_82CA5768"

# Process a value by checking its state and driving sub-operations (two thousand four hundred and ninety-fourth form). It takes a value (a); it checks the value's state, and drives sub-operations (ProcessAndProcessAndProcess2492) based on the result. This is the "process this data" routine (two thousand four hundred and ninety-fourth form), part of the 0x82CA5 process cluster. 9 lines. 1 callee. 12 callers (sub_82E6A1C8, sub_82E92610, sub_82EB9650, and more).
[entrypoint.functions."0x82CA5860"]
name = "ProcessAndProcessAndProcess2493_82CA5860"

# Process a value by checking its state and driving many sub-operations (two thousand four hundred and ninety-fifth form). It takes a value (a), a value (b), and a value (c); it checks the value's state, and drives many sub-operations based on the result. This is the "process this data" routine (two thousand four hundred and ninety-fifth form), part of the 0x82CB0 process cluster. 109 lines. 2 callees. 2 callers (ProcessAndProcessAndProcess2492 and sub_82CA9828).
[entrypoint.functions."0x82CB0320"]
name = "ProcessAndProcessAndProcess2494_82CB0320"

# Process a value by checking its state and driving many sub-operations (two thousand four hundred and ninety-sixth form). It takes a value (a), a value (b), a value (c), a value (d), and a value (e); it checks the value's state, and drives many sub-operations (MemCpy_SdkRuntime, and more) based on the result. This is the "process this data" routine (two thousand four hundred and ninety-sixth form), part of the 0x82CB0 process cluster. 1380 lines. 4 callees. 3 callers (sub_82CB6E30, ProcessAndProcessAndProcess2494, and sub_82CB6D88).
[entrypoint.functions."0x82CB0430"]
name = "ProcessAndProcessAndProcess2495_82CB0430"

# Process a structure by checking its state (two thousand four hundred and ninety-seventh form). It takes a structure (a: fields at a->0, a->10, a->2, and a->6); it checks the structure's state. This is the "process this data" routine (two thousand four hundred and ninety-seventh form), part of the 0x82CB9 process cluster. 1033 lines. 0 callees. 3 callers (sub_82CB6D88, ProcessAndProcessAndProcess2494, and sub_830036F0).
[entrypoint.functions."0x82CB9098"]
name = "ProcessAndProcessAndProcess2496_82CB9098"

# Process a value by checking its state and driving sub-operations (two thousand four hundred and ninety-eighth form). It takes a value (a); it checks the value's state, and drives sub-operations (ProcessAndCallMethods, ProcessAndCheckMethod, and more) based on the result. This is the "process this data" routine (two thousand four hundred and ninety-eighth form), part of the 0x82260 process cluster. 69 lines. 2 callees. 1 caller (sub_82260790).
[entrypoint.functions."0x82260638"]
name = "ProcessAndProcessAndProcess2497_82260638"

# Process a structure by checking its state and driving sub-operations (two thousand four hundred and ninety-ninth form). It takes a value (a), a structure (b: field at b->4), and a value (c); it checks the structure's state, and drives sub-operations (DestructRefCounted, Free_SizeBucketed_Thunk, and more) based on the result. This is the "process this data" routine (two thousand four hundred and ninety-ninth form), part of the 0x82260 process cluster. 129 lines. 2 callees. 2 callers (sub_829F9128 and ProcessAndProcessAndProcess506).
[entrypoint.functions."0x822606B0"]
name = "ProcessAndProcessAndProcess2498_822606B0"

# Process a structure by setting its vtable and driving sub-operations (two thousand four hundred and fiftieth form). It takes a structure (a) and a value (b); it sets the structure's vtable pointers, and drives sub-operations (ProcessAndProcessAndProcess2497, Free_Thunk, and more) based on the result. This is the "process this data" routine (two thousand four hundred and fiftieth form), part of the 0x82260 process cluster. 70 lines. 2 callees. 0 callers (vtable-dispatched).
[entrypoint.functions."0x82260790"]
name = "ProcessAndProcessAndProcess2499_82260790"

# Process a value by checking its state and driving sub-operations (two thousand four hundred and fifty-first form). It takes a value (a), a structure (b: field at b->20), a value (c), and a value (d); it checks the value's state, and drives sub-operations (Buffer_CopyOrZero_Error, ProcessValue_FlagNonZero, and more) based on the result. This is the "process this data" routine (two thousand four hundred and fifty-first form), part of the 0x82374 process cluster. 152 lines. 4 callees. 2 callers (sub_82B3EB30 and ProcessAndProcessAndProcess124).
[entrypoint.functions."0x82374F20"]
name = "ProcessAndProcessAndProcess2500_82374F20"

# Process a value by checking its state and driving many sub-operations (two thousand four hundred and fifty-second form). It takes a value (a), a value (b), a value (c), a value (d), and a value (e); it allocates a large local buffer, and drives many sub-operations (RangeUpdate, ProcessAndProcessAndProcess2500, StrLenUTF16, and more) based on the result. This is the "process this data" routine (two thousand four hundred and fifty-second form), part of the 0x82B3E process cluster. 379 lines. 13 callees. 1 caller (sub_82B3EDD0).
[entrypoint.functions."0x82B3EB30"]
name = "ProcessAndProcessAndProcess2501_82B3EB30"

# Process a value by checking its state and driving sub-operations (two thousand four hundred and fifty-third form). It takes a value (a), a value (b), and a value (c); it compares the values' state, and drives sub-operations (sub_82B14958) based on the result. This is the "process this data" routine (two thousand four hundred and fifty-third form), part of the 0x82B14 process cluster. 118 lines. 1 callee. 1 caller (sub_82B14830).
[entrypoint.functions."0x82B14B00"]
name = "ProcessAndProcessAndProcess2502_82B14B00"

# Process a structure by checking its state and driving sub-operations (two thousand four hundred and fifty-fourth form). It takes a structure (a: field at a->4); it resets the structure's pointers, drives sub-operations (Free_Thunk, and more) based on the result, and makes a virtual call. This is the "process this data" routine (two thousand four hundred and fifty-fourth form), part of the 0x82BFF process cluster. 76 lines. 1 callee. 0 callers (vtable-dispatched).
[entrypoint.functions."0x82BFF690"]
name = "ProcessAndProcessAndProcess2503_82BFF690"

# Process a structure by checking its state and driving sub-operations (two thousand four hundred and fifty-fifth form). It takes a structure (a); if the structure is non-null, it checks the structure's state, and drives sub-operations (a virtual call, vtable[36], with argument 1) based on the result. This is the "process this data" routine (two thousand four hundred and fifty-fifth form), part of the 0x82C0B process cluster. 17 lines. 0 callees. 0 callers (vtable-dispatched).
[entrypoint.functions."0x82C0B268"]
name = "ProcessAndProcessAndProcess2504_82C0B268"

# Process a structure by checking its state (two thousand four hundred and fifty-sixth form). It takes a value (a) and a structure (b: fields at b->104 and b->96); it compares the structure's state, and unlinks it from a linked list based on the result. This is the "process this data" routine (two thousand four hundred and fifty-sixth form), part of the 0x82C0B process cluster. 81 lines. 0 callees. 1 caller (ProcessAndProcessAndProcess1563).
[entrypoint.functions."0x82C0B9C8"]
name = "ProcessAndProcessAndProcess2505_82C0B9C8"

# Process a structure by checking its state (two thousand four hundred and fifty-seventh form). It takes a value (a) and a structure (b: fields at b->92 and b->84); it compares the structure's state, and unlinks it from a linked list based on the result. This is the "process this data" routine (two thousand four hundred and fifty-seventh form), part of the 0x82C0B process cluster. 81 lines. 0 callees. 1 caller (sub_82C0E6E8).
[entrypoint.functions."0x82C0BA60"]
name = "ProcessAndProcessAndProcess2506_82C0BA60"

# Process a structure by checking its state and driving sub-operations (two thousand four hundred and fifty-eighth form). It takes a structure (a: field at a->4) and a value (b); it checks the structure's state, and drives sub-operations (BinarySearch) based on the result, decrementing a counter. This is the "process this data" routine (two thousand four hundred and fifty-eighth form), part of the 0x82C0D process cluster. 69 lines. 1 callee. 1 caller (sub_82C0E6E8).
[entrypoint.functions."0x82C0DF98"]
name = "ProcessAndProcessAndProcess2507_82C0DF98"

# Process a structure by checking its state and driving sub-operations (two thousand four hundred and fifty-ninth form). It takes a structure (a: fields at a->132, a->120, a->116, and more); it checks the structure's state, and drives sub-operations (ProcessAndProcessAndProcess2506, ProcessAndProcessAndProcess2507, and more) based on the result, resetting the structure's pointers. This is the "process this data" routine (two thousand four hundred and fifty-ninth form), part of the 0x82C0E process cluster. 153 lines. 3 callees. 1 caller (sub_82C0F648).
[entrypoint.functions."0x82C0E6E8"]
name = "ProcessAndProcessAndProcess2508_82C0E6E8"

# Process a structure by checking its state and driving sub-operations (two thousand four hundred and sixtieth form). It takes a structure (a) and a value (b); it checks the structure's state, and drives sub-operations (ProcessAndProcessAndProcess2508, Free_Thunk, and more) based on the result, optionally freeing the structure. This is the "process this data" routine (two thousand four hundred and sixtieth form), part of the 0x82C0F process cluster. 50 lines. 2 callees. 0 callers (vtable-dispatched).
[entrypoint.functions."0x82C0F648"]
name = "ProcessAndProcessAndProcess2509_82C0F648"

# Process a value by checking its state and driving sub-operations (two thousand four hundred and sixty-first form). It takes a value (a), a value (b), a value (c), and a value (d); it allocates a small structure, initializes it, and drives sub-operations (Allocate_OrDbgBreak, ProcessAndVcallAndProcess10, and more) based on the result. This is the "process this data" routine (two thousand four hundred and sixty-first form), part of the 0x82C21 process cluster. 153 lines. 3 callees. 1 caller (sub_82C217B8).
[entrypoint.functions."0x82C216A8"]
name = "ProcessAndProcessAndProcess2510_82C216A8"

# Process a value by checking its state and driving sub-operations (two thousand four hundred and sixty-second form). It uses global state to check its state, and drives sub-operations (ProcessAndAllocateAndProcess2) based on the result. This is the "process this data" routine (two thousand four hundred and sixty-second form), part of the 0x82E78 process cluster. 69 lines. 1 callee. 3 callers (sub_82E78B38, sub_82E78DA8, and sub_82E78A70).
[entrypoint.functions."0x82E788B8"]
name = "ProcessAndProcessAndProcess2511_82E788B8"

# Process a value by checking its state and driving sub-operations (two thousand four hundred and sixty-third form). It uses global state to check its state, and drives sub-operations (ProcessAndAllocateAndProcess2) based on the result. This is the "process this data" routine (two thousand four hundred and sixty-third form), part of the 0x82E7C process cluster. 69 lines. 1 callee. 4 callers (sub_82E7C5F8, sub_82E7C418, sub_82E7EED0, and sub_82E7ED40).
[entrypoint.functions."0x82E7C3A0"]
name = "ProcessAndProcessAndProcess2512_82E7C3A0"

# Process a value by checking its state (two thousand four hundred and sixty-fourth form). It takes a structure (a: field at a->4) and a structure (b: field at b->0); it traverses a linked list, comparing each node's state, and returns the matching node based on the result. This is the "process this data" routine (two thousand four hundred and sixty-fourth form), part of the 0x82C10 process cluster. 87 lines. 0 callees. 5 callers (UIFont_LookupGlyph, sub_82C10868, sub_82C386C0, sub_82C10770, and sub_82C12750).
[entrypoint.functions."0x82C10608"]
name = "ProcessAndProcessAndProcess2513_82C10608"

# Process a value by checking its state and driving many sub-operations (two thousand four hundred and sixty-fifth form). It takes a value (a), a structure (b: field at b->8), a value (c), a value (d), and a value (e); it checks the value's state, and drives many sub-operations (Allocate_OrDbgBreak, SetString, Free_Thunk, and more) based on the result. This is the "process this data" routine (two thousand four hundred and sixty-fifth form), part of the 0x82C11 process cluster. 467 lines. 8 callees. 2 callers (sub_82C11EB8 and sub_82C120D8).
[entrypoint.functions."0x82C11868"]
name = "ProcessAndProcessAndProcess2514_82C11868"

# Process a value by checking its state and driving sub-operations (two thousand four hundred and sixty-sixth form). It takes a value (a), a structure (b: fields at b->8 and b->4), a value (c), and a value (d); it checks the value's state, and drives sub-operations (ProcessAndProcessAndProcess2514, and more) based on the result. This is the "process this data" routine (two thousand four hundred and sixty-sixth form), part of the 0x82C12 process cluster. 298 lines. 4 callees. 1 caller (sub_82C122E8).
[entrypoint.functions."0x82C120D8"]
name = "ProcessAndProcessAndProcess2515_82C120D8"

# Process a value by checking its state and driving sub-operations (two thousand four hundred and sixty-seventh form). It takes a structure (a: field at a->4) and a structure (b: field at b->0); it traverses a linked list, and drives sub-operations (ProcessAndProcessAndProcess2515) based on the result. This is the "process this data" routine (two thousand four hundred and sixty-seventh form), part of the 0x82C12 process cluster. 148 lines. 1 callee. 1 caller (sub_82C12750).
[entrypoint.functions."0x82C122E8"]
name = "ProcessAndProcessAndProcess2516_82C122E8"

# Process a value by checking its state and driving many sub-operations (two thousand four hundred and sixty-eighth form). It takes a value (a), a float (b), a float (c), and a float (d); it constructs a refcounted object, and drives many sub-operations (ConstructRefCounted, ProcessAndProcessAndProcess2513, ProcessAndProcessAndProcess2516, and more) based on the result. This is the "process this data" routine (two thousand four hundred and sixty-eighth form), part of the 0x82C12 process cluster. 160 lines. 5 callees. 1 caller (ProcessAndProcessAndProcess2429).
[entrypoint.functions."0x82C12750"]
name = "ProcessAndProcessAndProcess2517_82C12750"

# Process a value by checking its state (two thousand four hundred and sixty-ninth form). It takes a value (a), a value (b), and a value (c); it checks the value's state, and sets two fields at the value's start based on the result. This is the "process this data" routine (two thousand four hundred and sixty-ninth form), part of the 0x82FFC process cluster. 23 lines. 0 callees. 12 callers.
[entrypoint.functions."0x82FFCAE8"]
name = "ProcessAndProcessAndProcess2518_82FFCAE8"

# Process a value by checking its state and driving sub-operations (two thousand four hundred and seventieth form). It takes a structure (a: fields at a->20, a->12, and a->4), a value (b), a value (c), and a value (d); it checks the value's state, and drives sub-operations (ProcessAndCopyStructRegion) based on the result. This is the "process this data" routine (two thousand four hundred and seventieth form), part of the 0x82A1D process cluster. 216 lines. 1 callee. 1 caller (ProcessAndProcessAndProcess2449).
[entrypoint.functions."0x82A1D440"]
name = "ProcessAndProcessAndProcess2519_82A1D440"

# Process a value by checking its state (two thousand four hundred and seventy-first form). It takes a value (a), a value (b), and a value (c); it checks the value's state, and packs the value's bits based on the result. This is the "process this data" routine (two thousand four hundred and seventy-first form), part of the 0x82B91 process cluster. 78 lines. 0 callees. 0 callers (vtable-dispatched).
[entrypoint.functions."0x82B91440"]
name = "ProcessAndProcessAndProcess2520_82B91440"

# Process a value by checking its state (two thousand four hundred and seventy-second form). It takes a value (a), a value (b), and a value (c); it checks the value's state, and packs the value's bits based on the result. This is the "process this data" routine (two thousand four hundred and seventy-second form), part of the 0x82B91 process cluster. 74 lines. 0 callees. 0 callers (vtable-dispatched).
[entrypoint.functions."0x82B91520"]
name = "ProcessAndProcessAndProcess2521_82B91520"

# Process a value by checking its state (two thousand four hundred and seventy-third form). It takes a value (a), a value (b), and a value (c); it checks the value's state, and packs the value's bits based on the result. This is the "process this data" routine (two thousand four hundred and seventy-third form), part of the 0x82B91 process cluster. 78 lines. 0 callees. 0 callers (vtable-dispatched).
[entrypoint.functions."0x82B915D8"]
name = "ProcessAndProcessAndProcess2522_82B915D8"

# Process a value by checking its state (two thousand four hundred and seventy-fourth form). It takes a structure (a: field at a->24), a value (b), and a value (c); it checks the value's state, and packs the value's bits (scaling by a constant) based on the result. This is the "process this data" routine (two thousand four hundred and seventy-fourth form), part of the 0x82B91 process cluster. 50 lines. 0 callees. 0 callers (vtable-dispatched).
[entrypoint.functions."0x82B91690"]
name = "ProcessAndProcessAndProcess2523_82B91690"

# Process a value by checking its state (two thousand four hundred and seventy-fifth form). It takes a value (a), a value (b), and a value (c); it checks the value's state, and packs the value's bits based on the result. This is the "process this data" routine (two thousand four hundred and seventy-fifth form), part of the 0x82B91 process cluster. 60 lines. 0 callees. 0 callers (vtable-dispatched).
[entrypoint.functions."0x82B91778"]
name = "ProcessAndProcessAndProcess2524_82B91778"

# Process a value by checking its state (two thousand four hundred and seventy-sixth form). It takes a value (a), a value (b), and a value (c); it checks the value's state, and packs the value's bits based on the result. This is the "process this data" routine (two thousand four hundred and seventy-sixth form), part of the 0x82B91 process cluster. 60 lines. 0 callees. 0 callers (vtable-dispatched).
[entrypoint.functions."0x82B917F8"]
name = "ProcessAndProcessAndProcess2525_82B917F8"

# Process a value by checking its state (two thousand four hundred and seventy-seventh form). It takes a value (a), a value (b), and a value (c); it checks the value's state, and packs the value's bits based on the result. This is the "process this data" routine (two thousand four hundred and seventy-seventh form), part of the 0x82B91 process cluster. 30 lines. 0 callees. 0 callers (vtable-dispatched).
[entrypoint.functions."0x82B918F8"]
name = "ProcessAndProcessAndProcess2526_82B918F8"

# Process a value by checking its state (two thousand four hundred and seventy-eighth form). It takes a value (a), a value (b), and a value (c); it checks the value's state, and packs the value's bits based on the result. This is the "process this data" routine (two thousand four hundred and seventy-eighth form), part of the 0x82B91 process cluster. 30 lines. 0 callees. 0 callers (vtable-dispatched).
[entrypoint.functions."0x82B91950"]
name = "ProcessAndProcessAndProcess2527_82B91950"

# Process a value by checking its state (two thousand four hundred and seventy-ninth form). It takes a value (a), a value (b), and a value (c); it checks the value's state, and packs the value's bits based on the result. This is the "process this data" routine (two thousand four hundred and seventy-ninth form), part of the 0x82B91 process cluster. 30 lines. 0 callees. 0 callers (vtable-dispatched).
[entrypoint.functions."0x82B919A8"]
name = "ProcessAndProcessAndProcess2528_82B919A8"

# Process a value by checking its state (two thousand four hundred and eightieth form). It takes a value (a), a value (b), and a value (c); it checks the value's state, and packs the value's bits based on the result. This is the "process this data" routine (two thousand four hundred and eightieth form), part of the 0x82B91 process cluster. 30 lines. 0 callees. 0 callers (vtable-dispatched).
[entrypoint.functions."0x82B91A00"]
name = "ProcessAndProcessAndProcess2529_82B91A00"

# Process a structure by checking its state (two thousand four hundred and eighty-first form). It takes a structure (a), a structure (b: fields at b->0, b->16, and b->8), and a structure (c: field at c->0); it checks the structure's state, and initializes the structure's fields based on the result. This is the "process this data" routine (two thousand four hundred and eighty-first form), part of the 0x82CD7 process cluster. 146 lines. 0 callees. 1 caller (sub_82CD8050).
[entrypoint.functions."0x82CD7D98"]
name = "ProcessAndProcessAndProcess2530_82CD7D98"

# Process a value by checking its state (two thousand four hundred and eighty-second form). It takes a value (a) and a value (b); it checks the value's state, and stores a pointer (or zero) based on the result. This is the "process this data" routine (two thousand four hundred and eighty-second form), part of the 0x82CD7 process cluster. 15 lines. 0 callees. 0 callers (vtable-dispatched).
[entrypoint.functions."0x82CD7EA0"]
name = "ProcessAndProcessAndProcess2531_82CD7EA0"

# Process a value by checking its state and driving sub-operations (two thousand four hundred and eighty-third form). It takes a value (a), a structure (b: field at b->0), and a value (c); it checks the value's state, and drives sub-operations (a virtual call, ProcessAndProcessAndProcess2530, and more) based on the result. This is the "process this data" routine (two thousand four hundred and eighty-third form), part of the 0x82CD8 process cluster. 78 lines. 1 callee. 2 callers (sub_82CDB5E0 and ProcessAndProcessAndProcess2089).
[entrypoint.functions."0x82CD8050"]
name = "ProcessAndProcessAndProcess2532_82CD8050"

# Process a value by checking its state and driving sub-operations (two thousand four hundred and eighty-fourth form). It uses global state to check its state, and drives sub-operations (ProcessAndAllocateAndProcess2) based on the result. This is the "process this data" routine (two thousand four hundred and eighty-fourth form), part of the 0x82E80 process cluster. 69 lines. 1 callee. 4 callers (sub_82E824A0, sub_82E814F8, sub_82E81128, and sub_82E820D8).
[entrypoint.functions."0x82E80FA8"]
name = "ProcessAndProcessAndProcess2533_82E80FA8"

# Process a value by checking its state (two thousand four hundred and eighty-fifth form). It takes a value (a); it checks the value's state, and returns a pointer to the value's field based on the result. This is the "process this data" routine (two thousand four hundred and eighty-fifth form), part of the 0x82EE1 process cluster. 10 lines. 0 callees. 12 callers.
[entrypoint.functions."0x82EE1CF8"]
name = "ProcessAndProcessAndProcess2534_82EE1CF8"

# Process a value by checking its state (two thousand four hundred and eighty-sixth form). It takes a value (a); it checks the value's state, and returns a pointer to the value's field based on the result. This is the "process this data" routine (two thousand four hundred and eighty-sixth form), part of the 0x82FFE process cluster. 10 lines. 0 callees. 12 callers.
[entrypoint.functions."0x82FFE3C8"]
name = "ProcessAndProcessAndProcess2535_82FFE3C8"

# Process a value by checking its state and driving many sub-operations (two thousand four hundred and eighty-seventh form). It takes a structure (a) and a structure (b: fields at b->4, b->0, and b->8); it checks the value's state, and drives many sub-operations (Allocate_OrDbgBreak, MemCpy_SdkRuntime, MemCpy_Vectorized, and more) based on the result. This is the "process this data" routine (two thousand four hundred and eighty-seventh form), part of the 0x82B84 process cluster. 149 lines. 4 callees. 2 callers (sub_82B8B330 and ProcessAndProcessAndProcess2450).
[entrypoint.functions."0x82B84550"]
name = "ProcessAndProcessAndProcess2536_82B84550"

# Process a structure by checking its state (two thousand four hundred and eighty-eighth form). It takes a structure (a: field at a->24) and a value (b); it checks the structure's state, and sets or clears a bit based on the result. This is the "process this data" routine (two thousand four hundred and eighty-eighth form), part of the 0x82C5D process cluster. 30 lines. 0 callees. 2 callers (ProcessAndProcessAndProcess995 and sub_82C5B700).
[entrypoint.functions."0x82C5D170"]
name = "ProcessAndProcessAndProcess2537_82C5D170"

# Process a structure by checking its state (two thousand four hundred and eighty-ninth form). It takes a structure (a: fields at a->0, a->8, and a->5) and a value (b); it checks the structure's state, and computes a size based on the result, storing it. This is the "process this data" routine (two thousand four hundred and eighty-ninth form), part of the 0x82CD7 process cluster. 50 lines. 0 callees. 2 callers (sub_82CDA758 and ProcessAndProcessAndProcess2087).
[entrypoint.functions."0x82CD7FF8"]
name = "ProcessAndProcessAndProcess2538_82CD7FF8"

# Process a value by checking its state and driving sub-operations (two thousand four hundred and ninetieth form). It takes a value (a), a value (b), and a value (c); it checks the values' state, and drives sub-operations (two virtual calls, vtable[36] and vtable[32], ProcessAndProcessAndProcess1815, and more) based on the result. This is the "process this data" routine (two thousand four hundred and ninetieth form), part of the 0x823F9 process cluster. 91 lines. 1 callee. 12 callers.
[entrypoint.functions."0x823F9040"]
name = "ProcessAndProcessAndProcess2539_823F9040"

# Process a structure by checking its state (two thousand four hundred and ninety-first form). It takes a structure (a: field at a->16); it checks the structure's state, and returns the structure's field based on the result. This is the "process this data" routine (two thousand four hundred and ninety-first form), part of the 0x826B4 process cluster. 5 lines. 0 callees. 12 callers.
[entrypoint.functions."0x826B4B60"]
name = "ProcessAndProcessAndProcess2540_826B4B60"

# Process a value by checking its state and driving sub-operations (two thousand four hundred and ninety-second form). It takes a value (a), a value (b), and a value (c); it compares the values' state, and drives sub-operations (sub_82A80708) based on the result. This is the "process this data" routine (two thousand four hundred and ninety-second form), part of the 0x82A80 process cluster. 118 lines. 1 callee. 1 caller (sub_82A80798).
[entrypoint.functions."0x82A808C0"]
name = "ProcessAndProcessAndProcess2541_82A808C0"

# Process a value by checking its state and driving sub-operations (two thousand four hundred and ninety-third form). It takes a structure (a: field at a->4), a structure (b: field at b->4), and a value (c); it compares the structures' state, and drives sub-operations (ProcessAndProcessAndProcess2250) based on the result. This is the "process this data" routine (two thousand four hundred and ninety-third form), part of the 0x82B3D process cluster. 308 lines. 1 callee. 1 caller (sub_82B3D988).
[entrypoint.functions."0x82B3DC48"]
name = "ProcessAndProcessAndProcess2542_82B3DC48"

# Process a structure by checking its state (two thousand four hundred and ninety-fourth form). It takes a structure (a: field at a->24) and a value (b); it checks the structure's state, and sets or clears a bit based on the result. This is the "process this data" routine (two thousand four hundred and ninety-fourth form), part of the 0x82C5D process cluster. 30 lines. 0 callees. 1 caller (ProcessAndProcessAndProcess995).
[entrypoint.functions."0x82C5D1A0"]
name = "ProcessAndProcessAndProcess2543_82C5D1A0"

# Process a structure by checking its state and driving sub-operations (two thousand four hundred and ninety-fifth form). It takes a structure (a: fields at a->24, a->4, a->0, and a->28); it checks the structure's state, and drives sub-operations (a virtual call) based on the result. This is the "process this data" routine (two thousand four hundred and ninety-fifth form), part of the 0x82C60 process cluster. 40 lines. 0 callees. 2 callers (sub_82C60AB0 and sub_82C60820).
[entrypoint.functions."0x82C607E0"]
name = "ProcessAndProcessAndProcess2544_82C607E0"

# Process a value by checking its state (two thousand four hundred and ninety-sixth form). It takes a value (a), a value (b), and a value (c); it checks the value's state, and performs an atomic compare-and-swap under the global lock, returning whether the swap succeeded based on the result. This is the "process this data" routine (two thousand four hundred and ninety-sixth form), part of the 0x82C61 process cluster. 62 lines. 0 callees. 2 callers (ProcessAndProcessAndProcess622 and sub_82C5C768).
[entrypoint.functions."0x82C61128"]
name = "ProcessAndProcessAndProcess2545_82C61128"

# Process a structure by checking its state (two thousand four hundred and ninety-seventh form). It takes a structure (a: field at a->36) and a structure (b); it checks the structure's state, and sets a vtable pointer and a field based on the result. This is the "process this data" routine (two thousand four hundred and ninety-seventh form), part of the 0x82CFC process cluster. 25 lines. 0 callees. 0 callers (vtable-dispatched).
[entrypoint.functions."0x82CFC8A0"]
name = "ProcessAndProcessAndProcess2546_82CFC8A0"

# Process a value by checking its state and driving sub-operations (two thousand four hundred and ninety-eighth form). It takes a value (a) and a value (b); it allocates a small structure, and drives sub-operations (ProcessAndProcessAndProcess1657, a virtual call, and more) based on the result. This is the "process this data" routine (two thousand four hundred and ninety-eighth form), part of the 0x82CFE process cluster. 115 lines. 2 callees. 1 caller (sub_82CFE7F0).
[entrypoint.functions."0x82CFEB00"]
name = "ProcessAndProcessAndProcess2547_82CFEB00"

# Process a value by checking its state and driving sub-operations (two thousand four hundred and ninety-ninth form). It takes a value (a), a value (b), a value (c), a value (d), and a value (e); it allocates a small structure, and drives sub-operations (ProcessAndProcessAndProcess1657, and more) based on the result. This is the "process this data" routine (two thousand four hundred and ninety-ninth form), part of the 0x82CFE process cluster. 135 lines. 2 callees. 4 callers (sub_82CFF878, sub_82CFDA90, sub_82D03DA8, and sub_82CFCDE8).
[entrypoint.functions."0x82CFEBC0"]
name = "ProcessAndProcessAndProcess2548_82CFEBC0"

# Process a value by checking its state and driving sub-operations (two thousand four hundred and one hundredth form). It uses global state to check its state, and drives sub-operations (ProcessAndAllocateAndProcess2) based on the result. This is the "process this data" routine (two thousand four hundred and one hundredth form), part of the 0x82E78 process cluster. 69 lines. 1 callee. 4 callers (sub_82E789A8, sub_82E7D388, sub_82E78CE0, and sub_82E7D0D8).
[entrypoint.functions."0x82E78930"]
name = "ProcessAndProcessAndProcess2549_82E78930"

# Process a value by checking its state (two thousand four hundred and one hundred and first form). It takes a value (a); it checks the value's state, and returns a pointer to the value's field based on the result. This is the "process this data" routine (two thousand four hundred and one hundred and first form), part of the 0x82FFE process cluster. 10 lines. 0 callees. 9 callers.
[entrypoint.functions."0x82FFE3B8"]
name = "ProcessAndProcessAndProcess2550_82FFE3B8"

# Process a structure by checking its state (two thousand four hundred and one hundred and second form). It takes a structure (a: fields at a->8 and a->4); it checks the structure's state, and manipulates the structure's linked-list pointers based on the result. This is the "process this data" routine (two thousand four hundred and one hundred and second form), part of the 0x827E0 process cluster. 71 lines. 0 callees. 2 callers (sub_827E03E0 and ProcessAndProcessAndProcess2283).
[entrypoint.functions."0x827E0838"]
name = "ProcessAndProcessAndProcess2551_827E0838"

# Process a value by checking its state and driving many sub-operations (two thousand four hundred and one hundred and third form). It takes a value (a) and a value (b); it enters a critical section, and drives many sub-operations (CriticalSection_EnterOrTry, CopyAssign_RefCounted, DestructRefCounted, and more) based on the result, including an atomic increment. This is the "process this data" routine (two thousand four hundred and one hundred and third form), part of the 0x82AA3 process cluster. 167 lines. 6 callees. 1 caller (sub_82A9FBB8).
[entrypoint.functions."0x82AA33E0"]
name = "ProcessAndProcessAndProcess2552_82AA33E0"

# Process a value by checking its state (two thousand four hundred and one hundred and fourth form). It takes no meaningful arguments; it checks the value's state, and returns a global pointer based on the result. This is the "process this data" routine (two thousand four hundred and one hundred and fourth form), part of the 0x82C20 process cluster. 8 lines. 0 callees. 0 callers (vtable-dispatched or referenced elsewhere).
[entrypoint.functions."0x82C20C90"]
name = "ProcessAndProcessAndProcess2553_82C20C90"

# Process a value by checking its state and driving sub-operations (two thousand four hundred and one hundred and fifth form). It takes a value (a) and a value (b); it checks the value's state, and drives sub-operations (RtlEnterCriticalSection, sub_82CDD4C8, RtlLeaveCriticalSection, and more) based on the result, all under a critical section. This is the "process this data" routine (two thousand four hundred and one hundred and fifth form), part of the 0x82CD3 process cluster. 59 lines. 3 callees. 5 callers.
[entrypoint.functions."0x82CD3308"]
name = "ProcessAndProcessAndProcess2554_82CD3308"

# Process a value by checking its state and driving sub-operations (two thousand four hundred and one hundred and sixth form). It takes no meaningful arguments; it checks the value's state, and drives sub-operations (ProcessAndProcessAndProcess2094) based on the result. This is the "process this data" routine (two thousand four hundred and one hundred and sixth form), part of the 0x82CDD process cluster. 5 lines. 1 callee. 0 callers (vtable-dispatched).
[entrypoint.functions."0x82CDD318"]
name = "ProcessAndProcessAndProcess2555_82CDD318"

# Process a value by checking its state and driving sub-operations (two thousand four hundred and one hundred and seventh form). It takes a structure (a: field at a->32) and a value (b); it checks the value's state, and drives sub-operations (ProcessAndProcessAndProcess2281, ProcessAndProcessAndProcess2093, and more) based on the result. This is the "process this data" routine (two thousand four hundred and one hundred and seventh form), part of the 0x82CDD process cluster. 63 lines. 3 callees. 1 caller (sub_82CDD4C8).
[entrypoint.functions."0x82CDD320"]
name = "ProcessAndProcessAndProcess2556_82CDD320"

# Process a value by checking its state and driving many sub-operations (two thousand four hundred and one hundred and eighth form). It takes a structure (a), a structure (b: field at b->10), and a value (c); it checks the value's state, and drives many sub-operations (ProcessAndProcessAndProcess2091, KeRaiseIrqlToDpcLevel, KeAcquireSpinLockAtRaisedIrql, and more) based on the result, using spin locks and IRQL. This is the "process this data" routine (two thousand four hundred and one hundred and eighth form), part of the 0x82CDD process cluster. 153 lines. 5 callees. 1 caller (sub_82CDD4C8).
[entrypoint.functions."0x82CDD388"]
name = "ProcessAndProcessAndProcess2557_82CDD388"

# Process a value by checking its state and driving many sub-operations (two thousand four hundred and one hundred and ninth form). It takes a value (a), a value (b), and a value (c); it checks the value's state, and drives many sub-operations (ProcessAndProcessAndProcess2281, ProcessAndProcessAndProcess2092, ProcessAndProcessAndProcess2556, and more) based on the result. This is the "process this data" routine (two thousand four hundred and one hundred and ninth form), part of the 0x82CDD process cluster. 157 lines. 5 callees. 1 caller (ProcessAndProcessAndProcess2554).
[entrypoint.functions."0x82CDD4C8"]
name = "ProcessAndProcessAndProcess2558_82CDD4C8"

# Process a value by checking its state and driving sub-operations (two thousand four hundred and one hundred and tenth form). It takes a value (a), a value (b), a value (c), and a structure (d); it checks the value's state, and drives sub-operations (MemSet_SdkRuntime, and more) based on the result, setting fields and checking bits. This is the "process this data" routine (two thousand four hundred and one hundred and tenth form), part of the 0x83005 process cluster. 78 lines. 1 callee. 2 callers (sub_82B1A348 and sub_82B8B960).
[entrypoint.functions."0x830053D0"]
name = "ProcessAndProcessAndProcess2559_830053D0"

# Process a value by checking its state and driving many sub-operations (two thousand four hundred and one hundred and eleventh form). It takes a structure (a) and a float (b); it checks the value's state, and drives many sub-operations (ProcessAndProcessAndProcess1120, Free_SizeBucketed_Thunk, and more) based on the result, looping over a table. This is the "process this data" routine (two thousand four hundred and one hundred and eleventh form), part of the 0x8322B process cluster. 201 lines. 2 callees. 12 callers.
[entrypoint.functions."0x8322BB68"]
name = "ProcessAndProcessAndProcess2560_8322BB68"

# Process a value by checking its state and driving many sub-operations (two thousand four hundred and one hundred and twelfth form). It takes a value (a), a structure (b: field at b->0), a mask (c), a value (d), a value (e), and a float (f); it checks the value's state, and drives many sub-operations (GetTimebase, MemCpy_SdkRuntime, ProcessAndProcessAndProcess1934, and more) based on the result. This is the "process this data" routine (two thousand four hundred and one hundred and twelfth form), part of the 0x82188 process cluster. 187 lines. 3 callees. 1 caller (ProcessAndProcessAndProcess1288).
[entrypoint.functions."0x82188F20"]
name = "ProcessAndProcessAndProcess2561_82188F20"

# Process a structure by checking its state (two thousand four hundred and one hundred and thirteenth form). It takes a structure (a: field at a->12) and a flag (b); it checks the structure's state, and sets or clears a bit in the structure based on the flag. This is the "process this data" routine (two thousand four hundred and one hundred and thirteenth form), part of the 0x82C5C process cluster. 15 lines. 0 callees. 3 callers (sub_82C5A780, sub_82C5CC60, and sub_82C5A950).
[entrypoint.functions."0x82C5C748"]
name = "ProcessAndProcessAndProcess2562_82C5C748"

# Process a structure by checking its state (two thousand four hundred and one hundred and fourteenth form). It takes a structure (a: fields at a->20, a->0, a->24, a->4, a->8, a->12, and a->16) and several values (b through g); it checks the structure's state, and stores the values into the structure's fields, defaulting some to a global pointer if zero, based on the result. This is the "process this data" routine (two thousand four hundred and one hundred and fourteenth form), part of the 0x82C5D process cluster. 30 lines. 0 callees. 1 caller (sub_82C5DAD8).
[entrypoint.functions."0x82C5D820"]
name = "ProcessAndProcessAndProcess2563_82C5D820"

# Process a value by checking its state and driving sub-operations (two thousand four hundred and one hundred and fifteenth form). It takes a value (a); it checks the value's state, and drives sub-operations (ProcessAndProcessAndProcess2563) based on the result. This is the "process this data" routine (two thousand four hundred and one hundred and fifteenth form), part of the 0x82C5D process cluster. 20 lines. 1 callee. 12 callers.
[entrypoint.functions."0x82C5DAD8"]
name = "ProcessAndProcessAndProcess2564_82C5DAD8"

# Process a value by checking its state and driving sub-operations (two thousand four hundred and one hundred and sixteenth form). It uses global state to check its state, and drives sub-operations (ProcessAndAllocateAndProcess2) based on the result. This is the "process this data" routine (two thousand four hundred and one hundred and sixteenth form), part of the 0x82E77 process cluster. 69 lines. 1 callee. 3 callers (sub_82E77390, sub_82E772C8, and sub_82E77508).
[entrypoint.functions."0x82E77110"]
name = "ProcessAndProcessAndProcess2565_82E77110"

# Process a value by checking its state and driving sub-operations (two thousand four hundred and one hundred and seventeenth form). It takes a value (a), a value (b), a value (c), and a value (d); it checks the value's state, and drives sub-operations (CopyAssign_RefCounted, and more) based on the result, looping over 16-byte elements with refcounting. This is the "process this data" routine (two thousand four hundred and one hundred and seventeenth form), part of the 0x82358 process cluster. 69 lines. 1 callee. 1 caller (sub_82358250).
[entrypoint.functions."0x82358BE0"]
name = "ProcessAndProcessAndProcess2566_82358BE0"

# Process a value by checking its state and driving sub-operations (two thousand four hundred and one hundred and eighteenth form). It takes a value (a), a value (b), a value (c), and a value (d); it checks the value's state, and drives sub-operations (CopyAssign_RefCounted, ProcessAndProcessAndProcess1875, and more) based on the result, looping over 20-byte elements. This is the "process this data" routine (two thousand four hundred and one hundred and eighteenth form), part of the 0x82358 process cluster. 60 lines. 2 callees. 1 caller (sub_82358590).
[entrypoint.functions."0x82358C50"]
name = "ProcessAndProcessAndProcess2567_82358C50"

# Process a value by checking its state and driving sub-operations (two thousand four hundred and one hundred and nineteenth form). It takes a value (a), a structure (b: fields at b->4 and b->8), and a value (c); it checks the value's state, and drives sub-operations (sub_82610990) based on the result, performing a bounds check. This is the "process this data" routine (two thousand four hundred and one hundred and nineteenth form), part of the 0x82610 process cluster. 132 lines. 1 callee. 3 callers (sub_82B18688, sub_827325C8, and sub_82192848).
[entrypoint.functions."0x826107D8"]
name = "ProcessAndProcessAndProcess2568_826107D8"

# Process a value by checking its state and driving many sub-operations (two thousand four hundred and one hundred and twentieth form). It takes a structure (a: fields at a->4, a->8, and a->12), a value (b), and a value (c); it checks the value's state, and drives many sub-operations (AllocateChecked4, Free_SizeBucketed_Thunk, and more) based on the result. This is the "process this data" routine (two thousand four hundred and one hundred and twentieth form), part of the 0x82610 process cluster. 493 lines. 3 callees. 1 caller (ProcessAndProcessAndProcess2568).
[entrypoint.functions."0x82610990"]
name = "ProcessAndProcessAndProcess2569_82610990"

# Process a structure by checking its state (two thousand four hundred and one hundred and twenty-first form). It takes a structure (a: fields at a->0 and a->4); it checks the structure's state, and manipulates the structure's linked-list pointers based on the result. This is the "process this data" routine (two thousand four hundred and one hundred and twenty-first form), part of the 0x828A5 process cluster. 112 lines. 0 callees. 6 callers.
[entrypoint.functions."0x828A52A0"]
name = "ProcessAndProcessAndProcess2570_828A52A0"

# Process a value by checking its state and driving sub-operations (two thousand four hundred and one hundred and twenty-second form). It takes a structure (a: field at a->0) and a structure (b: field at b->0); it checks the value's state, and drives sub-operations (ProcessAndProcessAndProcess1869, Release_RefCounted, and more) based on the result, including an atomic increment. This is the "process this data" routine (two thousand four hundred and one hundred and twenty-second form), part of the 0x82B39 process cluster. 125 lines. 2 callees. 8 callers.
[entrypoint.functions."0x82B39768"]
name = "ProcessAndProcessAndProcess2571_82B39768"

# Process a value by checking its state and driving sub-operations (two thousand four hundred and one hundred and twenty-third form). It takes a value (a) and a value (b); it checks the value's state, and drives sub-operations (VcallTriple_AllSuccess, VcallChain_AllSuccess, and more) based on the result. This is the "process this data" routine (two thousand four hundred and one hundred and twenty-third form), part of the 0x82C49 process cluster. 92 lines. 2 callees. 1 caller (sub_82C4B160).
[entrypoint.functions."0x82C49720"]
name = "ProcessAndProcessAndProcess2572_82C49720"

# Process a structure by checking its state (two thousand four hundred and one hundred and twenty-fourth form). It takes a structure (a: fields at a->8 and a->24) and a value (b, a float pointer); it checks the structure's state, and stores the float pointer and its value into the structure's fields, defaulting the value to a constant if the pointer is null, based on the result. This is the "process this data" routine (two thousand four hundred and one hundred and twenty-fourth form), part of the 0x82C5C process cluster. 30 lines. 0 callees. 1 caller (sub_82C5C8D8).
[entrypoint.functions."0x82C5C820"]
name = "ProcessAndProcessAndProcess2573_82C5C820"

# Process a value by checking its state and driving sub-operations (two thousand four hundred and one hundred and twenty-fifth form). It takes a structure (a: fields at a->28, a->0, a->4, a->12, a->32, and a->20) and several values (b through g); it checks the value's state, and drives sub-operations (ProcessAndProcessAndProcess2573, and more) based on the result, storing values into the structure's fields. This is the "process this data" routine (two thousand four hundred and one hundred and twenty-fifth form), part of the 0x82C5C process cluster. 65 lines. 1 callee. 2 callers (sub_82C5CE58 and sub_82C5C940).
[entrypoint.functions."0x82C5C8D8"]
name = "ProcessAndProcessAndProcess2574_82C5C8D8"

# Process a value by checking its state and driving sub-operations (two thousand four hundred and one hundred and twenty-sixth form). It takes a structure (a: fields at a->4, a->12, a->8, a->16, a->24, a->20, a->28, and a->0), a value (b), a value (c), a value (d), a value (e), and several floats; it checks the value's state, and drives sub-operations (ProcessAndProcessAndProcess2544, sub_82C608E0, and more) based on the result, storing values and floats into the structure's fields. This is the "process this data" routine (two thousand four hundred and one hundred and twenty-sixth form), part of the 0x82C60 process cluster. 82 lines. 2 callees. 2 callers (sub_82C60BA0 and sub_82C5B700).
[entrypoint.functions."0x82C60AB0"]
name = "ProcessAndProcessAndProcess2575_82C60AB0"

# Process a value by checking its state and driving many sub-operations (two thousand four hundred and one hundred and twenty-seventh form). It takes a structure (a: field at a->0) and a structure (b); it checks the value's state, and drives many sub-operations (Tree_GetOrCreate, ResetTreeObj, Allocate_OrDbgBreak, and more) based on the result. This is the "process this data" routine (two thousand four hundred and one hundred and twenty-seventh form), part of the 0x82C1D process cluster. 120 lines. 3 callees. 2 callers (sub_82C1E230 and sub_82C1E2E0).
[entrypoint.functions."0x82C1DD30"]
name = "ProcessAndProcessAndProcess2576_82C1DD30"

# Process a value by checking its state and driving sub-operations (two thousand four hundred and one hundred and twenty-eighth form). It takes a structure (a: fields at a->0, a->4, and a->36); it checks the value's state, and drives sub-operations (Construct20ByteNode, ResetTreeObj, and more) based on the result, storing global pointers into the structure's fields. This is the "process this data" routine (two thousand four hundred and one hundred and twenty-eighth form), part of the 0x82C1E process cluster. 69 lines. 2 callees. 2 callers (sub_82C1E2E0 and sub_82C1E230).
[entrypoint.functions."0x82C1E130"]
name = "ProcessAndProcessAndProcess2577_82C1E130"

# Process a value by checking its state and driving many sub-operations (two thousand four hundred and one hundred and twenty-ninth form). It takes a value (a), a value (b), and a structure (c: fields at c->8, c->4, and c->0); it checks the value's state, and drives many sub-operations (MemCpy_SdkRuntime, and more) based on the result, looping over elements. This is the "process this data" routine (two thousand four hundred and one hundred and twenty-ninth form), part of the 0x82CB9 process cluster. 360 lines. 1 callee. 2 callers (ProcessAndProcessAndProcess2495 and sub_83003A40).
[entrypoint.functions."0x82CB9E88"]
name = "ProcessAndProcessAndProcess2578_82CB9E88"

# Process a value by checking its state and driving sub-operations (two thousand four hundred and one hundred and thirtieth form). It takes several values (a through g); it checks the value's state, and drives sub-operations (ExCreateThread, StoreLastErrorCode, and more) based on the result, creating a thread. This is the "process this data" routine (two thousand four hundred and one hundred and thirtieth form), part of the 0x82CC8 process cluster. 68 lines. 2 callees. 2 callers (sub_82CBD280 and sub_82D014A8).
[entrypoint.functions."0x82CC8758"]
name = "ProcessAndProcessAndProcess2579_82CC8758"

# Process a structure by checking its state (two thousand four hundred and one hundred and thirty-first form). It takes a structure (a: fields at a->4, a->8, and a->12); it checks the structure's state, and drives sub-operations (Free_SizeBucketed_Thunk) based on the result, freeing a field and nulling out the structure's fields. This is the "process this data" routine (two thousand four hundred and one hundred and thirty-first form), part of the 0x823D2 process cluster. 35 lines. 1 callee. 12 callers.
[entrypoint.functions."0x823D2D28"]
name = "ProcessAndProcessAndProcess2580_823D2D28"

# Process a value by checking its state and driving many sub-operations (two thousand four hundred and one hundred and thirty-second form). It takes a value (a), a value (b), a value (c), and a value (d); it checks the value's state, and drives many sub-operations (sub_82A802E0, ProcessAndProcessAndProcess1248, and more) based on the result, looping over elements. This is the "process this data" routine (two thousand four hundred and one hundred and thirty-second form), part of the 0x82A7F process cluster. 257 lines. 6 callees. 1 caller (sub_82A7F580).
[entrypoint.functions."0x82A7FEE8"]
name = "ProcessAndProcessAndProcess2581_82A7FEE8"

# Process a value by checking its state and driving sub-operations (two thousand four hundred and one hundred and thirty-third form). It takes a value (a) and a value (b); it checks the value's state, and drives sub-operations (sub_82A80CF8) based on the result, performing a binary search over a range. This is the "process this data" routine (two thousand four hundred and one hundred and thirty-third form), part of the 0x82A80 process cluster. 157 lines. 1 callee. 1 caller (ProcessAndProcessAndProcess2581).
[entrypoint.functions."0x82A805F0"]
name = "ProcessAndProcessAndProcess2582_82A805F0"

# Process a value by checking its state and driving sub-operations (two thousand four hundred and one hundred and thirty-fourth form). It takes a value (a), a value (b), a value (c), and a value (d); it checks the value's state, and drives sub-operations (CopyAssign_RefCounted, and more) based on the result, looping over 8-byte elements. This is the "process this data" routine (two thousand four hundred and one hundred and thirty-fourth form), part of the 0x82AA3 process cluster. 57 lines. 1 callee. 1 caller (sub_82AA3760).
[entrypoint.functions."0x82AA3C50"]
name = "ProcessAndProcessAndProcess2583_82AA3C50"

# Process a value by checking its state and driving many sub-operations (two thousand four hundred and one hundred and thirty-fifth form). It takes a value (a) and a structure (b: field at b->3); it checks the value's state, and drives many sub-operations (ProcessAndProcessAndProcess1343, ProcessAndProcessAndProcess1251, and more) based on the result, processing an array of 112-byte elements. This is the "process this data" routine (two thousand four hundred and one hundred and thirty-fifth form), part of the 0x82B7B process cluster. 327 lines. 3 callees. 3 callers (sub_82A6E200, sub_82A6E310, and sub_82B608B8).
[entrypoint.functions."0x82B7B9B8"]
name = "ProcessAndProcessAndProcess2584_82B7B9B8"

# Process a structure by checking its state and driving sub-operations (two thousand four hundred and one hundred and thirty-sixth form). It takes a structure (a: fields at a->16 and a->4); it checks the structure's state, and drives sub-operations (sub_82B8A510, Free_SizeBucketed_Thunk, DestructRefCounted, and more) based on the result, freeing fields and destructing the structure. This is the "process this data" routine (two thousand four hundred and one hundred and thirty-sixth form), part of the 0x82B8A process cluster. 67 lines. 3 callees. 2 callers (sub_82B7EC80 and sub_82B7E658).
[entrypoint.functions."0x82B8A4A8"]
name = "ProcessAndProcessAndProcess2585_82B8A4A8"

# Process a value by checking its state and driving many sub-operations (two thousand four hundred and one hundred and thirty-seventh form). It takes several values (a through g); it checks the value's state, and drives many sub-operations (ProcessAndProcessAndProcess652, ProcessAndProcessAndProcess606, and more) based on the result, allocating a small structure. This is the "process this data" routine (two thousand four hundred and one hundred and thirty-seventh form), part of the 0x82B92 process cluster. 168 lines. 3 callees. 11 callers.
[entrypoint.functions."0x82B923B8"]
name = "ProcessAndProcessAndProcess2586_82B923B8"

# Process a value by checking its state (two thousand four hundred and one hundred and thirty-eighth form). It takes a value (a) and a value (b); it checks the value's state, and drives sub-operations (a virtual call) based on the result, storing values into the result structure's fields. This is the "process this data" routine (two thousand four hundred and one hundred and thirty-eighth form), part of the 0x82BAF process cluster. 40 lines. 0 callees. 0 callers (vtable-dispatched).
[entrypoint.functions."0x82BAF940"]
name = "ProcessAndProcessAndProcess2587_82BAF940"

# Process a value by checking its state and driving sub-operations (two thousand four hundred and one hundred and thirty-ninth form). It takes a structure (a: fields at a->12 and a->44) and several floats (b, c); it checks the value's state, and drives sub-operations (ProcessAndProcessAndProcess818, and more) based on the result, performing float operations. This is the "process this data" routine (two thousand four hundred and one hundred and thirty-ninth form), part of the 0x82C58 process cluster. 79 lines. 1 callee. 2 callers (ProcessAndProcessAndProcess1851 and sub_82C59050).
[entrypoint.functions."0x82C58318"]
name = "ProcessAndProcessAndProcess2588_82C58318"

# Process a value by checking its state and driving sub-operations (two thousand four hundred and one hundred and fortieth form). It takes a structure (a: fields at a->16 and a->48) and several floats (b, c); it checks the value's state, and drives sub-operations (ProcessAndProcessAndProcess818, and more) based on the result, performing float operations. This is the "process this data" routine (two thousand four hundred and one hundred and fortieth form), part of the 0x82C58 process cluster. 79 lines. 1 callee. 2 callers (sub_82C59050 and ProcessAndProcessAndProcess1851).
[entrypoint.functions."0x82C58398"]
name = "ProcessAndProcessAndProcess2589_82C58398"

# Process a value by checking its state and driving sub-operations (two thousand four hundred and one hundred and forty-first form). It takes a structure (a: field at a->4) and a float (b); it checks the value's state, and drives sub-operations (ProcessAndProcessAndProcess818, and more) based on the result, clamping the float to a range. This is the "process this data" routine (two thousand four hundred and one hundred and forty-first form), part of the 0x82C58 process cluster. 89 lines. 1 callee. 2 callers (sub_82C59050 and ProcessAndProcessAndProcess1851).
[entrypoint.functions."0x82C58680"]
name = "ProcessAndProcessAndProcess2590_82C58680"

# Process a value by checking its state and driving sub-operations (two thousand four hundred and one hundred and forty-second form). It takes a structure (a: fields at a->16 and a->12); it checks the value's state, and drives sub-operations (ProcessAndProcessAndProcess2338) based on the result. This is the "process this data" routine (two thousand four hundred and one hundred and forty-second form), part of the 0x82C61 process cluster. 10 lines. 1 callee. 2 callers (ProcessAndProcessAndProcess513 and sub_82C5F768).
[entrypoint.functions."0x82C61888"]
name = "ProcessAndProcessAndProcess2591_82C61888"

# Process a structure by checking its state (two thousand four hundred and one hundred and forty-third form). It takes a structure (a: fields at a->36, a->12, and a->40); it checks the structure's state, and stores a constant float into the elements at a->12 (a table) based on the result, storing 0 to a->40. This is the "process this data" routine (two thousand four hundred and one hundred and forty-third form), part of the 0x82C62 process cluster. 40 lines. 0 callees. 2 callers (sub_82C62510 and sub_82C5FE88).
[entrypoint.functions."0x82C62470"]
name = "ProcessAndProcessAndProcess2592_82C62470"

# Process a value by checking its state (two thousand four hundred and one hundred and forty-fourth form). It takes a value (a) and a structure (b: fields at b->24 and b->32); it checks the value's state, and drives sub-operations (a virtual call) based on the result. This is the "process this data" routine (two thousand four hundred and one hundred and forty-fourth form), part of the 0x82C6C process cluster. 35 lines. 0 callees. 0 callers (vtable-dispatched).
[entrypoint.functions."0x82C6C680"]
name = "ProcessAndProcessAndProcess2593_82C6C680"

# Process a value by checking its state and driving sub-operations (two thousand four hundred and one hundred and forty-fifth form). It takes a value (a); it checks the value's state, and drives sub-operations (ProcessAndProcessAndProcess2579) based on the result. This is the "process this data" routine (two thousand four hundred and one hundred and forty-fifth form), part of the 0x82CBD process cluster. 8 lines. 1 callee. 9 callers.
[entrypoint.functions."0x82CBD280"]
name = "ProcessAndProcessAndProcess2594_82CBD280"

# Process a value by checking its state and driving many sub-operations (two thousand four hundred and one hundred and forty-sixth form). It takes a value (a) and a value (b); it checks the value's state, and drives many sub-operations (sub_82BE36E0, MemCpy_SdkRuntime, ProcessAndLookupWithDefaultInit, and more) based on the result, performing a bounds check. This is the "process this data" routine (two thousand four hundred and one hundred and forty-sixth form), part of the 0x832B9 process cluster. ~100 lines. 3 callees. 1 caller (sub_832B3EB8).
[entrypoint.functions."0x832B93F8"]
name = "ProcessAndProcessAndProcess2595_832B93F8"

# Process a structure by checking its state and driving sub-operations (two thousand four hundred and one hundred and forty-seventh form). It takes a structure (a: fields at a->20, a->24, and a->4); it checks the structure's state, and drives sub-operations (ClampAndUpdate) based on the result, setting the structure's fields. This is the "process this data" routine (two thousand four hundred and one hundred and forty-seventh form), part of the 0x822F5 process cluster. 40 lines. 1 callee. 2 callers (ProcessAndProcessAndProcess1727 and sub_82C7A220).
[entrypoint.functions."0x822F51B8"]
name = "ProcessAndProcessAndProcess2596_822F51B8"

# Process a value by checking its state and driving sub-operations (two thousand four hundred and one hundred and forty-eighth form). It takes a structure (a: fields at a->0 and a->4) and a structure (b: fields at b->16 and b->4); it checks the value's state, and drives sub-operations (sub_82370340) based on the result, unlinking from a list. This is the "process this data" routine (two thousand four hundred and one hundred and forty-eighth form), part of the 0x8236E process cluster. 64 lines. 1 callee. 1 caller (sub_8236E830).
[entrypoint.functions."0x8236E7C0"]
name = "ProcessAndProcessAndProcess2597_8236E7C0"

# Process a value by checking its state and driving many sub-operations (two thousand four hundred and one hundred and forty-ninth form). It takes a structure (a: field at a->28), a value (b), and a value (c); it checks the value's state, and drives many sub-operations (ProcessAndProcessAndProcess1719, sub_8236E9F0, sub_8236F800, and more) based on the result. This is the "process this data" routine (two thousand four hundred and one hundred and forty-ninth form), part of the 0x8236E process cluster. 145 lines. 5 callees. 1 caller (ProcessAndProcessAndProcess1727).
[entrypoint.functions."0x8236E830"]
name = "ProcessAndProcessAndProcess2598_8236E830"

# Process a value by checking its state and driving sub-operations (two thousand four hundred and one hundred and fiftieth form). It takes a structure (a: field at a->12), a value (b), and a value (c); it checks the value's state, and drives sub-operations (ProcessAndProcessAndProcess1128, sub_8236F9E0, and more) based on the result, incrementing a counter. This is the "process this data" routine (two thousand four hundred and one hundred and fiftieth form), part of the 0x8236E process cluster. 107 lines. 2 callees. 1 caller (ProcessAndProcessAndProcess2598).
[entrypoint.functions."0x8236E928"]
name = "ProcessAndProcessAndProcess2599_8236E928"

# Process a value by checking its state and driving sub-operations (two thousand four hundred and one hundred and fifty-first form). It takes a structure (a: fields at a->20, a->24, and a->4) and a structure (b: fields at b->0 and b->4); it checks the value's state, and drives sub-operations (ProcessAndProcessAndProcess1275, sub_82370978, and more) based on the result, performing bounds checks. This is the "process this data" routine (two thousand four hundred and one hundred and fifty-first form), part of the 0x8236E process cluster. 77 lines. 2 callees. 1 caller (ProcessAndProcessAndProcess1727).
[entrypoint.functions."0x8236EF80"]
name = "ProcessAndProcessAndProcess2600_8236EF80"

# Process a value by checking its state and driving many sub-operations (two thousand four hundred and one hundred and fifty-second form). It takes a value (a), a structure (b: field at b->4), and a structure (c: field at c->0); it checks the value's state, and drives many sub-operations (FindChainRoot4, sub_82370430, and more) based on the result, traversing a linked list. This is the "process this data" routine (two thousand four hundred and one hundred and fifty-second form), part of the 0x8236F process cluster. 183 lines. 2 callees. 1 caller (ProcessAndProcessAndProcess2599).
[entrypoint.functions."0x8236F9E0"]
name = "ProcessAndProcessAndProcess2601_8236F9E0"

# Process a value by checking its state and driving many sub-operations (two thousand four hundred and one hundred and fifty-third form). It takes a value (a), a value (b), and a value (c); it checks the value's state, and drives many sub-operations (Allocate_OrDbgBreak, SetString, ClampAndUpdate, and more) based on the result, allocating a small structure. This is the "process this data" routine (two thousand four hundred and one hundred and fifty-third form), part of the 0x82370 process cluster. 142 lines. 7 callees. 1 caller (ProcessAndProcessAndProcess2597).
[entrypoint.functions."0x82370340"]
name = "ProcessAndProcessAndProcess2602_82370340"

# Process a value by checking its state and driving many sub-operations (two thousand four hundred and one hundred and fifty-fourth form). It takes a value (a), a structure (b: field at b->8), a value (c), a value (d), and a value (e); it checks the value's state, and drives many sub-operations (SetString, List_SpliceNode2, sub_826C3EF0, and more) based on the result. This is the "process this data" routine (two thousand four hundred and one hundred and fifty-fourth form), part of the 0x82370 process cluster. 329 lines. 8 callees. 1 caller (ProcessAndProcessAndProcess2601).
[entrypoint.functions."0x82370430"]
name = "ProcessAndProcessAndProcess2603_82370430"

# Process a value by checking its state and driving many sub-operations (two thousand four hundred and one hundred and fifty-fifth form). It takes a structure (a: field at a->4), a value (b), and a value (c); it checks the value's state, and drives many sub-operations (RtlEnterCriticalSection, Allocate_OrDbgBreak, Free_SizeBucketed_Thunk, and more) based on the result, all under a critical section. This is the "process this data" routine (two thousand four hundred and one hundred and fifty-fifth form), part of the 0x82370 process cluster. 124 lines. 6 callees. 1 caller (ProcessAndProcessAndProcess2600).
[entrypoint.functions."0x82370978"]
name = "ProcessAndProcessAndProcess2604_82370978"

# Process a value by checking its state and driving sub-operations (two thousand four hundred and one hundred and fifty-sixth form). It takes a value (a), a value (b), a value (c), a value (d), and a value (e); it checks the value's state, and drives sub-operations (Allocate_OrDbgBreak, and more) based on the result, allocating a small structure and copying elements. This is the "process this data" routine (two thousand four hundred and one hundred and fifty-sixth form), part of the 0x82370 process cluster. 69 lines. 1 callee. 1 caller (ProcessAndProcessAndProcess2603).
[entrypoint.functions."0x82370F68"]
name = "ProcessAndProcessAndProcess2605_82370F68"

# Process a value by checking its state and driving many sub-operations (two thousand four hundred and one hundred and fifty-seventh form). It takes a value (a), a value (b), a value (c), and a value (d); it checks the value's state, and drives many sub-operations (a virtual call, ConstructLinkedNode, FindAndRemoveFromList, and more) based on the result. This is the "process this data" routine (two thousand four hundred and one hundred and fifty-seventh form), part of the 0x82371 process cluster. 99 lines. 3 callees. 1 caller (ProcessAndProcessAndProcess2604).
[entrypoint.functions."0x823710A0"]
name = "ProcessAndProcessAndProcess2606_823710A0"

# Process a value by checking its state and driving many sub-operations (two thousand four hundred and one hundred and fifty-eighth form). It takes a value (a) and a value (b); it checks the value's state, and drives many sub-operations (Allocate_OrDbgBreak, ConstructRefCounted, CopyAssign_RefCounted, DestructRefCounted, and more) based on the result, allocating a small structure. This is the "process this data" routine (two thousand four hundred and one hundred and fifty-eighth form), part of the 0x82A6C process cluster. 153 lines. 5 callees. 5 callers.
[entrypoint.functions."0x82A6C8E8"]
name = "ProcessAndProcessAndProcess2607_82A6C8E8"

# Process a value by checking its state and driving many sub-operations (two thousand four hundred and one hundred and fifty-ninth form). It takes a value (a); it checks the value's state, and drives many sub-operations (ProcessAndProcessAndProcess1794, ProcessAndProcessAndProcess2131, and more) based on the result, including an atomic decrement. This is the "process this data" routine (two thousand four hundred and one hundred and fifty-ninth form), part of the 0x82B38 process cluster. 152 lines. 3 callees. 10 callers.
[entrypoint.functions."0x82B38478"]
name = "ProcessAndProcessAndProcess2608_82B38478"

# Process a value by checking its state and driving many sub-operations (two thousand four hundred and one hundred and sixtieth form). It takes a structure (a: fields at a->0 and a->4) and a structure (b: fields at b->4, b->0, and b->8); it checks the value's state, and drives many sub-operations (Allocate_OrDbgBreak, MemCpy_SdkRuntime, MemSet_SdkRuntime, and more) based on the result. This is the "process this data" routine (two thousand four hundred and one hundred and sixtieth form), part of the 0x82B84 process cluster. 127 lines. 6 callees. 2 callers (sub_82B8B330 and ProcessAndProcessAndProcess2450).
[entrypoint.functions."0x82B843F8"]
name = "ProcessAndProcessAndProcess2609_82B843F8"

# Process a value by checking its state and driving sub-operations (two thousand four hundred and one hundred and sixty-first form). It takes a structure (a: fields at a->32, a->0, a->4, a->20, and a->872) and a value (b); it checks the value's state, and drives sub-operations (MemSet_SdkRuntime, MemCpy_SdkRuntime, and more) based on the result, initializing a large structure. This is the "process this data" routine (two thousand four hundred and one hundred and sixty-first form), part of the 0x82B92 process cluster. 146 lines. 2 callees. 2 callers (ProcessAndProcessAndProcess2609 and sub_82B92D48).
[entrypoint.functions."0x82B92C50"]
name = "ProcessAndProcessAndProcess2610_82B92C50"

# Process a value by checking its state and driving many sub-operations (two thousand four hundred and one hundred and sixty-second form). It takes a value (a), a value (b), a value (c), and a value (d); it checks the value's state, and drives many sub-operations (Allocate_OrDbgBreak, ResetTreeObj, AtomicIncrement3, and more) based on the result, allocating a small structure. This is the "process this data" routine (two thousand four hundred and one hundred and sixty-second form), part of the 0x82C0C process cluster. 78 lines. 5 callees. 1 caller (sub_82C0E1C0).
[entrypoint.functions."0x82C0CD48"]
name = "ProcessAndProcessAndProcess2611_82C0CD48"

# Process a value by checking its state and driving sub-operations (two thousand four hundred and one hundred and sixty-third form). It takes a structure (a: fields at a->0 and a->68), a value (b), a value (c), a value (d), and a value (e); it checks the value's state, and drives sub-operations (a virtual call and ProcessAndProcessAndProcess2611) based on the result. This is the "process this data" routine (two thousand four hundred and one hundred and sixty-third form), part of the 0x82C0E process cluster. 40 lines. 1 callee. 0 callers (vtable-dispatched).
[entrypoint.functions."0x82C0E1C0"]
name = "ProcessAndProcessAndProcess2612_82C0E1C0"

# Process a value by checking its state and driving sub-operations (two thousand four hundred and one hundred and sixty-fourth form). It takes a value (a, a critical section); it checks the value's state, and drives sub-operations (RtlInitializeCriticalSection) based on the result, returning a success indicator. This is the "process this data" routine (two thousand four hundred and one hundred and sixty-fourth form), part of the 0x82CB5 process cluster. 20 lines. 1 callee. 0 callers (vtable-dispatched).
[entrypoint.functions."0x82CB5B48"]
name = "ProcessAndProcessAndProcess2613_82CB5B48"

# Process a value by checking its state (two thousand four hundred and one hundred and sixty-fifth form). It uses global state to check its state, and drives sub-operations (a virtual call) based on the result. This is the "process this data" routine (two thousand four hundred and one hundred and sixty-fifth form), part of the 0x82CB5 process cluster. 63 lines. 0 callees. 10 callers.
[entrypoint.functions."0x82CB5B78"]
name = "ProcessAndProcessAndProcess2614_82CB5B78"

# Process a value by checking its state (two thousand four hundred and one hundred and sixty-sixth form). It takes a value (a), a structure (b: fields at b->0 and b->36), and a float (c); it checks the value's state, and drives sub-operations (a virtual call) based on the result, performing float operations. This is the "process this data" routine (two thousand four hundred and one hundred and sixty-sixth form), part of the 0x823F9 process cluster. 73 lines. 0 callees. 7 callers.
[entrypoint.functions."0x823F92B8"]
name = "ProcessAndProcessAndProcess2615_823F92B8"

# Process a value by checking its state (two thousand four hundred and one hundred and sixty-seventh form). It takes a structure (a: fields at a->4 and a->0) and a value (b); it checks the value's state, and stores values and floats into the structure's fields based on the result, using vector operations. This is the "process this data" routine (two thousand four hundred and one hundred and sixty-seventh form), part of the 0x82A3A process cluster. 154 lines. 0 callees. 10 callers.
[entrypoint.functions."0x82A3AD50"]
name = "ProcessAndProcessAndProcess2616_82A3AD50"

# Process a value by checking its state and driving many sub-operations (two thousand four hundred and one hundred and sixty-eighth form). It takes a value (a); it checks the value's state, and drives many sub-operations (ConstructRefCounted, ProcessAndProcessAndProcess1804, DestructRefCounted, and more) based on the result. This is the "process this data" routine (two thousand four hundred and one hundred and sixty-eighth form), part of the 0x82ABB process cluster. 262 lines. 3 callees. 1 caller (sub_82ABBBD0).
[entrypoint.functions."0x82ABBA00"]
name = "ProcessAndProcessAndProcess2617_82ABBA00"

# Process a value by checking its state and driving many sub-operations (two thousand four hundred and one hundred and sixty-ninth form). It takes a value (a); it checks the value's state, and drives many sub-operations (ProcessAndProcessAndProcess2617, Free_SizeBucketed, and more) based on the result, looping over elements. This is the "process this data" routine (two thousand four hundred and one hundred and sixty-ninth form), part of the 0x82ABB process cluster. 350 lines. 8 callees. 1 caller (sub_82A53168).
[entrypoint.functions."0x82ABBBD0"]
name = "ProcessAndProcessAndProcess2618_82ABBBD0"

# Process a value by checking its state and driving many sub-operations (two thousand four hundred and one hundred and seventieth form). It takes a structure (a: fields at a->4 and a->8), a value (b), a value (c), and a value (d); it checks the value's state, and drives many sub-operations based on the result, looping over elements. This is the "process this data" routine (two thousand four hundred and one hundred and seventieth form), part of the 0x82C4A process cluster. 165 lines. 3 callees. 1 caller (sub_82C4B160).
[entrypoint.functions."0x82C4AFC8"]
name = "ProcessAndProcessAndProcess2619_82C4AFC8"

# Process a value by checking its state and driving sub-operations (two thousand four hundred and one hundred and seventy-first form). It takes a value (a); it checks the value's state, and drives sub-operations (ProcessAndAllocateAndProcess2) based on the result. This is the "process this data" routine (two thousand four hundred and one hundred and seventy-first form), part of the 0x82E7F process cluster. 69 lines. 1 callee. 5 callers.
[entrypoint.functions."0x82E7F208"]
name = "ProcessAndProcessAndProcess2620_82E7F208"

# Process a value by checking its state and driving many sub-operations (two thousand four hundred and one hundred and seventy-second form). It takes a value (a), a value (b), a value (c), and a value (d); it checks the value's state, and drives many sub-operations based on the result, looping over elements and calling itself recursively. This is the "process this data" routine (two thousand four hundred and one hundred and seventy-second form), part of the 0x82B13 process cluster. 230 lines. 6 callees. 1 caller (sub_82B13160).
[entrypoint.functions."0x82B13830"]
name = "ProcessAndProcessAndProcess2621_82B13830"

# Process a value by checking its state and driving sub-operations (two thousand four hundred and one hundred and seventy-third form). It takes a value (a), a value (b), a value (c), and a value (d); it checks the value's state, and drives sub-operations based on the result, performing a binary search. This is the "process this data" routine (two thousand four hundred and one hundred and seventy-third form), part of the 0x82B14 process cluster. 157 lines. 1 callee. 1 caller (ProcessAndProcessAndProcess2621).
[entrypoint.functions."0x82B14210"]
name = "ProcessAndProcessAndProcess2622_82B14210"

# Process a value by checking its state (two thousand four hundred and one hundred and seventy-fourth form). It takes a structure (a, with a vtable pointer at a->0); it checks the value's state, and drives sub-operations (sub_82C1E230) based on the result. This is the "process this data" routine (two thousand four hundred and one hundred and seventy-fourth form), part of the 0x82C00 process cluster. 4 lines. 1 callee. 1 caller (sub_82C00B10).
[entrypoint.functions."0x82C00030"]
name = "ProcessAndProcessAndProcess2623_82C00030"

# Process a value by checking its state and driving many sub-operations (two thousand four hundred and one hundred and seventy-fifth form). It takes a structure (a: fields at a->24 and a->4) and a value (b); it checks the value's state, and drives many sub-operations (Allocate_OrDbgBreak, ProcessAndProcessAndProcess2577, ProcessAndProcessAndProcess2576, Tree_GetOrCreate, and more) based on the result, allocating a small structure. This is the "process this data" routine (two thousand four hundred and one hundred and seventy-fifth form), part of the 0x82C1E process cluster. 61 lines. 4 callees. 1 caller (ProcessAndProcessAndProcess2623).
[entrypoint.functions."0x82C1E230"]
name = "ProcessAndProcessAndProcess2624_82C1E230"

# Process a value by checking its state (two thousand four hundred and one hundred and seventy-sixth form). It takes a value (a) and a structure (b: fields at b->0, b->4, and b->8); it checks the value's state, and manipulates linked-list pointers based on the result. This is the "process this data" routine (two thousand four hundred and one hundred and seventy-sixth form), part of the 0x827E0 process cluster. 45 lines. 0 callees. 2 callers.
[entrypoint.functions."0x827E08B8"]
name = "ProcessAndProcessAndProcess2625_827E08B8"

# Process a value by checking its state (two thousand four hundred and one hundred and seventy-seventh form). It takes a value (a) and a structure (b: fields at b->0, b->4, and b->8); it checks the value's state, and manipulates linked-list pointers based on the result. This is the "process this data" routine (two thousand four hundred and one hundred and seventy-seventh form), part of the 0x82A52 process cluster. 45 lines. 0 callees. 2 callers.
[entrypoint.functions."0x82A523F0"]
name = "ProcessAndProcessAndProcess2626_82A523F0"

# Process a value by checking its state and driving many sub-operations (two thousand four hundred and one hundred and seventy-eighth form). It takes a structure (a: field at a->240), a value (b), and a value (c); it checks the value's state, and drives many sub-operations (ProcessAndProcessAndProcess852, ProcessAndProcessAndProcess858, and more) based on the result. This is the "process this data" routine (two thousand four hundred and one hundred and seventy-eighth form), part of the 0x82A68 process cluster. 135 lines. 3 callees. 2 callers.
[entrypoint.functions."0x82A68538"]
name = "ProcessAndProcessAndProcess2627_82A68538"

# Process a value by checking its state and driving many sub-operations (two thousand four hundred and one hundred and seventy-ninth form). It takes a value (a), a value (b), a value (c), and a value (d); it checks the value's state, and drives many sub-operations based on the result, looping over elements and calling itself recursively. This is the "process this data" routine (two thousand four hundred and one hundred and seventy-ninth form), part of the 0x82A68 process cluster. 255 lines. 4 callees. 1 caller (ProcessAndProcessAndProcess2627).
[entrypoint.functions."0x82A68E70"]
name = "ProcessAndProcessAndProcess2628_82A68E70"

# Process a value by checking its state and driving sub-operations (two thousand four hundred and one hundred and eightieth form). It takes a structure (a: fields at a->4, a->8, and a->12) and a structure (b: fields at b->8 and b->4); it checks the value's state, and drives sub-operations (sub_82B800A0) based on the result. This is the "process this data" routine (two thousand four hundred and one hundred and eightieth form), part of the 0x82B64 process cluster. 138 lines. 1 callee. 8 callers.
[entrypoint.functions."0x82B647B0"]
name = "ProcessAndProcessAndProcess2629_82B647B0"

# Process a value by checking its state and driving many sub-operations (two thousand four hundred and one hundred and eighty-first form). It takes a value (a) and a value (b); it checks the value's state, and drives many sub-operations (Allocate_OrDbgBreak, ProcessAndProcessAndProcess1888, ProcessAndProcessAndProcess2559, and more) based on the result, allocating a small structure. This is the "process this data" routine (two thousand four hundred and one hundred and eighty-first form), part of the 0x82B8B process cluster. 95 lines. 4 callees. 1 caller (sub_82B84BD0).
[entrypoint.functions."0x82B8B960"]
name = "ProcessAndProcessAndProcess2630_82B8B960"

# Process a value by checking its state (two thousand four hundred and one hundred and eighty-second form). It takes a structure (a: fields at a->0, a->24, and a->20); it checks the value's state, and processes elements based on the result, looping over elements. This is the "process this data" routine (two thousand four hundred and one hundred and eighty-second form), part of the 0x82BE6 process cluster. 121 lines. 0 callees. 2 callers.
[entrypoint.functions."0x82BE6508"]
name = "ProcessAndProcessAndProcess2631_82BE6508"

# Process a value by checking its state (two thousand four hundred and one hundred and eighty-third form). It takes a structure (a: fields at a->4, a->0, a->8, and a->16) and a value (b); it checks the value's state, and drives sub-operations (a virtual call) based on the result, looping over elements. This is the "process this data" routine (two thousand four hundred and one hundred and eighty-third form), part of the 0x82C21 process cluster. 71 lines. 0 callees. 0 callers (vtable-dispatched).
[entrypoint.functions."0x82C21028"]
name = "ProcessAndProcessAndProcess2632_82C21028"

# Process a value by checking its state (two thousand four hundred and one hundred and eighty-fourth form). It takes a structure (a: fields at a->4 and a->0); it checks the value's state, and drives sub-operations (a virtual call) based on the result, looping over elements. This is the "process this data" routine (two thousand four hundred and one hundred and eighty-fourth form), part of the 0x82C21 process cluster. 96 lines. 0 callees. 0 callers (vtable-dispatched).
[entrypoint.functions."0x82C21190"]
name = "ProcessAndProcessAndProcess2633_82C21190"

# Process a value by checking its state and driving many sub-operations (two thousand four hundred and one hundred and eighty-fifth form). It takes a value (a), a value (b), a structure (c: field at c->16), and a value (d); it checks the value's state, and drives many sub-operations (ProcessBinarySearch, Allocate_OrDbgBreak, ConstructAndIncrementCounter, and more) based on the result. This is the "process this data" routine (two thousand four hundred and one hundred and eighty-fifth form), part of the 0x82C21 process cluster. 400 lines. 16 callees. 0 callers (vtable-dispatched).
[entrypoint.functions."0x82C217B8"]
name = "ProcessAndProcessAndProcess2634_82C217B8"

# Process a value by checking its state and driving many sub-operations (two thousand four hundred and one hundred and eighty-sixth form). It takes a structure (a: fields at a->4, a->0, a->16, and a->8); it checks the value's state, and drives many sub-operations (Free_Thunk, ProcessAndProcessAndProcess1811, and more) based on the result, looping over elements. This is the "process this data" routine (two thousand four hundred and one hundred and eighty-sixth form), part of the 0x82C22 process cluster. 137 lines. 2 callees. 1 caller (sub_82C23198).
[entrypoint.functions."0x82C22EC8"]
name = "ProcessAndProcessAndProcess2635_82C22EC8"

# Process a value by checking its state and driving sub-operations (two thousand four hundred and one hundred and eighty-seventh form). It takes a structure (a) and a value (b); it checks the value's state, and drives sub-operations (ProcessAndProcessAndProcess2635, Free_Thunk) based on the result. This is the "process this data" routine (two thousand four hundred and one hundred and eighty-seventh form), part of the 0x82C23 process cluster. 45 lines. 2 callees. 0 callers (vtable-dispatched).
[entrypoint.functions."0x82C23198"]
name = "ProcessAndProcessAndProcess2636_82C23198"

# Process a value by checking its state (two thousand four hundred and one hundred and eighty-eighth form). It takes a structure (a: fields at a->8 and a->12); it checks the value's state, and returns a float value from the structure or a default value. This is the "process this data" routine (two thousand four hundred and one hundred and eighty-eighth form), part of the 0x82C5C process cluster. 20 lines. 0 callees. 4 callers.
[entrypoint.functions."0x82C5C848"]
name = "ProcessAndProcessAndProcess2637_82C5C848"

# Process a value by checking its state (two thousand four hundred and one hundred and eighty-ninth form). It takes a structure (a: fields at a->4 and a->20) and a float (b); it checks the value's state, and stores the float and a zero into the structure's fields. This is the "process this data" routine (two thousand four hundred and one hundred and eighty-ninth form), part of the 0x82C5D process cluster. 10 lines. 0 callees. 1 caller (sub_82C5DFF0).
[entrypoint.functions."0x82C5DFE0"]
name = "ProcessAndProcessAndProcess2638_82C5DFE0"

# Process a value by checking its state (two thousand four hundred and one hundred and ninetieth form). It takes a value (a), a value (b), and a value (c); it checks the value's state, and dispatches to one of two sub-operations (sub_82C5DF90 or ProcessAndProcessAndProcess2638) based on the result. This is the "process this data" routine (two thousand four hundred and one hundred and ninetieth form), part of the 0x82C5D process cluster. 10 lines. 2 callees. 3 callers.
[entrypoint.functions."0x82C5DFF0"]
name = "ProcessAndProcessAndProcess2639_82C5DFF0"

# Process a value by checking its state and driving sub-operations (two thousand four hundred and one hundred and ninety-first form). It takes a value (a); it checks the value's state, and drives sub-operations (ProcessAndAllocateAndProcess2) based on the result. This is the "process this data" routine (two thousand four hundred and one hundred and ninety-first form), part of the 0x82E77 process cluster. 69 lines. 1 callee. 5 callers.
[entrypoint.functions."0x82E77AD0"]
name = "ProcessAndProcessAndProcess2640_82E77AD0"

# Process a value by checking its state and driving sub-operations (two thousand four hundred and one hundred and ninety-second form). It takes a structure (a: fields at a->12, a->1024, and a->0); it checks the value's state, and drives sub-operations (ProcessAndProcessAndProcess2511) based on the result, looping over elements. This is the "process this data" routine (two thousand four hundred and one hundred and ninety-second form), part of the 0x82E78 process cluster. 116 lines. 1 callee. 1 caller (sub_82E78DA8).
[entrypoint.functions."0x82E78A70"]
name = "ProcessAndProcessAndProcess2641_82E78A70"

# Process a value by checking its state and driving many sub-operations (two thousand four hundred and one hundred and ninety-third form). It takes a structure (a: fields at a->12 and a->0), a value (b), a value (c), a value (d), a value (e), and a value (f); it checks the value's state, and drives many sub-operations (ProcessAndProcessAndProcess2641, ProcessAndProcessAndProcess2511, and more) based on the result. This is the "process this data" routine (two thousand four hundred and one hundred and ninety-third form), part of the 0x82E78 process cluster. 118 lines. 3 callees. 1 caller (sub_82E78EB8).
[entrypoint.functions."0x82E78DA8"]
name = "ProcessAndProcessAndProcess2642_82E78DA8"

# Process a value by checking its state and driving sub-operations (two thousand four hundred and one hundred and ninety-fourth form). It takes a structure (a); it checks the value's state, and drives sub-operations (ProcessAndProcessAndProcess2642) based on the result. This is the "process this data" routine (two thousand four hundred and one hundred and ninety-fourth form), part of the 0x82E78 process cluster. 35 lines. 1 callee. 12 callers.
[entrypoint.functions."0x82E78EB8"]
name = "ProcessAndProcessAndProcess2643_82E78EB8"

# Process a value by checking its state and driving sub-operations (two thousand four hundred and one hundred and ninety-fifth form). It takes a structure (a: fields at a->12, a->1024, and a->0); it checks the value's state, and drives sub-operations (ProcessAndProcessAndProcess2512) based on the result, looping over elements. This is the "process this data" routine (two thousand four hundred and one hundred and ninety-fifth form), part of the 0x82E7E process cluster. 116 lines. 1 callee. 1 caller (sub_82E7EED0).
[entrypoint.functions."0x82E7ED40"]
name = "ProcessAndProcessAndProcess2644_82E7ED40"

# Process a value by checking its state and driving many sub-operations (two thousand four hundred and one hundred and ninety-sixth form). It takes a structure (a: fields at a->12 and a->0), a value (b), a value (c), a value (d), a value (e), and a value (f); it checks the value's state, and drives many sub-operations (ProcessAndProcessAndProcess2644, ProcessAndProcessAndProcess2512, and more) based on the result. This is the "process this data" routine (two thousand four hundred and one hundred and ninety-sixth form), part of the 0x82E7E process cluster. 118 lines. 3 callees. 1 caller (sub_82E7EFD8).
[entrypoint.functions."0x82E7EED0"]
name = "ProcessAndProcessAndProcess2645_82E7EED0"

# Process a value by checking its state and driving sub-operations (two thousand four hundred and one hundred and ninety-seventh form). It takes a structure (a); it checks the value's state, and drives sub-operations (ProcessAndProcessAndProcess2645) based on the result. This is the "process this data" routine (two thousand four hundred and one hundred and ninety-seventh form), part of the 0x82E7E process cluster. 35 lines. 1 callee. 12 callers.
[entrypoint.functions."0x82E7EFD8"]
name = "ProcessAndProcessAndProcess2646_82E7EFD8"

# Process a value by checking its state and driving sub-operations (two thousand four hundred and one hundred and ninety-eighth form). It takes a value (a); it checks the value's state, and drives sub-operations (Allocate_OrDbgBreak) based on the result, allocating a small structure. This is the "process this data" routine (two thousand four hundred and one hundred and ninety-eighth form), part of the 0x823B0 process cluster. 87 lines. 1 callee. 6 callers.
[entrypoint.functions."0x823B0A18"]
name = "ProcessAndProcessAndProcess2647_823B0A18"

# Process a value by checking its state and driving sub-operations (two thousand four hundred and one hundred and ninety-ninth form). It takes a value (a), a structure (b: field at b->4), and a structure (c: field at c->0); it checks the value's state, and drives sub-operations (Allocate_OrDbgBreak, ProcessAndProcessAndProcess275, and more) based on the result, allocating a small structure. This is the "process this data" routine (two thousand four hundred and one hundred and ninety-ninth form), part of the 0x825BB process cluster. 76 lines. 2 callees. 12 callers.
[entrypoint.functions."0x825BB9D0"]
name = "ProcessAndProcessAndProcess2648_825BB9D0"

# Process a value by checking its state (two thousand four hundred and one hundred and two hundredth form). It takes a value (a) and a structure (b: fields at b->8, b->4, and b->0); it checks the value's state, and manipulates linked-list pointers based on the result. This is the "process this data" routine (two thousand four hundred and one hundred and two hundredth form), part of the 0x82A52 process cluster. 45 lines. 0 callees. 2 callers.
[entrypoint.functions."0x82A52370"]
name = "ProcessAndProcessAndProcess2649_82A52370"

# Process a value by checking its state and driving sub-operations (two thousand four hundred and one hundred and two hundred and first form). It takes a value (a), a value (b), a value (c), and a value (d); it checks the value's state, and drives sub-operations (ProcessAndProcessAndProcess634) based on the result, performing a binary search. This is the "process this data" routine (two thousand four hundred and one hundred and two hundred and first form), part of the 0x82A69 process cluster. 134 lines. 1 callee. 1 caller (ProcessAndProcessAndProcess2628).
[entrypoint.functions."0x82A69320"]
name = "ProcessAndProcessAndProcess2650_82A69320"

# Process a value by checking its state (two thousand four hundred and one hundred and two hundred and second form). It takes a structure (a: fields at a->8, a->4, and a->0); it checks the value's state, and performs an atomic decrement based on the result. This is the "process this data" routine (two thousand four hundred and one hundred and two hundred and second form), part of the 0x82B60 process cluster. 80 lines. 0 callees. 8 callers.
[entrypoint.functions."0x82B607A0"]
name = "ProcessAndProcessAndProcess2651_82B607A0"

# Process a value by checking its state and driving sub-operations (two thousand four hundred and one hundred and two hundred and third form). It takes a value (a), a value (b), and a float (c); it checks the value's state, and drives sub-operations (GetGlobalField19764, sub_82C13050) based on the result. This is the "process this data" routine (two thousand four hundred and one hundred and two hundred and third form), part of the 0x82BFF process cluster. 40 lines. 2 callees. 2 callers.
[entrypoint.functions."0x82BFFD30"]
name = "ProcessAndProcessAndProcess2652_82BFFD30"

# Process a value by checking its state (two thousand four hundred and one hundred and two hundred and fourth form). It takes a value (a), a value (b), a value (c), and a float (d); it checks the value's state, and drives sub-operations (sub_82C4D790) based on the result. This is the "process this data" routine (two thousand four hundred and one hundred and two hundred and fourth form), part of the 0x82C13 process cluster. 4 lines. 1 callee. 1 caller (ProcessAndProcessAndProcess2652).
[entrypoint.functions."0x82C13050"]
name = "ProcessAndProcessAndProcess2653_82C13050"

# Process a value by checking its state (two thousand four hundred and one hundred and two hundred and fifth form). It uses global state to check its state, and returns a global pointer. This is the "process this data" routine (two thousand four hundred and one hundred and two hundred and fifth form), part of the 0x82C2E process cluster. 5 lines. 0 callees. 0 callers (vtable-dispatched).
[entrypoint.functions."0x82C2EDD8"]
name = "ProcessAndProcessAndProcess2654_82C2EDD8"

# Process a value by checking its state and driving sub-operations (two thousand four hundred and one hundred and two hundred and sixth form). It takes a structure (a: fields at a->4, a->8, a->12, a->0, and a->80); it checks the value's state, and drives sub-operations (ProcessAndProcessAndProcess964 and a virtual call) based on the result. This is the "process this data" routine (two thousand four hundred and one hundred and two hundred and sixth form), part of the 0x82C4C process cluster. 40 lines. 1 callee. 0 callers (vtable-dispatched).
[entrypoint.functions."0x82C4C6B8"]
name = "ProcessAndProcessAndProcess2655_82C4C6B8"

# Process a value by checking its state and driving many sub-operations (two thousand four hundred and one hundred and two hundred and seventh form). It takes a value (a), a value (b), and a float (c); it checks the value's state, and drives many sub-operations (ProcessAndCallMethods, ProcessAndCheckMethod, and more) based on the result. This is the "process this data" routine (two thousand four hundred and one hundred and two hundred and seventh form), part of the 0x82C4D process cluster. 67 lines. 2 callees. 1 caller (ProcessAndProcessAndProcess2653).
[entrypoint.functions."0x82C4D790"]
name = "ProcessAndProcessAndProcess2656_82C4D790"

# Process a value by checking its state (two thousand four hundred and one hundred and two hundred and eighth form). It takes a value (a), a value (b), and a value (c); it checks the value's state, and drives sub-operations (sub_82C566A0) based on the result. This is the "process this data" routine (two thousand four hundred and one hundred and two hundred and eighth form), part of the 0x82C51 process cluster. 4 lines. 1 callee. 0 callers (vtable-dispatched).
[entrypoint.functions."0x82C51D80"]
name = "ProcessAndProcessAndProcess2657_82C51D80"

# Process a value by checking its state and driving many sub-operations (two thousand four hundred and one hundred and two hundred and ninth form). It takes a value (a, a type code), a value (b), and a float (c); it checks the value's state, and drives many sub-operations based on the result, dispatching on the type code. This is the "process this data" routine (two thousand four hundred and one hundred and two hundred and ninth form), part of the 0x82C56 process cluster. 218 lines. 2 callees. 1 caller (ProcessAndProcessAndProcess2657).
[entrypoint.functions."0x82C566A0"]
name = "ProcessAndProcessAndProcess2658_82C566A0"

# Process a value by checking its state (two thousand four hundred and one hundred and two hundred and tenth form). It takes a structure (a: fields at a->8 and a->4) and a value (b); it checks the value's state, and stores the value and a global pointer into the structure's fields. This is the "process this data" routine (two thousand four hundred and one hundred and two hundred and tenth form), part of the 0x82D9E process cluster. 10 lines. 0 callees. 12 callers.
[entrypoint.functions."0x82D9E708"]
name = "ProcessAndProcessAndProcess2659_82D9E708"

# Process a value by checking its state and driving sub-operations (two thousand four hundred and one hundred and two hundred and eleventh form). It takes a structure (a: fields at a->12, a->1024, and a->0); it checks the value's state, and drives sub-operations (ProcessAndProcessAndProcess2533) based on the result, looping over elements. This is the "process this data" routine (two thousand four hundred and one hundred and two hundred and eleventh form), part of the 0x82E82 process cluster. 116 lines. 1 callee. 1 caller (sub_82E824A0).
[entrypoint.functions."0x82E820D8"]
name = "ProcessAndProcessAndProcess2660_82E820D8"

# Process a value by checking its state and driving many sub-operations (two thousand four hundred and one hundred and two hundred and twelfth form). It takes a structure (a: fields at a->12 and a->0), a value (b), a value (c), a value (d), a value (e), and a value (f); it checks the value's state, and drives many sub-operations (ProcessAndProcessAndProcess2660, ProcessAndProcessAndProcess2533, and more) based on the result. This is the "process this data" routine (two thousand four hundred and one hundred and two hundred and twelfth form), part of the 0x82E82 process cluster. 118 lines. 3 callees. 3 callers.
[entrypoint.functions."0x82E824A0"]
name = "ProcessAndProcessAndProcess2661_82E824A0"

# Process a value by checking its state and driving sub-operations (two thousand four hundred and one hundred and two hundred and thirteenth form). It takes a structure (a: fields at a->0, a->32, a->4, and a->80); it checks the value's state, and drives sub-operations (RtlInitializeCriticalSectionAndSpinCount) based on the result, initializing a structure. This is the "process this data" routine (two thousand four hundred and one hundred and two hundred and thirteenth form), part of the 0x82FFC process cluster. 66 lines. 1 callee. 12 callers.
[entrypoint.functions."0x82FFCB78"]
name = "ProcessAndProcessAndProcess2662_82FFCB78"

# Process a value by checking its state and driving sub-operations (two thousand four hundred and one hundred and two hundred and fourteenth form). It takes a structure (a), a value (b), and a value (c); it checks the value's state, and drives sub-operations (ProcessAndProcessAndProcess2095, sub_82FFCC98) based on the result. This is the "process this data" routine (two thousand four hundred and one hundred and two hundred and fourteenth form), part of the 0x82FFC process cluster. 45 lines. 2 callees. 2 callers.
[entrypoint.functions."0x82FFCC38"]
name = "ProcessAndProcessAndProcess2663_82FFCC38"

# Process a value by checking its state and driving sub-operations (two thousand four hundred and one hundred and two hundred and fifteenth form). It takes a structure (a), a value (b), and a value (c); it checks the value's state, and drives sub-operations (ProcessAndProcessAndProcess2095, sub_82FFE3D8) based on the result. This is the "process this data" routine (two thousand four hundred and one hundred and two hundred and fifteenth form), part of the 0x82FFC process cluster. 45 lines. 2 callees. 1 caller (ProcessAndProcessAndProcess2663).
[entrypoint.functions."0x82FFCC98"]
name = "ProcessAndProcessAndProcess2664_82FFCC98"

# Process a value by checking its state (two thousand four hundred and one hundred and two hundred and sixteenth form). It takes a value (a) and a value (b); it checks the value's state, and stores the values, returning one of them. This is the "process this data" routine (two thousand four hundred and one hundred and two hundred and sixteenth form), part of the 0x82FFC process cluster. 8 lines. 0 callees. 1 caller (sub_82FFE3D8).
[entrypoint.functions."0x82FFCCF8"]
name = "ProcessAndProcessAndProcess2665_82FFCCF8"

# Process a value by checking its state and driving sub-operations (two thousand four hundred and one hundred and two hundred and seventeenth form). It takes a structure (a: field at a->4); it checks the value's state, and drives sub-operations (ProcessAndProcessAndProcess2534) based on the result. This is the "process this data" routine (two thousand four hundred and one hundred and two hundred and seventeenth form), part of the 0x82FFC process cluster. 20 lines. 1 callee. 6 callers.
[entrypoint.functions."0x82FFCD08"]
name = "ProcessAndProcessAndProcess2666_82FFCD08"

# Process a value by checking its state and driving sub-operations (two thousand four hundred and one hundred and two hundred and eighteenth form). It takes a structure (a: field at a->4); it checks the value's state, and drives sub-operations (ProcessAndProcessAndProcess2422) based on the result. This is the "process this data" routine (two thousand four hundred and one hundred and two hundred and eighteenth form), part of the 0x82FFC process cluster. 20 lines. 1 callee. 7 callers.
[entrypoint.functions."0x82FFCD48"]
name = "ProcessAndProcessAndProcess2667_82FFCD48"

# Process a value by checking its state and driving sub-operations (two thousand four hundred and one hundred and two hundred and nineteenth form). It takes a structure (a: field at a->4); it checks the value's state, and drives sub-operations (ProcessAndProcessAndProcess2535) based on the result. This is the "process this data" routine (two thousand four hundred and one hundred and two hundred and nineteenth form), part of the 0x82FFC process cluster. 20 lines. 1 callee. 10 callers.
[entrypoint.functions."0x82FFCF18"]
name = "ProcessAndProcessAndProcess2668_82FFCF18"

# Process a value by checking its state and driving sub-operations (two thousand four hundred and one hundred and two hundred and twentieth form). It takes a structure (a), a structure (b: field at b->0), and a value (c); it checks the value's state, and drives sub-operations (ProcessAndProcessAndProcess2665, ProcessAndProcessAndProcess2095) based on the result. This is the "process this data" routine (two thousand four hundred and one hundred and two hundred and twentieth form), part of the 0x82FFE process cluster. 35 lines. 2 callees. 1 caller (ProcessAndProcessAndProcess2664).
[entrypoint.functions."0x82FFE3D8"]
name = "ProcessAndProcessAndProcess2669_82FFE3D8"

# Process a value by checking its state and driving many sub-operations (two thousand four hundred and one hundred and two hundred and twenty-first form). It takes a value (a), a value (b), and a float (c); it checks the value's state, and drives many sub-operations (FNV1Hash, ProcessAndProcessAndProcess1754, and more) based on the result. This is the "process this data" routine (two thousand four hundred and one hundred and two hundred and twenty-first form), part of the 0x82196 process cluster. 72 lines. 2 callees. 12 callers.
[entrypoint.functions."0x82196E10"]
name = "ProcessAndProcessAndProcess2670_82196E10"

# Process a value by checking its state and driving sub-operations (two thousand four hundred and one hundred and two hundred and twenty-second form). It takes a structure (a: fields at a->0 and a->4); it checks the value's state, and drives sub-operations (sub_821E3E10) based on the result. This is the "process this data" routine (two thousand four hundred and one hundred and two hundred and twenty-second form), part of the 0x821B2 process cluster. 71 lines. 1 callee. 12 callers.
[entrypoint.functions."0x821B2710"]
name = "ProcessAndProcessAndProcess2671_821B2710"

# Process a value by checking its state (two thousand four hundred and one hundred and two hundred and twenty-third form). It takes a value (a), a value (b), a structure (c: field at c->0), and a structure (d: field at d->0); it checks the value's state, and processes values based on the result, storing a structure pointer. This is the "process this data" routine (two thousand four hundred and one hundred and two hundred and twenty-third form), part of the 0x8229C process cluster. 184 lines. 0 callees. 1 caller (ProcessAndProcessAndProcess1754).
[entrypoint.functions."0x8229CA50"]
name = "ProcessAndProcessAndProcess2672_8229CA50"

# Process a value by checking its state and driving sub-operations (two thousand four hundred and one hundred and two hundred and twenty-fourth form). It takes a value (a), a structure (b: fields at b->4 and b->8), and a value (c); it checks the value's state, and drives sub-operations (sub_82308690) based on the result, looping over elements. This is the "process this data" routine (two thousand four hundred and one hundred and two hundred and twenty-fourth form), part of the 0x82307 process cluster. 132 lines. 1 callee. 1 caller (ProcessAndProcessAndProcess2121).
[entrypoint.functions."0x823076D8"]
name = "ProcessAndProcessAndProcess2673_823076D8"

# Process a value by checking its state and driving many sub-operations (two thousand four hundred and one hundred and two hundred and twenty-fifth form). It takes a structure (a: fields at a->4 and a->12), a value (b), a value (c), and a value (d); it checks the value's state, and drives many sub-operations (CopyAssign_RefCounted, ConstructArray4, Free_SizeBucketed, and more) based on the result. This is the "process this data" routine (two thousand four hundred and one hundred and two hundred and twenty-fifth form), part of the 0x82308 process cluster. 550 lines. 7 callees. 2 callers.
[entrypoint.functions."0x82308690"]
name = "ProcessAndProcessAndProcess2674_82308690"

# Check a global flag and entity type (must be >= 2), then dereference a global context chain and call an indirect vtable method. Used to invoke a method on the active player entity. 90 lines. 0 direct callees. Called from the main game loop.
[entrypoint.functions."0x821A11E8"]
name = "CallActivePlayerMethod_821A11E8"

# Complex per-frame game state update. Accesses structure fields at offsets 88 and 140, calls multiple sub-functions (sub_8218CA10, sub_82231A70, sub_828581B0, ProcessAndProcessAndProcess1290) and an indirect vtable method. 190 lines. 10+ callees. Called from the main game loop.
[entrypoint.functions."0x821C9008"]
name = "ProcessGameStateUpdate_821C9008"

# Dispatch a per-frame callback via vtable. Takes the game state structure, a frame counter, and a float (delta time). Dereferences structure->128->4->8 and calls an indirect vtable method. 132 lines. 1 callee. Called from the main game loop.
[entrypoint.functions."0x821A6010"]
name = "DispatchFrameCallback_821A6010"

# Register or update a frame callback. Takes a structure and a result pointer (from ResolveSubsystemReference). Checks a global flag, then calls sub_82265F50 and sub_82422090. 197 lines. 2 callees. Called from the main game loop.
[entrypoint.functions."0x821DCF10"]
name = "RegisterFrameCallback_821DCF10"

# Query a method on the active player entity. Checks a global flag, dereferences a global context chain, calls an indirect vtable method, and returns the result. 114 lines. 0 direct callees. Called from the main game loop.
[entrypoint.functions."0x821EC8A0"]
name = "QueryActivePlayerMethod_821EC8A0"

# Resolve a subsystem reference. Dereferences global->12->140, checks flags at offsets 52 and 54, calls ProcessAndProcessAndProcess1124, and returns a pointer. 117 lines. 1 callee. Called twice from the main game loop.
[entrypoint.functions."0x821F8760"]
name = "ResolveSubsystemReference_821F8760"

# Complex game entity initialization. Takes the game state and a constructed reference. Calls ProcessAndProcessAndProcess2254, ProcessAndProcessAndProcess938, ProcessAndProcessAndProcess1309, and sub_822F9EC0. 199 lines. 5+ callees. Called from the main game loop.
[entrypoint.functions."0x8233CAC8"]
name = "InitializeGameEntity_8233CAC8"

# The main per-frame game loop. 512-byte stack frame. Processes timing, calls all major per-frame subsystems (notifications, player update, state update, callbacks, yield). 1052 lines. 20+ callees. Called once per frame.
[entrypoint.functions."0x82276C30"]
name = "ProcessGameFrame_82276C30"

# Dispatch OS notifications. Uses XNotifyGetNext to retrieve pending notifications and dispatches by type (9, 10, 11, >11). 272 lines. 10 callees. Called from the main game loop.
[entrypoint.functions."0x82185080"]
name = "DispatchOsNotifications_82185080"

# Existing Functions

# The main per-frame render/present loop (one call per frame: ~30/s at the
# 30 fps cap). Presents the frame via VdSwap and
# calls the frame limiter (0x82242628), the per-frame helpers, and the
# 60 FPS dispatch function (0x82B9C7F8). fps_meter.h hooks this symbol.
[entrypoint.functions."0x82B9CD68"]
name = "MainRenderLoop_82B9CD68"
[entrypoint.functions."0x8222B3C8"]
name = "SwitchDispatch_8222B3C8"
size = 0x00F8

[entrypoint.functions."0x822A2AB0"]
name = "VtableSlotX3173_822A2AB0"
size = 0x00F0

[entrypoint.functions."0x822D7C50"]
name = "SwitchDispatch_822D7C50"
size = 0x0060

[entrypoint.functions."0x822D7CB0"]
name = "SwitchDispatch_822D7CB0"
size = 0x0078

[entrypoint.functions."0x822D7D58"]
name = "SwitchDispatch_822D7D58"
size = 0x00A0

[entrypoint.functions."0x822D7DF8"]
name = "SwitchDispatch_822D7DF8"
size = 0x00A8

[entrypoint.functions."0x822D7EA0"]
name = "SwitchDispatch_822D7EA0"
size = 0x00E8

[entrypoint.functions."0x822EAA98"]
name = "SwitchDispatch_822EAA98"
size = 0x0098

[entrypoint.functions."0x823A5A28"]
name = "SwitchDispatch_823A5A28"
size = 0x0118

[entrypoint.functions."0x824063B0"]
name = "SwitchDispatch_824063B0"
size = 0x00F0

[entrypoint.functions."0x82415A20"]
name = "SwitchDispatch_82415A20"
size = 0x01B8

[entrypoint.functions."0x8241BAA8"]
name = "SwitchDispatch_8241BAA8"
size = 0x0120

[entrypoint.functions."0x8242C5E0"]
name = "SwitchDispatch_8242C5E0"
size = 0x0088

[entrypoint.functions."0x824B7E70"]
name = "SwitchDispatch_824B7E70"
size = 0x0090

[entrypoint.functions."0x824BDE60"]
name = "SwitchDispatch_824BDE60"
size = 0x00A8

[entrypoint.functions."0x824CAC90"]
name = "SwitchDispatch_824CAC90"
size = 0x0158

[entrypoint.functions."0x824D2800"]
name = "SwitchDispatch_824D2800"
size = 0x0080

[entrypoint.functions."0x8250B778"]
name = "SwitchDispatch_8250B778"
size = 0x0058

[entrypoint.functions."0x82520C00"]
name = "SwitchDispatch_82520C00"
size = 0x0070

[entrypoint.functions."0x82522E18"]
name = "SwitchDispatch_82522E18"
size = 0x0178

[entrypoint.functions."0x82592C80"]
name = "SwitchDispatch_82592C80"
size = 0x0088

[entrypoint.functions."0x8259AF48"]
name = "SwitchDispatch_8259AF48"
size = 0x0080

[entrypoint.functions."0x825A0B58"]
name = "SwitchDispatch_825A0B58"
size = 0x0090

[entrypoint.functions."0x82622360"]
name = "SwitchDispatch_82622360"
size = 0x00C0

[entrypoint.functions."0x826224B8"]
name = "SwitchDispatch_826224B8"
size = 0x00B0

[entrypoint.functions."0x8272B2C0"]
name = "SwitchDispatch_8272B2C0"
size = 0x0168

[entrypoint.functions."0x82771250"]
name = "SwitchDispatch_82771250"
size = 0x0070

[entrypoint.functions."0x827D30C0"]
name = "SwitchDispatch_827D30C0"
size = 0x02E0

[entrypoint.functions."0x8283A008"]
name = "SwitchDispatch_8283A008"
size = 0x0088

[entrypoint.functions."0x8283FF98"]
name = "SwitchDispatch_8283FF98"
size = 0x00D0

[entrypoint.functions."0x8289FB40"]
name = "SwitchDispatch_8289FB40"
size = 0x02C8

[entrypoint.functions."0x8299F668"]
name = "SwitchDispatch_8299F668"
size = 0x0090

[entrypoint.functions."0x82A2F3E8"]
name = "SwitchDispatch_82A2F3E8"
size = 0x0118

[entrypoint.functions."0x82A34680"]
name = "SwitchDispatch_82A34680"
size = 0x0480

[entrypoint.functions."0x82B38270"]
name = "SwitchDispatch_82B38270"
size = 0x0110

[entrypoint.functions."0x82B57148"]
name = "SwitchDispatch_82B57148"
size = 0x02E0

[entrypoint.functions."0x82B809E8"]
name = "SwitchDispatch_82B809E8"
size = 0x0230

[entrypoint.functions."0x82B8F5E8"]
name = "SwitchDispatch_82B8F5E8"
size = 0x0128

[entrypoint.functions."0x82BC8260"]
name = "SwitchDispatch_82BC8260"
size = 0x0080

[entrypoint.functions."0x82BC9B28"]
name = "SwitchDispatch_82BC9B28"
size = 0x0128

[entrypoint.functions."0x82BE0100"]
name = "SwitchDispatch_82BE0100"
size = 0x00A8

[entrypoint.functions."0x82BE9668"]
name = "TailCall_82BE9668"
size = 0x0018  # was 0x08D4 in XenonRecomp; trimmed to avoid overlap with 0x82BE9680

[entrypoint.functions."0x82BE9680"]
name = "VtableSlotX1_82BE9680"
size = 0x08BC

[entrypoint.functions."0x82C56828"]
name = "SwitchDispatch_82C56828"
size = 0x0078

[entrypoint.functions."0x82C89840"]
name = "SwitchDispatch_82C89840"
size = 0x0078  # was 0x01D8 in XenonRecomp; trimmed to avoid overlap with 0x82C898B8

[entrypoint.functions."0x82C898B8"]
name = "SwitchDispatch_82C898B8"
size = 0x00A8  # was 0x0154 in XenonRecomp; trimmed to avoid overlap with 0x82C89960

[entrypoint.functions."0x82C89960"]
name = "SwitchDispatch_82C89960"
size = 0x00AC

[entrypoint.functions."0x82C8C9F0"]
name = "SwitchDispatch_82C8C9F0"
size = 0x0150

[entrypoint.functions."0x82C8CF00"]
name = "SwitchDispatch_82C8CF00"
size = 0x0100

[entrypoint.functions."0x82C8D3F0"]
name = "SwitchDispatch_82C8D3F0"
size = 0x01E8  # original XenonRecomp size; nested 0x82C8D4E8 entry removed (see comment above)

[entrypoint.functions."0x82C8D630"]
name = "Str_BoundedCopy_82C8D630"
size = 0x00D8

[entrypoint.functions."0x82C8D750"]
name = "SwitchDispatch_82C8D750"
size = 0x00D0

[entrypoint.functions."0x82C8DAE8"]
name = "SwitchDispatch_82C8DAE8"
size = 0x00F8

[entrypoint.functions."0x82C97120"]
name = "SwitchDispatch_82C97120"
size = 0x0098

[entrypoint.functions."0x82C99CB8"]
name = "SwitchDispatch_82C99CB8"
size = 0x0180

[entrypoint.functions."0x82C99ED0"]
name = "SwitchDispatch_82C99ED0"
size = 0x0190

[entrypoint.functions."0x82C9A118"]
name = "SwitchDispatch_82C9A118"
size = 0x0190

[entrypoint.functions."0x82C9B420"]
name = "SwitchDispatch_82C9B420"
size = 0x00D0

[entrypoint.functions."0x82C9D280"]
name = "SwitchDispatch_82C9D280"
size = 0x0198

[entrypoint.functions."0x82C9DB00"]
name = "SwitchDispatch_82C9DB00"
size = 0x0130

[entrypoint.functions."0x82C9E878"]
name = "SwitchDispatch_82C9E878"
size = 0x00E0

[entrypoint.functions."0x82C9E9B8"]
name = "SwitchDispatch_82C9E9B8"
size = 0x0108

[entrypoint.functions."0x82CA1A00"]
name = "SwitchDispatch_82CA1A00"
size = 0x0118

[entrypoint.functions."0x82CA1E98"]
name = "SwitchDispatch_82CA1E98"
size = 0x0140

[entrypoint.functions."0x82D7F5B0"]
name = "SwitchDispatch_82D7F5B0"
size = 0x0158

[entrypoint.functions."0x82D82B48"]
name = "SwitchDispatch_82D82B48"
size = 0x01A0

[entrypoint.functions."0x82D97098"]
name = "SwitchDispatch_82D97098"
size = 0x03F0

[entrypoint.functions."0x82D9E998"]
name = "SwitchDispatch_82D9E998"
size = 0x0058

[entrypoint.functions."0x82D9E9F0"]
name = "SwitchDispatch_82D9E9F0"
size = 0x0074

[entrypoint.functions."0x82DA60B0"]
name = "SwitchDispatch_82DA60B0"
size = 0x0320

[entrypoint.functions."0x82DDC208"]
name = "SwitchDispatch_82DDC208"
size = 0x0250

[entrypoint.functions."0x82DE4C10"]
name = "SwitchDispatch_82DE4C10"
size = 0x01E8

[entrypoint.functions."0x82DE6750"]
name = "SwitchDispatch_82DE6750"
size = 0x00F8

[entrypoint.functions."0x82E0F3A8"]
name = "SwitchDispatch_82E0F3A8"
size = 0x00D0

[entrypoint.functions."0x82E33B90"]
name = "SwitchDispatch_82E33B90"
size = 0x00B8

[entrypoint.functions."0x82FE7850"]
name = "VtableSlotX2_82FE7850"
size = 0x0108

[entrypoint.functions."0x83046BE8"]
name = "VtableSlotX2_83046BE8"
size = 0x0118

[entrypoint.functions."0x83095890"]
name = "IndirectCall_83095890"
size = 0x0110

[entrypoint.functions."0x8310FC68"]
name = "IndirectCall_8310FC68"
size = 0x0080

[entrypoint.functions."0x831B31B8"]
name = "IndirectCall_831B31B8"
size = 0x0078

[entrypoint.functions."0x831FBA60"]
name = "IndirectCall_831FBA60"
size = 0x0068

[entrypoint.functions."0x831FBAC8"]
name = "IndirectCall_831FBAC8"
size = 0x0068

[entrypoint.functions."0x831FBB30"]
name = "IndirectCall_831FBB30"
size = 0x0060

[entrypoint.functions."0x831FBB90"]
name = "IndirectCall_831FBB90"
size = 0x0198

# Gap targets: branches from discovered functions land at addresses the
# auto-discovery never registered as functions. Seeded here without a size so
# the scanner determines the extent (added 2026-09-19 after
# "Validation failed: 12 unresolved calls").
[entrypoint.functions."0x82BEA25C"]
name = "TailCall_82BEA25C"
[entrypoint.functions."0x82CD7948"]
name = "Func_82CD7948"
[entrypoint.functions."0x82C00108"]
name = "TailCall_82C00108"
[entrypoint.functions."0x82C00118"]
name = "TailCall_82C00118"
[entrypoint.functions."0x82C14D58"]
name = "TailCall_82C14D58"
[entrypoint.functions."0x82F279D8"]
name = "RetConst1_82F279D8"
[entrypoint.functions."0x82E7E4F8"]
name = "TailCall_82E7E4F8"
[entrypoint.functions."0x82C0B680"]
name = "TailCall_82C0B680"
[entrypoint.functions."0x82C00120"]
name = "TailCall_82C00120"
[entrypoint.functions."0x82C00110"]
name = "TailCall_82C00110"
[entrypoint.functions."0x82C00128"]
name = "TailCall_82C00128"
[entrypoint.functions."0x82C0B688"]
name = "TailCall_82C0B688"


# Additional function-pointer-table entries (broader scan: in-body code-range
# pointers stored within +-20B of >=2 registered starts). Fixes the 0x829FCB00
# FATAL (2026-09-02) and pre-seeds similar table entries.
[entrypoint.functions."0x82238828"]
name = "Heap_AllocateBlock_82238828"
[entrypoint.functions."0x82238FCC"]
name = "TailCall_82238FCC"
[entrypoint.functions."0x822394F0"]
name = "Heap_FreeBlock_822394F0"
[entrypoint.functions."0x822396C8"]
name = "TailCall_822396C8"
[entrypoint.functions."0x829FCB00"]
name = "VtableSlotX3_829FCB00"
[entrypoint.functions."0x82B387D0"]
name = "Func_82B387D0"
[entrypoint.functions."0x82B387E8"]
name = "Func_82B387E8"
[entrypoint.functions."0x82C59698"]
name = "Func_82C59698"
[entrypoint.functions."0x82C5AC4C"]
name = "Func_82C5AC4C"
[entrypoint.functions."0x82C5CFBC"]
name = "TailCall_82C5CFBC"
[entrypoint.functions."0x82C5DA68"]
name = "Func_82C5DA68"
[entrypoint.functions."0x82C5DA94"]
name = "Func_82C5DA94"
[entrypoint.functions."0x82C89A0C"]
name = "Func_82C89A0C"
[entrypoint.functions."0x82CA33AC"]
name = "Func_82CA33AC"
[entrypoint.functions."0x82CA33E4"]
name = "Func_82CA33E4"
[entrypoint.functions."0x82CA3614"]
name = "Func_82CA3614"
[entrypoint.functions."0x82CA36C4"]
name = "TailCall_82CA36C4"
[entrypoint.functions."0x82CA47C4"]
name = "Func_82CA47C4"
[entrypoint.functions."0x82CA4814"]
name = "Func_82CA4814"
[entrypoint.functions."0x82CA4A64"]
name = "Func_82CA4A64"
[entrypoint.functions."0x82CA4A70"]
name = "Func_82CA4A70"
[entrypoint.functions."0x82CA4DE4"]
name = "Func_82CA4DE4"
[entrypoint.functions."0x82CA4DFC"]
name = "TailCall_82CA4DFC"
[entrypoint.functions."0x82CA4FB0"]
name = "Func_82CA4FB0"
[entrypoint.functions."0x82CA5004"]
name = "Func_82CA5004"
[entrypoint.functions."0x82CA5064"]
name = "TailCall_82CA5064"
[entrypoint.functions."0x82CA507C"]
name = "Func_82CA507C"
[entrypoint.functions."0x82CA51C0"]
name = "Func_82CA51C0"
[entrypoint.functions."0x82CA51CC"]
name = "Func_82CA51CC"
[entrypoint.functions."0x82CA53BC"]
name = "Func_82CA53BC"
[entrypoint.functions."0x82CA53D0"]
name = "TailCall_82CA53D0"
[entrypoint.functions."0x82CA56DC"]
name = "Func_82CA56DC"
[entrypoint.functions."0x82CA56E8"]
name = "Func_82CA56E8"
[entrypoint.functions."0x82CA71C8"]
name = "Func_82CA71C8"
[entrypoint.functions."0x82CA7300"]
name = "TailCall_82CA7300"
[entrypoint.functions."0x82CA7C70"]
name = "Func_82CA7C70"
[entrypoint.functions."0x82CA7C90"]
name = "TailCall_82CA7C90"
[entrypoint.functions."0x82CA7C9C"]
name = "TailCall_82CA7C9C"
[entrypoint.functions."0x82CA8038"]
name = "Func_82CA8038"
[entrypoint.functions."0x82CA8060"]
name = "TailCall_82CA8060"
[entrypoint.functions."0x82CA806C"]
name = "TailCall_82CA806C"
[entrypoint.functions."0x82CA8128"]
name = "Func_82CA8128"
[entrypoint.functions."0x82CA8154"]
name = "TailCall_82CA8154"
[entrypoint.functions."0x82CA8248"]
name = "Func_82CA8248"
[entrypoint.functions."0x82CA827C"]
name = "TailCall_82CA827C"
[entrypoint.functions."0x82CA8B14"]
name = "Func_82CA8B14"
[entrypoint.functions."0x82CA8B44"]
name = "Func_82CA8B44"
[entrypoint.functions."0x82CA9064"]
name = "Func_82CA9064"
[entrypoint.functions."0x82CA91E8"]
name = "TailCall_82CA91E8"
[entrypoint.functions."0x82CA9660"]
name = "BugCheck_82CA9660"
[entrypoint.functions."0x82CA9710"]
name = "BugCheck_82CA9710"
[entrypoint.functions."0x82CAB2B8"]
name = "Func_82CAB2B8"
[entrypoint.functions."0x82CAB2D4"]
name = "TailCall_82CAB2D4"
[entrypoint.functions."0x82CAF2C0"]
name = "Sync_82CAF2C0"
[entrypoint.functions."0x82CAF40C"]
name = "TailCall_82CAF40C"
[entrypoint.functions."0x82CAF620"]
name = "Func_82CAF620"
[entrypoint.functions."0x82CAF658"]
name = "TailCall_82CAF658"
[entrypoint.functions."0x82CAFAB0"]
name = "Func_82CAFAB0"
[entrypoint.functions."0x82CAFAFC"]
name = "TailCall_82CAFAFC"
[entrypoint.functions."0x82CAFC28"]
name = "Func_82CAFC28"
[entrypoint.functions."0x82CAFC88"]
name = "TailCall_82CAFC88"
[entrypoint.functions."0x82CAFE98"]
name = "Func_82CAFE98"
[entrypoint.functions."0x82CAFEE4"]
name = "TailCall_82CAFEE4"
[entrypoint.functions."0x82CB0138"]
name = "Func_82CB0138"
[entrypoint.functions."0x82CB0184"]
name = "TailCall_82CB0184"
[entrypoint.functions."0x82CB0FB0"]
name = "Func_82CB0FB0"
[entrypoint.functions."0x82CB1040"]
name = "Func_82CB1040"
[entrypoint.functions."0x82CB1390"]
name = "Func_82CB1390"
[entrypoint.functions."0x82CB13EC"]
name = "Func_82CB13EC"
[entrypoint.functions."0x82CB4938"]
name = "Func_82CB4938"
[entrypoint.functions."0x82CB4974"]
name = "Func_82CB4974"
[entrypoint.functions."0x82CB49B0"]
name = "TailCall_82CB49B0"
[entrypoint.functions."0x82CB49C0"]
name = "Func_82CB49C0"
[entrypoint.functions."0x82CB49DC"]
name = "Func_82CB49DC"
[entrypoint.functions."0x82CB4B1C"]
name = "Func_82CB4B1C"
[entrypoint.functions."0x82CB4B28"]
name = "Func_82CB4B28"
[entrypoint.functions."0x82CB4B38"]
name = "Func_82CB4B38"
[entrypoint.functions."0x82CB4CB8"]
name = "Func_82CB4CB8"
[entrypoint.functions."0x82CB4E9C"]
name = "Func_82CB4E9C"
[entrypoint.functions."0x82CB4EAC"]
name = "TailCall_82CB4EAC"
[entrypoint.functions."0x82CB4F18"]
name = "Func_82CB4F18"
[entrypoint.functions."0x82CB4FDC"]
name = "TailCall_82CB4FDC"
[entrypoint.functions."0x82CB4FEC"]
name = "TailCall_82CB4FEC"
[entrypoint.functions."0x82CB56B0"]
name = "Func_82CB56B0"
[entrypoint.functions."0x82CB56BC"]
name = "Func_82CB56BC"
[entrypoint.functions."0x82CB56CC"]
name = "Func_82CB56CC"
[entrypoint.functions."0x82CB57CC"]
name = "BugCheck_82CB57CC"
[entrypoint.functions."0x82CB57D4"]
name = "BugCheck_82CB57D4"
[entrypoint.functions."0x82CB57E0"]
name = "BugCheck_82CB57E0"
[entrypoint.functions."0x82CB582C"]
name = "Func_82CB582C"
[entrypoint.functions."0x82CB5834"]
name = "Func_82CB5834"
[entrypoint.functions."0x82CB5840"]
name = "Func_82CB5840"
[entrypoint.functions."0x82CB5894"]
name = "Func_82CB5894"
[entrypoint.functions."0x82CB5918"]
name = "TailCall_82CB5918"
[entrypoint.functions."0x82CB5BB4"]
name = "Func_82CB5BB4"
[entrypoint.functions."0x82CB5BC0"]
name = "Func_82CB5BC0"
[entrypoint.functions."0x82CB5BD0"]
name = "Func_82CB5BD0"
[entrypoint.functions."0x82CB68A8"]
name = "Func_82CB68A8"
[entrypoint.functions."0x82CB68F4"]
name = "TailCall_82CB68F4"
[entrypoint.functions."0x82CB6C48"]
name = "Func_82CB6C48"
[entrypoint.functions."0x82CB6C90"]
name = "TailCall_82CB6C90"
[entrypoint.functions."0x82CB89E8"]
name = "Func_82CB89E8"
[entrypoint.functions."0x82CB8A08"]
name = "Func_82CB8A08"
[entrypoint.functions."0x82CB8D48"]
name = "Sync_82CB8D48"
[entrypoint.functions."0x82CB8D7C"]
name = "Sync_82CB8D7C"
[entrypoint.functions."0x82CB8E64"]
name = "Sync_82CB8E64"
[entrypoint.functions."0x82CB8ECC"]
name = "Sync_82CB8ECC"
[entrypoint.functions."0x82CB8F00"]
name = "Sync_82CB8F00"
[entrypoint.functions."0x82CB9018"]
name = "TailCall_82CB9018"
[entrypoint.functions."0x82CBA360"]
name = "Func_82CBA360"
[entrypoint.functions."0x82CBA370"]
name = "TailCall_82CBA370"
[entrypoint.functions."0x82CBA76C"]
name = "Func_82CBA76C"
[entrypoint.functions."0x82CBA8D0"]
name = "TailCall_82CBA8D0"
[entrypoint.functions."0x82CBAE6C"]
name = "Func_82CBAE6C"
[entrypoint.functions."0x82CBAE88"]
name = "Func_82CBAE88"
[entrypoint.functions."0x82CBAF00"]
name = "Func_82CBAF00"
[entrypoint.functions."0x82CBAF0C"]
name = "Func_82CBAF0C"
[entrypoint.functions."0x82CBB98C"]
name = "FatalError_82CBB98C"
[entrypoint.functions."0x82CBBB24"]
name = "FatalError_82CBBB24"
[entrypoint.functions."0x82CBBB30"]
name = "FatalError_82CBBB30"
[entrypoint.functions."0x82CBC820"]
name = "FsVolume_82CBC820"
[entrypoint.functions."0x82CBC888"]
name = "Func_82CBC888"
[entrypoint.functions."0x82CBC930"]
name = "Func_82CBC930"
[entrypoint.functions."0x82CBC940"]
name = "Func_82CBC940"
[entrypoint.functions."0x82CBC97C"]
name = "Func_82CBC97C"
[entrypoint.functions."0x82CBF740"]
name = "MemMgr_82CBF740"
[entrypoint.functions."0x82CBF760"]
name = "MemMgr_82CBF760"
[entrypoint.functions."0x82CBF770"]
name = "MemMgr_82CBF770"
[entrypoint.functions."0x82CBFD74"]
name = "MemFree_82CBFD74"
[entrypoint.functions."0x82CBFD9C"]
name = "MemFree_82CBFD9C"
[entrypoint.functions."0x82CC02FC"]
name = "Exception_82CC02FC"
[entrypoint.functions."0x82CC031C"]
name = "Exception_82CC031C"
[entrypoint.functions."0x82CC032C"]
name = "Exception_82CC032C"
[entrypoint.functions."0x82CC0494"]
name = "TailCall_82CC0494"
[entrypoint.functions."0x82CC04A4"]
name = "Func_82CC04A4"
[entrypoint.functions."0x82CC04B0"]
name = "TailCall_82CC04B0"
[entrypoint.functions."0x82CCA428"]
name = "Thread_82CCA428"
[entrypoint.functions."0x82CCA448"]
name = "Thread_82CCA448"
[entrypoint.functions."0x82CCA454"]
name = "Thread_82CCA454"
[entrypoint.functions."0x82CD1690"]
name = "TailCall_82CD1690"
[entrypoint.functions."0x82CD182C"]
name = "Func_82CD182C"
[entrypoint.functions."0x82CD18AC"]
name = "Func_82CD18AC"
[entrypoint.functions."0x83000858"]
name = "Func_83000858"
[entrypoint.functions."0x830009AC"]
name = "Func_830009AC"
[entrypoint.functions."0x83000AF0"]
name = "Func_83000AF0"
[entrypoint.functions."0x83000CF4"]
name = "Func_83000CF4"
[entrypoint.functions."0x83001060"]
name = "Func_83001060"
[entrypoint.functions."0x83001184"]
name = "TailCall_83001184"
[entrypoint.functions."0x83001318"]
name = "Func_83001318"
[entrypoint.functions."0x830013A8"]
name = "TailCall_830013A8"
[entrypoint.functions."0x83001514"]
name = "Func_83001514"
[entrypoint.functions."0x8300164C"]
name = "TailCall_8300164C"
[entrypoint.functions."0x8300172C"]
name = "Func_8300172C"
[entrypoint.functions."0x830017A0"]
name = "TailCall_830017A0"
[entrypoint.functions."0x83002974"]
name = "Func_83002974"
[entrypoint.functions."0x8300298C"]
name = "Func_8300298C"
[entrypoint.functions."0x83002E84"]
name = "Func_83002E84"
[entrypoint.functions."0x83002F18"]
name = "TailCall_83002F18"
[entrypoint.functions."0x8300338C"]
name = "Func_8300338C"
[entrypoint.functions."0x830033E0"]
name = "Func_830033E0"
[entrypoint.functions."0x83003414"]
name = "TailCall_83003414"
[entrypoint.functions."0x8300342C"]
name = "TailCall_8300342C"
[entrypoint.functions."0x830046B4"]
name = "Func_830046B4"
[entrypoint.functions."0x830046C0"]
name = "Func_830046C0"
[entrypoint.functions."0x83040FEC"]
name = "Func_83040FEC"
[entrypoint.functions."0x8304100C"]
name = "Func_8304100C"
[entrypoint.functions."0x8304101C"]
name = "Func_8304101C"
[entrypoint.functions."0x830F07A8"]
name = "Func_830F07A8"
[entrypoint.functions."0x830F07F8"]
name = "Func_830F07F8"
[entrypoint.functions."0x830F0914"]
name = "Func_830F0914"
[entrypoint.functions."0x830F0980"]
name = "TailCall_830F0980"
[entrypoint.functions."0x830F1208"]
name = "Func_830F1208"
[entrypoint.functions."0x830F1228"]
name = "Func_830F1228"
[entrypoint.functions."0x830FB43C"]
name = "Func_830FB43C"
[entrypoint.functions."0x830FB490"]
name = "Func_830FB490"
[entrypoint.functions."0x830FB4A0"]
name = "Func_830FB4A0"
[entrypoint.functions."0x830FD788"]
name = "Func_830FD788"
[entrypoint.functions."0x830FD7A8"]
name = "Func_830FD7A8"
[entrypoint.functions."0x830FD7B8"]
name = "Func_830FD7B8"
[entrypoint.functions."0x830FD7C8"]
name = "Func_830FD7C8"
[entrypoint.functions."0x830FD7E4"]
name = "Func_830FD7E4"
[entrypoint.functions."0x830FD7F4"]
name = "Func_830FD7F4"
[entrypoint.functions."0x830FDCEC"]
name = "Func_830FDCEC"
[entrypoint.functions."0x830FDCFC"]
name = "Func_830FDCFC"
[entrypoint.functions."0x830FDD08"]
name = "Func_830FDD08"
[entrypoint.functions."0x830FE168"]
name = "Func_830FE168"
[entrypoint.functions."0x830FE220"]
name = "Func_830FE220"
[entrypoint.functions."0x830FE230"]
name = "Func_830FE230"
[entrypoint.functions."0x830FE2E8"]
name = "Func_830FE2E8"
[entrypoint.functions."0x830FE380"]
name = "Func_830FE380"
[entrypoint.functions."0x830FE390"]
name = "Func_830FE390"
[entrypoint.functions."0x83117EDC"]
name = "Func_83117EDC"
[entrypoint.functions."0x83117F88"]
name = "Func_83117F88"
[entrypoint.functions."0x83117F94"]
name = "Func_83117F94"

# Function-pointer-table entries (found by scanning the decrypted XEX image for
# data words in the code range that are not registered function starts). The
# dispatcher can only call registered starts, so these mid-function pointer
# targets are seeded as functions here (scanner sizes them). First hit:
# [FATAL] Call to invalid or unregistered function at 0x82CD8650 (2026-09-19).
[entrypoint.functions."0x82267568"]
name = "RetAddr_82267568"
[entrypoint.functions."0x82267C88"]
name = "RetAddr_82267C88"
[entrypoint.functions."0x824D56C8"]
name = "Math_FloatIndexStore_824D56C8"
[entrypoint.functions."0x824F0600"]
name = "VtableSlot29_824F0600"
[entrypoint.functions."0x825EF568"]
name = "VtableSlotX6922_825EF568"
[entrypoint.functions."0x8274B798"]
name = "RetConst67_8274B798"
[entrypoint.functions."0x82903F70"]
name = "VtableSlot5_82903F70"
[entrypoint.functions."0x82903F80"]
name = "VtableSlot2_82903F80"
[entrypoint.functions."0x82996258"]
name = "RetConst173_82996258"
[entrypoint.functions."0x82B56870"]
name = "VtableSlotX4_82B56870"
[entrypoint.functions."0x82B84350"]
name = "TailCall_82B84350"
[entrypoint.functions."0x82BBC898"]
name = "VtableSlotX4765_82BBC898"
[entrypoint.functions."0x82BFA4A8"]
name = "VtableSlot1_82BFA4A8"
[entrypoint.functions."0x82C09188"]
name = "VtableSlotX17_82C09188"
[entrypoint.functions."0x82C0B400"]
name = "VtableSlotX7_82C0B400"
[entrypoint.functions."0x82C1DC50"]
name = "VtableSlot7_82C1DC50"
[entrypoint.functions."0x82C1DC60"]
name = "VtableSlot6_82C1DC60"
[entrypoint.functions."0x82C1DC80"]
name = "VtableSlotX3_82C1DC80"
[entrypoint.functions."0x82C2F500"]
name = "VtableSlotX12_82C2F500"
[entrypoint.functions."0x82C4C2C8"]
name = "VtableSlotX10_82C4C2C8"
[entrypoint.functions."0x82C4C2E8"]
name = "VtableSlotX11_82C4C2E8"
[entrypoint.functions."0x82C4C300"]
name = "VtableSlotX6_82C4C300"
[entrypoint.functions."0x82C4C320"]
name = "VtableSlotX2_82C4C320"
[entrypoint.functions."0x82C4C5A8"]
name = "VtableSlotX2_82C4C5A8"
[entrypoint.functions."0x82C4C5C8"]
name = "VtableSlotX1_82C4C5C8"
[entrypoint.functions."0x82C4C5E8"]
name = "VtableSlotX1_82C4C5E8"
[entrypoint.functions."0x82C867E8"]
name = "TailCall_82C867E8"
[entrypoint.functions."0x82C8D820"]
name = "Func_82C8D820"
[entrypoint.functions."0x82CD8650"]
name = "Func_82CD8650"
[entrypoint.functions."0x82CDBAE0"]
name = "Func_82CDBAE0"
[entrypoint.functions."0x82CE1510"]
name = "TailCall_82CE1510"
[entrypoint.functions."0x82CE1520"]
name = "TailCall_82CE1520"
[entrypoint.functions."0x82CE1548"]
name = "TailCall_82CE1548"
[entrypoint.functions."0x82CE3750"]
name = "TailCall_82CE3750"
[entrypoint.functions."0x82CE3760"]
name = "TailCall_82CE3760"
[entrypoint.functions."0x82CE3768"]
name = "TailCall_82CE3768"
[entrypoint.functions."0x82CE3770"]
name = "TailCall_82CE3770"
[entrypoint.functions."0x82CE3798"]
name = "TailCall_82CE3798"
[entrypoint.functions."0x82CE37A0"]
name = "TailCall_82CE37A0"
[entrypoint.functions."0x82CE37A8"]
name = "TailCall_82CE37A8"
[entrypoint.functions."0x82CE5D50"]
name = "VtableSlotX4_82CE5D50"
[entrypoint.functions."0x82CE5D98"]
name = "Func_82CE5D98"
[entrypoint.functions."0x82E86188"]
name = "Func_82E86188"
[entrypoint.functions."0x82E87A10"]
name = "Func_82E87A10"
[entrypoint.functions."0x82E8F9E8"]
name = "RetAddr_82E8F9E8"
[entrypoint.functions."0x82EB7ED0"]
name = "RetAddr_82EB7ED0"
[entrypoint.functions."0x82EC1918"]
name = "RetAddr_82EC1918"
[entrypoint.functions."0x82EC4E58"]
name = "RetAddr_82EC4E58"
[entrypoint.functions."0x82EF41D8"]
name = "VtableSlotX3_82EF41D8"
[entrypoint.functions."0x82EF41F0"]
name = "VtableSlotX4_82EF41F0"
[entrypoint.functions."0x82EF8D20"]
name = "Func_82EF8D20"
[entrypoint.functions."0x82F04420"]
name = "VtableSlot26_82F04420"
[entrypoint.functions."0x82F04430"]
name = "RetConst0_82F04430"
[entrypoint.functions."0x82F0BAF8"]
name = "TailCall_82F0BAF8"
[entrypoint.functions."0x82F0BB08"]
name = "TailCall_82F0BB08"
[entrypoint.functions."0x82F1D508"]
name = "VtableSlotX10_82F1D508"
[entrypoint.functions."0x82F1D520"]
name = "VtableSlotX4_82F1D520"
[entrypoint.functions."0x82F1D538"]
name = "VtableSlotX11_82F1D538"
[entrypoint.functions."0x82F1D550"]
name = "VtableSlotX5_82F1D550"
[entrypoint.functions."0x82F1D568"]
name = "VtableSlotX6_82F1D568"
[entrypoint.functions."0x82F1D580"]
name = "VtableSlotX7_82F1D580"
[entrypoint.functions."0x82F1D598"]
name = "VtableSlotX8_82F1D598"
[entrypoint.functions."0x82F1D5B0"]
name = "VtableSlotX9_82F1D5B0"
[entrypoint.functions."0x82F2C2E8"]
name = "Func_82F2C2E8"
[entrypoint.functions."0x82F2C3A8"]
name = "VtableSlotX14_82F2C3A8"
[entrypoint.functions."0x82F2C3C0"]
name = "Func_82F2C3C0"
[entrypoint.functions."0x82F38DA0"]
name = "TailCall_82F38DA0"
[entrypoint.functions."0x82F435A8"]
name = "Func_82F435A8"
[entrypoint.functions."0x82F56690"]
name = "TailCall_82F56690"
[entrypoint.functions."0x82F67E18"]
name = "VtableSlotX3_82F67E18"
[entrypoint.functions."0x82F67E40"]
name = "VtableSlotX6_82F67E40"
[entrypoint.functions."0x82F67E68"]
name = "VtableSlotX7_82F67E68"
[entrypoint.functions."0x82FB7470"]
name = "Func_82FB7470"
[entrypoint.functions."0x82FE7EF8"]
name = "TailCall_82FE7EF8"
[entrypoint.functions."0x82FE7F00"]
name = "TailCall_82FE7F00"
[entrypoint.functions."0x83056810"]
name = "VtableSlot95_83056810"
[entrypoint.functions."0x83056BC0"]
name = "VtableSlotX133_83056BC0"
[entrypoint.functions."0x83056BE8"]
name = "VtableSlotX134_83056BE8"
[entrypoint.functions."0x83056C10"]
name = "VtableSlotX133_83056C10"
[entrypoint.functions."0x83056C38"]
name = "VtableSlotX134_83056C38"
[entrypoint.functions."0x83057068"]
name = "VtableSlotX151_83057068"
[entrypoint.functions."0x830ACB28"]
name = "Func_830ACB28"
[entrypoint.functions."0x831C5CD8"]
name = "VtableSlotX3_831C5CD8"
[entrypoint.functions."0x831C5D28"]
name = "Func_831C5D28"
[entrypoint.functions."0x831FFA08"]
name = "Func_831FFA08"


[entrypoint.functions."0x831FBD28"]
name = "IndirectCall_831FBD28"
size = 0x0170


# ============================================================================
# UI text rendering pipeline (reverse-engineered; see TEXT_RENDERING_NOTES.md)
# ----------------------------------------------------------------------------
# The title prompt "Press <a_img> to start" is a live UTF-16BE string in the
# heap (0x426690F0); <a_img> renders as the Xbox A-button icon. The per-frame
# chain:
#   UIText_FrameRender (0x82C03FA8)
#     -> UIText_FrameRenderIter (0x82190760)   [iterates UI text list @ 0x83334AA0]
#        -> UITextItem_Dispatch (0x82BFDB68)   [bctrl *(item+4)]
#           -> UIText_RenderCurrent (0x82C0AD60)   [loads *(0x83334D2C)]
#              -> UIText_RenderCurrentObject (0x82C0ACD0)
#                 -> UIText_RenderSegment (0x82C0A8F8) x3  [segment glyph list]
#                    -> UIText_RenderElement (0x82C0A6A0)  [per-glyph/element draw]
# ============================================================================

# Per-frame entry. Sets r11 = 0x83330000 (UI text manager), r3 = r11 + 19104
# (the UI text list at 0x83334AA0), r4 = -1, then tail-calls
# UIText_FrameRenderIter. Fires every frame during title/loading screens.
[entrypoint.functions."0x82C03FA8"]
name = "UIText_FrameRender"

# Walks the UI text list at 0x83334AA0. For each live item it calls
# UITextItem_Dispatch (the item's slot-4 vcall); disposed items are removed
# via sub_82BFDB80 (destructor).
[entrypoint.functions."0x82190760"]
name = "UIText_FrameRenderIter"

# vcall thunk: r11 = *(item + 4); if (r11) bctrl r11. Invokes a UI text
# item's slot-4 method (its render). This is what actually runs each item's
# draw code each frame.
[entrypoint.functions."0x82BFDB68"]
name = "UITextItem_Dispatch"

# The title prompt's slot-4 render method (the bctrl target for the title
# screen's single text item, object at 0x832D60F0 whose *(item+4) points
# here). Ignores its argument: sets r31 = 0x83334E20 (UI text manager
# 0x83330000 + 0x4E20, the prompt's element list) and loops:
#   elem = sub_82B458C0(0x83334E20);   # fetch/advance element list
#   while (elem) { UITextPrompt_RenderElement(elem); elem = sub_82B458C0(...); }
# This is the function that renders the "Press <a_img> to start" prompt.
[entrypoint.functions."0x82C44CF0"]
name = "UITextPrompt_Render"

# Per-element draw of the prompt, called in a loop by UITextPrompt_Render.
# r3 = element (r31). Key element fields: *(elem+0x7C) = drawable pointer
# (0 => element skipped), elem+0xAC/0xBB/0xBC = cached draw state.
# Flow: visibility check via sub_82C44688(elem+8) (result & 0x3F), then
# sub_82BFCB58/sub_82C50A88 bookkeeping, then the actual draw: a vcall into
# the drawable's vtable[8] (mtctr *( *(elem+0x7C) + 0x20 ); bctrl) with the
# drawable in r4 and a stack scratch buffer in r3.
[entrypoint.functions."0x82C44B50"]
name = "UITextPrompt_RenderElement"

# r3 = *(0x83330000 + 19756 = 0x83334D2C)  [pointer to the "current text"
# object], then calls UIText_RenderCurrentObject(r3).
[entrypoint.functions."0x82C0AD60"]
name = "UIText_RenderCurrent"

# Renders the "current text" object (r3). Loads the font slot *(0x83334A08),
# checks the visible flag *(obj + 152), then calls UIText_RenderSegment x3
# for the object's three text segments (at +0/+12/+24).
[entrypoint.functions."0x82C0ACD0"]
name = "UIText_RenderCurrentObject"

# Iterates one text segment's linked list (head at segment + 4) and calls
# UIText_RenderElement for each node. Called three times per text object.
[entrypoint.functions."0x82C0A8F8"]
name = "UIText_RenderSegment"

# The hot per-glyph/element draw. Reads the element's flags (+157/+158/+159/
# +172) and its draw pointer (+16), then invokes that pointer's vtable slot 9
# (+36). State cached at +92/+160/+168. This is where each visible character
# of the prompt is actually drawn.
[entrypoint.functions."0x82C0A6A0"]
name = "UIText_RenderElement"

# Per-item text draw used by the list iterator. Loads font slot[2]
# (0x83334A08) and calls UIText_RenderWithFont.
[entrypoint.functions."0x82BFD9E8"]
name = "UITextItem_Render"

# Draws a string using the given font object (called by UITextItem_Render).
[entrypoint.functions."0x82C000F8"]
name = "UIText_RenderWithFont"

# Binary search over the font's glyph/text table to resolve a character to a
# glyph slot (uses large float sentinels ~9.64683e14 as the search bounds).
[entrypoint.functions."0x82C106A8"]
name = "UIFont_LookupGlyph"

# Emits one glyph quad: takes the glyph origin (position - size), transforms
# it with the 3x3 matrix staged in r28, and writes the resulting quad.
[entrypoint.functions."0x82C0CAF8"]
name = "UIFont_EmitGlyphQuad"

# Per-glyph draw: reads the glyph position (float at +8), derives scale and
# final screen position, and feeds the quad emitter.
[entrypoint.functions."0x82BFE948"]
name = "UIFont_RenderGlyph"

# Font/.bft (font texture) setup. Loads FNV-1 hash constants, the font name
# strings (0x82008098 / 0x820080A8) and the per-slot font values.
[entrypoint.functions."0x82BF7DF0"]
name = "UIFont_Initialize"

# ===========================================================================
# Lua script system (found 2026-07 via the player-coordinate utility hunt)
#
# The in-game Lua 5.1 (LuaPlus) script API is built at load time by
# ScriptApi_RegisterGlobalTable: for each entry it materializes a native
# code address (r4 at the ProcessAndResolveTypedField call), a name string
# (r5 at the ProcessAndCopyReferenceAndHash call), hashes it, then appends
# the pair. `python tools/dump_lua_api.py` replays this to extract the
# full name -> native-function table.
# ===========================================================================

# Builds the global script-function table: 19 (name, native fn) pairs.
# Pattern per entry: addi r4,<fn> ; bl ProcessAndResolveTypedField(0x8219ab28)
# ; addi r29,<name str> ; mr r5,r29 ; bl ProcessAndCopyReferenceAndHash
# (0x824ef160) ; bl ProcessAndHashString(0x82A1EC08) ; bl
# ProcessAndAppendElement(0x82229208). Subtables (Debug., Player., ...) are
# attached via ScriptApi_AddTableEntry_825B7858. Extracted table
# (see tools/dump_lua_api.py; guest = file offset + 0x82000000):
#   0x821C21F8 GetRandomNumber           0x821F6CE0 GetTickRate
#   0x82447918 GetSquaredDistanceBetweenEntityAndLODCentre
#   0x8249BA10 GetRandomFloat            0x825B9018 CreateEntityAtEntitysPosition
#   0x825B90D8 GetPlayerEntityWithIndex  0x825B9210 GetNearestPlayerTo
#   0x825B9308 FNVHash                   0x825B9388 StartPubGame
#   0x825B9580 GetSquaredDistanceBetweenPositionAndLODCentre
#   0x825B9608 GetCurrentCamera          0x825B96B8 LogAIBehaviourEvent
#   0x825B9750 LogEvent                  0x825B97D8 CreateEntityFromLimbo
#   0x826064D0 IsEntityToCallFunction    0x8266F570 PutEntityInLimbo
#   0x8279DCD8 IsEntityWithinDistanceOfLODCentre
#   0x82806168 RunScript  <- injection point for external Lua scripts
#   0x828BE3F0 GetCurrentWorldFrame
[entrypoint.functions."0x825AF6D0"]
name = "ScriptApi_RegisterGlobalTable_825AF6D0"

# Native binding of the Lua global RunScript(path). r3 = the script
# environment table (refcounted; ->0 = self, ->4 = vtable); it vtable-
# dispatches the actual load-and-execute (file read, compile, pcall) on
# that table, then releases it and returns 0. Lua-side semantics: load
# and run another (possibly external, outside the .bnk archives) script.
# This is the hook/call target for running custom in-game Lua utilities.
[entrypoint.functions."0x82806168"]
name = "LuaBind_RunScript_82806168"

# Appends one (name, descriptor) entry to a script-API table. Args:
# r3 = table container (->0 = element list), r4 = ?, r5 = name (int or
# string), r6 = descriptor pointer. Allocates an 8-byte node [r5, r6]
# (ProcessAndDispatchAndResolve with size 8) and pushes it onto the list
# (ProcessAndAppendElement). Called from ScriptApi_RegisterGlobalTable
# for subtables (the 0x821Cxxxx descriptors are per-table metadata).
[entrypoint.functions."0x825B7858"]
name = "ScriptApi_AddTableEntry_825B7858"

# Formats the script-load error message: "Lua <type> script \"<name>\" has
# an error in it". References the format fragments "Lua serialise script "
# (0x820C2388), "Lua save load system script " (0x820C23A0), "Lua general
# script " (0x820C23C0), "Lua AI script " (0x820C23D8), "Lua quest script "
# (0x820C23F8), "Lua Camera script " (0x820C2414), "Lua script " (0x820C2448).
# r3 = string object (CopyAssign'd), r4 = context. Part of the C++ script
# loader core's error path (the loader itself is the caller).
[entrypoint.functions."0x825ADB40"]
name = "LuaScriptError_FormatMessage_825ADB40"

# Registers the string-name table for combat/action effect types.
# Interns the type names (ACTION, STRIKE_START, STRIKE_END, STRIKE,
# HIT_DIRECTION, STANCE, PARTICLE, SOUND, SPEECH, CAMERA_SHAKE,
# CONTROL_RUMBLE, EARTHQUAKE, SHOOT, THROW, POUND, CREATE_ENTITY,
# DESTROY_ENTITY, COMBAT, UNSHEATHE, INSTANT_KILL, InterruptKORecoverAttack,
# TROLL_FOOTSTEP, ...) via LookupString2_8246C010 from the 0x820B/0x820C
# data pages. Backs the ScriptEnum-style action-type constants used by
# script code (e.g. EScriptableAction tables).
[entrypoint.functions."0x82514818"]
name = "RegisterActionEffectTypeStrings_82514818"
